---
quick: 260518-gg3
plan: 01
subsystem: prices-wb / cards-wb / orders-chart
type: execute
wave: 1
depends_on: []
files_modified:
  - components/cards/WbCardOrdersChart.tsx
  - components/prices/PriceCalculatorTable.tsx
  - app/(dashboard)/prices/wb/page.tsx
autonomous: true
requirements:
  - quick-260518-gg3-T1  # Single-expand в /prices/wb (UX consistency с /cards/wb)
  - quick-260518-gg3-T2  # Визуальные правки графика (точка, тысячи, "арт.", tooltip)
  - quick-260518-gg3-T3  # Per-nmId легенда + лента отзывов

must_haves:
  truths:
    - "В /prices/wb раскрыт только один товар одновременно — клик по другой Сводке закрывает предыдущую и открывает новую"
    - "Красная точка цены на графике в 2 раза меньше (r:1.5 вместо r:3) — линия меньше «зашумлена»"
    - "Подпись «Цена сейчас» отображается с разделителем тысяч пробелом: «4 207 ₽» вместо «4207 ₽»"
    - "В tooltip графика число цены покупателя отображается с разделителем тысяч пробелом (формат ru-RU): «4 207» вместо «4,207» или «4207»"
    - "Заголовок графика начинается с «арт.» вместо «nm»: «арт. 800750522 · …»"
    - "Под каждым графиком в /prices/wb отображается легенда с: Остаток, Остаток в днях, Рейтинг связки, Кол-во оценок"
    - "Под легендой отображается компактная лента до 10 последних отзывов (звёзды + цвет фона по рейтингу); пустые отзывы не рендерят блок"
    - "Hover на отзыв в ленте показывает Tooltip с текстом отзыва и датой"
    - "Изменения в WbCardOrdersChart.tsx автоматически применяются на обеих страницах (/cards/wb и /prices/wb) — компонент шарится"
  artifacts:
    - path: "components/cards/WbCardOrdersChart.tsx"
      provides: "Визуальные правки: dot.r=1.5, header «арт.», ru-RU formatter для tooltip и «Цена сейчас»"
      contains: "dot={{ r: 1.5"
    - path: "components/prices/PriceCalculatorTable.tsx"
      provides: "Single-expand state (string | null) вместо Set<string>; per-nmId легенда рядом с графиком"
      contains: "useState<string | null>"
    - path: "app/(dashboard)/prices/wb/page.tsx"
      provides: "Загрузка SupportTicket feedbacks per nmId (top-10 desc) → передача в PriceCalculatorTable"
      contains: "supportTicket.findMany"
  key_links:
    - from: "components/prices/PriceCalculatorTable.tsx"
      to: "components/cards/WbCardOrdersChart.tsx"
      via: "переиспользование компонента (без дублирования)"
      pattern: "import.*WbCardOrdersChart"
    - from: "app/(dashboard)/prices/wb/page.tsx"
      to: "prisma.supportTicket"
      via: "findMany({where:{channel:'FEEDBACK',nmId:{in:[...]}}}) + group/take-10 per nmId на JS"
      pattern: "supportTicket\\.findMany"
    - from: "components/prices/PriceCalculatorTable.tsx ordersCharts"
      to: "WbCardOrdersChart + Legend + ReviewsRibbon"
      via: "flex-col внутри per-nmId блока — chart сверху, legend ниже"
      pattern: "flex flex-col"
---

<objective>
Доработать графики /cards/wb и /prices/wb по трём осям:

1. **Single-expand в /prices/wb** — заменить `Set<string>` (multi-expand из quick 260518-fg5) на `useState<string | null>`, как в `WbCardsTable` (UX consistency).
2. **Визуальные правки графика** (`WbCardOrdersChart.tsx`, применяются на обеих страницах автоматически):
   - Красная точка цены — в 2 раза меньше (`dot.r: 3 → 1.5`)
   - Тысячи с пробелом (формат ru-RU) — в tooltip цены и в подписи «Цена сейчас»
   - «арт.» вместо «nm» в заголовке
   - Пробел между текстом «(15 мая)» и значением цены в tooltip (через `ChartTooltipContent.formatter`)
3. **Per-nmId легенда в /prices/wb expand-панели** — под каждым графиком вывести:
   - Остаток, Остаток в днях (из `stockQty` / `avgSalesSpeed7d`)
   - Рейтинг связки (`wbStoreRating ?? ratingImt ?? rating`)
   - Кол-во оценок (`wbStoreFeedbacks ?? reviewsTotalImt ?? reviewsTotal`)
   - Лента до 10 последних отзывов (`SupportTicket` где `channel='FEEDBACK'`, `nmId=...`, `rating IS NOT NULL`) — компактные чипы со звёздами + цвет фона по рейтингу + tooltip с текстом (`SupportMessage.text` первого INBOUND или `SupportTicket.previewText`)

Purpose: повысить плотность полезной информации в expand-панели /prices/wb (один товар = виден остаток + рейтинг + динамика негатива в одном окне) + визуальные refinements графика.

Output: 3 модифицированных файла, atomic commits per task, smoke build + tsc --noEmit зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260518-fg5-prices-wb-expandable-row-cards-wb-per-nm/260518-fg5-SUMMARY.md
@.planning/quick/260515-o4o-cards-wb-expand-v2-2x-narrower-design-po/260515-o4o-SUMMARY.md
@.planning/quick/260514-mci-cards-wb/260514-mci-SUMMARY.md

@components/cards/WbCardOrdersChart.tsx
@components/prices/PriceCalculatorTable.tsx
@components/cards/WbCardsTable.tsx
@app/(dashboard)/prices/wb/page.tsx
@components/ui/chart.tsx
@components/ui/tooltip.tsx
@prisma/schema.prisma

<interfaces>
<!-- Ключевые контракты для исполнителя — извлечены из codebase, чтобы избежать «scavenger hunt». -->

### components/cards/WbCardOrdersChart.tsx (текущая сигнатура — без изменений)
```typescript
interface Props {
  nmId: number
  timeSeries: DayPoint[]  // 28 точек: { date, qty, sellerPrice?, buyerPrice? }
}
export function WbCardOrdersChart({ nmId, timeSeries }: Props): JSX.Element
```

### components/prices/PriceCalculatorTable.tsx (текущий ProductGroup)
```typescript
export interface ProductGroup {
  product: { id, name, photoUrl, brandName, totalStock, totalAvgSalesSpeed, totalOrdersYesterday }
  cards: WbCardRowGroup[]
  totalRowsInProduct: number
  ordersCharts?: Array<{ nmId: number; timeSeries: DayPoint[] }>
}

// Текущий expand state (этот PR заменяет на single-expand):
const [expandedProductIds, setExpandedProductIds] = useState<Set<string>>(() => new Set())
const toggleProductExpand = useCallback((productId: string) => { /* Set add/delete */ }, [])
```

### prisma SupportTicket (для ленты отзывов)
```prisma
model SupportTicket {
  id           String        @id @default(cuid())
  channel      TicketChannel // FEEDBACK для отзывов
  wbExternalId String?
  nmId         Int?          // связь с WbCard.nmId без FK
  rating       Int?          // productValuation 1..5
  previewText  String?  @db.Text  // первые 140 символов последнего INBOUND
  createdAt    DateTime @default(now())
  messages     SupportMessage[]
}
model SupportMessage {
  id        String   @id @default(cuid())
  ticketId  String
  direction Direction  // INCOMING / OUTGOING
  text      String?  @db.Text
  sentAt    DateTime @default(now())
}
```

### WbCard (поля для легенды — все уже select'ятся в page.tsx через findMany без явного select)
```typescript
type WbCard = {
  nmId: number
  stockQty: number | null
  avgSalesSpeed7d: number | null
  rating: number | null        // отдельной карточки (Feedbacks по nmId)
  reviewsTotal: number | null  // отдельной карточки
  ratingImt: number | null     // склейки (наш расчёт по imtId)
  reviewsTotalImt: number | null
  wbStoreRating: number | null    // витрина WB (приоритет)
  wbStoreFeedbacks: number | null
  // ...other fields not needed for этот PR
}
```

### Поведение «Сохранить как расчётную цену» и клик по строкам в expanded view
**Не трогать** — клик по ценовым строкам (Текущая/Regular/Auto/Расчётная) по-прежнему открывает PricingCalculatorDialog (через onRowClick). Это сохраняется как было.

### Tooltip pattern (base-ui wrapper)
```typescript
// components/ui/tooltip.tsx — base-ui wrapper (render-prop, НЕ asChild)
<Tooltip>
  <TooltipTrigger render={<span />}>...trigger</TooltipTrigger>
  <TooltipContent>...content</TooltipContent>
</Tooltip>
```
</interfaces>

### Текущие проблемы (для понимания scope)

| Что | Где | Текущее | Должно |
|-----|-----|---------|--------|
| dot.r | WbCardOrdersChart.tsx:107 | `r: 3` | `r: 1.5` |
| Header chart | WbCardOrdersChart.tsx:48 | `nm {nmId}` | `арт. {nmId}` |
| «Цена сейчас» | WbCardOrdersChart.tsx:142 | `{lastBuyerPrice}` | `{lastBuyerPrice.toLocaleString('ru-RU')}` |
| Tooltip число | components/ui/chart.tsx:258 | `item.value.toLocaleString()` (default locale) | в tooltip нашего chart передать `formatter` с ru-RU → переопределит default |
| Expand state | PriceCalculatorTable.tsx:567 | `Set<string>` (multi-expand) | `string \| null` (single-expand) |
| Легенда per-nmId | PriceCalculatorTable.tsx:1130 | Нет | Под каждым chart блок с метаданными + лента отзывов |

### Где брать данные отзывов

В `app/(dashboard)/prices/wb/page.tsx` после загрузки `ordersRows` (line ~322) добавить:

```typescript
const reviewsRaw = visibleNmIds.length > 0
  ? await prisma.supportTicket.findMany({
      where: {
        channel: "FEEDBACK",
        nmId: { in: visibleNmIds },
        rating: { not: null },
      },
      orderBy: { createdAt: "desc" },
      select: {
        id: true,
        nmId: true,
        rating: true,
        previewText: true,
        createdAt: true,
        messages: {
          where: { direction: "INCOMING" },
          orderBy: { sentAt: "asc" },
          take: 1,
          select: { text: true },
        },
      },
    })
  : []

// Group by nmId, take 10 per group (sorted desc already)
const reviewsByNmId: Record<number, Array<{
  id: string; rating: number; text: string; createdAt: string
}>> = {}
for (const t of reviewsRaw) {
  if (t.nmId == null) continue
  const arr = reviewsByNmId[t.nmId] ?? []
  if (arr.length >= 10) continue  // skip — нам нужны top-10 per nmId
  const text = t.messages[0]?.text ?? t.previewText ?? ""
  arr.push({
    id: t.id,
    rating: t.rating!,
    text,
    createdAt: t.createdAt.toISOString(),  // serialize Date → string для RSC→client boundary
  })
  reviewsByNmId[t.nmId] = arr
}
```

**Sort:** `orderBy: createdAt desc` уже даёт top-10 свежих первыми — при `arr.length >= 10` cap корректен. Возвращаем `Record<number, FeedbackItem[]>` (не `Map` — Map не сериализуется через RSC→client).

### Передача в PriceCalculatorTable

Расширить `ProductGroup.ordersCharts` элемент:

```typescript
ordersCharts?: Array<{
  nmId: number
  timeSeries: DayPoint[]
  stockQty: number | null
  avgSalesSpeed7d: number | null
  rating: number | null
  reviewsTotal: number | null
  reviews: Array<{ id: string; rating: number; text: string; createdAt: string }>
}>
```

В page.tsx loop (line ~688) при формировании `productNmIdsWithCharts.push(...)` добавить эти поля из `card` + `reviewsByNmId[card.nmId] ?? []`.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-expand в /prices/wb (Set → string | null) + визуальные правки графика</name>
  <files>
    components/prices/PriceCalculatorTable.tsx,
    components/cards/WbCardOrdersChart.tsx
  </files>
  <action>
**Часть A — PriceCalculatorTable.tsx: single-expand state.**

1. Заменить state (line ~567):
   ```typescript
   // БЫЛО:
   const [expandedProductIds, setExpandedProductIds] = useState<Set<string>>(() => new Set())
   const toggleProductExpand = useCallback((productId: string) => {
     setExpandedProductIds((prev) => {
       const next = new Set(prev)
       if (next.has(productId)) next.delete(productId)
       else next.add(productId)
       return next
     })
   }, [])

   // СТАЛО:
   const [expandedProductId, setExpandedProductId] = useState<string | null>(null)
   const toggleProductExpand = useCallback((productId: string) => {
     setExpandedProductId((prev) => (prev === productId ? null : productId))
   }, [])
   ```
   Имя state и setter переименовать. Toggle логика идентична `WbCardsTable.tsx:379`.

2. Заменить **все** обращения `expandedProductIds.has(group.product.id)` на `expandedProductId === group.product.id` (line ~830, ~840, ~1118). Это 3 места — найди grep'ом по `expandedProductIds`.

3. Удалить `prev`/`next`/`Set` импорты, если они теперь не используются (вероятно, нет — `Set` ещё используется в `selectedCalcIds`).

4. Прокомментировать: `// quick 260518-gg3: single-expand (UX consistency с /cards/wb)`

**Часть B — WbCardOrdersChart.tsx: визуальные правки.**

Файл `components/cards/WbCardOrdersChart.tsx`:

1. **Line 48** — header текст:
   ```diff
   - nm {nmId} · заказы и цена покупателя · 28 дней
   + арт. {nmId} · заказы и цена покупателя · 28 дней
   ```

2. **Line 107** — dot radius:
   ```diff
   - dot={{ r: 3, fill: "var(--color-buyerPrice)" }}
   + dot={{ r: 1.5, fill: "var(--color-buyerPrice)" }}
   ```
   (recharts принимает float radius, проверено в recharts docs — `dot.r: 1.5` рендерится как 3px diameter SVG circle.)

3. **Line 142** — «Цена сейчас» формат с разделителем тысяч:
   ```diff
   - {lastBuyerPrice}
   + {lastBuyerPrice.toLocaleString("ru-RU")}
   ```
   Безопасно: блок рендерится только при `lastBuyerPrice != null` (line 135 condition).

4. **Tooltip formatter — пробел после ")" и ru-RU тысячи** (line 87-92):
   ```typescript
   // БЫЛО:
   <ChartTooltip
     content={
       <ChartTooltipContent
         labelFormatter={(label) => `Дата: ${label}`}
       />
     }
   />

   // СТАЛО:
   <ChartTooltip
     content={
       <ChartTooltipContent
         labelFormatter={(label) => `Дата: ${label}`}
         formatter={(value, name, item) => {
           const numValue = typeof value === "number" ? value : Number(value)
           const formatted = Number.isFinite(numValue)
             ? numValue.toLocaleString("ru-RU")
             : String(value)
           const label = item?.payload?.[item.dataKey as string] !== undefined
             ? (name === "qty" ? "Заказы" : "Цена покупателя (₽)")
             : String(name)
           return (
             <>
               <div className="shrink-0 rounded-[2px] h-2.5 w-2.5"
                    style={{ backgroundColor: item?.color }} />
               <div className="flex flex-1 justify-between items-center gap-2">
                 <span className="text-muted-foreground">{label}</span>
                 <span className="font-mono font-medium tabular-nums">{formatted}</span>
               </div>
             </>
           )
         }}
       />
     }
   />
   ```

   **Альтернатива (проще, если formatter render усложняет):** оставить дефолтный render, но переопределить только `Intl` локаль через formatter, возвращающий чистый React.Node. Реализатор: попробуй сначала **минимальный formatter**, который только подменяет число — если formatter override ломает layout/indicator dot, переключись на полный render как выше. **Минимальная версия:**
   ```typescript
   formatter={(value, name) => [
     typeof value === "number" ? value.toLocaleString("ru-RU") : String(value),
     name === "qty" ? "Заказы" : name === "buyerPrice" ? "Цена покупателя (₽)" : String(name),
   ]}
   ```
   Recharts ожидает `[value, name]` массив от formatter — но **в нашей shadcn-chart обёртке** `formatter` вызывается как React-render (line 215-216 в `components/ui/chart.tsx`):
   ```typescript
   formatter(item.value, item.name, item, index, item.payload)
   ```
   и **результат рендерится прямо как JSX** (не как массив). Значит нужно вернуть JSX/ReactNode, не tuple. Используй полную версию выше.

**Smoke check:**
   - `npx tsc --noEmit` → 0 errors
   - `npm run build` → success
   - Открыть `/cards/wb` (без deploy на VPS — для проверки достаточно `npm run dev`), кликнуть строку → раскрывается; заголовок начинается с «арт.»; точка цены меньше; tooltip содержит число с пробелом (типа «4 207»); подпись «Цена сейчас» — «4 207 ₽».
   - Открыть `/prices/wb` → клик по Сводке товара A → раскрылся; клик по Сводке товара B → товар A закрылся, товар B раскрылся (single-expand работает); те же визуальные изменения графика применены.

  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "error|expandedProductId|WbCardOrdersChart" | head -20</automated>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
1. В `PriceCalculatorTable.tsx`: state переименован в `expandedProductId: string | null`, все 3 обращения `expandedProductIds.has(...)` заменены на `expandedProductId === ...`; toggle делает open/close + single-active.
2. В `WbCardOrdersChart.tsx`: header «арт.», dot.r=1.5, `lastBuyerPrice.toLocaleString("ru-RU")`, tooltip formatter возвращает JSX с ru-RU числами.
3. tsc --noEmit: 0 errors; build success.
4. Commit: `feat(quick-260518-gg3): single-expand /prices/wb + chart polish (арт./tooltip ru-RU/смaller dot)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Загрузка отзывов и расширение ProductGroup.ordersCharts</name>
  <files>
    app/(dashboard)/prices/wb/page.tsx,
    components/prices/PriceCalculatorTable.tsx
  </files>
  <action>
**Часть A — page.tsx: загрузка SupportTicket feedbacks.**

1. После загрузки `ordersRows` (примерно line 322), добавить блок загрузки отзывов:

```typescript
// ── 6.6. Загрузить последние отзывы per nmId (для ленты в expand-панели) ──
// SupportTicket channel=FEEDBACK, rating IS NOT NULL, top-10 desc per nmId.
// Текст отзыва — из первого INCOMING SupportMessage; fallback на previewText.
type FeedbackItem = {
  id: string
  rating: number
  text: string
  createdAt: string  // ISO string для RSC→client serialization
}

const reviewsRaw =
  visibleNmIds.length > 0
    ? await prisma.supportTicket.findMany({
        where: {
          channel: "FEEDBACK",
          nmId: { in: visibleNmIds },
          rating: { not: null },
        },
        orderBy: { createdAt: "desc" },
        select: {
          id: true,
          nmId: true,
          rating: true,
          previewText: true,
          createdAt: true,
          messages: {
            where: { direction: "INCOMING" },
            orderBy: { sentAt: "asc" },
            take: 1,
            select: { text: true },
          },
        },
      })
    : []

const reviewsByNmId: Record<number, FeedbackItem[]> = {}
for (const t of reviewsRaw) {
  if (t.nmId == null || t.rating == null) continue
  const arr = reviewsByNmId[t.nmId] ?? []
  if (arr.length >= 10) continue
  const text = t.messages[0]?.text ?? t.previewText ?? ""
  arr.push({
    id: t.id,
    rating: t.rating,
    text,
    createdAt: t.createdAt.toISOString(),
  })
  reviewsByNmId[t.nmId] = arr
}
```

**Важно про `direction`:** в проекте есть `enum Direction { INCOMING, OUTGOING }` (проверить `prisma/schema.prisma`; если значение реально `INBOUND`/`OUTBOUND` — поменять). Используй grep по `enum Direction`. Если grep покажет иной набор — подставь правильное значение.

2. В loop формирования `productNmIdsWithCharts` (примерно line ~688), при push расширить shape:

```typescript
// БЫЛО:
productNmIdsWithCharts.push({
  nmId: card.nmId,
  timeSeries: fillTimeSeries(rawRows),
})

// СТАЛО:
productNmIdsWithCharts.push({
  nmId: card.nmId,
  timeSeries: fillTimeSeries(rawRows),
  stockQty: card.stockQty ?? null,
  avgSalesSpeed7d: card.avgSalesSpeed7d ?? null,
  rating: card.wbStoreRating ?? card.ratingImt ?? card.rating ?? null,
  reviewsTotal: card.wbStoreFeedbacks ?? card.reviewsTotalImt ?? card.reviewsTotal ?? null,
  reviews: reviewsByNmId[card.nmId] ?? [],
})
```

**Часть B — PriceCalculatorTable.tsx: расширить тип ProductGroup.**

Найти определение `ProductGroup` (line ~165-183) и расширить тип `ordersCharts`:

```typescript
ordersCharts?: Array<{
  nmId: number
  timeSeries: DayPoint[]
  stockQty: number | null
  avgSalesSpeed7d: number | null
  rating: number | null
  reviewsTotal: number | null
  reviews: Array<{ id: string; rating: number; text: string; createdAt: string }>
}>
```

В `expandedTr` JSX (line ~1130) пока что **не используем** новые поля — только passthrough. Финальный UI ленты + легенды реализуется в Task 3.

**Smoke check:**
- `npx tsc --noEmit` → 0 errors
- `npm run build` → success
- Открыть `/prices/wb` → expand-панель работает как раньше (Task 3 ещё не отрендерил легенду)
- В RSC page.tsx новый запрос `supportTicket.findMany` не падает (Prisma валидируется на этапе build).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
1. В page.tsx: блок загрузки reviews (`supportTicket.findMany`) + `reviewsByNmId: Record<number, FeedbackItem[]>` готов.
2. В page.tsx loop: `productNmIdsWithCharts.push({...})` расширен 5 новыми полями (stockQty, avgSalesSpeed7d, rating, reviewsTotal, reviews).
3. В PriceCalculatorTable.tsx: `ProductGroup.ordersCharts` тип расширен — все 6 новых полей видны компилятору.
4. tsc + build зелёные. Старое поведение expand-панели не сломано (легенда ещё не рендерится — но данные уже доходят).
5. Commit: `feat(quick-260518-gg3): load SupportTicket feedbacks per nmId для expand-панели /prices/wb`
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-nmId легенда + лента отзывов под графиком в /prices/wb</name>
  <files>components/prices/PriceCalculatorTable.tsx</files>
  <action>
**Цель:** под каждым `<WbCardOrdersChart>` в expand-панели `/prices/wb` отрендерить блок с:
- Метаданными (Остаток, Остаток в днях, Рейтинг связки, Кол-во оценок)
- Лентой до 10 последних отзывов (звёзды + цвет фона по рейтингу + tooltip с текстом)

**Layout:** flex-col внутри per-nmId блока — `[Chart сверху][Legend снизу]`. Per-nmId блоки выровнены `flex-row flex-wrap gap-3 justify-start items-start` (уже так).

1. Найти `expandedTr` (line ~1120). Заменить `charts.map(({ nmId, timeSeries }) => (...))` на:

```typescript
{charts.map((c) => {
  const daysLeft =
    c.avgSalesSpeed7d != null && c.avgSalesSpeed7d > 0 && c.stockQty != null
      ? Math.floor(c.stockQty / c.avgSalesSpeed7d)
      : null
  return (
    <div
      key={c.nmId}
      className="flex flex-col gap-2"
    >
      <WbCardOrdersChart nmId={c.nmId} timeSeries={c.timeSeries} />
      <NmIdLegend
        stockQty={c.stockQty}
        daysLeft={daysLeft}
        rating={c.rating}
        reviewsTotal={c.reviewsTotal}
        reviews={c.reviews}
      />
    </div>
  )
})}
```

2. Добавить локальный компонент `NmIdLegend` (внутри `PriceCalculatorTable.tsx` файла, перед основным `export function` — паттерн локальных subcomponents используется в проекте):

```typescript
function NmIdLegend({
  stockQty,
  daysLeft,
  rating,
  reviewsTotal,
  reviews,
}: {
  stockQty: number | null
  daysLeft: number | null
  rating: number | null
  reviewsTotal: number | null
  reviews: Array<{ id: string; rating: number; text: string; createdAt: string }>
}) {
  return (
    <div className="max-w-[640px] px-2 pb-2 flex flex-col gap-2">
      {/* Метаданные */}
      <div className="flex flex-row gap-4 text-xs">
        <LegendItem label="Остаток" value={stockQty != null ? `${stockQty} шт` : "—"} />
        <LegendItem label="Остаток в днях" value={daysLeft != null ? `${daysLeft} дн` : "—"} />
        <LegendItem label="Рейтинг связки" value={rating != null ? rating.toFixed(1) : "—"} />
        <LegendItem label="Кол-во оценок" value={reviewsTotal != null ? `${reviewsTotal}` : "—"} />
      </div>
      {/* Лента отзывов */}
      {reviews.length > 0 && (
        <div className="flex flex-row gap-1 flex-wrap">
          {reviews.map((r) => (
            <ReviewChip key={r.id} review={r} />
          ))}
        </div>
      )}
    </div>
  )
}

function LegendItem({ label, value }: { label: string; value: string }) {
  return (
    <div className="flex flex-col">
      <span className="text-muted-foreground">{label}</span>
      <span className="font-medium tabular-nums">{value}</span>
    </div>
  )
}

const RATING_BG: Record<number, string> = {
  5: "bg-emerald-500/80 text-white",
  4: "bg-emerald-400/70 text-white",
  3: "bg-yellow-400/80 text-foreground",
  2: "bg-orange-500/80 text-white",
  1: "bg-red-500/85 text-white",
}

function ReviewChip({
  review,
}: {
  review: { id: string; rating: number; text: string; createdAt: string }
}) {
  const r = Math.max(1, Math.min(5, review.rating))
  const bg = RATING_BG[r] ?? "bg-muted text-foreground"
  const dateStr = (() => {
    try {
      return new Date(review.createdAt).toLocaleDateString("ru-RU", {
        day: "2-digit",
        month: "2-digit",
        year: "numeric",
      })
    } catch {
      return ""
    }
  })()
  return (
    <Tooltip>
      <TooltipTrigger
        render={
          <span
            className={`inline-flex items-center justify-center rounded-sm px-1.5 py-0.5 text-[10px] font-medium cursor-default ${bg}`}
          >
            {"★".repeat(r)}
          </span>
        }
      />
      <TooltipContent className="max-w-[320px] text-xs whitespace-pre-wrap">
        <div className="text-muted-foreground mb-1">
          {r}★ · {dateStr}
        </div>
        <div>{review.text || "(пустой отзыв)"}</div>
      </TooltipContent>
    </Tooltip>
  )
}
```

3. Импорты:
   - `import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip"` — добавить в шапку файла, если ещё нет.

4. **Проверить max-width:** `WbCardOrdersChart` уже `max-w-[640px]`, легенда тоже `max-w-[640px]` — блок Chart+Legend визуально одной ширины. Не центрировать (`mx-auto` ❌) — `justify-start` родителя оставляем.

5. **Edge case:** если все 4 метрики null И reviews пустой → блок легенды не должен пустовать визуально. Текущий код рендерит «—» в `LegendItem` — это OK (информативно). Ленту скрываем `{reviews.length > 0 && ...}`.

**Smoke check:**
- `npx tsc --noEmit` → 0 errors
- `npm run build` → success
- Открыть `/prices/wb` → expand товара с активными nmId:
  - Под каждым графиком ряд из 4 «label / value» элементов
  - При наличии отзывов — ниже лента маленьких чипов со звёздами разного цвета
  - Hover на чип → tooltip с текстом отзыва + датой
- Открыть товар без отзывов → блок легенды есть (4 метрики), ленты нет.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
1. `NmIdLegend`, `LegendItem`, `ReviewChip` локальные компоненты добавлены.
2. `expandedTr` рендерит per-nmId блок `flex-col` (chart сверху + legend снизу).
3. Цветовая схема рейтинга: 5★ зелёный → 1★ красный (RATING_BG map).
4. Tooltip с текстом отзыва (`SupportMessage.text` или `previewText`) на hover.
5. tsc + build зелёные.
6. Commit: `feat(quick-260518-gg3): per-nmId легенда + лента отзывов в expand-панели /prices/wb`
  </done>
</task>

</tasks>

<verification>
### Overall checks (после всех 3 задач)

1. **TypeScript:** `npx tsc --noEmit` → 0 errors
2. **Build:** `npm run build` → success, /prices/wb bundle delta в пределах +2 kB (legend код небольшой)
3. **Smoke на /cards/wb:**
   - Существующая single-expand работает как раньше
   - Заголовок графика теперь «арт. {nmId} · …»
   - Точка цены меньше (визуально)
   - В подписи «Цена сейчас» число с пробелом (типа «4 207 ₽»)
   - Tooltip при hover на точку line / bar — число с пробелом
4. **Smoke на /prices/wb:**
   - Single-expand: клик по Сводке товара A → раскрылся; клик по Сводке товара B → товар A закрылся, B раскрылся
   - Под каждым графиком: ряд легенды + лента отзывов (если есть)
   - Hover на чип отзыва → tooltip с текстом
   - Клик по ценовой строке (Текущая / Regular / Calc) внутри открытого товара → НЕ закрывает expand-панель + открывает PricingCalculatorDialog (regression check)
5. **Регрессии:**
   - Phase 7 golden test: `npm run test -- tests/pricing-math.test.ts` → passed
   - Chart fill: `npm run test -- tests/wb-orders-chart-fill.test.ts` → passed

### Edge cases (verified by design)
- Товар с 0 отзывов на все nmId → ленты не рендерятся, легенда (4 метрики) есть
- nmId с `avgSalesSpeed7d=0` или `null` → `daysLeft="—"`
- nmId без `stockQty` → `stockQty="—"`
- Все три rating-source null → `rating="—"`
- Очень длинный текст отзыва → tooltip `max-w-[320px] whitespace-pre-wrap`
- 10+ отзывов per nmId → отображаются только 10 первых (top-desc cap в page.tsx)
</verification>

<success_criteria>
- [ ] `/prices/wb` single-expand: открыт только один товар одновременно
- [ ] График: «арт.», точка r:1.5, ru-RU тысячи в tooltip и в «Цена сейчас»
- [ ] Под графиком в /prices/wb легенда + лента отзывов
- [ ] Hover на отзыв → tooltip с текстом
- [ ] tsc 0 errors, build success
- [ ] Regression: golden test pricing-math + chart-fill зелёные
- [ ] /cards/wb не сломан (тот же chart-компонент, single-expand уже был там)
- [ ] 3 atomic commits (по одному per task)
</success_criteria>

<output>
After completion, create `.planning/quick/260518-gg3-cards-wb-prices-wb-1-single-expand-price/260518-gg3-SUMMARY.md` со стандартными секциями:
- frontmatter (quick, plan, files_modified, files_created, decisions, metrics)
- One-liner
- What got built (per task)
- Files Modified
- Commits table
- Smoke results table
- Deploy инструкции (стандартный `bash deploy.sh`)
- UAT Checklist
- Self-Check: PASSED
</output>
