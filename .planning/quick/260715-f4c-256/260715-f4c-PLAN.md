---
phase: 260715-f4c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-weekly/types.ts
  - lib/finance-weekly/engine.ts
  - lib/finance-weekly/data.ts
  - app/actions/finance-weekly.ts
  - lib/finance-weekly/snapshot.ts
  - app/(dashboard)/finance/weekly/page.tsx
  - components/finance/WeeklyFinReportControls.tsx
  - tests/finance-weekly-engine.test.ts
  - tests/finance-weekly-snapshot.test.ts
  - CLAUDE.md
autonomous: true
requirements:
  - OVERHEAD-CLOTH-PERUNIT: общие расходы одежды = фикс 256 ₽/ед × кол-во + переменный пул

must_haves:
  truths:
    - "Одежда: общие расходы на единицу = 256 ₽ (фикс, одинаково на каждую единицу, НЕ по выручке) + доля переменного пула по выручке"
    - "Бытовая техника: общие расходы без изменений (пул overheadAppl по выручке); фикс 256 к ней НЕ применяется"
    - "Переменный пул одежды = manualPools.overheadCloth ЦЕЛИКОМ (без прибавления/вычитания фикса)"
    - "Golden-тест движка (nmId 165967746 → ИУ 523.6 / Оферта −2176.7) НЕ меняется"
    - "Поле в Controls «Фикс общих одежды, ₽/ед» редактируется и сохраняется (глобальный AppSetting, дефолт 256)"
    - "Строка financeWeekly.clothingOverheadFixedRub отсутствует в коде (заменена на clothingOverheadPerUnitRub)"
  artifacts:
    - path: "lib/finance-weekly/types.ts"
      provides: "WeeklyArticleInput.overheadFixedPerUnit?: number (аддитивное, опциональное)"
      contains: "overheadFixedPerUnit"
    - path: "lib/finance-weekly/engine.ts"
      provides: "overheadPerUnit = (overheadFixedPerUnit ?? 0) + poolPerUnit(...)"
      contains: "overheadFixedPerUnit"
    - path: "lib/finance-weekly/data.ts"
      provides: "CLOTHING_OVERHEAD_PER_UNIT_KEY + default 256; per-unit на clothing-articles; пул одежды = overheadCloth"
      contains: "clothingOverheadPerUnitRub"
    - path: "tests/finance-weekly-engine.test.ts"
      provides: "новый describe: overheadFixedPerUnit (256 + доля пула, оба сценария; appliances/golden не затронуты)"
      contains: "overheadFixedPerUnit"
  key_links:
    - from: "lib/finance-weekly/data.ts"
      to: "lib/finance-weekly/engine.ts"
      via: "WeeklyArticleInput.overheadFixedPerUnit (256 только для clothing)"
      pattern: "overheadFixedPerUnit"
    - from: "components/finance/WeeklyFinReportControls.tsx"
      to: "AppSetting financeWeekly.clothingOverheadPerUnitRub"
      via: "saveWeeklyPools opts.clothingOverheadPerUnitRub → upsert CLOTHING_OVERHEAD_PER_UNIT_KEY"
      pattern: "clothingOverheadPerUnitRub"
---

<objective>
Исправить модель общих расходов ОДЕЖДЫ в понедельном фин-отчёте `/finance/weekly` по указанию пользователя (2026-07-15).

**Было (неверно):** глобальный недельный ФИКС `clothingOverheadFixedRub` прибавляется к ПУЛУ одежды (`data.ts:780` `total: clothingOverheadFixedRub + manualPools.overheadCloth`) и распределяется по выручке.

**Стало (верно):**
- ФИКС общих одежды = **256 ₽ на единицу товара** (составляющие 205 + 51 ₽), одинаково на каждую единицу — НЕ по выручке. Хранится в новом глобальном AppSetting `financeWeekly.clothingOverheadPerUnitRub` (дефолт 256).
- ПЕРЕМЕННАЯ часть = недельный пул `manualPools.overheadCloth` ЦЕЛИКОМ (распределяется по выручке, как раньше), без прибавления фикса.

Итог per unit одежды: `overheadPerUnit = 256 + (K/clothBase)×overheadCloth`. Бытовая техника не затрагивается.

Purpose: привести отчёт к методологии экономиста (фикс на единицу, не размазан по выручке).
Output: аддитивное поле движка `overheadFixedPerUnit`, rename каскад `clothingOverheadFixedRub → clothingOverheadPerUnitRub`, редактируемое поле «₽/ед» в Controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Конвенции: язык русский; МИНИМАЛЬНЫЕ аддитивные диффы; движок менять минимально (diff-guard engine.ts — CLAUDE.md gate).

Гейты (CLAUDE.md «Фин. отчёт за неделю»): `npx tsc --noEmit` + `npm run test` (finance-weekly-* + pricing-math зелёные). ⚠ tsconfig ИСКЛЮЧАЕТ `tests/**` — tsc НЕ проверяет тест-файлы, поэтому rename в тестах проверяется чтением + прогоном vitest, не tsc.

Ключевые контракты (уже в коде — использовать напрямую, без исследования):

```typescript
// lib/finance-weekly/types.ts — WeeklyArticleInput (добавить поле)
export interface WeeklyArticleInput {
  nmId: number
  universe: Universe            // "appliances" | "clothing"
  qtyOrders: number             // H
  grossPricePerUnit: number     // K
  commIuPct: number; commStdPct: number
  costPerUnit: number; adSpendTotal: number; reviewWriteoffTotal: number
  logisticsIuPerUnit: number; logisticsStdPerUnit: number
  storagePerUnit?: number
  // ← ДОБАВИТЬ: overheadFixedPerUnit?: number
}

// lib/finance-weekly/engine.ts:135 (resolveCommon) — единственная точка правки движка
const overheadPerUnit = poolPerUnit(K, pools.overhead.baseRevenue, pools.overhead.total)
// overheadPerUnit применяется к ОБОИМ сценариям (scenario-independent) и уже
// проходит в breakdown.overheadPerUnit → модалка WeeklyFinArticleDialog:85
// { label: "Общие расходы", key: "overheadPerUnit" } → водопад acc.overhead.
// Требование 6: модалка/водопад ничего дополнительно НЕ требуют.

// lib/finance-weekly/data.ts:114 — переименовать константу + добавить дефолт
export const CLOTHING_OVERHEAD_FIXED_KEY = "financeWeekly.clothingOverheadFixedRub"
// data.ts:780 (clothing.overhead.total) — снять фикс-слагаемое:
total: clothingOverheadFixedRub + manualPools.overheadCloth,   // → total: manualPools.overheadCloth
```

Полный список мест `clothingOverheadFixedRub` / `CLOTHING_OVERHEAD_FIXED_KEY` (grep, все в диффе):
data.ts:114,178,285,336,366,718-720,780,805 · actions:17,30,39,63,64,67,68 · Controls:103,125,138,177 · snapshot.ts:46,74 · page.tsx:83,117 · engine-test/snapshot-test.
live.ts — чистый транзит `data`, правок НЕ требует.

@lib/finance-weekly/data.ts
@lib/finance-weekly/engine.ts
@lib/finance-weekly/types.ts
@app/actions/finance-weekly.ts
@components/finance/WeeklyFinReportControls.tsx
@lib/finance-weekly/snapshot.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Движок — аддитивное поле overheadFixedPerUnit (types + engine + engine-тест)</name>
  <files>lib/finance-weekly/types.ts, lib/finance-weekly/engine.ts, tests/finance-weekly-engine.test.ts</files>
  <behavior>
    - Clothing-артикул с overheadFixedPerUnit=256 + пул overhead(total=100, baseRevenue=K=1000) → breakdown.overheadPerUnit = 256 + 100 = 356 в ОБОИХ сценариях (ИУ и Оферта, overhead scenario-independent).
    - Тот же артикул БЕЗ поля → overheadPerUnit = 100 (только пул); разница на единицу = ровно 256; разница profit total = −256×H.
    - Appliances-артикул без поля (goldenArticle) → overheadPerUnit не меняется; golden профит ИУ 523.6 / Оферта −2176.7 НЕ меняется (проверить, что coalesce `?? 0` не трогает существующие входы).
  </behavior>
  <action>
    **types.ts** — в `WeeklyArticleInput` (после `storagePerUnit?: number`) добавить опциональное аддитивное поле:
    ```typescript
    // Фикс общих расходов / ед (₽), одинаково на каждую единицу — НЕ по выручке.
    // Указание пользователя 2026-07-15: одежда получает 256 ₽/ед (205 + 51);
    // appliances не задают (undefined → движок coalesce `?? 0`, golden неизменен).
    // Аддитивно к пулу overhead в обоих сценариях (scenario-independent).
    overheadFixedPerUnit?: number
    ```

    **engine.ts** — ЕДИНСТВЕННАЯ правка логики (diff-guard), строка 135 в `resolveCommon`:
    ```typescript
    // было:
    const overheadPerUnit = poolPerUnit(K, pools.overhead.baseRevenue, pools.overhead.total)
    // стало (фикс/ед + доля переменного пула):
    const overheadPerUnit =
      (article.overheadFixedPerUnit ?? 0) +
      poolPerUnit(K, pools.overhead.baseRevenue, pools.overhead.total)
    ```
    Больше в engine.ts НИЧЕГО не менять (overheadPerUnit уже течёт в breakdown/waterfall).

    **tests/finance-weekly-engine.test.ts** — ДОБАВИТЬ новый `describe` в конец файла (не трогая golden/jem/storage блоки):
    ```typescript
    // ──────────────────────────────────────────────────────────────────
    // Quick 260715-f4c: overheadFixedPerUnit — фикс общих одежды/ед (256 ₽),
    // аддитивно к пулу overhead в ОБОИХ сценариях. Указание 2026-07-15.
    // ──────────────────────────────────────────────────────────────────
    describe("computeWeeklyFinReport — overheadFixedPerUnit (фикс общих/ед)", () => {
      const base: WeeklyArticleInput = {
        nmId: 810001, universe: "clothing", qtyOrders: 10, grossPricePerUnit: 1000,
        commIuPct: 30, commStdPct: 24, costPerUnit: 300, adSpendTotal: 0,
        reviewWriteoffTotal: 0, logisticsIuPerUnit: 0, logisticsStdPerUnit: 0,
        storagePerUnit: 0,
      }
      // baseRevenue=K → poolPerUnit = total; пул overhead=100 → доля=100/ед.
      function build(article: WeeklyArticleInput): WeeklyFinReportInputs {
        return {
          articles: [article],
          pools: {
            appliances: zeroPools(),
            clothing: {
              deliveryToMp: { total: 0, baseRevenue: 1000 },
              creditInterest: { total: 0, baseRevenue: 1000 },
              overhead: { total: 100, baseRevenue: 1000 },
              acceptance: { total: 0, baseRevenue: 1000 },
              storage: { total: 0, baseRevenue: 1000 },
            },
          },
        }
      }
      const withFix = computeWeeklyFinReport(build({ ...base, overheadFixedPerUnit: 256 })).articles[0]
      const noFix = computeWeeklyFinReport(build({ ...base })).articles[0]

      it("overheadPerUnit = 256 + доля пула (100) = 356 в ОБОИХ сценариях", () => {
        expect(withFix.iu.breakdown.overheadPerUnit).toBeCloseTo(356, 6)
        expect(withFix.std.breakdown.overheadPerUnit).toBeCloseTo(356, 6)
      })
      it("без поля overheadPerUnit = только пул (100); дельта = ровно 256/ед", () => {
        expect(noFix.iu.breakdown.overheadPerUnit).toBeCloseTo(100, 6)
        expect(withFix.iu.breakdown.overheadPerUnit - noFix.iu.breakdown.overheadPerUnit).toBeCloseTo(256, 6)
      })
      it("profit падает ровно на 256×H = 2560 (оба сценария)", () => {
        expect(noFix.iu.profit - withFix.iu.profit).toBeCloseTo(256 * 10, 4)
        expect(noFix.std.profit - withFix.std.profit).toBeCloseTo(256 * 10, 4)
      })
      it("appliances без поля не затронут (golden ИУ 523.6 / Оферта −2176.7 неизменны)", () => {
        const g = computeWeeklyFinReport(goldenInputs).articles[0]
        expect(g.iu.profit).toBeCloseTo(523.6, 0)
        expect(g.std.profit).toBeCloseTo(-2176.7, 0)
      })
    })
    ```
  </action>
  <verify>
    <automated>cd /c/Users/User/zoiten-pro && npx vitest run finance-weekly-engine && npx tsc --noEmit</automated>
  </verify>
  <done>Новый describe-блок зелёный; golden/jem/storage блоки по-прежнему зелёные; tsc без ошибок в types.ts/engine.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Данные + проводка — rename каскад clothingOverheadFixedRub → clothingOverheadPerUnitRub</name>
  <files>lib/finance-weekly/data.ts, app/actions/finance-weekly.ts, lib/finance-weekly/snapshot.ts, app/(dashboard)/finance/weekly/page.tsx, components/finance/WeeklyFinReportControls.tsx</files>
  <action>
    **lib/finance-weekly/data.ts:**
    1. Строка 114 — переименовать константу + добавить дефолт (обновить комментарий на per-unit модель):
       ```typescript
       /** Quick 260715-f4c: глобальный AppSetting-ключ ФИКСА общих расходов одежды
        *  НА ЕДИНИЦУ товара (₽/ед: 205 + 51), НЕ per неделя, НЕ по выручке.
        *  Пул одежды теперь = ТОЛЬКО manualPools.overheadCloth (переменная целиком). */
       export const CLOTHING_OVERHEAD_PER_UNIT_KEY = "financeWeekly.clothingOverheadPerUnitRub"
       export const CLOTHING_OVERHEAD_PER_UNIT_DEFAULT = 256
       ```
    2. Строка 178 (интерфейс `WeeklyFinReportPageData`) — переименовать поле + комментарий:
       ```typescript
       /** Quick 260715-f4c: фикс общих расходов одежды НА ЕДИНИЦУ (глобальный AppSetting, дефолт 256). */
       clothingOverheadPerUnitRub: number
       ```
    3. Строка 366 — в `key: { in: [...] }` заменить `CLOTHING_OVERHEAD_FIXED_KEY` → `CLOTHING_OVERHEAD_PER_UNIT_KEY`.
    4. Вынести парсинг ВВЕРХ (значение нужно раньше — в цикле сборки articles). Сразу ПОСЛЕ парсинга ставок (после строки 460, где `appliancesEff`/`clothingEff`) добавить:
       ```typescript
       // Quick 260715-f4c: фикс общих одежды/ед — глобальная константа (дефолт 256), НЕ по выручке.
       const perUnitParsed = parseFloat(settingsMap.get(CLOTHING_OVERHEAD_PER_UNIT_KEY) ?? "")
       const clothingOverheadPerUnitRub =
         Number.isFinite(perUnitParsed) && perUnitParsed >= 0
           ? perUnitParsed
           : CLOTHING_OVERHEAD_PER_UNIT_DEFAULT
       ```
    5. УДАЛИТЬ старый парсинг (строки 718-720: `fixedParsed` / `clothingOverheadFixedRub`).
    6. В `articles.push({ ... })` (около строки 682, рядом с `storagePerUnit НЕ задаём`) добавить поле — фикс ТОЛЬКО одежде:
       ```typescript
       // Quick 260715-f4c: фикс общих/ед — только одежда (appliances: undefined → движок `?? 0`).
       overheadFixedPerUnit: universe === "clothing" ? clothingOverheadPerUnitRub : undefined,
       ```
    7. Строка 780 (clothing.overhead) — снять фикс-слагаемое (пул = переменная целиком):
       ```typescript
       overhead: {
         total: manualPools.overheadCloth, // Quick 260715-f4c: переменная ЦЕЛИКОМ; фикс теперь per-unit (overheadFixedPerUnit)
         baseRevenue: clothBase,
       },
       ```
    8. Строка 805 (return, shorthand) — `clothingOverheadFixedRub,` → `clothingOverheadPerUnitRub,`.
    9. Оба early-return'а (строки 285 и 336) — `clothingOverheadFixedRub: 0,` → `clothingOverheadPerUnitRub: CLOTHING_OVERHEAD_PER_UNIT_DEFAULT,` (дефолт 256, чтобы поле в Controls показывало корректный дефолт при отсутствии данных).

    **app/actions/finance-weekly.ts:**
    - Строка 17 — импорт `CLOTHING_OVERHEAD_FIXED_KEY` → `CLOTHING_OVERHEAD_PER_UNIT_KEY`.
    - Строка 39 — `opts?: { clothingOverheadFixedRub?: number; jemOptionPct?: number }` → `clothingOverheadPerUnitRub?: number` (jemOptionPct без изменений).
    - Строки 30 (docstring), 63-70 — переименовать переменную/поле/ключ: читать `opts?.clothingOverheadPerUnitRub`, `Math.max(0, n)`, upsert `CLOTHING_OVERHEAD_PER_UNIT_KEY`. Логика (санитизация ≥0) без изменений.

    **lib/finance-weekly/snapshot.ts** (version НЕ бампить — 0 снапшотов на проде):
    - Строка 46 — `clothingOverheadFixedRub: number` → `clothingOverheadPerUnitRub: number`.
    - Строка 74 — `clothingOverheadFixedRub: data.clothingOverheadFixedRub,` → `clothingOverheadPerUnitRub: data.clothingOverheadPerUnitRub,`.

    **app/(dashboard)/finance/weekly/page.tsx:**
    - Строка 83 (снапшот-режим) — `clothingOverheadFixedRub={payload.clothingOverheadFixedRub}` → `clothingOverheadPerUnitRub={payload.clothingOverheadPerUnitRub}`.
    - Строка 117 (live-режим) — `clothingOverheadFixedRub={data.clothingOverheadFixedRub}` → `clothingOverheadPerUnitRub={data.clothingOverheadPerUnitRub}`.

    **components/finance/WeeklyFinReportControls.tsx:**
    - Строка 103 (Props) — `clothingOverheadFixedRub: number` → `clothingOverheadPerUnitRub: number` (комментарий: «фикс общих одежды НА ЕДИНИЦУ»).
    - Строка 125 (деструктуризация) — переименовать.
    - Строка 138 (state) — `const [fixedCloth, setFixedCloth] = useState(clothingOverheadFixedRub)` → `const [perUnitCloth, setPerUnitCloth] = useState(clothingOverheadPerUnitRub)`.
    - Строка 177 (handleSave opts) — `clothingOverheadFixedRub: fixedCloth` → `clothingOverheadPerUnitRub: perUnitCloth`.
    - Блок поля «Одежда» (строки ~410-431): label `Общие расходы (фикс.)` → `Фикс общих одежды, ₽/ед`; hint `глобальная константа (не per неделя)` → `205 + 51 ₽/ед · глобальная константа`; `value`/`onChange` перевести на `perUnitCloth`/`setPerUnitCloth`.
    - Блок состава пула (строки ~477-483) — переписать (фикс больше НЕ в пуле):
      ```tsx
      {group === "Одежда" && (
        <div className="text-[10px] text-muted-foreground tabular-nums">
          пул общих (переменная, по выручке) = {pools.overheadCloth.toLocaleString("ru-RU")} ₽
          {" · "}фикс {(Number.isFinite(perUnitCloth) ? perUnitCloth : 0).toLocaleString("ru-RU")} ₽/ед × кол-во
        </div>
      )}
      ```
  </action>
  <verify>
    <automated>cd /c/Users/User/zoiten-pro && npx tsc --noEmit</automated>
  </verify>
  <done>tsc без ошибок; строка `clothingOverheadFixedRub` / `CLOTHING_OVERHEAD_FIXED_KEY` отсутствует во всех .ts/.tsx (кроме .planning-архивов); пул одежды в data.ts = `manualPools.overheadCloth`; clothing-articles несут `overheadFixedPerUnit`.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot-тест fakeData + CLAUDE.md as-built + финальный гейт</name>
  <files>tests/finance-weekly-snapshot.test.ts, CLAUDE.md</files>
  <action>
    **tests/finance-weekly-snapshot.test.ts** — строка 98: `clothingOverheadFixedRub: 0,` → `clothingOverheadPerUnitRub: 256,` (в объекте `fakeData: WeeklyFinReportPageData`). ⚠ tsconfig исключает tests/** → tsc это НЕ ловит; правка обязательна вручную, иначе поле остаётся с прежним именем и roundtrip-тест теряет смысл.

    **CLAUDE.md** — обновить две формулировки под новую модель (аккуратно, точечно):
    - Строка 686 (секция «ДВА МИРА ЗАТРАТ»): `общие расходы одежды = AppSetting-фикс + недельная переменная (не из банка)` → `общие расходы одежды = фикс 256 ₽/ед (глобальный AppSetting) × кол-во + недельная переменная-пул (по выручке; не из банка)`.
    - Строка 703 (секция «Пулы затрат»): `фикс одежды \`financeWeekly.clothingOverheadFixedRub\`` → `фикс общих одежды НА ЕДИНИЦУ \`financeWeekly.clothingOverheadPerUnitRub\` (глобальный, дефолт 256 ₽/ед; переменная — недельный пул overheadCloth)`.
  </action>
  <verify>
    <automated>cd /c/Users/User/zoiten-pro && npm run test && npx tsc --noEmit</automated>
  </verify>
  <done>`npm run test` — все finance-weekly-* + pricing-math зелёные (вкл. новый overheadFixedPerUnit-блок и snapshot roundtrip); tsc чист; CLAUDE.md отражает per-unit модель.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — ноль ошибок (ловит весь rename-каскад в prod-коде).
- `npm run test` — вся сюита зелёная; критично: golden движка (523.6 / −2176.7) НЕ изменился, новый overheadFixedPerUnit-блок проходит, snapshot roundtrip проходит.
- Grep-контроль: `clothingOverheadFixedRub` и `CLOTHING_OVERHEAD_FIXED_KEY` отсутствуют в `lib/`, `app/`, `components/`, `tests/` (допустимы только в `.planning/` архивах и это исторические SUMMARY/PLAN/VERIFICATION).
- Ручная логика: для одежды `overheadPerUnit = 256 + доля(overheadCloth)`; для бытовой — как раньше (256 не применяется).
</verification>

<success_criteria>
- Модель общих одежды: ФИКС 256 ₽/ед × кол-во (не по выручке) + ПЕРЕМЕННЫЙ пул `overheadCloth` целиком (по выручке).
- Движок изменён минимально-аддитивно (одна строка в resolveCommon + одно опциональное поле в типах); golden неизменен.
- Bytовая техника не затронута.
- Старый ключ/поле полностью убраны из кода; новый глобальный AppSetting `financeWeekly.clothingOverheadPerUnitRub` (дефолт 256).
- Controls: редактируемое поле «Фикс общих одежды, ₽/ед» (205 + 51), сохраняется через saveWeeklyPools.
- tsc + vitest зелёные.
</success_criteria>

<output>
После завершения создать `.planning/quick/260715-f4c-256/260715-f4c-SUMMARY.md`.

**Пост-деплой (оркестратор, НЕ в задачу исполнителя):** SQL на проде —
`financeWeekly.clothingOverheadPerUnitRub = '256'`; удалить `financeWeekly.clothingOverheadFixedRub`;
перезалить `overheadCloth` пулов недель (теперь переменная целиком): 06.07 и 29.06 → 41451, 01.06/15.06/22.06 → 85951.
</output>
