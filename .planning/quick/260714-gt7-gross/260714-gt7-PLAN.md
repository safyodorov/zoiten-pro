---
phase: quick-260714-gt7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-weekly/clothing-net.ts
  - lib/finance-weekly/data.ts
  - tests/finance-weekly-clothing-net.test.ts
  - components/finance/WeeklyFinReportTable.tsx
autonomous: true
requirements: [GT7-01, GT7-02, GT7-03]
must_haves:
  truths:
    - "Базис одежды (clothing) в /finance/weekly считается по НЕТТО-выкупам (выкупы − возвраты), а не gross"
    - "Недельный агрегат per nmId клампится в 0 при отрицательном нетто (дневные возвраты > дневных выкупов не ломают неделю)"
    - "Артикул с нулевым нетто не вызывает деление на 0 и не ломает отчёт"
    - "Базис appliances (заказы из WbCardFunnelDaily) остаётся без изменений"
    - "Бейдж базиса одежды в UI отражает 'выкупы нетто (− возвраты)'"
  artifacts:
    - path: "lib/finance-weekly/clothing-net.ts"
      provides: "Pure-хелпер netClothingSales — нетто qty/rub из недельного агрегата WbSalesDaily"
      contains: "netClothingSales"
    - path: "lib/finance-weekly/data.ts"
      provides: "Нетто-агрегация salesRows для clothing (buyoutsCount − returnsCount, buyoutsRub + returnsRub)"
    - path: "tests/finance-weekly-clothing-net.test.ts"
      provides: "Кейсы: возвраты вычитаются, отрицательное нетто клампится в 0, нулевое нетто безопасно"
      contains: "netClothingSales"
    - path: "components/finance/WeeklyFinReportTable.tsx"
      provides: "Бейдж базиса одежды 'выкупы нетто (− возвраты)'"
  key_links:
    - from: "lib/finance-weekly/data.ts"
      to: "lib/finance-weekly/clothing-net.ts"
      via: "import netClothingSales, применён к salesRows _sum"
      pattern: "netClothingSales"
---

<objective>
Переключить базис одежды (clothing) в понедельном фин-отчёте /finance/weekly с GROSS-выкупов на НЕТТО (выкупы − возвраты).

Purpose: Экономист компании считает количество и выручку одежды по нетто. На 10 проверенных артикулах недели 06.07–12.07 нетто совпадает с его цифрами (пример 848714305: выкупы 12 − возвраты 4 = 8 = Excel). Прежнее решение «gross» (W2d Фикс 1) было принято на неделе без возвратов — теперь опровергнуто сверкой.

Output: Нетто-агрегация clothing в data.ts через pure-хелпер netClothingSales + обновлённый бейдж базиса в UI + тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Целевые файлы
@lib/finance-weekly/data.ts
@lib/finance-weekly/types.ts
@components/finance/WeeklyFinReportTable.tsx

<facts>
Проверено в прод-БД 2026-07-14 + подтверждено в prisma/schema.prisma:
- WbSalesDaily.buyoutsRub  Float — Σ priceWithDisc по выкупам (цена продавца до СПП)
- WbSalesDaily.buyoutsCount Int
- WbSalesDaily.returnsRub  Float — Σ priceWithDisc по возвратам, суммы ОТРИЦАТЕЛЬНЫЕ
- WbSalesDaily.returnsCount Int — счётчик ПОЛОЖИТЕЛЬНЫЙ

Канонический прецедент нетто-агрегации в проекте:
- lib/sales-plan/data.ts:678 → buyoutsRub + returnsRub (сложение, т.к. returnsRub < 0)
- app/actions/sales-plan.ts:880 → Math.max(0, buyoutsCount − returnsCount)
</facts>

<interfaces>
Текущая gross-агрегация в data.ts (строки ~357-443, W2d Фикс 1):

groupBy WbSalesDaily:
```typescript
prisma.wbSalesDaily.groupBy({
  by: ["nmId"],
  where: { nmId: { in: linkedNmIds }, date: { gte: weekStart, lte: weekEnd } },
  _sum: { buyoutsCount: true, buyoutsRub: true },
}),
```

Сборка salesByNmId:
```typescript
const salesByNmId = new Map<number, { qty: number; rub: number }>()
for (const r of salesRows) {
  salesByNmId.set(r.nmId, {
    qty: r._sum.buyoutsCount ?? 0,
    rub: r._sum.buyoutsRub ?? 0,
  })
}
```

Guard в цикле candidates (строка ~500): `if (qty <= 0) continue` — уже отсекает нулевой/отрицательный базис (артикул с нетто 0 пропускается, деления rub/qty на 0 не происходит).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-хелпер netClothingSales + нетто-агрегация в data.ts + тест</name>
  <files>lib/finance-weekly/clothing-net.ts, lib/finance-weekly/data.ts, tests/finance-weekly-clothing-net.test.ts</files>
  <behavior>
    netClothingSales({ buyoutsCount, buyoutsRub, returnsCount, returnsRub }) → { qty, rub }:
    - Test 1 (возвраты вычитаются): buyoutsCount 12, returnsCount 4, buyoutsRub 24000, returnsRub -8000 → qty 8, rub 16000
    - Test 2 (отрицательное нетто клампится в 0): buyoutsCount 3, returnsCount 5, buyoutsRub 6000, returnsRub -10000 → qty 0 (Math.max(0, ...)); rub допускается отрицательным (не клампится — цену считает data.ts после guard qty<=0)
    - Test 3 (нулевое нетто безопасно): buyoutsCount 4, returnsCount 4, buyoutsRub 8000, returnsRub -8000 → qty 0, rub 0 — не бросает, деления нет
    - Test 4 (null-поля _sum): все поля undefined/null → qty 0, rub 0 (?? 0 внутри)
    - Test 5 (нет возвратов): buyoutsCount 5, returnsCount 0, buyoutsRub 10000, returnsRub 0 → qty 5, rub 10000 (обратная совместимость с gross-неделями без возвратов)
    ВАЖНО про конвенцию: returnsRub уже ОТРИЦАТЕЛЬНЫЙ → в хелпере СЛОЖЕНИЕ (buyoutsRub + returnsRub), НЕ вычитание. returnsCount ПОЛОЖИТЕЛЬНЫЙ → ВЫЧИТАНИЕ (buyoutsCount − returnsCount). Свериться с фактами в &lt;facts&gt; — не полагаться на память.
  </behavior>
  <action>
1. Создать lib/finance-weekly/clothing-net.ts — pure-модуль (ноль импортов Prisma/React):
   - Интерфейс `ClothingSalesAgg { buyoutsCount: number; buyoutsRub: number; returnsCount: number; returnsRub: number }`
   - Функция `netClothingSales(agg: ClothingSalesAgg): { qty: number; rub: number }`:
     ```typescript
     const qty = Math.max(0, (agg.buyoutsCount ?? 0) - (agg.returnsCount ?? 0))
     const rub = (agg.buyoutsRub ?? 0) + (agg.returnsRub ?? 0)
     return { qty, rub }
     ```
   - JSDoc на русском: пояснить конвенцию БД (returnsRub &lt; 0 → сложение; returnsCount &gt; 0 → вычитание), кламп агрегата НЕДЕЛИ (не дня), ссылка на прецедент lib/sales-plan/data.ts. Дефолты через `?? 0` — переносят null из Prisma _sum.

2. Правки lib/finance-weekly/data.ts:
   - import: `import { netClothingSales } from "@/lib/finance-weekly/clothing-net"`
   - В groupBy WbSalesDaily (строка ~357) добавить в _sum: `returnsCount: true, returnsRub: true`
   - Заменить сборку salesByNmId (строки ~437-443) на:
     ```typescript
     const salesByNmId = new Map<number, { qty: number; rub: number }>()
     for (const r of salesRows) {
       salesByNmId.set(r.nmId, netClothingSales({
         buyoutsCount: r._sum.buyoutsCount ?? 0,
         buyoutsRub: r._sum.buyoutsRub ?? 0,
         returnsCount: r._sum.returnsCount ?? 0,
         returnsRub: r._sum.returnsRub ?? 0,
       }))
     }
     ```
   - Guard `if (qty <= 0) continue` (строка ~500) НЕ трогать — он корректно отсекает нетто &lt;= 0 (защита от деления на 0 при K = rub/qty). Только убедиться, что он остаётся.
   - ОБНОВИТЬ КОММЕНТАРИИ (это Фикс W2d, там написано «GROSS выкупы (БЕЗ вычета returns)»):
     * Строка ~9 (шапка модуля): «выкупы/выручка (clothing) — WbSalesDaily gross buyouts (W2d, Фикс 1)» → нетто (выкупы − возвраты), quick 260714-gt7
     * Строки ~354-356 (комментарий над groupBy): убрать «GROSS выкупы (БЕЗ вычета returns); сверка 2026-07-10: Excel F=37 = gross точно» → нетто-версия: возвраты вычитаются, кламп агрегата недели, сверка 06.07-12.07 (848714305: 12−4=8=Excel)
     * Строки ~491-494 (в цикле candidates, `rub = sales?.rub ?? 0 // gross до СПП, БЕЗ вычета returns`) → «нетто до СПП (выкупы + returnsRub<0)»
     * Строка ~28 (шапка про базис), ~467-468, ~585 — заменить «gross выкупы» → «нетто-выкупы» где встречается
   - Базис appliances (funnelRows / WbCardFunnelDaily) и plan-fact НЕ трогать.

3. Создать tests/finance-weekly-clothing-net.test.ts — vitest, стиль как tests/finance-weekly-jem-option.test.ts (import из "@/lib/finance-weekly/clothing-net", describe/it/expect). Покрыть Test 1-5 из &lt;behavior&gt;.
  </action>
  <verify>
    <automated>npx vitest run tests/finance-weekly-clothing-net.test.ts</automated>
  </verify>
  <done>netClothingSales экспортирован pure-модулем; data.ts агрегирует clothing по нетто через хелпер; groupBy тянет returnsCount/returnsRub; комментарии W2d обновлены на нетто; новый тест зелёный (5 кейсов); appliances-базис не изменён.</done>
</task>

<task type="auto">
  <name>Task 2: Бейдж базиса одежды в UI → «выкупы нетто (− возвраты)»</name>
  <files>components/finance/WeeklyFinReportTable.tsx</files>
  <action>
1. UNIVERSE_BASIS (строка ~36-39): `clothing: "по выкупам"` → `clothing: "по выкупам нетто"` (компактно для заголовка направления; полный текст в подписи ниже). appliances оставить «по заказам».
2. Подпись KPI-блока (строка ~302-304): `база: бытовая — заказы, одежда — выкупы` → `база: бытовая — заказы, одежда — выкупы нетто (− возвраты)`.
3. Обновить комментарии W2d про базис (строки ~34-35, ~301) — «выкупы gross» → «нетто-выкупы (− возвраты)», отметить quick 260714-gt7.
Ничего в логике таблицы/рендере не менять — только текст бейджей и комментарии.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Заголовок направления одежды показывает «· по выкупам нетто»; подпись KPI-блока — «одежда — выкупы нетто (− возвраты)»; tsc зелёный.</done>
</task>

</tasks>

<verification>
- npx vitest run tests/finance-weekly-clothing-net.test.ts — 5 кейсов зелёные
- npx vitest run tests/finance-weekly-engine.test.ts tests/finance-weekly-plan-fact.test.ts — регресс профильных тестов зелёный (движок/plan-fact не затронуты, базис appliances цел)
- npx tsc --noEmit -p tsconfig.json — типизация чистая
- Ручная сверка (после деплоя, UAT): 848714305 неделя 06.07-12.07 → нетто qty 8 = Excel экономиста
</verification>

<success_criteria>
- Clothing базис = нетто (выкупы − возвраты) на уровне недельного агрегата per nmId
- Отрицательное нетто клампится в 0, нулевое нетто не ломает отчёт (guard qty<=0 + Math.max)
- returnsRub складывается (не вычитается) — конвенция БД соблюдена
- Appliances-базис (заказы), план одежды (planBuyoutsRub), engine.ts — БЕЗ изменений
- UI-бейджи и все W2d-комментарии про gross обновлены на нетто
- Все профильные finance-weekly тесты + tsc зелёные
</success_criteria>

<output>
После завершения создать `.planning/quick/260714-gt7-gross/260714-gt7-SUMMARY.md`
</output>
