---
quick_id: 260706-q5a
title: Индекс сезонности в План продаж → Товары
status: in-progress
date: 2026-07-06
---

Дизайн (source of truth): `docs/superpowers/specs/2026-07-06-sales-plan-seasonality-design.md`.

# Порядок реализации (атомарные коммиты)

1. **Schema + миграция** — enum `SeasonalityScope`, model `SalesPlanSeasonality` (versionId? / scope / scopeId / month / indexPct), relation на `SalesPlanVersion`. Ручная миграция.
2. **Движок + разрешение + тесты (TDD)** — `types.ts` (ProductPlanInput.indexByMonth), `data.ts` (загрузка набора → resolve per товар → нормировка → indexByMonth), `engine.ts` (rate × index/100). Новый `tests/sales-plan-seasonality.test.ts` (resolve/scope/нормировка) + расширить engine-тест.
3. **Actions** — `saveSeasonalityIndex`, `resetSeasonality`, снапшот-хук в `fixSalesPlanVersion`. RBAC SALES MANAGE + regenerate.
4. **UI** — `SeasonalityBar.tsx` над `ProductPlanTable` + wiring в `products/page.tsx`.
5. **Deploy** — tsc + npm test, миграция на VPS (`prisma migrate deploy` в deploy.sh), detached nohup, curl 200.

# Verify
`npx tsc --noEmit` чисто; `npm run test` зелёный (seasonality + engine + golden не сломан при index=100).
