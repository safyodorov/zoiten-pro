---
phase: quick-260723-hr5-2-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wb-virtual-warehouse.ts
  - .planning/quick/260723-hr5-2-gate/burned-stock-wave2-2026-07-23.json
  - scripts/seed-bpla-warehouses.ts
  - tests/wb-virtual-warehouse.test.ts
autonomous: true
requirements: [QUICK-260723-hr5]
must_haves:
  truths:
    - "При необнулённых реальных складах Невинномысск(90024)/Краснодар(304) запуск сида НЕ засевает виртуальные 99003/99004 (gate-blocked, лог «GATE: … ещё не обнулён WB»)"
    - "После обнуления реального склада WB повторный запуск сида засевает соответствующий виртуальный склад зафиксированными остатками (per-size techSize)"
    - "После засева повторный запуск = no-op (already-seeded skip), даже если WB снова повезёт товар на реальный склад"
    - "Данные волны 1 (99001 Электросталь / 99002 Котовск) в проде не затронуты"
    - "npx tsc --noEmit проходит; gate-логика покрыта тестами (vitest)"
  artifacts:
    - path: "lib/wb-virtual-warehouse.ts"
      provides: "BPLA_WAREHOUSES с 99003/99004 (+realWarehouseId/wave) + pure decideBplaSeedAction"
      contains: "decideBplaSeedAction"
    - path: ".planning/quick/260723-hr5-2-gate/burned-stock-wave2-2026-07-23.json"
      provides: "Единый data-файл волны 2 (per-size items + warehouseName + realWarehouseId)"
    - path: "scripts/seed-bpla-warehouses.ts"
      provides: "Идемпотентный gate-сид волны 2 поверх существующего сида волны 1"
    - path: "tests/wb-virtual-warehouse.test.ts"
      provides: "Юнит-тесты pure gate-функции"
  key_links:
    - from: "scripts/seed-bpla-warehouses.ts"
      to: "decideBplaSeedAction"
      via: "решение seed/gate-blocked/already-seeded per склад волны 2"
      pattern: "decideBplaSeedAction"
    - from: "scripts/seed-bpla-warehouses.ts"
      to: "prisma.wbCardWarehouseStock.aggregate"
      via: "сумма quantity>0 на realWarehouseId (90024 / 304)"
      pattern: "aggregate"
---

<objective>
БПЛА-склады волна 2: Невинномысск и Краснодар сгорели 22.07.2026 утром. По аналогии с
волной 1 (Электросталь/Котовск, quick 260720-oh2) зафиксировать сгоревшие остатки на
новых виртуальных складах — НО с gate-механизмом: WB ещё НЕ обнулил реальные склады
(на 23.07 отдаёт Невинномысск 1283 шт / Краснодар 485 шт). Сеять виртуальный склад можно
ТОЛЬКО после обнуления реального прообраза, иначе двойной счёт в /finance/balance +
преждевременный вычет из inWayFromClient.

Purpose: скрипт становится безопасным для ежедневного крон-запуска — сам «дожидается»
обнуления реального склада, засевает ровно один раз, дальше no-op.
Output: расширенный BPLA_WAREHOUSES + pure gate-функция, единый data-файл волны 2,
переработанный идемпотентный gate-сид, юнит-тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260720-oh2-bpla-virtual-warehouses/260720-oh2-SUMMARY.md
@lib/wb-virtual-warehouse.ts
@scripts/seed-bpla-warehouses.ts
@.planning/quick/260723-hr5-2-gate/nevinnomyssk-rows.json
@.planning/quick/260723-hr5-2-gate/krasnodar-rows.json

<facts>
Проверено при подготовке плана:
- Невинномысск-rows.json: 128 строк, 1283 шт, techSizes {0,44,46,48,50,52,54,56,58,60}.
- Краснодар-rows.json: 134 строки, 485 шт, techSizes {0,42,44,46,48,50,52,54,56,58,60}.
  → В ОТЛИЧИЕ от волны 1 (techSize="") у волны 2 РЕАЛЬНЫЕ per-size остатки — upsert
  обязан использовать techSize из строки, ключ (wbCardId, warehouseId, techSize).
- Реальные склады-прообразы: Невинномысск id=90024, Краснодар id=304.
  ⚠ НЕ путать с реальным WB-складом «Виртуальный Краснодар» id=15998395 (isVirtual=false,
  транзитный) — его НЕ трогаем, gate смотрит только на 90024 / 304.
- Виртуальные волны 1: 99001 Электросталь (реальный 686), 99002 Котовск (реальный 90011) —
  засеяны, работают, НЕ трогать.
- Инфраструктура (isVirtual, защита clean-replace, вычет inWayFromClient через
  loadBurnedQtyByNmId, красная строка WB_BURNED в /finance/balance, бейдж БПЛА в /stock/wb)
  подхватит новые склады автоматически по isVirtual=true — менять её НЕ надо.
- Sync хранит per-size остаток: `techSize = item.techSize || ""`, upsert по compound
  unique (wbCardId, warehouseId, techSize). Реальные one-size строки имеют techSize="0".
</facts>

<interfaces>
<!-- Текущее содержимое lib/wb-virtual-warehouse.ts (расширяем аддитивно) -->

export const BPLA_WAREHOUSES = [
  { id: 99001, name: "Электросталь БПЛА" },
  { id: 99002, name: "Котовск БПЛА" },
] as const

export const BPLA_SHORT_CLUSTER = "БПЛА"
export function applyBurnedInWay(apiInWayFrom: number, burnedQty: number): number
export async function loadVirtualWarehouseIds(db: Db): Promise<Set<number>>
export async function loadBurnedQtyByNmId(db: Db): Promise<Map<number, number>>

<!-- WbCardWarehouseStock (Prisma) — compound unique для upsert -->
model WbCardWarehouseStock {
  wbCardId, warehouseId Int, techSize String @default(""), quantity Int
  @@unique([wbCardId, warehouseId, techSize])  // wbCardId_warehouseId_techSize
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить BPLA_WAREHOUSES + pure gate-функция + единый data-файл волны 2 + тесты</name>
  <files>lib/wb-virtual-warehouse.ts, .planning/quick/260723-hr5-2-gate/burned-stock-wave2-2026-07-23.json, tests/wb-virtual-warehouse.test.ts</files>
  <behavior>
    Тесты decideBplaSeedAction (pure, tests/wb-virtual-warehouse.test.ts):
    - { virtualHasRows: true,  realWarehouseQty: 1283 } → "already-seeded" (засеян → skip, не пересеиваем даже если WB снова повёз товар)
    - { virtualHasRows: true,  realWarehouseQty: 0 }    → "already-seeded"
    - { virtualHasRows: false, realWarehouseQty: 1283 } → "gate-blocked" (реальный склад ещё не обнулён)
    - { virtualHasRows: false, realWarehouseQty: 0 }    → "seed" (обнулён и ещё не засеян)
  </behavior>
  <action>
    1. lib/wb-virtual-warehouse.ts — аддитивно:
       - Расширить BPLA_WAREHOUSES до 4 записей с полями { id, name, wave, realWarehouseId }:
         { id: 99001, name: "Электросталь БПЛА", wave: 1, realWarehouseId: 686 },
         { id: 99002, name: "Котовск БПЛА",      wave: 1, realWarehouseId: 90011 },
         { id: 99003, name: "Невинномысск БПЛА", wave: 2, realWarehouseId: 90024 },
         { id: 99004, name: "Краснодар БПЛА",    wave: 2, realWarehouseId: 304 },
         (`as const` сохранить). Обновить doc-комментарий шапки: добавить волну 2, пожар 22.07.2026.
       - Добавить экспортируемый тип и pure-функцию:
         export type BplaSeedAction = "seed" | "gate-blocked" | "already-seeded"
         export function decideBplaSeedAction(input: { virtualHasRows: boolean; realWarehouseQty: number }): BplaSeedAction {
           if (input.virtualHasRows) return "already-seeded"   // засеян ранее — идемпотентный skip, не пересеиваем
           if (input.realWarehouseQty > 0) return "gate-blocked" // WB ещё не обнулил реальный склад-прообраз
           return "seed"                                        // обнулён и ещё не засеян
         }
         JSDoc: описать инвариант «сеять только после обнуления реального склада, ровно один раз».
    2. Создать единый data-файл волны 2 .planning/quick/260723-hr5-2-gate/burned-stock-wave2-2026-07-23.json,
       СОБРАННЫЙ из nevinnomyssk-rows.json и krasnodar-rows.json (одноразовым merge-скриптом в
       scratchpad через tsx/node — читает два снапшота, эмитит объект; сам merge-скрипт коммитить НЕ нужно).
       Формат:
       {
         "_source": "Снапшот прод-БД WbCardWarehouseStock 23.07.2026 02:30 (пожар 22.07 утром). Невинномысск 128 строк=1283 шт, Краснодар 134 строки=485 шт. techSize — реальные размеры (per-size, в отличие от волны 1).",
         "nevinnomyssk": {
           "virtualWarehouseId": 99003, "warehouseName": "Невинномысск БПЛА", "realWarehouseId": 90024,
           "totalQty": 1283, "items": [ { "nmId": ..., "techSize": "...", "quantity": ... }, ... ]
         },
         "krasnodar": {
           "virtualWarehouseId": 99004, "warehouseName": "Краснодар БПЛА", "realWarehouseId": 304,
           "totalQty": 485,  "items": [ ... ]
         }
       }
       items — 1:1 копия строк из соответствующего снапшота (nmId, techSize КАК ЕСТЬ включая "0", quantity).
       Проверить: sum(items.quantity) === totalQty для обоих (1283 / 485).
    3. tests/wb-virtual-warehouse.test.ts — добавить describe("decideBplaSeedAction") с 4 кейсами из <behavior>.
       Импорт: добавить decideBplaSeedAction к существующему import из "@/lib/wb-virtual-warehouse".
  </action>
  <verify>
    <automated>npx vitest run tests/wb-virtual-warehouse.test.ts</automated>
  </verify>
  <done>4 записи в BPLA_WAREHOUSES; decideBplaSeedAction экспортирована и покрыта 4 тестами (все зелёные); data-файл волны 2 существует, totals 1283/485 сходятся.</done>
</task>

<task type="auto">
  <name>Task 2: Переработать seed-bpla-warehouses.ts — gate-сид волны 2 поверх идемпотентного сида волны 1</name>
  <files>scripts/seed-bpla-warehouses.ts</files>
  <action>
    Переработать scripts/seed-bpla-warehouses.ts, сохранив совместимость с ежедневным крон-запуском.

    БЛОК ВОЛНЫ 1 (99001/99002) — оставить как есть по логике, но итерировать
    BPLA_WAREHOUSES.filter(w => w.wave === 1) вместо всего массива (чтобы новые wave-2
    склады не создавались этим блоком). Источник — прежний burned-stock-2026-07-17.json,
    upsert склада + upsert остатков REPLACE (техSize=""), nmId без WbCard → warn+skip.
    Идемпотентен (повторный запуск = no-op по значениям). НЕ менять прод-данные волны 1.

    БЛОК ВОЛНЫ 2 (99003/99004) — новый, GATE-механизм. Читать data-файл
    .planning/quick/260723-hr5-2-gate/burned-stock-wave2-2026-07-23.json.
    Для каждого wh из BPLA_WAREHOUSES.filter(w => w.wave === 2):
      a) group = data по virtualWarehouseId === wh.id (nevinnomyssk / krasnodar).
      b) virtualHasRows: (await prisma.wbCardWarehouseStock.count({ where: { warehouseId: wh.id } })) > 0
      c) realWarehouseQty: aggregate _sum.quantity по
         { warehouseId: wh.realWarehouseId, quantity: { gt: 0 } } → (sum ?? 0).
         (Обрабатывает и «склад отсутствует в свежих данных» → sum null → 0 → seed.)
      d) action = decideBplaSeedAction({ virtualHasRows, realWarehouseQty }).
      e) switch:
         - "already-seeded": console.log(`[seed-bpla] ${wh.name}: уже засеян (виртуальные строки есть) — skip`); continue.
         - "gate-blocked":  console.log(`GATE: ${wh.name} ещё не обнулён WB (qty=${realWarehouseQty}) — сид отложен`); continue.
         - "seed": upsert склада (id=wh.id, name=wh.name, cluster="БПЛА (сгоревшие склады)",
             shortCluster="БПЛА", isActive=true, needsClusterReview=false, isVirtual=true) —
             тот же create/update, что в блоке волны 1; затем per-item upsert остатков по
             compound unique (wbCardId, warehouseId=wh.id, techSize=item.techSize) с quantity=item.quantity
             (create+update REPLACE); nmId без WbCard → warn+skip (не throw).
             console.log сводку по складу: seeded rows, skippedNoCard.
    Финальный JSON-лог: warehousesUpserted, wave2 действия per склад (action + counts).

    ВАЖНО:
    - Скрипт остаётся безопасным для ежедневного запуска: gate + already-seeded гарантируют
      «засеять ровно один раз, дальше no-op».
    - Не деплоить, не менять crontab из задачи — это делает оркестратор (см. <output>).
    - Использовать techSize из строки как есть (per-size), НЕ "" — иначе потеряются размеры.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Скрипт: волна 1 итерирует wave===1 (без затрагивания новых складов); волна 2 применяет decideBplaSeedAction per склад с логами GATE/already-seeded/seed; upsert остатков по реальному techSize; tsc чист. Не выполнялся против прод-БД из задачи.</done>
</task>

</tasks>

<verification>
- npx vitest run tests/wb-virtual-warehouse.test.ts — 4 новых теста gate зелёные (+ прежние 8 не сломаны).
- npx tsc --noEmit — без ошибок.
- Ручная проверка data-файла: суммы 1283 (Невинномысск) / 485 (Краснодар).
- Прочитать код seed-скрипта: волна 1 не создаёт 99003/99004; волна 2 за gate.
</verification>

<success_criteria>
- Gate-функция pure, экспортирована, покрыта тестами.
- BPLA_WAREHOUSES содержит 4 склада с realWarehouseId.
- Единый data-файл волны 2 с per-size остатками собран из двух снапшотов.
- seed-скрипт: идемпотентен, gate-защищён, безопасен для ежедневного крона, не пересеивает засеянное.
- Данные волны 1 не тронуты; инфраструктура (isVirtual) не менялась.
</success_criteria>

<output>
После завершения создать `.planning/quick/260723-hr5-2-gate/260723-hr5-SUMMARY.md`.

**Ручные шаги ПОСЛЕ мержа (оркестратор делает push+deploy, но НЕ выполняет следующее):**

1. **Установить крон на VPS** — ежедневно 04:00 МСК, безопасен (gate + already-seeded):
   ```
   0 4 * * * cd /opt/zoiten-pro && set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/seed-bpla-warehouses.ts >> /var/log/bpla-seed.log 2>&1
   ```
   (crontab -e на 85.198.97.89; МСК = серверное время).

2. **Первый ручной прогон для проверки gate** (пока WB не обнулил — ожидается GATE-лог):
   ```
   ssh root@85.198.97.89
   cd /opt/zoiten-pro && set -a; . /etc/zoiten.pro.env; set +a
   npx tsx scripts/seed-bpla-warehouses.ts
   ```
   Ожидаемо на 23.07: волна 1 — no-op; волна 2 — `GATE: Невинномысск БПЛА ещё не обнулён WB (qty=1283)` и `GATE: Краснодар БПЛА ещё не обнулён WB (qty=485)`.

3. **После обнуления WB** (через ~1.5-2 дня по опыту Электростали) крон сам засеет 99003/99004.
   Проверить: `/finance/balance` красная строка вырастет; `/stock/wb` бейджи БПЛА на новых nmId;
   `SELECT COUNT(*) FROM "WbCardWarehouseStock" WHERE "warehouseId" IN (99003,99004);` > 0.
</output>
