---
quick_id: 260707-m5v
title: "Фаза B v1 — второй фин-рез «на стандартных условиях» в /prices/wb (срез отложён)"
status: in-progress
date: 2026-07-07
phase: quick-260707-m5v
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PRICES-STD-B]   # спека §7 Фаза B (v1, срез отложён)
autonomous: false               # финальный шаг — делегированный detached-деплой + миграция
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260707_wb_box_tariff/migration.sql
  - lib/wb-api.ts
  - lib/wb-box-tariffs.ts
  - lib/pricing-schemas.ts
  - app/api/wb-box-tariffs-sync/route.ts
  - app/api/cron/wb-box-tariffs/route.ts
  - app/api/cron/dispatch/route.ts
  - components/prices/WbBoxTariffsSyncButton.tsx
  - components/prices/GlobalRatesBar.tsx
  - lib/pricing-math.ts
  - tests/pricing-math.test.ts
  - app/(dashboard)/prices/wb/page.tsx
  - components/prices/PriceCalculatorTable.tsx
  - components/prices/PricingCalculatorDialog.tsx
  - docs/superpowers/specs/2026-07-07-wb-planned-prices-standard-finres-design.md

must_haves:
  truths:
    - "Кнопка «Обновить тарифы складов» тянет /tariffs/box и записывает AppSetting.wbBoxTariffEffective"
    - "Крон раз в сутки (через dispatcher) обновляет box-тарифы"
    - "В GlobalRatesBar редактируются возврат-логистика (₽) и индекс локализации (множитель)"
    - "В /prices/wb для каждой строки видны 3 столбца: Прибыль-std / ROI-std / Re-std"
    - "В модалке юнит-экономики есть второй блок «стандартные условия» со строками Хранение и Логистика-эфф"
    - "Golden nmId 800750522 (первый блок ИУ) не сломан; std-golden зелёный"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "model WbBoxTariff (сырые box-тарифы складов)"
      contains: "model WbBoxTariff"
    - path: "prisma/migrations/20260707_wb_box_tariff/migration.sql"
      provides: "CREATE TABLE WbBoxTariff + seed AppSetting ставок"
    - path: "lib/wb-api.ts"
      provides: "fetchBoxTariffs(date) — парсинг /tariffs/box"
      contains: "fetchBoxTariffs"
    - path: "lib/wb-box-tariffs.ts"
      provides: "syncBoxTariffs(db) — fetch→upsert→computeEffective→AppSetting"
      contains: "syncBoxTariffs"
    - path: "app/api/wb-box-tariffs-sync/route.ts"
      provides: "POST-эндпоинт кнопки (RBAC PRICES MANAGE)"
    - path: "app/api/cron/wb-box-tariffs/route.ts"
      provides: "GET-эндпоинт крона (x-cron-secret + LastRun)"
    - path: "lib/pricing-math.ts"
      provides: "calculatePricingStandard + опц. std-outputs"
      contains: "calculatePricingStandard"
    - path: "components/prices/PriceCalculatorTable.tsx"
      provides: "3 столбца std (profitStd/roiPctStd/returnOnSalesPctStd)"
    - path: "components/prices/PricingCalculatorDialog.tsx"
      provides: "второй блок фин-реза + Хранение/Логистика-эфф"
    - path: "components/prices/GlobalRatesBar.tsx"
      provides: "редакторы wbReturnLogisticsRub + wbLocalizationIndex"
  key_links:
    - from: "app/(dashboard)/prices/wb/page.tsx"
      to: "lib/pricing-math.ts:calculatePricingStandard"
      via: "per-row вызов → row.computedStd"
      pattern: "calculatePricingStandard"
    - from: "components/prices/PriceCalculatorTable.tsx"
      to: "row.computedStd"
      via: "рендер 3 std-столбцов"
      pattern: "computedStd"
    - from: "lib/wb-box-tariffs.ts:syncBoxTariffs"
      to: "AppSetting.wbBoxTariffEffective"
      via: "upsert JSON эффективных ставок"
      pattern: "wbBoxTariffEffective"
    - from: "app/(dashboard)/prices/wb/page.tsx"
      to: "AppSetting.wbBoxTariffEffective"
      via: "чтение + JSON.parse → входы std-расчёта"
      pattern: "wbBoxTariffEffective"
    - from: "components/prices/WbBoxTariffsSyncButton.tsx"
      to: "/api/wb-box-tariffs-sync"
      via: "fetch POST"
      pattern: "wb-box-tariffs-sync"
    - from: "app/api/cron/dispatch/route.ts"
      to: "/api/cron/wb-box-tariffs"
      via: "shouldFireCron ветка + dynamic import"
      pattern: "wb-box-tariffs"
---

<objective>
Реализовать **Фазу B v1** дизайна «Плановые цены + второй фин-рез» — параллельно текущему
расчёту по ИУ показать в /prices/wb прибыль/ROI/Re продаж **на стандартных условиях**
(стандартная комиссия + хранение + логистика к клиенту с учётом выкупа + возврат при невыкупе).

Фаза A (плановые цены — строка «Плановая», `WbCard.plannedSellerPrice`, `savePlannedPrice`)
**уже задеплоена** (quick 260707-k9g). Здесь строим только второй фин-рез.

Purpose: дать управленцу «честную» юнит-экономику по стандартным (не льготным ИУ) условиям
с полной моделью логистики/хранения — база для решений о плановых ценах.

Output: тариф-синк складов (`/tariffs/box` → `WbBoxTariff` → эффективные ставки в
`AppSetting.wbBoxTariffEffective`, крон + кнопка); `calculatePricingStandard` в pricing-math;
3 новых столбца + второй блок в модалке; 2 новые ставки в GlobalRatesBar.

**⚠ СРЕЗ ОТЛОЖЕН (спека §5):** НЕ взвешивать по `WbCardWarehouseStock`/кластерам. Эффективные
ставки = представитель/среднее по округам (сейчас идентичны, коэффициенты все 100%) → одна
флэт-запись на все товары. НЕ грузить commission.xlsx.
</objective>

<context>
@docs/superpowers/specs/2026-07-07-wb-planned-prices-standard-finres-design.md
@lib/pricing-math.ts
@tests/pricing-math.test.ts
@app/(dashboard)/prices/wb/page.tsx
@components/prices/PriceCalculatorTable.tsx
@components/prices/PricingCalculatorDialog.tsx
@components/prices/GlobalRatesBar.tsx
@lib/wb-api.ts
@app/api/cron/dispatch/route.ts
@app/actions/pricing.ts
@lib/pricing-schemas.ts

<interfaces>
<!-- Контракты, извлечённые из кодовой базы. Использовать напрямую, без доразведки. -->

Разведка прод-API `/tariffs/box` (ФАКТ, перебивает спеку):
  host `common-api.wildberries.ru/api/v1/tariffs/box?date=YYYY-MM-DD`, scope **Тарифы**, лимит ~60/мин.
  Ответ: response.data.warehouseList[] со СТРОКАМИ через запятую; "-" = н/д:
    boxDeliveryBase   "46"    → deliveryBase   (₽ за первый литр)
    boxDeliveryLiter  "14"    → deliveryLiter  (₽ за каждый доп. литр)
    boxDeliveryCoefExpr "100" → deliveryCoefPct (% логистики, сейчас 100)
    boxStorageBase    "0,07"  → storageBase    (₽/л/сут)  ← 0,07 (НЕ 0,08!)
    boxStorageLiter   "0,07"  → storageLiter   (₽/л/сут доп.)
    boxStorageCoefExpr "100"  → storageCoefPct (% хранения, сейчас 100)
    warehouseName             → ОКРУГ/ВРЦ
  + response.data.{currency, dtTillMax, dtNextBox}.
  Парсинг: s == null || s.trim()==="-" → null; иначе Number(s.replace(",",".")) (NaN → null).

lib/wb-api.ts:
  // wbFetch(endpoint, url, opts) — endpoint-строка "Tariffs API" маппится в bucket "tariffs"
  // (см. resolveBucketFromEndpoint). На 429 бросает WbRateLimitError. Использовать её.
  export async function fetchStandardCommissions(): Promise<Map<number,{fbw:number;fbs:number}>>
  // ↑ образец tariffs-запроса (host common-api, wbFetch("Tariffs API", ...), res.ok guard).

lib/pricing-math.ts:
  export interface PricingInputs { priceBeforeDiscount, sellerDiscountPct, wbDiscountPct,
    clubDiscountPct, commFbwPct, drrPct, defectRatePct, deliveryCostRub, costPrice, buyoutPct,
    walletPct, acquiringPct, jemPct, creditPct, overheadPct, taxPct }   // всё number
  export interface PricingOutputs { ...18 полей..., profit, returnOnSalesPct, roiPct }
  export function calculatePricing(inputs: PricingInputs): PricingOutputs   // ⚠ НЕ ТРОГАТЬ ТЕЛО
  // Golden nmId 800750522: profit≈567.683 / roiPct≈25.76 / returnOnSalesPct≈7.33

components/prices/PriceCalculatorTable.tsx:
  export interface PriceRow { ..., computed: PricingOutputs, inputs: PricingInputs,
    context:{productId,subcategoryId,categoryId}, globalValues:{...} }
  const COLUMN_KEYS = [ ... "profit","returnOnSalesPct","roiPct" ] as const   // добавить 3 в конец
  const DEFAULT_WIDTHS: Record<ColumnKey, number> = { ... }
  const HIDEABLE_COLUMN_KEYS: ColumnKey[] = [ ... ]
  const SCROLL_COLUMNS = [ ... {key:"roiPct",label:"ROI, %"} ]              // добавить 3 в конец
  // рендер значений: массив [key, fmt...] у row.computed.X (строки ~1329-1351)

app/(dashboard)/prices/wb/page.tsx:
  // include product → все скаляры Product (heightCm/widthCm/depthCm/*OverridePct) уже доступны.
  // resolvedCommission = product.commissionOverridePct ?? card.commFbwIu ?? card.commFbwStd ?? 0
  // каждая строка: computed: calculatePricing(inputs)  → добавить computedStd
  // rates строится из RATE_KEYS-запроса AppSetting; card.{stockQty,avgSalesSpeed7d,buyoutPercent,commFbwStd}

app/actions/pricing.ts + lib/pricing-schemas.ts:
  export const APP_SETTING_KEYS = [...7 ключей] as const          // += 2 новых
  export const APP_SETTING_DEFAULTS: Record<AppSettingKey,number>  // += 2 дефолта
  export async function updateAppSetting(key,value)                // Zod [0,100], .toFixed(1)
  export async function getPricingSettings(): Record<AppSettingKey,number>  // питает GlobalRatesBar.initialRates

app/api/cron/dispatch/route.ts:
  // findMany по списку ключей {X}CronTime + {X}LastRun; ветка shouldFireCron({currentHHMM,storedTime,lastRunDate,today})
  // → dynamic import("../<cron>/route").GET(req). Cron-роут сам upsert-ит {X}LastRun = getMskTodayString().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Тариф-синк складов (WbBoxTariff + миграция) + новые AppSetting-ставки + GlobalRatesBar</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260707_wb_box_tariff/migration.sql,
    lib/wb-api.ts,
    lib/wb-box-tariffs.ts,
    lib/pricing-schemas.ts,
    app/api/wb-box-tariffs-sync/route.ts,
    app/api/cron/wb-box-tariffs/route.ts,
    app/api/cron/dispatch/route.ts,
    components/prices/WbBoxTariffsSyncButton.tsx,
    components/prices/GlobalRatesBar.tsx,
    app/(dashboard)/prices/wb/page.tsx
  </files>
  <action>
1) **schema.prisma** — новая модель (после блока Phase 7 / рядом с WbCard):
```prisma
// Фаза B (2026-07-07): сырые box-тарифы складов WB из /tariffs/box.
// Срез по стоку ОТЛОЖЕН — эффективные ставки хранятся флэтом в AppSetting.wbBoxTariffEffective.
model WbBoxTariff {
  warehouseName   String    @id            // ОКРУГ/ВРЦ (warehouseName из API)
  deliveryBase    Float?                   // boxDeliveryBase, ₽ (первый литр)
  deliveryLiter   Float?                   // boxDeliveryLiter, ₽ (каждый доп. литр)
  deliveryCoefPct Float?                   // boxDeliveryCoefExpr, % (сейчас 100)
  storageBase     Float?                   // boxStorageBase, ₽/л/сут
  storageLiter    Float?                   // boxStorageLiter, ₽/л/сут (доп.)
  storageCoefPct  Float?                   // boxStorageCoefExpr, % (сейчас 100)
  dtTillMax       DateTime?                // response.data.dtTillMax
  updatedAt       DateTime  @updatedAt
}
```
После правки — `npx prisma generate` локально (клиент типов; локальной PG нет — миграция применится на VPS).

2) **Ручная миграция** `prisma/migrations/20260707_wb_box_tariff/migration.sql` (паттерн 260706-q5a — SQL пишем руками, `prisma migrate deploy` применит на проде):
```sql
CREATE TABLE "WbBoxTariff" (
  "warehouseName"   TEXT NOT NULL,
  "deliveryBase"    DOUBLE PRECISION,
  "deliveryLiter"   DOUBLE PRECISION,
  "deliveryCoefPct" DOUBLE PRECISION,
  "storageBase"     DOUBLE PRECISION,
  "storageLiter"    DOUBLE PRECISION,
  "storageCoefPct"  DOUBLE PRECISION,
  "dtTillMax"       TIMESTAMP(3),
  "updatedAt"       TIMESTAMP(3) NOT NULL,
  CONSTRAINT "WbBoxTariff_pkey" PRIMARY KEY ("warehouseName")
);
-- Ставки Фазы B (Зод [0,100], .toFixed(1)): возврат-логистика ₽, индекс локализации ×.
INSERT INTO "AppSetting" ("key","value","updatedAt") VALUES
  ('wbReturnLogisticsRub','50.0', now()),
  ('wbLocalizationIndex','1.0', now())
ON CONFLICT ("key") DO NOTHING;
```

3) **lib/wb-api.ts** → `fetchBoxTariffs(date: string)` по образцу `fetchStandardCommissions`:
   - `wbFetch("Tariffs API", "https://common-api.wildberries.ru/api/v1/tariffs/box?date=${date}", { headers:{ Authorization: token } })` (endpoint-строка "Tariffs API" → bucket tariffs; getToken() как в fetchStandardCommissions).
   - `if (!res.ok) throw new Error(...)`.
   - helper `parseNum(s): number|null = (s==null || String(s).trim()==="-") ? null : (Number.isNaN(Number(String(s).replace(",","."))) ? null : Number(String(s).replace(",",".")))`.
   - Вернуть `{ warehouses: {warehouseName, deliveryBase, deliveryLiter, deliveryCoefPct, storageBase, storageLiter, storageCoefPct}[], dtTillMax: Date|null }` из `response.data.warehouseList` + `response.data.dtTillMax`.

4) **lib/wb-box-tariffs.ts** → `export async function syncBoxTariffs(db)` (принимает PrismaClient — DI, БЕЗ next-auth импортов, тестируемо):
   - `const t = await fetchBoxTariffs(getMskTodayString())` (или YYYY-MM-DD).
   - upsert каждого warehouse в `WbBoxTariff` (where warehouseName).
   - **computeEffective** (срез отложён → просто среднее не-null по всем складам per поле; округа сейчас идентичны):
     `effective = { delivBase, delivLiter, delivCoefPct, storageBasePerLiter, storageLiterPerDay, storageCoefPct, dtTillMax }`
     где delivBase=avg(deliveryBase), delivLiter=avg(deliveryLiter), delivCoefPct=avg(deliveryCoefPct),
     storageBasePerLiter=avg(storageBase), storageLiterPerDay=avg(storageLiter), storageCoefPct=avg(storageCoefPct),
     dtTillMax=ISO(dtTillMax). (avg игнорирует null; если все null → null.)
   - `db.appSetting.upsert({ where:{key:"wbBoxTariffEffective"}, create/update:{ value: JSON.stringify(effective) } })`.
   - Вернуть `{ warehouses: N, effective }`.

5) **POST /api/wb-box-tariffs-sync** (`app/api/wb-box-tariffs-sync/route.ts`, runtime nodejs):
   - `try { await requireSection("PRICES","MANAGE") } catch → 401 UNAUTHORIZED / 403 FORBIDDEN` (маппинг как в pricing.ts handleAuthError, но вернуть NextResponse.json со status).
   - `const r = await syncBoxTariffs(prisma)`; `revalidatePath("/prices/wb")`; `NextResponse.json({ ok:true, ...r })`. Ошибку → 500 с сообщением.

6) **Cron** `app/api/cron/wb-box-tariffs/route.ts` (GET, паттерн wb-sales-daily):
   - x-cron-secret gate; `await syncBoxTariffs(prisma)`;
   - `await prisma.appSetting.upsert({ where:{key:"wbBoxTariffsLastRun"}, create/update:{ value:getMskTodayString() } })`;
   - `NextResponse.json({ ok:true })`.
   **dispatch/route.ts**: в findMany-список добавить `"wbBoxTariffsCronTime","wbBoxTariffsLastRun"`; извлечь `boxTariffsTime = settings.wbBoxTariffsCronTime ?? "05:20"` + `boxTariffsLastRun`; добавить ветку `shouldFireCron({...})` → `import("../wb-box-tariffs/route").GET(req)` → `fired.push('box-tariffs:'+res.status)`.

7) **components/prices/WbBoxTariffsSyncButton.tsx** (client, паттерн WbSyncSppButton): POST `/api/wb-box-tariffs-sync`, toast success/error, `router.refresh()`, иконка Warehouse/PackageOpen (lucide), текст «Тарифы складов» / «Тарифы…». Вставить в шапку page.tsx в `<div className="ml-auto flex flex-wrap gap-2">` рядом с WbSyncSppButton.

8) **Ставки в UI**: в `lib/pricing-schemas.ts` → `APP_SETTING_KEYS` += `"wbReturnLogisticsRub","wbLocalizationIndex"`; `APP_SETTING_DEFAULTS` += `wbReturnLogisticsRub:50.0, wbLocalizationIndex:1.0`. В `GlobalRatesBar.tsx`: добавить 2 поля в `RateKey`/`RATES` — `{key:"wbReturnLogisticsRub", label:"Возврат-логистика", unit:"₽"}`, `{key:"wbLocalizationIndex", label:"Индекс локализации", unit:"×"}`. Ввести опциональный `unit` в RateSpec (дефолт "%") и рендерить суффикс из него вместо жёсткого «%». (initialRates придут из getPricingSettings, который теперь вернёт новые ключи.)

**НЕ дёргать /tariffs/box в билд-тайме** (429) — синк только по кнопке/крону.
  </action>
  <verify>
    <automated>npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <done>
    Схема+миграция созданы; `fetchBoxTariffs`/`syncBoxTariffs` компилируются; POST-эндпоинт (RBAC MANAGE)
    и cron-роут + ветка dispatcher на месте; кнопка «Тарифы складов» в шапке; 2 новые ставки в
    GlobalRatesBar и в APP_SETTING_KEYS/DEFAULTS. `npx tsc --noEmit` чисто.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calculatePricingStandard в pricing-math + std-golden (первый блок не трогаем)</name>
  <files>lib/pricing-math.ts, tests/pricing-math.test.ts</files>
  <behavior>
    - Golden nmId 800750522 первого блока НЕ меняется: calculatePricing(goldenInputs).profit≈567.683, roiPct≈25.76, returnOnSalesPct≈7.33 (существующие тесты зелёные).
    - calculatePricingStandard считает по формулам спеки §4:
        V (входной volumeLiters, округл. до 0,1 л);
        Л_туда = (delivBase + delivLiter × max(0, V−1)) × (delivCoefPct/100) × ИЛ;
        Л_эфф  = [Л_туда + (1−ПВ) × returnLogisticsRub] / ПВ,  где ПВ=buyoutPct/100, guard ПВ>0
                 (если ПВ≤0 → Л_эфф = Л_туда, т.е. без амортизации возврата);
        Хранение = storageBasePerLiter × V × (storageCoefPct/100) × daysInStock.
      Прибыль-std = первый блок, НО: комиссия=commStdPct, deliveryCostRub ЗАМЕНЯЕТСЯ на Л_эфф,
      и ДОПОЛНИТЕЛЬНО вычитается Хранение. ROI-std/Re-std — из profitStd по тем же формулам.
    - std-golden (детерминированный): goldenInputs + { commStdPct:25, volumeLiters:5, buyoutPct:90,
      delivBase:46, delivLiter:14, delivCoefPct:100, localizationIndex:1.0, returnLogisticsRub:50,
      storageBasePerLiter:0.07, storageCoefPct:100, daysInStock:60 }
      → Л_туда=102, Л_эфф=118.888…, Хранение=21, profitStd≈1044.74 ₽ (исполнитель пересчитывает
      точное значение из формул и пинит с tolerance; roiStd≈47.4%, reStd≈13.48%).
    - Zero-guards: buyoutPct=0 → без NaN/Infinity; costPrice=0 → roiPctStd=0; sellerPrice=0 → returnOnSalesPctStd=0.
  </behavior>
  <action>
1) **PricingInputs** += опциональные std-входы: `commStdPct?`, `volumeLiters?`, `delivBase?`, `delivLiter?`,
   `delivCoefPct?`, `localizationIndex?`, `returnLogisticsRub?`, `storageBasePerLiter?`, `storageCoefPct?`,
   `daysInStock?` — ВСЕ optional (golden test передаёт их без — тогда std не считается / выходы undefined).
   `buyoutPct` уже есть (проценты 0-100) — переиспользовать как ПВ×100.
2) **PricingOutputs** += опциональные `profitStd?`, `roiPctStd?`, `returnOnSalesPctStd?`, `storageAmount?`,
   `logisticsEffAmount?`, `logisticsToAmount?` — все optional, чтобы существующий golden (без std-входов) не падал.
3) `calculatePricing` — **тело НЕ трогать** (кроме, при желании, ничего). Добавить ОТДЕЛЬНУЮ pure-функцию
   `export function calculatePricingStandard(inputs: PricingInputs): PricingOutputs`:
     - `const V = Math.round(Math.max(0, inputs.volumeLiters ?? 0) * 10) / 10`
     - `const pv = (inputs.buyoutPct ?? 100) / 100`
     - `const logTo = (delivBase + delivLiter*Math.max(0,V-1)) * (delivCoefPct/100) * localizationIndex`
     - `const logEff = pv > 0 ? (logTo + (1-pv)*returnLogisticsRub)/pv : logTo`
     - `const storage = storageBasePerLiter * V * (storageCoefPct/100) * daysInStock`
     - `const base = calculatePricing({ ...inputs, commFbwPct: (inputs.commStdPct ?? inputs.commFbwPct), deliveryCostRub: logEff })`
       (переиспользуем golden-протестированное ядро: комиссия=std, доставка=Л_эфф.)
     - `const profitStd = base.profit - storage`
     - `roiPctStd = base.sellerPrice... costPrice>0 ? profitStd/costPrice*100 : 0`; `returnOnSalesPctStd = sellerPrice>0 ? profitStd/sellerPrice*100 : 0`
     - вернуть `{ ...base, profit: base.profit, ..., profitStd, roiPctStd, returnOnSalesPctStd, storageAmount: storage, logisticsEffAmount: logEff, logisticsToAmount: logTo }`.
       (используй дефолты: delivBase= inputs.delivBase ?? 0, delivLiter ?? 0, delivCoefPct ?? 100, localizationIndex ?? 1,
        returnLogisticsRub ?? 50, storageBasePerLiter ?? 0, storageCoefPct ?? 100, daysInStock ?? 0.)
4) **tests/pricing-math.test.ts** — существующие describe-блоки НЕ менять. Добавить новый describe
   «calculatePricingStandard — std-golden» с входами выше; пин profitStd/roiPctStd/returnOnSalesPctStd
   (toBeCloseTo, tolerance 0.1) + std zero-guards (buyoutPct=0/costPrice=0/sellerPrice=0 → конечные числа).
  </action>
  <verify>
    <automated>npm run test -- pricing-math</automated>
  </verify>
  <done>
    Существующий golden (profit≈567.68/roiPct≈25.76/re≈7.33) зелёный; `calculatePricingStandard` считает
    Л_туда/Л_эфф/Хранение/profitStd по §4; std-golden + zero-guards зелёные. `npm run test` весь зелёный
    (pricing-math + sales-plan не сломаны).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: page.tsx резолвинг std-входов + 3 столбца + второй блок модалки + спека + деплой</name>
  <files>
    app/(dashboard)/prices/wb/page.tsx,
    components/prices/PriceCalculatorTable.tsx,
    components/prices/PricingCalculatorDialog.tsx,
    docs/superpowers/specs/2026-07-07-wb-planned-prices-standard-finres-design.md
  </files>
  <what-built>
1) **page.tsx резолвинг** (внутри per-card блока, рядом с resolvedCommission):
   - Прочитать std-настройки из AppSetting: расширить `RATE_KEYS`-запрос ИЛИ отдельным `findMany`
     по `["wbReturnLogisticsRub","wbLocalizationIndex","wbBoxTariffEffective"]`. Распарсить
     `wbBoxTariffEffective` через `JSON.parse` (fallback объект при отсутствии: delivBase=46, delivLiter=14,
     delivCoefPct=100, storageBasePerLiter=0.07, storageCoefPct=100). `returnLogisticsRub` дефолт 50, `localizationIndex` дефолт 1.0.
   - `const commStdPct = product.commissionOverridePct ?? card.commFbwStd ?? 0`.
   - `const V = ((product.heightCm ?? 0) * (product.widthCm ?? 0) * (product.depthCm ?? 0)) / 1000` (0 если нет габаритов).
   - `дни_на_складе`: `const salesPerDay = (card.avgSalesSpeed7d ?? 0)/7; const daysInStock = salesPerDay > 0 && (card.stockQty ?? 0) > 0 ? (card.stockQty!/salesPerDay) : 60` (fallback 60).
   - Собрать `stdParams = { commStdPct, volumeLiters:V, delivBase, delivLiter, delivCoefPct, storageBasePerLiter,
     storageCoefPct, localizationIndex, returnLogisticsRub, daysInStock }` (buyoutPct уже в inputs).
   - Для **каждой** строки (current/planned/regular/auto/calculated): при формировании PriceRow добавить
     `computedStd: calculatePricingStandard({ ...<inputs этой строки>, ...stdParams })`. Т.е. рядом с
     `computed: calculatePricing(xInputs)` добавить `computedStd: calculatePricingStandard({ ...xInputs, ...stdParams })`.
   - Прокинуть stdParams в PriceRow (новое опц. поле `stdContext`), чтобы модалка realtime-пересчитывала при правке цены.
2) **PriceRow тип** (PriceCalculatorTable.tsx): `computedStd?: PricingOutputs` + `stdContext?: { commStdPct, volumeLiters, delivBase, delivLiter, delivCoefPct, storageBasePerLiter, storageCoefPct, localizationIndex, returnLogisticsRub, daysInStock }`.
3) **3 столбца** в PriceCalculatorTable.tsx: в конец `COLUMN_KEYS` добавить `"profitStd","roiPctStd","returnOnSalesPctStd"`;
   в `DEFAULT_WIDTHS` (по ~100/90/90); в `HIDEABLE_COLUMN_KEYS`; в `SCROLL_COLUMNS` — `{key:"profitStd",label:"Прибыль-std, руб."}`,
   `{key:"roiPctStd",label:"ROI-std, %"}`, `{key:"returnOnSalesPctStd",label:"Re-std, %"}`. В массиве рендера
   td (рядом с profit/roiPct) добавить 3 записи: `["profitStd", row.computedStd ? fmtMoneyInt(row.computedStd.profitStd) : "—", profitClass(row.computedStd?.profitStd)]` и аналогично для roiPctStd/returnOnSalesPctStd (fmtPct). "—" если computedStd отсутствует.
4) **Модалка** PricingCalculatorDialog.tsx: добавить `liveOutputsStd = useMemo(()=>calculatePricingStandard({ ...inputs, ...row.stdContext }), [...])`
   (deps — те же watch-поля + row.stdContext). Ниже первого блока прибыли — второй блок «Стандартные условия»
   с OutputRow: «Логистика туда» (logisticsToAmount), «Логистика эфф.» (logisticsEffAmount), «Хранение» (storageAmount),
   «Прибыль-std» (profitStd, цветной), «ROI-std %», «Re продаж-std %». Если row.stdContext нет — блок скрыть.
5) **Спека**: в шапке `docs/superpowers/.../2026-07-07-...design.md` пометить «Статус: Фаза A задеплоена; **Фаза B v1 реализована (срез §5 ОТЛОЖЕН)**»; в §7 у «Фаза B» дописать «✅ v1 (флэт-ставки, без взвешивания по стоку — отложено)».

**Гейты перед деплоем:** `npx tsc --noEmit` чисто; `npm run test` зелёный (golden pricing-math + std-golden + sales-plan).
Затем деплой (ДЕЛЕГИРОВАН, detached — миграция WbBoxTariff применится через `prisma migrate deploy` в deploy.sh):
`git add -A && git commit` → `git push origin main` → `ssh root@85.198.97.89 "cd /opt/zoiten-pro && nohup bash deploy.sh > /var/log/zoiten-deploy.log 2>&1 &"` → следить за логом до `==> Done` → `curl https://zoiten.pro` 200.
Пост-деплой: один раз нажать «Тарифы складов» на /prices/wb (заполнит wbBoxTariffEffective; до этого std-столбцы считают по fallback-ставкам).
  </what-built>
  <how-to-verify>
1) Открыть https://zoiten.pro/prices/wb — справа появились 3 столбца **Прибыль-std / ROI-std / Re-std**, значения заполнены (не «—») для строк с габаритами.
2) Нажать кнопку **«Тарифы складов»** в шапке → toast «обновлено N складов»; после refresh значения std-столбцов пересчитались по реальным тарифам.
3) В **GlobalRatesBar** появились поля «Возврат-логистика ₽» (50) и «Индекс локализации ×» (1.0); правка → std-столбцы меняются.
4) Клик по строке → в модалке под первым блоком прибыли виден **второй блок «Стандартные условия»** со строками Логистика туда/эфф, Хранение, Прибыль-std, ROI-std, Re-std; правка цены продавца пересчитывает оба блока realtime.
5) Первый блок (ИУ) и все существующие столбцы не изменились.
  </how-to-verify>
  <resume-signal>Напишите «approved» или опишите проблемы (напр. std-столбцы пустые / модалка без второго блока / тарифы не тянутся)</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — 0 ошибок.
- `npm run test` — весь зелёный: существующий golden pricing-math (nmId 800750522) + новый std-golden + sales-plan (не сломаны).
- `npx prisma generate` — клиент содержит WbBoxTariff.
- Прод: `curl https://zoiten.pro` → 200; `/prices/wb` рендерит 3 std-столбца; кнопка «Тарифы складов» заполняет `AppSetting.wbBoxTariffEffective`; крон-ветка `box-tariffs` в dispatcher.
</verification>

<success_criteria>
- WbBoxTariff + ручная миграция; `fetchBoxTariffs`/`syncBoxTariffs` тянут `/tariffs/box`, парсят запятые/"-", пишут флэт-эффективные ставки в AppSetting (СРЕЗ НЕ реализован — отложен).
- POST-кнопка (RBAC PRICES MANAGE) + cron раз/сутки через dispatcher.
- `calculatePricingStandard` считает Прибыль-std/ROI-std/Re-std по §4 (Л_туда, Л_эфф с выкупом+возвратом, Хранение), golden первого блока не сломан, std-golden зелёный.
- page.tsx резолвит commStdPct/V/дни_на_складе/box-ставки → row.computedStd для каждой строки.
- UI: 3 столбца в таблице + второй блок в модалке (Хранение/Логистика-эфф) + 2 ставки в GlobalRatesBar.
- Спека помечена «Фаза B v1».
- Задеплоено detached, миграция применена на проде.
</success_criteria>

<output>
После завершения создать `.planning/quick/260707-m5v-b-prices-wb/260707-m5v-SUMMARY.md`.
</output>
