---
quick: 260518-hz7
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wb-support-api.ts
  - lib/support-sync.ts
  - app/api/cron/feedbacks-backfill-pros-cons/route.ts
  - tests/format-feedback-body.test.ts
autonomous: true
requirements:
  - QUICK-260518-HZ7

must_haves:
  truths:
    - "Новые WB feedback'и сохраняются в SupportMessage.text с блоками text + Достоинства + Недостатки"
    - "Существующие FEEDBACK тикеты с потерянными pros/cons можно восстановить через one-shot endpoint"
    - "Helper formatFeedbackBody корректно склеивает 1/2/3 части и пустые поля даёт пустую строку"
    - "Существующий sync flow не сломан — все pre-existing тесты проходят"
  artifacts:
    - path: "lib/wb-support-api.ts"
      provides: "Helper formatFeedbackBody(fb) — pure function"
      contains: "export function formatFeedbackBody"
    - path: "lib/support-sync.ts"
      provides: "syncFeedbacks использует formatFeedbackBody для INBOUND message.text"
      contains: "formatFeedbackBody(fb)"
    - path: "app/api/cron/feedbacks-backfill-pros-cons/route.ts"
      provides: "One-shot endpoint POST для backfill исторических feedback с pros/cons"
      exports: ["POST"]
    - path: "tests/format-feedback-body.test.ts"
      provides: "Unit-тесты helper'а formatFeedbackBody"
      contains: "describe(\"formatFeedbackBody\""
  key_links:
    - from: "lib/support-sync.ts"
      to: "lib/wb-support-api.ts"
      via: "import { formatFeedbackBody, ... } from @/lib/wb-support-api"
      pattern: "formatFeedbackBody"
    - from: "app/api/cron/feedbacks-backfill-pros-cons/route.ts"
      to: "lib/wb-support-api.ts"
      via: "listFeedbacks + formatFeedbackBody"
      pattern: "listFeedbacks|formatFeedbackBody"
    - from: "app/api/cron/feedbacks-backfill-pros-cons/route.ts"
      to: "lib/prisma.ts"
      via: "prisma.supportTicket.findUnique + supportMessage.update"
      pattern: "supportMessage\\.update"
---

<objective>
Фикс WB feedbacks sync: при создании SupportMessage из WB Feedback теряются поля `fb.pros` (Достоинства) и `fb.cons` (Недостатки) — в БД пишется только `fb.text`. Ввести единый helper `formatFeedbackBody(fb)`, применить его в `syncFeedbacks`, добавить one-shot backfill для исторических feedback'ов.

Purpose: Восстановить полноту WB отзывов в UI ленте /support (и в /prices/wb expand-панелях) — менеджеры видят достоинства/недостатки, которые покупатель оставил отдельно.

Output:
- `formatFeedbackBody` helper в `lib/wb-support-api.ts`
- `syncFeedbacks` пишет полный текст (text + pros + cons)
- Endpoint `/api/cron/feedbacks-backfill-pros-cons` для одноразового восстановления исторических данных
- Unit-тесты helper'а
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/support-sync.ts
@lib/wb-support-api.ts
@tests/support-sync.test.ts
@app/api/cron/support-sync-reviews/route.ts

<interfaces>
<!-- Контракты для executor'а — не лазить по кодовой базе -->

From `lib/wb-support-api.ts` (lines 74-86):
```typescript
export interface Feedback {
  id: string
  text: string                 // основной комментарий (может быть "")
  pros?: string                // достоинства
  cons?: string                // недостатки
  productValuation: number     // 1..5
  createdDate: string
  state: string                // "wbRu" | "none"
  answer: FeedbackAnswer | null
  productDetails: ProductDetails
  photoLinks: PhotoLink[]
  video: FeedbackVideo | null
}

export interface ListParams {
  isAnswered?: boolean
  take: number
  skip: number
  dateFrom?: number            // Unix seconds
  dateTo?: number
  nmId?: number
}

export async function listFeedbacks(p: ListParams): Promise<Feedback[]>
```

From `lib/support-sync.ts` (lines 235-243) — bug location:
```typescript
const msg = await tx.supportMessage.create({
  data: {
    ticketId: ticket.id,
    direction: "INBOUND",
    text: fb.text,           // ← Bug: теряем fb.pros + fb.cons
    authorId: null,
    wbSentAt: wbCreatedAt,
  },
})
```

Auth pattern для cron endpoints (from `app/api/cron/support-sync-reviews/route.ts`):
```typescript
const secret = req.headers.get("x-cron-secret")
if (!secret || secret !== process.env.CRON_SECRET) {
  return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper formatFeedbackBody + unit-тесты</name>
  <files>lib/wb-support-api.ts, tests/format-feedback-body.test.ts</files>
  <behavior>
    Helper `formatFeedbackBody(fb: { text?: string | null; pros?: string | null; cons?: string | null }): string`:

    - Все три пустые / null / undefined / только whitespace → возвращает `""`
    - Только `text` непустой → возвращает `text` (trimmed)
    - Только `pros` непустой → возвращает `"Достоинства: pros"` (без ведущей пустой строки)
    - Только `cons` непустой → возвращает `"Недостатки: cons"`
    - `text` + `pros` + `cons` → multiline через `"\n\n"` разделитель в порядке: text, Достоинства, Недостатки
    - `pros` + `cons` без text → начинается с `"Достоинства:"` (без ведущего `\n\n`)
    - Whitespace в любой части → trim перед проверкой/конкатенацией

    Тесты в `tests/format-feedback-body.test.ts`:
    ```typescript
    import { describe, it, expect } from "vitest"
    import { formatFeedbackBody } from "@/lib/wb-support-api"

    describe("formatFeedbackBody", () => {
      it("returns empty string when all parts empty", ...)
      it("returns only text when pros/cons absent", ...)
      it("formats only pros block", ...)
      it("formats only cons block", ...)
      it("combines all three parts with double newline separators", ...)
      it("skips leading empty text when pros/cons present", ...)
      it("trims whitespace in each part", ...)
      it("treats null/undefined as empty", ...)
    })
    ```
  </behavior>
  <action>
    1. В `lib/wb-support-api.ts` после блока `export interface Feedback {...}` (после строки 86, до строки 88 `export interface Question`) добавить:

    ```typescript
    /**
     * Склеивает text + Достоинства + Недостатки в единый body для SupportMessage.text.
     * Пустые/null/whitespace части пропускаются. Все пустые → "".
     * Используется при создании INBOUND message из WB Feedback (sync + backfill).
     */
    export function formatFeedbackBody(fb: {
      text?: string | null
      pros?: string | null
      cons?: string | null
    }): string {
      const parts: string[] = []
      const text = fb.text?.trim()
      const pros = fb.pros?.trim()
      const cons = fb.cons?.trim()
      if (text) parts.push(text)
      if (pros) parts.push(`Достоинства: ${pros}`)
      if (cons) parts.push(`Недостатки: ${cons}`)
      return parts.join("\n\n")
    }
    ```

    2. Создать `tests/format-feedback-body.test.ts` с 8 кейсами по списку из `<behavior>`. Использовать vitest (паттерн из других tests/*.test.ts — `import { describe, it, expect } from "vitest"`).

    3. Запустить `npm run test -- format-feedback-body` — все тесты должны проходить (RED → GREEN happens in single step since helper is pure + small).
  </action>
  <verify>
    <automated>npm run test -- format-feedback-body</automated>
  </verify>
  <done>Helper экспортирован из `lib/wb-support-api.ts`. Все 8 unit-тестов проходят. Существующие тесты `wb-support-api.test.ts`, `support-sync.test.ts` не сломаны (запустить `npm run test -- wb-support-api support-sync` для подтверждения).</done>
</task>

<task type="auto">
  <name>Task 2: Применить formatFeedbackBody в syncFeedbacks + self-heal для existing</name>
  <files>lib/support-sync.ts</files>
  <action>
    В `lib/support-sync.ts`:

    1. Добавить `formatFeedbackBody` в существующий импорт-блок (строки 8-20):

    ```typescript
    import {
      listFeedbacks,
      listQuestions,
      listReturns,
      listChats,
      getChatEvents,
      downloadChatAttachment,
      WbRateLimitError,
      formatFeedbackBody,   // ← добавить
      type Feedback,
      type Question,
      type Claim,
      type Chat,
    } from "@/lib/wb-support-api"
    ```

    2. В блоке создания INBOUND message (строки 234-243) заменить `text: fb.text` на использование helper'а. Сохраняем семантику "пустой → NULL в БД":

    ```typescript
    if (!inbound) {
      const formatted = formatFeedbackBody(fb)
      const msg = await tx.supportMessage.create({
        data: {
          ticketId: ticket.id,
          direction: "INBOUND",
          text: formatted || null,
          authorId: null,
          wbSentAt: wbCreatedAt,
        },
      })
      // ... остальной код с photos/video БЕЗ изменений
    }
    ```

    3. Self-heal для existing INBOUND с потерянным контентом. После блока `if (!inbound) {...}` (после закрывающей `}` ~строка 278, ПЕРЕД `if (fb.answer?.text) {...}` на строке 280) добавить ветку обновления:

    ```typescript
    } else {
      // Self-heal: если existing INBOUND text не содержит pros/cons из текущего fb,
      // перегенерируем (idempotent). Защищает от регрессии раньше синканутых feedback'ов
      // без необходимости запускать backfill endpoint.
      const formatted = formatFeedbackBody(fb)
      if (formatted && formatted !== inbound.text) {
        await tx.supportMessage.update({
          where: { id: inbound.id },
          data: { text: formatted },
        })
      }
    }
    ```

    Note: текущий код имеет `if (!inbound) { ...создаём... }` — превращаем в `if/else`. Существующая логика photo/video создания остаётся ВНУТРИ `if (!inbound)` ветки (не дублируется в else — медиа создаётся один раз при первой syncе).

    4. Проверка: `previewText` на строке 205 берёт `(fb.text ?? "").slice(0, 140)` — оставить как есть (это превью для ticket, не для message; полный текст уходит в `supportMessage.text` через helper).

    5. Также проверь secondary INBOUND-write-обработку в `syncReturns` (строки 564-571) и `syncChats` — там используются другие поля (`claim.user_comment` / chat events), pros/cons не релевантно. Не трогать.
  </action>
  <verify>
    <automated>npm run test -- support-sync && npx tsc --noEmit</automated>
  </verify>
  <done>`lib/support-sync.ts` импортирует и использует `formatFeedbackBody` для INBOUND message в Feedbacks-блоке. Self-heal обновляет существующие INBOUND message с устаревшим text. Существующие тесты `tests/support-sync.test.ts` проходят без изменений (или адаптированы — pre-existing моки не проверяют content fb.pros/cons, должно остаться зелёным). Type-check проходит.</done>
</task>

<task type="auto">
  <name>Task 3: One-shot backfill endpoint /api/cron/feedbacks-backfill-pros-cons</name>
  <files>app/api/cron/feedbacks-backfill-pros-cons/route.ts</files>
  <action>
    Создать `app/api/cron/feedbacks-backfill-pros-cons/route.ts` — POST endpoint, защищён `x-cron-secret` по образу `/api/cron/support-sync-reviews/route.ts`. Endpoint можно запускать вручную через `curl -X POST -H "x-cron-secret: $CRON_SECRET" https://zoiten.pro/api/cron/feedbacks-backfill-pros-cons?days=180`.

    Спецификация:

    ```typescript
    // POST /api/cron/feedbacks-backfill-pros-cons — one-shot backfill потерянных pros/cons
    // в исторических FEEDBACK тикетах. Защищён x-cron-secret.
    //
    // Логика:
    //   1. Дёргаем WB Feedbacks API за окно `days` (default 180, query ?days=N)
    //      isAnswered=undefined (без фильтра — и отвеченные, и нет)
    //   2. Для каждого fb с непустым pros||cons:
    //      - находим SupportTicket по channel=FEEDBACK + wbExternalId=fb.id
    //      - находим первый INBOUND SupportMessage (orderBy createdAt asc, take 1)
    //      - формируем новый text через formatFeedbackBody(fb)
    //      - update если newText !== currentText (idempotent skip)
    //   3. Rate-limit-safe: callApi в lib/wb-support-api.ts уже ретраит 429 → пишет cooldown
    //      в AppSetting. Используем существующий listFeedbacks helper.

    import { NextRequest, NextResponse } from "next/server"
    import { prisma } from "@/lib/prisma"
    import { listFeedbacks, formatFeedbackBody, WbRateLimitError } from "@/lib/wb-support-api"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function POST(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }

      const url = new URL(req.url)
      const daysParam = Number.parseInt(url.searchParams.get("days") ?? "180", 10)
      const days = Number.isFinite(daysParam) && daysParam > 0 ? Math.min(daysParam, 365) : 180

      const dateTo = Math.floor(Date.now() / 1000)
      const dateFrom = dateTo - days * 24 * 60 * 60

      let scanned = 0
      let updated = 0
      let skipped = 0
      const errors: string[] = []

      try {
        // Пагинация: take=5000 (max WB), skip += 5000 до empty page.
        for (let skip = 0; ; skip += 5000) {
          let batch: Awaited<ReturnType<typeof listFeedbacks>>
          try {
            batch = await listFeedbacks({ take: 5000, skip, dateFrom, dateTo })
          } catch (err) {
            if (err instanceof WbRateLimitError) {
              errors.push(`WB rate-limit на skip=${skip}: ${err.message}`)
            } else {
              errors.push(`listFeedbacks skip=${skip}: ${err instanceof Error ? err.message : "unknown"}`)
            }
            break
          }
          if (batch.length === 0) break

          for (const fb of batch) {
            scanned++
            const hasPros = !!fb.pros?.trim()
            const hasCons = !!fb.cons?.trim()
            if (!hasPros && !hasCons) {
              skipped++
              continue
            }
            try {
              const ticket = await prisma.supportTicket.findUnique({
                where: { channel_wbExternalId: { channel: "FEEDBACK", wbExternalId: fb.id } },
                select: { id: true },
              })
              if (!ticket) {
                skipped++
                continue
              }
              const inbound = await prisma.supportMessage.findFirst({
                where: { ticketId: ticket.id, direction: "INBOUND" },
                orderBy: { createdAt: "asc" },
                select: { id: true, text: true },
              })
              if (!inbound) {
                skipped++
                continue
              }
              const newText = formatFeedbackBody(fb)
              if (!newText || newText === inbound.text) {
                skipped++
                continue
              }
              await prisma.supportMessage.update({
                where: { id: inbound.id },
                data: { text: newText },
              })
              updated++
            } catch (err) {
              errors.push(`fb ${fb.id}: ${err instanceof Error ? err.message : "unknown"}`)
            }
          }

          if (batch.length < 5000) break
        }
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка backfill"
        return NextResponse.json(
          { ok: false, scanned, updated, skipped, errors: [...errors, msg] },
          { status: 500 }
        )
      }

      return NextResponse.json({ ok: true, scanned, updated, skipped, days, errors })
    }
    ```

    Notes для executor'а:
    - НЕ добавлять endpoint в `/api/cron/dispatch` — это one-shot manual tool.
    - Поле `SupportMessage.createdAt` стандартное Prisma — должно существовать (проверь в `prisma/schema.prisma` если не уверен; если в схеме поле называется иначе — используй то имя).
    - Endpoint idempotent: повторный запуск с теми же params → updated=0, всё в skipped.
    - Если WB вернул rate-limit lock — endpoint вернёт частичный результат и errors[] с описанием. Перезапуск после истечения cooldown продолжит работу (другой skip range подскочит на новых данных, но idempotent skip всё равно работает).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Endpoint существует в `app/api/cron/feedbacks-backfill-pros-cons/route.ts`. Type-check проходит. POST без `x-cron-secret` → 401. POST с правильным secret → JSON `{ ok: true, scanned, updated, skipped, days, errors }`. Endpoint idempotent.</done>
</task>

</tasks>

<verification>
1. `npm run test` — все тесты проходят (новый `format-feedback-body.test.ts` + не сломаны existing `support-sync.test.ts`, `wb-support-api.test.ts`).
2. `npx tsc --noEmit` — type-check без ошибок.
3. Manual review:
   - `lib/wb-support-api.ts` экспортирует `formatFeedbackBody`
   - `lib/support-sync.ts` использует `formatFeedbackBody(fb) || null` вместо `fb.text` в INBOUND create
   - self-heal обновляет existing INBOUND если text устарел
   - endpoint защищён `x-cron-secret`
4. После merge — пользователь запустит `bash deploy.sh` на VPS, затем `curl -X POST -H "x-cron-secret: $CRON_SECRET" https://zoiten.pro/api/cron/feedbacks-backfill-pros-cons?days=365` для backfill.
</verification>

<success_criteria>
- [ ] `formatFeedbackBody` экспортируется из `lib/wb-support-api.ts`
- [ ] 8 unit-тестов в `tests/format-feedback-body.test.ts` проходят
- [ ] `syncFeedbacks` пишет полный text (text + pros + cons) для новых feedback'ов
- [ ] `syncFeedbacks` self-heal обновляет existing INBOUND с устаревшим text
- [ ] Endpoint `/api/cron/feedbacks-backfill-pros-cons` создан, защищён `x-cron-secret`, idempotent
- [ ] Все existing тесты не сломаны
- [ ] Type-check (`npx tsc --noEmit`) проходит
- [ ] Один атомарный commit: `feat(quick-260518-hz7): WB feedbacks sync — pros/cons в SupportMessage + backfill endpoint`
</success_criteria>

<output>
После завершения создать `.planning/quick/260518-hz7-wb-feedbacks-sync-lib-support-sync-ts-su/260518-hz7-SUMMARY.md` с:
- Список изменений (4 файла: 2 modified + 2 created)
- Commit hash
- Команда для deploy + backfill на VPS:
  ```
  ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
  ssh root@85.198.97.89 "source /etc/zoiten.pro.env && curl -sS -X POST -H \"x-cron-secret: \$CRON_SECRET\" 'https://zoiten.pro/api/cron/feedbacks-backfill-pros-cons?days=365'"
  ```
- Метрики из backfill response (scanned/updated/skipped) после исполнения
</output>
