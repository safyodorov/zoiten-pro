---
phase: 260515-jq6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/cards/layout.tsx
  - components/cards/WbCardsTable.tsx
autonomous: true
---

<objective>
3 UX-улучшения в таблице /cards/wb (компонент `components/cards/WbCardsTable.tsx`):

1. **Sticky header** — шапка таблицы при scroll'е остаётся наверху (паттерн CLAUDE.md «Sticky data-таблицы»).
2. **Tooltip на имени карточки** — at hover на name показывает полное название (когда truncate режет длинные).
3. **Click-to-copy артикул** — клик по ячейке с nmId копирует его в clipboard.

Паттерны переиспользуются из quick 260513-phu (применены в `/prices/wb`, `/stock`, `/stock/wb`).
</objective>

<context>
@./CLAUDE.md
@components/cards/WbCardsTable.tsx
@components/prices/PriceCalculatorTable.tsx
@app/(dashboard)/cards/layout.tsx

<existing_patterns>
- `lib/copy-to-clipboard.ts` — pure helper `copyToClipboard(text, label?)` с toast.
- `components/ui/tooltip.tsx` — `<Tooltip>` + `<TooltipTrigger render={<X />}>` + `<TooltipContent>` (base-ui).
- Пример Tooltip+truncate: `components/prices/PriceCalculatorTable.tsx:797-810` (Сводная row name cell).
- Пример sticky thead: `components/stock/StockProductTable.tsx:178-180` — `<div overflow-auto h-full>` + `<thead bg-background>` + `<TableHead sticky top-0 z-20 bg-background border-b>`.
- Пример copy-to-clipboard cell: `components/prices/PriceCalculatorTable.tsx:919-940` — `onClick stopPropagation + copyToClipboard + cursor-copy + hover:text-primary`.
- Flex layout chain: `app/(dashboard)/stock/layout.tsx` — `<div h-full flex flex-col p-6 gap-4>` → `<div flex-1 min-h-0 flex flex-col gap-4>` для children.
</existing_patterns>
</context>

<tasks>

<task type="auto">
<name>Task 1: layout + page — flex chain для sticky header</name>
<files>
  app/(dashboard)/cards/layout.tsx
</files>
<action>
**Цель:** layout chain `h-full flex flex-col` → `flex-1 min-h-0` чтобы таблица в child page'е могла иметь свой `overflow-auto` контейнер с правильной высотой.

`/cards/layout.tsx`: заменить `<div className="space-y-4">` на `<div className="h-full flex flex-col gap-4">` (без `p-6` — DashboardShell main уже даёт padding). Children wrap-up:
```tsx
<div className="h-full flex flex-col gap-4">
  <CardsTabs />
  <div className="flex-1 min-h-0 flex flex-col gap-4">
    {children}
  </div>
</div>
```

NOTE: страницы `/cards/wb`, `/cards/ozon` обе используют `<div className="space-y-4">` верхнего уровня — это норма, ёспаce-y-4 не блокирует flex от родителя. НО для /cards/wb нужен flex chain до таблицы. В Task 2 поменяю верхний `<div>` в page.tsx и WbCardsTable.

`page.tsx` сейчас: `<div className="space-y-4">` → меняем на `<div className="h-full flex flex-col gap-4">`. Внутри есть header (search + buttons), WbFilters, WbCardsTable — последний должен быть `flex-1 min-h-0`. Передадим класс `flex-1 min-h-0` в WbCardsTable через root div.
</action>
<verify>
  <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(cards/wb|layout)" | head -5; echo "ok if empty"</automated>
</verify>
<done>Layout chain работает: при длинной таблице main НЕ scroll'ится, scroll'ится table area.</done>
</task>

<task type="auto">
<name>Task 2: WbCardsTable — sticky header + tooltip name + click-to-copy nmId</name>
<files>
  components/cards/WbCardsTable.tsx
</files>
<action>
**Цель:** 3 изменения в одном файле, минимальный diff.

### 2.1. Imports
Добавить:
```typescript
import { Tooltip, TooltipTrigger, TooltipContent } from "@/components/ui/tooltip"
import { copyToClipboard } from "@/lib/copy-to-clipboard"
```

### 2.2. Sticky header (паттерн CLAUDE.md «Sticky data-таблицы»)

Root div компонента сейчас: `<div className="space-y-3">`. Меняем на `<div className="h-full flex flex-col gap-3">`.

Внутри:
- Панель действий, Pagination — `shrink-0`
- Таблица wrapper — `flex-1 min-h-0`

Table wrapper currently:
```tsx
<div className="rounded-md border">
  <Table>
    <TableHeader>
      <TableRow>
        <TableHead>...</TableHead>
```

Меняем на:
```tsx
<div className="flex-1 min-h-0 rounded-md border overflow-auto">
  <table className="w-full border-separate border-spacing-0">
    <thead className="bg-background">
      <tr>
        <TableHead className="sticky top-0 z-20 bg-background border-b ...">
```

**ВСЕ** `<TableHead>` в шапке получают prefix классов `sticky top-0 z-20 bg-background border-b` (добавить к существующим классам — некоторые имеют `text-center text-xs border-l` уже).

**Body** остаётся как было: `<TableBody>` + `<TableRow>` (там hover OK per CLAUDE.md).

**Внешний `<Table>`** заменяем на native `<table>` (всё ещё работает потому что TableBody/Row рендерят `<tbody>/<tr>`).

После таблицы — `</table></div>` вместо `</Table></div>`.

### 2.3. Tooltip с полным именем

Текущая строка 366:
```tsx
<TableCell className="font-medium max-w-[160px] truncate">{card.name}</TableCell>
```

Меняем на:
```tsx
<TableCell className="font-medium">
  <Tooltip>
    <TooltipTrigger render={<div className="max-w-[160px] truncate cursor-default" />}>
      {card.name}
    </TooltipTrigger>
    <TooltipContent>
      <div className="max-w-sm text-sm">{card.name}</div>
    </TooltipContent>
  </Tooltip>
</TableCell>
```

(паттерн PriceCalculatorTable.tsx:797-810)

### 2.4. Click-to-copy nmId

Текущая строка 367:
```tsx
<TableCell className="font-mono text-xs">{card.nmId}</TableCell>
```

Меняем на:
```tsx
<TableCell
  className="font-mono text-xs cursor-copy hover:text-primary"
  onClick={(e) => {
    e.stopPropagation()
    void copyToClipboard(String(card.nmId), "Артикул")
  }}
  title="Нажмите, чтобы скопировать артикул"
>
  {card.nmId}
</TableCell>
```

(паттерн PriceCalculatorTable.tsx:919-940)
</action>
<verify>
  <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(WbCardsTable|cards/wb)" | head -10; echo "ok if empty"</automated>
</verify>
<done>
- Sticky header работает (scroll body, шапка остаётся)
- Hover на name показывает full name (если truncated)
- Click на nmId копирует в буфер + toast «Артикул XXXXXX скопирован»
- Никаких новых tsc errors
</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` — clean.
2. После deploy на прод: открыть /cards/wb, скролить таблицу — шапка фиксирована.
3. Hover на длинном name — tooltip показывает полное.
4. Клик на nmId — toast и в clipboard.
</verification>

<output>
SUMMARY с тестовыми checkmark'ами по 3 фичам + deploy command для prod (ssh ... deploy.sh).
</output>
