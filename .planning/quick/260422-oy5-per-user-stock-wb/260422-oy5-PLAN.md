---
phase: 260422-oy5-per-user-stock-wb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql
  - app/actions/stock-wb.ts
  - components/stock/WarehouseVisibilityPopover.tsx
  - components/stock/StockWbTable.tsx
  - app/(dashboard)/stock/wb/page.tsx
autonomous: true
requirements:
  - QUICK-260422-oy5
must_haves:
  truths:
    - "Пользователь видит рядом с «Без СЦ» / «Развернуть все» новую кнопку «Склады», при клике открывается попап со списком всех WB-складов сгруппированных по кластерам (ЦФО, ЮГ, Урал, ПФО, СЗО, СФО, Прочие) с чекбоксами"
    - "По умолчанию все чекбоксы отмечены (массив User.stockWbHiddenWarehouses пуст); снятие чекбокса скрывает склад из expanded view, отметка обратно — показывает"
    - "При snятии/отметке чекбокса изменение видно немедленно (optimistic), и после перезагрузки страницы (или повторного логина) сохраняется — значит пишется в БД per-user"
    - "Скрытые склады НЕ отображаются в expanded-колонках кластера, НО их остатки, заказы и все cluster-level агрегаты (totalStock, ordersPerDay per-кластер, row-level Сводная) считаются как сейчас (пример: если скрыть все склады ЦФО, в collapsed view ЦФО цифры не меняются)"
    - "Оба фильтра совместимы: склад показан только если !isSortingCenter(name) (при hideSc=true) && !hiddenByUser — снятие галки перекрывает кнопку «Все склады» для выбранного склада"
    - "Кнопка «Сбросить» в попапе очищает массив (показать все склады) и persists в БД"
    - "Server action защищён requireSection('STOCK') — VIEWER может менять свою настройку (это не admin-операция)"
    - "Настройка строго per-user: изменение у одного пользователя не влияет на отображение у другого"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Поле User.stockWbHiddenWarehouses Int[] @default([])"
      contains: "stockWbHiddenWarehouses"
    - path: "prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql"
      provides: "ALTER TABLE User ADD COLUMN stockWbHiddenWarehouses INT[] DEFAULT '{}'"
    - path: "app/actions/stock-wb.ts"
      provides: "saveStockWbHiddenWarehouses(ids: number[])"
      exports: ["saveStockWbHiddenWarehouses"]
    - path: "components/stock/WarehouseVisibilityPopover.tsx"
      provides: "Клиентский компонент — popup со списком складов per-cluster + checkboxes + кнопка Сбросить + optimistic save"
    - path: "components/stock/StockWbTable.tsx"
      provides: "Добавлен prop hiddenWarehouseIds: number[], используется в visibleClusterWarehouses"
    - path: "app/(dashboard)/stock/wb/page.tsx"
      provides: "Читает session.user.id → User.stockWbHiddenWarehouses → передаёт в StockWbTable"
  key_links:
    - from: "components/stock/WarehouseVisibilityPopover.tsx"
      to: "app/actions/stock-wb.ts"
      via: "server action saveStockWbHiddenWarehouses"
      pattern: "saveStockWbHiddenWarehouses\\("
    - from: "app/(dashboard)/stock/wb/page.tsx"
      to: "components/stock/StockWbTable.tsx"
      via: "prop hiddenWarehouseIds"
      pattern: "hiddenWarehouseIds="
    - from: "components/stock/StockWbTable.tsx"
      to: "visibleClusterWarehouses filter"
      via: "filter not in hiddenSet"
      pattern: "hiddenSet\\.has|hiddenWarehouseIds"
---

<objective>
Добавить на странице `/stock/wb` per-user фильтр складов: попап со списком всех WB-складов (сгруппированных по кластерам) с чекбоксами. Скрытые склады не показываются в expanded view, но продолжают участвовать во всех расчётах. Настройка персистится в БД per-user.

Purpose: Разные менеджеры следят за разными регионами — пустые/чужие склады в expanded-view мешают. Фильтр — чисто визуальный и per-user (не влияет на коллег).

Output: миграция `User.stockWbHiddenWarehouses Int[]`, server action `saveStockWbHiddenWarehouses`, новый компонент `WarehouseVisibilityPopover`, интеграция в `StockWbTable` + RSC page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@prisma/schema.prisma
@lib/stock-wb-data.ts
@app/(dashboard)/stock/wb/page.tsx
@components/stock/StockWbTable.tsx
@components/ui/multi-select-dropdown.tsx
@lib/wb-clusters.ts
@lib/auth.ts
@app/actions/stock.ts

<interfaces>
<!-- Ключевые контракты — executor'у НЕ нужно шариться по коду для этого. -->

Из `lib/wb-clusters.ts`:
```typescript
export const CLUSTER_ORDER = ["ЦФО", "ЮГ", "Урал", "ПФО", "СЗО", "СФО", "Прочие"] as const
export type ClusterShortName = typeof CLUSTER_ORDER[number]
export const CLUSTER_FULL_NAMES: Record<string, string>  // для tooltip/подписи кластера
```

Из `lib/stock-wb-data.ts` (только уже используемые в page.tsx типы):
```typescript
export interface StockWbDataResult {
  groups: ProductWbGroup[]
  turnoverNormDays: number
  clusterWarehouses: Record<ClusterShortName, Array<{
    warehouseId: number
    warehouseName: string
    needsClusterReview: boolean
  }>>
}
```
`clusterWarehouses` — это готовый список ВСЕХ складов per-кластер (включая СЦ). Именно он нужен для отрисовки списка в попапе.

Из `lib/auth.ts`:
```typescript
// session.user.id доступен через auth() на сервере
import { auth } from "@/lib/auth"
const session = await auth()
const userId = session?.user?.id
```

Текущий StockWbTable:
```typescript
interface Props {
  groups: ProductWbGroup[]
  turnoverNormDays: number
  clusterWarehouses: StockWbDataResult["clusterWarehouses"]
}
```
Внутри компонента сейчас есть `visibleClusterWarehouses` — фильтрует ТОЛЬКО по `isSortingCenter` + `hideSc`. Надо добавить ещё один фильтр: `!hiddenSet.has(w.warehouseId)`.

Паттерн popover без библиотеки (из `components/ui/multi-select-dropdown.tsx`):
```typescript
const ref = useRef<HTMLDivElement>(null)
useEffect(() => {
  function handleClick(e: MouseEvent) {
    if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false)
  }
  document.addEventListener("mousedown", handleClick)
  return () => document.removeEventListener("mousedown", handleClick)
}, [])
```
В проекте НЕТ `components/ui/popover.tsx` — использовать такой же inline-паттерн (нативный div + click-outside), НЕ тянуть новый shadcn-компонент.

Паттерн server action (из `app/actions/stock.ts`):
```typescript
"use server"
import { requireSection } from "@/lib/rbac"
import { revalidatePath } from "next/cache"
await requireSection("STOCK")  // для user preference — достаточно VIEW (D-09 из constraints)
await prisma.user.update({ where: { id: session.user.id }, data: { ... } })
revalidatePath("/stock/wb")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema + migration + server action</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql,
    app/actions/stock-wb.ts
  </files>
  <action>
    Шаг 1 — Prisma schema. В `prisma/schema.prisma` в модели `User` добавить поле **непосредственно перед** `preferences UserPreference[]` (рядом с другими persistence полями UI):

    ```prisma
    // Per-user список скрытых WB-складов на странице /stock/wb (quick 260422-oy5).
    // Чисто визуальный фильтр — НЕ влияет на агрегаты.
    stockWbHiddenWarehouses Int[] @default([])
    ```

    Шаг 2 — Миграция. Создать папку `prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/` и файл `migration.sql` (локальной PG нет, миграция применится через `deploy.sh` на VPS, это установленный паттерн Phase 14/9). Содержимое:

    ```sql
    -- Per-user hidden WB warehouses on /stock/wb (quick 260422-oy5)
    ALTER TABLE "User" ADD COLUMN "stockWbHiddenWarehouses" INTEGER[] NOT NULL DEFAULT '{}';
    ```

    Использовать именно `"User"` с заглавной U и двойными кавычками — такой же pattern, как в `20260417_phase9_returns/migration.sql`.

    НЕ запускать `npx prisma migrate dev` (нет локальной PG). Только создать SQL вручную + `npx prisma generate` для обновления клиентских типов. Если `prisma generate` падает — это нормально для Windows без БД; проверить что в `node_modules/.prisma/client/index.d.ts` появилось поле `stockWbHiddenWarehouses`.

    Шаг 3 — Server action `app/actions/stock-wb.ts` (новый файл). Паттерн `app/actions/stock.ts`:

    ```typescript
    // app/actions/stock-wb.ts
    // Quick 260422-oy5: per-user фильтр видимости WB-складов на /stock/wb.
    "use server"

    import { z } from "zod"
    import { prisma } from "@/lib/prisma"
    import { auth } from "@/lib/auth"
    import { requireSection } from "@/lib/rbac"
    import { revalidatePath } from "next/cache"

    const InputSchema = z.object({
      ids: z.array(z.number().int()).max(500),  // разумный верх для защиты от мусора
    })

    export type SaveStockWbHiddenWarehousesResult =
      | { ok: true }
      | { ok: false; error: string }

    /**
     * Сохраняет массив warehouseId скрытых WB-складов для текущего пользователя.
     * RBAC: STOCK VIEW — user менять СВОЮ preference имеет право (не admin).
     * Дедупликация + сортировка для стабильности.
     */
    export async function saveStockWbHiddenWarehouses(
      ids: number[],
    ): Promise<SaveStockWbHiddenWarehousesResult> {
      await requireSection("STOCK")  // D-09: user preference, не MANAGE

      const session = await auth()
      const userId = session?.user?.id
      if (!userId) return { ok: false, error: "Не авторизован" }

      const parsed = InputSchema.safeParse({ ids })
      if (!parsed.success) return { ok: false, error: "Некорректные данные" }

      // dedupe + sort (stable)
      const clean = [...new Set(parsed.data.ids)].sort((a, b) => a - b)

      try {
        await prisma.user.update({
          where: { id: userId },
          data: { stockWbHiddenWarehouses: clean },
        })
      } catch (e) {
        return { ok: false, error: e instanceof Error ? e.message : "DB error" }
      }

      revalidatePath("/stock/wb")
      return { ok: true }
    }
    ```

    ВАЖНО:
    - Имя файла `stock-wb.ts` (не `stock.ts`) — чтобы не мешать существующему файлу и не вызвать конфликт/regression.
    - `requireSection("STOCK")` БЕЗ `"MANAGE"` — это user preference (пользователь меняет СВОЮ настройку), VIEWER имеет право (D-09 из constraints).
    - Zod ограничивает `ids.length <= 500` — защита от мусора.
    - Dedupe через `new Set` + sort — массивы Int[] в PG хранятся ordered, стабильность UI.

    Коммит не делать — оставить для task 3 (один общий коммит по quick-задаче).
  </action>
  <verify>
    <automated>cd C:/Claude/zoiten-pro && grep -n "stockWbHiddenWarehouses" prisma/schema.prisma && cat prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql && grep -n "saveStockWbHiddenWarehouses\|requireSection" app/actions/stock-wb.ts && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `prisma/schema.prisma`: поле `stockWbHiddenWarehouses Int[] @default([])` в модели User
    - `prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql` существует с ALTER TABLE
    - `app/actions/stock-wb.ts`: экспортирует `saveStockWbHiddenWarehouses`, защищён `requireSection("STOCK")` (без MANAGE), zod-валидация, revalidatePath("/stock/wb")
    - `npx tsc --noEmit` проходит без ошибок в новых файлах (тип `stockWbHiddenWarehouses` доступен в Prisma client)
  </done>
</task>

<task type="auto">
  <name>Task 2: Компонент WarehouseVisibilityPopover (клиент)</name>
  <files>components/stock/WarehouseVisibilityPopover.tsx</files>
  <action>
    Создать `components/stock/WarehouseVisibilityPopover.tsx` — клиентский попап с чекбоксами складов, сгруппированных по кластерам.

    Структура:

    ```typescript
    "use client"

    // components/stock/WarehouseVisibilityPopover.tsx
    // Quick 260422-oy5: попап с чекбоксами видимости WB-складов per-cluster.
    // Optimistic update + save в БД через server action.

    import { useEffect, useRef, useState, useTransition } from "react"
    import { ChevronDown } from "lucide-react"
    import { Button } from "@/components/ui/button"
    import { Checkbox } from "@/components/ui/checkbox"
    import { CLUSTER_ORDER, type ClusterShortName } from "@/lib/wb-clusters"
    import { saveStockWbHiddenWarehouses } from "@/app/actions/stock-wb"
    import type { StockWbDataResult } from "@/lib/stock-wb-data"

    interface Props {
      clusterWarehouses: StockWbDataResult["clusterWarehouses"]
      hiddenIds: number[]
      onChange: (next: number[]) => void  // optimistic update наружу (StockWbTable)
    }

    export function WarehouseVisibilityPopover({
      clusterWarehouses,
      hiddenIds,
      onChange,
    }: Props) {
      const [open, setOpen] = useState(false)
      const [isPending, startTransition] = useTransition()
      const ref = useRef<HTMLDivElement>(null)

      // click-outside (паттерн MultiSelectDropdown)
      useEffect(() => {
        function handleClick(e: MouseEvent) {
          if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false)
        }
        document.addEventListener("mousedown", handleClick)
        return () => document.removeEventListener("mousedown", handleClick)
      }, [])

      const hiddenSet = new Set(hiddenIds)

      const totalCount = CLUSTER_ORDER.reduce(
        (acc, c) => acc + (clusterWarehouses[c]?.length ?? 0),
        0
      )
      const visibleCount = totalCount - hiddenSet.size

      function persist(nextIds: number[]) {
        // Уже отсортированный dedup сделает server; optimistic обновление мгновенно
        onChange(nextIds)
        startTransition(async () => {
          const res = await saveStockWbHiddenWarehouses(nextIds)
          if (!res.ok) {
            // Откат: сигналим наружу исходное состояние
            // (page.tsx при следующем revalidate синхронизирует из БД)
            console.error("Не удалось сохранить настройку складов:", res.error)
          }
        })
      }

      function toggle(warehouseId: number, visible: boolean) {
        // visible=true → показать (убрать из hidden); false → скрыть (добавить)
        const next = new Set(hiddenSet)
        if (visible) next.delete(warehouseId)
        else next.add(warehouseId)
        persist([...next])
      }

      function reset() {
        persist([])
      }

      function toggleCluster(cluster: ClusterShortName, showAll: boolean) {
        const ids = (clusterWarehouses[cluster] ?? []).map((w) => w.warehouseId)
        const next = new Set(hiddenSet)
        if (showAll) ids.forEach((id) => next.delete(id))
        else ids.forEach((id) => next.add(id))
        persist([...next])
      }

      const label = hiddenSet.size > 0
        ? `Склады (${visibleCount}/${totalCount})`
        : "Склады"

      return (
        <div ref={ref} className="relative">
          <Button
            variant={hiddenSet.size > 0 ? "default" : "outline"}
            size="sm"
            onClick={() => setOpen(!open)}
            className="gap-1.5"
            title="Выбрать какие склады отображать в expanded view (только визуально, на расчёты не влияет)"
          >
            {label}
            <ChevronDown className={`h-3.5 w-3.5 transition-transform ${open ? "rotate-180" : ""}`} />
          </Button>
          {open && (
            <div className="absolute top-full left-0 mt-1 z-50 min-w-[300px] max-w-[400px] max-h-[60vh] overflow-y-auto rounded-md border bg-popover p-2 shadow-md">
              <div className="flex items-center justify-between gap-2 mb-2 pb-2 border-b">
                <span className="text-xs text-muted-foreground">
                  Фильтр только визуальный — все расчёты считаются по всем складам.
                </span>
                <Button variant="ghost" size="sm" onClick={reset} disabled={hiddenSet.size === 0 || isPending}>
                  Сбросить
                </Button>
              </div>

              {CLUSTER_ORDER.map((cluster) => {
                const warehouses = clusterWarehouses[cluster] ?? []
                if (warehouses.length === 0) return null
                const clusterIds = warehouses.map((w) => w.warehouseId)
                const clusterHiddenCount = clusterIds.filter((id) => hiddenSet.has(id)).length
                const allClusterHidden = clusterHiddenCount === clusterIds.length
                const allClusterVisible = clusterHiddenCount === 0

                return (
                  <div key={cluster} className="mb-2 last:mb-0">
                    <div className="flex items-center justify-between gap-2 px-1 py-1 text-xs font-semibold">
                      <span>{cluster} <span className="text-muted-foreground font-normal">({warehouses.length - clusterHiddenCount}/{warehouses.length})</span></span>
                      <button
                        type="button"
                        className="text-xs text-muted-foreground hover:text-foreground underline-offset-2 hover:underline"
                        onClick={() => toggleCluster(cluster, !allClusterVisible)}
                      >
                        {allClusterVisible ? "Скрыть все" : "Показать все"}
                      </button>
                    </div>
                    <div className="flex flex-col">
                      {warehouses.map((w) => {
                        const visible = !hiddenSet.has(w.warehouseId)
                        return (
                          <label
                            key={w.warehouseId}
                            className="flex items-center gap-2 px-2 py-1 rounded hover:bg-muted cursor-pointer text-sm"
                          >
                            <Checkbox
                              checked={visible}
                              onCheckedChange={(checked) => toggle(w.warehouseId, checked === true)}
                            />
                            <span className="truncate">
                              {w.needsClusterReview && <span className="mr-1">⚠️</span>}
                              {w.warehouseName}
                            </span>
                          </label>
                        )
                      })}
                    </div>
                  </div>
                )
              })}
            </div>
          )}
        </div>
      )
    }
    ```

    Ключевые детали:
    - Семантика чекбокса: **отмечен = видим, снят = скрыт** (интуитивно). В БД храним ID скрытых (массив пустой = все видимы) — это удобно: default `[]` означает "всё видно" без миграции существующих пользователей.
    - `variant={hiddenSet.size > 0 ? "default" : "outline"}` — такой же паттерн как у `hideSc` кнопки «Без СЦ» в StockWbTable (визуально подчёркивает, что фильтр активен).
    - Label `"Склады (12/20)"` когда есть скрытые — быстрая обратная связь.
    - `onChange` prop — optimistic update локального state в родителе (StockWbTable). Server action асинхронный, UI обновляется мгновенно.
    - Group toggle "Скрыть все" / "Показать все" per-cluster — удобство для «скрыть весь ЦФО одним кликом».
    - НЕ использовать shadcn Popover (в проекте его нет) — inline div + click-outside как в `MultiSelectDropdown`.

    Не добавлять тосты/спиннеры (keep it simple, паттерн проекта для silent-save — см. TurnoverNormInput из Phase 14).
  </action>
  <verify>
    <automated>cd C:/Claude/zoiten-pro && grep -n "saveStockWbHiddenWarehouses\|CLUSTER_ORDER\|useTransition" components/stock/WarehouseVisibilityPopover.tsx && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Файл существует, экспортирует `WarehouseVisibilityPopover`
    - Импортирует `saveStockWbHiddenWarehouses` из `@/app/actions/stock-wb` и `CLUSTER_ORDER` из `@/lib/wb-clusters`
    - Использует click-outside через ref (паттерн MultiSelectDropdown)
    - Семантика: отмечен=видим, снят=скрыт; кнопка «Сбросить» очищает hiddenIds
    - `npx tsc --noEmit` проходит без ошибок
  </done>
</task>

<task type="auto">
  <name>Task 3: Интеграция в StockWbTable + RSC page + коммит</name>
  <files>
    components/stock/StockWbTable.tsx,
    app/(dashboard)/stock/wb/page.tsx
  </files>
  <action>
    Шаг 1 — `app/(dashboard)/stock/wb/page.tsx`: читать `User.stockWbHiddenWarehouses` из сессии и передать в таблицу.

    Изменить существующий файл:

    ```typescript
    // app/(dashboard)/stock/wb/page.tsx
    import { requireSection } from "@/lib/rbac"
    import { auth } from "@/lib/auth"
    import { prisma } from "@/lib/prisma"
    import { getStockWbData } from "@/lib/stock-wb-data"
    import { StockWbTable } from "@/components/stock/StockWbTable"

    export const metadata = { title: "Управление остатками — WB склады" }

    export default async function StockWbPage() {
      await requireSection("STOCK")
      const [data, session] = await Promise.all([getStockWbData(), auth()])

      // Quick 260422-oy5: per-user фильтр скрытых WB-складов
      let hiddenWarehouseIds: number[] = []
      if (session?.user?.id) {
        const user = await prisma.user.findUnique({
          where: { id: session.user.id },
          select: { stockWbHiddenWarehouses: true },
        })
        hiddenWarehouseIds = user?.stockWbHiddenWarehouses ?? []
      }

      if (data.groups.length === 0) {
        return (/* ...existing empty state... */)
      }

      return (
        <div className="flex-1 min-h-0">
          <StockWbTable
            groups={data.groups}
            turnoverNormDays={data.turnoverNormDays}
            clusterWarehouses={data.clusterWarehouses}
            hiddenWarehouseIds={hiddenWarehouseIds}
          />
        </div>
      )
    }
    ```

    Сохранить существующий empty-state JSX без изменений. Только добавить auth/prisma fetch и новый prop.

    Шаг 2 — `components/stock/StockWbTable.tsx`: добавить prop + локальный state + фильтр в `visibleClusterWarehouses` + рендер попапа.

    **Добавить импорт** в начале файла:
    ```typescript
    import { useState } from "react"  // useState УЖЕ через React, проверь — если нужно добавь явно
    import { WarehouseVisibilityPopover } from "./WarehouseVisibilityPopover"
    ```
    (В текущем файле `import React, { useCallback } from "react"` — добавить `useState` в именованный импорт.)

    **Расширить Props**:
    ```typescript
    interface Props {
      groups: ProductWbGroup[]
      turnoverNormDays: number
      clusterWarehouses: StockWbDataResult["clusterWarehouses"]
      hiddenWarehouseIds: number[]  // новый prop, приходит из БД через RSC
    }
    ```

    **Внутри компонента** (после деструктуризации props) добавить optimistic local state:
    ```typescript
    // Optimistic локальный state для мгновенной реакции на toggle;
    // RSC revalidatePath синхронизирует с БД при следующем render.
    const [hiddenIds, setHiddenIds] = useState<number[]>(hiddenWarehouseIds)
    const hiddenSet = new Set(hiddenIds)
    ```
    Важно: при изменении `hiddenWarehouseIds` из props (revalidate после server action) state будет продолжать держать старое значение — для quick-задачи это ОК, т.к. optimistic state совпадает (persist отправляет идентичный массив). Не усложняем useEffect синхронизацией.

    **Расширить `visibleClusterWarehouses`** — добавить второй фильтр:
    ```typescript
    const visibleClusterWarehouses: typeof clusterWarehouses = Object.fromEntries(
      CLUSTER_ORDER.map((c) => [
        c,
        (clusterWarehouses[c] ?? []).filter((w) => {
          // Фильтр 1: СЦ по кнопке hideSc
          if (hideSc && isSortingCenter(w.warehouseName)) return false
          // Фильтр 2: per-user hidden (quick 260422-oy5)
          if (hiddenSet.has(w.warehouseId)) return false
          return true
        }),
      ])
    ) as typeof clusterWarehouses
    ```

    **В toolbar** (`<div className="flex gap-2 items-center shrink-0">`) добавить попап рядом с «Без СЦ»:
    ```tsx
    <div className="flex gap-2 items-center shrink-0">
      <Button variant="ghost" size="sm" onClick={expandAll}>Развернуть все</Button>
      <Button variant="ghost" size="sm" onClick={collapseAll}>Свернуть все</Button>
      <span className="text-muted-foreground mx-2">·</span>
      <Button variant={hideSc ? "default" : "outline"} size="sm" onClick={toggleHideSc} title={/* existing */}>
        {hideSc ? "Без СЦ" : "Все склады"}
      </Button>
      <WarehouseVisibilityPopover
        clusterWarehouses={clusterWarehouses}
        hiddenIds={hiddenIds}
        onChange={setHiddenIds}
      />
    </div>
    ```

    КРИТИЧЕСКИ ВАЖНО — проверки семантики при тесте:
    - Cluster aggregates (строка «Сводная», collapsed-кластерные колонки О/З/Об/Д) НЕ зависят от `visibleClusterWarehouses` — они считаются в `lib/stock-wb-data.ts` по всем складам. Двойная проверка: в текущем `StockWbTable.tsx` `rowClusterAgg` берёт данные из `card.clusters[cluster].totalStock / ordersPerDay` (это pre-computed агрегаты в data-helper'е) — НЕ трогать эту логику. В data-helper изменения НЕ делаем.
    - `allClusterHidden` в попапе → в expanded view кластер будет показывать «нет складов» placeholder (это уже обработано в текущем коде через `warehouses.length === 0` ветку). НЕ надо добавлять специальную логику для этого случая.
    - `hideSc=true` + `hiddenByUser=true` для одного склада → просто скрыт (OR-комбинация filter-а, что и нужно).

    Шаг 3 — Коммит. Сделать один атомарный коммит по всему quick-заданию:
    ```bash
    cd C:/Claude/zoiten-pro && git add prisma/schema.prisma prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses app/actions/stock-wb.ts components/stock/WarehouseVisibilityPopover.tsx components/stock/StockWbTable.tsx app/(dashboard)/stock/wb/page.tsx
    git commit -m "feat(stock-wb): per-user фильтр видимости складов на /stock/wb (260422-oy5)

    - Добавлено поле User.stockWbHiddenWarehouses Int[] @default([]) (миграция применится через deploy.sh)
    - Новый server action saveStockWbHiddenWarehouses (STOCK VIEW — user preference)
    - Новый компонент WarehouseVisibilityPopover с чекбоксами по кластерам + optimistic save
    - Интеграция в StockWbTable: второй фильтр visibleClusterWarehouses рядом с hideSc
    - Фильтр ТОЛЬКО визуальный: скрытые склады продолжают участвовать во всех агрегатах (per-cluster, Сводная)

    🤖 Generated with [Claude Code](https://claude.com/claude-code)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    НЕ пушить (это quick, пользователь сам пушит). НЕ запускать deploy.sh.
  </action>
  <verify>
    <automated>cd C:/Claude/zoiten-pro && grep -n "hiddenWarehouseIds\|stockWbHiddenWarehouses" app/\(dashboard\)/stock/wb/page.tsx && grep -n "WarehouseVisibilityPopover\|hiddenWarehouseIds\|hiddenIds\|hiddenSet" components/stock/StockWbTable.tsx && npx tsc --noEmit 2>&1 | head -50 && npx next lint 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `app/(dashboard)/stock/wb/page.tsx` читает `User.stockWbHiddenWarehouses` через `auth()` + prisma и передаёт в `<StockWbTable>` как `hiddenWarehouseIds`
    - `components/stock/StockWbTable.tsx`:
      - добавлен prop `hiddenWarehouseIds: number[]`
      - добавлен `useState<number[]>(hiddenWarehouseIds)` для optimistic
      - `visibleClusterWarehouses` фильтрует И по `isSortingCenter(hideSc)`, И по `hiddenSet.has(warehouseId)` (совместимы)
      - `<WarehouseVisibilityPopover>` отрендерен в toolbar рядом с кнопкой «Без СЦ»
    - Структура per-cluster aggregates (`rowClusterAgg`, `card.clusters[cluster]`) НЕ тронута — фильтр только визуальный
    - `npx tsc --noEmit` без ошибок; `npx next lint` без новых warnings
    - Git commit создан одним атомарным коммитом по всем 6 файлам
  </done>
</task>

</tasks>

<verification>
1. **Prisma**: `grep -n "stockWbHiddenWarehouses" prisma/schema.prisma` → 1 строка; migration.sql содержит `ALTER TABLE "User"`.
2. **Server action**: `grep -n "saveStockWbHiddenWarehouses" app/actions/stock-wb.ts` → export существует; `requireSection("STOCK")` без MANAGE.
3. **Component**: `components/stock/WarehouseVisibilityPopover.tsx` экспортирует `WarehouseVisibilityPopover`, использует click-outside, optimistic через `onChange`.
4. **Integration**:
   - page.tsx читает `User.stockWbHiddenWarehouses` и передаёт в `<StockWbTable>`
   - StockWbTable: `visibleClusterWarehouses` фильтрует по обоим фильтрам (`hideSc` + `hiddenSet`)
   - попап отрендерен в toolbar
5. **TypeScript**: `npx tsc --noEmit` чистый (важно для Rule 3 — production build pass).
6. **Lint**: `npx next lint` без новых ошибок.
7. **Коммит**: `git log -1 --stat` показывает ровно 6 файлов.

**Инварианты расчётов (ручной smoke после деплоя):**
- Открыть `/stock/wb`, развернуть кластер ЦФО — видны все склады.
- Скрыть 1-2 склада через попап → исчезают из expanded.
- Collapsed-кластерные колонки О/З/Об/Д **не меняют значения** (это самое главное — проверить до и после).
- Строка «Сводная» — значения не меняются.
- Перезагрузить страницу → скрытые склады остаются скрытыми.
- Открыть `/stock/wb` в другом браузере / под другим пользователем → скрытие НЕ применяется (per-user).
- Нажать «Сбросить» в попапе → все склады снова видимы.
</verification>

<success_criteria>
- Миграция создана (не применена локально — применится на VPS через `bash deploy.sh`, это установленный паттерн проекта).
- Попап работает: чекбоксы + «Сбросить» + группировка по кластерам + per-cluster «Скрыть все / Показать все».
- Optimistic update: toggle чекбокса → мгновенное исчезновение склада из expanded; server save в фоне; после refresh настройка persistent.
- Изоляция per-user: настройка одного пользователя не видна другим.
- Оба фильтра («Без СЦ» и «Склады») совместимы — работают через AND (склад показан если оба разрешают).
- Агрегаты (cluster totals, Сводная, Итого WB) неизменны по значениям независимо от настройки видимости.
- TypeScript + ESLint чистые (Rule 3 — production build pass).
- Один коммит со всеми 6 файлами.
</success_criteria>

<output>
После завершения создать `.planning/quick/260422-oy5-per-user-stock-wb/260422-oy5-SUMMARY.md` с кратким отчётом:
- Что изменено (файлы)
- Коммит-хэш
- Напоминание: миграция применится автоматически через `bash deploy.sh` на VPS (`prisma migrate deploy`)
- Smoke-test шаги для пользователя (открыть /stock/wb, попробовать попап, проверить агрегаты неизменны, перезагрузить страницу)
</output>
