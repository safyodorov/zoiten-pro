---
phase: quick-260720-oh2-bpla-virtual-warehouses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260720_bpla_virtual_warehouses/migration.sql
  - lib/wb-virtual-warehouse.ts
  - scripts/seed-bpla-warehouses.ts
  - app/api/wb-sync/route.ts
  - app/api/cron/wb-cards-refresh/route.ts
  - lib/stock-wb-data.ts
  - components/stock/StockWbTable.tsx
  - lib/finance-snapshot.ts
  - lib/balance-data.ts
  - components/finance/BalanceSheetTable.tsx
  - tests/wb-virtual-warehouse.test.ts
  - tests/finance-snapshot.test.ts
autonomous: true
requirements: [QUICK-260720-oh2]

must_haves:
  truths:
    - "После запуска /api/wb-sync и /api/cron/wb-cards-refresh строки виртуальных складов «Электросталь БПЛА»/«Котовск БПЛА» в WbCardWarehouseStock остаются нетронутыми (clean-replace их не удаляет)"
    - "WbCard.stockQty НЕ включает сгоревшие остатки (денормализация считает totalStock только по ответу API, виртуальные строки в него не попадают)"
    - "inWayFromClient каждой карточки после обеих денормализаций = max(0, значение_из_API − сгоревшее_qty_по_nmId)"
    - "В /finance/balance есть отдельная красная строка «Сгоревший товар (потенциальная компенсация WB)» ≈ 9,2 млн ₽ по себестоимости, отдельная от товарных остатков, без двойного счёта с «В пути возвраты»"
    - "В /stock/wb сгоревший товар виден отдельной группой «БПЛА» и не участвует в дефиците/оборачиваемости кластеров"
    - "План продаж (stockNow) и /stock не показывают сгоревшие остатки (читают WbCard.stockQty, который их не включает)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "WbWarehouse.isVirtual Boolean @default(false)"
      contains: "isVirtual"
    - path: "lib/wb-virtual-warehouse.ts"
      provides: "loadVirtualWarehouseIds, loadBurnedQtyByNmId, applyBurnedInWay"
      min_lines: 25
    - path: "scripts/seed-bpla-warehouses.ts"
      provides: "Идемпотентный сид виртуальных складов из burned-stock-2026-07-17.json"
      min_lines: 30
    - path: "app/api/wb-sync/route.ts"
      provides: "Защита виртуальных строк + вычет inWayFromClient"
    - path: "app/api/cron/wb-cards-refresh/route.ts"
      provides: "Защита виртуальных строк + вычет inWayFromClient"
    - path: "lib/balance-data.ts"
      provides: "Красная строка WB_BURNED"
      contains: "WB_BURNED"
  key_links:
    - from: "app/api/wb-sync/route.ts + app/api/cron/wb-cards-refresh/route.ts"
      to: "lib/wb-virtual-warehouse.ts"
      via: "virtualIds.has(r.warehouseId) в фильтре clean-replace + applyBurnedInWay в денормализации"
      pattern: "applyBurnedInWay|virtualIds"
    - from: "lib/finance-snapshot.ts"
      to: "lib/balance-data.ts"
      via: "location WB_BURNED в FinanceStockSnapshot → красная строка баланса"
      pattern: "WB_BURNED"
---

<objective>
Зафиксировать сгоревшие 17.07 остатки складов Электросталь и Котовск (атака БПЛА, WB обнулил остатки) на двух виртуальных складах «Электросталь БПЛА»/«Котовск БПЛА», защитить их от затирания синхронизацией, вычесть из «в пути возвраты», исключить из планирования/операционных метрик и показать красной строкой потенциальной компенсации в /finance/balance.

Purpose: сохранить единственный оставшийся источник данных (прод-строки удалены clean-replace'ом 20.07 13:43), убрать искажение inWayFromClient (6414 при норме 1-2 тыс) и корректно отразить потенциальную компенсацию WB.

Output: миграция WbWarehouse.isVirtual, helper-модуль, идемпотентный сид, защита обоих sync-роутов, БПЛА-группа в /stock/wb, красная строка в балансе, тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260720-oh2-bpla-virtual-warehouses/260720-oh2-CONTEXT.md
@.planning/quick/260720-oh2-bpla-virtual-warehouses/burned-stock-2026-07-17.json
@CLAUDE.md

<interfaces>
<!-- Ключевые контракты из кодовой базы — использовать напрямую, без разведки. -->

Prisma (prisma/schema.prisma ~1042-1069):
```prisma
model WbWarehouse {
  id           Int    @id
  name         String
  cluster      String
  shortCluster String
  isActive     Boolean @default(true)
  needsClusterReview Boolean @default(false)
  stocks       WbCardWarehouseStock[]
  orders       WbCardWarehouseOrders[]
}
model WbCardWarehouseStock {
  id String @id @default(cuid())
  wbCardId String
  warehouseId Int
  techSize String @default("")   // "" для товаров без размерной сетки
  quantity Int @default(0)
  @@unique([wbCardId, warehouseId, techSize])
}
```

Реальные склады (НЕ трогать): Электросталь id=686, Котовск id=90011. Оба сейчас 0 строк.

Денормализация inWayFromClient — ОДИН и тот же блок в двух местах:
- app/api/wb-sync/route.ts ~470-481 (totalInWayFrom из warehouseItems ответа API)
- app/api/cron/wb-cards-refresh/route.ts ~198-209
Оба считают totalInWayFrom = SUM(warehouseItems[].inWayFromClient) и пишут в WbCard.inWayFromClient.

Clean-replace фильтр удаления — ОДИН и тот же паттерн в двух местах:
- app/api/wb-sync/route.ts ~453-459
- app/api/cron/wb-cards-refresh/route.ts ~182-188
```ts
const toDeleteIds = existingRows
  .filter((r) => !incomingSet.has(`${r.warehouseId}::${r.techSize}`))
  .map((r) => r.id)
```

Balance snapshot (lib/finance-snapshot.ts):
```ts
type FinanceStockLocationName = "WB_WAREHOUSE" | "WB_IN_WAY_TO_CLIENT" | "WB_IN_WAY_FROM_CLIENT" | "IVANOVO"
export function computeStockSnapshotRows(products: StockSnapshotProductInput[], wbCardsByNmId: Map<number, WbCardStockInput>): StockSnapshotRowInput[]
// PURE: агрегирует qty × costPrice per (product, location); qty<=0 → строка не создаётся; costPrice=null → valueRub=null
```

Balance UI (components/finance/BalanceSheetTable.tsx ~232): рендерит `<span>{line.label}</span>`; BalanceLine (lib/balance-data.ts ~99) — {key,label,amountRub,currency?,approximate?,note?,children?}.

/stock/wb (lib/stock-wb-data.ts ~269-307): каждый warehouseId маппится в кластер по meta.shortCluster; неизвестный shortCluster → "Прочие". Запрос ~142 включает `warehouses: { include: { warehouse: true } }`.

План продаж (lib/sales-plan/data.ts ~136) и /stock (lib/stock-data.ts) читают ТОЛЬКО WbCard.stockQty — виртуальные строки их не заденут (подтверждено). Правок в этих файлах НЕ требуется — только проверить.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Схема isVirtual + helper-модуль + идемпотентный сид</name>
  <files>prisma/schema.prisma, prisma/migrations/20260720_bpla_virtual_warehouses/migration.sql, lib/wb-virtual-warehouse.ts, scripts/seed-bpla-warehouses.ts, tests/wb-virtual-warehouse.test.ts</files>
  <behavior>
    - applyBurnedInWay(apiInWayFrom, burnedQty) → Math.max(0, apiInWayFrom - burnedQty). Тест: (6414, 5142) → 1272; (100, 200) → 0 (floor); (100, 0) → 100.
    - Сид идемпотентен: повторный запуск не создаёт дублей складов и строк остатков (upsert по id и по (wbCardId, warehouseId, techSize)).
    - nmId без WbCard → console.warn + skip, не throw.
  </behavior>
  <action>
    1. prisma/schema.prisma: добавить в модель WbWarehouse поле `isVirtual Boolean @default(false)` И добавить значение `WB_BURNED` в enum `FinanceStockLocation` (~line 1979 — это реальный Postgres enum, которым типизирован FinanceStockSnapshot.location; без него Task 3 упадёт в tsc и в рантайме). Запустить `npx prisma format && npx prisma generate` (generate НЕ требует живой БД — конвенция репо, см. quick 260707-k9g: «prisma generate локально для типов, миграция pending до деплоя»; без generate локальный клиент не увидит isVirtual/WB_BURNED и tsc-гейты Task 2/3 упадут).
    2. Создать migration.sql в папке prisma/migrations/20260720_bpla_virtual_warehouses/ с ДВУМЯ statement'ами: `ALTER TABLE "WbWarehouse" ADD COLUMN "isVirtual" BOOLEAN NOT NULL DEFAULT false;` и `ALTER TYPE "FinanceStockLocation" ADD VALUE 'WB_BURNED';` (без потери данных — существующие строки получат false, enum расширяется аддитивно).
    3. lib/wb-virtual-warehouse.ts — pure + Prisma-loaders:
       - `export const BPLA_WAREHOUSES = [{ id: 99001, name: "Электросталь БПЛА" }, { id: 99002, name: "Котовск БПЛА" }] as const` (99001/99002 — вне занятого диапазона, реальный Котовск=90011).
       - `export const BPLA_SHORT_CLUSTER = "БПЛА"`.
       - `export function applyBurnedInWay(apiInWayFrom: number, burnedQty: number): number` — pure, `Math.max(0, apiInWayFrom - burnedQty)`.
       - `export async function loadVirtualWarehouseIds(db): Promise<Set<number>>` — `db.wbWarehouse.findMany({ where: { isVirtual: true }, select: { id: true } })` → Set.
       - `export async function loadBurnedQtyByNmId(db): Promise<Map<number, number>>` — сумма quantity по строкам WbCardWarehouseStock виртуальных складов, сгруппированная по nmId карточки. Реализация: `db.wbCardWarehouseStock.findMany({ where: { warehouse: { isVirtual: true } }, select: { quantity: true, wbCard: { select: { nmId: true } } } })` → аккумулировать в Map<nmId, sumQty>.
       Типизировать db как `PrismaClient | Prisma.TransactionClient` (паттерн проекта — см. lib/sales-plan/data.ts сигнатуры) чтобы вызывать из транзакции.
    4. scripts/seed-bpla-warehouses.ts — идемпотентный tsx/node-скрипт (паттерн scripts/bootstrap-balance-snapshot.ts): читает burned-stock-2026-07-17.json из task-директории, upsert двух складов (id 99001/99002, cluster "БПЛА (сгоревшие склады)", shortCluster="БПЛА", isVirtual=true, isActive=true, needsClusterReview=false), затем для каждого item: `wbCard.findUnique({ where: { nmId } })`; если нет — `console.warn` и continue; иначе `wbCardWarehouseStock.upsert` по (wbCardId, warehouseId, techSize="") с quantity=qty (update: { quantity } — REPLACE, не суммировать). В конце вывести сводку: складов создано/обновлено, строк upsert, nmId без карточки. Скрипт запускается ВРУЧНУЮ после деплоя (задокументировать в SUMMARY) — deploy сам его не вызывает.
    5. tests/wb-virtual-warehouse.test.ts — unit-тесты applyBurnedInWay (3 кейса из behavior). loadBurnedQtyByNmId/loadVirtualWarehouseIds покрыть в Task 2 (нужен prisma mock).
  </action>
  <verify>
    <automated>npx vitest run tests/wb-virtual-warehouse.test.ts</automated>
  </verify>
  <done>isVirtual в схеме + миграция; helper экспортирует 3 функции + константы; сид идемпотентен и терпим к nmId без карточки; unit-тест applyBurnedInWay зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Защита от clean-replace + вычет inWayFromClient (оба роута) + БПЛА-группа в /stock/wb</name>
  <files>app/api/wb-sync/route.ts, app/api/cron/wb-cards-refresh/route.ts, lib/stock-wb-data.ts, components/stock/StockWbTable.tsx, tests/wb-virtual-warehouse.test.ts</files>
  <behavior>
    - Clean-replace: строка виртуального склада (warehouseId ∈ virtualIds) НИКОГДА не попадает в toDeleteIds, даже если её нет в incoming ответе API. Тест на delete-фильтр: existing=[{real,not-incoming},{virtual,not-incoming}], incoming=[] → toDelete содержит только real.
    - Денормализация: WbCard.inWayFromClient = applyBurnedInWay(SUM(api inWayFromClient), burnedQty[nmId]). totalStock (WbCard.stockQty) НЕ меняется — считается только по warehouseItems ответа API.
    - /stock/wb: виртуальные склады не попадают в кластерные totalStock/ordersPerDay/Д/Об; отображаются отдельной группой «БПЛА».
  </behavior>
  <action>
    Оба роута (app/api/wb-sync/route.ts блок ~369-482, app/api/cron/wb-cards-refresh/route.ts блок ~119-218) — единообразно через helper из Task 1:
    1. ПЕРЕД транзакцией per-warehouse (один раз): `const virtualIds = await loadVirtualWarehouseIds(prisma)` и `const burnedByNmId = await loadBurnedQtyByNmId(prisma)`.
    2. В фильтре clean-replace добавить условие сохранения виртуальных: `.filter((r) => !incomingSet.has(\`${r.warehouseId}::${r.techSize}\`) && !virtualIds.has(r.warehouseId))`. Комментарий: «виртуальные склады БПЛА (сгоревшие остатки) защищены от clean-replace — quick 260720-oh2».
    3. В денормализации заменить `inWayFromClient: totalInWayFrom` на `inWayFromClient: applyBurnedInWay(totalInWayFrom, burnedByNmId.get(nmId) ?? 0)`. stockQty/inWayToClient оставить как есть. Комментарий про вычет сгоревших.
    4. lib/stock-wb-data.ts: перед кластерной агрегацией (~230-307) отделить виртуальные строки от `card.warehouses` в ЕДИНУЮ отфильтрованную переменную (например `physicalWarehouses` + `virtualWarehouses`). В цикле по allWarehouseIds пропускать warehouseId у которого `warehouseMetaById.get(id)` соответствует isVirtual (добавить isVirtual в warehouseMetaById из `ws.warehouse?.isVirtual`). Виртуальные собирать в новый слот `bpla: { warehouses: WarehouseSlot[]; totalStock: number }` на WbStockRow (расширить интерфейс). Виртуальные НЕ прибавляются к totalStock карточки и к cluster-агрегатам (значит не влияют на Д/Об). ВАЖНО (warn чекера): `cardWarehouses = card?.warehouses ?? []` (~line 322) уходит в `buildSizeBreakdown` НЕфильтрованным — виртуальные строки (techSize="") протекут спурриозной строкой размера в кластер «Прочие»; передавать в buildSizeBreakdown ту же отфильтрованную `physicalWarehouses`, не сырой массив. Также добавить isVirtual в select запроса warehouse (уже `include: { warehouse: true }` — поле доступно после prisma generate).
    5. components/stock/StockWbTable.tsx: если у строки есть bpla.warehouses с quantity>0 — показать их отдельной подгруппой/бейджем «БПЛА» (не в кластерных колонках, не в Д/Об). Минимально: отдельная строка/бейдж со складами и qty при expand; соблюдать sticky-паттерн CLAUDE.md (сплошной bg-*, без /NN на sticky). Без переусложнения — читаемая индикация «сгорело: N шт».
    6. Дополнить tests/wb-virtual-warehouse.test.ts: тест delete-фильтра (вынести чистую функцию отбора toDeleteIds или проверить логику фильтра инлайн-хелпером) + loadBurnedQtyByNmId/loadVirtualWarehouseIds через prisma mock (паттерн tests/finance-snapshot.test.ts / существующие mock-тесты).
  </action>
  <verify>
    <automated>npx vitest run tests/wb-virtual-warehouse.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Оба роута: виртуальные строки переживают clean-replace, inWayFromClient вычитает сгоревшее (floor 0), stockQty неизменён. /stock/wb: БПЛА отдельной группой, кластерные Д/Об без сгоревших. tsc + тесты зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Красная строка «Сгоревший товар (потенциальная компенсация WB)» в /finance/balance</name>
  <files>lib/finance-snapshot.ts, lib/balance-data.ts, components/finance/BalanceSheetTable.tsx, tests/finance-snapshot.test.ts</files>
  <behavior>
    - computeStockSnapshotRows принимает burnedQtyByNmId и эмитит строки location="WB_BURNED" (qty × costPrice, qty<=0 → строка не создаётся). Тест: продукт с nmId в burnedMap → строка WB_BURNED с valueRub = qty×costPrice; без сгоревшего → строки WB_BURNED нет.
    - Баланс: отдельная строка «Сгоревший товар (потенциальная компенсация WB)», красная, ≈ 9,2 млн ₽, отдельно от «Склады WB» и без двойного счёта с «WB в пути от клиента» (двойной счёт исключён вычетом Task 2).
  </behavior>
  <action>
    1. lib/finance-snapshot.ts (Prisma-enum WB_BURNED уже добавлен в Task 1 — здесь только TS-логика):
       - Расширить FinanceStockLocationName типом "WB_BURNED".
       - computeStockSnapshotRows: добавить параметр `burnedQtyByNmId: Map<number, number> = new Map()` (после wbCardsByNmId, С ДЕФОЛТОМ — warn чекера: scripts/bootstrap-balance-snapshot.ts:345 вызывает функцию со старой 2-арг сигнатурой и НЕ входит в scope задачи; дефолт сохраняет совместимость). Внутри цикла по product.nmIds аккумулировать `burnedQty += burnedQtyByNmId.get(nmId) ?? 0`; добавить ["WB_BURNED", burnedQty] в массив locations. costPrice берётся тот же product.costPrice (по себестоимости — решение пользователя).
       - runFinanceSnapshot: перед вызовом computeStockSnapshotRows загрузить burnedQtyByNmId через loadBurnedQtyByNmId(prisma) (helper Task 1) и передать. Существующий select уже даёт nmId/stockQty/inWay — не менять.
    2. lib/balance-data.ts:
       - Зарегистрировать "WB_BURNED" во внутренних Map (stockByLocation/stockContribsByLocation init) НЕ добавляя его в STOCK_LOCATION_ORDER (чтобы контролировать позицию и стиль отдельно).
       - Добавить в BalanceLine опциональный флаг `destructive?: boolean`.
       - После цикла обычных stockLines добавить отдельную строку (паттерн push readyToShip/inTransit ~824-843): key="stock-wb-burned", label="Сгоревший товар (потенциальная компенсация WB)", amountRub из stockByLocation("WB_BURNED"), destructive:true, children = buildProductTree("stock-wb-burned", WB_BURNED contribs, productMetaMap), note про потенциальную компенсацию. Строка входит в inventoryGroup (учитывается в активах) но визуально обособлена красным.
    3. components/finance/BalanceSheetTable.tsx: при рендере `<span>{line.label}</span>` (~232) применить `text-red-600` если `line.destructive` (аналогично существующим text-* классам). Проверить, что то же применимо в StockLine и рекурсивном рендере вложенных узлов, если строка имеет children.
    4. tests/finance-snapshot.test.ts: добавить кейсы из behavior (WB_BURNED строка эмитится/не эмитится). Не сломать существующий golden — новый параметр по умолчанию не влияет на продукты без сгоревшего (пустой Map).
  </action>
  <verify>
    <automated>npx vitest run tests/finance-snapshot.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>WB_BURNED эмитится computeStockSnapshotRows; в /finance/balance красная строка «Сгоревший товар (потенциальная компенсация WB)» ≈ 9,2 млн ₽ отдельно от товарных остатков, без двойного счёта; тесты + tsc зелёные.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit — без ошибок.
- npm run test — вся сюита зелёная (не сломаны finance-snapshot golden, balance-*, stock-wb тесты).
- Логическая проверка инвариантов (в must_haves.truths).
- РУЧНОЙ шаг после деплоя (в SUMMARY): запустить `npx tsx scripts/seed-bpla-warehouses.ts` на проде для восстановления сгоревших остатков из JSON.
</verification>

<success_criteria>
- WbWarehouse.isVirtual в схеме + миграция без потери данных.
- Виртуальные склады «Электросталь БПЛА» (99001) / «Котовск БПЛА» (99002) переживают clean-replace в обоих sync-роутах.
- inWayFromClient = max(0, API − сгоревшее) в обеих точках денормализации через единый helper.
- WbCard.stockQty, план продаж, /stock — без сгоревших остатков.
- /stock/wb — БПЛА отдельной группой, без влияния на Д/Об.
- /finance/balance — красная строка ≈ 9,2 млн ₽, без двойного счёта.
- Сид идемпотентен, терпим к nmId без карточки.
</success_criteria>

<output>
After completion, create `.planning/quick/260720-oh2-bpla-virtual-warehouses/260720-oh2-SUMMARY.md`.
Обязательно задокументировать в SUMMARY: (1) ручной запуск `npx tsx scripts/seed-bpla-warehouses.ts` на проде ПОСЛЕ деплоя; (2) сценарий «закрытия» виртуального склада (компенсация деньгами / возврат товара) — ручной SQL, out of scope этой задачи.
</output>
