---
phase: quick-260714-ij9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260714_loan_monthly_commission_ndfl/migration.sql
  - lib/finance-weekly/credit-accrual.ts
  - lib/finance-weekly/data.ts
  - tests/finance-weekly-credit-accrual.test.ts
  - components/credits/LoanModal.tsx
  - app/actions/credits.ts
  - lib/credits-data.ts
  - components/credits/CreditsTable.tsx
autonomous: true
requirements:
  - quick-260714-ij9-credit-pool-v2
must_haves:
  truths:
    - "Loan хранит два новых nullable-поля: monthlyCommissionRub и monthlyNdflRub (₽/мес)"
    - "Кредитный пул /finance/weekly считается по ВСЕМ кредитам группы (не только ЗОЙТЕН)"
    - "Пул = weeklyAccruedInterest(все) + weeklyLoanExtras(все) — проценты + амортизация комиссий/НДФЛ ×7/30"
    - "weeklyLoanExtras гейтит вклад по issueDate (< weekEnd) и по дате последнего планового платежа (>= weekStart)"
    - "Существующая функция weeklyAccruedInterest не изменена (сигнатура и поведение)"
    - "Пользователь вводит «Комиссия, ₽/мес» и «НДФЛ, ₽/мес» в модалке кредита; значения сохраняются и восстанавливаются при повторном открытии"
    - "Пул по-прежнему только appliances (clothing.creditInterest = 0)"
  artifacts:
    - path: "prisma/migrations/20260714_loan_monthly_commission_ndfl/migration.sql"
      provides: "ALTER TABLE Loan ADD COLUMN monthlyCommissionRub, monthlyNdflRub"
      contains: "ADD COLUMN"
    - path: "lib/finance-weekly/credit-accrual.ts"
      provides: "export function weeklyLoanExtras + interface LoanExtrasInput"
      exports: ["weeklyLoanExtras"]
    - path: "lib/finance-weekly/data.ts"
      provides: "creditWeekTotal по всем кредитам группы (accrual + extras), без фильтра ЗОЙТЕН"
      contains: "weeklyLoanExtras"
    - path: "tests/finance-weekly-credit-accrual.test.ts"
      provides: "unit-тесты weeklyLoanExtras (append к существующему файлу)"
      contains: "weeklyLoanExtras"
    - path: "components/credits/LoanModal.tsx"
      provides: "два опциональных числовых поля Комиссия/НДФЛ ₽/мес"
      contains: "monthlyCommissionRub"
    - path: "app/actions/credits.ts"
      provides: "createLoan/updateLoan принимают и пишут два новых поля"
      contains: "monthlyCommissionRub"
  key_links:
    - from: "lib/finance-weekly/data.ts"
      to: "lib/finance-weekly/credit-accrual.ts"
      via: "import weeklyLoanExtras; creditWeekTotal = accrual + extras"
      pattern: "weeklyLoanExtras"
    - from: "components/credits/LoanModal.tsx"
      to: "app/actions/credits.ts"
      via: "createLoan/updateLoan получают monthlyCommissionRub/monthlyNdflRub"
      pattern: "monthlyCommissionRub"
    - from: "app/actions/credits.ts"
      to: "prisma.loan"
      via: "create/update data пишет оба поля"
      pattern: "monthlyCommissionRub"
    - from: "lib/credits-data.ts + components/credits/CreditsTable.tsx"
      to: "components/credits/LoanModal.tsx"
      via: "CreditRow + loanForModal несут оба поля → edit-режим восстанавливает значения (без затирания SQL-залитых)"
      pattern: "monthlyCommissionRub"
---

<objective>
Закрыть W3b недельного фин-отчёта `/finance/weekly`: расширить кредитный пул с v1 (проценты только по ЗОЙТЕН) до v2 — начисление процентов по ВСЕЙ группе + амортизация единовременных комиссий JetLend и НДФЛ инвесторам, усреднённые на срок транша.

Методика согласована с пользователем и сверена с экономистом 2026-07-14: константа экономиста 1 686 960 ₽/мес реконструирована как проценты ГРУППЫ (включая Дрим Лайн) + амортизация комиссий 275 130/мес + НДФЛ ~128 431/мес.

Purpose: недельный кредитный пул отражает полную экономическую нагрузку кредитов группы, а не только процентную часть по одной компании.
Output: два новых поля на `Loan`, миграция, новая pure-функция `weeklyLoanExtras`, расширенный кредитный пул в `data.ts`, поля в модалке кредита + server action, unit-тесты.

НЕ входит в задачу (out of scope):
- Заполнение прод-данных (значения полей per транш зальёт оркестратор SQL-ом после деплоя).
- `lib/finance-weekly/engine.ts` — пул приходит в движок готовым числом, контракт не меняется.
- Пересчёт/снапшоты прошлых недель.
- Пре-существующее lossy-поведение edit-из-таблицы для `notes`/`payments` (пустой график при редактировании) — НЕ трогать.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Все контракты извлечены из кодовой базы. Исполнителю НЕ нужно исследовать репозиторий. -->

## prisma/schema.prisma — model Loan (строки 1414-1434, ТЕКУЩЕЕ состояние)
```prisma
model Loan {
  id             String        @id @default(cuid())
  contractNumber String
  companyId      String
  company        Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  lenderId       String
  lender         Lender        @relation(fields: [lenderId], references: [id], onDelete: Restrict)
  amount         Decimal       @db.Decimal(14, 2)
  annualRatePct  Decimal       @db.Decimal(6, 3)
  termMonths     Int?
  issueDate      DateTime?
  notes          String?       @db.Text
  payments       LoanPayment[]
  deletedAt      DateTime?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  @@index([deletedAt])
  @@index([companyId])
  @@index([lenderId])
}
```
LoanPayment: `date DateTime @db.Date`, `principal Decimal`, `interest Decimal`.

## lib/finance-weekly/credit-accrual.ts — ТЕКУЩЕЕ (расширять АДДИТИВНО, не менять weeklyAccruedInterest)
Импорт `round2` из `@/lib/loan-math`. Helper `utcMidnightMs(val: Date|string): number` уже в файле.
`weeklyAccruedInterest(loans: AccrualLoanInput[], weekStart: Date): number` — Σ balance(weekStart) × rate/100 × 7/365.
`AccrualLoanInput` = `{ amount, annualRatePct, issueDate?, payments: {date, principal}[] }`.
Гейт issueDate в accrual: `issueDate != null && utcMidnightMs(issueDate) >= weekStartMs + 7д → continue`.

## lib/finance-weekly/data.ts — ТЕКУЩЕЕ
- Строка 51: `import { weeklyAccruedInterest } from "@/lib/finance-weekly/credit-accrual"`
- Строки 344-345: деструктуризация `const [ ..., loans, realizationRows, ... ] = await Promise.all([...])`
- Строки 385-394 — запрос кредитов внутри Promise.all:
```ts
    prisma.loan.findMany({
      where: { deletedAt: null },
      select: {
        amount: true,
        annualRatePct: true,
        issueDate: true,
        company: { select: { name: true } },
        payments: { select: { date: true, principal: true } },
      },
    }),
```
- Строки 463-473 — обработка кредитов (v1, фильтр ЗОЙТЕН):
```ts
  // 8. Проценты по кредиту ЗОЙТЕН (W2d Фикс 4): начисление остаток×ставка×7/365
  // вместо платежей по дате (большинство недель платежей не имело → пул был 0).
  const zoitenLoans = loans
    .filter((l) => l.company.name.toUpperCase().includes("ЗОЙТЕН"))
    .map((l) => ({
      amount: Number(l.amount),
      annualRatePct: Number(l.annualRatePct),
      issueDate: l.issueDate,
      payments: l.payments.map((p) => ({ date: p.date, principal: Number(p.principal) })),
    }))
  const zoitenWeekInterest = weeklyAccruedInterest(zoitenLoans, weekStart)
```
- Строка 694 — использование в appliancesPools:
```ts
    creditInterest: { total: zoitenWeekInterest, baseRevenue: applBase },
```
- Строка 707 — clothingPools (НЕ трогать): `creditInterest: { total: 0, baseRevenue: 0 }`

## components/credits/LoanModal.tsx — ТЕКУЩЕЕ
- `interface LoanForModal` (строки 22-33) — локальный тип props (id, contractNumber, companyId, lenderId, amount, annualRatePct, termMonths, issueDate, notes, payments).
- `LoanFormSchema` (z.object, строки 52-62), `LoanFormValues = z.infer<...>`.
- `defaultValues` (строки 111-121), `reset(...)` edit-режим (133-147) + create-режим (149-159).
- `onSubmit`: `createLoan({...})` (168-178) и `updateLoan({ id, ... })` (188-198).
- Паттерн nullable-числа (termMonths, строки 358-361): `register("...", { valueAsNumber: true, setValueAs: (v) => (v === "" || isNaN(Number(v)) ? null : Number(v)) })`.
- Классы: `inputCls`, `errorCls`; секции через `<SectionDivider label="..." />`.

## app/actions/credits.ts — ТЕКУЩЕЕ
- `"use server"`, `requireSection("CREDITS", "MANAGE")` во всех write-действиях.
- `LoanSchema` (z.object, строки 24-34): contractNumber, companyId, lenderId, amount, annualRatePct, termMonths, issueDate, notes, payments.
- `UpdateLoanSchema = LoanSchema.extend({ id })`.
- `createLoan`: `prisma.loan.create({ data: { ... } })` (64-80). `updateLoan`: внутри `$transaction` → `tx.loan.update({ where:{id}, data:{...} })` (108-120).
- `revalidatePath("/credits")` + `revalidatePath("/credits/schedule")`.

## lib/credits-data.ts — ТЕКУЩЕЕ
- `interface CreditRow` (строки 16-31): id, contractNumber, companyName, lenderName, amount, annualRatePct, termMonths, issueDate, effectiveIssueDate, currentBalance, ..., status.
- `loadCredits()` (строки 49-110): `prisma.loan.findMany({ where:{deletedAt:null}, include:{ company, lender, payments } })` → `.map((loan) => ({ ... }))`. include ⇒ все скалярные поля Loan доступны на `loan` автоматически.

## components/credits/CreditsTable.tsx — ТЕКУЩЕЕ
- Свой `interface LoanForModal` (строки 98-109) — ДУБЛЬ типа из LoanModal, передаётся в `<LoanModal mode="edit" loan={loanForModal} />`.
- Реконструкция `loanForModal` из `row` (строки 202-213): id, contractNumber, companyId, lenderId, amount, annualRatePct, termMonths, issueDate, `notes: null`, `payments: []` (пре-существующее lossy-поведение — НЕ трогать notes/payments).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma — два nullable-поля на Loan + миграция</name>
  <files>prisma/schema.prisma, prisma/migrations/20260714_loan_monthly_commission_ndfl/migration.sql</files>
  <action>
Добавить в `model Loan` (после `issueDate DateTime?`, перед `notes`) два поля:
```prisma
  monthlyCommissionRub Decimal? @db.Decimal(12, 2) // амортизация единовременной комиссии JetLend (вкл. перенесённые с досрочно погашенных траншей), ₽/мес — кредитный пул /finance/weekly (quick 260714-ij9)
  monthlyNdflRub       Decimal? @db.Decimal(12, 2) // НДФЛ инвесторам к уплате, усреднённый на срок, ₽/мес — кредитный пул /finance/weekly (quick 260714-ij9)
```

Создать файл миграции `prisma/migrations/20260714_loan_monthly_commission_ndfl/migration.sql` (naming по образцу соседних, напр. `20260707_wb_card_planned_price`):
```sql
-- quick 260714-ij9: амортизация единовременных комиссий JetLend + НДФЛ per транш
-- (кредитный пул /finance/weekly v2, закрывает W3b). Оба поля nullable.
ALTER TABLE "Loan" ADD COLUMN "monthlyCommissionRub" DECIMAL(12,2);
ALTER TABLE "Loan" ADD COLUMN "monthlyNdflRub" DECIMAL(12,2);
```

Локальной PostgreSQL нет (устоявшийся паттерн проекта) — миграция применится на VPS через `deploy.sh` (`prisma migrate deploy`). Здесь только зарегистрировать миграцию и перегенерировать Prisma Client (типы нужны для Task 2/3).
  </action>
  <verify>
    <automated>npx prisma generate</automated>
  </verify>
  <done>Схема валидна, `npx prisma generate` завершается без ошибок; Prisma Client содержит поля `monthlyCommissionRub`/`monthlyNdflRub` на `Loan`; файл миграции существует с двумя `ADD COLUMN`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: weeklyLoanExtras (pure) + расширение кредитного пула в data.ts + тесты</name>
  <files>lib/finance-weekly/credit-accrual.ts, lib/finance-weekly/data.ts, tests/finance-weekly-credit-accrual.test.ts</files>
  <behavior>
Новая функция `weeklyLoanExtras(loans, weekStart): number` (WEEK_START = 2026-06-29 UTC-Пн, weekEndExclusive = 2026-07-06):
    - Базовый ×7/30: `{monthlyCommissionRub: 30000, monthlyNdflRub: 0, payments: []}` → 30000 × 7/30 = **7000**
    - Сумма комиссии+НДФЛ: `{30000, 15000, payments:[{date:"2026-07-15"}]}` → 45000 × 7/30 = **10500**
    - Оба поля null/undefined → вклад **0** (monthly ≤ 0)
    - Гейт issueDate: `issueDate:"2026-07-06"` (= weekEndExclusive) → **0**; `"2026-08-01"` → **0**; `"2026-07-01"` (внутри недели) → **7000**
    - Гейт последнего платежа: payments max date `"2026-06-15"` (< weekStart, срок истёк) → **0**; max date `"2026-07-15"` (>= weekStart) → **7000**; ровно `"2026-06-29"` (= weekStart) → **7000**
    - Кредит без платежей + issueDate null → включается (**7000** при commission 30000)
    - Смесь: `[{30000, payments:[]}, {30000, 15000, payments:[{date:"2026-07-15"}]}, {99999, payments:[{date:"2026-06-15"}]}]` → 7000 + 10500 + 0 = **17500**
  </behavior>
  <action>
**2a. lib/finance-weekly/credit-accrual.ts (аддитивно, НЕ менять weeklyAccruedInterest):**
Добавить новый экспортируемый интерфейс + функцию (переиспользовать существующий `utcMidnightMs` и `round2`):
```ts
export interface LoanExtrasInput {
  /** Ежемесячная амортизация единовременной комиссии JetLend, ₽ (nullable). */
  monthlyCommissionRub?: number | null
  /** Ежемесячный НДФЛ инвесторам, усреднённый на срок, ₽ (nullable). */
  monthlyNdflRub?: number | null
  /** Дата выдачи (nullable). issueDate >= weekEnd (эксклюзивно) → вклад 0. */
  issueDate?: Date | string | null
  /**
   * График платежей. «Жив» на неделе, если дата ПОСЛЕДНЕГО планового платежа
   * (максимальная date) >= weekStart. Пустой график → гейт по последнему платежу
   * не применяется (консервативно включаем при прошедшем issueDate).
   */
  payments: { date: Date | string }[]
}

/**
 * Недельная доля ежемесячных «экстра»-нагрузок кредита (амортизация комиссии
 * JetLend + НДФЛ): Σ per кредит (commission + ndfl) × 7/30, если транш «жив»
 * на неделе. quick 260714-ij9 (кредитный пул /finance/weekly v2, W3b).
 *
 * Гейты «жив»:
 *  - issueDate (если задана) < weekEnd (эксклюзивно, weekStart + 7д);
 *  - дата последнего планового платежа >= weekStart (амортизация прекращается
 *    после последнего планового платежа); при пустом графике — гейт не применяется.
 * monthly <= 0 (оба поля null/0) → вклад 0. Результат = round2(Σ), ₽.
 */
export function weeklyLoanExtras(
  loans: LoanExtrasInput[],
  weekStart: Date,
): number {
  const weekStartMs = utcMidnightMs(weekStart)
  const weekEndExclusiveMs = weekStartMs + 7 * 86_400_000

  let total = 0
  for (const loan of loans) {
    const monthly = (loan.monthlyCommissionRub ?? 0) + (loan.monthlyNdflRub ?? 0)
    if (monthly <= 0) continue

    // Гейт 1: транш выдан после (эксклюзивного) конца недели → не существовал
    if (loan.issueDate != null && utcMidnightMs(loan.issueDate) >= weekEndExclusiveMs) {
      continue
    }

    // Гейт 2: срок истёк — последний плановый платёж раньше weekStart. Пустой график
    // → гейт пропускается (дата конца срока неизвестна, полагаемся на issueDate).
    if (loan.payments.length > 0) {
      let lastPaymentMs = -Infinity
      for (const p of loan.payments) {
        const ms = utcMidnightMs(p.date)
        if (ms > lastPaymentMs) lastPaymentMs = ms
      }
      if (lastPaymentMs < weekStartMs) continue
    }

    total += monthly * (7 / 30)
  }
  return round2(total)
}
```

**2b. lib/finance-weekly/data.ts:**
- Строка 51: расширить импорт → `import { weeklyAccruedInterest, weeklyLoanExtras } from "@/lib/finance-weekly/credit-accrual"`
- Запрос кредитов (строки 385-394): добавить два поля в `select`, убрать теперь-неиспользуемый `company` (его читал только удаляемый фильтр):
```ts
    prisma.loan.findMany({
      where: { deletedAt: null },
      select: {
        amount: true,
        annualRatePct: true,
        issueDate: true,
        monthlyCommissionRub: true,
        monthlyNdflRub: true,
        payments: { select: { date: true, principal: true } },
      },
    }),
```
- Блок 463-473: убрать фильтр ЗОЙТЕН, считать по ВСЕЙ группе, добавить extras. Заменить целиком на:
```ts
  // 8. Кредитный пул (quick 260714-ij9, v2 — закрывает W3b): начисление процентов
  // остаток×ставка×7/365 ПО ВСЕЙ ГРУППЕ (погашенные кредиты дают 0 автоматически)
  // + амортизация единовременных комиссий JetLend и НДФЛ per транш (×7/30 «живых»).
  // Пул по-прежнему только appliances (одежда кредит не несёт).
  const groupLoans = loans.map((l) => ({
    amount: Number(l.amount),
    annualRatePct: Number(l.annualRatePct),
    issueDate: l.issueDate,
    monthlyCommissionRub: l.monthlyCommissionRub != null ? Number(l.monthlyCommissionRub) : null,
    monthlyNdflRub: l.monthlyNdflRub != null ? Number(l.monthlyNdflRub) : null,
    payments: l.payments.map((p) => ({ date: p.date, principal: Number(p.principal) })),
  }))
  const creditWeekTotal =
    weeklyAccruedInterest(groupLoans, weekStart) + weeklyLoanExtras(groupLoans, weekStart)
```
- Строка 694 (appliancesPools): `creditInterest: { total: zoitenWeekInterest, ... }` → `creditInterest: { total: creditWeekTotal, baseRevenue: applBase }`.
- clothingPools (строка 707) — НЕ трогать (`{ total: 0, baseRevenue: 0 }`).

**2c. tests/finance-weekly-credit-accrual.test.ts (только ДОПОЛНИТЬ, существующие тесты weeklyAccruedInterest НЕ трогать):**
Добавить импорт `weeklyLoanExtras` + `type LoanExtrasInput` к существующему импорту из credit-accrual, и новый `describe("weeklyLoanExtras", ...)` с кейсами из блока `<behavior>` (переиспользовать константу `WEEK_START = 2026-06-29`).
  </action>
  <verify>
    <automated>npx vitest run tests/finance-weekly-credit-accrual.test.ts</automated>
  </verify>
  <done>Все тесты weeklyLoanExtras зелёные (golden 7000 / 10500 / 17500 + гейты + null → 0); существующие weeklyAccruedInterest тесты по-прежнему проходят; `npx tsc --noEmit` без ошибок в data.ts (creditWeekTotal, groupLoans типизированы); фильтр ЗОЙТЕН удалён; пул appliances = accrual + extras по всем кредитам; clothing.creditInterest = 0.</done>
</task>

<task type="auto">
  <name>Task 3: UI кредитов — поля Комиссия/НДФЛ ₽/мес + server action + edit round-trip</name>
  <files>components/credits/LoanModal.tsx, app/actions/credits.ts, lib/credits-data.ts, components/credits/CreditsTable.tsx</files>
  <action>
**3a. app/actions/credits.ts** (RBAC не менять — остаётся `requireSection("CREDITS", "MANAGE")`):
- В `LoanSchema` добавить два поля:
```ts
  monthlyCommissionRub: z.number().nonnegative().nullable().optional(),
  monthlyNdflRub: z.number().nonnegative().nullable().optional(),
```
- `createLoan` → в объект `data` `prisma.loan.create`:
```ts
        monthlyCommissionRub: parsed.monthlyCommissionRub ?? null,
        monthlyNdflRub: parsed.monthlyNdflRub ?? null,
```
- `updateLoan` → в `tx.loan.update({ data: { ... } })` те же две строки. (`UpdateLoanSchema` наследует поля через `.extend` — доп. правок не нужно.)

**3b. components/credits/LoanModal.tsx:**
- В локальный `interface LoanForModal` добавить: `monthlyCommissionRub: number | null` и `monthlyNdflRub: number | null`.
- В `LoanFormSchema`: `monthlyCommissionRub: z.number().nonnegative().nullable().optional()` + `monthlyNdflRub: ...` (аналогично).
- В `defaultValues` (create-дефолты) и в обеих ветках `reset(...)`:
  - create-ветки: `monthlyCommissionRub: null, monthlyNdflRub: null`
  - edit-ветка: `monthlyCommissionRub: loan.monthlyCommissionRub ?? null, monthlyNdflRub: loan.monthlyNdflRub ?? null`
- В `onSubmit`: и в `createLoan({...})`, и в `updateLoan({ id, ... })` передать `monthlyCommissionRub: values.monthlyCommissionRub ?? null, monthlyNdflRub: values.monthlyNdflRub ?? null`.
- Новый UI-блок (добавить перед блоком «Заметки», строка ~369). Отдельная секция + 2-колоночная сетка + hint:
```tsx
              <SectionDivider label="Кредитный пул (фин-отчёт за неделю)" />
              <div className="grid grid-cols-2 gap-3">
                <div className="flex flex-col gap-1">
                  <label className="text-xs font-medium text-muted-foreground">
                    Комиссия, ₽/мес
                    <span className="text-muted-foreground/60 ml-1 text-xs">(необязательно)</span>
                  </label>
                  <input
                    type="number"
                    step="0.01"
                    min="0"
                    {...register("monthlyCommissionRub", {
                      valueAsNumber: true,
                      setValueAs: (v) => (v === "" || isNaN(Number(v)) ? null : Number(v)),
                    })}
                    placeholder="0"
                    className={inputCls}
                  />
                </div>
                <div className="flex flex-col gap-1">
                  <label className="text-xs font-medium text-muted-foreground">
                    НДФЛ, ₽/мес
                    <span className="text-muted-foreground/60 ml-1 text-xs">(необязательно)</span>
                  </label>
                  <input
                    type="number"
                    step="0.01"
                    min="0"
                    {...register("monthlyNdflRub", {
                      valueAsNumber: true,
                      setValueAs: (v) => (v === "" || isNaN(Number(v)) ? null : Number(v)),
                    })}
                    placeholder="0"
                    className={inputCls}
                  />
                </div>
              </div>
              <p className="text-xs text-muted-foreground">
                Амортизация единовременной комиссии JetLend / НДФЛ инвесторам, равномерно
                на срок; входит в кредитный пул /finance/weekly.
              </p>
```

**3c. lib/credits-data.ts** (чтобы edit-режим восстанавливал значения, а не затирал SQL-залитые):
- В `interface CreditRow` добавить: `monthlyCommissionRub: number | null` и `monthlyNdflRub: number | null`.
- В `.map((loan) => ({ ... }))` внутри `loadCredits` добавить:
```ts
      monthlyCommissionRub: loan.monthlyCommissionRub != null ? Number(loan.monthlyCommissionRub) : null,
      monthlyNdflRub: loan.monthlyNdflRub != null ? Number(loan.monthlyNdflRub) : null,
```
(запрос использует `include` → скалярные поля `loan.monthlyCommissionRub`/`monthlyNdflRub` доступны без правки select.)

**3d. components/credits/CreditsTable.tsx:**
- В его локальный `interface LoanForModal` (строки 98-109) добавить те же два поля (`number | null`).
- В реконструкции `loanForModal` (строки 202-213) добавить `monthlyCommissionRub: row.monthlyCommissionRub, monthlyNdflRub: row.monthlyNdflRub`. НЕ трогать `notes: null` / `payments: []` (пре-существующее поведение).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Проект типизируется без ошибок; в модалке кредита видны поля «Комиссия, ₽/мес» и «НДФЛ, ₽/мес» с hint; create и edit сохраняют оба поля через createLoan/updateLoan; edit-режим (из CreditsTable) восстанавливает ранее сохранённые/SQL-залитые значения (не обнуляет их); RBAC кредитов не изменён (CREDITS MANAGE).</done>
</task>

</tasks>

<verification>
- `npx prisma generate` — схема валидна, клиент содержит новые поля (Task 1).
- `npx vitest run tests/finance-weekly-credit-accrual.test.ts` — новые тесты weeklyLoanExtras + существующие weeklyAccruedInterest зелёные (Task 2).
- `npx tsc --noEmit` — весь проект (data.ts + UI + actions) типизируется (Tasks 2, 3).
- Ручная сверка контракта: `data.ts` больше не содержит `.filter(... ЗОЙТЕН ...)`; кредитный пул appliances = `weeklyAccruedInterest + weeklyLoanExtras` по ВСЕМ кредитам; clothing.creditInterest остаётся 0; `weeklyAccruedInterest` не изменён.
</verification>

<success_criteria>
- Loan имеет `monthlyCommissionRub` и `monthlyNdflRub` (Decimal(12,2), nullable) + миграция `20260714_loan_monthly_commission_ndfl`.
- `weeklyLoanExtras` экспортирована, pure (ноль импортов Prisma/React), покрыта тестами (×7/30, оба гейта, null → 0, смесь).
- Кредитный пул /finance/weekly считается по всей группе (проценты + амортизация комиссий + НДФЛ), только appliances.
- Модалка кредита принимает и сохраняет оба поля (create + edit round-trip), server action защищён CREDITS MANAGE.
- Существующее поведение weeklyAccruedInterest и clothing-пула не изменено; engine.ts не тронут.
</success_criteria>

<output>
После завершения создать `.planning/quick/260714-ij9-v2-per/260714-ij9-SUMMARY.md`.
</output>
