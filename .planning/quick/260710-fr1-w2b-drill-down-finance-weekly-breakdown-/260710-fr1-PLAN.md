---
phase: quick-260710-fr1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-weekly/types.ts
  - lib/finance-weekly/engine.ts
  - tests/finance-weekly-engine.test.ts
  - components/finance/WeeklyFinArticleDialog.tsx
  - components/finance/WeeklyFinReportTable.tsx
autonomous: true
requirements: [W2b]
must_haves:
  truths:
    - "Клик по строке артикула в /finance/weekly открывает модалку с полной пооперационной юнит-экономикой (₽/ед) в ДВУХ сценариях — ИУ и Оферта"
    - "Модалка показывает per-unit разбивку по всем статьям Excel «Показатели» + Прибыль/ед, Выручка, Прибыль, Re продаж %, ROI % для обоих сценариев"
    - "Строки, различающиеся между сценариями (комиссия %, цена минус комиссия, логистика, прибыль), визуально выделены"
    - "Модалка read-only — параметры не редактируются, нет server action"
    - "Строки Вселенная / Бренд / Подытог / Итого НЕ кликабельны; только строки артикулов открывают модалку"
    - "Golden-тест nmId 165967746 остаётся зелёным; новые breakdown-ассерты проходят; pricing-math не тронут"
  artifacts:
    - path: "lib/finance-weekly/types.ts"
      provides: "CostBreakdown interface + breakdown:CostBreakdown на ScenarioResult + qtyOrders на ArticleResult"
      contains: "export interface CostBreakdown"
    - path: "lib/finance-weekly/engine.ts"
      provides: "Populated breakdown для обоих сценариев + qtyOrders на ArticleResult (additive, без изменения существующих полей)"
      contains: "breakdown"
    - path: "tests/finance-weekly-engine.test.ts"
      provides: "3-4 ассерта на breakdown golden-артикула (netOfCommissionPerUnit, taxPerUnit, acquiringPerUnit, std logisticsPerUnit)"
      contains: "breakdown"
    - path: "components/finance/WeeklyFinArticleDialog.tsx"
      provides: "Read-only drill-down модалка per-article breakdown (shadcn Dialog)"
      min_lines: 90
    - path: "components/finance/WeeklyFinReportTable.tsx"
      provides: "Кликабельные строки артикулов → открытие WeeklyFinArticleDialog; open+selectedNmId state"
      contains: "WeeklyFinArticleDialog"
  key_links:
    - from: "lib/finance-weekly/engine.ts (toScenarioResult)"
      to: "ScenarioResult.breakdown"
      via: "объект CostBreakdown из уже посчитанного ScenarioBreakdown + K"
      pattern: "breakdown:"
    - from: "components/finance/WeeklyFinReportTable.tsx (article <tr> onClick)"
      to: "WeeklyFinArticleDialog open/selected article"
      via: "setSelectedNmId + setOpen(true), only kind==='article'"
      pattern: "onClick"
    - from: "components/finance/WeeklyFinArticleDialog.tsx"
      to: "article.iu.breakdown / article.std.breakdown"
      via: "чтение CostBreakdown полей per-scenario"
      pattern: "breakdown"
---

<objective>
W2b: drill-down модалка для понедельного WB фин-отчёта `/finance/weekly`. Клик по строке артикула
→ модалка с полной пооперационной юнит-экономикой (₽/ед) в ОБОИХ сценариях (ИУ и Оферта), как строка
Excel «Показатели» и `PricingCalculatorDialog` из `/prices/wb`. READ-ONLY (отчёт фактический — правки
параметров здесь нет). НЕ план-факт (это W2c), НЕ Prisma, НЕ WB API.

Purpose: пользователь видит, из чего складывается прибыль каждого артикула и почему ИУ vs Оферта
различаются, не покидая роллап-таблицу W2a.

Output:
- Расширенный движок: per-unit line-item breakdown как публичное поле (значения УЖЕ считаются внутри,
  просто возвращаем их — additive, golden-тест остаётся зелёным).
- Новая read-only модалка `WeeklyFinArticleDialog.tsx`.
- Кликабельные строки артикулов в существующей таблице W2a.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@docs/superpowers/specs/2026-07-08-weekly-finreport-design.md

# Движок (ИЗВЕСТЕН — прочитан планировщиком; per-unit значения УЖЕ считаются в ScenarioBreakdown)
@lib/finance-weekly/types.ts
@lib/finance-weekly/engine.ts
@tests/finance-weekly-engine.test.ts

# UI (образец модалки + таблица для wiring)
@components/finance/WeeklyFinReportTable.tsx
@components/prices/PricingCalculatorDialog.tsx
@components/ui/dialog.tsx

<interfaces>
<!-- Контракты, которые исполнителю НЕ нужно искать по кодовой базе -->

Текущий ScenarioResult (lib/finance-weekly/types.ts:90) — НЕ трогать существующие поля, только ДОБАВИТЬ breakdown:
```typescript
export interface ScenarioResult {
  cutPricePerUnit: number  // I
  profitPerUnit: number    // AA
  revenue: number          // AE = K×H
  profit: number           // AF = AA×H
  rePct: number            // AC (доля 0..1)
  roi: number              // AD (доля 0..1)
  // + breakdown: CostBreakdown   ← ДОБАВИТЬ
}
```

Текущий ArticleResult (types.ts:99) — ДОБАВИТЬ qtyOrders:
```typescript
export interface ArticleResult {
  nmId: number
  universe: Universe
  // + qtyOrders: number  ← ДОБАВИТЬ (H, для gross = perUnit×H в модалке)
  iu: ScenarioResult
  std: ScenarioResult
}
```

Внутренний ScenarioBreakdown (engine.ts:74) УЖЕ содержит все per-unit значения:
cutPricePerUnit, logisticsPerUnit, costPerUnit, adPerUnit, reviewPerUnit, defectPerUnit,
jemPerUnit, taxPerUnit, acquiringPerUnit, deliveryPerUnit, creditPerUnit, overheadPerUnit,
acceptancePerUnit, storagePerUnit, profitPerUnit. Не хватает только commissionPct (J) — добавить.

Таблица W2a (WeeklyFinReportTable.tsx) уже "use client"; buildRows() плющит в Row[]; строки артикулов
имеют kind==="article". `articles: ArticleResult[]` и `meta: Record<number,{brandName,productName}>`
уже в props — из них достаётся ArticleResult + meta по nmId для модалки.

shadcn Dialog (components/ui/dialog.tsx): Dialog(open,onOpenChange) / DialogContent(className) /
DialogHeader / DialogTitle / DialogDescription. base-ui, паттерн см. PricingCalculatorDialog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose per-unit breakdown из движка (types + engine + golden-тест)</name>
  <files>lib/finance-weekly/types.ts, lib/finance-weekly/engine.ts, tests/finance-weekly-engine.test.ts</files>
  <behavior>
    - out.articles[0].iu.breakdown.netOfCommissionPerUnit ≈ 8047.93 (K×(100−31.5)/100)
    - out.articles[0].iu.breakdown.taxPerUnit ≈ 939.9 (K×8%)
    - out.articles[0].iu.breakdown.acquiringPerUnit ≈ 337.19 (K×2.87%)
    - out.articles[0].std.breakdown.logisticsPerUnit ≈ 1380 (N_std)
    - out.articles[0].qtyOrders === 4 (H)
    - Все существующие ассерты (profitPerUnit, revenue, profit, cutPricePerUnit, водопад, guard одежды) остаются зелёными
  </behavior>
  <action>
    ADDITIVE-ONLY. Не менять формулы, не переименовывать существующие поля (profitPerUnit / cutPricePerUnit /
    revenue / profit / rePct / roi) — иначе golden-тест сломается.

    1) lib/finance-weekly/types.ts:
       - Добавить exported interface CostBreakdown (все ₽/ед, кроме commissionPct):
         ```typescript
         // Пооперационная per-unit разбивка одного сценария (строка Excel «Показатели»).
         // Все поля — ₽/ед, кроме commissionPct (%). Различаются ИУ vs Оферта только
         // commissionPct (J), netOfCommissionPerUnit (I) и logisticsPerUnit (N);
         // остальные (пулы + брак/джем/налог/эквайринг/закупка) идентичны в обоих сценариях.
         export interface CostBreakdown {
           pricePerUnit: number           // K — цена продажи / ед
           commissionPct: number          // J — комиссия % (различается ИУ/Оферта)
           netOfCommissionPerUnit: number // I = K×(100−J)/100 — цена минус комиссия / ед
           costPerUnit: number            // O — закупка / ед
           adPerUnit: number              // реклама / ед (L/H)
           reviewPerUnit: number          // списание за отзыв / ед (M/H)
           logisticsPerUnit: number       // N — логистика / ед (различается ИУ/Оферта)
           deliveryPerUnit: number        // доставка до МП / ед (пул P)
           creditPerUnit: number          // проценты по кредиту / ед (пул U, 0 для clothing)
           overheadPerUnit: number        // общие расходы / ед (пул W)
           acceptancePerUnit: number      // платная приёмка / штрафы / ед (пул Y)
           storagePerUnit: number         // хранение / ед (пул Z или override)
           defectPerUnit: number          // брак / ед (O×defectPct)
           jemPerUnit: number             // джем / ед (K×jemPct)
           taxPerUnit: number             // налог / ед (K×taxPct)
           acquiringPerUnit: number       // эквайринг / ед (K×acquiringPct)
         }
         ```
       - В ScenarioResult добавить строку `breakdown: CostBreakdown` (после roi).
       - В ArticleResult добавить строку `qtyOrders: number  // H` (после universe).

    2) lib/finance-weekly/engine.ts:
       - В internal interface ScenarioBreakdown (около строки 74) добавить `commissionPct: number // J`.
       - В computeScenario(...) в возвращаемый объект добавить `commissionPct: commPct` (commPct — уже параметр функции).
       - Импортировать тип CostBreakdown из "./types".
       - В toScenarioResult(article, b) собрать breakdown и вернуть его в ScenarioResult:
         ```typescript
         const breakdown: CostBreakdown = {
           pricePerUnit: K,
           commissionPct: b.commissionPct,
           netOfCommissionPerUnit: b.cutPricePerUnit,
           costPerUnit: b.costPerUnit,
           adPerUnit: b.adPerUnit,
           reviewPerUnit: b.reviewPerUnit,
           logisticsPerUnit: b.logisticsPerUnit,
           deliveryPerUnit: b.deliveryPerUnit,
           creditPerUnit: b.creditPerUnit,
           overheadPerUnit: b.overheadPerUnit,
           acceptancePerUnit: b.acceptancePerUnit,
           storagePerUnit: b.storagePerUnit,
           defectPerUnit: b.defectPerUnit,
           jemPerUnit: b.jemPerUnit,
           taxPerUnit: b.taxPerUnit,
           acquiringPerUnit: b.acquiringPerUnit,
         }
         ```
         (K уже деструктурируется в toScenarioResult как grossPricePerUnit). Добавить `breakdown` в возвращаемый объект ScenarioResult.
       - В computeWeeklyFinReport при articles.push добавить `qtyOrders: article.qtyOrders`:
         `articles.push({ nmId: article.nmId, universe, qtyOrders: article.qtyOrders, iu, std })`.

    3) tests/finance-weekly-engine.test.ts:
       - В describe golden добавить 3-4 новых it (НЕ удаляя существующие):
         ```typescript
         it("ИУ: breakdown.netOfCommissionPerUnit ≈ 8047.93", () => {
           expect(Math.abs(art.iu.breakdown.netOfCommissionPerUnit - 8047.93)).toBeLessThan(0.5)
         })
         it("ИУ: breakdown.taxPerUnit ≈ 939.9 и acquiringPerUnit ≈ 337.19", () => {
           expect(art.iu.breakdown.taxPerUnit).toBeCloseTo(939.9, 1)
           expect(art.iu.breakdown.acquiringPerUnit).toBeCloseTo(337.19, 1)
         })
         it("Оферта: breakdown.logisticsPerUnit ≈ 1380 (N_std)", () => {
           expect(art.std.breakdown.logisticsPerUnit).toBeCloseTo(1380, 6)
         })
         it("ArticleResult несёт qtyOrders=4 (H)", () => {
           expect(art.qtyOrders).toBe(4)
         })
         ```
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx vitest run tests/finance-weekly-engine.test.ts</automated>
  </verify>
  <done>vitest engine-тест зелёный (все старые + 4 новых ассерта); CostBreakdown exported; ScenarioResult.breakdown и ArticleResult.qtyOrders присутствуют; существующие поля/формулы не изменены.</done>
</task>

<task type="auto">
  <name>Task 2: WeeklyFinArticleDialog — read-only drill-down модалка</name>
  <files>components/finance/WeeklyFinArticleDialog.tsx</files>
  <action>
    Создать НОВЫЙ client-компонент. READ-ONLY: никаких input/form/server action — только чтение и рендер.
    Использовать shadcn Dialog из @/components/ui/dialog (образец структуры — PricingCalculatorDialog.tsx,
    но БЕЗ react-hook-form / useTransition / кнопок сохранения).

    Заголовок файла: комментарий-баннер (RU) + "use client".

    Props:
    ```typescript
    import type { ArticleResult } from "@/lib/finance-weekly/types"
    interface Props {
      open: boolean
      onOpenChange: (open: boolean) => void
      article: ArticleResult | null            // null → ничего не рендерим внутри (guard)
      meta: { brandName: string | null; productName: string }
    }
    ```
    Если article === null → всё равно рендерить <Dialog open={open} onOpenChange>, но с пустым/минимальным
    контентом (ранний return внутри после открытия Dialog не нужен — просто guard: `if (!article) return null`
    ВНУТРИ, обернув в Dialog не обязательно; проще — родитель монтирует Dialog всегда, но чтобы избежать
    обращения к article.* при null, сделать `{article && (...)}` вокруг тела DialogContent). Достаточно:
    рендерить Dialog всегда, тело — под `{article && ...}`.

    Форматтеры (локальные):
    - fmtRub2(n): n.toLocaleString("ru-RU",{minimumFractionDigits:2,maximumFractionDigits:2}) + guard !isFinite → "—"
    - fmtRub0(n): maximumFractionDigits:0
    - fmtPct(fraction): (fraction*100).toLocaleString("ru-RU",{maximumFractionDigits:1}) + "%"   // для rePct/roi (доля 0..1)
    - fmtPctRaw(pct): pct.toLocaleString("ru-RU",{maximumFractionDigits:2}) + "%"   // для commissionPct (уже %)
    - profitColor(n): n>0 emerald / n<0 red (как в WeeklyFinReportTable)

    Конфиг строк разбивки (порядок = Excel «Показатели»), key ∈ keyof CostBreakdown:
    ```typescript
    const ROWS: { label: string; key: keyof import("@/lib/finance-weekly/types").CostBreakdown; kind: "money" | "pct"; differs?: boolean }[] = [
      { label: "Цена", key: "pricePerUnit", kind: "money" },
      { label: "Комиссия %", key: "commissionPct", kind: "pct", differs: true },
      { label: "Цена минус комиссия", key: "netOfCommissionPerUnit", kind: "money", differs: true },
      { label: "Закупка", key: "costPerUnit", kind: "money" },
      { label: "Реклама", key: "adPerUnit", kind: "money" },
      { label: "Списание за отзыв", key: "reviewPerUnit", kind: "money" },
      { label: "Логистика", key: "logisticsPerUnit", kind: "money", differs: true },
      { label: "Доставка до МП", key: "deliveryPerUnit", kind: "money" },
      { label: "Кредит", key: "creditPerUnit", kind: "money" },
      { label: "Общие расходы", key: "overheadPerUnit", kind: "money" },
      { label: "Платная приёмка", key: "acceptancePerUnit", kind: "money" },
      { label: "Хранение", key: "storagePerUnit", kind: "money" },
      { label: "Брак", key: "defectPerUnit", kind: "money" },
      { label: "Джем 1%", key: "jemPerUnit", kind: "money" },
      { label: "Налог", key: "taxPerUnit", kind: "money" },
      { label: "Эквайринг", key: "acquiringPerUnit", kind: "money" },
    ]
    ```

    Разметка DialogContent (className="sm:max-w-2xl max-h-[92vh] overflow-y-auto"):
    - DialogHeader:
        DialogTitle → meta.productName (fallback String(article.nmId))
        DialogDescription → `Артикул: {article.nmId} · Бренд: {meta.brandName ?? "—"} · Заказов: {article.qtyOrders} · Цена: {fmtRub2(article.iu.breakdown.pricePerUnit)} ₽`
    - Таблица разбивки (обычный <table>, НЕ sticky — контент короткий, ~16 строк):
        thead: колонки «Статья | ИУ, ₽/ед | Оферта, ₽/ед»
        tbody: ROWS.map → <tr>:
          - td label
          - td ИУ: kind==="pct" ? fmtPctRaw(article.iu.breakdown[key]) : fmtRub2(article.iu.breakdown[key])
          - td Оферта: аналогично из article.std.breakdown[key]
          - если row.differs → подсветить всю строку классом bg-amber-50 dark:bg-amber-500/10 (highlight отличий)
        Числовые ячейки: text-right tabular-nums. Опционально: под таблицей мелкий note
        «× {article.qtyOrders} = валовая сумма за неделю» (без отдельной колонки — достаточно подписи).
    - Футер-блок (обычный div, НЕ DialogFooter с кнопками сохранения) с итогами обоих сценариев,
      две колонки (ИУ | Оферта) или таблица со строками:
        Прибыль / ед  → iu.profitPerUnit / std.profitPerUnit (profitColor, differs → подсветка)
        Выручка       → iu.revenue / std.revenue (fmtRub0; одинаковы)
        Прибыль       → iu.profit / std.profit (fmtRub0, profitColor)
        Re продаж %   → fmtPct(iu.rePct) / fmtPct(std.rePct)
        ROI %         → fmtPct(iu.roi) / fmtPct(std.roi)
      Блок обернуть в bg-muted/50 p-3 rounded-md (не sticky — это модалка, alpha OK).

    Никаких server action, никакого router.refresh, никакого редактирования. Компонент чисто presentational.
    Экспорт: `export function WeeklyFinArticleDialog(props: Props)`.
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx tsc --noEmit</automated>
  </verify>
  <done>Файл создан, "use client", tsc clean; модалка read-only (нет form/action/input); рендерит header + 16-строчную breakdown-таблицу (ИУ/Оферта) + футер с Прибыль/ед, Выручка, Прибыль, Re, ROI; строки differs (комиссия/цена минус комиссия/логистика/прибыль) подсвечены.</done>
</task>

<task type="auto">
  <name>Task 3: Wire — кликабельные строки артикулов в WeeklyFinReportTable</name>
  <files>components/finance/WeeklyFinReportTable.tsx</files>
  <action>
    Сделать строки артикулов кликабельными → открывать WeeklyFinArticleDialog. Сохранить существующий
    рендер (роллап + водопад + подытоги/итого) БЕЗ изменений в числах/структуре.

    1) Импортировать: `import { useState } from "react"` и
       `import { WeeklyFinArticleDialog } from "@/components/finance/WeeklyFinArticleDialog"`.

    2) В interface Row добавить `nmId?: number` (только у строк kind==="article").
       В buildRows() при push артикульной строки добавить `nmId: a.nmId`.

    3) В компоненте WeeklyFinReportTable:
       - Добавить state:
         ```typescript
         const [open, setOpen] = useState(false)
         const [selectedNmId, setSelectedNmId] = useState<number | null>(null)
         ```
       - Derived: `const selectedArticle = selectedNmId == null ? null : articles.find((a) => a.nmId === selectedNmId) ?? null`
       - Derived meta: `const selectedMeta = selectedNmId == null ? { brandName: null, productName: "" } : (meta[selectedNmId] ?? { brandName: null, productName: String(selectedNmId) })`
       - На article-строке (<tr>): при `row.kind === "article" && row.nmId != null`:
           добавить className "cursor-pointer" (к существующему hover:bg-muted/20 transition-colors)
           и `onClick={() => { setSelectedNmId(row.nmId!); setOpen(true) }}`.
         Строки kind universe/brand/subtotal/grand — БЕЗ onClick и БЕЗ cursor-pointer (не кликабельны).
       - Смонтировать модалку один раз (вне таблицы, внутри верхнего фрагмента/обёртки):
         ```tsx
         <WeeklyFinArticleDialog
           open={open}
           onOpenChange={setOpen}
           article={selectedArticle}
           meta={selectedMeta}
         />
         ```
         Разместить перед закрытием внешнего <div className="flex flex-col gap-4"> (после блока «Водопад затрат»).

    4) Не трогать: sticky-паттерн, сплошные bg-* на sticky-ячейках, водопад, подытоги/итого, форматтеры.
       Клик по label-ячейке артикула не должен конфликтовать со sticky — onClick на <tr> достаточно.
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx tsc --noEmit</automated>
  </verify>
  <done>tsc clean; строки артикулов имеют cursor-pointer + onClick, открывают WeeklyFinArticleDialog для своего nmId; universe/brand/subtotal/grand НЕ кликабельны; водопад/подытоги/итого не изменены.</done>
</task>

</tasks>

<verification>
Финальные гейты (запускать из корня проекта c:/Users/User/zoiten-pro):

1. Типы: `npx tsc --noEmit` — чисто (0 ошибок).
2. Юнит-тесты (движок + pricing неизменён):
   `npx vitest run tests/finance-weekly-engine.test.ts tests/pricing-math.test.ts` — зелёные
   (golden + 4 новых breakdown-ассерта; pricing-math не тронут).
3. Полный `npm run test` — ~42 ИЗВЕСТНЫХ pre-existing НЕ связанных падения. Подтвердить, что они
   не относятся к finance-weekly / pricing-math и НЕ чинить их. finance-weekly-engine.test.ts и
   pricing-math.test.ts обязаны быть зелёными.
4. Ручная проверка контракта (grep): в WeeklyFinReportTable.tsx присутствует WeeklyFinArticleDialog,
   onClick только на article-строках; в types.ts — `export interface CostBreakdown`.
</verification>

<success_criteria>
- lib/finance-weekly/types.ts: CostBreakdown exported; ScenarioResult.breakdown; ArticleResult.qtyOrders — additive, существующие поля не изменены.
- lib/finance-weekly/engine.ts: breakdown заполняется для обоих сценариев из уже посчитанных per-unit значений; qtyOrders проброшен.
- tests/finance-weekly-engine.test.ts: все прежние ассерты зелёные + 4 новых на breakdown/qtyOrders.
- components/finance/WeeklyFinArticleDialog.tsx: read-only модалка (header + breakdown-таблица ИУ/Оферта + футер Прибыль/ед/Выручка/Прибыль/Re/ROI), differs-строки подсвечены.
- components/finance/WeeklyFinReportTable.tsx: строки артикулов кликабельны → модалка; заголовочные/итоговые строки не кликабельны; W2a-функционал (водопад/подытоги/итого) нетронут.
- `npx tsc --noEmit` чисто; `npx vitest run tests/finance-weekly-engine.test.ts tests/pricing-math.test.ts` зелёные.
- Атомарный коммит `git add -A` + push origin/main. DEPLOY (UI-change) выполняет ОРКЕСТРАТОР после верификации — исполнитель только коммитит и пушит.
</success_criteria>

<output>
После завершения создать `.planning/quick/260710-fr1-w2b-drill-down-finance-weekly-breakdown-/260710-fr1-SUMMARY.md`
с кратким итогом (изменённые файлы, что добавлено, результаты гейтов).
</output>
