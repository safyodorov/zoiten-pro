---
phase: quick-260710-e7h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-weekly/types.ts
  - lib/finance-weekly/engine.ts
  - tests/finance-weekly-engine.test.ts
autonomous: true
requirements: [WFR-ENGINE]

must_haves:
  truths:
    - "computeWeeklyFinReport возвращает per-article юнит-экономику в ДВУХ сценариях (ИУ и Оферта), совпадающую с Excel-эталоном nmId 165967746 в пределах ±0.5 ₽"
    - "Пуловые затраты распределяются пропорционально доле выручки: poolPerUnit = (K / baseRevenue) × poolTotal"
    - "Одежда (clothing) НИКОГДА не получает проценты по кредиту (creditPerUnit=0), даже если пул кредита передан во входе"
    - "Движок PURE: не импортирует Prisma/React/Next и НЕ импортирует calculatePricing/calculatePricingStandard"
    - "Существующие тесты (pricing-math, sales-plan-engine) остаются зелёными — движок изолирован"
  artifacts:
    - path: "lib/finance-weekly/types.ts"
      provides: "Input/output интерфейсы движка + дефолтные константы"
      min_lines: 40
    - path: "lib/finance-weekly/engine.ts"
      provides: "computeWeeklyFinReport(inputs) + отдельный pure-хелпер распределения пула"
      exports: ["computeWeeklyFinReport", "poolPerUnit"]
      min_lines: 80
    - path: "tests/finance-weekly-engine.test.ts"
      provides: "Golden test (3 теста): per-article дуал-сценарий + pool-distribution + clothing credit guard"
      contains: "165967746"
  key_links:
    - from: "lib/finance-weekly/engine.ts"
      to: "lib/finance-weekly/types.ts"
      via: "import type { ... } from './types'"
      pattern: "from ['\"]\\./types['\"]"
    - from: "tests/finance-weekly-engine.test.ts"
      to: "lib/finance-weekly/engine.ts"
      via: "import { computeWeeklyFinReport, poolPerUnit } from '@/lib/finance-weekly/engine'"
      pattern: "@/lib/finance-weekly/engine"
    - from: "lib/finance-weekly/engine.ts"
      to: "НЕ импортировать pricing-math"
      via: "запрет импорта calculatePricing"
      pattern: "pricing-math"
---

<objective>
Построить PURE-движок расчёта понедельного WB фин-отчёта (4-я таблица `/finance`, §4.3 дизайн-спеки) — ИЗОЛИРОВАННЫЙ чанк: только чистые функции + типы + golden-тест. БЕЗ UI, БЕЗ Prisma-схемы, БЕЗ проводки данных, БЕЗ WB API.

Движок воспроизводит per-article (nmId) недельную юнит-экономику из Excel-листа «Показатели» в ДВУХ сценариях комиссии одновременно (ИУ и Оферта), с распределением недельных затратных пулов пропорционально выручке, в ДВУХ непересекающихся мирах затрат (appliances=бытовая / clothing=одежда).

Purpose: единая детерминированная source-of-truth расчёта, тестируемая в изоляции; слой страницы (позже) поставляет фактические данные и оценки логистики/хранения на вход.
Output: `lib/finance-weekly/types.ts`, `lib/finance-weekly/engine.ts`, `tests/finance-weekly-engine.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/superpowers/specs/2026-07-08-weekly-finreport-design.md

# Образцы стиля/конвенций (читать для стиля, НЕ импортировать логику)
@tests/pricing-math.test.ts
@lib/sales-plan/types.ts

<interfaces>
<!-- Конвенции проекта, извлечённые из образцов. Исполнитель использует их напрямую. -->

Стиль pure-модуля (образец lib/sales-plan/types.ts, lib/pricing-math.ts):
- Русские комментарии, TypeScript strict, ZERO импортов Prisma/React/Next.
- Входы сериализуемые (number/string/boolean/null; без Date/Decimal).
- Zero-guards: делители проверять `x > 0 ? a/x : 0` (без NaN/Infinity).

vitest (config уже есть, vitest.config.ts): alias `@` → корень проекта, pool="vmForks",
include "tests/**/*.test.ts". Запуск: `npm run test` (= `vitest run`).
Стиль golden-теста — зеркалить tests/pricing-math.test.ts:
  import { describe, it, expect } from "vitest"
  toBeCloseTo(expected, digits) — для ±0.5 ₽ достаточно digits=0 (|Δ|<0.5) ИЛИ явная проверка Math.abs(a-b) < 0.5.

Дизайн-спека — ключевые разделы (уже прочитаны):
- §2 таблица формул листа «Показатели» (I, K, Q/R, S/T/V/X, P/U/W/Y/Z, AA, AE/AF, AC/AD).
- §2.1 пулы недели (у КАЖДОГО пула СВОЯ база распределения — не хардкодить).
- §2.2 ДВА непересекающихся мира затрат; у одежды НЕТ пула кредита (U=0).
- §7 п.6 базы распределения как в Excel (per-pool), НЕ упрощать.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: типы + движок lib/finance-weekly/ (pure)</name>
  <files>lib/finance-weekly/types.ts, lib/finance-weekly/engine.ts</files>
  <behavior>
    Golden nmId 165967746 (appliances, H=4, K=11748.8, O=4339, L=4015, M=0):
    - ИУ (commIuPct=31.5, N_iu=0): I≈8047.93, adPerUnit=1003.75, brak=86.78, jem=117.49,
      tax=939.9, acq=337.19, profitPerUnit≈130.90, profit≈523.6, revenue=46995.2.
    - Оферта (commStdPct=25.5, N_std=1380): I≈8752.86, profitPerUnit≈−544.18, profit≈−2176.7.
    - poolPerUnit(K=11748.8, baseRevenue=17614883, poolTotal=262300) ≈ 175.
    - clothing article → creditPerUnit=0 даже если пул кредита передан.
  </behavior>
  <action>
    Создать `lib/finance-weekly/types.ts` — все входные/выходные интерфейсы + дефолтные константы.

    Определить (имена/поля — на усмотрение исполнителя, но покрыть весь контракт):
    - `Universe = "appliances" | "clothing"`.
    - `WeeklyArticleInput`: { nmId:number, universe:Universe, qtyOrders:number (H), grossPricePerUnit:number (K),
      commIuPct:number, commStdPct:number, costPerUnit:number (O), adSpendTotal:number (L),
      reviewWriteoffTotal:number (M), logisticsIuPerUnit:number (N_iu), logisticsStdPerUnit:number (N_std),
      storagePerUnit?:number (опциональный per-article override хранения) }.
    - `WeeklyPool`: { total:number, baseRevenue:number } — пул несёт СВОЮ базу (§2.1/§7 п.6).
    - `UniversePools`: { deliveryToMp:WeeklyPool, creditInterest:WeeklyPool, overhead:WeeklyPool,
      acceptance:WeeklyPool, storage:WeeklyPool }. Для clothing creditInterest НЕ распределяется (см. guard в engine).
    - `WeeklyConstants`: { taxPct:number, jemPct:number, defectPct:number, acquiringPct:number }.
    - `DEFAULT_WEEKLY_CONSTANTS`: taxPct=8, jemPct=1, defectPct=2, acquiringPct=2.87
      (Excel-значение эквайринга 2.87%, НЕ 2.7% — по §7 п.4; overridable через input.constants).
    - `WeeklyFinReportInputs`: { articles:WeeklyArticleInput[], pools:{ appliances:UniversePools, clothing:UniversePools },
      constants?:Partial<WeeklyConstants> }.
    - `ScenarioResult`: { profitPerUnit, revenue, profit, rePct, roi }.
    - `ArticleResult`: { nmId, universe, iu:ScenarioResult, std:ScenarioResult }.
    - Роллап: Σ per universe И grand total, отдельно для iu и std (Прибыль/Re/Выручка).
    - Водопад затрат per сценарий (iu & std): Σ каждого бакета × H —
      закупка (O·H), реклама (adPerUnit·H), отзывы (reviewPerUnit·H), логистика (N·H, где N=N_iu для iu, N_std для std),
      доставка, кредит, общие, приёмка, хранение, брак, джем, налог, эквайринг.
      (Логистика различается по сценариям → водопад ведётся отдельно для iu и std.)
    - `WeeklyFinReportOutput`: { articles:ArticleResult[], rollup, waterfall }.

    Создать `lib/finance-weekly/engine.ts` — PURE, `import type { ... } from "./types"` (ТОЛЬКО типы).
    ⚠ ЗАПРЕЩЕНО: любой import из `@/lib/pricing-math`, Prisma, React, Next. Модель другая
    (там per-product overrides) — движок остаётся data-agnostic, N_std и хранение приходят как ВХОД.

    Экспортировать ОТДЕЛЬНЫЙ pure-хелпер распределения:
      `export function poolPerUnit(grossPricePerUnit:number, baseRevenue:number, poolTotal:number): number`
      → `baseRevenue > 0 ? (grossPricePerUnit / baseRevenue) * poolTotal : 0`.

    Экспортировать `export function computeWeeklyFinReport(inputs:WeeklyFinReportInputs): WeeklyFinReportOutput`.
    Per article, для КАЖДОГО сценария (J = commIuPct для iu, commStdPct для std; N = N_iu для iu, N_std для std):
      const c = { ...DEFAULT_WEEKLY_CONSTANTS, ...inputs.constants }
      I = K*(100−J)/100
      adPerUnit    = H>0 ? L/H : 0
      reviewPerUnit= H>0 ? M/H : 0
      brak = O*c.defectPct/100 ; jem = K*c.jemPct/100 ; tax = K*c.taxPct/100 ; acq = K*c.acquiringPct/100
      pools = inputs.pools[article.universe]
      deliveryPerUnit   = poolPerUnit(K, pools.deliveryToMp.baseRevenue,   pools.deliveryToMp.total)
      creditPerUnit     = article.universe === "clothing" ? 0
                          : poolPerUnit(K, pools.creditInterest.baseRevenue, pools.creditInterest.total)   // ⚠ GUARD: кредит только для appliances (§2.2)
      overheadPerUnit   = poolPerUnit(K, pools.overhead.baseRevenue,   pools.overhead.total)
      acceptancePerUnit = poolPerUnit(K, pools.acceptance.baseRevenue, pools.acceptance.total)
      storagePerUnit    = article.storagePerUnit ?? poolPerUnit(K, pools.storage.baseRevenue, pools.storage.total)
      profitPerUnit = I − N − O − adPerUnit − reviewPerUnit − brak − jem − tax − acq
                        − deliveryPerUnit − creditPerUnit − overheadPerUnit − acceptancePerUnit − storagePerUnit
      revenue = K*H ; profit = profitPerUnit*H
      rePct = revenue>0 ? profit/revenue : 0 ; roi = (O*H)>0 ? profit/(O*H) : 0
    Полная точность внутри — НЕ пред-округлять промежуточные значения.
    Затем собрать rollup (Σ per universe + grand total, iu & std) и waterfall (Σ бакетов × H, iu & std).
    Русские комментарии в шапке файла (см. образец lib/sales-plan/engine.ts): назначение, «pure/детерминированный»,
    ссылка на golden-тест tests/finance-weekly-engine.test.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Оба файла существуют; `npx tsc --noEmit` чист; engine.ts НЕ содержит строки "pricing-math",
    экспортирует `computeWeeklyFinReport` и `poolPerUnit`; импортирует только типы из "./types".
  </done>
</task>

<task type="auto">
  <name>Task 2: golden-тест tests/finance-weekly-engine.test.ts + прогон гейтов</name>
  <files>tests/finance-weekly-engine.test.ts</files>
  <action>
    Создать `tests/finance-weekly-engine.test.ts` в стиле tests/pricing-math.test.ts
    (`import { describe, it, expect } from "vitest"`; import из `@/lib/finance-weekly/engine` и типов из `@/lib/finance-weekly/types`).
    Толеранс ±0.5 ₽ — использовать `toBeCloseTo(x, 0)` ИЛИ `expect(Math.abs(a−b)).toBeLessThan(0.5)`.

    ТЕСТ 1 — Golden per-article, дуал-сценарий (nmId 165967746 «Массажер для ног 250», appliances, H=4, K=11748.8, O=4339, L=4015, M=0).
    Пулы задать так, чтобы per-unit разрешились в ТОЧНЫЕ значения: deliveryPerUnit=175, creditPerUnit=365.1,
    overheadPerUnit=542.05, acceptancePerUnit=10.77, storagePerUnit=0. Т.к. poolPerUnit=(K/baseRevenue)*total,
    достаточно на каждый пул задать baseRevenue=K и total=нужный perUnit (тогда perUnit=total), напр.
    deliveryToMp={ total:175, baseRevenue:11748.8 } → 175. (storage: total:0 или передать article.storagePerUnit=0.)
    Собрать WeeklyFinReportInputs с одним article (commIuPct=31.5, logisticsIuPerUnit=0, commStdPct=25.5, logisticsStdPerUnit=1380).
    Проверить ArticleResult:
      iu:  profitPerUnit≈130.90 (±0.5), profit≈523.6 (±0.5), revenue=46995.2.
      std: profitPerUnit≈−544.18 (±0.5), profit≈−2176.7 (±0.5).
    (Опц. промежуточные: I_iu≈8047.93, I_std≈8752.86 — если движок их выставляет наружу; иначе достаточно profit.)

    ТЕСТ 2 — pool-distribution хелпер В ИЗОЛЯЦИИ (доказывает revenue-share):
      expect(poolPerUnit(11748.8, 17614883, 262300)).toBeCloseTo(175, 0)  // |Δ|<0.5, факт ≈174.95
    + guard: `poolPerUnit(100, 0, 999)` === 0 (baseRevenue=0 не даёт Infinity).

    ТЕСТ 3 — clothing credit guard: article с universe:"clothing", pools.clothing.creditInterest задан
    НЕнулевым (total>0, baseRevenue>0) → в результате creditPerUnit НЕ применяется (кредит не вычитается).
    Проверить косвенно: profit одежды при заданном creditInterest РАВЕН profit при creditInterest.total=0
    (т.е. кредит для clothing игнорируется). Либо, если движок отдаёт per-unit разбивку — creditPerUnit===0.

    После написания — прогнать полный набор: существующие golden'ы (pricing-math, sales-plan-engine и др.)
    ДОЛЖНЫ остаться зелёными (движок изолирован, ничего общего не трогает).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>
    `npm run test` зелёный: 3 новых теста проходят (golden ±0.5 ₽, pool-distribution ≈175, clothing credit=0)
    И весь существующий набор (pricing-math, sales-plan-engine, balance-math и т.д.) НЕ сломан.
    Коммит lib/ + tests/ + push в origin/main (docs/lib-only, runtime не меняется). БЕЗ деплоя (pure lib).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — чист (strict, весь проект компилируется).
- `npm run test` — 3 новых теста зелёные + весь существующий набор не сломан.
- `git grep -n "pricing-math" lib/finance-weekly/engine.ts` — пусто (движок НЕ импортирует pricing-math).
- Изоляция: `git grep -nE "from ['\"]@?/?(lib/prisma|next|react)" lib/finance-weekly/` — пусто.
</verification>

<success_criteria>
- lib/finance-weekly/types.ts + engine.ts + tests/finance-weekly-engine.test.ts созданы.
- computeWeeklyFinReport воспроизводит Excel-эталон nmId 165967746 в обоих сценариях в пределах ±0.5 ₽.
- poolPerUnit доказывает revenue-share распределение (≈175 при K=11748.8 / base=17614883 / pool=262300).
- Одежда не получает кредит (guard).
- Движок PURE (без Prisma/React/Next/pricing-math). Существующие тесты зелёные.
- Коммит + push в origin/main. Деплой НЕ выполняется (pure lib).
</success_criteria>

<output>
После завершения создать `.planning/quick/260710-e7h-lib-finance-weekly-golden/260710-e7h-SUMMARY.md`
</output>
