---
phase: quick-260708-h9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/prices/PriceCalculatorTable.tsx
autonomous: true
requirements:
  - QUICK-260708-h9l
must_haves:
  truths:
    - "На /prices/wb в таблице после колонки ROI, % идут 3 новые колонки: Логистика МП-std, Хранение-std, Возврат прод.-std, затем Прибыль-std / ROI-std / Re-std"
    - "Значения в новых колонках берутся из row.computedStd (logisticsEffAmount / storageAmount / returnToSellerAmount) и форматируются как целые рубли"
    - "Новые колонки видимы по умолчанию и скрываются через кнопку «Вид»"
    - "Число ячеек thead === число td tbody (таблица не разъезжается)"
  artifacts:
    - path: "components/prices/PriceCalculatorTable.tsx"
      provides: "3 новые std-компонентные колонки в таблице юнит-экономики"
      contains: "logisticsEffStd"
  key_links:
    - from: "SCROLL_COLUMNS (thead)"
      to: "render-row массив (tbody)"
      via: "идентичный порядок ColumnKey"
      pattern: "logisticsEffStd.*storageStd.*returnToSellerStd"
    - from: "render-row массив"
      to: "row.computedStd"
      via: "logisticsEffAmount / storageAmount / returnToSellerAmount ?? 0"
      pattern: "row\\.computedStd\\?\\.logisticsEffAmount"
---

<objective>
Добавить в таблицу `/prices/wb` 3 столбца компонентов стандартного расчёта — «Логистика МП-std, руб.», «Хранение-std, руб.», «Возврат прод.-std, руб.» — рядом с уже существующими итоговыми std-столбцами (Прибыль-std / ROI-std / Re-std).

Purpose: пользователь видит из чего складывается фин-рез на стандартных условиях (расходные компоненты), а не только итоги.
Output: изменённый `components/prices/PriceCalculatorTable.tsx` (единственный файл), задеплоено на прод.

**Значения уже посчитаны** в `row.computedStd` (`PricingOutputs.logisticsEffAmount / storageAmount / returnToSellerAmount`, поля подтверждены в `lib/pricing-math.ts:160-166`). Задача — только вывести колонки. НЕ трогать `page.tsx`, `lib/pricing-math.ts`, модалку, первый (ИУ) блок колонок.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Поля уже существуют в lib/pricing-math.ts (PricingOutputs, строки 150-167). Только читаем. -->
PricingOutputs (опциональные Фаза-B поля, заполняет calculatePricingStandard):
```typescript
storageAmount?: number          // Хранение на проданную единицу, ₽
logisticsEffAmount?: number      // Логистика эффективная (выкуп + амортизация возврата), ₽
returnToSellerAmount?: number    // Возврат продавцу брака, ₽
```
На строке таблицы доступны через `row.computedStd?.<field>` (defensive-optional, page.tsx всегда заполняет).
</interfaces>

<current_structure>
<!-- Все правки — в components/prices/PriceCalculatorTable.tsx. Опорные точки (могут сдвинуться на ±пару строк): -->

1. COLUMN_KEYS (~269-307) — union-массив всех колонок. Хвост:
   `..."profit", "returnOnSalesPct", "roiPct", "profitStd", "roiPctStd", "returnOnSalesPctStd"`
   Вставка 3 ключей между `"roiPct"` и `"profitStd"`.

2. DEFAULT_WIDTHS (~313-348) — Record<ColumnKey, number>. Соседние amount-колонки: deliveryAmount:100, creditAmount:95, profitStd:100.

3. HIDEABLE_COLUMN_KEYS (~357-387) — массив скрываемых. Хвост: `..."roiPct", "profitStd", "roiPctStd", "returnOnSalesPctStd"`.

4. SCROLL_COLUMNS (~391-422) — [{key,label}] для thead. Хвост:
   `{ key: "roiPct", label: "ROI, %" }, { key: "profitStd", label: "Прибыль-std, руб." }, ...`
   Вставка 3 объектов ПЕРЕД `{ key: "profitStd" }`.

5. Render-row массив в tbody (~1360-1393) — `[ColumnKey, string, string?][]`. Порядок td СТРОГО === SCROLL_COLUMNS.
   Пример нейтральной amount-ячейки (БЕЗ третьего элемента → нейтральный CELL_CLASS):
   `["deliveryAmount", fmtMoneyInt(row.computed.deliveryAmount)],`
   Пример profit-ячейки (с profitClass — НЕ использовать для новых):
   `["profitStd", fmtMoneyInt(row.computedStd?.profitStd ?? 0), profitClass(row.computedStd?.profitStd ?? 0)],`
   Вставка 3 строк ПЕРЕД `["profitStd", ...]`.
   `.filter(([k]) => !hiddenColumns.has(k))` уже подхватит новые ключи автоматически.
</current_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить 3 std-компонентные колонки в PriceCalculatorTable + задеплоить</name>
  <files>components/prices/PriceCalculatorTable.tsx</files>
  <action>
Открыть `components/prices/PriceCalculatorTable.tsx`. Внести РОВНО 5 правок (все в порядке: …roiPct → **logisticsEffStd → storageStd → returnToSellerStd** → profitStd → roiPctStd → returnOnSalesPctStd).

**Правка 1 — COLUMN_KEYS (~302).** Между строкой `"roiPct",` и комментарием `// Фаза B (2026-07-07)...` / `"profitStd",` вставить:
```
  "logisticsEffStd",
  "storageStd",
  "returnToSellerStd",
```

**Правка 2 — DEFAULT_WIDTHS (~344).** Между `roiPct: 80,` и `profitStd: 100,` вставить (ширина как у соседних amount-колонок):
```
  logisticsEffStd: 120,
  storageStd: 110,
  returnToSellerStd: 130,
```

**Правка 3 — HIDEABLE_COLUMN_KEYS (~383).** Между `"roiPct",` и `"profitStd",` вставить (видимы по умолчанию — в default-hidden их НЕ добавляем, значит просто присутствие в HIDEABLE делает их скрываемыми, но не скрытыми):
```
  "logisticsEffStd",
  "storageStd",
  "returnToSellerStd",
```

**Правка 4 — SCROLL_COLUMNS (~419, thead).** ПЕРЕД `{ key: "profitStd", label: "Прибыль-std, руб." },` вставить:
```
  { key: "logisticsEffStd", label: "Логистика МП-std, руб." },
  { key: "storageStd", label: "Хранение-std, руб." },
  { key: "returnToSellerStd", label: "Возврат прод.-std, руб." },
```

**Правка 5 — render-row массив в tbody (~1390).** ПЕРЕД строкой `["profitStd", fmtMoneyInt(row.computedStd?.profitStd ?? 0), profitClass(...)],` вставить 3 НЕЙТРАЛЬНЫЕ amount-ячейки (БЕЗ третьего элемента / без profitClass — как `deliveryAmount`; nullable-safe `?? 0`, без non-null `!`):
```
                        ["logisticsEffStd", fmtMoneyInt(row.computedStd?.logisticsEffAmount ?? 0)],
                        ["storageStd", fmtMoneyInt(row.computedStd?.storageAmount ?? 0)],
                        ["returnToSellerStd", fmtMoneyInt(row.computedStd?.returnToSellerAmount ?? 0)],
```
(Отступ = как у соседних элементов массива, ~24 пробела.)

**Инвариант (проверить глазами перед сборкой):** число элементов SCROLL_COLUMNS === число элементов render-row массива === +3 к прежнему. Порядок 3 новых ключей ОДИНАКОВ в COLUMN_KEYS / DEFAULT_WIDTHS(как Record — порядок не важен, но все 3 ключа должны присутствовать) / HIDEABLE / SCROLL_COLUMNS / render-row. Иначе thead и tbody разъедутся.

НЕ трогать: `page.tsx`, `lib/pricing-math.ts`, `PricingCalculatorDialog`, первый (ИУ) блок non-std колонок, sticky-колонки (photo/svodka/yarlyk/artikul).

**После правок — гейты и деплой (деплой ДЕЛЕГИРОВАН, финальный шаг):**
1. `npx tsc --noEmit` — 0 ошибок (DEFAULT_WIDTHS покрывает все ключи ColumnKey → компилятор поймает, если ключ забыт).
2. `npm run test` — golden pricing-math + sales-plan зелёные (регресс; логику не меняли).
3. Деплой (правила CLAUDE.md «Правила деплоя»):
   - `ssh root@85.198.97.89 "df -h /"` → минимум 5GB свободно.
   - `git add -A && git commit -m "feat(prices/wb): std-компоненты — Логистика МП / Хранение / Возврат прод. (3 столбца)"` (заканчивая строкой Co-Authored-By как обычно).
   - `git push origin main`.
   - Деплой ТОЛЬКО detached: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && nohup bash deploy.sh > /var/log/zoiten-deploy.log 2>&1 &"`.
   - Следить за логом до `==> Done`: `ssh root@85.198.97.89 "tail -f /var/log/zoiten-deploy.log"`.
   - `curl -so /dev/null -w "%{http_code}" https://zoiten.pro` → `200`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test</automated>
  </verify>
  <done>
- `npx tsc --noEmit` — 0 ошибок; `npm run test` — все тесты зелёные (golden pricing-math nmId 800750522 + sales-plan без изменений).
- В `components/prices/PriceCalculatorTable.tsx`: 3 ключа `logisticsEffStd/storageStd/returnToSellerStd` присутствуют в COLUMN_KEYS, DEFAULT_WIDTHS, HIDEABLE_COLUMN_KEYS, SCROLL_COLUMNS и render-row массиве — все между `roiPct` и `profitStd`.
- Число элементов SCROLL_COLUMNS === render-row массива (thead/tbody совпадают).
- Render-ячейки читают `row.computedStd?.logisticsEffAmount/storageAmount/returnToSellerAmount ?? 0` через `fmtMoneyInt`, нейтральный класс (без profitClass), без non-null assertion.
- Задеплоено: коммит запушен в origin/main, deploy.sh дошёл до `==> Done`, `curl https://zoiten.pro` → 200.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — типы целы (Record<ColumnKey,number> заставит объявить ширины всех новых ключей).
- `npm run test` — регресс pricing-math golden + sales-plan (движок расчёта не менялся).
- Прод: `curl https://zoiten.pro` → 200; на `/prices/wb` после «ROI, %» видны 3 новые колонки перед «Прибыль-std».
</verification>

<success_criteria>
- 3 новые колонки «Логистика МП-std / Хранение-std / Возврат прод.-std» отображаются на `/prices/wb` в правильном порядке (после ROI, перед Прибыль-std), с целочисленными рублёвыми значениями из `row.computedStd`.
- Колонки видимы по умолчанию, управляются кнопкой «Вид», таблица не разъезжается (thead === tbody).
- Изменён ровно один файл; тесты и tsc зелёные; задеплоено на прод (200).
</success_criteria>

<output>
После завершения создать `.planning/quick/260708-h9l-std-prices-wb/260708-h9l-SUMMARY.md`.
</output>
