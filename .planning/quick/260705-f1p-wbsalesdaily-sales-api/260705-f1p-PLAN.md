---
phase: quick-260705-f1p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260705_wb_sales_daily/migration.sql
  - lib/wb-api.ts
  - tests/wb-sales-daily.test.ts
  - app/api/cron/wb-sales-daily/route.ts
  - app/api/cron/dispatch/route.ts
  - lib/sales-plan/data.ts
  - app/(dashboard)/sales-plan/page.tsx
  - components/sales-plan/PlanFactMatrix.tsx
  - components/sales-plan/PlanFactSummaryCards.tsx
  - components/sales-plan/PlanFactControls.tsx
autonomous: true
requirements: [QUICK-260705-f1p]

must_haves:
  truths:
    - "Есть источник дневного факта выкупов по ДАТЕ РЕАЛИЗАЦИИ (не по когорте заказа)"
    - "WbSalesDaily заполняется из Statistics Sales API cron'ом ~04:30 МСК + ручной backfill ?days=30"
    - "Сводный таб /sales-plan (KPI «Факт за период», ИУ-сравнение, «Вне плана», график, ряд «факт») считается по WbSalesDaily, а не по WbCardFunnelDaily.buyoutsSumRub"
    - "Воронка (WbCardFunnelDaily) и per-товар ячейки Товаров не тронуты"
    - "Golden ИУ 438 068 120 ₽, iuMetric='buyouts', 118 существующих sales-plan тестов остаются зелёными"
  artifacts:
    - path: "prisma/migrations/20260705_wb_sales_daily/migration.sql"
      provides: "Таблица WbSalesDaily (аддитивная миграция)"
      contains: "CREATE TABLE"
    - path: "lib/wb-api.ts"
      provides: "fetchSalesDaily + aggregateSalesRows"
      contains: "fetchSalesDaily"
    - path: "app/api/cron/wb-sales-daily/route.ts"
      provides: "Sync-роут WbSalesDaily (clean-replace per date-окно)"
      exports: ["GET"]
    - path: "tests/wb-sales-daily.test.ts"
      provides: "Pure-тест агрегатора продаж"
      contains: "aggregateSalesRows"
  key_links:
    - from: "app/api/cron/dispatch/route.ts"
      to: "app/api/cron/wb-sales-daily/route.ts"
      via: "dynamic import GET + shouldFireCron(wbSalesDailyCronTime)"
      pattern: "wb-sales-daily/route"
    - from: "lib/sales-plan/data.ts"
      to: "prisma.wbSalesDaily"
      via: "loadFactDaily redemption-разрез"
      pattern: "wbSalesDaily"
    - from: "app/(dashboard)/sales-plan/page.tsx"
      to: "loadFactDaily redemption"
      via: "companyFactDays/factDays = redemption"
      pattern: "redemption"
---

<objective>
Факт выкупов /sales-plan сейчас берётся из `WbCardFunnelDaily.buyoutsSumRub` — привязан к дате ЗАКАЗА (когорте). Для свежего периода когорта не дозрела: 01–05.07 показывает ~1,85 М/день вместо реальных ~3,3–3,9 М/день, что ломает сравнение с ИУ (2 380 805 ₽/день) и KPI «Факт за период».

Пользователь подтвердил: ИУ = «выкуплено за КАЛЕНДАРНЫЙ день» (дата реализации), метрика — цена продавца до СПП. Эмпирика (smoke supplier/sales на проде): `priceWithDisc` по дате реализации = 01.07:3,88 М / 02.07:3,32 М / 03.07:3,38 М — совпадает с ожиданием.

Решение: добавить источник дневного факта выкупов по ДАТЕ РЕАЛИЗАЦИИ (WB Statistics Sales API → таблица `WbSalesDaily`, no-FK как `WbCardFunnelDaily`) и завязать на него факт/ИУ Сводного таба. Воронку НЕ трогать (остаётся для per-товар когортного плана Товаров).

Purpose: KPI «Факт за период» / ИУ-сравнение /sales-plan становятся правдивыми на свежем окне.
Output: модель + миграция + fetch/aggregator + pure-тест + sync-роут + cron-wire + завязка Сводного на redemption-факт.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Извлечено из кодбазы. Executor использует напрямую — без разведки. -->

Образец no-FK модели (prisma/schema.prisma ~1110):
```prisma
model WbCardFunnelDaily {
  id            Int      @id @default(autoincrement())
  nmId          Int // ссылка на WbCard.nmId БЕЗ FK
  date          DateTime @db.Date // MSK day
  ordersCount   Int      @default(0)
  buyoutsSumRub Float    @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  @@unique([nmId, date])
  @@index([nmId])
  @@index([date])
}
```

WB API токен + cooldown (lib/wb-api.ts):
```typescript
// строка 53 — токен кабинета ИУ (WB_API_TOKEN scope)
async function getToken(): Promise<string> { return await getWbToken("WB_API_TOKEN") }

// строка 746 — обёртка с per-bucket cooldown bus. На 429 бросает WbRateLimitError.
// endpoint-лейбл резолвится в bucket: "Statistics API ... sales" → "statistics-sales"
async function wbFetch(endpoint: string, url: string, opts?: RequestInit): Promise<Response>

// строка 732 — resolveBucketFromEndpoint:
//   endpoint.startsWith("Statistics API") && endpoint.includes("sales") → "statistics-sales"
```
bucket `statistics-sales` уже зарегистрирован в lib/wb-cooldown.ts (WB_COOLDOWN_BUCKETS).

Существующий вызов supplier/sales (lib/wb-api.ts ~488, БЕЗ cooldown, только СПП-fallback):
```typescript
const dateFrom = new Date(Date.now() - 30*24*60*60*1000).toISOString().split("T")[0]
const res = await fetch(
  `https://statistics-api.wildberries.ru/api/v1/supplier/sales?dateFrom=${dateFrom}&flag=0`,
  { headers: { Authorization: token } }
)
// item поля: date (ISO), nmId, saleID, priceWithDisc, finishedPrice, forPay, spp
```

Cron-образец (app/api/cron/wb-funnel-daily/route.ts):
```typescript
export const runtime = "nodejs"; export const maxDuration = 600
// auth: req.headers.get("x-cron-secret") !== process.env.CRON_SECRET → 401
// ?days=N override (1..30); AppSetting.upsert("wbFunnelDailyLastRun", getMskTodayString())
// helpers из "@/lib/wb-cron-schedule": getMskTodayString()
```
todayMsk helper локально: `new Date(Date.now()+3*3600_000).toISOString().split("T")[0]`

Dispatch-wire (app/api/cron/dispatch/route.ts):
```typescript
// 1. добавить в findMany where.key.in: "wbSalesDailyCronTime", "wbSalesDailyLastRun"
// 2. const salesTime = settings.wbSalesDailyCronTime ?? "04:30"
//    const salesLastRun = settings.wbSalesDailyLastRun ?? null
// 3. if (shouldFireCron({currentHHMM, storedTime: salesTime, lastRunDate: salesLastRun, today})) {
//      const { GET: salesHandler } = await import("../wb-sales-daily/route")
//      const res = await salesHandler(req); fired.push(`sales:${res.status}`)
//    }
```

loadFactDaily текущий контракт (lib/sales-plan/data.ts:504):
```typescript
export interface FactDailyRow { buyoutsRub: number; ordersRub: number; buyoutsUnits: number; ordersUnits: number }
export interface FactDailyResult {
  company: Map<string, FactDailyRow>            // company-level SUM GROUP BY date
  byProduct: Map<string, Map<string, FactDailyRow>>  // productId → date → row
  settledThroughIso: string
}
export async function loadFactDaily(db, from, to): Promise<FactDailyResult>
```
nmId→productId join: `MarketplaceArticle` где marketplace.slug="wb", parseInt(article).
FUNNEL_SETTLE_LAG_DAYS=7 (helpers parseDate/toIso/addDays уже в файле).

buildPlanFactReport (lib/sales-plan/plan-fact.ts): pure, принимает factDays/companyFactDays/settledThroughIso; metric="buyouts-rub" читает day.buyoutsRub. НЕ менять сигнатуру.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Модель WbSalesDaily + миграция + fetchSalesDaily/aggregateSalesRows + pure-тест</name>
  <files>prisma/schema.prisma, prisma/migrations/20260705_wb_sales_daily/migration.sql, lib/wb-api.ts, tests/wb-sales-daily.test.ts</files>
  <read_first>prisma/schema.prisma (~1110 WbCardFunnelDaily), lib/wb-api.ts (~53 getToken, ~488 supplier/sales, ~732 resolveBucketFromEndpoint, ~746 wbFetch), tests/sales-plan-plan-fact.test.ts (стиль vitest)</read_first>
  <action>
1. **prisma/schema.prisma** — добавить модель рядом с WbCardFunnelDaily (no-FK паттерн):
```prisma
// Дневной факт ВЫКУПОВ по дате РЕАЛИЗАЦИИ (Statistics Sales API supplier/sales).
// В отличие от WbCardFunnelDaily (дата ЗАКАЗА/когорта) — дата продажи.
// nmId БЕЗ FK (паттерн проекта: WbCard может быть soft-delete).
model WbSalesDaily {
  id           Int      @id @default(autoincrement())
  nmId         Int
  date         DateTime @db.Date // дата реализации (MSK day)
  buyoutsRub   Float    @default(0) // Σ priceWithDisc по выкупам (saleID startsWith 'S'), цена продавца до СПП
  buyoutsCount Int      @default(0)
  returnsRub   Float    @default(0) // Σ priceWithDisc по возвратам (saleID НЕ 'S', обычно 'R')
  returnsCount Int      @default(0)
  forPayRub    Float    @default(0) // Σ forPay по выкупам (к перечислению)
  updatedAt    DateTime @updatedAt

  @@unique([nmId, date])
  @@index([date])
}
```

2. **prisma/migrations/20260705_wb_sales_daily/migration.sql** — рукописная АДДИТИВНАЯ миграция (локальной PG нет, применится на проде через `prisma migrate deploy`):
```sql
-- WbSalesDaily: дневной факт выкупов по дате реализации (Statistics Sales API)
CREATE TABLE "WbSalesDaily" (
    "id" SERIAL NOT NULL,
    "nmId" INTEGER NOT NULL,
    "date" DATE NOT NULL,
    "buyoutsRub" DOUBLE PRECISION NOT NULL DEFAULT 0,
    "buyoutsCount" INTEGER NOT NULL DEFAULT 0,
    "returnsRub" DOUBLE PRECISION NOT NULL DEFAULT 0,
    "returnsCount" INTEGER NOT NULL DEFAULT 0,
    "forPayRub" DOUBLE PRECISION NOT NULL DEFAULT 0,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    CONSTRAINT "WbSalesDaily_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "WbSalesDaily_nmId_date_key" ON "WbSalesDaily"("nmId", "date");
CREATE INDEX "WbSalesDaily_date_idx" ON "WbSalesDaily"("date");
```

3. **lib/wb-api.ts** — добавить экспортируемый ЧИСТЫЙ агрегатор + fetch (в конец файла или рядом с supplier/sales логикой):
```typescript
export interface WbSaleRow { date: string; nmId: number; saleID: string; priceWithDisc: number; forPay: number }
export interface WbSalesDailyAgg { nmId: number; date: string; buyoutsRub: number; buyoutsCount: number; returnsRub: number; returnsCount: number; forPayRub: number }

/** Pure: агрегирует записи supplier/sales в дневной разрез по (nmId, дата реализации).
 *  Дата реализации = row.date.slice(0,10). saleID startsWith 'S' → выкуп, иначе → возврат. */
export function aggregateSalesRows(rows: WbSaleRow[]): WbSalesDailyAgg[] {
  const map = new Map<string, WbSalesDailyAgg>()
  for (const r of rows) {
    const nmId = Number(r.nmId)
    if (!Number.isFinite(nmId)) continue
    const iso = String(r.date ?? "").slice(0, 10)
    if (iso.length !== 10) continue
    const key = `${nmId}|${iso}`
    let agg = map.get(key)
    if (!agg) { agg = { nmId, date: iso, buyoutsRub: 0, buyoutsCount: 0, returnsRub: 0, returnsCount: 0, forPayRub: 0 }; map.set(key, agg) }
    const price = Number(r.priceWithDisc) || 0
    const forPay = Number(r.forPay) || 0
    if (String(r.saleID ?? "").startsWith("S")) { agg.buyoutsRub += price; agg.buyoutsCount++; agg.forPayRub += forPay }
    else { agg.returnsRub += price; agg.returnsCount++ }
  }
  return Array.from(map.values())
}

/** Тянет supplier/sales за [dateFrom; now], агрегирует в дневной разрез выкупов по дате реализации. */
export async function fetchSalesDaily(dateFrom: string): Promise<WbSalesDailyAgg[]> {
  const token = await getToken()
  const res = await wbFetch(
    "Statistics API sales",
    `https://statistics-api.wildberries.ru/api/v1/supplier/sales?dateFrom=${dateFrom}&flag=0`,
    { headers: { Authorization: token } },
  )
  if (!res.ok) throw new Error(`Sales API ошибка ${res.status}: ${await res.text()}`)
  const data = await res.json()
  if (!Array.isArray(data)) return []
  return aggregateSalesRows(data as WbSaleRow[])
}
```
Примечание: `wbFetch("Statistics API sales", ...)` даёт cooldown bucket `statistics-sales` (resolveBucketFromEndpoint строка 734-736) + WbRateLimitError на 429. НЕ трогать существующий СПП-fallback вызов на ~488.

4. **tests/wb-sales-daily.test.ts** — pure-тест `aggregateSalesRows` на фикстуре 5-6 записей:
```typescript
import { describe, it, expect } from "vitest"
import { aggregateSalesRows } from "@/lib/wb-api"

describe("aggregateSalesRows", () => {
  const rows = [
    { date: "2026-07-01T10:00:00", nmId: 100, saleID: "S001", priceWithDisc: 1000, forPay: 800 },
    { date: "2026-07-01T12:00:00", nmId: 100, saleID: "S002", priceWithDisc: 500,  forPay: 400 },
    { date: "2026-07-01T14:00:00", nmId: 100, saleID: "R003", priceWithDisc: 300,  forPay: 0   }, // возврат
    { date: "2026-07-02T09:00:00", nmId: 100, saleID: "S004", priceWithDisc: 700,  forPay: 560 },
    { date: "2026-07-01T11:00:00", nmId: 200, saleID: "S005", priceWithDisc: 2000, forPay: 1600 },
    { date: "2026-07-01T13:00:00", nmId: 200, saleID: "R006", priceWithDisc: 900,  forPay: 0   },
  ]
  it("суммирует priceWithDisc по (nmId, дата реализации), разделяя выкуп/возврат", () => {
    const agg = aggregateSalesRows(rows)
    const get = (nm: number, d: string) => agg.find((a) => a.nmId === nm && a.date === d)!
    const a = get(100, "2026-07-01")
    expect(a.buyoutsRub).toBe(1500); expect(a.buyoutsCount).toBe(2); expect(a.forPayRub).toBe(1200)
    expect(a.returnsRub).toBe(300);  expect(a.returnsCount).toBe(1)
    expect(get(100, "2026-07-02").buyoutsRub).toBe(700)
    const b = get(200, "2026-07-01")
    expect(b.buyoutsRub).toBe(2000); expect(b.returnsRub).toBe(900); expect(b.returnsCount).toBe(1)
  })
  it("пропускает записи с невалидными nmId/date", () => {
    expect(aggregateSalesRows([{ date: "", nmId: NaN as unknown as number, saleID: "S", priceWithDisc: 1, forPay: 1 }])).toEqual([])
  })
})
```

5. `npx prisma validate` + `npx prisma generate` (без БД — только схема/клиент).
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npx prisma validate && npx prisma generate && npx vitest run tests/wb-sales-daily.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` — схема валидна; `WbSalesDaily` в клиенте после generate
    - `grep -n "CREATE TABLE \"WbSalesDaily\"" prisma/migrations/20260705_wb_sales_daily/migration.sql` — 1 совпадение
    - `npx vitest run tests/wb-sales-daily.test.ts` — зелёный (агрегатор корректен)
    - `npx tsc --noEmit` — чисто
  </acceptance_criteria>
  <done>Модель WbSalesDaily + аддитивная миграция + fetchSalesDaily/aggregateSalesRows экспортированы; pure-тест зелёный; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: Sync-роут wb-sales-daily + cron-wire в dispatch</name>
  <files>app/api/cron/wb-sales-daily/route.ts, app/api/cron/dispatch/route.ts</files>
  <read_first>app/api/cron/wb-funnel-daily/route.ts (образец), app/api/cron/dispatch/route.ts (findMany where + shouldFireCron ветка funnel строки 27-67, 112-128), lib/wb-cron-schedule.ts (getMskTodayString/shouldFireCron)</read_first>
  <action>
1. **app/api/cron/wb-sales-daily/route.ts** — GET, по образцу funnel:
```typescript
// app/api/cron/wb-sales-daily/route.ts
// Daily cron (~04:30 МСК через dispatcher). Тянет Statistics Sales API за rolling
// N дней (default 7, ?days=N 1..60 override для backfill), clean-replace per date-окно
// в WbSalesDaily (дневной факт выкупов по дате реализации). Защищён x-cron-secret.
import { NextRequest, NextResponse } from "next/server"
import { prisma } from "@/lib/prisma"
import { fetchSalesDaily } from "@/lib/wb-api"
import { getMskTodayString } from "@/lib/wb-cron-schedule"

export const runtime = "nodejs"
export const maxDuration = 600

const DAILY_ROLLING_DAYS = 7
const BACKFILL_DAYS = 30

function mskDateMinusDays(days: number): string {
  return new Date(Date.now() + 3 * 3600_000 - days * 24 * 3600_000).toISOString().split("T")[0]
}

export async function GET(req: NextRequest): Promise<NextResponse> {
  if (req.headers.get("x-cron-secret") !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
  }
  try {
    const existing = await prisma.wbSalesDaily.count()
    const url = new URL(req.url)
    const daysParam = Number(url.searchParams.get("days"))
    const override = Number.isFinite(daysParam) && daysParam >= 1 && daysParam <= 60 ? Math.floor(daysParam) : null
    const days = override ?? (existing === 0 ? BACKFILL_DAYS : DAILY_ROLLING_DAYS)
    const mode: "backfill" | "delta" = override != null || existing === 0 ? "backfill" : "delta"

    const dateFrom = mskDateMinusDays(days)
    console.log(`[wb-sales-daily cron] start mode=${mode} dateFrom=${dateFrom}`)
    const agg = await fetchSalesDaily(dateFrom)

    // clean-replace date-окна: удаляем [>= dateFrom] и вставляем свежий агрегат.
    // Идемпотентно — повторный прогон не задваивает.
    const fromDate = new Date(dateFrom + "T00:00:00Z")
    const written = await prisma.$transaction(async (tx) => {
      await tx.wbSalesDaily.deleteMany({ where: { date: { gte: fromDate } } })
      if (agg.length === 0) return 0
      const created = await tx.wbSalesDaily.createMany({
        data: agg.map((a) => ({
          nmId: a.nmId,
          date: new Date(a.date + "T00:00:00Z"),
          buyoutsRub: a.buyoutsRub,
          buyoutsCount: a.buyoutsCount,
          returnsRub: a.returnsRub,
          returnsCount: a.returnsCount,
          forPayRub: a.forPayRub,
        })),
      })
      return created.count
    })

    const todayStr = getMskTodayString()
    await prisma.appSetting.upsert({
      where: { key: "wbSalesDailyLastRun" },
      create: { key: "wbSalesDailyLastRun", value: todayStr },
      update: { value: todayStr },
    })

    console.log(`[wb-sales-daily cron] done rows=${agg.length} written=${written}`)
    return NextResponse.json({ ok: true, mode, windowDays: days, dateFrom, rows: agg.length, written })
  } catch (err) {
    console.error("[wb-sales-daily cron] error:", err)
    return NextResponse.json({ ok: false, error: (err as Error).message }, { status: 500 })
  }
}
```
Примечание: clean-replace (deleteMany date>=dateFrom + createMany) в одной транзакции — проще per-row upsert и идемпотентно. `@@unique([nmId,date])` защищает от дублей внутри createMany (агрегатор уже уникален по ключу).

2. **app/api/cron/dispatch/route.ts** — добавить ветку по образцу funnel:
   - В `findMany.where.key.in` (строки ~30-48) добавить `"wbSalesDailyCronTime"` и `"wbSalesDailyLastRun"`.
   - После `const funnelTime = ...` добавить: `const salesTime = settings.wbSalesDailyCronTime ?? "04:30"` и `const salesLastRun = settings.wbSalesDailyLastRun ?? null`.
   - После блока funnel (строка ~128) добавить:
```typescript
  // Дневной факт выкупов по дате реализации (Statistics Sales API). 04:30 МСК —
  // после funnel (04:00), до orders (05:00). Отдельный bucket statistics-sales.
  if (shouldFireCron({ currentHHMM, storedTime: salesTime, lastRunDate: salesLastRun, today })) {
    try {
      const { GET: salesHandler } = await import("../wb-sales-daily/route")
      const res = await salesHandler(req)
      fired.push(`sales:${res.status}`)
    } catch (e) {
      console.error("[dispatch] sales error:", e)
      fired.push("sales:error")
    }
  }
```
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && grep -q "wb-sales-daily/route" app/api/cron/dispatch/route.ts && grep -q "wbSalesDailyCronTime" app/api/cron/dispatch/route.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/cron/wb-sales-daily/route.ts` экспортирует GET, x-cron-secret guard, ?days override 1..60, upsert wbSalesDailyLastRun
    - `grep -c "wbSalesDailyCronTime\|wbSalesDailyLastRun" app/api/cron/dispatch/route.ts` ≥ 3 (в where.in ×2 + чтение)
    - `grep -q "wb-sales-daily/route" app/api/cron/dispatch/route.ts` — dynamic import подключён
    - `npx tsc --noEmit` — чисто
  </acceptance_criteria>
  <done>Sync-роут создаёт/обновляет WbSalesDaily clean-replace per date-окно; dispatch fires sales ~04:30 МСК с lastRun-guard; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 3: Завязка Сводного /sales-plan на redemption-факт (WbSalesDaily)</name>
  <files>lib/sales-plan/data.ts, app/(dashboard)/sales-plan/page.tsx, components/sales-plan/PlanFactMatrix.tsx, components/sales-plan/PlanFactSummaryCards.tsx, components/sales-plan/PlanFactControls.tsx</files>
  <read_first>lib/sales-plan/data.ts (loadFactDaily 504-602), app/(dashboard)/sales-plan/page.tsx (177-404), components/sales-plan/PlanFactMatrix.tsx (154-158 footer), PlanFactControls.tsx (28-31 METRIC_OPTIONS), tests/sales-plan-plan-fact.test.ts</read_first>
  <action>
Цель: KPI «Факт за период», ИУ-сравнение, «Вне плана», график и ряд «факт» Сводного считать по WbSalesDaily (дата реализации), а НЕ по WbCardFunnelDaily.buyoutsSumRub. Воронку оставить нетронутой.

1. **lib/sales-plan/data.ts — добавить redemption-разрез в loadFactDaily** (расширяем результат, funnel-логику НЕ удаляем — она нужна для orders-метрик и как fallback):
   - Расширить `FactDailyResult`:
```typescript
export interface FactDailyResult {
  company: Map<string, FactDailyRow>
  byProduct: Map<string, Map<string, FactDailyRow>>
  settledThroughIso: string
  /** Redemption-факт выкупов по дате РЕАЛИЗАЦИИ (WbSalesDaily) — company-level */
  redemptionCompany: Map<string, FactDailyRow>
  /** Redemption-факт по товарам (productId → date → row) */
  redemptionByProduct: Map<string, Map<string, FactDailyRow>>
  /** Дни <= этой даты считаются settled для redemption (today−2, НЕ today−7) */
  redemptionSettledThroughIso: string
}
```
   - В теле loadFactDaily, ПОСЛЕ funnel-блоков, добавить redemption:
```typescript
  // ── Redemption (дата реализации, WbSalesDaily) ─────────────────────────────
  const redemptionSettledThroughIso = addDays(todayMsk, -2) // выкупы финализируются быстрее funnel

  const salesRows = await db.wbSalesDaily.findMany({
    where: { date: { gte: parseDate(from), lte: parseDate(to) } },
    select: { nmId: true, date: true, buyoutsRub: true, buyoutsCount: true },
  })

  const redemptionCompany = new Map<string, FactDailyRow>()
  for (const s of salesRows) {
    const iso = toIso(s.date)
    const cur = redemptionCompany.get(iso) ?? { buyoutsRub: 0, ordersRub: 0, buyoutsUnits: 0, ordersUnits: 0 }
    cur.buyoutsRub += s.buyoutsRub ?? 0
    cur.buyoutsUnits += s.buyoutsCount ?? 0
    redemptionCompany.set(iso, cur)
  }

  // product-level: тот же nmId→productId join (wbMarketplace/nmIdToProductId уже собраны выше)
  const redemptionByProduct = new Map<string, Map<string, FactDailyRow>>()
  if (wbMarketplace) {
    for (const s of salesRows) {
      const productId = nmIdToProductId.get(s.nmId)
      if (!productId) continue
      const iso = toIso(s.date)
      if (!redemptionByProduct.has(productId)) redemptionByProduct.set(productId, new Map())
      const m = redemptionByProduct.get(productId)!
      const cur = m.get(iso) ?? { buyoutsRub: 0, ordersRub: 0, buyoutsUnits: 0, ordersUnits: 0 }
      cur.buyoutsRub += s.buyoutsRub ?? 0
      cur.buyoutsUnits += s.buyoutsCount ?? 0
      m.set(iso, cur)
    }
  }

  return { company, byProduct, settledThroughIso, redemptionCompany, redemptionByProduct, redemptionSettledThroughIso }
```
   ВАЖНО: `nmIdToProductId` собирается внутри блока `if (wbMarketplace)`. Вынести объявление `nmIdToProductId`/`wbMarketplace` в область видимости функции (перед funnel product-блоком), чтобы redemption-блок к ним имел доступ. Если сейчас они локальны — поднять на уровень тела функции (const → let/const в общем scope). НЕ менять funnel-логику по существу.

2. **app/(dashboard)/sales-plan/page.tsx** — переключить факт на redemption:
   - Из `loadFactDaily` деструктурировать redemption:
```typescript
const {
  company: companyFactMap, byProduct: byProductMap, settledThroughIso,
  redemptionCompany, redemptionByProduct, redemptionSettledThroughIso,
} = factResult
```
   - Для метрик выкупов (`buyouts-rub`/`buyouts-units`) факт брать из redemption; для orders-метрик — оставить funnel (в redemption нет orders). Определить:
```typescript
const isBuyoutMetric = metric === "buyouts-rub" || metric === "buyouts-units"
const factSource = isBuyoutMetric ? redemptionByProduct : byProductMap
const companySource = isBuyoutMetric ? redemptionCompany : companyFactMap
const settledIso = isBuyoutMetric ? redemptionSettledThroughIso : settledThroughIso
```
   - `productFactByDate` строить из `factSource` (заменить `byProductMap` на `factSource` в цикле, строки ~365-375).
   - `companyFactDays` строить из `companySource` (строка ~383).
   - В `buildPlanFactReport({...})` передать `settledThroughIso: settledIso`.
   - Примечание: redemption не содержит `ordersRub/ordersUnits` — для buyout-метрик это не нужно (dayRub читает только buyoutsRub/buyoutsUnits). «Вне плана» = companySource − factSource оба из redemption → консистентно.

3. **components/sales-plan/PlanFactControls.tsx** — уточнить подпись метрики выкупов (METRIC_OPTIONS ~28):
```typescript
  { value: "buyouts-rub",   label: "Выкуплено ₽ (реализация)" },
  { value: "buyouts-units", label: "Выкуплено шт (реализация)" },
```
(orders-метки не трогать).

4. **components/sales-plan/PlanFactMatrix.tsx** — footer-заметка (строки 154-158): для buyout-метрик заменить текст на честный для redemption-источника:
```typescript
  const footerNote =
    metric === "buyouts-rub" || metric === "buyouts-units"
      ? "Факт = выкуплено за день (дата реализации, Statistics Sales API), цена продавца до СПП. Свежие дни (позже today−2) дозаполняются."
      : "Факт включает 73 артикула WB без привязки к товарам (~13,2 М ₽/мес по заказам, ~6%)"
```
   Заголовок колонки «Выкупы ₽/шт» (строки 225-227) — оставить как есть ИЛИ обновить на «Выкуплено ₽/шт»; допустимо оставить (не load-bearing).

5. **components/sales-plan/PlanFactSummaryCards.tsx** — карточка «Факт за период» (строка 99): добавить подпись-источник под заголовком:
```tsx
        <div className="text-xs text-muted-foreground">Факт за период</div>
        {/* + под значением, мелким: */}
```
   Добавить строку `<div className="text-[10px] text-muted-foreground mt-0.5">выкуплено за день (реализация)</div>` под `fmtM(factTotalRub)` блоком (внутри карточки 1). Не менять расчёты KPI.

**НЕ трогать:**
   - Golden ИУ 438 068 120 ₽, iuMetric='buyouts', DEFAULT_IU_TARGETS 2 380 805 ₽/день.
   - Per-товар ячейки Товаров (funnel остаётся источником для них).
   - buildPlanFactReport сигнатуру/логику.
   - 118 существующих sales-plan тестов должны остаться зелёными (redemption — новые поля, старые не сломаны).
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && grep -q "redemptionCompany" lib/sales-plan/data.ts && grep -q "redemptionSettledThroughIso" app/(dashboard)/sales-plan/page.tsx && npx tsc --noEmit && npx vitest run tests/sales-plan-plan-fact.test.ts tests/sales-plan-engine.test.ts tests/sales-plan-iu.test.ts tests/wb-sales-daily.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "redemption" lib/sales-plan/data.ts` ≥ 4 (интерфейс + company + byProduct + settledThrough)
    - `grep -q "redemptionSettledThroughIso\|redemptionCompany" app/(dashboard)/sales-plan/page.tsx` — факт переключён на redemption для buyout-метрик
    - `grep -q "реализаци" components/sales-plan/PlanFactControls.tsx` — подпись метрики обновлена
    - `npx tsc --noEmit` — чисто
    - `npx vitest run tests/sales-plan-*.test.ts tests/wb-sales-daily.test.ts` — ВСЕ зелёные (118 sales-plan + новый)
  </acceptance_criteria>
  <done>Сводный таб (KPI/ИУ/«Вне плана»/график/ряд факта для метрик выкупов) считается по WbSalesDaily; redemptionSettledThroughIso=today−2; воронка и per-товар Товары нетронуты; все тесты зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron dispatch → wb-sales-daily route | x-cron-secret заголовок; без него 401 |
| Statistics Sales API → БД | untrusted WB JSON (priceWithDisc/saleID/date/forPay) парсится в WbSalesDaily |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f1p-01 | Spoofing | GET /api/cron/wb-sales-daily | mitigate | x-cron-secret === process.env.CRON_SECRET, иначе 401 (образец funnel) |
| T-f1p-02 | Tampering | aggregateSalesRows входные строки | mitigate | Number()/startsWith guards; невалидные nmId/date пропускаются (unit-тест кейс) |
| T-f1p-03 | Denial of Service | Statistics Sales API 429 | mitigate | wbFetch → bucket statistics-sales cooldown + WbRateLimitError; sync возвращает 500, dispatch ловит в try/catch |
| T-f1p-04 | Info Disclosure | route ответ | accept | Возвращает только счётчики (rows/written), не PII; кабинет-агрегаты |
| T-f1p-05 | Elevation | /sales-plan read | mitigate | requireSection("SALES") уже на page.tsx (не меняем) |
</threat_model>

<verification>
- `npx prisma validate` — схема валидна, WbSalesDaily присутствует
- `npx tsc --noEmit` — чисто по всему проекту
- `npx vitest run` (sales-plan suite + wb-sales-daily) — все зелёные, 118 sales-plan тестов не сломаны
- `grep`-гейты из acceptance_criteria каждой задачи проходят
- Деплой (prisma migrate deploy + ручной backfill `curl ...?days=30`) + UAT — ВНЕ плана, выполняет пользователь
</verification>

<success_criteria>
- Таблица WbSalesDaily + аддитивная миграция + fetchSalesDaily/aggregateSalesRows + pure-тест
- Cron wb-sales-daily wired в dispatch (~04:30 МСК, lastRun-guard, ?days backfill)
- Сводный таб /sales-plan для метрик выкупов считается по дате реализации (WbSalesDaily), settledThrough=today−2
- Воронка WbCardFunnelDaily, per-товар Товары, golden ИУ, iuMetric='buyouts' — не тронуты
- tsc чист, все vitest зелёные
</success_criteria>

<output>
После завершения создать `.planning/quick/260705-f1p-wbsalesdaily-sales-api/260705-f1p-SUMMARY.md`
</output>
