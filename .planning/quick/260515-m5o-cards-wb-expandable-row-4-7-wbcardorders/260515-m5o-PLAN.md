---
phase: quick-260515-m5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260515_wb_card_orders_daily/migration.sql
  - lib/wb-api.ts
  - lib/wb-orders-chart.ts
  - tests/wb-card-orders-daily.test.ts
  - tests/wb-orders-chart-fill.test.ts
  - tests/wb-orders-chart-msk.test.ts
  - app/api/cron/wb-orders-daily/route.ts
  - app/api/wb-orders-backfill/route.ts
  - app/(dashboard)/cards/wb/page.tsx
  - components/cards/WbCardsTable.tsx
  - components/cards/WbCardOrdersChart.tsx
  - components/cards/WbOrdersBackfillButton.tsx
  - components/ui/chart.tsx
  - package.json
autonomous: true
requirements:
  - M5O-01
  - M5O-02
  - M5O-03
  - M5O-04
user_setup:
  - service: systemd (VPS root@85.198.97.89)
    why: "Daily cron 05:00 МСК для записи заказов за вчерашний день"
    env_vars:
      - name: CRON_SECRET
        source: "Уже в /etc/zoiten.pro.env (используется другими cron endpoints)"
    dashboard_config:
      - task: "Создать /etc/systemd/system/zoiten-wb-orders.service + .timer (инструкции в SUMMARY.md)"
        location: "VPS, выполняется через bash deploy.sh + ручной systemctl enable --now zoiten-wb-orders.timer"

must_haves:
  truths:
    - "Клик по строке карточки в /cards/wb раскрывает панель с bar chart и средними"
    - "Bar chart показывает ровно 28 столбцов (today-28 .. today-1), пустые дни = qty=0"
    - "Среднее заказов/день за 30 дней и за 7 дней отображается числом"
    - "Одновременно открыта только одна строка; клик по другой переключает"
    - "Клик по чекбоксу или артикулу не раскрывает строку (stopPropagation)"
    - "Sticky header не ломается при раскрытии строки"
    - "Daily cron 05:00 МСК пишет qty за вчерашний день в WbCardOrdersDaily"
    - "Backfill с 2026-04-01 запускается автоматически при пустой таблице (внутри cron) или вручную через кнопку"
    - "Повторный backfill идемпотентен (upsert по @@unique([nmId, date]))"
    - "Cron endpoint защищён x-cron-secret == process.env.CRON_SECRET (401 без секрета)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "model WbCardOrdersDaily с @@unique([nmId, date]) + индексы"
      contains: "model WbCardOrdersDaily"
    - path: "prisma/migrations/20260515_wb_card_orders_daily/migration.sql"
      provides: "Ручная SQL-миграция (CREATE TABLE + UNIQUE + индексы)"
    - path: "lib/wb-api.ts"
      provides: "fetchOrdersForRange(dateFrom, dateTo?) + upsertOrdersDaily(rows)"
      exports: ["fetchOrdersForRange", "upsertOrdersDaily"]
    - path: "lib/wb-orders-chart.ts"
      provides: "Pure helpers getMskTodayDate / getMskYesterdayDate / getLast28DaysMsk() + fillTimeSeries(rawRows)"
      exports: ["getMskTodayDate", "getMskYesterdayDate", "getLast28DaysMsk", "fillTimeSeries"]
    - path: "app/api/cron/wb-orders-daily/route.ts"
      provides: "GET endpoint с x-cron-secret guard: backfill if empty, иначе delta за вчера; maxDuration=600 + per-iteration logging"
    - path: "app/api/wb-orders-backfill/route.ts"
      provides: "POST endpoint с requireSection('PRODUCTS','MANAGE'): ручной re-run backfill с 2026-04-01"
    - path: "components/cards/WbCardOrdersChart.tsx"
      provides: "recharts BarChart (28 баров) + 2 числа (avg30d / avg7d)"
    - path: "components/cards/WbCardsTable.tsx"
      provides: "Expand state (single-open) + onClick row + expanded <tr> с motion-анимацией"
    - path: "components/cards/WbOrdersBackfillButton.tsx"
      provides: "Кнопка «Backfill заказов» в шапке /cards/wb"
    - path: "components/ui/chart.tsx"
      provides: "shadcn-charts wrapper (npx shadcn@latest add chart)"
    - path: "tests/wb-card-orders-daily.test.ts"
      provides: "Golden test группировки (nmId, date MSK) + isCancel фильтр + 80k pagination"
    - path: "tests/wb-orders-chart-fill.test.ts"
      provides: "Тест 28-дневного fill (дни без заказов → qty=0)"
    - path: "tests/wb-orders-chart-msk.test.ts"
      provides: "Тест MSK midnight boundary для getMskTodayDate / getMskYesterdayDate"
  key_links:
    - from: "components/cards/WbCardsTable.tsx"
      to: "components/cards/WbCardOrdersChart.tsx"
      via: "import + <WbCardOrdersChart nmId timeSeries /> при expandedId === card.id"
      pattern: "WbCardOrdersChart"
    - from: "app/(dashboard)/cards/wb/page.tsx"
      to: "prisma.wbCardOrdersDaily.findMany"
      via: "query за [today-28, today-1] для visible nmIds → ordersTimeSeries Record<nmId, DayPoint[]>; границы окна берутся из getMskTodayDate/getMskYesterdayDate"
      pattern: "wbCardOrdersDaily\\.findMany"
    - from: "app/api/cron/wb-orders-daily/route.ts"
      to: "lib/wb-api.ts → fetchOrdersForRange + upsertOrdersDaily"
      via: "GET handler вызывает helper с dateFrom=getMskYesterdayDate() (или 2026-04-01 если БД пустая)"
      pattern: "fetchOrdersForRange|upsertOrdersDaily"
    - from: "components/cards/WbOrdersBackfillButton.tsx"
      to: "/api/wb-orders-backfill"
      via: "fetch POST с credentials → requireSection('PRODUCTS','MANAGE')"
      pattern: "wb-orders-backfill"
    - from: "components/cards/WbCardsTable.tsx"
      to: "row onClick + stopPropagation на чекбоксе/артикуле"
      via: "useState<expandedId>; setExpandedId(prev===id?null:id) на TableRow onClick"
      pattern: "expandedId|setExpandedId"
---

<objective>
Реализовать раскрывающуюся панель на строках карточек /cards/wb со столбчатой диаграммой заказов за 4 недели (28 столбцов) + средние заказов в день за 30 и 7 дней. Источник данных — новая таблица WbCardOrdersDaily, заполняемая daily cron в 05:00 МСК (с автоматическим одноразовым backfill с 2026-04-01).

Purpose: дать пользователю быстрый визуальный контекст спроса на конкретную карточку без перехода на /stock/wb. Текущая avgSalesSpeed7d даёт только число — нужна тенденция (стартовала ли карточка, есть ли всплески, был ли провал).

Output:
- БД-таблица WbCardOrdersDaily с idempotent upsert по (nmId, date)
- Helper fetchOrdersForRange + chart fill helper + MSK date helpers + unit-тесты
- Cron endpoint + backfill endpoint + UI-кнопка
- Expandable row в WbCardsTable с recharts BarChart
</objective>

<execution_context>
@C:\Users\User\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\User\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/quick/260515-m5o-cards-wb-expandable-row-4-7-wbcardorders/260515-m5o-CONTEXT.md
@.planning/quick/260515-m5o-cards-wb-expandable-row-4-7-wbcardorders/260515-m5o-RESEARCH.md
@CLAUDE.md
@lib/wb-api.ts
@app/api/wb-sync/route.ts
@app/api/cron/purge-deleted/route.ts
@app/api/cron/support-sync-reviews/route.ts
@app/(dashboard)/cards/wb/page.tsx
@components/cards/WbCardsTable.tsx
@prisma/schema.prisma
@prisma/migrations/20260515_wb_card_soft_delete/migration.sql

<interfaces>
<!-- Контракты, которые executor обязан использовать как есть. Не исследовать кодовую базу — всё ниже. -->

## Существующий wbFetch + per-bucket cooldown (lib/wb-api.ts:741)
```typescript
async function wbFetch(endpoint: string, url: string, opts?: RequestInit): Promise<Response>
// endpoint строка → resolveBucketFromEndpoint(endpoint) → bucket "statistics-orders" для всего что matches "Orders".
// При 429: bucket cooldown через setWbCooldownUntil, throws WbRateLimitError(retryAfterSec).
// ВАЖНО: использовать wbFetch (не fetch напрямую) — short-circuit при активном cooldown.
```

## Существующий fetchOrdersPerWarehouse pattern (lib/wb-api.ts:1083)
```typescript
// URL pattern (точно такой же endpoint):
const url = `https://statistics-api.wildberries.ru/api/v1/supplier/orders?dateFrom=${encodeURIComponent(dateFromIso)}&flag=0`
const res = await wbFetch("Orders API (per-warehouse)", url, { headers: { Authorization: token } })

// Поля orders: { nmId?: number, nm_id?: number, isCancel?: boolean, date?: string, lastChangeDate?: string }
// date = ISO без TZ = MSK локальное → .slice(0,10) = YYYY-MM-DD MSK
// MSK now паттерн (тот же файл, :1227):
const mskNow = new Date(Date.now() + 3 * 3600_000)
const yy = mskNow.getUTCFullYear()
const mm = String(mskNow.getUTCMonth() + 1).padStart(2, "0")
const dd = String(mskNow.getUTCDate() - 1).padStart(2, "0") // вчера
const yesterdayPrefix = `${yy}-${mm}-${dd}`
```

## getToken (lib/wb-api.ts:53)
```typescript
async function getToken(): Promise<string>  // обёртка над getWbToken("WB_API_TOKEN")
```

## requireSection pattern (lib/rbac.ts) — КРИТИЧНО
```typescript
// ERP_SECTION enum в prisma/schema.prisma:21-32 содержит ТОЛЬКО:
//   PRODUCTS, PRICES, WEEKLY_CARDS, STOCK, COST, PROCUREMENT, SALES, SUPPORT, EMPLOYEES, USER_MANAGEMENT
// Раздела "CARDS" НЕТ. Все existing endpoints/actions для /cards/wb используют requireSection("PRODUCTS"...)
// см. app/actions/wb-cards.ts. Пытаться передать "CARDS" → throws FORBIDDEN для не-SUPERADMIN.
await requireSection("PRODUCTS", "MANAGE")  // правильно для write на /cards/wb
```

## Cron endpoint pattern (app/api/cron/purge-deleted/route.ts)
```typescript
export const runtime = "nodejs"
export const maxDuration = 300  // для wb-orders-daily бампим до 600 (см. Task 2.3)

export async function GET(req: NextRequest): Promise<NextResponse> {
  const secret = req.headers.get("x-cron-secret")
  if (!secret || secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
  }
  try {
    // бизнес-логика
    return NextResponse.json({ ok: true, ... })
  } catch (err) {
    return NextResponse.json({ ok: false, error: (err as Error).message }, { status: 500 })
  }
}
```

## WbCardsTable текущая структура (components/cards/WbCardsTable.tsx)
- 19 колонок в <thead> (line 302-346) → colSpan={19} для expanded <tr>
- Sticky pattern: native `<table>` + `<thead className="bg-background">` + каждый `<TableHead className="sticky top-0 z-20 bg-background border-b">` (НЕ shadcn <Table>/<TableHeader>)
- `<TableBody>` + `<TableRow>` (shadcn) для body — OK
- Существующий stopPropagation на cell артикула (line 392-395)
- Checkbox на line 360 — base-ui сам гасит event
- Tooltip на cell названия (line 376-387) — render-prop без onClick

## Prisma migration pattern (50 миграций в проекте)
- НЕ запускать `prisma migrate dev` локально (нет PG)
- Вручную создать `prisma/migrations/{TIMESTAMP_name}/migration.sql`
- Обновить schema.prisma
- `npx prisma generate` локально → проверить компиляцию типов
- На VPS: `prisma migrate deploy` через deploy.sh
- Стиль SQL: см. `prisma/migrations/20260515_wb_card_soft_delete/migration.sql`

## CLAUDE.md gotchas
- Sticky data-таблицы: НЕ ломать `<thead className="bg-background">` + `position: sticky` на cells
- `<Link prefetch={false}>` — n/a (раскрытие не Link)
- WB API rate-limit: Statistics ~1 req/min, wbFetch handles это сам
- nginx уже выкручен до `proxy_read_timeout 600s` (см. CLAUDE.md «WB Promotions Calendar API»)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema WbCardOrdersDaily + миграция + WB API helper fetchOrdersForRange + unit-тесты</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260515_wb_card_orders_daily/migration.sql,
    lib/wb-api.ts,
    tests/wb-card-orders-daily.test.ts
  </files>
  <behavior>
    - Test 1 (golden): fetchOrdersForRange с mocked WB-ответом [{nmId:111, date:"2026-05-14T10:00:00", isCancel:false}, {nmId:111, date:"2026-05-14T15:00:00", isCancel:false}, {nmId:222, date:"2026-05-14T11:00:00", isCancel:true}, {nmId:222, date:"2026-05-13T11:00:00", isCancel:false}] → возвращает [{nmId:111, date:new Date("2026-05-14"), qty:2}, {nmId:222, date:new Date("2026-05-13"), qty:1}] (isCancel:true отфильтрован, dedup по nmId+date)
    - Test 2 (MSK timezone): WB orders с date "2026-05-14T23:30:00" (без TZ → MSK локальное) группируется по дате "2026-05-14", а не сдвигается в UTC
    - Test 3 (80k pagination): первый mocked-ответ возвращает 80_000 элементов с lastChangeDate="2026-05-10T...", второй — 50 элементов; helper делает 2 запроса; final map содержит данные из обоих
    - Test 4 (empty response): пустой массив orders → возвращает пустой массив (не throws)
    - Test 5 (snake_case nmId): order с `nm_id:333` (без camelCase nmId) обрабатывается корректно
  </behavior>
  <action>
    **1.1 Обновить prisma/schema.prisma** — добавить model в конец файла (после WbCardWarehouseOrders, around line 970):

    ```prisma
    // 2026-05-15 (quick 260515-m5o): snapshot заказов per nmId per day для bar chart на /cards/wb.
    // Daily cron 05:00 МСК пишет qty за вчерашний день; backfill с 2026-04-01 одноразовый.
    // Только дни с qty > 0 хранятся — fill пустых дней делается на сервере при сборке chart timeSeries.
    model WbCardOrdersDaily {
      id        Int      @id @default(autoincrement())
      nmId      Int
      date      DateTime @db.Date
      qty       Int
      // Future: sumRub Decimal?, returnsQty Int?, cancelQty Int? — расширение в следующих фазах.
      createdAt DateTime @default(now())

      @@unique([nmId, date])
      @@index([nmId])
      @@index([date])
    }
    ```

    Обрати внимание: НЕ ставим FK на WbCard.nmId — `nmId` остаётся Int без relation. Причина: WbCard может быть soft-deleted/hard-deleted, а исторические orders должны выживать.

    **1.2 Создать prisma/migrations/20260515_wb_card_orders_daily/migration.sql** (вручную):

    ```sql
    -- 2026-05-15 (quick 260515-m5o): WbCardOrdersDaily — snapshot заказов per nmId per day.
    -- Daily cron 05:00 MSK пишет данные за вчера; одноразовый backfill с 2026-04-01.

    CREATE TABLE "WbCardOrdersDaily" (
      "id"        SERIAL PRIMARY KEY,
      "nmId"      INTEGER NOT NULL,
      "date"      DATE NOT NULL,
      "qty"       INTEGER NOT NULL,
      "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
    );

    CREATE UNIQUE INDEX "WbCardOrdersDaily_nmId_date_key" ON "WbCardOrdersDaily"("nmId", "date");
    CREATE INDEX "WbCardOrdersDaily_nmId_idx" ON "WbCardOrdersDaily"("nmId");
    CREATE INDEX "WbCardOrdersDaily_date_idx" ON "WbCardOrdersDaily"("date");
    ```

    Запустить `npx prisma generate` локально → убедиться нет TS-ошибок в проекте после генерации client. Migration НЕ применять локально (нет PG) — она применится через `prisma migrate deploy` на VPS.

    **1.3 Добавить fetchOrdersForRange в lib/wb-api.ts** (после fetchAvgSalesSpeed7d, в конец файла перед WbRateLimitError если он там в конце; см. existing pattern fetchOrdersPerWarehouse:1083):

    ```typescript
    export interface OrdersDailyRow {
      nmId: number
      date: Date
      qty: number
    }

    /** Запросить WB Orders за период [dateFrom, ?] и сгруппировать в (nmId, date MSK) → qty.
     *  isCancel=true исключаются. При response.length >= 80_000 — итерируем с lastChangeDate.
     *  Используется в backfill с 2026-04-01 и daily delta (dateFrom = вчера 00:00 MSK).
     *  Возвращает массив строк готовых к upsert (date = JS Date 00:00 UTC).
     *
     *  Per-iteration logging для backfill diagnostics (W-3 fix): console.log с page=N, rowsReturned, total.
     */
    export async function fetchOrdersForRange(dateFrom: Date): Promise<OrdersDailyRow[]> {
      const token = await getToken()
      const counts = new Map<string, { nmId: number; date: string; qty: number }>()
      // ISO без `Z` → MSK интерпретация на WB-стороне (см. CONTEXT.md + fetchAvgSalesSpeed7d)
      let currentDateFrom = dateFrom.toISOString().split(".")[0] // "2026-04-01T00:00:00"
      let safetyIters = 0
      let totalRowsSeen = 0

      while (true) {
        if (++safetyIters > 50) {
          throw new Error("fetchOrdersForRange: 80k pagination loop > 50 iterations, aborting")
        }
        const url =
          `https://statistics-api.wildberries.ru/api/v1/supplier/orders` +
          `?dateFrom=${encodeURIComponent(currentDateFrom)}&flag=0`
        const res = await wbFetch("Orders API (range)", url, { headers: { Authorization: token } })
        if (!res.ok) {
          const text = await res.text()
          throw new Error(`WB Orders API (fetchOrdersForRange) ${res.status}: ${text}`)
        }
        const orders = (await res.json()) as Array<{
          nmId?: number
          nm_id?: number
          date?: string
          isCancel?: boolean
          lastChangeDate?: string
        }>
        totalRowsSeen += Array.isArray(orders) ? orders.length : 0
        console.log(
          `[wb-orders backfill] page=${safetyIters} dateFrom=${currentDateFrom} rowsReturned=${Array.isArray(orders) ? orders.length : 0} total=${totalRowsSeen}`
        )
        if (!Array.isArray(orders) || orders.length === 0) break

        for (const o of orders) {
          if (o.isCancel) continue
          const nm = o.nmId ?? o.nm_id
          if (nm == null || !o.date) continue
          const dateKey = o.date.slice(0, 10) // YYYY-MM-DD MSK
          const k = `${nm}::${dateKey}`
          const existing = counts.get(k)
          if (existing) existing.qty++
          else counts.set(k, { nmId: nm, date: dateKey, qty: 1 })
        }

        // 80k soft-limit → продолжаем pagination с lastChangeDate последней записи.
        // NOTE: WB Statistics Orders endpoint redundantly returning srid across pages маловероятен
        // на Zoiten volume (~45 дней). Если когда-то начнёт двоить — добавить Set<srid> dedup сюда.
        if (orders.length >= 80_000) {
          const last = orders[orders.length - 1]
          if (!last.lastChangeDate || last.lastChangeDate === currentDateFrom) break
          currentDateFrom = last.lastChangeDate
          continue
        }
        break
      }

      return Array.from(counts.values()).map((r) => ({
        nmId: r.nmId,
        date: new Date(r.date), // 00:00 UTC, Prisma @db.Date нормализует к DATE
        qty: r.qty,
      }))
    }

    /** Idempotent upsert строк OrdersDailyRow в WbCardOrdersDaily.
     *  Используется и в cron, и в manual backfill. Transaction с timeout 90s.
     *  ON CONFLICT (nmId,date) UPDATE qty (overwrite — backfill rerun может вернуть скорректированное число).
     *  Per-chunk logging для backfill diagnostics (W-3 fix).
     */
    export async function upsertOrdersDaily(rows: OrdersDailyRow[]): Promise<{ upserted: number }> {
      if (rows.length === 0) return { upserted: 0 }
      // Чанками по 500 для безопасности transaction timeout
      const CHUNK = 500
      const totalChunks = Math.ceil(rows.length / CHUNK)
      let total = 0
      for (let i = 0; i < rows.length; i += CHUNK) {
        const chunk = rows.slice(i, i + CHUNK)
        const chunkIdx = Math.floor(i / CHUNK) + 1
        await prisma.$transaction(
          chunk.map((r) =>
            prisma.wbCardOrdersDaily.upsert({
              where: { nmId_date: { nmId: r.nmId, date: r.date } },
              create: { nmId: r.nmId, date: r.date, qty: r.qty },
              update: { qty: r.qty },
            })
          ),
          { timeout: 90_000 }
        )
        total += chunk.length
        console.log(
          `[wb-orders upsert] chunk=${chunkIdx}/${totalChunks} processed=${total}/${rows.length}`
        )
      }
      return { upserted: total }
    }
    ```

    Импорт `prisma` уже есть в lib/wb-api.ts (line 4). Использовать `wbFetch` (не fetch) — bucket `statistics-orders` уже разрешается по строке "Orders" в endpoint name.

    **1.4 Создать tests/wb-card-orders-daily.test.ts** (vitest 4.x, проект-конвенция: vi.hoisted для mock factories):

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    // Mock prisma + getWbToken до import lib/wb-api
    vi.mock("@/lib/prisma", () => ({ prisma: {} }))
    vi.mock("@/lib/wb-token", () => ({
      getWbToken: vi.fn().mockResolvedValue("test-token"),
    }))
    vi.mock("@/lib/wb-cooldown", () => ({
      getWbCooldownSecondsRemaining: vi.fn().mockResolvedValue(0),
      setWbCooldownUntil: vi.fn(),
    }))

    import { fetchOrdersForRange } from "@/lib/wb-api"

    beforeEach(() => {
      vi.restoreAllMocks()
    })

    describe("fetchOrdersForRange", () => {
      it("группирует по (nmId, date MSK), фильтрует isCancel=true", async () => {
        global.fetch = vi.fn().mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => [
            { nmId: 111, date: "2026-05-14T10:00:00", isCancel: false },
            { nmId: 111, date: "2026-05-14T15:00:00", isCancel: false },
            { nmId: 222, date: "2026-05-14T11:00:00", isCancel: true },
            { nmId: 222, date: "2026-05-13T11:00:00", isCancel: false },
          ],
        }) as any

        const rows = await fetchOrdersForRange(new Date("2026-05-13"))
        const key = (r: any) => `${r.nmId}::${r.date.toISOString().slice(0,10)}`
        const byKey = new Map(rows.map(r => [key(r), r.qty]))
        expect(byKey.get("111::2026-05-14")).toBe(2)
        expect(byKey.get("222::2026-05-13")).toBe(1)
        expect(byKey.has("222::2026-05-14")).toBe(false) // isCancel:true отброшен
      })

      it("обрабатывает snake_case nm_id", async () => {
        global.fetch = vi.fn().mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => [
            { nm_id: 333, date: "2026-05-14T10:00:00", isCancel: false },
          ],
        }) as any
        const rows = await fetchOrdersForRange(new Date("2026-05-13"))
        expect(rows.find(r => r.nmId === 333)?.qty).toBe(1)
      })

      it("date MSK интерпретация: 23:30 остаётся в той же дате", async () => {
        global.fetch = vi.fn().mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => [
            { nmId: 444, date: "2026-05-14T23:30:00", isCancel: false },
          ],
        }) as any
        const rows = await fetchOrdersForRange(new Date("2026-05-13"))
        expect(rows[0].date.toISOString().slice(0,10)).toBe("2026-05-14")
      })

      it("80k pagination: продолжает с lastChangeDate", async () => {
        const first80k = Array.from({ length: 80_000 }, (_, i) => ({
          nmId: 555,
          date: "2026-04-10T10:00:00",
          isCancel: false,
          lastChangeDate: "2026-04-15T12:00:00",
        }))
        // Все 80k за тот же (nmId,date) — qty=80000. Второй ответ — ещё 1.
        global.fetch = vi.fn()
          .mockResolvedValueOnce({ ok: true, status: 200, json: async () => first80k })
          .mockResolvedValueOnce({
            ok: true,
            status: 200,
            json: async () => [
              { nmId: 555, date: "2026-04-20T10:00:00", isCancel: false, lastChangeDate: "2026-04-20T10:01:00" },
            ],
          }) as any

        const rows = await fetchOrdersForRange(new Date("2026-04-01"))
        const map = new Map(rows.map(r => [`${r.nmId}::${r.date.toISOString().slice(0,10)}`, r.qty]))
        expect(map.get("555::2026-04-10")).toBe(80_000)
        expect(map.get("555::2026-04-20")).toBe(1)
        expect((global.fetch as any).mock.calls.length).toBe(2)
      })

      it("пустой ответ → пустой массив", async () => {
        global.fetch = vi.fn().mockResolvedValueOnce({
          ok: true, status: 200, json: async () => [],
        }) as any
        const rows = await fetchOrdersForRange(new Date("2026-05-13"))
        expect(rows).toEqual([])
      })
    })
    ```

    Запуск: `npm run test -- tests/wb-card-orders-daily.test.ts` — все 5 тестов проходят.

    Per D-01..D-07 (CONTEXT.md decisions): источник = Statistics Orders API, isCancel фильтр, MSK timezone, 80k pagination через lastChangeDate, idempotent upsert через @@unique.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-card-orders-daily.test.ts</automated>
  </verify>
  <done>
    - prisma/schema.prisma содержит `model WbCardOrdersDaily` с @@unique([nmId, date]) + 2 индекса
    - prisma/migrations/20260515_wb_card_orders_daily/migration.sql существует, SQL валиден (CREATE TABLE + UNIQUE + 2 INDEX)
    - `npx prisma generate` проходит без ошибок, TypeScript типы `prisma.wbCardOrdersDaily` доступны
    - lib/wb-api.ts экспортирует `fetchOrdersForRange` и `upsertOrdersDaily` с правильными сигнатурами
    - fetchOrdersForRange содержит `console.log("[wb-orders backfill] page=...")` per iteration
    - upsertOrdersDaily содержит `console.log("[wb-orders upsert] chunk=...")` per chunk
    - `npm run test -- tests/wb-card-orders-daily.test.ts` — 5/5 тестов зелёные
    - `npx tsc --noEmit` — нет новых TS-ошибок в проекте
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MSK date helpers + Cron endpoint /api/cron/wb-orders-daily + manual backfill endpoint + chart fill helper + тесты</name>
  <files>
    app/api/cron/wb-orders-daily/route.ts,
    app/api/wb-orders-backfill/route.ts,
    lib/wb-orders-chart.ts,
    tests/wb-orders-chart-fill.test.ts,
    tests/wb-orders-chart-msk.test.ts
  </files>
  <behavior>
    - Test 1 (chart fill — 28 точек): `fillTimeSeries([{date: new Date("2026-05-14"), qty: 5}], new Date("2026-05-15T12:00:00Z"))` возвращает массив length=28, последний элемент `{date: "2026-05-14", qty: 5}`, остальные 27 элементов с qty=0
    - Test 2 (chart fill — empty input): пустой raw → 28 точек все qty=0
    - Test 3 (chart fill — out-of-window): rows с date вне окна [today-28, today-1] игнорируются (не появляются в результате, qty=0 на их месте)
    - Test 4 (getLast28DaysMsk MSK): для now = "2026-05-15T12:00:00Z" возвращает массив строк YYYY-MM-DD от "2026-04-17" (today-28) до "2026-05-14" (today-1) включительно, length=28
    - Test 5 (W-4: MSK midnight before): `getMskTodayDate(new Date("2026-05-15T20:30:00Z"))` (= 23:30 MSK still 2026-05-15) → Date.UTC(2026,4,15); `getMskYesterdayDate(...)` → Date.UTC(2026,4,14)
    - Test 6 (W-4: MSK midnight after): `getMskTodayDate(new Date("2026-05-15T21:30:00Z"))` (= 00:30 MSK next day = 2026-05-16) → Date.UTC(2026,4,16); `getMskYesterdayDate(...)` → Date.UTC(2026,4,15)
  </behavior>
  <action>
    **2.1 Создать lib/wb-orders-chart.ts** — pure helpers без Prisma/Next зависимостей (тестируется в vitest). Per W-4 fix: единая точка истины для MSK today/yesterday math — re-used cron, page, fill helper:

    ```typescript
    // lib/wb-orders-chart.ts
    // Pure-helpers для сборки 28-дневного chart timeSeries из raw WbCardOrdersDaily rows.
    // MSK timezone — date keys всегда в МСК-локальной дате.
    // W-4 fix: helpers getMskTodayDate/getMskYesterdayDate — единая точка истины,
    // re-used из cron route + page.tsx + getLast28DaysMsk.

    export interface DayPoint {
      date: string // "YYYY-MM-DD"
      qty: number
    }

    /** 00:00:00 UTC даты, соответствующей сегодняшнему дню в MSK (UTC+3).
     *  Используется для построения окна [today-28, today-1] и для границы "вчера".
     *  `now` — для тестов; в проде не задаётся.
     */
    export function getMskTodayDate(now?: Date): Date {
      const baseUtcMs = (now ?? new Date()).getTime()
      const mskNow = new Date(baseUtcMs + 3 * 3600_000)
      return new Date(Date.UTC(mskNow.getUTCFullYear(), mskNow.getUTCMonth(), mskNow.getUTCDate()))
    }

    /** 00:00:00 UTC даты, соответствующей вчерашнему дню в MSK.
     *  = getMskTodayDate(now) - 24h. Используется как dateFrom для daily delta cron.
     */
    export function getMskYesterdayDate(now?: Date): Date {
      const today = getMskTodayDate(now)
      return new Date(today.getTime() - 24 * 3600_000)
    }

    /** Возвращает массив 28 строк YYYY-MM-DD от today_msk - 28 до today_msk - 1 (включительно).
     *  Если `now` не задано — использует Date.now() в MSK (UTC+3).
     *  Реализуется через getMskTodayDate — без дублирования MSK math.
     */
    export function getLast28DaysMsk(now?: Date): string[] {
      const today = getMskTodayDate(now)
      const result: string[] = []
      for (let offset = 28; offset >= 1; offset--) {
        const d = new Date(today.getTime() - offset * 24 * 3600_000)
        const yy = d.getUTCFullYear()
        const mm = String(d.getUTCMonth() + 1).padStart(2, "0")
        const dd = String(d.getUTCDate()).padStart(2, "0")
        result.push(`${yy}-${mm}-${dd}`)
      }
      return result
    }

    /** Складывает 28-точечный массив для bar chart.
     *  raw: записи из WbCardOrdersDaily (qty > 0 only — дни без заказов в БД отсутствуют).
     *  Дни вне окна игнорируются. Дни без записей → qty=0.
     *  `now` — для тестов; в проде не задаётся.
     */
    export function fillTimeSeries(
      raw: Array<{ date: Date; qty: number }>,
      now?: Date,
    ): DayPoint[] {
      const window = getLast28DaysMsk(now)
      const byKey = new Map<string, number>()
      for (const r of raw) {
        // r.date — JS Date с time=00:00 UTC после Prisma @db.Date чтения.
        // Конвертируем в MSK YYYY-MM-DD ключ.
        const mskDate = new Date(r.date.getTime() + 3 * 3600_000)
        const yy = mskDate.getUTCFullYear()
        const mm = String(mskDate.getUTCMonth() + 1).padStart(2, "0")
        const dd = String(mskDate.getUTCDate()).padStart(2, "0")
        const key = `${yy}-${mm}-${dd}`
        byKey.set(key, (byKey.get(key) ?? 0) + r.qty)
      }
      return window.map((date) => ({ date, qty: byKey.get(date) ?? 0 }))
    }
    ```

    **2.2 Создать tests/wb-orders-chart-fill.test.ts**:

    ```typescript
    import { describe, it, expect } from "vitest"
    import { getLast28DaysMsk, fillTimeSeries } from "@/lib/wb-orders-chart"

    describe("getLast28DaysMsk", () => {
      it("возвращает 28 дат от today-28 до today-1 MSK", () => {
        // 2026-05-15 12:00 UTC = 15:00 MSK
        const now = new Date("2026-05-15T12:00:00Z")
        const days = getLast28DaysMsk(now)
        expect(days.length).toBe(28)
        expect(days[0]).toBe("2026-04-17") // today-28
        expect(days[27]).toBe("2026-05-14") // today-1 (вчера)
      })
    })

    describe("fillTimeSeries", () => {
      const now = new Date("2026-05-15T12:00:00Z")

      it("заполняет 28 точек с qty=0 при пустом input", () => {
        const ts = fillTimeSeries([], now)
        expect(ts.length).toBe(28)
        expect(ts.every(p => p.qty === 0)).toBe(true)
      })

      it("маппит запись с datе=2026-05-14 в последний элемент", () => {
        const ts = fillTimeSeries([{ date: new Date("2026-05-14"), qty: 5 }], now)
        expect(ts[27]).toEqual({ date: "2026-05-14", qty: 5 })
        expect(ts.slice(0, 27).every(p => p.qty === 0)).toBe(true)
      })

      it("игнорирует записи вне окна [today-28, today-1]", () => {
        const ts = fillTimeSeries([
          { date: new Date("2026-04-16"), qty: 100 }, // = today-29, вне окна
          { date: new Date("2026-05-15"), qty: 50 },  // = today, вне окна (полный день не считаем)
          { date: new Date("2026-04-17"), qty: 7 },   // = today-28, в окне (граница)
        ], now)
        expect(ts.length).toBe(28)
        expect(ts[0]).toEqual({ date: "2026-04-17", qty: 7 })
        expect(ts.reduce((s,p)=>s+p.qty, 0)).toBe(7) // 100 и 50 отброшены
      })
    })
    ```

    **2.3 Создать tests/wb-orders-chart-msk.test.ts** — W-4 fix: pinning MSK midnight boundary поведения:

    ```typescript
    import { describe, it, expect } from "vitest"
    import { getMskTodayDate, getMskYesterdayDate } from "@/lib/wb-orders-chart"

    describe("MSK date helpers — midnight boundary", () => {
      it("23:30 MSK still тот же день (UTC = 20:30 same day)", () => {
        // 2026-05-15T20:30Z = 23:30 MSK still 2026-05-15
        const now = new Date("2026-05-15T20:30:00Z")
        expect(getMskTodayDate(now).toISOString()).toBe("2026-05-15T00:00:00.000Z")
        expect(getMskYesterdayDate(now).toISOString()).toBe("2026-05-14T00:00:00.000Z")
      })

      it("00:30 MSK уже следующий день (UTC = 21:30 previous day)", () => {
        // 2026-05-15T21:30Z = 00:30 MSK next day = 2026-05-16
        const now = new Date("2026-05-15T21:30:00Z")
        expect(getMskTodayDate(now).toISOString()).toBe("2026-05-16T00:00:00.000Z")
        expect(getMskYesterdayDate(now).toISOString()).toBe("2026-05-15T00:00:00.000Z")
      })

      it("ровно полночь MSK (UTC = 21:00) — flip happened", () => {
        const now = new Date("2026-05-15T21:00:00Z")
        expect(getMskTodayDate(now).toISOString()).toBe("2026-05-16T00:00:00.000Z")
      })

      it("ровно перед полночью MSK (UTC = 20:59:59) — ещё прежний день", () => {
        const now = new Date("2026-05-15T20:59:59Z")
        expect(getMskTodayDate(now).toISOString()).toBe("2026-05-15T00:00:00.000Z")
      })
    })
    ```

    **2.4 Создать app/api/cron/wb-orders-daily/route.ts** (паттерн purge-deleted + бизнес-логика). W-3 fix: maxDuration=600 + console.log per iteration в helpers (Task 1):

    ```typescript
    // app/api/cron/wb-orders-daily/route.ts
    // GET /api/cron/wb-orders-daily — daily 05:00 МСК cron + auto-backfill при пустой таблице.
    // Защищён x-cron-secret == process.env.CRON_SECRET. Per D-03 (CONTEXT.md).
    // W-3 fix: maxDuration=600 (nginx уже до 600s — см. CLAUDE.md «WB Promotions Calendar API»).

    import { NextRequest, NextResponse } from "next/server"
    import { prisma } from "@/lib/prisma"
    import { fetchOrdersForRange, upsertOrdersDaily, WbRateLimitError } from "@/lib/wb-api"
    import { getMskYesterdayDate } from "@/lib/wb-orders-chart"

    export const runtime = "nodejs"
    export const maxDuration = 600

    const BACKFILL_START = new Date("2026-04-01T00:00:00")

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }

      try {
        const existing = await prisma.wbCardOrdersDaily.count()
        let dateFrom: Date
        let mode: "backfill" | "delta"

        if (existing === 0) {
          // Первый запуск — backfill с 2026-04-01
          dateFrom = BACKFILL_START
          mode = "backfill"
        } else {
          // Daily delta — вчерашний день MSK 00:00 (W-4 fix: shared helper)
          dateFrom = getMskYesterdayDate()
          mode = "delta"
        }

        console.log(`[wb-orders-daily cron] start mode=${mode} dateFrom=${dateFrom.toISOString()}`)
        const rows = await fetchOrdersForRange(dateFrom)
        const { upserted } = await upsertOrdersDaily(rows)
        console.log(`[wb-orders-daily cron] done mode=${mode} fetched=${rows.length} upserted=${upserted}`)

        return NextResponse.json({
          ok: true,
          mode,
          dateFrom: dateFrom.toISOString(),
          rowsFetched: rows.length,
          upserted,
        })
      } catch (err) {
        if (err instanceof WbRateLimitError) {
          return NextResponse.json(
            { ok: false, error: "rate-limit", retryAfterSec: err.retryAfterSec },
            { status: 429 }
          )
        }
        console.error("[wb-orders-daily cron] error:", err)
        return NextResponse.json(
          { ok: false, error: (err as Error).message },
          { status: 500 }
        )
      }
    }
    ```

    **2.5 Создать app/api/wb-orders-backfill/route.ts** — manual re-run для UI-кнопки (D-04 одноразовый backfill + кнопка для re-sync). B-1 fix: requireSection("PRODUCTS", "MANAGE") — раздела "CARDS" в ERP_SECTION enum нет, все existing actions на /cards/wb используют PRODUCTS (см. app/actions/wb-cards.ts):

    ```typescript
    // app/api/wb-orders-backfill/route.ts
    // POST /api/wb-orders-backfill — ручной re-run backfill с 2026-04-01.
    // Защищён сессией + requireSection("PRODUCTS", "MANAGE").
    // B-1 fix: ERP_SECTION enum НЕ содержит "CARDS" — все /cards/wb actions использует "PRODUCTS".
    // W-3 fix: maxDuration=600 (backfill 45 дней может занять до 1-2 мин из-за rate limit).

    import { NextResponse } from "next/server"
    import { requireSection } from "@/lib/rbac"
    import { fetchOrdersForRange, upsertOrdersDaily, WbRateLimitError } from "@/lib/wb-api"

    export const runtime = "nodejs"
    export const maxDuration = 600

    const BACKFILL_START = new Date("2026-04-01T00:00:00")

    export async function POST(): Promise<NextResponse> {
      try {
        await requireSection("PRODUCTS", "MANAGE")
      } catch {
        return NextResponse.json({ error: "Нет прав" }, { status: 403 })
      }

      try {
        console.log(`[wb-orders-backfill] start dateFrom=${BACKFILL_START.toISOString()}`)
        const rows = await fetchOrdersForRange(BACKFILL_START)
        const { upserted } = await upsertOrdersDaily(rows)
        console.log(`[wb-orders-backfill] done fetched=${rows.length} upserted=${upserted}`)
        return NextResponse.json({
          ok: true,
          dateFrom: BACKFILL_START.toISOString(),
          rowsFetched: rows.length,
          upserted,
        })
      } catch (err) {
        if (err instanceof WbRateLimitError) {
          return NextResponse.json(
            { ok: false, error: "rate-limit", retryAfterSec: err.retryAfterSec },
            { status: 429 }
          )
        }
        console.error("[wb-orders-backfill] error:", err)
        return NextResponse.json(
          { ok: false, error: (err as Error).message },
          { status: 500 }
        )
      }
    }
    ```

    **Verify requireSection signature** перед использованием: см. `lib/rbac.ts` — стандартный паттерн `await requireSection("PRODUCTS", "MANAGE")` throws при отсутствии прав. Если в твоём проекте сигнатура другая (например, выкидывает Response/NextResponse сама) — адаптируй try/catch. См. также existing usage в `app/actions/wb-cards.ts`.

    Запуск тестов: `npm run test -- tests/wb-orders-chart-fill.test.ts tests/wb-orders-chart-msk.test.ts` — все тесты проходят.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-orders-chart-fill.test.ts tests/wb-orders-chart-msk.test.ts</automated>
  </verify>
  <done>
    - lib/wb-orders-chart.ts экспортирует `getMskTodayDate`, `getMskYesterdayDate`, `getLast28DaysMsk`, `fillTimeSeries` (pure, без import-side effects)
    - app/api/cron/wb-orders-daily/route.ts существует, GET handler с x-cron-secret guard, авто-выбор backfill vs delta, `export const maxDuration = 600`, использует `getMskYesterdayDate` (не inline MSK math)
    - app/api/wb-orders-backfill/route.ts существует, POST handler с `requireSection("PRODUCTS","MANAGE")` (НЕ "CARDS"!), `export const maxDuration = 600`
    - `npm run test -- tests/wb-orders-chart-fill.test.ts tests/wb-orders-chart-msk.test.ts` — все тесты (4 fill + 4 msk = 8) зелёные
    - `grep "CARDS" app/api/wb-orders-backfill/route.ts` — пусто (no false-positive enum value)
    - `curl -X GET http://localhost:3001/api/cron/wb-orders-daily` без secret → 401 (после `npm run dev`)
    - `npx tsc --noEmit` без новых ошибок
  </done>
</task>

<task type="auto">
  <name>Task 3: shadcn chart + WbCardOrdersChart + expandable row в WbCardsTable + backfill button + page query</name>
  <files>
    package.json,
    components/ui/chart.tsx,
    components/cards/WbCardOrdersChart.tsx,
    components/cards/WbOrdersBackfillButton.tsx,
    components/cards/WbCardsTable.tsx,
    app/(dashboard)/cards/wb/page.tsx
  </files>
  <action>
    **3.1 Установить recharts + shadcn chart wrapper:**

    ```bash
    npm install recharts
    npx shadcn@latest add chart
    ```

    `npx shadcn@latest add chart` создаст `components/ui/chart.tsx` (типизированный wrapper над recharts: `ChartContainer`, `ChartTooltip`, `ChartTooltipContent`, `ChartLegend`). Если CLI просит подтверждение overwrite или style — используй default (base-nova, уже настроенный в проекте). Если файл уже создан существующим проектом — пропусти.

    Verify: `package.json` содержит `"recharts"`, `components/ui/chart.tsx` существует и компилируется.

    **3.2 Создать components/cards/WbCardOrdersChart.tsx:**

    ```tsx
    "use client"
    import { BarChart, Bar, XAxis, CartesianGrid } from "recharts"
    import {
      ChartContainer,
      ChartTooltip,
      ChartTooltipContent,
      type ChartConfig,
    } from "@/components/ui/chart"

    interface DayPoint { date: string; qty: number }

    interface Props {
      nmId: number
      timeSeries: DayPoint[] // 28 точек, qty=0 для дней без заказов
    }

    const chartConfig = {
      qty: { label: "Заказы", color: "hsl(var(--primary))" },
    } satisfies ChartConfig

    export function WbCardOrdersChart({ nmId, timeSeries }: Props) {
      // CONTEXT.md §График: 28 баров. Среднее за месяц = sum(28)/28. Среднее за 7 = sum(last 7)/7.
      const last7 = timeSeries.slice(-7)
      const sumAll = timeSeries.reduce((s, d) => s + d.qty, 0)
      const sum7 = last7.reduce((s, d) => s + d.qty, 0)
      const avg30d = timeSeries.length > 0 ? sumAll / timeSeries.length : 0
      const avg7d = last7.length > 0 ? sum7 / last7.length : 0

      return (
        <div className="p-4 grid grid-cols-[1fr_auto] gap-6 items-center">
          <div>
            <div className="text-xs text-muted-foreground mb-1">
              Заказы за 28 дней (nm {nmId})
            </div>
            <ChartContainer config={chartConfig} className="h-40 w-full">
              <BarChart data={timeSeries} margin={{ top: 4, right: 4, left: 4, bottom: 4 }}>
                <CartesianGrid vertical={false} strokeDasharray="2 2" opacity={0.3} />
                <XAxis
                  dataKey="date"
                  tickFormatter={(s: string) => s.slice(5)} // MM-DD
                  fontSize={10}
                  tickLine={false}
                  axisLine={false}
                  interval={3}
                />
                <ChartTooltip
                  content={
                    <ChartTooltipContent
                      labelFormatter={(label) => `Дата: ${label}`}
                    />
                  }
                />
                <Bar dataKey="qty" fill="var(--color-qty)" radius={[2, 2, 0, 0]} />
              </BarChart>
            </ChartContainer>
          </div>
          <div className="flex flex-col gap-4 text-sm min-w-[140px] pr-4">
            <div>
              <div className="text-muted-foreground text-xs">Ср. за месяц</div>
              <div className="text-lg font-medium">{avg30d.toFixed(1)} <span className="text-xs text-muted-foreground">/ день</span></div>
            </div>
            <div>
              <div className="text-muted-foreground text-xs">Ср. за 7 дней</div>
              <div className="text-lg font-medium">{avg7d.toFixed(1)} <span className="text-xs text-muted-foreground">/ день</span></div>
            </div>
          </div>
        </div>
      )
    }
    ```

    **3.3 Создать components/cards/WbOrdersBackfillButton.tsx** — кнопка ручного backfill в шапке:

    ```tsx
    "use client"
    import { useState } from "react"
    import { toast } from "sonner"
    import { useRouter } from "next/navigation"
    import { Button } from "@/components/ui/button"
    import { History } from "lucide-react"

    export function WbOrdersBackfillButton() {
      const [loading, setLoading] = useState(false)
      const router = useRouter()

      async function handleClick() {
        if (!confirm("Перезагрузить заказы с 2026-04-01? Может занять до минуты.")) return
        setLoading(true)
        const id = toast.loading("Backfill заказов с 2026-04-01…")
        try {
          const res = await fetch("/api/wb-orders-backfill", { method: "POST" })
          const data = await res.json()
          toast.dismiss(id)
          if (data.ok) {
            toast.success(`Backfill завершён: ${data.upserted} строк`)
            router.refresh()
          } else if (res.status === 429) {
            toast.error(`Rate limit WB: retry through ${data.retryAfterSec ?? "?"}s`)
          } else {
            toast.error(data.error ?? "Ошибка backfill")
          }
        } catch (e) {
          toast.dismiss(id)
          toast.error((e as Error).message)
        } finally {
          setLoading(false)
        }
      }

      return (
        <Button variant="outline" size="sm" disabled={loading} onClick={handleClick}>
          <History className="h-4 w-4 mr-1" />
          Backfill заказов
        </Button>
      )
    }
    ```

    **3.4 Изменения в components/cards/WbCardsTable.tsx** — добавить expand-state + onClick row + expanded TableRow:

    a) Добавить импорты в начале файла (после существующих):
    ```typescript
    import { useState, useTransition, Fragment } from "react"
    import { motion, AnimatePresence } from "motion/react"
    import { WbCardOrdersChart } from "@/components/cards/WbCardOrdersChart"
    import { cn } from "@/lib/utils"
    ```
    (если `cn` уже импортирован — пропустить; Fragment добавить к существующему `useState, useTransition` импорту React).

    b) Добавить новое prop в `WbCardsTableProps`:
    ```typescript
    ordersTimeSeries: Record<string, Array<{ date: string; qty: number }>>
    // ключ = String(nmId), value = 28-точечный массив
    ```

    c) Добавить state ниже `const [searchingProducts, setSearchingProducts] = useState(false)`:
    ```typescript
    const [expandedId, setExpandedId] = useState<string | null>(null)
    ```

    d) Заменить существующую строку `{cards.map((card) => (` + `<TableRow key={card.id} ...>` на Fragment-wrapped pattern с onClick row и expanded TableRow ниже. Подсказка: текущий код:
    ```tsx
    {cards.map((card) => (
      <TableRow key={card.id} className={selected.has(card.id) ? "bg-muted/50" : ""}>
        {/* 19 cells */}
      </TableRow>
    ))}
    ```
    Стать:
    ```tsx
    {cards.map((card) => {
      const isExpanded = expandedId === card.id
      const ts = ordersTimeSeries[String(card.nmId)] ?? []
      return (
        <Fragment key={card.id}>
          <TableRow
            className={cn(
              "cursor-pointer hover:bg-muted/30",
              selected.has(card.id) && "bg-muted/50",
              isExpanded && "bg-muted/40"
            )}
            onClick={() => setExpandedId(isExpanded ? null : card.id)}
          >
            {/* 19 cells без изменений */}
          </TableRow>
          <AnimatePresence initial={false}>
            {isExpanded && (
              <TableRow key={`${card.id}-expand`} className="hover:bg-transparent">
                <TableCell colSpan={19} className="bg-muted/10 p-0 border-b">
                  <motion.div
                    initial={{ height: 0, opacity: 0 }}
                    animate={{ height: "auto", opacity: 1 }}
                    exit={{ height: 0, opacity: 0 }}
                    transition={{ duration: 0.2, ease: "easeOut" }}
                    className="overflow-hidden"
                  >
                    <WbCardOrdersChart nmId={card.nmId} timeSeries={ts} />
                  </motion.div>
                </TableCell>
              </TableRow>
            )}
          </AnimatePresence>
        </Fragment>
      )
    })}
    ```

    e) **stopPropagation guard** — Checkbox cell (line 358-365) и Артикул cell (390-399) уже изолированы (Checkbox base-ui гасит, артикул-cell уже имеет stopPropagation). Но для безопасности на Checkbox `<TableCell>` (line 358) добавить `onClick={(e) => e.stopPropagation()}` — чтобы клик в pad вокруг checkbox не раскрывал row. Также добавить `onClick={(e) => e.stopPropagation()}` на Tooltip-cell (line 375) — клик по truncated имени не должен раскрывать (хотя это allowed по UX, на phase спор не критично — оставим non-propagating, потому что raskrytie происходит по клику ВЕЗДЕ в row кроме интерактивных элементов). Проверь визуально: чекбокс toggleSelect работает БЕЗ раскрытия, артикул копируется БЕЗ раскрытия, клик по фото/бренду/цене — раскрывает.

    Решение: добавить stopPropagation на Checkbox cell, Артикул cell уже имеет. Tooltip-cell оставить без — клик по имени раскроет (это естественно).

    **3.5 Изменения в app/(dashboard)/cards/wb/page.tsx** — query WbCardOrdersDaily + передача props + backfill button. W-4 fix: использовать `getMskTodayDate` вместо inline MSK math:

    a) Добавить запрос timeseries для visible nmIds после block `cards = await prisma.wbCard.findMany(...)`:

    ```typescript
    // 28-дневное окно для bar chart (MSK) — W-4 fix: shared helper, не inline math
    import { getMskTodayDate, fillTimeSeries } from "@/lib/wb-orders-chart"
    // (импорт добавить в начало файла, рядом с другими imports)

    const todayMsk = getMskTodayDate()
    const windowStart = new Date(todayMsk.getTime() - 28 * 24 * 3600_000)
    const windowEnd = new Date(todayMsk.getTime() - 1 * 24 * 3600_000) // today - 1 (вчера), включительно

    const visibleNmIds = cards.map((c) => c.nmId)
    const ordersRows = visibleNmIds.length > 0
      ? await prisma.wbCardOrdersDaily.findMany({
          where: {
            nmId: { in: visibleNmIds },
            date: { gte: windowStart, lte: windowEnd },
          },
          select: { nmId: true, date: true, qty: true },
        })
      : []

    // Группировка → Record<nmId, DayPoint[]>
    const byNm = new Map<number, Array<{ date: Date; qty: number }>>()
    for (const r of ordersRows) {
      const arr = byNm.get(r.nmId) ?? []
      arr.push({ date: r.date, qty: r.qty })
      byNm.set(r.nmId, arr)
    }
    const ordersTimeSeries: Record<string, Array<{ date: string; qty: number }>> = {}
    for (const nmId of visibleNmIds) {
      ordersTimeSeries[String(nmId)] = fillTimeSeries(byNm.get(nmId) ?? [])
    }
    ```

    Static import предпочтителен (без dynamic import).

    b) Передать prop в `<WbCardsTable>`:
    ```tsx
    <WbCardsTable
      ...
      ordersTimeSeries={ordersTimeSeries}
    />
    ```

    c) Добавить `<WbOrdersBackfillButton />` в шапку рядом с `<WbSyncButton />` (внутри `<div className="flex gap-2">`):
    ```tsx
    <div className="flex gap-2">
      <WbOrdersBackfillButton />
      <WbUploadIuButton />
      <WbSyncRatingsButton />
      <WbSyncSppButton />
      <WbSyncButton />
    </div>
    ```
    И добавить import: `import { WbOrdersBackfillButton } from "@/components/cards/WbOrdersBackfillButton"`.

    **3.6 Verification:**
    - `npx tsc --noEmit` — нет новых TS-ошибок
    - `npm run lint` — нет новых ESLint-ошибок
    - `npm run test` — все существующие тесты + 3 новых файла зелёные
    - `npm run build` — проходит без ошибок

    **Per CLAUDE.md gotcha (sticky):** `<thead className="bg-background">` + `<TableHead className="sticky top-0 z-20 bg-background border-b">` оставить как есть. Expanded `<TableRow>` НЕ должна иметь `position: relative` / `z-index` — обычный поток, не перекроет sticky header.

    **Per D-05 (CONTEXT.md UI способ раскрытия):** Single-open реализован через `useState<string | null>` (а не Set). Клик по другой строке вызовет `setExpandedId(card.id)` — старая закроется автоматически.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test</automated>
  </verify>
  <done>
    - `npm install recharts` добавил пакет в package.json dependencies
    - components/ui/chart.tsx существует (shadcn-charts wrapper)
    - components/cards/WbCardOrdersChart.tsx рендерит recharts BarChart с 28 барами + 2 числа (avg30d, avg7d)
    - components/cards/WbOrdersBackfillButton.tsx существует и POST'ит на /api/wb-orders-backfill
    - components/cards/WbCardsTable.tsx: import Fragment + motion, expandedId state, onClick row, AnimatePresence + motion.div для expanded TableRow с colSpan=19, stopPropagation на Checkbox cell
    - app/(dashboard)/cards/wb/page.tsx импортирует и использует `getMskTodayDate` + `fillTimeSeries` из `lib/wb-orders-chart` (не дублирует inline MSK math), запрашивает WbCardOrdersDaily для visible nmIds, передаёт ordersTimeSeries в WbCardsTable, рендерит <WbOrdersBackfillButton /> в шапке
    - `npm run build` проходит без ошибок
    - `npm run test` — все тесты зелёные (включая 3 новых файла из Task 1-2)
    - Manual smoke (dev): запустить `npm run dev`, открыть /cards/wb, клик по строке → раскрывается панель; клик по другой строке → переключается; клик по чекбоксу → НЕ раскрывает, переключает selection; sticky header не дёргается при scroll.
  </done>
</task>

</tasks>

<verification>
## Phase-Level Verification

### Automated
- `npm run test` — все тесты зелёные (5 tests в `wb-card-orders-daily.test.ts` + 4 в `wb-orders-chart-fill.test.ts` + 4 в `wb-orders-chart-msk.test.ts` + все existing).
- `npm run build` — production build проходит.
- `npx tsc --noEmit` — нет TS-ошибок.
- `npx prisma format && npx prisma generate` — schema валидна, client сгенерирован.

### Manual (dev, после `npm run dev`)
- `curl http://localhost:3001/api/cron/wb-orders-daily` → 401 (без x-cron-secret).
- `curl -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/wb-orders-daily` → 401 локально без env, OK; на VPS после deploy → 200 с `mode: "backfill"` или `delta`.
- В UI: `/cards/wb` → клик по строке → раскрывается панель с chart + 2 числами; клик по другой → переключается. Чекбокс и артикул работают как раньше.

### Manual (prod, после deploy на VPS)
- `bash deploy.sh` применит миграцию `20260515_wb_card_orders_daily`.
- Создать systemd unit `/etc/systemd/system/zoiten-wb-orders.{service,timer}` (инструкции — в SUMMARY.md, executor пишет, user деплоит).
- `systemctl daemon-reload && systemctl enable --now zoiten-wb-orders.timer`.
- `systemctl start zoiten-wb-orders.service` — manual trigger первого backfill → проверить таблицу: `psql -d zoiten_erp -c "SELECT COUNT(*) FROM \"WbCardOrdersDaily\";"` → > 0.
- В логах backfill виден прогресс: `journalctl -u zoiten-wb-orders.service -f` должен показывать `[wb-orders backfill] page=N rowsReturned=X total=Y` + `[wb-orders upsert] chunk=N/total processed=X`.
- Открыть https://zoiten.pro/cards/wb → раскрыть строку → bar chart с реальными данными за 28 дней.

### Wave 0 Gaps (per RESEARCH.md)
- [x] `tests/wb-card-orders-daily.test.ts` — golden test (Task 1)
- [x] `tests/wb-orders-chart-fill.test.ts` — fill helper test (Task 2)
- [x] `tests/wb-orders-chart-msk.test.ts` — MSK midnight boundary test (Task 2, W-4 fix)
- [x] `prisma/migrations/20260515_wb_card_orders_daily/migration.sql` — ручная миграция (Task 1)
- [x] systemd unit files — инструкции пишутся в SUMMARY.md (Task 3 done критерий + output section)
</verification>

<success_criteria>
- Все 4 требования выполнены:
  - M5O-01: схема + миграция применима (применится через deploy.sh)
  - M5O-02: backfill через `fetchOrdersForRange` (auto при пустой БД + manual через UI), с per-iteration logging
  - M5O-03: cron endpoint + инструкции для systemd timer 05:00 МСК в SUMMARY, maxDuration=600
  - M5O-04: expandable row + bar chart 28 баров + avg30d/avg7d числа
- `npm run build` + `npm run test` оба зелёные.
- Pre-deploy checklist: schema.prisma + migration.sql synced, `npx prisma generate` без ошибок.
- RBAC: оба endpoint используют существующий ERP_SECTION enum (`PRODUCTS` для backfill, x-cron-secret для cron); никаких ссылок на несуществующий `"CARDS"`.
- MSK date math не дублируется — все три места (cron, page.tsx, chart helper) идут через `getMskTodayDate`/`getMskYesterdayDate` из `lib/wb-orders-chart.ts`.
</success_criteria>

<output>
После завершения создать `.planning/quick/260515-m5o-cards-wb-expandable-row-4-7-wbcardorders/260515-m5o-SUMMARY.md` со следующим содержимым (помимо стандартных секций per template):

## VPS Deploy Steps (для пользователя)

После `git pull && bash deploy.sh` (применит миграцию через `prisma migrate deploy`):

1. Создать `/etc/systemd/system/zoiten-wb-orders.service`:
   ```ini
   [Unit]
   Description=Zoiten WB orders daily sync runner
   After=network.target zoiten-erp.service

   [Service]
   Type=oneshot
   EnvironmentFile=/etc/zoiten.pro.env
   ExecStart=/usr/bin/curl -fsS -X GET -H "x-cron-secret: ${CRON_SECRET}" http://localhost:3001/api/cron/wb-orders-daily
   ```

2. Создать `/etc/systemd/system/zoiten-wb-orders.timer`:
   ```ini
   [Unit]
   Description=Zoiten WB orders daily sync (05:00 MSK)

   [Timer]
   OnCalendar=*-*-* 05:00:00 Europe/Moscow
   Persistent=true

   [Install]
   WantedBy=timers.target
   ```

3. Активировать:
   ```bash
   systemctl daemon-reload
   systemctl enable --now zoiten-wb-orders.timer
   systemctl start zoiten-wb-orders.service  # первый backfill вручную
   systemctl list-timers | grep zoiten-wb-orders  # проверка
   journalctl -u zoiten-wb-orders.service -n 50  # log
   ```

4. Проверка БД:
   ```bash
   psql -U zoiten -d zoiten_erp -c 'SELECT COUNT(*), MIN(date), MAX(date) FROM "WbCardOrdersDaily";'
   ```
</output>
