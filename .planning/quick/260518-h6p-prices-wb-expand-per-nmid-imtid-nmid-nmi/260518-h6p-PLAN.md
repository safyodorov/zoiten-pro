---
phase: quick-260518-h6p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/prices/wb/page.tsx
  - components/prices/PriceCalculatorTable.tsx
autonomous: true
requirements:
  - QUICK-260518-h6p
must_haves:
  truths:
    - "В expand-панели /prices/wb для каждого nmId под графиком рендерятся ДВЕ строки отзывов: «По связке» (по imtId) и «По товару» (по nmId)"
    - "Если по связке нет ни одного отзыва — строка «По связке» полностью скрывается (включая подпись)"
    - "Если по товару (nmId) нет ни одного отзыва — строка «По товару» полностью скрывается"
    - "Если imtId карточки = null — строка «По связке» не рендерится"
    - "Подпись строки содержит счётчик: «По связке (N)», «По товару (N)»"
    - "Каждая строка — горизонтальная лента до 10 свежих ReviewChip (звёзды + цветовая шкала, существующий компонент)"
    - "Существующие 4 метрики (Остаток / Остаток в днях / Рейтинг связки / Кол-во оценок) остаются без изменений выше лент"
    - "На /cards/wb ничего не меняется — изменения изолированы в /prices/wb"
  artifacts:
    - path: "app/(dashboard)/prices/wb/page.tsx"
      provides: "Загрузка feedbacks по всем nmId внутри склеек visibleNmIds + группировка в shape Record<nmId, { byImt, byNmId }>"
      contains: "imtIdsByNmId"
    - path: "components/prices/PriceCalculatorTable.tsx"
      provides: "ProductGroup.ordersCharts[].reviews → { byImt: FeedbackItem[]; byNmId: FeedbackItem[] }; NmIdLegend рендерит две строки лент с подписями и счётчиками; пустые строки скрыты"
      contains: "byImt"
  key_links:
    - from: "page.tsx (visibleNmIds + imtId)"
      to: "page.tsx reviewsRaw findMany"
      via: "nmId IN [all nmIds, принадлежащих тем же imtId-ам, что и visibleNmIds]"
      pattern: "nmId: \\{ in:"
    - from: "page.tsx reviewsByNmId aggregation"
      to: "components/prices/PriceCalculatorTable.tsx NmIdLegend props"
      via: "reviews: { byImt: FeedbackItem[]; byNmId: FeedbackItem[] }"
      pattern: "byImt"
    - from: "PriceCalculatorTable NmIdLegend"
      to: "ReviewChip (existing component)"
      via: "reviews.byImt.map / reviews.byNmId.map"
      pattern: "<ReviewChip"
---

<objective>
В /prices/wb expand-панели разделить ленту отзывов на ДВЕ строки per nmId-блок: верхняя — «По связке (N)» (по imtId, объединяет варианты товара), нижняя — «По товару (N)» (по nmId, текущее поведение). Пустая строка скрывается полностью. Если imtId = null, строка «По связке» не рендерится.

Purpose: Пользователь видит и отзывы конкретного варианта (nmId), и сводный feed всей склейки (imtId) — типичный кейс одежды/обуви, где варианты по размеру/цвету = отдельные nmId внутри одной imtId.

Output: Два изменённых файла, single-commit. Бандл /prices/wb ожидаемое изменение: ±0.3 kB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260518-gg3-cards-wb-prices-wb-1-single-expand-price/260518-gg3-SUMMARY.md
@.planning/quick/260514-mci-cards-wb/260514-mci-SUMMARY.md
@app/(dashboard)/prices/wb/page.tsx
@components/prices/PriceCalculatorTable.tsx

<interfaces>
<!-- Текущая shape per-nmId reviews (после quick 260518-gg3) -->

В page.tsx сейчас:
```typescript
type FeedbackItem = {
  id: string
  rating: number
  text: string
  createdAt: string // ISO
}

const reviewsByNmId: Record<number, FeedbackItem[]> = {}
// top-10 desc per nmId (FEEDBACK channel, rating IS NOT NULL)

productNmIdsWithCharts.push({
  nmId, timeSeries, stockQty, avgSalesSpeed7d, rating, reviewsTotal,
  reviews: reviewsByNmId[card.nmId] ?? [],
})
```

В PriceCalculatorTable.tsx:
```typescript
type ProductGroup = {
  // ...
  ordersCharts?: Array<{
    nmId: number
    timeSeries: DayPoint[]
    stockQty: number | null
    avgSalesSpeed7d: number | null
    rating: number | null
    reviewsTotal: number | null
    reviews: Array<{ id: string; rating: number; text: string; createdAt: string }>
  }>
}

function NmIdLegend({ stockQty, daysLeft, rating, reviewsTotal, reviews })
// рендерит 4 метрики + одну ленту chips (если reviews.length > 0)
```

WbCard schema (prisma/schema.prisma):
```
WbCard {
  nmId Int @unique
  imtId Int?            // id склейки, добавлен в quick 260514-mci
  ...
}
```

ReviewChip — существующий компонент (PriceCalculatorTable.tsx:525-562), переиспользуется без изменений.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — загрузка feedbacks по всем nmId внутри склеек + группировка в { byImt, byNmId } per nmId</name>
  <files>app/(dashboard)/prices/wb/page.tsx</files>
  <action>
    Изменения в файле `app/(dashboard)/prices/wb/page.tsx`:

    1. **Построить indexes по imtId** (после блока, где собираются `visibleNmIds`, до текущего `reviewsRaw` findMany — секция «6.6. Загрузить последние отзывы per nmId»):

    ```typescript
    // quick 260518-h6p: индексы для группировки отзывов по imtId.
    // imtIdsByNmId: для каждого visible nmId → его imtId (или null).
    // nmIdsByImtId: для каждой imtId → ВСЕ nmId этой склейки (включая невидимые!).
    //   Нам нужны feedbacks с любого nmId склейки — даже если конкретный вариант
    //   отфильтрован (cardsInStockOnly), его отзывы должны попасть в byImt.
    const imtIdsByNmId = new Map<number, number | null>()
    for (const c of wbCards) {
      if (visibleNmIds.includes(c.nmId)) imtIdsByNmId.set(c.nmId, c.imtId ?? null)
    }
    const relevantImtIds = Array.from(
      new Set(Array.from(imtIdsByNmId.values()).filter((v): v is number => v != null)),
    )

    // Подтянуть ВСЕ WbCard с этими imtId (нам нужны их nmId для расширения запроса
    // SupportTicket). Используем lightweight select, soft-deleted тоже включаем —
    // удалённая карточка может содержать релевантные исторические отзывы.
    const cardsInImts = relevantImtIds.length > 0
      ? await prisma.wbCard.findMany({
          where: { imtId: { in: relevantImtIds } },
          select: { nmId: true, imtId: true },
        })
      : []

    const nmIdsByImtId = new Map<number, number[]>()
    for (const c of cardsInImts) {
      if (c.imtId == null) continue
      const arr = nmIdsByImtId.get(c.imtId) ?? []
      arr.push(c.nmId)
      nmIdsByImtId.set(c.imtId, arr)
    }

    // allRelatedNmIds = union(visibleNmIds, всех nmId всех relevant imtId)
    const allRelatedNmIds = Array.from(
      new Set([
        ...visibleNmIds,
        ...cardsInImts.map((c) => c.nmId),
      ]),
    )
    ```

    2. **Расширить existing `reviewsRaw` findMany** — заменить `nmId: { in: visibleNmIds }` на `nmId: { in: allRelatedNmIds }` (и условие `visibleNmIds.length > 0` оставить как было). Остальной select/orderBy не трогать.

    3. **Переделать aggregation `reviewsByNmId`** — теперь это `Record<number, { byImt: FeedbackItem[]; byNmId: FeedbackItem[] }>`:

    ```typescript
    // Группируем feedbacks один раз по nmId.
    const allByNmId = new Map<number, FeedbackItem[]>()
    for (const t of reviewsRaw) {
      if (t.nmId == null || t.rating == null) continue
      const arr = allByNmId.get(t.nmId) ?? []
      const text = t.messages[0]?.text ?? t.previewText ?? ""
      arr.push({
        id: t.id,
        rating: t.rating,
        text,
        createdAt: t.createdAt.toISOString(),
      })
      allByNmId.set(t.nmId, arr)
    }

    // Per visibleNmId формируем два feed:
    //  - byNmId: top-10 desc только по этому nmId
    //  - byImt: top-10 desc по ВСЕМ nmId той же склейки (если imtId есть)
    // reviewsRaw уже отсортирован orderBy createdAt desc → slice(0,10) даст top-10.
    const reviewsByNmId: Record<
      number,
      { byImt: FeedbackItem[]; byNmId: FeedbackItem[] }
    > = {}
    for (const nmId of visibleNmIds) {
      const imtId = imtIdsByNmId.get(nmId) ?? null
      const byNmIdArr = (allByNmId.get(nmId) ?? []).slice(0, 10)
      let byImtArr: FeedbackItem[] = []
      if (imtId != null) {
        const siblingNmIds = nmIdsByImtId.get(imtId) ?? []
        // Собираем feedbacks всех nmId склейки, сортировка по createdAt desc.
        const merged: FeedbackItem[] = []
        for (const s of siblingNmIds) {
          const arr = allByNmId.get(s)
          if (arr) merged.push(...arr)
        }
        merged.sort((a, b) => (a.createdAt < b.createdAt ? 1 : a.createdAt > b.createdAt ? -1 : 0))
        byImtArr = merged.slice(0, 10)
      }
      reviewsByNmId[nmId] = { byImt: byImtArr, byNmId: byNmIdArr }
    }
    ```

    Старый цикл заполнения `reviewsByNmId` УДАЛИТЬ (строки ~377-389 текущего файла). FeedbackItem type оставить.

    4. **Изменить `productNmIdsWithCharts` shape** — поле `reviews` теперь объект, а не массив:

    ```typescript
    const productNmIdsWithCharts: Array<{
      nmId: number
      timeSeries: DayPoint[]
      stockQty: number | null
      avgSalesSpeed7d: number | null
      rating: number | null
      reviewsTotal: number | null
      reviews: { byImt: FeedbackItem[]; byNmId: FeedbackItem[] }
    }> = []
    ```

    И в push:
    ```typescript
    reviews: reviewsByNmId[card.nmId] ?? { byImt: [], byNmId: [] },
    ```

    **Важно: не трогать `wbCards` findMany select** — он без select, возвращает все поля, включая `imtId`. Никаких других изменений в page.tsx не нужно.

    **Защита от пустого relevantImtIds:** если ни одна visible карточка не имеет imtId, `cardsInImts = []`, `nmIdsByImtId` пуста, и `allRelatedNmIds = visibleNmIds` — поведение деградирует к исходному состоянию gracefully.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "prices/wb/page\.tsx|error TS" | head -20</automated>
  </verify>
  <done>tsc returns 0 errors на page.tsx. Файл компилируется. Shape данных, передаваемых в PriceCalculatorTable, изменён: каждая запись `ordersCharts[i].reviews` теперь объект `{ byImt: FeedbackItem[]; byNmId: FeedbackItem[] }`, при этом TS ругается на consumer (PriceCalculatorTable.tsx) — это ожидаемо, фиксится в Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Client — обновить ProductGroup type + NmIdLegend рендерит две строки лент с подписями и счётчиками</name>
  <files>components/prices/PriceCalculatorTable.tsx</files>
  <action>
    Изменения в файле `components/prices/PriceCalculatorTable.tsx`:

    1. **Обновить type ProductGroup.ordersCharts** (строки ~182-190 текущего файла). Заменить:
    ```typescript
    reviews: Array<{ id: string; rating: number; text: string; createdAt: string }>
    ```
    На:
    ```typescript
    reviews: {
      byImt: Array<{ id: string; rating: number; text: string; createdAt: string }>
      byNmId: Array<{ id: string; rating: number; text: string; createdAt: string }>
    }
    ```

    2. **Обновить компонент `NmIdLegend`** (строки ~564-608). Заменить prop `reviews` и логику рендера:

    ```typescript
    function NmIdLegend({
      stockQty,
      daysLeft,
      rating,
      reviewsTotal,
      reviews,
    }: {
      stockQty: number | null
      daysLeft: number | null
      rating: number | null
      reviewsTotal: number | null
      reviews: {
        byImt: Array<{ id: string; rating: number; text: string; createdAt: string }>
        byNmId: Array<{ id: string; rating: number; text: string; createdAt: string }>
      }
    }) {
      return (
        <div className="max-w-[640px] px-2 pb-2 flex flex-col gap-2">
          {/* Метаданные — без изменений */}
          <div className="flex flex-row gap-4 text-xs">
            <LegendItem
              label="Остаток"
              value={stockQty != null ? `${stockQty} шт` : "—"}
            />
            <LegendItem
              label="Остаток в днях"
              value={daysLeft != null ? `${daysLeft} дн` : "—"}
            />
            <LegendItem
              label="Рейтинг связки"
              value={rating != null ? rating.toFixed(1) : "—"}
            />
            <LegendItem
              label="Кол-во оценок"
              value={reviewsTotal != null ? `${reviewsTotal}` : "—"}
            />
          </div>
          {/* quick 260518-h6p: две строки отзывов — по связке и по товару.
              Пустые ленты НЕ рендерятся (вместе с подписью). */}
          {reviews.byImt.length > 0 && (
            <div className="flex flex-row items-center gap-2">
              <span className="text-[10px] text-muted-foreground whitespace-nowrap">
                По связке ({reviews.byImt.length})
              </span>
              <div className="flex flex-row gap-1 flex-wrap">
                {reviews.byImt.map((r) => (
                  <ReviewChip key={`imt-${r.id}`} review={r} />
                ))}
              </div>
            </div>
          )}
          {reviews.byNmId.length > 0 && (
            <div className="flex flex-row items-center gap-2">
              <span className="text-[10px] text-muted-foreground whitespace-nowrap">
                По товару ({reviews.byNmId.length})
              </span>
              <div className="flex flex-row gap-1 flex-wrap">
                {reviews.byNmId.map((r) => (
                  <ReviewChip key={`nm-${r.id}`} review={r} />
                ))}
              </div>
            </div>
          )}
        </div>
      )
    }
    ```

    Ключевые моменты:
    - Подпись слева (whitespace-nowrap), лента справа. Это компактнее, чем подпись над лентой (заголовок отдельной строкой). Если хочется иначе — можно flex-col, но flex-row item-center занимает меньше места по высоте.
    - Префиксы key `imt-` и `nm-` гарантируют уникальность даже если один и тот же feedback id попадёт в обе ленты (single-nmId склейка).
    - Если БОТЕ ленты пустые — между метаданными и лентами не рендерится ничего лишнего.
    - **Edge case по решению из scouting:** если в склейке только один nmId, обе ленты содержат идентичный контент — намеренно показываем обе (визуальный сигнал: «связка = один товар»).

    3. **Никаких других изменений** в файле. `ReviewChip`, `LegendItem`, top-level layout — без правок.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "PriceCalculatorTable\.tsx|prices/wb/page\.tsx|error TS" | head -20</automated>
  </verify>
  <done>
    tsc returns 0 errors в обоих файлах. `npm run build` проходит. Визуально (smoke на dev): в expand-панели /prices/wb под каждым графиком после ряда метрик появляются до двух строк лент со звёздами, каждая со своей подписью и счётчиком. Если по nmId/imtId нет отзывов — соответствующая строка отсутствует. На /cards/wb ничего не изменилось.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p tsconfig.json` — 0 errors, особенно в `app/(dashboard)/prices/wb/page.tsx` и `components/prices/PriceCalculatorTable.tsx`.
2. `npm run build` — успешно. Размер /prices/wb route не должен прибавить больше ±1 kB (логика осталась почти той же, добавилась пара flex-контейнеров + JSON shape в payload чуть больше).
3. Существующие тесты `tests/pricing-math.test.ts` и `tests/wb-orders-chart-fill.test.ts` — PASS (regression check, должно остаться 17/17 и 14/14).
4. Smoke (после deploy на prod):
   - https://zoiten.pro/prices/wb → expand товара с несколькими nmId одной склейки → две строки лент.
   - Товар, чей nmId без imtId (старые карточки) → только строка «По товару».
   - Товар без отзывов → только 4 метрики, обе ленты скрыты.
</verification>

<success_criteria>
- В expand-панели /prices/wb для каждого nmId-блока могут рендериться до двух строк лент отзывов: «По связке (N)» и «По товару (N)».
- Пустые ленты скрыты вместе с подписью.
- Если imtId карточки = null — строка «По связке» отсутствует.
- Каждая лента — ≤10 свежих ReviewChip, отсортированных по createdAt desc.
- Существующие 4 метрики (Остаток / Остаток в днях / Рейтинг связки / Кол-во оценок) остаются без изменений.
- Регрессионный smoke: на /cards/wb визуально ничего не изменилось.
- tsc + build чистые, relevant tests зелёные.
</success_criteria>

<output>
После завершения создать `.planning/quick/260518-h6p-prices-wb-expand-per-nmid-imtid-nmid-nmi/260518-h6p-SUMMARY.md`.

Commit messages:
- Task 1: `feat(quick-260518-h6p): load feedbacks по imtId-склейкам + shape { byImt, byNmId } в /prices/wb`
- Task 2: `feat(quick-260518-h6p): две строки лент отзывов «По связке / По товару» в expand-панели /prices/wb`

Deploy: стандартный `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`.
</output>
