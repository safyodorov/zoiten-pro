---
phase: quick-260721-o4b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-weekly/types.ts
  - lib/finance-weekly/engine.ts
  - lib/finance-weekly/data.ts
  - lib/finance-weekly/live.ts
  - components/finance/WeeklyFinReportTable.tsx
  - tests/finance-weekly-engine.test.ts
autonomous: true
requirements:
  - WK-01-commission-bucket
  - WK-02-ad-review-tails
  - WK-03-storage-offer-modeled
  - WK-04-golden-recompute
  - WK-05-gates
  - WK-06-snapshots-immutable

must_haves:
  truths:
    - "В «Водопаде затрат» /finance/weekly появилась строка «Комиссия» в обоих столбцах (ИУ и Оферта)"
    - "Строка «Реклама» водопада = updTotal недели (нераспределённый хвост включён; сверка 13–19.07: 145 340 ₽)"
    - "Строка «Отзывы» водопада = Σ reviewPointsRub недели, включая отзывы nmId, выпавших из candidates (сверка 13–19.07: 51 167 ₽)"
    - "Сценарий Оферта вычитает хранение по РАСЧЁТНОЙ модели (объём × ставки × дни, calculatePricingStandard.storageAmount), а не из пула; ИУ хранение = 0 как было"
    - "Per-article строки таблицы и весь ИУ-сценарий (профит ИУ) не изменились"
  artifacts:
    - path: "lib/finance-weekly/types.ts"
      provides: "CostWaterfall.commission (новый бакет) + WeeklyFinReportInputs.waterfallTails (опц., аддитивно)"
      contains: "commission"
    - path: "lib/finance-weekly/engine.ts"
      provides: "commissionPerUnit в ScenarioBreakdown, аккумуляция commission в addToWaterfall, применение waterfallTails к iu+std"
      contains: "commissionPerUnit"
    - path: "lib/finance-weekly/data.ts"
      provides: "storagePerUnit из calculatePricingStandard (Оферта-модель) + расчёт adTail/reviewTail + waterfallTails в WeeklyFinReportPageData"
      contains: "storageAmount"
    - path: "lib/finance-weekly/live.ts"
      provides: "проброс data.waterfallTails в computeWeeklyFinReport"
      contains: "waterfallTails"
    - path: "components/finance/WeeklyFinReportTable.tsx"
      provides: "строка «Комиссия» в WATERFALL_BUCKETS"
      contains: "Комиссия"
  key_links:
    - from: "lib/finance-weekly/data.ts"
      to: "lib/finance-weekly/live.ts"
      via: "WeeklyFinReportPageData.waterfallTails"
      pattern: "waterfallTails"
    - from: "lib/finance-weekly/live.ts"
      to: "lib/finance-weekly/engine.ts"
      via: "computeWeeklyFinReport({ ..., waterfallTails })"
      pattern: "waterfallTails"
    - from: "lib/finance-weekly/data.ts"
      to: "lib/finance-weekly/engine.ts"
      via: "calculatePricingStandard(...).storageAmount → article.storagePerUnit"
      pattern: "storageAmount"
---

<objective>
Три правки «Понедельного фин-отчёта» /finance/weekly, чтобы водопад затрат бился с кабинетом WB:

1. Добавить бакет «Комиссия» в водопад (оба сценария). С ним «Итого затрат» = Выручка − Прибыль (комиссия = K − цена-минус-комиссия).
2. Довести хвосты рекламы и отзывов до водопада: `ad` = ТОЧНО updTotal недели, `review` = ТОЧНО Σ reviewPointsRub недели. Сейчас теряется доля nmId вне candidates (реклама Δ ≈ 9 969 ₽, отзывы Δ ≈ 4 026 ₽ на неделе 13–19.07). Хвосты — только в водопад, per-article строки НЕ трогать; в ОБА сценария.
3. Хранение Оферты считать РАСЧЁТНОЙ моделью (calculatePricingStandard.storageAmount — тот же вызов, где берётся logisticsEffAmount), как уже считается std-логистика. В отчётах реализации этих цифр нет (мы на ИУ, 100% сторнируется), они расчётные. ИУ хранение = 0 как было.

Purpose: водопад — управленческая сверка с кабинетом; суммы рекламы/отзывов должны совпадать, а Оферта-оценка «на случай отказа от ИУ» — быть полной (с хранением).
Output: аддитивные правки движка (контракт engine.ts только additive — diff-guard), data-слоя, UI-строки и golden-теста.

Решение пользователя 2026-07-21: хранение Оферты = модель (объём × ставки × дни), как логистика.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/finance-weekly/engine.ts
@lib/finance-weekly/types.ts
@lib/finance-weekly/data.ts
@lib/finance-weekly/live.ts
@lib/finance-weekly/realization.ts
@lib/finance-weekly/attribution.ts
@components/finance/WeeklyFinReportTable.tsx
@tests/finance-weekly-engine.test.ts

<interfaces>
<!-- Контракты, которые executor использует напрямую — без разведки по кодовой базе. -->

// lib/finance-weekly/pricing-math.ts (уже вызывается в data.ts:668)
export function calculatePricingStandard(inputs: PricingInputs): PricingOutputs
// PricingOutputs (pricing-math.ts:109) содержит:
//   storageAmount?: number      // ← ХРАНЕНИЕ / ед (модель: (storageBaseLiter + storageAddLiter×max(0,V−1))×daysInStock)
//   logisticsEffAmount?: number // ← уже используется в data.ts (logisticsStdPerUnit)

// lib/finance-weekly/engine.ts — интерфейс ScenarioBreakdown (internal): содержит
//   cutPricePerUnit (I = K×(100−J)/100). Комиссия/ед = K − cutPricePerUnit.

// lib/finance-weekly/types.ts — CostWaterfall (13 бакетов сейчас), WeeklyWaterfall {iu, std}.
// WeeklyFinReportInputs { articles, pools, constants? }.

// data.ts ключевые точки:
//   :497 const updTotal = Number(updAgg._sum.updSum ?? 0)   — тотал рекламы недели
//   :501 const adByNmId = attributeSpendByShares(...)        — реклама по nmId
//   :454 splitRealizationRows(realizationRows) → { byNmId, accountLevel } — отзывы
//   :577 if (qty <= 0) continue                              — тут nmId выпадают из candidates
//   :620-668 volumeLiters + calculatePricingStandard(pricingInputs).logisticsEffAmount
//   :693 // storagePerUnit НЕ задаём → движок берёт из пула хранения  (МЕНЯЕМ)

// live.ts:31 computeWeeklyFinReport({ articles, pools, constants }) — сюда добавить waterfallTails
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Движок — бакет «Комиссия» + аддитивный вход waterfallTails</name>
  <files>lib/finance-weekly/types.ts, lib/finance-weekly/engine.ts</files>
  <behavior>
    - Комиссия/ед вычислена как K − cutPricePerUnit → бакет commission аккумулируется × H в обоих сценариях.
    - С бакетом commission инвариант: waterfall.<scen> суммарно = revenue − profit (для сценария без tails). Проверить на golden: sum(waterfall.iu buckets) ≈ 46995.2 − 523.6.
    - waterfallTails (опц.) добавляется к обоим сценариям (iu и std) после цикла; отсутствует → водопад не меняется (golden не трогается).
    - ИУ golden profit 523.6 и Оферта golden −2176.7 (при storagePerUnit=0) НЕ меняются от этих правок (они лишь про водопад/новый бакет).
  </behavior>
  <action>
    types.ts (аддитивно — diff-guard engine контракта, менять только добавлением):
    1. В `CostWaterfall` добавить поле `commission: number` (₽, комиссия × H). Разместить логично — сразу после `cost` с комментарием «комиссия WB (K−I)×H».
    2. В `WeeklyFinReportInputs` добавить опциональное поле:
       `waterfallTails?: Partial<CostWaterfall>` — лямп-суммы (не per-unit), добавляемые к обоим сценариям водопада. Комментарий: хвосты рекламы/отзывов, не привязанные к candidates (data.ts). Отсутствие → default {} → водопад не меняется (golden-инвариант).

    engine.ts (только additive):
    3. В internal-интерфейс `ScenarioBreakdown` добавить `commissionPerUnit: number` (₽/ед = K − I).
    4. В `computeScenario` после расчёта `cutPricePerUnit` добавить
       `const commissionPerUnit = K - cutPricePerUnit` и вернуть его в объекте (рядом с cutPricePerUnit). profitPerUnit НЕ трогать.
    5. В `emptyWaterfall()` добавить `commission: 0`.
    6. В `addToWaterfall(acc, b, H)` добавить `acc.commission += b.commissionPerUnit * H`.
    7. В `computeWeeklyFinReport` ПОСЛЕ цикла по articles (после accumулации водопада) применить tails к обоим сценариям:
       ```
       const tails = inputs.waterfallTails ?? {}
       for (const key of Object.keys(tails) as (keyof CostWaterfall)[]) {
         const v = tails[key] ?? 0
         waterfall.iu[key] += v
         waterfall.std[key] += v
       }
       ```
    НЕ менять: profit-формулу, storage-семантику (ИУ=0 / Оферта=common.storagePerUnit), другие бакеты.
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx tsc --noEmit && npx vitest run finance-weekly-engine</automated>
  </verify>
  <done>CostWaterfall имеет commission; ScenarioBreakdown.commissionPerUnit=K−I; addToWaterfall накапливает commission; waterfallTails добавляется к iu+std; golden ИУ 523.6 / Оферта −2176.7 не изменились; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: Data-слой — хранение Оферты моделью + хвосты рекламы/отзывов + проброс в движок</name>
  <files>lib/finance-weekly/data.ts, lib/finance-weekly/live.ts</files>
  <action>
    data.ts:

    A. Хранение Оферты моделью (WK-03). В блоке calculatePricingStandard (~:620-668):
       - Заменить `logisticsStdPerUnit = calculatePricingStandard(pricingInputs).logisticsEffAmount ?? 0`
         на захват полного результата:
         `const stdOut = calculatePricingStandard(pricingInputs)`
         `logisticsStdPerUnit = stdOut.logisticsEffAmount ?? 0`
       - Ввести `let storageStdPerUnit: number | undefined = undefined` рядом с `let logisticsStdPerUnit = 0` (до `if (volumeLiters > 0)`).
       - Внутри `if (volumeLiters > 0)` присвоить `storageStdPerUnit = stdOut.storageAmount ?? 0`.
       - Volume<=0 → storageStdPerUnit остаётся undefined → fallback прежний (пул хранения), как сейчас.
       - В объекте `articles.push({ ... })` заменить строку-комментарий `// storagePerUnit НЕ задаём …` на:
         `storagePerUnit: storageStdPerUnit,` с комментарием: «Оферта: хранение = модель (calculatePricingStandard.storageAmount), решение 2026-07-21; ИУ движок вычитает 0. volume<=0 → undefined → пул».
       Замечание: тот же вызов, что уже даёт logisticsEffAmount — дублирующего расчёта нет. daysInStock=60 как в текущем pricingInputs. Общий фикс/ед одежды (overheadFixedPerUnit) НЕ трогать.

    B. Хвост рекламы (WK-02). После сборки массива `articles` (после цикла :594-708, где известны финальные article.adSpendTotal):
       - `const attributedAd = articles.reduce((s, a) => s + a.adSpendTotal, 0)`
       - `const adTail = Math.max(0, updTotal - attributedAd)` — нераспределённый остаток (доли nmId вне candidates: qty<=0 или непривязанные). updTotal уже посчитан (:497).

    C. Хвост отзывов (WK-02). Полный факт отзывов недели:
       - `const totalReviews = realizationAccountLevel.reviewPointsRub + Array.from(realizationByNmId.values()).reduce((s, b) => s + b.reviewPointsRub, 0)`
         (realizationByNmId/realizationAccountLevel уже есть из splitRealizationRows :454).
       - `const attributedReviews = articles.reduce((s, a) => s + a.reviewWriteoffTotal, 0)`
       - `const reviewTail = hasRealization ? Math.max(0, totalReviews - attributedReviews) : 0`
       (attributedReviews = Σ собственных отзывов candidates + полностью распределённый account-level; reviewTail = отзывы nmId, выпавших по `qty<=0 continue`.)

    D. Экспорт хвостов. В `WeeklyFinReportPageData` добавить опциональное поле:
       `waterfallTails?: { ad: number; review: number }` (комментарий: лямп-хвосты для водопада, не per-article).
       В финальном `return { ... }` добавить `waterfallTails: { ad: adTail, review: reviewTail }`.
       Также добавить `waterfallTails: { ad: 0, review: 0 }` в ОБА early-return (нет marketplace / нет linkedNmIds) — для полноты (поле опционально, но заполнить нулями явно).

    live.ts:
    E. Пробросить хвосты в движок:
       `const result = computeWeeklyFinReport({ articles: data.articles, pools: data.pools, constants: data.constants, waterfallTails: data.waterfallTails })`
       (data.waterfallTails: { ad, review } — совместимо с Partial<CostWaterfall>, т.к. ad и review — валидные ключи CostWaterfall).
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx tsc --noEmit && npx vitest run finance-weekly pricing-math</automated>
  </verify>
  <done>data.ts: Оферта storagePerUnit из stdOut.storageAmount (volume>0), undefined→пул; adTail=updTotal−Σaderated; reviewTail=totalReviews−Σattributed (hasRealization); waterfallTails в PageData + оба early-return. live.ts пробрасывает waterfallTails. tsc + targeted vitest зелёные (без учёта golden-обновления в Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: UI-строка «Комиссия» + пересчёт golden Оферты (хранение-модель) + сверка тестов</name>
  <files>components/finance/WeeklyFinReportTable.tsx, tests/finance-weekly-engine.test.ts</files>
  <action>
    UI (WeeklyFinReportTable.tsx):
    1. В `WATERFALL_BUCKETS` добавить `{ key: "commission", label: "Комиссия" }`. Разместить сразу после `{ key: "cost", label: "Закупка" }`. `sumWaterfall` итерирует по WATERFALL_BUCKETS → «Итого затрат» автоматически включит комиссию (инвариант: Выручка − Прибыль).

    Golden-тест (tests/finance-weekly-engine.test.ts) — WK-04:
    2. Отразить решение «хранение Оферты = модель» в golden nmId 165967746: задать `goldenArticle.storagePerUnit` = МОДЕЛЬНОЕ хранение/ед этого артикула вместо `0`.
       - Определить значение S = calculatePricingStandard.storageAmount для реальных габаритов nmId 165967746 (взять габариты из прод-БД Product по этому nmId ИЛИ из reconcile-отчёта docs/superpowers/specs/2026-07-10-weekly-finreport-reconcile-report.md; формула storage = (0.16 + 0.16×max(0,V−1))×60, V — объём в литрах, округл. до 0.1). Зафиксировать S числом в тесте с комментарием-обоснованием (откуда габариты/V/S).
       - Обновить ожидания Оферты: `profitPerUnit ≈ −544.18 − S`, `profit ≈ −2176.7 − S×4`. ИУ ожидания (523.6 / 130.90 / I≈8047.93) НЕ менять — движок вычитает хранение ТОЛЬКО в Оферте.
       - Комментарий над goldenArticle: «Решение 2026-07-21: хранение Оферты = расчётная модель (S ₽/ед); ИУ хранение=0».
       - Тест водопада `std.logistics = 1380×4` и `iu/std.cost` — НЕ меняются. При желании добавить проверку `waterfall.std.storage ≈ S×4` и `waterfall.iu.storage = 0`.
       Если реальные габариты nmId 165967746 недоступны — использовать документированную оценку V и вывести S по формуле, зафиксировав допущение в комментарии (тест самосогласован: Оферта = −2176.7 − S×4).
       - ВАЖНО (warn чекера): goldenArticle — shared const, std.profit сдвинется на −S×4 во ВСЕХ трёх describe-блоках файла. Обновить также захардкоженные assertions в блоках «Опция Джема» (~line 267-270, artWithoutJem.std.profit) и «overheadFixedPerUnit» (~line 403-407, g.std.profit) — тоже −S×4; ИУ-значения в них не трогать.
    3. Добавить/адаптировать проверку нового бакета и хвостов в этом же файле:
       - Комиссия: `waterfall.iu.commission ≈ (K − iu.cutPricePerUnit)×H` и `waterfall.std.commission ≈ (K − std.cutPricePerUnit)×H`.
       - Инвариант: `sum(всех бакетов waterfall.iu) ≈ revenue − iu.profit` (для golden без tails).
       - Tails: отдельный кейс — `computeWeeklyFinReport({ ...goldenInputs, waterfallTails: { ad: 100, review: 20 } })` → `waterfall.iu.ad = базовый + 100`, `waterfall.std.review = базовый + 20` (хвост в ОБА сценария).
    4. Прочие finance-weekly-тесты: snapshot-тест использует опциональные поля (waterfallTails опц. в PageData и inputs) → правок fakeData НЕ требуется; если tsc укажет иначе — добавить `waterfallTails: { ad: 0, review: 0 }` в fakeData. Проверить, что тесты «хранение только Оферта» и «overheadFixedPerUnit» (storagePerUnit хардкод) остаются зелёными.
  </action>
  <verify>
    <automated>cd "c:/Users/User/zoiten-pro" && npx tsc --noEmit && npx vitest run finance-weekly pricing-math && npm run test 2>&1 | tail -30</automated>
  </verify>
  <done>Водопад в UI имеет строку «Комиссия»; golden ИУ неизменен (523.6), Оферта = −2176.7 − S×4 (S — модель хранения/ед, обосновано комментарием); добавлены проверки commission-бакета, инварианта Итого=Выручка−Прибыль и tails в обоих сценариях; targeted-тесты зелёные; полный `npm run test` — без НОВЫХ падений сверх baseline 41.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — чисто (аддитивные поля не ломают типы).
- `npx vitest run finance-weekly pricing-math` — все зелёные; golden ИУ 523.6 неизменен, Оферта пересчитана на хранение-модель.
- `npm run test` — без НОВЫХ падений сверх baseline (41 pre-existing в 11 файлах; см. .planning/quick/260720-mj0-.../deferred-items.md).
- Ручная сверка (после деплоя оркестратором, неделя 13–19.07): водопад «Реклама» ИУ+Оферта = 145 340 ₽; «Отзывы» = 51 167 ₽; появилась строка «Комиссия»; Оферта хранение > 0, ИУ хранение = 0.
- engine.ts — только additive (новое поле CostWaterfall.commission, новый опц. вход waterfallTails, новое internal-поле ScenarioBreakdown.commissionPerUnit) — diff-guard проходит.
- Снапшоты недель (WeeklyFinReportSnapshot) НЕ пересчитываются — immutable, правки только на live-расчёте (WK-06).
</verification>

<success_criteria>
- Водопад затрат /finance/weekly содержит бакет «Комиссия» (оба сценария), «Итого затрат» = Выручка − Прибыль (без tails).
- waterfall.ad = updTotal недели, waterfall.review = Σ reviewPointsRub недели (хвосты вне candidates учтены), в обоих сценариях; per-article строки не изменены.
- Оферта вычитает хранение по модели (calculatePricingStandard.storageAmount), ИУ хранение = 0; volume<=0 → fallback на пул.
- Golden ИУ не изменился; Оферта golden пересчитана с обоснованием.
- tsc + targeted vitest зелёные; полный test без новых падений.
</success_criteria>

<output>
После завершения создать `.planning/quick/260721-o4b-finance-weekly/260721-o4b-SUMMARY.md`
</output>
