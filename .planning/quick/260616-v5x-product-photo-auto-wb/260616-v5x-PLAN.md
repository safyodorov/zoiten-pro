---
phase: quick-260616-v5x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260616_product_photo_overridden/migration.sql
  - app/actions/products.ts
  - app/actions/wb-cards.ts
  - app/api/wb-sync/route.ts
  - components/products/ProductForm.tsx
  - components/products/PhotoUploadField.tsx
autonomous: true
requirements: [v5x-PHOTO-AUTO]
must_haves:
  truths:
    - "Когда photoOverridden=false, Product.photoUrl автоматически = photoUrl первой (sortOrder=0) WB-карточки"
    - "При смене порядка артикулов в форме (updateProduct) фото перевыводится из нового первого WB-артикула"
    - "При полной WB-синхронизации (/api/wb-sync) Product.photoUrl обновляется из первой карточки для всех товаров с photoOverridden=false"
    - "Ручная загрузка/кроп фото в форме ставит photoOverridden=true → автоперевывод отключается"
    - "Кнопка «Вернуть авто из WB» сбрасывает photoOverridden=false и фото немедленно перевыводится из карточки"
    - "nameOverridden / regenerateProductName поведение не сломано"
  artifacts:
    - path: "prisma/migrations/20260616_product_photo_overridden/migration.sql"
      provides: "ALTER TABLE Product ADD photoOverridden"
      contains: "photoOverridden"
    - path: "app/actions/products.ts"
      provides: "resolveProductPhoto helper + интеграция в create/update/duplicate"
      contains: "resolveProductPhoto"
    - path: "app/api/wb-sync/route.ts"
      provides: "batch-проход перевывода Product.photoUrl после upsert карточек"
      contains: "photoOverridden"
  key_links:
    - from: "app/actions/products.ts:updateProduct"
      to: "resolveProductPhoto (после пересоздания articles)"
      via: "tx call"
      pattern: "resolveProductPhoto\\(tx"
    - from: "app/api/wb-sync/route.ts"
      to: "Product.photoUrl"
      via: "updateMany/update where photoOverridden=false"
      pattern: "photoOverridden"
    - from: "components/products/ProductForm.tsx:PhotoUploadField"
      to: "form.setValue photoOverridden=true при ручной загрузке"
      via: "onUploadComplete callback"
      pattern: "photoOverridden"
---

<objective>
Авто-фото товара из первой (sortOrder=0) WB-карточки + флаг ручного override, по образцу Product.nameOverridden (Phase 18).

Сейчас Product.photoUrl берётся из firstCard.photoUrl ТОЛЬКО при создании товара из WB (createProductFromCards). При смене порядка артикулов и при WB-синхронизации фото товара не перевыводится. Цель: фото товара = фото первой WB-карточки, автоматически обновляется при создании, при смене порядка артикулов и при синхронизации; ручная загрузка фиксирует фото (photoOverridden=true).

Purpose: устранить рассинхрон фото товара с основной WB-карточкой, дать пользователю контроль через override-флаг.
Output: новое поле Product.photoOverridden + миграция, server helper resolveProductPhoto, интеграция в create/update/duplicate/wb-sync, UI override в форме товара.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Образец паттерна nameOverridden (Phase 18) — ОБЯЗАТЕЛЬНО соблюсти симметрию

<interfaces>
<!-- Извлечено из кодовой базы. Использовать напрямую — без доп. разведки. -->

## prisma/schema.prisma (model Product, ~line 386-388)
```prisma
  name                     String                 @db.VarChar(255)
  nameOverridden           Boolean                @default(false)
  photoUrl                 String?
```

## app/actions/products.ts — regenerateProductName (ОБРАЗЕЦ для resolveProductPhoto), ~line 107
```typescript
async function regenerateProductName(
  tx: Prisma.TransactionClient,
  productId: string
): Promise<void> {
  const p = await tx.product.findUnique({ where: { id: productId }, include: {...} })
  if (!p || p.nameOverridden) return
  // ... вычисляет name, если изменился → tx.product.update({ data: { name } })
}
```

## app/actions/products.ts — findPrimaryWbCardForProduct (ПЕРЕИСПОЛЬЗОВАТЬ логику), ~line 664
```typescript
async function findPrimaryWbCardForProduct(productId: string) {
  const wbMarketplace = await prisma.marketplace.findUnique({ where: { slug: "wb" }, select: { id: true } })
  if (!wbMarketplace) return null
  const article = await prisma.marketplaceArticle.findFirst({
    where: { productId, marketplaceId: wbMarketplace.id },
    orderBy: { sortOrder: "asc" },
  })
  if (!article) return null
  const nmId = parseInt(article.article, 10)
  if (Number.isNaN(nmId)) return null
  return prisma.wbCard.findUnique({ where: { nmId } })
}
```
ВАЖНО: эта функция использует глобальный `prisma`, не tx. resolveProductPhoto должна работать ВНУТРИ транзакции → нужна tx-версия lookup (см. Task 2).

## app/actions/products.ts — UpdateProductSchema / ProductSchema (~line 39-62)
```typescript
const ProductSchema = z.object({
  article: z.string().min(1).max(100),
  name: z.string().max(255).optional(),
  nameOverridden: z.boolean().optional().default(false),
  photoUrl: z.string().nullable().optional(),
  // ...
})
const UpdateProductSchema = ProductSchema.extend({ id: z.string().min(1) })
```

## app/actions/products.ts — updateProduct поток (~line 248-316)
- updateData.photoUrl = parsed.photoUrl ?? null (СЕЙЧАС всегда перезаписывает)
- tx.marketplaceArticle.deleteMany({ where: { productId } }) — затем пересоздаёт articles с sortOrder=i (~278-312)
- В конце: await regenerateProductName(tx, parsed.id) (~315)

## app/api/wb-sync/route.ts — структура
- Цикл upsert WbCard (~176-317): для каждой карточки create/update, photoUrl=card.photoUrl
- Точка инъекции нового прохода: ПОСЛЕ блока per-warehouse orders (~585), ПЕРЕД `return NextResponse.json(...)` (~587)
- В цикле upsert доступны card.nmId и card.photoUrl — собрать map nmId→photoUrl по ходу.

## components/products/ProductForm.tsx
- ProductData interface (~128): добавить photoOverridden?: boolean
- formSchema (~166): добавить photoOverridden: z.boolean()
- defaultValues (~261): photoOverridden: product?.photoOverridden ?? false
- PhotoUploadField usage (~884-899): внутри FormField name="photoUrl"
- Inline ProductNameField (~1105-1178) — ОБРАЗЕЦ UI для override-блока
- commitSave (~470) — values уже спредятся в updateProduct/createProduct, photoOverridden попадёт автоматически если в схеме

## components/products/PhotoUploadField.tsx
- props: { productId?, currentPhotoUrl?, onUploadComplete: (url: string) => void }
- uploadFile() → onUploadComplete(url) при успехе; handleClear() → onUploadComplete("")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + миграция Product.photoOverridden</name>
  <files>prisma/schema.prisma, prisma/migrations/20260616_product_photo_overridden/migration.sql</files>
  <action>
1. В prisma/schema.prisma в model Product добавить поле СРАЗУ после `photoUrl String?` (line ~388), по образцу nameOverridden:
   ```prisma
   photoOverridden          Boolean                @default(false)
   ```
   Выровнять по существующему форматированию столбцов (типы выровнены пробелами как у nameOverridden / brandId).

2. Создать директорию миграции prisma/migrations/20260616_product_photo_overridden/ и файл migration.sql со структурой как у существующих (см. 20260511_product_name_refactor — комментарий-заголовок + ALTER):
   ```sql
   -- Quick 260616-v5x — Product Photo Overridden
   --
   -- Авто-фото товара из первой WB-карточки + флаг ручного override (по образцу nameOverridden, Phase 18).
   -- photoOverridden=false → Product.photoUrl автоматически = photoUrl первой (sortOrder=0) WB-карточки.

   ALTER TABLE "Product" ADD COLUMN "photoOverridden" BOOLEAN NOT NULL DEFAULT false;
   ```

ВАЖНО: НЕ запускать prisma migrate / generate локально (нет node_modules). Только создать файлы. Применится на VPS через deploy.sh `prisma migrate deploy`.
  </action>
  <verify>
    <automated>grep -q 'photoOverridden' /c/Users/serge/zoiten-pro/prisma/schema.prisma && test -f /c/Users/serge/zoiten-pro/prisma/migrations/20260616_product_photo_overridden/migration.sql && grep -q 'ADD COLUMN "photoOverridden"' /c/Users/serge/zoiten-pro/prisma/migrations/20260616_product_photo_overridden/migration.sql && echo OK</automated>
  </verify>
  <done>Поле photoOverridden есть в schema.prisma в model Product; migration.sql создан с корректным ALTER TABLE.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: resolveProductPhoto helper + интеграция в actions (create/update/duplicate/wb-cards)</name>
  <files>app/actions/products.ts, app/actions/wb-cards.ts</files>
  <behavior>
    - resolveProductPhoto(tx, productId): если photoOverridden=true → no-op; иначе берёт photoUrl первой WB-карточки (sortOrder=0 артикул → WbCard.photoUrl) и пишет в Product.photoUrl только если отличается.
    - У товара нет WB-артикулов / WbCard не найдена / nmId не парсится → no-op (фото не трогаем).
    - createProduct: если photoOverridden=true пишет parsed.photoUrl как есть; иначе после создания вызывает resolveProductPhoto.
    - updateProduct: если photoOverridden=true пишет parsed.photoUrl + photoOverridden=true; иначе photoOverridden=false и после пересоздания articles вызывает resolveProductPhoto.
    - duplicateProduct: дубль не копирует фото (photoUrl:null, photoOverridden остаётся default false) — вызвать resolveProductPhoto после создания (если есть WB-артикулы — фото подтянется; если нет — останется null). Не ломать существующее поведение «photo не копируется».
  </behavior>
  <action>
1. В app/actions/products.ts добавить tx-aware helper РЯДОМ с regenerateProductName (после ~line 143). Использовать переданный tx (НЕ глобальный prisma — иначе вне транзакции):
   ```typescript
   // ── Quick 260616-v5x: resolveProductPhoto ──────────────────────
   // Если photoOverridden=true → не трогаем. Иначе Product.photoUrl = photoUrl
   // первой (sortOrder=0) WB-карточки. Вызывать внутри tx после пересоздания articles.
   async function resolveProductPhoto(
     tx: Prisma.TransactionClient,
     productId: string
   ): Promise<void> {
     const product = await tx.product.findUnique({
       where: { id: productId },
       select: { photoOverridden: true, photoUrl: true },
     })
     if (!product || product.photoOverridden) return

     const wbMarketplace = await tx.marketplace.findUnique({
       where: { slug: "wb" },
       select: { id: true },
     })
     if (!wbMarketplace) return

     const article = await tx.marketplaceArticle.findFirst({
       where: { productId, marketplaceId: wbMarketplace.id },
       orderBy: { sortOrder: "asc" },
       select: { article: true },
     })
     if (!article) return

     const nmId = parseInt(article.article, 10)
     if (Number.isNaN(nmId)) return

     const card = await tx.wbCard.findUnique({
       where: { nmId },
       select: { photoUrl: true },
     })
     const newPhoto = card?.photoUrl ?? null
     if (newPhoto !== product.photoUrl) {
       await tx.product.update({ where: { id: productId }, data: { photoUrl: newPhoto } })
     }
   }
   ```

2. Schema: в ProductSchema (~line 44) добавить рядом с photoUrl:
   ```typescript
   photoOverridden: z.boolean().optional().default(false),
   ```

3. createProduct (~line 167-209): в tx.product.create data добавить photoOverridden и сделать photoUrl условным:
   - `photoUrl: parsed.photoOverridden ? (parsed.photoUrl ?? null) : null,`
   - `photoOverridden: parsed.photoOverridden ?? false,`
   После regenerateProductName (~209) добавить: `await resolveProductPhoto(tx, created.id)` (перевыведет если не override и есть WB-карточка).

4. updateProduct (~line 248-316): изменить photoUrl/photoOverridden логику в updateData. Заменить безусловное `photoUrl: parsed.photoUrl ?? null` на: убрать photoUrl из общего updateData, добавить после блока nameOverridden (~272):
   ```typescript
   if (parsed.photoOverridden) {
     updateData.photoUrl = parsed.photoUrl ?? null
     updateData.photoOverridden = true
   } else {
     updateData.photoOverridden = false
     // photoUrl перевыведется через resolveProductPhoto после пересоздания articles
   }
   ```
   В самом конце транзакции, ПОСЛЕ `await regenerateProductName(tx, parsed.id)` (~315), добавить: `await resolveProductPhoto(tx, parsed.id)`.
   ВАЖНО: resolveProductPhoto должна идти ПОСЛЕ пересоздания marketplaceArticle (~290-312), чтобы новый первый артикул уже был в БД.

5. duplicateProduct (~line 453-484): photoUrl:null остаётся (D-26). Добавить в create data `photoOverridden: false`. После regenerateProductName (~482) добавить: `await resolveProductPhoto(tx, duplicated.id)` (если у дубля есть WB-артикулы — фото подтянется; иначе no-op оставит null).

6. app/actions/wb-cards.ts createProductFromCards (~164-180): photoUrl=firstCard.photoUrl уже есть. Поле photoOverridden получит default false из schema — НИЧЕГО менять не обязательно. ПРОВЕРИТЬ что Prisma create не требует явного photoOverridden (есть @default — не требует). Опционально для явности добавить `photoOverridden: false,` рядом с nameOverridden. Не ломать существующий поток.

7. ПРОВЕРИТЬ что importFromWb (~779) НЕ трогает фото — он и не должен (фото идёт отдельным путём через resolveProductPhoto). Оставить как есть.
  </action>
  <verify>
    <automated>grep -q 'resolveProductPhoto' /c/Users/serge/zoiten-pro/app/actions/products.ts && grep -q 'photoOverridden' /c/Users/serge/zoiten-pro/app/actions/products.ts && grep -c 'resolveProductPhoto(tx' /c/Users/serge/zoiten-pro/app/actions/products.ts | grep -qE '[3-9]' && echo OK</automated>
  </verify>
  <done>resolveProductPhoto объявлен, вызван в createProduct, updateProduct (после пересоздания articles + regenerateProductName), duplicateProduct. photoOverridden в ProductSchema. updateProduct пишет photoUrl только при override, иначе перевыводит. Type review: tx все вызовы внутри $transaction; нет вызовов глобального prisma в helper.</done>
</task>

<task type="auto">
  <name>Task 3: wb-sync batch-перевывод Product.photoUrl + UI override в форме</name>
  <files>app/api/wb-sync/route.ts, components/products/ProductForm.tsx, components/products/PhotoUploadField.tsx</files>
  <action>
ЧАСТЬ A — app/api/wb-sync/route.ts (batch-перевывод фото):

1. В цикле upsert карточек (~176-317) собирать map nmId→photoUrl. Перед циклом (~172, рядом с `const errors: string[] = []`) добавить:
   ```typescript
   const cardPhotoByNmId = new Map<number, string | null>()
   ```
   Внутри цикла, после `const card = parseCard(raw)` (~178), добавить:
   ```typescript
   cardPhotoByNmId.set(card.nmId, card.photoUrl ?? null)
   ```

2. ПОСЛЕ блока per-warehouse orders (~585), ПЕРЕД `return NextResponse.json(...)` (~587) добавить новый проход. Цель: для всех Product с WB-артикулами и photoOverridden=false перевывести photoUrl из первой (min sortOrder) WB-карточки. Учесть no-FK паттерн (WbCard.nmId ↔ MarketplaceArticle.article строкой). Батчево, не N+1:
   ```typescript
   // ── Quick 260616-v5x: перевывод Product.photoUrl из первой WB-карточки ──
   // Для всех товаров с photoOverridden=false: photoUrl = photoUrl первого
   // (min sortOrder) WB-артикула. no-FK: WbCard.nmId ↔ MarketplaceArticle.article (string).
   let productPhotosUpdated = 0
   try {
     const wbMarketplace = await prisma.marketplace.findUnique({
       where: { slug: "wb" },
       select: { id: true },
     })
     if (wbMarketplace) {
       // Все WB-артикулы товаров с photoOverridden=false, отсортированы по продукту+sortOrder.
       const articles = await prisma.marketplaceArticle.findMany({
         where: {
           marketplaceId: wbMarketplace.id,
           product: { photoOverridden: false, deletedAt: null },
         },
         orderBy: [{ productId: "asc" }, { sortOrder: "asc" }],
         select: { productId: true, article: true },
       })
       // Первый (min sortOrder) артикул на продукт.
       const firstArticleByProduct = new Map<string, string>()
       for (const a of articles) {
         if (!firstArticleByProduct.has(a.productId)) {
           firstArticleByProduct.set(a.productId, a.article)
         }
       }
       // Текущие photoUrl продуктов (чтобы писать только при отличии).
       const productIds = Array.from(firstArticleByProduct.keys())
       const products = await prisma.product.findMany({
         where: { id: { in: productIds } },
         select: { id: true, photoUrl: true },
       })
       const currentPhotoById = new Map(products.map((p) => [p.id, p.photoUrl]))

       for (const [productId, articleStr] of firstArticleByProduct) {
         const nmId = parseInt(articleStr, 10)
         if (Number.isNaN(nmId)) continue
         // Фото из синхронизированной карточки (если была в ответе), иначе из БД.
         let newPhoto: string | null | undefined = cardPhotoByNmId.get(nmId)
         if (newPhoto === undefined) {
           const dbCard = await prisma.wbCard.findUnique({
             where: { nmId },
             select: { photoUrl: true },
           })
           newPhoto = dbCard?.photoUrl ?? null
         }
         if (newPhoto !== currentPhotoById.get(productId)) {
           await prisma.product.update({
             where: { id: productId },
             data: { photoUrl: newPhoto ?? null },
           })
           productPhotosUpdated++
         }
       }
     }
   } catch (e) {
     console.error("[wb-sync] product photo resolve failed:", e)
     errors.push(`product-photo-resolve: ${(e as Error).message}`)
   }
   ```

3. В `return NextResponse.json({...})` (~587) добавить поле `productPhotosUpdated,` в объект ответа.

ЧАСТЬ B — components/products/ProductForm.tsx (override UI):

4. ProductData interface (~128-150): добавить `photoOverridden?: boolean` рядом с nameOverridden (~134).

5. formSchema (~166): добавить рядом с nameOverridden (~170): `photoOverridden: z.boolean(),`

6. defaultValues (~261-284): добавить рядом с photoUrl (~276): `photoOverridden: product?.photoOverridden ?? false,`

7. Секция Фото (~880-900): заменить блок на версию с override-UI по образцу ProductNameField. Над PhotoUploadField добавить header с пометкой статуса и кнопкой переключения, прокинуть photoOverridden в PhotoUploadField и ставить флаг при ручной загрузке:
   ```tsx
   {/* Section 2: Фото */}
   <section className="space-y-4">
     <div className="flex items-center justify-between border-b pb-2">
       <h2 className="text-lg font-medium">
         Фото
         {form.watch("photoOverridden") ? (
           <span className="ml-2 text-xs font-normal text-amber-600 dark:text-amber-400">
             (загружено вручную)
           </span>
         ) : (
           <span className="ml-2 text-xs font-normal text-muted-foreground">
             (авто из первой карточки WB)
           </span>
         )}
       </h2>
       {form.watch("photoOverridden") ? (
         <Button
           type="button"
           variant="ghost"
           size="sm"
           onClick={() => {
             form.setValue("photoOverridden", false, { shouldDirty: true })
             form.setValue("photoUrl", null, { shouldDirty: true })
             // photoUrl перевыведется на сервере из карточки WB при сохранении
           }}
         >
           Вернуть авто из WB
         </Button>
       ) : null}
     </div>

     <FormField
       control={form.control}
       name="photoUrl"
       render={({ field }) => (
         <FormItem>
           <FormControl>
             <PhotoUploadField
               productId={product?.id}
               currentPhotoUrl={field.value ?? null}
               onUploadComplete={(url) => {
                 field.onChange(url || null)
                 // Ручная загрузка/очистка → фиксируем override
                 form.setValue("photoOverridden", true, { shouldDirty: true })
               }}
             />
           </FormControl>
           {!form.watch("photoOverridden") && (
             <p className="text-xs text-muted-foreground">
               Фото берётся из первой (основной) WB-карточки и обновляется при синхронизации. Загрузите своё фото, чтобы зафиксировать его.
             </p>
           )}
           <FormMessage />
         </FormItem>
       )}
     />
   </section>
   ```
   ПРИМЕЧАНИЕ: `Button` уже импортирован в ProductForm (используется в ProductNameField). `form.watch` доступен. Не вводить новых импортов кроме уже имеющихся.

8. commitSave (~470-567): values спредятся в updateProduct/createProduct через `...values`. photoOverridden теперь в formSchema → попадёт в payload автоматически. ПРОВЕРИТЬ что в спреде нет явного исключения photoOverridden — нет. Ничего дополнительно менять не нужно.

ЧАСТЬ C — components/products/PhotoUploadField.tsx:
9. Менять НЕ обязательно — компонент уже вызывает onUploadComplete(url) при загрузке и onUploadComplete("") при очистке. Override-флаг ставится в onUploadComplete callback на стороне ProductForm (шаг 7). Оставить PhotoUploadField без изменений (или убедиться что сигнатура onUploadComplete совместима — она `(url: string) => void`, в форме передаём `url || null` через field.onChange).
  </action>
  <verify>
    <automated>grep -q 'productPhotosUpdated' /c/Users/serge/zoiten-pro/app/api/wb-sync/route.ts && grep -q 'photoOverridden' /c/Users/serge/zoiten-pro/app/api/wb-sync/route.ts && grep -q 'Вернуть авто из WB' /c/Users/serge/zoiten-pro/components/products/ProductForm.tsx && grep -q 'photoOverridden' /c/Users/serge/zoiten-pro/components/products/ProductForm.tsx && echo OK</automated>
  </verify>
  <done>wb-sync собирает cardPhotoByNmId и в финальном проходе обновляет Product.photoUrl для photoOverridden=false товаров (батчево, по min sortOrder WB-артикулу), productPhotosUpdated в ответе. Форма: photoOverridden в schema/defaults/interface, секция Фото показывает статус + кнопку «Вернуть авто из WB», ручная загрузка ставит photoOverridden=true. Type review: form.watch/setValue типизированы, нет новых импортов.</done>
</task>

</tasks>

<verification>
- Type review (build локально нельзя — нет node_modules; проверка глазами):
  - resolveProductPhoto принимает Prisma.TransactionClient, все вызовы внутри $transaction в products.ts.
  - photoOverridden в Prisma model, в ProductSchema, в formSchema, в ProductData — типы согласованы (Boolean / boolean).
  - wb-sync проход использует глобальный prisma (вне транзакции — это ОК, отдельные update, не критичны к атомарности).
  - ProductForm: photoOverridden попадает в updateProduct/createProduct через `...values` спред.
- Логика first-card: и resolveProductPhoto, и wb-sync-проход берут min sortOrder WB-артикул → консистентно с findPrimaryWbCardForProduct.
- Регрессия: regenerateProductName и nameOverridden не тронуты (resolveProductPhoto вызывается ПОСЛЕ regenerateProductName, не вместо).
- no-FK паттерн: WbCard.nmId ↔ MarketplaceArticle.article через parseInt, NaN-guard на месте.
</verification>

<success_criteria>
- Product.photoOverridden Boolean @default(false) в schema + миграция (только файл, не применённая локально).
- При создании/обновлении товара и при WB-синхронизации Product.photoUrl авто-выводится из первой WB-карточки, если photoOverridden=false.
- Смена порядка WB-артикулов в форме → updateProduct перевыводит фото из нового первого артикула.
- Ручная загрузка/кроп → photoOverridden=true → автоперевывод отключён.
- Кнопка «Вернуть авто из WB» → photoOverridden=false + photoUrl перевыведется при сохранении.
- nameOverridden / regenerateProductName поведение сохранено.
- Нет новых npm-зависимостей. RBAC requireSection("PRODUCTS","MANAGE") не ослаблен.
</success_criteria>

<output>
После завершения создать `.planning/quick/260616-v5x-product-photo-auto-wb/260616-v5x-SUMMARY.md`.

Деплой: миграция применится на VPS через `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` (prisma migrate deploy). Атомарные коммиты по задачам.
</output>
