---
phase: 26-roll-forward
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/sales-plan/products/page.tsx
  - components/sales-plan/ProductPlanTable.tsx
autonomous: true
requirements: [SP-16]
must_haves:
  truths:
    - "Ячейка месяца в матрице «Товары» показывает бейдж «срезано · приход dd.mm», когда план урезан стоком выше порога"
    - "Полностью нулевой месяц из-за отсутствия товара показывает плашку «нет товара · приход dd.mm» (или «придёт в <месяц>»)"
    - "Бейдж не появляется на копеечных срезах (порог по доле lostRub от планового)"
    - "Дата ближайшего прихода берётся из arrivals (первый приход > firstStockoutDate/today)"
  artifacts:
    - path: "app/(dashboard)/sales-plan/products/page.tsx"
      provides: "Полный проброс planResult (firstStockoutDate/lostRubToStockout/lostUnitsToStockout) в строку таблицы"
      contains: "lostRubToStockout"
    - path: "components/sales-plan/ProductPlanTable.tsx"
      provides: "Бейдж среза / плашка «нет товара» в non-edit ячейке месяца + хелпер nextArrivalAfter"
      contains: "срезано"
  key_links:
    - from: "app/(dashboard)/sales-plan/products/page.tsx"
      to: "tableProducts[].planResult"
      via: "сериализация полного pr (метрики среза уже в объекте)"
      pattern: "firstStockoutDate"
    - from: "components/sales-plan/ProductPlanTable.tsx"
      to: "arrivals"
      via: "поиск первого прихода после стокаута для даты бейджа"
      pattern: "nextArrivalAfter"
---

<objective>
SP-16 — Явное предупреждение о срезе плана в матрице «Товары».

Движок УЖЕ режет план по стоку (orders = min(ставка, сток)) и считает firstStockoutDate / lostUnitsToStockout / lostRubToStockout per товар (engine.ts ~272-283). Но в матрице «Товары» ячейка месяца показывает срезанное «П ₽» без причины. Добавляем presentational-объяснение: бейдж «срезано · приход dd.mm» на ячейке месяца, где план урезан выше порога; полностью нулевой месяц из-за отсутствия товара → плашка «нет товара · приход dd.mm». Данные уже в движке — расчёт НЕ меняем (D-3).

Purpose: пользователь сразу видит ПОЧЕМУ месяц показывает меньше запланированного (сток кончился / приход поздно), а не идёт в модалку «Дни».
Output: полный проброс метрик среза в tableProducts[].planResult, вычисление «ближайшего прихода» из arrivals, бейдж/плашка в non-edit ячейке месяца ProductPlanTable. Никаких Prisma/движок изменений.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-roll-forward/26-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Контракты — использовать напрямую, без разведки. -->

ProductPlanResult (lib/sales-plan/types.ts):
```typescript
interface ProductPlanResult {
  productId: string
  days: PlanDayRow[]
  monthTotals: Array<{ month: string; ordersUnits: number; buyoutsUnits: number; buyoutsRub: number }>
  firstStockoutDate: string | null   // первый день пробоя стока (ISO)
  lostUnitsToStockout: number        // Σ(rateRequested − ordersUnits) за горизонт
  lostRubToStockout: number          // цена промедления, ₽
}
```
PlanDayRow: `{ date, ordersUnits, buyoutsUnits, buyoutsRub, ordersRub, stockEnd, rateRequested }`.

ArrivalBatch (lib/sales-plan/types.ts): `{ date: string; qty: number; source: "purchase"|"virtual"|"incoming-legacy"; refId: string; dateSource }`.
`tableProducts[].arrivals` уже содержит все приходы (реальные + виртуальные SUGGESTED/ACCEPTED) в ISO-датах.

page.tsx сериализация строки (строки 224-248): `planResult: pr ?? { ...firstStockoutDate:null, lostUnitsToStockout:0, lostRubToStockout:0 }`. Метрики УЖЕ в объекте planResult (кладётся целиком).

ProductPlanTable non-edit ячейка (строки 467-499): рендерит «П {fmtRub(planRub)}», факт «Ф», pct, «≈ N шт». Сюда добавляется бейдж.
`today: string` проп доступен. `p.planResult`, `p.arrivals` доступны в scope map по products.
Существующие module-хелперы: fmtNum, fmtAdaptive, fmtRub, fmtPct, getMonthFact, monthLabel, daysInMonth. `ArrivalBatch` импортирован (строка 14).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проброс полного planResult + хелперы «ближайший приход» и форматирование дат</name>
  <read_first>
    - app/(dashboard)/sales-plan/products/page.tsx (строки 195-248 — productResultMap, tableProducts map, сериализация planResult)
    - lib/sales-plan/engine.ts (строки 272-283 — семантика firstStockoutDate/lostUnitsToStockout/lostRubToStockout)
    - components/sales-plan/ProductPlanTable.tsx (интерфейс ProductRow строки 53-67; module-хелперы строки 16-116)
  </read_first>
  <action>
page.tsx: метрики среза уже кладутся в строку целиком через `planResult: pr ?? {...}`. Явно проверь, что сериализуемый объект содержит `firstStockoutDate`, `lostUnitsToStockout`, `lostRubToStockout` и `monthTotals`. Если `pr` мапится целиком — оставь как есть (поля уже на клиенте). НЕ меняй сам расчёт.

ProductPlanTable.tsx — добавь чистые module-хелперы (рядом с getMonthFact, строка ~116):

```typescript
/** Первый приход строго после reference-даты (стокаут или today). ISO или null. */
function nextArrivalAfter(arrivals: ArrivalBatch[], ref: string): string | null {
  const future = arrivals.map((a) => a.date).filter((d) => d > ref).sort()
  return future[0] ?? null
}

/** "2026-09-28" -> "28.09" */
function fmtDayMonth(iso: string): string {
  const [, m, d] = iso.split("-")
  return `${d}.${m}`
}

const MONTH_SHORT: Record<string, string> = {
  "01": "янв", "02": "фев", "03": "мар", "04": "апр", "05": "май", "06": "июн",
  "07": "июл", "08": "авг", "09": "сен", "10": "окт", "11": "ноя", "12": "дек",
}
/** "2026-09-01" -> "сен" */
function fmtMonthShort(iso: string): string {
  return MONTH_SHORT[iso.slice(5, 7)] ?? iso.slice(0, 7)
}
```

`ArrivalBatch` уже импортирован (строка 14). ProductRow.planResult (ProductPlanResult) уже несёт нужные поля — тип менять не нужно.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function nextArrivalAfter" components/sales-plan/ProductPlanTable.tsx` возвращает 1
    - `grep -c "function fmtDayMonth" components/sales-plan/ProductPlanTable.tsx` возвращает 1
    - `grep -c "function fmtMonthShort" components/sales-plan/ProductPlanTable.tsx` возвращает 1
    - `grep -c "firstStockoutDate" "app/(dashboard)/sales-plan/products/page.tsx"` возвращает не менее 1
    - `npx tsc --noEmit` без новых ошибок в этих двух файлах
  </acceptance_criteria>
  <done>Полный planResult доступен на клиенте; чистые хелперы nextArrivalAfter/fmtDayMonth/fmtMonthShort добавлены; tsc чист</done>
</task>

<task type="auto">
  <name>Task 2: Бейдж среза «срезано · приход dd.mm» + плашка «нет товара» в ячейке месяца</name>
  <read_first>
    - components/sales-plan/ProductPlanTable.tsx (рендер non-edit ячейки строки 428-502; блок «П {fmtRub(planRub)}»)
    - components/sales-plan/ProductPlanDialog.tsx (строки 323-328 — образец подсветки нехватки: text-red-500 + маркер ⚠)
  </read_first>
  <action>
В ProductPlanTable.tsx внутри map по месяцам (перед `return` ячейки, где считаются planRub/planUnits/factRow/pct) добавь вычисление признаков среза для ЭТОГО месяца:

```typescript
// Метрики среза (SP-16) — данные из движка, порог по доле потерь.
const psr = p.planResult
const monthPrefix = month.slice(0, 7)
const monthPlanUnits = mt?.buyoutsUnits ?? 0
// Стокаут пришёлся на этот месяц или раньше?
const stockoutInOrBefore = psr.firstStockoutDate != null && psr.firstStockoutDate.slice(0, 7) <= monthPrefix
// Ближайший приход после стокаута (или после today, если стокаут раньше горизонта):
const arrivalRef = psr.firstStockoutDate ?? today
const nextArr = nextArrivalAfter(p.arrivals, arrivalRef)
// Доля потерь товара (индикатор «есть срез»). Порог D-4 ~2%.
const totalPlanRubProduct = psr.monthTotals.reduce((s, t) => s + t.buyoutsRub, 0)
const lostShare = totalPlanRubProduct + psr.lostRubToStockout > 0
  ? psr.lostRubToStockout / (totalPlanRubProduct + psr.lostRubToStockout)
  : 0
const isEmptyMonth = monthPlanUnits < 0.5 && stockoutInOrBefore
const isCutMonth = !isEmptyMonth && stockoutInOrBefore && lostShare > 0.02
```

В non-edit ветке ячейки (блок после `) : (`, строки ~467-499) после существующих span-ов добавь условный рендер бейджа/плашки (компактно, ячейка ~110px):

```tsx
{isEmptyMonth && (
  <span className="text-[10px] text-red-500 whitespace-nowrap" title="План обнулён: нет товара">
    ⚠ нет товара{nextArr ? ` · ${fmtDayMonth(nextArr)}` : ` · придёт в ${fmtMonthShort(month)}`}
  </span>
)}
{isCutMonth && (
  <span className="text-[10px] text-amber-600 whitespace-nowrap" title="План срезан из-за нехватки стока">
    срезано{nextArr ? ` · приход ${fmtDayMonth(nextArr)}` : ""}
  </span>
)}
```

Требования:
- Бейдж рендерится ТОЛЬКО в non-edit (compare) режиме ячейки — в edit-режиме ячейка редактируемая (ProductPlanCell), туда не добавляем.
- `isEmptyMonth` (красный «нет товара») и `isCutMonth` (янтарный «срезано») взаимоисключающи (isCutMonth = `!isEmptyMonth && ...`).
- Порог 0.02 (2%) из D-4 — не мельтешить на копеечных срезах.
- Дата: `fmtDayMonth(nextArr)` если есть будущий приход; иначе для пустого месяца — «придёт в <месяц>» (`fmtMonthShort(month)`); для срезанного без будущего прихода — без даты.
- НЕ трогай edit-ветку, факт «Ф», pct, «≈ N шт».
- `whitespace-nowrap` + `text-[10px]`, чтобы не разъезжалась узкая ячейка (Claude's Discretion — компактно).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "срезано" components/sales-plan/ProductPlanTable.tsx` возвращает не менее 1
    - `grep -c "нет товара" components/sales-plan/ProductPlanTable.tsx` возвращает не менее 1
    - `grep -c "lostShare > 0.02" components/sales-plan/ProductPlanTable.tsx` возвращает 1
    - `grep -c "isEmptyMonth" components/sales-plan/ProductPlanTable.tsx` возвращает не менее 2
    - `grep -c "nextArrivalAfter(p.arrivals" components/sales-plan/ProductPlanTable.tsx` возвращает 1
    - `npx tsc --noEmit` без новых ошибок
    - `npm run build` завершается успешно
  </acceptance_criteria>
  <done>Бейдж «срезано · приход dd.mm» и плашка «нет товара» рендерятся в non-edit ячейке при стокауте выше порога; дата из arrivals; build зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (RSC page.tsx) → client (ProductPlanTable) | сериализация planResult/arrivals; данные внутренние, только read; SALES VIEW уже enforced на page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Information Disclosure | бейдж среза раскрывает плановые метрики | accept | внутренняя ERP, раздел под RBAC SALES (VIEW уже требуется в page.tsx через requireSection("SALES")); метрики не PII |
| T-26-06 | Tampering | presentational-бейдж искажает план | mitigate | UI-only, движок и расчёт НЕ меняются (D-3); бейдж читает готовые метрики firstStockoutDate/lostRubToStockout; golden engine test остаётся GREEN как регресс-гарантия |
</threat_model>

<verification>
- `npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-iu.test.ts` — GREEN (движок не тронут: engine golden, iu===438_068_120)
- `npx tsc --noEmit` — без новых ошибок
- `npm run build` — успешно
</verification>

<success_criteria>
- Ячейка месяца в матрице «Товары» (compare-режим) показывает «срезано · приход dd.mm» при срезе выше 2% и «⚠ нет товара · приход dd.mm / придёт в <месяц>» при полностью нулевом месяце из-за стокаута (D-3)
- Дата ближайшего прихода вычислена из arrivals (первый > firstStockoutDate/today)
- Движок и golden-тесты не тронуты (UI-only)
</success_criteria>

<output>
После завершения создай `.planning/phases/26-roll-forward/26-02-SUMMARY.md`
</output>
