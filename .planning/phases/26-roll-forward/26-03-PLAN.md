---
phase: 26-roll-forward
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - lib/sales-plan/virtual-purchases.ts
  - app/actions/sales-plan.ts
  - app/api/cron/sales-plan-rollforward/route.ts
  - app/api/cron/dispatch/route.ts
  - tests/sales-plan-rollforward.test.ts
autonomous: true
requirements: [SP-17]
must_haves:
  truths:
    - "Просроченная авто-ACCEPTED виртуальная закупка (orderDate < today) сдвигается: orderDate -> today, expectedArrivalDate -> today + leadTime"
    - "manual виртуальные закупки НЕ сдвигаются (пользователь управляет датой)"
    - "Ежедневный крон (~04:40 МСК через dispatcher) регенерирует авто-SUGGESTED и сдвигает просроченные авто-ACCEPTED без ручного клика"
    - "Инвариант «не прошлым числом» держится для ACCEPTED: orderDate >= today, expectedArrivalDate >= today + leadTime"
  artifacts:
    - path: "lib/sales-plan/virtual-purchases.ts"
      provides: "Чистый хелпер rollForwardAcceptedArrivals (сдвиг просроченных авто-ACCEPTED)"
      contains: "rollForwardAccepted"
    - path: "app/actions/sales-plan.ts"
      provides: "regenerateVirtualPurchasesInternal сдвигает авто-ACCEPTED (UPDATE orderDate/expectedArrivalDate) + подаёт сдвинутые даты в suggester"
      contains: "rollForwardAccepted"
    - path: "app/api/cron/sales-plan-rollforward/route.ts"
      provides: "GET-роут крона (x-cron-secret), вызывает regenerateVirtualPurchases + пишет vpRollforwardLastRun"
      contains: "x-cron-secret"
    - path: "app/api/cron/dispatch/route.ts"
      provides: "Wiring крона: vpRollforwardCronTime (default 04:40) + vpRollforwardLastRun guard"
      contains: "vpRollforwardCronTime"
    - path: "tests/sales-plan-rollforward.test.ts"
      provides: "Тест инварианта «не прошлым числом» для ACCEPTED"
      contains: "не прошлым числом"
  key_links:
    - from: "app/actions/sales-plan.ts"
      to: "prisma.virtualPurchase.update"
      via: "сдвиг просроченных авто-ACCEPTED в транзакции regenerate"
      pattern: "virtualPurchase.update"
    - from: "app/api/cron/dispatch/route.ts"
      to: "app/api/cron/sales-plan-rollforward/route.ts"
      via: "dynamic import + shouldFireCron по vpRollforwardCronTime"
      pattern: "sales-plan-rollforward/route"
---

<objective>
SP-17 — Динамический roll-forward виртуальных отгрузок + ежедневный крон.

«Отгрузку не сделали реальной» = виртуальную закупку не сконвертировали в `Purchase`. Инвариант «не прошлым числом» (Phase 25, T-25-08) сейчас держится только для авто-SUGGESTED в `suggestVirtualPurchases`. Проблемы (D-4):
1. **ACCEPTED не двигается**: в `regenerateVirtualPurchasesInternal` авто-ACCEPTED подаётся в suggester на исходную `expectedArrivalDate` и не сдвигается, даже если `orderDate` прошёл. → сдвигаем авто-ACCEPTED (`orderDate -> today`, `expectedArrivalDate -> today + leadTime`), UPDATE в БД; `source="manual"` — не трогаем.
2. **Нет ежедневного триггера**: регенерация только при saveMonthLevels/saveDayOverrides/ручной кнопке → SUGGESTED «застывают» с датами вчерашнего дня. → крон в dispatcher ~04:40 МСК (после wb-sales-daily 04:30) вызывает regenerate.

Модель `VirtualPurchase` уже мутабельна (`orderDate`/`expectedArrivalDate` @db.Date) — миграция НЕ нужна. Крон = новый route + AppSetting KV-ключи (без схемы).

Purpose: план сам отражает «нет товара — нет продаж» без ручного клика; даты виртуальных отгрузок живут во времени.
Output: чистый хелпер `rollForwardAcceptedArrivals`, сдвиг авто-ACCEPTED в `regenerateVirtualPurchasesInternal`, крон-роут `sales-plan-rollforward` + wiring в dispatcher, тест инварианта для ACCEPTED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-roll-forward/26-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Контракты — использовать напрямую, без разведки. -->

VirtualPurchase (prisma/schema.prisma ~1955): мутабельная модель; `orderDate DateTime @db.Date`,
`expectedArrivalDate DateTime @db.Date`, `source String @default("auto")` ("auto"|"manual"),
`status VirtualPurchaseStatus @default(SUGGESTED)` (SUGGESTED|ACCEPTED|DISMISSED|CONVERTED),
`leadTimeDaysUsed Int?`, `qty Int`. UPDATE разрешён (Phase 25).

suggestVirtualPurchases (lib/sales-plan/virtual-purchases.ts): инвариант orderDate = max(today, breach − leadTime);
workArrivals (строки ~181-191) заполняется из existingVirtualPurchases по фильтру
`(vp.status === "ACCEPTED" || vp.source === "manual") && status !== DISMISSED/CONVERTED`
на ИСХОДНУЮ vp.expectedArrivalDate. Точка правки: подавать сдвинутую дату для авто-ACCEPTED.
VpArrivalInput: `{ id, status, orderDate, expectedArrivalDate, qty, source }`.

regenerateVirtualPurchasesInternal (app/actions/sales-plan.ts ~702):
- загружает existingVPs (ACCEPTED/DISMISSED/CONVERTED) + manualVPs → existingByProduct: Map<productId, {id,status,orderDate,expectedArrivalDate,qty,source}[]> (ISO-строки).
- строит vpProducts, вызывает suggestVirtualPurchases с today=todayMsk.
- транзакция: deleteMany(SUGGESTED+auto) + createMany(новые). ACCEPTED/DISMISSED/CONVERTED/manual неприкосновенны в deleteMany.
- todayMsk доступен из loadModelParamsForRegenerate() (~654): MSK today "YYYY-MM-DD".
- leadTimeDays: minLeadTimeByProduct.get(productId) ?? defaultLeadTimeDays.

addDays (lib/sales-plan/dates.ts): `addDays(iso, n): string`.

regenerateVirtualPurchases (public action ~882): requireSection("SALES","MANAGE") + regenerateVirtualPurchasesInternal() + revalidate.

Cron dispatcher (app/api/cron/dispatch/route.ts): читает AppSetting-ключи в findMany `key.in [...]`,
`settings.X ?? default`, `shouldFireCron({ currentHHMM, storedTime, lastRunDate, today })`,
dynamic import `../<name>/route` → `handler(req)`. Образец: wbSalesDailyCronTime (04:30, строки 134-152).
shouldFireCron/getMskHHMM/getMskTodayString из @/lib/wb-cron-schedule.

Cron endpoint образец (app/api/cron/wb-sales-daily/route.ts): `runtime="nodejs"`, `maxDuration=600`,
guard `req.headers.get("x-cron-secret") !== process.env.CRON_SECRET → 401`,
в конце upsert AppSetting lastRun = getMskTodayString().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистый хелпер rollForwardAcceptedArrivals + тест инварианта «не прошлым числом»</name>
  <read_first>
    - lib/sales-plan/virtual-purchases.ts (весь — workArrivals строки 179-191, инвариант orderDate строки 229-235)
    - lib/sales-plan/dates.ts (addDays)
    - tests/sales-plan-virtual.test.ts (стиль теста + фикстуры)
  </read_first>
  <behavior>
    - Test 1 (RED first) «не прошлым числом» для ACCEPTED: авто-ACCEPTED с orderDate="2026-06-01" (< today="2026-07-01"),
      leadTime=45 -> rollForwardAcceptedArrivals возвращает запись с orderDate="2026-07-01",
      expectedArrivalDate="2026-08-15" (today + 45). Инвариант: orderDate >= today И expectedArrivalDate >= today+leadTime.
    - Test 2: авто-ACCEPTED с orderDate >= today (не просрочена) -> НЕ сдвигается (даты без изменений).
    - Test 3: manual (source="manual") просроченная -> НЕ сдвигается (даты без изменений), возвращается как есть.
    - Test 4: возвращает изменённые записи с флагом shifted=true и исходную дату для аудита; неизменённые shifted=false.
  </behavior>
  <action>
Добавь в lib/sales-plan/virtual-purchases.ts чистый (не async, экспортируемый) хелпер:

```typescript
export interface RollForwardResult {
  id: string
  orderDate: string              // >= today (инвариант)
  expectedArrivalDate: string    // >= today + leadTimeDays (инвариант)
  qty: number
  shifted: boolean               // true если даты сдвинуты
}

/**
 * Сдвигает просроченные авто-ACCEPTED виртуальные закупки вперёд (SP-17, D-4).
 * Просроченная = source === "auto" && status === "ACCEPTED" && orderDate < today.
 * Сдвиг: orderDate -> today, expectedArrivalDate -> today + leadTimeDays.
 * source === "manual" НЕ трогается (пользователь управляет датой вручную).
 * Инвариант «не прошлым числом»: orderDate >= today, expectedArrivalDate >= today + leadTimeDays.
 *
 * @param items существующие VP (ACCEPTED/manual/…)
 * @param today ISO "YYYY-MM-DD"
 * @param leadTimeDaysByFn функция productId/id -> leadTimeDays (или единый leadTimeDays)
 */
export function rollForwardAcceptedArrivals(
  items: Array<{ id: string; status: string; source: string; orderDate: string; expectedArrivalDate: string; qty: number }>,
  today: string,
  leadTimeDays: number,
): RollForwardResult[] {
  return items.map((vp) => {
    const isAutoAccepted = vp.source === "auto" && vp.status === "ACCEPTED"
    const overdue = vp.orderDate < today
    if (isAutoAccepted && overdue) {
      return {
        id: vp.id,
        orderDate: today,
        expectedArrivalDate: addDays(today, leadTimeDays),
        qty: vp.qty,
        shifted: true,
      }
    }
    return {
      id: vp.id,
      orderDate: vp.orderDate,
      expectedArrivalDate: vp.expectedArrivalDate,
      qty: vp.qty,
      shifted: false,
    }
  })
}
```

`addDays` уже импортирован (строка 12). Хелпер pure — ноль Prisma.

Создай tests/sales-plan-rollforward.test.ts с 4 тестами из &lt;behavior&gt; (import `rollForwardAcceptedArrivals` из `@/lib/sales-plan/virtual-purchases`). Одно из описаний тестов должно содержать строку «не прошлым числом».
  </action>
  <verify>
    <automated>npx vitest run tests/sales-plan-rollforward.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-rollforward.test.ts` — все 4 теста GREEN
    - `grep -c "export function rollForwardAcceptedArrivals" lib/sales-plan/virtual-purchases.ts` возвращает 1
    - `grep -c "не прошлым числом" tests/sales-plan-rollforward.test.ts` возвращает не менее 1
    - `npx vitest run tests/sales-plan-virtual.test.ts` — GREEN (существующий контракт suggester не сломан)
    - `npx tsc --noEmit` без новых ошибок
  </acceptance_criteria>
  <done>rollForwardAcceptedArrivals реализован (сдвигает только авто-ACCEPTED overdue, инвариант держится); тест «не прошлым числом» GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Сдвиг авто-ACCEPTED в regenerateVirtualPurchasesInternal (UPDATE в БД + подача сдвинутых дат в suggester)</name>
  <read_first>
    - app/actions/sales-plan.ts (строки 694-873 — regenerateVirtualPurchasesInternal; строки 654-692 — loadModelParamsForRegenerate)
    - lib/sales-plan/virtual-purchases.ts (rollForwardAcceptedArrivals из Task 1 + фильтр workArrivals)
  </read_first>
  <action>
В app/actions/sales-plan.ts, в `regenerateVirtualPurchasesInternal`:

1. Импортируй `rollForwardAcceptedArrivals` из `@/lib/sales-plan/virtual-purchases` (рядом с существующим импортом suggestVirtualPurchases).

2. После построения `existingByProduct` (Map productId -> VP[] в ISO-строках, строки ~759-782) и ПЕРЕД построением vpProducts:
   - Для каждого товара примени `rollForwardAcceptedArrivals(existingByProduct.get(productId), todayMsk, leadTimeDays)` где `leadTimeDays = minLeadTimeByProduct.get(productId) ?? defaultLeadTimeDays`.
   - Собери список фактически сдвинутых записей `shiftedVps: Array<{ id, orderDate, expectedArrivalDate }>` (только `shifted === true`).
   - Замени даты в `existingByProduct` на сдвинутые (чтобы suggester увидел авто-ACCEPTED на новой expectedArrivalDate — тогда workArrivals использует сдвинутую дату).

   Примечание: `minLeadTimeByProduct`/`defaultLeadTimeDays` уже вычислены выше в функции (строки ~784-802, 704-709). Если порядок кода мешает — перенеси вычисление supplierLinks/minLeadTimeByProduct ВЫШЕ применения roll-forward, сохранив семантику.

3. В транзакции (строки ~844-868), ПОСЛЕ deleteMany(SUGGESTED+auto) и createMany(новые), добавь UPDATE сдвинутых авто-ACCEPTED:
   ```typescript
   for (const s of allShiftedVps) {
     await tx.virtualPurchase.update({
       where: { id: s.id },
       data: {
         orderDate: new Date(s.orderDate + "T00:00:00Z"),
         expectedArrivalDate: new Date(s.expectedArrivalDate + "T00:00:00Z"),
       },
     })
   }
   ```
   где `allShiftedVps` — плоский список shiftedVps по всем товарам, собранный на шаге 2.

Требования:
- Сдвигаем ТОЛЬКО `source="auto" && status="ACCEPTED" && orderDate < today` (обеспечивает хелпер). manual/CONVERTED/DISMISSED/SUGGESTED не трогаем.
- deleteMany по-прежнему сносит только SUGGESTED+auto — ACCEPTED переживают (сдвиг через UPDATE, не delete+create).
- Не ломай существующий флоу manual VPs / DISMISSED-подавления.
- Оставь `try/catch` обёртку функции и лог-строку `[regenerateVirtualPurchasesInternal]` при ошибке.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollForwardAcceptedArrivals" app/actions/sales-plan.ts` возвращает не менее 1
    - `grep -c "tx.virtualPurchase.update" app/actions/sales-plan.ts` возвращает не менее 1 (UPDATE сдвинутых в транзакции)
    - `grep -c "status: \"SUGGESTED\"" app/actions/sales-plan.ts` — deleteMany по-прежнему таргетит только SUGGESTED (ACCEPTED не удаляются)
    - `npx tsc --noEmit` без новых ошибок
    - `npm run build` успешно
  </acceptance_criteria>
  <done>regenerateVirtualPurchasesInternal сдвигает просроченные авто-ACCEPTED (UPDATE orderDate/expectedArrivalDate) и подаёт сдвинутые даты в suggester; manual не тронут; build зелёный</done>
</task>

<task type="auto">
  <name>Task 3: Крон-роут sales-plan-rollforward + wiring в dispatcher</name>
  <read_first>
    - app/api/cron/wb-sales-daily/route.ts (весь — образец guard x-cron-secret + upsert lastRun)
    - app/api/cron/dispatch/route.ts (весь — findMany ключей, settings.X ?? default, shouldFireCron, dynamic import; блок sales 134-152)
    - app/actions/sales-plan.ts (regenerateVirtualPurchases public action ~882)
  </read_first>
  <action>
**A. Новый роут app/api/cron/sales-plan-rollforward/route.ts:**

```typescript
// app/api/cron/sales-plan-rollforward/route.ts
// Daily cron (~04:40 МСК через dispatcher). Регенерирует авто-SUGGESTED виртуальные
// закупки и сдвигает просроченные авто-ACCEPTED (orderDate < today -> today),
// чтобы план сам отражал «нет товара — нет продаж» без ручного клика (SP-17).
// Защищён x-cron-secret. Phase 26.

import { NextRequest, NextResponse } from "next/server"
import { prisma } from "@/lib/prisma"
import { regenerateVirtualPurchases } from "@/app/actions/sales-plan"
import { getMskTodayString } from "@/lib/wb-cron-schedule"

export const runtime = "nodejs"
export const maxDuration = 600

export async function GET(req: NextRequest): Promise<NextResponse> {
  if (req.headers.get("x-cron-secret") !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
  }
  try {
    const r = await regenerateVirtualPurchases()
    const todayStr = getMskTodayString()
    await prisma.appSetting.upsert({
      where: { key: "vpRollforwardLastRun" },
      create: { key: "vpRollforwardLastRun", value: todayStr },
      update: { value: todayStr },
    })
    if (!r.ok) return NextResponse.json({ ok: false, error: r.error }, { status: 500 })
    return NextResponse.json({ ok: true })
  } catch (err) {
    console.error("[sales-plan-rollforward cron] error:", err)
    return NextResponse.json({ ok: false, error: (err as Error).message }, { status: 500 })
  }
}
```

ВАЖНО: `regenerateVirtualPurchases` — server action с `requireSection("SALES","MANAGE")`. Крон вызывается вне HTTP-сессии пользователя, requireSection может кинуть (нет auth-контекста). Проверь поведение: если requireSection в крон-контексте падает — вместо public action вызови внутренний `regenerateVirtualPurchasesInternal()` напрямую. Для этого экспортируй `regenerateVirtualPurchasesInternal` из sales-plan.ts (сейчас не экспортирован — добавь `export`) и вызови его в роуте вместо `regenerateVirtualPurchases`, затем сделай `revalidatePath` внутри роута НЕ нужно (крон не рендерит). Предпочтительный вариант: экспортировать и вызвать `regenerateVirtualPurchasesInternal()` напрямую (без RBAC-гейта, крон уже за x-cron-secret). Обнови импорт роута соответственно.

**B. Wiring в dispatcher app/api/cron/dispatch/route.ts:**
- В массив `key.in [...]` (строки 30-50) добавь два ключа: `"vpRollforwardCronTime"` и `"vpRollforwardLastRun"`.
- Добавь: `const rollforwardTime = settings.vpRollforwardCronTime ?? "04:40"` и `const rollforwardLastRun = settings.vpRollforwardLastRun ?? null`.
- Добавь блок shouldFireCron (по образцу sales, строки 134-152), РАСПОЛОЖИ его после блока sales (04:30) и перед orders (05:00):
  ```typescript
  // Phase 26 (SP-17): roll-forward виртуальных отгрузок. 04:40 МСК — после
  // wb-sales-daily (04:30). Регенерирует авто-SUGGESTED + сдвигает авто-ACCEPTED.
  if (shouldFireCron({ currentHHMM, storedTime: rollforwardTime, lastRunDate: rollforwardLastRun, today })) {
    try {
      const { GET: rollforwardHandler } = await import("../sales-plan-rollforward/route")
      const res = await rollforwardHandler(req)
      fired.push(`sp-rollforward:${res.status}`)
    } catch (e) {
      console.error("[dispatch] sp-rollforward error:", e)
      fired.push("sp-rollforward:error")
    }
  }
  ```

Требования:
- Default время 04:40 (после wb-sales-daily 04:30 — D-4). Валидное HH:MM с шагом 5 (isValidCronHHMM: 40 % 5 === 0 OK).
- AppSetting-ключи `vpRollforwardCronTime` / `vpRollforwardLastRun` (KV, без миграции).
- x-cron-secret guard в роуте (как во всех крон-роутах).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/api/cron/sales-plan-rollforward/route.ts` — файл существует
    - `grep -c "x-cron-secret" app/api/cron/sales-plan-rollforward/route.ts` возвращает 1
    - `grep -c "vpRollforwardLastRun" app/api/cron/sales-plan-rollforward/route.ts` возвращает не менее 1
    - `grep -c "vpRollforwardCronTime" app/api/cron/dispatch/route.ts` возвращает не менее 1
    - `grep -c "sales-plan-rollforward/route" app/api/cron/dispatch/route.ts` возвращает 1 (dynamic import)
    - `grep -c "04:40" app/api/cron/dispatch/route.ts` возвращает не менее 1 (default)
    - `grep -c "export async function regenerateVirtualPurchasesInternal" app/actions/sales-plan.ts` возвращает 1 (экспортирован для крона)
    - `npx tsc --noEmit` без новых ошибок
    - `npm run build` успешно
  </acceptance_criteria>
  <done>Роут sales-plan-rollforward (x-cron-secret, вызывает regenerateVirtualPurchasesInternal, пишет vpRollforwardLastRun) + wiring в dispatcher (vpRollforwardCronTime default 04:40, shouldFireCron guard); build зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron dispatcher -> sales-plan-rollforward route | внешний HTTP-триггер; защищён x-cron-secret == process.env.CRON_SECRET |
| regenerate -> VirtualPurchase.update (даты) | мутация плановых дат; риск даты в прошлом |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26 (главная) | Tampering | сдвиг авто-ACCEPTED в regenerate | mitigate | rollForwardAcceptedArrivals гарантирует orderDate >= today И expectedArrivalDate >= today + leadTime; покрыто тестом «не прошлым числом» для ACCEPTED — нереализуемый план (отгрузка прошлым числом) невозможен |
| T-26-07 | Spoofing | крон-роут без auth | mitigate | guard `x-cron-secret !== process.env.CRON_SECRET -> 401` (как во всех крон-роутах); прямой доступ извне заблокирован (Next.js слушает только localhost за nginx) |
| T-26-08 | Tampering | сдвиг manual-ACCEPTED | mitigate | хелпер сдвигает только `source==="auto"`; manual возвращается без изменений (D-4, Deferred: manual управляет пользователь) — покрыто Test 3 |
| T-26-09 | Denial of Service | крон дублируется / долго идёт | accept | shouldFireCron + vpRollforwardLastRun guard (1 раз/сутки); maxDuration=600; regenerate уже идёт при saveMonthLevels без проблем |
| T-26-10 | Elevation of Privilege | крон обходит SALES MANAGE | accept | крон вызывает regenerateVirtualPurchasesInternal напрямую (за x-cron-secret вместо RBAC); эквивалентно системной операции — не пользовательский путь |
</threat_model>

<verification>
- `npx vitest run tests/sales-plan-rollforward.test.ts` — GREEN (инвариант «не прошлым числом» для ACCEPTED)
- `npx vitest run tests/sales-plan-virtual.test.ts tests/sales-plan-engine.test.ts tests/sales-plan-iu.test.ts` — GREEN (golden не сломан)
- `npx tsc --noEmit` — без новых ошибок
- `npm run build` — успешно (крон-роут + dispatcher компилируются)
</verification>

<success_criteria>
- Просроченная авто-ACCEPTED (orderDate < today) сдвигается: orderDate -> today, expectedArrivalDate -> today + leadTime; manual не тронут (D-4)
- Ежедневный крон (~04:40 МСК через dispatcher, x-cron-secret) регенерирует авто-SUGGESTED и сдвигает авто-ACCEPTED без ручного клика
- Инвариант «не прошлым числом» держится для ACCEPTED (тест GREEN); golden-anchor iu===438_068_120 и engine golden не сломаны
</success_criteria>

<output>
После завершения создай `.planning/phases/26-roll-forward/26-03-SUMMARY.md`
</output>
