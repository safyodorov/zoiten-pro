---
phase: 26-roll-forward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/actions/sales-plan.ts
  - components/sales-plan/ProductPlanTable.tsx
  - components/sales-plan/ProductPlanCell.tsx
  - tests/sales-plan-distribute-forward.test.ts
autonomous: true
requirements: [SP-15]
must_haves:
  truths:
    - "При сохранении уровня в месяц с включённой галкой «распространить дальше» уровень записывается во все последующие месяцы горизонта без собственного явного уровня"
    - "Протяжка не перезаписывает месяцы с ручным (явным) SalesPlanMonthLevel"
    - "Пользователь может массово сбросить ручные уровни в авто по товару (строке) и по месяцу (колонке)"
    - "Поштучный сброс ✕ в ячейке виден без входа в режим редактирования (заметный маркер)"
  artifacts:
    - path: "app/actions/sales-plan.ts"
      provides: "distributeMonthLevelForward (чистый хелпер) + saveMonthLevels(distributeForward) + resetMonthLevelsToAuto action"
      contains: "resetMonthLevelsToAuto"
    - path: "components/sales-plan/ProductPlanTable.tsx"
      provides: "Тумблер «распространить дальше» в тулбаре + действия массового сброса по товару/месяцу"
      contains: "распространить"
    - path: "tests/sales-plan-distribute-forward.test.ts"
      provides: "Тест «протяжка не перезаписывает ручные»"
      contains: "не перезаписывает ручные"
  key_links:
    - from: "components/sales-plan/ProductPlanTable.tsx"
      to: "saveMonthLevels"
      via: "applyRecalc передаёт distributeForward-флаг тумблера"
      pattern: "saveMonthLevels\\("
    - from: "components/sales-plan/ProductPlanTable.tsx"
      to: "resetMonthLevelsToAuto"
      via: "onClick кнопок массового сброса"
      pattern: "resetMonthLevelsToAuto\\("
---

<objective>
SP-15 — Автопротяжка месячного уровня вперёд + механизм избавления от ручных уровней.

Пользователь ставит уровень (заказов/шт в день) в месяц и при включённой галке «распространить на последующие месяцы» (по умолчанию ВКЛ, D-1) уровень протягивается во все месяцы горизонта ≥ выбранного, у которых НЕТ собственного явного `SalesPlanMonthLevel` (авто-месяцы). Месяцы с ручным уровнем протяжка НЕ трогает (D-2). Плюс: массовый сброс ручных уровней в авто по товару / по месяцу (D-2), и более заметный поштучный ✕ в ячейке.

Purpose: довести правку уровней до ручной рабочей модели пользователя — «поставил месяц → протянулось дальше, но ручные не затёрло; натыкал лишних → сбросил разом».
Output: чистый хелпер `distributeMonthLevelForward`, изменённый `saveMonthLevels(distributeForward)`, новый action `resetMonthLevelsToAuto`, тумблер + кнопки массового сброса в UI, заметный ✕ в ячейке, unit-тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-roll-forward/26-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без разведки по кодовой базе. -->

SalesPlanMonthLevel (prisma/schema.prisma): уникальность `productId_month`; поля
`targetOrdersPerDay Float?`, `priceRub Float?`, `buyoutPct Float?`. Все три null → запись удаляется (fallback на baseline).

Существующий saveMonthLevels (app/actions/sales-plan.ts ~122):
```typescript
export async function saveMonthLevels(
  payload: Array<{ productId: string; month: string;
    targetOrdersPerDay: number | null; priceRub: number | null; buyoutPct: number | null }>,
): Promise<ActionResult>
// "use server" + requireSection("SALES","MANAGE") + Zod SaveMonthLevelsSchema + upsert/deleteMany
// в конце: await regenerateVirtualPurchasesInternal(); revalidateSalesPlanPaths()
```

Горизонт (page.tsx): `MONTHS = ["2026-07-01","2026-08-01","2026-09-01","2026-10-01","2026-11-01","2026-12-01"]`.
Клиент ProductPlanTable получает `months: string[]` пропом — источник истины для «последующих месяцев».

ActionResult = `{ ok: true } | { ok: false; error: string }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистый хелпер distributeMonthLevelForward + интеграция в saveMonthLevels(distributeForward) + тест</name>
  <read_first>
    - app/actions/sales-plan.ts (строки 106-174 — saveMonthLevels; строки 176-260 — scaleMonthLevels как аналог массовой операции)
    - lib/sales-plan/dates.ts (addDays — если понадобится; здесь достаточно строкового сравнения ISO-месяцев)
    - tests/sales-plan-virtual.test.ts (стиль vitest-теста для sales-plan)
  </read_first>
  <behavior>
    - Test 1 (Wave 0, RED first): «протяжка не перезаписывает ручные». Дано 3 месяца горизонта
      ["2026-07-01","2026-08-01","2026-09-01"], средний (авг) имеет ручной уровень.
      distributeMonthLevelForward({ targetMonth:"2026-07-01", value:30, horizonMonths, manualMonths:["2026-08-01"] })
      → возвращает записи для "2026-07-01" и "2026-09-01" со значением 30, НЕ содержит "2026-08-01".
    - Test 2: distributeForward=false-эквивалент — вызов только с targetMonth в manualMonths (или пустой horizon после target) → возвращает [] дополнительных.
    - Test 3: targetMonth сам всегда в результате (target пишется всегда, независимо от manual — его пишет вызывающий явно).
  </behavior>
  <action>
Добавь в app/actions/sales-plan.ts чистый (не async, экспортируемый для теста) хелпер:

```typescript
/**
 * Возвращает список ДОПОЛНИТЕЛЬНЫХ месяцев (кроме targetMonth) горизонта > targetMonth,
 * у которых НЕТ собственного явного уровня (авто-месяцы) — куда протянуть value.
 * Месяцы из manualMonths (ручные, явный SalesPlanMonthLevel) исключаются (D-2).
 * targetMonth сам НЕ включается (его пишет вызывающий отдельно).
 */
export function distributeMonthLevelForward(args: {
  targetMonth: string          // "2026-08-01"
  horizonMonths: string[]      // все месяцы горизонта (из клиента / MONTHS)
  manualMonths: string[]       // месяцы с явным SalesPlanMonthLevel для этого товара
}): string[] {
  const manual = new Set(args.manualMonths)
  return args.horizonMonths.filter(
    (m) => m > args.targetMonth && !manual.has(m),
  )
}
```

Измени сигнатуру `saveMonthLevels` — добавь второй опциональный параметр `opts?: { distributeForward?: boolean; horizonMonths?: string[] }`:
- Если `opts?.distributeForward === true` И `opts.horizonMonths` передан:
  1. Перед upsert-циклом загрузи для затронутых товаров существующие явные уровни:
     `const existing = await prisma.salesPlanMonthLevel.findMany({ where: { productId: { in: [...uniq productId из payload] } }, select: { productId, month } })`.
     Собери `manualMonthsByProduct: Map<productId, string[]>` (month → ISO "YYYY-MM-01" через `.toISOString().slice(0,10)`).
  2. Для каждого item payload, где `targetOrdersPerDay !== null` (протягиваем только заданный уровень, не удаление):
     вызови `distributeMonthLevelForward({ targetMonth: item.month, horizonMonths: opts.horizonMonths, manualMonths: manualMonthsByProduct.get(item.productId) ?? [] })`
     и для каждого полученного месяца добавь синтетический upsert того же `targetOrdersPerDay` (priceRub/buyoutPct = null — протягиваем только скорость заказов, как в applyRecalc).
  3. Дедуплицируй: если протянутый месяц уже присутствует в payload явно — payload-запись имеет приоритет (не перезаписывай её протяжкой). Реализуй через `Set` ключей `productId|month`, заполненный из исходного payload ДО добавления протянутых.
- Сохрани существующее поведение при `distributeForward` не задан / false (текущие вызовы без opts работают как раньше — обратная совместимость).
- НЕ трогай финальный `regenerateVirtualPurchasesInternal()` + `revalidateSalesPlanPaths()`.

Создай tests/sales-plan-distribute-forward.test.ts с тремя тестами из &lt;behavior&gt; (import `distributeMonthLevelForward` из `@/app/actions/sales-plan`). Ключевая строка описания теста: «не перезаписывает ручные».

ВАЖНО: `distributeMonthLevelForward` должен быть pure и НЕ иметь `"use server"`-ограничений на импорт в тест — он экспортируется из server-actions файла, но сам не async. Проверь что vitest грузит его без ошибки (файл уже "use server" на уровне модуля — экспорт чистой функции допустим, тест импортирует только её).
  </action>
  <verify>
    <automated>npx vitest run tests/sales-plan-distribute-forward.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-distribute-forward.test.ts` — все 3 теста GREEN
    - `grep -n "export function distributeMonthLevelForward" app/actions/sales-plan.ts` → 1 совпадение
    - `grep -c "не перезаписывает ручные" tests/sales-plan-distribute-forward.test.ts` → ≥ 1
    - `npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-iu.test.ts tests/sales-plan-virtual.test.ts` — GREEN (golden не сломан)
    - `npx tsc --noEmit` — без новых ошибок в app/actions/sales-plan.ts
  </acceptance_criteria>
  <done>distributeMonthLevelForward + saveMonthLevels(opts.distributeForward) реализованы; тест «не перезаписывает ручные» GREEN; golden-тесты не сломаны</done>
</task>

<task type="auto">
  <name>Task 2: resetMonthLevelsToAuto action (массовый сброс ручных → авто)</name>
  <read_first>
    - app/actions/sales-plan.ts (строки 122-174 saveMonthLevels — паттерн deleteMany + regenerate + revalidate; строки 176-260 scaleMonthLevels — Zod-схема с optional productIds)
  </read_first>
  <action>
Добавь в app/actions/sales-plan.ts новый server action `resetMonthLevelsToAuto`:

```typescript
const ResetMonthLevelsSchema = z.object({
  productId: z.string().min(1).optional(),           // сброс по товару (строке)
  month: z.string().regex(/^\d{4}-\d{2}-01$/).optional(), // сброс по месяцу (колонке)
  productIds: z.array(z.string().min(1)).optional(), // сброс по выбранным товарам (опц.)
}).refine((v) => v.productId != null || v.month != null || (v.productIds && v.productIds.length > 0),
  { message: "Нужен хотя бы один критерий" })

export async function resetMonthLevelsToAuto(payload: {
  productId?: string; month?: string; productIds?: string[]
}): Promise<ActionResult & { deletedCount?: number }> {
  await requireSection("SALES", "MANAGE")
  const parsed = ResetMonthLevelsSchema.safeParse(payload)
  if (!parsed.success) return { ok: false, error: "Невалидные критерии сброса" }
  try {
    const where: Record<string, unknown> = {}
    if (parsed.data.productId) where.productId = parsed.data.productId
    if (parsed.data.productIds && parsed.data.productIds.length > 0)
      where.productId = { in: parsed.data.productIds }
    if (parsed.data.month) where.month = new Date(parsed.data.month + "T00:00:00Z")
    const res = await prisma.salesPlanMonthLevel.deleteMany({ where })
    await regenerateVirtualPurchasesInternal(parsed.data.productId ? [parsed.data.productId] : parsed.data.productIds)
    revalidateSalesPlanPaths()
    return { ok: true, deletedCount: res.count }
  } catch (err) {
    console.error("[resetMonthLevelsToAuto]", err)
    return { ok: false, error: "Не удалось сбросить уровни" }
  }
}
```

Требования:
- Семантика: удаляет ВСЕ явные `SalesPlanMonthLevel` по заданному критерию → эти месяцы возвращаются на авто (baseline). Это именно «сброс ручных → авто» из D-2.
- `productId` (строка) и `month` (колонка) можно комбинировать (AND) — сброс одной ячейки массово тоже допустим.
- Регенерация VP после сброса (как в saveMonthLevels), передавай productIds для сужения если известны.
- Разместить action рядом с saveMonthLevels/scaleMonthLevels (после scaleMonthLevels).
  </action>
  <verify>
    <automated>grep -n "export async function resetMonthLevelsToAuto" app/actions/sales-plan.ts && npx tsc --noEmit 2>&1 | grep -c "sales-plan.ts" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function resetMonthLevelsToAuto" app/actions/sales-plan.ts` → 1 совпадение
    - `grep -n "requireSection(\"SALES\", \"MANAGE\")" app/actions/sales-plan.ts` внутри тела нового action (RBAC на месте)
    - `grep -n "regenerateVirtualPurchasesInternal" app/actions/sales-plan.ts` — вызывается и в новом action
    - `npx tsc --noEmit` — без новых ошибок
  </acceptance_criteria>
  <done>resetMonthLevelsToAuto реализован с RBAC SALES MANAGE, deleteMany по critерию, regenerate + revalidate</done>
</task>

<task type="auto">
  <name>Task 3: UI — тумблер «распространить дальше» + заметный ✕ + кнопки массового сброса</name>
  <read_first>
    - components/sales-plan/ProductPlanTable.tsx (весь — тулбар строки 271-319, applyRecalc строки 177-215, рендер ячеек 428-502)
    - components/sales-plan/ProductPlanCell.tsx (весь — крестик ✕ строки 92-103, кнопка-режим строки 112-138)
  </read_first>
  <action>
**A. ProductPlanTable.tsx — тумблер «распространить дальше»:**
- Добавь state: `const [distributeForward, setDistributeForward] = useState(true)` (по умолчанию ВКЛ, D-1; сессионный, НЕ persist — Deferred).
- В тулбаре рядом с кнопкой «Пересчитать план» (внутри `isEditMode && !readOnly` блока) добавь checkbox-лейбл:
  ```tsx
  <label className="flex items-center gap-1.5 text-xs text-muted-foreground cursor-pointer select-none">
    <input type="checkbox" checked={distributeForward}
      onChange={(e) => setDistributeForward(e.target.checked)} className="h-3.5 w-3.5" />
    Распространить на последующие месяцы
  </label>
  ```
- В `applyRecalc` передай флаг: `await saveMonthLevels(payload, { distributeForward, horizonMonths: months })`.

**B. ProductPlanTable.tsx — массовый сброс:**
- Импортируй `resetMonthLevelsToAuto` и иконку `Eraser` из lucide-react.
- Добавь в тулбар (внутри `isEditMode && !readOnly`) выпадающее/inline-действие «Сбросить ручные → авто по месяцу»: для каждого месяца из `months` — маленькая ghost-кнопка (по образцу блока «Масштабировать») с onClick, вызывающим:
  ```tsx
  startTransition(async () => {
    const r = await resetMonthLevelsToAuto({ month })
    if (!r.ok) { toast.error(r.error || "Не удалось сбросить"); return }
    toast.success(`Сброшено ручных уровней: ${r.deletedCount ?? 0}`)
    router.refresh()
  })
  ```
  Подпись группы: «Сбросить ручные (месяц):». Держи компактно (h-7 px-2 text-xs), можно отдельной строкой тулбара.
- В per-строке (в ячейке «Название» или отдельной sticky-ячейке действий) добавь маленькую кнопку-иконку `Eraser` (title=«Сбросить ручные уровни товара → авто»), вызывающую `resetMonthLevelsToAuto({ productId: p.productId })` с тем же toast+refresh. Клик оберни `stopPropagation`, чтобы не открывать диалог товара.

**C. ProductPlanCell.tsx — заметный ✕:**
- Требование D-2: сейчас ✕ виден только в режиме редактирования ячейки. Сделай заметнее — в НЕ-editing состоянии (return внизу компонента), когда `value != null` (есть ручной уровень) И `!readOnly`, покажи маленький ✕ рядом со значением, который при клике вызывает `onClear()` (обёрнутый `e.stopPropagation()`, чтобы не входить в editing).
  ```tsx
  {value != null && !readOnly && (
    <button type="button" title="Сбросить на авто"
      onClick={(e) => { e.stopPropagation(); onClear() }}
      className="text-[10px] text-muted-foreground hover:text-red-500">✕</button>
  )}
  ```
  Размести так, чтобы не ломать выравнивание значения (например в той же строке `<span>` со значением, слева от числа, или под ним). Кнопка внутри существующего `<button>`-обёртки ячейки — используй `<span role="button">` или вложенный button с stopPropagation (валидный HTML: не вкладывай `<button>` в `<button>` — оберни значение в `<div>` вместо внешнего `<button>`, либо вынеси ✕ за пределы кликабельной кнопки). Выбери валидный вариант: замени внешний `<button>` на `<div role="button" tabIndex>` при `!readOnly`, чтобы вложить настоящий `<button>` для ✕.
- onClear уже пробрасывается в clearDraft; при не-editing клике ✕ также вызови немедленный сброс через сохранение: НЕТ — оставь как draft-механику (clearDraft ставит "" → в applyRecalc уйдёт targetOrdersPerDay=null → deleteMany). Достаточно draft-сброса; НЕ добавляй прямой server-вызов из ячейки.
  </action>
  <verify>
    <automated>grep -n "Распространить на последующие месяцы" components/sales-plan/ProductPlanTable.tsx && grep -n "resetMonthLevelsToAuto" components/sales-plan/ProductPlanTable.tsx && npx tsc --noEmit 2>&1 | grep -Ec "ProductPlanTable|ProductPlanCell" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Распространить на последующие месяцы" components/sales-plan/ProductPlanTable.tsx` → ≥ 1
    - `grep -c "distributeForward" components/sales-plan/ProductPlanTable.tsx` → ≥ 3 (state + checkbox + вызов saveMonthLevels)
    - `grep -c "resetMonthLevelsToAuto" components/sales-plan/ProductPlanTable.tsx` → ≥ 2 (по месяцу + по товару)
    - `grep -n "saveMonthLevels(payload, { distributeForward" components/sales-plan/ProductPlanTable.tsx` → присутствует передача opts
    - `grep -c "onClear" components/sales-plan/ProductPlanCell.tsx` → ≥ 2 (editing ✕ + не-editing ✕)
    - Нет вложенного `<button>` в `<button>`: `grep -n "role=\"button\"" components/sales-plan/ProductPlanCell.tsx` → присутствует (внешняя обёртка div при !readOnly)
    - `npx tsc --noEmit` — без новых ошибок в этих двух файлах
    - `npm run build` завершается успешно (RSC/client границы валидны)
  </acceptance_criteria>
  <done>Тумблер «распространить дальше» (default ВКЛ) передаёт distributeForward в saveMonthLevels; массовый сброс по месяцу и по товару вызывает resetMonthLevelsToAuto; заметный ✕ в ячейке при наличии ручного уровня; build зелёный</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ProductPlanTable) → server action saveMonthLevels / resetMonthLevelsToAuto | пользовательский ввод (уровни, критерии сброса) пересекает границу; уже за RBAC SALES MANAGE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | resetMonthLevelsToAuto без критерия | mitigate | Zod `.refine` требует ≥1 критерий (productId/month/productIds); пустой where НЕ допускается — иначе deleteMany снесёт ВСЕ уровни всех товаров |
| T-26-02 | Elevation of Privilege | saveMonthLevels / resetMonthLevelsToAuto | mitigate | `requireSection("SALES","MANAGE")` первой строкой в обоих action (уже enforced, сохранить) |
| T-26-03 | Tampering | distributeForward протяжка затирает ручные | mitigate | распространение исключает manualMonths (D-2) + payload-приоритет через Set-дедуп; покрыто тестом «не перезаписывает ручные» |
| T-26-04 | Denial of Service | массовая протяжка × много товаров | accept | горизонт ≤ 6 месяцев, внутренняя ERP, единственный оператор SUPERADMIN; объём upsert ограничен числом товаров × 6 |
</threat_model>

<verification>
- `npx vitest run tests/sales-plan-distribute-forward.test.ts` — GREEN
- `npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-iu.test.ts tests/sales-plan-virtual.test.ts` — GREEN (golden не сломан: iu===438_068_120, engine golden)
- `npx tsc --noEmit` — без новых ошибок
- `npm run build` — успешно
</verification>

<success_criteria>
- Галка «распространить на последующие месяцы» в тулбаре «Товары» (default ВКЛ); при пересчёте протягивает уровень в авто-месяцы ≥ выбранного, не трогая ручные (D-1, D-2)
- resetMonthLevelsToAuto сбрасывает ручные уровни в авто по товару и по месяцу; заметный ✕ в ячейке при наличии ручного уровня (D-2)
- Тест «протяжка не перезаписывает ручные» GREEN; golden-anchor iu===438_068_120 и engine golden не сломаны
</success_criteria>

<output>
После завершения создай `.planning/phases/26-roll-forward/26-01-SUMMARY.md`
</output>
