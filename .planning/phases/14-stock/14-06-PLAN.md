---
phase: 14-stock
plan: 06
type: execute
wave: 4
depends_on:
  - "14-01"
  - "14-03"
  - "14-04"
  - "14-05"
files_modified:
  - app/(dashboard)/stock/page.tsx
  - components/stock/StockProductTable.tsx
  - components/stock/StockFilters.tsx
  - lib/stock-data.ts
requirements:
  - STOCK-16
  - STOCK-17
  - STOCK-18
  - STOCK-19
  - STOCK-20
autonomous: true

must_haves:
  truths:
    - "Страница /stock — RSC, получает products (с MarketplaceArticle + WbCard + WbCardWarehouseStock) через Prisma"
    - "Таблица использует rowSpan: одна Сводная строка на Product (агрегат) + N строк на каждый MarketplaceArticle"
    - "Sticky 4 колонки: Фото 80px (left-0) + Сводка 240px (left-[80px]) + Ярлык 80px (left-[320px]) + Артикул 120px (left-[400px])"
    - "Colgroup headers 2 уровня (группа + sub-column О/З/Об/Д): sticky top-0 и top-[40px]"
    - "6 групп колонок: РФ / Иваново / Производство / МП / WB / Ozon (Ozon все '—' placeholder)"
    - "formatStockValue: <10 → toFixed(1), ≥10 → Math.floor; null → '—' с text-muted-foreground"
    - "Цветовая кодировка Д (3-уровневая): зелёный Д≤0, жёлтый 0<Д<threshold, красный Д≥threshold"
    - "threshold = turnoverNormDays × 0.3 × ordersPerDay; null если ordersPerDay null"
    - "Inline input productionStock в Сводной строке: debounced 500ms через updateProductionStock"
    - "Фильтры MultiSelect бренд/категория/подкатегория + toggle «только с дефицитом» через URL searchParams"
    - "TurnoverNormInput + WbRefreshButton + IvanovoUploadButton в шапке страницы"
  artifacts:
    - path: "lib/stock-data.ts"
      provides: "getStockData() RSC helper: собирает Product-level остатки с агрегацией МП/WB"
      exports: ["getStockData", "ProductStockRow", "StockAggregates"]
    - path: "app/(dashboard)/stock/page.tsx"
      provides: "RSC страница с шапкой (TurnoverNormInput + buttons) + StockFilters + StockProductTable"
      min_lines: 60
    - path: "components/stock/StockProductTable.tsx"
      provides: "Client sticky-таблица с rowSpan, 6 групп, цветовой кодировкой Д, inline productionStock input"
      min_lines: 200
    - path: "components/stock/StockFilters.tsx"
      provides: "MultiSelect бренд/кат/подкат + toggle deficit через URL searchParams"
      min_lines: 40
  key_links:
    - from: "app/(dashboard)/stock/page.tsx"
      to: "lib/stock-data.ts getStockData"
      via: "await getStockData(searchParams)"
      pattern: "getStockData"
    - from: "components/stock/StockProductTable.tsx"
      to: "lib/stock-math.ts calculateStockMetrics + deficitThreshold"
      via: "client-side per-cell compute"
      pattern: "calculateStockMetrics|deficitThreshold"
    - from: "components/stock/StockProductTable.tsx"
      to: "app/actions/stock.ts updateProductionStock"
      via: "debounced server action call inline input"
      pattern: "updateProductionStock"
    - from: "components/stock/StockFilters.tsx"
      to: "URL searchParams (bands, categories, subcategories, deficit)"
      via: "router.replace"
      pattern: "router.replace|searchParams"
---

<objective>
Plan 14-06 — RSC page /stock с Product-level таблицей (Wave 4, зависит от 14-01/03/04/05).

**Purpose:** Главная страница раздела. Реализует Product-level таблицу остатков: одна строка «Сводная» (агрегат по Product) + N строк (per-артикул, MarketplaceArticle). Sticky 4 колонки слева, 6 групп данных (РФ / Иваново / Производство / МП / WB / Ozon), цветовая кодировка Д (3 уровня), фильтры MultiSelect + toggle дефицит. Интегрирует все компоненты из Plans 14-04/05 (IvanovoUploadButton, TurnoverNormInput, WbRefreshButton) в шапку.

**Output:**
- `lib/stock-data.ts` — RSC helper для сбора Product[] с MarketplaceArticle[] + WbCard[] + WbCardWarehouseStock[], агрегация суммы per-Product
- `app/(dashboard)/stock/page.tsx` — полноценная RSC (заменяет placeholder из Plan 14-01)
- `components/stock/StockProductTable.tsx` — sticky-таблица с rowSpan, 2-уровневый header, 6 групп, inline productionStock input, цветовая кодировка Д
- `components/stock/StockFilters.tsx` — URL searchParams-driven фильтры
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stock/14-UI-SPEC.md
@.planning/phases/14-stock/14-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-stock/14-01-SUMMARY.md
@.planning/phases/14-stock/14-03-SUMMARY.md
@.planning/phases/14-stock/14-04-SUMMARY.md
@.planning/phases/14-stock/14-05-SUMMARY.md
@components/prices/PriceCalculatorTable.tsx
@components/prices/PricesFilters.tsx
@app/(dashboard)/prices/wb/page.tsx
@prisma/schema.prisma
@lib/stock-math.ts
@lib/normalize-sku.ts

<interfaces>
From lib/stock-math.ts (Plan 14-01):
```typescript
export function calculateStockMetrics({stock, ordersPerDay, turnoverNormDays}): {turnoverDays, deficit}
export function deficitThreshold(turnoverNormDays: number, ordersPerDay: number | null): number | null
```

From prisma/schema.prisma (актуальное состояние после Plan 14-01):
```prisma
model Product {
  id                        String
  sku                       String @unique     // УКТ-000001
  name                      String
  brand                     Brand @relation(...)
  brandId                   String
  category                  Category? @relation(...)
  subcategory               Subcategory? @relation(...)
  abcStatus                 AbcStatus?
  ivanovoStock              Int?
  productionStock           Int?
  articles                  MarketplaceArticle[]
  photoUrl                  String?             // проверить точное имя поля в schema
  deletedAt                 DateTime?
}

model MarketplaceArticle {
  id           String
  productId    String
  marketplace  Marketplace @relation(...)
  marketplaceId String
  article      String      // nmId как строка; для WB parse to Int
}

model WbCard {
  id              String
  nmId            Int @unique
  name            String
  stockQty        Int?        // денорм из Plan 14-03 (SUM всех warehouses)
  avgSalesSpeed7d Float?      // З (заказы/день)
  warehouses      WbCardWarehouseStock[]
}

model WbCardWarehouseStock {
  wbCardId    String
  warehouseId Int
  quantity    Int
}

model AppSetting {
  key   String @id
  value String
}
```

From app/actions/stock.ts (Plan 14-05):
```typescript
export async function updateProductionStock(productId: string, value: number | null): Promise<{ok: true} | {ok: false, error: string}>
```

From components/prices/PriceCalculatorTable.tsx (паттерн sticky + rowSpan + accumulated left):
```typescript
// left-0, left-[80px], left-[320px], left-[400px] — sticky accumulated
// z-20 для data cells, z-30 для header
// bg-background (не transparent) на sticky ячейках
```

From components/prices/PricesFilters.tsx (паттерн URL-driven MultiSelect):
```typescript
const router = useRouter()
const searchParams = useSearchParams()
const selectedBrands = searchParams.get("brands")?.split(",") ?? []
// router.replace + new URLSearchParams pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/stock-data.ts RSC helper + StockFilters client</name>
  <files>
    lib/stock-data.ts,
    components/stock/StockFilters.tsx
  </files>
  <read_first>
    - prisma/schema.prisma (Product, MarketplaceArticle, WbCard, WbCardWarehouseStock, Marketplace, Brand, Category, Subcategory)
    - lib/prisma.ts (singleton import)
    - app/(dashboard)/prices/wb/page.tsx (паттерн data fetch + агрегация для RSC)
    - components/prices/PricesFilters.tsx (паттерн MultiSelectDropdown + URL searchParams + router.replace)
    - components/ui/multi-select-dropdown.tsx (props API)
    - .planning/phases/14-stock/14-UI-SPEC.md §1 Layout + §rowSpan-схема для /stock
    - .planning/REQUIREMENTS.md STOCK-16, STOCK-18, STOCK-20
  </read_first>
  <action>
    ШАГ 1 — Создать `lib/stock-data.ts`:

    ```typescript
    // lib/stock-data.ts
    // Phase 14: RSC data helper для /stock — собирает Product[] с агрегацией остатков.
    // Возвращает типы, которые потребляет StockProductTable (client).

    import { prisma } from "@/lib/prisma"

    export interface StockArticleRow {
      id: string                // MarketplaceArticle.id
      marketplaceName: string   // "WB", "Ozon", "ДМ", "ЯМ"
      article: string           // nmId (WB) или др.
      // WB-specific данные (null для non-WB)
      wbCard: {
        id: string
        nmId: number
        stockQty: number | null         // денорм SUM остатков
        avgSalesSpeed7d: number | null  // З
      } | null
    }

    export interface StockProductRow {
      id: string
      sku: string
      name: string
      brandName: string
      categoryName: string | null
      subcategoryName: string | null
      abcStatus: string | null
      photoUrl: string | null

      // Inline редактируемые поля:
      ivanovoStock: number | null
      productionStock: number | null

      articles: StockArticleRow[]

      // Агрегаты (вычислены в SQL/JS на уровне Product):
      aggregates: {
        wbTotalStock: number | null       // SUM(wbCard.stockQty) по всем WB articles
        wbTotalOrdersPerDay: number | null // SUM(wbCard.avgSalesSpeed7d)
        mpTotalStock: number | null       // WB + Ozon (Ozon placeholder 0 или null)
        mpTotalOrdersPerDay: number | null
        rfTotalStock: number | null       // Иваново + Производство + МП
      }
    }

    export interface StockDataResult {
      products: StockProductRow[]
      turnoverNormDays: number // default 37
    }

    export interface StockFilters {
      brandIds?: string[]
      categoryIds?: string[]
      subcategoryIds?: string[]
      onlyDeficit?: boolean
    }

    const TURNOVER_NORM_KEY = "stock.turnoverNormDays"

    export async function getStockData(filters: StockFilters = {}): Promise<StockDataResult> {
      // 1. Получить норму оборачиваемости
      const setting = await prisma.appSetting.findUnique({ where: { key: TURNOVER_NORM_KEY } })
      const turnoverNormDays = setting ? parseInt(setting.value, 10) : 37

      // 2. Собрать Product с relations
      const whereFilters: any = { deletedAt: null }
      if (filters.brandIds && filters.brandIds.length > 0) {
        whereFilters.brandId = { in: filters.brandIds }
      }
      if (filters.categoryIds && filters.categoryIds.length > 0) {
        whereFilters.categoryId = { in: filters.categoryIds }
      }
      if (filters.subcategoryIds && filters.subcategoryIds.length > 0) {
        whereFilters.subcategoryId = { in: filters.subcategoryIds }
      }

      const products = await prisma.product.findMany({
        where: whereFilters,
        include: {
          brand: true,
          category: true,
          subcategory: true,
          articles: {
            include: { marketplace: true },
          },
        },
        orderBy: { sku: "asc" },
      })

      // 3. Для всех WB-артикулов одним запросом собрать WbCard по nmId
      const wbArticleValues = products.flatMap((p) =>
        p.articles
          .filter((a) => a.marketplace.name.toLowerCase() === "wb" || a.marketplace.name === "WB")
          .map((a) => a.article)
      )
      const wbNmIds = wbArticleValues
        .map((s) => parseInt(s, 10))
        .filter((n) => !isNaN(n))

      const wbCards = wbNmIds.length > 0
        ? await prisma.wbCard.findMany({
            where: { nmId: { in: wbNmIds } },
            select: { id: true, nmId: true, stockQty: true, avgSalesSpeed7d: true },
          })
        : []
      const wbCardByNmId = new Map(wbCards.map((c) => [c.nmId, c]))

      // 4. Сформировать строки
      const rows: StockProductRow[] = products.map((p) => {
        const articles: StockArticleRow[] = p.articles.map((a) => {
          const mpName = a.marketplace.name
          const isWb = mpName.toLowerCase() === "wb" || mpName === "WB"
          let wbCard: StockArticleRow["wbCard"] = null
          if (isWb) {
            const nmId = parseInt(a.article, 10)
            if (!isNaN(nmId)) {
              const card = wbCardByNmId.get(nmId)
              if (card) {
                wbCard = {
                  id: card.id,
                  nmId: card.nmId,
                  stockQty: card.stockQty,
                  avgSalesSpeed7d: card.avgSalesSpeed7d,
                }
              }
            }
          }
          return {
            id: a.id,
            marketplaceName: mpName,
            article: a.article,
            wbCard,
          }
        })

        // Агрегаты по Product
        const wbStocks = articles.map((a) => a.wbCard?.stockQty).filter((s): s is number => s !== null && s !== undefined)
        const wbOrders = articles.map((a) => a.wbCard?.avgSalesSpeed7d).filter((s): s is number => s !== null && s !== undefined)

        const wbTotalStock = wbStocks.length > 0 ? wbStocks.reduce((a, b) => a + b, 0) : null
        const wbTotalOrdersPerDay = wbOrders.length > 0 ? wbOrders.reduce((a, b) => a + b, 0) : null

        // МП = WB + Ozon (Ozon пока null)
        const mpTotalStock = wbTotalStock  // + Ozon когда реализуется (STOCK-FUT-06)
        const mpTotalOrdersPerDay = wbTotalOrdersPerDay

        // РФ = Иваново + Производство + МП
        const parts: number[] = []
        if (p.ivanovoStock !== null) parts.push(p.ivanovoStock)
        if (p.productionStock !== null) parts.push(p.productionStock)
        if (mpTotalStock !== null) parts.push(mpTotalStock)
        const rfTotalStock = parts.length > 0 ? parts.reduce((a, b) => a + b, 0) : null

        return {
          id: p.id,
          sku: p.sku,
          name: p.name,
          brandName: p.brand.name,
          categoryName: p.category?.name ?? null,
          subcategoryName: p.subcategory?.name ?? null,
          abcStatus: p.abcStatus,
          photoUrl: (p as any).photoUrl ?? (p as any).photo ?? null, // адаптация под схему
          ivanovoStock: p.ivanovoStock,
          productionStock: p.productionStock,
          articles,
          aggregates: {
            wbTotalStock,
            wbTotalOrdersPerDay,
            mpTotalStock,
            mpTotalOrdersPerDay,
            rfTotalStock,
          },
        }
      })

      // 5. Фильтр "только с дефицитом" — применить на уровне JS после агрегации
      // Д > 0 на любом уровне (РФ или МП или WB)
      // Реализация: импорт calculateStockMetrics из lib/stock-math.ts
      let filtered = rows
      if (filters.onlyDeficit) {
        const { calculateStockMetrics } = await import("@/lib/stock-math")
        filtered = rows.filter((r) => {
          const rfMetrics = calculateStockMetrics({
            stock: r.aggregates.rfTotalStock,
            ordersPerDay: r.aggregates.wbTotalOrdersPerDay,
            turnoverNormDays,
          })
          return rfMetrics.deficit !== null && rfMetrics.deficit > 0
        })
      }

      return {
        products: filtered,
        turnoverNormDays,
      }
    }

    // Helper: брендs/кategories/subcategories для фильтров
    export async function getStockFilterOptions() {
      const [brands, categories, subcategories] = await Promise.all([
        prisma.brand.findMany({ orderBy: { name: "asc" } }),
        prisma.category.findMany({ orderBy: { name: "asc" }, include: { brand: true } }),
        prisma.subcategory.findMany({ orderBy: { name: "asc" }, include: { category: true } }),
      ])
      return { brands, categories, subcategories }
    }
    ```

    **Примечание:** Точное имя поля фото в Product (photoUrl vs photo vs imageUrl) — проверить в schema.prisma и адаптировать. Если не существует — оставить null placeholder.

    ШАГ 2 — Создать `components/stock/StockFilters.tsx`:

    ```typescript
    // components/stock/StockFilters.tsx
    // Phase 14 (STOCK-20): Filters для /stock через URL searchParams.
    // Паттерн: components/prices/PricesFilters.tsx — MultiSelectDropdown + router.replace.

    "use client"

    import { useRouter, useSearchParams } from "next/navigation"
    import { useCallback } from "react"
    import { MultiSelectDropdown } from "@/components/ui/multi-select-dropdown"
    import { Switch } from "@/components/ui/switch"
    import { Label } from "@/components/ui/label"

    interface Option {
      id: string
      name: string
    }

    interface StockFiltersProps {
      brands: Option[]
      categories: Option[]
      subcategories: Option[]
    }

    export function StockFilters({ brands, categories, subcategories }: StockFiltersProps) {
      const router = useRouter()
      const searchParams = useSearchParams()

      const selectedBrands = searchParams.get("brands")?.split(",").filter(Boolean) ?? []
      const selectedCategories = searchParams.get("categories")?.split(",").filter(Boolean) ?? []
      const selectedSubcategories = searchParams.get("subcategories")?.split(",").filter(Boolean) ?? []
      const onlyDeficit = searchParams.get("deficit") === "1"

      const updateParam = useCallback((key: string, values: string[]) => {
        const params = new URLSearchParams(searchParams.toString())
        if (values.length > 0) {
          params.set(key, values.join(","))
        } else {
          params.delete(key)
        }
        router.replace(`?${params.toString()}`, { scroll: false })
      }, [searchParams, router])

      const toggleDeficit = useCallback((checked: boolean) => {
        const params = new URLSearchParams(searchParams.toString())
        if (checked) {
          params.set("deficit", "1")
        } else {
          params.delete("deficit")
        }
        router.replace(`?${params.toString()}`, { scroll: false })
      }, [searchParams, router])

      return (
        <div className="flex flex-wrap items-center gap-2">
          <MultiSelectDropdown
            label="Бренд"
            options={brands.map((b) => ({ value: b.id, label: b.name }))}
            selected={selectedBrands}
            onChange={(v) => updateParam("brands", v)}
          />
          <MultiSelectDropdown
            label="Категория"
            options={categories.map((c) => ({ value: c.id, label: c.name }))}
            selected={selectedCategories}
            onChange={(v) => updateParam("categories", v)}
          />
          <MultiSelectDropdown
            label="Подкатегория"
            options={subcategories.map((s) => ({ value: s.id, label: s.name }))}
            selected={selectedSubcategories}
            onChange={(v) => updateParam("subcategories", v)}
          />
          <div className="flex items-center gap-2 ml-2">
            <Switch
              id="deficit-toggle"
              checked={onlyDeficit}
              onCheckedChange={toggleDeficit}
            />
            <Label htmlFor="deficit-toggle" className="text-sm cursor-pointer">
              Только с дефицитом
            </Label>
          </div>
        </div>
      )
    }
    ```

    **Важно:** Props API `MultiSelectDropdown` — проверить реальный интерфейс в components/ui/multi-select-dropdown.tsx (label/options/selected/onChange могут быть иначе). Адаптировать под существующий компонент.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "lib/stock-data.ts" && test -f "components/stock/StockFilters.tsx" && grep -q "getStockData" lib/stock-data.ts && grep -q "MultiSelectDropdown" components/stock/StockFilters.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `lib/stock-data.ts` экспортирует `getStockData`, `StockProductRow`, `StockArticleRow`, `StockDataResult`, `StockFilters`, `getStockFilterOptions`
    - `getStockData` использует Prisma для Product + MarketplaceArticle + Brand/Category/Subcategory; отдельный запрос `wbCard.findMany({nmId: {in}})` батчем
    - Агрегаты: wbTotalStock, wbTotalOrdersPerDay, mpTotalStock, mpTotalOrdersPerDay, rfTotalStock — корректно null-safe
    - Фильтр onlyDeficit: динамический импорт `calculateStockMetrics`, фильтр по deficit>0 на уровне РФ
    - `components/stock/StockFilters.tsx` — "use client", использует `useRouter` + `useSearchParams`, `router.replace({scroll: false})`
    - Фильтры через 4 URL params: brands, categories, subcategories, deficit
    - `npx tsc --noEmit` → 0 ошибок
  </acceptance_criteria>
  <done>
    RSC data layer + фильтры готовы. Data type контракты зафиксированы для Task 2 (таблица).
  </done>
</task>

<task type="auto">
  <name>Task 2: StockProductTable + /stock/page.tsx (полная интеграция)</name>
  <files>
    components/stock/StockProductTable.tsx,
    app/(dashboard)/stock/page.tsx
  </files>
  <read_first>
    - lib/stock-data.ts (Task 1 — типы StockProductRow, StockArticleRow, StockDataResult)
    - components/prices/PriceCalculatorTable.tsx (паттерн sticky + rowSpan + accumulated left, z-index, 2-level header)
    - components/ui/table.tsx (shadcn Table components API)
    - lib/stock-math.ts (calculateStockMetrics, deficitThreshold из Plan 14-01)
    - app/actions/stock.ts (updateProductionStock из Plan 14-05)
    - components/stock/TurnoverNormInput.tsx (Plan 14-05)
    - components/stock/WbRefreshButton.tsx (Plan 14-05)
    - components/stock/IvanovoUploadButton.tsx (Plan 14-04)
    - .planning/phases/14-stock/14-UI-SPEC.md §1-4, §5 (Dialog), §Typography, §Color, §rowSpan, §formatStockValue, §Цветовая кодировка Д
    - .planning/REQUIREMENTS.md STOCK-16, STOCK-17, STOCK-18, STOCK-19
  </read_first>
  <action>
    ШАГ 1 — Создать `components/stock/StockProductTable.tsx`:

    ```typescript
    // components/stock/StockProductTable.tsx
    // Phase 14 (STOCK-16..19): Client sticky-таблица Product-level остатков.
    // 4 sticky колонки (Фото/Сводка/Ярлык/Артикул) + 6 групп данных (РФ/Иваново/Производство/МП/WB/Ozon).
    // rowSpan: Сводная строка на Product + N строк per-article.
    //
    // Паттерн: components/prices/PriceCalculatorTable.tsx — sticky accumulated left, z-20/30, bg-background.

    "use client"

    import Image from "next/image"
    import { useRef, useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { toast } from "sonner"
    import { cn } from "@/lib/utils"
    import {
      Table,
      TableBody,
      TableCell,
      TableHead,
      TableHeader,
      TableRow,
    } from "@/components/ui/table"
    import { calculateStockMetrics, deficitThreshold } from "@/lib/stock-math"
    import { updateProductionStock } from "@/app/actions/stock"
    import type { StockProductRow } from "@/lib/stock-data"

    function formatStockValue(n: number): string {
      if (n < 10) return n.toFixed(1)
      return Math.floor(n).toString()
    }

    // ──────────────────────────────────────────────────────────────────
    // Типизированные колонки заголовка
    // ──────────────────────────────────────────────────────────────────

    const GROUP_HEADERS = [
      { key: "rf", label: "РФ", colSpan: 1 },           // только О
      { key: "ivanovo", label: "Иваново", colSpan: 1 }, // только О
      { key: "production", label: "Производство", colSpan: 1 }, // только О (inline input)
      { key: "mp", label: "МП", colSpan: 4 },          // О/З/Об/Д
      { key: "wb", label: "WB", colSpan: 4 },          // О/З/Об/Д
      { key: "ozon", label: "Ozon", colSpan: 4 },      // О/З/Об/Д (placeholder)
    ] as const

    interface Props {
      products: StockProductRow[]
      turnoverNormDays: number
    }

    export function StockProductTable({ products, turnoverNormDays }: Props) {
      const router = useRouter()
      const [, startTransition] = useTransition()
      const timersRef = useRef<Map<string, ReturnType<typeof setTimeout>>>(new Map())

      const debouncedSaveProduction = (productId: string, value: number | null) => {
        const existing = timersRef.current.get(productId)
        if (existing) clearTimeout(existing)
        const timer = setTimeout(() => {
          startTransition(async () => {
            const result = await updateProductionStock(productId, value)
            if (result.ok) {
              toast.success("Производство обновлено")
              router.refresh()
            } else {
              toast.error(`Не удалось сохранить производство: ${result.error}`)
            }
          })
        }, 500)
        timersRef.current.set(productId, timer)
      }

      if (products.length === 0) {
        return (
          <div className="text-center py-16">
            <h3 className="text-sm font-medium">Нет товаров для отображения</h3>
            <p className="text-xs text-muted-foreground mt-1">
              Добавьте товары в разделе Товары и привяжите артикулы WB.
            </p>
          </div>
        )
      }

      return (
        <div className="overflow-x-auto border rounded">
          <Table>
            <TableHeader>
              {/* Уровень 1 — группы */}
              <TableRow>
                <TableHead className="sticky top-0 z-30 bg-background w-20 text-xs font-medium text-center border-b" rowSpan={2}>Фото</TableHead>
                <TableHead className="sticky top-0 z-30 bg-background w-60 text-xs font-medium text-center border-b" rowSpan={2}>Сводка</TableHead>
                <TableHead className="sticky top-0 z-30 bg-background w-20 text-xs font-medium text-center border-b" rowSpan={2}>Ярлык</TableHead>
                <TableHead className="sticky top-0 z-30 bg-background w-[120px] text-xs font-medium text-center border-b" rowSpan={2}>Артикул</TableHead>
                {GROUP_HEADERS.map((g) => (
                  <TableHead
                    key={g.key}
                    colSpan={g.colSpan}
                    className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b border-r"
                  >
                    {g.label}
                  </TableHead>
                ))}
              </TableRow>
              {/* Уровень 2 — sub-columns */}
              <TableRow>
                {/* РФ / Иваново / Производство: по 1 колонке О */}
                <TableHead className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Остаток (шт)">О</TableHead>
                <TableHead className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Остаток (шт)">О</TableHead>
                <TableHead className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Остаток (шт)">О</TableHead>
                {/* МП / WB / Ozon: по 4 колонки О/З/Об/Д */}
                {(["mp", "wb", "ozon"] as const).flatMap((group) => [
                  <TableHead key={`${group}-o`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Остаток (шт)">О</TableHead>,
                  <TableHead key={`${group}-z`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Заказы в день (шт/д)">З</TableHead>,
                  <TableHead key={`${group}-ob`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Оборачиваемость (дней)">Об</TableHead>,
                  <TableHead key={`${group}-d`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b border-r px-2 py-1" title="Дефицит (дней). Красный = срочно, жёлтый = пора думать, зелёный = всё ок">Д</TableHead>,
                ])}
              </TableRow>
            </TableHeader>
            <TableBody>
              {products.map((p, idx) => {
                const rowSpan = 1 + p.articles.length
                const mpMetrics = calculateStockMetrics({
                  stock: p.aggregates.mpTotalStock,
                  ordersPerDay: p.aggregates.mpTotalOrdersPerDay,
                  turnoverNormDays,
                })
                const wbMetrics = calculateStockMetrics({
                  stock: p.aggregates.wbTotalStock,
                  ordersPerDay: p.aggregates.wbTotalOrdersPerDay,
                  turnoverNormDays,
                })
                const mpThreshold = deficitThreshold(turnoverNormDays, p.aggregates.mpTotalOrdersPerDay)
                const wbThreshold = deficitThreshold(turnoverNormDays, p.aggregates.wbTotalOrdersPerDay)

                return (
                  <React.Fragment key={p.id}>
                    {/* Сводная строка — rowSpan по Фото/Сводка + данные Product-level */}
                    <TableRow className={cn(idx > 0 && "border-t-4 border-t-border")}>
                      <TableCell rowSpan={rowSpan} className="sticky left-0 z-20 bg-background border-r w-20 align-top p-2">
                        <div className="flex justify-center">
                          {p.photoUrl ? (
                            <Image
                              src={p.photoUrl}
                              alt={p.name}
                              width={72} height={96}
                              className="rounded border object-cover aspect-[3/4]"
                            />
                          ) : (
                            <div className="w-[72px] h-[96px] rounded border bg-muted/30 flex items-center justify-center text-xs text-muted-foreground">—</div>
                          )}
                        </div>
                      </TableCell>
                      <TableCell rowSpan={rowSpan} className="sticky left-[80px] z-20 bg-background border-r w-60 align-top p-3">
                        <div className="flex flex-col gap-1">
                          <div className="text-sm font-medium leading-snug line-clamp-2">{p.name}</div>
                          <div className="text-xs text-muted-foreground">{p.sku}</div>
                          <div className="text-xs text-muted-foreground">{p.brandName}</div>
                          {p.categoryName && <div className="text-xs text-muted-foreground">{p.categoryName}</div>}
                        </div>
                      </TableCell>
                      {/* Ярлык (ABC) — на Сводной строке */}
                      <TableCell className="sticky left-[320px] z-20 bg-background border-r w-20 align-top text-center text-xs">{p.abcStatus ?? "—"}</TableCell>
                      {/* Артикул на Сводной = "Сводная" placeholder */}
                      <TableCell className="sticky left-[400px] z-20 bg-background border-r w-[120px] text-xs font-medium">Сводная</TableCell>

                      {/* РФ — О */}
                      <StockCell value={p.aggregates.rfTotalStock} />
                      {/* Иваново — О */}
                      <StockCell value={p.ivanovoStock} />
                      {/* Производство — inline input */}
                      <TableCell className="px-2 py-1 text-xs tabular-nums text-right">
                        <input
                          type="number"
                          min={0}
                          max={99999}
                          className="h-8 w-20 rounded border border-input bg-transparent px-2 text-xs tabular-nums text-right focus:ring-2 focus:ring-ring"
                          defaultValue={p.productionStock ?? ""}
                          placeholder="—"
                          aria-label={`Производство: ${p.name}`}
                          onChange={(e) => {
                            const v = e.target.value
                            if (v === "") {
                              debouncedSaveProduction(p.id, null)
                            } else {
                              const parsed = parseInt(v, 10)
                              if (!isNaN(parsed) && parsed >= 0) debouncedSaveProduction(p.id, parsed)
                            }
                          }}
                        />
                      </TableCell>

                      {/* МП О/З/Об/Д */}
                      <StockCell value={p.aggregates.mpTotalStock} />
                      <StockCell value={p.aggregates.mpTotalOrdersPerDay} />
                      <StockCell value={mpMetrics.turnoverDays} />
                      <DeficitCell deficit={mpMetrics.deficit} threshold={mpThreshold} />

                      {/* WB О/З/Об/Д */}
                      <StockCell value={p.aggregates.wbTotalStock} />
                      <StockCell value={p.aggregates.wbTotalOrdersPerDay} />
                      <StockCell value={wbMetrics.turnoverDays} />
                      <DeficitCell deficit={wbMetrics.deficit} threshold={wbThreshold} />

                      {/* Ozon О/З/Об/Д — placeholder */}
                      <StockCell value={null} />
                      <StockCell value={null} />
                      <StockCell value={null} />
                      <StockCell value={null} />
                    </TableRow>

                    {/* Строки per-article */}
                    {p.articles.map((a) => {
                      const isWb = a.wbCard !== null
                      const aMetrics = isWb
                        ? calculateStockMetrics({
                            stock: a.wbCard!.stockQty,
                            ordersPerDay: a.wbCard!.avgSalesSpeed7d,
                            turnoverNormDays,
                          })
                        : { turnoverDays: null, deficit: null }
                      const aThreshold = isWb
                        ? deficitThreshold(turnoverNormDays, a.wbCard!.avgSalesSpeed7d)
                        : null

                      return (
                        <TableRow key={a.id} className="border-t border-t-border/60">
                          {/* Фото + Сводка взяты через rowSpan из Сводной строки */}
                          <TableCell className="sticky left-[320px] z-20 bg-background border-r w-20 text-center text-xs text-muted-foreground">—</TableCell>
                          <TableCell className="sticky left-[400px] z-20 bg-background border-r w-[120px] text-xs">
                            {a.marketplaceName}: {a.article}
                          </TableCell>
                          {/* РФ/Иваново/Производство: «—» на строках per-article (только Сводная) */}
                          <StockCell value={null} />
                          <StockCell value={null} />
                          <StockCell value={null} />
                          {/* МП per-article = только если это WB */}
                          <StockCell value={isWb ? a.wbCard!.stockQty : null} />
                          <StockCell value={isWb ? a.wbCard!.avgSalesSpeed7d : null} />
                          <StockCell value={aMetrics.turnoverDays} />
                          <DeficitCell deficit={aMetrics.deficit} threshold={aThreshold} />
                          {/* WB per-article */}
                          <StockCell value={isWb ? a.wbCard!.stockQty : null} />
                          <StockCell value={isWb ? a.wbCard!.avgSalesSpeed7d : null} />
                          <StockCell value={aMetrics.turnoverDays} />
                          <DeficitCell deficit={aMetrics.deficit} threshold={aThreshold} />
                          {/* Ozon */}
                          <StockCell value={null} />
                          <StockCell value={null} />
                          <StockCell value={null} />
                          <StockCell value={null} />
                        </TableRow>
                      )
                    })}
                  </React.Fragment>
                )
              })}
            </TableBody>
          </Table>
        </div>
      )
    }

    // Вспомогательная ячейка О/З/Об
    function StockCell({ value }: { value: number | null }) {
      return (
        <TableCell className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
          {value !== null ? formatStockValue(value) : <span className="text-muted-foreground">—</span>}
        </TableCell>
      )
    }

    // Ячейка Д с цветовой кодировкой
    function DeficitCell({ deficit, threshold }: { deficit: number | null; threshold: number | null }) {
      return (
        <TableCell
          className={cn(
            "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right border-r",
            deficit === null && "text-muted-foreground",
            deficit !== null && deficit <= 0 && "text-green-600 dark:text-green-500",
            deficit !== null && threshold !== null && deficit > 0 && deficit < threshold && "text-yellow-600 dark:text-yellow-400",
            deficit !== null && threshold !== null && deficit >= threshold && "text-red-600 dark:text-red-500 font-medium",
          )}
        >
          {deficit !== null ? formatStockValue(deficit) : "—"}
        </TableCell>
      )
    }

    // React import для Fragment
    import React from "react"
    ```

    **Примечание:** Размести `import React` в начале файла, а не в конце (исправь при имплементации).

    ШАГ 2 — Заменить `app/(dashboard)/stock/page.tsx` (placeholder из Plan 14-01) полной реализацией:

    ```typescript
    // app/(dashboard)/stock/page.tsx
    // Phase 14 (STOCK-16): RSC главная страница /stock — Product-level остатки.

    import { requireSection } from "@/lib/rbac"
    import { getStockData, getStockFilterOptions, type StockFilters as StockFiltersType } from "@/lib/stock-data"
    import { StockFilters } from "@/components/stock/StockFilters"
    import { StockProductTable } from "@/components/stock/StockProductTable"
    import { TurnoverNormInput } from "@/components/stock/TurnoverNormInput"
    import { WbRefreshButton } from "@/components/stock/WbRefreshButton"
    import { IvanovoUploadButton } from "@/components/stock/IvanovoUploadButton"

    export const metadata = {
      title: "Управление остатками",
    }

    interface PageProps {
      searchParams: Promise<{
        brands?: string
        categories?: string
        subcategories?: string
        deficit?: string
      }>
    }

    export default async function StockPage({ searchParams }: PageProps) {
      await requireSection("STOCK")

      const params = await searchParams
      const filters: StockFiltersType = {
        brandIds: params.brands?.split(",").filter(Boolean),
        categoryIds: params.categories?.split(",").filter(Boolean),
        subcategoryIds: params.subcategories?.split(",").filter(Boolean),
        onlyDeficit: params.deficit === "1",
      }

      const [stockData, filterOptions] = await Promise.all([
        getStockData(filters),
        getStockFilterOptions(),
      ])

      return (
        <div className="space-y-6">
          {/* Шапка */}
          <div className="flex items-center justify-between flex-wrap gap-4">
            <TurnoverNormInput initialDays={stockData.turnoverNormDays} />
            <div className="flex gap-2 ml-auto">
              <IvanovoUploadButton />
              <WbRefreshButton />
            </div>
          </div>

          {/* Фильтры */}
          <StockFilters
            brands={filterOptions.brands}
            categories={filterOptions.categories}
            subcategories={filterOptions.subcategories}
          />

          {/* Таблица */}
          <StockProductTable
            products={stockData.products}
            turnoverNormDays={stockData.turnoverNormDays}
          />
        </div>
      )
    }
    ```

    ШАГ 3 — Проверка: `npx tsc --noEmit` → 0 ошибок.

    **Локальный тест (если dev server работает):** открыть `http://localhost:3001/stock` → проверить что таблица рендерится, sticky колонки работают при горизонтальном скролле, inline input productionStock сохраняет значение, TurnoverNormInput сохраняет при изменении.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "components/stock/StockProductTable.tsx" && grep -q "rowSpan" components/stock/StockProductTable.tsx && grep -q "sticky left-0" components/stock/StockProductTable.tsx && grep -q "calculateStockMetrics" components/stock/StockProductTable.tsx && grep -q "debouncedSaveProduction\|updateProductionStock" components/stock/StockProductTable.tsx && grep -q "getStockData" app/\(dashboard\)/stock/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/stock/StockProductTable.tsx` — "use client", использует calculateStockMetrics + deficitThreshold
    - Sticky 4 колонки с точными left: left-0, left-[80px], left-[320px], left-[400px]
    - 2-level TableHeader: top-0 + top-[40px], bg-background
    - Colspan: Фото/Сводка/Ярлык/Артикул rowSpan=2, группы colspan=1/1/1/4/4/4
    - rowSpan на TableCell Фото + Сводка = 1 + N_articles
    - DeficitCell — 3 цветовых уровня: зелёный/жёлтый/красный + text-muted-foreground на null
    - Inline input productionStock: debounced 500ms, updateProductionStock action
    - Empty state: "Нет товаров для отображения" + hint
    - `app/(dashboard)/stock/page.tsx` — async RSC, `requireSection("STOCK")`, searchParams Promise-обёрнут (Next 15)
    - Шапка: TurnoverNormInput (слева) + IvanovoUploadButton + WbRefreshButton (справа через ml-auto)
    - `npx tsc --noEmit` → 0 ошибок
  </acceptance_criteria>
  <done>
    RSC page /stock + клиентская таблица с rowSpan/sticky/цветовой Д/inline production input. Все компоненты из 14-04/14-05 интегрированы в шапку. Страница функциональна.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 ошибок
- `/stock` открывается без client error (если dev server + DB работает)
- Фильтры меняют URL searchParams, таблица перерендеривается RSC
- Inline productionStock input сохраняет значение (debounced), toast появляется
- TurnoverNormInput меняет значение, обе таблицы (/stock и /stock/wb) revalidate
- Sticky колонки не просвечивают при горизонтальном скролле
</verification>

<success_criteria>
- [x] lib/stock-data.ts: getStockData + getStockFilterOptions + типы
- [x] components/stock/StockFilters.tsx: URL searchParams + 3 MultiSelect + 1 toggle
- [x] components/stock/StockProductTable.tsx: sticky 4 cols + 2-level header + 6 группы + rowSpan + цветовая кодировка Д + inline productionStock
- [x] app/(dashboard)/stock/page.tsx: RSC с шапкой (TurnoverNorm + 2 buttons) + Filters + Table
- [x] formatStockValue: <10 → toFixed(1), ≥10 → Math.floor, null → "—"
- [x] DeficitCell 3 цвета: зелёный/жёлтый/красный
- [x] Empty state при пустых products
- [x] npx tsc --noEmit → 0 ошибок
</success_criteria>

<output>
Create `.planning/phases/14-stock/14-06-SUMMARY.md`:
- Data layer: как собираются агрегаты (SQL vs JS), оптимизация через batched wbCard.findMany
- UI: паттерны sticky inherited из PriceCalculatorTable (ссылка на точные строки)
- Empty state: реализован
- Interactive states: debounced productionStock, router.refresh после save
- Готовность к интеграции /stock/wb (Plan 14-07 добавит StockTabs поверх)
</output>
