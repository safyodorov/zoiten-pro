---
phase: 14-stock
plan: 05
type: execute
wave: 3
depends_on:
  - "14-01"
files_modified:
  - app/actions/stock.ts
  - components/stock/TurnoverNormInput.tsx
  - components/stock/WbRefreshButton.tsx
requirements:
  - STOCK-13
  - STOCK-14
  - STOCK-15
autonomous: true

must_haves:
  truths:
    - "updateProductionStock(productId, value) server action — debounced 500ms от UI, requireSection STOCK MANAGE, Zod int(0..99999) или null"
    - "updateTurnoverNorm(days) server action — требует STOCK MANAGE, Zod int(1..100), сохраняет в AppSetting key='stock.turnoverNormDays'"
    - "TurnoverNormInput — client компонент, input type=number, h-8 w-16, debounced 500ms save через useRef<ReturnType<typeof setTimeout>>"
    - "WbRefreshButton — primary CTA (variant=default), вызывает POST /api/wb-sync, toast.loading с dismiss, ~1-2 мин длительность"
    - "Все toast strings — русские согласно UI-SPEC Copywriting Contract"
    - "revalidatePath('/stock') + revalidatePath('/stock/wb') после updateTurnoverNorm и WbRefresh"
  artifacts:
    - path: "app/actions/stock.ts"
      provides: "Расширенный server actions: upsertIvanovoStock (Plan 14-04) + updateProductionStock + updateTurnoverNorm"
      exports: ["upsertIvanovoStock", "updateProductionStock", "updateTurnoverNorm"]
    - path: "components/stock/TurnoverNormInput.tsx"
      provides: "Client компонент в шапке /stock — inline edit нормы оборачиваемости"
      min_lines: 50
    - path: "components/stock/WbRefreshButton.tsx"
      provides: "Primary CTA кнопка «Обновить из WB» с toast.loading"
      min_lines: 40
  key_links:
    - from: "components/stock/TurnoverNormInput.tsx"
      to: "app/actions/stock.ts updateTurnoverNorm"
      via: "debounced server action call 500ms"
      pattern: "updateTurnoverNorm"
    - from: "components/stock/WbRefreshButton.tsx"
      to: "/api/wb-sync"
      via: "fetch POST"
      pattern: "/api/wb-sync"
    - from: "app/actions/stock.ts updateTurnoverNorm"
      to: "prisma.appSetting"
      via: "upsert key='stock.turnoverNormDays'"
      pattern: "stock.turnoverNormDays"
---

<objective>
Plan 14-05 — Production manual input + Turnover norm + WB refresh (Wave 3, параллельно с 14-04).

**Purpose:** Расширить `app/actions/stock.ts` (из Plan 14-04) двумя server actions (`updateProductionStock`, `updateTurnoverNorm`) и создать два client-компонента: `TurnoverNormInput` (единственный input нормы в шапке `/stock`, паттерн `GlobalRatesBar`) + `WbRefreshButton` (primary CTA «Обновить из WB», паттерн `WbSyncButton` с `toast.loading`).

**Output:**
- `app/actions/stock.ts` расширен 2 новыми actions (upsertIvanovoStock остаётся из Plan 14-04)
- `components/stock/TurnoverNormInput.tsx` — debounced save через useRef таймер, `revalidatePath("/stock")` + `revalidatePath("/stock/wb")`
- `components/stock/WbRefreshButton.tsx` — primary CTA с toast.loading/dismiss

**Важно:** Inline input `productionStock` в ячейке таблицы реализуется в Plan 14-06 внутри `StockProductTable` — в этом плане только server action. Кнопка Button в шапке также будет добавлена в Plan 14-06 (WbRefreshButton готов к использованию).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stock/14-RESEARCH.md
@.planning/phases/14-stock/14-UI-SPEC.md
@.planning/REQUIREMENTS.md
@app/actions/stock.ts
@components/prices/GlobalRatesBar.tsx
@components/cards/WbSyncButton.tsx
@lib/rbac.ts
@prisma/schema.prisma

<interfaces>
From app/actions/stock.ts (Plan 14-04):
```typescript
export async function upsertIvanovoStock(rows): Promise<UpsertIvanovoResult>
// Плейсхолдеры (реализовать в этом плане):
// export async function updateProductionStock(productId: string, value: number | null)
// export async function updateTurnoverNorm(days: number)
```

From components/prices/GlobalRatesBar.tsx (ПАТТЕРН debounced save):
```typescript
const timers = useRef<Partial<Record<RateKey, ReturnType<typeof setTimeout>>>>({})

const debouncedSave = useCallback((key: RateKey, value: number) => {
  if (timers.current[key]) clearTimeout(timers.current[key])
  timers.current[key] = setTimeout(async () => {
    startTransition(async () => {
      try {
        await updateAppSetting(key, String(value))
        toast.success("Сохранено")
      } catch (e) {
        toast.error("Ошибка сохранения")
      }
    })
  }, 500)
}, [startTransition])
```

From components/cards/WbSyncButton.tsx (ПАТТЕРН toast.loading):
```typescript
const toastId = toast.loading("Синхронизация...")
try {
  const res = await fetch("/api/wb-sync", { method: "POST" })
  toast.dismiss(toastId)
  if (!res.ok) { toast.error("..."); return }
  toast.success("Готово")
  router.refresh()
} catch {
  toast.dismiss(toastId)
  toast.error("...")
}
```

From lib/rbac.ts:
```typescript
await requireSection("STOCK", "MANAGE")
```

From prisma/schema.prisma:
```prisma
model AppSetting {
  key   String @id
  value String
}

model Product {
  id                        String    @id
  productionStock           Int?
  productionStockUpdatedAt  DateTime?
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить app/actions/stock.ts двумя новыми server actions</name>
  <files>
    app/actions/stock.ts,
    tests/stock-actions.test.ts
  </files>
  <read_first>
    - app/actions/stock.ts (Plan 14-04 — существующий файл с upsertIvanovoStock)
    - .planning/REQUIREMENTS.md STOCK-13, STOCK-14
    - .planning/phases/14-stock/14-UI-SPEC.md §2 TurnoverNormInput (validation 1..100)
    - lib/rbac.ts
    - prisma/schema.prisma (Product.productionStock + AppSetting)
    - app/actions/pricing.ts (паттерн updateAppSetting с Zod)
  </read_first>
  <behavior>
    - updateProductionStock("prod-id", 500) → {ok: true} + БД updated (Product.productionStock=500, productionStockUpdatedAt=now)
    - updateProductionStock("prod-id", null) → {ok: true} + БД (productionStock=null, productionStockUpdatedAt=now)
    - updateProductionStock("prod-id", -5) → throws Zod validation error
    - updateProductionStock("prod-id", 100000) → throws Zod validation error
    - updateProductionStock("nonexistent-id", 500) → Prisma throws RecordNotFound
    - updateProductionStock без auth → throws (requireSection rejects)
    - updateTurnoverNorm(37) → {ok: true} + AppSetting "stock.turnoverNormDays"="37"
    - updateTurnoverNorm(0) → throws Zod
    - updateTurnoverNorm(101) → throws Zod
    - updateTurnoverNorm(1) → valid
    - updateTurnoverNorm(100) → valid
  </behavior>
  <action>
    ШАГ 1 — Открыть `app/actions/stock.ts` (создан в Plan 14-04) и ДОБАВИТЬ две новые функции (после `upsertIvanovoStock`):

    ```typescript
    // ... существующий код из Plan 14-04 ...

    // ──────────────────────────────────────────────────────────────────
    // Plan 14-05: updateProductionStock (STOCK-13)
    // ──────────────────────────────────────────────────────────────────

    const ProductionStockSchema = z.object({
      productId: z.string().min(1),
      value: z.number().int().min(0).max(99999).nullable(),
    })

    export async function updateProductionStock(
      productId: string,
      value: number | null
    ): Promise<{ ok: true } | { ok: false; error: string }> {
      await requireSection("STOCK", "MANAGE")

      const parsed = ProductionStockSchema.safeParse({ productId, value })
      if (!parsed.success) {
        return { ok: false, error: parsed.error.issues[0]?.message ?? "Validation failed" }
      }

      try {
        await prisma.product.update({
          where: { id: parsed.data.productId },
          data: {
            productionStock: parsed.data.value,
            productionStockUpdatedAt: new Date(),
          },
        })
        revalidatePath("/stock")
        return { ok: true }
      } catch (e) {
        return { ok: false, error: (e as Error).message }
      }
    }

    // ──────────────────────────────────────────────────────────────────
    // Plan 14-05: updateTurnoverNorm (STOCK-14)
    // ──────────────────────────────────────────────────────────────────

    const TurnoverNormSchema = z.object({
      days: z.number().int().min(1).max(100),
    })

    const TURNOVER_NORM_KEY = "stock.turnoverNormDays"

    export async function updateTurnoverNorm(
      days: number
    ): Promise<{ ok: true } | { ok: false; error: string }> {
      await requireSection("STOCK", "MANAGE")

      const parsed = TurnoverNormSchema.safeParse({ days })
      if (!parsed.success) {
        return { ok: false, error: parsed.error.issues[0]?.message ?? "Допустимо от 1 до 100 дней" }
      }

      try {
        await prisma.appSetting.upsert({
          where: { key: TURNOVER_NORM_KEY },
          create: { key: TURNOVER_NORM_KEY, value: String(parsed.data.days) },
          update: { value: String(parsed.data.days) },
        })
        revalidatePath("/stock")
        revalidatePath("/stock/wb")
        return { ok: true }
      } catch (e) {
        return { ok: false, error: (e as Error).message }
      }
    }
    ```

    Убедиться что импорт `z` (из `zod`) и `revalidatePath` (из `next/cache`) уже есть в файле из Plan 14-04 — если нет, добавить.

    ШАГ 2 — Создать `tests/stock-actions.test.ts` с моками Prisma + requireSection:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    // Mock rbac + prisma
    vi.mock("@/lib/rbac", () => ({
      requireSection: vi.fn().mockResolvedValue({ user: { id: "u1" } }),
    }))

    const prismaMock = {
      product: { update: vi.fn() },
      appSetting: { upsert: vi.fn() },
      $transaction: vi.fn().mockImplementation((cb: any) => cb(prismaMock)),
    }

    vi.mock("@/lib/prisma", () => ({
      prisma: prismaMock,
    }))

    vi.mock("next/cache", () => ({
      revalidatePath: vi.fn(),
    }))

    import { updateProductionStock, updateTurnoverNorm } from "@/app/actions/stock"

    beforeEach(() => {
      vi.resetAllMocks()
      prismaMock.product.update.mockResolvedValue({ id: "p1" })
      prismaMock.appSetting.upsert.mockResolvedValue({ key: "x", value: "y" })
    })

    describe("updateProductionStock", () => {
      it("valid 500 → update success", async () => {
        const result = await updateProductionStock("p1", 500)
        expect(result).toEqual({ ok: true })
        expect(prismaMock.product.update).toHaveBeenCalled()
      })

      it("valid null → update success", async () => {
        const result = await updateProductionStock("p1", null)
        expect(result).toEqual({ ok: true })
      })

      it("invalid -5 → ok:false error", async () => {
        const result = await updateProductionStock("p1", -5)
        expect(result.ok).toBe(false)
      })

      it("invalid 100000 → ok:false error", async () => {
        const result = await updateProductionStock("p1", 100000)
        expect(result.ok).toBe(false)
      })
    })

    describe("updateTurnoverNorm", () => {
      it("valid 37 → upsert success", async () => {
        const result = await updateTurnoverNorm(37)
        expect(result).toEqual({ ok: true })
        expect(prismaMock.appSetting.upsert).toHaveBeenCalledWith(expect.objectContaining({
          where: { key: "stock.turnoverNormDays" },
          create: { key: "stock.turnoverNormDays", value: "37" },
          update: { value: "37" },
        }))
      })

      it("invalid 0 → ok:false", async () => {
        const result = await updateTurnoverNorm(0)
        expect(result.ok).toBe(false)
      })

      it("invalid 101 → ok:false", async () => {
        const result = await updateTurnoverNorm(101)
        expect(result.ok).toBe(false)
      })

      it("valid boundary 1 → ok:true", async () => {
        expect((await updateTurnoverNorm(1)).ok).toBe(true)
      })

      it("valid boundary 100 → ok:true", async () => {
        expect((await updateTurnoverNorm(100)).ok).toBe(true)
      })
    })
    ```

    ШАГ 3 — `npm run test -- tests/stock-actions.test.ts` → все GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/stock-actions.test.ts && grep -q "updateProductionStock" app/actions/stock.ts && grep -q "updateTurnoverNorm" app/actions/stock.ts && grep -q "stock.turnoverNormDays" app/actions/stock.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/actions/stock.ts` экспортирует `upsertIvanovoStock` (Plan 14-04), `updateProductionStock`, `updateTurnoverNorm`
    - Обе новые функции используют `requireSection("STOCK", "MANAGE")`
    - Zod схемы: ProductionStock (int 0..99999 nullable), TurnoverNorm (int 1..100)
    - `updateTurnoverNorm` использует key `"stock.turnoverNormDays"`
    - После успеха: `revalidatePath("/stock")` в обоих (плюс `revalidatePath("/stock/wb")` в updateTurnoverNorm)
    - `tests/stock-actions.test.ts` — минимум 9 GREEN тестов
  </acceptance_criteria>
  <done>
    Два server actions добавлены с Zod validation, RBAC, revalidatePath. Тесты GREEN. Готово для client components в Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: TurnoverNormInput + WbRefreshButton client компоненты</name>
  <files>
    components/stock/TurnoverNormInput.tsx,
    components/stock/WbRefreshButton.tsx
  </files>
  <read_first>
    - components/prices/GlobalRatesBar.tsx (паттерн debounced save через useRef таймер per field)
    - components/cards/WbSyncButton.tsx (паттерн toast.loading/dismiss + fetch /api/wb-sync)
    - components/ui/input.tsx (shadcn Input props)
    - components/ui/button.tsx (shadcn Button variants)
    - components/ui/card.tsx (shadcn Card)
    - .planning/phases/14-stock/14-UI-SPEC.md §2 TurnoverNormInput, §3 кнопки шапки, §Copywriting Contract
    - .planning/REQUIREMENTS.md STOCK-14, STOCK-15
  </read_first>
  <action>
    ШАГ 1 — Создать `components/stock/TurnoverNormInput.tsx`:

    ```typescript
    // components/stock/TurnoverNormInput.tsx
    // Phase 14 (STOCK-14): Inline редактор "Нормы оборачиваемости" в шапке /stock.
    // Паттерн: components/prices/GlobalRatesBar.tsx — debounced save 500ms через useRef таймер.
    // Одно поле, хранится в AppSetting ("stock.turnoverNormDays"), валидация Zod int(1..100).

    "use client"

    import { useRef, useState, useTransition } from "react"
    import { toast } from "sonner"
    import { Card } from "@/components/ui/card"
    import { Input } from "@/components/ui/input"
    import { Label } from "@/components/ui/label"
    import { updateTurnoverNorm } from "@/app/actions/stock"

    interface TurnoverNormInputProps {
      initialDays: number  // из AppSetting через RSC (page.tsx Plan 14-06)
    }

    export function TurnoverNormInput({ initialDays }: TurnoverNormInputProps) {
      const [value, setValue] = useState<number>(initialDays)
      const [isPending, startTransition] = useTransition()
      const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

      const debouncedSave = (newValue: number) => {
        if (timerRef.current) clearTimeout(timerRef.current)
        timerRef.current = setTimeout(() => {
          startTransition(async () => {
            const result = await updateTurnoverNorm(newValue)
            if (result.ok) {
              toast.success("Норма сохранена")
            } else {
              toast.error(`Не удалось сохранить норму: ${result.error}. Допустимо от 1 до 100 дней.`)
            }
          })
        }, 500)
      }

      const handleChange = (raw: string) => {
        const parsed = parseInt(raw, 10)
        if (isNaN(parsed)) return
        setValue(parsed)
        debouncedSave(parsed)
      }

      return (
        <Card className="inline-flex items-center gap-2 p-4 bg-muted/30 border">
          <Label
            htmlFor="turnover-norm"
            className="text-xs text-muted-foreground font-normal whitespace-nowrap"
          >
            Норма оборачиваемости
          </Label>
          <Input
            id="turnover-norm"
            type="number"
            min={1}
            max={100}
            step={1}
            className="h-8 w-16 text-sm tabular-nums"
            value={value}
            onChange={(e) => handleChange(e.target.value)}
            disabled={isPending}
          />
          <span className="text-sm text-muted-foreground">дней</span>
        </Card>
      )
    }
    ```

    ШАГ 2 — Создать `components/stock/WbRefreshButton.tsx`:

    ```typescript
    // components/stock/WbRefreshButton.tsx
    // Phase 14 (STOCK-15): Primary CTA "Обновить из WB" в шапке /stock.
    // Вызывает POST /api/wb-sync (расширен в Plan 14-03 per-warehouse).
    // Длительность ~1-2 мин → обязателен toast.loading с dismiss.

    "use client"

    import { useState, useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { toast } from "sonner"
    import { RefreshCw } from "lucide-react"
    import { Button } from "@/components/ui/button"

    export function WbRefreshButton() {
      const [isLoading, setIsLoading] = useState(false)
      const router = useRouter()

      const handleClick = async () => {
        if (isLoading) return
        setIsLoading(true)
        const toastId = toast.loading("Загружаем остатки из WB…")
        try {
          const res = await fetch("/api/wb-sync", { method: "POST" })
          toast.dismiss(toastId)
          if (!res.ok) {
            const { error } = await res.json().catch(() => ({ error: `HTTP ${res.status}` }))
            toast.error(`Не удалось обновить остатки из WB: ${error}. Повторите через минуту.`)
            return
          }
          toast.success("WB остатки обновлены")
          router.refresh()
        } catch (e) {
          toast.dismiss(toastId)
          toast.error(`Не удалось обновить остатки из WB: ${(e as Error).message}. Повторите через минуту.`)
        } finally {
          setIsLoading(false)
        }
      }

      return (
        <Button onClick={handleClick} disabled={isLoading}>
          <RefreshCw className={`mr-2 h-4 w-4 ${isLoading ? "animate-spin" : ""}`} />
          {isLoading ? "Обновление…" : "Обновить из WB"}
        </Button>
      )
    }
    ```

    ШАГ 3 — Проверить что оба компонента компилируются: `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "components/stock/TurnoverNormInput.tsx" && test -f "components/stock/WbRefreshButton.tsx" && grep -q "updateTurnoverNorm" components/stock/TurnoverNormInput.tsx && grep -q "/api/wb-sync" components/stock/WbRefreshButton.tsx && grep -q "toast.loading" components/stock/WbRefreshButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/stock/TurnoverNormInput.tsx` — "use client", принимает `initialDays: number`, использует useRef таймер для debounce 500ms, вызывает `updateTurnoverNorm`
    - Layout: Card с bg-muted/30 + Label "Норма оборачиваемости" + Input h-8 w-16 + span "дней"
    - Toast success: "Норма сохранена", error: "Не удалось сохранить норму: {error}. Допустимо от 1 до 100 дней."
    - `components/stock/WbRefreshButton.tsx` — "use client", variant default (primary), иконка RefreshCw
    - При клике: toast.loading("Загружаем остатки из WB…") → fetch /api/wb-sync → toast.dismiss + toast.success/error → router.refresh()
    - Во время загрузки: disabled + animate-spin + label "Обновление…"
    - `npx tsc --noEmit` → 0 ошибок
  </acceptance_criteria>
  <done>
    TurnoverNormInput + WbRefreshButton созданы. Debounced save паттерн из GlobalRatesBar. WB sync паттерн из WbSyncButton с toast.loading. Оба готовы к интеграции в /stock/page.tsx (Plan 14-06).
  </done>
</task>

</tasks>

<verification>
- `npm run test -- tests/stock-actions.test.ts` → GREEN (9+ тестов)
- `npx tsc --noEmit` → 0 ошибок
- Grep: `updateProductionStock|updateTurnoverNorm` в app/actions/stock.ts
- Grep: `"use client"` в обоих новых компонентах
- Grep: `toast.loading|toast.dismiss` в WbRefreshButton
</verification>

<success_criteria>
- [x] `app/actions/stock.ts` расширен 2 новыми actions (updateProductionStock + updateTurnoverNorm)
- [x] Оба action требуют requireSection STOCK MANAGE
- [x] Zod валидация: Production(0..99999, nullable), Turnover(1..100)
- [x] revalidatePath("/stock") в обоих; updateTurnoverNorm также revalidatePath("/stock/wb")
- [x] TurnoverNormInput — debounced 500ms через useRef, паттерн GlobalRatesBar
- [x] WbRefreshButton — primary CTA с toast.loading + animate-spin + router.refresh
- [x] Все строки на русском
- [x] tests/stock-actions.test.ts GREEN минимум 9
- [x] npx tsc --noEmit → 0 ошибок
</success_criteria>

<output>
Create `.planning/phases/14-stock/14-05-SUMMARY.md`:
- Server actions созданы: 2 новых (+ существующий upsertIvanovoStock)
- Client компонентов: 2 (TurnoverNormInput, WbRefreshButton)
- Debounce 500ms паттерн — из GlobalRatesBar
- Toast.loading паттерн — из WbSyncButton
- Тесты GREEN count
- Готовность к интеграции в Plan 14-06 (RSC page /stock с шапкой)
</output>
