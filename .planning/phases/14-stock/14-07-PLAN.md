---
phase: 14-stock
plan: 07
type: execute
wave: 5
depends_on:
  - "14-01"
  - "14-02"
  - "14-03"
  - "14-04"
  - "14-05"
  - "14-06"
files_modified:
  - components/stock/StockTabs.tsx
  - app/(dashboard)/stock/layout.tsx
  - app/(dashboard)/stock/wb/page.tsx
  - app/(dashboard)/stock/ozon/page.tsx
  - components/stock/StockWbTable.tsx
  - components/stock/ClusterTooltip.tsx
  - lib/stock-wb-data.ts
  - deploy.sh
  - DEPLOY.md
requirements:
  - STOCK-21
  - STOCK-22
  - STOCK-24
  - STOCK-25
  - STOCK-29
autonomous: false

must_haves:
  truths:
    - "StockTabs — client компонент, 3 таба (Остатки/WB склады/Ozon), паттерн PricesTabs"
    - "/stock/ozon — ComingSoon с sectionName='Управление остатками Ozon'"
    - "/stock/wb — RSC с таблицей nmId-level: sticky 4 + 4 сводных колонки (РФ/Иваново/Производство/МП) + 7 кластерных колонок (ЦФО/ЮГ/Урал/ПФО/СЗО/СФО/Прочие)"
    - "Каждая кластерная колонка: свёрнутая = 4 sub-column (О/З/Об/Д), развёрнутая = per-warehouse columns (по 1 О per склад)"
    - "ClusterTooltip показывает full name из CLUSTER_FULL_NAMES при hover на коротком названии"
    - "URL state: ?expandedClusters=ЦФО,ПФО — comma-separated, shareable; toggle через router.replace({scroll:false})"
    - "Toolbar: «Развернуть все» / «Свернуть все» ghost кнопки"
    - "Неизвестный склад (needsClusterReview=true) имеет ⚠️ prefix в заголовке per-warehouse колонки + text-yellow-600"
    - "deploy.sh запускает prisma migrate deploy (применяет Phase 14 миграцию) + опциональный seed-wb-warehouses"
    - "DEPLOY.md документирует Phase 14 шаги deploy"
    - "Human UAT: 9 пунктов из STOCK-29 верифицированы пользователем на проде zoiten.pro/stock"
  artifacts:
    - path: "lib/stock-wb-data.ts"
      provides: "getStockWbData() RSC helper — per-nmId данные с per-warehouse split по кластерам"
      exports: ["getStockWbData", "WbStockRow", "ClusterAggregate", "WarehouseSlot"]
    - path: "components/stock/StockTabs.tsx"
      provides: "3 tabs — Остатки / WB склады / Ozon"
      min_lines: 30
    - path: "components/stock/StockWbTable.tsx"
      provides: "Client sticky-таблица с 7 кластерными колонками + expand URL state + per-warehouse drill-down"
      min_lines: 200
    - path: "components/stock/ClusterTooltip.tsx"
      provides: "Wrapper над shadcn Tooltip с CLUSTER_FULL_NAMES + count складов"
      min_lines: 20
    - path: "app/(dashboard)/stock/wb/page.tsx"
      provides: "RSC /stock/wb"
    - path: "app/(dashboard)/stock/ozon/page.tsx"
      provides: "ComingSoon stub"
    - path: "app/(dashboard)/stock/layout.tsx"
      provides: "Обновлён: содержит <StockTabs /> поверх children"
  key_links:
    - from: "app/(dashboard)/stock/layout.tsx"
      to: "StockTabs"
      via: "render поверх children"
      pattern: "<StockTabs"
    - from: "components/stock/StockWbTable.tsx"
      to: "URL searchParams (expandedClusters)"
      via: "useSearchParams + router.replace"
      pattern: "expandedClusters"
    - from: "components/stock/ClusterTooltip.tsx"
      to: "lib/wb-clusters.ts CLUSTER_FULL_NAMES"
      via: "lookup by shortCluster"
      pattern: "CLUSTER_FULL_NAMES"
    - from: "deploy.sh"
      to: "/stock deployment"
      via: "prisma migrate deploy + optional seed"
      pattern: "prisma migrate deploy"
---

<objective>
Plan 14-07 — Финальный план: /stock/wb кластеры + /stock/ozon ComingSoon + StockTabs + deploy + human UAT (Wave 5, финальный).

**Purpose:** Завершает Phase 14:
1. `StockTabs` компонент (3 таба) + обновление `/stock/layout.tsx`
2. `/stock/ozon/page.tsx` — `<ComingSoon sectionName="Управление остатками Ozon" />`
3. `/stock/wb/page.tsx` — RSC nmId-level таблица с 7 кластерными колонками + expand до per-warehouse columns через URL state
4. `ClusterTooltip` для tooltip full name кластера
5. Deploy на VPS через `deploy.sh` (включая prisma migrate + опциональный seed-wb-warehouses если не выполнен локально)
6. Human UAT: 9 пунктов из STOCK-29 (i.e. nginx rewrite /inventory → /stock, /stock открывается, Excel Иваново работает, Производство inline, Норма inline, Обновить из WB, /stock/wb кластеры, expand, tooltip)

**Output:**
- 4 новых компонента: `StockTabs`, `StockWbTable`, `ClusterTooltip`, `stock-wb-data.ts`
- 3 новых/обновлённых RSC pages: layout.tsx (+tabs), wb/page.tsx, ozon/page.tsx
- `deploy.sh` + `DEPLOY.md` обновлены с Phase 14 шагами
- Phase 14 deployed на https://zoiten.pro/stock
- Human UAT выполнен — все 9 пунктов STOCK-29 GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stock/14-UI-SPEC.md
@.planning/phases/14-stock/14-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-stock/14-01-SUMMARY.md
@.planning/phases/14-stock/14-06-SUMMARY.md
@components/prices/PricesTabs.tsx
@components/stock/StockProductTable.tsx
@components/ui/ComingSoon.tsx
@components/ui/tooltip.tsx
@lib/wb-clusters.ts
@lib/stock-math.ts
@prisma/schema.prisma
@deploy.sh

<interfaces>
From lib/wb-clusters.ts (Plan 14-01):
```typescript
export const CLUSTER_FULL_NAMES: Record<string, string>
export const CLUSTER_ORDER = ["ЦФО", "ЮГ", "Урал", "ПФО", "СЗО", "СФО", "Прочие"] as const
export type ClusterShortName = typeof CLUSTER_ORDER[number]
```

From prisma/schema.prisma:
```prisma
model WbWarehouse {
  id                 Int      @id
  name               String
  cluster            String
  shortCluster       String
  isActive           Boolean
  needsClusterReview Boolean
  stocks             WbCardWarehouseStock[]
}

model WbCardWarehouseStock {
  wbCardId    String
  warehouseId Int
  quantity    Int
  warehouse   WbWarehouse
}
```

From components/prices/PricesTabs.tsx (паттерн):
```typescript
"use client"
const pathname = usePathname()
const isActive = (path: string) => pathname === path || pathname.startsWith(path + "/")
// <Link href={...} className={cn("border-b-2", isActive && "border-primary text-primary")}>...
```

From components/ui/ComingSoon.tsx (существует):
```typescript
export function ComingSoon({ sectionName }: { sectionName: string })
```

From components/ui/tooltip.tsx:
```typescript
export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StockTabs + ComingSoon ozon + layout update + ClusterTooltip</name>
  <files>
    components/stock/StockTabs.tsx,
    components/stock/ClusterTooltip.tsx,
    app/(dashboard)/stock/layout.tsx,
    app/(dashboard)/stock/ozon/page.tsx
  </files>
  <read_first>
    - components/prices/PricesTabs.tsx (ТОЧНЫЙ паттерн для копирования)
    - components/ui/ComingSoon.tsx (props API)
    - components/ui/tooltip.tsx (структура shadcn tooltip)
    - lib/wb-clusters.ts (CLUSTER_FULL_NAMES)
    - app/(dashboard)/stock/layout.tsx (Plan 14-01 — добавить StockTabs)
    - .planning/phases/14-stock/14-UI-SPEC.md §8 StockTabs, §7 ClusterTooltip, §Copywriting
    - .planning/REQUIREMENTS.md STOCK-21, STOCK-24
  </read_first>
  <action>
    ШАГ 1 — Создать `components/stock/StockTabs.tsx` (паттерн PricesTabs):

    ```typescript
    // components/stock/StockTabs.tsx
    // Phase 14 (STOCK-21): 3 таба — Остатки / WB склады / Ozon.
    // Паттерн: components/prices/PricesTabs.tsx.

    "use client"

    import Link from "next/link"
    import { usePathname } from "next/navigation"
    import { cn } from "@/lib/utils"

    const TABS = [
      { href: "/stock", label: "Остатки", exact: true },
      { href: "/stock/wb", label: "WB склады", exact: false },
      { href: "/stock/ozon", label: "Ozon", exact: false },
    ] as const

    export function StockTabs() {
      const pathname = usePathname()

      const isActive = (href: string, exact: boolean) => {
        if (exact) return pathname === href
        return pathname.startsWith(href)
      }

      return (
        <div className="flex gap-6 border-b">
          {TABS.map((tab) => {
            const active = isActive(tab.href, tab.exact)
            return (
              <Link
                key={tab.href}
                href={tab.href}
                className={cn(
                  "pb-2 border-b-2 text-sm transition-colors",
                  active
                    ? "border-primary text-primary font-medium"
                    : "border-transparent text-muted-foreground hover:text-foreground"
                )}
              >
                {tab.label}
              </Link>
            )
          })}
        </div>
      )
    }
    ```

    **Важно:** В PricesTabs проверить паттерн isActive — для "Остатки" на URL `/stock/xxx` нужно исключить т.к. `/stock` prefix match = true для `/stock/wb`. Выше применили `exact: true` для `/stock`, остальные — startsWith. Если паттерн проекта иной — адаптировать.

    ШАГ 2 — Обновить `app/(dashboard)/stock/layout.tsx` — добавить StockTabs:

    ```typescript
    // app/(dashboard)/stock/layout.tsx
    // Phase 14: RSC layout с RBAC + StockTabs.

    import { requireSection } from "@/lib/rbac"
    import { StockTabs } from "@/components/stock/StockTabs"

    export default async function StockLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      await requireSection("STOCK")

      return (
        <div className="p-6 space-y-6">
          <StockTabs />
          {children}
        </div>
      )
    }
    ```

    ШАГ 3 — Создать `app/(dashboard)/stock/ozon/page.tsx`:

    ```typescript
    // app/(dashboard)/stock/ozon/page.tsx
    // Phase 14 (STOCK-21): Placeholder /stock/ozon — Ozon Stocks API milestone v1.3+ (STOCK-FUT-06).

    import { requireSection } from "@/lib/rbac"
    import { ComingSoon } from "@/components/ui/ComingSoon"

    export const metadata = {
      title: "Управление остатками Ozon",
    }

    export default async function StockOzonPage() {
      await requireSection("STOCK")
      return <ComingSoon sectionName="Управление остатками Ozon" />
    }
    ```

    **Важно:** Проверить точное имя ComingSoon компонента (components/ui/ComingSoon.tsx или иначе). Если компонент в другом месте или имеет другое API — адаптировать.

    ШАГ 4 — Создать `components/stock/ClusterTooltip.tsx`:

    ```typescript
    // components/stock/ClusterTooltip.tsx
    // Phase 14 (STOCK-24): Tooltip с полным названием кластера + кол-во складов.

    "use client"

    import {
      Tooltip,
      TooltipContent,
      TooltipTrigger,
    } from "@/components/ui/tooltip"
    import { CLUSTER_FULL_NAMES } from "@/lib/wb-clusters"

    interface ClusterTooltipProps {
      shortName: string       // "ЦФО", "ЮГ", ...
      warehouseCount?: number // опционально: количество складов в кластере
      children: React.ReactNode
    }

    export function ClusterTooltip({ shortName, warehouseCount, children }: ClusterTooltipProps) {
      const fullName = CLUSTER_FULL_NAMES[shortName] ?? shortName
      return (
        <Tooltip delayDuration={200}>
          <TooltipTrigger asChild>
            {children}
          </TooltipTrigger>
          <TooltipContent>
            <div className="text-xs">
              <div className="font-medium">{fullName}</div>
              {warehouseCount !== undefined && (
                <div className="text-muted-foreground">Складов: {warehouseCount}</div>
              )}
            </div>
          </TooltipContent>
        </Tooltip>
      )
    }
    ```

    **Важно:** shadcn base-ui не использует `asChild` — вместо него render-prop. Проверить components/ui/tooltip.tsx и адаптировать TooltipTrigger. Если паттерн — `<TooltipTrigger render={(props) => <button {...props}>...</button>}>`, использовать его.

    ШАГ 5 — `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "components/stock/StockTabs.tsx" && test -f "components/stock/ClusterTooltip.tsx" && test -f "app/(dashboard)/stock/ozon/page.tsx" && grep -q "StockTabs" app/\(dashboard\)/stock/layout.tsx && grep -q "ComingSoon" app/\(dashboard\)/stock/ozon/page.tsx && grep -q "CLUSTER_FULL_NAMES" components/stock/ClusterTooltip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/stock/StockTabs.tsx` — "use client", 3 tabs: Остатки (/stock), WB склады (/stock/wb), Ozon (/stock/ozon)
    - Active state: `border-primary text-primary font-medium`
    - Inactive: `border-transparent text-muted-foreground hover:text-foreground`
    - isActive для /stock использует `exact: true` (иначе матчит /stock/wb и /stock/ozon)
    - `app/(dashboard)/stock/layout.tsx` содержит `<StockTabs />` перед `{children}`
    - `app/(dashboard)/stock/ozon/page.tsx` — async RSC с requireSection + `<ComingSoon sectionName="Управление остатками Ozon" />`
    - `components/stock/ClusterTooltip.tsx` — принимает shortName + children, использует CLUSTER_FULL_NAMES lookup
    - `npx tsc --noEmit` → 0 ошибок
  </acceptance_criteria>
  <done>
    3 таба + Ozon placeholder + tooltip компонент готовы. Layout обновлён.
  </done>
</task>

<task type="auto">
  <name>Task 2: /stock/wb RSC + StockWbTable клиентский с кластерами + expand URL state</name>
  <files>
    lib/stock-wb-data.ts,
    app/(dashboard)/stock/wb/page.tsx,
    components/stock/StockWbTable.tsx
  </files>
  <read_first>
    - lib/stock-data.ts (Plan 14-06 — похожие types и патерн getStockData)
    - components/stock/StockProductTable.tsx (Plan 14-06 — паттерн sticky + rowSpan + DeficitCell + StockCell)
    - components/stock/ClusterTooltip.tsx (Task 1)
    - lib/wb-clusters.ts (CLUSTER_ORDER, CLUSTER_FULL_NAMES)
    - lib/stock-math.ts (calculateStockMetrics + deficitThreshold)
    - prisma/schema.prisma (WbWarehouse + WbCardWarehouseStock relations)
    - .planning/phases/14-stock/14-UI-SPEC.md §6 Layout /stock/wb, §7 StockWbTable (cluster/expand/per-warehouse), §Copywriting (empty state)
    - .planning/phases/14-stock/14-RESEARCH.md §Pattern 8: URL state
    - .planning/REQUIREMENTS.md STOCK-22, STOCK-25
  </read_first>
  <action>
    ШАГ 1 — Создать `lib/stock-wb-data.ts`:

    ```typescript
    // lib/stock-wb-data.ts
    // Phase 14 (STOCK-22): RSC data helper для /stock/wb — per-nmId данные с per-warehouse split.
    // Группировка: Product → WbCard → кластер → склад

    import { prisma } from "@/lib/prisma"
    import { CLUSTER_ORDER, type ClusterShortName } from "@/lib/wb-clusters"

    export interface WarehouseSlot {
      warehouseId: number
      warehouseName: string
      needsClusterReview: boolean
      quantity: number
    }

    export interface ClusterAggregate {
      shortCluster: string  // "ЦФО" | ...
      totalStock: number | null
      warehouses: WarehouseSlot[]
    }

    export interface WbStockRow {
      // Per-nmId
      wbCardId: string
      nmId: number
      wbCardName: string | null
      avgSalesSpeed7d: number | null
      totalStock: number | null  // SUM всех складов
      clusters: Record<ClusterShortName, ClusterAggregate>
    }

    export interface ProductWbGroup {
      productId: string
      productSku: string
      productName: string
      brandName: string
      photoUrl: string | null
      ivanovoStock: number | null
      productionStock: number | null
      wbCards: WbStockRow[]
    }

    export interface StockWbDataResult {
      groups: ProductWbGroup[]
      turnoverNormDays: number
      // Набор реальных складов per-кластер (для expanded columns headers)
      clusterWarehouses: Record<ClusterShortName, Array<{ warehouseId: number; warehouseName: string; needsClusterReview: boolean }>>
    }

    const TURNOVER_NORM_KEY = "stock.turnoverNormDays"

    export async function getStockWbData(): Promise<StockWbDataResult> {
      const setting = await prisma.appSetting.findUnique({ where: { key: TURNOVER_NORM_KEY } })
      const turnoverNormDays = setting ? parseInt(setting.value, 10) : 37

      // Все Product с WB-артикулами → WbCard
      const products = await prisma.product.findMany({
        where: {
          deletedAt: null,
          articles: { some: { marketplace: { name: { in: ["WB", "wb"] } } } },
        },
        include: {
          brand: true,
          articles: {
            include: { marketplace: true },
          },
        },
        orderBy: { sku: "asc" },
      })

      // Все WbCard + warehouses
      const wbNmIds = products.flatMap((p) =>
        p.articles
          .filter((a) => a.marketplace.name.toLowerCase() === "wb")
          .map((a) => parseInt(a.article, 10))
          .filter((n) => !isNaN(n))
      )

      const wbCards = wbNmIds.length > 0
        ? await prisma.wbCard.findMany({
            where: { nmId: { in: wbNmIds } },
            include: {
              warehouses: {
                include: { warehouse: true },
              },
            },
          })
        : []
      const wbCardByNmId = new Map(wbCards.map((c) => [c.nmId, c]))

      // Построить clusterWarehouses — уникальные склады per-кластер (по всем WbCards)
      const clusterWarehousesMap: Record<string, Map<number, { warehouseId: number; warehouseName: string; needsClusterReview: boolean }>> = {}
      for (const cluster of CLUSTER_ORDER) clusterWarehousesMap[cluster] = new Map()

      for (const card of wbCards) {
        for (const ws of card.warehouses) {
          const shortCluster = (ws.warehouse?.shortCluster as ClusterShortName | undefined) ?? "Прочие"
          const map = clusterWarehousesMap[shortCluster] ?? clusterWarehousesMap["Прочие"]!
          if (!map.has(ws.warehouseId)) {
            map.set(ws.warehouseId, {
              warehouseId: ws.warehouseId,
              warehouseName: ws.warehouse?.name ?? `Склад ${ws.warehouseId}`,
              needsClusterReview: ws.warehouse?.needsClusterReview ?? false,
            })
          }
        }
      }
      const clusterWarehouses = Object.fromEntries(
        CLUSTER_ORDER.map((c) => [
          c,
          [...(clusterWarehousesMap[c]?.values() ?? [])].sort((a, b) => a.warehouseName.localeCompare(b.warehouseName, "ru")),
        ])
      ) as StockWbDataResult["clusterWarehouses"]

      // Построить rows
      const groups: ProductWbGroup[] = products.map((p) => {
        const wbArticles = p.articles.filter((a) => a.marketplace.name.toLowerCase() === "wb")
        const wbRows: WbStockRow[] = wbArticles.map((a) => {
          const nmId = parseInt(a.article, 10)
          const card = !isNaN(nmId) ? wbCardByNmId.get(nmId) : null

          // Инициализировать кластеры
          const clusters = {} as Record<ClusterShortName, ClusterAggregate>
          for (const shortCluster of CLUSTER_ORDER) {
            clusters[shortCluster] = { shortCluster, totalStock: null, warehouses: [] }
          }

          let totalStock: number | null = null
          if (card) {
            totalStock = 0
            for (const ws of card.warehouses) {
              const shortCluster = ((ws.warehouse?.shortCluster as ClusterShortName | undefined) ?? "Прочие")
              const aggregate = clusters[shortCluster] ?? clusters["Прочие"]!
              aggregate.warehouses.push({
                warehouseId: ws.warehouseId,
                warehouseName: ws.warehouse?.name ?? `Склад ${ws.warehouseId}`,
                needsClusterReview: ws.warehouse?.needsClusterReview ?? false,
                quantity: ws.quantity,
              })
              aggregate.totalStock = (aggregate.totalStock ?? 0) + ws.quantity
              totalStock += ws.quantity
            }
          }

          return {
            wbCardId: card?.id ?? `missing-${a.article}`,
            nmId: isNaN(nmId) ? 0 : nmId,
            wbCardName: card?.name ?? null,
            avgSalesSpeed7d: card?.avgSalesSpeed7d ?? null,
            totalStock,
            clusters,
          }
        })

        return {
          productId: p.id,
          productSku: p.sku,
          productName: p.name,
          brandName: p.brand.name,
          photoUrl: (p as any).photoUrl ?? (p as any).photo ?? null,
          ivanovoStock: p.ivanovoStock,
          productionStock: p.productionStock,
          wbCards: wbRows,
        }
      })

      return { groups, turnoverNormDays, clusterWarehouses }
    }
    ```

    ШАГ 2 — Создать `app/(dashboard)/stock/wb/page.tsx`:

    ```typescript
    // app/(dashboard)/stock/wb/page.tsx
    // Phase 14 (STOCK-22, STOCK-25): RSC /stock/wb — nmId-level с кластерами.

    import { requireSection } from "@/lib/rbac"
    import { getStockWbData } from "@/lib/stock-wb-data"
    import { StockWbTable } from "@/components/stock/StockWbTable"

    export const metadata = {
      title: "Управление остатками — WB",
    }

    export default async function StockWbPage() {
      await requireSection("STOCK")
      const data = await getStockWbData()

      if (data.groups.length === 0) {
        return (
          <div className="text-center py-16">
            <h3 className="text-sm font-medium">Остатки WB не загружены</h3>
            <p className="text-xs text-muted-foreground mt-1">
              Нажмите «Обновить из WB» на странице <a href="/stock" className="underline">Остатки</a>, чтобы загрузить актуальные данные по складам.
            </p>
          </div>
        )
      }

      return (
        <StockWbTable
          groups={data.groups}
          turnoverNormDays={data.turnoverNormDays}
          clusterWarehouses={data.clusterWarehouses}
        />
      )
    }
    ```

    ШАГ 3 — Создать `components/stock/StockWbTable.tsx` — ключевой компонент с 7 кластерами + expand:

    ```typescript
    // components/stock/StockWbTable.tsx
    // Phase 14 (STOCK-22, STOCK-25): Client sticky-таблица /stock/wb с 7 кластерными колонками + expand.
    // URL state: ?expandedClusters=ЦФО,ПФО — shareable.

    "use client"

    import React, { useCallback } from "react"
    import Image from "next/image"
    import { useRouter, useSearchParams } from "next/navigation"
    import { ChevronLeft, ChevronRight, AlertTriangle } from "lucide-react"
    import { cn } from "@/lib/utils"
    import {
      Table,
      TableBody,
      TableCell,
      TableHead,
      TableHeader,
      TableRow,
    } from "@/components/ui/table"
    import { Button } from "@/components/ui/button"
    import { calculateStockMetrics, deficitThreshold } from "@/lib/stock-math"
    import { CLUSTER_ORDER, CLUSTER_FULL_NAMES, type ClusterShortName } from "@/lib/wb-clusters"
    import { ClusterTooltip } from "./ClusterTooltip"
    import type { ProductWbGroup, StockWbDataResult } from "@/lib/stock-wb-data"

    function formatStockValue(n: number): string {
      if (n < 10) return n.toFixed(1)
      return Math.floor(n).toString()
    }

    interface Props {
      groups: ProductWbGroup[]
      turnoverNormDays: number
      clusterWarehouses: StockWbDataResult["clusterWarehouses"]
    }

    export function StockWbTable({ groups, turnoverNormDays, clusterWarehouses }: Props) {
      const router = useRouter()
      const searchParams = useSearchParams()

      const expandedSet = new Set<string>(
        (searchParams.get("expandedClusters") ?? "").split(",").filter(Boolean)
      )

      const toggleCluster = useCallback((cluster: string) => {
        const next = new Set(expandedSet)
        if (next.has(cluster)) next.delete(cluster)
        else next.add(cluster)

        const params = new URLSearchParams(searchParams.toString())
        if (next.size > 0) params.set("expandedClusters", [...next].join(","))
        else params.delete("expandedClusters")
        router.replace(`?${params.toString()}`, { scroll: false })
      }, [expandedSet, searchParams, router])

      const expandAll = useCallback(() => {
        const params = new URLSearchParams(searchParams.toString())
        params.set("expandedClusters", CLUSTER_ORDER.join(","))
        router.replace(`?${params.toString()}`, { scroll: false })
      }, [searchParams, router])

      const collapseAll = useCallback(() => {
        const params = new URLSearchParams(searchParams.toString())
        params.delete("expandedClusters")
        router.replace(`?${params.toString()}`, { scroll: false })
      }, [searchParams, router])

      // Вспомогательные cells
      const StockCell = ({ value }: { value: number | null }) => (
        <TableCell className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
          {value !== null ? formatStockValue(value) : <span className="text-muted-foreground">—</span>}
        </TableCell>
      )

      const DeficitCell = ({ deficit, threshold }: { deficit: number | null; threshold: number | null }) => (
        <TableCell
          className={cn(
            "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right border-r",
            deficit === null && "text-muted-foreground",
            deficit !== null && deficit <= 0 && "text-green-600 dark:text-green-500",
            deficit !== null && threshold !== null && deficit > 0 && deficit < threshold && "text-yellow-600 dark:text-yellow-400",
            deficit !== null && threshold !== null && deficit >= threshold && "text-red-600 dark:text-red-500 font-medium",
          )}
        >
          {deficit !== null ? formatStockValue(deficit) : "—"}
        </TableCell>
      )

      return (
        <div className="space-y-4">
          <div className="flex gap-2">
            <Button variant="ghost" size="sm" onClick={expandAll}>Развернуть все</Button>
            <Button variant="ghost" size="sm" onClick={collapseAll}>Свернуть все</Button>
          </div>

          <div className="overflow-x-auto border rounded">
            <Table>
              <TableHeader>
                {/* Уровень 1 — группы */}
                <TableRow>
                  <TableHead className="sticky top-0 z-30 bg-background w-20 text-xs font-medium text-center border-b" rowSpan={2}>Фото</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background w-60 text-xs font-medium text-center border-b" rowSpan={2}>Сводка</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background w-20 text-xs font-medium text-center border-b" rowSpan={2}>nmId</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background w-[120px] text-xs font-medium text-center border-b" rowSpan={2}>Артикул</TableHead>
                  {/* 4 сводных — РФ/Иваново/Произв/МП (компактно per nmId) */}
                  <TableHead className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b" rowSpan={2} title="Остаток (шт) МП = WB+Ozon">МП О</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b" rowSpan={2} title="WB Заказы/день">З</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b" rowSpan={2} title="Оборачиваемость">Об</TableHead>
                  <TableHead className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b border-r" rowSpan={2} title="Дефицит">Д</TableHead>
                  {/* 7 кластерных колонок */}
                  {CLUSTER_ORDER.map((cluster) => {
                    const isExpanded = expandedSet.has(cluster)
                    const warehouses = clusterWarehouses[cluster] ?? []
                    const colSpan = isExpanded ? Math.max(warehouses.length, 1) : 4  // expanded: per-warehouse; collapsed: О/З/Об/Д
                    return (
                      <TableHead
                        key={cluster}
                        colSpan={colSpan}
                        className="sticky top-0 z-30 bg-background text-xs font-medium text-center border-b border-r px-2 py-1"
                      >
                        <div className="flex items-center justify-center gap-1">
                          <ClusterTooltip shortName={cluster} warehouseCount={warehouses.length}>
                            <span className="cursor-help">{cluster}</span>
                          </ClusterTooltip>
                          <button
                            type="button"
                            onClick={() => toggleCluster(cluster)}
                            className="text-muted-foreground hover:text-foreground"
                            aria-label={isExpanded ? `Свернуть кластер ${cluster}` : `Развернуть кластер ${cluster}`}
                          >
                            {isExpanded ? <ChevronLeft size={12} /> : <ChevronRight size={12} />}
                          </button>
                        </div>
                      </TableHead>
                    )
                  })}
                </TableRow>
                {/* Уровень 2 — sub-columns */}
                <TableRow>
                  {CLUSTER_ORDER.flatMap((cluster) => {
                    const isExpanded = expandedSet.has(cluster)
                    const warehouses = clusterWarehouses[cluster] ?? []

                    if (isExpanded && warehouses.length > 0) {
                      return warehouses.map((w, idx) => (
                        <TableHead
                          key={`${cluster}-wh-${w.warehouseId}`}
                          className={cn(
                            "sticky top-[40px] z-30 bg-background text-xs text-center border-b px-2 py-1",
                            idx === warehouses.length - 1 && "border-r",
                            w.needsClusterReview && "text-yellow-600"
                          )}
                        >
                          {w.needsClusterReview && <span className="mr-1">⚠️</span>}
                          {w.warehouseName}
                        </TableHead>
                      ))
                    }

                    if (isExpanded && warehouses.length === 0) {
                      return [
                        <TableHead key={`${cluster}-empty`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b border-r px-2 py-1">—</TableHead>
                      ]
                    }

                    return [
                      <TableHead key={`${cluster}-o`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Остаток">О</TableHead>,
                      <TableHead key={`${cluster}-z`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Заказы/день">З</TableHead>,
                      <TableHead key={`${cluster}-ob`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b px-2 py-1" title="Оборачиваемость">Об</TableHead>,
                      <TableHead key={`${cluster}-d`} className="sticky top-[40px] z-30 bg-background text-xs text-muted-foreground text-center border-b border-r px-2 py-1" title="Дефицит">Д</TableHead>,
                    ]
                  })}
                </TableRow>
              </TableHeader>
              <TableBody>
                {groups.map((g, idx) => {
                  const rowSpan = 1 + g.wbCards.length
                  return (
                    <React.Fragment key={g.productId}>
                      {/* Product Сводная строка */}
                      <TableRow className={cn(idx > 0 && "border-t-4 border-t-border")}>
                        <TableCell rowSpan={rowSpan} className="sticky left-0 z-20 bg-background border-r w-20 align-top p-2">
                          <div className="flex justify-center">
                            {g.photoUrl ? (
                              <Image src={g.photoUrl} alt={g.productName} width={72} height={96} className="rounded border object-cover aspect-[3/4]" />
                            ) : (
                              <div className="w-[72px] h-[96px] rounded border bg-muted/30 flex items-center justify-center text-xs text-muted-foreground">—</div>
                            )}
                          </div>
                        </TableCell>
                        <TableCell rowSpan={rowSpan} className="sticky left-[80px] z-20 bg-background border-r w-60 align-top p-3">
                          <div className="flex flex-col gap-1">
                            <div className="text-sm font-medium leading-snug line-clamp-2">{g.productName}</div>
                            <div className="text-xs text-muted-foreground">{g.productSku}</div>
                            <div className="text-xs text-muted-foreground">{g.brandName}</div>
                          </div>
                        </TableCell>
                        <TableCell className="sticky left-[320px] z-20 bg-background border-r w-20 align-top text-xs font-medium">Сводная</TableCell>
                        <TableCell className="sticky left-[400px] z-20 bg-background border-r w-[120px] text-xs text-muted-foreground">—</TableCell>
                        {/* 4 сводных placeholder (per Product — нет простой агрегации в этой таблице, показываем —) */}
                        <StockCell value={null} />
                        <StockCell value={null} />
                        <StockCell value={null} />
                        <DeficitCell deficit={null} threshold={null} />
                        {/* Кластеры — пусто на Сводной строке (агрегация per-nmId) */}
                        {CLUSTER_ORDER.flatMap((cluster) => {
                          const isExpanded = expandedSet.has(cluster)
                          const warehouses = clusterWarehouses[cluster] ?? []
                          const cellCount = isExpanded ? Math.max(warehouses.length, 1) : 4
                          return Array.from({ length: cellCount }, (_, i) => <StockCell key={`${cluster}-sum-${i}`} value={null} />)
                        })}
                      </TableRow>

                      {/* Per-nmId rows */}
                      {g.wbCards.map((card) => {
                        const cardMetrics = calculateStockMetrics({
                          stock: card.totalStock,
                          ordersPerDay: card.avgSalesSpeed7d,
                          turnoverNormDays,
                        })
                        const cardThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)

                        return (
                          <TableRow key={card.wbCardId} className="border-t border-t-border/60">
                            <TableCell className="sticky left-[320px] z-20 bg-background border-r w-20 text-xs tabular-nums">{card.nmId}</TableCell>
                            <TableCell className="sticky left-[400px] z-20 bg-background border-r w-[120px] text-xs line-clamp-1">{card.wbCardName ?? "—"}</TableCell>
                            {/* МП О/З/Об/Д */}
                            <StockCell value={card.totalStock} />
                            <StockCell value={card.avgSalesSpeed7d} />
                            <StockCell value={cardMetrics.turnoverDays} />
                            <DeficitCell deficit={cardMetrics.deficit} threshold={cardThreshold} />
                            {/* Кластеры */}
                            {CLUSTER_ORDER.flatMap((cluster) => {
                              const isExpanded = expandedSet.has(cluster)
                              const clusterData = card.clusters[cluster]
                              const warehouses = clusterWarehouses[cluster] ?? []

                              if (isExpanded) {
                                if (warehouses.length === 0) {
                                  return [<StockCell key={`${card.wbCardId}-${cluster}-empty`} value={null} />]
                                }
                                return warehouses.map((w, idx) => {
                                  const slot = clusterData?.warehouses.find((s) => s.warehouseId === w.warehouseId)
                                  return (
                                    <TableCell
                                      key={`${card.wbCardId}-${cluster}-${w.warehouseId}`}
                                      className={cn(
                                        "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right",
                                        idx === warehouses.length - 1 && "border-r"
                                      )}
                                    >
                                      {slot ? formatStockValue(slot.quantity) : <span className="text-muted-foreground">—</span>}
                                    </TableCell>
                                  )
                                })
                              }

                              // Collapsed: 4 sub-columns О/З/Об/Д
                              const clusterMetrics = calculateStockMetrics({
                                stock: clusterData?.totalStock ?? null,
                                ordersPerDay: card.avgSalesSpeed7d, // З общий для nmId
                                turnoverNormDays,
                              })
                              const clusterThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)
                              return [
                                <StockCell key={`${card.wbCardId}-${cluster}-o`} value={clusterData?.totalStock ?? null} />,
                                <StockCell key={`${card.wbCardId}-${cluster}-z`} value={card.avgSalesSpeed7d} />,
                                <StockCell key={`${card.wbCardId}-${cluster}-ob`} value={clusterMetrics.turnoverDays} />,
                                <DeficitCell key={`${card.wbCardId}-${cluster}-d`} deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
                              ]
                            })}
                          </TableRow>
                        )
                      })}
                    </React.Fragment>
                  )
                })}
              </TableBody>
            </Table>
          </div>
        </div>
      )
    }
    ```

    ШАГ 4 — Проверка: `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "lib/stock-wb-data.ts" && test -f "app/(dashboard)/stock/wb/page.tsx" && test -f "components/stock/StockWbTable.tsx" && grep -q "getStockWbData" lib/stock-wb-data.ts && grep -q "expandedClusters" components/stock/StockWbTable.tsx && grep -q "CLUSTER_ORDER.map" components/stock/StockWbTable.tsx && grep -q "needsClusterReview" components/stock/StockWbTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `lib/stock-wb-data.ts` экспортирует `getStockWbData`, `WbStockRow`, `ProductWbGroup`, `ClusterAggregate`, `WarehouseSlot`, `StockWbDataResult`
    - getStockWbData возвращает groups + turnoverNormDays + clusterWarehouses (для expanded headers)
    - `/stock/wb/page.tsx` — async RSC, empty state при groups.length === 0
    - `StockWbTable.tsx`: sticky 4 колонки + 4 сводных (МП О/З/Об/Д) + 7 кластерных
    - Каждая кластерная колонка: collapsed colSpan=4 (О/З/Об/Д), expanded colSpan=warehouseCount (per-warehouse О)
    - URL state: `?expandedClusters=ЦФО,ПФО` через useSearchParams + router.replace({scroll: false})
    - Toolbar: «Развернуть все» / «Свернуть все» (variant=ghost, size=sm)
    - `needsClusterReview` склад в заголовке: ⚠️ + text-yellow-600
    - ClusterTooltip wraps короткое имя кластера
    - Expand chevron aria-label "Развернуть/Свернуть кластер {X}"
    - `npx tsc --noEmit` → 0 ошибок
  </acceptance_criteria>
  <done>
    /stock/wb RSC + client таблица готовы. Кластеры переключаются через URL. Per-warehouse drill-down работает. Empty state показывается когда нет данных.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy на VPS — обновить deploy.sh + DEPLOY.md с Phase 14 шагами</name>
  <files>
    deploy.sh,
    DEPLOY.md
  </files>
  <read_first>
    - deploy.sh (текущий скрипт — паттерн migrate + build + restart)
    - DEPLOY.md (текущий runbook)
    - prisma/migrations/20260421_phase14_stock/migration.sql (Plan 14-01)
    - prisma/seed-wb-warehouses.ts (Plan 14-02)
    - /etc/nginx/sites-enabled/zoiten-pro (на VPS — nginx конфиг)
    - .planning/REQUIREMENTS.md STOCK-29 (UAT чеклист)
  </read_first>
  <action>
    ШАГ 1 — Обновить `deploy.sh` — убедиться что:
    1. `prisma migrate deploy` присутствует (автоматически применит Phase 14 миграцию)
    2. После deploy — опциональная заметка о запуске seed-wb-warehouses:
       ```bash
       # Phase 14: ручной запуск seed WB складов (one-time, после первого deploy)
       # npm run seed:wb-warehouses
       ```

    Если `deploy.sh` уже корректен — не менять. Просто верифицировать.

    ШАГ 2 — Обновить `DEPLOY.md` — добавить секцию Phase 14 (перед SSL или в конце):

    ```markdown
    ## Phase 14: Управление остатками (v1.2)

    **После deploy скрипта выполнить:**

    1. Seed справочника WB складов (один раз после первого deploy):
       ```bash
       ssh root@85.198.97.89 "cd /opt/zoiten-pro && npm run seed:wb-warehouses"
       ```

    2. Добавить nginx rewrite `/inventory → /stock` (на 1 релиз для закладок):

       В `/etc/nginx/sites-enabled/zoiten-pro` перед `location /` добавить:
       ```nginx
       location ~* ^/inventory(.*)$ {
         return 301 /stock$1;
       }
       ```

       Проверить:
       ```bash
       nginx -t && systemctl reload nginx
       curl -I https://zoiten.pro/inventory  # должен вернуть 301 Location: /stock
       ```

    3. Первый sync per-warehouse остатков:
       - Открыть https://zoiten.pro/stock
       - Нажать «Обновить из WB» → ждать ~1-2 мин
       - Проверить: таблица показывает остатки в колонках Иваново/Производство/МП/WB (Ozon = «—» placeholder)

    4. Если WB_API_TOKEN не имеет scope Аналитика (HTTP 403 в логах):
       - Регенерировать токен в seller.wildberries.ru → Настройки → Доступ к API
       - Выбрать тип: Personal (не Standard) + scope: Контент, Цены, Статистика, Аналитика, Отзывы, Тарифы
       - Обновить `WB_API_TOKEN` в `/etc/zoiten.pro.env`
       - `systemctl restart zoiten-erp.service`
    ```

    ШАГ 3 — Проверить что `deploy.sh` можно запустить без ошибок локально (dry-run через `bash -n deploy.sh` синтаксическая проверка).

    ШАГ 4 — Запустить deploy:
    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    ```

    Верифицировать на VPS:
    - `systemctl status zoiten-erp.service` → active (running)
    - `curl -I https://zoiten.pro/stock` → 200 (или 307/308 на /login если не авторизован)
    - `curl -I https://zoiten.pro/inventory` → 301 → /stock (если nginx rewrite добавлен) ИЛИ 308 (если Next.js redirect обрабатывает)

    После deploy (на VPS):
    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && npm run seed:wb-warehouses"
    ```
    Проверить: `psql -U zoiten -d zoiten_erp -c 'SELECT COUNT(*) FROM "WbWarehouse"'` > 10.
  </action>
  <verify>
    <automated>bash -n deploy.sh && grep -q "Phase 14" DEPLOY.md && grep -q "seed:wb-warehouses" DEPLOY.md && grep -q "inventory.*stock" DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `deploy.sh` содержит `prisma migrate deploy`
    - `DEPLOY.md` содержит секцию Phase 14 с инструкциями по seed-wb-warehouses, nginx rewrite, troubleshooting WB токена
    - `bash -n deploy.sh` → синтаксически валиден
    - Deploy на VPS выполнен успешно
    - `https://zoiten.pro/stock` открывается (HTTP 200/3xx)
    - `/inventory` redirects на `/stock` (через Next.js 308 или nginx 301)
    - seed-wb-warehouses запущен, `WbWarehouse` таблица populated
  </acceptance_criteria>
  <done>
    Phase 14 deployed на production. Все миграции применены. nginx rewrite работает. seed-wb-warehouses выполнен.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — 9 пунктов STOCK-29</name>
  <read_first>
    - .planning/REQUIREMENTS.md STOCK-29
    - .planning/phases/14-stock/14-UI-SPEC.md (copywriting + interactions)
  </read_first>
  <what-built>
    Phase 14 полностью deployed на https://zoiten.pro/stock. Пользователю нужно пройти 9-пунктовый UAT чеклист для подтверждения что всё работает end-to-end.
  </what-built>
  <how-to-verify>
    Пользователь проходит следующие пункты на https://zoiten.pro (production):

    **(a) /stock открывается без ошибок**
    - [ ] Зайти на https://zoiten.pro/stock после логина
    - [ ] Страница загружается, sticky 4 колонки видны слева
    - [ ] 6 групп колонок видны (РФ/Иваново/Производство/МП/WB/Ozon)
    - [ ] Нет client errors в консоли браузера

    **(b) Excel Иваново загружается с preview**
    - [ ] Нажать «Загрузить Excel Иваново» → выбрать файл
    - [ ] Открывается Dialog с preview: 4 секции (Изменения / Не найдено / Дубликаты / Невалидные)
    - [ ] Изменения показывают old → new qty
    - [ ] Нажать «Применить (N строк)» → toast.success «Импортировано N строк остатков Иваново»
    - [ ] Dialog закрывается, таблица обновляется с новыми значениями в колонке Иваново

    **(c) Производство редактируется inline**
    - [ ] В Сводной строке товара ввести число в поле Производство
    - [ ] После 500ms debounce — toast.success «Производство обновлено»
    - [ ] При refresh страницы значение сохраняется

    **(d) Норма редактируется в шапке**
    - [ ] Изменить значение в TurnoverNormInput (например, 37 → 45)
    - [ ] После 500ms debounce — toast.success «Норма сохранена»
    - [ ] Цветовая кодировка Д (жёлтый/красный) пересчитывается с новой нормой

    **(e) Кнопка «Обновить из WB» работает**
    - [ ] Нажать «Обновить из WB» (primary CTA)
    - [ ] Иконка спиннера + label «Обновление…» + toast.loading «Загружаем остатки из WB…»
    - [ ] Через ~1-2 мин: toast.dismiss + toast.success «WB остатки обновлены»
    - [ ] Колонки МП / WB показывают обновлённые значения

    **(f) /stock/wb показывает кластеры**
    - [ ] Перейти на вкладку «WB склады»
    - [ ] Видно 7 кластерных колонок (ЦФО / ЮГ / Урал / ПФО / СЗО / СФО / Прочие)
    - [ ] Каждая свёрнутая кластерная колонка содержит 4 sub-cell (О/З/Об/Д)
    - [ ] rowSpan per Product работает (Фото/Сводка объединены)

    **(g) Expand кластера показывает склады**
    - [ ] Нажать chevron у кластера ЦФО → раскрывается до per-warehouse columns
    - [ ] URL меняется: `?expandedClusters=ЦФО`
    - [ ] Скопировать URL, открыть в новой вкладке → ЦФО раскрыт (shareable)
    - [ ] Нажать «Развернуть все» → все 7 кластеров раскрыты
    - [ ] Нажать «Свернуть все» → URL очищается, все свёрнуты

    **(h) Tooltip работает**
    - [ ] Навести курсор на «ЦФО» в заголовке → tooltip показывает «Центральный федеральный округ» + «Складов: N»
    - [ ] Проверить tooltip для каждого из 7 кластеров

    **(i) nginx rewrite /inventory → /stock работает**
    - [ ] Открыть https://zoiten.pro/inventory в браузере → редирект на /stock
    - [ ] Открыть https://zoiten.pro/inventory/wb → редирект на /stock/wb (если path сохраняется)
    - [ ] Проверить через curl: `curl -I https://zoiten.pro/inventory` → HTTP 301 или 308, Location: /stock

    **Если любой пункт FAIL:**
    - Указать номер пункта + описание проблемы
    - Приложить скриншот ошибки / логи console
    - Claude зарегистрирует gap для /gsd:plan-phase --gaps
  </how-to-verify>
  <acceptance_criteria>
    - Все 9 пунктов (a..i) UAT подтверждены пользователем
    - Любые найденные баги задокументированы для gap closure plan
  </acceptance_criteria>
  <resume-signal>Тип "uat-passed" если всё GREEN. "uat-failed: {список пунктов}" если что-то не работает + детали.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 ошибок
- Все компоненты Phase 14 существуют
- Deploy на VPS успешен: systemctl status zoiten-erp.service → active
- https://zoiten.pro/stock отвечает 200/3xx
- nginx rewrite /inventory → /stock работает
- seed-wb-warehouses выполнен
- Human UAT — все 9 пунктов GREEN
</verification>

<success_criteria>
- [x] StockTabs + Layout обновлён
- [x] /stock/ozon ComingSoon
- [x] /stock/wb RSC + StockWbTable с 7 кластерами + expand URL state
- [x] ClusterTooltip с full names
- [x] needsClusterReview warehouses — ⚠️ prefix + yellow color
- [x] Toolbar «Развернуть все / Свернуть все»
- [x] deploy.sh + DEPLOY.md обновлены с Phase 14
- [x] Phase 14 deployed на https://zoiten.pro/stock
- [x] Human UAT 9/9 пунктов GREEN
</success_criteria>

<output>
Create `.planning/phases/14-stock/14-07-SUMMARY.md`:
- Deploy log: migration applied, seed-wb-warehouses count
- nginx rewrite config добавлен: да/нет
- Human UAT results: 9/9 passed ИЛИ список failed пунктов
- Phase 14 milestone v1.2 complete status
- Roadmap update needed: Phase 14 статус → Complete, progress → 7/7

Также обновить:
- `.planning/ROADMAP.md` — пометить Phase 14 как Complete, дата
- `.planning/STATE.md` — обновить stopped_at + completed_phases + completed_plans
- `.planning/REQUIREMENTS.md` Traceability — STOCK-01..STOCK-29 → Complete
</output>
