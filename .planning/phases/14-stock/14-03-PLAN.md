---
phase: 14-stock
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - lib/wb-api.ts
  - app/api/wb-sync/route.ts
  - tests/wb-stocks-per-warehouse.test.ts
requirements:
  - STOCK-07
  - STOCK-08
  - STOCK-10
autonomous: true

must_haves:
  truths:
    - "fetchStocksPerWarehouse(nmIds) возвращает Map<nmId, WarehouseStockItem[]> с корректным форматом"
    - "Batch size 1000 nmIds, пауза 20s между batches, retry 60s при 429 (1 попытка)"
    - "Старая fetchStocks() помечена @deprecated комментарием, физически не удалена"
    - "/api/wb-sync вызывает fetchStocksPerWarehouse после Content API, clean-replaces per wbCardId в транзакции"
    - "Неизвестные склады (warehouseId без записи в WbWarehouse) auto-insert с cluster='Прочие' + needsClusterReview=true + console.warn"
    - "WbCard.stockQty = SUM(quantity) после clean-replace (denormalized для backward compat /prices/wb)"
    - "Существующие вызовы fetchStocks() в /api/wb-sync не ломают старый flow (backward compat до физического удаления 2026-06-23)"
  artifacts:
    - path: "lib/wb-api.ts"
      provides: "fetchStocksPerWarehouse + WarehouseStockItem interface, @deprecated marker на fetchStocks"
      exports: ["fetchStocksPerWarehouse", "WarehouseStockItem"]
    - path: "app/api/wb-sync/route.ts"
      provides: "/api/wb-sync интегрирует fetchStocksPerWarehouse, clean-replace транзакция per wbCardId"
    - path: "tests/wb-stocks-per-warehouse.test.ts"
      provides: "Unit тесты с mocked fetch для rate limit + 429 retry"
      min_lines: 30
  key_links:
    - from: "lib/wb-api.ts fetchStocksPerWarehouse"
      to: "https://seller-analytics-api.wildberries.ru/api/analytics/v1/stocks-report/wb-warehouses"
      via: "POST с Authorization header"
      pattern: "analytics/v1/stocks-report/wb-warehouses"
    - from: "app/api/wb-sync/route.ts"
      to: "prisma.wbCardWarehouseStock"
      via: "$transaction deleteMany + upsert"
      pattern: "wbCardWarehouseStock"
    - from: "app/api/wb-sync/route.ts"
      to: "prisma.wbWarehouse.upsert"
      via: "auto-insert неизвестных складов"
      pattern: "needsClusterReview: true"
---

<objective>
Plan 14-03 — WB API migration + per-warehouse stocks sync (Wave 2, параллельно с 14-02).

**Purpose:** Добавить новую функцию `fetchStocksPerWarehouse()` в `lib/wb-api.ts`, использующую новый Analytics endpoint (`POST /api/analytics/v1/stocks-report/wb-warehouses` — launched 2026-03-23). Расширить `POST /api/wb-sync` для clean-replace per-warehouse stocks с auto-insert неизвестных складов. Помечает старую `fetchStocks()` как `@deprecated — sunset 2026-06-23` без физического удаления (backward compat).

**Output:**
- `lib/wb-api.ts`: новая функция + interface + `@deprecated` JSDoc над старой fetchStocks
- `app/api/wb-sync/route.ts`: вызов нового API после Content API, clean-replace per wbCardId, auto-insert неизвестных складов, обновление `WbCard.stockQty` (denorm)
- `tests/wb-stocks-per-warehouse.test.ts`: unit тесты mocked fetch (happy + 429 retry + batch pagination)

**Rate limit compliance:** 3 req/min → sleep 20s между batches, batch до 1000 nmIds, retry 60s при 429 (1 попытка).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stock/14-RESEARCH.md
@.planning/phases/14-stock/14-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@lib/wb-api.ts
@app/api/wb-sync/route.ts
@prisma/schema.prisma
@tests/wb-promotions-api.test.ts

<interfaces>
From lib/wb-api.ts (существующие хелперы):
```typescript
function getToken(): string  // читает WB_API_TOKEN
function sleep(ms: number): Promise<void>
export async function fetchStocks(...): Promise<...>  // ← пометить @deprecated
// Другие: fetchCardsList, fetchPricesList, fetchTariffs, fetchAvgSalesSpeed7d, ...
```

From prisma/schema.prisma (Plan 14-01):
```prisma
model WbCardWarehouseStock {
  id          String   @id @default(cuid())
  wbCardId    String
  warehouseId Int
  quantity    Int
  updatedAt   DateTime @updatedAt
  @@unique([wbCardId, warehouseId])
}

model WbWarehouse {
  id                 Int      @id
  name               String
  cluster            String
  shortCluster       String
  isActive           Boolean  @default(true)
  needsClusterReview Boolean  @default(false)
}

model WbCard {
  id           String  @id
  nmId         Int     @unique
  stockQty     Int?    // Denormalized — обновляется через SUM
  warehouses   WbCardWarehouseStock[]
}
```

From tests/wb-promotions-api.test.ts (паттерн mocked fetch):
```typescript
import { vi, describe, it, expect, beforeEach } from "vitest"
beforeEach(() => {
  vi.stubGlobal("fetch", vi.fn())
})
// mock response: fetch.mockResolvedValueOnce(new Response(JSON.stringify({...}), {status: 200}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchStocksPerWarehouse в lib/wb-api.ts + Wave 0 test</name>
  <files>
    lib/wb-api.ts,
    tests/wb-stocks-per-warehouse.test.ts
  </files>
  <read_first>
    - lib/wb-api.ts (весь файл — изучить `getToken`, `sleep`, паттерн `fetchAvgSalesSpeed7d`, `fetchStocks`)
    - .planning/phases/14-stock/14-RESEARCH.md §Pattern 1: fetchStocksPerWarehouse (полный код шаблон)
    - .planning/phases/14-stock/14-RESEARCH.md §Ошибка 1 (scope токена + curl smoke test)
    - .planning/REQUIREMENTS.md STOCK-07
    - tests/wb-promotions-api.test.ts (паттерн mocked fetch + rate limit + 429)
  </read_first>
  <behavior>
    - fetchStocksPerWarehouse([]) → Map<number, WarehouseStockItem[]>() (пустой)
    - fetchStocksPerWarehouse([12345]) mocked HTTP 200 response → Map с одним entry
    - fetchStocksPerWarehouse([12345]) mocked HTTP 429 first + HTTP 200 second → retry срабатывает, результат success
    - fetchStocksPerWarehouse([12345]) mocked HTTP 429 twice → throws Error
    - fetchStocksPerWarehouse([12345]) mocked HTTP 401 → throws Error с text body
    - fetchStocksPerWarehouse(batch_2000_ids) mocked → ДВА fetch вызова (batch 1000 + batch 1000), sleep между ними
    - Response без data field: fetchStocksPerWarehouse → empty Map без throw (robust to empty response)
  </behavior>
  <action>
    ШАГ 1 — Добавить в `lib/wb-api.ts` (в конец файла после последней функции):

    ```typescript
    // ──────────────────────────────────────────────────────────────────
    // Phase 14: WB Analytics Stocks Per-Warehouse (STOCK-07)
    // ──────────────────────────────────────────────────────────────────

    const ANALYTICS_API = "https://seller-analytics-api.wildberries.ru"
    const STOCKS_BATCH_SIZE = 1000
    const STOCKS_INTERVAL_MS = 20_000  // 20s между batches (rate limit 3/min)
    const STOCKS_RETRY_MS = 60_000     // 60s retry при 429

    export interface WarehouseStockItem {
      warehouseId: number
      warehouseName: string
      regionName: string
      quantity: number
      inWayToClient: number
      inWayFromClient: number
    }

    /** POST /api/analytics/v1/stocks-report/wb-warehouses
     *  Rate limit: 3 req/min, интервал 20s между batches.
     *  Batch size: до 1000 nmIds.
     *  При 429 — retry через 60s (1 попытка).
     *  Запущен WB 2026-03-23, заменяет GET /api/v1/supplier/stocks (sunset 2026-06-23).
     *
     *  @returns Map<nmId, Array<WarehouseStockItem>>
     */
    export async function fetchStocksPerWarehouse(
      nmIds: number[]
    ): Promise<Map<number, WarehouseStockItem[]>> {
      const token = getToken()
      const result = new Map<number, WarehouseStockItem[]>()
      if (nmIds.length === 0) return result

      for (let i = 0; i < nmIds.length; i += STOCKS_BATCH_SIZE) {
        if (i > 0) await sleep(STOCKS_INTERVAL_MS)

        const batch = nmIds.slice(i, i + STOCKS_BATCH_SIZE)
        let attempt = 0

        while (attempt <= 1) {
          const res = await fetch(
            `${ANALYTICS_API}/api/analytics/v1/stocks-report/wb-warehouses`,
            {
              method: "POST",
              headers: { Authorization: token, "Content-Type": "application/json" },
              body: JSON.stringify({ nmIds: batch, limit: 250000, offset: 0 }),
            }
          )

          if (res.status === 429) {
            if (attempt >= 1) throw new Error("stocks-report 429 после retry")
            attempt++
            await sleep(STOCKS_RETRY_MS)
            continue
          }

          if (!res.ok) {
            const text = await res.text()
            throw new Error(`stocks-report ${res.status}: ${text}`)
          }

          const data = await res.json()
          const rows: Array<{
            nmId: number
            warehouseId: number
            warehouseName: string
            regionName: string
            quantity: number
            inWayToClient: number
            inWayFromClient: number
          }> = data?.data ?? data ?? []

          for (const row of rows) {
            const items = result.get(row.nmId) ?? []
            items.push({
              warehouseId: row.warehouseId,
              warehouseName: row.warehouseName,
              regionName: row.regionName,
              quantity: row.quantity,
              inWayToClient: row.inWayToClient,
              inWayFromClient: row.inWayFromClient,
            })
            result.set(row.nmId, items)
          }
          break
        }
      }

      return result
    }
    ```

    ШАГ 2 — Пометить старую `fetchStocks` `@deprecated` JSDoc-ом (найти функцию в lib/wb-api.ts, добавить над ней):

    ```typescript
    /**
     * @deprecated Использовать fetchStocksPerWarehouse() — Statistics API endpoint sunset 2026-06-23.
     * Физически удалить в STOCK-FUT-09 (v1.3+) после 1-2 циклов validation в проде.
     */
    export async function fetchStocks(...) { ... }
    ```

    НЕ УДАЛЯТЬ функцию физически — другие места могут использовать, плюс fallback до sunset date.

    ШАГ 3 — Создать `tests/wb-stocks-per-warehouse.test.ts` с 6+ тестами из `<behavior>`:

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    import { fetchStocksPerWarehouse } from "@/lib/wb-api"

    beforeEach(() => {
      vi.stubEnv("WB_API_TOKEN", "test-token")
      vi.useFakeTimers()
    })

    afterEach(() => {
      vi.restoreAllMocks()
      vi.useRealTimers()
    })

    function mockFetch(impl: ReturnType<typeof vi.fn>) {
      vi.stubGlobal("fetch", impl)
    }

    describe("fetchStocksPerWarehouse", () => {
      it("пустой массив nmIds → пустой Map без HTTP вызовов", async () => {
        const fetchMock = vi.fn()
        mockFetch(fetchMock)
        const result = await fetchStocksPerWarehouse([])
        expect(result.size).toBe(0)
        expect(fetchMock).not.toHaveBeenCalled()
      })

      it("happy path 200 → Map с данными", async () => {
        const fetchMock = vi.fn().mockResolvedValueOnce(
          new Response(JSON.stringify({
            data: [
              { nmId: 12345, warehouseId: 507, warehouseName: "Коледино", regionName: "Московская обл.", quantity: 100, inWayToClient: 5, inWayFromClient: 2 },
            ]
          }), { status: 200 })
        )
        mockFetch(fetchMock)

        const result = await fetchStocksPerWarehouse([12345])
        expect(result.get(12345)).toHaveLength(1)
        expect(result.get(12345)![0]!.warehouseId).toBe(507)
        expect(result.get(12345)![0]!.quantity).toBe(100)
      })

      it("429 + 200 → retry срабатывает, результат success", async () => {
        // ... (vi.advanceTimersByTimeAsync нужен для ожидания sleep)
      })

      // ... остальные тесты
    })
    ```

    Запустить `npm run test -- tests/wb-stocks-per-warehouse.test.ts` → все GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-stocks-per-warehouse.test.ts && grep -q "fetchStocksPerWarehouse" lib/wb-api.ts && grep -q "@deprecated" lib/wb-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wb-api.ts` экспортирует `fetchStocksPerWarehouse` и `WarehouseStockItem`
    - `fetchStocksPerWarehouse([])` возвращает пустой Map без HTTP вызовов
    - Старая `fetchStocks` содержит `@deprecated` JSDoc комментарий с упоминанием sunset 2026-06-23
    - `tests/wb-stocks-per-warehouse.test.ts` существует с 6+ тестами
    - `npm run test -- tests/wb-stocks-per-warehouse.test.ts` → все GREEN
    - Endpoint URL ровно `https://seller-analytics-api.wildberries.ru/api/analytics/v1/stocks-report/wb-warehouses`
    - Request body: `{nmIds: [...], limit: 250000, offset: 0}`
  </acceptance_criteria>
  <done>
    Новая функция добавлена, старая помечена deprecated без удаления, тесты GREEN, никаких 3rd-party зависимостей не добавлено.
  </done>
</task>

<task type="auto">
  <name>Task 2: /api/wb-sync — per-warehouse clean-replace транзакция + auto-insert</name>
  <files>
    app/api/wb-sync/route.ts
  </files>
  <read_first>
    - app/api/wb-sync/route.ts (весь файл — найти место после Content API / Prices API / Tariffs API, перед Analytics API — там где обычно фетчатся stocks)
    - lib/wb-api.ts fetchStocksPerWarehouse (новая функция)
    - .planning/phases/14-stock/14-RESEARCH.md §Pattern 6: clean-replace транзакция
    - .planning/REQUIREMENTS.md STOCK-08, STOCK-10
    - prisma/schema.prisma (WbWarehouse + WbCardWarehouseStock модели)
  </read_first>
  <action>
    ШАГ 1 — Найти в `app/api/wb-sync/route.ts` секцию где вызывается `fetchStocks()` и собираются остатки (обычно после Content API + Prices API, перед Analytics API buyoutPercent).

    ШАГ 2 — ДОБАВИТЬ (не заменить!) после существующего блока `fetchStocks` следующий блок:

    ```typescript
    // Phase 14: per-warehouse stocks (STOCK-08, STOCK-10)
    // Fetch per-warehouse остатки через новый Analytics API
    const nmIdsForStocks = allCards.map(c => c.nmId)  // все nmIds из Content API
    let stocksPerWarehouse: Map<number, WarehouseStockItem[]>
    try {
      stocksPerWarehouse = await fetchStocksPerWarehouse(nmIdsForStocks)
      console.log(`[wb-sync] Получено per-warehouse остатков для ${stocksPerWarehouse.size} nmIds`)
    } catch (e) {
      console.warn(`[wb-sync] fetchStocksPerWarehouse failed, skipping per-warehouse update:`, e)
      stocksPerWarehouse = new Map()
    }

    // Clean-replace per wbCardId в транзакции
    if (stocksPerWarehouse.size > 0) {
      await prisma.$transaction(async (tx) => {
        for (const [nmId, warehouseItems] of stocksPerWarehouse.entries()) {
          const card = await tx.wbCard.findUnique({ where: { nmId }, select: { id: true } })
          if (!card) continue

          const incomingIds = warehouseItems.map(w => w.warehouseId)

          // Удалить склады, которых нет в ответе (clean part of clean-replace)
          await tx.wbCardWarehouseStock.deleteMany({
            where: {
              wbCardId: card.id,
              NOT: { warehouseId: { in: incomingIds } }
            }
          })

          // Upsert входящие + auto-insert неизвестных складов
          for (const item of warehouseItems) {
            // Auto-insert неизвестного склада (STOCK-10)
            const existing = await tx.wbWarehouse.findUnique({
              where: { id: item.warehouseId },
              select: { id: true }
            })
            if (!existing) {
              console.warn(`[wb-sync] Auto-insert неизвестный склад: id=${item.warehouseId} name="${item.warehouseName}"`)
              await tx.wbWarehouse.create({
                data: {
                  id: item.warehouseId,
                  name: item.warehouseName,
                  cluster: "Прочие склады",
                  shortCluster: "Прочие",
                  isActive: true,
                  needsClusterReview: true,
                },
              })
            }

            await tx.wbCardWarehouseStock.upsert({
              where: {
                wbCardId_warehouseId: {
                  wbCardId: card.id,
                  warehouseId: item.warehouseId,
                }
              },
              create: {
                wbCardId: card.id,
                warehouseId: item.warehouseId,
                quantity: item.quantity,
              },
              update: {
                quantity: item.quantity,
              },
            })
          }

          // Денормализация WbCard.stockQty = SUM(quantity) для backward compat /prices/wb
          const totalQty = warehouseItems.reduce((sum, w) => sum + w.quantity, 0)
          await tx.wbCard.update({
            where: { id: card.id },
            data: { stockQty: totalQty },
          })
        }
      }, { timeout: 60_000 })  // транзакция может быть длинной — 60s timeout

      console.log(`[wb-sync] Обновлено per-warehouse остатков для ${stocksPerWarehouse.size} wbCards`)
    }
    ```

    ШАГ 3 — Добавить импорт в начало файла:
    ```typescript
    import { fetchStocksPerWarehouse, type WarehouseStockItem } from "@/lib/wb-api"
    ```

    ШАГ 4 — **НЕ УДАЛЯТЬ** существующий блок `fetchStocks()` — оставить как есть для backward compat (до sunset 2026-06-23, физическое удаление в STOCK-FUT-09).

    ШАГ 5 — Проверить: если `nmIdsForStocks` собирается из `allCards` — убедиться что `allCards` определено выше. Если в файле используется другой паттерн сбора nmIds (например, `prisma.wbCard.findMany`) — использовать его.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "fetchStocksPerWarehouse" app/api/wb-sync/route.ts && grep -q "wbCardWarehouseStock.deleteMany" app/api/wb-sync/route.ts && grep -q "needsClusterReview: true" app/api/wb-sync/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/wb-sync/route.ts` импортирует `fetchStocksPerWarehouse` из `@/lib/wb-api`
    - Вызов `fetchStocksPerWarehouse(nmIds)` обёрнут в try/catch (degraded mode при failure)
    - `prisma.$transaction` содержит `deleteMany` + `upsert` для `wbCardWarehouseStock`
    - Auto-insert `wbWarehouse` с `cluster: "Прочие склады"`, `shortCluster: "Прочие"`, `needsClusterReview: true`
    - После clean-replace: `wbCard.update` обновляет `stockQty = SUM(quantity)`
    - Старый блок `fetchStocks` НЕ удалён
    - `npx tsc --noEmit` → 0 ошибок
    - Транзакция с `timeout: 60_000`
  </acceptance_criteria>
  <done>
    /api/wb-sync расширен per-warehouse stocks. Clean-replace per wbCardId. Auto-insert неизвестных складов с флагом needsClusterReview. Backward compat (stockQty denorm + старый fetchStocks).
  </done>
</task>

</tasks>

<verification>
- `npm run test -- tests/wb-stocks-per-warehouse.test.ts` → GREEN
- `npx tsc --noEmit` → 0 ошибок
- `grep "fetchStocksPerWarehouse" lib/wb-api.ts app/api/wb-sync/route.ts` → matches в обоих файлах
- Если DB есть: manual curl `POST /api/wb-sync` (после auth) → проверить что в БД появились WbCardWarehouseStock записи
</verification>

<success_criteria>
- [x] `lib/wb-api.ts` содержит `fetchStocksPerWarehouse` + `WarehouseStockItem` interface
- [x] `fetchStocks` помечена `@deprecated` JSDoc (НЕ удалена)
- [x] Rate limit: 20s между batches, 60s retry при 429
- [x] `app/api/wb-sync/route.ts` вызывает новую функцию после Content API
- [x] Clean-replace транзакция: `deleteMany NOT IN` + `upsert`
- [x] Auto-insert неизвестных складов с `needsClusterReview: true` + `shortCluster: "Прочие"`
- [x] `WbCard.stockQty = SUM(quantity)` для backward compat
- [x] `tests/wb-stocks-per-warehouse.test.ts` — 6+ GREEN тестов
- [x] `npx tsc --noEmit` → 0 ошибок
</success_criteria>

<output>
Create `.planning/phases/14-stock/14-03-SUMMARY.md`:
- Ключевое решение: как обрабатывается degraded mode (fetchStocksPerWarehouse fail → sync продолжается)
- Тесты GREEN count
- Если auto-insert протестирован (unknown warehouseId) — задокументировать
- Integration notes для Plan 14-05 (кнопка «Обновить из WB» будет вызывать этот endpoint)
</output>
