---
phase: 14-stock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260421_phase14_stock/migration.sql
  - prisma/seed.ts
  - lib/sections.ts
  - lib/section-titles.ts
  - components/layout/nav-items.ts
  - next.config.ts
  - app/(dashboard)/inventory/page.tsx
  - app/(dashboard)/stock/layout.tsx
  - app/(dashboard)/stock/page.tsx
  - lib/stock-math.ts
  - lib/normalize-sku.ts
  - lib/wb-clusters.ts
  - tests/stock-math.test.ts
  - tests/normalize-sku.test.ts
  - tests/parse-ivanovo-excel.test.ts
autonomous: false
requirements:
  - STOCK-01
  - STOCK-02
  - STOCK-03
  - STOCK-04
  - STOCK-05
  - STOCK-06
  - STOCK-26
  - STOCK-27

user_setup:
  - service: wildberries-analytics
    why: "Проверка scope нового endpoint /api/analytics/v1/stocks-report/wb-warehouses"
    env_vars:
      - name: WB_API_TOKEN
        source: "Уже существует в /etc/zoiten.pro.env, scope Аналитика (bit 2), тип Personal/Service"
    dashboard_config:
      - task: "Если Wave 0 curl возвращает 401/403 — регенерировать токен Personal type с scope Аналитика"
        location: "seller.wildberries.ru → Настройки → Доступ к API"

must_haves:
  truths:
    - "Wave 0 curl smoke test нового WB endpoint выполнен, пользователь подтвердил HTTP 200 (или блокер обозначен)"
    - "Prisma миграция создаёт WbWarehouse, WbCardWarehouseStock, 4 новых поля Product, AppSetting stock.turnoverNormDays=37"
    - "lib/stock-math.ts calculateStockMetrics возвращает корректные Об/Д со всеми null-guards"
    - "lib/normalize-sku.ts трансформирует `УКТ—000001` (em-dash), `укт-1`, `1`, ` 1 ` → `УКТ-000001`"
    - "Все 3 теста (stock-math, normalize-sku, parse-ivanovo-excel) запускаются через `npm run test` (2 GREEN + 1 RED stub)"
    - "URL /stock открывается, /inventory возвращает 308 redirect на /stock"
    - "RBAC requireSection('STOCK') работает на /stock, /stock/wb недоступен без SECTION STOCK"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "WbWarehouse + WbCardWarehouseStock models, Product.ivanovoStock/productionStock/*UpdatedAt поля"
      contains: "model WbWarehouse"
    - path: "prisma/migrations/20260421_phase14_stock/migration.sql"
      provides: "Одна миграция для всей Phase 14 схемы"
    - path: "lib/stock-math.ts"
      provides: "calculateStockMetrics + deficitThreshold pure functions"
      exports: ["calculateStockMetrics", "deficitThreshold"]
    - path: "lib/normalize-sku.ts"
      provides: "normalizeSku canonical transformer"
      exports: ["normalizeSku"]
    - path: "lib/wb-clusters.ts"
      provides: "CLUSTER_FULL_NAMES + CLUSTER_ORDER константы"
      exports: ["CLUSTER_FULL_NAMES", "CLUSTER_ORDER"]
    - path: "app/(dashboard)/stock/layout.tsx"
      provides: "RSC layout с requireSection('STOCK') + <h1> + <StockTabs>"
    - path: "app/(dashboard)/stock/page.tsx"
      provides: "Временная RSC page.tsx — Plan 14-06 заменит реальной таблицей"
      min_lines: 10
    - path: "tests/stock-math.test.ts"
      provides: "5+ GREEN test cases"
    - path: "tests/normalize-sku.test.ts"
      provides: "Canonical + invalid cases GREEN"
    - path: "tests/parse-ivanovo-excel.test.ts"
      provides: "RED stub — real fixture в Plan 14-04"
  key_links:
    - from: "middleware.ts (через lib/sections.ts)"
      to: "/stock/*"
      via: "SECTION_PATHS map"
      pattern: "\"/stock\": \"STOCK\""
    - from: "next.config.ts redirects()"
      to: "/stock/:path*"
      via: "permanent 308"
      pattern: "/inventory/:path\\*"
    - from: "components/layout/nav-items.ts"
      to: "/stock"
      via: "NAV_ITEMS href"
      pattern: "href: \"/stock\""
    - from: "prisma/schema.prisma (WbCard)"
      to: "WbCardWarehouseStock"
      via: "warehouses relation"
      pattern: "warehouses\\s+WbCardWarehouseStock\\[\\]"
---

<objective>
Plan 14-01 — Foundation для всей Phase 14 (Wave 1, блокирует всё).

**Purpose:** Создать единую миграцию БД (WbWarehouse + WbCardWarehouseStock + 4 поля Product + AppSetting `stock.turnoverNormDays`), pure-функции `stock-math` + `normalize-sku` + `wb-clusters`, переименовать роут `/inventory` → `/stock` с next.config redirect, поднять RBAC `requireSection("STOCK")`, подготовить Wave 0 test infrastructure (2 GREEN теста + 1 RED stub), провести Zero Wave smoke test нового WB Analytics endpoint с пользователем.

**Output:**
- Prisma миграция применена (`prisma migrate dev`) локально или помечена pending если локальной PG нет
- 4 новых файла: `lib/stock-math.ts`, `lib/normalize-sku.ts`, `lib/wb-clusters.ts`, `app/(dashboard)/stock/layout.tsx`
- Папка `app/(dashboard)/inventory/` удалена, создана `app/(dashboard)/stock/`
- 3 теста: 2 GREEN (stock-math, normalize-sku), 1 RED (parse-ivanovo-excel — ждёт fixture в Plan 14-04)
- next.config.ts с redirect `/inventory/:path*` → `/stock/:path*`
- Zero Wave: curl smoke test нового endpoint, HTTP 200 подтверждён
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-stock/14-RESEARCH.md
@.planning/phases/14-stock/14-UI-SPEC.md
@CLAUDE.md
@prisma/schema.prisma
@lib/sections.ts
@lib/section-titles.ts
@components/layout/nav-items.ts
@components/prices/PricesTabs.tsx
@tests/pricing-math.test.ts

<interfaces>
<!-- Ключевые интерфейсы для исполнителя — извлечены из существующего кода. -->

From prisma/schema.prisma (существующее):
```prisma
enum ERP_SECTION {
  PRODUCTS
  PRICES
  WEEKLY_CARDS
  STOCK          // ← уже существует
  COST
  PROCUREMENT
  SALES
  SUPPORT
  EMPLOYEES
  USER_MANAGEMENT
}

model WbCard {
  id                  String        @id @default(cuid())
  nmId                Int           @unique
  // ... существующие поля
  stockQty            Int?          // остаток товара, шт (денормализованный)
  avgSalesSpeed7d     Float?        // скорость продаж (З)
  // Нужно добавить relation:
  // warehouses          WbCardWarehouseStock[]
}

model Product {
  id                  String                @id @default(cuid())
  sku                 String                @unique  // УКТ-000001
  // ... существующие поля
  // Нужно добавить:
  // ivanovoStock              Int?
  // productionStock           Int?
  // ivanovoStockUpdatedAt     DateTime?
  // productionStockUpdatedAt  DateTime?
}

model AppSetting {
  key   String @id
  value String
}
```

From lib/rbac.ts:
```typescript
export async function requireSection(section: ERP_SECTION, minRole?: "VIEW" | "MANAGE"): Promise<Session>
```

From lib/sections.ts (текущее состояние, строка 11):
```typescript
export const SECTION_PATHS: Record<string, ERP_SECTION> = {
  "/products": "PRODUCTS",
  "/cards": "PRODUCTS",
  "/prices": "PRICES",
  "/inventory": "STOCK",   // ← меняем на "/stock"
  "/batches": "COST",
  // ...
}
```

From components/prices/PricesTabs.tsx (паттерн для StockTabs, будет создан в Plan 14-07):
```typescript
// pathname.startsWith("/prices/wb") → "WB" active
// pathname === "/prices/wb" default
```

From tests/pricing-math.test.ts (паттерн vitest):
```typescript
import { describe, it, expect } from "vitest"
import { calculatePricing } from "@/lib/pricing-math"

describe("calculatePricing", () => {
  it("golden test nmId 800750522", () => {
    const result = calculatePricing({ ... })
    expect(result.profit).toBeCloseTo(567.68, 1)
  })
})
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 (Zero Wave): Smoke test нового WB Analytics endpoint</name>
  <read_first>
    - .planning/phases/14-stock/14-RESEARCH.md §Ошибка 1: WB Analytics API — неправильный токен scope
    - CLAUDE.md §WB API токен (scope info)
  </read_first>
  <what-built>
    Wave 0 Zero Gate — пользователь выполняет curl smoke test нового endpoint `POST /api/analytics/v1/stocks-report/wb-warehouses` с текущим WB_API_TOKEN на VPS. Проверяем scope Аналитика (bit 2) + тип токена Personal/Service (Standard может не работать).
  </what-built>
  <how-to-verify>
    На VPS (ssh root@85.198.97.89) выполнить:
    ```bash
    source /etc/zoiten.pro.env
    curl -X POST \
      "https://seller-analytics-api.wildberries.ru/api/analytics/v1/stocks-report/wb-warehouses" \
      -H "Authorization: $WB_API_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"nmIds":[800750522],"limit":10,"offset":0}' \
      -w "\nHTTP %{http_code}\n"
    ```

    Ожидаемый результат:
    - HTTP 200 → scope OK, токен подходит, план 14-03 разблокирован
    - HTTP 401 → токен невалиден → регенерировать в seller.wildberries.ru
    - HTTP 403 → scope неверный ИЛИ тип токена Standard → нужен Personal/Service
    - HTTP 429 → rate limit, подождать 20 сек и повторить
    - HTTP 422/400 → body неверный (не блокер, endpoint отвечает — значит scope OK)

    Скопировать ответ в сообщение Claude ПОЛНОСТЬЮ (включая HTTP статус и первые 20 строк тела).
  </how-to-verify>
  <acceptance_criteria>
    - Пользователь выполнил curl на VPS
    - Получен HTTP ответ (200 / 401 / 403 / 422)
    - Если 401/403 — процесс регенерации токена документирован пользователем, решение задокументировано в SUMMARY плана
    - Если 200 или 422 — scope OK, можно кодировать fetchStocksPerWarehouse в Plan 14-03
  </acceptance_criteria>
  <resume-signal>Тип "ok-200" если 200 получен, или "ok-422" если 422 (scope работает), или "blocker-{код}" с деталями если 401/403 + план регенерации токена</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Prisma миграция Phase 14 + 3 pure-функции + Wave 0 тесты</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260421_phase14_stock/migration.sql,
    prisma/seed.ts,
    lib/stock-math.ts,
    lib/normalize-sku.ts,
    lib/wb-clusters.ts,
    tests/stock-math.test.ts,
    tests/normalize-sku.test.ts,
    tests/parse-ivanovo-excel.test.ts
  </files>
  <read_first>
    - prisma/schema.prisma (весь файл — изучить текущую структуру моделей Product, WbCard, AppSetting)
    - .planning/phases/14-stock/14-RESEARCH.md §Pattern 2: Prisma схема, §Pattern 3: stock-math, §Pattern 4: normalize-sku
    - .planning/phases/14-stock/14-UI-SPEC.md §Color — семантические цвета дефицита (формулы Д)
    - .planning/REQUIREMENTS.md STOCK-01, STOCK-02, STOCK-03, STOCK-26, STOCK-27
    - prisma/seed.ts (существующая структура seed)
    - tests/pricing-math.test.ts (паттерн vitest)
  </read_first>
  <behavior>
    - calculateStockMetrics({stock=100, ordersPerDay=5, turnoverNormDays=37}) → {turnoverDays: 20, deficit: -44.5}
    - calculateStockMetrics({stock=null, ...}) → {turnoverDays: null, deficit: null}
    - calculateStockMetrics({stock=100, ordersPerDay=0, turnoverNormDays=37}) → {turnoverDays: null, deficit: -100}
    - calculateStockMetrics({stock=100, ordersPerDay=null, turnoverNormDays=37}) → {turnoverDays: null, deficit: null}
    - calculateStockMetrics({stock=100, ordersPerDay=5, turnoverNormDays=0}) → {turnoverDays: null, deficit: null}
    - calculateStockMetrics({stock=0, ordersPerDay=5, turnoverNormDays=37}) → {turnoverDays: 0, deficit: 55.5} (красный)
    - deficitThreshold(37, 5) → 55.5 (norm × 0.3 × З)
    - deficitThreshold(37, null) → null
    - normalizeSku("УКТ-000001") → "УКТ-000001"
    - normalizeSku("УКТ-1") → "УКТ-000001"
    - normalizeSku("1") → "УКТ-000001"
    - normalizeSku(" укт-000001 ") → "УКТ-000001"
    - normalizeSku("УКТ\u2014000001") → "УКТ-000001" (em-dash)
    - normalizeSku("abc") → throws Error
    - normalizeSku("УКТ-") → throws Error
    - normalizeSku("") → throws Error
    - parse-ivanovo-excel.test.ts — RED stub с `it.skip` или `expect(false).toBe(true)` + TODO комментарий "fixture от пользователя в Plan 14-04"
  </behavior>
  <action>
    ШАГ 1 — Prisma schema (prisma/schema.prisma):

    Добавить в конец файла (перед последней закрывающей скобкой если есть, иначе в конец):
    ```prisma
    // ──────────────────────────────────────────────────────────────────
    // Phase 14: Stock Management
    // ──────────────────────────────────────────────────────────────────

    model WbWarehouse {
      id                 Int                    @id // warehouseId из WB Analytics API
      name               String                 // "Коледино", "Казань"
      cluster            String                 // Полное название, напр. "Центральный федеральный округ"
      shortCluster       String                 // "ЦФО" | "ЮГ" | "Урал" | "ПФО" | "СЗО" | "СФО" | "Прочие"
      isActive           Boolean                @default(true)
      needsClusterReview Boolean                @default(false)
      stocks             WbCardWarehouseStock[]
    }

    model WbCardWarehouseStock {
      id          String      @id @default(cuid())
      wbCardId    String
      wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
      warehouseId Int
      warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
      quantity    Int         @default(0)
      updatedAt   DateTime    @updatedAt

      @@unique([wbCardId, warehouseId])
      @@index([wbCardId])
      @@index([warehouseId])
    }
    ```

    В модель WbCard добавить обратную relation (найти существующую модель, добавить поле перед закрывающей `}`):
    ```prisma
    warehouses  WbCardWarehouseStock[]
    ```

    В модель Product добавить 4 поля (найти существующую модель, добавить перед `deletedAt`):
    ```prisma
    ivanovoStock              Int?
    productionStock           Int?
    ivanovoStockUpdatedAt     DateTime?
    productionStockUpdatedAt  DateTime?
    ```

    ШАГ 2 — Создать миграцию `prisma/migrations/20260421_phase14_stock/migration.sql` вручную (локальной PG нет, deploy.sh применит на VPS):
    ```sql
    -- Phase 14: Stock Management
    -- CreateTable WbWarehouse
    CREATE TABLE "WbWarehouse" (
      "id" INTEGER NOT NULL,
      "name" TEXT NOT NULL,
      "cluster" TEXT NOT NULL,
      "shortCluster" TEXT NOT NULL,
      "isActive" BOOLEAN NOT NULL DEFAULT true,
      "needsClusterReview" BOOLEAN NOT NULL DEFAULT false,
      CONSTRAINT "WbWarehouse_pkey" PRIMARY KEY ("id")
    );

    -- CreateTable WbCardWarehouseStock
    CREATE TABLE "WbCardWarehouseStock" (
      "id" TEXT NOT NULL,
      "wbCardId" TEXT NOT NULL,
      "warehouseId" INTEGER NOT NULL,
      "quantity" INTEGER NOT NULL DEFAULT 0,
      "updatedAt" TIMESTAMP(3) NOT NULL,
      CONSTRAINT "WbCardWarehouseStock_pkey" PRIMARY KEY ("id")
    );

    CREATE UNIQUE INDEX "WbCardWarehouseStock_wbCardId_warehouseId_key" ON "WbCardWarehouseStock"("wbCardId", "warehouseId");
    CREATE INDEX "WbCardWarehouseStock_wbCardId_idx" ON "WbCardWarehouseStock"("wbCardId");
    CREATE INDEX "WbCardWarehouseStock_warehouseId_idx" ON "WbCardWarehouseStock"("warehouseId");

    -- AddForeignKey
    ALTER TABLE "WbCardWarehouseStock" ADD CONSTRAINT "WbCardWarehouseStock_wbCardId_fkey" FOREIGN KEY ("wbCardId") REFERENCES "WbCard"("id") ON DELETE CASCADE ON UPDATE CASCADE;
    ALTER TABLE "WbCardWarehouseStock" ADD CONSTRAINT "WbCardWarehouseStock_warehouseId_fkey" FOREIGN KEY ("warehouseId") REFERENCES "WbWarehouse"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

    -- AlterTable Product — add 4 columns
    ALTER TABLE "Product" ADD COLUMN "ivanovoStock" INTEGER;
    ALTER TABLE "Product" ADD COLUMN "productionStock" INTEGER;
    ALTER TABLE "Product" ADD COLUMN "ivanovoStockUpdatedAt" TIMESTAMP(3);
    ALTER TABLE "Product" ADD COLUMN "productionStockUpdatedAt" TIMESTAMP(3);

    -- Seed AppSetting stock.turnoverNormDays
    INSERT INTO "AppSetting" ("key", "value") VALUES ('stock.turnoverNormDays', '37') ON CONFLICT ("key") DO NOTHING;
    ```

    ШАГ 3 — prisma/seed.ts — добавить ensure AppSetting:
    ```typescript
    // В конце main() функции, перед prisma.$disconnect():
    await prisma.appSetting.upsert({
      where: { key: "stock.turnoverNormDays" },
      create: { key: "stock.turnoverNormDays", value: "37" },
      update: {},
    })
    ```

    ШАГ 4 — Создать lib/stock-math.ts с полной реализацией из 14-RESEARCH.md §Pattern 3. Экспорты: `calculateStockMetrics`, `deficitThreshold`, типы `StockMetricsInput`, `StockMetricsOutput`.

    ШАГ 5 — Создать lib/normalize-sku.ts из 14-RESEARCH.md §Pattern 4:
    ```typescript
    const EM_DASH = "\u2014"
    const SKU_REGEX = /^(?:УКТ-?)?(\d+)$/

    export function normalizeSku(raw: string): string {
      const s = raw.trim().toUpperCase().replace(EM_DASH, "-")
      const match = SKU_REGEX.exec(s)
      if (!match || !match[1]) {
        throw new Error(`Невалидный УКТ: "${raw}"`)
      }
      return `УКТ-${match[1].padStart(6, "0")}`
    }
    ```

    ШАГ 6 — Создать lib/wb-clusters.ts:
    ```typescript
    export const CLUSTER_FULL_NAMES: Record<string, string> = {
      "ЦФО": "Центральный федеральный округ",
      "ЮГ": "Южный + Северо-Кавказский ФО",
      "Урал": "Уральский федеральный округ",
      "ПФО": "Приволжский федеральный округ",
      "СЗО": "Северо-Западный федеральный округ",
      "СФО": "Сибирский + Дальневосточный ФО",
      "Прочие": "Прочие склады",
    } as const

    export const CLUSTER_ORDER = ["ЦФО", "ЮГ", "Урал", "ПФО", "СЗО", "СФО", "Прочие"] as const

    export type ClusterShortName = typeof CLUSTER_ORDER[number]
    ```

    ШАГ 7 — Создать tests/stock-math.test.ts — 6+ test cases из `<behavior>` выше. Паттерн tests/pricing-math.test.ts.

    ШАГ 8 — Создать tests/normalize-sku.test.ts — 8 test cases (5 canonical + 3 invalid).

    ШАГ 9 — Создать tests/parse-ivanovo-excel.test.ts — RED stub:
    ```typescript
    import { describe, it, expect } from "vitest"

    describe("parseIvanovoExcel", () => {
      // TODO: Real fixture от пользователя в Plan 14-04 Zero Wave.
      // После получения ivanovo-sample.xlsx: реализовать lib/parse-ivanovo-excel.ts + заменить stub тестами.
      it.skip("happy path с реальной fixture (ждёт Plan 14-04 Zero Wave)", () => {
        expect(true).toBe(false)
      })
    })
    ```

    ШАГ 10 — Выполнить `npx prisma generate` (обновить Prisma Client типы) и `npm run test` — должно быть GREEN для stock-math + normalize-sku; parse-ivanovo-excel skipped.

    Если локальная PG есть — `npx prisma migrate dev --name phase14_stock`. Если нет — миграция остаётся pending, применится через deploy.sh на VPS в Plan 14-07.
  </action>
  <verify>
    <automated>npx prisma validate && npm run test -- tests/stock-math.test.ts tests/normalize-sku.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` содержит `model WbWarehouse` и `model WbCardWarehouseStock`
    - `prisma/schema.prisma` модель `Product` содержит `ivanovoStock`, `productionStock`, `ivanovoStockUpdatedAt`, `productionStockUpdatedAt`
    - `prisma/schema.prisma` модель `WbCard` содержит `warehouses  WbCardWarehouseStock[]`
    - Файл `prisma/migrations/20260421_phase14_stock/migration.sql` существует и содержит все CREATE TABLE/ALTER TABLE
    - `lib/stock-math.ts` экспортирует `calculateStockMetrics` и `deficitThreshold`
    - `lib/normalize-sku.ts` экспортирует `normalizeSku`
    - `lib/wb-clusters.ts` экспортирует `CLUSTER_FULL_NAMES` (7 keys) и `CLUSTER_ORDER` (7 items)
    - `npm run test -- tests/stock-math.test.ts` → все тесты GREEN (минимум 6)
    - `npm run test -- tests/normalize-sku.test.ts` → все тесты GREEN (минимум 8)
    - `npm run test -- tests/parse-ivanovo-excel.test.ts` → 1 skipped, 0 failed
    - `npx prisma validate` → "The schema is valid"
    - `prisma/seed.ts` содержит upsert для `stock.turnoverNormDays`
  </acceptance_criteria>
  <done>
    Prisma schema + миграция + 3 pure-функции + 3 test файла созданы. `npm run test` → GREEN (stock-math + normalize-sku), parse-ivanovo-excel skipped. Prisma Client regenerated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route rename /inventory → /stock + RBAC + layout + placeholder page</name>
  <files>
    lib/sections.ts,
    lib/section-titles.ts,
    components/layout/nav-items.ts,
    next.config.ts,
    app/(dashboard)/stock/layout.tsx,
    app/(dashboard)/stock/page.tsx,
    app/(dashboard)/inventory/page.tsx
  </files>
  <read_first>
    - lib/sections.ts (строка 11 — "/inventory": "STOCK")
    - lib/section-titles.ts (строка 21 — regex /^\/inventory/)
    - components/layout/nav-items.ts (строка 34 — href: "/inventory")
    - next.config.ts (весь файл — текущая структура)
    - app/(dashboard)/inventory/page.tsx (текущий stub ComingSoon)
    - app/(dashboard)/prices/layout.tsx (паттерн layout с RBAC + tabs placeholder)
    - .planning/phases/14-stock/14-RESEARCH.md §Pattern 5: Route rename
    - .planning/phases/14-stock/14-UI-SPEC.md §Layout страницы /stock
    - .planning/REQUIREMENTS.md STOCK-04, STOCK-05
  </read_first>
  <action>
    ШАГ 1 — `lib/sections.ts` строка 11: заменить `"/inventory": "STOCK"` на `"/stock": "STOCK"`. Ничего другого в файле не менять.

    ШАГ 2 — `lib/section-titles.ts` строка 21: заменить `{ match: /^\/inventory/, title: "Управление остатками" }` на `{ match: /^\/stock/, title: "Управление остатками" }`.

    ШАГ 3 — `components/layout/nav-items.ts` строка 34: заменить `href: "/inventory"` на `href: "/stock"`. Label и icon остаются: `"Управление остатками"`, `"Boxes"`.

    ШАГ 4 — `next.config.ts` — добавить метод `redirects()` в `nextConfig` объект:
    ```typescript
    const nextConfig: NextConfig = {
      output: "standalone",
      // ... существующие поля (typedRoutes, images и т.д.)
      async redirects() {
        return [
          {
            source: "/inventory/:path*",
            destination: "/stock/:path*",
            permanent: true, // 308 permanent redirect
          },
        ]
      },
    }
    ```
    Если уже есть async методы — добавить redirects рядом. Если нет редиректов — создать новый.

    ШАГ 5 — Удалить папку `app/(dashboard)/inventory/` полностью (физически удалить файл page.tsx). Файл `app/(dashboard)/inventory/page.tsx` должен перестать существовать — Next.js redirect из next.config.ts обрабатывает старый URL.

    ШАГ 6 — Создать `app/(dashboard)/stock/layout.tsx` (RSC layout):
    ```typescript
    import { requireSection } from "@/lib/rbac"

    export default async function StockLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      await requireSection("STOCK")

      return (
        <div className="p-6 space-y-6">
          {/* StockTabs будет добавлен в Plan 14-07 */}
          {children}
        </div>
      )
    }
    ```

    Note: `<h1>` не добавляем здесь — проект использует `getSectionTitle` из `section-titles.ts` через общий dashboard header (CLAUDE.md §Header). StockTabs добавим в Plan 14-07.

    ШАГ 7 — Создать `app/(dashboard)/stock/page.tsx` (временный placeholder — Plan 14-06 заменит реальной таблицей):
    ```typescript
    import { requireSection } from "@/lib/rbac"

    export const metadata = {
      title: "Управление остатками",
    }

    export default async function StockPage() {
      await requireSection("STOCK")

      return (
        <div className="space-y-4">
          <p className="text-sm text-muted-foreground">
            Раздел в разработке. Plan 14-06 реализует таблицу Product-level остатков.
          </p>
        </div>
      )
    }
    ```

    ШАГ 8 — Проверить что `middleware.ts` не требует правок (импортирует SECTION_PATHS из sections.ts — автоматически подхватит новый путь).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "app/(dashboard)/stock/layout.tsx" && test -f "app/(dashboard)/stock/page.tsx" && ! test -f "app/(dashboard)/inventory/page.tsx" && grep -q "\"/stock\": \"STOCK\"" lib/sections.ts && grep -q "href: \"/stock\"" components/layout/nav-items.ts && grep -q "source: \"/inventory/:path\\*\"" next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/sections.ts` содержит `"/stock": "STOCK"` (НЕ `"/inventory": "STOCK"`)
    - `lib/section-titles.ts` содержит regex `/^\/stock/` (НЕ `/^\/inventory/`)
    - `components/layout/nav-items.ts` содержит `href: "/stock"`
    - `next.config.ts` содержит `redirects()` метод с source `/inventory/:path*` → destination `/stock/:path*`, permanent: true
    - Файл `app/(dashboard)/inventory/page.tsx` НЕ существует (папка удалена)
    - Файл `app/(dashboard)/stock/layout.tsx` существует, содержит `requireSection("STOCK")`
    - Файл `app/(dashboard)/stock/page.tsx` существует, содержит `requireSection("STOCK")`
    - `npx tsc --noEmit` → 0 ошибок
    - `grep -r "/inventory" app/ lib/ components/` → 0 matches (кроме next.config.ts redirects source)
  </acceptance_criteria>
  <done>
    Route rename завершён. `/stock` открывается через layout + page placeholder. `/inventory/*` → 308 redirect на `/stock/*`. RBAC работает. TypeScript compile clean.
  </done>
</task>

</tasks>

<verification>
**Phase-level checks после завершения Plan 14-01:**

1. `npx prisma validate` — schema валидна
2. `npm run test` — все Phase 14 тесты (stock-math, normalize-sku) GREEN, parse-ivanovo-excel skipped
3. `npx tsc --noEmit` — 0 TypeScript ошибок
4. Локальный dev сервер (`npm run dev`) — `/stock` открывается (requires login + SECTION STOCK), `/inventory` → 308 → `/stock`
5. Навигация в sidebar показывает пункт «Управление остатками» с href `/stock`
6. Wave 0 smoke test результат задокументирован в SUMMARY (HTTP code, решение если блокер)

**Что НЕ в scope этого плана (следующие):**
- Plan 14-02: WbWarehouse seed script
- Plan 14-03: fetchStocksPerWarehouse + /api/wb-sync extension
- Plan 14-04: Excel Иваново (требует real fixture)
- Plan 14-05: Production inline + TurnoverNormInput + WbRefreshButton
- Plan 14-06: Реальная RSC таблица /stock
- Plan 14-07: /stock/wb кластеры + /stock/ozon + deploy + UAT
</verification>

<success_criteria>
- [x] Wave 0 curl smoke test выполнен, пользователь подтвердил результат (HTTP 200/422 = OK, 401/403 = blocker с планом fix)
- [x] Prisma schema содержит WbWarehouse + WbCardWarehouseStock + 4 новых поля Product + WbCard.warehouses relation
- [x] Миграция SQL создана в `prisma/migrations/20260421_phase14_stock/migration.sql` (для VPS deploy)
- [x] `prisma/seed.ts` upsert `stock.turnoverNormDays=37`
- [x] `lib/stock-math.ts` экспортирует `calculateStockMetrics` + `deficitThreshold` (все null-guards)
- [x] `lib/normalize-sku.ts` экспортирует `normalizeSku` (em-dash + padStart + throw на invalid)
- [x] `lib/wb-clusters.ts` экспортирует `CLUSTER_FULL_NAMES` + `CLUSTER_ORDER`
- [x] `tests/stock-math.test.ts` — минимум 6 GREEN
- [x] `tests/normalize-sku.test.ts` — минимум 8 GREEN
- [x] `tests/parse-ivanovo-excel.test.ts` — RED stub skipped (ждёт Plan 14-04 fixture)
- [x] `/inventory` → 308 redirect на `/stock` через `next.config.ts redirects()`
- [x] `app/(dashboard)/stock/layout.tsx` — RSC с `requireSection("STOCK")`
- [x] `app/(dashboard)/stock/page.tsx` — placeholder RSC
- [x] `lib/sections.ts`, `lib/section-titles.ts`, `components/layout/nav-items.ts` — все содержат `/stock` (НЕ `/inventory`)
- [x] `npx tsc --noEmit` → 0 ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/14-stock/14-01-SUMMARY.md` со следующим содержанием:
- Wave 0 curl результат (HTTP код + тело первые 20 строк)
- Решение по токену (OK / blocker + план regen)
- Список созданных файлов
- Список применённых миграций (локально vs pending для VPS)
- Тесты GREEN count + RED stub details
- Ключевые решения (если были отклонения от плана — описать)
</output>
