---
phase: 28-cashflow
plan: 03
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - lib/cashflow-schemas.ts
  - app/actions/cashflow.ts
  - components/finance/CashflowAssumptionsBar.tsx
  - components/finance/CashflowMethodologyDialog.tsx
  - docs/finance-cashflow-methodology.md
  - app/(dashboard)/finance/cashflow/page.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "MANAGE-пользователь редактирует 4 допущения (wbPayoutPct, wbPayoutLagWeeks, opexMonthlyRub, gapThresholdRub) через дебаунснутый бар"
    - "AssumptionsBar рендерится ТОЛЬКО при getSectionRole('FINANCE')==='MANAGE'"
    - "Server action updateCashflowSetting требует requireSection('FINANCE','MANAGE') и zod-валидирует числовые границы"
    - "Изменение допущения → сохранение в AppSetting → router.refresh() → пересчёт RSC"
    - "Кнопка «Как считается» открывает диалог с методологией ПДДС"
    - "docs/finance-cashflow-methodology.md описывает формулы притоков/оттоков/тайминга/налогов"
  artifacts:
    - path: "lib/cashflow-schemas.ts"
      provides: "zod-схемы + ключи + дефолты допущений (pure, без 'use server')"
      contains: "cashflowSettingSchema"
    - path: "app/actions/cashflow.ts"
      provides: "updateCashflowSetting (MANAGE + zod)"
      exports: ["updateCashflowSetting"]
    - path: "components/finance/CashflowAssumptionsBar.tsx"
      provides: "дебаунснутый редактор 4 допущений"
      contains: "router.refresh"
    - path: "components/finance/CashflowMethodologyDialog.tsx"
      provides: "диалог «Как считается»"
      contains: "sm:max-w"
    - path: "docs/finance-cashflow-methodology.md"
      provides: "методология ПДДС"
      contains: "wbPayoutPct"
  key_links:
    - from: "components/finance/CashflowAssumptionsBar.tsx"
      to: "app/actions/cashflow.ts"
      via: "updateCashflowSetting + router.refresh"
      pattern: "updateCashflowSetting"
    - from: "app/(dashboard)/finance/cashflow/page.tsx"
      to: "components/finance/CashflowAssumptionsBar.tsx"
      via: "canManage && <CashflowAssumptionsBar>"
      pattern: "CashflowAssumptionsBar"
---

<objective>
Добавить редактируемые допущения (AssumptionsBar, MANAGE-only) + server actions с zod-валидацией + методологию (docs + диалог). Стадия 3 (финальный видимый деплой + UAT).

Purpose: Дать пользователю управление 4 допущениями ПДДС (payout%, лаг, опекс/мес, порог тревоги) без версионирования — сценарии на лету (D-9). Прозрачность расчёта через методологию. Payout 55% — первое приближение, редактируемое (D-1).

Output: cashflow-schemas.ts (zod), app/actions/cashflow.ts, CashflowAssumptionsBar, CashflowMethodologyDialog, docs, интеграция в page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-cashflow/28-CONTEXT.md
@.planning/phases/28-cashflow/28-PATTERNS.md

<interfaces>
<!-- Паттерны-образцы (уже в кодовой базе) — реплицировать. -->
Из lib/pricing-schemas.ts (образец zod для AppSetting-значений — pure, БЕЗ 'use server'):
```typescript
export const appSettingValueSchema = z.string()... // числовые границы
export function isValidAppSettingKey(key: string): key is AppSettingKey
```
Из app/actions/pricing.ts (образец action):
```typescript
type ActionResult<T = undefined> = { ok: true; data?: T } | { ok: false; error: string }
function handleAuthError(e: unknown): { ok: false; error: string } | null
export async function updateAppSetting(key, value): Promise<ActionResult>  // requireSection + zod + upsert + revalidatePath
```
Из components/prices/GlobalRatesBar.tsx (образец дебаунснутого бара):
```typescript
interface GlobalRatesBarProps { initialRates: ... }
// useRef<Partial<Record<key, timer>>> дебаунс 500ms → startTransition → action → router.refresh()
```
Из lib/rbac.ts: `requireSection("FINANCE", "MANAGE")`, `getSectionRole("FINANCE")`
Из components/finance/BalanceMethodologyDialog.tsx (образец диалога): DialogTrigger render={...}, sm:max-w-Xxl (sm-префикс обязателен, base-ui gotcha)
AppSetting: колонки key/value/updatedAt (createdAt НЕТ).
</interfaces>

<read_first>
- lib/pricing-schemas.ts (образец zod-схем для AppSetting — pure-файл, БЕЗ 'use server', vitest-загружаемый)
- app/actions/pricing.ts:41-139 (ActionResult, handleAuthError, updateAppSetting: requireSection→zod→upsert→revalidatePath)
- components/prices/GlobalRatesBar.tsx (полный образец дебаунснутого бара: useRef таймеры, startTransition, router.refresh)
- components/finance/BalanceMethodologyDialog.tsx (образец диалога «Как считается»: DialogTrigger render, sm:max-w)
- docs/finance-balance-methodology.md (образец структуры/стиля методологии)
- app/(dashboard)/finance/cashflow/page.tsx (создан в 28-02 — добавить canManage + AssumptionsBar + MethodologyDialog)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 394-503 (schemas+action), 452-504 (bar), 686-735 (dialog)
- memory/project_baseui_dialog_gotchas.md (sm-префикс ширины; данные через useEffect(open))
  </read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cashflow-schemas.ts (zod) + app/actions/cashflow.ts (MANAGE)</name>
  <files>lib/cashflow-schemas.ts, app/actions/cashflow.ts</files>
  <read_first>
- lib/pricing-schemas.ts (образец pure zod-файла — НЕ 'use server', экспорт sync-значений для vitest)
- app/actions/pricing.ts:41-139 (ActionResult, handleAuthError, updateAppSetting)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 394-449, 766-777 (action + AppSetting upsert)
  </read_first>
  <action>
1) Создать `lib/cashflow-schemas.ts` — PURE (только `import { z } from "zod"`), БЕЗ "use server" (Next.js 15 'use server' файлы не экспортируют sync-значения; отдельный schema-файл — паттерн pricing-schemas). Экспортировать:
```typescript
import { z } from "zod"

export const CASHFLOW_SETTING_KEYS = [
  "finance.cashflow.wbPayoutPct",
  "finance.cashflow.wbPayoutLagWeeks",
  "finance.cashflow.opexMonthlyRub",
  "finance.cashflow.gapThresholdRub",
] as const
export type CashflowSettingKey = (typeof CASHFLOW_SETTING_KEYS)[number]

export const CASHFLOW_SETTING_DEFAULTS: Record<CashflowSettingKey, number> = {
  "finance.cashflow.wbPayoutPct": 55,
  "finance.cashflow.wbPayoutLagWeeks": 1,
  "finance.cashflow.opexMonthlyRub": 0,
  "finance.cashflow.gapThresholdRub": 0,
}

export function isValidCashflowSettingKey(key: string): key is CashflowSettingKey {
  return (CASHFLOW_SETTING_KEYS as readonly string[]).includes(key)
}

// Границы per-ключ (D-9): payout 0-100, lag 0-8, opex/gap ≥0
const BOUNDS: Record<CashflowSettingKey, { min: number; max: number; int?: boolean }> = {
  "finance.cashflow.wbPayoutPct": { min: 0, max: 100 },
  "finance.cashflow.wbPayoutLagWeeks": { min: 0, max: 8, int: true },
  "finance.cashflow.opexMonthlyRub": { min: 0, max: 1_000_000_000 },
  "finance.cashflow.gapThresholdRub": { min: 0, max: 1_000_000_000 },
}

// Валидирует {key, value:string} → нормализованное строковое значение
export const cashflowSettingSchema = z
  .object({ key: z.string(), value: z.string() })
  .superRefine((data, ctx) => {
    if (!isValidCashflowSettingKey(data.key)) {
      ctx.addIssue({ code: "custom", message: `Неизвестный ключ: ${data.key}` }); return
    }
    const n = Number(data.value)
    const b = BOUNDS[data.key]
    if (!Number.isFinite(n)) { ctx.addIssue({ code: "custom", message: "Значение должно быть числом" }); return }
    if (b.int && !Number.isInteger(n)) ctx.addIssue({ code: "custom", message: "Значение должно быть целым" })
    if (n < b.min || n > b.max) ctx.addIssue({ code: "custom", message: `Диапазон ${b.min}–${b.max}` })
  })
  .transform((data) => ({ key: data.key, value: String(Number(data.value)) }))
```

2) Создать `app/actions/cashflow.ts` — "use server". Реплицировать ActionResult + handleAuthError из pricing.ts. Экспорт:
```typescript
"use server"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"
import { revalidatePath } from "next/cache"
import { cashflowSettingSchema } from "@/lib/cashflow-schemas"

export async function updateCashflowSetting(key: string, value: string): Promise<ActionResult> {
  try {
    await requireSection("FINANCE", "MANAGE")   // D-8
  } catch (e) {
    const authErr = handleAuthError(e); if (authErr) return authErr
    return { ok: false, error: (e as Error).message }
  }
  const parsed = cashflowSettingSchema.safeParse({ key, value })
  if (!parsed.success) return { ok: false, error: parsed.error.issues[0].message }
  await prisma.appSetting.upsert({
    where: { key: parsed.data.key },
    create: { key: parsed.data.key, value: parsed.data.value, updatedAt: new Date() },  // createdAt НЕТ в AppSetting
    update: { value: parsed.data.value, updatedAt: new Date() },
  })
  revalidatePath("/finance/cashflow")
  return { ok: true }
}
```
handleAuthError: UNAUTHORIZED→«Не авторизован», FORBIDDEN→«Нет доступа к разделу «Финансы»».
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'cashflow-schemas|actions/cashflow')" && echo "schemas+action typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "\"use server\"" lib/cashflow-schemas.ts` = 0 (pure — vitest-загружаемый, паттерн pricing-schemas)
- `grep -c "export const cashflowSettingSchema\|export function isValidCashflowSettingKey" lib/cashflow-schemas.ts` = 2
- `grep -c "0, max: 100\|max: 100" lib/cashflow-schemas.ts` ≥ 1 (payout границы) и `grep -c "max: 8" lib/cashflow-schemas.ts` ≥ 1 (лаг)
- `grep -c "\"use server\"" app/actions/cashflow.ts` = 1
- `grep -c "requireSection(\"FINANCE\", \"MANAGE\")" app/actions/cashflow.ts` = 1
- `grep -c "cashflowSettingSchema.safeParse" app/actions/cashflow.ts` = 1
- `grep -c "revalidatePath(\"/finance/cashflow\")" app/actions/cashflow.ts` = 1
- `grep -c "createdAt" app/actions/cashflow.ts` = 0
- `npx tsc --noEmit` без ошибок про эти файлы
  </acceptance_criteria>
  <done>cashflow-schemas.ts — pure zod с per-ключ границами (payout 0-100, лаг 0-8 int, opex/gap ≥0); updateCashflowSetting — MANAGE-гейт + zod + upsert (updatedAt, без createdAt) + revalidatePath; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: CashflowAssumptionsBar + CashflowMethodologyDialog + docs</name>
  <files>components/finance/CashflowAssumptionsBar.tsx, components/finance/CashflowMethodologyDialog.tsx, docs/finance-cashflow-methodology.md</files>
  <read_first>
- components/prices/GlobalRatesBar.tsx (полный образец: useRef таймеры per-поле, дебаунс 500ms, startTransition, router.refresh)
- components/finance/BalanceMethodologyDialog.tsx (образец диалога: DialogTrigger render, sm:max-w, max-h overflow)
- docs/finance-balance-methodology.md (стиль методологии)
- app/actions/cashflow.ts (updateCashflowSetting — Task 1)
- lib/cashflow-schemas.ts (CASHFLOW_SETTING_KEYS/DEFAULTS — Task 1)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 452-504 (bar), 686-735 (dialog)
  </read_first>
  <action>
1) `components/finance/CashflowAssumptionsBar.tsx` — "use client". Props `{ initialSettings: Record<CashflowSettingKey, number> }`. Реплицировать GlobalRatesBar:
- `const timersRef = useRef<Partial<Record<CashflowSettingKey, ReturnType<typeof setTimeout>>>>({})`
- state values (строки), handleChange(key, value): setValues + clearTimeout(existing) + setTimeout(500) → startTransition(async () => { const r = await updateCashflowSetting(key, value); if (r.ok) { toast.success("Настройка сохранена"); router.refresh() } else toast.error(r.error) })
- 4 поля (D-9):
```typescript
const SETTINGS = [
  { key: "finance.cashflow.wbPayoutPct",     label: "Выплата WB",    unit: "%",   step: 1 },
  { key: "finance.cashflow.wbPayoutLagWeeks", label: "Лаг (нед.)",    unit: "нед", step: 1 },
  { key: "finance.cashflow.opexMonthlyRub",  label: "Опекс/мес",     unit: "₽",   step: 100000 },
  { key: "finance.cashflow.gapThresholdRub", label: "Порог тревоги", unit: "₽",   step: 100000 },
] as const
```
- Обёртка Card/border, каждое поле: Label + native `<input type="number">` (проект-конвенция) + unit. Компонент-контейнер `flex flex-wrap gap-3` или Card.

2) `components/finance/CashflowMethodologyDialog.tsx` — "use client". Реплицировать BalanceMethodologyDialog:
- DialogTrigger `render={<Button variant="outline" size="sm" className="gap-1.5"><HelpCircle className="h-3.5 w-3.5" />Как считается</Button>}` (render-prop, НЕ asChild)
- DialogContent `className="sm:max-w-3xl"` (sm-префикс обязателен — base-ui gotcha)
- Внутри `max-h-[70vh] overflow-y-auto` секции: Притоки WB (выкупы×payout%, тайминг понедельник+лаг), Оттоки (закупки реал+виртуальные, кредиты, налоги 7%+1% конец квартала, опекс равномерно), Старт (банк RUR+касса консолидированно), Разрыв (остаток<порога), оговорка «приближённо — payout 55% первое приближение, уточняется». Контент из docs (п.3).
- Данные НЕ грузятся (статический текст) — useEffect(open) не нужен, но НЕ использовать onOpenChange для загрузки.

3) `docs/finance-cashflow-methodology.md` — методология (стиль docs/finance-balance-methodology.md):
- Формула ядра: остаток(d) = остаток(d−1) + притоки(d) − оттоки(d), старт = банк(RUR)+касса
- Притоки WB: плановые выкупы × wbPayoutPct/100 (дефолт 55% net-to-bank = forPay ~66% − реклама/ДРР ~12%), тайминг = понедельник недели выкупа +7д + wbPayoutLagWeeks×7
- Оттоки: реальные закупки (PurchasePayment PLANNED, amountRub приоритет), виртуальные (pdds-feed, анти-двойной счёт CONVERTED), кредиты (тело+%), налоги (7%+1% конец квартала — упрощение), опекс (константа/мес ÷ дни)
- Консолидация юрлиц (D-6), горизонт H2-2026, порог тревоги
- Ограничения v1: payout — первое приближение (v2 per-товар из юнит-экономики), налоги упрощены до конца квартала, реклама зашита в коэффициент
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'CashflowAssumptionsBar|CashflowMethodologyDialog')" && echo "bar+dialog typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "\"use client\"" components/finance/CashflowAssumptionsBar.tsx` = 1
- `grep -c "router.refresh" components/finance/CashflowAssumptionsBar.tsx` ≥ 1
- `grep -c "updateCashflowSetting" components/finance/CashflowAssumptionsBar.tsx` ≥ 1
- `grep -c "setTimeout" components/finance/CashflowAssumptionsBar.tsx` ≥ 1 (дебаунс)
- `grep -c "wbPayoutPct\|wbPayoutLagWeeks\|opexMonthlyRub\|gapThresholdRub" components/finance/CashflowAssumptionsBar.tsx` ≥ 4
- `grep -c "sm:max-w" components/finance/CashflowMethodologyDialog.tsx` ≥ 1 (sm-префикс)
- `grep -c "asChild" components/finance/CashflowMethodologyDialog.tsx` = 0 (render-prop, не asChild)
- `grep -c "wbPayoutPct\|55\|понедельник" docs/finance-cashflow-methodology.md` ≥ 2
- `grep -ci "первое приближение\|упрощени" docs/finance-cashflow-methodology.md` ≥ 1
- `npx tsc --noEmit` без ошибок про эти файлы
  </acceptance_criteria>
  <done>AssumptionsBar — дебаунснутый (500ms) редактор 4 допущений → updateCashflowSetting → router.refresh; MethodologyDialog — sm:max-w, render-prop, статический контент; docs описывает притоки/оттоки/тайминг/ограничения v1; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 3: Интеграция бара + диалога в page.tsx (canManage)</name>
  <files>app/(dashboard)/finance/cashflow/page.tsx</files>
  <read_first>
- app/(dashboard)/finance/cashflow/page.tsx (создан 28-02 — добавить canManage + бар + диалог)
- components/finance/CashflowAssumptionsBar.tsx, CashflowMethodologyDialog.tsx (Task 2)
- lib/cashflow-schemas.ts (CASHFLOW_SETTING_KEYS/DEFAULTS для чтения initialSettings)
- app/(dashboard)/finance/balance/page.tsx (образец getSectionRole → canManage)
  </read_first>
  <action>
Дополнить `app/(dashboard)/finance/cashflow/page.tsx` (созданный в 28-02), НЕ переписывая заново:
1) Импорты: `getSectionRole` (добавить к requireSection), `CashflowAssumptionsBar`, `CashflowMethodologyDialog`, `CASHFLOW_SETTING_KEYS`, `CASHFLOW_SETTING_DEFAULTS`, `isValidCashflowSettingKey` из "@/lib/cashflow-schemas".
2) После requireSection: `const canManage = (await getSectionRole("FINANCE")) === "MANAGE"`.
3) Прочитать значения допущений: расширить существующий appSetting.findMany, добавив CASHFLOW_SETTING_KEYS в `key: { in: [...] }`. Собрать `initialSettings: Record<CashflowSettingKey, number>`: для каждого ключа `Number(map.get(key)) ?? CASHFLOW_SETTING_DEFAULTS[key]` (при NaN — дефолт).
4) В блоке рендера (не в пустом состоянии) — в строке над versionStale добавить панель управления:
```tsx
<div className="flex items-center gap-2 flex-wrap">
  <CashflowMethodologyDialog />
  {/* granularity switcher (уже есть из 28-02) */}
</div>
{canManage && <CashflowAssumptionsBar initialSettings={initialSettings} />}
```
Разместить `<CashflowMethodologyDialog />` рядом с granularity switcher; `<CashflowAssumptionsBar>` — только при canManage (D-8/D-9, MANAGE-only рендер; server action всё равно перепроверяет MANAGE). Пустое состояние (нет активной версии) — НЕ показывать бар/диалог.

СТАТИЧЕСКИЕ классы (RSC trap — НЕ buttonVariants).
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npm run build 2>&1 | grep -E "finance/cashflow|Failed to compile|error" | head -10; test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep 'finance/cashflow/page')" && echo "page typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "getSectionRole(\"FINANCE\")" app/(dashboard)/finance/cashflow/page.tsx` ≥ 1
- `grep -c "canManage" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2 (объявление + условный рендер)
- `grep -c "CashflowAssumptionsBar" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2 (импорт + рендер)
- `grep -c "CashflowMethodologyDialog" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2
- `grep -c "canManage &&" app/(dashboard)/finance/cashflow/page.tsx` ≥ 1 (MANAGE-only бар)
- `grep -c "CASHFLOW_SETTING_KEYS\|CASHFLOW_SETTING_DEFAULTS" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2
- `grep -c "buttonVariants" app/(dashboard)/finance/cashflow/page.tsx` = 0
- `npm run build` компилируется без ошибок
  </acceptance_criteria>
  <done>page.tsx: canManage из getSectionRole, initialSettings из AppSetting (fallback дефолты), MethodologyDialog в панели, AssumptionsBar только при MANAGE; пустое состояние без бара; build зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → server action updateCashflowSetting | недоверенный {key, value} — валидируется allow-list + zod-границы |
| VIEW-пользователь → редактирование допущений | бар не рендерится, но клиент может попытаться вызвать action напрямую |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Elevation | read-only (VIEW) юзер меняет финансовые допущения | mitigate | двойная защита: AssumptionsBar рендерится только при getSectionRole==="MANAGE" (D-8); server action updateCashflowSetting всё равно requireSection("FINANCE","MANAGE") — прямой вызов из VIEW-сессии отклоняется на сервере |
| T-28-08 | Tampering | инъекция произвольного ключа/значения в AppSetting | mitigate | isValidCashflowSettingKey allow-list (4 ключа) + cashflowSettingSchema с per-ключ числовыми границами (payout 0-100, лаг 0-8 int, opex/gap 0..1e9) перед upsert; невалидные значения отклоняются, не пишутся |
| T-28-09 | Tampering | обход zod через прямой Prisma-вызов | accept | updateCashflowSetting — единственная точка записи cashflow-настроек; иных write-путей к finance.cashflow.* ключам в фазе нет |
</threat_model>

<verification>
- `npm run build` — компилируется без ошибок
- `npx tsc --noEmit` — ноль ошибок
- `npm run test` — полный suite green (движок 28-01 не тронут, sales-plan golden цел iu=438_068_120)
- cashflow-schemas.ts — pure (без 'use server'), zod-границы корректны
- Двойная RBAC-защита: рендер (canManage) + server action (requireSection MANAGE)
</verification>

<success_criteria>
- MANAGE-пользователь редактирует 4 допущения дебаунснуто → пересчёт RSC
- VIEW не видит бар; server action отклоняет VIEW
- zod-валидация числовых границ (D-9)
- Методология доступна («Как считается» + docs)
- payout 55% редактируем (D-1 первое приближение)
</success_criteria>

<output>
После завершения создать `.planning/phases/28-cashflow/28-03-SUMMARY.md`
</output>
