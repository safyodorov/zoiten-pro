---
phase: 28-cashflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/finance-cashflow/types.ts
  - lib/finance-cashflow/engine.ts
  - lib/finance-cashflow/data.ts
  - tests/finance-cashflow-engine.test.ts
  - prisma/migrations/20260705_phase28_cashflow_seed/migration.sql
autonomous: true
requirements: []
must_haves:
  truths:
    - "computeCashflow даёт дневной ряд: остаток(d) = остаток(d-1) + притоки(d) − оттоки(d), старт = startingBalance"
    - "Приток WB агрегируется по дню выплаты (понедельник недели выкупа + 7д + lagWeeks×7) × wbPayoutPct/100"
    - "Разрыв детектируется когда balanceEnd < gapThresholdRub; firstGapDate = первая такая дата"
    - "payoutModel сменная: кастомная payoutFn (date, buyoutsRub, byProduct?)→rub применяется вместо коэффициента (задел под v2 per-product)"
    - "loadCashflowInputs собирает притоки/виртуальные/закупки/кредиты/налоги/старт-баланс из БД через DI (db: PrismaClient)"
    - "Анти-двойной счёт: virtualPayments из getPlannedVirtualPayments (CONVERTED/DISMISSED исключены на уровне контракта), versionStale проброшен"
    - "AppSetting finance.cashflow.* засеяны: wbPayoutPct=55, wbPayoutLagWeeks=1, opexMonthlyRub=0, gapThresholdRub=0"
  artifacts:
    - path: "lib/finance-cashflow/types.ts"
      provides: "CashflowInputs, CashflowDay, CashflowBucket, CashflowResult, PayoutModelType"
      contains: "export interface CashflowInputs"
    - path: "lib/finance-cashflow/engine.ts"
      provides: "computeCashflow(inputs, granularity) — PURE, ноль Prisma/React"
      exports: ["computeCashflow"]
    - path: "lib/finance-cashflow/data.ts"
      provides: "loadCashflowInputs(db, params) — DI-загрузчик"
      exports: ["loadCashflowInputs", "LoadCashflowParams"]
    - path: "tests/finance-cashflow-engine.test.ts"
      provides: "golden + тайминг + gap + анти-двойной счёт + сменная payout-модель"
      contains: "describe(\"computeCashflow"
    - path: "prisma/migrations/20260705_phase28_cashflow_seed/migration.sql"
      provides: "AppSetting-сид finance.cashflow.*"
      contains: "finance.cashflow.wbPayoutPct"
  key_links:
    - from: "lib/finance-cashflow/data.ts"
      to: "lib/sales-plan/pdds-feed.ts"
      via: "getPlannedRevenueSeries + getPlannedVirtualPayments"
      pattern: "getPlanned(Revenue|Virtual)"
    - from: "lib/finance-cashflow/engine.ts"
      to: "lib/date-buckets.ts"
      via: "bucketKey/bucketLabel для агрегации бакетов"
      pattern: "bucket(Key|Label)"
---

<objective>
Реализовать pure-движок ПДДС + DI-загрузчик данных + golden-тесты + AppSetting-сид. Невидимый деплой (нет UI-изменений): вычислительное ядро, потребляемое RSC-страницей в 28-02.

Purpose: Второй финансовый отчёт (Баланс → **ОДДС** → ОПиУ) — прогноз денежных потоков на горизонте плана продаж H2-2026: когда придут выплаты WB, когда уйдут закупки/кредиты/налоги/опекс, где кассовые разрывы. Движок изолирован (pure) и тестируем, payout-модель спроектирована сменной под v2 (per-product из юнит-экономики, D-1).

Output: `lib/finance-cashflow/{types,engine,data}.ts` + `tests/finance-cashflow-engine.test.ts` + сид-миграция AppSetting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-cashflow/28-CONTEXT.md
@.planning/phases/28-cashflow/28-RESEARCH.md
@.planning/phases/28-cashflow/28-PATTERNS.md

<interfaces>
<!-- Контракты потребляемых модулей. Использовать напрямую — исследование кодовой базы НЕ требуется. -->

Из lib/sales-plan/pdds-feed.ts (НЕ менять — потреблять как есть):
```typescript
export interface PlannedRevenueDay {
  date: string          // "YYYY-MM-DD"
  buyoutsRub: number    // Σ плановых выкупов ₽ по всем товарам за день
  buyoutsUnits: number
  byProduct: Array<{ productId: string; buyoutsRub: number; buyoutsUnits: number }>
}
export async function getPlannedRevenueSeries(db: PrismaClient, versionId: string): Promise<PlannedRevenueDay[]>

export interface VpPaymentRub {
  type: "DEPOSIT" | "BALANCE"
  dueDate: string        // "YYYY-MM-DD"
  amount: number         // в валюте закупки
  currency: string
  amountRub: number      // сконвертировано в ₽ (forward-fill курса)
  rateUsed: number
  rateApproximate: boolean
  vpId: string
  productId: string
}
export interface PlannedVirtualPaymentsResult {
  payments: VpPaymentRub[]        // CONVERTED/DISMISSED УЖЕ исключены (анти-двойной счёт)
  versionStale: boolean           // true если VP сменила статус → предупреждение в UI
  convertedVpIds: string[]
  dismissedVpIds: string[]
}
export async function getPlannedVirtualPayments(db: PrismaClient, versionId: string): Promise<PlannedVirtualPaymentsResult>
```

Из lib/balance-data.ts (НЕ менять):
```typescript
export async function getBankBalanceAsOf(accountId: string, asOf: Date): Promise<number | null>
export interface RateAsOf { code: string; rateToRub: number; asOf: Date; approximate: boolean }
export async function getRateForDate(code: string, asOf: Date): Promise<RateAsOf | null>
```
ВАЖНО: getRateForDate возвращает объект с полем `rateToRub` (НЕ `rate`). У getBankBalanceAsOf валюта счёта в БД = "RUR" (НЕ "RUB").

Из lib/balance-math.ts (НЕ менять):
```typescript
export function computeQuarterAccrual(buyouts: number, vatPct: number, incomeTaxPct: number): number
```

Из lib/date-buckets.ts (НЕ менять):
```typescript
export type Granularity = "day" | "week" | "month" | "quarter" | "halfyear" | "year"
export function bucketKey(date: Date, granularity: Granularity): string    // "2026-07" / "2026-W28" / "2026-07-01"
export function bucketLabel(key: string, granularity: Granularity): string
```

Из lib/finance-model/engine.ts:66-74 (формула wbCashDay — legacy, СКОПИРОВАТЬ в новый engine, НЕ импортировать):
```typescript
// number-based версия (legacy). В новом engine пересобрать под ISO-строки (см. action task 2).
function wbCashDay(startMs, saleDay, payoutWeeks): number {
  const date = new Date(startMs + saleDay * MS_PER_DAY)
  const dow = date.getUTCDay()
  const daysSinceMonday = (dow + 6) % 7
  const weekMonday = saleDay - daysSinceMonday
  const reportMonday = weekMonday + 7
  return reportMonday + payoutWeeks * 7
}
```

Prisma-модели (для data.ts):
```
PurchasePayment { type PaymentType, amount Decimal, amountRub Decimal?, currency String, dueDate DateTime, status PaymentStatus } — status=PLANNED
LoanPayment { loanId, date DateTime @db.Date, principal Decimal, interest Decimal }
CashEntry { date DateTime @db.Date, direction CashDirection (INCOME|EXPENSE), amount Decimal }
BankAccount { id, currency String } — RUR-счета: currency === "RUR"
AppSetting { key String @id, value String, updatedAt DateTime } — createdAt НЕТ
```
</interfaces>

<read_first>
- lib/sales-plan/types.ts — образец структуры типов (сериализуемые, ISO-строки, ноль импортов)
- lib/sales-plan/engine.ts — образец pure-движка (симуляция дневного ряда, firstStockoutDate детекция ~строки 272-282)
- lib/sales-plan/data.ts — образец DI-загрузчика (принимает db, Promise.all запросы)
- lib/sales-plan/pdds-feed.ts — контракт притоков/виртуальных (сигнатуры выше, НЕ менять)
- lib/balance-data.ts:346-440 — образец сборки старт-баланса (RUR bankAccounts + cashEntry.groupBy)
- lib/balance-math.ts:26-34 — computeQuarterAccrual сигнатура
- lib/date-buckets.ts — bucketKey/bucketLabel
- lib/finance-model/engine.ts:61-74 — формула wbCashDay для копирования
- tests/sales-plan-engine.test.ts — образец структуры vitest golden-теста
- prisma/migrations/20260410_app_setting_defect_rate/migration.sql — образец INSERT AppSetting ON CONFLICT
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + сид-миграция AppSetting (контракты первыми)</name>
  <files>lib/finance-cashflow/types.ts, prisma/migrations/20260705_phase28_cashflow_seed/migration.sql</files>
  <read_first>
- lib/sales-plan/types.ts (образец: сериализуемые интерфейсы, ISO-строки, ноль импортов Prisma/React)
- prisma/migrations/20260410_app_setting_defect_rate/migration.sql (образец INSERT ON CONFLICT)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 41-108 (точная структура типов)
  </read_first>
  <behavior>
    - CashflowInputs содержит все входы движка сериализуемо (string/number/boolean, ноль Date/Decimal)
    - PayoutModelType = 'coefficient' | 'per-product' — сменная модель (D-1)
    - CashflowResult содержит days[], buckets[], startingBalance, minBalance, firstGapDate, netTotal
    - Сид-миграция вставляет 4 ключа finance.cashflow.* с ON CONFLICT DO NOTHING
  </behavior>
  <action>
1) Создать `lib/finance-cashflow/types.ts` — pure, ноль импортов кроме `import type { Granularity } from "@/lib/date-buckets"`. Экспортировать ВЕРБАТИМ (значения скопированы из 28-PATTERNS.md §types.ts):

```typescript
// lib/finance-cashflow/types.ts
//
// Публичные интерфейсы движка ПДДС (Phase 28). Pure — ноль импортов Prisma/React/Next.
// Входы сериализуемые (string/number/boolean/null) — используются и на сервере (RSC) и на клиенте.

import type { Granularity } from "@/lib/date-buckets"

// Сменная payout-модель (D-1 из CONTEXT — v1 coefficient, v2 per-product из юнит-экономики)
export type PayoutModelType = "coefficient" | "per-product"

export interface CashflowInputs {
  horizonFrom: string        // "2026-07-01"
  horizonTo: string          // "2026-12-31"
  startingBalance: number    // банк(RUR) + касса на horizonFrom (₽)
  gapThresholdRub: number    // порог тревоги (дефолт 0)
  // byProduct — задел v2 per-product payout (D-1); v1 НЕ заполняет (undefined)
  revenueSeries: Array<{ date: string; buyoutsRub: number; byProduct?: Array<{ productId: string; buyoutsRub: number }> }>   // из getPlannedRevenueSeries
  wbPayoutPct: number        // 55 (% net-to-bank от buyoutsRub)
  wbPayoutLagWeeks: number   // 1
  payoutModel: PayoutModelType  // v1 = "coefficient"
  realPurchasePayments: Array<{ date: string; amountRub: number }>  // PurchasePayment PLANNED
  virtualPayments: Array<{ date: string; amountRub: number }>       // getPlannedVirtualPayments (CONVERTED/DISMISSED уже исключены)
  loanPayments: Array<{ date: string; amountRub: number }>          // principal + interest
  taxPayments: Array<{ date: string; amountRub: number }>           // computeQuarterAccrual per квартал → дата уплаты
  opexMonthlyRub: number     // раскладывается равномерно ÷ дни месяца
  actualBalanceSeries?: Array<{ date: string; balanceRub: number }> // факт-ряд остатка (D-4)
  versionStale?: boolean
}

export interface CashflowDay {
  date: string
  wbPayoutRub: number
  realPurchaseRub: number
  virtualPurchaseRub: number
  loanRub: number
  taxRub: number
  opexRub: number
  totalInflow: number
  totalOutflow: number
  netFlow: number
  balanceEnd: number
  isGap: boolean
  actualBalance: number | null   // факт-остаток за прошедшие дни, null для будущих (D-4)
}

export interface CashflowBucket {
  key: string
  label: string
  wbPayoutRub: number
  realPurchaseRub: number
  virtualPurchaseRub: number
  loanRub: number
  taxRub: number
  opexRub: number
  totalInflow: number
  totalOutflow: number
  netFlow: number
  balanceEnd: number       // остаток на конец последнего дня бакета
  hasGap: boolean
}

export interface CashflowResult {
  days: CashflowDay[]
  buckets: CashflowBucket[]
  granularity: Granularity
  startingBalance: number
  minBalance: number
  firstGapDate: string | null
  netTotal: number
  versionStale: boolean
}
```

2) Создать `prisma/migrations/20260705_phase28_cashflow_seed/migration.sql` — ТОЛЬКО AppSetting-сид (Prisma-миграции схемы в фазе НЕТ). Колонки key/value/updatedAt (createdAt в AppSetting НЕТ). Значения: wbPayoutPct=55, wbPayoutLagWeeks=1, opexMonthlyRub=0, gapThresholdRub=0.

```sql
-- Phase 28 ПДДС — сид глобальных допущений (AppSetting KV). Схема НЕ меняется.
INSERT INTO "AppSetting" ("key", "value", "updatedAt")
VALUES
  ('finance.cashflow.wbPayoutPct', '55', NOW()),
  ('finance.cashflow.wbPayoutLagWeeks', '1', NOW()),
  ('finance.cashflow.opexMonthlyRub', '0', NOW()),
  ('finance.cashflow.gapThresholdRub', '0', NOW())
ON CONFLICT ("key") DO NOTHING;
```
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "finance-cashflow/types" | grep -q "^0$" && grep -q "ON CONFLICT" prisma/migrations/20260705_phase28_cashflow_seed/migration.sql && [ "$(grep -c "finance.cashflow." prisma/migrations/20260705_phase28_cashflow_seed/migration.sql)" -ge 4 ] && echo "types + seed OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "export interface CashflowInputs" lib/finance-cashflow/types.ts` = 1
- `grep -c "export type PayoutModelType" lib/finance-cashflow/types.ts` = 1
- `grep -c "actualBalance" lib/finance-cashflow/types.ts` ≥ 2 (в CashflowDay + инпут-серии)
- `grep -c "import.*prisma\|from \"@prisma\|from \"react\|from \"next" lib/finance-cashflow/types.ts` = 0 (pure)
- `grep -c "finance.cashflow.wbPayoutPct.*55\|'55'" prisma/migrations/20260705_phase28_cashflow_seed/migration.sql` ≥ 1
- `grep -c "ON CONFLICT" prisma/migrations/20260705_phase28_cashflow_seed/migration.sql` = 1
- `grep -c "createdAt" prisma/migrations/20260705_phase28_cashflow_seed/migration.sql` = 0
- `npx tsc --noEmit` не выдаёт ошибок про lib/finance-cashflow/types.ts
  </acceptance_criteria>
  <done>types.ts экспортирует 5 интерфейсов + PayoutModelType, pure (ноль запрещённых импортов); сид-миграция вставляет 4 ключа ON CONFLICT DO NOTHING без createdAt; tsc чист.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engine.ts (pure computeCashflow) + golden-тесты</name>
  <files>lib/finance-cashflow/engine.ts, tests/finance-cashflow-engine.test.ts</files>
  <read_first>
- lib/finance-cashflow/types.ts (создан в Task 1 — импортировать типы)
- lib/sales-plan/engine.ts (образец pure-симуляции дневного ряда + firstStockoutDate детекция)
- lib/finance-model/engine.ts:61-74 (формула wbCashDay для копирования)
- lib/date-buckets.ts (bucketKey/bucketLabel)
- tests/sales-plan-engine.test.ts (образец vitest golden структуры)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 136-198, 276-308 (шаблоны engine + тестов)
  </read_first>
  <behavior>
    - Test 1 (conservation): остаток последнего дня = startingBalance + Σ всех netFlow за горизонт
    - Test 2 (тайминг): выкупы 2026-07-01 (Ср) при lagWeeks=1 → выплата 2026-07-13 (Пн), wbPayoutRub на эту дату > 0; на 2026-07-01 wbPayoutRub = 0
    - Test 3 (gap): при большом оттоке balanceEnd < gapThresholdRub → isGap=true, firstGapDate = первая такая дата
    - Test 4 (анти-двойной счёт): virtualPayments передаются как есть (движок их НЕ переоценивает по статусу), сумма оттока = Σ переданных virtualPayments
    - Test 5 (сменная payout-модель): computeCashflow принимает опциональную payoutFn; кастомная (_d, rub)=>rub*0.9 применяется вместо wbPayoutPct
  </behavior>
  <action>
1) Создать `lib/finance-cashflow/engine.ts` — PURE (импорты ТОЛЬКО типов + `bucketKey`/`bucketLabel`/`Granularity` из `@/lib/date-buckets`). Ноль Prisma/React/Next.

Скопировать формулу wbCashDay из lib/finance-model/engine.ts:66-74, но пересобрать под ISO-строки (вход/выход "YYYY-MM-DD", UTC):
```typescript
/** День прихода денег WB за выкупы в неделю, содержащую saleDateIso.
 * reportMonday = понедельник недели выкупа + 7д; cashDate = reportMonday + lagWeeks×7.
 * Формула скопирована из lib/finance-model/engine.ts:wbCashDay (legacy НЕ импортировать). */
function wbCashDay(saleDateIso: string, wbPayoutLagWeeks: number): string {
  const date = new Date(saleDateIso + "T00:00:00Z")
  const dow = date.getUTCDay()                          // 0=Вс..6=Сб
  const daysSinceMonday = (dow + 6) % 7
  const weekMondayMs = date.getTime() - daysSinceMonday * 86_400_000
  const reportMondayMs = weekMondayMs + 7 * 86_400_000
  const cashMs = reportMondayMs + wbPayoutLagWeeks * 7 * 86_400_000
  return new Date(cashMs).toISOString().slice(0, 10)
}
```

Реализовать сигнатуру со СМЕННОЙ payout-моделью (D-1 — инъекция функции; опциональный 3-й аргумент, задел под v2 без переписывания движка). Третий параметр byProduct опционален — v2 (per-product из pricing-math) получит разбивку по товарам без смены типа:
```typescript
export type PayoutFn = (
  date: string,
  buyoutsRub: number,
  byProduct?: Array<{ productId: string; buyoutsRub: number }>,
) => number

export function computeCashflow(
  inputs: CashflowInputs,
  granularity: Granularity = "month",
  payoutFn?: PayoutFn,   // v2: per-product из pricing-math; v1 undefined → coefficient
): CashflowResult
```

Алгоритм:
- `effectivePayout: PayoutFn = payoutFn ?? ((_d, rub) => rub * (inputs.wbPayoutPct / 100))`
- Построить `wbSchedule: Map<string, number>`: для каждой строки revenueSeries → cashDate = wbCashDay(date, lagWeeks); прибавить `effectivePayout(row.date, row.buyoutsRub, row.byProduct)` в бакет cashDate (byProduct прокидывается как есть — v1 undefined).
- Индексы Map<isoDate, number> для realPurchasePayments, virtualPayments, loanPayments, taxPayments (суммирование по дате).
- Опекс: для каждого дня d — `opexMonthlyRub / (число дней в месяце d)`.
- Факт-ряд: Map<isoDate, number> из actualBalanceSeries (может отсутствовать).
- Итерация по дням [horizonFrom..horizonTo] включительно (шаг 86_400_000 UTC): собрать CashflowDay; `balanceEnd = prevBalance + netFlow` (prevBalance стартует с startingBalance); `isGap = balanceEnd < gapThresholdRub`; `actualBalance = actualMap.get(d) ?? null`.
- KPI: minBalance = min всех balanceEnd; firstGapDate = первая дата с isGap; netTotal = Σ netFlow.
- Бакеты: сгруппировать дни через `bucketKey(new Date(d+"T00:00:00Z"), granularity)`; суммировать потоки; balanceEnd бакета = balanceEnd последнего дня бакета; hasGap = любой день бакета isGap; label = bucketLabel(key, granularity).
- Вернуть CashflowResult с granularity и versionStale (проброс `inputs.versionStale ?? false`).

2) Создать `tests/finance-cashflow-engine.test.ts` — 5 тестов (см. behavior). Импорты `import { describe, it, expect } from "vitest"`; `import { computeCashflow } from "@/lib/finance-cashflow/engine"`; тип из "@/lib/finance-cashflow/types". Собрать goldenInputs (образец 28-PATTERNS.md строки 282-296). В Test 5 передать 3-м аргументом `(_d, rub) => rub * 0.9` и проверить, что сумма wbPayoutRub = 0.9 × Σ buyoutsRub (а не 0.55×).
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npm run test -- finance-cashflow-engine 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- `npm run test -- finance-cashflow-engine` — все 5 тестов green (0 failed)
- `grep -c "import.*@prisma\|from \"react\|from \"next" lib/finance-cashflow/engine.ts` = 0 (pure)
- `grep -c "export function computeCashflow" lib/finance-cashflow/engine.ts` = 1
- `grep -c "payoutFn\|PayoutFn" lib/finance-cashflow/engine.ts` ≥ 2 (сменная модель D-1)
- `grep -c "wbCashDay" lib/finance-cashflow/engine.ts` ≥ 2 (определение + вызов)
- Тест «тайминг»: `grep -c "2026-07-13" tests/finance-cashflow-engine.test.ts` ≥ 1
- Тест «сменная payout-модель»: в тесте есть 3-й аргумент-функция и проверка её применения
  </acceptance_criteria>
  <done>computeCashflow — pure, реализует дневную симуляцию + бакеты + KPI + gap-детекцию + сменную payout-модель; 5 тестов green; ноль Prisma/React импортов.</done>
</task>

<task type="auto">
  <name>Task 3: data.ts (DI-загрузчик loadCashflowInputs)</name>
  <files>lib/finance-cashflow/data.ts</files>
  <read_first>
- lib/finance-cashflow/types.ts (CashflowInputs — цель загрузчика)
- lib/sales-plan/data.ts:1-40 (образец DI: db: PrismaClient, Promise.all)
- lib/sales-plan/pdds-feed.ts:155-346 (сигнатуры getPlannedRevenueSeries/getPlannedVirtualPayments)
- lib/balance-data.ts:346-440 (сборка старт-баланса: RUR bankAccounts + cashEntry.groupBy + getBankBalanceAsOf)
- lib/balance-math.ts:26-34 (computeQuarterAccrual)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 223-268 (шаблон loadCashflowInputs + PurchasePayment конвертация)
  </read_first>
  <action>
Создать `lib/finance-cashflow/data.ts` — принимает `db: PrismaClient` (DI, НЕ импортирует глобальный prisma напрямую в сигнатуру; для getBankBalanceAsOf/getRateForDate — они уже используют глобальный prisma, вызывать их как есть). Ноль импортов React/Next.

Импорты:
```typescript
import type { PrismaClient } from "@prisma/client"
import { getPlannedRevenueSeries, getPlannedVirtualPayments } from "@/lib/sales-plan/pdds-feed"
import { getBankBalanceAsOf, getRateForDate } from "@/lib/balance-data"
import { computeQuarterAccrual } from "@/lib/balance-math"
import type { CashflowInputs } from "./types"
```

Экспортировать:
```typescript
export interface LoadCashflowParams {
  versionId: string
  horizonFrom: string   // "2026-07-01"
  horizonTo: string     // "2026-12-31"
}
export async function loadCashflowInputs(db: PrismaClient, params: LoadCashflowParams): Promise<CashflowInputs>
```

Шаги (все VERIFIED из read_first-файлов):
1) AppSetting: `db.appSetting.findMany({ where: { key: { in: [...] } } })` для ключей finance.cashflow.wbPayoutPct/wbPayoutLagWeeks/opexMonthlyRub/gapThresholdRub + finance.vatPct + finance.incomeTaxPct. Собрать Map. Дефолты: `Number(map.get("finance.cashflow.wbPayoutPct") ?? "55")`, lagWeeks `?? "1"`, opex `?? "0"`, gap `?? "0"`, vatPct `?? "7"`, incomeTaxPct `?? "1"`.
2) Старт-баланс (консолидированно, D-6): `getBankBalanceAsOf(acc.id, new Date(horizonFrom+"T00:00:00Z"))` по всем `db.bankAccount.findMany({ where: { currency: "RUR" }, select: { id: true } })` — суммировать не-null (валюта в БД "RUR", НЕ "RUB"). Касса: `db.cashEntry.groupBy({ by: ["direction"], where: { date: { lte: new Date(horizonFrom+"T00:00:00Z") } }, _sum: { amount: true } })` → `Σ(INCOME) − Σ(EXPENSE)`. startingBalance = bankRur + cash.
3) revenueSeries: `getPlannedRevenueSeries(db, versionId)` → map в `{ date, buyoutsRub }`, отфильтровать по [horizonFrom..horizonTo].
4) virtualPayments + versionStale: `getPlannedVirtualPayments(db, versionId)` → `result.payments.map(p => ({ date: p.dueDate, amountRub: p.amountRub }))` (в горизонте); versionStale = result.versionStale.
5) realPurchasePayments: `db.purchasePayment.findMany({ where: { status: "PLANNED", dueDate: { gte, lte } } })`. Для каждого: `amountRub = payment.amountRub != null ? Number(payment.amountRub) : Number(payment.amount) * ((await getRateForDate(payment.currency, payment.dueDate))?.rateToRub ?? 1)` (приоритет amountRub — паттерн balance-data B1 / 260704-go2; поле курса = rateToRub). Дата = dueDate.toISOString().slice(0,10).
6) loanPayments: `db.loanPayment.findMany({ where: { date: { gte, lte } } })` → `{ date: p.date ISO, amountRub: Number(p.principal) + Number(p.interest) }`.
7) taxPayments: для каждого квартала в горизонте — Σ buyoutsRub квартала (из revenueSeries) → `computeQuarterAccrual(qtrBuyouts, vatPct, incomeTaxPct)`; дата уплаты = последний день квартала (упрощение v1). Кварталы H2-2026: Q3 конец 2026-09-30, Q4 конец 2026-12-31.
8) opexMonthlyRub — просто прокинуть значение (движок раскладывает по дням).
9) actualBalanceSeries (D-4): собрать дневную агрегацию `BankTransaction` (RUR-счета) + `CashEntry` от startingBalance накопительно. Реализовать в этой же функции: получить дневные суммы `direction CREDIT=+amount / DEBIT=−amount` по RUR-счетам через `db.bankTransaction.groupBy({ by: ["date"], where: { date: {gte, lte}, account: { currency: "RUR" } }, _sum: { amount: true } })` — но groupBy по direction тоже нужен; проще: `db.bankTransaction.findMany({ where: { date: {gte, lte}, account: { currency: "RUR" } }, select: { date, direction, amount } })` + агрегировать в JS: `delta(day) = Σ(CREDIT) − Σ(DEBIT)`. Аналогично CashEntry `{ date, direction, amount }` → `Σ(INCOME) − Σ(EXPENSE)`. Затем накопительно: `bal(d) = bal(d-1) + delta(d)`, старт = startingBalance; заполнить только дни ≤ сегодня по МСК — явно: `const todayIsoMsk = new Date(Date.now() + 3 * 3600_000).toISOString().slice(0, 10)` (согласованно с finance/balance; НЕ голый `new Date().toISOString()`). Собрать `Array<{ date, balanceRub }>`. Если данных нет — вернуть пустой массив (undefined допустим).

Вернуть CashflowInputs со всеми полями + `payoutModel: "coefficient"`.

ВАЖНО: getRateForDate — async, вызывать через await внутри map через `Promise.all`, не в синхронном .map.
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "finance-cashflow/data" | head -5; test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep 'finance-cashflow/data')" && echo "data.ts typechecks"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "export async function loadCashflowInputs" lib/finance-cashflow/data.ts` = 1
- `grep -c "getPlannedRevenueSeries\|getPlannedVirtualPayments" lib/finance-cashflow/data.ts` ≥ 2
- `grep -c "currency: \"RUR\"" lib/finance-cashflow/data.ts` ≥ 1 (НЕ "RUB")
- `grep -c "rateToRub" lib/finance-cashflow/data.ts` ≥ 1 (поле getRateForDate)
- `grep -c "amountRub != null\|amountRub !== null" lib/finance-cashflow/data.ts` ≥ 1 (приоритет amountRub)
- `grep -c "computeQuarterAccrual" lib/finance-cashflow/data.ts` = 1
- `grep -c "payoutModel: \"coefficient\"" lib/finance-cashflow/data.ts` = 1
- `grep -c "from \"react\|from \"next" lib/finance-cashflow/data.ts` = 0
- `npx tsc --noEmit` не выдаёт ошибок про lib/finance-cashflow/data.ts
  </acceptance_criteria>
  <done>loadCashflowInputs собирает 8 групп данных (настройки, старт-баланс RUR+касса, притоки, виртуальные, реальные закупки, кредиты, налоги, факт-ряд) через DI; amountRub-приоритет; consolidation D-6; tsc чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Активная версия плана → движок | versionId читается из AppSetting; движок доверяет данным pdds-feed |
| БД → loadCashflowInputs | суммы из PurchasePayment/LoanPayment/CashEntry не переоцениваются на валидность (доверенный внутренний источник) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | двойной отток (виртуальная + реальная закупка) | mitigate | getPlannedVirtualPayments исключает CONVERTED/DISMISSED на уровне контракта (§8 Phase 25); loadCashflowInputs НЕ добавляет собственную логику статусов — потребляет payments как есть; тест engine Test 4 пинит, что движок не переоценивает virtualPayments |
| T-28-02 | Information Disclosure | старт-баланс включает все RUR-счета всех юрлиц | accept | консолидация D-6 — намеренное поведение v1; раздел за requireSection("FINANCE") (проверяется в 28-02 на уровне page.tsx); движок/загрузчик — внутренние модули без прямого HTTP-доступа |
| T-28-03 | Tampering | некорректный AppSetting-коэффициент искажает притоки | accept (mitigate в 28-03) | движок применяет wbPayoutPct как есть; zod-границы на запись значений реализуются в 28-03 (server action); здесь read-only fallback-дефолты (55/1/0/0) при отсутствии ключа |
</threat_model>

<verification>
- `npm run test -- finance-cashflow-engine` — 5 тестов green
- `npm run test` — полный suite green, sales-plan golden не тронут (iuTotalForRange == 438_068_120)
- `npx tsc --noEmit` — ноль ошибок
- Все 3 модуля lib/finance-cashflow/*.ts созданы; engine + types pure (ноль Prisma/React)
</verification>

<success_criteria>
- computeCashflow: остаток = старт + Σпритоки − Σоттоки, тайминг WB по понедельникам+лаг, gap-детекция, сменная payout-модель (D-1) — все проверены тестами
- loadCashflowInputs собирает все входы через DI; amountRub-приоритет; консолидация RUR (D-6)
- AppSetting-сид 4 ключа finance.cashflow.* (55/1/0/0)
- sales-plan golden не задет
</success_criteria>

<output>
После завершения создать `.planning/phases/28-cashflow/28-01-SUMMARY.md`
</output>
