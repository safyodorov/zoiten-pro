---
phase: 28-cashflow
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - app/(dashboard)/finance/cashflow/page.tsx
  - components/finance/CashflowKpiCards.tsx
  - components/finance/CashflowChart.tsx
  - components/finance/CashflowMatrix.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Страница /finance/cashflow рендерит матрицу потоков × бакеты вместо ComingSoon-заглушки"
    - "Нет активной версии плана (salesPlan.activeVersionId=null) → пустое состояние с CTA «Зафиксируйте план продаж»"
    - "KPI-карточки показывают Стартовый остаток · Мин. остаток · Дата первого разрыва · Net за горизонт"
    - "График показывает линию остатка (прогноз) + линию факта до сегодня (D-4) + порог/сегодня ReferenceLine"
    - "Матрица подсвечивает красным остаток на конец бакета при разрыве (balanceEnd < порога)"
    - "Гранулярность день/неделя/месяц переключается через URL searchParam"
    - "При versionStale — жёлтое предупреждение «виртуальные закупки изменили статус»"
  artifacts:
    - path: "app/(dashboard)/finance/cashflow/page.tsx"
      provides: "RSC-страница ПДДС (force-dynamic, requireSection FINANCE)"
      contains: "computeCashflow"
    - path: "components/finance/CashflowMatrix.tsx"
      provides: "sticky-матрица потоков × бакеты с gap-подсветкой"
      contains: "border-separate"
    - path: "components/finance/CashflowKpiCards.tsx"
      provides: "4 KPI-карточки"
      contains: "firstGapDate"
    - path: "components/finance/CashflowChart.tsx"
      provides: "recharts линия остатка прогноз+факт+пороги"
      contains: "ReferenceLine"
  key_links:
    - from: "app/(dashboard)/finance/cashflow/page.tsx"
      to: "lib/finance-cashflow/data.ts + engine.ts"
      via: "loadCashflowInputs → computeCashflow"
      pattern: "loadCashflowInputs|computeCashflow"
    - from: "app/(dashboard)/finance/cashflow/page.tsx"
      to: "AppSetting salesPlan.activeVersionId"
      via: "findFirst/findMany активной версии"
      pattern: "activeVersionId"
---

<objective>
Заменить ComingSoon-заглушку /finance/cashflow на RSC-страницу: загрузка входов → computeCashflow → рендер KPI + график + sticky-матрица. Стадия 2 (видимый деплой).

Purpose: Дать пользователю визуальный прогноз денежных потоков H2-2026 — где притоки WB, где оттоки, где кассовые разрывы. Read-only матрица (платежи не редактируются здесь — их источники план продаж/закупки/кредиты, D-9). Факт-ряд остатка за прошедшие дни (D-4).

Output: page.tsx (RSC) + 3 клиентских компонента (KPI, Chart, Matrix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-cashflow/28-CONTEXT.md
@.planning/phases/28-cashflow/28-PATTERNS.md

<interfaces>
<!-- Контракты из 28-01 (уже реализованы) — использовать напрямую. -->
Из lib/finance-cashflow/types.ts:
```typescript
export interface CashflowDay { date; wbPayoutRub; realPurchaseRub; virtualPurchaseRub; loanRub; taxRub; opexRub; totalInflow; totalOutflow; netFlow; balanceEnd; isGap; actualBalance: number|null }
export interface CashflowBucket { key; label; wbPayoutRub; realPurchaseRub; virtualPurchaseRub; loanRub; taxRub; opexRub; totalInflow; totalOutflow; netFlow; balanceEnd; hasGap }
export interface CashflowResult { days; buckets; granularity; startingBalance; minBalance; firstGapDate: string|null; netTotal; versionStale }
```
Из lib/finance-cashflow/data.ts:
```typescript
export interface LoadCashflowParams { versionId: string; horizonFrom: string; horizonTo: string }
export async function loadCashflowInputs(db: PrismaClient, params: LoadCashflowParams): Promise<CashflowInputs>
```
Из lib/finance-cashflow/engine.ts:
```typescript
export function computeCashflow(inputs: CashflowInputs, granularity?: Granularity, payoutFn?: PayoutFn): CashflowResult
```
Из lib/date-buckets.ts: `type Granularity = "day"|"week"|"month"|...`
Из lib/rbac.ts: `requireSection("FINANCE")`, `getSectionRole("FINANCE"): Promise<"VIEW"|"MANAGE"|null>`
Из components/finance/FinanceTabs.tsx: `<FinanceTabs />` (уже включает вкладку «ОДДС» → /finance/cashflow)
</interfaces>

<read_first>
- app/(dashboard)/finance/cashflow/page.tsx (текущая заглушка — заменить полностью)
- app/(dashboard)/finance/balance/page.tsx (образец RSC: force-dynamic, requireSection, getSectionRole, MSK-today, FinanceTabs)
- components/sales-plan/PlanFactMatrix.tsx (образец sticky-таблицы — заголовок HTML <table>, sticky bg-background БЕЗ /NN)
- components/sales-plan/PlanFactChart.tsx (образец recharts — тики fill var(--muted-foreground), ReferenceLine сегодня)
- lib/finance-cashflow/types.ts, engine.ts, data.ts (контракты 28-01)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 313-388 (page skeleton), 508-573 (matrix sticky), 577-646 (chart), 650-682 (KPI)
- CLAUDE.md §Sticky data-таблицы (сплошной bg БЕЗ /NN)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CashflowKpiCards + CashflowChart (клиентские компоненты)</name>
  <files>components/finance/CashflowKpiCards.tsx, components/finance/CashflowChart.tsx</files>
  <read_first>
- components/sales-plan/PlanFactChart.tsx (recharts тики var(--muted-foreground), ReferenceLine, токены var(--chart-*))
- lib/finance-cashflow/types.ts (CashflowResult, CashflowDay)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 577-682 (chart + KPI шаблоны)
  </read_first>
  <action>
1) `components/finance/CashflowKpiCards.tsx` — "use client". Props `{ result: CashflowResult }`. 4 карточки (D-4 §Specific Ideas):
- «Стартовый остаток» → fmtRub(result.startingBalance)
- «Мин. остаток» → fmtRub(result.minBalance), красный класс если < 0
- «Первый разрыв» → result.firstGapDate ? fmtDate(...) : «нет», красный если есть разрыв, иначе emerald
- «Net за горизонт» → fmtRub(result.netTotal), красный если < 0
Локальные хелперы fmtRub (`new Intl.NumberFormat("ru-RU").format(Math.round(n))` + " ₽") и fmtDate (ISO → "DD.MM.YYYY"). Grid `grid grid-cols-2 gap-3 sm:grid-cols-4`, карточки `rounded-lg border bg-card p-3`. Цвет через СТАТИЧЕСКИЕ классы (`text-red-600 dark:text-red-400` / `text-emerald-600`) — НЕ через client-функцию (RSC trap не касается client-компонента, но статические классы стабильнее).

2) `components/finance/CashflowChart.tsx` — "use client". Props `{ days: CashflowDay[]; today: string; gapThresholdRub: number }`. Recharts ComposedChart:
- data = days.map(d => ({ label: d.date.slice(5), balanceEnd: d.balanceEnd, actualBalance: d.actualBalance }))
- XAxis dataKey="label" tick `{ fontSize: 11, fill: "var(--muted-foreground)" }`
- YAxis tick аналогично, tickFormatter → компактные числа (÷1e6 + "М")
- CartesianGrid className="stroke-muted"
- Line dataKey="balanceEnd" name="Остаток (прогноз)" stroke="var(--chart-2)" strokeWidth={2} dot={false} type="monotone"
- Line dataKey="actualBalance" name="Остаток (факт)" stroke="var(--chart-1)" strokeWidth={2} dot={false} type="monotone" connectNulls={false} (D-4 — линия факта до сегодня, null дальше)
- ReferenceLine y={gapThresholdRub} stroke="var(--destructive)" strokeDasharray="4 4" label «порог»
- ReferenceLine x={today.slice(5)} stroke="var(--muted-foreground)" strokeDasharray="4 4" label «сегодня» (аналог PlanFactChart)
- Tooltip + Legend + ResponsiveContainer height ~280. Обёртка `rounded-lg border bg-card p-3`.
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'CashflowKpiCards|CashflowChart')" && echo "kpi+chart typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "\"use client\"" components/finance/CashflowKpiCards.tsx` = 1
- `grep -c "firstGapDate" components/finance/CashflowKpiCards.tsx` ≥ 1
- `grep -c "startingBalance\|minBalance\|netTotal" components/finance/CashflowKpiCards.tsx` ≥ 3
- `grep -c "ReferenceLine" components/finance/CashflowChart.tsx` ≥ 2 (порог + сегодня)
- `grep -c "var(--muted-foreground)" components/finance/CashflowChart.tsx` ≥ 2 (тики)
- `grep -c "actualBalance" components/finance/CashflowChart.tsx` ≥ 1 (факт-линия D-4)
- `grep -c "connectNulls" components/finance/CashflowChart.tsx` ≥ 1
- `npx tsc --noEmit` без ошибок про эти файлы
  </acceptance_criteria>
  <done>KpiCards рендерит 4 KPI со статической цветовой подсветкой; Chart рисует прогноз+факт-линии, порог и «сегодня» ReferenceLine, тики var(--muted-foreground); tsc чист.</done>
</task>

<task type="auto">
  <name>Task 2: CashflowMatrix (sticky-таблица потоков × бакеты)</name>
  <files>components/finance/CashflowMatrix.tsx</files>
  <read_first>
- components/sales-plan/PlanFactMatrix.tsx (sticky HTML-таблица: <thead bg-background>, <th sticky bg-background БЕЗ /NN>, LABEL_WIDTH, PERIOD_BASE)
- lib/finance-cashflow/types.ts (CashflowBucket)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 508-573 (matrix шаблон + gap-подсветка + sticky-константы)
- CLAUDE.md §Sticky data-таблицы (сплошной bg БЕЗ /NN — повторяющийся баг)
  </read_first>
  <action>
`components/finance/CashflowMatrix.tsx` — "use client". Props `{ buckets: CashflowBucket[]; gapThresholdRub: number }`.

Структура строк матрицы (порядок из CONTEXT §Claude's Discretion: Старт → Притоки → Оттоки → Net → Остаток):
- «Выплаты WB» → bucket.wbPayoutRub (приток, зелёный знак)
- «Прочие притоки» → 0 (v1 нет; строка-плейсхолдер или пропустить)
- «Итого притоки» → bucket.totalInflow (подытог, bg-muted СПЛОШНОЙ)
- «Закупки (реальные)» → bucket.realPurchaseRub
- «Виртуальные закупки» → bucket.virtualPurchaseRub
- «Кредиты» → bucket.loanRub
- «Налоги» → bucket.taxRub
- «Опекс» → bucket.opexRub
- «Итого оттоки» → bucket.totalOutflow (подытог, bg-muted СПЛОШНОЙ)
- «Net поток» → bucket.netFlow
- «Остаток на конец» → bucket.balanceEnd (подсветка красным если bucket.hasGap)

Sticky-паттерн (КРИТИЧНО — сплошной bg БЕЗ /NN, повторяющийся баг):
```
- НЕ shadcn <Table>/<TableHeader>/<TableRow> в шапке — прямой <table className="w-full border-separate border-spacing-0">
- <thead className="bg-background"> сплошной
- Первая колонка (label): <th className="sticky left-0 top-0 z-30 bg-background border-b border-r ..."> (угол) и в tbody <td className="sticky left-0 z-20 bg-background border-b border-r ...">
- Заголовки бакетов: <th className="sticky top-0 z-10 bg-background border-b border-r border-r-border/40 text-right ...">
- Подытоги (Итого притоки/оттоки): фон bg-muted СПЛОШНОЙ (НЕ bg-muted/40)
```
LABEL_WIDTH = 240; ячейки чисел `text-right tabular-nums px-2 h-8`.

Gap-подсветка строки «Остаток на конец»:
```typescript
function gapCellClass(hasGap: boolean): string {
  return hasGap ? "bg-red-50 dark:bg-red-950/20 text-red-600 dark:text-red-400 font-medium" : ""
}
```
Форматирование: `new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 0 }).format(n)`. Оборачивающий контейнер `rounded-md border bg-card overflow-hidden` → `overflow-auto`.
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep 'CashflowMatrix')" && echo "matrix typecheck OK" && grep -c "bg-background/\|bg-muted/40\|bg-muted/60" components/finance/CashflowMatrix.tsx | grep -q "^0$" && echo "no transparent sticky bg"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "\"use client\"" components/finance/CashflowMatrix.tsx` = 1
- `grep -c "border-separate border-spacing-0" components/finance/CashflowMatrix.tsx` = 1
- `grep -c "bg-background/\|bg-muted/40\|bg-muted/60\|bg-card/" components/finance/CashflowMatrix.tsx` = 0 (ЗАПРЕЩЁН /NN на sticky — повторяющийся баг)
- `grep -c "<Table\b\|<TableHeader\|<TableRow" components/finance/CashflowMatrix.tsx` = 0 (в шапке — прямой HTML)
- `grep -c "wbPayoutRub\|virtualPurchaseRub\|loanRub\|taxRub\|opexRub\|realPurchaseRub" components/finance/CashflowMatrix.tsx` ≥ 6 (все строки потоков)
- `grep -c "hasGap" components/finance/CashflowMatrix.tsx` ≥ 1 (gap-подсветка)
- `npx tsc --noEmit` без ошибок про CashflowMatrix
  </acceptance_criteria>
  <done>CashflowMatrix — sticky HTML-таблица (11 строк потоков), sticky-ячейки со сплошным bg БЕЗ /NN, подытоги bg-muted сплошной, gap-подсветка остатка; tsc чист.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx (RSC — замена заглушки: загрузка → computeCashflow → рендер)</name>
  <files>app/(dashboard)/finance/cashflow/page.tsx</files>
  <read_first>
- app/(dashboard)/finance/cashflow/page.tsx (текущая заглушка — заменить)
- app/(dashboard)/finance/balance/page.tsx (образец RSC: force-dynamic, requireSection, getSectionRole, MSK-today, searchParams Promise)
- lib/finance-cashflow/data.ts + engine.ts (loadCashflowInputs → computeCashflow)
- components/finance/CashflowKpiCards.tsx, CashflowChart.tsx, CashflowMatrix.tsx (Task 1-2 этого плана)
- .planning/phases/28-cashflow/28-PATTERNS.md строки 313-390 (page skeleton + пустое состояние + RSC trap)
- memory/project_rsc_client_fn_runtime_trap.md (НЕ вызывать buttonVariants/client-функции из RSC)
  </read_first>
  <action>
Заменить `app/(dashboard)/finance/cashflow/page.tsx` полностью (образец balance/page.tsx):

Топ-уровень:
```typescript
import { requireSection, getSectionRole } from "@/lib/rbac"
import { prisma } from "@/lib/prisma"
import type { Granularity } from "@/lib/date-buckets"
import { FinanceTabs } from "@/components/finance/FinanceTabs"
import { CashflowKpiCards } from "@/components/finance/CashflowKpiCards"
import { CashflowChart } from "@/components/finance/CashflowChart"
import { CashflowMatrix } from "@/components/finance/CashflowMatrix"
import { loadCashflowInputs } from "@/lib/finance-cashflow/data"
import { computeCashflow } from "@/lib/finance-cashflow/engine"

export const metadata = { title: "Финансы — ОДДС — Zoiten ERP" }
export const dynamic = "force-dynamic"

function mskTodayDateString(): string {
  const ms = Date.now() + 3 * 3600_000
  return new Date(ms).toISOString().split("T")[0]
}
```

Компонент `async function FinanceCashflowPage({ searchParams }: { searchParams: Promise<{ granularity?: string }> })`:
1) `await requireSection("FINANCE")` (D-8; canManage не нужен в этом плане — AssumptionsBar добавит 28-03).
2) `const sp = await searchParams`; `const granularity = (["day","week","month"].includes(sp.granularity ?? "") ? sp.granularity : "month") as Granularity`.
3) Прочитать активную версию + горизонт из AppSetting: `prisma.appSetting.findMany({ where: { key: { in: ["salesPlan.activeVersionId", "salesPlan.horizon"] } } })`. activeVersionId = value ключа salesPlan.activeVersionId ?? null. horizon: JSON.parse(value {from,to}) ?? { from: "2026-07-01", to: "2026-12-31" }.
4) ПУСТОЕ СОСТОЯНИЕ (D-7 — нет fallback на драфт): если `!activeVersionId` → вернуть:
```tsx
<div className="h-full flex flex-col gap-4">
  <FinanceTabs />
  <div className="flex-1 flex items-center justify-center">
    <div className="text-center text-muted-foreground">
      <p className="text-sm">Нет активной версии плана продаж</p>
      <p className="text-xs mt-1">Зафиксируйте план продаж в разделе «План продаж»</p>
    </div>
  </div>
</div>
```
5) `const inputs = await loadCashflowInputs(prisma, { versionId: activeVersionId, horizonFrom: horizon.from, horizonTo: horizon.to })`.
6) `const result = computeCashflow(inputs, granularity)`.
7) `const today = mskTodayDateString()`.
8) Рендер (СТАТИЧЕСКИЕ классы, НЕ buttonVariants — RSC trap):
```tsx
<div className="h-full flex flex-col gap-4">
  <FinanceTabs />
  {/* granularity switcher: 3 ссылки <Link href="?granularity=day|week|month"> со статическими классами, prefetch={false} */}
  {result.versionStale && (
    <div className="rounded-md border border-amber-300 bg-amber-50 px-3 py-2 text-xs text-amber-700 dark:bg-amber-950/30 dark:text-amber-300">
      Виртуальные закупки изменили статус — рекомендуется перефиксация плана продаж.
    </div>
  )}
  <CashflowKpiCards result={result} />
  <CashflowChart days={result.days} today={today} gapThresholdRub={inputs.gapThresholdRub} />
  <CashflowMatrix buckets={result.buckets} gapThresholdRub={inputs.gapThresholdRub} />
</div>
```
Granularity switcher: 3 `<Link prefetch={false} href={\`/finance/cashflow?granularity=${g}\`}>` со статическими классами (`px-2 py-1 text-xs rounded border` + активный `bg-muted`), метки «День/Неделя/Месяц». НЕ использовать buttonVariants.
  </action>
  <verify>
    <automated>cd c:/Users/serge/zoiten-pro && npm run build 2>&1 | grep -E "finance/cashflow|Failed to compile|error" | head -10; test -z "$(npx tsc --noEmit -p tsconfig.json 2>&1 | grep 'finance/cashflow/page')" && echo "page typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "ComingSoon" app/(dashboard)/finance/cashflow/page.tsx` = 0 (заглушка удалена)
- `grep -c "requireSection(\"FINANCE\")" app/(dashboard)/finance/cashflow/page.tsx` = 1
- `grep -c "export const dynamic = \"force-dynamic\"" app/(dashboard)/finance/cashflow/page.tsx` = 1
- `grep -c "activeVersionId" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2 (чтение + пустое состояние)
- `grep -c "Зафиксируйте план продаж" app/(dashboard)/finance/cashflow/page.tsx` = 1 (CTA пустого состояния)
- `grep -c "loadCashflowInputs\|computeCashflow" app/(dashboard)/finance/cashflow/page.tsx` ≥ 2
- `grep -c "versionStale" app/(dashboard)/finance/cashflow/page.tsx` ≥ 1
- `grep -c "buttonVariants" app/(dashboard)/finance/cashflow/page.tsx` = 0 (RSC trap)
- `grep -c "prefetch={false}" app/(dashboard)/finance/cashflow/page.tsx` ≥ 1 (granularity switcher)
- `npm run build` компилируется без ошибок
  </acceptance_criteria>
  <done>page.tsx — RSC без ComingSoon: requireSection FINANCE, пустое состояние при отсутствии activeVersionId (CTA), loadCashflowInputs→computeCashflow, рендер KPI+Chart+Matrix+versionStale-предупреждение, granularity switcher statically-classed prefetch=false; build зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → RSC page | GET /finance/cashflow?granularity=X — granularity валидируется allow-list |
| Пользователь → раздел | requireSection("FINANCE") гейт на входе в RSC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Elevation | доступ к финансовым данным без прав | mitigate | requireSection("FINANCE") в начале RSC page.tsx (D-8) — редирект/403 до любой загрузки данных |
| T-28-05 | Tampering | инъекция через granularity searchParam | mitigate | allow-list `["day","week","month"].includes(...)` с fallback "month" — произвольное значение отбрасывается, не доходит до bucketKey |
| T-28-06 | Information Disclosure | пустое состояние раскрывает наличие/отсутствие плана | accept | текст «нет активной версии» безобиден, за FINANCE-гейтом; не раскрывает суммы |
</threat_model>

<verification>
- `npm run build` — компилируется без ошибок, /finance/cashflow — динамический роут
- `npx tsc --noEmit` — ноль ошибок
- `npm run test` — полный suite green (движок не тронут, sales-plan golden цел)
- Матрица без /NN на sticky-ячейках (grep-гейт в Task 2)
- RSC не вызывает client-функции (grep buttonVariants = 0)
</verification>

<success_criteria>
- Заглушка ComingSoon заменена рабочей страницей ПДДС
- Пустое состояние при отсутствии активной версии (D-7)
- KPI + график (факт-линия D-4) + sticky-матрица с gap-подсветкой
- Гранулярность день/неделя/месяц через URL
- versionStale-предупреждение
</success_criteria>

<output>
После завершения создать `.planning/phases/28-cashflow/28-02-SUMMARY.md`
</output>
