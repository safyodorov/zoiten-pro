---
phase: 09-returns
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - app/actions/support.ts
  - components/support/ReturnActionsPanel.tsx
  - components/support/ReturnInfoPanel.tsx
  - app/(dashboard)/support/[ticketId]/page.tsx
  - tests/return-actions.test.ts
autonomous: false
context_budget_warning: true  # revision Warning 7 — 3 крупные задачи + human UAT
requirements:
  - SUP-14
  - SUP-19
  - SUP-20
must_haves:
  truths:
    - "app/actions/support.ts экспортирует approveReturn, rejectReturn, reconsiderReturn — все требуют requireSection(\"SUPPORT\", \"MANAGE\")"
    - "approveReturn: PENDING → APPROVED, выбирает wbAction из ticket.wbActions (prefer approve1 > autorefund1 > approvecc1), создаёт ReturnDecision{action:APPROVE, reconsidered:false}"
    - "rejectReturn: PENDING → REJECTED, требует reason [10, 1000] chars, PATCH с action=rejectcustom comment=reason, создаёт ReturnDecision{action:REJECT, reason}"
    - "reconsiderReturn: REJECTED → APPROVED, wbAction из свежих wbActions (обычно approve1), создаёт ReturnDecision{action:RECONSIDER, reconsidered:true}"
    - "State machine guards: APPROVED финал (все actions reject), REJECTED→PENDING невозможен, PENDING→RECONSIDER невозможен"
    - "Transaction order: PATCH WB API ПЕРВЫМ (может вернуть ошибку), ReturnDecision.create + SupportTicket.update ВТОРЫМ — если WB 4xx/5xx, Decision НЕ создаётся"
    - "revalidatePath(\"/support/returns\") + revalidatePath(\"/support/\" + ticketId) после каждого успешного action"
    - "ReturnActionsPanel — sticky client-компонент в диалоге /support/[ticketId], рендерится ВМЕСТО ReplyPanel когда channel===RETURN"
    - "UI кнопки: PENDING → [Одобрить, Отклонить], REJECTED → [Пересмотреть], APPROVED → [readonly сообщение]. Все disabled если соответствующий wbAction не в wbActions[]"
    - "Модалка Отклонить: textarea с counter 10/1000, submit disabled < 10 символов, zod валидация и на клиенте и на сервере"
    - "tests/return-actions.test.ts GREEN — минимум 10 тестов покрывают happy path, state guards, RBAC, WB error rollback, revalidatePath"
  artifacts:
    - path: "app/actions/support.ts"
      provides: "3 новых server action для возвратов"
      exports: ["approveReturn", "rejectReturn", "reconsiderReturn"]
    - path: "components/support/ReturnActionsPanel.tsx"
      provides: "Sticky панель 3 кнопок с модалкой Отклонить"
      exports: ["ReturnActionsPanel"]
    - path: "components/support/ReturnInfoPanel.tsx"
      provides: "Блок в левой колонке диалога — цена, wbComment, srid"
      exports: ["ReturnInfoPanel"]
    - path: "tests/return-actions.test.ts"
      provides: "Integration тесты server actions с mock WB + mock Prisma"
      contains: "describe"
  key_links:
    - from: "app/actions/support.ts approveReturn"
      to: "lib/wb-support-api.ts approveReturn"
      via: "import (same name — alias или namespace)"
      pattern: "@/lib/wb-support-api"
    - from: "app/actions/support.ts"
      to: "requireSection(\"SUPPORT\", \"MANAGE\")"
      via: "RBAC guard в начале каждого action"
      pattern: "requireSection\\(\"SUPPORT\",\\s*\"MANAGE\"\\)"
    - from: "app/actions/support.ts"
      to: "prisma.returnDecision.create + prisma.supportTicket.update"
      via: "$transaction после успешного WB PATCH"
      pattern: "\\$transaction"
    - from: "app/(dashboard)/support/[ticketId]/page.tsx"
      to: "components/support/ReturnActionsPanel"
      via: "conditional render — channel === \"RETURN\""
      pattern: "ReturnActionsPanel"
    - from: "components/support/ReturnActionsPanel.tsx"
      to: "app/actions/support.ts (approveReturn/rejectReturn/reconsiderReturn)"
      via: "import + onClick handlers + sonner toast"
      pattern: "@/app/actions/support"
---

<objective>
Phase 9 Plan 04 — финальный UX-слой возвратов:
1. `app/actions/support.ts` — 3 новых server action с RBAC `requireSection("SUPPORT", "MANAGE")`:
   - `approveReturn(ticketId)` — PENDING → APPROVED, WB action picker, Decision{action:APPROVE}
   - `rejectReturn(ticketId, reason)` — PENDING → REJECTED, zod reason 10-1000, WB rejectcustom, Decision{action:REJECT, reason}
   - `reconsiderReturn(ticketId)` — REJECTED → APPROVED, WB approve1 (из свежих wbActions), Decision{action:RECONSIDER, reconsidered:true}
   - Все 3: guard state machine + transaction PATCH→Decision→update + revalidatePath
2. `components/support/ReturnActionsPanel.tsx` — client sticky-панель в диалоге, 3 кнопки зависящие от returnState и wbActions; модалка «Отклонить» с textarea + counter + zod.
3. `components/support/ReturnInfoPanel.tsx` — блок в левой колонке диалога (price, wbComment collapsible, srid link).
4. Модификация `app/(dashboard)/support/[ticketId]/page.tsx` — conditional render ReturnActionsPanel для `channel === "RETURN"` вместо ReplyPanel + preload latestDecision + ReturnInfoPanel в aside.
5. Замена Wave 0 stub `tests/return-actions.test.ts` на минимум 10 GREEN integration-тестов (mock WB client + mock Prisma).
6. Human UAT checkpoint: полный end-to-end flow на проде (после deploy через 09-deploy задачу).

Purpose: Менеджер полностью закрывает петлю обработки возврата без выхода в личный кабинет WB. Все write-операции защищены RBAC и обёрнуты в transactions с WB-first порядком (если WB вернёт ошибку — локальное решение не фиксируется). Критичная реализация state machine инвариантов из SUP-20.

Output: 1 обновлённый server actions файл, 2 новых клиентских компонента, 1 обновлённая RSC-страница диалога, 1 наполненный integration-тест, 1 human UAT checkpoint, 1 VPS deploy задача.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-returns/09-RESEARCH.md
@.planning/phases/09-returns/09-01-SUMMARY.md
@.planning/phases/09-returns/09-02-SUMMARY.md
@.planning/phases/08-support-mvp/08-04-SUMMARY.md
@CLAUDE.md
@app/(dashboard)/support/[ticketId]/page.tsx
@app/actions/support.ts
@components/support/ReplyPanel.tsx
@components/support/TicketSidePanel.tsx
@lib/wb-support-api.ts
@lib/rbac.ts
@lib/prisma.ts

<interfaces>
**Новые методы lib/wb-support-api.ts (Plan 09-01):**
```typescript
export async function approveReturn(id: string, wbAction: string, comment?: string): Promise<{ ok: true }>
export async function rejectReturn(id: string, reason: string): Promise<{ ok: true }>
export async function reconsiderReturn(id: string, wbAction: string): Promise<{ ok: true }>
```

**Новые Prisma (Plan 09-01):**
```prisma
model ReturnDecision {
  id String; ticketId String; action ReturnDecisionAction
  wbAction String; reason String?; decidedById String; decidedAt DateTime
  reconsidered Boolean; wbResponseOk Boolean; wbError String?
}
enum ReturnDecisionAction { APPROVE; REJECT; RECONSIDER }

// SupportTicket (Phase 9 расширения):
returnState     ReturnState?  // PENDING | APPROVED | REJECTED
wbActions       String[]      // свежие от WB sync
wbComment       String?
srid            String?
price           Float?
```

**КРИТИЧНО — контракт requireSection и получение userId (из lib/rbac.ts + Phase 8 паттерн):**

`requireSection(section, minRole?)` → `Promise<void>` — бросает при неуспехе, НЕ возвращает session. Userid нужно получить отдельно через `auth()`.

Phase 8 уже содержит helper в `app/actions/support.ts` (строки 24-27) — **переиспользовать, не дублировать:**

```typescript
// Уже есть в app/actions/support.ts:
async function getSessionUserId(): Promise<string | null> {
  const session = await auth()
  return session?.user?.id ?? null
}

// Импорты уже на месте (строки 10-11):
import { requireSection } from "@/lib/rbac"
import { auth } from "@/lib/auth"
```

**Phase 8 server actions pattern (реальный, из app/actions/support.ts строки 29-45):**
```typescript
"use server"

import { revalidatePath } from "next/cache"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"
import { auth } from "@/lib/auth"

export type ActionResult = { ok: true } | { ok: false; error: string }

export async function myAction(ticketId: string): Promise<ActionResult> {
  try {
    await requireSection("SUPPORT", "MANAGE")  // ← возвращает Promise<void>, бросает при reject
    const userId = await getSessionUserId()
    if (!userId) return { ok: false, error: "Сессия без user.id" }
    // ...
    revalidatePath("/support")
    return { ok: true }
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
  }
}
```

⚠ **НЕ использовать `const session = await requireSection(...)` — это TypeScript error (requireSection возвращает void).**

**Phase 8 диалог (app/(dashboard)/support/[ticketId]/page.tsx строки 138-152):**
```typescript
const canReply = ticket.channel === "FEEDBACK" || ticket.channel === "QUESTION"
// ...
<SupportDialog messages={messages} />
{canReply && <ReplyPanel ticketId={ticket.id} disabled={ticket.status === "CLOSED"} />}
{!canReply && (
  <div className="border-t p-3 text-xs text-muted-foreground text-center">
    Ответ через интерфейс доступен для каналов «Отзыв» и «Вопрос». Чат/возврат/мессенджер — в следующих фазах.
  </div>
)}
```
Phase 9 меняет `!canReply` branch: теперь если channel===RETURN → ReturnActionsPanel, иначе fallback-сообщение (CHAT/MESSENGER).

**Phase 8 aside (левая колонка) в том же файле строки 104-136:**
```typescript
<aside className="border-r p-4 overflow-y-auto">
  {/* карточка покупателя + товар WbCard */}
</aside>
```
Plan 09-04 добавляет ReturnInfoPanel после WbCard секции если channel===RETURN.

**Паттерн toast (sonner) для client компонента:**
```typescript
import { toast } from "sonner"
const id = toast.loading("Отправка решения в WB...")
const res = await approveReturn(ticketId)
if (res.ok) toast.success("Возврат одобрен", { id })
else toast.error(res.error, { id })
```

**Zod для reason:**
```typescript
import { z } from "zod"
const rejectReasonSchema = z.string().min(10, "Минимум 10 символов").max(1000, "Максимум 1000 символов")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: 3 server actions для возвратов + integration тесты (10+ GREEN)</name>
  <files>app/actions/support.ts, tests/return-actions.test.ts</files>
  <read_first>
    - app/actions/support.ts (существующие Phase 8 actions — replyToTicket/assignTicket/updateTicketStatus, паттерн getSessionUserId + requireSection + try/catch + revalidatePath)
    - lib/wb-support-api.ts (Plan 09-01 — approveReturn/rejectReturn/reconsiderReturn + Claim type)
    - lib/rbac.ts (реальный контракт: requireSection → Promise<void>, НЕ возвращает session)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «State Machine» строки 612-716 — guards + transaction pattern + disable logic)
    - tests/support-actions.test.ts (Phase 8 паттерн mock WB client + mock Prisma + mock auth)
  </read_first>
  <behavior>
    **approveReturn(ticketId: string): Promise<ActionResult>**
    - `await requireSection("SUPPORT", "MANAGE")` — бросит при отсутствии доступа, пойман в try/catch
    - `const userId = await getSessionUserId()` — переиспользовать существующий helper из app/actions/support.ts (строки 24-27). НЕ дублировать.
    - Если `!userId` → `{ ok: false, error: "Сессия без user.id" }`
    - Найти ticket: если не найден → `{ ok: false, error: "Тикет не найден" }`
    - Guard: channel === "RETURN" (иначе "Не RETURN-тикет")
    - Guard: returnState === "PENDING" (APPROVED → "Возврат уже одобрен (финал)"; REJECTED → "Используйте reconsiderReturn")
    - Пик wbAction из `ticket.wbActions` в приоритете: `approve1` > `autorefund1` > `approvecc1`
    - Если ни одного нет → `{ ok: false, error: "WB API не предоставляет доступных action-ов для одобрения" }`
    - PATCH WB: `await wbApi.approveReturn(ticket.wbExternalId, wbAction)` — если throws → `{ ok: false, error: "WB: " + msg }` (Decision НЕ создаётся)
    - Transaction: create ReturnDecision{action:APPROVE, wbAction, reason:null, decidedById: userId, reconsidered:false} + update supportTicket{returnState:APPROVED, status:ANSWERED, resolvedAt:now}
    - revalidatePath("/support/returns") + revalidatePath(`/support/${ticketId}`)
    - return `{ ok: true }`

    **rejectReturn(ticketId: string, reason: string): Promise<ActionResult>**
    - `await requireSection("SUPPORT", "MANAGE")` + `await getSessionUserId()` (same pattern)
    - Zod: reason.trim().length in [10, 1000] — иначе `{ ok: false, error: "Причина должна быть от 10 до 1000 символов" }`
    - Guard: channel === RETURN, returnState === PENDING (иначе "Отклонить можно только из PENDING")
    - Guard: ticket.wbActions.includes("rejectcustom") (иначе "WB не предоставляет rejectcustom для этой заявки")
    - PATCH WB: `wbApi.rejectReturn(ticket.wbExternalId, reason)` — если throws → error
    - Transaction: create ReturnDecision{action:REJECT, wbAction:"rejectcustom", reason, decidedById: userId, reconsidered:false} + update supportTicket{returnState:REJECTED, status:IN_PROGRESS}
    - revalidatePath x2
    - return { ok: true }

    **reconsiderReturn(ticketId: string): Promise<ActionResult>**
    - `await requireSection("SUPPORT", "MANAGE")` + `await getSessionUserId()` (same pattern)
    - Guard: channel === RETURN, returnState === REJECTED (иначе "Пересмотреть можно только отклонённые")
    - Пик wbAction: `approve1` (из свежих ticket.wbActions)
    - Если approve1 отсутствует → `{ ok: false, error: "WB не позволяет пересмотреть эту заявку" }`
    - PATCH WB: `wbApi.reconsiderReturn(ticket.wbExternalId, "approve1")`
    - Transaction: create ReturnDecision{action:RECONSIDER, wbAction:"approve1", decidedById: userId, reconsidered:TRUE} + update supportTicket{returnState:APPROVED, status:ANSWERED, resolvedAt:now}
    - revalidatePath x2
    - return { ok: true }

    **Все 3:**
    - Если wbExternalId null → error "У тикета нет wbExternalId"
    - WB ошибка ловится в try/catch — Decision не создаётся, state machine не меняется
    - Возвращают `ActionResult = { ok: true } | { ok: false; error: string }`
  </behavior>
  <action>
    **Шаг A — дополнить `app/actions/support.ts`:**

    Добавить в конец файла (сохраняя существующие Phase 8 actions). **КРИТИЧНО:** `getSessionUserId` уже существует в том же файле (строки 24-27) — переиспользовать, НЕ дублировать.

    ```typescript
    // Phase 9 — Возвраты

    import {
      approveReturn as wbApproveReturn,
      rejectReturn as wbRejectReturn,
      reconsiderReturn as wbReconsiderReturn,
    } from "@/lib/wb-support-api"

    function pickApproveAction(wbActions: string[]): string | null {
      if (wbActions.includes("approve1")) return "approve1"
      if (wbActions.includes("autorefund1")) return "autorefund1"
      if (wbActions.includes("approvecc1")) return "approvecc1"
      return null
    }

    export async function approveReturn(ticketId: string): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: ticketId },
          select: { id: true, channel: true, wbExternalId: true, returnState: true, wbActions: true },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (ticket.channel !== "RETURN") return { ok: false, error: "Не RETURN-тикет" }
        if (ticket.returnState === "APPROVED") return { ok: false, error: "Возврат уже одобрен (финал)" }
        if (ticket.returnState === "REJECTED") return { ok: false, error: "Используйте reconsiderReturn" }
        if (!ticket.wbExternalId) return { ok: false, error: "У тикета нет wbExternalId" }

        const wbAction = pickApproveAction(ticket.wbActions)
        if (!wbAction) {
          return { ok: false, error: "WB API не предоставляет доступных action-ов для одобрения" }
        }

        // 1. PATCH WB первым — если ошибка, Decision не создаётся
        try {
          await wbApproveReturn(ticket.wbExternalId, wbAction)
        } catch (err) {
          const msg = err instanceof Error ? err.message : "Неизвестная ошибка"
          return { ok: false, error: `WB: ${msg}` }
        }

        // 2. Transaction: Decision + ticket update
        await prisma.$transaction([
          prisma.returnDecision.create({
            data: {
              ticketId: ticket.id,
              action: "APPROVE",
              wbAction,
              reason: null,
              decidedById: userId,
              reconsidered: false,
              wbResponseOk: true,
            },
          }),
          prisma.supportTicket.update({
            where: { id: ticket.id },
            data: { returnState: "APPROVED", status: "ANSWERED", resolvedAt: new Date() },
          }),
        ])

        revalidatePath("/support/returns")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }

    export async function rejectReturn(ticketId: string, reason: string): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const trimmed = reason.trim()
        if (trimmed.length < 10 || trimmed.length > 1000) {
          return { ok: false, error: "Причина должна быть от 10 до 1000 символов" }
        }

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: ticketId },
          select: { id: true, channel: true, wbExternalId: true, returnState: true, wbActions: true },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (ticket.channel !== "RETURN") return { ok: false, error: "Не RETURN-тикет" }
        if (ticket.returnState !== "PENDING") {
          return { ok: false, error: "Отклонить можно только из PENDING" }
        }
        if (!ticket.wbExternalId) return { ok: false, error: "У тикета нет wbExternalId" }
        if (!ticket.wbActions.includes("rejectcustom")) {
          return { ok: false, error: "WB не предоставляет rejectcustom для этой заявки" }
        }

        try {
          await wbRejectReturn(ticket.wbExternalId, trimmed)
        } catch (err) {
          const msg = err instanceof Error ? err.message : "Неизвестная ошибка"
          return { ok: false, error: `WB: ${msg}` }
        }

        await prisma.$transaction([
          prisma.returnDecision.create({
            data: {
              ticketId: ticket.id,
              action: "REJECT",
              wbAction: "rejectcustom",
              reason: trimmed,
              decidedById: userId,
              reconsidered: false,
              wbResponseOk: true,
            },
          }),
          prisma.supportTicket.update({
            where: { id: ticket.id },
            data: { returnState: "REJECTED", status: "IN_PROGRESS" },
          }),
        ])

        revalidatePath("/support/returns")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }

    export async function reconsiderReturn(ticketId: string): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: ticketId },
          select: { id: true, channel: true, wbExternalId: true, returnState: true, wbActions: true },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (ticket.channel !== "RETURN") return { ok: false, error: "Не RETURN-тикет" }
        if (ticket.returnState !== "REJECTED") {
          return { ok: false, error: "Пересмотреть можно только отклонённые" }
        }
        if (!ticket.wbExternalId) return { ok: false, error: "У тикета нет wbExternalId" }
        if (!ticket.wbActions.includes("approve1")) {
          return { ok: false, error: "WB не позволяет пересмотреть эту заявку" }
        }

        try {
          await wbReconsiderReturn(ticket.wbExternalId, "approve1")
        } catch (err) {
          const msg = err instanceof Error ? err.message : "Неизвестная ошибка"
          return { ok: false, error: `WB: ${msg}` }
        }

        await prisma.$transaction([
          prisma.returnDecision.create({
            data: {
              ticketId: ticket.id,
              action: "RECONSIDER",
              wbAction: "approve1",
              reason: null,
              decidedById: userId,
              reconsidered: true,
              wbResponseOk: true,
            },
          }),
          prisma.supportTicket.update({
            where: { id: ticket.id },
            data: { returnState: "APPROVED", status: "ANSWERED", resolvedAt: new Date() },
          }),
        ])

        revalidatePath("/support/returns")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }
    ```

    **Шаг B — создать `tests/return-actions.test.ts`** (заменить Wave 0 stub):

    **КРИТИЧНО:** моки должны соответствовать реальным контрактам:
    - `requireSection` → `Promise<void>`, мокаем как `.mockResolvedValue(undefined)` (или `.mockRejectedValue(new Error(...))` для RBAC-reject)
    - `auth()` → возвращает session с `user.id` — мок **обязателен** для `getSessionUserId`
    - `$transaction` должен поддерживать обе формы (array + callback — единый mock).

    Паттерн из `tests/support-actions.test.ts` Phase 8. Скелет (минимум 10 GREEN):

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))

    // ⚠ requireSection возвращает Promise<void>, НЕ session.
    vi.mock("@/lib/rbac", () => ({
      requireSection: vi.fn().mockResolvedValue(undefined),
    }))

    // ⚠ userId приходит из auth(), а не из requireSection. Обязателен мок.
    vi.mock("@/lib/auth", () => ({
      auth: vi.fn().mockResolvedValue({ user: { id: "user-1" } }),
    }))

    vi.mock("@/lib/wb-support-api", () => ({
      approveReturn: vi.fn(),
      rejectReturn: vi.fn(),
      reconsiderReturn: vi.fn(),
      // reply* функции требуются импортами в app/actions/support.ts — мокаем
      replyFeedback: vi.fn(),
      replyQuestion: vi.fn(),
    }))

    // Единый mock Prisma — работает и для callback, и для array-формы $transaction.
    // (тот же паттерн использует Plan 09-02)
    const prismaMock = {
      supportTicket: { findUnique: vi.fn(), update: vi.fn() },
      supportMessage: { findFirst: vi.fn(), create: vi.fn() },
      supportMedia: { create: vi.fn() },
      returnDecision: { create: vi.fn() },
      $transaction: vi.fn(async (arg: unknown) => {
        if (Array.isArray(arg)) return Promise.all(arg as unknown[])
        if (typeof arg === "function") return (arg as (tx: unknown) => unknown)(prismaMock)
        return arg
      }),
    }
    vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

    beforeEach(() => {
      vi.clearAllMocks()
      // восстановить дефолтные успешные моки
      prismaMock.returnDecision.create.mockResolvedValue({ id: "d-1" })
      prismaMock.supportTicket.update.mockResolvedValue({ id: "t-1" })
    })

    async function setupMocks(options: {
      sectionOk?: boolean
      userId?: string | null
      ticket?: unknown
      wbOk?: boolean
    } = {}) {
      const rbac = await import("@/lib/rbac")
      const authMod = await import("@/lib/auth")
      const wbApi = await import("@/lib/wb-support-api")

      if (options.sectionOk === false) {
        ;(rbac.requireSection as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error("FORBIDDEN"))
      } else {
        ;(rbac.requireSection as ReturnType<typeof vi.fn>).mockResolvedValueOnce(undefined)
      }

      ;(authMod.auth as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
        options.userId === null ? null : { user: { id: options.userId ?? "user-1" } }
      )

      prismaMock.supportTicket.findUnique.mockResolvedValueOnce(options.ticket ?? null)

      if (options.wbOk === false) {
        ;(wbApi.approveReturn as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error("WB 500"))
        ;(wbApi.rejectReturn as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error("WB 500"))
        ;(wbApi.reconsiderReturn as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error("WB 500"))
      } else {
        ;(wbApi.approveReturn as ReturnType<typeof vi.fn>).mockResolvedValueOnce({ ok: true })
        ;(wbApi.rejectReturn as ReturnType<typeof vi.fn>).mockResolvedValueOnce({ ok: true })
        ;(wbApi.reconsiderReturn as ReturnType<typeof vi.fn>).mockResolvedValueOnce({ ok: true })
      }

      return { rbac, authMod, wbApi, prismaMock }
    }

    const PENDING_TICKET = {
      id: "t-1",
      channel: "RETURN",
      wbExternalId: "uuid-1",
      returnState: "PENDING",
      wbActions: ["approve1", "autorefund1", "rejectcustom"],
    }
    const REJECTED_TICKET = { ...PENDING_TICKET, returnState: "REJECTED", wbActions: ["approve1"] }
    const APPROVED_TICKET = { ...PENDING_TICKET, returnState: "APPROVED" }

    describe("approveReturn", () => {
      it("happy path PENDING → APPROVED: создаёт ReturnDecision APPROVE + обновляет ticket", async () => {
        const mocks = await setupMocks({ ticket: PENDING_TICKET })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res).toEqual({ ok: true })
        expect(mocks.wbApi.approveReturn).toHaveBeenCalledWith("uuid-1", "approve1")
        expect(prismaMock.returnDecision.create).toHaveBeenCalledWith({
          data: expect.objectContaining({
            ticketId: "t-1",
            action: "APPROVE",
            wbAction: "approve1",
            decidedById: "user-1",
            reconsidered: false,
          }),
        })
        expect(prismaMock.supportTicket.update).toHaveBeenCalledWith({
          where: { id: "t-1" },
          data: expect.objectContaining({ returnState: "APPROVED", status: "ANSWERED" }),
        })
      })

      it("reject если channel !== RETURN", async () => {
        await setupMocks({ ticket: { ...PENDING_TICKET, channel: "FEEDBACK" } })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res).toEqual({ ok: false, error: "Не RETURN-тикет" })
      })

      it("reject если returnState === APPROVED (финал)", async () => {
        await setupMocks({ ticket: APPROVED_TICKET })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res).toEqual({ ok: false, error: "Возврат уже одобрен (финал)" })
      })

      it("reject если returnState === REJECTED → направляет на reconsiderReturn", async () => {
        await setupMocks({ ticket: REJECTED_TICKET })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res.ok).toBe(false)
        if (!res.ok) expect(res.error).toContain("reconsiderReturn")
      })

      it("НЕ создаёт Decision если WB API вернул ошибку", async () => {
        await setupMocks({ ticket: PENDING_TICKET, wbOk: false })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res.ok).toBe(false)
        if (!res.ok) expect(res.error).toContain("WB")
        expect(prismaMock.returnDecision.create).not.toHaveBeenCalled()
        expect(prismaMock.supportTicket.update).not.toHaveBeenCalled()
      })

      it("reject если ни approve1/autorefund1/approvecc1 нет в wbActions", async () => {
        await setupMocks({
          ticket: { ...PENDING_TICKET, wbActions: ["rejectcustom"] },
        })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res.ok).toBe(false)
        if (!res.ok) expect(res.error).toContain("action-ов для одобрения")
      })
    })

    describe("rejectReturn", () => {
      it("happy path PENDING → REJECTED: создаёт Decision REJECT с reason", async () => {
        const mocks = await setupMocks({ ticket: PENDING_TICKET })
        const { rejectReturn } = await import("@/app/actions/support")
        const res = await rejectReturn("t-1", "Фото не соответствует товару из заявки")
        expect(res.ok).toBe(true)
        expect(mocks.wbApi.rejectReturn).toHaveBeenCalledWith("uuid-1", "Фото не соответствует товару из заявки")
        expect(prismaMock.returnDecision.create).toHaveBeenCalledWith({
          data: expect.objectContaining({
            action: "REJECT",
            wbAction: "rejectcustom",
            reason: "Фото не соответствует товару из заявки",
          }),
        })
      })

      it("validation: reason < 10 символов", async () => {
        await setupMocks({ ticket: PENDING_TICKET })
        const { rejectReturn } = await import("@/app/actions/support")
        const res = await rejectReturn("t-1", "Коротко")
        expect(res).toEqual({ ok: false, error: "Причина должна быть от 10 до 1000 символов" })
      })

      it("validation: reason > 1000 символов", async () => {
        await setupMocks({ ticket: PENDING_TICKET })
        const { rejectReturn } = await import("@/app/actions/support")
        const res = await rejectReturn("t-1", "x".repeat(1001))
        expect(res).toEqual({ ok: false, error: "Причина должна быть от 10 до 1000 символов" })
      })

      it("reject если returnState !== PENDING", async () => {
        await setupMocks({ ticket: REJECTED_TICKET })
        const { rejectReturn } = await import("@/app/actions/support")
        const res = await rejectReturn("t-1", "Валидная причина минимум 10")
        expect(res).toEqual({ ok: false, error: "Отклонить можно только из PENDING" })
      })
    })

    describe("reconsiderReturn", () => {
      it("happy path REJECTED → APPROVED + Decision{reconsidered:true}", async () => {
        const mocks = await setupMocks({ ticket: REJECTED_TICKET })
        const { reconsiderReturn } = await import("@/app/actions/support")
        const res = await reconsiderReturn("t-1")
        expect(res.ok).toBe(true)
        expect(mocks.wbApi.reconsiderReturn).toHaveBeenCalledWith("uuid-1", "approve1")
        expect(prismaMock.returnDecision.create).toHaveBeenCalledWith({
          data: expect.objectContaining({ action: "RECONSIDER", reconsidered: true }),
        })
      })

      it("reject если returnState === PENDING", async () => {
        await setupMocks({ ticket: PENDING_TICKET })
        const { reconsiderReturn } = await import("@/app/actions/support")
        const res = await reconsiderReturn("t-1")
        expect(res).toEqual({ ok: false, error: "Пересмотреть можно только отклонённые" })
      })

      it("reject если approve1 не в wbActions", async () => {
        await setupMocks({
          ticket: { ...REJECTED_TICKET, wbActions: [] },
        })
        const { reconsiderReturn } = await import("@/app/actions/support")
        const res = await reconsiderReturn("t-1")
        expect(res.ok).toBe(false)
        if (!res.ok) expect(res.error).toContain("не позволяет пересмотреть")
      })
    })

    describe("RBAC: все 3 action требуют SUPPORT+MANAGE", () => {
      it("VIEWER получает reject на approveReturn (requireSection throws)", async () => {
        await setupMocks({ sectionOk: false, ticket: PENDING_TICKET })
        const { approveReturn } = await import("@/app/actions/support")
        const res = await approveReturn("t-1")
        expect(res.ok).toBe(false)
      })
    })

    describe("revalidatePath вызван после успеха", () => {
      it("approveReturn: revalidates /support/returns и /support/[id]", async () => {
        await setupMocks({ ticket: PENDING_TICKET })
        const { revalidatePath } = await import("next/cache")
        const { approveReturn } = await import("@/app/actions/support")
        await approveReturn("t-1")
        expect(revalidatePath).toHaveBeenCalledWith("/support/returns")
        expect(revalidatePath).toHaveBeenCalledWith("/support/t-1")
      })
    })
    ```

    **Шаг C — запустить тесты:** `npm run test -- tests/return-actions.test.ts` → минимум 10 GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/return-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function approveReturn" app/actions/support.ts` == 1
    - `grep -c "export async function rejectReturn" app/actions/support.ts` == 1
    - `grep -c "export async function reconsiderReturn" app/actions/support.ts` == 1
    - `grep -c "await requireSection(\"SUPPORT\", \"MANAGE\")" app/actions/support.ts` >= 6 (3 existing Phase 8 + 3 new Phase 9)
    - `grep -c "getSessionUserId()" app/actions/support.ts` >= 6 (3 existing + 3 new reuse)
    - `! grep -q "const session = await requireSection" app/actions/support.ts` — паттерн НЕ используется (requireSection возвращает void)
    - `grep "returnDecision.create" app/actions/support.ts | wc -l` >= 3
    - `grep "revalidatePath(\`/support/\${ticketId}\`)\|revalidatePath(\"/support/returns\")" app/actions/support.ts | wc -l` >= 6 (3 actions × 2 revalidates)
    - `grep -c "it(" tests/return-actions.test.ts` >= 10 (не it.skip)
    - `grep "vi.mock(\"@/lib/auth\"" tests/return-actions.test.ts` находит mock auth
    - `grep "mockResolvedValue(undefined)" tests/return-actions.test.ts` находит правильный mock requireSection
    - `! grep -q "requireSection.*mockResolvedValue.*user:" tests/return-actions.test.ts` — НЕ мокаем requireSection с session (неправильный контракт)
    - `npm run test -- tests/return-actions.test.ts` exit 0 (10+ GREEN)
    - `npm run test` exit 0 (Phase 7/8/9 regression clean)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    3 новых server action экспортируются из app/actions/support.ts, используют Phase 8 паттерн `await requireSection(...)` + `await getSessionUserId()` (helper из строк 24-27), реализуют state machine guards SUP-20 (PENDING→APPROVED/REJECTED, REJECTED→APPROVED, APPROVED финал), транзакционный порядок WB-first/Decision-second защищает от inconsistent state на WB ошибках; tests/return-actions.test.ts содержит минимум 10 GREEN тестов с корректными моками (requireSection → void, auth() → session с userId); полная suite GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: ReturnActionsPanel + ReturnInfoPanel + интеграция в диалог /support/[ticketId]</name>
  <files>components/support/ReturnActionsPanel.tsx, components/support/ReturnInfoPanel.tsx, app/(dashboard)/support/[ticketId]/page.tsx</files>
  <read_first>
    - components/support/ReplyPanel.tsx (Phase 8 паттерн client sticky-панели с textarea + submit + sonner toast)
    - components/support/TicketSidePanel.tsx (Phase 8 паттерн правой колонки с native select для status/assignee)
    - app/(dashboard)/support/[ticketId]/page.tsx (строки 100-167 — структура 3-колоночного layout)
    - app/actions/support.ts (задача 1 — 3 server actions для клиента)
    - CLAUDE.md (русский UI, native HTML select, sonner toast)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «UI Architecture — ReturnActionsPanel» строки 548-609)
  </read_first>
  <action>
    **Шаг A — создать `components/support/ReturnActionsPanel.tsx`** (client sticky-панель):

    ```typescript
    "use client"

    import { useState, useTransition } from "react"
    import { toast } from "sonner"
    import { Button } from "@/components/ui/button"
    import { approveReturn, rejectReturn, reconsiderReturn } from "@/app/actions/support"
    import type { ReturnState } from "@prisma/client"

    export interface ReturnActionsPanelProps {
      ticketId: string
      returnState: ReturnState | null
      wbActions: string[]
    }

    export function ReturnActionsPanel({ ticketId, returnState, wbActions }: ReturnActionsPanelProps) {
      const [isPending, startTransition] = useTransition()
      const [rejectOpen, setRejectOpen] = useState(false)
      const [reason, setReason] = useState("")

      const canApprove =
        returnState === "PENDING" &&
        (wbActions.includes("approve1") || wbActions.includes("autorefund1") || wbActions.includes("approvecc1"))
      const canReject = returnState === "PENDING" && wbActions.includes("rejectcustom")
      const canReconsider = returnState === "REJECTED" && wbActions.includes("approve1")

      const handleApprove = () => {
        if (!confirm("Одобрить возврат?")) return
        const id = toast.loading("Отправка решения в WB...")
        startTransition(async () => {
          const res = await approveReturn(ticketId)
          if (res.ok) toast.success("Возврат одобрен", { id })
          else toast.error(res.error, { id })
        })
      }

      const handleReject = () => {
        const trimmed = reason.trim()
        if (trimmed.length < 10 || trimmed.length > 1000) {
          toast.error("Причина должна быть от 10 до 1000 символов")
          return
        }
        const id = toast.loading("Отправка отклонения в WB...")
        startTransition(async () => {
          const res = await rejectReturn(ticketId, trimmed)
          if (res.ok) {
            toast.success("Возврат отклонён", { id })
            setRejectOpen(false)
            setReason("")
          } else {
            toast.error(res.error, { id })
          }
        })
      }

      const handleReconsider = () => {
        if (!confirm("Пересмотреть отклонённое решение и одобрить возврат?")) return
        const id = toast.loading("Отправка пересмотра в WB...")
        startTransition(async () => {
          const res = await reconsiderReturn(ticketId)
          if (res.ok) toast.success("Возврат одобрен (пересмотрено)", { id })
          else toast.error(res.error, { id })
        })
      }

      if (returnState === "APPROVED") {
        return (
          <div className="sticky bottom-0 border-t bg-background p-3 text-sm text-muted-foreground text-center">
            Возврат одобрен — действия завершены
          </div>
        )
      }

      return (
        <>
          <div className="sticky bottom-0 border-t bg-background p-3 flex items-center gap-2">
            {returnState === "PENDING" && (
              <>
                <Button onClick={handleApprove} disabled={!canApprove || isPending}>
                  Одобрить
                </Button>
                <Button variant="destructive" onClick={() => setRejectOpen(true)} disabled={!canReject || isPending}>
                  Отклонить
                </Button>
              </>
            )}
            {returnState === "REJECTED" && (
              <Button onClick={handleReconsider} disabled={!canReconsider || isPending}>
                Пересмотреть
              </Button>
            )}
            {!canApprove && !canReject && !canReconsider && returnState !== "APPROVED" && (
              <span className="text-xs text-muted-foreground">
                WB не предоставляет доступных действий для этой заявки
              </span>
            )}
          </div>

          {rejectOpen && (
            <div
              className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4"
              onClick={() => !isPending && setRejectOpen(false)}
            >
              <div
                className="bg-background rounded-lg p-6 w-full max-w-md space-y-3"
                onClick={(e) => e.stopPropagation()}
              >
                <h2 className="text-lg font-semibold">Отклонить возврат</h2>
                <p className="text-sm text-muted-foreground">
                  Укажите причину (от 10 до 1000 символов). Покупатель увидит этот текст.
                </p>
                <textarea
                  value={reason}
                  onChange={(e) => setReason(e.target.value)}
                  rows={6}
                  maxLength={1000}
                  className="w-full border rounded p-2 text-sm"
                  placeholder="Причина отклонения..."
                  disabled={isPending}
                />
                <div className="flex items-center justify-between text-xs text-muted-foreground">
                  <span>{reason.trim().length}/1000</span>
                  <span>{reason.trim().length < 10 ? `Нужно ещё ${10 - reason.trim().length} символов` : "ОК"}</span>
                </div>
                <div className="flex justify-end gap-2">
                  <Button variant="outline" onClick={() => setRejectOpen(false)} disabled={isPending}>
                    Отмена
                  </Button>
                  <Button
                    variant="destructive"
                    onClick={handleReject}
                    disabled={isPending || reason.trim().length < 10 || reason.trim().length > 1000}
                  >
                    Отклонить
                  </Button>
                </div>
              </div>
            </div>
          )}
        </>
      )
    }
    ```

    ⚠ **Примечание:** базовый shadcn `Button` должен быть импортируемым (существующий в проекте). Если модалка требует base-ui Dialog — использовать простую custom overlay (как выше) или существующий Dialog компонент из проекта.

    **Шаг B — создать `components/support/ReturnInfoPanel.tsx`** (блок в левой колонке):

    ```typescript
    "use client"

    import { useState } from "react"

    export interface ReturnInfoPanelProps {
      price: number | null
      wbComment: string | null
      srid: string | null
    }

    export function ReturnInfoPanel({ price, wbComment, srid }: ReturnInfoPanelProps) {
      const [expanded, setExpanded] = useState(false)

      return (
        <section className="mt-4 pt-4 border-t space-y-2">
          <h3 className="text-sm font-semibold text-muted-foreground">Информация о возврате</h3>
          {price !== null && price !== undefined && (
            <div className="text-sm">
              <span className="text-muted-foreground">Сумма: </span>
              <span className="font-medium">{price.toFixed(2)} ₽</span>
            </div>
          )}
          {srid && (
            <div className="text-xs">
              <span className="text-muted-foreground">Shipment ID: </span>
              <code className="bg-muted px-1 rounded">{srid}</code>
            </div>
          )}
          {wbComment && (
            <div className="text-xs">
              <button
                className="text-primary hover:underline"
                onClick={() => setExpanded((v) => !v)}
              >
                {expanded ? "Свернуть инструкцию WB" : "Показать инструкцию WB"}
              </button>
              {expanded && (
                <p className="mt-1 text-muted-foreground whitespace-pre-wrap">{wbComment}</p>
              )}
            </div>
          )}
        </section>
      )
    }
    ```

    **Шаг C — обновить `app/(dashboard)/support/[ticketId]/page.tsx`:**

    1. Расширить select ticket (добавить `returnState`, `wbActions`, `wbComment`, `srid`, `price` в существующий include/select) + preload `latestDecision`:

       Найти `const ticket = await prisma.supportTicket.findUnique(...)` и добавить в select/include новые Phase 9 поля. Если используется include без select, добавить `returnDecisions: { orderBy: { decidedAt: "desc" }, take: 1, include: { decidedBy: true } }`.

    2. Import новых компонентов:
       ```typescript
       import { ReturnActionsPanel } from "@/components/support/ReturnActionsPanel"
       import { ReturnInfoPanel } from "@/components/support/ReturnInfoPanel"
       ```

    3. Изменить логику `canReply` + добавить `isReturn`:
       ```typescript
       const canReply = ticket.channel === "FEEDBACK" || ticket.channel === "QUESTION"
       const isReturn = ticket.channel === "RETURN"
       ```

    4. Заменить блок `{!canReply && (<div>...</div>)}` на:
       ```typescript
       {canReply && <ReplyPanel ticketId={ticket.id} disabled={ticket.status === "CLOSED"} />}
       {isReturn && (
         <ReturnActionsPanel
           ticketId={ticket.id}
           returnState={ticket.returnState}
           wbActions={ticket.wbActions}
         />
       )}
       {!canReply && !isReturn && (
         <div className="border-t p-3 text-xs text-muted-foreground text-center">
           Ответ через интерфейс доступен для каналов «Отзыв» и «Вопрос». Чат/мессенджер — в следующих фазах.
         </div>
       )}
       ```

    5. Добавить ReturnInfoPanel в левую колонку (aside) — после блока WbCard, если `isReturn`:
       ```typescript
       {isReturn && (
         <ReturnInfoPanel
           price={ticket.price}
           wbComment={ticket.wbComment}
           srid={ticket.srid}
         />
       )}
       ```

    **Шаг D — verification:**
    - `npx tsc --noEmit`
    - `npm run build`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/ReturnActionsPanel.tsx` exit 0
    - `test -f components/support/ReturnInfoPanel.tsx` exit 0
    - `grep "export function ReturnActionsPanel" components/support/ReturnActionsPanel.tsx` находит
    - `grep "approveReturn\|rejectReturn\|reconsiderReturn" components/support/ReturnActionsPanel.tsx | wc -l` >= 3
    - `grep "import.*sonner" components/support/ReturnActionsPanel.tsx` находит
    - `grep "1000\|10 до 1000" components/support/ReturnActionsPanel.tsx` находит counter/validation
    - `grep "export function ReturnInfoPanel" components/support/ReturnInfoPanel.tsx` находит
    - `grep "ReturnActionsPanel" app/(dashboard)/support/[ticketId]/page.tsx` находит import и render
    - `grep "isReturn\|channel === \"RETURN\"" app/(dashboard)/support/[ticketId]/page.tsx` находит conditional
    - `grep "ReturnInfoPanel" app/(dashboard)/support/[ticketId]/page.tsx` находит render в aside
    - `grep "returnState\|wbActions" app/(dashboard)/support/[ticketId]/page.tsx` находит передачу props
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    ReturnActionsPanel — sticky client-компонент с 3 кнопками зависящими от returnState и wbActions, модалкой Отклонить с textarea counter + validation, sonner toast-ами; ReturnInfoPanel — блок price/srid/wbComment (collapsible) в левой колонке; /support/[ticketId] правильно conditional рендерит ReturnActionsPanel для channel===RETURN и ReturnInfoPanel в aside; tsc + build проходят.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>app/(dashboard)/support/returns/page.tsx, app/(dashboard)/support/[ticketId]/page.tsx, deploy.sh</files>
  <name>Задача 3: Deploy на VPS + Human UAT — end-to-end flow возвратов</name>
  <what-built>
    - Phase 9 полный комплект: БД миграция, WB Claims API клиент, sync логика в cron, страница /support/returns, ReturnActionsPanel в диалоге, 3 server actions
    - Все unit/integration тесты GREEN (09-01 + 09-02 + 09-04 тесты + regression Phase 7/8)
    - Wave 0 VPS scope bit 11 проверен (pre-flight перед 09-01)
  </what-built>
  <how-to-verify>
    **Перед UAT — задача execute-phase выполнит deploy:**

    1. Commit всех изменений Plan 09-04 (автоматически после предыдущих задач)
    2. На VPS: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — прогнать migration deploy + rebuild + systemctl restart
    3. Проверить статус: `systemctl status zoiten-erp.service` — active (running)
    4. Проверить что nginx → 3001 работает: `curl -sI https://zoiten.pro/support/returns` → 200 или 302 на login

    **Human UAT checklist** (выполнить в браузере):

    **Подготовка:**
    - [ ] Залогинен суперадмином (sergey.fyodorov@gmail.com) или пользователем с SUPPORT+MANAGE
    - [ ] Есть хотя бы одна заявка на возврат в WB (или создать тестовую если тестовый аккаунт WB)

    **Раздел /support/returns:**
    - [ ] Открыть https://zoiten.pro/support/returns — страница рендерится без ошибок
    - [ ] Если заявок нет — видно сообщение «Заявок на возврат пока нет. Нажмите Синхронизировать»
    - [ ] Клик «Синхронизировать» → toast loading → success → после revalidate появляются тикеты в таблице
    - [ ] Таблица содержит 9 колонок (Товар/Покупатель/Причина/Фото брака/Дата/Решение/Кто принял/Пересмотрено/Действия)
    - [ ] Колонка «Товар» содержит фото (12×9), название, nmID
    - [ ] Даты в Moscow timezone (ru-RU формат dd.MM.yyyy HH:mm)
    - [ ] Sidebar содержит новый пункт «Возвраты» с иконкой (PackageX или выбранная)
    - [ ] Клик на строку «Открыть» → переход на /support/{ticketId}
    - [ ] Фильтры работают (returnStates/nmId/assignees/dateFrom/dateTo/reconsideredOnly) — запросы в URL + таблица обновляется

    **Диалог /support/[ticketId] канала RETURN:**
    - [ ] 3-колоночный layout как Phase 8 (покупатель+товар слева, сообщения по центру, статус/мета справа)
    - [ ] Вместо ReplyPanel внизу центральной колонки видна ReturnActionsPanel
    - [ ] В левой колонке под карточкой товара виден блок ReturnInfoPanel (цена, srid, раскрываемая инструкция WB)
    - [ ] Для PENDING тикета: видны кнопки «Одобрить» и «Отклонить» (enabled если wbActions содержат соответствующие)
    - [ ] Для REJECTED тикета: видна кнопка «Пересмотреть» (enabled если approve1 в wbActions)
    - [ ] Для APPROVED тикета: видно «Возврат одобрен — действия завершены»

    **Actions (⚠ отправка в WB, необратимо — на тест-заявке):**
    - [ ] Тест 1: выбрать PENDING тикет → «Одобрить» → confirm → toast loading → success → returnState=APPROVED → кнопки скрываются, видно readonly сообщение
    - [ ] Проверить в seller.wildberries.ru — заявка показана как одобренная
    - [ ] Проверить в /support/returns — тикет в колонке «Решение» = «Одобрен» (зелёный badge), «Кто принял» = ваше имя + дата
    - [ ] Тест 2: другой PENDING → «Отклонить» → модалка → ввод reason < 10 символов → Submit disabled, видно «Нужно ещё N символов»
    - [ ] Ввести валидную причину (> 10 символов) → Submit enabled → отправить → returnState=REJECTED → появляется «Пересмотреть»
    - [ ] Проверить в seller.wildberries.ru — rejection с нашим comment-ом
    - [ ] Тест 3: только что отклонённый тикет → «Пересмотреть» → confirm → toast → returnState=APPROVED, в Decision reconsidered=true
    - [ ] В /support/returns колонка «Пересмотрено» показывает «Да» (зелёный)

    **RBAC:**
    - [ ] Залогиниться под пользователем VIEWER (SUPPORT=VIEW) → /support/returns открывается, но action-кнопки вызывают toast error «Нет доступа» (server action reject)

    **Cron:**
    - [ ] Дождаться следующего cron tick (15 мин) или вручную `ssh root@85.198.97.89 "curl -s http://localhost:3001/api/cron/support-sync-reviews -H x-cron-secret:$CRON_SECRET"` → response содержит `{ ok: true, support: {...}, returns: {...} }`
    - [ ] `journalctl -u zoiten-erp.service -f | grep -i "return"` показывает активность syncReturns

    **Media:**
    - [ ] В таблице фото брака отображаются 3 превью 40px → клик открывает диалог
    - [ ] В диалоге в сообщении первой INBOUND — фото кликабельны на полный размер
    - [ ] Проверить `/var/www/zoiten-uploads/support/{ticketId}/{messageId}/` содержит скачанные файлы

    **Регрессия Phase 7/8:**
    - [ ] /support (лента) работает как раньше
    - [ ] /support/[ticketId] для FEEDBACK — ReplyPanel работает, ответ уходит в WB
    - [ ] /prices/wb работает
    - [ ] Фото товаров в /products открываются
  </how-to-verify>
  <resume-signal>
    Пользователь отправляет:
    - "approved" — все пункты OK, phase 9 complete
    - "issues: [список проблем]" — фиксировать gap-ы для `--gaps` replan
    - "partial: [что работает / что нет]" — создать gap-closure plan
  </resume-signal>
  <action>
    Deploy и human UAT не автоматизируются полностью — выполнить пункты из <how-to-verify>. execute-phase делает:
    1. git commit изменений (после задач 1-2)
    2. ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    3. Вывести пользователю чеклист из <how-to-verify> для ручной верификации в браузере и seller.wildberries.ru
    4. Ждать resume-signal
  </action>
  <verify>
    <automated>ssh root@85.198.97.89 "systemctl is-active zoiten-erp.service"</automated>
  </verify>
  <done>
    Deploy пройден (service active), пользователь отправил "approved" после прохождения всех пунктов UAT checklist, либо зафиксировал gap-ы для replan.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач (включая human UAT):
1. `npx prisma validate` exit 0
2. `npx tsc --noEmit` exit 0
3. `npm run build` exit 0
4. `npm run test` exit 0 (полная suite, Phase 7/8/9 GREEN, 0 failed)
5. `grep -c "export async function approveReturn\|rejectReturn\|reconsiderReturn" app/actions/support.ts` == 3
6. `grep "ReturnActionsPanel\|ReturnInfoPanel" app/(dashboard)/support/[ticketId]/page.tsx | wc -l` >= 2
7. Human UAT passed (resume-signal "approved")
8. VPS: `systemctl status zoiten-erp.service` = active, cron tick в journalctl содержит `returns: {...}`
</verification>

<success_criteria>
Plan 09-04 завершён когда:
- 3 новых server action (approveReturn/rejectReturn/reconsiderReturn) в app/actions/support.ts с RBAC + state machine guards + WB-first transaction order + переиспользование getSessionUserId helper
- ReturnActionsPanel + ReturnInfoPanel компоненты созданы и интегрированы в /support/[ticketId]
- tests/return-actions.test.ts — минимум 10 GREEN тестов с корректными моками (requireSection → void, auth() → session)
- Deploy на VPS прошёл: migration applied, service restarted
- Human UAT passed: создана/обновлена минимум 1 реальная заявка через UI с верификацией в seller.wildberries.ru
- Commit `feat(09-04): ReturnActionsPanel + 3 server actions + E2E flow возвратов`
</success_criteria>

<output>
После завершения создать `.planning/phases/09-returns/09-04-SUMMARY.md` с:
- 3 server actions: approveReturn, rejectReturn, reconsiderReturn — контракты + state machine guards + паттерн getSessionUserId
- 2 клиентских компонента: ReturnActionsPanel (sticky + модалка Отклонить + sonner), ReturnInfoPanel (левая колонка)
- Модификация /support/[ticketId]/page.tsx: conditional render для channel===RETURN
- Integration tests: 10+ GREEN в return-actions.test.ts с правильными контрактами моков
- Deploy: migration phase9_returns применена на VPS, service рестартован, cron 15-мин hit подтверждён
- Human UAT results: скриншоты / описание реальных одобрения/отклонения/пересмотра с проверкой в seller.wildberries.ru
- Известные ограничения: reconsider механика работает только если WB возвращает approve1 в actions[] после rejectcustom (валидировано на проде); если нет — toast error «WB не позволяет пересмотреть»
- Phase 9 COMPLETE marker для ROADMAP.md update
</output>
</context>
