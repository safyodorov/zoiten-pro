---
phase: 09-returns
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - app/(dashboard)/support/returns/page.tsx
  - components/support/ReturnsTable.tsx
  - components/support/ReturnsFilters.tsx
  - components/layout/nav-items.ts
autonomous: true
requirements:
  - SUP-18
must_haves:
  truths:
    - "Страница /support/returns рендерится как RSC с requireSection(\"SUPPORT\") и таблицей заявок"
    - "Таблица показывает 9 колонок: Товар (фото+nmId+название), Покупатель, Причина, Фото брака, Дата, Решение, Кто принял, Пересмотрено, Действия"
    - "Фильтры через searchParams: returnStates[] (MultiSelectDropdown), nmId (text), assignees[] (MultiSelectDropdown), dateFrom/dateTo, reconsideredOnly toggle"
    - "Preload latest ReturnDecision per ticket через distinct ON паттерн (orderBy decidedAt desc + distinct ticketId)"
    - "Пагинация по 20 штук, паттерн SupportPagination из Phase 8"
    - "Sidebar — новый пункт «Возвраты» или подпункт под «Служба поддержки» с href /support/returns и иконкой"
    - "Пустое состояние: «Заявок на возврат пока нет» когда нет тикетов"
  artifacts:
    - path: "app/(dashboard)/support/returns/page.tsx"
      provides: "RSC страница таблицы заявок с фильтрами и пагинацией"
      contains: "requireSection(\"SUPPORT\")"
    - path: "components/support/ReturnsTable.tsx"
      provides: "Таблица 9 колонок, рендерит SupportTicket[] + latestDecision per ticket"
      exports: ["ReturnsTable"]
    - path: "components/support/ReturnsFilters.tsx"
      provides: "MultiSelectDropdown + inputs через searchParams"
      exports: ["ReturnsFilters"]
    - path: "components/layout/nav-items.ts"
      provides: "Добавлен пункт «Возвраты»"
      contains: "/support/returns"
  key_links:
    - from: "app/(dashboard)/support/returns/page.tsx"
      to: "prisma.supportTicket.findMany"
      via: "where = { channel: \"RETURN\", returnState: { in: ... } } + buildWhere(searchParams)"
      pattern: "channel:\\s*\"RETURN\""
    - from: "app/(dashboard)/support/returns/page.tsx"
      to: "prisma.returnDecision.findMany"
      via: "distinct: [ticketId] + orderBy decidedAt desc — preload latest decision per ticket"
      pattern: "returnDecision\\.findMany"
    - from: "components/support/ReturnsTable.tsx"
      to: "next/link /support/{ticketId}"
      via: "row click → диалог с ReturnActionsPanel (Plan 09-04)"
      pattern: "/support/\\$\\{|/support/\\["
    - from: "components/layout/nav-items.ts"
      to: "app/(dashboard)/support/returns/page.tsx"
      via: "NAV_ITEMS entry with section SUPPORT, href /support/returns"
      pattern: "/support/returns"
---

<objective>
Phase 9 Plan 03 — страница-таблица заявок на возврат `/support/returns`:
1. Новая RSC-страница `app/(dashboard)/support/returns/page.tsx` — таблица заявок (тикетов канала RETURN), 9 колонок (Товар, Покупатель, Причина, Фото брака, Дата, Решение, Кто принял, Пересмотрено, Действия), пагинация по 20, фильтры через searchParams.
2. Клиентские компоненты: `ReturnsTable` (рендерит SupportTicket[] с preload WbCard/latestDecision/media) и `ReturnsFilters` (MultiSelectDropdown + inputs).
3. Sidebar: добавить пункт «Возвраты» в `components/layout/nav-items.ts` (section: "SUPPORT", href: "/support/returns", icon: "PackageX" или любой Lucide подходящий из уже импортированных — если нужен новый, добавить в ICON_MAP).
4. Reuse: `SupportPagination`, `MultiSelectDropdown` из Phase 8/проекта.

Purpose: Первый UI поверх sync-данных Plan 09-02. Отдельный route (а не фильтр на `/support`) — чтобы у менеджера было специализированное рабочее место с recruitment-оптимизированным layout. Независимо от Plan 09-04 (actions-panel в диалоге) — клик по строке → переход на `/support/{ticketId}`, где Plan 09-04 добавляет ReturnActionsPanel.

Output: 1 новая RSC-страница, 2 новых компонента в `components/support/`, 1 обновлённый nav-items.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-returns/09-RESEARCH.md
@.planning/phases/09-returns/09-01-SUMMARY.md
@.planning/phases/09-returns/09-02-SUMMARY.md
@.planning/phases/08-support-mvp/08-03-SUMMARY.md
@CLAUDE.md
@app/(dashboard)/support/page.tsx
@components/support/SupportTicketCard.tsx
@components/support/SupportFilters.tsx
@components/support/SupportPagination.tsx
@components/layout/nav-items.ts
@components/cards/WbFilters.tsx
@lib/rbac.ts
@lib/prisma.ts

<interfaces>
**Новые Prisma модели (Plan 09-01):**
```prisma
model SupportTicket {
  // ...existing + Phase 9 fields...
  returnState     ReturnState?       // PENDING | APPROVED | REJECTED
  wbComment       String?
  wbActions       String[]
  srid            String?
  price           Float?
  returnDecisions ReturnDecision[]
  messages        SupportMessage[]
}
model ReturnDecision {
  id          String; ticketId String; action ReturnDecisionAction
  wbAction    String; reason String?; decidedById String; decidedAt DateTime
  reconsidered Boolean
  decidedBy   User  // relation "ReturnDecider"
}
enum ReturnState { PENDING; APPROVED; REJECTED }
```

**Существующие Phase 8 компоненты (reuse):**
- `components/support/SupportPagination.tsx` — (page, pageSize, total) → navigation через searchParams
- `components/support/SupportFilters.tsx` — паттерн MultiSelectDropdown + nmId input + date inputs
- `components/ui/MultiSelectDropdown.tsx` (или аналог) — с чекбоксами, фильтры через searchParams
- `lib/rbac.ts` → `requireSection("SUPPORT")` для RSC

**Существующие Prisma modes для preload photo:**
```typescript
// WbCard для фото по nmId:
const wbCards = await prisma.wbCard.findMany({
  where: { nmId: { in: nmIds } },
  select: { nmId: true, name: true, photoUrl: true }  // или то поле, где хранится URL
})
```
(Смотреть реальную структуру WbCard в prisma/schema.prisma — photos может быть Json массивом.)

**Реальные users с SUPPORT access (для фильтра assignees):**
```typescript
const supportUsers = await prisma.user.findMany({
  where: { isActive: true, sectionRoles: { some: { section: "SUPPORT" } } },
  select: { id: true, firstName: true, lastName: true, name: true },
})
```

**Distinct-on latest decision per ticket (Postgres):**
```typescript
// Preload latest ReturnDecision per ticket одним запросом:
const decisions = await prisma.returnDecision.findMany({
  where: { ticketId: { in: ticketIds } },
  orderBy: [{ ticketId: "asc" }, { decidedAt: "desc" }],
  distinct: ["ticketId"],  // Prisma PostgreSQL distinct
  include: { decidedBy: { select: { id: true, firstName: true, lastName: true, name: true } } },
})
// Преобразовать в Map<ticketId, Decision>
const decisionByTicket = new Map(decisions.map((d) => [d.ticketId, d]))
```

**Для "reconsideredOnly" фильтра:**
```typescript
// Тикет "пересмотрен" если есть хотя бы одна Decision с reconsidered=true
// where.returnDecisions: { some: { reconsidered: true } }
```

**Паттерн Lucide icon для sidebar:** В nav-items.ts уже импортируются `Package, CreditCard, Tag, ...`. Для возвратов подходят `PackageX`, `ArchiveRestore`, `Undo2`. Выбрать один и добавить в imports + ICON_MAP.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: RSC-страница /support/returns + ReturnsTable компонент</name>
  <files>app/(dashboard)/support/returns/page.tsx, components/support/ReturnsTable.tsx</files>
  <read_first>
    - app/(dashboard)/support/page.tsx (Phase 8 паттерн RSC — session + fetch + render SupportTicketCard + SupportPagination + SupportFilters)
    - components/support/SupportTicketCard.tsx (паттерн карточки тикета с индикатор-полосой)
    - components/support/SupportPagination.tsx (reuse как есть)
    - prisma/schema.prisma (смотреть WbCard — какое поле содержит URL фото: `photoUrl` или `photos: Json`)
    - app/(dashboard)/support/[ticketId]/page.tsx (паттерн include media в message — photos первого INBOUND)
    - lib/rbac.ts (requireSection)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «UI Architecture — /support/returns» строки 499-540 — описание 9 колонок и паттерн preload decisions)
  </read_first>
  <action>
    **Шаг A — создать `app/(dashboard)/support/returns/page.tsx`:**

    ```typescript
    // /support/returns — таблица заявок на возврат (RSC, Phase 9).
    // Требует requireSection("SUPPORT"). Фильтры через searchParams.
    // Preload: WbCard (фото+название), latestDecision per ticket, media (первого INBOUND).

    import { prisma } from "@/lib/prisma"
    import { requireSection } from "@/lib/rbac"
    import { ReturnsTable } from "@/components/support/ReturnsTable"
    import { ReturnsFilters } from "@/components/support/ReturnsFilters"
    import { SupportSyncButton } from "@/components/support/SupportSyncButton" // из Phase 8
    import type { ReturnState, Prisma } from "@prisma/client"

    const PAGE_SIZE = 20

    interface PageSearchParams {
      page?: string
      returnStates?: string // CSV "PENDING,REJECTED"
      nmId?: string
      assignees?: string // CSV user ids
      dateFrom?: string
      dateTo?: string
      reconsideredOnly?: string // "1" | undefined
    }

    function parseSearchParams(sp: PageSearchParams) {
      const page = Math.max(1, parseInt(sp.page ?? "1", 10) || 1)
      const returnStates = (sp.returnStates?.split(",").filter(Boolean) ?? []) as ReturnState[]
      const nmId = sp.nmId ? parseInt(sp.nmId, 10) : undefined
      const assignees = sp.assignees?.split(",").filter(Boolean) ?? []
      const dateFrom = sp.dateFrom ? new Date(sp.dateFrom) : undefined
      const dateTo = sp.dateTo ? new Date(sp.dateTo) : undefined
      const reconsideredOnly = sp.reconsideredOnly === "1"
      return { page, returnStates, nmId, assignees, dateFrom, dateTo, reconsideredOnly }
    }

    function buildWhere(f: ReturnType<typeof parseSearchParams>): Prisma.SupportTicketWhereInput {
      const where: Prisma.SupportTicketWhereInput = { channel: "RETURN" }
      if (f.returnStates.length) where.returnState = { in: f.returnStates }
      if (f.nmId !== undefined && !Number.isNaN(f.nmId)) where.nmId = f.nmId
      if (f.assignees.length) where.assignedToId = { in: f.assignees }
      if (f.dateFrom || f.dateTo) {
        where.createdAt = {}
        if (f.dateFrom) where.createdAt.gte = f.dateFrom
        if (f.dateTo) where.createdAt.lte = f.dateTo
      }
      if (f.reconsideredOnly) {
        where.returnDecisions = { some: { reconsidered: true } }
      }
      return where
    }

    export default async function ReturnsPage({
      searchParams,
    }: {
      searchParams: Promise<PageSearchParams>
    }) {
      await requireSection("SUPPORT")
      const sp = await searchParams
      const filters = parseSearchParams(sp)
      const where = buildWhere(filters)

      const [total, tickets, supportUsers] = await Promise.all([
        prisma.supportTicket.count({ where }),
        prisma.supportTicket.findMany({
          where,
          orderBy: { createdAt: "desc" },
          skip: (filters.page - 1) * PAGE_SIZE,
          take: PAGE_SIZE,
          include: {
            messages: {
              where: { direction: "INBOUND" },
              orderBy: { sentAt: "asc" },
              take: 1,
              include: { media: { where: { type: "IMAGE" }, take: 3 } },
            },
            assignedTo: { select: { id: true, firstName: true, lastName: true, name: true } },
          },
        }),
        prisma.user.findMany({
          where: { isActive: true, sectionRoles: { some: { section: "SUPPORT" } } },
          select: { id: true, firstName: true, lastName: true, name: true },
          orderBy: [{ firstName: "asc" }, { lastName: "asc" }],
        }),
      ])

      // Preload latest ReturnDecision per ticket (distinct on)
      const ticketIds = tickets.map((t) => t.id)
      const latestDecisions =
        ticketIds.length > 0
          ? await prisma.returnDecision.findMany({
              where: { ticketId: { in: ticketIds } },
              orderBy: [{ ticketId: "asc" }, { decidedAt: "desc" }],
              distinct: ["ticketId"],
              include: {
                decidedBy: { select: { id: true, firstName: true, lastName: true, name: true } },
              },
            })
          : []
      const decisionByTicket = new Map(latestDecisions.map((d) => [d.ticketId, d]))

      // Preload WbCard фото/название
      const nmIds = tickets.map((t) => t.nmId).filter((n): n is number => typeof n === "number")
      const wbCards =
        nmIds.length > 0
          ? await prisma.wbCard.findMany({
              where: { nmId: { in: nmIds } },
              select: { nmId: true, name: true /* photoUrl or photos field — check schema */ },
            })
          : []
      const cardByNm = new Map(wbCards.map((c) => [c.nmId, c]))

      return (
        <div className="p-6 space-y-4">
          <div className="flex items-center justify-between">
            <h1 className="text-2xl font-semibold">Возвраты</h1>
            <SupportSyncButton />
          </div>

          <ReturnsFilters supportUsers={supportUsers} />

          <ReturnsTable
            tickets={tickets}
            decisionByTicket={Object.fromEntries(decisionByTicket)}
            cardByNm={Object.fromEntries(cardByNm)}
          />

          {/* Использовать существующий SupportPagination или создать ReturnsPagination — пересоздание не требуется, reuse */}
          {total > PAGE_SIZE && (
            <nav className="flex items-center justify-between">
              {/* TODO: reuse SupportPagination: передать total/page/pageSize */}
            </nav>
          )}

          {total === 0 && (
            <div className="text-center py-12 text-muted-foreground">
              Заявок на возврат пока нет. Нажмите «Синхронизировать» в шапке.
            </div>
          )}
        </div>
      )
    }
    ```

    ⚠ **КРИТИЧНО:** поле `WbCard.photoUrl` может не существовать — проверить `prisma/schema.prisma` и смотреть как `/support/[ticketId]/page.tsx` получает URL фото товара (строки 120-127 — `wbCard.photoUrl` там используется). Если поле называется иначе (`photos: Json` + первый элемент) — адаптировать select и ReturnsTable.

    ⚠ Пагинация: если `SupportPagination` существует (Phase 8), импортировать и передать total/page/PAGE_SIZE. Путь и props — смотреть в `app/(dashboard)/support/page.tsx`.

    **Шаг B — создать `components/support/ReturnsTable.tsx`** (client, рендерит таблицу):

    ```typescript
    "use client"

    import Link from "next/link"
    import Image from "next/image"
    import type { SupportTicket, ReturnDecision, User, SupportMessage, SupportMedia, ReturnState } from "@prisma/client"

    type TicketWithMessages = SupportTicket & {
      messages: (SupportMessage & { media: SupportMedia[] })[]
      assignedTo: Pick<User, "id" | "firstName" | "lastName" | "name"> | null
    }
    type DecisionWithUser = ReturnDecision & {
      decidedBy: Pick<User, "id" | "firstName" | "lastName" | "name">
    }

    export interface ReturnsTableProps {
      tickets: TicketWithMessages[]
      decisionByTicket: Record<string, DecisionWithUser>
      cardByNm: Record<number, { nmId: number; name: string | null }>
    }

    const STATE_BADGE: Record<ReturnState, { label: string; className: string }> = {
      PENDING: { label: "Ожидает", className: "bg-gray-200 text-gray-800" },
      APPROVED: { label: "Одобрен", className: "bg-green-100 text-green-800" },
      REJECTED: { label: "Отклонён", className: "bg-red-100 text-red-800" },
    }

    const MOSCOW_TZ: Intl.DateTimeFormatOptions = {
      timeZone: "Europe/Moscow",
      day: "2-digit",
      month: "2-digit",
      year: "numeric",
      hour: "2-digit",
      minute: "2-digit",
    }

    function formatUser(u: Pick<User, "firstName" | "lastName" | "name"> | null): string {
      if (!u) return "—"
      const full = [u.firstName, u.lastName].filter(Boolean).join(" ")
      return full || u.name || "—"
    }

    export function ReturnsTable({ tickets, decisionByTicket, cardByNm }: ReturnsTableProps) {
      if (tickets.length === 0) return null

      return (
        <div className="overflow-x-auto rounded border">
          <table className="w-full text-sm">
            <thead className="bg-muted">
              <tr>
                <th className="px-3 py-2 text-left">Товар</th>
                <th className="px-3 py-2 text-left">Покупатель</th>
                <th className="px-3 py-2 text-left">Причина</th>
                <th className="px-3 py-2 text-left">Фото брака</th>
                <th className="px-3 py-2 text-left">Дата</th>
                <th className="px-3 py-2 text-left">Решение</th>
                <th className="px-3 py-2 text-left">Кто принял</th>
                <th className="px-3 py-2 text-left">Пересмотрено</th>
                <th className="px-3 py-2 text-left">Действия</th>
              </tr>
            </thead>
            <tbody>
              {tickets.map((t) => {
                const card = t.nmId ? cardByNm[t.nmId] : undefined
                const decision = decisionByTicket[t.id]
                const firstMsg = t.messages[0]
                const media = firstMsg?.media ?? []
                const state = t.returnState ?? "PENDING"
                const badge = STATE_BADGE[state]
                const buyerLabel = `Покупатель #${t.id.slice(-6)}`

                return (
                  <tr key={t.id} className="border-t hover:bg-accent/20">
                    <td className="px-3 py-2">
                      <div className="flex items-center gap-2">
                        {/* Фото: если WbCard содержит поле photoUrl — рендерить. Иначе — пустышка */}
                        <div>
                          <div className="font-medium">{card?.name ?? t.wbClaimType ?? "—"}</div>
                          <div className="text-xs text-muted-foreground">
                            {t.nmId ? (
                              <Link href={`/cards/wb?nmId=${t.nmId}`} className="text-primary hover:underline">
                                {t.nmId}
                              </Link>
                            ) : (
                              "—"
                            )}
                          </div>
                        </div>
                      </div>
                    </td>
                    <td className="px-3 py-2">{buyerLabel}</td>
                    <td className="px-3 py-2 max-w-[280px]">
                      <p className="line-clamp-2" title={firstMsg?.text ?? undefined}>
                        {firstMsg?.text ?? t.previewText ?? "—"}
                      </p>
                    </td>
                    <td className="px-3 py-2">
                      <div className="flex items-center gap-1">
                        {media.slice(0, 3).map((m) => (
                          <Link
                            key={m.id}
                            href={`/support/${t.id}`}
                            className="w-10 h-10 rounded border overflow-hidden bg-muted"
                          >
                            {/* Fallback на wbUrl если localPath не скачан */}
                            {/* eslint-disable-next-line @next/next/no-img-element */}
                            <img
                              src={m.localPath ? m.localPath.replace("/var/www/zoiten-uploads", "/uploads") : m.wbUrl}
                              alt=""
                              className="w-full h-full object-cover"
                            />
                          </Link>
                        ))}
                        {media.length === 0 && <span className="text-xs text-muted-foreground">—</span>}
                      </div>
                    </td>
                    <td className="px-3 py-2 text-xs">
                      {new Intl.DateTimeFormat("ru-RU", MOSCOW_TZ).format(t.createdAt)}
                    </td>
                    <td className="px-3 py-2">
                      <span className={`inline-block px-2 py-0.5 rounded text-xs ${badge.className}`}>
                        {badge.label}
                      </span>
                    </td>
                    <td className="px-3 py-2 text-xs">
                      {decision ? (
                        <div>
                          <div>{formatUser(decision.decidedBy)}</div>
                          <div className="text-muted-foreground">
                            {new Intl.DateTimeFormat("ru-RU", MOSCOW_TZ).format(decision.decidedAt)}
                          </div>
                        </div>
                      ) : (
                        "—"
                      )}
                    </td>
                    <td className="px-3 py-2">
                      {decision?.reconsidered ? (
                        <span className="text-green-700">Да</span>
                      ) : (
                        <span className="text-muted-foreground">—</span>
                      )}
                    </td>
                    <td className="px-3 py-2">
                      <Link
                        href={`/support/${t.id}`}
                        className="inline-block px-3 py-1 text-xs rounded border hover:bg-accent"
                      >
                        Открыть
                      </Link>
                    </td>
                  </tr>
                )
              })}
            </tbody>
          </table>
        </div>
      )
    }
    ```

    ⚠ **Фото товара**: если `WbCard.photoUrl` есть (Phase 8 использует в `support/[ticketId]/page.tsx`) — добавить `photoUrl: true` в select на page.tsx и рендерить в первой ячейке. Если нет — пропустить.

    **Шаг C — smoke test:**
    ```bash
    npx tsc --noEmit
    npm run build
    ```

    Rendering smoke (если есть тестовая БД) — опционально: открыть http://localhost:3001/support/returns, увидеть таблицу или пустое состояние.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/(dashboard)/support/returns/page.tsx` exit 0
    - `test -f components/support/ReturnsTable.tsx` exit 0
    - `grep "requireSection(\"SUPPORT\")" app/(dashboard)/support/returns/page.tsx` находит
    - `grep "channel: \"RETURN\"" app/(dashboard)/support/returns/page.tsx` находит
    - `grep "distinct: \[\"ticketId\"\]" app/(dashboard)/support/returns/page.tsx` находит
    - `grep "returnDecision.findMany" app/(dashboard)/support/returns/page.tsx` находит
    - `grep "export function ReturnsTable" components/support/ReturnsTable.tsx` находит
    - `grep "Европ\|Europe/Moscow" components/support/ReturnsTable.tsx` находит MSK timezone
    - `grep "/support/\${t.id}\|/support/\${" components/support/ReturnsTable.tsx` находит ссылку на диалог
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    RSC-страница /support/returns создана с корректным requireSection("SUPPORT"), фильтрацией по channel=RETURN, preload decisions (distinct on ticketId) и WbCard; ReturnsTable рендерит 9 колонок с Moscow TZ, badge статусов, ссылками на /cards/wb и /support/{ticketId}; tsc и build проходят.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: ReturnsFilters + добавление пункта «Возвраты» в sidebar nav-items.ts</name>
  <files>components/support/ReturnsFilters.tsx, components/layout/nav-items.ts</files>
  <read_first>
    - components/support/SupportFilters.tsx (Phase 8 паттерн — MultiSelectDropdown, nmId input, date inputs, searchParams через useRouter/usePathname)
    - components/layout/nav-items.ts (текущий список NAV_ITEMS и ICON_MAP — посмотреть пункт «Служба поддержки»)
    - app/(dashboard)/support/returns/page.tsx (какие searchParams ожидаются: returnStates, nmId, assignees, dateFrom, dateTo, reconsideredOnly)
  </read_first>
  <action>
    **Шаг A — создать `components/support/ReturnsFilters.tsx`:**

    Паттерн 1-в-1 с `SupportFilters.tsx` Phase 8, но с другими полями. Скелет (полный код адаптировать под существующий `MultiSelectDropdown`):

    ```typescript
    "use client"

    import { useRouter, usePathname, useSearchParams } from "next/navigation"
    import { useCallback, useState } from "react"
    // Reuse существующий MultiSelectDropdown (смотреть путь из SupportFilters)
    import { MultiSelectDropdown } from "@/components/ui/multi-select-dropdown" // адаптировать путь под реальный
    import type { User } from "@prisma/client"

    const RETURN_STATE_OPTIONS = [
      { value: "PENDING", label: "Ожидает" },
      { value: "APPROVED", label: "Одобрен" },
      { value: "REJECTED", label: "Отклонён" },
    ]

    export interface ReturnsFiltersProps {
      supportUsers: Pick<User, "id" | "firstName" | "lastName" | "name">[]
    }

    export function ReturnsFilters({ supportUsers }: ReturnsFiltersProps) {
      const router = useRouter()
      const pathname = usePathname()
      const searchParams = useSearchParams()

      const setParam = useCallback(
        (key: string, value: string | null) => {
          const params = new URLSearchParams(searchParams.toString())
          if (value === null || value === "") params.delete(key)
          else params.set(key, value)
          params.set("page", "1") // reset pagination
          router.push(`${pathname}?${params.toString()}`)
        },
        [router, pathname, searchParams]
      )

      const currentStates = (searchParams.get("returnStates")?.split(",").filter(Boolean) ?? []) as string[]
      const currentAssignees = searchParams.get("assignees")?.split(",").filter(Boolean) ?? []

      const userOptions = supportUsers.map((u) => ({
        value: u.id,
        label: [u.firstName, u.lastName].filter(Boolean).join(" ") || u.name || u.id,
      }))

      return (
        <div className="flex flex-wrap items-center gap-2">
          <MultiSelectDropdown
            label="Статус решения"
            options={RETURN_STATE_OPTIONS}
            selected={currentStates}
            onChange={(values) => setParam("returnStates", values.length ? values.join(",") : null)}
          />
          <input
            type="number"
            placeholder="Артикул WB (nmId)"
            defaultValue={searchParams.get("nmId") ?? ""}
            onBlur={(e) => setParam("nmId", e.target.value.trim() || null)}
            className="border rounded px-2 py-1 text-sm w-40"
          />
          <MultiSelectDropdown
            label="Менеджер"
            options={userOptions}
            selected={currentAssignees}
            onChange={(values) => setParam("assignees", values.length ? values.join(",") : null)}
          />
          <input
            type="date"
            defaultValue={searchParams.get("dateFrom") ?? ""}
            onChange={(e) => setParam("dateFrom", e.target.value || null)}
            className="border rounded px-2 py-1 text-sm"
            aria-label="Дата от"
          />
          <input
            type="date"
            defaultValue={searchParams.get("dateTo") ?? ""}
            onChange={(e) => setParam("dateTo", e.target.value || null)}
            className="border rounded px-2 py-1 text-sm"
            aria-label="Дата до"
          />
          <label className="flex items-center gap-1 text-sm">
            <input
              type="checkbox"
              checked={searchParams.get("reconsideredOnly") === "1"}
              onChange={(e) => setParam("reconsideredOnly", e.target.checked ? "1" : null)}
            />
            Только пересмотренные
          </label>
        </div>
      )
    }
    ```

    ⚠ **КРИТИЧНО:** импорт `MultiSelectDropdown` — найти актуальный путь через `grep -r "MultiSelectDropdown" components/support/SupportFilters.tsx` ДО написания. Имя экспорта может быть camelCase или snake-case. Если `MultiSelectDropdown` экспортируется как `default` — адаптировать import.

    **Шаг B — обновить `components/layout/nav-items.ts`:**

    Добавить иконку и новый пункт подменю/рядом. Простейший вариант — top-level под «Служба поддержки»:

    ```typescript
    import {
      Package,
      CreditCard,
      Tag,
      LayoutGrid,
      Boxes,
      Calculator,
      ShoppingCart,
      TrendingUp,
      Headphones,
      PackageX,  // новый — для возвратов
      UserCheck,
      Users,
      Settings,
    } from "lucide-react"

    // В массиве NAV_ITEMS после { section: "SUPPORT", href: "/support", ... } добавить:
    export const NAV_ITEMS: NavItem[] = [
      // ...existing...
      { section: "SUPPORT", href: "/support", label: "Служба поддержки", icon: "Headphones" },
      { section: "SUPPORT", href: "/support/returns", label: "Возвраты", icon: "PackageX" }, // Phase 9
      // ...existing...
    ]

    // В ICON_MAP добавить:
    export const ICON_MAP: Record<string, React.ComponentType<{ className?: string }>> = {
      Package,
      // ...existing...
      Headphones,
      PackageX,  // Phase 9
      UserCheck,
      Users,
      Settings,
    }
    ```

    ⚠ **КРИТИЧНО:** не сломать существующую NAV_ITEMS разметку. Sidebar компонент (Phase 4/5) рендерит по section активный/неактивный путь — для `/support/returns` он будет подсвечиваться через startsWith(`/support`) — это нужно проверить.

    **Шаг C — verification:**
    - `npx tsc --noEmit`
    - `npm run build`
    - Smoke (опц., после dev-сервера): sidebar показывает «Возвраты» под «Служба поддержки» с иконкой, клик → /support/returns
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/ReturnsFilters.tsx` exit 0
    - `grep "export function ReturnsFilters" components/support/ReturnsFilters.tsx` находит
    - `grep "returnStates\|nmId\|assignees\|dateFrom\|dateTo\|reconsideredOnly" components/support/ReturnsFilters.tsx` находит все 6 параметров
    - `grep "MultiSelectDropdown\|MultiSelect" components/support/ReturnsFilters.tsx` находит (reuse из Phase 8)
    - `grep "/support/returns" components/layout/nav-items.ts` находит новый пункт
    - `grep "PackageX\|ArchiveRestore\|Undo2" components/layout/nav-items.ts` находит иконку
    - `grep -c "section: \"SUPPORT\"" components/layout/nav-items.ts` >= 2 (существующий «Служба поддержки» + новый «Возвраты»)
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    ReturnsFilters создан с 6 полями (returnStates[], nmId, assignees[], dateFrom, dateTo, reconsideredOnly), использует searchParams через next/navigation, reuse MultiSelectDropdown из проекта; nav-items.ts содержит новый пункт «Возвраты» с href /support/returns и подходящей Lucide иконкой; tsc и build проходят.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npx tsc --noEmit` exit 0
2. `npm run build` exit 0
3. `npm run test` exit 0 (Phase 7/8/9 тесты GREEN, stubs SKIPPED)
4. Manual dev smoke (если dev сервер работает): http://localhost:3001/support/returns рендерится без ошибок
5. Sidebar содержит новый пункт «Возвраты» (или подпункт «Служба поддержки»)
</verification>

<success_criteria>
Plan 09-03 завершён когда:
- app/(dashboard)/support/returns/page.tsx — RSC с requireSection, фильтрацией channel=RETURN, preload latest decisions + WbCard, пагинацией 20/page
- components/support/ReturnsTable.tsx — таблица 9 колонок, Moscow TZ, badge статусов, ссылки на /cards/wb и /support/{ticketId}
- components/support/ReturnsFilters.tsx — 6 фильтров через searchParams (MultiSelectDropdown + input + toggle)
- components/layout/nav-items.ts — новый пункт «Возвраты» с подходящей иконкой
- tsc + build + test — все green
- Commit `feat(09-03): страница /support/returns с таблицей заявок и фильтрами`
</success_criteria>

<output>
После завершения создать `.planning/phases/09-returns/09-03-SUMMARY.md` с:
- Новая страница /support/returns — структура (9 колонок, 6 фильтров, пагинация 20/page)
- Компоненты: ReturnsTable (таблица), ReturnsFilters (MultiSelectDropdown + inputs)
- Sidebar: пункт «Возвраты» добавлен в nav-items.ts с иконкой PackageX (или выбранной)
- Preload decisions: distinct on ticketId паттерн (orderBy decidedAt desc + distinct: ["ticketId"])
- Smoke-скриншот или ссылка на рендер (если dev доступен)
- Зависимости для 09-04: клик по строке → /support/{ticketId}, где 09-04 добавит ReturnActionsPanel
</output>
