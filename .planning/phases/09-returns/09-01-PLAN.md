---
phase: 09-returns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/wb-support-api.ts
  - tests/fixtures/wb-claim-sample.json
  - tests/wb-returns-api.test.ts
  - tests/support-sync-returns.test.ts
  - tests/return-actions.test.ts
autonomous: true
requirements:
  - SUP-17
must_haves:
  truths:
    - "prisma/schema.prisma содержит новую модель ReturnDecision + enum ReturnDecisionAction + enum ReturnState"
    - "SupportTicket расширена 8 nullable-полями (wbClaimStatus, wbClaimStatusEx, wbClaimType, wbActions, wbComment, srid, price, returnState) без ломающих изменений для Phase 8"
    - "User получает обратную relation returnDecisions @relation(\"ReturnDecider\")"
    - "lib/wb-support-api.ts экспортирует listReturns, approveReturn, rejectReturn, reconsiderReturn, работающие против returns-api.wildberries.ru"
    - "tests/wb-returns-api.test.ts GREEN — покрывает listReturns URL/headers/pagination/429 retry/401/403, PATCH bodies 3 action-функций"
    - "tests/support-sync-returns.test.ts и tests/return-actions.test.ts существуют как Wave 0 stubs (it.skip), npm run test не падает"
    - "tests/fixtures/wb-claim-sample.json содержит canonical WB Claim JSON для reuse в unit-тестах"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ReturnDecision model + ReturnDecisionAction enum + ReturnState enum + 8 полей в SupportTicket + relation на User"
      contains: "model ReturnDecision"
    - path: "lib/wb-support-api.ts"
      provides: "WB Returns API клиент (4 метода) с rate-limit retry"
      exports: ["listReturns", "approveReturn", "rejectReturn", "reconsiderReturn"]
    - path: "tests/wb-returns-api.test.ts"
      provides: "Unit-тесты клиента (mock fetch)"
      contains: "describe"
    - path: "tests/support-sync-returns.test.ts"
      provides: "Wave 0 stub для Plan 09-02"
      contains: "it.skip"
    - path: "tests/return-actions.test.ts"
      provides: "Wave 0 stub для Plan 09-04"
      contains: "it.skip"
    - path: "tests/fixtures/wb-claim-sample.json"
      provides: "Canonical WB claim JSON fixture"
      contains: "fe3e9337"
  key_links:
    - from: "lib/wb-support-api.ts listReturns"
      to: "https://returns-api.wildberries.ru/api/v1/claims"
      via: "fetch с Authorization header"
      pattern: "returns-api\\.wildberries\\.ru"
    - from: "lib/wb-support-api.ts approveReturn/rejectReturn/reconsiderReturn"
      to: "https://returns-api.wildberries.ru/api/v1/claim"
      via: "PATCH body {id, action, comment?}"
      pattern: "/api/v1/claim"
    - from: "prisma/schema.prisma ReturnDecision"
      to: "User (decidedBy)"
      via: "@relation(\"ReturnDecider\")"
      pattern: "ReturnDecider"
    - from: "prisma/schema.prisma SupportTicket"
      to: "ReturnDecision[]"
      via: "@relation(\"ReturnDecisions\")"
      pattern: "ReturnDecisions"
---

<objective>
Phase 9 Plan 01 — foundation для возвратов:
1. Prisma миграция: новая модель `ReturnDecision` (history/audit log решений), новые enum `ReturnDecisionAction` (APPROVE/REJECT/RECONSIDER) и `ReturnState` (PENDING/APPROVED/REJECTED), 8 новых nullable-полей в `SupportTicket` (wbClaimStatus, wbClaimStatusEx, wbClaimType, wbActions, wbComment, srid, price, returnState), обратная relation `User.returnDecisions`.
2. Расширение `lib/wb-support-api.ts`: новая константа `RETURNS_API`, второй helper `callReturnsApi` (или параметризация callWb), типы `Claim`/`ListReturnsParams`, функции `listReturns`, `approveReturn`, `rejectReturn`, `reconsiderReturn` с rate-limit retry (20 req/min = 600ms пауза, X-Ratelimit-Retry header, 6000ms fallback).
3. Wave 0: полноценный `tests/wb-returns-api.test.ts` (GREEN, ~10 тестов) + 2 stub-файла (`support-sync-returns.test.ts`, `return-actions.test.ts`) + canonical fixture `tests/fixtures/wb-claim-sample.json`.

Purpose: Database schema + API client — foundation для Plan 09-02 (sync) и 09-04 (actions). Миграция применяется локально, на VPS — через Plan 09-04 deploy. Клиент изолирован в `lib/` с unit-тестами против mock fetch (паттерн Phase 8 `tests/wb-support-api.test.ts`).

Output: Обновлённая `prisma/schema.prisma` + миграция `prisma/migrations/<ts>_phase9_returns/`, расширенный `lib/wb-support-api.ts`, 3 тест-файла (1 GREEN + 2 stub), 1 JSON fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-returns/09-RESEARCH.md
@.planning/phases/09-returns/09-VALIDATION.md
@.planning/phases/08-support-mvp/08-01-SUMMARY.md
@CLAUDE.md
@prisma/schema.prisma
@lib/wb-support-api.ts
@lib/prisma.ts
@lib/rbac.ts

<interfaces>
**Существующие (Phase 8) в prisma/schema.prisma:**
```prisma
enum TicketChannel { FEEDBACK; QUESTION; CHAT; RETURN; MESSENGER }   // RETURN уже есть!
enum TicketStatus  { NEW; IN_PROGRESS; ANSWERED; CLOSED; APPEALED }

model User {
  id                String            @id
  // ...
  assignedTickets   SupportTicket[]   @relation("SupportAssignee")
  authoredMessages  SupportMessage[]  @relation("SupportAuthor")
  // Plan 09-01 добавит:
  // returnDecisions  ReturnDecision[] @relation("ReturnDecider")
}

model SupportTicket {
  id            String           @id @default(cuid())
  channel       TicketChannel
  wbExternalId  String?
  nmId          Int?
  status        TicketStatus     @default(NEW)
  // existing fields ...
  messages      SupportMessage[]
  @@unique([channel, wbExternalId])
}
```

**Существующие в lib/wb-support-api.ts (Phase 8):**
```typescript
const FEEDBACKS_API = "https://feedbacks-api.wildberries.ru"
const RATE_LIMIT_FALLBACK_MS = 6000
function getToken(): string
async function callWb(path: string, init: RequestInit, attempt = 0): Promise<Response>
export async function listFeedbacks(p: ListParams): Promise<Feedback[]>
export async function listQuestions(p: ListParams): Promise<Question[]>
export async function replyFeedback(id: string, text: string): Promise<{ ok: true }>
export async function editFeedbackAnswer(id: string, text: string): Promise<{ ok: true }>
export async function replyQuestion(id: string, text: string): Promise<{ ok: true }>
```

**WB Returns API (verified в 09-RESEARCH.md §4-7):**
- Base URL: `https://returns-api.wildberries.ru`
- Auth: `Authorization: ${WB_RETURNS_TOKEN}` (без Bearer) — **отдельный токен** с scope bit 11 (Buyers Returns). Существующий `WB_API_TOKEN` не имеет bit 11 и не может быть расширен быстро (требует перегенерации всей учётки). Если `WB_RETURNS_TOKEN` не задан — fallback на `WB_API_TOKEN` (для dev/test).
- `GET /api/v1/claims?is_archive={false|true}&limit=1-200&offset=0&id={uuid}&nm_id={int}`
  - Response: `{ claims: Claim[], total: number }`
- `PATCH /api/v1/claim` body `{id: uuid, action: string, comment?: string 10-1000}`
  - Actions: `autorefund1`, `approve1`, `approvecc1`, `rejectcustom` (`comment` required только при `rejectcustom`)
- Rate limit: 20 req/min, 10 burst, 3 sec interval, header `X-Ratelimit-Retry` при 429 (паттерн 08 callWb)

**Canonical Claim пример из 09-RESEARCH.md §6:**
```json
{
  "id": "fe3e9337-e9f9-423c-8930-946a8ebef80",
  "claim_type": 1,
  "status": 2,
  "status_ex": 8,
  "nm_id": 196320101,
  "user_comment": "Длина провода не соответствует описанию",
  "wb_comment": "Продавец одобрил вашу заявку...",
  "dt": "2024-03-26T17:06:12.245611",
  "imt_name": "Кабель 0.5 м, 3797",
  "order_dt": "2020-10-27T05:18:56",
  "dt_update": "2024-05-10T18:01:06.999613",
  "photos": ["//photos.wbstatic.net/claim/fe3e9337-.../1.webp"],
  "video_paths": ["//video.wbstatic.net/claim/fe3e9337-.../1.mp4"],
  "actions": ["autorefund1", "approve1"],
  "price": 157,
  "currency_code": "643",
  "srid": "v5o_7143225816503318733.0.0"
}
```

**Vitest паттерн (из tests/wb-support-api.test.ts, Phase 8):**
- `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"`
- `process.env.WB_API_TOKEN = "test-token"` + `vi.stubGlobal("fetch", vi.fn())`
- Каждый тест динамически импортирует модуль: `const { listReturns } = await import("@/lib/wb-support-api")`
- 429 retry → `vi.mocked(fetch).mockResolvedValueOnce(429-response).mockResolvedValueOnce(200-response)`
</interfaces>

**⚠ Wave 0 pre-flight (MUST DO до execute-phase):**
Добавить **отдельный** `WB_RETURNS_TOKEN` на VPS (существующий `WB_API_TOKEN` НЕ трогаем — у него нет scope bit 11 и расширить быстро нельзя):
```bash
ssh root@85.198.97.89
echo 'WB_RETURNS_TOKEN=<новый токен с bit 11 Buyers Returns>' >> /etc/zoiten.pro.env
systemctl restart zoiten-erp.service
# verify:
source /etc/zoiten.pro.env && curl -s -o /dev/null -w "%{http_code}\n" \
  "https://returns-api.wildberries.ru/api/v1/claims?is_archive=false&limit=1" \
  -H "Authorization: $WB_RETURNS_TOKEN"
```
- `200` или `429` → scope OK, продолжать
- `401 / 403` → блокер: токен неверный или scope bit 11 отсутствует, перегенерировать на seller.wildberries.ru ДО начала Plan 09-02 sync.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: Prisma миграция — ReturnDecision + 2 enum + 8 полей в SupportTicket + relation на User</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (текущая схема — смотреть existing модели User, SupportTicket; обратные relations "SupportAssignee" и "SupportAuthor" уже есть на User строки 65-66)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «Data Model» строки 268-381 — финальная схема ReturnDecision + 8 полей)
    - .planning/phases/08-support-mvp/08-01-SUMMARY.md (паттерн миграции Phase 8)
    - CLAUDE.md (раздел «Связи между таблицами БД» — onDelete convention, именованные relations)
  </read_first>
  <action>
    Открыть `prisma/schema.prisma` и внести следующие изменения.

    **1. Добавить два новых enum в конец файла** (после существующих enum TicketChannel/TicketStatus/AppealStatus/Direction/MediaType):

    ```prisma
    // Phase 9 — Возвраты

    enum ReturnDecisionAction {
      APPROVE     // approve1, autorefund1, approvecc1
      REJECT      // rejectcustom
      RECONSIDER  // повторный approve* после REJECT
    }

    enum ReturnState {
      PENDING   // заявка открыта, решения нет
      APPROVED  // одобрен (финал)
      REJECTED  // отклонён (можно пересмотреть)
    }
    ```

    **2. Добавить новую модель `ReturnDecision` в конец файла** (после `SupportMedia`):

    ```prisma
    // ReturnDecision — история решений по заявке на возврат (audit log).
    // Один ticket → до N decisions (из-за «пересмотреть»).
    // Актуальное состояние денормализовано в SupportTicket.returnState для быстрой фильтрации.
    model ReturnDecision {
      id           String                @id @default(cuid())
      ticketId     String
      ticket       SupportTicket         @relation("ReturnDecisions", fields: [ticketId], references: [id], onDelete: Cascade)
      action       ReturnDecisionAction
      wbAction     String // оригинальный WB action (autorefund1/approve1/approvecc1/rejectcustom)
      reason       String?               @db.Text // comment для rejectcustom или approvecc1
      decidedById  String
      decidedBy    User                  @relation("ReturnDecider", fields: [decidedById], references: [id], onDelete: Restrict)
      decidedAt    DateTime              @default(now())
      reconsidered Boolean               @default(false) // true для RECONSIDER action
      wbResponseOk Boolean               @default(true)  // false если WB API вернул ошибку (но попытку фиксируем)
      wbError      String?               @db.Text

      @@index([ticketId, decidedAt])
      @@index([decidedById])
    }
    ```

    **3. Расширить модель `SupportTicket`** — найти `model SupportTicket` (строки 532-558) и **ДО** блока `@@unique([channel, wbExternalId])` добавить 8 полей + 1 relation:

    ```prisma
      // ── Phase 9: Возвраты ──
      wbClaimStatus   Int?              // оригинальный status из WB (denormalized, для диагностики)
      wbClaimStatusEx Int?              // status_ex из WB
      wbClaimType     Int?              // claim_type из WB
      wbActions       String[]          @default([]) // актуальные action strings из WB (для UI кнопок)
      wbComment       String?           @db.Text // wb_comment — длинная инструкция покупателю
      srid            String?           // Shipment ID из WB (для связки с заказом)
      price           Float?            // price из заявки (рубли)
      returnState     ReturnState?      // локальное состояние (PENDING/APPROVED/REJECTED)
      returnDecisions ReturnDecision[]  @relation("ReturnDecisions")
    ```

    **4. Добавить index по returnState** внутри `SupportTicket` (после existing `@@index([createdAt])`):

    ```prisma
      @@index([returnState])
    ```

    **5. Расширить модель `User`** — найти `model User` (строка 51) и после существующей `authoredMessages` (строка 66) добавить:

    ```prisma
      returnDecisions  ReturnDecision[]  @relation("ReturnDecider") // Phase 9 — audit log решений по возвратам
    ```

    **6. Запустить миграцию:**
    ```bash
    npx prisma format
    npx prisma validate
    npx prisma migrate dev --name phase9_returns
    npx prisma generate
    ```

    Если локальная БД отсутствует (паттерн проекта — STATE.md decision «Migration marked pending»), выполнить только `npx prisma format && npx prisma validate`. Применение `migrate deploy` — в Plan 09-04 deploy-задаче на VPS.

    **⚠ КРИТИЧНО:** НЕ трогать existing поля SupportTicket (wbExternalId, channel, status и т.д.) — новые поля только nullable/default, без breaking changes для Phase 8 кода.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma format --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model ReturnDecision\b" prisma/schema.prisma` == 1
    - `grep -c "^enum ReturnDecisionAction\b" prisma/schema.prisma` == 1
    - `grep -c "^enum ReturnState\b" prisma/schema.prisma` == 1
    - `grep "wbClaimStatus\s*Int?" prisma/schema.prisma` находит строку
    - `grep "wbClaimStatusEx\s*Int?" prisma/schema.prisma` находит строку
    - `grep "wbActions\s*String\[\]" prisma/schema.prisma` находит строку
    - `grep "returnState\s*ReturnState?" prisma/schema.prisma` находит строку
    - `grep "@@unique(\[channel, wbExternalId\])" prisma/schema.prisma` всё ещё находит (Phase 8 не сломали)
    - `grep -c "ReturnDecider" prisma/schema.prisma` >= 2 (forward + reverse на User)
    - `grep -c "ReturnDecisions" prisma/schema.prisma` >= 2 (forward на ReturnDecision.ticket + reverse на SupportTicket)
    - `grep "onDelete: Restrict" prisma/schema.prisma` находит строку (ReturnDecision.decidedBy)
    - `grep "@@index(\[returnState\])" prisma/schema.prisma` находит строку
    - `npx prisma validate` exit 0
    - `npx prisma format --check` exit 0
  </acceptance_criteria>
  <done>
    prisma/schema.prisma содержит новую модель ReturnDecision, 2 новых enum (ReturnDecisionAction, ReturnState), 8 новых полей в SupportTicket, 1 новую relation в User; миграция `phase9_returns` либо применена локально, либо зафиксирована как pending для Plan 09-04 deploy; `npx prisma validate` и `npx prisma format --check` проходят без ошибок.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/wb-support-api.ts — расширение методами WB Returns API + unit-тесты + fixture</name>
  <files>lib/wb-support-api.ts, tests/wb-returns-api.test.ts, tests/fixtures/wb-claim-sample.json</files>
  <read_first>
    - lib/wb-support-api.ts (существующий клиент Phase 8 — паттерн callWb, getToken, типы, 429 retry)
    - tests/wb-support-api.test.ts (Phase 8 паттерн vitest mock fetch, beforeEach/afterEach)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «WB Buyers Returns API» строки 93-266 — VERIFIED endpoints, response schema, rate limits)
    - .planning/phases/09-returns/09-VALIDATION.md (раздел «Per-Task Verification Map» — test command)
    - CLAUDE.md (раздел «WB API токен» — Authorization без Bearer)
  </read_first>
  <behavior>
    - `listReturns({ is_archive, limit?, offset?, id?, nm_id? })` → `Promise<{ claims: Claim[]; total: number }>`:
      - Строит URL `/api/v1/claims?is_archive={true|false}&limit={1-200}&offset={N}[&id={uuid}][&nm_id={int}]`
      - `is_archive` обязателен, остальные optional
      - Шлёт header `Authorization: ${WB_API_TOKEN}`
      - Возвращает `{ claims, total }` как есть
    - `approveReturn(id: string, wbAction: string, comment?: string)` → `Promise<{ ok: true }>`:
      - PATCH `/api/v1/claim` body `{ id, action: wbAction }` (comment только если передан — для approvecc1)
      - wbAction: одна из `approve1`, `autorefund1`, `approvecc1`
      - Возвращает `{ ok: true }` при 200-204
    - `rejectReturn(id: string, reason: string)` → `Promise<{ ok: true }>`:
      - Валидация: `reason.length` в [10, 1000] — throw `Error("Причина должна быть от 10 до 1000 символов")` иначе
      - PATCH `/api/v1/claim` body `{ id, action: "rejectcustom", comment: reason }`
    - `reconsiderReturn(id: string, wbAction: string)` → `Promise<{ ok: true }>`:
      - PATCH `/api/v1/claim` body `{ id, action: wbAction }` (обычно `approve1`)
      - Без comment (reconsider — семантика повторного approve)
    - При 429 с header `X-Ratelimit-Retry: N` → `sleep(N*1000)` → retry 1 раз
    - При 429 без header → `sleep(6000ms)` → retry 1 раз
    - При 401 → throw `Error("Неверный токен WB API")`
    - При 403 → throw `Error("Нет доступа — проверьте scope токена (bit 11 Buyers Returns)")`
    - Helper `callReturnsApi` ИЛИ параметризованный `callWb(baseUrl, path, init)` — не дублировать 429 логику
  </behavior>
  <action>
    **Шаг A — расширить `lib/wb-support-api.ts`:**

    1. Добавить новую константу рядом с `FEEDBACKS_API` (строка 6):
       ```typescript
       const FEEDBACKS_API = "https://feedbacks-api.wildberries.ru"
       const RETURNS_API = "https://returns-api.wildberries.ru"  // Phase 9
       const RATE_LIMIT_FALLBACK_MS = 6000
       ```

    2. Добавить второй token-helper для Returns API (отдельный env var, fallback на `WB_API_TOKEN` для dev):
       ```typescript
       // Существующий helper оставляем as-is (читает WB_API_TOKEN)
       // function getToken(): string { ... }

       // Новый helper для Returns API — отдельный scope bit 11 (Buyers Returns)
       function getReturnsToken(): string {
         const token = process.env.WB_RETURNS_TOKEN ?? process.env.WB_API_TOKEN
         if (!token) throw new Error("WB_RETURNS_TOKEN или WB_API_TOKEN не настроен")
         return token
       }
       ```

    3. Рефакторить `callWb(path, init, attempt)` → параметризовать base URL + token, чтобы не дублировать retry-логику:
       ```typescript
       async function callApi(baseUrl: string, token: string, path: string, init: RequestInit, attempt = 0): Promise<Response> {
         const res = await fetch(`${baseUrl}${path}`, {
           ...init,
           headers: { Authorization: token, "Content-Type": "application/json", ...(init.headers ?? {}) },
         })
         if (res.status === 429 && attempt === 0) {
           const retry = Number(res.headers.get("X-Ratelimit-Retry")) || 0
           const waitMs = retry > 0 ? retry * 1000 : RATE_LIMIT_FALLBACK_MS
           await new Promise((r) => setTimeout(r, waitMs))
           return callApi(baseUrl, token, path, init, 1)
         }
         if (res.status === 401) throw new Error("Неверный токен WB API")
         if (res.status === 403) {
           // Разный текст для Feedbacks (bit 5) и Returns (bit 11)
           const scopeHint = baseUrl.includes("returns-api") ? "bit 11 Buyers Returns (WB_RETURNS_TOKEN)" : "bit 5"
           throw new Error(`Нет доступа — проверьте scope токена (${scopeHint})`)
         }
         if (!res.ok) {
           const body = await res.text().catch(() => "")
           throw new Error(`WB API ${res.status}: ${body.slice(0, 200)}`)
         }
         return res
       }

       // Переименовать существующий callWb → переключить на callApi(FEEDBACKS_API, getToken(), ...)
       async function callWb(path: string, init: RequestInit, attempt = 0): Promise<Response> {
         return callApi(FEEDBACKS_API, getToken(), path, init, attempt)
       }

       // Новый helper для Returns — использует отдельный token (WB_RETURNS_TOKEN)
       async function callReturnsApi(path: string, init: RequestInit, attempt = 0): Promise<Response> {
         return callApi(RETURNS_API, getReturnsToken(), path, init, attempt)
       }
       ```

       ⚠ Существующие listFeedbacks/replyFeedback/replyQuestion продолжают использовать `callWb` с `WB_API_TOKEN` — поведение не меняется.
       ⚠ Новые listReturns/approveReturn/rejectReturn/reconsiderReturn используют `callReturnsApi` с `WB_RETURNS_TOKEN`.

    3. Добавить типы Returns API (после existing `export interface Question` / `export interface ListParams`):

       ```typescript
       // ── WB Buyers Returns API (Phase 9) ──────────────────────────

       export interface Claim {
         id: string              // UUID v4
         claim_type: number      // enum integer (не документирован; as-is)
         status: number          // status integer (не документирован; as-is)
         status_ex: number       // status_ex integer
         nm_id: number           // артикул WB
         user_comment: string    // причина покупателя
         wb_comment?: string     // инструкция WB (не продавца)
         dt: string              // ISO 8601 — когда заявка подана
         imt_name: string        // название товара (денормализованное)
         order_dt?: string       // ISO 8601 — дата заказа
         dt_update?: string      // ISO 8601 — последнее обновление
         photos?: string[]       // //photos.wbstatic.net/... (без схемы!)
         video_paths?: string[]  // //video.wbstatic.net/...
         actions: string[]       // ["autorefund1", "approve1", "rejectcustom", ...]
         price?: number          // рубли (подтвердить на живых данных)
         currency_code?: string  // "643" = RUB
         srid?: string           // Shipment ID
       }

       export interface ListReturnsParams {
         is_archive: boolean     // обязательный
         limit?: number          // default 50, max 200
         offset?: number         // default 0
         id?: string             // UUID фильтр
         nm_id?: number          // артикул фильтр
       }

       export interface ListReturnsResult {
         claims: Claim[]
         total: number
       }
       ```

    4. Добавить 4 функции в конец файла:

       ```typescript
       // ── Returns API methods ──────────────────────────────────────

       export async function listReturns(p: ListReturnsParams): Promise<ListReturnsResult> {
         const qs = new URLSearchParams()
         qs.set("is_archive", String(p.is_archive))
         if (p.limit !== undefined) qs.set("limit", String(p.limit))
         if (p.offset !== undefined) qs.set("offset", String(p.offset))
         if (p.id !== undefined) qs.set("id", p.id)
         if (p.nm_id !== undefined) qs.set("nm_id", String(p.nm_id))
         const res = await callReturnsApi(`/api/v1/claims?${qs}`, { method: "GET" })
         const json = (await res.json()) as { claims?: Claim[]; total?: number }
         return { claims: json.claims ?? [], total: json.total ?? 0 }
       }

       export async function approveReturn(id: string, wbAction: string, comment?: string): Promise<{ ok: true }> {
         const body: { id: string; action: string; comment?: string } = { id, action: wbAction }
         if (comment !== undefined) body.comment = comment
         await callReturnsApi("/api/v1/claim", { method: "PATCH", body: JSON.stringify(body) })
         return { ok: true }
       }

       export async function rejectReturn(id: string, reason: string): Promise<{ ok: true }> {
         if (reason.length < 10 || reason.length > 1000) {
           throw new Error("Причина должна быть от 10 до 1000 символов")
         }
         await callReturnsApi("/api/v1/claim", {
           method: "PATCH",
           body: JSON.stringify({ id, action: "rejectcustom", comment: reason }),
         })
         return { ok: true }
       }

       export async function reconsiderReturn(id: string, wbAction: string): Promise<{ ok: true }> {
         await callReturnsApi("/api/v1/claim", {
           method: "PATCH",
           body: JSON.stringify({ id, action: wbAction }),
         })
         return { ok: true }
       }
       ```

    **Шаг B — создать `tests/fixtures/wb-claim-sample.json`** (canonical пример из 09-RESEARCH.md §6):

    ```json
    {
      "claims": [
        {
          "id": "fe3e9337-e9f9-423c-8930-946a8ebef80",
          "claim_type": 1,
          "status": 2,
          "status_ex": 8,
          "nm_id": 196320101,
          "user_comment": "Длина провода не соответствует описанию",
          "wb_comment": "Продавец одобрил вашу заявку на возврат товара.",
          "dt": "2024-03-26T17:06:12.245611",
          "imt_name": "Кабель 0.5 м, 3797",
          "order_dt": "2020-10-27T05:18:56",
          "dt_update": "2024-05-10T18:01:06.999613",
          "photos": [
            "//photos.wbstatic.net/claim/fe3e9337-e9f9-423c-8930-946a8ebef80/1.webp",
            "//photos.wbstatic.net/claim/fe3e9337-e9f9-423c-8930-946a8ebef80/2.webp"
          ],
          "video_paths": [
            "//video.wbstatic.net/claim/fe3e9337-e9f9-423c-8930-946a8ebef80/1.mp4"
          ],
          "actions": ["autorefund1", "approve1"],
          "price": 157,
          "currency_code": "643",
          "srid": "v5o_7143225816503318733.0.0"
        }
      ],
      "total": 1
    }
    ```

    **Шаг C — создать `tests/wb-returns-api.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    import sampleResponse from "./fixtures/wb-claim-sample.json"

    const ORIGINAL_ENV = process.env.WB_API_TOKEN
    const ORIGINAL_RETURNS_ENV = process.env.WB_RETURNS_TOKEN

    beforeEach(() => {
      process.env.WB_API_TOKEN = "test-token"
      process.env.WB_RETURNS_TOKEN = "test-returns-token"
      vi.stubGlobal("fetch", vi.fn())
      vi.resetModules()
    })

    afterEach(() => {
      process.env.WB_API_TOKEN = ORIGINAL_ENV
      process.env.WB_RETURNS_TOKEN = ORIGINAL_RETURNS_ENV
      vi.unstubAllGlobals()
    })

    function mockResponse(body: unknown, status = 200, headers: Record<string, string> = {}): Response {
      return new Response(JSON.stringify(body), {
        status,
        headers: { "Content-Type": "application/json", ...headers },
      })
    }

    describe("listReturns", () => {
      it("строит URL returns-api.wildberries.ru/api/v1/claims с is_archive и шлёт Authorization", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({ claims: [], total: 0 }))
        await listReturns({ is_archive: false, limit: 200, offset: 0 })
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("returns-api.wildberries.ru")
        expect(call[0]).toContain("/api/v1/claims?")
        expect(call[0]).toContain("is_archive=false")
        expect(call[0]).toContain("limit=200")
        expect(call[0]).toContain("offset=0")
        expect(call[1].headers.Authorization).toBe("test-returns-token")
      })

      it("парсит {claims, total} из canonical fixture", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse(sampleResponse))
        const res = await listReturns({ is_archive: false })
        expect(res.claims).toHaveLength(1)
        expect(res.claims[0].id).toBe("fe3e9337-e9f9-423c-8930-946a8ebef80")
        expect(res.claims[0].actions).toContain("approve1")
        expect(res.total).toBe(1)
      })

      it("поддерживает offset pagination", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({ claims: [], total: 500 }))
        await listReturns({ is_archive: true, limit: 200, offset: 400 })
        const url = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0][0] as string
        expect(url).toContain("is_archive=true")
        expect(url).toContain("offset=400")
      })

      it("ретраит при 429 с заголовком X-Ratelimit-Retry", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock.mockResolvedValueOnce(mockResponse({}, 429, { "X-Ratelimit-Retry": "1" }))
        fetchMock.mockResolvedValueOnce(mockResponse({ claims: [], total: 0 }))
        const res = await listReturns({ is_archive: false })
        expect(res.claims).toEqual([])
        expect(fetchMock).toHaveBeenCalledTimes(2)
      }, 10000)

      it("кидает русскую ошибку при 401", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}, 401))
        await expect(listReturns({ is_archive: false })).rejects.toThrow("Неверный токен WB API")
      })

      it("кидает ошибку про scope bit 11 при 403 от returns-api", async () => {
        const { listReturns } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}, 403))
        await expect(listReturns({ is_archive: false })).rejects.toThrow("bit 11 Buyers Returns")
      })
    })

    describe("approveReturn", () => {
      it("шлёт PATCH /api/v1/claim с body {id, action}", async () => {
        const { approveReturn } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await approveReturn("uuid-1", "approve1")
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("returns-api.wildberries.ru/api/v1/claim")
        expect(call[1].method).toBe("PATCH")
        expect(JSON.parse(call[1].body)).toEqual({ id: "uuid-1", action: "approve1" })
      })

      it("добавляет comment для approvecc1 если передан", async () => {
        const { approveReturn } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await approveReturn("uuid-2", "approvecc1", "С пояснением от продавца")
        const body = JSON.parse(((globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0] as [string, RequestInit])[1].body as string)
        expect(body).toEqual({ id: "uuid-2", action: "approvecc1", comment: "С пояснением от продавца" })
      })
    })

    describe("rejectReturn", () => {
      it("шлёт PATCH с action rejectcustom + comment", async () => {
        const { rejectReturn } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await rejectReturn("uuid-3", "Фото не соответствует товару из заявки")
        const body = JSON.parse(((globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0] as [string, RequestInit])[1].body as string)
        expect(body).toEqual({
          id: "uuid-3",
          action: "rejectcustom",
          comment: "Фото не соответствует товару из заявки",
        })
      })

      it("throws если reason < 10 символов", async () => {
        const { rejectReturn } = await import("@/lib/wb-support-api")
        await expect(rejectReturn("uuid", "Коротко")).rejects.toThrow("от 10 до 1000")
      })

      it("throws если reason > 1000 символов", async () => {
        const { rejectReturn } = await import("@/lib/wb-support-api")
        await expect(rejectReturn("uuid", "x".repeat(1001))).rejects.toThrow("от 10 до 1000")
      })
    })

    describe("reconsiderReturn", () => {
      it("шлёт PATCH с переданным wbAction (обычно approve1) без comment", async () => {
        const { reconsiderReturn } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await reconsiderReturn("uuid-4", "approve1")
        const body = JSON.parse(((globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0] as [string, RequestInit])[1].body as string)
        expect(body).toEqual({ id: "uuid-4", action: "approve1" })
      })
    })
    ```

    **Шаг D — запустить тесты:** `npm run test -- tests/wb-returns-api.test.ts` — все GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-returns-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "returns-api.wildberries.ru" lib/wb-support-api.ts` >= 1
    - `grep -c "export async function listReturns" lib/wb-support-api.ts` == 1
    - `grep -c "export async function approveReturn" lib/wb-support-api.ts` == 1
    - `grep -c "export async function rejectReturn" lib/wb-support-api.ts` == 1
    - `grep -c "export async function reconsiderReturn" lib/wb-support-api.ts` == 1
    - `grep -c "export interface Claim" lib/wb-support-api.ts` == 1
    - `grep "rejectcustom" lib/wb-support-api.ts` находит строку
    - `grep "bit 11" lib/wb-support-api.ts` находит строку (403 handling)
    - `test -f tests/fixtures/wb-claim-sample.json` exit 0
    - `grep "fe3e9337-e9f9-423c-8930-946a8ebef80" tests/fixtures/wb-claim-sample.json` находит строку
    - `test -f tests/wb-returns-api.test.ts` exit 0
    - `npm run test -- tests/wb-returns-api.test.ts` exit 0
    - `npm run test -- tests/wb-support-api.test.ts` exit 0 (Phase 8 тесты не сломаны рефакторингом callApi)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/wb-support-api.ts экспортирует 4 новых метода Returns API (listReturns/approveReturn/rejectReturn/reconsiderReturn) + типы Claim/ListReturnsParams/ListReturnsResult; рефакторинг callApi не сломал existing Phase 8 методы; tests/wb-returns-api.test.ts — минимум 10 GREEN test cases (URL/headers, parsing fixture, pagination, 429 retry, 401 error, 403 scope hint, 3 action bodies, reason validation); fixture JSON файл создан; `npm run test -- tests/wb-returns-api.test.ts` и `tests/wb-support-api.test.ts` оба exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Wave 0 — 2 stub-теста для downstream планов (09-02 sync, 09-04 actions)</name>
  <files>tests/support-sync-returns.test.ts, tests/return-actions.test.ts</files>
  <read_first>
    - .planning/phases/09-returns/09-VALIDATION.md (раздел «Wave 0 Requirements» — список stub-файлов)
    - tests/support-sync.test.ts (Phase 8 паттерн integration test)
    - tests/support-actions.test.ts (Phase 8 паттерн server actions test)
    - tests/wb-returns-api.test.ts (созданный в задаче 2 — style reference)
  </read_first>
  <action>
    Создать 2 stub-файла с `it.skip` placeholder-ами, чтобы `npm run test` не падал при наличии ссылок из планов, но явно делегировал реализацию downstream-планам.

    **1. `tests/support-sync-returns.test.ts`** (реализация в Plan 09-02):

    ```typescript
    import { describe, it } from "vitest"

    describe("support-sync-returns (Wave 0 stub — Plan 09-02 will implement)", () => {
      it.skip("syncReturns() создаёт SupportTicket с channel=RETURN, returnState=PENDING", () => {
        // Реализация: Plan 09-02 задача 1 — integration test с mock fetch + real Prisma
      })
      it.skip("syncReturns() идемпотентен по @@unique([channel, wbExternalId])", () => {
        // Реализация: Plan 09-02 — двойной вызов не создаёт дубликаты
      })
      it.skip("syncReturns() создаёт SupportMedia с https: префиксом для photos и video_paths", () => {
        // Реализация: Plan 09-02 — //photos.wbstatic.net/... → https://photos.wbstatic.net/...
      })
      it.skip("syncReturns() вызывает обе страницы is_archive=false и is_archive=true", () => {
        // Реализация: Plan 09-02
      })
      it.skip("syncReturns() НЕ перезаписывает returnState при update (защита локальных решений)", () => {
        // Реализация: Plan 09-02 — update только wbClaimStatus/wbActions/previewText
      })
    })
    ```

    **2. `tests/return-actions.test.ts`** (реализация в Plan 09-04):

    ```typescript
    import { describe, it } from "vitest"

    describe("return-actions (Wave 0 stub — Plan 09-04 will implement)", () => {
      it.skip("approveReturn: happy path PENDING → APPROVED + ReturnDecision создан", () => {
        // Реализация: Plan 09-04 задача 2 — mock patchClaim + Prisma
      })
      it.skip("approveReturn: reject если ticket.channel !== RETURN", () => {
        // Реализация: Plan 09-04
      })
      it.skip("approveReturn: reject если returnState === APPROVED (финал)", () => {
        // Реализация: Plan 09-04
      })
      it.skip("rejectReturn: happy path PENDING → REJECTED + ReturnDecision с reason", () => {
        // Реализация: Plan 09-04
      })
      it.skip("rejectReturn: validation reason < 10 или > 1000 символов", () => {
        // Реализация: Plan 09-04 — Zod на сервере
      })
      it.skip("reconsiderReturn: happy path REJECTED → APPROVED + Decision{reconsidered:true}", () => {
        // Реализация: Plan 09-04
      })
      it.skip("reconsiderReturn: reject если returnState === PENDING", () => {
        // Реализация: Plan 09-04
      })
      it.skip("Все 3 action требуют requireSection(\"SUPPORT\", \"MANAGE\") — VIEWER получает reject", () => {
        // Реализация: Plan 09-04 — RBAC guard test
      })
      it.skip("Все 3 action НЕ создают ReturnDecision если WB API вернул ошибку", () => {
        // Реализация: Plan 09-04 — order: patchClaim первым, Decision только после успеха
      })
      it.skip("Все 3 action вызывают revalidatePath('/support/returns') и revalidatePath('/support/[ticketId]')", () => {
        // Реализация: Plan 09-04
      })
    })
    ```

    **Запуск для проверки:** `npm run test` — все stub-сюиты `it.skip` помечаются как skipped, 0 failed.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/support-sync-returns.test.ts` exit 0
    - `test -f tests/return-actions.test.ts` exit 0
    - `grep -c "it.skip" tests/support-sync-returns.test.ts` >= 5
    - `grep -c "it.skip" tests/return-actions.test.ts` >= 10
    - `grep "Plan 09-02" tests/support-sync-returns.test.ts` находит ссылку
    - `grep "Plan 09-04" tests/return-actions.test.ts` находит ссылку
    - `npm run test` exit 0 (wb-returns-api.test.ts GREEN + stubs SKIPPED + Phase 7/8 тесты GREEN, 0 FAILED)
  </acceptance_criteria>
  <done>
    2 stub-файла созданы с `it.skip` placeholder-ами (минимум 5 + 10 стабов), `npm run test` exit 0 без regression на Phase 7/8 тестах, явные ссылки на downstream планы 09-02 и 09-04 в describe-блоках.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач:
1. `npx prisma validate` exit 0
2. `npx prisma format --check` exit 0
3. `npm run test -- tests/wb-returns-api.test.ts` exit 0 (~10 GREEN tests)
4. `npm run test -- tests/wb-support-api.test.ts` exit 0 (Phase 8 regression — рефакторинг callApi не сломал)
5. `npm run test` exit 0 (полная suite: 89+ Phase 8 тестов + новые Phase 9 — GREEN; stubs SKIPPED)
6. `npx tsc --noEmit` exit 0
7. `grep -c "model ReturnDecision" prisma/schema.prisma` == 1
8. `grep -c "export async function " lib/wb-support-api.ts` >= 9 (5 Phase 8 + 4 Phase 9)
</verification>

<success_criteria>
Plan 09-01 завершён когда:
- Prisma schema содержит ReturnDecision + 2 enum (ReturnDecisionAction, ReturnState) + 8 полей в SupportTicket + relation User.returnDecisions; `npx prisma validate` passes
- lib/wb-support-api.ts экспортирует listReturns/approveReturn/rejectReturn/reconsiderReturn с типами; рефакторинг callApi не сломал Phase 8 методы
- tests/wb-returns-api.test.ts — минимум 10 GREEN test cases
- tests/fixtures/wb-claim-sample.json содержит canonical WB claim
- 2 stub-файла (support-sync-returns, return-actions) созданы с it.skip и ссылками на 09-02/09-04
- `npm run test` 0 failed (включая Phase 8 regression)
- Commit создан с сообщением `feat(09-01): Prisma миграция ReturnDecision + WB Claims API клиент + Wave 0 stubs`
</success_criteria>

<output>
После завершения создать `.planning/phases/09-returns/09-01-SUMMARY.md` с:
- Изменения schema.prisma: ReturnDecision + 2 enum + 8 полей SupportTicket + relation User
- Миграция `<ts>_phase9_returns` — применена локально или pending (для Plan 09-04 deploy)
- Новые методы lib/wb-support-api.ts: listReturns, approveReturn, rejectReturn, reconsiderReturn + контракты
- Рефакторинг: callWb → callApi(baseUrl, ...) с backward compat; callReturnsApi helper
- 3 тест-файла: wb-returns-api.test.ts (GREEN, 10+), support-sync-returns.test.ts (stub), return-actions.test.ts (stub)
- fixture: tests/fixtures/wb-claim-sample.json
- Deploy задача для 09-04: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && npx prisma migrate deploy"`
- Wave 0 VPS проверка scope bit 11 — результат (если была выполнена pre-flight)
</output>
