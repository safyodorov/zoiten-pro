---
phase: 09-returns
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - lib/support-sync.ts
  - app/api/support-sync/route.ts
  - app/api/cron/support-sync-reviews/route.ts
  - tests/support-sync-returns.test.ts
autonomous: true
requirements:
  - SUP-17
must_haves:
  truths:
    - "lib/support-sync.ts экспортирует syncReturns(), загружает обе страницы (is_archive=false и true) с паузой 600ms между запросами"
    - "syncReturns() идемпотентен: двойной вызов не создаёт дубликатов SupportTicket по @@unique([channel=RETURN, wbExternalId=UUID])"
    - "syncReturns() создаёт ровно 1 INBOUND SupportMessage на тикет (user_comment) + SupportMedia для photos и video_paths с https: префиксом"
    - "syncReturns() НЕ перезаписывает returnState при update — защита локальных решений менеджера"
    - "POST /api/support-sync возвращает backward-compatible shape { ok, synced (union), support: {...}, returns: {...}, errors: [...] } — старый SupportSyncButton продолжает работать"
    - "GET /api/cron/support-sync-reviews расширен вызовом syncReturns() (Option A) — единый cron 15 мин покрывает отзывы+вопросы+возвраты"
    - "tests/support-sync-returns.test.ts GREEN — 5 integration тестов через mock fetch с единым mock Prisma (array + callback $transaction)"
  artifacts:
    - path: "lib/support-sync.ts"
      provides: "syncReturns() — полная sync-логика RETURN канала с медиа"
      exports: ["syncSupport", "syncReturns"]
    - path: "app/api/support-sync/route.ts"
      provides: "Обновлённый POST endpoint — вызывает syncSupport + syncReturns, backward-compat response"
      exports: ["POST"]
    - path: "app/api/cron/support-sync-reviews/route.ts"
      provides: "Обновлённый cron — syncSupport + syncReturns в одном hit"
      exports: ["GET"]
    - path: "tests/support-sync-returns.test.ts"
      provides: "Integration-тесты sync логики (mock fetch + mock Prisma)"
      contains: "describe"
  key_links:
    - from: "lib/support-sync.ts syncReturns"
      to: "lib/wb-support-api.ts listReturns"
      via: "import"
      pattern: "listReturns"
    - from: "lib/support-sync.ts syncReturns"
      to: "prisma.supportTicket.upsert"
      via: "channel_wbExternalId composite unique"
      pattern: "channel_wbExternalId"
    - from: "lib/support-sync.ts syncReturns"
      to: "lib/support-media.ts downloadMediaBatch"
      via: "import — reuse Phase 8 медиа pipeline"
      pattern: "downloadMediaBatch"
    - from: "app/api/support-sync/route.ts"
      to: "lib/support-sync.ts syncReturns"
      via: "import"
      pattern: "syncReturns"
    - from: "app/api/cron/support-sync-reviews/route.ts"
      to: "lib/support-sync.ts syncReturns"
      via: "import + объединённый response"
      pattern: "syncReturns"
---

<objective>
Phase 9 Plan 02 — синхронизация возвратов WB → ERP:
1. Расширить `lib/support-sync.ts` новой функцией `syncReturns()`: загружает обе страницы (`is_archive=false` для в работе + `is_archive=true` для архива последних 14 дней) с пагинацией limit=200 + паузой 600ms, делает транзакционный upsert в БД по `(channel=RETURN, wbExternalId=UUID)`, создаёт 1 INBOUND SupportMessage с `user_comment` + SupportMedia для фото/видео (с префиксом `https:`), скачивает медиа через существующий `downloadMediaBatch`.
2. Расширить `POST /api/support-sync` вызовом `syncReturns()` после `syncSupport()` — возвращает **backward-compatible** объединённый результат (Warning 8): сохраняет flat `synced`/`errors` для старого SupportSyncButton + добавляет вложенные `support`/`returns` объекты для новых клиентов.
3. Расширить `GET /api/cron/support-sync-reviews` вызовом `syncReturns()` (Option A из 09-RESEARCH.md §8 — единый CRON_SECRET, единые логи, 15-мин достаточно).
4. Заменить Wave 0 stub `tests/support-sync-returns.test.ts` на 5 GREEN integration-тестов (единый mock Prisma, поддерживающий обе формы $transaction).

Purpose: Автономная подкачка возвратов без участия менеджера. Идемпотентность через composite unique ключ. Переиспользование Phase 8 медиа-pipeline (downloadMediaBatch). КРИТИЧНО: update не трогает `returnState` — защита локальных решений от race condition со sync.

Output: 1 расширенный lib-файл (`support-sync.ts`), 2 обновлённых route-файла, 1 наполненный тест-файл (заменяет Wave 0 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-returns/09-RESEARCH.md
@.planning/phases/09-returns/09-VALIDATION.md
@.planning/phases/09-returns/09-01-SUMMARY.md
@.planning/phases/08-support-mvp/08-02-SUMMARY.md
@CLAUDE.md
@lib/support-sync.ts
@lib/support-media.ts
@lib/wb-support-api.ts
@app/api/support-sync/route.ts
@app/api/cron/support-sync-reviews/route.ts
@components/support/SupportSyncButton.tsx
@lib/prisma.ts

<interfaces>
**Новые (Plan 09-01) из lib/wb-support-api.ts:**
```typescript
export interface Claim {
  id: string; claim_type: number; status: number; status_ex: number
  nm_id: number; user_comment: string; wb_comment?: string
  dt: string; imt_name: string; order_dt?: string; dt_update?: string
  photos?: string[]; video_paths?: string[]  // //photos.wbstatic.net/... (без схемы!)
  actions: string[]; price?: number; currency_code?: string; srid?: string
}
export interface ListReturnsParams { is_archive: boolean; limit?: number; offset?: number; id?: string; nm_id?: number }
export interface ListReturnsResult { claims: Claim[]; total: number }
export async function listReturns(p: ListReturnsParams): Promise<ListReturnsResult>
```

**Новые Prisma поля (Plan 09-01):**
```prisma
model SupportTicket {
  // ...existing Phase 8 fields...
  wbClaimStatus   Int?
  wbClaimStatusEx Int?
  wbClaimType     Int?
  wbActions       String[] @default([])
  wbComment       String?  @db.Text
  srid            String?
  price           Float?
  returnState     ReturnState?  // PENDING | APPROVED | REJECTED
  returnDecisions ReturnDecision[]
  @@unique([channel, wbExternalId])  // Phase 8 — переиспользуем для channel=RETURN
}
enum ReturnState { PENDING; APPROVED; REJECTED }
```

**Существующие (Phase 8) в lib/support-sync.ts (274 строки):**
- `syncSupport({ isAnswered })` — основная функция Phase 8 для feedbacks+questions
- `const YEAR_MS = 365 * 24 * 60 * 60 * 1000` (для SupportMedia.expiresAt)
- Per-ticket `prisma.$transaction` паттерн (строки 76-168)
- `downloadMediaBatch(items, 5)` вызов после transaction

**Существующие (Phase 8) в lib/support-media.ts:**
```typescript
export interface MediaDownloadItem { wbUrl: string; ticketId: string; messageId: string }
export async function downloadMedia(item: MediaDownloadItem): Promise<{ localPath: string; sizeBytes: number }>
export async function downloadMediaBatch(items: MediaDownloadItem[], concurrency: number): Promise<void>
```

**Существующий cron (Phase 8, app/api/cron/support-sync-reviews/route.ts):**
```typescript
export async function GET(req: NextRequest): Promise<NextResponse> {
  const secret = req.headers.get("x-cron-secret")
  if (!secret || secret !== process.env.CRON_SECRET) return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
  try {
    const result = await syncSupport({ isAnswered: false })
    return NextResponse.json({ ok: true, ...result })
  } catch (err) { /* ... */ }
}
```

**Существующий `POST /api/support-sync` (Phase 8) + клиент:**
- session + requireSection("SUPPORT", "MANAGE")
- вызов `syncSupport({ isAnswered: false })`
- возвращает `{ ok, ...result }` с полями типа `feedbacksSynced`, `questionsSynced`, `mediaSaved`

**SupportSyncButton (components/support/SupportSyncButton.tsx Phase 8, реальный response-usage):**
```typescript
const body = await res.json()
if (!res.ok || !body.ok) { toast.error(body.error ?? "Ошибка синхронизации") }
else {
  const summary = `Отзывы: ${body.feedbacksSynced ?? 0}, вопросы: ${body.questionsSynced ?? 0}, медиа: ${body.mediaSaved ?? 0}`
  toast.success(`Готово. ${summary}`)
}
```
⚠ Клиент читает `body.feedbacksSynced`, `body.questionsSynced`, `body.mediaSaved` — **flat top-level поля** из `syncSupport` результата. Новый response должен сохранить эти поля (Warning 8).

**Canonical fixture для тестов (Plan 09-01 создал):**
`tests/fixtures/wb-claim-sample.json` содержит 1 Claim с UUID fe3e9337-...-946a8ebef80, nm_id 196320101, user_comment, photos, video_paths, actions=["autorefund1","approve1"].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: lib/support-sync.ts — syncReturns() с pagination, медиа и идемпотентностью + integration тесты</name>
  <files>lib/support-sync.ts, tests/support-sync-returns.test.ts</files>
  <read_first>
    - lib/support-sync.ts (существующая syncSupport — паттерн per-ticket transaction, upsert по channel_wbExternalId, mediaQueue + downloadMediaBatch, error collection)
    - lib/wb-support-api.ts (Plan 09-01 добавил listReturns + Claim type)
    - lib/support-media.ts (Phase 8 — downloadMediaBatch paths, concurrency)
    - tests/support-sync.test.ts (Phase 8 паттерн integration test с mock fetch)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «Upsert логика в sync» строки 430-496 — полный код syncReturns)
    - tests/fixtures/wb-claim-sample.json (canonical claim для мока)
  </read_first>
  <behavior>
    - `syncReturns(): Promise<{ synced: number; created: number; updated: number; mediaDownloaded: number; errors: string[] }>`:
      - Итерирует по `is_archive in [false, true]`
      - Для каждого: пагинация offset=0, 200, 400... пока `claims.length === limit`
      - Между запросами `sleep(600)` — rate limit 20 req/min
      - На каждый claim → per-claim transaction:
        - upsert SupportTicket по `channel_wbExternalId = ("RETURN", claim.id)`
          - CREATE: `channel="RETURN"`, `wbExternalId=claim.id`, `nmId=claim.nm_id`, `status="NEW"`, `returnState="PENDING"`, `wbClaimStatus`, `wbClaimStatusEx`, `wbClaimType`, `wbActions=claim.actions`, `wbComment=claim.wb_comment`, `srid`, `price`, `previewText=claim.user_comment.slice(0,140)`, `lastMessageAt=new Date(claim.dt)`, `createdAt=new Date(claim.dt)`
          - UPDATE: ТОЛЬКО `wbClaimStatus`, `wbClaimStatusEx`, `wbActions`, `wbComment`, `previewText` (НЕ трогаем `returnState`/`status` — локальные решения)
        - создать 1 INBOUND SupportMessage (только если ещё нет) с `text=claim.user_comment`, `wbSentAt=new Date(claim.dt)`
        - для каждого photo/video — SupportMedia `{ type, wbUrl: "https:"+url, expiresAt: now + YEAR_MS }` + push в mediaQueue `{ wbUrl, ticketId, messageId }`
      - После цикла — `downloadMediaBatch(mediaQueue, 5)`
      - Коллектить ошибки в массив `errors`, не ломать весь sync из-за одной заявки
    - Идемпотентность: повторный вызов с тем же fixture → 0 новых SupportTicket, 0 новых SupportMessage
    - URL normalization: `//photos.wbstatic.net/x` → `https://photos.wbstatic.net/x`; уже `https://...` оставить как есть
  </behavior>
  <action>
    **Шаг A — открыть `lib/support-sync.ts`** и добавить новую функцию после existing `syncSupport`.

    Скелет (полный код вывести из паттерна syncSupport + 09-RESEARCH.md §10):

    ```typescript
    // Phase 9 — синхронизация возвратов
    import { listReturns, type Claim } from "@/lib/wb-support-api"

    export interface SyncReturnsResult {
      synced: number
      created: number
      updated: number
      mediaDownloaded: number
      errors: string[]
    }

    const RETURNS_PAGE_LIMIT = 200
    const RETURNS_PAGE_PAUSE_MS = 600

    function normalizeWbUrl(url: string): string {
      if (url.startsWith("//")) return `https:${url}`
      return url
    }

    async function fetchAllClaims(is_archive: boolean): Promise<Claim[]> {
      const out: Claim[] = []
      for (let offset = 0; ; offset += RETURNS_PAGE_LIMIT) {
        const { claims } = await listReturns({ is_archive, limit: RETURNS_PAGE_LIMIT, offset })
        out.push(...claims)
        if (claims.length < RETURNS_PAGE_LIMIT) break
        await new Promise((r) => setTimeout(r, RETURNS_PAGE_PAUSE_MS))
      }
      return out
    }

    export async function syncReturns(): Promise<SyncReturnsResult> {
      const result: SyncReturnsResult = { synced: 0, created: 0, updated: 0, mediaDownloaded: 0, errors: [] }
      const mediaQueue: { wbUrl: string; ticketId: string; messageId: string }[] = []

      let allClaims: Claim[] = []
      try {
        const pending = await fetchAllClaims(false)
        const archive = await fetchAllClaims(true)
        allClaims = [...pending, ...archive]
      } catch (err) {
        result.errors.push(`listReturns: ${err instanceof Error ? err.message : "unknown"}`)
        return result
      }

      for (const claim of allClaims) {
        try {
          await prisma.$transaction(async (tx) => {
            // existing ticket?
            const existing = await tx.supportTicket.findUnique({
              where: { channel_wbExternalId: { channel: "RETURN", wbExternalId: claim.id } },
              select: { id: true },
            })
            const isCreate = !existing

            const ticket = await tx.supportTicket.upsert({
              where: { channel_wbExternalId: { channel: "RETURN", wbExternalId: claim.id } },
              create: {
                channel: "RETURN",
                wbExternalId: claim.id,
                nmId: claim.nm_id,
                status: "NEW",
                returnState: "PENDING",
                wbClaimStatus: claim.status,
                wbClaimStatusEx: claim.status_ex,
                wbClaimType: claim.claim_type,
                wbActions: claim.actions,
                wbComment: claim.wb_comment,
                srid: claim.srid,
                price: claim.price,
                previewText: claim.user_comment?.slice(0, 140),
                lastMessageAt: new Date(claim.dt),
                createdAt: new Date(claim.dt),
              },
              update: {
                // ⚠ НЕ трогаем returnState/status — локальные решения защищены
                wbClaimStatus: claim.status,
                wbClaimStatusEx: claim.status_ex,
                wbActions: claim.actions,
                wbComment: claim.wb_comment,
                previewText: claim.user_comment?.slice(0, 140),
              },
            })

            if (isCreate) result.created++
            else result.updated++

            // 1 INBOUND message per ticket (только при create или если ещё нет)
            const existingMsg = await tx.supportMessage.findFirst({
              where: { ticketId: ticket.id, direction: "INBOUND" },
              select: { id: true },
            })
            if (!existingMsg) {
              const msg = await tx.supportMessage.create({
                data: {
                  ticketId: ticket.id,
                  direction: "INBOUND",
                  text: claim.user_comment,
                  wbSentAt: new Date(claim.dt),
                },
              })

              // photos
              for (const photo of claim.photos ?? []) {
                const url = normalizeWbUrl(photo)
                await tx.supportMedia.create({
                  data: {
                    messageId: msg.id,
                    type: "IMAGE",
                    wbUrl: url,
                    expiresAt: new Date(Date.now() + YEAR_MS),
                  },
                })
                mediaQueue.push({ wbUrl: url, ticketId: ticket.id, messageId: msg.id })
              }
              // videos
              for (const videoUrl of claim.video_paths ?? []) {
                const url = normalizeWbUrl(videoUrl)
                await tx.supportMedia.create({
                  data: {
                    messageId: msg.id,
                    type: "VIDEO",
                    wbUrl: url,
                    expiresAt: new Date(Date.now() + YEAR_MS),
                  },
                })
                mediaQueue.push({ wbUrl: url, ticketId: ticket.id, messageId: msg.id })
              }
            }

            result.synced++
          })
        } catch (err) {
          result.errors.push(`claim ${claim.id}: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      // Media download вне transaction (не блокируем БД)
      if (mediaQueue.length > 0) {
        try {
          await downloadMediaBatch(mediaQueue, 5)
          result.mediaDownloaded = mediaQueue.length
        } catch (err) {
          result.errors.push(`media: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      return result
    }
    ```

    ⚠ **КРИТИЧНО:** `YEAR_MS` и `downloadMediaBatch` — reuse из Phase 8 (уже импортированы в `lib/support-sync.ts`). Если `YEAR_MS` не экспортирован — использовать `365 * 24 * 60 * 60 * 1000` inline.

    **Шаг B — создать `tests/support-sync-returns.test.ts`** (заменить Wave 0 stub):

    **КРИТИЧНО (Warning 5):** используется **единый mock Prisma**, который корректно поддерживает обе формы `$transaction` (callback для 09-02, array для 09-04). `tx` внутри callback — это сам же `prismaMock`. Тот же паттерн копируется в 09-04 задачу 1 идентично.

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    import sampleResponse from "./fixtures/wb-claim-sample.json"

    // ── Единый mock Prisma (Warning 5) ──────────────────────────────
    // Поддерживает обе формы $transaction:
    //   - callback: prisma.$transaction(async (tx) => {...}) — 09-02 syncReturns
    //   - array: prisma.$transaction([op1, op2]) — 09-04 actions
    const prismaMock = {
      supportTicket: { upsert: vi.fn(), findUnique: vi.fn(), update: vi.fn() },
      supportMessage: { findFirst: vi.fn(), create: vi.fn() },
      supportMedia: { create: vi.fn() },
      returnDecision: { create: vi.fn() },
      $transaction: vi.fn(async (arg: unknown) => {
        if (Array.isArray(arg)) return Promise.all(arg as unknown[])
        if (typeof arg === "function") return (arg as (tx: unknown) => unknown)(prismaMock)
        return arg
      }),
    }

    vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

    vi.mock("@/lib/support-media", () => ({
      downloadMediaBatch: vi.fn().mockResolvedValue(undefined),
    }))

    const ORIGINAL_ENV = process.env.WB_API_TOKEN

    beforeEach(() => {
      process.env.WB_API_TOKEN = "test-token"
      vi.stubGlobal("fetch", vi.fn())
      vi.resetModules()
      // сбросить все Prisma моки
      Object.values(prismaMock).forEach((v) => {
        if (typeof v === "object" && v !== null) {
          Object.values(v).forEach((fn) => {
            if (typeof fn === "function" && "mockReset" in fn) (fn as ReturnType<typeof vi.fn>).mockReset()
          })
        }
      })
      prismaMock.$transaction.mockImplementation(async (arg: unknown) => {
        if (Array.isArray(arg)) return Promise.all(arg as unknown[])
        if (typeof arg === "function") return (arg as (tx: unknown) => unknown)(prismaMock)
        return arg
      })
    })

    afterEach(() => {
      process.env.WB_API_TOKEN = ORIGINAL_ENV
      vi.unstubAllGlobals()
      vi.clearAllMocks()
    })

    function mockRes(body: unknown, status = 200): Response {
      return new Response(JSON.stringify(body), { status, headers: { "Content-Type": "application/json" } })
    }

    describe("syncReturns()", () => {
      it("создаёт SupportTicket с channel=RETURN, returnState=PENDING и нужные поля из Claim", async () => {
        ;(globalThis.fetch as ReturnType<typeof vi.fn>)
          .mockResolvedValueOnce(mockRes(sampleResponse))
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 }))

        prismaMock.supportTicket.upsert.mockResolvedValue({ id: "ticket-1" })
        prismaMock.supportTicket.findUnique.mockResolvedValue(null) // isCreate=true
        prismaMock.supportMessage.findFirst.mockResolvedValue(null)
        prismaMock.supportMessage.create.mockResolvedValue({ id: "msg-1" })

        const { syncReturns } = await import("@/lib/support-sync")
        const result = await syncReturns()

        expect(result.synced).toBe(1)
        expect(result.created).toBe(1)
        expect(prismaMock.supportTicket.upsert).toHaveBeenCalledWith(
          expect.objectContaining({
            where: { channel_wbExternalId: { channel: "RETURN", wbExternalId: "fe3e9337-e9f9-423c-8930-946a8ebef80" } },
            create: expect.objectContaining({
              channel: "RETURN",
              returnState: "PENDING",
              wbActions: ["autorefund1", "approve1"],
              nmId: 196320101,
            }),
          })
        )
      })

      it("идемпотентен: повторный вызов обновляет, но НЕ трогает returnState", async () => {
        ;(globalThis.fetch as ReturnType<typeof vi.fn>)
          .mockResolvedValueOnce(mockRes(sampleResponse))
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 }))

        prismaMock.supportTicket.upsert.mockResolvedValue({ id: "ticket-1" })
        prismaMock.supportTicket.findUnique.mockResolvedValue({ id: "ticket-1" }) // already exists
        prismaMock.supportMessage.findFirst.mockResolvedValue({ id: "msg-1" }) // already exists

        const { syncReturns } = await import("@/lib/support-sync")
        const result = await syncReturns()

        expect(result.updated).toBe(1)
        expect(result.created).toBe(0)

        const upsertCall = prismaMock.supportTicket.upsert.mock.calls[0][0] as { update: Record<string, unknown> }
        expect(upsertCall.update).not.toHaveProperty("returnState")
        expect(upsertCall.update).not.toHaveProperty("status")
        expect(upsertCall.update).toHaveProperty("wbClaimStatus")
        expect(upsertCall.update).toHaveProperty("wbActions")
      })

      it("создаёт SupportMedia с https: префиксом для photos и video_paths", async () => {
        ;(globalThis.fetch as ReturnType<typeof vi.fn>)
          .mockResolvedValueOnce(mockRes(sampleResponse))
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 }))

        prismaMock.supportTicket.upsert.mockResolvedValue({ id: "ticket-1" })
        prismaMock.supportTicket.findUnique.mockResolvedValue(null)
        prismaMock.supportMessage.findFirst.mockResolvedValue(null)
        prismaMock.supportMessage.create.mockResolvedValue({ id: "msg-1" })
        prismaMock.supportMedia.create.mockResolvedValue({})

        const { syncReturns } = await import("@/lib/support-sync")
        await syncReturns()

        // 2 photos + 1 video = 3 media calls
        expect(prismaMock.supportMedia.create).toHaveBeenCalledTimes(3)
        const urls = prismaMock.supportMedia.create.mock.calls.map((c: [{ data: { wbUrl: string } }]) => c[0].data.wbUrl)
        expect(urls.every((u: string) => u.startsWith("https:"))).toBe(true)
        expect(urls).toContain("https://photos.wbstatic.net/claim/fe3e9337-e9f9-423c-8930-946a8ebef80/1.webp")
        expect(urls).toContain("https://video.wbstatic.net/claim/fe3e9337-e9f9-423c-8930-946a8ebef80/1.mp4")
      })

      it("вызывает listReturns с is_archive=false И is_archive=true", async () => {
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 })) // is_archive=false
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 })) // is_archive=true

        const { syncReturns } = await import("@/lib/support-sync")
        await syncReturns()

        expect(fetchMock).toHaveBeenCalledTimes(2)
        const urls = fetchMock.mock.calls.map((c: [string]) => String(c[0]))
        expect(urls.some((u: string) => u.includes("is_archive=false"))).toBe(true)
        expect(urls.some((u: string) => u.includes("is_archive=true"))).toBe(true)
      })

      it("при ошибке одной заявки продолжает обработку остальных (error collected, не throws)", async () => {
        const doubleClaim = {
          claims: [
            { ...sampleResponse.claims[0], id: "uuid-1" },
            { ...sampleResponse.claims[0], id: "uuid-2" },
          ],
          total: 2,
        }
        ;(globalThis.fetch as ReturnType<typeof vi.fn>)
          .mockResolvedValueOnce(mockRes(doubleClaim))
          .mockResolvedValueOnce(mockRes({ claims: [], total: 0 }))

        prismaMock.supportTicket.findUnique.mockResolvedValue(null)
        prismaMock.supportTicket.upsert
          .mockRejectedValueOnce(new Error("DB error uuid-1"))
          .mockResolvedValueOnce({ id: "ticket-2" })
        prismaMock.supportMessage.findFirst.mockResolvedValue(null)
        prismaMock.supportMessage.create.mockResolvedValue({ id: "msg-2" })

        const { syncReturns } = await import("@/lib/support-sync")
        const result = await syncReturns()

        expect(result.errors).toHaveLength(1)
        expect(result.errors[0]).toContain("uuid-1")
        expect(result.synced).toBe(1) // uuid-2 прошёл
      })
    })
    ```

    ⚠ Если mock Prisma получается слишком сложным — допустимо использовать реальную тестовую БД (паттерн из `tests/support-sync.test.ts` если он в Phase 8 использует `prisma.$transaction` rollback). Главное — 5 тестов GREEN и покрытие behavior-контракта.

    **Шаг C — запустить тесты:** `npm run test -- tests/support-sync-returns.test.ts` → 5 GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/support-sync-returns.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function syncReturns" lib/support-sync.ts` == 1
    - `grep "channel: \"RETURN\"" lib/support-sync.ts` находит создание тикета
    - `grep "returnState: \"PENDING\"" lib/support-sync.ts` находит в create-блоке
    - `grep "normalizeWbUrl\|startsWith(\"//\")" lib/support-sync.ts` находит https: префиксование
    - `grep "downloadMediaBatch" lib/support-sync.ts` — минимум 2 вызова (existing Phase 8 + новый Phase 9)
    - `grep "is_archive" lib/support-sync.ts` находит обе страницы
    - `! grep -A5 "update:" lib/support-sync.ts | grep -q "returnState"` — returnState НЕ в update-блоке (bash: проверить inlined)
    - `grep -c "it(" tests/support-sync-returns.test.ts` >= 5 (не it.skip)
    - `grep -q "Array.isArray(arg)" tests/support-sync-returns.test.ts` — единый $transaction mock (Warning 5)
    - `! grep -q "return fn(tx)" tests/support-sync-returns.test.ts` — старый баг с undefined tx ИСПРАВЛЕН
    - `npm run test -- tests/support-sync-returns.test.ts` exit 0 (5 GREEN)
    - `npm run test` exit 0 (Phase 7/8 regression OK)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/support-sync.ts экспортирует syncReturns() с полной логикой (pagination, per-ticket transaction, idempotent upsert, media download, error collection); returnState НЕ затирается при update; photos/videos получают https: префикс; tests/support-sync-returns.test.ts содержит минимум 5 GREEN тестов (не it.skip) с единым mock Prisma (Warning 5 — поддержка обеих форм $transaction, tx — это сам prismaMock, не undefined); npm run test exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: интеграция syncReturns() в POST /api/support-sync и GET /api/cron/support-sync-reviews (backward-compatible)</name>
  <files>app/api/support-sync/route.ts, app/api/cron/support-sync-reviews/route.ts</files>
  <read_first>
    - app/api/support-sync/route.ts (существующий POST endpoint Phase 8 — session + syncSupport + response shape)
    - app/api/cron/support-sync-reviews/route.ts (Phase 8 cron — 23 строки, x-cron-secret + syncSupport)
    - components/support/SupportSyncButton.tsx (читает body.feedbacksSynced, body.questionsSynced, body.mediaSaved — Warning 8)
    - lib/support-sync.ts (задача 1 добавила syncReturns + SyncReturnsResult)
    - .planning/phases/09-returns/09-RESEARCH.md (раздел «Sync Strategy», Option A строки 384-428)
  </read_first>
  <action>
    **Шаг A — обновить `app/api/support-sync/route.ts`** (ручная синхронизация) — **backward-compatible (Warning 8):**

    Старый SupportSyncButton читает `body.feedbacksSynced`, `body.questionsSynced`, `body.mediaSaved` — flat top-level поля из `syncSupport` результата. НЕ ломаем их. Добавляем вложенные `support`/`returns` для новых клиентов + объединённый `synced` и `errors`.

    После существующего `await syncSupport({ isAnswered: false })` добавить `await syncReturns()` и собрать backward-compatible response:

    ```typescript
    import { syncSupport, syncReturns } from "@/lib/support-sync"

    // ...existing auth/session checks...

    try {
      const supportResult = await syncSupport({ isAnswered: false })
      const returnsResult = await syncReturns()

      // Backward-compatible response (Warning 8):
      // - spread supportResult на top-level → старые поля (feedbacksSynced, questionsSynced, mediaSaved)
      //   продолжают читаться клиентом SupportSyncButton.tsx
      // - добавляем объединённый synced = support + returns для общего summary
      // - вложенные support/returns объекты для новых клиентов
      // - errors union для старых клиентов, которые могут их читать

      return NextResponse.json({
        ok: true,
        ...supportResult, // flat: feedbacksSynced, questionsSynced, mediaSaved, errors (если есть)
        synced: (supportResult.synced ?? 0) + returnsResult.synced, // объединённый счётчик (backward-compat flat)
        support: supportResult,
        returns: returnsResult,
        errors: [
          ...((supportResult as { errors?: string[] }).errors ?? []),
          ...returnsResult.errors,
        ],
      })
    } catch (err) {
      // existing error handling — не трогаем
    }
    ```

    ⚠ **КРИТИЧНО (Warning 8):** spread `supportResult` ПЕРЕД явными полями — чтобы наши `synced`, `support`, `returns`, `errors` перекрыли одноимённые поля из `supportResult`, но старые поля типа `feedbacksSynced` остались. Клиент SupportSyncButton продолжит работать без изменений. Новый toast UI (с разбивкой support/returns) опционален — можно оставить на позже в рамках gap-closure.

    **Шаг B — обновить `app/api/cron/support-sync-reviews/route.ts`** (cron 15 мин, Option A из research):

    ```typescript
    // GET /api/cron/support-sync-reviews — cron синхронизация отзывов/вопросов/возвратов (15 мин).
    // Защищён заголовком x-cron-secret.
    // Phase 9: расширен вызовом syncReturns() — единый CRON_SECRET, единые логи.

    import { NextRequest, NextResponse } from "next/server"
    import { syncSupport, syncReturns } from "@/lib/support-sync"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }
      try {
        const support = await syncSupport({ isAnswered: false })
        const returns = await syncReturns()
        return NextResponse.json({ ok: true, support, returns })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка синхронизации"
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    ⚠ **Option A vs Option B:** SUP-07 в REQUIREMENTS.md перечисляет только `support-sync-reviews/chat/appeals` — не упоминает отдельный returns cron. 09-RESEARCH.md §8 рекомендует Option A (единый cron). Используем A.

    **Шаг C — верификация:** `npx tsc --noEmit` + `npm run build`.

    ⚠ Смоуки на живых HTTP endpoint-ах (реальный sync через POST) — это human UAT из Plan 09-04, не часть этой задачи.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "syncReturns" app/api/support-sync/route.ts` находит import и вызов
    - `grep "syncReturns" app/api/cron/support-sync-reviews/route.ts` находит import и вызов
    - `grep "from \"@/lib/support-sync\"" app/api/cron/support-sync-reviews/route.ts` находит импорт (обе функции)
    - `grep "x-cron-secret" app/api/cron/support-sync-reviews/route.ts` всё ещё присутствует (не сломали auth)
    - `grep "returns" app/api/cron/support-sync-reviews/route.ts` находит в response
    - `grep -q "synced: (supportResult.synced ?? 0) + returnsResult.synced\|\.\.\.supportResult" app/api/support-sync/route.ts` — Warning 8 backward-compat spread
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    POST /api/support-sync вызывает syncSupport + syncReturns, возвращает backward-compatible response: flat spread `supportResult` (старые поля feedbacksSynced/questionsSynced/mediaSaved для SupportSyncButton) + объединённый `synced` + вложенные `support`/`returns` объекты + union `errors`; GET /api/cron/support-sync-reviews аналогично расширен без ломания x-cron-secret guard; tsc и build проходят.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npx prisma validate` exit 0
2. `npx tsc --noEmit` exit 0
3. `npm run build` exit 0
4. `npm run test -- tests/support-sync-returns.test.ts` exit 0 (5 GREEN)
5. `npm run test` exit 0 (Phase 7/8/9 полная suite, 0 failed, stubs SKIPPED, returns tests GREEN)
6. `grep -c "export async function syncReturns" lib/support-sync.ts` == 1
7. `grep -c "syncReturns" app/api/` >= 2 (два route-файла)
8. SupportSyncButton (Phase 8) продолжает работать без изменений — body.feedbacksSynced всё ещё читается
</verification>

<success_criteria>
Plan 09-02 завершён когда:
- lib/support-sync.ts экспортирует syncReturns() с полной pagination, per-claim transaction, media pipeline и защитой returnState от затирания
- POST /api/support-sync и GET /api/cron/support-sync-reviews оба вызывают syncReturns() после syncSupport()
- POST response backward-compatible (Warning 8) — старый SupportSyncButton продолжает работать
- tests/support-sync-returns.test.ts содержит минимум 5 GREEN интеграционных тестов с единым mock Prisma (Warning 5 — fixed tx undefined bug)
- `npm run test` 0 failed
- Commit создан с сообщением `feat(09-02): syncReturns + cron интеграция возвратов WB`
</success_criteria>

<output>
После завершения создать `.planning/phases/09-returns/09-02-SUMMARY.md` с:
- Новая функция syncReturns() — контракт, pagination strategy (600ms пауза, оба is_archive флага), защита returnState
- Обновлённые routes — POST /api/support-sync (backward-compat shape) и GET /api/cron/support-sync-reviews
- Тесты: 5 GREEN integration в support-sync-returns.test.ts (паттерн mock fetch + единый mock Prisma с dual-mode $transaction)
- Decision: Option A выбран (единый cron 15 мин), reasoning
- Задача для 09-04 deploy: `systemctl restart zoiten-erp.service` + проверить следующий cron tick через `journalctl`
</output>
</context>
