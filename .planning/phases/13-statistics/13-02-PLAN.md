---
phase: 13-statistics
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - app/(dashboard)/support/stats/page.tsx
  - components/support/stats/StatsTabs.tsx
  - components/support/stats/PeriodFilter.tsx
  - components/support/stats/ProductStatsTab.tsx
  - components/support/stats/ManagerStatsTab.tsx
  - components/support/stats/TopReturnReasonsList.tsx
  - components/support/stats/AutoRepliesSummary.tsx
  - components/layout/nav-items.ts
  - components/layout/section-titles.ts
  - tests/support-stats-page.test.ts
autonomous: true
requirements:
  - SUP-36
  - SUP-37
  - SUP-38
must_haves:
  truths:
    - "app/(dashboard)/support/stats/page.tsx — RSC с requireSection('SUPPORT') (D-10 VIEW, не MANAGE), parse searchParams (tab, period, dateFrom, dateTo, nmId, userId) через Zod + fallback на 'products' tab + '30d' period"
    - "Страница собирает данные через Plan 13-01 helpers: listProductsWithStats для tab=products, listManagersWithStats + getAutoReplyCount + getTopReturnReasons для tab=managers"
    - "StatsTabs — client компонент с 2 табами (По товарам / По менеджерам), активный таб определяется searchParams.tab, переключение через router.push с preserved period params"
    - "PeriodFilter — client компонент: native <select> с 4 опциями (D-05 квартал = календарный) + 2 <input type='date'> для custom + кнопка «Применить» (URL searchParams update)"
    - "ProductStatsTab — RSC секция: summary cards (total tickets / avg rating / topReturnReasons list) + таблица товаров с 10 колонками (фото / nmId+название / feedbacks / rating / answered% / questions / returns total / approved / rejected / avgResponse)"
    - "ManagerStatsTab — RSC секция: 3 summary cards (totalProcessed aggregate / avgResponse global / AutoRepliesSummary D-02 global) + таблица менеджеров с 10 колонками (имя / isLive badge / totalProcessed / feedbacks / questions / chats / returns decided / approval% / appeals / avgResponse)"
    - "TopReturnReasonsList — RSC компонент: top-10 причин с count bars (шириной пропорциональной count/max), D-03 глобально"
    - "AutoRepliesSummary — RSC компонент: одна карточка «Автоответов за период: {N}» (D-02 global, не per-manager)"
    - "UI — только таблицы + числа (D-07 без recharts/графиков)"
    - "nav-items.ts — добавлен пункт { section: 'SUPPORT', href: '/support/stats', label: 'Статистика', icon: 'BarChart3' } перед 'Автоответ'"
    - "section-titles.ts — добавлен regex /^\\/support\\/stats/ → 'Статистика службы поддержки' (до /^\\/support/)"
    - "tests/support-stats-page.test.ts заменяет it.skip на 5+ GREEN тестов: parseSearchParams happy/fallback, tab default='products', period default='30d', RBAC rejects без sectionRoles SUPPORT"
  artifacts:
    - path: "app/(dashboard)/support/stats/page.tsx"
      provides: "RSC страница /support/stats с фильтрами и 2 табами"
      contains: "requireSection"
    - path: "components/support/stats/StatsTabs.tsx"
      provides: "Client tab navigation через URL searchParams"
      exports: ["StatsTabs"]
    - path: "components/support/stats/PeriodFilter.tsx"
      provides: "Client фильтр периода (native select + date inputs)"
      exports: ["PeriodFilter"]
    - path: "components/support/stats/ProductStatsTab.tsx"
      provides: "RSC секция метрик по товарам"
      exports: ["ProductStatsTab"]
    - path: "components/support/stats/ManagerStatsTab.tsx"
      provides: "RSC секция метрик по менеджерам"
      exports: ["ManagerStatsTab"]
    - path: "components/support/stats/TopReturnReasonsList.tsx"
      provides: "RSC компонент топ-10 причин возвратов"
      exports: ["TopReturnReasonsList"]
    - path: "components/support/stats/AutoRepliesSummary.tsx"
      provides: "RSC карточка глобального счётчика автоответов"
      exports: ["AutoRepliesSummary"]
    - path: "components/layout/nav-items.ts"
      provides: "Обновлённый nav с пунктом Статистика"
      contains: "stats"
    - path: "components/layout/section-titles.ts"
      provides: "Обновлённый routing с title для /support/stats"
      contains: "stats"
    - path: "tests/support-stats-page.test.ts"
      provides: "5+ GREEN тестов page-level behavior + RBAC + searchParams parsing"
      contains: "describe"
  key_links:
    - from: "app/(dashboard)/support/stats/page.tsx"
      to: "lib/support-stats.ts"
      via: "import listProductsWithStats + listManagersWithStats + getTopReturnReasons + getAutoReplyCount"
      pattern: "@/lib/support-stats"
    - from: "app/(dashboard)/support/stats/page.tsx"
      to: "lib/date-periods.ts"
      via: "import getPeriod для парсинга searchParams"
      pattern: "@/lib/date-periods"
    - from: "app/(dashboard)/support/stats/page.tsx"
      to: "requireSection(\"SUPPORT\")"
      via: "RBAC guard (D-10 VIEW, не MANAGE)"
      pattern: "requireSection\\(\"SUPPORT\"\\)"
    - from: "components/support/stats/StatsTabs.tsx"
      to: "next/navigation useRouter/useSearchParams"
      via: "tab switching через URL push"
      pattern: "useSearchParams"
    - from: "components/support/stats/PeriodFilter.tsx"
      to: "next/navigation useRouter"
      via: "period update через URL push"
      pattern: "router.push"
    - from: "components/layout/nav-items.ts"
      to: "/support/stats"
      via: "nav entry с section=SUPPORT + icon=BarChart3"
      pattern: "/support/stats"
---

<objective>
Phase 13 Plan 02 — UI слой страницы `/support/stats`:

1. **RSC `/support/stats/page.tsx`** — requireSection('SUPPORT') VIEW (D-10), парсинг searchParams (tab, period, dateFrom, dateTo, nmId, userId) через Zod, data assembly через Plan 13-01 helpers, render 2-секционного layout (фильтры сверху + StatsTabs + активный таб).

2. **7 новых компонентов в `components/support/stats/`:**
   - `StatsTabs` (client) — tab navigation через URL searchParams (По товарам / По менеджерам)
   - `PeriodFilter` (client) — native `<select>` пресет + 2 `<input type="date">` для custom, submit по кнопке «Применить»
   - `ProductStatsTab` (RSC) — summary cards + таблица SUP-37 с 10 колонками
   - `ManagerStatsTab` (RSC) — summary cards + таблица SUP-38 с 10 колонками + AutoRepliesSummary + avg response global
   - `TopReturnReasonsList` (RSC) — топ-10 причин с визуальными bars (D-03 глобально)
   - `AutoRepliesSummary` (RSC) — одна карточка глобального счётчика (D-02)

3. **Навигация:**
   - `nav-items.ts` — новый пункт Статистика (section=SUPPORT, icon=BarChart3, href=/support/stats)
   - `section-titles.ts` — /support/stats → «Статистика службы поддержки»

4. **tests/support-stats-page.test.ts заменён** — 5+ GREEN тестов для searchParams helpers + smoke (без React-рендера, проверяем контракт pure helpers).

Purpose: SUP-36/37/38 — руководитель открывает `/support/stats`, переключает табы «По товарам» и «По менеджерам», меняет период (7д/30д/квартал/кастом), видит реальные агрегированные числа из БД. UI — только числа и таблицы (D-07).

Output: 1 новая RSC страница, 7 новых компонентов (5 RSC + 2 client), 2 модифицированных навигационных файла, 1 расширенный test (5+ GREEN). Без cron (в 13-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-statistics/13-RESEARCH.md
@.planning/phases/13-statistics/13-VALIDATION.md
@.planning/phases/13-statistics/13-01-PLAN.md
@CLAUDE.md
@app/(dashboard)/support/customers/[customerId]/page.tsx
@app/(dashboard)/support/returns/page.tsx
@components/support/SupportFilters.tsx
@components/layout/nav-items.ts
@components/layout/section-titles.ts
@lib/rbac.ts

<interfaces>
**Plan 13-01 exports (используются напрямую):**
```typescript
// lib/date-periods.ts
export const PERIOD_PRESETS: readonly ["7d", "30d", "quarter", "custom"]
export type PeriodPreset = "7d" | "30d" | "quarter" | "custom"
export interface Period { dateFrom: Date; dateTo: Date }
export function getPeriod(preset: PeriodPreset, custom?: { from: string; to: string }): Period

// lib/support-stats.ts
export interface ProductStatRow {
  nmId: number; name: string | null; photoUrl: string | null
  feedbacksTotal: number; avgRating: number | null; feedbacksAnsweredPct: number | null
  questionsTotal: number; returnsTotal: number; returnsApproved: number; returnsRejected: number
  avgResponseTimeSec: number | null
}
export interface ManagerStatFields {
  totalProcessed: number; feedbacksAnswered: number; questionsAnswered: number; chatsAnswered: number
  returnsDecided: number; returnsApproved: number; returnsRejected: number; appealsResolved: number
  avgResponseTimeSec: number | null
}
export interface ManagerStatRow extends ManagerStatFields {
  userId: string; name: string | null; isLive: boolean
}

export async function listProductsWithStats(dateFrom: Date, dateTo: Date, filters?: { nmIds?: number[] }): Promise<ProductStatRow[]>
export async function listManagersWithStats(dateFrom: Date, dateTo: Date): Promise<ManagerStatRow[]>
export async function getTopReturnReasons(dateFrom: Date, dateTo: Date, limit?: number): Promise<Array<{ reason: string; count: number }>>
export async function getAutoReplyCount(dateFrom: Date, dateTo: Date): Promise<number>
```

**Паттерн RSC page с searchParams (из app/(dashboard)/support/returns/page.tsx и /support/customers/[id]/page.tsx):**
```typescript
export const dynamic = "force-dynamic"

export default async function Page({
  searchParams,
}: {
  searchParams: Promise<Record<string, string | string[] | undefined>>
}) {
  await requireSection("SUPPORT")
  const sp = await searchParams
  // ... parse, fetch data, render
}
```

**Паттерн client tab через URL (из components/cards/CardsTabs.tsx):**
```typescript
"use client"
import { useSearchParams, usePathname, useRouter } from "next/navigation"

const sp = useSearchParams()
const tab = sp.get("tab") ?? "products"
const router = useRouter()
const pathname = usePathname()

function setTab(t: string) {
  const newSp = new URLSearchParams(sp.toString())
  newSp.set("tab", t)
  router.push(`${pathname}?${newSp.toString()}`)
}
```

**Паттерн native select (CLAUDE.md convention):**
```tsx
<select
  value={preset}
  onChange={(e) => setPreset(e.target.value)}
  className="border rounded px-3 py-1 text-sm"
>
  <option value="7d">7 дней</option>
  <option value="30d">30 дней</option>
  <option value="quarter">Квартал</option>
  <option value="custom">Кастом</option>
</select>
```

**Moscow TZ formatter:**
```typescript
const fmt = new Intl.DateTimeFormat("ru-RU", {
  timeZone: "Europe/Moscow",
  day: "2-digit", month: "2-digit", year: "numeric",
})
fmt.format(date) // "17.04.2026"
```

**Формат avgResponseTimeSec → человеческий текст:**
```typescript
function formatDuration(sec: number | null): string {
  if (sec === null) return "—"
  if (sec < 60) return `${sec} сек`
  if (sec < 3600) return `${Math.round(sec / 60)} мин`
  return `${(sec / 3600).toFixed(1)} ч`
}
```

**Zod для searchParams parse:**
```typescript
import { z } from "zod"

const searchParamsSchema = z.object({
  tab: z.enum(["products", "managers"]).default("products"),
  period: z.enum(["7d", "30d", "quarter", "custom"]).default("30d"),
  dateFrom: z.string().optional(),
  dateTo: z.string().optional(),
  nmId: z.coerce.number().int().positive().optional(),
  userId: z.string().optional(),
})
```

**Photo URL паттерн (WbCard.photoUrl из Phase 8):**
- Абсолютный URL на WB CDN ИЛИ /uploads/...
- В табах статистики рендерить через `<img src={photoUrl} className="h-10 w-10 object-cover rounded">` с fallback на placeholder div
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: RSC page /support/stats + PeriodFilter + StatsTabs + nav integration</name>
  <files>app/(dashboard)/support/stats/page.tsx, components/support/stats/StatsTabs.tsx, components/support/stats/PeriodFilter.tsx, components/layout/nav-items.ts, components/layout/section-titles.ts</files>
  <read_first>
    - app/(dashboard)/support/returns/page.tsx (Phase 9 паттерн RSC + searchParams parse + filters)
    - app/(dashboard)/support/customers/[customerId]/page.tsx (Phase 12 паттерн RSC + aggregation helpers + 2-col layout)
    - components/cards/CardsTabs.tsx (паттерн client tabs через URL — если не существует, использовать паттерн из components/prices/PricesTabs.tsx)
    - components/support/SupportFilters.tsx (Phase 8 паттерн native select фильтров)
    - components/layout/nav-items.ts (текущий список пунктов)
    - components/layout/section-titles.ts (текущие регексы)
    - lib/rbac.ts (requireSection signature)
  </read_first>
  <action>
    **Шаг A — создать `components/support/stats/StatsTabs.tsx`:**

    ```typescript
    "use client"

    import { useSearchParams, usePathname, useRouter } from "next/navigation"

    export interface StatsTabsProps {
      currentTab: "products" | "managers"
    }

    export function StatsTabs({ currentTab }: StatsTabsProps) {
      const sp = useSearchParams()
      const pathname = usePathname()
      const router = useRouter()

      function setTab(tab: "products" | "managers") {
        const newSp = new URLSearchParams(sp.toString())
        newSp.set("tab", tab)
        router.push(`${pathname}?${newSp.toString()}`)
      }

      const tabClass = (t: string) =>
        t === currentTab
          ? "border-b-2 border-primary text-foreground"
          : "border-b-2 border-transparent text-muted-foreground hover:text-foreground"

      return (
        <div className="flex gap-6 border-b">
          <button
            type="button"
            onClick={() => setTab("products")}
            className={`${tabClass("products")} pb-2 text-sm font-medium`}
          >
            По товарам
          </button>
          <button
            type="button"
            onClick={() => setTab("managers")}
            className={`${tabClass("managers")} pb-2 text-sm font-medium`}
          >
            По менеджерам
          </button>
        </div>
      )
    }
    ```

    **Шаг B — создать `components/support/stats/PeriodFilter.tsx`:**

    ```typescript
    "use client"

    import { useSearchParams, usePathname, useRouter } from "next/navigation"
    import { useState, useEffect } from "react"

    export interface PeriodFilterProps {
      currentPeriod: "7d" | "30d" | "quarter" | "custom"
      currentFrom?: string
      currentTo?: string
    }

    export function PeriodFilter({ currentPeriod, currentFrom, currentTo }: PeriodFilterProps) {
      const sp = useSearchParams()
      const pathname = usePathname()
      const router = useRouter()

      const [preset, setPreset] = useState(currentPeriod)
      const [from, setFrom] = useState(currentFrom ?? "")
      const [to, setTo] = useState(currentTo ?? "")

      useEffect(() => {
        setPreset(currentPeriod)
        setFrom(currentFrom ?? "")
        setTo(currentTo ?? "")
      }, [currentPeriod, currentFrom, currentTo])

      function apply() {
        const newSp = new URLSearchParams(sp.toString())
        newSp.set("period", preset)
        if (preset === "custom") {
          if (from) newSp.set("dateFrom", from); else newSp.delete("dateFrom")
          if (to) newSp.set("dateTo", to); else newSp.delete("dateTo")
        } else {
          newSp.delete("dateFrom")
          newSp.delete("dateTo")
        }
        router.push(`${pathname}?${newSp.toString()}`)
      }

      return (
        <div className="flex flex-wrap items-end gap-3">
          <label className="text-sm">
            <span className="block text-xs text-muted-foreground mb-1">Период</span>
            <select
              value={preset}
              onChange={(e) => setPreset(e.target.value as typeof preset)}
              className="border rounded px-3 py-1.5 text-sm"
            >
              <option value="7d">7 дней</option>
              <option value="30d">30 дней</option>
              <option value="quarter">Квартал (календарный)</option>
              <option value="custom">Кастом</option>
            </select>
          </label>

          {preset === "custom" && (
            <>
              <label className="text-sm">
                <span className="block text-xs text-muted-foreground mb-1">С</span>
                <input
                  type="date"
                  value={from}
                  onChange={(e) => setFrom(e.target.value)}
                  className="border rounded px-3 py-1.5 text-sm"
                />
              </label>
              <label className="text-sm">
                <span className="block text-xs text-muted-foreground mb-1">По</span>
                <input
                  type="date"
                  value={to}
                  onChange={(e) => setTo(e.target.value)}
                  className="border rounded px-3 py-1.5 text-sm"
                />
              </label>
            </>
          )}

          <button
            type="button"
            onClick={apply}
            disabled={preset === "custom" && (!from || !to)}
            className="rounded bg-primary px-4 py-1.5 text-sm text-primary-foreground disabled:opacity-50"
          >
            Применить
          </button>
        </div>
      )
    }
    ```

    **Шаг C — создать `app/(dashboard)/support/stats/page.tsx`:**

    ```typescript
    // app/(dashboard)/support/stats/page.tsx
    // Phase 13 — RSC страница /support/stats (SUP-36/37/38).
    // RBAC D-10: SUPPORT VIEW (не MANAGE — read-only).

    import { z } from "zod"
    import { requireSection } from "@/lib/rbac"
    import { getPeriod } from "@/lib/date-periods"
    import {
      listProductsWithStats,
      listManagersWithStats,
      getTopReturnReasons,
      getAutoReplyCount,
    } from "@/lib/support-stats"
    import { StatsTabs } from "@/components/support/stats/StatsTabs"
    import { PeriodFilter } from "@/components/support/stats/PeriodFilter"
    import { ProductStatsTab } from "@/components/support/stats/ProductStatsTab"
    import { ManagerStatsTab } from "@/components/support/stats/ManagerStatsTab"

    export const dynamic = "force-dynamic"

    const searchParamsSchema = z.object({
      tab: z.enum(["products", "managers"]).default("products"),
      period: z.enum(["7d", "30d", "quarter", "custom"]).default("30d"),
      dateFrom: z.string().optional(),
      dateTo: z.string().optional(),
      nmId: z.coerce.number().int().positive().optional(),
      userId: z.string().optional(),
    })

    export function parseStatsSearchParams(
      sp: Record<string, string | string[] | undefined>
    ): z.infer<typeof searchParamsSchema> {
      const flat: Record<string, string | undefined> = {}
      for (const k of Object.keys(sp)) {
        const v = sp[k]
        flat[k] = Array.isArray(v) ? v[0] : v
      }
      const parsed = searchParamsSchema.safeParse(flat)
      return parsed.success
        ? parsed.data
        : searchParamsSchema.parse({ tab: "products", period: "30d" })
    }

    export default async function SupportStatsPage({
      searchParams,
    }: {
      searchParams: Promise<Record<string, string | string[] | undefined>>
    }) {
      await requireSection("SUPPORT")
      const sp = await searchParams
      const parsed = parseStatsSearchParams(sp)

      const period =
        parsed.period === "custom" && parsed.dateFrom && parsed.dateTo
          ? getPeriod("custom", { from: parsed.dateFrom, to: parsed.dateTo })
          : getPeriod(parsed.period === "custom" ? "30d" : parsed.period)

      // Data assembly depending on tab
      let tabContent: React.ReactNode
      if (parsed.tab === "products") {
        const [products, topReasons] = await Promise.all([
          listProductsWithStats(period.dateFrom, period.dateTo, parsed.nmId ? { nmIds: [parsed.nmId] } : undefined),
          getTopReturnReasons(period.dateFrom, period.dateTo, 10),
        ])
        tabContent = <ProductStatsTab products={products} topReasons={topReasons} />
      } else {
        const [managers, autoReplyCount] = await Promise.all([
          listManagersWithStats(period.dateFrom, period.dateTo),
          getAutoReplyCount(period.dateFrom, period.dateTo),
        ])
        tabContent = <ManagerStatsTab managers={managers} autoReplyCount={autoReplyCount} />
      }

      return (
        <div className="space-y-6">
          <PeriodFilter
            currentPeriod={parsed.period}
            currentFrom={parsed.dateFrom}
            currentTo={parsed.dateTo}
          />
          <StatsTabs currentTab={parsed.tab} />
          {tabContent}
        </div>
      )
    }
    ```

    **Шаг D — обновить `components/layout/nav-items.ts`** — добавить пункт «Статистика»:

    Найти импорт lucide (около строк 3-19) и добавить `BarChart3` в импорт:
    ```typescript
    import {
      Package, CreditCard, Tag, LayoutGrid, Boxes, Calculator,
      ShoppingCart, TrendingUp, Headphones, PackageX, FileText, Bot,
      BarChart3, UserCheck, Users, Settings,
    } from "lucide-react"
    ```

    В NAV_ITEMS добавить между `Автоответ` и `Сотрудники`:
    ```typescript
    { section: "SUPPORT", href: "/support/stats", label: "Статистика", icon: "BarChart3" },
    ```

    В ICON_MAP добавить:
    ```typescript
    BarChart3,
    ```

    **Шаг E — обновить `components/layout/section-titles.ts`** — добавить регекс для /support/stats ДО `/^\/support/`:

    Найти блок `{ match: /^\/support\/returns/, title: "Возвраты" },` и добавить ПЕРЕД ним:
    ```typescript
    { match: /^\/support\/stats/, title: "Статистика службы поддержки" },
    ```

    (Порядок важен — более специфичные регексы идут раньше общего `/^\/support/`.)

    **Шаг F — sanity check:**
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/(dashboard)/support/stats/page.tsx` exit 0
    - `test -f components/support/stats/StatsTabs.tsx` exit 0
    - `test -f components/support/stats/PeriodFilter.tsx` exit 0
    - `grep "requireSection(\"SUPPORT\")" app/(dashboard)/support/stats/page.tsx` находит (D-10 VIEW без MANAGE)
    - `grep -c "requireSection(\"SUPPORT\", \"MANAGE\")" app/(dashboard)/support/stats/page.tsx` == 0 (read-only page)
    - `grep "export function parseStatsSearchParams" app/(dashboard)/support/stats/page.tsx` находит
    - `grep "listProductsWithStats\|listManagersWithStats" app/(dashboard)/support/stats/page.tsx | wc -l` >= 2
    - `grep "getTopReturnReasons\|getAutoReplyCount" app/(dashboard)/support/stats/page.tsx | wc -l` >= 2
    - `grep "/support/stats" components/layout/nav-items.ts` находит
    - `grep "BarChart3" components/layout/nav-items.ts | wc -l` >= 2 (импорт + ICON_MAP)
    - `grep "/support/stats" components/layout/section-titles.ts` находит
    - `grep "Статистика службы поддержки" components/layout/section-titles.ts` находит
    - `grep "use client" components/support/stats/StatsTabs.tsx` находит
    - `grep "use client" components/support/stats/PeriodFilter.tsx` находит
    - `grep "useSearchParams\|usePathname\|useRouter" components/support/stats/StatsTabs.tsx | wc -l` >= 3
    - `grep "календарный\|Квартал" components/support/stats/PeriodFilter.tsx` находит (D-05 hint в UI)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    /support/stats RSC page рендерится с requireSection('SUPPORT') VIEW (D-10), парсит searchParams через Zod с fallback на products/30d, вызывает Plan 13-01 helpers; StatsTabs + PeriodFilter реализованы как client-компоненты с URL-based state; навигация и title обновлены; tsc проходит.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: 5 RSC компонентов контента табов — ProductStatsTab, ManagerStatsTab, TopReturnReasonsList, AutoRepliesSummary + 1 модификация page.tsx</name>
  <files>components/support/stats/ProductStatsTab.tsx, components/support/stats/ManagerStatsTab.tsx, components/support/stats/TopReturnReasonsList.tsx, components/support/stats/AutoRepliesSummary.tsx</files>
  <read_first>
    - app/(dashboard)/support/returns/page.tsx (Phase 9 — паттерн таблицы с photo+nmId+название)
    - components/support/customers/ChannelStats.tsx (Phase 12 — паттерн summary cards)
    - components/support/customers/TicketsTable.tsx (Phase 12 — паттерн client table DESC)
    - Задача 1 — page.tsx (использует эти компоненты как props)
    - CLAUDE.md (русский UI, native select)
  </read_first>
  <behavior>
    **ProductStatsTab** — RSC, props: `{ products: ProductStatRow[]; topReasons: Array<{reason, count}> }`
    - Summary cards (3 штуки): Всего отзывов (SUM feedbacksTotal), Всего возвратов (SUM returnsTotal), Средний рейтинг (AVG по avgRating не-null)
    - TopReturnReasonsList секция (передаётся topReasons)
    - Таблица 10 колонок: Фото / nmId+название / Отзывов / Рейтинг / % отв. / Вопросов / Возвраты всего / Одобрено / Отклонено / Ср. время
    - Сортировка DESC по feedbacksTotal (первый товар — самый активный)
    - Empty state: «Нет данных за выбранный период»
    - Все числа форматируются: rating → `.toFixed(1)`, null → «—», avgResponseTimeSec → formatDuration helper

    **ManagerStatsTab** — RSC, props: `{ managers: ManagerStatRow[]; autoReplyCount: number }`
    - Summary cards (3 штуки): Всего обработано (SUM totalProcessed), Глобальное среднее время ответа (AVG avgResponseTimeSec не-null), AutoRepliesSummary (передаётся autoReplyCount)
    - Таблица 10 колонок: Имя / Live / Всего / Отзывы / Вопросы / Чаты / Возвраты / % одобр / Обжалования / Ср. время
    - isLive=true → badge «сегодня» (зелёный)
    - % одобрения = returnsApproved / returnsDecided * 100, или «—» если returnsDecided=0
    - Сортировка DESC по totalProcessed

    **TopReturnReasonsList** — RSC, props: `{ reasons: Array<{reason, count}> }`
    - D-03 глобально (не per-product)
    - Топ-10 в виде visual bars: ширина = count / maxCount * 100%
    - Empty: «Нет отклонённых возвратов за период»

    **AutoRepliesSummary** — RSC, props: `{ count: number }`
    - D-02 глобально
    - Одна карточка с большой цифрой и подписью «Автоответов за период»

    **Shared helper formatDuration** — inline в каждом компоненте или общий:
    ```typescript
    function formatDuration(sec: number | null): string {
      if (sec === null) return "—"
      if (sec < 60) return `${sec} сек`
      if (sec < 3600) return `${Math.round(sec / 60)} мин`
      return `${(sec / 3600).toFixed(1)} ч`
    }
    ```
  </behavior>
  <action>
    **Шаг A — создать `components/support/stats/AutoRepliesSummary.tsx`** (RSC, D-02 глобальный счётчик):

    ```typescript
    // components/support/stats/AutoRepliesSummary.tsx
    // Phase 13 D-02 — глобальный счётчик автоответов (не per-manager).

    import { Bot } from "lucide-react"

    export interface AutoRepliesSummaryProps {
      count: number
    }

    export function AutoRepliesSummary({ count }: AutoRepliesSummaryProps) {
      return (
        <div className="rounded-lg border p-4 flex items-center gap-3">
          <div className="rounded-full bg-muted p-2">
            <Bot className="h-5 w-5 text-muted-foreground" />
          </div>
          <div>
            <div className="text-2xl font-semibold">{count}</div>
            <div className="text-xs text-muted-foreground">Автоответов за период</div>
          </div>
        </div>
      )
    }
    ```

    **Шаг B — создать `components/support/stats/TopReturnReasonsList.tsx`** (RSC, D-03 глобально):

    ```typescript
    // components/support/stats/TopReturnReasonsList.tsx
    // Phase 13 D-03 — топ-10 причин отказов (глобально, из ReturnDecision.reason где action=REJECT).

    export interface TopReturnReasonsListProps {
      reasons: Array<{ reason: string; count: number }>
    }

    export function TopReturnReasonsList({ reasons }: TopReturnReasonsListProps) {
      if (reasons.length === 0) {
        return (
          <div className="rounded-lg border p-4 text-sm text-muted-foreground">
            Нет отклонённых возвратов за выбранный период
          </div>
        )
      }
      const max = Math.max(...reasons.map(r => r.count))

      return (
        <section className="rounded-lg border p-4">
          <h3 className="text-sm font-semibold mb-3">Топ причин отказов по возвратам</h3>
          <ul className="space-y-2">
            {reasons.map((r, i) => (
              <li key={i} className="flex items-center gap-3">
                <span className="text-sm flex-1 truncate" title={r.reason}>{r.reason}</span>
                <div className="flex-[2] relative h-5 bg-muted rounded overflow-hidden">
                  <div
                    className="absolute inset-y-0 left-0 bg-primary/70"
                    style={{ width: `${(r.count / max) * 100}%` }}
                  />
                </div>
                <span className="text-xs font-medium w-10 text-right">{r.count}</span>
              </li>
            ))}
          </ul>
        </section>
      )
    }
    ```

    **Шаг C — создать `components/support/stats/ProductStatsTab.tsx`** (RSC):

    ```typescript
    // components/support/stats/ProductStatsTab.tsx
    // Phase 13 SUP-37 — вкладка «По товарам».

    import type { ProductStatRow } from "@/lib/support-stats"
    import { TopReturnReasonsList } from "./TopReturnReasonsList"

    function formatDuration(sec: number | null): string {
      if (sec === null) return "—"
      if (sec < 60) return `${sec} сек`
      if (sec < 3600) return `${Math.round(sec / 60)} мин`
      return `${(sec / 3600).toFixed(1)} ч`
    }

    export interface ProductStatsTabProps {
      products: ProductStatRow[]
      topReasons: Array<{ reason: string; count: number }>
    }

    export function ProductStatsTab({ products, topReasons }: ProductStatsTabProps) {
      const totalFeedbacks = products.reduce((s, p) => s + p.feedbacksTotal, 0)
      const totalReturns = products.reduce((s, p) => s + p.returnsTotal, 0)
      const ratings = products.map(p => p.avgRating).filter((r): r is number => r !== null)
      const avgRatingOverall = ratings.length > 0
        ? (ratings.reduce((s, r) => s + r, 0) / ratings.length).toFixed(1)
        : "—"

      const sorted = [...products].sort((a, b) => b.feedbacksTotal - a.feedbacksTotal)

      return (
        <div className="space-y-4">
          {/* Summary cards */}
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
            <div className="rounded-lg border p-4">
              <div className="text-2xl font-semibold">{totalFeedbacks}</div>
              <div className="text-xs text-muted-foreground">Всего отзывов</div>
            </div>
            <div className="rounded-lg border p-4">
              <div className="text-2xl font-semibold">{totalReturns}</div>
              <div className="text-xs text-muted-foreground">Всего возвратов</div>
            </div>
            <div className="rounded-lg border p-4">
              <div className="text-2xl font-semibold">{avgRatingOverall}</div>
              <div className="text-xs text-muted-foreground">Средний рейтинг</div>
            </div>
          </div>

          <TopReturnReasonsList reasons={topReasons} />

          {/* Products table */}
          {sorted.length === 0 ? (
            <div className="rounded-lg border p-8 text-center text-sm text-muted-foreground">
              Нет данных за выбранный период
            </div>
          ) : (
            <div className="rounded-lg border overflow-x-auto">
              <table className="w-full text-sm">
                <thead className="bg-muted text-xs">
                  <tr>
                    <th className="text-left px-3 py-2 w-12">Фото</th>
                    <th className="text-left px-3 py-2">Товар</th>
                    <th className="text-right px-3 py-2">Отзывов</th>
                    <th className="text-right px-3 py-2">Рейтинг</th>
                    <th className="text-right px-3 py-2">% отв.</th>
                    <th className="text-right px-3 py-2">Вопросов</th>
                    <th className="text-right px-3 py-2">Возвраты</th>
                    <th className="text-right px-3 py-2">Одобрено</th>
                    <th className="text-right px-3 py-2">Отклонено</th>
                    <th className="text-right px-3 py-2">Ср. время</th>
                  </tr>
                </thead>
                <tbody>
                  {sorted.map((p) => (
                    <tr key={p.nmId} className="border-t">
                      <td className="px-3 py-2">
                        {p.photoUrl ? (
                          /* eslint-disable-next-line @next/next/no-img-element */
                          <img src={p.photoUrl} alt={p.name ?? String(p.nmId)} className="h-10 w-10 object-cover rounded" />
                        ) : (
                          <div className="h-10 w-10 bg-muted rounded" />
                        )}
                      </td>
                      <td className="px-3 py-2">
                        <div className="font-medium text-xs truncate max-w-xs">{p.name ?? "Карточка не найдена"}</div>
                        <div className="text-xs text-muted-foreground">{p.nmId}</div>
                      </td>
                      <td className="text-right px-3 py-2">{p.feedbacksTotal}</td>
                      <td className="text-right px-3 py-2">{p.avgRating?.toFixed(1) ?? "—"}</td>
                      <td className="text-right px-3 py-2">{p.feedbacksAnsweredPct !== null ? `${p.feedbacksAnsweredPct}%` : "—"}</td>
                      <td className="text-right px-3 py-2">{p.questionsTotal}</td>
                      <td className="text-right px-3 py-2">{p.returnsTotal}</td>
                      <td className="text-right px-3 py-2">{p.returnsApproved}</td>
                      <td className="text-right px-3 py-2">{p.returnsRejected}</td>
                      <td className="text-right px-3 py-2">{formatDuration(p.avgResponseTimeSec)}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </div>
      )
    }
    ```

    **Шаг D — создать `components/support/stats/ManagerStatsTab.tsx`** (RSC):

    ```typescript
    // components/support/stats/ManagerStatsTab.tsx
    // Phase 13 SUP-38 — вкладка «По менеджерам». D-02: AutoRepliesSummary глобально.

    import type { ManagerStatRow } from "@/lib/support-stats"
    import { AutoRepliesSummary } from "./AutoRepliesSummary"

    function formatDuration(sec: number | null): string {
      if (sec === null) return "—"
      if (sec < 60) return `${sec} сек`
      if (sec < 3600) return `${Math.round(sec / 60)} мин`
      return `${(sec / 3600).toFixed(1)} ч`
    }

    function approvalPct(approved: number, decided: number): string {
      if (decided === 0) return "—"
      return `${Math.round((approved / decided) * 100)}%`
    }

    export interface ManagerStatsTabProps {
      managers: ManagerStatRow[]
      autoReplyCount: number
    }

    export function ManagerStatsTab({ managers, autoReplyCount }: ManagerStatsTabProps) {
      const totalProcessedAll = managers.reduce((s, m) => s + m.totalProcessed, 0)
      const responseTimes = managers.map(m => m.avgResponseTimeSec).filter((s): s is number => s !== null)
      const avgResponseGlobal = responseTimes.length > 0
        ? Math.round(responseTimes.reduce((s, v) => s + v, 0) / responseTimes.length)
        : null

      const sorted = [...managers].sort((a, b) => b.totalProcessed - a.totalProcessed)

      return (
        <div className="space-y-4">
          {/* Summary cards */}
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
            <div className="rounded-lg border p-4">
              <div className="text-2xl font-semibold">{totalProcessedAll}</div>
              <div className="text-xs text-muted-foreground">Всего обработано</div>
            </div>
            <div className="rounded-lg border p-4">
              <div className="text-2xl font-semibold">{formatDuration(avgResponseGlobal)}</div>
              <div className="text-xs text-muted-foreground">Ср. время ответа (общее)</div>
            </div>
            <AutoRepliesSummary count={autoReplyCount} />
          </div>

          {/* Managers table */}
          {sorted.length === 0 ? (
            <div className="rounded-lg border p-8 text-center text-sm text-muted-foreground">
              Нет менеджеров с доступом SUPPORT или нет активности за период
            </div>
          ) : (
            <div className="rounded-lg border overflow-x-auto">
              <table className="w-full text-sm">
                <thead className="bg-muted text-xs">
                  <tr>
                    <th className="text-left px-3 py-2">Менеджер</th>
                    <th className="text-center px-3 py-2">Live</th>
                    <th className="text-right px-3 py-2">Всего</th>
                    <th className="text-right px-3 py-2">Отзывы</th>
                    <th className="text-right px-3 py-2">Вопросы</th>
                    <th className="text-right px-3 py-2">Чаты</th>
                    <th className="text-right px-3 py-2">Возвраты</th>
                    <th className="text-right px-3 py-2">% одобр.</th>
                    <th className="text-right px-3 py-2">Обжалования</th>
                    <th className="text-right px-3 py-2">Ср. время</th>
                  </tr>
                </thead>
                <tbody>
                  {sorted.map((m) => (
                    <tr key={m.userId} className="border-t">
                      <td className="px-3 py-2 font-medium">{m.name ?? "—"}</td>
                      <td className="text-center px-3 py-2">
                        {m.isLive && (
                          <span className="inline-flex items-center rounded-full bg-green-100 text-green-800 px-2 py-0.5 text-xs">
                            сегодня
                          </span>
                        )}
                      </td>
                      <td className="text-right px-3 py-2 font-medium">{m.totalProcessed}</td>
                      <td className="text-right px-3 py-2">{m.feedbacksAnswered}</td>
                      <td className="text-right px-3 py-2">{m.questionsAnswered}</td>
                      <td className="text-right px-3 py-2">{m.chatsAnswered}</td>
                      <td className="text-right px-3 py-2">{m.returnsDecided}</td>
                      <td className="text-right px-3 py-2">{approvalPct(m.returnsApproved, m.returnsDecided)}</td>
                      <td className="text-right px-3 py-2">{m.appealsResolved}</td>
                      <td className="text-right px-3 py-2">{formatDuration(m.avgResponseTimeSec)}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </div>
      )
    }
    ```

    **Шаг E — verification:**
    ```bash
    npx tsc --noEmit
    npm run build
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/stats/ProductStatsTab.tsx` exit 0
    - `test -f components/support/stats/ManagerStatsTab.tsx` exit 0
    - `test -f components/support/stats/TopReturnReasonsList.tsx` exit 0
    - `test -f components/support/stats/AutoRepliesSummary.tsx` exit 0
    - `grep "export function ProductStatsTab" components/support/stats/ProductStatsTab.tsx` находит
    - `grep "export function ManagerStatsTab" components/support/stats/ManagerStatsTab.tsx` находит
    - `grep "export function TopReturnReasonsList" components/support/stats/TopReturnReasonsList.tsx` находит
    - `grep "export function AutoRepliesSummary" components/support/stats/AutoRepliesSummary.tsx` находит
    - `! grep -q '"use client"' components/support/stats/ProductStatsTab.tsx` — RSC без use client
    - `! grep -q '"use client"' components/support/stats/ManagerStatsTab.tsx` — RSC без use client
    - `grep "TopReturnReasonsList" components/support/stats/ProductStatsTab.tsx` находит (композиция)
    - `grep "AutoRepliesSummary" components/support/stats/ManagerStatsTab.tsx` находит (композиция)
    - `grep "photoUrl" components/support/stats/ProductStatsTab.tsx` находит
    - `grep "isLive" components/support/stats/ManagerStatsTab.tsx` находит (badge «сегодня»)
    - `grep "approvalPct\|returnsApproved / returnsDecided\|returnsApproved / m.returnsDecided" components/support/stats/ManagerStatsTab.tsx` находит
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    4 RSC компонента контента (ProductStatsTab, ManagerStatsTab, TopReturnReasonsList, AutoRepliesSummary) реализуют SUP-37/38 UI; D-02 (AutoRepliesSummary глобальный счётчик) и D-03 (TopReturnReasonsList глобально) соблюдаются; build проходит.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: tests/support-stats-page.test.ts — 5+ GREEN тестов (заменить Plan 13-01 stub)</name>
  <files>tests/support-stats-page.test.ts</files>
  <read_first>
    - tests/customer-profile-page.test.ts (Phase 12-02 — паттерн unit тестов RSC-adjacent helpers)
    - tests/template-picker.test.ts (Phase 11 — паттерн экспорта pure helper для тестирования)
    - app/(dashboard)/support/stats/page.tsx (Задача 1 — parseStatsSearchParams экспорт)
    - lib/rbac.ts (requireSection mocking pattern)
  </read_first>
  <behavior>
    Тесты покрывают pure helper `parseStatsSearchParams` экспортированный из page.tsx (избегаем полного RSC-рендера с React вызовами — vitest-плохо работает с app router render).

    5+ GREEN тестов:
    1. parseStatsSearchParams happy path — все параметры заданы корректно
    2. parseStatsSearchParams fallback — tab default = "products", period default = "30d"
    3. parseStatsSearchParams невалидный tab → fallback
    4. parseStatsSearchParams невалидный period → fallback
    5. parseStatsSearchParams nmId coerce из строки в int
    6. parseStatsSearchParams array values → берётся первый (Next.js searchParams)

    Smoke test импорта Plan 13-01 helpers через page.tsx (проверяет что импорты корректны без TS ошибок).
  </behavior>
  <action>
    **Заменить содержимое `tests/support-stats-page.test.ts`:**

    ```typescript
    import { describe, it, expect } from "vitest"
    import { parseStatsSearchParams } from "@/app/(dashboard)/support/stats/page"

    describe("parseStatsSearchParams", () => {
      it("happy path — все параметры корректны", () => {
        const r = parseStatsSearchParams({
          tab: "managers",
          period: "custom",
          dateFrom: "2026-04-01",
          dateTo: "2026-04-30",
          nmId: "12345",
          userId: "user-1",
        })
        expect(r.tab).toBe("managers")
        expect(r.period).toBe("custom")
        expect(r.dateFrom).toBe("2026-04-01")
        expect(r.dateTo).toBe("2026-04-30")
        expect(r.nmId).toBe(12345)
        expect(r.userId).toBe("user-1")
      })

      it("fallback: tab default = 'products', period default = '30d'", () => {
        const r = parseStatsSearchParams({})
        expect(r.tab).toBe("products")
        expect(r.period).toBe("30d")
      })

      it("невалидный tab → fallback на products", () => {
        const r = parseStatsSearchParams({ tab: "invalid-tab" })
        expect(r.tab).toBe("products")
      })

      it("невалидный period → fallback на 30d", () => {
        const r = parseStatsSearchParams({ period: "10y" })
        expect(r.period).toBe("30d")
      })

      it("nmId coerce из строки в int", () => {
        const r = parseStatsSearchParams({ nmId: "999" })
        expect(r.nmId).toBe(999)
      })

      it("array values берёт первый (Next.js searchParams behaviour)", () => {
        const r = parseStatsSearchParams({ tab: ["managers", "products"] })
        expect(r.tab).toBe("managers")
      })

      it("отрицательный nmId → undefined (Zod int positive)", () => {
        const r = parseStatsSearchParams({ nmId: "-5" })
        expect(r.nmId).toBeUndefined()
      })

      it("пропущенные dateFrom/dateTo при period=custom → сохраняет период=custom без дат", () => {
        const r = parseStatsSearchParams({ period: "custom" })
        expect(r.period).toBe("custom")
        expect(r.dateFrom).toBeUndefined()
        expect(r.dateTo).toBeUndefined()
      })
    })

    describe("Plan 13-01 integration smoke", () => {
      it("lib/support-stats exports expected API", async () => {
        const mod = await import("@/lib/support-stats")
        expect(typeof mod.listProductsWithStats).toBe("function")
        expect(typeof mod.listManagersWithStats).toBe("function")
        expect(typeof mod.getTopReturnReasons).toBe("function")
        expect(typeof mod.getAutoReplyCount).toBe("function")
      })

      it("lib/date-periods exports getPeriod + PERIOD_PRESETS", async () => {
        const mod = await import("@/lib/date-periods")
        expect(typeof mod.getPeriod).toBe("function")
        expect(mod.PERIOD_PRESETS).toEqual(["7d", "30d", "quarter", "custom"])
      })
    })
    ```

    **Запустить тесты:**
    ```bash
    npm run test -- tests/support-stats-page.test.ts
    npm run test
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-stats-page.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/support-stats-page.test.ts` >= 5 (real it, не it.skip)
    - `grep -c "it.skip(" tests/support-stats-page.test.ts` == 0 (все stub заменены)
    - `grep "parseStatsSearchParams" tests/support-stats-page.test.ts` находит
    - `grep "listProductsWithStats\|listManagersWithStats" tests/support-stats-page.test.ts | wc -l` >= 2
    - `npm run test -- tests/support-stats-page.test.ts` exit 0 (все GREEN)
    - `npm run test` exit 0 (полная suite Phase 7-13 GREEN)
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    tests/support-stats-page.test.ts — 5+ GREEN тестов покрывают parseStatsSearchParams (все кейсы Zod валидации + fallback) + smoke тест для Plan 13-01 helpers; полная test suite и build проходят.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач:
1. `npx tsc --noEmit` exit 0
2. `npm run build` exit 0
3. `npm run test` exit 0 (Phase 7-13 GREEN)
4. `test -f app/(dashboard)/support/stats/page.tsx && ls components/support/stats/*.tsx | wc -l` >= 6 (page + 6 компонентов, если считать StatsTabs/PeriodFilter/ProductStatsTab/ManagerStatsTab/TopReturnReasonsList/AutoRepliesSummary)
5. `grep "/support/stats" components/layout/nav-items.ts && grep "/support/stats" components/layout/section-titles.ts` обе находят
6. `grep -c "it(" tests/support-stats-page.test.ts` >= 5
</verification>

<success_criteria>
Plan 13-02 завершён когда:
- /support/stats RSC рендерится c 2 табами (products/managers) и фильтром периода
- D-10 RBAC: VIEW (не MANAGE)
- D-05 квартал = календарный (hint в UI)
- D-07 только числа и таблицы (без recharts)
- D-02 AutoRepliesSummary — глобальный счётчик на табе «По менеджерам»
- D-03 TopReturnReasonsList — глобально на табе «По товарам»
- Sidebar содержит пункт «Статистика» с иконкой BarChart3
- Header title /support/stats = «Статистика службы поддержки»
- 5+ GREEN тестов для parseStatsSearchParams + 2 smoke для Plan 13-01 imports
- Полная test suite + build GREEN
- Commit `feat(13-02): /support/stats RSC page + 7 компонентов + nav + 5+ GREEN tests`
</success_criteria>

<output>
После завершения создать `.planning/phases/13-statistics/13-02-SUMMARY.md` с:
- /support/stats RSC страница — searchParams parse контракт + Zod schema + data assembly
- 7 новых компонентов (2 client — StatsTabs/PeriodFilter, 5 RSC — ProductStatsTab/ManagerStatsTab/TopReturnReasonsList/AutoRepliesSummary)
- Exported `parseStatsSearchParams` для test coverage
- nav-items.ts + section-titles.ts intergration — BarChart3 иконка, regex перед /^\/support/
- User decisions UI: D-02 AutoRepliesSummary global, D-03 TopReturnReasonsList global, D-05 «Квартал (календарный)» в label, D-07 без графиков, D-10 SUPPORT VIEW
- Test coverage: 5+ GREEN parseStatsSearchParams + 2 smoke Plan 13-01 import
- Known UX limitations: таблицы без сортировки по колонкам (MVP — DESC по primary metric), без pagination (MVP — ограниченный список 50-200 SKU и ≤10 менеджеров)
- Next: Plan 13-03 cron /api/cron/support-stats-refresh + systemd timer 03:00 МСК + human UAT
</output>
</context>
