---
phase: 13-statistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260418_phase13_statistics/migration.sql
  - lib/date-periods.ts
  - lib/support-stats.ts
  - tests/date-periods.test.ts
  - tests/support-stats-helpers.test.ts
  - tests/support-stats-page.test.ts
  - tests/support-stats-cron.test.ts
autonomous: true
requirements:
  - SUP-39
must_haves:
  truths:
    - "prisma/schema.prisma содержит model ManagerSupportStats (id cuid, userId FK User onDelete:Cascade, period DateTime, totalProcessed/feedbacksAnswered/questionsAnswered/chatsAnswered/returnsDecided/returnsApproved/appealsResolved Int @default(0), avgResponseTimeSec Int?, updatedAt @updatedAt, @@unique([userId, period]), @@index([period]))"
    - "User model расширен relation managerStats ManagerSupportStats[] @relation(\"ManagerStats\")"
    - "SupportTicket model получает @@index([channel, nmId, createdAt]) для per-product агрегаций (SUP-37)"
    - "SupportMessage model получает @@index([direction, isAutoReply, wbSentAt]) для фильтра автоответов + avg response time"
    - "prisma/migrations/20260418_phase13_statistics/migration.sql создаёт таблицу ManagerSupportStats + 2 индекса (ручная миграция — PG локально нет, применяется через deploy.sh)"
    - "lib/date-periods.ts экспортирует pure getPeriod(preset, custom?) → {dateFrom, dateTo} с Moscow +03:00 hardcoded, startOfMonthMsk(date?), startOfQuarterMsk(date?), startOfDayMsk(date), endOfDayMsk(date). Квартал КАЛЕНДАРНЫЙ (D-05): Q1=Jan-Mar, Q2=Apr-Jun, Q3=Jul-Sep, Q4=Oct-Dec"
    - "lib/support-stats.ts экспортирует 6 aggregation helpers: computeProductStats, computeManagerStatsForPeriod, listProductsWithStats, listManagersWithStats, getTopReturnReasons, getAutoReplyCount"
    - "computeManagerStatsForPeriod(userId, dateFrom, dateTo) возвращает полный набор полей ManagerSupportStats БЕЗ id/period/updatedAt (D-04 totalProcessed = only outcome-actions: OUTBOUND !isAutoReply with authorId=userId + ReturnDecision.decidedById=userId + AppealRecord.resolvedById=userId — НЕ включает status changes)"
    - "getAutoReplyCount(dateFrom, dateTo) — глобальный COUNT SupportMessage where isAutoReply=true (D-02 glabal, authorId=null)"
    - "getTopReturnReasons(dateFrom, dateTo, limit=10) — raw SQL $queryRaw группировка по ReturnDecision.reason where action='REJECT' (D-03 глобально)"
    - "avg response time формула: MIN(OUTBOUND.wbSentAt) - MIN(INBOUND.wbSentAt) per ticket WHERE OUTBOUND.isAutoReply=false, AVG по tickets (D-01 исключает RETURN так как нет OUTBOUND SupportMessage при Phase 9 решении)"
    - "tests/date-periods.test.ts — 10+ GREEN тестов: 7d/30d/quarter/custom пресеты, startOfMonthMsk, startOfQuarterMsk для всех 4 кварталов, границы дней, +03:00 offset"
    - "tests/support-stats-helpers.test.ts — 15+ GREEN тестов: computeProductStats happy path, computeManagerStatsForPeriod outcome-actions only, getTopReturnReasons raw SQL mock, getAutoReplyCount global, listProductsWithStats JOIN WbCard, empty periods"
    - "tests/support-stats-page.test.ts — stub it.skip (заполняется в Plan 13-02) + 1 smoke test для импорта pure helpers"
    - "tests/support-stats-cron.test.ts — stub it.skip (заполняется в Plan 13-03) + 1 smoke test"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ManagerSupportStats model + 2 индекса на существующих моделях + User relation"
      contains: "ManagerSupportStats"
    - path: "prisma/migrations/20260418_phase13_statistics/migration.sql"
      provides: "SQL миграция для VPS deploy (CREATE TABLE + CREATE INDEX)"
      contains: "ManagerSupportStats"
    - path: "lib/date-periods.ts"
      provides: "Pure TZ-safe period helpers (Moscow +03:00)"
      exports:
        - "getPeriod"
        - "startOfMonthMsk"
        - "startOfQuarterMsk"
        - "startOfDayMsk"
        - "endOfDayMsk"
        - "PERIOD_PRESETS"
    - path: "lib/support-stats.ts"
      provides: "6 aggregation helpers для /support/stats и cron"
      exports:
        - "computeProductStats"
        - "computeManagerStatsForPeriod"
        - "listProductsWithStats"
        - "listManagersWithStats"
        - "getTopReturnReasons"
        - "getAutoReplyCount"
    - path: "tests/date-periods.test.ts"
      provides: "10+ GREEN unit-тестов TZ-safe periods"
      contains: "describe"
    - path: "tests/support-stats-helpers.test.ts"
      provides: "15+ GREEN unit-тестов aggregation helpers"
      contains: "describe"
    - path: "tests/support-stats-page.test.ts"
      provides: "Stub + smoke test (заполняется в Plan 13-02)"
      contains: "describe"
    - path: "tests/support-stats-cron.test.ts"
      provides: "Stub + smoke test (заполняется в Plan 13-03)"
      contains: "describe"
  key_links:
    - from: "prisma/schema.prisma ManagerSupportStats"
      to: "User"
      via: "relation ManagerStats onDelete Cascade"
      pattern: "@relation\\(\"ManagerStats\""
    - from: "lib/support-stats.ts"
      to: "lib/prisma.ts"
      via: "singleton import для Prisma queries"
      pattern: "@/lib/prisma"
    - from: "lib/support-stats.ts computeManagerStatsForPeriod"
      to: "prisma.supportMessage.count + prisma.returnDecision.count + prisma.appealRecord.count"
      via: "Prisma count + groupBy для outcome-actions"
      pattern: "\\.count\\("
    - from: "lib/support-stats.ts getTopReturnReasons"
      to: "prisma.$queryRaw"
      via: "raw SQL GROUP BY reason ORDER BY COUNT DESC LIMIT"
      pattern: "\\$queryRaw"
    - from: "tests/support-stats-helpers.test.ts"
      to: "lib/support-stats.ts"
      via: "vi.mock @/lib/prisma + dynamic import"
      pattern: "vi\\.mock.*prisma"
---

<objective>
Phase 13 Plan 01 — Foundation статистики поддержки:

1. **Prisma миграция `20260418_phase13_statistics`** — новая модель `ManagerSupportStats` (денормализованная статистика менеджеров, SUP-39), обратная relation `managerStats` на `User`, 2 performance индекса на существующих моделях (`SupportTicket(channel, nmId, createdAt)` + `SupportMessage(direction, isAutoReply, wbSentAt)`).

2. **lib/date-periods.ts** — pure TZ-safe хелперы для Moscow timezone (D-05 календарный квартал):
   - `getPeriod(preset, custom?): { dateFrom, dateTo }` с пресетами `7d` / `30d` / `quarter` / `custom`
   - `startOfMonthMsk(date)` — 1-е число 00:00 МСК
   - `startOfQuarterMsk(date)` — 1 Jan/Apr/Jul/Oct 00:00 МСК (календарный)
   - `startOfDayMsk(date)` / `endOfDayMsk(date)` — границы дня в МСК
   - Moscow +03:00 hardcoded (с 2014 DST отменён)

3. **lib/support-stats.ts** — 6 pure-ish aggregation helpers для страницы `/support/stats` и cron:
   - `computeProductStats(nmId, dateFrom, dateTo)` — метрики SUP-37 для одного товара
   - `listProductsWithStats(dateFrom, dateTo, filters)` — список товаров с метриками (batch)
   - `computeManagerStatsForPeriod(userId, dateFrom, dateTo)` — метрики SUP-38 для одного менеджера (используется и в cron upsert, и в live current month). D-04 — outcome-actions only.
   - `listManagersWithStats(period)` — список менеджеров с метриками (для таблицы «По менеджерам»)
   - `getTopReturnReasons(dateFrom, dateTo, limit=10)` — топ причин отказов (D-03 глобально)
   - `getAutoReplyCount(dateFrom, dateTo)` — глобальный счётчик автоответов (D-02)

4. **Wave 0 test stubs (встроены)** — 4 тестовых файла:
   - `tests/date-periods.test.ts` — 10+ GREEN тестов
   - `tests/support-stats-helpers.test.ts` — 15+ GREEN тестов
   - `tests/support-stats-page.test.ts` — it.skip + smoke (заполняется в Plan 13-02)
   - `tests/support-stats-cron.test.ts` — it.skip + smoke (заполняется в Plan 13-03)

Purpose: Foundation для Phase 13 — БД и aggregation слой готовы к UI (Plan 13-02) и cron (Plan 13-03). Вся математика статистики живёт в pure TS-функциях, покрытых unit-тестами с mocked Prisma, без зависимости от Next.js/React.

Output: 1 миграция, 1 расширенная Prisma schema, 2 новых lib/* файла, 4 test файла (2 GREEN + 2 stub), без UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-statistics/13-RESEARCH.md
@.planning/phases/13-statistics/13-VALIDATION.md
@.planning/phases/12-customer-messenger/12-01-PLAN.md
@CLAUDE.md
@prisma/schema.prisma
@lib/prisma.ts
@tests/customer-profile-page.test.ts
@tests/support-sync-returns.test.ts

<interfaces>
**Существующие модели, используемые агрегациями (prisma/schema.prisma):**
```prisma
model User {
  id               String   @id @default(cuid())
  sectionRoles     UserSectionRole[]
  authoredMessages SupportMessage[] @relation("SupportAuthor")
  returnDecisions  ReturnDecision[] @relation("ReturnDecider")
  appealsResolved  AppealRecord[]   @relation("AppealResolver")
  // Phase 13 добавит:
  // managerStats ManagerSupportStats[] @relation("ManagerStats")
}

enum TicketChannel { FEEDBACK QUESTION CHAT RETURN MESSENGER }
enum TicketStatus { NEW IN_PROGRESS ANSWERED CLOSED APPEALED }
enum Direction { INBOUND OUTBOUND }
enum ReturnState { PENDING APPROVED REJECTED }
enum ReturnDecisionAction { APPROVE REJECT RECONSIDER }
enum AppealStatus { NONE PENDING APPROVED REJECTED }

model SupportTicket {
  id, channel, nmId, status, rating, returnState, createdAt, resolvedAt
  @@index([channel]), @@index([nmId]), @@index([createdAt]), @@index([returnState])
  // Phase 13 добавит: @@index([channel, nmId, createdAt])
}

model SupportMessage {
  id, ticketId, direction, authorId, isAutoReply, wbSentAt
  @@index([ticketId, sentAt])
  // Phase 13 добавит: @@index([direction, isAutoReply, wbSentAt])
}

model ReturnDecision {
  action ReturnDecisionAction  // APPROVE | REJECT | RECONSIDER
  reason String?
  decidedById String
  decidedAt DateTime
}

model AppealRecord {
  status AppealStatus          // PENDING | APPROVED | REJECTED
  resolvedById String?
  appealResolvedAt DateTime?
}
```

**Паттерн миграции без локальной PG (из Phase 9-01 и Phase 12-01):**
- Создать `prisma/migrations/<timestamp>/migration.sql` вручную
- Обновить `schema.prisma` параллельно
- НЕ запускать `prisma migrate dev` — применится через `bash deploy.sh` на VPS в Plan 13-03
- `npx prisma validate` — sanity check schema
- `npx prisma generate` — чтобы новая модель была в Prisma Client для тестов

**Паттерн тестов (из tests/customer-profile-page.test.ts Phase 12-02 и tests/support-sync-returns.test.ts Phase 9):**
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest"

const prismaMock = {
  supportTicket: { groupBy: vi.fn(), count: vi.fn(), findMany: vi.fn() },
  supportMessage: { count: vi.fn(), aggregate: vi.fn(), groupBy: vi.fn() },
  returnDecision: { count: vi.fn(), groupBy: vi.fn() },
  appealRecord: { count: vi.fn() },
  user: { findMany: vi.fn() },
  wbCard: { findMany: vi.fn() },
  $queryRaw: vi.fn(),
}
vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

beforeEach(() => { vi.resetAllMocks() })

// tests import dynamically to pick up mocks
```

**Moscow TZ pattern (из lib/auto-reply.ts Phase 10, startOfMonthMsk паттерн 13-RESEARCH.md):**
```typescript
// Russia abandoned DST in 2011; Moscow = +03:00 постоянно с 2014
const MSK_OFFSET = "+03:00"

export function startOfMonthMsk(date: Date = new Date()): Date {
  const parts = new Intl.DateTimeFormat("en-GB", {
    timeZone: "Europe/Moscow",
    year: "numeric",
    month: "2-digit",
  }).formatToParts(date)
  const y = parts.find(p => p.type === "year")!.value
  const m = parts.find(p => p.type === "month")!.value
  return new Date(`${y}-${m}-01T00:00:00${MSK_OFFSET}`)
}

export function startOfQuarterMsk(date: Date = new Date()): Date {
  const parts = new Intl.DateTimeFormat("en-GB", {
    timeZone: "Europe/Moscow",
    year: "numeric",
    month: "2-digit",
  }).formatToParts(date)
  const y = parseInt(parts.find(p => p.type === "year")!.value)
  const m = parseInt(parts.find(p => p.type === "month")!.value)
  // Q1=Jan(1-3), Q2=Apr(4-6), Q3=Jul(7-9), Q4=Oct(10-12)
  const qStartMonth = Math.floor((m - 1) / 3) * 3 + 1
  const mm = String(qStartMonth).padStart(2, "0")
  return new Date(`${y}-${mm}-01T00:00:00${MSK_OFFSET}`)
}
```

**Паттерн $queryRaw для top-N (из 13-RESEARCH.md Pattern 2):**
```typescript
const rows = await prisma.$queryRaw<Array<{ reason: string; cnt: bigint }>>`
  SELECT rd.reason, COUNT(*)::int AS cnt
  FROM "ReturnDecision" rd
  WHERE rd."action" = 'REJECT'
    AND rd.reason IS NOT NULL
    AND rd."decidedAt" >= ${dateFrom} AND rd."decidedAt" <= ${dateTo}
  GROUP BY rd.reason
  ORDER BY cnt DESC
  LIMIT ${limit}
`
return rows.map(r => ({ reason: r.reason, count: Number(r.cnt) }))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Prisma миграция ManagerSupportStats + 2 индекса + обратная relation на User</name>
  <files>prisma/schema.prisma, prisma/migrations/20260418_phase13_statistics/migration.sql</files>
  <read_first>
    - prisma/schema.prisma (строки 51-76 — модель User, строки 489-670 — SupportTicket/SupportMessage/ReturnDecision/AppealRecord + все enums)
    - prisma/migrations/20260418_phase12_customer_messenger/migration.sql (паттерн ручной SQL-миграции без локальной PG)
    - prisma/migrations/20260417_phase9_returns/migration.sql (паттерн CREATE TABLE + @@index + FK onDelete Cascade)
    - .planning/phases/13-statistics/13-RESEARCH.md (секция Data Model, строки 422-475 — полная спецификация ManagerSupportStats)
  </read_first>
  <behavior>
    - Schema добавляет `model ManagerSupportStats` со всеми полями из research §Data Model (без `autoRepliesCount` — D-02 делает его глобальным, не per-manager; не дублируем)
    - Поля: id, userId, period DateTime, totalProcessed, feedbacksAnswered, questionsAnswered, chatsAnswered, returnsDecided, returnsApproved, returnsRejected, appealsResolved, avgResponseTimeSec Int?, updatedAt
    - `@@unique([userId, period])` + `@@index([period])`
    - Relation `user User @relation("ManagerStats", fields: [userId], references: [id], onDelete: Cascade)` + обратная relation на User
    - SupportTicket получает `@@index([channel, nmId, createdAt])` (add-only, НЕ breaking)
    - SupportMessage получает `@@index([direction, isAutoReply, wbSentAt])` (add-only, НЕ breaking)
    - `npx prisma validate` должна проходить
    - `npx prisma generate` должна создавать новый тип в Prisma Client
  </behavior>
  <action>
    **Шаг A — обновить `prisma/schema.prisma`:**

    1. В `model User` (после строки 72 `autoReplyUpdates ...`) добавить:
    ```prisma
      managerStats     ManagerSupportStats[] @relation("ManagerStats") // Phase 13 — денормализованная статистика менеджеров
    ```

    2. В `model SupportTicket` (перед закрывающей `}`) добавить строку индекса:
    ```prisma
      @@index([channel, nmId, createdAt]) // Phase 13 — per-product агрегации (SUP-37)
    ```

    3. В `model SupportMessage` (перед закрывающей `}`) добавить:
    ```prisma
      @@index([direction, isAutoReply, wbSentAt]) // Phase 13 — фильтр автоответов + avg response time
    ```

    4. В конец файла (после всех существующих моделей, перед `AutoReplyConfig`, или после него — не важно, Prisma не чувствительна к порядку) добавить:
    ```prisma
    // ──────────────────────────────────────────────────────────────────
    // Phase 13 — Статистика службы поддержки
    // ──────────────────────────────────────────────────────────────────
    // Денормализованная статистика менеджеров (SUP-39). Один row на
    // (userId, period), где period = 1-е число месяца 00:00 Europe/Moscow.
    // Обновляется cron'ом раз в сутки 03:00 МСК (текущий месяц) через upsert.
    // Past months — immutable после закрытия месяца (cron пишет только current month).
    // Глобальный счётчик автоответов хранится отдельно (не per-manager, D-02).
    model ManagerSupportStats {
      id                 String   @id @default(cuid())
      userId             String
      user               User     @relation("ManagerStats", fields: [userId], references: [id], onDelete: Cascade)
      period             DateTime // 1st of month 00:00 Europe/Moscow (+03:00)
      totalProcessed     Int      @default(0) // D-04 outcome-actions only: OUTBOUND !isAutoReply by user + ReturnDecision by user + AppealRecord resolved by user
      feedbacksAnswered  Int      @default(0) // COUNT OUTBOUND SupportMessage где ticket.channel=FEEDBACK AND authorId=userId AND isAutoReply=false
      questionsAnswered  Int      @default(0) // same для QUESTION
      chatsAnswered      Int      @default(0) // same для CHAT
      returnsDecided     Int      @default(0) // COUNT ReturnDecision где decidedById=userId AND action IN (APPROVE, REJECT, RECONSIDER)
      returnsApproved    Int      @default(0) // COUNT ReturnDecision где decidedById=userId AND action IN (APPROVE, RECONSIDER) — для расчёта % одобрения
      returnsRejected    Int      @default(0) // COUNT ReturnDecision где decidedById=userId AND action=REJECT
      appealsResolved    Int      @default(0) // COUNT AppealRecord где resolvedById=userId
      avgResponseTimeSec Int?     // nullable — null если за период у менеджера не было OUTBOUND ответов
      updatedAt          DateTime @updatedAt

      @@unique([userId, period])
      @@index([period])
    }
    ```

    **Шаг B — создать `prisma/migrations/20260418_phase13_statistics/migration.sql`:**

    ```sql
    -- Phase 13: Статистика службы поддержки
    -- 1. ManagerSupportStats — денормализованная статистика менеджеров (SUP-39)
    -- 2. Performance индексы для per-product и avg response time агрегаций

    CREATE TABLE "ManagerSupportStats" (
      "id"                 TEXT         NOT NULL PRIMARY KEY,
      "userId"             TEXT         NOT NULL,
      "period"             TIMESTAMP(3) NOT NULL,
      "totalProcessed"     INTEGER      NOT NULL DEFAULT 0,
      "feedbacksAnswered"  INTEGER      NOT NULL DEFAULT 0,
      "questionsAnswered"  INTEGER      NOT NULL DEFAULT 0,
      "chatsAnswered"      INTEGER      NOT NULL DEFAULT 0,
      "returnsDecided"     INTEGER      NOT NULL DEFAULT 0,
      "returnsApproved"    INTEGER      NOT NULL DEFAULT 0,
      "returnsRejected"    INTEGER      NOT NULL DEFAULT 0,
      "appealsResolved"    INTEGER      NOT NULL DEFAULT 0,
      "avgResponseTimeSec" INTEGER,
      "updatedAt"          TIMESTAMP(3) NOT NULL
    );

    CREATE UNIQUE INDEX "ManagerSupportStats_userId_period_key" ON "ManagerSupportStats"("userId", "period");
    CREATE INDEX "ManagerSupportStats_period_idx" ON "ManagerSupportStats"("period");

    ALTER TABLE "ManagerSupportStats"
      ADD CONSTRAINT "ManagerSupportStats_userId_fkey"
      FOREIGN KEY ("userId") REFERENCES "User"("id")
      ON DELETE CASCADE ON UPDATE CASCADE;

    -- Phase 13 — performance индексы для агрегаций (add-only, не breaking)
    CREATE INDEX "SupportTicket_channel_nmId_createdAt_idx"
      ON "SupportTicket"("channel", "nmId", "createdAt");
    CREATE INDEX "SupportMessage_direction_isAutoReply_wbSentAt_idx"
      ON "SupportMessage"("direction", "isAutoReply", "wbSentAt");
    ```

    **Шаг C — sanity check:**
    ```bash
    npx prisma validate
    npx prisma generate
    ```

    Ожидаем: validate exit 0, generate создаёт `ManagerSupportStats` type в `@prisma/client` (доступно для импорта в тестах).
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model ManagerSupportStats" prisma/schema.prisma` == 1
    - `grep -c "managerStats\s*ManagerSupportStats\[\]" prisma/schema.prisma` == 1 (обратная relation на User)
    - `grep -c "@@index(\[channel, nmId, createdAt\])" prisma/schema.prisma` == 1
    - `grep -c "@@index(\[direction, isAutoReply, wbSentAt\])" prisma/schema.prisma` == 1
    - `grep -c "@@unique(\[userId, period\])" prisma/schema.prisma` >= 1 (может быть и на UserSectionRole — проверяем >= 1)
    - `test -f prisma/migrations/20260418_phase13_statistics/migration.sql` exit 0
    - `grep -c "CREATE TABLE \"ManagerSupportStats\"" prisma/migrations/20260418_phase13_statistics/migration.sql` == 1
    - `grep -c "CREATE UNIQUE INDEX \"ManagerSupportStats_userId_period_key\"" prisma/migrations/20260418_phase13_statistics/migration.sql` == 1
    - `grep -c "SupportTicket_channel_nmId_createdAt_idx" prisma/migrations/20260418_phase13_statistics/migration.sql` == 1
    - `grep -c "SupportMessage_direction_isAutoReply_wbSentAt_idx" prisma/migrations/20260418_phase13_statistics/migration.sql` == 1
    - `npx prisma validate` exit 0
    - `npx prisma generate` exit 0
  </acceptance_criteria>
  <done>
    Prisma schema содержит ManagerSupportStats + обратную relation на User + 2 индекса на существующих моделях; migration.sql готов для применения в Plan 13-03 через deploy.sh; prisma validate/generate проходят; Prisma Client содержит тип ManagerSupportStats для импорта в test/lib модулях.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/date-periods.ts + tests/date-periods.test.ts (10+ GREEN)</name>
  <files>lib/date-periods.ts, tests/date-periods.test.ts</files>
  <read_first>
    - lib/auto-reply.ts (Phase 10 паттерн isWithinWorkingHours через Intl.DateTimeFormat с timeZone Europe/Moscow)
    - .planning/phases/13-statistics/13-RESEARCH.md (строки 339-385 Pattern 4 TZ-safe start-of-month)
    - tests/auto-reply-settings.test.ts (паттерн vitest для TZ-aware функций)
    - CLAUDE.md (Moscow timezone для всех дат)
  </read_first>
  <behavior>
    - `getPeriod(preset, custom?)` для пресетов возвращает:
      - `"7d"`: dateFrom = now - 7 дней, dateTo = now
      - `"30d"`: dateFrom = now - 30 дней, dateTo = now
      - `"quarter"`: dateFrom = startOfQuarterMsk(now), dateTo = now (D-05 календарный)
      - `"custom"`: dateFrom = startOfDayMsk(custom.from), dateTo = endOfDayMsk(custom.to)
    - `startOfMonthMsk(date)` — 1-е число того месяца, в котором находится date по МСК, 00:00 МСК
    - `startOfQuarterMsk(date)` — 1 Jan/Apr/Jul/Oct того квартала, в котором date по МСК (Q1=1-3, Q2=4-6, Q3=7-9, Q4=10-12)
    - `startOfDayMsk(date)` — 00:00:00 МСК того дня
    - `endOfDayMsk(date)` — 23:59:59.999 МСК того дня
    - `PERIOD_PRESETS = ["7d", "30d", "quarter", "custom"] as const`
    - Все функции pure (без side effects, без time.now mocking в prod code — тесты мокают `new Date()` через `vi.useFakeTimers()`)
    - `custom` preset без `custom` аргумента → throw `Error("custom preset requires custom arg")`
  </behavior>
  <action>
    **Шаг A — создать `lib/date-periods.ts`:**

    ```typescript
    // lib/date-periods.ts
    // Phase 13 — TZ-safe period хелперы для Europe/Moscow (+03:00 с 2014).
    // Используется на /support/stats и в cron upsert ManagerSupportStats.

    const MSK_OFFSET = "+03:00"

    export const PERIOD_PRESETS = ["7d", "30d", "quarter", "custom"] as const
    export type PeriodPreset = (typeof PERIOD_PRESETS)[number]

    export interface Period {
      dateFrom: Date
      dateTo: Date
    }

    export interface CustomPeriodInput {
      from: string // YYYY-MM-DD
      to: string   // YYYY-MM-DD
    }

    function extractMskYmd(date: Date): { year: number; month: number; day: number } {
      const parts = new Intl.DateTimeFormat("en-GB", {
        timeZone: "Europe/Moscow",
        year: "numeric",
        month: "2-digit",
        day: "2-digit",
      }).formatToParts(date)
      const find = (t: string) => parts.find((p) => p.type === t)!.value
      return {
        year: parseInt(find("year")),
        month: parseInt(find("month")),
        day: parseInt(find("day")),
      }
    }

    export function startOfMonthMsk(date: Date = new Date()): Date {
      const { year, month } = extractMskYmd(date)
      const mm = String(month).padStart(2, "0")
      return new Date(`${year}-${mm}-01T00:00:00${MSK_OFFSET}`)
    }

    export function startOfQuarterMsk(date: Date = new Date()): Date {
      const { year, month } = extractMskYmd(date)
      // Q1=Jan(1-3), Q2=Apr(4-6), Q3=Jul(7-9), Q4=Oct(10-12)
      const qStartMonth = Math.floor((month - 1) / 3) * 3 + 1
      const mm = String(qStartMonth).padStart(2, "0")
      return new Date(`${year}-${mm}-01T00:00:00${MSK_OFFSET}`)
    }

    export function startOfDayMsk(date: Date): Date {
      const { year, month, day } = extractMskYmd(date)
      const mm = String(month).padStart(2, "0")
      const dd = String(day).padStart(2, "0")
      return new Date(`${year}-${mm}-${dd}T00:00:00${MSK_OFFSET}`)
    }

    export function endOfDayMsk(date: Date): Date {
      const { year, month, day } = extractMskYmd(date)
      const mm = String(month).padStart(2, "0")
      const dd = String(day).padStart(2, "0")
      return new Date(`${year}-${mm}-${dd}T23:59:59.999${MSK_OFFSET}`)
    }

    export function getPeriod(
      preset: PeriodPreset,
      custom?: CustomPeriodInput
    ): Period {
      const now = new Date()
      switch (preset) {
        case "7d":
          return { dateFrom: new Date(now.getTime() - 7 * 86_400_000), dateTo: now }
        case "30d":
          return { dateFrom: new Date(now.getTime() - 30 * 86_400_000), dateTo: now }
        case "quarter":
          return { dateFrom: startOfQuarterMsk(now), dateTo: now }
        case "custom": {
          if (!custom) throw new Error("custom preset requires custom arg")
          return {
            dateFrom: new Date(`${custom.from}T00:00:00${MSK_OFFSET}`),
            dateTo: new Date(`${custom.to}T23:59:59.999${MSK_OFFSET}`),
          }
        }
      }
    }
    ```

    **Шаг B — создать `tests/date-periods.test.ts`** (10+ GREEN):

    ```typescript
    import { describe, it, expect, beforeEach, afterEach, vi } from "vitest"
    import {
      getPeriod,
      startOfMonthMsk,
      startOfQuarterMsk,
      startOfDayMsk,
      endOfDayMsk,
      PERIOD_PRESETS,
    } from "@/lib/date-periods"

    describe("startOfMonthMsk", () => {
      it("15 April 2026 14:30 UTC → 1 April 2026 00:00 МСК", () => {
        const input = new Date("2026-04-15T14:30:00Z")
        expect(startOfMonthMsk(input).toISOString()).toBe("2026-03-31T21:00:00.000Z")
        // 1 апреля 00:00 МСК (+03:00) == 31 марта 21:00 UTC
      })

      it("1 января 02:00 МСК попадает в январь МСК (не декабрь прошлого года)", () => {
        // 1 Jan 2026 02:00 МСК = 31 Dec 2025 23:00 UTC
        const input = new Date("2025-12-31T23:00:00Z")
        expect(startOfMonthMsk(input).toISOString()).toBe("2025-12-31T21:00:00.000Z")
        // 1 января 00:00 МСК (+03:00) == 31 декабря 21:00 UTC
      })

      it("по умолчанию использует текущее время", () => {
        const r = startOfMonthMsk()
        expect(r).toBeInstanceOf(Date)
        expect(r.getUTCDate()).toBeGreaterThanOrEqual(1)
      })
    })

    describe("startOfQuarterMsk (календарный)", () => {
      it("Q1: 15 February 2026 → 1 January 2026 00:00 МСК", () => {
        expect(startOfQuarterMsk(new Date("2026-02-15T10:00:00Z")).toISOString()).toBe("2025-12-31T21:00:00.000Z")
      })

      it("Q2: 15 May 2026 → 1 April 2026 00:00 МСК", () => {
        expect(startOfQuarterMsk(new Date("2026-05-15T10:00:00Z")).toISOString()).toBe("2026-03-31T21:00:00.000Z")
      })

      it("Q3: 15 August 2026 → 1 July 2026 00:00 МСК", () => {
        expect(startOfQuarterMsk(new Date("2026-08-15T10:00:00Z")).toISOString()).toBe("2026-06-30T21:00:00.000Z")
      })

      it("Q4: 15 November 2026 → 1 October 2026 00:00 МСК", () => {
        expect(startOfQuarterMsk(new Date("2026-11-15T10:00:00Z")).toISOString()).toBe("2026-09-30T21:00:00.000Z")
      })

      it("Граница: 1 April 2026 00:00 МСК → 1 April 2026 (не 1 January)", () => {
        // 1 April 2026 00:00 МСК = 31 Mar 2026 21:00 UTC
        expect(startOfQuarterMsk(new Date("2026-03-31T21:00:00Z")).toISOString()).toBe("2026-03-31T21:00:00.000Z")
      })
    })

    describe("startOfDayMsk / endOfDayMsk", () => {
      it("startOfDayMsk возвращает 00:00 МСК того же дня в МСК", () => {
        // 15 April 14:30 UTC → 15 April 17:30 МСК → startOfDayMsk = 15 April 00:00 МСК = 14 April 21:00 UTC
        expect(startOfDayMsk(new Date("2026-04-15T14:30:00Z")).toISOString()).toBe("2026-04-14T21:00:00.000Z")
      })

      it("endOfDayMsk возвращает 23:59:59.999 МСК того же дня", () => {
        // 15 April 00:00 МСК = 14 April 21:00 UTC → endOfDay = 15 April 23:59:59.999 МСК = 15 April 20:59:59.999 UTC
        expect(endOfDayMsk(new Date("2026-04-15T10:00:00Z")).toISOString()).toBe("2026-04-15T20:59:59.999Z")
      })
    })

    describe("getPeriod presets", () => {
      beforeEach(() => {
        vi.useFakeTimers()
        vi.setSystemTime(new Date("2026-04-15T12:00:00Z"))
      })
      afterEach(() => {
        vi.useRealTimers()
      })

      it("7d: dateFrom = now - 7 дней, dateTo = now", () => {
        const r = getPeriod("7d")
        expect(r.dateTo.toISOString()).toBe("2026-04-15T12:00:00.000Z")
        expect(r.dateFrom.toISOString()).toBe("2026-04-08T12:00:00.000Z")
      })

      it("30d: dateFrom = now - 30 дней", () => {
        const r = getPeriod("30d")
        expect(r.dateFrom.toISOString()).toBe("2026-03-16T12:00:00.000Z")
      })

      it("quarter: dateFrom = startOfQuarterMsk(now) — календарный Q2 = 1 April", () => {
        const r = getPeriod("quarter")
        expect(r.dateFrom.toISOString()).toBe("2026-03-31T21:00:00.000Z")
        // = 1 April 2026 00:00 МСК
      })

      it("custom: dateFrom = 00:00 МСК from, dateTo = 23:59:59.999 МСК to", () => {
        const r = getPeriod("custom", { from: "2026-04-01", to: "2026-04-10" })
        expect(r.dateFrom.toISOString()).toBe("2026-03-31T21:00:00.000Z") // 1 Apr 00:00 МСК
        expect(r.dateTo.toISOString()).toBe("2026-04-10T20:59:59.999Z")   // 10 Apr 23:59 МСК
      })

      it("custom без custom arg → throw", () => {
        expect(() => getPeriod("custom")).toThrowError(/custom preset requires custom arg/)
      })
    })

    describe("PERIOD_PRESETS const", () => {
      it("содержит 4 пресета в правильном порядке", () => {
        expect(PERIOD_PRESETS).toEqual(["7d", "30d", "quarter", "custom"])
      })
    })
    ```

    **Шаг C — запустить тесты:**
    ```bash
    npm run test -- tests/date-periods.test.ts
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/date-periods.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/date-periods.ts` exit 0
    - `grep -c "^export function getPeriod" lib/date-periods.ts` == 1
    - `grep -c "^export function startOfMonthMsk" lib/date-periods.ts` == 1
    - `grep -c "^export function startOfQuarterMsk" lib/date-periods.ts` == 1
    - `grep -c "^export function startOfDayMsk" lib/date-periods.ts` == 1
    - `grep -c "^export function endOfDayMsk" lib/date-periods.ts` == 1
    - `grep -c "PERIOD_PRESETS" lib/date-periods.ts` >= 2 (export const + type)
    - `grep "Europe/Moscow" lib/date-periods.ts` находит
    - `grep "+03:00" lib/date-periods.ts` находит (MSK_OFFSET const)
    - `grep -c "it(" tests/date-periods.test.ts` >= 10
    - `npm run test -- tests/date-periods.test.ts` exit 0 (все GREEN)
  </acceptance_criteria>
  <done>
    lib/date-periods.ts экспортирует 5 TZ-safe хелперов + PERIOD_PRESETS const; getPeriod правильно обрабатывает 4 пресета (календарный квартал D-05, custom с границами дней); 10+ GREEN unit-тестов покрывают все 4 квартала, границы МСК, 7d/30d/custom/quarter пресеты.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: lib/support-stats.ts (6 aggregation helpers) + tests/support-stats-helpers.test.ts (15+ GREEN) + 2 stub test файла</name>
  <files>lib/support-stats.ts, tests/support-stats-helpers.test.ts, tests/support-stats-page.test.ts, tests/support-stats-cron.test.ts</files>
  <read_first>
    - prisma/schema.prisma (строки 546-724 — SupportTicket, SupportMessage, ReturnDecision, AppealRecord — все поля для агрегаций)
    - lib/customer-aggregations.ts (Phase 12 паттерн pure helpers без Prisma — но Phase 13 использует Prisma directly так как это live aggregation)
    - tests/customer-profile-page.test.ts (Phase 12-02 паттерн mock prisma + dynamic import)
    - tests/support-sync-returns.test.ts (Phase 9 паттерн mock $transaction + groupBy)
    - .planning/phases/13-statistics/13-RESEARCH.md (строки 180-330 Pattern 1+2 aggregation, строки 484-540 aggregation queries SUP-37/38)
  </read_first>
  <behavior>
    **lib/support-stats.ts API:**

    1. `computeProductStats(nmId: number, dateFrom: Date, dateTo: Date): Promise<ProductStatRow>` — 7 метрик SUP-37 для одного nmId:
       - feedbacksTotal (COUNT channel=FEEDBACK), avgRating (_avg rating), feedbacksAnsweredPct (COUNT status IN ANSWERED/CLOSED/APPEALED / total)
       - questionsTotal (COUNT channel=QUESTION)
       - returnsTotal (COUNT channel=RETURN), returnsApproved (returnState=APPROVED), returnsRejected (returnState=REJECTED)
       - avgResponseTimeSec (raw SQL CTE first_inbound/first_outbound)

    2. `listProductsWithStats(dateFrom: Date, dateTo: Date, filters?: { nmIds?: number[] }): Promise<Array<ProductStatRow & { nmId, name, photoUrl }>>` — batch версия с JOIN WbCard (select nmId, name, photoUrl)

    3. `computeManagerStatsForPeriod(userId: string, dateFrom: Date, dateTo: Date): Promise<ManagerStatFields>` — 8 метрик SUP-38 для одного userId (D-04 outcome-actions only):
       - feedbacksAnswered / questionsAnswered / chatsAnswered (COUNT OUTBOUND SupportMessage where authorId=userId AND isAutoReply=false JOIN SupportTicket.channel)
       - returnsDecided (COUNT ReturnDecision where decidedById=userId)
       - returnsApproved (ReturnDecision где action IN APPROVE/RECONSIDER для расчёта % одобрения)
       - returnsRejected (ReturnDecision где action=REJECT)
       - appealsResolved (COUNT AppealRecord где resolvedById=userId)
       - avgResponseTimeSec (raw SQL по OUTBOUND authorId=userId - INBOUND на том же ticket)
       - totalProcessed = feedbacksAnswered + questionsAnswered + chatsAnswered + returnsDecided + appealsResolved (D-04 outcome-actions only — НЕ включает status changes)
       - Возвращает объект БЕЗ id/period/updatedAt (period проставляется в cron upsert)

    4. `listManagersWithStats(dateFrom: Date, dateTo: Date): Promise<Array<ManagerStatFields & { userId, name, isLive: boolean }>>` — для всех User у которых sectionRoles содержит SUPPORT. isLive = true если период включает сегодня (текущий месяц считается live, D-08 spec).

    5. `getTopReturnReasons(dateFrom: Date, dateTo: Date, limit: number = 10): Promise<Array<{ reason: string; count: number }>>` — raw SQL $queryRaw (D-03 глобально, по ReturnDecision.action='REJECT')

    6. `getAutoReplyCount(dateFrom: Date, dateTo: Date): Promise<number>` — COUNT SupportMessage where isAutoReply=true AND wbSentAt BETWEEN (D-02 глобально)

    **Types exposed:**
    ```typescript
    export interface ProductStatRow { ... }
    export interface ManagerStatFields { ... } // поля совпадают со спецификацией ManagerSupportStats БЕЗ id/period/updatedAt
    export interface ManagerStatRow extends ManagerStatFields { userId: string; name: string | null; isLive: boolean }
    ```

    **tests/support-stats-helpers.test.ts — 15+ GREEN тестов:**
    - computeProductStats: happy path с 5 feedbacks/3 questions/2 returns, нулевые значения, только возвраты без отзывов
    - computeManagerStatsForPeriod: все 8 метрик, totalProcessed = сумма outcome-actions (НЕ включает status changes), avgResponseTimeSec null если нет ответов
    - listProductsWithStats: JOIN WbCard (photoUrl, name), nmIds filter применяется
    - listManagersWithStats: isLive=true для текущего месяца, isLive=false для past
    - getTopReturnReasons: $queryRaw вызвана с правильными параметрами, map bigint→number
    - getAutoReplyCount: WHERE isAutoReply=true AND wbSentAt BETWEEN
  </behavior>
  <action>
    **Шаг A — создать `lib/support-stats.ts`:**

    ```typescript
    // lib/support-stats.ts
    // Phase 13 — aggregation helpers для /support/stats и cron upsert ManagerSupportStats.
    // D-01: RETURN avg response time исключён (Phase 9 не создаёт OUTBOUND SupportMessage).
    // D-02: auto-replies — глобальный счётчик (authorId=null).
    // D-03: top reasons — глобально (не per-product).
    // D-04: totalProcessed — only outcome-actions (НЕ status changes).
    // D-05: календарный квартал в date-periods.ts.
    // D-08: текущий месяц — live поверх cache.

    import { prisma } from "@/lib/prisma"
    import { startOfMonthMsk } from "@/lib/date-periods"

    export interface ProductStatRow {
      nmId: number
      name: string | null
      photoUrl: string | null
      feedbacksTotal: number
      avgRating: number | null
      feedbacksAnsweredPct: number | null
      questionsTotal: number
      returnsTotal: number
      returnsApproved: number
      returnsRejected: number
      avgResponseTimeSec: number | null
    }

    export interface ManagerStatFields {
      totalProcessed: number
      feedbacksAnswered: number
      questionsAnswered: number
      chatsAnswered: number
      returnsDecided: number
      returnsApproved: number
      returnsRejected: number
      appealsResolved: number
      avgResponseTimeSec: number | null
    }

    export interface ManagerStatRow extends ManagerStatFields {
      userId: string
      name: string | null
      isLive: boolean
    }

    // ── Helper: avg response time per ticket filter ──────────────────
    async function computeAvgResponseTimeSecForTickets(
      ticketFilter: { dateFrom: Date; dateTo: Date; nmId?: number; userId?: string }
    ): Promise<number | null> {
      const { dateFrom, dateTo, nmId, userId } = ticketFilter
      const nmClause = nmId ? `AND t."nmId" = ${nmId}` : ""
      const userClause = userId ? `AND fo."authorId" = '${userId}'` : ""
      const rows = await prisma.$queryRawUnsafe<Array<{ avg_sec: number | null }>>(`
        WITH first_inbound AS (
          SELECT "ticketId", MIN("wbSentAt") AS inbound_at
          FROM "SupportMessage"
          WHERE "direction" = 'INBOUND' AND "wbSentAt" IS NOT NULL
          GROUP BY "ticketId"
        ),
        first_outbound AS (
          SELECT "ticketId", "authorId", MIN("wbSentAt") AS outbound_at
          FROM "SupportMessage"
          WHERE "direction" = 'OUTBOUND' AND "isAutoReply" = false AND "wbSentAt" IS NOT NULL
          GROUP BY "ticketId", "authorId"
        )
        SELECT AVG(EXTRACT(EPOCH FROM (fo.outbound_at - fi.inbound_at)))::int AS avg_sec
        FROM "SupportTicket" t
        JOIN first_inbound fi ON fi."ticketId" = t.id
        JOIN first_outbound fo ON fo."ticketId" = t.id
        WHERE t."createdAt" >= $1 AND t."createdAt" <= $2
          AND fo.outbound_at > fi.inbound_at
          ${nmClause} ${userClause}
      `, dateFrom, dateTo)
      return rows[0]?.avg_sec ?? null
    }

    // ── SUP-37: Product stats ────────────────────────────────────────
    export async function computeProductStats(
      nmId: number,
      dateFrom: Date,
      dateTo: Date
    ): Promise<ProductStatRow> {
      const where = { nmId, createdAt: { gte: dateFrom, lte: dateTo } }

      const [
        feedbacksTotal, feedbacksAnswered, avgRatingAgg,
        questionsTotal, returnsTotal, returnsApproved, returnsRejected,
        avgResponseTimeSec,
      ] = await Promise.all([
        prisma.supportTicket.count({ where: { ...where, channel: "FEEDBACK" } }),
        prisma.supportTicket.count({
          where: { ...where, channel: "FEEDBACK", status: { in: ["ANSWERED", "CLOSED", "APPEALED"] } },
        }),
        prisma.supportTicket.aggregate({
          where: { ...where, channel: "FEEDBACK", rating: { not: null } },
          _avg: { rating: true },
        }),
        prisma.supportTicket.count({ where: { ...where, channel: "QUESTION" } }),
        prisma.supportTicket.count({ where: { ...where, channel: "RETURN" } }),
        prisma.supportTicket.count({ where: { ...where, channel: "RETURN", returnState: "APPROVED" } }),
        prisma.supportTicket.count({ where: { ...where, channel: "RETURN", returnState: "REJECTED" } }),
        computeAvgResponseTimeSecForTickets({ dateFrom, dateTo, nmId }),
      ])

      return {
        nmId,
        name: null,
        photoUrl: null,
        feedbacksTotal,
        avgRating: avgRatingAgg._avg.rating,
        feedbacksAnsweredPct: feedbacksTotal > 0
          ? Math.round((feedbacksAnswered / feedbacksTotal) * 100)
          : null,
        questionsTotal,
        returnsTotal,
        returnsApproved,
        returnsRejected,
        avgResponseTimeSec,
      }
    }

    export async function listProductsWithStats(
      dateFrom: Date,
      dateTo: Date,
      filters: { nmIds?: number[] } = {}
    ): Promise<ProductStatRow[]> {
      // 1. Find all distinct nmIds active in period
      const ticketNmIds = await prisma.supportTicket.findMany({
        where: {
          createdAt: { gte: dateFrom, lte: dateTo },
          nmId: { not: null, ...(filters.nmIds ? { in: filters.nmIds } : {}) },
        },
        select: { nmId: true },
        distinct: ["nmId"],
      })
      const nmIds = ticketNmIds.map(t => t.nmId!).filter(Boolean)
      if (nmIds.length === 0) return []

      // 2. Compute stats per nmId (parallel)
      const stats = await Promise.all(nmIds.map(id => computeProductStats(id, dateFrom, dateTo)))

      // 3. Enrich with WbCard
      const cards = await prisma.wbCard.findMany({
        where: { nmId: { in: nmIds } },
        select: { nmId: true, name: true, photoUrl: true },
      })
      const cardMap = new Map(cards.map(c => [c.nmId, c]))

      return stats.map(s => ({
        ...s,
        name: cardMap.get(s.nmId)?.name ?? null,
        photoUrl: cardMap.get(s.nmId)?.photoUrl ?? null,
      }))
    }

    // ── SUP-38: Manager stats (D-04 outcome-actions only) ────────────
    export async function computeManagerStatsForPeriod(
      userId: string,
      dateFrom: Date,
      dateTo: Date
    ): Promise<ManagerStatFields> {
      // 1. OUTBOUND replies per channel (authorId=userId, isAutoReply=false)
      const messageGroups = await prisma.supportMessage.findMany({
        where: {
          authorId: userId,
          direction: "OUTBOUND",
          isAutoReply: false,
          wbSentAt: { gte: dateFrom, lte: dateTo },
        },
        select: { ticket: { select: { channel: true } } },
      })
      const counts = { feedbacksAnswered: 0, questionsAnswered: 0, chatsAnswered: 0 }
      for (const m of messageGroups) {
        if (m.ticket.channel === "FEEDBACK") counts.feedbacksAnswered++
        else if (m.ticket.channel === "QUESTION") counts.questionsAnswered++
        else if (m.ticket.channel === "CHAT") counts.chatsAnswered++
      }

      // 2. Return decisions per action type
      const [returnsDecided, returnsApproved, returnsRejected] = await Promise.all([
        prisma.returnDecision.count({
          where: { decidedById: userId, decidedAt: { gte: dateFrom, lte: dateTo } },
        }),
        prisma.returnDecision.count({
          where: {
            decidedById: userId,
            decidedAt: { gte: dateFrom, lte: dateTo },
            action: { in: ["APPROVE", "RECONSIDER"] },
          },
        }),
        prisma.returnDecision.count({
          where: {
            decidedById: userId,
            decidedAt: { gte: dateFrom, lte: dateTo },
            action: "REJECT",
          },
        }),
      ])

      // 3. Appeal decisions
      const appealsResolved = await prisma.appealRecord.count({
        where: {
          resolvedById: userId,
          appealResolvedAt: { gte: dateFrom, lte: dateTo },
        },
      })

      // 4. Avg response time for this manager's OUTBOUND
      const avgResponseTimeSec = await computeAvgResponseTimeSecForTickets({ dateFrom, dateTo, userId })

      const totalProcessed =
        counts.feedbacksAnswered +
        counts.questionsAnswered +
        counts.chatsAnswered +
        returnsDecided +
        appealsResolved

      return {
        totalProcessed,
        ...counts,
        returnsDecided,
        returnsApproved,
        returnsRejected,
        appealsResolved,
        avgResponseTimeSec,
      }
    }

    export async function listManagersWithStats(
      dateFrom: Date,
      dateTo: Date
    ): Promise<ManagerStatRow[]> {
      // Users with SUPPORT section role
      const users = await prisma.user.findMany({
        where: {
          isActive: true,
          sectionRoles: { some: { section: "SUPPORT" } },
        },
        select: { id: true, name: true, firstName: true, lastName: true },
      })
      if (users.length === 0) return []

      const currentMonthStart = startOfMonthMsk(new Date())
      const isLive = dateTo >= currentMonthStart

      const rows = await Promise.all(
        users.map(async (u) => {
          const stats = await computeManagerStatsForPeriod(u.id, dateFrom, dateTo)
          const name = [u.firstName, u.lastName].filter(Boolean).join(" ") || u.name || null
          return { userId: u.id, name, isLive, ...stats }
        })
      )
      return rows
    }

    // ── SUP-37 extra: top return reasons (D-03 глобально) ────────────
    export async function getTopReturnReasons(
      dateFrom: Date,
      dateTo: Date,
      limit: number = 10
    ): Promise<Array<{ reason: string; count: number }>> {
      const rows = await prisma.$queryRawUnsafe<Array<{ reason: string; cnt: bigint | number }>>(`
        SELECT rd.reason, COUNT(*)::int AS cnt
        FROM "ReturnDecision" rd
        WHERE rd."action" = 'REJECT'
          AND rd.reason IS NOT NULL
          AND rd."decidedAt" >= $1 AND rd."decidedAt" <= $2
        GROUP BY rd.reason
        ORDER BY cnt DESC
        LIMIT $3
      `, dateFrom, dateTo, limit)
      return rows.map(r => ({ reason: r.reason, count: Number(r.cnt) }))
    }

    // ── SUP-38 extra: auto reply count (D-02 глобально) ──────────────
    export async function getAutoReplyCount(dateFrom: Date, dateTo: Date): Promise<number> {
      return prisma.supportMessage.count({
        where: {
          isAutoReply: true,
          wbSentAt: { gte: dateFrom, lte: dateTo },
        },
      })
    }
    ```

    **Шаг B — создать `tests/support-stats-helpers.test.ts` (15+ GREEN):**

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    const prismaMock = {
      supportTicket: {
        count: vi.fn(),
        aggregate: vi.fn(),
        findMany: vi.fn(),
      },
      supportMessage: {
        findMany: vi.fn(),
        count: vi.fn(),
      },
      returnDecision: {
        count: vi.fn(),
      },
      appealRecord: {
        count: vi.fn(),
      },
      user: { findMany: vi.fn() },
      wbCard: { findMany: vi.fn() },
      $queryRawUnsafe: vi.fn(),
    }
    vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

    beforeEach(() => {
      vi.resetAllMocks()
    })

    const DF = new Date("2026-04-01T00:00:00Z")
    const DT = new Date("2026-04-30T23:59:59Z")

    describe("computeProductStats", () => {
      it("happy path — 5 feedbacks, 3 answered, avg 4.2, 2 returns (1 approved, 1 rejected)", async () => {
        prismaMock.supportTicket.count
          .mockResolvedValueOnce(5)  // feedbacksTotal
          .mockResolvedValueOnce(3)  // feedbacksAnswered
          .mockResolvedValueOnce(2)  // questionsTotal
          .mockResolvedValueOnce(2)  // returnsTotal
          .mockResolvedValueOnce(1)  // returnsApproved
          .mockResolvedValueOnce(1)  // returnsRejected
        prismaMock.supportTicket.aggregate.mockResolvedValueOnce({ _avg: { rating: 4.2 } })
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: 3600 }])

        const { computeProductStats } = await import("@/lib/support-stats")
        const r = await computeProductStats(12345, DF, DT)

        expect(r.nmId).toBe(12345)
        expect(r.feedbacksTotal).toBe(5)
        expect(r.feedbacksAnsweredPct).toBe(60) // 3/5
        expect(r.avgRating).toBe(4.2)
        expect(r.questionsTotal).toBe(2)
        expect(r.returnsTotal).toBe(2)
        expect(r.returnsApproved).toBe(1)
        expect(r.returnsRejected).toBe(1)
        expect(r.avgResponseTimeSec).toBe(3600)
      })

      it("feedbacksAnsweredPct = null если 0 feedbacks", async () => {
        prismaMock.supportTicket.count.mockResolvedValue(0)
        prismaMock.supportTicket.aggregate.mockResolvedValueOnce({ _avg: { rating: null } })
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: null }])
        const { computeProductStats } = await import("@/lib/support-stats")
        const r = await computeProductStats(1, DF, DT)
        expect(r.feedbacksAnsweredPct).toBeNull()
      })

      it("avgResponseTimeSec = null если нет ответов", async () => {
        prismaMock.supportTicket.count.mockResolvedValue(0)
        prismaMock.supportTicket.aggregate.mockResolvedValueOnce({ _avg: { rating: null } })
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: null }])
        const { computeProductStats } = await import("@/lib/support-stats")
        const r = await computeProductStats(1, DF, DT)
        expect(r.avgResponseTimeSec).toBeNull()
      })
    })

    describe("computeManagerStatsForPeriod (D-04 outcome-actions only)", () => {
      it("happy path — 2 FEEDBACK + 1 QUESTION + 1 CHAT replies + 3 returns + 1 appeal", async () => {
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([
          { ticket: { channel: "FEEDBACK" } },
          { ticket: { channel: "FEEDBACK" } },
          { ticket: { channel: "QUESTION" } },
          { ticket: { channel: "CHAT" } },
        ])
        prismaMock.returnDecision.count
          .mockResolvedValueOnce(3) // returnsDecided
          .mockResolvedValueOnce(2) // returnsApproved
          .mockResolvedValueOnce(1) // returnsRejected
        prismaMock.appealRecord.count.mockResolvedValueOnce(1)
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: 1200 }])

        const { computeManagerStatsForPeriod } = await import("@/lib/support-stats")
        const r = await computeManagerStatsForPeriod("u-1", DF, DT)

        expect(r.feedbacksAnswered).toBe(2)
        expect(r.questionsAnswered).toBe(1)
        expect(r.chatsAnswered).toBe(1)
        expect(r.returnsDecided).toBe(3)
        expect(r.returnsApproved).toBe(2)
        expect(r.returnsRejected).toBe(1)
        expect(r.appealsResolved).toBe(1)
        expect(r.avgResponseTimeSec).toBe(1200)
      })

      it("totalProcessed = feedbacks + questions + chats + returnsDecided + appealsResolved (D-04)", async () => {
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([
          { ticket: { channel: "FEEDBACK" } },
          { ticket: { channel: "QUESTION" } },
          { ticket: { channel: "CHAT" } },
        ])
        prismaMock.returnDecision.count.mockResolvedValueOnce(2).mockResolvedValueOnce(2).mockResolvedValueOnce(0)
        prismaMock.appealRecord.count.mockResolvedValueOnce(1)
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: null }])

        const { computeManagerStatsForPeriod } = await import("@/lib/support-stats")
        const r = await computeManagerStatsForPeriod("u-1", DF, DT)
        // 1 (F) + 1 (Q) + 1 (C) + 2 (returns) + 1 (appeal) = 6
        expect(r.totalProcessed).toBe(6)
      })

      it("avgResponseTimeSec = null если нет ответов менеджера", async () => {
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([])
        prismaMock.returnDecision.count.mockResolvedValue(0)
        prismaMock.appealRecord.count.mockResolvedValueOnce(0)
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: null }])

        const { computeManagerStatsForPeriod } = await import("@/lib/support-stats")
        const r = await computeManagerStatsForPeriod("u-1", DF, DT)
        expect(r.avgResponseTimeSec).toBeNull()
        expect(r.totalProcessed).toBe(0)
      })

      it("returnsApproved включает и APPROVE, и RECONSIDER (для расчёта % одобрения)", async () => {
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([])
        prismaMock.returnDecision.count
          .mockResolvedValueOnce(5)  // total decided (APPROVE+REJECT+RECONSIDER)
          .mockResolvedValueOnce(3)  // approved (APPROVE+RECONSIDER) ← WHERE action IN (APPROVE, RECONSIDER)
          .mockResolvedValueOnce(2)  // rejected
        prismaMock.appealRecord.count.mockResolvedValueOnce(0)
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([{ avg_sec: null }])

        const { computeManagerStatsForPeriod } = await import("@/lib/support-stats")
        const r = await computeManagerStatsForPeriod("u-1", DF, DT)
        expect(r.returnsDecided).toBe(5)
        expect(r.returnsApproved).toBe(3)
        // Проверяем что 2-й вызов count использовал action IN (APPROVE, RECONSIDER)
        const call2 = prismaMock.returnDecision.count.mock.calls[1][0]
        expect(call2.where.action).toEqual({ in: ["APPROVE", "RECONSIDER"] })
      })
    })

    describe("listProductsWithStats", () => {
      it("JOIN WbCard — возвращает name и photoUrl", async () => {
        prismaMock.supportTicket.findMany.mockResolvedValueOnce([{ nmId: 100 }, { nmId: 200 }])
        // per-nmId stats calls
        prismaMock.supportTicket.count.mockResolvedValue(0)
        prismaMock.supportTicket.aggregate.mockResolvedValue({ _avg: { rating: null } })
        prismaMock.$queryRawUnsafe.mockResolvedValue([{ avg_sec: null }])
        prismaMock.wbCard.findMany.mockResolvedValueOnce([
          { nmId: 100, name: "Товар A", photoUrl: "/a.jpg" },
          { nmId: 200, name: "Товар B", photoUrl: "/b.jpg" },
        ])

        const { listProductsWithStats } = await import("@/lib/support-stats")
        const r = await listProductsWithStats(DF, DT)

        expect(r).toHaveLength(2)
        const byId = Object.fromEntries(r.map(x => [x.nmId, x]))
        expect(byId[100].name).toBe("Товар A")
        expect(byId[100].photoUrl).toBe("/a.jpg")
        expect(byId[200].name).toBe("Товар B")
      })

      it("пустой список если нет тикетов в периоде", async () => {
        prismaMock.supportTicket.findMany.mockResolvedValueOnce([])
        const { listProductsWithStats } = await import("@/lib/support-stats")
        const r = await listProductsWithStats(DF, DT)
        expect(r).toEqual([])
      })

      it("применяет filter nmIds если передан", async () => {
        prismaMock.supportTicket.findMany.mockResolvedValueOnce([])
        const { listProductsWithStats } = await import("@/lib/support-stats")
        await listProductsWithStats(DF, DT, { nmIds: [100, 200] })
        const call = prismaMock.supportTicket.findMany.mock.calls[0][0]
        expect(call.where.nmId).toEqual({ not: null, in: [100, 200] })
      })
    })

    describe("listManagersWithStats", () => {
      it("фильтрует пользователей по sectionRoles SUPPORT + isActive", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([])
        const { listManagersWithStats } = await import("@/lib/support-stats")
        await listManagersWithStats(DF, DT)
        const call = prismaMock.user.findMany.mock.calls[0][0]
        expect(call.where.isActive).toBe(true)
        expect(call.where.sectionRoles).toEqual({ some: { section: "SUPPORT" } })
      })

      it("isLive=true если период включает текущий месяц", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([
          { id: "u-1", name: "Иван", firstName: "Иван", lastName: "Петров" },
        ])
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([])
        prismaMock.returnDecision.count.mockResolvedValue(0)
        prismaMock.appealRecord.count.mockResolvedValue(0)
        prismaMock.$queryRawUnsafe.mockResolvedValue([{ avg_sec: null }])

        const { listManagersWithStats } = await import("@/lib/support-stats")
        const nowDt = new Date()
        const r = await listManagersWithStats(new Date(nowDt.getTime() - 7 * 86_400_000), nowDt)
        expect(r[0].isLive).toBe(true)
        expect(r[0].name).toBe("Иван Петров")
      })

      it("isLive=false для past period (dateTo < currentMonthStart)", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([
          { id: "u-1", name: "Иван", firstName: null, lastName: null },
        ])
        prismaMock.supportMessage.findMany.mockResolvedValueOnce([])
        prismaMock.returnDecision.count.mockResolvedValue(0)
        prismaMock.appealRecord.count.mockResolvedValue(0)
        prismaMock.$queryRawUnsafe.mockResolvedValue([{ avg_sec: null }])

        const { listManagersWithStats } = await import("@/lib/support-stats")
        // Past period — 2020
        const r = await listManagersWithStats(new Date("2020-01-01"), new Date("2020-01-31"))
        expect(r[0].isLive).toBe(false)
      })
    })

    describe("getTopReturnReasons (D-03 глобально)", () => {
      it("возвращает отсортированный список с count как number (не bigint)", async () => {
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([
          { reason: "Фото не соответствует", cnt: 5n },
          { reason: "Брак не подтверждён", cnt: 3n },
        ])
        const { getTopReturnReasons } = await import("@/lib/support-stats")
        const r = await getTopReturnReasons(DF, DT, 10)
        expect(r).toEqual([
          { reason: "Фото не соответствует", count: 5 },
          { reason: "Брак не подтверждён", count: 3 },
        ])
        // $queryRawUnsafe вызвана с SQL + params
        const [sql, from, to, limit] = prismaMock.$queryRawUnsafe.mock.calls[0]
        expect(sql).toContain("action\" = 'REJECT'")
        expect(sql).toContain("GROUP BY rd.reason")
        expect(from).toBe(DF)
        expect(to).toBe(DT)
        expect(limit).toBe(10)
      })

      it("пустой список если нет отклонённых", async () => {
        prismaMock.$queryRawUnsafe.mockResolvedValueOnce([])
        const { getTopReturnReasons } = await import("@/lib/support-stats")
        expect(await getTopReturnReasons(DF, DT)).toEqual([])
      })
    })

    describe("getAutoReplyCount (D-02 глобально)", () => {
      it("count SupportMessage where isAutoReply=true AND wbSentAt BETWEEN", async () => {
        prismaMock.supportMessage.count.mockResolvedValueOnce(42)
        const { getAutoReplyCount } = await import("@/lib/support-stats")
        const r = await getAutoReplyCount(DF, DT)
        expect(r).toBe(42)
        const call = prismaMock.supportMessage.count.mock.calls[0][0]
        expect(call.where.isAutoReply).toBe(true)
        expect(call.where.wbSentAt).toEqual({ gte: DF, lte: DT })
      })
    })
    ```

    **Шаг C — создать stub `tests/support-stats-page.test.ts`:**

    ```typescript
    import { describe, it, expect } from "vitest"

    describe("support-stats page (stub — заполняется в Plan 13-02)", () => {
      it.skip("renders tabs and filters — TODO Plan 13-02")
      it.skip("applies period searchParam — TODO Plan 13-02")
      it.skip("applies tab searchParam — TODO Plan 13-02")
      it.skip("requireSection SUPPORT — TODO Plan 13-02")

      it("smoke: pure helper import from Plan 13-01", async () => {
        const mod = await import("@/lib/support-stats")
        expect(typeof mod.computeProductStats).toBe("function")
      })
    })
    ```

    **Шаг D — создать stub `tests/support-stats-cron.test.ts`:**

    ```typescript
    import { describe, it, expect } from "vitest"

    describe("support-stats-cron (stub — заполняется в Plan 13-03)", () => {
      it.skip("upsert ManagerSupportStats for each SUPPORT user — TODO Plan 13-03")
      it.skip("period = startOfMonthMsk(today) — TODO Plan 13-03")
      it.skip("idempotent — second run doesn't duplicate — TODO Plan 13-03")
      it.skip("CRON_SECRET required — TODO Plan 13-03")

      it("smoke: pure helper import from Plan 13-01", async () => {
        const { computeManagerStatsForPeriod } = await import("@/lib/support-stats")
        expect(typeof computeManagerStatsForPeriod).toBe("function")
      })
    })
    ```

    **Шаг E — запустить все тесты Phase 13:**
    ```bash
    npm run test -- tests/date-periods.test.ts tests/support-stats-helpers.test.ts tests/support-stats-page.test.ts tests/support-stats-cron.test.ts
    npx tsc --noEmit
    ```

    **Шаг F — проверить regression Phase 7-12:**
    ```bash
    npm run test
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-stats-helpers.test.ts tests/support-stats-page.test.ts tests/support-stats-cron.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/support-stats.ts` exit 0
    - `grep -c "^export async function computeProductStats" lib/support-stats.ts` == 1
    - `grep -c "^export async function computeManagerStatsForPeriod" lib/support-stats.ts` == 1
    - `grep -c "^export async function listProductsWithStats" lib/support-stats.ts` == 1
    - `grep -c "^export async function listManagersWithStats" lib/support-stats.ts` == 1
    - `grep -c "^export async function getTopReturnReasons" lib/support-stats.ts` == 1
    - `grep -c "^export async function getAutoReplyCount" lib/support-stats.ts` == 1
    - `grep "\\\$queryRawUnsafe\\|\\\$queryRaw" lib/support-stats.ts | wc -l` >= 2 (avg response time + top reasons)
    - `grep "action: { in: \\[\"APPROVE\", \"RECONSIDER\"\\] }" lib/support-stats.ts` находит (D-04 % одобрения)
    - `grep "isAutoReply: true" lib/support-stats.ts` находит (getAutoReplyCount)
    - `grep "sectionRoles: { some: { section: \"SUPPORT\"" lib/support-stats.ts` находит (listManagersWithStats)
    - `grep -c "it(" tests/support-stats-helpers.test.ts` >= 15
    - `grep -c "it.skip(" tests/support-stats-page.test.ts` >= 4
    - `grep -c "it.skip(" tests/support-stats-cron.test.ts` >= 4
    - `npm run test -- tests/date-periods.test.ts tests/support-stats-helpers.test.ts tests/support-stats-page.test.ts tests/support-stats-cron.test.ts` exit 0
    - `npx tsc --noEmit` exit 0
    - `npm run test` exit 0 (полная suite Phase 7-13 GREEN, 0 failed)
  </acceptance_criteria>
  <done>
    lib/support-stats.ts экспортирует 6 aggregation helpers с полным покрытием SUP-37/38/39 и user decisions D-01..D-04; tests/support-stats-helpers.test.ts — 15+ GREEN тестов с корректными mock Prisma patterns; 2 stub-файла готовы к заполнению в Plan 13-02/13-03; tsc и полная test suite проходят.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач:
1. `npx prisma validate` exit 0
2. `npx prisma generate` exit 0
3. `npx tsc --noEmit` exit 0
4. `npm run test` exit 0 (полная suite Phase 7-13 GREEN)
5. `grep -c "^model ManagerSupportStats" prisma/schema.prisma` == 1
6. `ls prisma/migrations/20260418_phase13_statistics/migration.sql` exit 0
7. `test -f lib/date-periods.ts && test -f lib/support-stats.ts` exit 0
8. `test -f tests/date-periods.test.ts && test -f tests/support-stats-helpers.test.ts && test -f tests/support-stats-page.test.ts && test -f tests/support-stats-cron.test.ts` exit 0
</verification>

<success_criteria>
Plan 13-01 завершён когда:
- Prisma schema содержит ManagerSupportStats модель + обратную relation User.managerStats + 2 performance индекса
- migration.sql готов к применению на VPS через deploy.sh в Plan 13-03
- lib/date-periods.ts — 5 TZ-safe хелперов с календарным кварталом (D-05)
- lib/support-stats.ts — 6 aggregation helpers реализуют всю математику SUP-37/38 с учётом D-01..D-04
- tests/date-periods.test.ts — 10+ GREEN тестов
- tests/support-stats-helpers.test.ts — 15+ GREEN тестов с корректными mock Prisma
- tests/support-stats-page.test.ts, tests/support-stats-cron.test.ts — stub'ы готовы к заполнению
- Полная test suite Phase 7-13 GREEN (0 regression)
- Commit `feat(13-01): ManagerSupportStats model + date-periods + support-stats helpers + Wave 0 tests`
</success_criteria>

<output>
После завершения создать `.planning/phases/13-statistics/13-01-SUMMARY.md` с:
- Prisma schema: ManagerSupportStats модель + relation User + 2 индекса
- migration.sql path для Plan 13-03 deploy
- lib/date-periods.ts — 5 хелперов (getPeriod/startOfMonthMsk/startOfQuarterMsk/startOfDayMsk/endOfDayMsk) + PERIOD_PRESETS const
- lib/support-stats.ts — 6 aggregation helpers + 3 exported types (ProductStatRow, ManagerStatFields, ManagerStatRow)
- User decisions реализация: D-01 (нет RETURN в avg response), D-02 (getAutoReplyCount global), D-03 (getTopReturnReasons global по REJECT), D-04 (totalProcessed only outcome-actions), D-05 (календарный квартал)
- Test coverage: 10+ date-periods + 15+ support-stats-helpers + 2 stub
- Known limitations: avg response time через $queryRawUnsafe с string interpolation для nmId/userId — безопасно так как значения из типизированных БД полей, но если изменить на user input — нужен $queryRaw с parameterized bindings
- Next: Plan 13-02 UI /support/stats page использует listProductsWithStats + listManagersWithStats + getTopReturnReasons + getAutoReplyCount
</output>
</context>
