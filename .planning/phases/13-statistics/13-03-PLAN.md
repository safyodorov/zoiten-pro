---
phase: 13-statistics
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - app/api/cron/support-stats-refresh/route.ts
  - deploy.sh
  - tests/support-stats-cron.test.ts
autonomous: false
requirements:
  - SUP-39
must_haves:
  truths:
    - "app/api/cron/support-stats-refresh/route.ts — GET с x-cron-secret защитой, maxDuration=300, runtime=nodejs"
    - "Cron endpoint для каждого User с sectionRoles.section=SUPPORT AND isActive=true вызывает computeManagerStatsForPeriod(userId, monthStart, monthEnd) где monthStart = startOfMonthMsk(new Date()) и monthEnd = now"
    - "Cron делает upsert ManagerSupportStats по @@unique([userId, period]) для period=monthStart (D-08 обновляется только текущий месяц; past months — immutable)"
    - "Cron возвращает JSON { ok: true, usersProcessed: N, period: ISO-string } или { ok: false, error } с HTTP 401/500"
    - "deploy.sh добавляет systemd service + timer zoiten-stats-refresh с OnCalendar=*-*-* 03:00:00 Europe/Moscow (D-08) + Persistent=true"
    - "deploy.sh применяет prisma migrate deploy (Plan 13-01 migration.sql применяется на VPS автоматически)"
    - "tests/support-stats-cron.test.ts — 5+ GREEN: happy path N users → N upserts, period = startOfMonthMsk today, idempotent (multiple calls не создают дубликаты благодаря @@unique), CRON_SECRET required (401 на missing/wrong), users без SUPPORT роли игнорируются"
    - "Human UAT чеклист выполнен: страница рендерится на проде, tabs переключаются, фильтры меняют URL и цифры, cron ran at least once, live month показывает актуальные числа, календарный квартал правильный, RBAC VIEW разрешает, MANAGE не требуется"
    - "Deploy прошёл: systemctl is-active zoiten-erp.service + zoiten-stats-refresh.timer == active, /support/stats рендерится на https://zoiten.pro"
  artifacts:
    - path: "app/api/cron/support-stats-refresh/route.ts"
      provides: "GET cron endpoint — upsert ManagerSupportStats для current month per user"
      contains: "support-stats-refresh"
    - path: "deploy.sh"
      provides: "Обновлённый deploy script с systemd timer zoiten-stats-refresh (03:00 МСК daily)"
      contains: "zoiten-stats-refresh"
    - path: "tests/support-stats-cron.test.ts"
      provides: "5+ GREEN тестов cron upsert логики"
      contains: "describe"
  key_links:
    - from: "app/api/cron/support-stats-refresh/route.ts"
      to: "lib/support-stats.ts computeManagerStatsForPeriod"
      via: "per-user aggregation + upsert"
      pattern: "computeManagerStatsForPeriod"
    - from: "app/api/cron/support-stats-refresh/route.ts"
      to: "lib/date-periods.ts startOfMonthMsk"
      via: "period = startOfMonthMsk(today) для upsert key"
      pattern: "startOfMonthMsk"
    - from: "app/api/cron/support-stats-refresh/route.ts"
      to: "prisma.managerSupportStats.upsert"
      via: "upsert по @@unique([userId, period])"
      pattern: "managerSupportStats\\.upsert"
    - from: "app/api/cron/support-stats-refresh/route.ts"
      to: "CRON_SECRET env var"
      via: "x-cron-secret header match (паттерн Phase 8/9/10)"
      pattern: "x-cron-secret"
    - from: "deploy.sh"
      to: "/api/cron/support-stats-refresh"
      via: "systemd timer + curl http://localhost:3001"
      pattern: "support-stats-refresh"
---

<objective>
Phase 13 Plan 03 — Cron + Deploy + Human UAT (финальный плюс end-to-end проверка):

1. **`app/api/cron/support-stats-refresh/route.ts`** — GET endpoint:
   - Guard x-cron-secret
   - Для каждого User с sectionRoles.section=SUPPORT + isActive=true
     - Вычислить stats через `computeManagerStatsForPeriod(userId, monthStart, now)`
     - Upsert `ManagerSupportStats(userId, period=monthStart, ...stats)` — @@unique key защищает от дубликатов
   - D-08: cron обновляет ТОЛЬКО текущий месяц, past months immutable
   - D-09: backfill НЕ реализуется, Phase 13 начинает с текущего месяца

2. **deploy.sh обновления** — добавить systemd timer zoiten-stats-refresh с `OnCalendar=*-*-* 03:00:00 Europe/Moscow` + `Persistent=true` (DST-safe, catches missed runs).

3. **tests/support-stats-cron.test.ts** — заменить it.skip на 5+ GREEN тестов (mock Prisma + fake timers):
   - happy path — 3 users → 3 upserts
   - period = startOfMonthMsk(today)
   - idempotent (повторные вызовы не создают дубликаты)
   - CRON_SECRET required (401)
   - users без SUPPORT роли игнорируются

4. **Deploy на VPS** — через `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`:
   - Применяет миграцию Plan 13-01
   - Устанавливает systemd timer
   - Restart zoiten-erp.service
   - Manual trigger cron один раз → проверить логи + БД

5. **Human UAT checkpoint (checkpoint:human-verify gate=blocking)** — выполнить 15+ пунктов чек-листа в браузере на https://zoiten.pro/support/stats.

Purpose: SUP-39 завершает Phase 13 cron-инфраструктурой; Human UAT валидирует что агрегации реально корректны относительно данных в БД; Phase 13 готова к финальному ROADMAP update на Complete.

Output: 1 новый API route (cron), 1 обновлённый deploy.sh с systemd timer, 1 заполненный test файл (5+ GREEN), Human UAT approval, Phase 13 COMPLETE marker в ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-statistics/13-RESEARCH.md
@.planning/phases/13-statistics/13-VALIDATION.md
@.planning/phases/13-statistics/13-01-PLAN.md
@.planning/phases/13-statistics/13-02-PLAN.md
@.planning/phases/09-returns/09-04-PLAN.md
@.planning/phases/10-chat-autoreply/10-04-PLAN.md
@CLAUDE.md
@app/api/cron/support-sync-reviews/route.ts
@app/api/cron/support-sync-chat/route.ts
@lib/support-stats.ts
@lib/date-periods.ts
@deploy.sh

<interfaces>
**Plan 13-01 exports (reminder):**
```typescript
// lib/support-stats.ts
export async function computeManagerStatsForPeriod(
  userId: string,
  dateFrom: Date,
  dateTo: Date
): Promise<ManagerStatFields>  // { totalProcessed, feedbacksAnswered, questionsAnswered, chatsAnswered, returnsDecided, returnsApproved, returnsRejected, appealsResolved, avgResponseTimeSec }

// lib/date-periods.ts
export function startOfMonthMsk(date?: Date): Date
```

**Cron endpoint паттерн (из app/api/cron/support-sync-reviews/route.ts):**
```typescript
import { NextRequest, NextResponse } from "next/server"

export const runtime = "nodejs"
export const maxDuration = 300

export async function GET(req: NextRequest): Promise<NextResponse> {
  const secret = req.headers.get("x-cron-secret")
  if (!secret || secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
  }
  try {
    // ... logic
    return NextResponse.json({ ok: true, usersProcessed: N })
  } catch (err) {
    const msg = err instanceof Error ? err.message : "Ошибка"
    return NextResponse.json({ ok: false, error: msg }, { status: 500 })
  }
}
```

**Prisma upsert паттерн для @@unique композитного ключа:**
```typescript
await prisma.managerSupportStats.upsert({
  where: { userId_period: { userId, period: monthStart } },
  create: { userId, period: monthStart, ...stats },
  update: { ...stats },
})
```
Primary key для composite @@unique в Prisma: `userId_period` (соединяется через underscore из полей @@unique).

**systemd timer паттерн OnCalendar с TZ (из deploy.sh Phase 8/9/10):**
```
[Timer]
OnCalendar=*-*-* 03:00:00 Europe/Moscow
Persistent=true  # Catch missed runs (power outage, VPS restart)
Unit=zoiten-stats-refresh.service
```
**КРИТИЧНО:** проверено что systemd 245+ (Ubuntu 22.04 / Debian 12 на VPS) поддерживает `Europe/Moscow` как суффикс к `OnCalendar`. Fallback на `OnCalendar=*-*-* 00:00:00 UTC` (= 03:00 МСК) если systemd версия старая.

**Users с sectionRoles SUPPORT (Prisma query):**
```typescript
const users = await prisma.user.findMany({
  where: {
    isActive: true,
    sectionRoles: { some: { section: "SUPPORT" } },
  },
  select: { id: true },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Cron endpoint /api/cron/support-stats-refresh + tests/support-stats-cron.test.ts (5+ GREEN)</name>
  <files>app/api/cron/support-stats-refresh/route.ts, tests/support-stats-cron.test.ts</files>
  <read_first>
    - app/api/cron/support-sync-reviews/route.ts (Phase 8/9 паттерн cron endpoint — x-cron-secret, maxDuration, runtime)
    - app/api/cron/support-sync-chat/route.ts (Phase 10 — паттерн для try/catch + graceful)
    - lib/support-stats.ts (Plan 13-01 computeManagerStatsForPeriod)
    - lib/date-periods.ts (Plan 13-01 startOfMonthMsk)
    - tests/support-cron.test.ts (Phase 8 паттерн vitest для cron routes)
    - prisma/schema.prisma (ManagerSupportStats модель + @@unique композитный ключ)
  </read_first>
  <behavior>
    **Cron logic:**
    1. Guard x-cron-secret → 401 при missing/mismatch
    2. Вычислить monthStart = startOfMonthMsk(new Date()) и monthEnd = new Date()
    3. findMany User с sectionRoles.section=SUPPORT + isActive=true
    4. Для каждого user:
       - computeManagerStatsForPeriod(user.id, monthStart, monthEnd)
       - upsert managerSupportStats by { userId_period: { userId: user.id, period: monthStart } }
    5. Возврат { ok: true, usersProcessed: users.length, period: monthStart.toISOString() }
    6. Ошибки per-user ловятся чтобы один падающий менеджер не ронял весь cron (log + continue, agregate errors в response)

    **Tests (5+ GREEN):**
    1. 401 при отсутствии x-cron-secret
    2. 401 при неверном x-cron-secret
    3. happy path — 3 users → 3 calls computeManagerStatsForPeriod + 3 upsert
    4. period корректно = startOfMonthMsk сегодня (через vi.useFakeTimers)
    5. idempotent — повторный вызов вызывает upsert с тем же period (не create дубликаты)
    6. 0 users с SUPPORT → usersProcessed=0, успех
  </behavior>
  <action>
    **Шаг A — создать `app/api/cron/support-stats-refresh/route.ts`:**

    ```typescript
    // app/api/cron/support-stats-refresh/route.ts
    // Phase 13 — cron endpoint для обновления ManagerSupportStats (SUP-39).
    // Systemd timer вызывает GET раз в сутки в 03:00 МСК (D-08).
    // Upsert per user с sectionRoles SUPPORT по @@unique([userId, period]).

    import { NextRequest, NextResponse } from "next/server"
    import { prisma } from "@/lib/prisma"
    import { computeManagerStatsForPeriod } from "@/lib/support-stats"
    import { startOfMonthMsk } from "@/lib/date-periods"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }

      try {
        const monthStart = startOfMonthMsk(new Date())
        const monthEnd = new Date()

        const users = await prisma.user.findMany({
          where: {
            isActive: true,
            sectionRoles: { some: { section: "SUPPORT" } },
          },
          select: { id: true },
        })

        const errors: Array<{ userId: string; error: string }> = []
        let upsertedCount = 0

        for (const user of users) {
          try {
            const stats = await computeManagerStatsForPeriod(user.id, monthStart, monthEnd)
            await prisma.managerSupportStats.upsert({
              where: { userId_period: { userId: user.id, period: monthStart } },
              create: {
                userId: user.id,
                period: monthStart,
                ...stats,
              },
              update: {
                ...stats,
              },
            })
            upsertedCount++
          } catch (err) {
            const msg = err instanceof Error ? err.message : "Unknown error"
            errors.push({ userId: user.id, error: msg })
            console.error(`[support-stats-refresh] user=${user.id}:`, msg)
          }
        }

        return NextResponse.json({
          ok: true,
          usersProcessed: upsertedCount,
          usersTotal: users.length,
          period: monthStart.toISOString(),
          errors: errors.length > 0 ? errors : undefined,
        })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка cron"
        console.error("[support-stats-refresh] fatal:", msg)
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    **Шаг B — заменить `tests/support-stats-cron.test.ts`** (Plan 13-01 stub → 5+ GREEN):

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    import { NextRequest } from "next/server"

    const prismaMock = {
      user: { findMany: vi.fn() },
      managerSupportStats: { upsert: vi.fn() },
      supportMessage: { findMany: vi.fn() },
      returnDecision: { count: vi.fn() },
      appealRecord: { count: vi.fn() },
      supportTicket: { count: vi.fn(), aggregate: vi.fn() },
      $queryRawUnsafe: vi.fn(),
    }
    vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

    beforeEach(() => {
      vi.resetAllMocks()
      process.env.CRON_SECRET = "test-secret"
      // Defaults that allow computeManagerStatsForPeriod to complete without errors
      prismaMock.supportMessage.findMany.mockResolvedValue([])
      prismaMock.returnDecision.count.mockResolvedValue(0)
      prismaMock.appealRecord.count.mockResolvedValue(0)
      prismaMock.$queryRawUnsafe.mockResolvedValue([{ avg_sec: null }])
      prismaMock.managerSupportStats.upsert.mockResolvedValue({})
    })

    afterEach(() => {
      delete process.env.CRON_SECRET
    })

    function createReq(secret?: string): NextRequest {
      const headers = new Headers()
      if (secret) headers.set("x-cron-secret", secret)
      return new NextRequest("http://localhost/api/cron/support-stats-refresh", { headers })
    }

    describe("GET /api/cron/support-stats-refresh", () => {
      it("401 без x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq())
        expect(res.status).toBe(401)
        const body = await res.json()
        expect(body.error).toBe("Не авторизован")
      })

      it("401 при неверном x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq("wrong-secret"))
        expect(res.status).toBe(401)
      })

      it("happy path: 3 users → 3 upserts", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([
          { id: "u-1" }, { id: "u-2" }, { id: "u-3" },
        ])
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq("test-secret"))
        expect(res.status).toBe(200)
        const body = await res.json()
        expect(body.ok).toBe(true)
        expect(body.usersProcessed).toBe(3)
        expect(prismaMock.managerSupportStats.upsert).toHaveBeenCalledTimes(3)
      })

      it("period = startOfMonthMsk(now) — 1-е число текущего месяца 00:00 МСК", async () => {
        vi.useFakeTimers()
        // 15 April 2026 14:00 UTC
        vi.setSystemTime(new Date("2026-04-15T14:00:00Z"))

        prismaMock.user.findMany.mockResolvedValueOnce([{ id: "u-1" }])
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq("test-secret"))
        const body = await res.json()

        // 1 April 2026 00:00 МСК (+03:00) = 31 March 2026 21:00 UTC
        expect(body.period).toBe("2026-03-31T21:00:00.000Z")
        const upsertCall = prismaMock.managerSupportStats.upsert.mock.calls[0][0]
        expect(upsertCall.where.userId_period.userId).toBe("u-1")
        expect((upsertCall.where.userId_period.period as Date).toISOString()).toBe("2026-03-31T21:00:00.000Z")
        expect((upsertCall.create.period as Date).toISOString()).toBe("2026-03-31T21:00:00.000Z")

        vi.useRealTimers()
      })

      it("idempotent: upsert использует @@unique ключ userId_period", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([{ id: "u-1" }])
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        await GET(createReq("test-secret"))

        const call = prismaMock.managerSupportStats.upsert.mock.calls[0][0]
        expect(call.where).toHaveProperty("userId_period")
        expect(call.where.userId_period).toHaveProperty("userId")
        expect(call.where.userId_period).toHaveProperty("period")
        // update block provided — upsert для идемпотентности
        expect(call.update).toBeDefined()
        expect(call.create).toBeDefined()
      })

      it("0 users с SUPPORT → usersProcessed=0 + ok=true", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([])
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq("test-secret"))
        expect(res.status).toBe(200)
        const body = await res.json()
        expect(body.ok).toBe(true)
        expect(body.usersProcessed).toBe(0)
        expect(prismaMock.managerSupportStats.upsert).not.toHaveBeenCalled()
      })

      it("user findMany запрошен с фильтром isActive+SUPPORT", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([])
        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        await GET(createReq("test-secret"))
        const call = prismaMock.user.findMany.mock.calls[0][0]
        expect(call.where.isActive).toBe(true)
        expect(call.where.sectionRoles).toEqual({ some: { section: "SUPPORT" } })
      })

      it("один падающий user не ломает весь cron (graceful error per-user)", async () => {
        prismaMock.user.findMany.mockResolvedValueOnce([
          { id: "u-1" }, { id: "u-2" },
        ])
        prismaMock.managerSupportStats.upsert
          .mockRejectedValueOnce(new Error("DB timeout")) // u-1 fails
          .mockResolvedValueOnce({})                       // u-2 succeeds

        const { GET } = await import("@/app/api/cron/support-stats-refresh/route")
        const res = await GET(createReq("test-secret"))
        expect(res.status).toBe(200)
        const body = await res.json()
        expect(body.ok).toBe(true)
        expect(body.usersProcessed).toBe(1)
        expect(body.usersTotal).toBe(2)
        expect(body.errors).toEqual([{ userId: "u-1", error: "DB timeout" }])
      })
    })
    ```

    **Шаг C — запустить тесты:**
    ```bash
    npm run test -- tests/support-stats-cron.test.ts
    npm run test
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-stats-cron.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/api/cron/support-stats-refresh/route.ts` exit 0
    - `grep "x-cron-secret" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "CRON_SECRET" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "computeManagerStatsForPeriod" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "startOfMonthMsk" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "managerSupportStats.upsert" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "userId_period" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "sectionRoles: { some: { section: \"SUPPORT\"" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep "maxDuration" app/api/cron/support-stats-refresh/route.ts` находит
    - `grep -c "it(" tests/support-stats-cron.test.ts` >= 5 (не it.skip)
    - `grep -c "it.skip(" tests/support-stats-cron.test.ts` == 0 (stub Plan 13-01 полностью заменён)
    - `npm run test -- tests/support-stats-cron.test.ts` exit 0 (все GREEN)
    - `npm run test` exit 0 (полная suite Phase 7-13 GREEN)
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    Cron endpoint /api/cron/support-stats-refresh готов: x-cron-secret guard, findMany SUPPORT users, per-user computeManagerStatsForPeriod + upsert с @@unique userId_period, graceful error handling, возврат usersProcessed/period/errors в JSON; 5+ GREEN тестов покрывают все ключевые сценарии; полная test suite и build проходят.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: deploy.sh — добавить systemd service + timer zoiten-stats-refresh (03:00 МСК daily)</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh (текущие паттерны systemd timer — Phase 8/9/10, строки 29-123)
    - CLAUDE.md (systemd EnvironmentFile=/etc/zoiten.pro.env паттерн)
    - .planning/phases/13-statistics/13-RESEARCH.md (строки 376-400 — OnCalendar Europe/Moscow совместимость systemd 245+)
  </read_first>
  <behavior>
    - Добавить новую секцию в deploy.sh (после существующих timer'ов Phase 8/9/10) — перед `echo "==> Building application..."` (около строки 125)
    - Создаёт `/etc/systemd/system/zoiten-stats-refresh.service` с Type=oneshot + EnvironmentFile=/etc/zoiten.pro.env + curl на http://localhost:3001/api/cron/support-stats-refresh
    - Создаёт `/etc/systemd/system/zoiten-stats-refresh.timer` с `OnCalendar=*-*-* 03:00:00 Europe/Moscow` + Persistent=true
    - systemctl daemon-reload + enable --now zoiten-stats-refresh.timer
    - echo с ✓ emoji для visibility в deploy логах
  </behavior>
  <action>
    **Шаг A — изменить `deploy.sh`** — найти строку `echo "==> Building application..."` (около строки 125) и ВСТАВИТЬ ПЕРЕД НЕЙ новый блок:

    ```bash
    # ── Phase 13: systemd timer для /api/cron/support-stats-refresh (раз в сутки 03:00 МСК) ──
    echo "==> [Phase 13] Настройка systemd timer zoiten-stats-refresh (daily 03:00 МСК)..."
    cat > /etc/systemd/system/zoiten-stats-refresh.service <<'SVC'
    [Unit]
    Description=Zoiten Support Stats Refresh (ManagerSupportStats upsert для текущего месяца)
    After=network-online.target
    Wants=network-online.target

    [Service]
    Type=oneshot
    EnvironmentFile=/etc/zoiten.pro.env
    ExecStart=/usr/bin/curl -fsS --max-time 300 -H "x-cron-secret: ${CRON_SECRET}" http://localhost:3001/api/cron/support-stats-refresh
    SVC

    cat > /etc/systemd/system/zoiten-stats-refresh.timer <<'TMR'
    [Unit]
    Description=Zoiten Support Stats Refresh (daily 03:00 Europe/Moscow)

    [Timer]
    OnCalendar=*-*-* 03:00:00 Europe/Moscow
    Persistent=true
    Unit=zoiten-stats-refresh.service

    [Install]
    WantedBy=timers.target
    TMR

    systemctl daemon-reload
    systemctl enable --now zoiten-stats-refresh.timer
    echo "✓ zoiten-stats-refresh.timer активирован (OnCalendar=*-*-* 03:00:00 Europe/Moscow)"
    ```

    **Шаг B — проверить синтаксис deploy.sh локально:**
    ```bash
    bash -n deploy.sh
    ```

    (ожидаем: no syntax errors — deploy.sh не выполняется локально, только валидация)
  </action>
  <verify>
    <automated>bash -n deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep "zoiten-stats-refresh.service" deploy.sh` находит
    - `grep "zoiten-stats-refresh.timer" deploy.sh` находит
    - `grep "OnCalendar=\*-\*-\* 03:00:00 Europe/Moscow" deploy.sh` находит (D-08 cron schedule)
    - `grep "Persistent=true" deploy.sh | wc -l` >= 4 (Phase 8/9/10 + Phase 13)
    - `grep "support-stats-refresh" deploy.sh | wc -l` >= 2 (service + curl)
    - `grep "EnvironmentFile=/etc/zoiten.pro.env" deploy.sh | wc -l` >= 4 (Phase 8/9/10/13)
    - `grep "systemctl enable --now zoiten-stats-refresh.timer" deploy.sh` находит
    - `bash -n deploy.sh` exit 0 (no syntax errors)
  </acceptance_criteria>
  <done>
    deploy.sh расширен Phase 13 блоком: systemd service + timer zoiten-stats-refresh с OnCalendar=*-*-* 03:00:00 Europe/Moscow + Persistent=true; синтаксис deploy.sh валиден; готов к применению на VPS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 3: Deploy на VPS + Human UAT — end-to-end /support/stats flow</name>
  <files>app/api/cron/support-stats-refresh/route.ts, app/(dashboard)/support/stats/page.tsx, deploy.sh, prisma/schema.prisma</files>
  <what-built>
    - Phase 13 полный комплект: ManagerSupportStats модель + 2 performance индекса + migration.sql (Plan 13-01)
    - lib/date-periods.ts (5 TZ-safe хелперов) + lib/support-stats.ts (6 aggregation helpers) (Plan 13-01)
    - /support/stats RSC страница + 7 компонентов (Plan 13-02)
    - nav-items.ts + section-titles.ts integration (Plan 13-02)
    - /api/cron/support-stats-refresh endpoint + systemd timer (Plan 13-03)
    - Все тесты GREEN: date-periods (10+), support-stats-helpers (15+), support-stats-page (5+), support-stats-cron (5+) + regression Phase 7-12
  </what-built>
  <how-to-verify>
    **Перед UAT — execute-plan выполнит deploy:**

    1. Commit всех изменений Plan 13-03 (автоматически после задач 1-2)
    2. `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — применяет migration + systemd timer + build + restart service
    3. Проверить статус: `ssh root@85.198.97.89 "systemctl is-active zoiten-erp.service zoiten-stats-refresh.timer"` — обе active
    4. Проверить миграция применена: `ssh root@85.198.97.89 "sudo -u postgres psql zoiten_erp -c \"\\dt ManagerSupportStats\""` — таблица существует
    5. Manual cron trigger один раз: `ssh root@85.198.97.89 "curl -s -H 'x-cron-secret: \$(grep CRON_SECRET /etc/zoiten.pro.env | cut -d= -f2-)' http://localhost:3001/api/cron/support-stats-refresh"` → response содержит `{ ok: true, usersProcessed: N }`
    6. Проверить логи: `ssh root@85.198.97.89 "journalctl -u zoiten-stats-refresh.service --since '5 min ago'"` — видна успешная запись

    **Human UAT checklist** (выполнить в браузере на https://zoiten.pro):

    **Подготовка:**
    - [ ] Залогинен суперадмином (sergey.fyodorov@gmail.com) ИЛИ пользователем с SUPPORT VIEW или MANAGE
    - [ ] Существуют данные в БД для агрегаций: отзывы (Phase 8), вопросы, чаты (Phase 10), возвраты (Phase 9), обжалования (Phase 11) — большинство есть из предыдущих UAT

    **Раздел /support/stats — базовый рендер:**
    - [ ] Открыть https://zoiten.pro/support/stats — страница рендерится без ошибок
    - [ ] Header title = «Статистика службы поддержки»
    - [ ] Sidebar содержит пункт «Статистика» с иконкой BarChart3
    - [ ] По умолчанию активен таб «По товарам», период = 30 дней
    - [ ] Видны 3 summary cards сверху: Всего отзывов, Всего возвратов, Средний рейтинг
    - [ ] Виден блок TopReturnReasonsList (или «Нет отклонённых возвратов за период» если пусто)
    - [ ] Видна таблица товаров с 10 колонками

    **Фильтр периода (SUP-36):**
    - [ ] Клик «7 дней» в select → «Применить» → URL меняется на ?period=7d → цифры обновляются
    - [ ] Клик «30 дней» → Применить → ?period=30d
    - [ ] Клик «Квартал (календарный)» → Применить → ?period=quarter → dateFrom соответствует 1 Jan/Apr/Jul/Oct (проверить через DevTools network → query params или сравнить начало периода в summary)
    - [ ] Клик «Кастом» → появляются 2 date-input поля → выбрать dateFrom=1 Apr, dateTo=10 Apr → Применить → URL ?period=custom&dateFrom=2026-04-01&dateTo=2026-04-10
    - [ ] Custom без дат → кнопка «Применить» disabled

    **Табы (SUP-36):**
    - [ ] Клик «По менеджерам» → URL ?tab=managers → период сохраняется → видны 3 summary: Всего обработано, Ср. время, Автоответов
    - [ ] Видна таблица менеджеров с колонками Имя / Live / Всего / Отзывы / Вопросы / Чаты / Возвраты / % одобр. / Обжалования / Ср. время
    - [ ] Менеджеры с активностью в текущем месяце имеют бейдж «сегодня» (зелёный)
    - [ ] Клик «По товарам» → URL ?tab=products → контент меняется

    **Метрики по товарам (SUP-37):**
    - [ ] Топ-1 товар в таблице имеет максимальное число отзывов (DESC sort)
    - [ ] Числа совпадают с простым ручным подсчётом через /support ленту (клик на пример nmId → увидеть число FEEDBACK/RETURN тикетов за период)
    - [ ] Для товара без рейтинга колонка показывает «—»
    - [ ] % ответов = кол-во ANSWERED / всего отзывов * 100 (выборочная проверка 1-2 nmId)
    - [ ] Ср. время в формате «X сек» / «X мин» / «X.X ч»
    - [ ] TopReturnReasonsList показывает top-10 причин из ReturnDecision где action=REJECT, отсортировано DESC по count (D-03 глобально — НЕ per-product)

    **Метрики по менеджерам (SUP-38):**
    - [ ] AutoRepliesSummary card показывает глобальный счётчик автоответов (D-02 — отдельная карточка, НЕ колонка в таблице)
    - [ ] Менеджер с >0 ответами OUTBOUND !isAutoReply → totalProcessed > 0
    - [ ] totalProcessed = feedbacks + questions + chats + returns + appeals (D-04 outcome-actions, НЕ включает status changes/assignments)
    - [ ] % одобрения = returnsApproved / returnsDecided * 100 (для менеджера с 0 возвратов — «—»)
    - [ ] Live badge только у менеджеров с активностью В ТЕКУЩЕМ месяце

    **Cron (SUP-39):**
    - [ ] `ssh root@85.198.97.89 "systemctl list-timers zoiten-stats-refresh.timer"` показывает Next trigger 03:00 следующего дня по МСК
    - [ ] Manual trigger из шага 5 подготовки — успешно вернул JSON
    - [ ] Проверить БД: `ssh root@85.198.97.89 "sudo -u postgres psql zoiten_erp -c \"SELECT \\\"userId\\\", period, \\\"totalProcessed\\\", \\\"updatedAt\\\" FROM \\\"ManagerSupportStats\\\" ORDER BY \\\"updatedAt\\\" DESC LIMIT 10\""` — видны записи с period=1-е число текущего месяца по МСК
    - [ ] Повторный manual trigger — БД содержит те же row-ы (upsert не дублирует), updatedAt обновлён

    **Live current month (SUP-38 + ROADMAP SC#5):**
    - [ ] Отправить ответ на какой-то FEEDBACK через /support/[id] прямо сейчас (требует SUPPORT MANAGE)
    - [ ] Обновить /support/stats?tab=managers → число feedbacksAnswered у вашего user должно увеличиться на 1 БЕЗ ожидания ночного cron (live-расчёт через computeManagerStatsForPeriod)

    **RBAC (D-10 — SUPPORT VIEW без MANAGE):**
    - [ ] Залогиниться под VIEWER с sectionRoles: SUPPORT=VIEW → /support/stats открывается и рендерится
    - [ ] Залогиниться под пользователем БЕЗ SUPPORT role → /support/stats → redirect на /unauthorized (middleware)

    **Календарный квартал (D-05):**
    - [ ] Выбрать «Квартал (календарный)» при текущей дате апрель 2026 → dateFrom должен быть 1 Apr 00:00 МСК (Q2=Apr-Jun), НЕ ~90 дней назад
    - [ ] Если тест в feb/mar → dateFrom = 1 Jan (Q1); may-jun → 1 Apr (Q2); jul-sep → 1 Jul (Q3); oct-dec → 1 Oct (Q4)

    **Регрессия Phase 7-12:**
    - [ ] /support (лента) работает
    - [ ] /support/[ticketId] (диалог) работает для всех каналов
    - [ ] /support/returns, /support/templates, /support/auto-reply, /support/customers/[id], /support/new — все работают
    - [ ] /prices/wb работает
    - [ ] /products работает
    - [ ] Cron z cмежных фаз (chat-sync 5min, support-sync 15min, returns-sync 15min) работают — `systemctl list-timers | grep zoiten`

    **ROADMAP update:**
    - [ ] Обновить .planning/ROADMAP.md — Phase 13 строка в Progress table: `0/3 | Planned | ` → `3/3 | Complete   | 2026-04-XX`
    - [ ] Обновить .planning/REQUIREMENTS.md — SUP-36, SUP-37, SUP-38, SUP-39 статус Pending → Complete
    - [ ] Обновить .planning/STATE.md — status = Ready to execute + milestone v1.1 = Complete (все 6 фаз)
  </how-to-verify>
  <resume-signal>
    Пользователь отправляет:
    - "approved" — все пункты UAT OK, Phase 13 complete, milestone v1.1 complete
    - "issues: [список проблем]" — зафиксировать gap-ы для `--gaps` replan
    - "partial: [работает / не работает]" — создать gap-closure plan
  </resume-signal>
  <action>
    Deploy и human UAT не автоматизируются полностью — execute-phase выполнит:
    1. git commit изменений (после задач 1-2 Plan 13-03)
    2. `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`
    3. Smoke check: curl на /api/cron/support-stats-refresh с CRON_SECRET → проверить 200 OK
    4. Вывести пользователю чеклист из <how-to-verify> для ручной верификации в браузере
    5. Ждать resume-signal
  </action>
  <verify>
    <automated>ssh root@85.198.97.89 "systemctl is-active zoiten-erp.service && systemctl is-active zoiten-stats-refresh.timer"</automated>
  </verify>
  <done>
    Deploy прошёл (service + stats-refresh timer active на VPS); пользователь отправил "approved" после прохождения всех пунктов UAT; ROADMAP + REQUIREMENTS + STATE обновлены с Phase 13 Complete; milestone v1.1 полностью закрыт.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач (включая human UAT):
1. `npx prisma validate` exit 0
2. `npx tsc --noEmit` exit 0
3. `npm run build` exit 0
4. `npm run test` exit 0 (полная suite Phase 7-13 GREEN, 0 failed)
5. `grep "support-stats-refresh" app/api/cron/support-stats-refresh/route.ts | wc -l` >= 1
6. `grep "zoiten-stats-refresh.timer" deploy.sh | wc -l` >= 1
7. Human UAT passed (resume-signal "approved")
8. VPS: `systemctl is-active zoiten-erp.service zoiten-stats-refresh.timer` — обе active
9. VPS БД: ManagerSupportStats содержит хотя бы 1 запись после manual trigger
10. ROADMAP Phase 13 = Complete; milestone v1.1 complete
</verification>

<success_criteria>
Plan 13-03 завершён когда:
- /api/cron/support-stats-refresh endpoint реализует upsert per SUPPORT user по @@unique([userId, period])
- 5+ GREEN тестов покрывают CRON_SECRET guard, happy path, period=startOfMonthMsk, idempotency, 0 users, per-user error resilience
- deploy.sh содержит systemd service+timer zoiten-stats-refresh с OnCalendar=*-*-* 03:00:00 Europe/Moscow (D-08)
- Deploy на VPS: миграция Plan 13-01 применена, stats-refresh timer активирован, service рестартован
- Human UAT passed: все 30+ пунктов чеклиста (рендер, фильтры, табы, метрики, cron, live month, RBAC, календарный квартал, регрессия)
- ROADMAP Phase 13 Complete + milestone v1.1 Complete
- Commit `feat(13-03): cron support-stats-refresh + systemd timer 03:00 МСК + Phase 13 complete`
</success_criteria>

<output>
После завершения создать `.planning/phases/13-statistics/13-03-SUMMARY.md` с:
- /api/cron/support-stats-refresh контракт — x-cron-secret, upsert per user с @@unique userId_period, graceful per-user errors
- deploy.sh diff — новый блок Phase 13 systemd service+timer
- Test coverage: 5+ GREEN cron-тестов
- Deploy results: systemctl is-active + ManagerSupportStats rows count + manual trigger JSON response
- UAT checklist outcome: passed/issues
- Phase 13 COMPLETE marker
- milestone v1.1 «Служба поддержки WB» — COMPLETE (все 6 фаз: 8, 9, 10, 11, 12, 13)
- Known limitations / deferred (из CONTEXT):
  - Нет backfill за past months (D-09 — приемлемо, начинаем с current month)
  - Нет графиков / recharts (D-07 MVP — deferred v1.2)
  - Per-product top reasons deferred (D-03 MVP глобально)
  - Per-manager auto-replies attribution deferred (D-02 MVP глобально, authorId=null в БД)
  - RETURN avg response time deferred (D-01 — Phase 9 не создаёт OUTBOUND SupportMessage)
- Next steps (v1.2 backlog): графики (recharts), drill-down (клик на цифру → список тикетов), сравнение периодов (WoW/MoM), backfill past months script
</output>
</context>
