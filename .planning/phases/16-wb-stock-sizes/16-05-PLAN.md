---
phase: 16-wb-stock-sizes
plan: 05
type: execute
wave: 3
depends_on:
  - "01"
  - "03"
files_modified:
  - components/stock/StockWbTable.tsx
autonomous: true
requirements:
  - STOCK-36
gap_closure: false
must_haves:
  truths:
    - "interface Props в StockWbTable.tsx расширен полем initialShowSizes: boolean"
    - "Кнопка «По размерам» отображена в верхней панели рядом с «Без СЦ» / «Склады»"
    - "Кнопка использует optimistic update (useState + useTransition) → saveStockWbShowSizes"
    - "Под per-nmId строкой при showSizes && card.hasMultipleSizes рендерятся card.sizeBreakdown.map(...) строки"
    # W10: UI-truth (соответствует implementation-truth в Plan 16-03)
    - "Размерная строка скрыта (НЕ рендерится) если card.hasMultipleSizes === false — even when showSizes=true"
    - "Размерная строка имеет приглушённый bg-muted/30 фон"
    - "Размерная строка показывает '↳ {techSize}' в Артикул-колонке"
    - "Размерная строка показывает placeholder '—' для Иваново и in-way (per-size данных нет)"
    - "Размерная строка имеет полную структуру кластерных колонок О/З/Об/Д идентичную per-nmId"
    - "rowSpan для Фото/Сводка/Иваново пересчитан с учётом размерных строк"
    - "Per-cluster агрегаты в размерной строке не зависят от hideSc / hiddenWarehouseIds (visual filter only)"
    - "При expand кластера размерная строка рендерит per-warehouse колонки только видимых складов (как nmId-row)"
  artifacts:
    - path: "components/stock/StockWbTable.tsx"
      provides: "Кнопка «По размерам» + рендер размерных строк"
      contains: "initialShowSizes"
  key_links:
    - from: "components/stock/StockWbTable.tsx Button «По размерам»"
      to: "app/actions/stock-wb.ts saveStockWbShowSizes"
      via: "client useTransition → server action"
      pattern: "saveStockWbShowSizes"
    - from: "card.sizeBreakdown[]"
      to: "WarehouseSizeRow render block"
      via: "{showSizes && card.hasMultipleSizes && card.sizeBreakdown.map(...)}"
      pattern: "sizeBreakdown\\.map"
---

<objective>
Расширить `components/stock/StockWbTable.tsx` для UI размерной разбивки:

1. **Расширить `interface Props`** полем `initialShowSizes: boolean`
2. **Добавить state** `const [showSizes, setShowSizes] = useState(initialShowSizes)`
3. **Добавить кнопку «По размерам»** в верхнюю toolbar-панель (рядом с «Без СЦ»,
   «Склады»). Optimistic update через `useTransition` → `saveStockWbShowSizes`
4. **Рендер размерных строк** под per-nmId строкой при `showSizes && card.hasMultipleSizes`:
   - `card.sizeBreakdown.map(sizeRow => ...)` итерация
   - Приглушённый `bg-muted/30` фон + `border-t border-t-border/40` (intra-group)
   - Sticky Артикул-колонка показывает `↳ {sizeRow.techSize}` с padding-left
   - Иваново / Всего на WB / в пути cells: placeholder `—`
   - Итого склады WB: О из `sizeRow.totalStock`, З=`—` (per-size orders нет),
     Об=`—`, Д=`—`
   - Кластеры: точно тот же flatMap по `CLUSTER_ORDER` что и в per-nmId row,
     **но с `sizeRow.clusters[cluster]` вместо `card.clusters[cluster]`**
5. **Пересчёт rowSpan** для sticky Фото/Сводка/Иваново:
   ```typescript
   const totalSizeRows = showSizes
     ? g.wbCards.reduce((acc, c) => acc + (c.hasMultipleSizes ? c.sizeBreakdown.length : 0), 0)
     : 0
   const rowSpan = 1 + g.wbCards.length + totalSizeRows
   ```

**Покрывает:** STOCK-36.

**Не делает:** server action (16-04), schema (16-01), data layer (16-03), sync
(16-02), deploy/UAT (16-06).

**Параллельно с Plan 16-04** — разные файлы. 04 trogает actions+page; 05 trogает
только StockWbTable. Контракт `initialShowSizes` prop задан в 04.

Purpose: Главная UI-фича Phase 16 — пользователь может включить размерные строки
и видеть полный per-cluster + per-warehouse breakdown, persisted per-user.

Output: Полностью функциональная UI с кнопкой и размерными строками; manual UAT
покажет визуальное поведение в Plan 16-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-03-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-04-SUMMARY.md

<interfaces>
<!-- Текущий components/stock/StockWbTable.tsx — interface Props -->

From `components/stock/StockWbTable.tsx:35-40`:
```typescript
interface Props {
  groups: ProductWbGroup[]
  turnoverNormDays: number
  clusterWarehouses: StockWbDataResult["clusterWarehouses"]
  hiddenWarehouseIds: number[]
  // Phase 16: добавляется initialShowSizes: boolean
}
```

<!-- Текущий toolbar (строки ~144-163) -->

From `components/stock/StockWbTable.tsx:144-163`:
```tsx
<div className="h-full flex flex-col gap-3">
  <div className="flex gap-2 items-center shrink-0">
    <Button variant="ghost" size="sm" onClick={expandAll}>Развернуть все</Button>
    <Button variant="ghost" size="sm" onClick={collapseAll}>Свернуть все</Button>
    <span className="text-muted-foreground mx-2">·</span>
    <Button
      variant={hideSc ? "default" : "outline"}
      size="sm"
      onClick={toggleHideSc}
      title={hideSc ? "Сейчас: только основные склады..." : "..."}
    >
      {hideSc ? "Без СЦ" : "Все склады"}
    </Button>
    <WarehouseVisibilityPopover ... />
    {/* Phase 16 — здесь же добавится Кнопка «По размерам» */}
  </div>
```

<!-- Текущий rowSpan расчёт -->

From `components/stock/StockWbTable.tsx:351`:
```typescript
const rowSpan = 1 + g.wbCards.length
// Phase 16: пересчёт через showSizes && hasMultipleSizes
```

<!-- Текущий рендер per-nmId rows (строки ~498-555) -->

```tsx
{g.wbCards.map((card) => {
  const cardMetrics = calculateStockMetrics({...})
  const cardThreshold = deficitThreshold(...)
  return (
    <TableRow key={card.wbCardId} className="border-t border-t-border/60">
      {/* Артикул-колонка */}
      <TableCell className="sticky left-[320px] z-20 bg-background border-r ...">
        {card.nmId}
      </TableCell>
      {/* Иваново — пусто */}
      <TableCell className="... text-muted-foreground">—</TableCell>
      {/* in-way + Всего на WB */}
      {/* ... */}
      {/* Итого склады WB О/З/Об/Д */}
      <StockCell value={card.totalStock} />
      <StockCell value={card.avgSalesSpeed7d} />
      <IntCell value={cardMetrics.turnoverDays} />
      <DeficitCell deficit={cardMetrics.deficit} threshold={cardThreshold} />
      {/* Кластеры */}
      {CLUSTER_ORDER.flatMap((cluster) => {
        // collapsed: О/З/Об/Д сводно по кластеру
        // expanded: per-warehouse cells
      })}
    </TableRow>
    {/* Phase 16: после этой строки рендерим размерные строки если applicable */}
  )
})}
```

<!-- Расширенный WbStockRow (после Plan 16-03) -->

From `lib/stock-wb-data.ts` (после 16-03):
```typescript
export interface WbStockRow {
  // ... existing fields ...
  sizeBreakdown: WbStockSizeRow[]
  hasMultipleSizes: boolean
}

export interface WbStockSizeRow {
  techSize: string
  totalStock: number | null
  clusters: Record<ClusterShortName, ClusterAggregate>
}
```

<!-- saveStockWbShowSizes (после Plan 16-04) -->

```typescript
import { saveStockWbShowSizes } from "@/app/actions/stock-wb"
// signature: (value: boolean) => Promise<{ ok: true } | { ok: false; error: string }>
```
</interfaces>

# Дизайн-замечания (locked в CONTEXT.md + RESEARCH.md)

- **Стиль строки:** `bg-muted/30` приглушённый фон, `border-t border-t-border/40`
  (intra-group thin border)
- **Префикс размера:** `↳ {techSize}` в Артикул-колонке, padding-left чтобы отступ
- **One-size hide:** если `card.hasMultipleSizes === false` (только 1 техразмер,
  обычно `"0"`), размерная строка НЕ рендерится — даже когда `showSizes=true`
- **per-cluster агрегаты:** `sizeRow.clusters[shortCluster]` уже посчитаны pure
  helper'ом `buildSizeBreakdown` (Plan 16-03) — UI просто использует их
- **hideSc / hiddenWarehouseIds:** применяются к **видимым складам в expanded
  колонках**, НЕ к агрегатам. Идентично per-nmId логике (locked)
- **Z колонка размерной строки:** `null` (per-size orders не хранятся в БД).
  UI показывает `—`. Соответствует locked default-гипотезе CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Расширить interface Props + state + кнопка «По размерам»</name>
  <files>components/stock/StockWbTable.tsx</files>
  <read_first>
    - `components/stock/StockWbTable.tsx:1-200` (от import до toolbar)
    - `components/stock/WarehouseVisibilityPopover.tsx` (паттерн optimistic + useTransition)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Optimistic toggle button» (строки 925-953)
  </read_first>
  <action>
    Использовать Edit tool — точечные замены.

    **Изменение 1** — добавить import `saveStockWbShowSizes`:

    Найти:
    ```typescript
    import { calculateStockMetrics, deficitThreshold } from "@/lib/stock-math"
    import { CLUSTER_ORDER, type ClusterShortName } from "@/lib/wb-clusters"
    import { ClusterTooltip } from "./ClusterTooltip"
    import { WarehouseVisibilityPopover } from "./WarehouseVisibilityPopover"
    import type { ProductWbGroup, StockWbDataResult } from "@/lib/stock-wb-data"
    ```

    Заменить (добавить новые импорты):
    ```typescript
    import { useTransition } from "react"
    // (оставить React, useCallback, useState уже импортированы выше)
    import { calculateStockMetrics, deficitThreshold } from "@/lib/stock-math"
    import { CLUSTER_ORDER, type ClusterShortName } from "@/lib/wb-clusters"
    import { ClusterTooltip } from "./ClusterTooltip"
    import { WarehouseVisibilityPopover } from "./WarehouseVisibilityPopover"
    import type { ProductWbGroup, StockWbDataResult, WbStockSizeRow } from "@/lib/stock-wb-data"
    import { saveStockWbShowSizes } from "@/app/actions/stock-wb"
    ```

    Замечание: `useTransition` могло уже быть в существующем импорте `import React, { useCallback, useState } from "react"`. Проверить и привести к виду:
    ```typescript
    import React, { useCallback, useState, useTransition } from "react"
    ```

    **Изменение 2** — расширить `interface Props`:

    Найти:
    ```typescript
    interface Props {
      groups: ProductWbGroup[]
      turnoverNormDays: number
      clusterWarehouses: StockWbDataResult["clusterWarehouses"]
      hiddenWarehouseIds: number[]
    }
    ```

    Заменить:
    ```typescript
    interface Props {
      groups: ProductWbGroup[]
      turnoverNormDays: number
      clusterWarehouses: StockWbDataResult["clusterWarehouses"]
      hiddenWarehouseIds: number[]
      // Phase 16 (STOCK-36): per-user toggle кнопки «По размерам»
      initialShowSizes: boolean
    }
    ```

    **Изменение 3** — расширить сигнатуру компонента:

    Найти:
    ```typescript
    export function StockWbTable({ groups, turnoverNormDays, clusterWarehouses, hiddenWarehouseIds }: Props) {
    ```

    Заменить:
    ```typescript
    export function StockWbTable({ groups, turnoverNormDays, clusterWarehouses, hiddenWarehouseIds, initialShowSizes }: Props) {
    ```

    **Изменение 4** — добавить state + handler ПОСЛЕ существующих state hooks
    (после `const [hiddenIds, setHiddenIds] = useState<number[]>(hiddenWarehouseIds)`
    или эквивалента):

    Найти строку:
    ```typescript
      const [hiddenIds, setHiddenIds] = useState<number[]>(hiddenWarehouseIds)
      const hiddenSet = new Set(hiddenIds)
    ```

    Добавить ПОСЛЕ:
    ```typescript
      // Phase 16 (STOCK-36): toggle «По размерам» — optimistic + persist в БД
      const [showSizes, setShowSizes] = useState<boolean>(initialShowSizes)
      const [isShowSizesPending, startShowSizesTransition] = useTransition()

      const toggleShowSizes = useCallback(() => {
        const next = !showSizes
        setShowSizes(next) // optimistic
        startShowSizesTransition(async () => {
          const res = await saveStockWbShowSizes(next)
          if (!res.ok) {
            // Не откатываем — следующий revalidate синхронизирует из БД
            console.error("Не удалось сохранить «По размерам»:", res.error)
          }
        })
      }, [showSizes])
    ```

    **Изменение 5** — добавить кнопку в toolbar:

    Найти блок toolbar (около строки 146-163):
    ```tsx
        <Button
          variant={hideSc ? "default" : "outline"}
          size="sm"
          onClick={toggleHideSc}
          title={hideSc ? "Сейчас: только основные склады..." : "..."}
        >
          {hideSc ? "Без СЦ" : "Все склады"}
        </Button>
        <WarehouseVisibilityPopover
          clusterWarehouses={clusterWarehouses}
          hiddenIds={hiddenIds}
          onChange={setHiddenIds}
        />
      </div>
    ```

    Добавить НОВУЮ кнопку ПОСЛЕ `WarehouseVisibilityPopover`:
    ```tsx
        <WarehouseVisibilityPopover
          clusterWarehouses={clusterWarehouses}
          hiddenIds={hiddenIds}
          onChange={setHiddenIds}
        />
        <Button
          variant={showSizes ? "default" : "outline"}
          size="sm"
          onClick={toggleShowSizes}
          disabled={isShowSizesPending}
          title={
            showSizes
              ? "Сейчас: размерные строки видны под каждым артикулом. Нажать — скрыть."
              : "Сейчас: размерные строки скрыты. Нажать — показать per-size разбивку."
          }
        >
          По размерам
        </Button>
      </div>
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('components/stock/StockWbTable.tsx','utf-8'); if(!c.includes('initialShowSizes: boolean')) throw 'no Props extension'; if(!c.includes('saveStockWbShowSizes')) throw 'no action import'; if(!c.includes('useTransition')) throw 'no useTransition'; if(!c.includes('toggleShowSizes')) throw 'no handler'; if(!c.includes('По размерам')) throw 'no button label'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initialShowSizes: boolean" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "saveStockWbShowSizes" components/stock/StockWbTable.tsx` >= 2 (import + использование)
    - `grep -c "useTransition" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "showSizes" components/stock/StockWbTable.tsx` >= 5 (state + handler + render + Button + import-derived) — проверка что используется в нескольких местах
    - `grep -c "По размерам" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "WbStockSizeRow" components/stock/StockWbTable.tsx` >= 1 (тип импортирован для следующего task)
    - `grep -c "isShowSizesPending" components/stock/StockWbTable.tsx` >= 1
  </acceptance_criteria>
  <done>
    Кнопка «По размерам» работает в toolbar; нажатие optimistic меняет state +
    persist в БД через server action. Если есть ошибки persist — игнорируются (revalidate
    исправит при следующем рендере). Готов к Task 2 (рендер размерных строк).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap per-nmId TableRow в React.Fragment + пересчёт rowSpan</name>
  <files>components/stock/StockWbTable.tsx</files>
  <read_first>
    - `components/stock/StockWbTable.tsx:340-360` (rowSpan computation)
    - `components/stock/StockWbTable.tsx:499-610` (g.wbCards.map → TableRow per-nmId → закрытие React.Fragment)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 8: UI рендер размерных строк» (строки 553-589)
  </read_first>
  <action>
    **B5: Этот task делает ТОЛЬКО структурные изменения** — пересчёт rowSpan и
    оборачивание per-nmId `<TableRow>` в `<React.Fragment>` с правильно
    балансированными скобками. Сами размерные строки добавятся в Task 3.

    **Изменение 1** — пересчёт `rowSpan` (строка ~351):

    ИСХОДНЫЙ блок:
    ```typescript
            {groups.map((g, idx) => {
              const rowSpan = 1 + g.wbCards.length
    ```

    ЦЕЛЕВОЙ блок (заменить):
    ```typescript
            {groups.map((g, idx) => {
              // Phase 16 (STOCK-36): rowSpan учитывает размерные строки если showSizes ON
              const totalSizeRows = showSizes
                ? g.wbCards.reduce(
                    (acc, c) => acc + (c.hasMultipleSizes ? c.sizeBreakdown.length : 0),
                    0,
                  )
                : 0
              const rowSpan = 1 + g.wbCards.length + totalSizeRows
    ```

    **Изменение 2** — обернуть per-nmId `<TableRow>` в `<React.Fragment>` (строки ~499-609).

    ИСХОДНЫЙ блок (фрагмент с map callback):
    ```tsx
                  {g.wbCards.map((card) => {
                    const cardMetrics = calculateStockMetrics({
                      stock: card.totalStock,
                      ordersPerDay: card.avgSalesSpeed7d,
                      turnoverNormDays,
                    })
                    const cardThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)

                    return (
                      <TableRow key={card.wbCardId} className="border-t border-t-border/60">
    ```

    ЦЕЛЕВОЙ блок (заменить):
    ```tsx
                  {g.wbCards.map((card) => {
                    const cardMetrics = calculateStockMetrics({
                      stock: card.totalStock,
                      ordersPerDay: card.avgSalesSpeed7d,
                      turnoverNormDays,
                    })
                    const cardThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)

                    return (
                      <React.Fragment key={card.wbCardId}>
                        <TableRow className="border-t border-t-border/60">
    ```

    **ВАЖНО:** `key={card.wbCardId}` ПЕРЕЕЗЖАЕТ с `<TableRow>` на `<React.Fragment>`.
    На `<TableRow>` остаётся только `className`. Это требование React: `key` должен
    быть на корневом возвращаемом элементе, а после оборачивания корнем стал Fragment.

    **Изменение 3** — закрыть Fragment ПОСЛЕ `</TableRow>` (строки ~607-608).

    ИСХОДНЫЙ блок:
    ```tsx
                            <DeficitCell key={`${card.wbCardId}-${cluster}-d`} deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
                          ]
                        })}
                      </TableRow>
                    )
                  })}
                </React.Fragment>
    ```

    ЦЕЛЕВОЙ блок (заменить — добавляет `</React.Fragment>` ПОСЛЕ `</TableRow>`,
    оставляет TODO-маркер для Task 3, ВНЕШНИЙ `</React.Fragment>` (от `g.productId`)
    остаётся как есть):
    ```tsx
                            <DeficitCell key={`${card.wbCardId}-${cluster}-d`} deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
                          ]
                        })}
                      </TableRow>
                      {/* Task 3 (B5): здесь будет блок размерных строк */}
                    </React.Fragment>
                    )
                  })}
                </React.Fragment>
    ```

    **ВАЖНО — балансировка скобок (B5 root cause):**
    - Внутренний `<React.Fragment key={card.wbCardId}>` ОТКРЫВАЕТСЯ в Изменении 2
    - Внутренний `</React.Fragment>` ЗАКРЫВАЕТСЯ в Изменении 3 ПОСЛЕ `</TableRow>`
    - Внешний `<React.Fragment key={g.productId}>` (строка ~402) и его `</React.Fragment>`
      (строка ~610) — НЕ ТРОГАТЬ
    - Закрывающий `)})` (line 611) — НЕ ТРОГАТЬ
    - После задачи структура: `g.wbCards.map → React.Fragment(card) → TableRow → /TableRow → comment-маркер → /React.Fragment(card) → )})`

    После завершения этого task'а `npx tsc --noEmit` должен быть зелёным
    (Fragment wrap + rowSpan recalc — самодостаточные изменения, размерных строк
    ещё нет, но это валидно).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('components/stock/StockWbTable.tsx','utf-8'); if(!c.includes('totalSizeRows')) throw 'no rowSpan recalc'; if(!c.includes('1 + g.wbCards.length + totalSizeRows')) throw 'no new rowSpan formula'; if(!/<React\.Fragment key={card\.wbCardId}>/.test(c)) throw 'no inner Fragment wrap'; if(/<TableRow key={card\.wbCardId}/.test(c)) throw 'key still on TableRow (must be on Fragment)'; if(!c.includes('Task 3 (B5): здесь будет блок размерных строк')) throw 'no Task 3 marker'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "totalSizeRows" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "1 + g.wbCards.length + totalSizeRows" components/stock/StockWbTable.tsx` >= 1 (новая формула rowSpan)
    - `grep -cE "const rowSpan = 1 \+ g\.wbCards\.length$" components/stock/StockWbTable.tsx` == 0 (старая формула без totalSizeRows удалена)
    - `grep -cE "<React\.Fragment key=\{card\.wbCardId\}>" components/stock/StockWbTable.tsx` == 1 (внутренний Fragment добавлен)
    - `grep -cE "<TableRow key=\{card\.wbCardId\}" components/stock/StockWbTable.tsx` == 0 (key переехал с TableRow на Fragment)
    - `grep -c "Task 3 (B5): здесь будет блок размерных строк" components/stock/StockWbTable.tsx` == 1 (TODO-маркер для Task 3)
    - `grep -cE "</React\.Fragment>" components/stock/StockWbTable.tsx` == 2 (внутренний за TableRow + внешний за g.wbCards.map)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Per-nmId `<TableRow>` обёрнут в `<React.Fragment key={card.wbCardId}>`, ключ
    переехал на Fragment, rowSpan учитывает размерные строки, TODO-маркер
    помечает место для Task 3. TypeScript зелёный, JSX балансирован.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render размерных строк внутри Fragment (после </TableRow>)</name>
  <files>components/stock/StockWbTable.tsx</files>
  <read_first>
    - `components/stock/StockWbTable.tsx` (целиком — после Task 2 структура: Fragment(card) → TableRow → comment-marker → /Fragment)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 8: UI рендер размерных строк» (строки 553-589)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pitfall 5: Sticky cells overlap» (строки 845-862)
  </read_first>
  <action>
    **B5 Task 3:** Заменить TODO-маркер из Task 2 на полный JSX блок размерных строк.

    ИСХОДНЫЙ блок (точная строка из Task 2):
    ```tsx
                      {/* Task 3 (B5): здесь будет блок размерных строк */}
    ```

    ЦЕЛЕВОЙ блок (заменить — внутри уже-открытого Fragment, до `</React.Fragment>`):
    ```tsx
                      {/* Phase 16 (STOCK-36): размерные строки под per-nmId */}
                      {showSizes && card.hasMultipleSizes && card.sizeBreakdown.map((sizeRow) => (
                        <TableRow
                          key={`${card.wbCardId}-size-${sizeRow.techSize}`}
                          className="border-t border-t-border/40 bg-muted/30"
                        >
                          {/* Артикул-колонка: ↳ {techSize} */}
                          <TableCell className="sticky left-[320px] z-20 bg-muted/30 border-r w-24 min-w-24 max-w-24 text-xs tabular-nums">
                            <span className="text-muted-foreground pl-3">↳ {sizeRow.techSize || "—"}</span>
                          </TableCell>
                          {/* Иваново — placeholder */}
                          <TableCell className="px-2 py-1 h-8 text-xs text-right border-r text-muted-foreground w-20 min-w-20 max-w-20">—</TableCell>
                          {/* Всего на WB — sizeRow.totalStock (без in-way) */}
                          <TableCell className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right border-r">
                            {sizeRow.totalStock !== null ? formatInt(sizeRow.totalStock) : <span className="text-muted-foreground">—</span>}
                          </TableCell>
                          {/* Товар в пути — placeholder × 3 (per-size in-way не хранится) */}
                          <TableCell className="px-2 py-1 h-8 text-xs text-right text-muted-foreground">—</TableCell>
                          <TableCell className="px-2 py-1 h-8 text-xs text-right text-muted-foreground">—</TableCell>
                          <TableCell className="px-2 py-1 h-8 text-xs text-right text-muted-foreground border-r">—</TableCell>
                          {/* Итого склады WB О/З/Об/Д — З=null → metrics все null */}
                          <StockCell value={sizeRow.totalStock} />
                          <StockCell value={null} />
                          <IntCell value={null} />
                          <DeficitCell deficit={null} threshold={null} />
                          {/* Кластеры — то же flatMap по CLUSTER_ORDER, но через sizeRow.clusters */}
                          {CLUSTER_ORDER.flatMap((cluster) => {
                            const isExpanded = expandedSet.has(cluster)
                            const visibleWarehouses = visibleClusterWarehouses[cluster as ClusterShortName] ?? []
                            const sizeClusterAgg = sizeRow.clusters[cluster as ClusterShortName]
                            if (isExpanded) {
                              if (visibleWarehouses.length === 0) {
                                return [
                                  <TableCell key={`${cluster}-size-empty`} colSpan={4} className="px-2 py-1 h-8 text-xs text-right text-muted-foreground border-r">—</TableCell>,
                                ]
                              }
                              return visibleWarehouses.flatMap((w, idx) => {
                                const isLast = idx === visibleWarehouses.length - 1
                                const borderClass = isLast ? "border-r" : "border-r border-r-border/40"
                                const slot = sizeClusterAgg.warehouses.find((slotW) => slotW.warehouseId === w.warehouseId)
                                const slotQty = slot?.quantity ?? 0
                                return [
                                  <StockCell key={`${cluster}-size-${w.warehouseId}-o`} value={slotQty} />,
                                  <StockCell key={`${cluster}-size-${w.warehouseId}-z`} value={null} />,
                                  <IntCell key={`${cluster}-size-${w.warehouseId}-ob`} value={null} />,
                                  <TableCell
                                    key={`${cluster}-size-${w.warehouseId}-d`}
                                    className={cn(
                                      "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right text-muted-foreground",
                                      borderClass,
                                    )}
                                  >—</TableCell>,
                                ]
                              })
                            }
                            return [
                              <StockCell key={`${cluster}-size-o`} value={sizeClusterAgg.totalStock ?? null} />,
                              <StockCell key={`${cluster}-size-z`} value={null} />,
                              <IntCell key={`${cluster}-size-ob`} value={null} />,
                              <DeficitCell key={`${cluster}-size-d`} deficit={null} threshold={null} />,
                            ]
                          })}
                        </TableRow>
                      ))}
    ```

    **B5 ВАЖНО — балансировка скобок:**
    - Открывающие: `{showSizes && card.hasMultipleSizes && card.sizeBreakdown.map((sizeRow) => (`
      имеет `{` + `(` + `(` (3 уровня)
    - Закрывающие: `))}` имеет `)` + `)` + `}` (3 уровня) — закрывает в обратном порядке
    - `<TableRow ...>` парный с `</TableRow>`
    - Каждая `{CLUSTER_ORDER.flatMap(...)}` парная: `{` → `}`, `(` → `)`
    - Стрелочные функции `(cluster) => {...}` и `(w, idx) => {...}` имеют balanced `{`/`}`
    - `<TableCell ...>...</TableCell>` парные

    После замены TODO-маркер исчезнет. Структура итоговая:
    ```tsx
    <React.Fragment key={card.wbCardId}>
      <TableRow className="border-t border-t-border/60">...</TableRow>
      {showSizes && card.hasMultipleSizes && card.sizeBreakdown.map((sizeRow) => (
        <TableRow key={...} className="...">...</TableRow>
      ))}
    </React.Fragment>
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('components/stock/StockWbTable.tsx','utf-8'); if(!c.includes('card.sizeBreakdown.map')) throw 'no sizeBreakdown render'; if(!c.includes('hasMultipleSizes')) throw 'no hasMultipleSizes guard'; if(!c.includes('bg-muted/30')) throw 'no muted bg'; if(!c.includes('↳')) throw 'no ↳ prefix'; if(!c.includes('sizeRow.totalStock')) throw 'no sizeRow.totalStock'; if(!c.includes('sizeRow.clusters')) throw 'no sizeRow.clusters'; if(c.includes('Task 3 (B5): здесь будет блок размерных строк')) throw 'TODO marker still present'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "card.sizeBreakdown.map" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "card.hasMultipleSizes" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "bg-muted/30" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "↳" components/stock/StockWbTable.tsx` >= 1 (UTF-8 префикс)
    - `grep -c "showSizes && card.hasMultipleSizes && card.sizeBreakdown.map" components/stock/StockWbTable.tsx` == 1
    - `grep -c "sizeRow.totalStock" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "sizeRow.clusters" components/stock/StockWbTable.tsx` >= 1
    - `grep -c "Task 3 (B5): здесь будет блок размерных строк" components/stock/StockWbTable.tsx` == 0 (TODO-маркер удалён)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Размерные строки рендерятся под per-nmId TableRow при `showSizes && hasMultipleSizes`,
    layout соответствует CLAUDE.md sticky-table pattern. TypeScript зелёный, JSX
    балансирован. Phase 16 UI-implementation полная — готов к Plan 16-06 deploy + UAT.
  </done>
</task>

</tasks>

<verification>
- `interface Props` имеет `initialShowSizes: boolean`
- Кнопка «По размерам» отображена в toolbar
- Кнопка вызывает `saveStockWbShowSizes` через `useTransition`
- Под per-nmId строкой при `showSizes && card.hasMultipleSizes` рендерятся
  размерные строки из `card.sizeBreakdown[]`
- Размерная строка имеет `bg-muted/30`, префикс `↳ {techSize}`, placeholders для
  in-way и Иваново
- Кластерные cells размерной строки используют `sizeRow.clusters[shortCluster]`
- rowSpan для Фото/Сводка/Иваново пересчитан с учётом showSizes
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- Plan 16-06 manual UAT увидит контрольные nmId 859398279 (8 размеров) и
  901585883 (8 размеров) с раскрытыми размерными строками
- Кнопка «По размерам» persist между сессиями
- При включённой «По размерам» one-size товары НЕ показывают размерных строк
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-05-SUMMARY.md`
с описанием:
- Расширение interface Props
- Новый state `showSizes` + optimistic toggle
- Структура размерной строки (sticky cells + кластеры)
- Decision: per-size orders не доступны → колонки З/Об/Д = null = «—»
- Roadmap-attribution: STOCK-36
- Замечание для UAT: layout проверять при expand-all + showSizes ON
  (combinatorial column count)
</output>
