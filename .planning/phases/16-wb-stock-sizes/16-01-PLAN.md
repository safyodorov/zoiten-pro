---
phase: 16-wb-stock-sizes
plan: 01
type: execute
wave: 1
depends_on:
  - W0
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260423_phase16_size_breakdown/migration.sql
autonomous: true
requirements:
  - STOCK-31
gap_closure: false
must_haves:
  truths:
    - "WbCardWarehouseStock.techSize присутствует в schema.prisma как `String @default(\"\")`"
    - "Unique-ключ WbCardWarehouseStock = (wbCardId, warehouseId, techSize)"
    - "User.stockWbShowSizes присутствует как `Boolean @default(false)`"
    - "Миграция SQL создаётся вручную в `prisma/migrations/20260423_phase16_size_breakdown/migration.sql`"
    - "Миграция содержит DELETE legacy rows WHERE techSize = '' для clean re-sync"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema с techSize и User.stockWbShowSizes"
      contains: "techSize String"
    - path: "prisma/migrations/20260423_phase16_size_breakdown/migration.sql"
      provides: "Manual SQL миграция (паттерн Phase 14/15)"
      min_lines: 20
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260423_phase16_size_breakdown/migration.sql"
      via: "schema reflects migration DDL"
      pattern: "techSize.*String|stockWbShowSizes.*Boolean"
---

<objective>
Расширить Prisma schema и создать manual SQL миграцию для Phase 16:

1. **WbCardWarehouseStock**:
   - Добавить колонку `techSize String NOT NULL DEFAULT ''`
   - Дропнуть старый unique `(wbCardId, warehouseId)`
   - Создать новый unique `(wbCardId, warehouseId, techSize)`
   - DELETE FROM legacy rows (`WHERE techSize = ''`) — мера perfect-clean-replace
     перед re-sync в Plan 16-06

2. **User**:
   - Добавить `stockWbShowSizes Boolean NOT NULL DEFAULT false` (per-user toggle
     для кнопки «По размерам», паттерн `stockWbHiddenWarehouses` quick 260422-oy5)

**Покрывает:** STOCK-31.

**Не делает:** изменения в `lib/wb-api.ts`, sync-код, UI — это в Plan 16-02..05.

Purpose: Foundation для всей фазы — без расширенного unique-ключа upsert per-size
не работает, без `stockWbShowSizes` UI-кнопка не сохранит persist.

Output: Обновлённая `schema.prisma` + новая SQL-миграция готовая к применению на
VPS через `bash deploy.sh` (Plan 16-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-W0-SUMMARY.md

<interfaces>
<!-- Текущая схема (до изменений) -->

From `prisma/schema.prisma:805-818`:
```prisma
model WbCardWarehouseStock {
  id          String      @id @default(cuid())
  wbCardId    String
  wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
  warehouseId Int
  warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
  quantity    Int         @default(0)
  updatedAt   DateTime    @updatedAt

  @@unique([wbCardId, warehouseId])
  @@index([wbCardId])
  @@index([warehouseId])
}
```

From `prisma/schema.prisma:54-67` (User поля рядом с stockWbHiddenWarehouses):
```prisma
model User {
  // ... existing fields ...
  stockWbHiddenWarehouses Int[]                 @default([])
  // ↑ Phase 16 добавит stockWbShowSizes Boolean @default(false) рядом
}
```

From `prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql`
(паттерн ALTER TABLE для User pref):
```sql
-- Per-user hidden WB warehouses on /stock/wb (quick 260422-oy5)
ALTER TABLE "User" ADD COLUMN "stockWbHiddenWarehouses" INTEGER[] NOT NULL DEFAULT '{}';
```
</interfaces>

# Контракт миграции (точный SQL)

```sql
-- prisma/migrations/20260423_phase16_size_breakdown/migration.sql
-- Phase 16 (STOCK-31): per-size breakdown в WbCardWarehouseStock + User.stockWbShowSizes

-- ─── 1. WbCardWarehouseStock + techSize ──────────────────────────

ALTER TABLE "WbCardWarehouseStock"
  ADD COLUMN "techSize" TEXT NOT NULL DEFAULT '';

ALTER TABLE "WbCardWarehouseStock"
  DROP CONSTRAINT IF EXISTS "WbCardWarehouseStock_wbCardId_warehouseId_key";

DROP INDEX IF EXISTS "WbCardWarehouseStock_wbCardId_warehouseId_key";

CREATE UNIQUE INDEX "WbCardWarehouseStock_wbCardId_warehouseId_techSize_key"
  ON "WbCardWarehouseStock" ("wbCardId", "warehouseId", "techSize");

-- Truncate legacy aggregates чтобы избежать конфликта с per-size rows при первом sync.
-- После применения этой миграции UI /stock/wb пустой ~30 секунд до re-sync (Plan 16-06).
DELETE FROM "WbCardWarehouseStock" WHERE "techSize" = '';

-- ─── 2. User.stockWbShowSizes ────────────────────────────────────

ALTER TABLE "User"
  ADD COLUMN "stockWbShowSizes" BOOLEAN NOT NULL DEFAULT false;
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Обновить prisma/schema.prisma — techSize + stockWbShowSizes</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - `prisma/schema.prisma:54-67` (User модель — добавить stockWbShowSizes рядом со stockWbHiddenWarehouses)
    - `prisma/schema.prisma:805-818` (WbCardWarehouseStock — обновить unique + добавить techSize)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 1: Schema Migration» (строки 261-302) — точные DDL команды
    - `.planning/phases/16-wb-stock-sizes/16-CONTEXT.md` §«Schema: techSize в WbCardWarehouseStock» (locked decisions)
  </read_first>
  <action>
    Использовать Edit tool — НЕ Write — потому что schema.prisma большой файл с
    другими моделями.

    **Изменение 1** — `WbCardWarehouseStock` (строки 805-818):

    ИСХОДНЫЙ блок:
    ```prisma
    model WbCardWarehouseStock {
      id          String      @id @default(cuid())
      wbCardId    String
      wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
      warehouseId Int
      warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
      quantity    Int         @default(0)
      updatedAt   DateTime    @updatedAt

      @@unique([wbCardId, warehouseId])
      @@index([wbCardId])
      @@index([warehouseId])
    }
    ```

    ЦЕЛЕВОЙ блок (добавить `techSize` поле и обновить @@unique):
    ```prisma
    model WbCardWarehouseStock {
      id          String      @id @default(cuid())
      wbCardId    String
      wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
      warehouseId Int
      warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
      // Phase 16 (STOCK-31): per-size остаток. "" = legacy/неизвестно (после миграции
      // все rows с techSize="" удалены, при первом sync API заполнит реальные значения
      // вроде "46", "48", "S", "M" или "0" для одно-размерных товаров).
      techSize    String      @default("")
      quantity    Int         @default(0)
      updatedAt   DateTime    @updatedAt

      @@unique([wbCardId, warehouseId, techSize])
      @@index([wbCardId])
      @@index([warehouseId])
    }
    ```

    **Изменение 2** — `User` (около строки 66, рядом с `stockWbHiddenWarehouses`):

    ИСХОДНЫЙ фрагмент (строка 64-67):
    ```prisma
      // Per-user список скрытых WB-складов на странице /stock/wb (quick 260422-oy5).
      // Чисто визуальный фильтр — НЕ влияет на агрегаты.
      stockWbHiddenWarehouses Int[]                 @default([])
      preferences      UserPreference[] // персистентные настройки UI per-user (план 260410-mya)
    ```

    ЦЕЛЕВОЙ фрагмент (добавить `stockWbShowSizes` сразу после `stockWbHiddenWarehouses`):
    ```prisma
      // Per-user список скрытых WB-складов на странице /stock/wb (quick 260422-oy5).
      // Чисто визуальный фильтр — НЕ влияет на агрегаты.
      stockWbHiddenWarehouses Int[]                 @default([])
      // Phase 16 (STOCK-31): per-user toggle кнопки «По размерам» в /stock/wb.
      stockWbShowSizes        Boolean               @default(false)
      preferences      UserPreference[] // персистентные настройки UI per-user (план 260410-mya)
    ```

    Файл валидируется визуально — не запускать `npx prisma validate` или `migrate
    deploy` локально (нет PG). Plan 16-06 применит миграцию на VPS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('prisma/schema.prisma','utf-8'); if(!/techSize\s+String\s+@default\(\"\"\)/.test(c)) throw 'no techSize default'; if(!/@@unique\(\[wbCardId, warehouseId, techSize\]\)/.test(c)) throw 'no compound unique'; if(!/stockWbShowSizes\s+Boolean\s+@default\(false\)/.test(c)) throw 'no stockWbShowSizes'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'techSize\s*String\s*@default("")' prisma/schema.prisma` >= 1
    - `grep -c '@@unique(\[wbCardId, warehouseId, techSize\])' prisma/schema.prisma` == 1
    - `grep -c '@@unique(\[wbCardId, warehouseId\])' prisma/schema.prisma` == 0 (старый unique удалён)
    - `grep -c 'stockWbShowSizes\s*Boolean\s*@default(false)' prisma/schema.prisma` == 1
    - `grep -c 'stockWbHiddenWarehouses' prisma/schema.prisma` == 1 (старое поле НЕ ТРОНУТО)
    - `grep -c '@@index(\[wbCardId\])' prisma/schema.prisma` >= 1 (индекс сохранён в WbCardWarehouseStock)
    - schema.prisma не содержит синтаксических ошибок (визуальный осмотр)
  </acceptance_criteria>
  <done>
    schema.prisma содержит обновлённое определение WbCardWarehouseStock с
    techSize + compound unique, и User с дополнительным stockWbShowSizes.
    Никакой другой код (lib/, app/, components/) не затронут.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Создать prisma/migrations/20260423_phase16_size_breakdown/migration.sql</name>
  <files>prisma/migrations/20260423_phase16_size_breakdown/migration.sql</files>
  <read_first>
    - `prisma/migrations/20260422_add_user_stock_wb_hidden_warehouses/migration.sql` (паттерн manual SQL для User)
    - `prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql` (паттерн manual SQL для compound unique)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 1: Schema Migration» (строки 261-302)
  </read_first>
  <action>
    Создать директорию `prisma/migrations/20260423_phase16_size_breakdown/` и
    в ней файл `migration.sql` со следующим содержимым (точно):

    ```sql
    -- Phase 16 (STOCK-31): per-size breakdown в WbCardWarehouseStock + User.stockWbShowSizes
    --
    -- WARNING (UAT): шаг DELETE стирает все legacy rows. После применения миграции
    -- /stock/wb пустой до re-sync. Plan 16-06: bash deploy.sh + node scripts/wb-sync-stocks.js
    -- ИЛИ нажать «Обновить из WB» в UI. Окно отсутствия данных ~30s.

    -- ─── 1. WbCardWarehouseStock + techSize ──────────────────────────

    ALTER TABLE "WbCardWarehouseStock"
      ADD COLUMN "techSize" TEXT NOT NULL DEFAULT '';

    ALTER TABLE "WbCardWarehouseStock"
      DROP CONSTRAINT IF EXISTS "WbCardWarehouseStock_wbCardId_warehouseId_key";

    DROP INDEX IF EXISTS "WbCardWarehouseStock_wbCardId_warehouseId_key";

    CREATE UNIQUE INDEX "WbCardWarehouseStock_wbCardId_warehouseId_techSize_key"
      ON "WbCardWarehouseStock" ("wbCardId", "warehouseId", "techSize");

    -- Truncate legacy aggregates чтобы избежать конфликта с per-size rows при первом sync.
    -- После применения этой миграции UI /stock/wb пустой ~30 секунд до re-sync (Plan 16-06).
    DELETE FROM "WbCardWarehouseStock" WHERE "techSize" = '';

    -- ─── 2. User.stockWbShowSizes ────────────────────────────────────

    ALTER TABLE "User"
      ADD COLUMN "stockWbShowSizes" BOOLEAN NOT NULL DEFAULT false;
    ```

    Использовать `Write` tool (новый файл). НЕ применять миграцию локально —
    `prisma migrate deploy` запустится в Plan 16-06 на VPS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='prisma/migrations/20260423_phase16_size_breakdown/migration.sql'; if(!fs.existsSync(p)) throw 'missing file'; const s=fs.readFileSync(p,'utf-8'); if(!s.includes('ADD COLUMN \"techSize\"')) throw 'no ADD COLUMN techSize'; if(!s.includes('CREATE UNIQUE INDEX \"WbCardWarehouseStock_wbCardId_warehouseId_techSize_key\"')) throw 'no compound unique idx'; if(!s.includes('DELETE FROM \"WbCardWarehouseStock\" WHERE \"techSize\" = \\'\\'')) throw 'no DELETE legacy'; if(!s.includes('ADD COLUMN \"stockWbShowSizes\"')) throw 'no User.stockWbShowSizes'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `prisma/migrations/20260423_phase16_size_breakdown/migration.sql` существует
    - `grep -c 'ADD COLUMN "techSize" TEXT NOT NULL DEFAULT' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1
    - `grep -c 'DROP CONSTRAINT IF EXISTS "WbCardWarehouseStock_wbCardId_warehouseId_key"' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1
    - `grep -c 'CREATE UNIQUE INDEX "WbCardWarehouseStock_wbCardId_warehouseId_techSize_key"' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1
    - `grep -c 'DELETE FROM "WbCardWarehouseStock"' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1
    - `grep -c 'ADD COLUMN "stockWbShowSizes" BOOLEAN NOT NULL DEFAULT false' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1
    - `grep -c 'WHERE "techSize" = ' prisma/migrations/20260423_phase16_size_breakdown/migration.sql` == 1 (DELETE с условием — НЕ truncate всю таблицу)
  </acceptance_criteria>
  <done>
    Manual SQL миграция готова в правильной директории. Готова к
    `prisma migrate deploy` на VPS. Локально не применена (нет PG).
  </done>
</task>

</tasks>

<verification>
- `prisma/schema.prisma` содержит `techSize String @default("")`,
  `@@unique([wbCardId, warehouseId, techSize])`, `User.stockWbShowSizes Boolean @default(false)`
- `prisma/migrations/20260423_phase16_size_breakdown/migration.sql` содержит
  все 5 DDL операций (ADD COLUMN techSize, DROP unique, CREATE compound unique,
  DELETE legacy, ADD COLUMN stockWbShowSizes)
- Старые модели и поля не сломаны
- НЕ запускали `prisma migrate deploy` локально
</verification>

<success_criteria>
- Plan 16-02 может использовать compound unique key `wbCardId_warehouseId_techSize`
  в `tx.wbCardWarehouseStock.upsert({where: {wbCardId_warehouseId_techSize: {...}}})`
  после `npx prisma generate` (произойдёт при первой сборке/тестах)
- Plan 16-04 может читать `User.stockWbShowSizes` через `prisma.user.findUnique`
- Plan 16-06 применит миграцию через `prisma migrate deploy` на VPS
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md`
с описанием:
- Что изменено в `prisma/schema.prisma` (с before/after diff WbCardWarehouseStock + User)
- Содержимое `prisma/migrations/20260423_phase16_size_breakdown/migration.sql`
- Roadmap-attribution: STOCK-31
- Что в Phase 16-02..06 будет использовать compound unique key и new field
- Reminder: миграция применится только в Plan 16-06 на VPS
</output>
