---
phase: 16-wb-stock-sizes
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/wb-clusters.ts
  - lib/stock-wb-data.ts
  - tests/stock-wb-size-sort.test.ts
  - tests/stock-wb-data-sizebreakdown.test.ts
autonomous: true
requirements:
  - STOCK-34
gap_closure: false
must_haves:
  truths:
    - "lib/wb-clusters.ts экспортирует sortSizes(sizes: string[]): string[]"
    - "sortSizes сортирует числовые ASC, буквенные через SIZE_ORDER (XS<S<M<L<XL<2XL<3XL<4XL), пустые/'0' в конец"
    - "lib/stock-wb-data.ts экспортирует тип WbStockSizeRow с полями techSize, totalStock, clusters"
    - "WbStockRow содержит поля sizeBreakdown: WbStockSizeRow[] и hasMultipleSizes: boolean"
    - "getStockWbData() заполняет sizeBreakdown агрегатами per-size per-cluster per-warehouse"
    # W10: разделено на implementation-truth (этот Plan 16-03) и UI-truth (Plan 16-05)
    - "hasMultipleSizes вычисляется как `uniqueTechSizes.size > 1` в getStockWbData() для каждого WbStockRow"
    - "Sizes в sizeBreakdown отсортированы через sortSizes()"
  artifacts:
    - path: "lib/wb-clusters.ts"
      provides: "sortSizes helper + SIZE_ORDER map"
      contains: "export function sortSizes"
    - path: "lib/stock-wb-data.ts"
      provides: "WbStockSizeRow тип + sizeBreakdown агрегация"
      contains: "WbStockSizeRow"
    - path: "tests/stock-wb-size-sort.test.ts"
      provides: "Unit тесты для sortSizes (5+ test cases)"
      min_lines: 30
    - path: "tests/stock-wb-data-sizebreakdown.test.ts"
      provides: "Unit тесты для sizeBreakdown structure"
      min_lines: 40
  key_links:
    - from: "lib/stock-wb-data.ts WbStockRow.sizeBreakdown"
      to: "components/stock/StockWbTable.tsx (Plan 16-05)"
      via: "RSC props.groups[].wbCards[].sizeBreakdown"
      pattern: "sizeBreakdown"
    - from: "lib/wb-clusters.ts sortSizes"
      to: "lib/stock-wb-data.ts (используется для sort размеров)"
      via: "import { sortSizes }"
      pattern: "sortSizes"
---

<objective>
Расширить data layer `/stock/wb` для per-size агрегации. Создать pure-helper
`sortSizes` для консистентной сортировки техразмеров.

1. **`lib/wb-clusters.ts`**: экспортировать `sortSizes(sizes: string[]): string[]`
   с правилами:
   - Пустые (`""`) и `"0"` → в конец
   - Все числовые → ASC numeric (46, 48, 50, ...)
   - Все буквенные из `SIZE_ORDER` (`XS<S<M<L<XL<2XL<3XL<4XL`) → по карте
   - Mixed/unknown → `localeCompare("ru")` fallback

2. **`lib/stock-wb-data.ts`**:
   - Новый тип `WbStockSizeRow { techSize, totalStock, clusters }` (clusters
     имеет ту же форму что и в `WbStockRow`)
   - Расширить `WbStockRow` полями `sizeBreakdown: WbStockSizeRow[]`,
     `hasMultipleSizes: boolean`
   - В `getStockWbData()` агрегировать per-size: для каждого `card.warehouses` row
     группировать по `techSize`, формировать `clusters` идентично nmId-уровню,
     результат сортировать через `sortSizes`
   - `hasMultipleSizes` = `uniqueTechSizes.size > 1`; если `false` →
     `sizeBreakdown = []`

**Покрывает:** STOCK-34.

**Не делает:** server action, UI, sync — это Plan 16-04..05 (server action + UI)
и Plan 16-02 (sync, runs параллельно).

**Зависимости:** только Plan 16-01 (schema). Может запускаться **параллельно с
Plan 16-02** в wave 2 (разные файлы).

Purpose: Без расширенного data layer Plan 16-05 не сможет рендерить размерные
строки. Pure helper `sortSizes` тестируется без зависимости от Prisma/Next.

Output: Тип `WbStockSizeRow` экспортирован, `getStockWbData()` возвращает
`sizeBreakdown[]`, тесты GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md

<interfaces>
<!-- Текущий lib/wb-clusters.ts -->

From `lib/wb-clusters.ts`:
```typescript
export const CLUSTER_FULL_NAMES: Record<string, string> = { ... }
export const CLUSTER_ORDER = ["ЦФО", "ЮГ", "Урал", "ПФО", "СЗО", "СФО", "Прочие"] as const
export type ClusterShortName = typeof CLUSTER_ORDER[number]
```

<!-- Текущий lib/stock-wb-data.ts тип WbStockRow -->

From `lib/stock-wb-data.ts:9-41`:
```typescript
export interface WarehouseSlot {
  warehouseId: number
  warehouseName: string
  needsClusterReview: boolean
  quantity: number
  ordersCount: number
  ordersPerDay: number | null
}

export interface ClusterAggregate {
  shortCluster: string
  totalStock: number | null
  warehouses: WarehouseSlot[]
  totalOrdersCount: number | null
  ordersPerDay: number | null
}

export interface WbStockRow {
  wbCardId: string
  nmId: number
  wbCardName: string | null
  avgSalesSpeed7d: number | null
  totalStock: number | null
  inWayToClient: number | null
  inWayFromClient: number | null
  clusters: Record<ClusterShortName, ClusterAggregate>
  periodDays: number | null
  // Phase 16 добавит: sizeBreakdown: WbStockSizeRow[], hasMultipleSizes: boolean
}
```

<!-- Текущая агрегация в getStockWbData() -->

From `lib/stock-wb-data.ts:147-282` — агрегация per-cluster для card.warehouses
+ card.warehouseOrders. Phase 16 добавит **второй проход** для sizeBreakdown.

<!-- Phase 16-02 добавил techSize в WarehouseStockItem -->

Prisma client (после `npx prisma generate` в 16-01):
- `WbCardWarehouseStock { id, wbCardId, warehouseId, techSize, quantity, ... }`
- `card.warehouses[i].techSize` теперь string

<!-- WB Orders perWarehouseSize -->

Plan 16-02 добавил `OrdersWarehouseStats.perWarehouseSize: Map<warehouseName, Map<techSize, count>>`.
**ОДНАКО:** в `lib/stock-wb-data.ts` `getStockWbData()` НЕ вызывает напрямую
`fetchOrdersPerWarehouse()` — он читает уже-агрегированный `WbCardWarehouseOrders`
из БД. **Per-size orders данных в БД нет.** В Phase 16 размерные строки могут
показывать в колонке З либо `—`, либо `null`, либо распределение пропорционально
quantity. Locked default-гипотеза CONTEXT.md: показывать `—` (честнее).

**Решение:** в `WbStockSizeRow.clusters[shortCluster].ordersPerDay` ВСЕГДА `null`
(размерных orders данных нет в БД). UI рендерит как `—`. Это документирует
"degradation" v1: size-row не имеет З/Об/Д осмысленно.

Если же мы хотим в будущем перевести Orders на per-size БД — добавить это в STOCK-FUT.
</interfaces>

# Контракт WbStockSizeRow

```typescript
export interface WbStockSizeRow {
  techSize: string
  totalStock: number | null  // sum quantity по всем складам этого размера
  clusters: Record<ClusterShortName, ClusterAggregate>
  // Замечание: внутри cluster.warehouses[i].quantity = только этот размер;
  // ordersCount/ordersPerDay = 0/null (per-size orders не хранятся в БД, см. interfaces).
}
```

# Контракт sortSizes

```typescript
const SIZE_ORDER: Record<string, number> = {
  "XS": 0, "S": 1, "M": 2, "L": 3, "XL": 4,
  "2XL": 5, "XXL": 5, "3XL": 6, "XXXL": 6, "4XL": 7,
}

export function sortSizes(sizes: string[]): string[] {
  // Empty + "0" в конец
  // Все числовые → ASC numeric
  // Все буквенные (case-insensitive в SIZE_ORDER) → по SIZE_ORDER
  // Mixed → localeCompare("ru")
  // Возвращает НОВЫЙ массив (не мутирует input)
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/wb-clusters.ts — экспорт sortSizes() + tests/stock-wb-size-sort.test.ts</name>
  <files>
    - lib/wb-clusters.ts
    - tests/stock-wb-size-sort.test.ts
  </files>
  <read_first>
    - `lib/wb-clusters.ts` (целиком — добавление в конец файла)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 7: sortSizes helper» (строки 514-549)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Sort sizes test» (строки 956-984)
    - `tests/stock-math.test.ts` (паттерн pure unit test без mock)
  </read_first>
  <behavior>
    Test cases:
    - Test 1 (RED): «числовые: 60, 46, 48 → 46, 48, 60» — числовая ASC сортировка
    - Test 2 (RED): «буквенные: XL, S, M, L → S, M, L, XL» — по SIZE_ORDER map
    - Test 3 (RED): «case-insensitive буквенные: xl, s, M, l → S, M, L, XL (или эквивалент по позициям 1,2,3,4)»
    - Test 4 (RED): «3XL, XL, 2XL → XL, 2XL, 3XL» — порядок XL → 2XL → 3XL
    - Test 5 (RED): «mixed (числа + буквы): 46, M, S → mix через localeCompare ru» — fallback alpha (verify length+contents, не точный порядок)
    - Test 6 (RED): «пустые: ['46', '0', '48'] → ['46', '48', '0']» — пустые в конец
    - Test 7 (RED): «pure empty: ['', 'S', '0', 'M'] → ['M', 'S', '', '0'] (или эквивалент с known sizes сначала, empty в конце)»
    - Test 8 (RED): «pure пустой массив: [] → []»
    - Test 9 (RED): «не мутирует input: вход остался unchanged»
  </behavior>
  <action>
    **Шаг A.** Расширить `lib/wb-clusters.ts` — добавить в конец файла (после
    `export type ClusterShortName...`):

    ```typescript
    /**
     * Phase 16 (STOCK-34): известный порядок буквенных размеров одежды/обуви.
     * Регистр case-insensitive — input нормализуется через `.toUpperCase()`.
     */
    const SIZE_ORDER: Record<string, number> = {
      XS: 0,
      S: 1,
      M: 2,
      L: 3,
      XL: 4,
      "2XL": 5,
      XXL: 5,
      "3XL": 6,
      XXXL: 6,
      "4XL": 7,
      XXXXL: 7,
    }

    /**
     * Phase 16 (STOCK-34): стабильная сортировка техразмеров для UI размерных строк
     * в /stock/wb.
     *
     * Правила:
     *   1. Пустые ("" и "0") — всегда в конец (товары без размера / одно-размерные)
     *   2. Все числовые → numeric ASC (46, 48, 50, …)
     *   3. Все буквенные (по SIZE_ORDER) → по карте порядка (XS<S<M<L<XL<2XL<3XL<4XL)
     *   4. Mixed (числа + буквы) → localeCompare("ru") fallback
     *
     * Возвращает НОВЫЙ массив (input не мутируется).
     */
    export function sortSizes(sizes: string[]): string[] {
      const empty = sizes.filter((s) => !s || s === "0")
      const real = sizes.filter((s) => s && s !== "0")

      const allNumeric = real.length > 0 && real.every((s) => /^\d+$/.test(s))
      let sortedReal: string[]
      if (allNumeric) {
        sortedReal = [...real].sort((a, b) => parseInt(a, 10) - parseInt(b, 10))
      } else {
        const allKnown =
          real.length > 0 && real.every((s) => s.toUpperCase() in SIZE_ORDER)
        if (allKnown) {
          sortedReal = [...real].sort(
            (a, b) => SIZE_ORDER[a.toUpperCase()]! - SIZE_ORDER[b.toUpperCase()]!,
          )
        } else {
          sortedReal = [...real].sort((a, b) => a.localeCompare(b, "ru"))
        }
      }

      return [...sortedReal, ...empty]
    }
    ```

    **Шаг B.** Создать `tests/stock-wb-size-sort.test.ts`:

    ```typescript
    import { describe, it, expect } from "vitest"
    import { sortSizes } from "@/lib/wb-clusters"

    describe("sortSizes (STOCK-34)", () => {
      it("числовые: 60, 46, 48 → 46, 48, 60", () => {
        expect(sortSizes(["60", "46", "48"])).toEqual(["46", "48", "60"])
      })

      it("буквенные: XL, S, M, L → S, M, L, XL", () => {
        expect(sortSizes(["XL", "S", "M", "L"])).toEqual(["S", "M", "L", "XL"])
      })

      it("case-insensitive: xl, s, M, l сортируются по позициям SIZE_ORDER", () => {
        const result = sortSizes(["xl", "s", "M", "l"])
        // Должны быть в позициях S(1) → M(2) → L(3) → XL(4) — независимо от регистра
        // Проверяем что result.map(toUpperCase) === ["S","M","L","XL"]
        expect(result.map((s) => s.toUpperCase())).toEqual(["S", "M", "L", "XL"])
      })

      it("3XL, XL, 2XL → XL, 2XL, 3XL", () => {
        expect(sortSizes(["3XL", "XL", "2XL"])).toEqual(["XL", "2XL", "3XL"])
      })

      it("mixed (числа + буквы) → localeCompare ru fallback", () => {
        const input = ["46", "M", "S"]
        const result = sortSizes(input)
        // length и набор сохраняются
        expect(result).toHaveLength(3)
        expect(new Set(result)).toEqual(new Set(input))
        // Конкретный порядок зависит от localeCompare, но input не мутирован
      })

      it("пустые '0' и '' попадают в конец: ['46', '0', '48'] → ['46', '48', '0']", () => {
        expect(sortSizes(["46", "0", "48"])).toEqual(["46", "48", "0"])
      })

      it("пустая строка попадает в конец: ['S', '', 'M'] → ['M', 'S', '']", () => {
        expect(sortSizes(["S", "", "M"])).toEqual(["M", "S", ""])
      })

      it("пустой массив: [] → []", () => {
        expect(sortSizes([])).toEqual([])
      })

      it("одиночный размер: ['46'] → ['46']", () => {
        expect(sortSizes(["46"])).toEqual(["46"])
      })

      it("input не мутируется", () => {
        const input = ["XL", "S", "M"]
        const before = [...input]
        sortSizes(input)
        expect(input).toEqual(before)
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/stock-wb-size-sort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function sortSizes" lib/wb-clusters.ts` == 1
    - `grep -c "SIZE_ORDER" lib/wb-clusters.ts` >= 1
    - `grep -c "XS\|XXXL" lib/wb-clusters.ts` >= 1 (полный SIZE_ORDER)
    - Файл `tests/stock-wb-size-sort.test.ts` существует
    - `grep -c "describe.*sortSizes" tests/stock-wb-size-sort.test.ts` == 1
    - `grep -c "it(" tests/stock-wb-size-sort.test.ts` >= 8 (минимум 8 test cases)
    - `npm run test -- tests/stock-wb-size-sort.test.ts` exit 0 (все green)
  </acceptance_criteria>
  <done>
    sortSizes экспортирован из lib/wb-clusters.ts, имеет полную тестовую таблицу
    (числовые, буквенные case-insensitive, mixed, пустые, edge cases). Готов к
    использованию в Plan 16-03 Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/stock-wb-data.ts — добавить WbStockSizeRow + sizeBreakdown агрегацию + tests</name>
  <files>
    - lib/stock-wb-data.ts
    - tests/stock-wb-data-sizebreakdown.test.ts
  </files>
  <read_first>
    - `lib/stock-wb-data.ts` (целиком — место расширения)
    - `lib/wb-clusters.ts` (после Task 1 — sortSizes доступен)
    - `prisma/schema.prisma:805-820` (после Plan 16-01 — WbCardWarehouseStock содержит techSize)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 6: lib/stock-wb-data.ts — добавить sizeBreakdown» (строки 488-512)
    - `tests/stock-math.test.ts` (паттерн pure unit test без Prisma)
  </read_first>
  <behavior>
    Test approach: extracted helper `buildSizeBreakdown` (pure function) →
    можно тестировать без Prisma mock.

    Test cases для `tests/stock-wb-data-sizebreakdown.test.ts`:
    - Test 1: «один размер ('46') → hasMultipleSizes=false, sizeBreakdown=[]» (одно-размерные товары не порождают размерных строк)
    - Test 2: «два размера ('46', '48') в одном складе → hasMultipleSizes=true, sizeBreakdown.length=2»
    - Test 3: «sizeBreakdown отсортирован через sortSizes — 60 → 46, 48, 60»
    - Test 4: «WbStockSizeRow.totalStock = sum(quantity) этого размера по всем складам»
    - Test 5: «WbStockSizeRow.clusters[shortCluster].totalStock = sum только этого размера в кластере»
    - Test 6: «WbStockSizeRow.clusters[shortCluster].warehouses[i].quantity = qty этого размера на этом складе»
    - Test 7: «WbStockSizeRow.clusters[shortCluster].ordersPerDay = null (per-size orders не хранятся)»
    - Test 8: «техSize="0" + "46" + "48" → "0" в конце sizeBreakdown (sortSizes правило)»
  </behavior>
  <action>
    **Шаг A.** Добавить `import { sortSizes } from "@/lib/wb-clusters"` в начало
    `lib/stock-wb-data.ts` (рядом с существующим импортом CLUSTER_ORDER).

    **Шаг B.** После `export interface ClusterAggregate { ... }` добавить новый тип:

    ```typescript
    /**
     * Phase 16 (STOCK-34): per-size агрегат для размерной строки в /stock/wb.
     * Структура clusters идентична WbStockRow.clusters — UI рендерит ту же сетку
     * О/З/Об/Д per cluster + per warehouse при expand.
     *
     * Note: ordersPerDay/totalOrdersCount per-size не хранятся в БД (per-size
     * orders агрегация только в Map в памяти от fetchOrdersPerWarehouse, в БД
     * есть только nmId-уровень WbCardWarehouseOrders). В размерных строках
     * ordersPerDay = null → UI показывает «—» в колонке З.
     */
    export interface WbStockSizeRow {
      techSize: string
      totalStock: number | null
      clusters: Record<ClusterShortName, ClusterAggregate>
    }
    ```

    **Шаг C.** Расширить `WbStockRow`:

    ИСХОДНЫЙ интерфейс (строки 28-41):
    ```typescript
    export interface WbStockRow {
      wbCardId: string
      nmId: number
      wbCardName: string | null
      avgSalesSpeed7d: number | null
      totalStock: number | null
      inWayToClient: number | null
      inWayFromClient: number | null
      clusters: Record<ClusterShortName, ClusterAggregate>
      periodDays: number | null
    }
    ```

    Добавить ДВА новых поля в конце:
    ```typescript
    export interface WbStockRow {
      wbCardId: string
      nmId: number
      wbCardName: string | null
      avgSalesSpeed7d: number | null
      totalStock: number | null
      inWayToClient: number | null
      inWayFromClient: number | null
      clusters: Record<ClusterShortName, ClusterAggregate>
      periodDays: number | null
      // Phase 16 (STOCK-34): per-size breakdown под этой nmId-строкой
      sizeBreakdown: WbStockSizeRow[]
      hasMultipleSizes: boolean
    }
    ```

    **Шаг D.** Создать pure helper в конце файла (или extracted-функцию рядом с
    getStockWbData) — `buildSizeBreakdown`:

    ```typescript
    /**
     * Phase 16 (STOCK-34): построить per-size breakdown для одного nmId по
     * card.warehouses[]. Pure function — можно тестировать без Prisma mock.
     *
     * @param warehouses — записи WbCardWarehouseStock с техРазмерами
     *   (поле techSize присутствует после миграции 16-01)
     * @returns массив WbStockSizeRow, отсортированный через sortSizes()
     */
    export function buildSizeBreakdown(
      warehouses: Array<{
        warehouseId: number
        techSize: string
        quantity: number
        warehouse: {
          name: string
          shortCluster: string | null
          needsClusterReview: boolean
        } | null
      }>,
    ): WbStockSizeRow[] {
      // 1. Уникальные размеры
      const uniqueSizes = new Set<string>(warehouses.map((w) => w.techSize ?? ""))
      if (uniqueSizes.size <= 1) return []

      // 2. Group по techSize
      const bySize = new Map<string, typeof warehouses>()
      for (const w of warehouses) {
        const ts = w.techSize ?? ""
        const arr = bySize.get(ts) ?? []
        arr.push(w)
        bySize.set(ts, arr)
      }

      // 3. Build WbStockSizeRow per size
      const rows: WbStockSizeRow[] = []
      for (const [techSize, sizeWarehouses] of bySize.entries()) {
        // Init clusters
        const clusters = {} as Record<ClusterShortName, ClusterAggregate>
        for (const sc of CLUSTER_ORDER) {
          clusters[sc] = {
            shortCluster: sc,
            totalStock: null,
            warehouses: [],
            totalOrdersCount: null,
            ordersPerDay: null,
          }
        }

        let totalStock: number | null = null
        for (const w of sizeWarehouses) {
          const rawCluster = w.warehouse?.shortCluster ?? null
          const shortCluster: ClusterShortName = (
            rawCluster && (CLUSTER_ORDER as readonly string[]).includes(rawCluster)
              ? rawCluster
              : "Прочие"
          ) as ClusterShortName
          const agg = clusters[shortCluster]!

          const qty = w.quantity ?? 0
          agg.warehouses.push({
            warehouseId: w.warehouseId,
            warehouseName: w.warehouse?.name ?? `Склад ${w.warehouseId}`,
            needsClusterReview: w.warehouse?.needsClusterReview ?? false,
            quantity: qty,
            ordersCount: 0,        // Phase 16: per-size orders не хранятся в БД
            ordersPerDay: null,    //          → null = "—" в UI
          })
          agg.totalStock = (agg.totalStock ?? 0) + qty
          totalStock = (totalStock ?? 0) + qty
          // ordersPerDay/totalOrdersCount остаются null
        }

        rows.push({ techSize, totalStock, clusters })
      }

      // 4. Sort через sortSizes
      const sortedSizes = sortSizes(rows.map((r) => r.techSize))
      const indexBySize = new Map(sortedSizes.map((s, i) => [s, i]))
      rows.sort((a, b) => (indexBySize.get(a.techSize) ?? 0) - (indexBySize.get(b.techSize) ?? 0))

      return rows
    }
    ```

    **Шаг E.** В `getStockWbData()` функции, в финальном `return { ... }` для
    каждого `WbStockRow` (строки ~260-272), добавить вычисление и пропагацию
    `sizeBreakdown` + `hasMultipleSizes`:

    Найти блок:
    ```typescript
    return {
      wbCardId: card?.id ?? `missing-${a.article}`,
      nmId: isNaN(nmId) ? 0 : nmId,
      wbCardName: card?.name ?? null,
      avgSalesSpeed7d: card?.avgSalesSpeed7d ?? null,
      totalStock,
      inWayToClient: card?.inWayToClient ?? null,
      inWayFromClient: card?.inWayFromClient ?? null,
      clusters,
      periodDays: cardPeriodDays,
    }
    ```

    Расширить:
    ```typescript
    // Phase 16 (STOCK-34): per-size breakdown
    const cardWarehouses = card?.warehouses ?? []
    const sizeBreakdown = buildSizeBreakdown(
      cardWarehouses.map((ws) => ({
        warehouseId: ws.warehouseId,
        techSize: ws.techSize ?? "",
        quantity: ws.quantity ?? 0,
        warehouse: ws.warehouse
          ? {
              name: ws.warehouse.name,
              shortCluster: ws.warehouse.shortCluster ?? null,
              needsClusterReview: ws.warehouse.needsClusterReview ?? false,
            }
          : null,
      })),
    )
    const uniqueSizes = new Set<string>(cardWarehouses.map((ws) => ws.techSize ?? ""))
    const hasMultipleSizes = uniqueSizes.size > 1

    return {
      wbCardId: card?.id ?? `missing-${a.article}`,
      nmId: isNaN(nmId) ? 0 : nmId,
      wbCardName: card?.name ?? null,
      avgSalesSpeed7d: card?.avgSalesSpeed7d ?? null,
      totalStock,
      inWayToClient: card?.inWayToClient ?? null,
      inWayFromClient: card?.inWayFromClient ?? null,
      clusters,
      periodDays: cardPeriodDays,
      sizeBreakdown,
      hasMultipleSizes,
    }
    ```

    **Шаг F.** Создать `tests/stock-wb-data-sizebreakdown.test.ts`:

    ```typescript
    import { describe, it, expect } from "vitest"
    import { buildSizeBreakdown } from "@/lib/stock-wb-data"

    describe("buildSizeBreakdown (STOCK-34)", () => {
      const koledino = {
        name: "Коледино",
        shortCluster: "ЦФО" as const,
        needsClusterReview: false,
      }
      const kazan = {
        name: "Казань",
        shortCluster: "ПФО" as const,
        needsClusterReview: false,
      }

      it("один размер → hasMultipleSizes=false неявно (sizeBreakdown пустой)", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
        ])
        expect(result).toEqual([])
      })

      it("два размера в одном складе → 2 WbStockSizeRow", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        expect(result).toHaveLength(2)
        expect(result.map((r) => r.techSize)).toEqual(["46", "48"])
      })

      it("sortSizes применён: 60, 46, 48 → 46, 48, 60", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "60", quantity: 5, warehouse: koledino },
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        expect(result.map((r) => r.techSize)).toEqual(["46", "48", "60"])
      })

      it("totalStock = sum по всем складам этого размера", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 2, techSize: "46", quantity: 4, warehouse: kazan },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        const size46 = result.find((r) => r.techSize === "46")!
        expect(size46.totalStock).toBe(15)
      })

      it("clusters[ЦФО].totalStock = только этот размер в кластере", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 2, techSize: "46", quantity: 4, warehouse: kazan },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        const size46 = result.find((r) => r.techSize === "46")!
        expect(size46.clusters["ЦФО"].totalStock).toBe(11)
        expect(size46.clusters["ПФО"].totalStock).toBe(4)
      })

      it("warehouses[i].quantity = только этот размер на этом складе", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        const size46 = result.find((r) => r.techSize === "46")!
        const wh = size46.clusters["ЦФО"].warehouses.find((w) => w.warehouseId === 1)!
        expect(wh.quantity).toBe(11)
      })

      it("ordersPerDay в каждом кластере и складе = null (per-size orders не хранятся)", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 1, techSize: "48", quantity: 10, warehouse: koledino },
        ])
        const size46 = result.find((r) => r.techSize === "46")!
        expect(size46.clusters["ЦФО"].ordersPerDay).toBeNull()
        const wh = size46.clusters["ЦФО"].warehouses.find((w) => w.warehouseId === 1)!
        expect(wh.ordersPerDay).toBeNull()
        expect(wh.ordersCount).toBe(0)
      })

      it("techSize='0' (одно-размерный mixed с реальными размерами) → '0' в конце", () => {
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "0", quantity: 5, warehouse: koledino },
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
        ])
        expect(result.map((r) => r.techSize)).toEqual(["46", "0"])
      })

      it("неизвестный shortCluster → попадает в 'Прочие'", () => {
        const unknown = { name: "X", shortCluster: "Антарктида", needsClusterReview: false }
        const result = buildSizeBreakdown([
          { warehouseId: 1, techSize: "46", quantity: 11, warehouse: koledino },
          { warehouseId: 999, techSize: "46", quantity: 1, warehouse: unknown },
        ])
        const size46 = result.find((r) => r.techSize === "46")!
        expect(size46.clusters["Прочие"].totalStock).toBe(1)
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/stock-wb-size-sort.test.ts tests/stock-wb-data-sizebreakdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface WbStockSizeRow" lib/stock-wb-data.ts` == 1
    - `grep -c "export function buildSizeBreakdown" lib/stock-wb-data.ts` == 1
    - `grep -c "sizeBreakdown: WbStockSizeRow\[\]" lib/stock-wb-data.ts` >= 1 (в WbStockRow)
    - `grep -c "hasMultipleSizes: boolean" lib/stock-wb-data.ts` >= 1 (в WbStockRow)
    - `grep -c "import.*sortSizes.*from.*wb-clusters" lib/stock-wb-data.ts` == 1
    - `grep -c "buildSizeBreakdown" lib/stock-wb-data.ts` >= 2 (export + использование в getStockWbData)
    - `grep -c "uniqueSizes.size > 1" lib/stock-wb-data.ts` >= 1
    - Файл `tests/stock-wb-data-sizebreakdown.test.ts` существует
    - `grep -c "buildSizeBreakdown" tests/stock-wb-data-sizebreakdown.test.ts` >= 1 (import + использование)
    - `grep -c "it(" tests/stock-wb-data-sizebreakdown.test.ts` >= 8 (минимум 8 cases)
    - `npm run test -- tests/stock-wb-data-sizebreakdown.test.ts` exit 0 (все green)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    `WbStockSizeRow` тип и `buildSizeBreakdown` helper экспортированы из
    `lib/stock-wb-data.ts`. `WbStockRow` содержит `sizeBreakdown[]` и
    `hasMultipleSizes`. Тесты GREEN. Готов к использованию в Plan 16-04
    (RSC page чтение) и Plan 16-05 (UI рендер).
  </done>
</task>

</tasks>

<verification>
- `lib/wb-clusters.ts` экспортирует `sortSizes(sizes: string[]): string[]`
- `lib/stock-wb-data.ts` экспортирует `WbStockSizeRow` и `buildSizeBreakdown`
- `WbStockRow` содержит `sizeBreakdown` и `hasMultipleSizes`
- `getStockWbData()` заполняет эти поля per nmId
- `npm run test -- tests/stock-wb-size-sort.test.ts tests/stock-wb-data-sizebreakdown.test.ts` GREEN
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- Plan 16-05 может рендерить размерные строки через `card.sizeBreakdown.map(...)`
  и условно через `card.hasMultipleSizes`
- Plan 16-04 RSC видит расширенный тип через import
- Никаких изменений в sync / UI / server actions / migrations
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-03-SUMMARY.md`
с описанием:
- Новые экспорты `lib/wb-clusters.ts` (sortSizes, SIZE_ORDER concept)
- Новые типы `WbStockSizeRow` + extension `WbStockRow`
- `buildSizeBreakdown` сигнатура + примеры
- Roadmap-attribution: STOCK-34
- Замечание про ordersPerDay=null в размерных строках (locked в research)
</output>
