---
phase: 16-wb-stock-sizes
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "03"
files_modified:
  - app/actions/stock-wb.ts
  - app/(dashboard)/stock/wb/page.tsx
  - tests/stock-wb-actions.test.ts
autonomous: true
requirements:
  - STOCK-35
gap_closure: false
must_haves:
  truths:
    - "saveStockWbShowSizes(value: boolean) экспортирован из app/actions/stock-wb.ts"
    - "Action проверяет requireSection('STOCK') без MANAGE (user меняет свою настройку)"
    - "Action использует Zod z.object({ value: z.boolean() }) для валидации"
    - "Action обновляет User.stockWbShowSizes для текущего session.user.id"
    - "Action возвращает { ok: true } | { ok: false, error: string }"
    - "Action делает revalidatePath('/stock/wb')"
    - "RSC page.tsx читает User.stockWbShowSizes через session, передаёт props.initialShowSizes"
    - "tests/stock-wb-actions.test.ts тестирует success path, RBAC отказ, Zod validation"
  artifacts:
    - path: "app/actions/stock-wb.ts"
      provides: "saveStockWbShowSizes server action"
      contains: "saveStockWbShowSizes"
    - path: "app/(dashboard)/stock/wb/page.tsx"
      provides: "RSC чтение stockWbShowSizes + prop drilling"
      contains: "stockWbShowSizes"
    - path: "tests/stock-wb-actions.test.ts"
      provides: "Unit тесты для server action"
      min_lines: 50
  key_links:
    - from: "app/actions/stock-wb.ts saveStockWbShowSizes"
      to: "components/stock/StockWbTable.tsx (Plan 16-05)"
      via: "Optimistic toggle button calls server action"
      pattern: "saveStockWbShowSizes"
    - from: "app/(dashboard)/stock/wb/page.tsx"
      to: "StockWbTable initialShowSizes prop"
      via: "RSC передаёт session-derived флаг"
      pattern: "initialShowSizes"
---

<objective>
1. **Server action `saveStockWbShowSizes(value: boolean)`** в `app/actions/stock-wb.ts`:
   - Паттерн `saveStockWbHiddenWarehouses` (quick 260422-oy5) — RBAC `requireSection("STOCK")`
     без MANAGE (user меняет свою настройку)
   - Zod `z.object({ value: z.boolean() })`
   - Update `User.stockWbShowSizes` для текущего user
   - `revalidatePath("/stock/wb")`
   - Возвращает `{ ok: true } | { ok: false, error }` (паттерн existing)

2. **RSC `app/(dashboard)/stock/wb/page.tsx`**:
   - В существующем `prisma.user.findUnique` extend `select` полем `stockWbShowSizes`
   - Передать `initialShowSizes` prop в `<StockWbTable />`

3. **Unit тесты** в `tests/stock-wb-actions.test.ts`:
   - Mock auth + Prisma (паттерн другого `tests/stock-actions.test.ts`)
   - Success path: возвращает `{ ok: true }`, вызывает Prisma update, revalidate
   - Unauthenticated: возвращает `{ ok: false, error }`
   - Invalid input (not boolean): Zod fails, `{ ok: false, error: "Некорректные данные" }`
   - DB error: catch block → `{ ok: false, error: <message> }`

**Покрывает:** STOCK-35.

**Не делает:** UI кнопку, рендер размерных строк — это Plan 16-05.
**Параллельно с Plan 16-05** (разные файлы; 05 trogает только StockWbTable.tsx).

**Зависимости:** Plan 16-01 (User.stockWbShowSizes колонка), Plan 16-03 (extended
WbStockRow для типов в page.tsx, импорт `StockWbDataResult`).

Purpose: Без server action UI кнопка не сохранит persist; без расширения page.tsx
StockWbTable не получит начальное значение showSizes. Это контракт-prop drilling
для UI.

Output: `saveStockWbShowSizes` готов к вызову из клиентских компонентов; page.tsx
передаёт `initialShowSizes` prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-03-SUMMARY.md

<interfaces>
<!-- Текущий app/actions/stock-wb.ts -->

From `app/actions/stock-wb.ts` (целиком):
```typescript
"use server"

import { z } from "zod"
import { prisma } from "@/lib/prisma"
import { auth } from "@/lib/auth"
import { requireSection } from "@/lib/rbac"
import { revalidatePath } from "next/cache"

const InputSchema = z.object({
  ids: z.array(z.number().int()).max(500),
})

export type SaveStockWbHiddenWarehousesResult =
  | { ok: true }
  | { ok: false; error: string }

export async function saveStockWbHiddenWarehouses(
  ids: number[],
): Promise<SaveStockWbHiddenWarehousesResult> {
  await requireSection("STOCK")
  const session = await auth()
  const userId = session?.user?.id
  if (!userId) return { ok: false, error: "Не авторизован" }
  const parsed = InputSchema.safeParse({ ids })
  if (!parsed.success) return { ok: false, error: "Некорректные данные" }
  const clean = [...new Set(parsed.data.ids)].sort((a, b) => a - b)
  try {
    await prisma.user.update({
      where: { id: userId },
      data: { stockWbHiddenWarehouses: clean },
    })
  } catch (e) {
    return { ok: false, error: e instanceof Error ? e.message : "DB error" }
  }
  revalidatePath("/stock/wb")
  return { ok: true }
}
```

<!-- Текущий app/(dashboard)/stock/wb/page.tsx -->

From `app/(dashboard)/stock/wb/page.tsx`:
```typescript
import { requireSection } from "@/lib/rbac"
import { auth } from "@/lib/auth"
import { prisma } from "@/lib/prisma"
import { getStockWbData } from "@/lib/stock-wb-data"
import { StockWbTable } from "@/components/stock/StockWbTable"

export default async function StockWbPage() {
  await requireSection("STOCK")
  const [data, session] = await Promise.all([getStockWbData(), auth()])

  let hiddenWarehouseIds: number[] = []
  if (session?.user?.id) {
    const user = await prisma.user.findUnique({
      where: { id: session.user.id },
      select: { stockWbHiddenWarehouses: true },
    })
    hiddenWarehouseIds = user?.stockWbHiddenWarehouses ?? []
  }
  // ↑ Phase 16: добавить stockWbShowSizes в select + передать prop

  return (
    <StockWbTable
      groups={data.groups}
      turnoverNormDays={data.turnoverNormDays}
      clusterWarehouses={data.clusterWarehouses}
      hiddenWarehouseIds={hiddenWarehouseIds}
      // initialShowSizes — добавить в Plan 16-04
    />
  )
}
```

<!-- StockWbTable Props (текущий) -->

From `components/stock/StockWbTable.tsx:35-40`:
```typescript
interface Props {
  groups: ProductWbGroup[]
  turnoverNormDays: number
  clusterWarehouses: StockWbDataResult["clusterWarehouses"]
  hiddenWarehouseIds: number[]
  // Phase 16 (Plan 16-05) добавит:
  // initialShowSizes: boolean
}
```

**Контракт между Plan 16-04 и Plan 16-05:** prop `initialShowSizes: boolean`
с дефолтом `false`. 04 передаёт значение, 05 принимает в `interface Props`.
Параллельно работает потому что 04 не trogает `StockWbTable.tsx`, а 05 не
trogает `page.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/actions/stock-wb.ts — saveStockWbShowSizes + tests/stock-wb-actions.test.ts</name>
  <files>
    - app/actions/stock-wb.ts
    - tests/stock-wb-actions.test.ts
  </files>
  <read_first>
    - `app/actions/stock-wb.ts` (целиком — паттерн которого надо следовать)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Per-user Boolean toggle» (строки 891-923)
    - `tests/stock-actions.test.ts` (если существует — паттерн mocking auth + Prisma)
    - Если `tests/stock-actions.test.ts` нет — посмотреть `tests/wb-stocks-per-warehouse.test.ts` для базового vitest mocking паттерна
  </read_first>
  <behavior>
    Test cases для `tests/stock-wb-actions.test.ts`:
    - Test 1 (RED): «success: vault → возвращает {ok: true}, prisma.user.update вызван с stockWbShowSizes: true, revalidatePath('/stock/wb') вызван»
    - Test 2 (RED): «success: false → vault → prisma.user.update вызван с stockWbShowSizes: false»
    - Test 3 (RED): «unauthenticated (auth() returns null) → возвращает {ok: false, error: 'Не авторизован'}, prisma.user.update НЕ вызван»
    - Test 4 (RED): «invalid input (not boolean) → возвращает {ok: false, error: 'Некорректные данные'}, prisma.user.update НЕ вызван». В TypeScript нужно cast `as any` потому что сигнатура форсит boolean.
    - Test 5 (RED): «DB error → возвращает {ok: false, error: <message>}»
  </behavior>
  <action>
    **Шаг A.** Расширить `app/actions/stock-wb.ts` — добавить новую секцию ПОСЛЕ
    существующего `saveStockWbHiddenWarehouses`:

    ```typescript
    // Phase 16 (STOCK-35): per-user toggle кнопки «По размерам» в /stock/wb.
    // Паттерн идентичен saveStockWbHiddenWarehouses (quick 260422-oy5).

    const ShowSizesSchema = z.object({
      value: z.boolean(),
    })

    export type SaveStockWbShowSizesResult =
      | { ok: true }
      | { ok: false; error: string }

    /**
     * Сохраняет per-user флаг «показывать размерные строки в /stock/wb».
     * RBAC: STOCK VIEW — user меняет СВОЮ preference.
     */
    export async function saveStockWbShowSizes(
      value: boolean,
    ): Promise<SaveStockWbShowSizesResult> {
      await requireSection("STOCK")

      const session = await auth()
      const userId = session?.user?.id
      if (!userId) return { ok: false, error: "Не авторизован" }

      const parsed = ShowSizesSchema.safeParse({ value })
      if (!parsed.success) return { ok: false, error: "Некорректные данные" }

      try {
        await prisma.user.update({
          where: { id: userId },
          data: { stockWbShowSizes: parsed.data.value },
        })
      } catch (e) {
        return { ok: false, error: e instanceof Error ? e.message : "DB error" }
      }

      revalidatePath("/stock/wb")
      return { ok: true }
    }
    ```

    **B4: ОБЯЗАТЕЛЬНО — именованная константа `ShowSizesSchema`.**
    Zod-схема ДОЛЖНА быть объявлена как top-level `const ShowSizesSchema = z.object({...})`
    (как показано выше) и использоваться в `safeParse` через имя `ShowSizesSchema.safeParse({ value })`.
    НЕ инлайнить `z.object({ value: z.boolean() }).safeParse(...)` напрямую в теле функции —
    `acceptance_criteria` требует `grep -c "ShowSizesSchema" >= 2` (определение + использование),
    и инлайн-вариант провалит этот критерий несмотря на зелёные тесты.

    **Шаг B.** Создать `tests/stock-wb-actions.test.ts` — паттерн с vi.hoisted
    (видно в `tests/stock-actions.test.ts` если существует, иначе использовать
    базовый паттерн):

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    // Mock dependencies (vi.hoisted для правильной инициализации до импорта)
    const mocks = vi.hoisted(() => ({
      auth: vi.fn(),
      requireSection: vi.fn(),
      revalidatePath: vi.fn(),
      prismaUserUpdate: vi.fn(),
    }))

    vi.mock("@/lib/auth", () => ({ auth: mocks.auth }))
    vi.mock("@/lib/rbac", () => ({ requireSection: mocks.requireSection }))
    vi.mock("next/cache", () => ({ revalidatePath: mocks.revalidatePath }))
    vi.mock("@/lib/prisma", () => ({
      prisma: {
        user: { update: mocks.prismaUserUpdate },
      },
    }))

    describe("saveStockWbShowSizes (STOCK-35)", () => {
      beforeEach(() => {
        vi.resetAllMocks()
        mocks.requireSection.mockResolvedValue(undefined) // RBAC pass
        mocks.auth.mockResolvedValue({ user: { id: "user-1" } })
        mocks.prismaUserUpdate.mockResolvedValue({ id: "user-1" })
      })

      it("happy path: value=true → ok + prisma.user.update + revalidatePath", async () => {
        const { saveStockWbShowSizes } = await import("@/app/actions/stock-wb")

        const res = await saveStockWbShowSizes(true)

        expect(res).toEqual({ ok: true })
        expect(mocks.requireSection).toHaveBeenCalledWith("STOCK")
        expect(mocks.prismaUserUpdate).toHaveBeenCalledWith({
          where: { id: "user-1" },
          data: { stockWbShowSizes: true },
        })
        expect(mocks.revalidatePath).toHaveBeenCalledWith("/stock/wb")
      })

      it("happy path: value=false → ok + prisma.user.update", async () => {
        const { saveStockWbShowSizes } = await import("@/app/actions/stock-wb")

        const res = await saveStockWbShowSizes(false)

        expect(res).toEqual({ ok: true })
        expect(mocks.prismaUserUpdate).toHaveBeenCalledWith({
          where: { id: "user-1" },
          data: { stockWbShowSizes: false },
        })
      })

      it("unauthenticated → возвращает error без update", async () => {
        mocks.auth.mockResolvedValue(null)
        const { saveStockWbShowSizes } = await import("@/app/actions/stock-wb")

        const res = await saveStockWbShowSizes(true)

        expect(res).toEqual({ ok: false, error: "Не авторизован" })
        expect(mocks.prismaUserUpdate).not.toHaveBeenCalled()
      })

      it("invalid input (not boolean) → Zod fails, error без update", async () => {
        const { saveStockWbShowSizes } = await import("@/app/actions/stock-wb")

        // Cast as any для bypass TypeScript check — тестируем runtime защиту
        const res = await saveStockWbShowSizes("yes" as unknown as boolean)

        expect(res).toEqual({ ok: false, error: "Некорректные данные" })
        expect(mocks.prismaUserUpdate).not.toHaveBeenCalled()
      })

      it("DB error → catch block, error message", async () => {
        mocks.prismaUserUpdate.mockRejectedValueOnce(new Error("Connection refused"))
        const { saveStockWbShowSizes } = await import("@/app/actions/stock-wb")

        const res = await saveStockWbShowSizes(true)

        expect(res).toEqual({ ok: false, error: "Connection refused" })
        expect(mocks.revalidatePath).not.toHaveBeenCalled()
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/stock-wb-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function saveStockWbShowSizes" app/actions/stock-wb.ts` == 1
    - `grep -c "ShowSizesSchema" app/actions/stock-wb.ts` >= 2 (определение + использование)
    - `grep -c "z.boolean()" app/actions/stock-wb.ts` >= 1
    - `grep -c "stockWbShowSizes: parsed.data.value" app/actions/stock-wb.ts` >= 1 (или эквивалент)
    - `grep -c "saveStockWbHiddenWarehouses" app/actions/stock-wb.ts` >= 1 (старый action не сломан)
    - Файл `tests/stock-wb-actions.test.ts` существует
    - `grep -c "saveStockWbShowSizes" tests/stock-wb-actions.test.ts` >= 5 (test cases)
    - `grep -c "it(" tests/stock-wb-actions.test.ts` >= 5
    - `npm run test -- tests/stock-wb-actions.test.ts` exit 0 (все green)
  </acceptance_criteria>
  <done>
    Server action `saveStockWbShowSizes` экспортирован, валидация + RBAC + persist
    + revalidate работают, unit тесты GREEN. Готов к импорту в Plan 16-05 для UI.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: app/(dashboard)/stock/wb/page.tsx — читать stockWbShowSizes + передать prop</name>
  <files>app/(dashboard)/stock/wb/page.tsx</files>
  <read_first>
    - `app/(dashboard)/stock/wb/page.tsx` (целиком)
    - `components/stock/StockWbTable.tsx:35-40` (текущий interface Props — Plan 16-05 расширит)
  </read_first>
  <action>
    Использовать Edit tool — точечные замены.

    **Изменение 1** — расширить `prisma.user.findUnique` select:

    Найти:
    ```typescript
        const user = await prisma.user.findUnique({
          where: { id: session.user.id },
          select: { stockWbHiddenWarehouses: true },
        })
        hiddenWarehouseIds = user?.stockWbHiddenWarehouses ?? []
    ```

    Заменить на:
    ```typescript
        const user = await prisma.user.findUnique({
          where: { id: session.user.id },
          select: { stockWbHiddenWarehouses: true, stockWbShowSizes: true },
        })
        hiddenWarehouseIds = user?.stockWbHiddenWarehouses ?? []
        // Phase 16 (STOCK-35): per-user toggle кнопки «По размерам»
        initialShowSizes = user?.stockWbShowSizes ?? false
    ```

    **Изменение 2** — declare `initialShowSizes` в начале функции:

    Найти:
    ```typescript
      let hiddenWarehouseIds: number[] = []
      if (session?.user?.id) {
    ```

    Заменить на:
    ```typescript
      let hiddenWarehouseIds: number[] = []
      let initialShowSizes = false
      if (session?.user?.id) {
    ```

    **Изменение 3** — передать prop в `<StockWbTable />`:

    Найти:
    ```typescript
          <StockWbTable
            groups={data.groups}
            turnoverNormDays={data.turnoverNormDays}
            clusterWarehouses={data.clusterWarehouses}
            hiddenWarehouseIds={hiddenWarehouseIds}
          />
    ```

    Заменить на:
    ```typescript
          <StockWbTable
            groups={data.groups}
            turnoverNormDays={data.turnoverNormDays}
            clusterWarehouses={data.clusterWarehouses}
            hiddenWarehouseIds={hiddenWarehouseIds}
            initialShowSizes={initialShowSizes}
          />
    ```

    Заметка: после Plan 16-05 `StockWbTable` Props примет `initialShowSizes: boolean`.
    Параллельность планов 04 и 05 поддерживается тем что:
    - Если выполняется ТОЛЬКО 04 — TypeScript error «property initialShowSizes does
      not exist on Props» — это ОК, так как 05 параллельно расширяет интерфейс
    - Если выполняется ТОЛЬКО 05 — page.tsx не передаст prop, default `false` в
      `useState(initialShowSizes ?? false)` (план 05 предусмотрит null-coalescing)

    Если в момент выполнения 04 видим, что 05 уже добавил prop — отлично. Если не
    добавил — оставляем prop pass, в финальной сборке оба сольются.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/(dashboard)/stock/wb/page.tsx','utf-8'); if(!c.includes('stockWbShowSizes: true')) throw 'no stockWbShowSizes in select'; if(!c.includes('initialShowSizes')) throw 'no initialShowSizes prop'; if(!c.includes('user?.stockWbShowSizes ?? false')) throw 'no stockWbShowSizes read'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stockWbShowSizes: true" "app/(dashboard)/stock/wb/page.tsx"` == 1 (в select)
    - `grep -c "user?.stockWbShowSizes" "app/(dashboard)/stock/wb/page.tsx"` >= 1
    - `grep -c "initialShowSizes" "app/(dashboard)/stock/wb/page.tsx"` >= 2 (declaration + prop)
    - `grep -c "stockWbHiddenWarehouses: true" "app/(dashboard)/stock/wb/page.tsx"` >= 1 (старый select сохранён)
    - `grep -c "hiddenWarehouseIds={hiddenWarehouseIds}" "app/(dashboard)/stock/wb/page.tsx"` == 1 (старый prop pass сохранён)
  </acceptance_criteria>
  <done>
    page.tsx читает per-user `stockWbShowSizes` через session, передаёт prop
    `initialShowSizes` в StockWbTable. После Plan 16-05 финальная сборка работает.
  </done>
</task>

</tasks>

<verification>
- `app/actions/stock-wb.ts` экспортирует `saveStockWbShowSizes`
- Action использует requireSection("STOCK") без MANAGE
- Action использует Zod boolean validation
- Action делает `prisma.user.update` + `revalidatePath`
- `tests/stock-wb-actions.test.ts` — 5+ test cases GREEN
- `app/(dashboard)/stock/wb/page.tsx` читает `stockWbShowSizes` из session, передаёт prop
- `npx tsc --noEmit` exit 0 (после Plan 16-05 финальная типизация)
</verification>

<success_criteria>
- Plan 16-05 импортирует `saveStockWbShowSizes` для optimistic toggle
- Plan 16-05 расширяет `interface Props` полем `initialShowSizes: boolean`
- В сочетании 04 + 05 page.tsx передаёт session-derived флаг в UI
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-04-SUMMARY.md`
с описанием:
- Server action signature + RBAC + Zod schema
- page.tsx изменения (select extension + prop drilling)
- Roadmap-attribution: STOCK-35
- Замечание: ожидается параллельное выполнение Plan 16-05 для UI; до этого
  TypeScript может показывать «property does not exist» на initialShowSizes prop
  до того как 05 расширит interface Props.
</output>
