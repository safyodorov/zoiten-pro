---
phase: 16-wb-stock-sizes
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/wb-api.ts
  - app/api/wb-sync/route.ts
  - scripts/wb-sync-stocks.js
  - tests/wb-stocks-per-warehouse.test.ts
  - tests/wb-orders-per-warehouse.test.ts
autonomous: true
requirements:
  - STOCK-32
  - STOCK-33
gap_closure: false
must_haves:
  truths:
    - "WarehouseStockItem в lib/wb-api.ts имеет поля techSize и barcode"
    - "fetchStocksPerWarehouse пропускает row.techSize и row.barcode в результат"
    - "OrdersWarehouseStats.perWarehouseSize существует как Map<string, Map<string, number>>"
    - "fetchOrdersPerWarehouse заполняет perWarehouseSize для каждого nmId"
    - "scripts/wb-sync-stocks.js использует upsert по compound key (wbCardId, warehouseId, techSize) с REPLACE (НЕ accumulate)"
    - "app/api/wb-sync/route.ts использует upsert по compound key (wbCardId, warehouseId, techSize)"
    - "Оба sync файла используют 2-step clean-replace (findMany → JS filter → deleteMany by id)"
    - "Тесты wb-stocks-per-warehouse.test.ts и wb-orders-per-warehouse.test.ts расширены проверкой techSize/barcode/perWarehouseSize"
  artifacts:
    - path: "lib/wb-api.ts"
      provides: "WarehouseStockItem.techSize, WarehouseStockItem.barcode, OrdersWarehouseStats.perWarehouseSize"
      contains: "techSize: string"
    - path: "scripts/wb-sync-stocks.js"
      provides: "Per-size upsert с REPLACE + 2-step clean-replace"
      contains: "wbCardId_warehouseId_techSize"
    - path: "app/api/wb-sync/route.ts"
      provides: "Per-size upsert + 2-step clean-replace"
      contains: "wbCardId_warehouseId_techSize"
  key_links:
    - from: "lib/wb-api.ts WarehouseStockItem"
      to: "scripts/wb-sync-stocks.js + app/api/wb-sync/route.ts"
      via: "item.techSize пропагируется в upsert call"
      pattern: "item\\.techSize"
    - from: "Оба sync файла"
      to: "Diagnostic скрипт (Wave 0)"
      via: "После re-sync diagnostic CSV diff=0"
      pattern: "node scripts/wb-stocks-diagnose.js"
---

<objective>
Фиксим **два разных бага** в **двух разных файлах** sync-кода (см. RESEARCH.md
§«Sync Bug Forensics — Detailed Analysis»):

1. **`scripts/wb-sync-stocks.js:106-122`** — `existing.quantity + qty` (accumulation
   при повторном запуске → БД ≫ API).
2. **`app/api/wb-sync/route.ts:238-264`** — upsert по 2-tuple `(wbCardId, warehouseId)`
   при том что API возвращает 6 rows per склад per размер → каждый techSize
   ПЕРЕЗАПИСЫВАЕТ предыдущий → БД содержит qty последнего techSize (БД ≪ API).

Оба путя переходят на per-size upsert по compound key
`(wbCardId, warehouseId, techSize)` с `update: { quantity: incoming }` (REPLACE).
Clean-replace переписан на 2-step pattern (Prisma не поддерживает compound NOT IN
deleteMany — см. RESEARCH.md §«Pitfall 1»).

Параллельно расширяем `WarehouseStockItem` полями `techSize`, `barcode` и
`OrdersWarehouseStats` полем `perWarehouseSize` для downstream использования
(Plan 16-03).

**Покрывает:** STOCK-32, STOCK-33.

**Не делает:** UI, server actions, lib/stock-wb-data.ts — это в Plan 16-03..05.

Purpose: После Plan 16-02 + Plan 16-06 re-sync диagnostic скрипт (Wave 0)
показывает `diff=0` — main verification критерий фазы.

Output: Sync пишет per-size rows корректно, `WarehouseStockItem` содержит
techSize/barcode, `OrdersWarehouseStats` содержит perWarehouseSize, тесты GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md

<interfaces>
<!-- Текущий API в lib/wb-api.ts (до изменений) -->

From `lib/wb-api.ts:771-780`:
```typescript
export interface WarehouseStockItem {
  warehouseName: string
  quantity: number
  inWayToClient: number
  inWayFromClient: number
}
```

From `lib/wb-api.ts:865-874`:
```typescript
export interface OrdersWarehouseStats {
  avg: number
  yesterday: number
  perWarehouse: Map<string, number>
  periodDays: number
}
```

From `lib/wb-api.ts:820-843` (fetchStocksPerWarehouse, парсинг row):
```typescript
const rows: Array<{
  nmId: number
  warehouseName: string
  quantity: number
  inWayToClient: number
  inWayFromClient: number
  quantityFull?: number
}> = await res.json()

for (const row of rows) {
  if (!nmIdSet.has(row.nmId)) continue
  const items = result.get(row.nmId) ?? []
  items.push({
    warehouseName: row.warehouseName ?? "",
    quantity: row.quantity ?? 0,
    inWayToClient: row.inWayToClient ?? 0,
    inWayFromClient: row.inWayFromClient ?? 0,
  })
  result.set(row.nmId, items)
}
```

From `lib/wb-api.ts:905-944` (fetchOrdersPerWarehouse):
```typescript
const orders = (await res.json()) as Array<{
  nmId?: number
  nm_id?: number
  warehouseName?: string
  isCancel?: boolean
  date?: string
}>
// ↑ Phase 16 расширит тип techSize?: string и заполнит perWarehouseSize
```

From `scripts/wb-sync-stocks.js:106-122` (BAGGY accumulate):
```javascript
const existing = await tx.wbCardWarehouseStock.findUnique({
  where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
  select: { quantity: true },
})
if (existing) {
  await tx.wbCardWarehouseStock.update({
    where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
    data: { quantity: existing.quantity + qty },  // ← BUG
  })
} else {
  await tx.wbCardWarehouseStock.create({
    data: { wbCardId, warehouseId, quantity: qty },
  })
}
```

From `app/api/wb-sync/route.ts:238-264` (BAGGY overwrite):
```typescript
await tx.wbCardWarehouseStock.upsert({
  where: { wbCardId_warehouseId: { wbCardId: card.id, warehouseId } },
  create: { wbCardId: card.id, warehouseId, quantity: item.quantity },
  update: { quantity: item.quantity },  // ← BUG: каждый techSize ПЕРЕЗАПИСЫВАЕТ
})
// ...
await tx.wbCardWarehouseStock.deleteMany({
  where: { wbCardId: card.id, NOT: { warehouseId: { in: incomingWarehouseIds } } },
})
```

From `prisma/schema.prisma:805-820` (PHASE 16 SHAPE — после Plan 16-01):
```prisma
model WbCardWarehouseStock {
  // ...
  techSize    String   @default("")
  // ...
  @@unique([wbCardId, warehouseId, techSize])
}
```

**Compound unique Prisma syntax** (Phase 16):
```typescript
where: {
  wbCardId_warehouseId_techSize: {
    wbCardId: card.id,
    warehouseId,
    techSize: item.techSize,
  },
}
```
</interfaces>

# Контракты

**Расширенный `WarehouseStockItem`:**
```typescript
export interface WarehouseStockItem {
  warehouseName: string
  techSize: string          // NEW: "0" если без размера, "46"/"S"/... иначе
  barcode: string           // NEW: WB barcode размерной позиции
  quantity: number
  inWayToClient: number
  inWayFromClient: number
}
```

**Расширенный `OrdersWarehouseStats`:**
```typescript
export interface OrdersWarehouseStats {
  avg: number
  yesterday: number
  perWarehouse: Map<string, number>
  perWarehouseSize: Map<string, Map<string, number>>  // NEW: warehouseName → techSize → count
  periodDays: number
}
```

**2-step clean-replace pattern** (применяется в обоих sync файлах):
```typescript
// 1. Собираем все incoming (warehouseId, techSize) ключи в массив
const incomingKeys: Array<{ warehouseId: number; techSize: string }> = []
for (const item of warehouseItems) {
  // ... upsert ...
  incomingKeys.push({ warehouseId, techSize: item.techSize ?? "" })
}

// 2. findMany existing → JS filter → deleteMany по id
const existing = await tx.wbCardWarehouseStock.findMany({
  where: { wbCardId },
  select: { id: true, warehouseId: true, techSize: true },
})
const incomingSet = new Set(incomingKeys.map((k) => `${k.warehouseId}::${k.techSize}`))
const toDelete = existing
  .filter((r) => !incomingSet.has(`${r.warehouseId}::${r.techSize}`))
  .map((r) => r.id)
if (toDelete.length > 0) {
  await tx.wbCardWarehouseStock.deleteMany({ where: { id: { in: toDelete } } })
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить WarehouseStockItem + OrdersWarehouseStats в lib/wb-api.ts; обновить тесты</name>
  <files>
    - lib/wb-api.ts
    - tests/wb-stocks-per-warehouse.test.ts
    - tests/wb-orders-per-warehouse.test.ts
  </files>
  <read_first>
    - `lib/wb-api.ts:755-960` (полностью fetchStocksPerWarehouse + fetchOrdersPerWarehouse — место изменения)
    - `tests/wb-stocks-per-warehouse.test.ts` (целиком — паттерн mock fetch + assertions)
    - `tests/wb-orders-per-warehouse.test.ts` (целиком если существует, для паттерна и расширения)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 2: WB API extension» (строки 309-336) и §«Pattern 3: per-size orders aggregation» (строки 339-379)
  </read_first>
  <behavior>
    Test cases для `tests/wb-stocks-per-warehouse.test.ts` (RED → GREEN cycle):
    - Test 1 (RED): «WarehouseStockItem содержит techSize и barcode» — fetchMock возвращает row с `techSize: "46"`, `barcode: "2044018340398"`; `result.get(nmId)![0]` имеет `.techSize === "46"`, `.barcode === "2044018340398"`.
    - Test 2 (RED): «Multiple techSize per warehouse — каждый размер отдельная WarehouseStockItem» — fetchMock возвращает 3 rows для одного nmId+warehouseName с разными techSize ("46", "48", "50"); `result.get(nmId)` имеет length 3, у каждого свой techSize и quantity.

    Test cases для `tests/wb-orders-per-warehouse.test.ts`:
    - Test 1 (RED): «perWarehouseSize заполнен» — fetchMock возвращает 4 orders {warehouseName: "Коледино", techSize: "M", isCancel: false} ×3 + {techSize: "L"} ×1; `result.get(nmId)!.perWarehouseSize.get("Коледино")` имеет {"M": 3, "L": 1}.
    - Test 2 (RED): «isCancel orders НЕ попадают в perWarehouseSize» — fetchMock возвращает 1 isCancel:true + 2 isCancel:false; perWarehouseSize содержит только не-отменённые.
    - Test 3 (RED): «Пустой techSize ('' или null) попадает как "0" в perWarehouseSize» — fetchMock с `techSize: undefined`; perWarehouseSize содержит ключ "0".
  </behavior>
  <action>
    **Изменение 1** — `lib/wb-api.ts` строки 771-780 (interface `WarehouseStockItem`):

    Заменить на:
    ```typescript
    /** Per-warehouse остаток для одного nmId на одном складе на одном размере. */
    export interface WarehouseStockItem {
      /** Название склада WB (напр. "Невинномысск", "Коледино") */
      warehouseName: string
      /** Phase 16 (STOCK-32): тех. размер ("46", "48", "S", "M" или "0" для одно-размерных) */
      techSize: string
      /** Phase 16 (STOCK-32): WB barcode размерной позиции */
      barcode: string
      /** Доступное кол-во, шт */
      quantity: number
      /** В пути к клиенту, шт */
      inWayToClient: number
      /** В пути от клиента (возвраты), шт */
      inWayFromClient: number
    }
    ```

    **Изменение 2** — `lib/wb-api.ts` строки 820-843 (тип rows + items.push):

    Расширить тип `rows`:
    ```typescript
    const rows: Array<{
      nmId: number
      warehouseName: string
      techSize?: string         // NEW
      barcode?: string          // NEW
      quantity: number
      inWayToClient: number
      inWayFromClient: number
      quantityFull?: number
    }> = await res.json()
    ```

    Расширить items.push:
    ```typescript
    items.push({
      warehouseName: row.warehouseName ?? "",
      techSize: row.techSize ?? "",       // NEW
      barcode: row.barcode ?? "",         // NEW
      quantity: row.quantity ?? 0,
      inWayToClient: row.inWayToClient ?? 0,
      inWayFromClient: row.inWayFromClient ?? 0,
    })
    ```

    **Изменение 3** — `lib/wb-api.ts` строки 865-874 (interface `OrdersWarehouseStats`):

    Расширить:
    ```typescript
    export interface OrdersWarehouseStats {
      /** Заказы минус отмены за periodDays / periodDays (шт/день). */
      avg: number
      /** Заказы минус отмены за вчерашний день (Moscow TZ), шт. */
      yesterday: number
      /** Заказы минус отмены per-warehouse по имени склада (warehouseName → count). */
      perWarehouse: Map<string, number>
      /** Phase 16 (STOCK-32): per-warehouse + per-size агрегат. warehouseName → (techSize → count) */
      perWarehouseSize: Map<string, Map<string, number>>
      /** Окно в днях (для downstream расчётов). */
      periodDays: number
    }
    ```

    **Изменение 4** — `lib/wb-api.ts` строки 905-944 (fetchOrdersPerWarehouse внутреннее):

    Расширить тип orders:
    ```typescript
    const orders = (await res.json()) as Array<{
      nmId?: number
      nm_id?: number
      warehouseName?: string
      techSize?: string         // NEW
      isCancel?: boolean
      date?: string
    }>
    ```

    После строки `const perWarehouseMap = new Map<number, Map<string, number>>()` добавить:
    ```typescript
    // Phase 16: per-warehouse + per-size агрегат
    const perWarehouseSizeMap = new Map<number, Map<string, Map<string, number>>>()
    ```

    Внутри цикла `for (const o of orders)` после блока `perWh.set(wh, ...)` добавить:
    ```typescript
    if (wh) {
      const size = (o.techSize ?? "").trim() || "0"
      let perWhSize = perWarehouseSizeMap.get(nm)
      if (!perWhSize) {
        perWhSize = new Map<string, Map<string, number>>()
        perWarehouseSizeMap.set(nm, perWhSize)
      }
      let perSize = perWhSize.get(wh)
      if (!perSize) {
        perSize = new Map<string, number>()
        perWhSize.set(wh, perSize)
      }
      perSize.set(size, (perSize.get(size) ?? 0) + 1)
    }
    ```

    **Изменение 4.5 (W6)** — точечный Find/Replace в финальном цикле где собирается
    `result.set(nmId, {...})` (строки ~945-957 в `lib/wb-api.ts`).

    ИСХОДНЫЙ блок:
    ```typescript
      for (const nmId of requested) {
        const t = totals.get(nmId) ?? 0
        const y = yesterdayCounts.get(nmId) ?? 0
        const perWh = perWarehouseMap.get(nmId) ?? new Map<string, number>()
        if (t === 0 && y === 0 && perWh.size === 0) continue
        result.set(nmId, {
          avg: t / periodDays,
          yesterday: y,
          perWarehouse: perWh,
          periodDays,
        })
      }
    ```

    ЦЕЛЕВОЙ блок (заменить — добавить `perWarehouseSize` поле, skip-условие НЕ
    меняется чтобы не нарушить существующее поведение):
    ```typescript
      for (const nmId of requested) {
        const t = totals.get(nmId) ?? 0
        const y = yesterdayCounts.get(nmId) ?? 0
        const perWh = perWarehouseMap.get(nmId) ?? new Map<string, number>()
        // Phase 16 (STOCK-32): per-warehouse + per-size агрегат для размерных строк UI
        const perWhSize = perWarehouseSizeMap.get(nmId) ?? new Map<string, Map<string, number>>()
        if (t === 0 && y === 0 && perWh.size === 0) continue
        result.set(nmId, {
          avg: t / periodDays,
          yesterday: y,
          perWarehouse: perWh,
          perWarehouseSize: perWhSize,
          periodDays,
        })
      }
    ```

    **Замечания (W6):**
    - skip-условие `t === 0 && y === 0 && perWh.size === 0` — НЕ ТРОГАТЬ. `perWarehouseSize`
      не участвует в фильтрации (если perWh.size === 0, то perWhSize по конструкции тоже пуст).
    - Объявление `const perWhSize` — ПЕРЕД skip-условием, чтобы не делать дублирующий
      lookup в `result.set`.

    **Изменение 5** — `tests/wb-stocks-per-warehouse.test.ts`:

    Добавить НОВЫЕ test cases в конец `describe(...)` блока, ПЕРЕД закрывающей скобкой `})`:
    ```typescript
    it("Phase 16 (STOCK-32): WarehouseStockItem содержит techSize и barcode", async () => {
      const { fetchStocksPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          {
            warehouseName: "Котовск",
            nmId: 859398279,
            techSize: "46",
            barcode: "2044018340398",
            quantity: 11,
            inWayToClient: 0,
            inWayFromClient: 0,
          },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchStocksPerWarehouse([859398279])
      const item = result.get(859398279)![0]!
      expect(item.techSize).toBe("46")
      expect(item.barcode).toBe("2044018340398")
    })

    it("Phase 16 (STOCK-32): несколько techSize для одного warehouseName — каждый отдельная WarehouseStockItem", async () => {
      const { fetchStocksPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { warehouseName: "Котовск", nmId: 859398279, techSize: "46", barcode: "b46", quantity: 11, inWayToClient: 0, inWayFromClient: 0 },
          { warehouseName: "Котовск", nmId: 859398279, techSize: "48", barcode: "b48", quantity: 10, inWayToClient: 0, inWayFromClient: 0 },
          { warehouseName: "Котовск", nmId: 859398279, techSize: "50", barcode: "b50", quantity: 10, inWayToClient: 0, inWayFromClient: 0 },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchStocksPerWarehouse([859398279])
      const items = result.get(859398279)!
      expect(items).toHaveLength(3)
      const sizes = items.map((i) => i.techSize).sort()
      expect(sizes).toEqual(["46", "48", "50"])
    })

    it("Phase 16 (STOCK-32): отсутствие techSize/barcode → пустые строки", async () => {
      const { fetchStocksPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { warehouseName: "Склад X", nmId: 999, quantity: 5, inWayToClient: 0, inWayFromClient: 0 },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchStocksPerWarehouse([999])
      const item = result.get(999)![0]!
      expect(item.techSize).toBe("")
      expect(item.barcode).toBe("")
    })
    ```

    **Изменение 6** — `tests/wb-orders-per-warehouse.test.ts`:

    Если файл существует, добавить test cases в конец основного `describe(...)` блока:
    ```typescript
    it("Phase 16 (STOCK-32): perWarehouseSize заполнен per warehouseName per techSize", async () => {
      const { fetchOrdersPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { nmId: 100, warehouseName: "Коледино", techSize: "M", isCancel: false, date: "2026-04-20T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", techSize: "M", isCancel: false, date: "2026-04-21T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", techSize: "M", isCancel: false, date: "2026-04-22T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", techSize: "L", isCancel: false, date: "2026-04-22T11:00:00" },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchOrdersPerWarehouse([100], 7)
      const stats = result.get(100)!
      const koledino = stats.perWarehouseSize.get("Коледино")!
      expect(koledino.get("M")).toBe(3)
      expect(koledino.get("L")).toBe(1)
    })

    it("Phase 16 (STOCK-32): isCancel:true исключаются из perWarehouseSize", async () => {
      const { fetchOrdersPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { nmId: 200, warehouseName: "Котовск", techSize: "46", isCancel: false, date: "2026-04-20T10:00:00" },
          { nmId: 200, warehouseName: "Котовск", techSize: "46", isCancel: true,  date: "2026-04-21T10:00:00" },
          { nmId: 200, warehouseName: "Котовск", techSize: "48", isCancel: false, date: "2026-04-22T10:00:00" },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchOrdersPerWarehouse([200], 7)
      const stats = result.get(200)!
      const kotovsk = stats.perWarehouseSize.get("Котовск")!
      expect(kotovsk.get("46")).toBe(1)  // отменённый исключён
      expect(kotovsk.get("48")).toBe(1)
    })

    it("Phase 16 (STOCK-32): отсутствие techSize → ключ '0' в perWarehouseSize", async () => {
      const { fetchOrdersPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { nmId: 300, warehouseName: "Склад X", isCancel: false, date: "2026-04-22T10:00:00" },
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchOrdersPerWarehouse([300], 7)
      const stats = result.get(300)!
      const wh = stats.perWarehouseSize.get("Склад X")!
      expect(wh.get("0")).toBe(1)
    })

    it("Phase 16 (STOCK-32, W11): nmId с avg=0, yesterday=0, perWh.size=0 → НЕ попадает в result Map (skip-condition)", async () => {
      // W11: проверяет что Изменение 4.5 не нарушает skip-condition.
      // nmId 999 в requested, но fetch возвращает orders только для других nmId
      // → totals/yesterday/perWh для 999 пустые → skip → result.has(999) === false
      const { fetchOrdersPerWarehouse } = await import("@/lib/wb-api")
      const fetchMock = vi.fn().mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => [
          { nmId: 100, warehouseName: "Коледино", techSize: "M", isCancel: false, date: "2026-04-22T10:00:00" },
          // 999 — нет ни одного заказа
        ],
      })
      vi.stubGlobal("fetch", fetchMock)
      const result = await fetchOrdersPerWarehouse([100, 999], 7)
      // 100 — попадает (есть заказ)
      expect(result.has(100)).toBe(true)
      // 999 — НЕ попадает (skip: t=0 && y=0 && perWh.size=0)
      expect(result.has(999)).toBe(false)
    })
    ```

    **Замечание (B1 + W11):** файл `tests/wb-orders-per-warehouse.test.ts` УЖЕ
    СУЩЕСТВУЕТ (5 КБ, 7 `it` блоков) — это **расширение**, не создание. Все 4 новых
    test cases добавляются в конец существующего `describe("fetchOrdersPerWarehouse")` блока,
    ПЕРЕД закрывающей скобкой `})`.

    Если файл по какой-то причине отсутствовал бы — создать с минимальным
    `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"` +
    `describe("fetchOrdersPerWarehouse Phase 16", () => { beforeEach(() => { vi.useFakeTimers(); vi.stubEnv("WB_API_TOKEN","test-token") }); afterEach(() => { vi.useRealTimers(); vi.unstubAllGlobals(); vi.unstubAllEnvs() }); /* test cases выше */ })`.

    После Изменения 1-6 запустить `npx prisma generate` локально (если возможно)
    или хотя бы `npm run test -- tests/wb-stocks-per-warehouse.test.ts tests/wb-orders-per-warehouse.test.ts` — все green.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-stocks-per-warehouse.test.ts tests/wb-orders-per-warehouse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "techSize: string" lib/wb-api.ts` >= 1 (в WarehouseStockItem)
    - `grep -c "barcode: string" lib/wb-api.ts` >= 1 (в WarehouseStockItem)
    - `grep -c "perWarehouseSize: Map<string, Map<string, number>>" lib/wb-api.ts` >= 1
    - `grep -c "row.techSize ?? \"\"" lib/wb-api.ts` >= 1 (пропагирование)
    - `grep -c "row.barcode ?? \"\"" lib/wb-api.ts` >= 1
    - `grep -c "perWarehouseSizeMap" lib/wb-api.ts` >= 1
    - `grep -c "STOCK-32" tests/wb-stocks-per-warehouse.test.ts` >= 3 (3 новых test cases)
    - `grep -c "STOCK-32" tests/wb-orders-per-warehouse.test.ts` >= 4 (4 новых test cases: perWarehouseSize, isCancel, '0' default, W11-skip)
    - `grep -c "W11" tests/wb-orders-per-warehouse.test.ts` >= 1 (W11 skip-condition test)
    - `npm run test -- tests/wb-stocks-per-warehouse.test.ts` exit 0 (все existing+new green)
    - `npm run test -- tests/wb-orders-per-warehouse.test.ts` exit 0
  </acceptance_criteria>
  <done>
    `WarehouseStockItem` и `OrdersWarehouseStats` расширены, `fetchStocksPerWarehouse`
    и `fetchOrdersPerWarehouse` пропагируют techSize/barcode/perWarehouseSize, тесты
    GREEN на новых assertions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sync-bug fix в scripts/wb-sync-stocks.js — REPLACE + per-size + 2-step clean-replace</name>
  <files>scripts/wb-sync-stocks.js</files>
  <read_first>
    - `scripts/wb-sync-stocks.js:90-132` (целиком блок per-warehouse upsert + clean-replace)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 5: scripts/wb-sync-stocks.js fix» (строки 463-485)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Hypothesis 1 #File 1» (строки 637-665) — trace bug
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pitfall 1: Compound NOT IN» (строки 769-787)
  </read_first>
  <action>
    **Цель:** заменить накапливающий upsert на per-size REPLACE с compound key
    `(wbCardId, warehouseId, techSize)` и переписать clean-replace на 2-step
    pattern.

    **Шаг A.** Найти блок upsert строки ~106-122 (find/update/create по `existing`):
    ```javascript
    const existing = await tx.wbCardWarehouseStock.findUnique({
      where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
      select: { quantity: true },
    })

    if (existing) {
      await tx.wbCardWarehouseStock.update({
        where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
        data: { quantity: existing.quantity + qty },
      })
    } else {
      await tx.wbCardWarehouseStock.create({
        data: { wbCardId, warehouseId, quantity: qty },
      })
      inserted++
    }
    ```

    **Заменить на:**
    ```javascript
    // Phase 16 (STOCK-33): per-size upsert через compound unique (wbCardId, warehouseId, techSize)
    // REPLACE (НЕ existing.quantity + qty — это был бug, см. RESEARCH §Hypothesis 1).
    const techSize = item.techSize || ""
    const wasExisting = await tx.wbCardWarehouseStock.findUnique({
      where: {
        wbCardId_warehouseId_techSize: { wbCardId, warehouseId, techSize },
      },
      select: { id: true },
    })
    await tx.wbCardWarehouseStock.upsert({
      where: {
        wbCardId_warehouseId_techSize: { wbCardId, warehouseId, techSize },
      },
      create: { wbCardId, warehouseId, techSize, quantity: qty },
      update: { quantity: qty },  // REPLACE
    })
    if (!wasExisting) inserted++
    ```

    **Шаг B.** Заменить `incoming` на `incomingKeys` (массив пар, не Set warehouseId):

    Перед циклом `for (const item of items)` (или после получения warehouseItems) заменить:
    ```javascript
    const incoming = new Set()
    ```
    на:
    ```javascript
    const incomingKeys = []  // [{ warehouseId, techSize }, ...]
    ```

    Внутри цикла после `incoming.add(warehouseId)` (или эквивалент) добавить:
    ```javascript
    incomingKeys.push({ warehouseId, techSize: item.techSize || "" })
    ```

    Если `incoming.add` не нужен дальше — удалить.

    **Шаг C.** Заменить блок clean-replace deleteMany (строки ~125-130):
    ```javascript
    await tx.wbCardWarehouseStock.deleteMany({
      where: {
        wbCardId,
        NOT: { warehouseId: { in: [...incoming] } },
      },
    })
    ```

    **Заменить на 2-step pattern:**
    ```javascript
    // Phase 16: 2-step clean-replace — Prisma не поддерживает compound NOT IN
    // (см. RESEARCH §Pitfall 1). findMany → JS-фильтр → deleteMany по id.
    const existingRows = await tx.wbCardWarehouseStock.findMany({
      where: { wbCardId },
      select: { id: true, warehouseId: true, techSize: true },
    })
    const incomingSet = new Set(
      incomingKeys.map((k) => `${k.warehouseId}::${k.techSize}`),
    )
    const toDeleteIds = existingRows
      .filter((r) => !incomingSet.has(`${r.warehouseId}::${r.techSize}`))
      .map((r) => r.id)
    if (toDeleteIds.length > 0) {
      await tx.wbCardWarehouseStock.deleteMany({
        where: { id: { in: toDeleteIds } },
      })
    }
    ```

    **Шаг D.** Убедиться, что `existing` в строке 106 (single var) больше не
    используется — удалить если рудимент.

    **Шаг E.** Проверить counter `inserted++` — он остаётся для логов («inserted N
    new rows»), inserted = wasExisting === null случаев.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('scripts/wb-sync-stocks.js','utf-8'); if(c.includes('existing.quantity + qty')) throw 'BUG STILL THERE'; if(!c.includes('wbCardId_warehouseId_techSize')) throw 'no compound unique'; if(!c.includes('incomingKeys')) throw 'no incomingKeys array'; if(!c.includes('incomingSet')) throw 'no incomingSet'; if(c.match(/NOT:\s*\{\s*warehouseId:\s*\{\s*in:\s*\[\.\.\.incoming\]/)) throw 'old NOT IN deleteMany still there'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "existing.quantity + qty" scripts/wb-sync-stocks.js` == 0 (старый bug удалён)
    - `grep -c "wbCardId_warehouseId_techSize" scripts/wb-sync-stocks.js` >= 2 (findUnique + upsert)
    - `grep -c "incomingKeys" scripts/wb-sync-stocks.js` >= 2 (объявление + push)
    - `grep -c "incomingSet" scripts/wb-sync-stocks.js` >= 1 (Set из incomingKeys)
    - `grep -c "NOT.*warehouseId.*incoming" scripts/wb-sync-stocks.js` == 0 (старый pattern удалён)
    - `grep -c "STOCK-33" scripts/wb-sync-stocks.js` >= 1 (комментарий-якорь для traceability)
    - `node --check scripts/wb-sync-stocks.js` exit 0
  </acceptance_criteria>
  <done>
    Скрипт `scripts/wb-sync-stocks.js` пишет per-size rows через REPLACE upsert,
    использует 2-step clean-replace, готов к запуску на VPS в Plan 16-06 после
    применения миграции.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync-bug fix в app/api/wb-sync/route.ts — per-size upsert + 2-step clean-replace</name>
  <files>app/api/wb-sync/route.ts</files>
  <read_first>
    - `app/api/wb-sync/route.ts:200-280` (полностью блок per-warehouse stocks upsert + clean-replace; видно что используются `warehouseItems` и `incomingWarehouseIds`)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Pattern 4: Sync bug fix in app/api/wb-sync/route.ts» (строки 384-449)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Hypothesis 1 #File 2» (строки 667-695) — trace bug
  </read_first>
  <action>
    **Шаг A.** Найти блок upsert строки ~238-253:
    ```typescript
    await tx.wbCardWarehouseStock.upsert({
      where: {
        wbCardId_warehouseId: {
          wbCardId: card.id,
          warehouseId,
        },
      },
      create: {
        wbCardId: card.id,
        warehouseId,
        quantity: item.quantity,
      },
      update: {
        quantity: item.quantity,
      },
    })
    ```

    **Заменить на:**
    ```typescript
    // Phase 16 (STOCK-33): per-size upsert через compound unique
    // (wbCardId, warehouseId, techSize). До Phase 16 был bug: 6 rows одного
    // склада (per techSize) ПЕРЕЗАПИСЫВАЛИ друг друга, БД содержала qty
    // последнего techSize. См. RESEARCH §Hypothesis 1 #File 2.
    const techSize = item.techSize || ""
    await tx.wbCardWarehouseStock.upsert({
      where: {
        wbCardId_warehouseId_techSize: {
          wbCardId: card.id,
          warehouseId,
          techSize,
        },
      },
      create: {
        wbCardId: card.id,
        warehouseId,
        techSize,
        quantity: item.quantity,
      },
      update: {
        quantity: item.quantity,  // REPLACE — не суммировать
      },
    })
    ```

    **Шаг B.** Заменить локальную переменную `incomingWarehouseIds` (массив `Int`)
    на `incomingKeys` (массив пар).

    Найти declaration:
    ```typescript
    const incomingWarehouseIds: number[] = []
    ```
    Заменить на:
    ```typescript
    const incomingKeys: Array<{ warehouseId: number; techSize: string }> = []
    ```

    Внутри цикла найти:
    ```typescript
    incomingWarehouseIds.push(warehouseId)
    ```
    Заменить на:
    ```typescript
    incomingKeys.push({ warehouseId, techSize: item.techSize || "" })
    ```

    **Шаг C.** Заменить блок clean-replace (строки ~256-264):
    ```typescript
    if (incomingWarehouseIds.length > 0) {
      await tx.wbCardWarehouseStock.deleteMany({
        where: {
          wbCardId: card.id,
          NOT: { warehouseId: { in: incomingWarehouseIds } },
        },
      })
    }
    ```

    **Заменить на 2-step pattern:**
    ```typescript
    // Phase 16 (STOCK-33): 2-step clean-replace — Prisma не поддерживает
    // compound NOT IN deleteMany (см. RESEARCH §Pitfall 1).
    if (incomingKeys.length > 0) {
      const existingRows = await tx.wbCardWarehouseStock.findMany({
        where: { wbCardId: card.id },
        select: { id: true, warehouseId: true, techSize: true },
      })
      const incomingSet = new Set(
        incomingKeys.map((k) => `${k.warehouseId}::${k.techSize}`),
      )
      const toDeleteIds = existingRows
        .filter((r) => !incomingSet.has(`${r.warehouseId}::${r.techSize}`))
        .map((r) => r.id)
      if (toDeleteIds.length > 0) {
        await tx.wbCardWarehouseStock.deleteMany({
          where: { id: { in: toDeleteIds } },
        })
      }
    }
    ```

    **Шаг D.** Денормализация `WbCard.stockQty` строка ~268:
    ```typescript
    const totalStock = warehouseItems.reduce((s, w) => s + w.quantity, 0)
    ```
    Это ЛОГИКА продолжает работать — `warehouseItems` теперь имеет per-size rows,
    `reduce` суммирует все. Не менять.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/api/wb-sync/route.ts','utf-8'); /* B3: context-aware regex anchored на wbCardWarehouseStock.upsert (не WbCardWarehouseOrders из Phase 15) */ if(/tx\.wbCardWarehouseStock\.upsert\(\s*\{[\s\S]{0,200}?wbCardId_warehouseId:\s*\{\s*wbCardId:\s*card\.id,\s*warehouseId,?\s*\}/.test(c)) throw 'old 2-tuple wbCardWarehouseStock upsert still there'; if(!c.includes('wbCardId_warehouseId_techSize')) throw 'no compound unique'; if(!c.includes('incomingKeys')) throw 'no incomingKeys'; /* B3: stocks-block-only check для incomingWarehouseIds — должен исчезнуть из stocks-block (lines ~200-264), но остаться в orders-block (lines ~311-369, Phase 15) */ const stocksBlock = c.split('Phase 15 (ORDERS-02): Clean-replace per-warehouse orders')[0]; if(/incomingWarehouseIds/.test(stocksBlock)) throw 'incomingWarehouseIds still used in stocks block (should be incomingKeys)'; if(/tx\.wbCardWarehouseStock\.deleteMany[\s\S]{0,500}?NOT:\s*\{\s*warehouseId:\s*\{\s*in:\s*incomingWarehouseIds/.test(c)) throw 'old stocks deleteMany NOT IN still there'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wbCardId_warehouseId_techSize" app/api/wb-sync/route.ts` >= 1 (новый upsert key для stocks)
    - `node -e "const c=require('fs').readFileSync('app/api/wb-sync/route.ts','utf-8'); /* B3: должно быть ровно ОДНО вхождение wbCardId_warehouseId БЕЗ techSize — у wbCardWarehouseOrders.upsert (Phase 15, НЕ ТРОГАТЬ). Stocks-блок переехал на _techSize. */ const matches = c.match(/wbCardId_warehouseId:\s*\{/g) || []; if (matches.length !== 1) throw 'expected exactly 1 (orders-block from Phase 15), got '+matches.length; console.log('OK')"` exit 0
    - `grep -c "incomingKeys" app/api/wb-sync/route.ts` >= 2 (declaration + push в stocks block)
    - `node -e "const c=require('fs').readFileSync('app/api/wb-sync/route.ts','utf-8'); /* B3: incomingWarehouseIds должен оставаться в orders-block (Phase 15) — это ЛЕГАЛЬНО. После фикса должен быть ровно 4 раза (declaration + push + length check + NOT IN), все в orders-block. */ const n = (c.match(/incomingWarehouseIds/g) || []).length; if (n !== 4) throw 'expected exactly 4 incomingWarehouseIds (orders-block), got '+n; console.log('OK')"` exit 0
    - `grep -c "STOCK-33" app/api/wb-sync/route.ts` >= 1 (traceability anchor)
    - `npx tsc --noEmit` exit 0 (TypeScript типы валидны после Prisma generate в Plan 16-01)
  </acceptance_criteria>
  <done>
    Route handler делает per-size upsert REPLACE + 2-step clean-replace.
    Готов к re-sync в Plan 16-06.
  </done>
</task>

</tasks>

<verification>
- `WarehouseStockItem` имеет techSize и barcode
- `OrdersWarehouseStats` имеет perWarehouseSize
- `fetchStocksPerWarehouse` пропагирует techSize/barcode из row в WarehouseStockItem
- `fetchOrdersPerWarehouse` заполняет perWarehouseSize per (nmId, warehouseName, techSize)
- `scripts/wb-sync-stocks.js` использует upsert по compound key с REPLACE
- `app/api/wb-sync/route.ts` использует upsert по compound key с REPLACE
- Оба sync файла используют 2-step clean-replace
- `npm run test -- tests/wb-stocks-per-warehouse.test.ts tests/wb-orders-per-warehouse.test.ts` GREEN
- `npx tsc --noEmit` exit 0
</verification>

<success_criteria>
- Plan 16-03 может потреблять `WarehouseStockItem.techSize` и `OrdersWarehouseStats.perWarehouseSize`
- Plan 16-06 после применения миграции + re-sync достигнет diff=0 в diagnostic скрипте
- Никаких изменений в UI / server actions / lib/stock-wb-data.ts / других местах
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-02-SUMMARY.md`
с описанием:
- Расширения `WarehouseStockItem` и `OrdersWarehouseStats`
- Точные diff-описания обоих sync файлов (before/after)
- Ссылки на тесты которые подтверждают новое поведение
- Roadmap-attribution: STOCK-32, STOCK-33
- Критическое условие для UAT: только Plan 16-06 после миграции даёт реальный
  re-sync; до этого locally данных в БД нет (или ошибки compound key)
</output>
