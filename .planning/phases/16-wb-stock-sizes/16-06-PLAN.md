---
phase: 16-wb-stock-sizes
plan: 06
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
  - "05"
files_modified:
  - .planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md
autonomous: false
requirements:
  - STOCK-37
gap_closure: false
must_haves:
  truths:
    - "16-HUMAN-UAT.md создан с 9 пунктами проверки"
    - "Миграция 20260423_phase16_size_breakdown применена на VPS"
    - "Re-sync выполнен через node scripts/wb-sync-stocks.js (или UI кнопка «Обновить из WB»)"
    - "node scripts/wb-stocks-diagnose.js после re-sync показывает diff=0 для всех контрольных rows"
    - "Контрольные nmId 859398279, 901585883 показывают корректные per-size данные"
    - "Кнопка «По размерам» persist между сессиями"
  artifacts:
    - path: ".planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md"
      provides: "9-point UAT чеклист"
      min_lines: 60
  key_links:
    - from: "deploy.sh + prisma migrate deploy"
      to: "WbCardWarehouseStock.techSize в проде"
      via: "ALTER TABLE + DELETE legacy"
      pattern: "phase16_size_breakdown"
    - from: "node scripts/wb-stocks-diagnose.js"
      to: "Verification diff=0"
      via: "Сравнение API vs БД snapshot"
      pattern: "diff: 0"
---

<objective>
Финальный план Phase 16 — deploy на VPS + re-sync + manual UAT.

**Шаги:**
1. **Deploy:** `bash deploy.sh` на VPS — применит миграцию `20260423_phase16_size_breakdown`
   через `prisma migrate deploy`
2. **DELETE legacy подтверждение:** после миграции таблица `WbCardWarehouseStock`
   пустая (DELETE WHERE techSize='' выполнился). UI /stock/wb пустой ~30 секунд
3. **Re-sync:** запустить `node scripts/wb-sync-stocks.js` на VPS ИЛИ нажать
   «Обновить из WB» в UI /stock — заполнит таблицу per-size rows из WB API
4. **Diagnostic verification:** `node scripts/wb-stocks-diagnose.js` → CSV должен
   быть пустой (только header) ИЛИ `diff` колонка везде 0 — это критерий success
5. **Manual UAT:** пользователь проходит по 9-пунктовому чеклисту в `16-HUMAN-UAT.md`
6. **ROADMAP update:** Phase 16 → Complete + актуализация Plans count

**Покрывает:** STOCK-37.

**Не делает:** изменения в коде — только UAT-документация и deploy/re-sync операции.

**Зависимости:** все предыдущие планы (02 — sync fix, 04 — server action, 05 — UI).

**autonomous: false** — этот план содержит human-verify checkpoint.

Purpose: Phase 16 ship-готов только когда верификация на проде показывает что
- размерные строки видны для контрольных nmId
- diff=0 в diagnostic script
- кнопка persist
- sticky-table layout работает при expand-all + showSizes ON

Output: 16-HUMAN-UAT.md (создан), Phase 16 → Complete в ROADMAP, milestone
завершён.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wb-stock-sizes/16-CONTEXT.md
@.planning/phases/16-wb-stock-sizes/16-RESEARCH.md
@.planning/phases/16-wb-stock-sizes/16-W0-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-01-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-02-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-03-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-04-SUMMARY.md
@.planning/phases/16-wb-stock-sizes/16-05-SUMMARY.md

<interfaces>
<!-- Существующий deploy.sh поведение -->

`deploy.sh` (на VPS):
1. `git pull`
2. `npm ci --omit=dev`
3. `npx prisma migrate deploy` — применит миграцию из `prisma/migrations/`
4. `npm run build` (standalone)
5. `cp -r public .next/standalone/` + `cp -r .next/static .next/standalone/.next/`
6. `systemctl restart zoiten-erp`

После шага 3 миграция `20260423_phase16_size_breakdown` сделает:
- ADD COLUMN techSize → WbCardWarehouseStock
- Compound unique pkdate
- DELETE FROM WbCardWarehouseStock WHERE techSize = '' → таблица ~ПУСТА
- ADD COLUMN stockWbShowSizes → User

<!-- Существующий wb-sync-stocks.js (после Plan 16-02) -->

После 16-02:
- per-size upsert REPLACE
- 2-step clean-replace
- Корректно заполнит per-size rows для всех ~50 nmId

<!-- Существующий /stock/wb (после 16-04 + 16-05) -->

UI:
- Кнопка «По размерам» в toolbar
- Размерные строки под per-nmId при `showSizes && hasMultipleSizes`
- bg-muted/30, ↳ {techSize}, placeholder в Иваново/in-way

<!-- Контрольные nmId -->

- **859398279** — Брюки классические, УКТ-000029 — 8 размеров (46/48/50/52/54/56/58/60)
- **901585883** — Костюм классический двойка, УКТ-000030 — 8 размеров

Известные данные API на 2026-04-22 для nmId 859398279, склад Котовск:
46:11, 48:10, 50:10, 54:10, 58:10, 60:10 → итого 61 шт.

В БД до фикса: Котовск qty=8 (последний techSize ПЕРЕЗАПИСАЛ предыдущие).
После фикса: 6 строк с qty {11,10,10,10,10,10}, sum=61.
</interfaces>

# Структура UAT чеклиста

```markdown
# Phase 16 — Human UAT

**Дата:** {заполнить дату deploy}
**Версия:** {git rev-parse --short HEAD}
**Migrated:** {timestamp ALTER TABLE на VPS}
**Re-synced:** {timestamp последнего sync}

## Чеклист (9 пунктов)

### 1. /stock/wb открывается без ошибок
[ ] Открыть https://zoiten.pro/stock/wb
[ ] Страница рендерится, нет «500 Internal Server Error», нет «Hydration mismatch»
[ ] Console (DevTools) — нет красных ошибок

### 2. Кнопка «По размерам» появилась и работает
[ ] В toolbar присутствует кнопка «По размерам» (рядом с «Без СЦ» / «Склады»)
[ ] При нажатии: размерные строки появляются (или скрываются) с приглушённым фоном
[ ] Кнопка меняет variant: outline ↔ default
[ ] Refresh страницы — состояние persist (т.е. если включил → после refresh всё ещё включён)

### 3. nmId 859398279 — Брюки: sum размеров = stockQty
[ ] Найти строку с nmId 859398279 в /stock/wb
[ ] Проверить колонку «Итого склады WB → О» = sum(всех размерных строк → totalStock)
[ ] Числа должны быть консистентны (например stockQty=412 → sum размеров=412)

### 4. nmId 859398279 Котовск — 6 размерных строк с правильными числами
[ ] Развернуть кластер где находится Котовск (предположительно ЦФО)
[ ] Включить «По размерам»
[ ] Найти размерные строки: ожидаются 46:11, 48:10, 50:10, 54:10, 58:10, 60:10
    (sizes отсортированы ASC)
[ ] Сумма по размерам в Котовск = 61

### 5. Per-cluster агрегаты при «Без СЦ»/hidden warehouses не меняются
[ ] Включить «По размерам»
[ ] Включить «Без СЦ» (hideSc)
[ ] Сравнить per-cluster O в кластерных колонках до/после: значения не должны
    измениться (агрегаты считаются по всем складам, скрытие визуальное)
[ ] То же самое с per-user скрытием через popover «Склады»

### 6. one-size товары без размерных строк
[ ] Найти nmId с techSize="0" (одно-размерный, например пылесос — quick поиск)
[ ] При showSizes ON размерных строк под этим nmId НЕТ (одинокий "0" не дублируется)

### 7. Sticky cells не пересекаются при showSizes + expand-all
[ ] «Развернуть все» (все кластеры expanded)
[ ] «По размерам» включить
[ ] Скроллить вертикально + горизонтально
[ ] Sticky колонки Фото/Сводка/Артикул/Иваново остаются на месте, не дублируются,
    не «съезжают»

### 8. /inventory → /stock редирект работает
[ ] Открыть https://zoiten.pro/inventory/wb
[ ] Должен быть 308 redirect на /stock/wb (старые закладки работают)

### 9. Diagnostic CSV: diff=0
[ ] На VPS: `cd /opt/zoiten-pro && WB_API_TOKEN=... node scripts/wb-stocks-diagnose.js`
[ ] Файл `wb-stocks-diff-{date}.csv` содержит ТОЛЬКО header (нет строк с diff!=0)
    ИЛИ console «No diffs found»
[ ] Если есть diffs — записать в Blockers (дальше расследование)

## Result

[ ] All 9 points PASS — Phase 16 верифицирован
[ ] Записать в STATE.md и SUMMARY.md
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Создать .planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md</name>
  <files>.planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/16-wb-stock-sizes/16-CONTEXT.md` §specifics (контрольные nmId)
    - `.planning/phases/16-wb-stock-sizes/16-RESEARCH.md` §«Plan 16-06: Deploy + re-sync + UAT» (строки 1256-1273)
    - `.planning/phases/16-wb-stock-sizes/16-VALIDATION.md` §«Re-sync UAT Steps» (строки 81-94)
  </read_first>
  <action>
    Создать файл `.planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md` со следующим содержимым:

    ```markdown
    # Phase 16 — Human UAT

    **Phase:** 16-wb-stock-sizes
    **Дата deploy:** _________________ (заполнить после `bash deploy.sh`)
    **Git rev:** _________________ (`git rev-parse --short HEAD` после deploy)
    **Migration applied:** _________________ (timestamp когда `prisma migrate deploy` отработал)
    **Re-synced:** _________________ (timestamp последнего `node scripts/wb-sync-stocks.js`)
    **Tester:** _________________ (имя)

    ---

    ## Pre-UAT (Deploy + Re-sync)

    ### Deploy на VPS

    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    ```

    После завершения проверить логи:
    - `prisma migrate deploy` применил `20260423_phase16_size_breakdown` — без ошибок
    - `npm run build` собран — без ошибок TypeScript
    - `systemctl status zoiten-erp` — active

    ### Re-sync

    На VPS:

    ```bash
    cd /opt/zoiten-pro
    node scripts/wb-sync-stocks.js
    ```

    Скрипт работает ~1-2 минуты (rate limit Statistics API). Должен вывести
    сколько rows обновил без accumulation/overwrite ошибок.

    Альтернатива: открыть https://zoiten.pro/stock и нажать «Обновить из WB»
    (запустит `/api/wb-sync` HTTP route — Plan 16-02 идентично пишет per-size).

    После re-sync таблица `WbCardWarehouseStock` имеет per-size rows для всех
    nmId. До re-sync таблица была пустой (legacy DELETE в миграции).

    ---

    ## UAT Чеклист (9 пунктов)

    ### 1. `/stock/wb` открывается без ошибок

    [ ] Открыть https://zoiten.pro/stock/wb
    [ ] Страница рендерится без «500 Internal Server Error» и без «Hydration mismatch»
    [ ] Console (DevTools → F12) — нет красных ошибок

    **Если FAIL:** проверить `journalctl -u zoiten-erp -n 100`, обычно
    Prisma generate не отработал или TS-ошибка не сборки.

    ### 2. Кнопка «По размерам» появилась и работает

    [ ] В toolbar `/stock/wb` присутствует кнопка «По размерам» рядом с «Без СЦ» /
        «Склады»
    [ ] При нажатии: размерные строки появляются (или скрываются) с приглушённым
        фоном `bg-muted/30`
    [ ] Кнопка меняет variant: outline (выкл) ↔ default (вкл)
    [ ] Refresh страницы (F5) — состояние persist в БД

    **Если FAIL persist:** проверить таблицу `User.stockWbShowSizes` — `SELECT id,
    "stockWbShowSizes" FROM "User" WHERE id = '<your-user-id>';` — должно меняться.

    ### 3. nmId 859398279 — Брюки: sum размеров = stockQty карточки

    Контрольный товар: nmId **859398279** «Брюки классические мужские прямые»
    (УКТ-000029, 8 размеров).

    [ ] Найти строку этого nmId в `/stock/wb`
    [ ] Запомнить значение колонки «Итого склады WB → О»
    [ ] Включить «По размерам»
    [ ] Сумма totalStock всех размерных строк под этой nmId = значение «О»
    [ ] (опционально) `SELECT "stockQty" FROM "WbCard" WHERE "nmId" = 859398279;`
        — то же значение

    ### 4. nmId 859398279 Котовск — 6 размерных строк {46:11, 48:10, 50:10, 54:10, 58:10, 60:10}

    [ ] Развернуть кластер с Котовском (см. справочник `WbWarehouse`, обычно ЦФО
        или ПФО — после `seed-wb-warehouses` уточнить)
    [ ] Включить «По размерам»
    [ ] В колонке Котовск под nmId 859398279 видны размерные строки. Ожидаемые
        числа (на момент 2026-04-22):
        - 46 → 11
        - 48 → 10
        - 50 → 10
        - 54 → 10
        - 58 → 10
        - 60 → 10
        - Сумма Котовск = 61
    [ ] sortSizes — размеры в порядке ASC numeric (46 → 60)

    **Замечание:** реальные числа могли измениться с 2026-04-22, главное —
    consistency между API snapshot и БД (см. пункт 9).

    ### 5. Per-cluster агрегаты при «Без СЦ» / hidden warehouses не меняются

    Locked в CONTEXT.md: фильтры визуальные, агрегаты считаются по всем складам.

    [ ] Зафиксировать значение колонки «ЦФО → О» (сводно по кластеру)
    [ ] Включить «По размерам»
    [ ] Включить «Без СЦ» (`hideSc`)
    [ ] «ЦФО → О» НЕ изменилось
    [ ] То же самое с per-user скрытием через popover «Склады» — выключить какой-нибудь
        склад и убедиться что cluster aggregate не меняется

    ### 6. one-size товары без размерных строк

    [ ] Найти nmId где `techSize = "0"` (одно-размерный товар — пылесос, чайник,
        ароматизатор и т.п.). Quick поиск:
        ```sql
        SELECT DISTINCT "wbCardId" FROM "WbCardWarehouseStock"
        WHERE "techSize" = '0' LIMIT 5;
        ```
    [ ] При showSizes ON размерных строк под этим nmId НЕТ (одинокий "0" не дублируется)
    [ ] У товаров с реальными размерами (брюки, костюм) размерные строки есть

    ### 7. Sticky cells не пересекаются при showSizes + expand-all

    [ ] «Развернуть все» (все 7 кластеров expanded)
    [ ] «По размерам» включить
    [ ] Скроллить **вертикально** — sticky header (Фото/Сводка/Артикул/Иваново)
        остаётся на месте
    [ ] Скроллить **горизонтально** — sticky левые 4 колонки остаются на месте
    [ ] Нет дубликатов sticky cells при rowSpan продление через размерные строки
    [ ] Performance acceptable (50+ nmId × 8 размеров × все кластеры expanded —
        scroll не лагает на dev VPS)

    **Если FAIL performance:** записать в Blockers + Plan 16-FUT — virtualization.

    ### 8. `/inventory` → `/stock` редирект работает

    [ ] Открыть https://zoiten.pro/inventory/wb (старый URL до Phase 14)
    [ ] Должен быть 308 redirect на https://zoiten.pro/stock/wb (старые закладки
        работают)
    [ ] (Это поведение должно быть установлено ещё в Phase 14 через next.config.ts —
        Phase 16 НЕ менял; пункт UAT для регрессии-проверки)

    ### 9. Diagnostic CSV: diff=0 — главное условие success

    На VPS:

    ```bash
    cd /opt/zoiten-pro
    WB_API_TOKEN="$(grep WB_API_TOKEN /etc/zoiten.pro.env | cut -d= -f2)" \
      node scripts/wb-stocks-diagnose.js
    ```

    [ ] Файл `wb-stocks-diff-YYYY-MM-DD.csv` содержит ТОЛЬКО header (нет строк
        с diff!=0)
    [ ] ИЛИ console output: «No diffs found — БД соответствует API»
    [ ] Если есть diffs — записать в Blockers (дальнейшее расследование, sync
        не идемпотентен или баг в diagnostic)

    Опционально расширить диапазон nmId:

    ```bash
    WB_STOCKS_DIAGNOSE_NMIDS="1,2,3,...всё" \
      WB_API_TOKEN="..." \
      node scripts/wb-stocks-diagnose.js
    ```

    Если diff=0 — Phase 16 sync corrupt resolved.

    ---

    ## Результаты

    | # | Пункт | Статус | Заметки |
    |---|-------|--------|---------|
    | 1 | /stock/wb открывается | [ ] PASS / [ ] FAIL | |
    | 2 | Кнопка «По размерам» persist | [ ] PASS / [ ] FAIL | |
    | 3 | nmId 859398279 sum=stockQty | [ ] PASS / [ ] FAIL | |
    | 4 | nmId 859398279 Котовск 6 строк | [ ] PASS / [ ] FAIL | |
    | 5 | hideSc/hidden не меняют агрегаты | [ ] PASS / [ ] FAIL | |
    | 6 | one-size без размерных строк | [ ] PASS / [ ] FAIL | |
    | 7 | Sticky cells при expand-all+showSizes | [ ] PASS / [ ] FAIL | |
    | 8 | /inventory → /stock 308 | [ ] PASS / [ ] FAIL | |
    | 9 | diagnostic diff=0 | [ ] PASS / [ ] FAIL | |

    **Verdict:** [ ] All PASS → Phase 16 Complete; [ ] Blockers → создать гап-плану

    ## Blockers (если есть)

    | # | Что не сработало | Reproduce | Hypothesis |
    |---|-------------------|-----------|-------------|

    ## Sign-off

    - **Tester:** ___________________
    - **Date:** ___________________
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md'; if(!fs.existsSync(p)) throw 'missing'; const c=fs.readFileSync(p,'utf-8'); const checks=['/stock/wb открывается','По размерам','859398279','Котовск','hideSc','one-size','Sticky cells','inventory','diff=0','wb-stocks-diagnose']; for(const ch of checks){ if(!c.includes(ch)) throw 'missing: '+ch; } if((c.match(/\\[ \\]/g)||[]).length < 20) throw 'not enough checkbox items'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `.planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md` существует
    - `wc -l 16-HUMAN-UAT.md` >= 60
    - Содержит секции: Pre-UAT, 9-point Чеклист, Результаты, Blockers, Sign-off
    - Содержит контрольные nmId 859398279 и 901585883
    - Содержит точные команды (`bash deploy.sh`, `node scripts/wb-sync-stocks.js`,
      `node scripts/wb-stocks-diagnose.js`)
    - Содержит ожидаемые числа Котовск {46:11, 48:10, 50:10, 54:10, 58:10, 60:10}
    - Содержит SQL-сниппеты для верификации persist (User.stockWbShowSizes)
    - Хотя бы 20 чекбоксов `[ ]`
  </acceptance_criteria>
  <done>
    16-HUMAN-UAT.md готов. Пользователь может пройти по нему после deploy на VPS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — deploy + re-sync + 9-point UAT</name>
  <what-built>
    Phase 16 полностью имплементирован:
    - Wave 0: scripts/wb-stocks-diagnose.js (baseline diagnostic скрипт)
    - Wave 1: schema.prisma + миграция 20260423_phase16_size_breakdown
    - Wave 2: lib/wb-api.ts расширения; sync fix в обоих файлах; lib/stock-wb-data.ts
      sizeBreakdown; sortSizes helper
    - Wave 3: saveStockWbShowSizes server action; UI кнопка «По размерам»; рендер
      размерных строк
    - Wave 4: 16-HUMAN-UAT.md создан

    Готово к deploy + re-sync + manual UAT.
  </what-built>
  <how-to-verify>
    1. **Deploy:**
       ```bash
       ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
       ```
       Проверить логи: `prisma migrate deploy` применил миграцию,
       `npm run build` без ошибок, `systemctl restart zoiten-erp` ok.

    2. **Re-sync:**
       ```bash
       ssh root@85.198.97.89 "cd /opt/zoiten-pro && node scripts/wb-sync-stocks.js"
       ```
       ИЛИ через UI: открыть `/stock`, нажать «Обновить из WB», подождать 1-2 мин.

    3. **Diagnostic:**
       ```bash
       ssh root@85.198.97.89 'cd /opt/zoiten-pro && WB_API_TOKEN="$(grep WB_API_TOKEN /etc/zoiten.pro.env | cut -d= -f2)" node scripts/wb-stocks-diagnose.js'
       ```
       Ожидание: console «No diffs found» ИЛИ CSV с пустыми строками.

    4. **Manual UAT:** пройти по `.planning/phases/16-wb-stock-sizes/16-HUMAN-UAT.md`
       9-точечному чеклисту. Записать результаты в таблицу «Результаты».

    5. **ROADMAP update:** после passing UAT обновить `.planning/ROADMAP.md`:
       - Phase 16 → Complete (тyck-mark)
       - Дата завершения
       - Plans complete: 7/7 (включая Wave 0)

    **Если все 9 пунктов PASS:** Phase 16 → Complete.
    **Если есть Blockers:** Создать `.planning/phases/16-wb-stock-sizes/16-VERIFICATION.md`
    с диагнозом, запустить `/gsd:plan-phase 16 --gaps` для создания gap closure плана.
  </how-to-verify>
  <resume-signal>
    После deploy + re-sync + UAT отправить:
    - "approved" — все 9 пунктов PASS, Phase 16 завершена
    - "blocker: <описание>" — что не сработало, для gap closure
  </resume-signal>
</task>

</tasks>

<verification>
- 16-HUMAN-UAT.md создан с минимум 9 пунктами проверки
- Деплой на VPS: миграция применена через `prisma migrate deploy`
- Re-sync: `WbCardWarehouseStock` заполнен per-size rows
- Diagnostic: `node scripts/wb-stocks-diagnose.js` показывает diff=0
- Все 9 пунктов UAT — PASS
- ROADMAP.md: Phase 16 → Complete
</verification>

<success_criteria>
- Контрольные nmId 859398279, 901585883 показывают корректные размерные строки
  с реальными числами (sum размеров = stockQty карточки)
- Кнопка «По размерам» persist между сессиями
- Diagnostic CSV пустой (только header) после re-sync
- Sticky-table layout работает при expand-all + showSizes ON без visual artifacts
- Phase 16 → Complete в roadmap, milestone v1.2 расширен 8 новыми STOCK-30..37
</success_criteria>

<output>
После завершения создать `.planning/phases/16-wb-stock-sizes/16-06-SUMMARY.md` +
расширенный `16-PHASE-SUMMARY.md` (главный отчёт по фазе) с:
- Final state системы (per-size остатки, кнопка «По размерам», sync fix)
- UAT результаты (выписка из 16-HUMAN-UAT.md)
- Diagnostic before/after (baseline diff vs final diff=0)
- Roadmap-attribution: STOCK-37
- Blockers (если есть) → ссылка на gap closure план
- Performance замечания (50 nmId × 8 sizes × все кластеры — fps на проде)
- Lessons learned для retrospective:
  - Два разных бага в одной таблице через 2 entry points
  - Compound NOT IN deleteMany не поддерживается → 2-step pattern стал общепроектным
  - Per-size orders НЕ хранятся в БД → null → "—" в UI (deferred до v2)
</output>
