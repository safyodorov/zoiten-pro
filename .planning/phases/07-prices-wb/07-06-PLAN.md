---
phase: 07-prices-wb
plan: 06
type: execute
wave: 4
depends_on:
  - 07-01
files_modified:
  - components/ui/tooltip.tsx
  - app/(dashboard)/prices/layout.tsx
  - app/(dashboard)/prices/page.tsx
  - app/(dashboard)/prices/ozon/page.tsx
  - components/prices/PricesTabs.tsx
autonomous: true
requirements:
  - PRICES-13
  - PRICES-14
  - PRICES-15
must_haves:
  truths:
    - "Shadcn tooltip компонент добавлен через `npx shadcn add tooltip`, файл `components/ui/tooltip.tsx` существует"
    - "Layout `/prices/layout.tsx` вызывает `requireSection(\"PRICES\")` и рендерит `<h1>Управление ценами</h1>` + `<PricesTabs>` + children"
    - "`/prices/page.tsx` делает redirect на `/prices/wb` (пользователь попадает сразу на главный раздел WB)"
    - "`/prices/ozon/page.tsx` рендерит `<ComingSoon sectionName=\"Управление ценами Ozon\" />`"
    - "`PricesTabs` — клиентский компонент с двумя ссылками (WB / Ozon), активный таб подсвечен `border-primary text-primary`"
    - "Страница `/prices/wb/page.tsx` существует как временная заглушка (будет заменена в плане 07-08) с `requireSection(\"PRICES\")` и текстом «В разработке»"
  artifacts:
    - path: "components/ui/tooltip.tsx"
      provides: "shadcn tooltip компонент (@base-ui/react)"
      min_lines: 20
    - path: "app/(dashboard)/prices/layout.tsx"
      provides: "RSC layout раздела «Управление ценами»"
      min_lines: 15
    - path: "app/(dashboard)/prices/page.tsx"
      provides: "redirect на /prices/wb"
      min_lines: 5
    - path: "app/(dashboard)/prices/ozon/page.tsx"
      provides: "ComingSoon stub"
      min_lines: 10
    - path: "app/(dashboard)/prices/wb/page.tsx"
      provides: "временная заглушка RSC (будет заменена в плане 07-08)"
      min_lines: 10
    - path: "components/prices/PricesTabs.tsx"
      provides: "клиентский компонент табов WB/Ozon"
      min_lines: 25
  key_links:
    - from: "app/(dashboard)/prices/layout.tsx"
      to: "requireSection(\"PRICES\")"
      via: "RBAC guard"
      pattern: "requireSection"
    - from: "app/(dashboard)/prices/layout.tsx"
      to: "components/prices/PricesTabs.tsx"
      via: "import + render <PricesTabs />"
      pattern: "PricesTabs"
    - from: "app/(dashboard)/prices/page.tsx"
      to: "/prices/wb"
      via: "redirect() from next/navigation"
      pattern: "redirect.*prices/wb"
---

<objective>
Создать UI infrastructure для раздела «Управление ценами»: shadcn tooltip компонент, layout с табами, redirect-страница, Ozon stub, и временная заглушка для `/prices/wb` (она будет заменена полноценной RSC страницей в плане 07-08).

Этот план — UI foundation. Не пытается построить сложные компоненты (таблица, модалка) — только навигационная структура и инфраструктура.

Purpose: обеспечить что `/prices/wb` и `/prices/ozon` routes работают (no 404), RBAC действует, и tooltip компонент готов для использования в планах 07-07 и 07-09.

Output:
- `components/ui/tooltip.tsx` (через shadcn CLI)
- Layout + redirect + ozon stub + tabs
- Временная заглушка `/prices/wb/page.tsx` (заменится в 07-08)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-UI-SPEC.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@CLAUDE.md
@app/(dashboard)/cards/layout.tsx
@app/(dashboard)/cards/wb/page.tsx
@app/(dashboard)/cards/ozon/page.tsx
@components/cards/CardsTabs.tsx
@components/ui/ComingSoon.tsx

<interfaces>
<!-- Паттерн существующего /cards/layout.tsx (используется как референс для /prices/layout.tsx): -->

```tsx
// app/(dashboard)/cards/layout.tsx (приблизительно)
import { requireSection } from "@/lib/rbac"
import { CardsTabs } from "@/components/cards/CardsTabs"

export default async function CardsLayout({ children }: { children: React.ReactNode }) {
  await requireSection("CARDS")
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-medium">Карточки товаров</h1>
      <CardsTabs />
      {children}
    </div>
  )
}
```

<!-- Паттерн /cards/ozon/page.tsx: -->

```tsx
import { ComingSoon } from "@/components/ui/ComingSoon"

export default function CardsOzonPage() {
  return <ComingSoon sectionName="Карточки Ozon" />
}
```

<!-- Паттерн CardsTabs.tsx (client component): -->

```tsx
"use client"
import Link from "next/link"
import { usePathname } from "next/navigation"
import { cn } from "@/lib/utils"

const tabs = [
  { href: "/cards/wb", label: "WB" },
  { href: "/cards/ozon", label: "Ozon" },
]

export function CardsTabs() {
  const pathname = usePathname()
  return (
    <div className="border-b flex gap-6">
      {tabs.map((tab) => {
        const active = pathname === tab.href
        return (
          <Link
            key={tab.href}
            href={tab.href}
            className={cn(
              "pb-2 border-b-2 -mb-px text-sm",
              active
                ? "border-primary text-primary font-medium"
                : "border-transparent text-muted-foreground hover:text-foreground"
            )}
          >
            {tab.label}
          </Link>
        )
      })}
    </div>
  )
}
```

<!-- ComingSoon component уже существует в components/ui/ComingSoon.tsx (из Phase 5): -->

Signature: `<ComingSoon sectionName="..." />`

<!-- UI-SPEC требования: -->

- H1 (Display size 24px): «Управление ценами» — `text-2xl font-medium`
- Tabs: WB / Ozon, активный — `border-primary text-primary`
- Spacing: `space-y-6` (24px между блоками)
- Layout находится в `(dashboard)` группе — наследует sidebar, header, theme provider

<!-- shadcn tooltip установка (project uses shadcn CLI 4.1.2 with base-nova preset): -->

Команда: `npx shadcn@4.1.2 add tooltip`

Альтернатива (если CLI падает): создать `components/ui/tooltip.tsx` вручную по паттерну других `components/ui/*.tsx` — база `@base-ui/react/tooltip`. Проверить `components/ui/dialog.tsx` как референс base-ui использования.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установить shadcn tooltip + создать layout/redirect/ozon/wb-stub страницы</name>
  <files>components/ui/tooltip.tsx, app/(dashboard)/prices/layout.tsx, app/(dashboard)/prices/page.tsx, app/(dashboard)/prices/ozon/page.tsx, app/(dashboard)/prices/wb/page.tsx</files>
  <action>
**1. Установить shadcn tooltip:**
```bash
npx shadcn@4.1.2 add tooltip
```

Команда должна создать `components/ui/tooltip.tsx`. Если CLI интерактивный и спрашивает overwrite — подтвердить.

**Если shadcn CLI падает или tooltip уже есть:** проверить `cat components/ui/tooltip.tsx` — файл содержит `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent` exports из `@base-ui/react/tooltip`. Если нет — создать вручную по паттерну других `components/ui/*.tsx` (особенно `dialog.tsx`):

```tsx
// components/ui/tooltip.tsx
"use client"

import * as React from "react"
import { Tooltip as TooltipPrimitive } from "@base-ui/react/tooltip"
import { cn } from "@/lib/utils"

const TooltipProvider = TooltipPrimitive.Provider

const Tooltip = TooltipPrimitive.Root

const TooltipTrigger = TooltipPrimitive.Trigger

const TooltipContent = React.forwardRef<
  React.ElementRef<typeof TooltipPrimitive.Popup>,
  React.ComponentPropsWithoutRef<typeof TooltipPrimitive.Popup>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <TooltipPrimitive.Portal>
    <TooltipPrimitive.Positioner sideOffset={sideOffset}>
      <TooltipPrimitive.Popup
        ref={ref}
        className={cn(
          "z-50 overflow-hidden rounded-md border bg-popover px-3 py-1.5 text-xs text-popover-foreground shadow-md",
          "data-[open]:animate-in data-[open]:fade-in-0 data-[open]:zoom-in-95",
          "data-[closed]:animate-out data-[closed]:fade-out-0 data-[closed]:zoom-out-95",
          className,
        )}
        {...props}
      />
    </TooltipPrimitive.Positioner>
  </TooltipPrimitive.Portal>
))
TooltipContent.displayName = "TooltipContent"

export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }
```

**Примечание:** точная структура base-ui Tooltip API может отличаться — executor должен проверить `components/ui/dialog.tsx` и `components/ui/select.tsx` для корректного паттерна импорта из `@base-ui/react`. Если shadcn CLI работает — использовать его вывод как source of truth.

**2. Создать `app/(dashboard)/prices/layout.tsx`:**

```tsx
// app/(dashboard)/prices/layout.tsx
import { requireSection } from "@/lib/rbac"
import { PricesTabs } from "@/components/prices/PricesTabs"

export default async function PricesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  await requireSection("PRICES")

  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-medium">Управление ценами</h1>
      <PricesTabs />
      {children}
    </div>
  )
}
```

**3. Создать `app/(dashboard)/prices/page.tsx`** (redirect на /prices/wb):

```tsx
// app/(dashboard)/prices/page.tsx
import { redirect } from "next/navigation"

export default function PricesIndexPage() {
  redirect("/prices/wb")
}
```

**4. Создать `app/(dashboard)/prices/ozon/page.tsx`:**

```tsx
// app/(dashboard)/prices/ozon/page.tsx
import { ComingSoon } from "@/components/ui/ComingSoon"

export default function PricesOzonPage() {
  return <ComingSoon sectionName="Управление ценами Ozon" />
}
```

**Примечание:** Путь к ComingSoon может быть другим. Executor: проверить `grep -r "export function ComingSoon\|export const ComingSoon\|export default.*ComingSoon" components/` и использовать правильный импорт. Если ComingSoon в `components/ui/ComingSoon.tsx` — импорт `@/components/ui/ComingSoon`. Если в `components/ComingSoon.tsx` — `@/components/ComingSoon`.

**5. Создать `app/(dashboard)/prices/wb/page.tsx`** — **временная заглушка** (будет полностью заменена в плане 07-08 на полноценную RSC страницу с таблицей):

```tsx
// app/(dashboard)/prices/wb/page.tsx
// Временная заглушка — план 07-08 заменит полноценной RSC страницей с PriceCalculatorTable.
import { requireSection } from "@/lib/rbac"

export default async function PricesWbPage() {
  await requireSection("PRICES")

  return (
    <div className="py-8 text-center text-muted-foreground">
      <p className="text-sm">Страница «Управление ценами WB» в разработке.</p>
      <p className="text-xs mt-2">Будет заполнена в плане 07-08.</p>
    </div>
  )
}
```

Эта временная заглушка нужна для того чтобы:
- Route `/prices/wb` отдавал не 404 а валидный HTML
- RBAC уже работал (проверка `requireSection("PRICES")`)
- Plan 07-08 заменит этот файл полностью с сохранением структуры `requireSection("PRICES")`

**Правила:**
- Все тексты на русском.
- Используется native `<h1>` + Tailwind, не shadcn component.
- Размер h1 — `text-2xl font-medium` (Display 24px из UI-SPEC).
- Spacing — `space-y-6` (24px).
- RBAC guard — в layout (действует на все подстраницы) + в `/prices/wb/page.tsx` (defense in depth).
- Не использовать серверные вызовы Prisma в layout — только `requireSection` (он читает session).
  </action>
  <verify>
    <automated>test -f components/ui/tooltip.tsx && test -f "app/(dashboard)/prices/layout.tsx" && test -f "app/(dashboard)/prices/page.tsx" && test -f "app/(dashboard)/prices/ozon/page.tsx" && test -f "app/(dashboard)/prices/wb/page.tsx" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
1. `ls components/ui/tooltip.tsx` — файл существует.
2. `ls app/(dashboard)/prices/` — содержит layout.tsx, page.tsx, ozon/, wb/
3. `cat "app/(dashboard)/prices/layout.tsx" | grep requireSection` — guard присутствует.
4. `cat "app/(dashboard)/prices/page.tsx" | grep redirect` — redirect на /prices/wb.
5. `cat "app/(dashboard)/prices/ozon/page.tsx" | grep ComingSoon` — stub присутствует.
6. `npx tsc --noEmit` — без errors.
    </manual>
  </verify>
  <done>
- `components/ui/tooltip.tsx` создан (через shadcn CLI или вручную)
- `/prices/layout.tsx` с RBAC, h1, PricesTabs
- `/prices/page.tsx` делает redirect на /prices/wb
- `/prices/ozon/page.tsx` показывает ComingSoon
- `/prices/wb/page.tsx` — временная заглушка с RBAC
- TypeScript компилируется
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать components/prices/PricesTabs.tsx</name>
  <files>components/prices/PricesTabs.tsx</files>
  <action>
**1. Создать директорию `components/prices`:**
```bash
mkdir -p components/prices
```

**2. Создать `components/prices/PricesTabs.tsx`** — клиентский компонент с двумя табами WB и Ozon. Использовать `usePathname` для определения активного таба (паттерн из `components/cards/CardsTabs.tsx`).

**Сначала прочитать существующий `components/cards/CardsTabs.tsx`** для точного копирования паттерна (особенно классы Tailwind для активного/неактивного состояния).

```tsx
// components/prices/PricesTabs.tsx
"use client"

import Link from "next/link"
import { usePathname } from "next/navigation"
import { cn } from "@/lib/utils"

const tabs = [
  { href: "/prices/wb", label: "WB" },
  { href: "/prices/ozon", label: "Ozon" },
] as const

export function PricesTabs() {
  const pathname = usePathname()

  return (
    <div className="border-b flex gap-6">
      {tabs.map((tab) => {
        const active = pathname.startsWith(tab.href)
        return (
          <Link
            key={tab.href}
            href={tab.href}
            className={cn(
              "pb-2 border-b-2 -mb-px text-sm transition-colors",
              active
                ? "border-primary text-primary font-medium"
                : "border-transparent text-muted-foreground hover:text-foreground",
            )}
          >
            {tab.label}
          </Link>
        )
      })}
    </div>
  )
}
```

**Замечания:**
- `pathname.startsWith(tab.href)` — обеспечивает что `/prices/wb` остаётся активным даже если URL будет `/prices/wb?filter=...` в будущем.
- Переходы через `<Link>` — client-side navigation без перезагрузки.
- Если CardsTabs.tsx в проекте имеет другие классы (например использует другие variants) — скопировать их точно для визуальной consistency.
- `cn()` — helper из `@/lib/utils`, обёрнутый `clsx` + `tailwind-merge`.

**3. Проверить что компонент импортируется в layout:**
```bash
grep "PricesTabs" app/\(dashboard\)/prices/layout.tsx
```
Должен быть импорт и рендер (сделано в Task 1).

**Правила:**
- `"use client"` directive — `usePathname` требует клиента.
- Labels «WB» и «Ozon» — буквально эти строки (не переводить).
- Цвет активного таба — `text-primary` (оранжево-красный oklch из globals.css).
  </action>
  <verify>
    <automated>test -f components/prices/PricesTabs.tsx && grep -c "PricesTabs" components/prices/PricesTabs.tsx && npx tsc --noEmit 2>&1 | grep -c "PricesTabs"</automated>
    <manual>
1. `cat components/prices/PricesTabs.tsx` — файл существует, `"use client"` на первой строке, exported `PricesTabs`.
2. `grep "usePathname" components/prices/PricesTabs.tsx` — client hook используется.
3. `grep "/prices/wb\|/prices/ozon" components/prices/PricesTabs.tsx` — оба таба определены.
4. `npx tsc --noEmit` без errors для этого файла.
    </manual>
  </verify>
  <done>
- `components/prices/PricesTabs.tsx` создан как client component
- Два таба (WB / Ozon) с ссылками через `<Link>`
- Активный таб подсвечен `border-primary text-primary`
- Hover state для неактивных: `text-muted-foreground hover:text-foreground`
- Импортируется в `/prices/layout.tsx`
- TypeScript компилируется
  </done>
</task>

</tasks>

<verification>
- `npm run dev` (локально) или `npm run build` — Next.js собирает проект без ошибок
- Navigation: `/prices` → redirect → `/prices/wb` (заглушка) → клик на Ozon таб → `/prices/ozon` (ComingSoon)
- RBAC: user без PRICES access получает redirect на /unauthorized
</verification>

<success_criteria>
- Все 6 файлов созданы, Next.js собирает проект без ошибок
- Tooltip компонент готов для использования в планах 07-07 (PromoTooltip) и 07-09 (не используется в модалке напрямую, но в подсказках)
- PricesTabs работает как навигация
- `/prices/wb` страница временно пустая но accessible
- План 07-07 может начинать создавать PriceCalculatorTable + GlobalRatesBar + PromoTooltip
- План 07-08 может заменить `/prices/wb/page.tsx` на полноценную RSC
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-06-SUMMARY.md`. Включить:
- Созданные файлы
- Статус shadcn tooltip install (успешен/вручную)
- Структуру routing `/prices/*`
- Подтверждение что `/prices/wb` — временная заглушка, будет заменена в 07-08
</output>
