---
phase: 07-prices-wb
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/pricing-math.test.ts
  - tests/pricing-fallback.test.ts
  - tests/pricing-settings.test.ts
  - tests/wb-promotions-api.test.ts
  - tests/excel-auto-promo.test.ts
  - tests/fixtures/auto-promo-sample.xlsx
  - .planning/phases/07-prices-wb/07-WAVE0-NOTES.md
autonomous: false
requirements:
  - PRICES-05
  - PRICES-10
  - PRICES-11
must_haves:
  truths:
    - "vitest установлен как dev-dep и запускается командой `npm run test`"
    - "Прочитан canonical Excel `Форма управления ценами.xlsx`, зафиксированы 30 заголовков колонок и значения golden test для nmId 800750522"
    - "WB Promotions Calendar API smoke test успешен — базовый URL подтверждён, токен имеет нужный scope"
    - "Все 5 тестовых файлов существуют как RED stubs (импортируют ещё несуществующий `@/lib/pricing-math` — это ожидаемо)"
    - "Fixture `tests/fixtures/auto-promo-sample.xlsx` скопирован из Downloads"
  artifacts:
    - path: "package.json"
      provides: "dev-dep vitest + scripts test/test:watch"
      contains: "\"vitest\""
    - path: "vitest.config.ts"
      provides: "конфигурация vitest с включением tests/**/*.test.ts"
      min_lines: 10
    - path: "tests/pricing-math.test.ts"
      provides: "RED golden test case nmId 800750522 + zero-guard"
      min_lines: 30
    - path: "tests/pricing-fallback.test.ts"
      provides: "RED fallback chain test (override → default → hardcoded)"
      min_lines: 20
    - path: "tests/pricing-settings.test.ts"
      provides: "RED Zod валидация ставок"
      min_lines: 15
    - path: "tests/wb-promotions-api.test.ts"
      provides: "RED mock fetch rate-limit timing"
      min_lines: 20
    - path: "tests/excel-auto-promo.test.ts"
      provides: "RED парсер Excel по индексам A/F/L/M/T/U"
      min_lines: 20
    - path: "tests/fixtures/auto-promo-sample.xlsx"
      provides: "реальный образец Excel auto-акции из кабинета WB"
      contains: ""
    - path: ".planning/phases/07-prices-wb/07-WAVE0-NOTES.md"
      provides: "WAVE0 верификационные заметки: базовый URL WB Promotions API, 30 заголовков колонок из Excel, golden test values, статус токена"
      min_lines: 40
  key_links:
    - from: "package.json scripts.test"
      to: "vitest"
      via: "npm run test → vitest run"
      pattern: "\"test\".*vitest"
    - from: "tests/pricing-math.test.ts"
      to: "@/lib/pricing-math"
      via: "import (ожидается RED — модуль будет создан в плане 07-02)"
      pattern: "from \"@/lib/pricing-math\""
    - from: "tests/excel-auto-promo.test.ts"
      to: "tests/fixtures/auto-promo-sample.xlsx"
      via: "fs.readFileSync для парсинга"
      pattern: "auto-promo-sample.xlsx"
---

<objective>
Wave 0 — Infrastructure и верификация before real code. Устанавливается vitest, создаются RED-stubs всех 5 тестовых suites (они будут GREEN в последующих волнах), выполняется smoke test WB Promotions Calendar API, читается canonical Excel `Форма управления ценами.xlsx` с фиксацией 30 заголовков и golden test values, копируется fixture auto-акции из Downloads.

Purpose: убрать все «неизвестные неизвестные» до того, как написана первая строка production кода. Без этой волны риск — не работающий URL WB API или неправильные формулы из-за неверного чтения Excel.

Output:
- vitest infra (vitest.config.ts + package.json scripts)
- 5 RED test stub файлов (импортируют несуществующий модуль — это корректно для TDD RED фазы)
- Excel fixture скопирован
- `.planning/phases/07-prices-wb/07-WAVE0-NOTES.md` с 30 заголовками из Excel, golden test values, базовым URL WB API, статусом токена
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-VALIDATION.md
@CLAUDE.md
@package.json

<interfaces>
<!-- Key references для executor'а. Не читать полные файлы — достаточно индекса. -->

Существующие паттерны проекта:
- `lib/wb-api.ts` — клиент WB API (Content/Prices/Tariffs/Statistics/Analytics). Использует обычный `fetch` с `Authorization: token` header, кроме `fetchWbDiscounts` (curl v4). Токен читается через `getToken()` из env `WB_API_TOKEN`.
- `app/api/wb-commission-iu/route.ts` — существующий паттерн чтения Excel через `xlsx` (`XLSX.read(buf, { type: "buffer" })` + `XLSX.utils.sheet_to_json(sheet, { header: 1 })` с индексами).

WB Promotions Calendar API (для smoke test):
- Base URL (предположение): `https://dp-calendar-api.wildberries.ru`
- Альтернативный URL: `https://discounts-prices-api.wildberries.ru`
- Smoke endpoint: `GET /api/v1/calendar/promotions?startDateTime={ISO}&endDateTime={ISO}&allPromo=true&limit=1`
- Header: `Authorization: {WB_API_TOKEN}`
- Rate limit: 10 req / 6 sec (600ms между запросами)

Excel canonical:
- Path: `C:/Users/User/Desktop/Форма управления ценами.xlsx`
- Имеет 30 колонок формул + 1 строку примера с nmId 800750522

Excel auto-promo sample:
- Path: `C:/Users/User/Downloads/Товары для исключения из акции_Весенняя распродажа_ бустинг продаж (автоматические скидки)_09.04.2026 16.37.31.xlsx`
- Колонки для парсера (0-indexed): A=0 inAction, F=5 nmId, L=11 planPrice, M=12 currentPrice, T=19 planDiscount, U=20 status

Golden test case (из `.planning/phases/07-prices-wb/07-CONTEXT.md` <specifics>):
- Inputs: priceBeforeDiscount=25833, sellerDiscountPct=70, wbDiscountPct=25, clubDiscountPct=0, commFbwPct=32.58, drrPct=10, walletPct=2, acquiringPct=2.7, jemPct=1, costPrice=2204, defectRatePct=2, deliveryCostRub=30, creditPct=7, overheadPct=6, taxPct=8
- Expected: sellerPrice=7749.9, priceAfterWbDiscount=5812.425, profit=567.68, returnOnSalesPct≈7, roiPct≈26
- Tolerance: profit ±0.01₽
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установка vitest и чтение canonical Excel</name>
  <files>package.json, vitest.config.ts, .planning/phases/07-prices-wb/07-WAVE0-NOTES.md, tests/fixtures/auto-promo-sample.xlsx</files>
  <action>
1. Создать директории, если их нет (Windows bash-совместимый путь):
   - `mkdir -p tests/fixtures`

2. Установить vitest как dev dependency:
   - `npm i -D vitest @vitest/ui`
   - Обратить внимание: минорные версии должны быть ^2.x или ^3.x (актуальная). Не upgrading существующие зависимости.

3. В `package.json` добавить в `scripts` (оставить все существующие скрипты нетронутыми):
   ```json
   "test": "vitest run",
   "test:watch": "vitest",
   "test:ui": "vitest --ui"
   ```

4. Создать `vitest.config.ts` в корне проекта:
   ```typescript
   import { defineConfig } from "vitest/config"
   import path from "path"

   export default defineConfig({
     test: {
       include: ["tests/**/*.test.ts"],
       environment: "node",
       globals: false,
     },
     resolve: {
       alias: {
         "@": path.resolve(__dirname, "./"),
       },
     },
   })
   ```
   Обоснование alias: проект использует `@/lib/*`, `@/components/*`, `@/app/*` через Next.js tsconfig `paths`. vitest не читает tsconfig автоматически — нужен явный alias.

5. Скопировать Excel fixture auto-акции (из Downloads в tests/fixtures):
   - Source: `C:/Users/User/Downloads/Товары для исключения из акции_Весенняя распродажа_ бустинг продаж (автоматические скидки)_09.04.2026 16.37.31.xlsx`
   - Target: `C:/Claude/zoiten-pro/tests/fixtures/auto-promo-sample.xlsx`
   - Команда: `cp "C:/Users/User/Downloads/Товары для исключения из акции_Весенняя распродажа_ бустинг продаж (автоматические скидки)_09.04.2026 16.37.31.xlsx" tests/fixtures/auto-promo-sample.xlsx`
   - Если файл не существует в Downloads, сообщить пользователю и остановиться.

6. Прочитать canonical Excel `C:/Users/User/Desktop/Форма управления ценами.xlsx` через одноразовый Node.js inline script:
   ```bash
   node -e "const XLSX=require('xlsx'); const wb=XLSX.readFile('C:/Users/User/Desktop/Форма управления ценами.xlsx'); const sh=wb.Sheets[wb.SheetNames[0]]; const rows=XLSX.utils.sheet_to_json(sh,{header:1,raw:false}); console.log(JSON.stringify(rows.slice(0,3),null,2));"
   ```
   Цель — извлечь:
   - Ряд 0 (row 0) — заголовки 30 колонок (русский текст)
   - Ряд 1 (row 1) — значения примера nmId 800750522

7. Создать `.planning/phases/07-prices-wb/07-WAVE0-NOTES.md` со следующей структурой (заполнить реальными значениями после чтения Excel и smoke test):

   ```markdown
   # Phase 7 — Wave 0 Verification Notes

   **Created:** (дата выполнения)
   **Status:** VERIFIED / BLOCKED

   ## 1. Excel 30 колонок (canonical)

   Source: `C:/Users/User/Desktop/Форма управления ценами.xlsx`

   | # | Заголовок (RU) | Тип | Формула (кратко) |
   |---|----------------|-----|------------------|
   | 1 | (заполнить) | ₽ / % | (формула из примера ряда) |
   | ... | ... | ... | ... |
   | 30 | (заполнить) | | |

   ## 2. Golden Test Values (nmId 800750522)

   Inputs:
   - priceBeforeDiscount: 25833
   - sellerDiscountPct: 70
   - wbDiscountPct: 25
   - clubDiscountPct: 0
   - commFbwPct: 32.58
   - drrPct: 10
   - walletPct: 2
   - acquiringPct: 2.7
   - jemPct: 1
   - costPrice: 2204
   - defectRatePct: 2
   - deliveryCostRub: 30
   - creditPct: 7
   - overheadPct: 6
   - taxPct: 8

   Expected outputs (из Excel ряд 1):
   - sellerPrice: 7749.9
   - priceAfterWbDiscount: 5812.425
   - walletAmount: (из Excel)
   - priceAfterWallet: 5696.18
   - acquiringAmount: 209.25 (пример)
   - commissionAmount: 2524.92
   - drrAmount: 774.99
   - jemAmount: 77.5
   - transferAmount: 4163.25
   - defectAmount: 44.08
   - deliveryCost: 30
   - creditAmount: 232.50
   - overheadAmount: 465
   - taxAmount: 620
   - profit: 567.68
   - returnOnSalesPct: 7
   - roiPct: 26

   Все 30 выходных значений зафиксированы для использования в `tests/pricing-math.test.ts`.

   ## 3. WB Promotions Calendar API

   **Base URL verified:** (один из):
   - [ ] `https://dp-calendar-api.wildberries.ru` (первая попытка)
   - [ ] `https://discounts-prices-api.wildberries.ru` (fallback)

   **Smoke test command:**
   ```bash
   curl -v "https://{BASE}/api/v1/calendar/promotions?startDateTime=2026-04-09T00:00:00Z&endDateTime=2026-04-10T00:00:00Z&allPromo=true&limit=1" \
     -H "Authorization: $WB_API_TOKEN"
   ```

   **Result:** (заполнить после вызова)
   - Status code: 200 / 401 / 403 / 429
   - Response shape: (первые 500 символов)
   - Верификация:
     - [ ] 200 → base URL правильный, scope токена достаточен, endpoint работает
     - [ ] 401 → токен невалидный или без scope "Цены и скидки". STOP: пользователь должен перегенерировать токен.
     - [ ] 403 → scope отсутствует. STOP: пользователь должен добавить scope.
     - [ ] 429 → rate limit hit (не должно быть на первом запросе, подождать 6 сек и повторить).

   **Confirmed PROMO_API base:** `https://{url}` → зафиксирован в константе для `lib/wb-api.ts`.

   ## 4. Известные риски и блокеры

   - Если Excel-файл не существует по пути — СТОП, сообщить пользователю.
   - Если токен не проходит 401 — СТОП, пользователь генерирует новый токен с правильным scope.
   - Если Excel имеет не 30 колонок — СТОП, сообщить пользователю, возможно другая версия файла.
   ```

8. Выполнить smoke test WB Promotions Calendar API:
   - Прочитать токен: `source /etc/zoiten.pro.env 2>/dev/null || true` + значение из локального `.env` если есть. Альтернативно: `export WB_API_TOKEN="..."` (пользователь знает где токен).
   - **ВАЖНО:** Smoke test выполняется ЛОКАЛЬНО на машине разработчика. Токен из `.env` или `.env.local`.
   - Попытка 1: `https://dp-calendar-api.wildberries.ru/api/v1/calendar/promotions?startDateTime=2026-04-09T00:00:00Z&endDateTime=2026-04-10T00:00:00Z&allPromo=true&limit=1`
   - Если 404 / NXDOMAIN — попытка 2: `https://discounts-prices-api.wildberries.ru/...` с тем же path.
   - Записать успешный URL в 07-WAVE0-NOTES.md секция 3.

9. Заполнить 07-WAVE0-NOTES.md реальными значениями из шагов 6-8. Commit файл вместе с остальными изменениями Wave 0.

**Правила и ограничения:**
- Запрещено использовать `prisma migrate dev` — только ручные SQL migrations (см. план 07-01).
- Все команды должны работать в Windows bash (используем `cp`, не `copy`).
- Toast текст, заголовки колонок, комментарии в коде — на русском (per CLAUDE.md).
- Не трогать `lib/wb-api.ts` в этой волне — только добавка в плане 07-03.
  </action>
  <verify>
    <automated>npm run test -- --version</automated>
    <manual>
1. `cat vitest.config.ts` — файл существует с алиасом `@`.
2. `cat package.json | grep '"test"'` — содержит `"test": "vitest run"`.
3. `ls tests/fixtures/auto-promo-sample.xlsx` — файл существует, размер > 0 байт.
4. `cat .planning/phases/07-prices-wb/07-WAVE0-NOTES.md` — 30 заголовков колонок заполнены русскими текстами из Excel, golden test values заполнены реальными числами, PROMO_API base URL зафиксирован.
5. Smoke test passed (200 OK от WB API).
    </manual>
  </verify>
  <done>
- vitest установлен, `npm run test` запускается без ошибок (тесты падают — это ожидаемо RED фаза)
- `vitest.config.ts` содержит alias `@` → корень проекта
- Fixture `tests/fixtures/auto-promo-sample.xlsx` скопирован из Downloads
- 30 заголовков из canonical Excel записаны в `07-WAVE0-NOTES.md`
- Golden test values для nmId 800750522 записаны
- WB Promotions Calendar base URL верифицирован smoke test'ом (200 OK), зафиксирован в заметках
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать 5 RED test stub файлов</name>
  <files>tests/pricing-math.test.ts, tests/pricing-fallback.test.ts, tests/pricing-settings.test.ts, tests/wb-promotions-api.test.ts, tests/excel-auto-promo.test.ts</files>
  <action>
Создать 5 тестовых файлов как **RED stubs**: они импортируют ещё несуществующие модули (`@/lib/pricing-math`, `@/app/actions/pricing`, `@/lib/wb-api`) и упадут при первом запуске. Это корректное состояние для TDD RED фазы — GREEN наступает в последующих waves.

**1. `tests/pricing-math.test.ts`** — golden test + zero-guard:

```typescript
import { describe, it, expect } from "vitest"
import { calculatePricing } from "@/lib/pricing-math"

// Источник значений: canonical Excel `Форма управления ценами.xlsx`, строка nmId 800750522.
// Значения зафиксированы в .planning/phases/07-prices-wb/07-WAVE0-NOTES.md секция 2.

describe("calculatePricing — golden test nmId 800750522", () => {
  const goldenInputs = {
    priceBeforeDiscount: 25833,
    sellerDiscountPct: 70,
    wbDiscountPct: 25,
    clubDiscountPct: 0,
    commFbwPct: 32.58,
    drrPct: 10,
    walletPct: 2,
    acquiringPct: 2.7,
    jemPct: 1,
    costPrice: 2204,
    defectRatePct: 2,
    deliveryCostRub: 30,
    creditPct: 7,
    overheadPct: 6,
    taxPct: 8,
    buyoutPct: 100,
  }

  it("returns sellerPrice = 7749.9", () => {
    const result = calculatePricing(goldenInputs)
    expect(result.sellerPrice).toBeCloseTo(7749.9, 1)
  })

  it("returns priceAfterWbDiscount = 5812.425", () => {
    const result = calculatePricing(goldenInputs)
    expect(result.priceAfterWbDiscount).toBeCloseTo(5812.425, 2)
  })

  it("returns profit ≈ 567.68 rub", () => {
    const result = calculatePricing(goldenInputs)
    expect(result.profit).toBeCloseTo(567.68, 1)
  })

  it("returns returnOnSalesPct ≈ 7", () => {
    const result = calculatePricing(goldenInputs)
    expect(result.returnOnSalesPct).toBeCloseTo(7, 0)
  })

  it("returns roiPct ≈ 26", () => {
    const result = calculatePricing(goldenInputs)
    expect(result.roiPct).toBeCloseTo(26, 0)
  })
})

describe("calculatePricing — zero-guard и edge cases", () => {
  it("не возвращает Infinity при sellerPrice = 0 (нулевая цена продавца)", () => {
    const result = calculatePricing({
      priceBeforeDiscount: 0,
      sellerDiscountPct: 0,
      wbDiscountPct: 0,
      clubDiscountPct: 0,
      commFbwPct: 30,
      drrPct: 10,
      walletPct: 2,
      acquiringPct: 2.7,
      jemPct: 1,
      costPrice: 100,
      defectRatePct: 2,
      deliveryCostRub: 30,
      creditPct: 7,
      overheadPct: 6,
      taxPct: 8,
      buyoutPct: 100,
    })
    expect(Number.isFinite(result.returnOnSalesPct)).toBe(true)
    expect(Number.isFinite(result.roiPct)).toBe(true)
    expect(Number.isFinite(result.profit)).toBe(true)
  })

  it("не возвращает NaN при costPrice = 0", () => {
    const result = calculatePricing({
      priceBeforeDiscount: 1000,
      sellerDiscountPct: 50,
      wbDiscountPct: 10,
      clubDiscountPct: 0,
      commFbwPct: 20,
      drrPct: 10,
      walletPct: 2,
      acquiringPct: 2.7,
      jemPct: 1,
      costPrice: 0,
      defectRatePct: 2,
      deliveryCostRub: 30,
      creditPct: 7,
      overheadPct: 6,
      taxPct: 8,
      buyoutPct: 100,
    })
    expect(Number.isNaN(result.roiPct)).toBe(false)
  })
})

describe("COLUMN_ORDER — constraint from D-12", () => {
  it("содержит ровно 30 заголовков колонок (requirement D-12)", async () => {
    const { COLUMN_ORDER } = await import("@/lib/pricing-math")
    expect(COLUMN_ORDER).toHaveLength(30)
  })

  it("все заголовки — непустые строки", async () => {
    const { COLUMN_ORDER } = await import("@/lib/pricing-math")
    COLUMN_ORDER.forEach((header, idx) => {
      expect(header, `COLUMN_ORDER[${idx}] должен быть непустой строкой`).toMatch(/\S/)
    })
  })
})
```

**2. `tests/pricing-fallback.test.ts`** — fallback chain для overrides:

```typescript
import { describe, it, expect } from "vitest"
import { resolveDrrPct, resolveDefectRatePct, resolveDeliveryCostRub } from "@/lib/pricing-math"

// Fallback chain (D-01):
// Product.override → Subcategory/Category.default → hardcoded (10% / 2% / 30₽)

describe("resolveDrrPct — fallback chain", () => {
  it("возвращает product.drrOverridePct если задан", () => {
    const result = resolveDrrPct({
      productOverride: 15,
      subcategoryDefault: 12,
    })
    expect(result).toBe(15)
  })

  it("возвращает subcategory.defaultDrrPct если product не задан", () => {
    const result = resolveDrrPct({
      productOverride: null,
      subcategoryDefault: 12,
    })
    expect(result).toBe(12)
  })

  it("возвращает hardcoded 10% если и product, и subcategory не заданы", () => {
    const result = resolveDrrPct({
      productOverride: null,
      subcategoryDefault: null,
    })
    expect(result).toBe(10)
  })
})

describe("resolveDefectRatePct — fallback chain", () => {
  it("возвращает product.defectRateOverridePct если задан", () => {
    const result = resolveDefectRatePct({
      productOverride: 5,
      categoryDefault: 3,
    })
    expect(result).toBe(5)
  })

  it("возвращает category.defaultDefectRatePct если product не задан", () => {
    const result = resolveDefectRatePct({
      productOverride: null,
      categoryDefault: 3,
    })
    expect(result).toBe(3)
  })

  it("возвращает hardcoded 2% при отсутствии всех", () => {
    const result = resolveDefectRatePct({
      productOverride: null,
      categoryDefault: null,
    })
    expect(result).toBe(2)
  })
})

describe("resolveDeliveryCostRub — только Product override + hardcoded", () => {
  it("возвращает product.deliveryCostRub если задан", () => {
    expect(resolveDeliveryCostRub(50)).toBe(50)
  })

  it("возвращает hardcoded 30₽ если null", () => {
    expect(resolveDeliveryCostRub(null)).toBe(30)
  })
})
```

**3. `tests/pricing-settings.test.ts`** — Zod валидация для updateAppSetting:

```typescript
import { describe, it, expect } from "vitest"
import { appSettingValueSchema, isValidAppSettingKey } from "@/app/actions/pricing"

describe("appSettingValueSchema — Zod валидация ставок", () => {
  it("принимает валидные значения", () => {
    expect(appSettingValueSchema.safeParse("2.0").success).toBe(true)
    expect(appSettingValueSchema.safeParse("2.5").success).toBe(true)
    expect(appSettingValueSchema.safeParse("0").success).toBe(true)
    expect(appSettingValueSchema.safeParse("99.9").success).toBe(true)
  })

  it("отклоняет значения > 100", () => {
    expect(appSettingValueSchema.safeParse("101").success).toBe(false)
    expect(appSettingValueSchema.safeParse("200").success).toBe(false)
  })

  it("отклоняет отрицательные значения", () => {
    expect(appSettingValueSchema.safeParse("-1").success).toBe(false)
    expect(appSettingValueSchema.safeParse("-0.5").success).toBe(false)
  })

  it("отклоняет не-числовые значения", () => {
    expect(appSettingValueSchema.safeParse("abc").success).toBe(false)
    expect(appSettingValueSchema.safeParse("").success).toBe(false)
  })
})

describe("isValidAppSettingKey — whitelist ключей", () => {
  it("принимает 6 известных ключей", () => {
    expect(isValidAppSettingKey("wbWalletPct")).toBe(true)
    expect(isValidAppSettingKey("wbAcquiringPct")).toBe(true)
    expect(isValidAppSettingKey("wbJemPct")).toBe(true)
    expect(isValidAppSettingKey("wbCreditPct")).toBe(true)
    expect(isValidAppSettingKey("wbOverheadPct")).toBe(true)
    expect(isValidAppSettingKey("wbTaxPct")).toBe(true)
  })

  it("отклоняет неизвестные ключи", () => {
    expect(isValidAppSettingKey("unknown")).toBe(false)
    expect(isValidAppSettingKey("wbEvilKey")).toBe(false)
  })
})
```

**4. `tests/wb-promotions-api.test.ts`** — mock fetch rate limit:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

// NOTE: функции fetchAllPromotions/fetchPromotionDetails/fetchPromotionNomenclatures
// будут добавлены в lib/wb-api.ts в плане 07-03.
// Эти тесты ДО этого — RED.

describe("WB Promotions API — rate limit (mocked)", () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  it("fetchAllPromotions делает паузу 600ms между pagination-запросами", async () => {
    const { fetchAllPromotions } = await import("@/lib/wb-api")

    const fetchMock = vi.fn()
      .mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => ({ data: { promotions: new Array(100).fill({ id: 1, name: "p", type: "regular", startDateTime: "2026-04-09T00:00:00Z", endDateTime: "2026-06-09T00:00:00Z" }) } }),
      })
      .mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => ({ data: { promotions: [] } }),
      })
    vi.stubGlobal("fetch", fetchMock)

    const promise = fetchAllPromotions(new Date("2026-04-09"), new Date("2026-06-09"))
    await vi.advanceTimersByTimeAsync(700)
    await promise

    expect(fetchMock).toHaveBeenCalledTimes(2)
  })

  it("fetchAllPromotions обрабатывает 429 через sleep(6000) и retry", async () => {
    const { fetchAllPromotions } = await import("@/lib/wb-api")

    const fetchMock = vi.fn()
      .mockResolvedValueOnce({
        ok: false,
        status: 429,
        text: async () => "too many requests",
      })
      .mockResolvedValueOnce({
        ok: true,
        status: 200,
        json: async () => ({ data: { promotions: [] } }),
      })
    vi.stubGlobal("fetch", fetchMock)

    const promise = fetchAllPromotions(new Date("2026-04-09"), new Date("2026-06-09"))
    await vi.advanceTimersByTimeAsync(6100)
    await promise

    expect(fetchMock).toHaveBeenCalledTimes(2)
  })
})
```

**5. `tests/excel-auto-promo.test.ts`** — реальный fixture из Downloads:

```typescript
import { describe, it, expect } from "vitest"
import * as XLSX from "xlsx"
import fs from "fs"
import path from "path"

// Парсер auto-акций читает колонки по индексам (D-06):
// A=0 inAction "Да"/"Нет", F=5 nmId, L=11 planPrice, M=12 currentPrice, T=19 planDiscount, U=20 status

describe("Excel auto-promo parser — реальный fixture", () => {
  const fixturePath = path.resolve(process.cwd(), "tests/fixtures/auto-promo-sample.xlsx")

  it("fixture существует", () => {
    expect(fs.existsSync(fixturePath)).toBe(true)
  })

  it("парсит строки по правильным индексам колонок", () => {
    const buf = fs.readFileSync(fixturePath)
    const wb = XLSX.read(buf, { type: "buffer" })
    const sheet = wb.Sheets[wb.SheetNames[0]]
    const rows = XLSX.utils.sheet_to_json<(string | number)[]>(sheet, { header: 1 })

    // Первая строка — заголовки, пропустить
    expect(rows.length).toBeGreaterThan(1)

    // Найти первую валидную data row (некоторые могут быть пустыми)
    const dataRows = rows.slice(1).filter((r) => r && r.length > 0 && r[5] != null)
    expect(dataRows.length).toBeGreaterThan(0)

    const firstDataRow = dataRows[0]
    // A (0) — inAction
    expect(firstDataRow[0]).toBeDefined()
    // F (5) — nmId (число или строка)
    const nmId = parseInt(String(firstDataRow[5]), 10)
    expect(Number.isNaN(nmId)).toBe(false)
    expect(nmId).toBeGreaterThan(0)
  })

  it("корректно распознаёт 'Да'/'Нет' в колонке A для поля inAction", () => {
    const buf = fs.readFileSync(fixturePath)
    const wb = XLSX.read(buf, { type: "buffer" })
    const sheet = wb.Sheets[wb.SheetNames[0]]
    const rows = XLSX.utils.sheet_to_json<(string | number)[]>(sheet, { header: 1 })

    const dataRows = rows.slice(1).filter((r) => r && r.length > 0 && r[5] != null)
    const values = dataRows.map((r) => String(r[0] ?? "").trim().toLowerCase())
    // Ожидаем валидные значения: "да" / "нет" / пустая строка
    const validValues = new Set(["да", "нет", ""])
    values.forEach((v) => expect(validValues.has(v)).toBe(true))
  })
})
```

**Запуск после создания файлов:**
`npm run test` → ожидаем RED (все тесты падают с "Cannot find module @/lib/pricing-math" или подобными). Это правильное состояние Wave 0 — GREEN наступает в планах 07-02, 07-03, 07-04, 07-05.

**Комментарии в тестах — на русском** (per CLAUDE.md). Code identifiers — на английском.
  </action>
  <verify>
    <automated>npm run test 2>&1 | head -50</automated>
    <manual>
1. `ls tests/*.test.ts` — 5 файлов существует.
2. `npm run test` — vitest запускается и падает на import'ах `@/lib/pricing-math`, `@/app/actions/pricing`, `@/lib/wb-api` (несуществующие модули). Это ожидаемое RED состояние Wave 0.
3. Проверить что файл `tests/excel-auto-promo.test.ts` корректно ссылается на `tests/fixtures/auto-promo-sample.xlsx` (fs.existsSync pass для fixture).
    </manual>
  </verify>
  <done>
- 5 тестовых файлов созданы в `tests/`:
  - `pricing-math.test.ts` — golden test + zero-guard
  - `pricing-fallback.test.ts` — fallback chain
  - `pricing-settings.test.ts` — Zod валидация
  - `wb-promotions-api.test.ts` — mocked rate limit
  - `excel-auto-promo.test.ts` — fixture parsing
- Все 5 — в RED состоянии (падают с import errors — это корректно, модули появятся в waves 1+)
- `npm run test` запускается без падения самого vitest
- Тесты содержат русские комментарии и docstrings, английские identifiers
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручная верификация Wave 0 артефактов</name>
  <what-built>
- vitest infra (vitest.config.ts + package.json scripts)
- 5 RED test stubs (tests/*.test.ts)
- Fixture `tests/fixtures/auto-promo-sample.xlsx` скопирован
- 07-WAVE0-NOTES.md с 30 заголовками из Excel, golden test values, WB API base URL
  </what-built>
  <how-to-verify>
1. **Проверить vitest установлен:**
   - `cat package.json | grep vitest` — должен быть в `devDependencies` и `scripts`
   - `npm run test -- --version` — возвращает версию vitest без ошибок

2. **Проверить Excel был прочитан:**
   - `cat .planning/phases/07-prices-wb/07-WAVE0-NOTES.md`
   - Секция 1 содержит 30 русских заголовков колонок
   - Секция 2 содержит golden test values для nmId 800750522 (profit = 567.68, roi = 26, returnOnSales = 7)

3. **Проверить WB API smoke test passed:**
   - В секции 3 07-WAVE0-NOTES.md зафиксирован **точный base URL** (`https://dp-calendar-api.wildberries.ru` или альтернатива)
   - Response status = 200 (не 401, не 403, не 404)
   - Если токен не проходит — **СТОП**, пользователь должен перегенерировать токен с scope «Цены и скидки» и после этого executor повторяет smoke test

4. **Проверить fixture:**
   - `ls -la tests/fixtures/auto-promo-sample.xlsx` — размер > 10 KB

5. **Проверить RED state тестов:**
   - `npm run test 2>&1 | head -30` — тесты падают с `Cannot find module` на `@/lib/pricing-math` и т.д. (это ожидаемое RED состояние)

6. **Подтвердить готовность к Wave 1:**
   - Executor подтверждает: все 6 пунктов выше passed → Wave 0 complete → можно начинать планы 07-01 и 07-02 параллельно.
  </how-to-verify>
  <resume-signal>Введите "approved" для продолжения на Wave 1, либо опишите issues (например "токен не проходит, перегенерирую и повторяй smoke test")</resume-signal>
</task>

</tasks>

<verification>
Все 3 задачи завершены:
- vitest работает (`npm run test -- --version` возвращает версию)
- `vitest.config.ts` с alias `@`
- `package.json.scripts.test = "vitest run"`
- 30 заголовков колонок из Excel зафиксированы в 07-WAVE0-NOTES.md
- Golden test values для nmId 800750522 зафиксированы
- WB Promotions API base URL verified через smoke test (200 OK)
- Токен имеет правильный scope
- Fixture `tests/fixtures/auto-promo-sample.xlsx` существует
- 5 RED test stubs созданы
- Human verification checkpoint passed
</verification>

<success_criteria>
- Wave 1 планы (07-01, 07-02) могут быть запущены без unknowns: база URL WB API известна, формулы выведены из Excel
- `npm run test` запускается без падения vitest-runner (тесты RED — это корректно)
- Все артефакты Wave 0 закоммичены в git
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-00-SUMMARY.md` по стандартному шаблону SUMMARY. Включить:
- Ссылку на 07-WAVE0-NOTES.md
- Зафиксированный PROMO_API base URL (для плана 07-03)
- Список 30 колонок (краткий)
- Golden test inputs/expected outputs (для плана 07-02)
</output>
