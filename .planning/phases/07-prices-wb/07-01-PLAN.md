---
phase: 07-prices-wb
plan: 01
type: execute
wave: 1
depends_on:
  - 07-00
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260409_prices_wb/migration.sql
autonomous: true
requirements:
  - PRICES-01
  - PRICES-06
  - PRICES-08
  - PRICES-09
  - PRICES-12
must_haves:
  truths:
    - "Миграция `20260409_prices_wb` создаёт 4 новых таблицы (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature)"
    - "Миграция добавляет 6 новых полей в существующие таблицы: Category.defaultDefectRatePct, Subcategory.defaultDrrPct, Product.drrOverridePct, Product.defectRateOverridePct, Product.deliveryCostRub, WbCard.avgSalesSpeed7d"
    - "Миграция seed-ит 6 AppSetting ключей с дефолтными значениями (wbWalletPct=2.0, wbAcquiringPct=2.7, wbJemPct=1.0, wbCreditPct=7.0, wbOverheadPct=6.0, wbTaxPct=8.0)"
    - "`prisma/schema.prisma` обновлён и синхронизирован с SQL миграцией"
    - "`npx prisma generate` успешно генерирует TypeScript клиент с новыми моделями и полями"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "обновлённая Prisma схема с 4 новыми моделями и 6 новыми полями"
      contains: "model AppSetting"
    - path: "prisma/migrations/20260409_prices_wb/migration.sql"
      provides: "ручная SQL миграция (CREATE TABLE + ALTER TABLE + INSERT seed)"
      min_lines: 70
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260409_prices_wb/migration.sql"
      via: "Prisma generate должен produce типы совместимые с миграцией"
      pattern: "model WbPromotion"
    - from: "WbCard model"
      to: "CalculatedPrice model"
      via: "relation calculatedPrices: CalculatedPrice[] с Cascade delete"
      pattern: "@relation.*WbCard.*onDelete: Cascade"
    - from: "WbPromotion model"
      to: "WbPromotionNomenclature model"
      via: "relation nomenclatures: WbPromotionNomenclature[] с Cascade delete"
      pattern: "@relation.*WbPromotion.*onDelete: Cascade"
---

<objective>
Создать миграцию базы данных для Phase 7. Добавляются 4 новые модели (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature) и 6 новых полей в существующие модели (Category/Subcategory/Product/WbCard) согласно decisions D-01..D-04 и D-09.

Миграция написана вручную как SQL (формат проекта — не `prisma migrate dev`, а ручные файлы в `prisma/migrations/{timestamp}_name/migration.sql`), `schema.prisma` обновляется зеркально.

Purpose: обеспечить структурный фундамент для всех последующих Wave 2+ планов. Без этого ни WB API sync, ни UI таблица, ни расчёты не могут работать.

Output:
- `prisma/migrations/20260409_prices_wb/migration.sql` — готовая миграция (applyable на локальной dev БД и VPS prod через `prisma migrate deploy`)
- `prisma/schema.prisma` — обновлённая схема
- `npx prisma generate` работает без ошибок
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-00-SUMMARY.md
@CLAUDE.md
@prisma/schema.prisma

<interfaces>
<!-- Текущая структура prisma/schema.prisma (главные модели, которые нужно изменить): -->

Существующие модели (НЕ переписывать, только ДОБАВИТЬ поля):
- `Category` — нужно ДОБАВИТЬ: `defaultDefectRatePct Float?`
- `Subcategory` — нужно ДОБАВИТЬ: `defaultDrrPct Float?`
- `Product` — нужно ДОБАВИТЬ: `drrOverridePct Float?`, `defectRateOverridePct Float?`, `deliveryCostRub Float?`
- `WbCard` — нужно ДОБАВИТЬ: `avgSalesSpeed7d Float?`, `calculatedPrices CalculatedPrice[]` (relation)

Новые модели (4 штуки):
- `AppSetting(key PK String, value String, updatedAt, updatedBy String?)`
- `CalculatedPrice(id cuid, wbCardId → WbCard [Cascade], slot Int 1|2|3, name String, sellerPrice Float, drrPct Float?, defectRatePct Float?, deliveryCostRub Float?, snapshot Json, createdAt, updatedAt, @@unique([wbCardId, slot]))`
- `WbPromotion(id Int PK = promotionID, name String, description String?, advantages String[], startDateTime, endDateTime, type String "auto"|"regular", rangingJson Json?, source String @default("API"), lastSyncedAt, createdAt, nomenclatures WbPromotionNomenclature[])`
- `WbPromotionNomenclature(id cuid, promotionId → WbPromotion [Cascade], nmId Int, inAction Boolean @default(false), planPrice Float?, planDiscount Float?, currentPrice Float?, status String?, @@unique([promotionId, nmId]))`

Seed значения AppSetting (INSERT ON CONFLICT DO NOTHING):
- wbWalletPct = 2.0
- wbAcquiringPct = 2.7
- wbJemPct = 1.0
- wbCreditPct = 7.0
- wbOverheadPct = 6.0
- wbTaxPct = 8.0

Существующие миграции — паттерн:
`prisma/migrations/20260407_add_commissions/migration.sql` — пример ручной ALTER TABLE
`prisma/migrations/20260406_add_wb_cards/migration.sql` — пример CREATE TABLE с foreign key и индексами

**КРИТИЧЕСКОЕ ОГРАНИЧЕНИЕ:** НЕ использовать `prisma migrate dev` — это команда для dev-мигратора, которая reset'ит БД. Проект использует ручные SQL файлы в `prisma/migrations/{dir}/migration.sql`, которые применяются через `prisma migrate deploy` на VPS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать SQL миграцию</name>
  <files>prisma/migrations/20260409_prices_wb/migration.sql</files>
  <action>
Создать директорию и файл миграции:
`mkdir -p prisma/migrations/20260409_prices_wb`
`touch prisma/migrations/20260409_prices_wb/migration.sql`

Содержимое файла — точный SQL из RESEARCH.md секции «SQL миграция»:

```sql
-- prisma/migrations/20260409_prices_wb/migration.sql
-- Phase 7: Управление ценами WB
-- Добавляет 4 новые таблицы (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature)
-- и 6 новых полей в существующие (Category/Subcategory/Product/WbCard).
-- Seed-ит 6 глобальных ставок в AppSetting.

-- 1. AppSetting (глобальные ставки KeyValue)
CREATE TABLE "AppSetting" (
    "key" TEXT NOT NULL,
    "value" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "updatedBy" TEXT,
    CONSTRAINT "AppSetting_pkey" PRIMARY KEY ("key")
);

-- 2. CalculatedPrice (расчётные цены пользователя, 1-3 слота на WbCard)
CREATE TABLE "CalculatedPrice" (
    "id" TEXT NOT NULL,
    "wbCardId" TEXT NOT NULL,
    "slot" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "sellerPrice" DOUBLE PRECISION NOT NULL,
    "drrPct" DOUBLE PRECISION,
    "defectRatePct" DOUBLE PRECISION,
    "deliveryCostRub" DOUBLE PRECISION,
    "snapshot" JSONB NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    CONSTRAINT "CalculatedPrice_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "CalculatedPrice_wbCardId_slot_key" ON "CalculatedPrice"("wbCardId", "slot");
ALTER TABLE "CalculatedPrice" ADD CONSTRAINT "CalculatedPrice_wbCardId_fkey"
    FOREIGN KEY ("wbCardId") REFERENCES "WbCard"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- 3. WbPromotion (акции WB — sync через API, id = promotionID из WB)
CREATE TABLE "WbPromotion" (
    "id" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT,
    "advantages" TEXT[] DEFAULT ARRAY[]::TEXT[],
    "startDateTime" TIMESTAMP(3) NOT NULL,
    "endDateTime" TIMESTAMP(3) NOT NULL,
    "type" TEXT NOT NULL,
    "rangingJson" JSONB,
    "source" TEXT NOT NULL DEFAULT 'API',
    "lastSyncedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "WbPromotion_pkey" PRIMARY KEY ("id")
);

-- 4. WbPromotionNomenclature (связи nmId с акциями)
CREATE TABLE "WbPromotionNomenclature" (
    "id" TEXT NOT NULL,
    "promotionId" INTEGER NOT NULL,
    "nmId" INTEGER NOT NULL,
    "inAction" BOOLEAN NOT NULL DEFAULT false,
    "planPrice" DOUBLE PRECISION,
    "planDiscount" DOUBLE PRECISION,
    "currentPrice" DOUBLE PRECISION,
    "status" TEXT,
    CONSTRAINT "WbPromotionNomenclature_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "WbPromotionNomenclature_promotionId_nmId_key"
    ON "WbPromotionNomenclature"("promotionId", "nmId");
ALTER TABLE "WbPromotionNomenclature" ADD CONSTRAINT "WbPromotionNomenclature_promotionId_fkey"
    FOREIGN KEY ("promotionId") REFERENCES "WbPromotion"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- 5. Поля в существующих таблицах (D-01, D-09)
ALTER TABLE "Category"    ADD COLUMN "defaultDefectRatePct"  DOUBLE PRECISION;
ALTER TABLE "Subcategory" ADD COLUMN "defaultDrrPct"         DOUBLE PRECISION;
ALTER TABLE "Product"     ADD COLUMN "drrOverridePct"        DOUBLE PRECISION;
ALTER TABLE "Product"     ADD COLUMN "defectRateOverridePct" DOUBLE PRECISION;
ALTER TABLE "Product"     ADD COLUMN "deliveryCostRub"       DOUBLE PRECISION;
ALTER TABLE "WbCard"      ADD COLUMN "avgSalesSpeed7d"       DOUBLE PRECISION;

-- 6. Seed 6 глобальных ставок в AppSetting (D-02)
INSERT INTO "AppSetting" ("key", "value", "updatedAt") VALUES
    ('wbWalletPct',    '2.0', NOW()),
    ('wbAcquiringPct', '2.7', NOW()),
    ('wbJemPct',       '1.0', NOW()),
    ('wbCreditPct',    '7.0', NOW()),
    ('wbOverheadPct',  '6.0', NOW()),
    ('wbTaxPct',       '8.0', NOW())
ON CONFLICT ("key") DO NOTHING;
```

**Замечания:**
- Формат даты `20260409_prices_wb` соответствует паттерну существующих миграций проекта (см. `prisma/migrations/20260407_add_commissions/`).
- `DOUBLE PRECISION` для всех числовых полей (совместимо с Prisma `Float`).
- `JSONB` для `snapshot` и `rangingJson` (совместимо с Prisma `Json`).
- `TEXT[]` для `advantages` массива строк (совместимо с Prisma `String[]`).
- `ON CONFLICT DO NOTHING` для seed — идемпотентно, безопасно при повторном применении.
- Имена constraint'ов точно как Prisma их именует (`{Table}_{field}_fkey`, `{Table}_{fields}_key`) — чтобы `prisma db pull` не сгенерировал новые.
  </action>
  <verify>
    <automated>test -f prisma/migrations/20260409_prices_wb/migration.sql && grep -c "CREATE TABLE" prisma/migrations/20260409_prices_wb/migration.sql</automated>
    <manual>
1. `cat prisma/migrations/20260409_prices_wb/migration.sql | head -10` — файл существует, начинается комментарием.
2. `grep "CREATE TABLE" prisma/migrations/20260409_prices_wb/migration.sql | wc -l` — возвращает `4` (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature).
3. `grep "ALTER TABLE" prisma/migrations/20260409_prices_wb/migration.sql | wc -l` — возвращает `8` (2 FK constraints + 6 ALTER ADD COLUMN).
4. `grep "INSERT INTO" prisma/migrations/20260409_prices_wb/migration.sql` — находит seed insert с 6 ставками.
    </manual>
  </verify>
  <done>
- Файл `prisma/migrations/20260409_prices_wb/migration.sql` создан
- Содержит 4 CREATE TABLE
- Содержит 6 ALTER TABLE ADD COLUMN
- Содержит 2 FK constraints (CalculatedPrice → WbCard, WbPromotionNomenclature → WbPromotion)
- Содержит 2 UNIQUE INDEX (CalculatedPrice_wbCardId_slot_key, WbPromotionNomenclature_promotionId_nmId_key)
- Содержит INSERT INTO AppSetting с 6 дефолтными ставками
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить prisma/schema.prisma и генерировать клиент</name>
  <files>prisma/schema.prisma</files>
  <action>
Прочитать текущий `prisma/schema.prisma` и внести изменения:

**1. Добавить 4 новые модели в конец файла** (после существующих моделей):

```prisma
// ──────────────────────────────────────────────────────────────────
// Phase 7: Управление ценами WB
// ──────────────────────────────────────────────────────────────────

// Глобальные настройки KeyValue (D-02)
// Ключи Phase 7: wbWalletPct, wbAcquiringPct, wbJemPct, wbCreditPct, wbOverheadPct, wbTaxPct
model AppSetting {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
  updatedBy String?
}

// Расчётные цены пользователя — до 3 слотов на WbCard (D-03)
model CalculatedPrice {
  id              String   @id @default(cuid())
  wbCardId        String
  wbCard          WbCard   @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
  slot            Int      // 1 | 2 | 3
  name            String
  sellerPrice     Float
  drrPct          Float?
  defectRatePct   Float?
  deliveryCostRub Float?
  snapshot        Json
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@unique([wbCardId, slot])
}

// Акции WB — sync через /api/v1/calendar/promotions (D-04)
// id = promotionID из WB API (не cuid — сопоставление прямое)
model WbPromotion {
  id             Int                         @id
  name           String
  description    String?
  advantages     String[]                    @default([])
  startDateTime  DateTime
  endDateTime    DateTime
  type           String                       // "auto" | "regular"
  rangingJson    Json?
  source         String                       @default("API")  // "API" | "EXCEL"
  lastSyncedAt   DateTime                     @default(now())
  createdAt      DateTime                     @default(now())
  nomenclatures  WbPromotionNomenclature[]
}

// Номенклатуры акций — связь nmId с WbPromotion (D-04)
// Для regular — заполняется через API, для auto — через Excel upload
model WbPromotionNomenclature {
  id           String      @id @default(cuid())
  promotionId  Int
  promotion    WbPromotion @relation(fields: [promotionId], references: [id], onDelete: Cascade)
  nmId         Int
  inAction     Boolean     @default(false)
  planPrice    Float?
  planDiscount Float?
  currentPrice Float?
  status       String?

  @@unique([promotionId, nmId])
}
```

**2. Добавить поля в существующие модели.** Найти каждую модель в `prisma/schema.prisma` и добавить поля **в конец блока модели перед закрывающей скобкой** (чтобы не сломать существующие relations):

**Category** — добавить:
```prisma
  // Phase 7: default процент брака (D-01)
  defaultDefectRatePct Float?
```

**Subcategory** — добавить:
```prisma
  // Phase 7: default ДРР (D-01)
  defaultDrrPct Float?
```

**Product** — добавить:
```prisma
  // Phase 7: Pricing overrides (D-01)
  drrOverridePct        Float?
  defectRateOverridePct Float?
  deliveryCostRub       Float?
```

**WbCard** — добавить:
```prisma
  // Phase 7: Средняя скорость продаж за 7 дней (D-09)
  avgSalesSpeed7d Float?

  // Phase 7: Relation to calculated prices (D-03)
  calculatedPrices CalculatedPrice[]
```

**3. Проверить syntax через `npx prisma format`:**
```bash
npx prisma format
```
Команда форматирует схему без изменения семантики — safe to run.

**4. Сгенерировать TypeScript клиент:**
```bash
npx prisma generate
```
Должно завершиться без ошибок. Это обновит `node_modules/.prisma/client/index.d.ts` с типами новых моделей.

**5. Проверить что схема parse-ится:**
```bash
npx prisma validate
```
Должно вывести `The schema at prisma/schema.prisma is valid`.

**Замечания:**
- НЕ запускать `prisma migrate dev` или `prisma db push` — только generate и validate.
- НЕ трогать существующие relations и поля — только добавлять.
- Комментарии в схеме — на русском (per CLAUDE.md convention, хотя существующие комментарии могут быть на английском — согласуем с RU).
- Порядок полей в моделях: новые поля добавляются в конец блока (не вставлять посреди).
  </action>
  <verify>
    <automated>npx prisma validate 2>&1 | tail -5 && npx prisma generate 2>&1 | tail -5</automated>
    <manual>
1. `grep "model AppSetting" prisma/schema.prisma` — модель существует.
2. `grep "model CalculatedPrice" prisma/schema.prisma` — модель существует.
3. `grep "model WbPromotion" prisma/schema.prisma` — модель существует.
4. `grep "model WbPromotionNomenclature" prisma/schema.prisma` — модель существует.
5. `grep "defaultDefectRatePct" prisma/schema.prisma` — поле добавлено в Category.
6. `grep "defaultDrrPct" prisma/schema.prisma` — поле добавлено в Subcategory.
7. `grep "drrOverridePct" prisma/schema.prisma` — поле добавлено в Product.
8. `grep "avgSalesSpeed7d" prisma/schema.prisma` — поле добавлено в WbCard.
9. `grep "calculatedPrices" prisma/schema.prisma` — relation добавлен в WbCard.
10. `npx prisma validate` → «The schema at prisma/schema.prisma is valid»
11. `npx prisma generate` → «Generated Prisma Client (v6.x.x)»
    </manual>
  </verify>
  <done>
- `prisma/schema.prisma` содержит 4 новые модели (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature)
- `Category.defaultDefectRatePct`, `Subcategory.defaultDrrPct` добавлены
- `Product.drrOverridePct`, `Product.defectRateOverridePct`, `Product.deliveryCostRub` добавлены
- `WbCard.avgSalesSpeed7d` добавлено
- `WbCard.calculatedPrices` relation добавлен (для back-reference)
- `npx prisma validate` passes
- `npx prisma generate` passes без ошибок
- TypeScript клиент содержит новые типы
  </done>
</task>

</tasks>

<verification>
- Миграция SQL файл существует с корректной структурой (4 CREATE TABLE, 6 ALTER ADD COLUMN, 2 FK, 2 UNIQUE INDEX, 1 INSERT)
- Prisma schema обновлена, все новые модели и поля добавлены
- `npx prisma validate` passes
- `npx prisma generate` passes
- БД НЕ изменяется в рамках этого плана — миграция применится позже:
  - Локально (если есть dev PostgreSQL): executor может опционально запустить `npx prisma migrate deploy` для dev БД
  - На VPS: через `deploy.sh` (уже содержит `prisma migrate deploy`) — в плане 07-11 (final deploy)
</verification>

<success_criteria>
- TypeScript код может `import { prisma }` и вызывать `prisma.appSetting.findMany()`, `prisma.calculatedPrice.create()`, `prisma.wbPromotion.upsert()`, и т.д. без ошибок компиляции
- План 07-02 (pricing-math.ts) может использовать Prisma типы `AppSetting`, `CalculatedPrice`, `WbPromotion` если нужно
- План 07-03 (WB API) может использовать `prisma.wbPromotion.*` методы
- VPS deploy через `deploy.sh` применит миграцию автоматически — никаких ручных действий на проде не требуется
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-01-SUMMARY.md` по стандартному шаблону. Включить:
- Список 4 новых таблиц и 6 новых полей
- Пример SQL миграции (первые несколько строк)
- Команды для применения миграции на VPS (`ssh root@85.198.97.89 "cd /opt/zoiten-pro && npx prisma migrate deploy"`)
- Напоминание: НЕ применять миграцию на проде в рамках этого плана, только в финальном deploy (07-11)
</output>
