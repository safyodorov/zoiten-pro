---
phase: 07-prices-wb
plan: 07
type: execute
wave: 5
depends_on:
  - 07-02
  - 07-05
  - 07-06
files_modified:
  - components/prices/GlobalRatesBar.tsx
  - components/prices/PromoTooltip.tsx
  - components/prices/PriceCalculatorTable.tsx
autonomous: true
requirements:
  - PRICES-02
  - PRICES-03
  - PRICES-04
  - PRICES-06
  - PRICES-13
  - PRICES-15
  - PRICES-16
must_haves:
  truths:
    - "`GlobalRatesBar` рендерит 6 inputs ставок в grid `grid-cols-3 md:grid-cols-6`, каждый с label-текстом (Кошелёк WB, Эквайринг, Тариф Джем, Кредит, Общие, Налог)"
    - "Изменения в `GlobalRatesBar` debounced на 500ms → вызывают `updateAppSetting(key, value)` → toast через sonner"
    - "`PromoTooltip` оборачивает children в shadcn Tooltip с контентом `description` + bulleted `advantages[]`, max-width sm"
    - "`PriceCalculatorTable` рендерит таблицу с 4 sticky колонками (Фото, Сводка, Ярлык, Артикул) слева + 30 расчётных колонок"
    - "Группировка через rowSpan: Фото+Сводка имеют rowSpan=sum(priceRows всех WbCards), Ярлык+Артикул — rowSpan=priceRows.length одной WbCard"
    - "Порядок ценовых строк: Текущая → Regular DESC by planPrice → Auto DESC by planPrice → Расчётные 1/2/3"
    - "Индикаторные полосы: regular = `border-l-4 border-l-blue-500`, auto = `border-l-purple-500`, calculated = `border-l-amber-500`"
    - "Значения Прибыль/Re продаж/ROI подсвечены `text-green-600` (≥0) или `text-red-600` (<0) с `font-medium`"
    - "Клик по любой ценовой строке вызывает `onRowClick` prop с данными строки — модалка создаётся в плане 07-09 и подключается здесь через `useState`"
  artifacts:
    - path: "components/prices/GlobalRatesBar.tsx"
      provides: "клиентский компонент inputs глобальных ставок с debounced save"
      min_lines: 80
    - path: "components/prices/PromoTooltip.tsx"
      provides: "wrapper для shadcn Tooltip с contents description + advantages"
      min_lines: 30
    - path: "components/prices/PriceCalculatorTable.tsx"
      provides: "клиентская таблица с rowSpan + sticky + clickable rows + indicator strips"
      min_lines: 200
  key_links:
    - from: "components/prices/GlobalRatesBar.tsx"
      to: "app/actions/pricing.ts updateAppSetting"
      via: "debounced server action call"
      pattern: "updateAppSetting"
    - from: "components/prices/PromoTooltip.tsx"
      to: "components/ui/tooltip.tsx"
      via: "import shadcn Tooltip primitives"
      pattern: "TooltipProvider|TooltipTrigger|TooltipContent"
    - from: "components/prices/PriceCalculatorTable.tsx"
      to: "lib/pricing-math.ts COLUMN_ORDER"
      via: "import 30 column headers"
      pattern: "COLUMN_ORDER"
    - from: "components/prices/PriceCalculatorTable.tsx"
      to: "components/prices/PromoTooltip.tsx"
      via: "wrap promotion name в PromoTooltip"
      pattern: "PromoTooltip"
---

<objective>
Создать 3 ключевых клиентских компонента Phase 7:
1. `GlobalRatesBar` — редактор 6 глобальных ставок с debounced save
2. `PromoTooltip` — обёртка shadcn Tooltip для показа описания и advantages акции
3. `PriceCalculatorTable` — главная таблица раздела с rowSpan группировкой, sticky колонками, clickable rows, indicator strips

Эти компоненты принимают готовые данные через props (RSC page их соберёт в плане 07-08). Они НЕ делают Prisma запросов напрямую — только принимают `rows`, `rates`, и вызывают server actions.

Purpose: весь UI таблицы и редактор ставок. После этого план 07-08 просто собирает данные и рендерит `<PriceCalculatorTable />`.

Output:
- 3 клиентских компонента
- Всё визуально соответствует UI-SPEC (spacing 8-point, typography 4 sizes/2 weights, цветовая схема 60/30/10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-UI-SPEC.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-02-SUMMARY.md
@.planning/phases/07-prices-wb/07-05-SUMMARY.md
@CLAUDE.md
@components/cards/WbCardsTable.tsx
@components/ui/table.tsx
@components/ui/tooltip.tsx
@components/ui/card.tsx

<interfaces>
<!-- Из lib/pricing-math.ts (план 07-02): -->

```typescript
import { calculatePricing, COLUMN_ORDER } from "@/lib/pricing-math"
import type { PricingInputs, PricingOutputs } from "@/lib/pricing-math"

// calculatePricing(inputs): PricingOutputs — pure function
// COLUMN_ORDER: readonly string[] — 30 заголовков колонок из Excel
```

<!-- Из app/actions/pricing.ts (план 07-05): -->

```typescript
import { updateAppSetting } from "@/app/actions/pricing"

// updateAppSetting(key: string, value: string): Promise<{ok: boolean, error?: string}>
// Keys: wbWalletPct, wbAcquiringPct, wbJemPct, wbCreditPct, wbOverheadPct, wbTaxPct
```

<!-- Из components/ui/tooltip.tsx (план 07-06): -->

```tsx
import { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from "@/components/ui/tooltip"
```

<!-- Типы для PriceCalculatorTable (определяются в этом плане, используются в плане 07-08): -->

```typescript
export type PriceRowType = "current" | "regular" | "auto" | "calculated"

export interface PriceRow {
  /** Уникальный ID строки (для React key). */
  id: string
  /** Тип строки — влияет на indicator strip. */
  type: PriceRowType
  /** Seller price (до скидки продавца), основа расчёта. Для current — берётся из WbCard, для акций — planPrice, для calculated — сохранённая. */
  sellerPriceBeforeDiscount: number
  /** Название для левой колонки строки: "Текущая", "Весенняя распродажа", "Расчётная цена 1". */
  label: string
  /** Только для regular/auto — для PromoTooltip. */
  promotionDescription?: string | null
  promotionAdvantages?: readonly string[]
  /** Только для calculated — для открытия модалки в режиме edit. */
  calculatedSlot?: 1 | 2 | 3
  /** Готовый расчёт юнит-экономики (30 колонок). */
  computed: PricingOutputs
}

export interface WbCardWithRows {
  card: {
    id: string
    nmId: number
    label?: string | null
    // ... другие поля для display (photo etc на уровне Product)
  }
  priceRows: PriceRow[]
}

export interface ProductGroup {
  product: {
    id: string
    name: string
    photoUrl: string | null
    totalStock: number         // сумма stockQty всех WbCard
    totalAvgSalesSpeed: number // сумма avgSalesSpeed7d всех WbCard
  }
  cards: WbCardWithRows[]
  totalRowsInProduct: number   // sum of priceRows.length across all cards
}
```

<!-- UI-SPEC layout для GlobalRatesBar: -->

```tsx
<Card className="p-4 bg-muted/30 border">
  <div className="grid grid-cols-3 md:grid-cols-6 gap-4">
    {/* 6 ячеек с label + input + % sign */}
  </div>
</Card>
```

6 ставок (labels + keys):
- Кошелёк WB → wbWalletPct (дефолт 2.0)
- Эквайринг → wbAcquiringPct (2.7)
- Тариф Джем → wbJemPct (1.0)
- Кредит → wbCreditPct (7.0)
- Общие → wbOverheadPct (6.0)
- Налог → wbTaxPct (8.0)

Input: `type="number" step="0.1" min="0" max="100"`, `className="h-8 w-20 text-sm"`.
Debounce: 500ms. При изменении → `updateAppSetting(key, String(value))` → toast success/error.

<!-- UI-SPEC для PromoTooltip: -->

Trigger: `<span className="text-sm hover:underline cursor-help">{promotionName}</span>`
Content:
```tsx
<div className="max-w-sm text-xs space-y-2">
  {description && <p>{description}</p>}
  {advantages && advantages.length > 0 && (
    <ul className="list-disc list-inside space-y-0.5">
      {advantages.map(a => <li key={a}>{a}</li>)}
    </ul>
  )}
</div>
```

<!-- UI-SPEC для PriceCalculatorTable: -->

Ключевые классы (из UI-SPEC):
- Container: `<div className="rounded-md border"><div className="relative overflow-x-auto"><table>...`
- Sticky Photo cell: `sticky left-0 z-10 bg-background border-r w-20 align-top p-2`
- Sticky Summary cell: `sticky left-20 z-10 bg-background border-r w-60 align-top p-3`
- Sticky Label cell: `sticky left-[320px] z-10 bg-background border-r w-20 align-top`
- Sticky Article cell: `sticky left-[400px] z-10 bg-background border-r w-28 align-top font-mono text-xs`
- Sticky TableHeader: `sticky top-0 z-30 bg-background border-b`
- Ценовая ячейка (расчёт): `px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right`
- Row height: `h-10` (40px) для price rows
- Profit cell подсветка: `cn("...", profit >= 0 ? "text-green-600 font-medium" : "text-red-600 font-medium")`
- Re/ROI cells: аналогично + префикс `+`/`−` для accessibility (UI-SPEC)

Row click handler: `<tr onClick={() => onRowClick(cardGroup.card, row)}>` с `cursor-pointer` и `hover:bg-muted/50`.

<!-- Типография из UI-SPEC: -->

- Sticky columns (Фото/Сводка/Ярлык/Артикул): `text-sm` (14px body)
- 30 расчётных колонок: `text-xs` (12px micro)
- Product name в Сводке: `text-sm font-medium`
- Sub-lines в Сводке (остаток, скорость): `text-xs text-muted-foreground` + `text-foreground` для чисел
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать GlobalRatesBar и PromoTooltip</name>
  <files>components/prices/GlobalRatesBar.tsx, components/prices/PromoTooltip.tsx</files>
  <action>
**1. Создать `components/prices/GlobalRatesBar.tsx`:**

```tsx
// components/prices/GlobalRatesBar.tsx
// Phase 7 (PRICES-06): Редактор 6 глобальных ставок.
// Debounced save (500ms) через updateAppSetting server action.

"use client"

import { useState, useRef, useCallback, useTransition } from "react"
import { Card } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { toast } from "sonner"
import { updateAppSetting } from "@/app/actions/pricing"

type RateKey =
  | "wbWalletPct"
  | "wbAcquiringPct"
  | "wbJemPct"
  | "wbCreditPct"
  | "wbOverheadPct"
  | "wbTaxPct"

interface RateSpec {
  key: RateKey
  label: string
}

const RATES: readonly RateSpec[] = [
  { key: "wbWalletPct", label: "Кошелёк WB" },
  { key: "wbAcquiringPct", label: "Эквайринг" },
  { key: "wbJemPct", label: "Тариф Джем" },
  { key: "wbCreditPct", label: "Кредит" },
  { key: "wbOverheadPct", label: "Общие" },
  { key: "wbTaxPct", label: "Налог" },
] as const

interface GlobalRatesBarProps {
  initialRates: Record<RateKey, number>
}

export function GlobalRatesBar({ initialRates }: GlobalRatesBarProps) {
  const [values, setValues] = useState<Record<RateKey, string>>(() => {
    const initial: Record<string, string> = {}
    for (const { key } of RATES) {
      initial[key] = String(initialRates[key] ?? 0)
    }
    return initial as Record<RateKey, string>
  })

  const [isPending, startTransition] = useTransition()
  const timersRef = useRef<Partial<Record<RateKey, ReturnType<typeof setTimeout>>>>({})

  /** Debounced save через updateAppSetting server action. */
  const handleChange = useCallback((key: RateKey, newValue: string) => {
    setValues((prev) => ({ ...prev, [key]: newValue }))

    // Clear pending timer для этого ключа
    if (timersRef.current[key]) {
      clearTimeout(timersRef.current[key]!)
    }

    // Установить новый таймер
    timersRef.current[key] = setTimeout(() => {
      startTransition(async () => {
        const result = await updateAppSetting(key, newValue)
        if (result.ok) {
          toast.success("Ставки сохранены")
        } else {
          toast.error(result.error || "Не удалось сохранить ставку")
        }
      })
    }, 500)
  }, [])

  return (
    <Card className="p-4 bg-muted/30 border">
      <div className="grid grid-cols-3 md:grid-cols-6 gap-4">
        {RATES.map(({ key, label }) => (
          <div key={key} className="flex flex-col gap-1">
            <Label
              htmlFor={`rate-${key}`}
              className="text-xs text-muted-foreground"
            >
              {label}
            </Label>
            <div className="flex items-center gap-1">
              <Input
                id={`rate-${key}`}
                type="number"
                step="0.1"
                min="0"
                max="100"
                className="h-8 w-20 text-sm"
                value={values[key]}
                onChange={(e) => handleChange(key, e.target.value)}
                disabled={isPending}
              />
              <span className="text-sm text-muted-foreground">%</span>
            </div>
          </div>
        ))}
      </div>
    </Card>
  )
}
```

**2. Создать `components/prices/PromoTooltip.tsx`:**

```tsx
// components/prices/PromoTooltip.tsx
// Phase 7 (PRICES-15, D-11): Tooltip на названии акции с description + advantages.

"use client"

import * as React from "react"
import {
  Tooltip,
  TooltipTrigger,
  TooltipContent,
  TooltipProvider,
} from "@/components/ui/tooltip"

interface PromoTooltipProps {
  children: React.ReactNode
  description?: string | null
  advantages?: readonly string[] | null
}

export function PromoTooltip({
  children,
  description,
  advantages,
}: PromoTooltipProps) {
  const hasContent = !!description || (advantages && advantages.length > 0)

  if (!hasContent) {
    return <>{children}</>
  }

  return (
    <TooltipProvider delayDuration={200}>
      <Tooltip>
        <TooltipTrigger asChild>
          <span className="text-sm hover:underline cursor-help">
            {children}
          </span>
        </TooltipTrigger>
        <TooltipContent>
          <div className="max-w-sm text-xs space-y-2">
            {description && <p className="leading-relaxed">{description}</p>}
            {advantages && advantages.length > 0 && (
              <ul className="list-disc list-inside space-y-0.5">
                {advantages.map((a, i) => (
                  <li key={i}>{a}</li>
                ))}
              </ul>
            )}
          </div>
        </TooltipContent>
      </Tooltip>
    </TooltipProvider>
  )
}
```

**Замечания:**
- Если shadcn tooltip API отличается — проверить `components/ui/tooltip.tsx` и адаптировать импорты/JSX.
- `TooltipTrigger asChild` — передаёт ref на внутренний `<span>` вместо обёртывания (shadcn паттерн).
- `delayDuration={200}` — 200ms hover delay (стандарт shadcn).
- Native HTML `<select>` не используется здесь, но convention помнить для других компонентов (CLAUDE.md).

**3. Проверить компиляцию:**
```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>test -f components/prices/GlobalRatesBar.tsx && test -f components/prices/PromoTooltip.tsx && npx tsc --noEmit 2>&1 | grep -E "GlobalRatesBar|PromoTooltip" | head -10</automated>
    <manual>
1. `grep "\"use client\"" components/prices/GlobalRatesBar.tsx` — directive присутствует.
2. `grep "updateAppSetting" components/prices/GlobalRatesBar.tsx` — server action импортирован и вызывается.
3. `grep "setTimeout.*500" components/prices/GlobalRatesBar.tsx` — debounce 500ms.
4. `grep "TooltipProvider\|TooltipTrigger\|TooltipContent" components/prices/PromoTooltip.tsx` — все три используются.
5. `npx tsc --noEmit` без errors для этих файлов.
    </manual>
  </verify>
  <done>
- `GlobalRatesBar.tsx` создан с 6 inputs + debounced save + toast
- `PromoTooltip.tsx` создан с description + bulleted advantages, early return если нет контента
- Оба компонента — client components (`"use client"`)
- TypeScript компилируется
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать PriceCalculatorTable (главный табличный компонент)</name>
  <files>components/prices/PriceCalculatorTable.tsx</files>
  <action>
**1. Создать `components/prices/PriceCalculatorTable.tsx`:**

```tsx
// components/prices/PriceCalculatorTable.tsx
// Phase 7: главная таблица раздела «Управление ценами WB».
//
// Фичи:
// - 4 sticky колонки слева: Фото / Сводка / Ярлык / Артикул
// - 30 расчётных колонок (из COLUMN_ORDER lib/pricing-math.ts)
// - Group by Product через rowSpan (D-07): Фото+Сводка на все строки всех карточек Product
// - rowSpan по WbCard для Ярлык+Артикул (D-07)
// - Indicator strips (D-10): regular=blue, auto=purple, calculated=amber
// - Clickable rows → onRowClick prop (модалка подключается в плане 07-09)
// - Подсветка Прибыль/Re/ROI зелёным/красным (D-13, PRICES-16)

"use client"

import * as React from "react"
import Image from "next/image"
import { Badge } from "@/components/ui/badge"
import { cn } from "@/lib/utils"
import { COLUMN_ORDER, type PricingOutputs } from "@/lib/pricing-math"
import { PromoTooltip } from "@/components/prices/PromoTooltip"

// ──────────────────────────────────────────────────────────────────
// Types (exported для использования в плане 07-08)
// ──────────────────────────────────────────────────────────────────

export type PriceRowType = "current" | "regular" | "auto" | "calculated"

export interface PriceRow {
  id: string
  type: PriceRowType
  sellerPriceBeforeDiscount: number
  label: string
  promotionDescription?: string | null
  promotionAdvantages?: readonly string[] | null
  calculatedSlot?: 1 | 2 | 3
  computed: PricingOutputs
}

export interface WbCardRowGroup {
  card: {
    id: string
    nmId: number
    label?: string | null
  }
  priceRows: PriceRow[]
}

export interface ProductGroup {
  product: {
    id: string
    name: string
    photoUrl: string | null
    totalStock: number
    totalAvgSalesSpeed: number
  }
  cards: WbCardRowGroup[]
  totalRowsInProduct: number
}

interface PriceCalculatorTableProps {
  groups: ProductGroup[]
  onRowClick?: (
    card: WbCardRowGroup["card"],
    row: PriceRow,
    productId: string,
  ) => void
}

// ──────────────────────────────────────────────────────────────────
// Helpers
// ──────────────────────────────────────────────────────────────────

/** Форматирование денег: до 2 знаков после запятой, ру локаль. */
function fmtMoney(n: number): string {
  if (!Number.isFinite(n)) return "—"
  return n.toLocaleString("ru-RU", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  })
}

/** Форматирование процентов с префиксом (для accessibility, UI-SPEC). */
function fmtPct(n: number, withSign = false): string {
  if (!Number.isFinite(n)) return "—"
  const rounded = Math.round(n * 10) / 10
  const prefix = withSign ? (rounded >= 0 ? "+" : "") : ""
  return `${prefix}${rounded.toFixed(1)}%`
}

// ──────────────────────────────────────────────────────────────────
// Component
// ──────────────────────────────────────────────────────────────────

export function PriceCalculatorTable({
  groups,
  onRowClick,
}: PriceCalculatorTableProps) {
  if (groups.length === 0) {
    return (
      <div className="rounded-md border py-16 text-center text-muted-foreground">
        <p className="text-sm font-medium">
          Нет карточек с привязкой к товарам
        </p>
        <p className="text-xs mt-2">
          Синхронизируйте карточки WB и привяжите их к товарам на странице{" "}
          <a
            href="/cards/wb"
            className="text-primary underline"
          >
            Карточки товаров → WB
          </a>
          , затем вернитесь сюда.
        </p>
      </div>
    )
  }

  return (
    <div className="rounded-md border">
      <div className="relative overflow-x-auto">
        <table className="w-full caption-bottom text-sm">
          <thead className="sticky top-0 z-30 bg-background border-b">
            <tr>
              <th className="sticky left-0 z-40 bg-background border-r w-20 px-2 py-2 text-xs font-medium text-muted-foreground text-left">
                Фото
              </th>
              <th className="sticky left-20 z-40 bg-background border-r w-60 px-3 py-2 text-xs font-medium text-muted-foreground text-left">
                Сводка
              </th>
              <th className="sticky left-[320px] z-40 bg-background border-r w-20 px-2 py-2 text-xs font-medium text-muted-foreground text-left">
                Ярлык
              </th>
              <th className="sticky left-[400px] z-40 bg-background border-r w-28 px-2 py-2 text-xs font-medium text-muted-foreground text-left">
                Артикул
              </th>
              {/* Левая колонка типа (с индикатором / названием акции) */}
              <th className="px-2 py-2 text-xs font-medium text-muted-foreground text-left min-w-[180px]">
                Строка
              </th>
              {/* 30 расчётных колонок */}
              {COLUMN_ORDER.map((header) => (
                <th
                  key={header}
                  className="px-2 py-2 text-xs font-medium text-muted-foreground text-right whitespace-nowrap"
                >
                  {header}
                </th>
              ))}
            </tr>
          </thead>
          <tbody>
            {groups.map((group, gIdx) => {
              // Плоский индекс строк внутри product (для rowSpan'ов)
              let productRowIdx = 0

              return group.cards.flatMap((cardGroup, cardIdx) => {
                const isLastCardInProduct = cardIdx === group.cards.length - 1

                return cardGroup.priceRows.map((row, rowIdx) => {
                  const isFirstRowOfProduct = productRowIdx === 0
                  const isFirstRowOfCard = rowIdx === 0
                  const isFirstRowOfGroup = gIdx > 0 && isFirstRowOfProduct
                  productRowIdx++

                  const stripClass =
                    row.type === "regular"
                      ? "border-l-4 border-l-blue-500 bg-blue-50/30 dark:bg-blue-500/10"
                      : row.type === "auto"
                      ? "border-l-4 border-l-purple-500 bg-purple-50/30 dark:bg-purple-500/10"
                      : row.type === "calculated"
                      ? "border-l-4 border-l-amber-500 bg-amber-50/30 dark:bg-amber-500/10"
                      : ""

                  return (
                    <tr
                      key={row.id}
                      onClick={() =>
                        onRowClick?.(
                          cardGroup.card,
                          row,
                          group.product.id,
                        )
                      }
                      className={cn(
                        "h-10 cursor-pointer group hover:bg-muted/50",
                        isFirstRowOfGroup && "border-t-4 border-t-border",
                        !isFirstRowOfGroup &&
                          isFirstRowOfProduct &&
                          "border-t",
                        !isFirstRowOfProduct &&
                          isFirstRowOfCard &&
                          "border-t border-t-border/60",
                        stripClass,
                      )}
                    >
                      {/* Sticky Column 1: Фото (rowSpan всего product) */}
                      {isFirstRowOfProduct && (
                        <td
                          rowSpan={group.totalRowsInProduct}
                          className="sticky left-0 z-10 bg-background border-r w-20 align-top p-2 group-hover:bg-muted/50"
                        >
                          <div className="flex items-start justify-center">
                            {group.product.photoUrl ? (
                              <Image
                                src={group.product.photoUrl}
                                alt={group.product.name}
                                width={72}
                                height={96}
                                className="rounded border object-cover aspect-[3/4]"
                              />
                            ) : (
                              <div className="w-[72px] h-[96px] rounded border bg-muted" />
                            )}
                          </div>
                        </td>
                      )}

                      {/* Sticky Column 2: Сводка (rowSpan всего product) */}
                      {isFirstRowOfProduct && (
                        <td
                          rowSpan={group.totalRowsInProduct}
                          className="sticky left-20 z-10 bg-background border-r w-60 align-top p-3 group-hover:bg-muted/50"
                        >
                          <div className="flex flex-col gap-1">
                            <div className="text-sm font-medium leading-snug line-clamp-3">
                              {group.product.name}
                            </div>
                            <div className="text-xs text-muted-foreground">
                              Остаток:{" "}
                              <span className="text-foreground tabular-nums">
                                {group.product.totalStock}
                              </span>{" "}
                              шт
                            </div>
                            <div className="text-xs text-muted-foreground">
                              Скорость 7д:{" "}
                              <span className="text-foreground tabular-nums">
                                {group.product.totalAvgSalesSpeed.toFixed(1)}
                              </span>{" "}
                              шт/день
                            </div>
                          </div>
                        </td>
                      )}

                      {/* Sticky Column 3: Ярлык (rowSpan card) */}
                      {isFirstRowOfCard && (
                        <td
                          rowSpan={cardGroup.priceRows.length}
                          className="sticky left-[320px] z-10 bg-background border-r w-20 align-top p-2 text-sm group-hover:bg-muted/50"
                        >
                          {cardGroup.card.label ?? "—"}
                        </td>
                      )}

                      {/* Sticky Column 4: Артикул (rowSpan card) */}
                      {isFirstRowOfCard && (
                        <td
                          rowSpan={cardGroup.priceRows.length}
                          className="sticky left-[400px] z-10 bg-background border-r w-28 align-top p-2 font-mono text-xs group-hover:bg-muted/50"
                        >
                          {cardGroup.card.nmId}
                        </td>
                      )}

                      {/* Левая колонка: тип строки / название акции */}
                      <td className="px-2 py-1 text-sm min-w-[180px]">
                        {row.type === "current" && (
                          <Badge
                            variant="outline"
                            className="bg-primary/10 text-primary border-primary/30"
                          >
                            Текущая
                          </Badge>
                        )}
                        {(row.type === "regular" || row.type === "auto") && (
                          <PromoTooltip
                            description={row.promotionDescription}
                            advantages={row.promotionAdvantages}
                          >
                            {row.label}
                          </PromoTooltip>
                        )}
                        {row.type === "calculated" && (
                          <span className="text-sm">{row.label}</span>
                        )}
                      </td>

                      {/* 30 расчётных колонок */}
                      {/* EXECUTOR: сопоставить COLUMN_ORDER с полями PricingOutputs.
                          Порядок колонок из Excel: Цена для установки, Скидка продавца, Цена продавца, Скидка WB,
                          Цена со скидкой WB, Скидка клуба, Кошелёк, Цена с кошельком, Эквайринг, Комиссия, ДРР, Джем,
                          К перечислению, Закупка, Брак, Доставка, Кредит, Общие, Налог, Прибыль, Re продаж, ROI.

                          Для каждой колонки: показать либо input значение (Цена для установки, Скидка продавца)
                          либо computed значение (Цена продавца и далее).
                      */}
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.sellerPriceBeforeDiscount)}
                      </td>
                      {/* Скидка продавца % (input — из card) — заглушка, нужен WbCard.sellerDiscount */}
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        —
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.sellerPrice)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        —
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.priceAfterWbDiscount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        —
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.walletAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.priceAfterWallet)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.acquiringAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.commissionAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.drrAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.jemAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.transferAmount)}
                      </td>
                      {/* Закупка (costPrice — передаётся через row или computed — EXECUTOR решает) */}
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        —
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.defectAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        —
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.creditAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.overheadAmount)}
                      </td>
                      <td className="px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right">
                        {fmtMoney(row.computed.taxAmount)}
                      </td>

                      {/* Прибыль (подсветка) */}
                      <td
                        className={cn(
                          "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right font-medium",
                          row.computed.profit >= 0
                            ? "text-green-600 dark:text-green-500"
                            : "text-red-600 dark:text-red-500",
                        )}
                      >
                        {fmtMoney(row.computed.profit)}
                      </td>

                      {/* Re продаж % */}
                      <td
                        className={cn(
                          "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right font-medium",
                          row.computed.returnOnSalesPct >= 0
                            ? "text-green-600 dark:text-green-500"
                            : "text-red-600 dark:text-red-500",
                        )}
                      >
                        {fmtPct(row.computed.returnOnSalesPct, true)}
                      </td>

                      {/* ROI % */}
                      <td
                        className={cn(
                          "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right font-medium",
                          row.computed.roiPct >= 0
                            ? "text-green-600 dark:text-green-500"
                            : "text-red-600 dark:text-red-500",
                        )}
                      >
                        {fmtPct(row.computed.roiPct, true)}
                      </td>
                    </tr>
                  )
                })
              })
            })}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

**Примечания для executor'а:**

1. **COLUMN_ORDER vs PricingOutputs fields:** Этот план даёт основу. Executor должен точно сопоставить:
   - Колонки-input'ы (Цена для установки, Скидка продавца, Скидка WB, Скидка клуба, Закупка, Доставка) — берутся из `row.sellerPriceBeforeDiscount` и метаданных PriceRow (которые план 07-08 добавит)
   - Колонки-output'ы (все остальные) — из `row.computed.*`

2. **Дополнить PriceRow интерфейс:** если нужны дополнительные поля для колонок (например `sellerDiscountPct`, `wbDiscountPct`, `costPrice`), расширить interface PriceRow в начале файла. План 07-08 уже заполнит их из данных.

3. **Sticky классы z-index layers:**
   - Header cells: `z-40` (поверх всего)
   - Body sticky cells: `z-10`
   - Обычные body cells: auto (0)
   - Non-sticky tbody > sticky tbody при hover — нужен `group-hover:bg-muted/50` на sticky cells

4. **rowSpan guard:** `isFirstRowOfProduct` и `isFirstRowOfCard` должны быть правильно вычислены — см. `productRowIdx` counter. Неправильный rowSpan → визуальный slip колонок.

5. **Пустое состояние:** если `groups.length === 0` → рендерится empty state с ссылкой на `/cards/wb`.

**Правила:**
- `"use client"` в первой строке.
- Все тексты на русском.
- Классы Tailwind строго из UI-SPEC (spacing 4-8-12-16-24-32, typography text-xs/text-sm/text-base/text-2xl, 2 font weights).
- Dark mode цвета через `dark:text-green-500` / `dark:text-red-500` (UI-SPEC).
- rowSpan паттерн критичен — протестировать визуально в плане 07-08.
  </action>
  <verify>
    <automated>test -f components/prices/PriceCalculatorTable.tsx && grep -c "rowSpan" components/prices/PriceCalculatorTable.tsx && grep -c "sticky" components/prices/PriceCalculatorTable.tsx && npx tsc --noEmit 2>&1 | grep -c "PriceCalculatorTable"</automated>
    <manual>
1. `cat components/prices/PriceCalculatorTable.tsx | head -20` — `"use client"` присутствует, types exported.
2. `grep "rowSpan={group.totalRowsInProduct}" components/prices/PriceCalculatorTable.tsx` — Фото и Сводка имеют правильный rowSpan.
3. `grep "rowSpan={cardGroup.priceRows.length}" components/prices/PriceCalculatorTable.tsx` — Ярлык и Артикул.
4. `grep "border-l-blue-500\|border-l-purple-500\|border-l-amber-500" components/prices/PriceCalculatorTable.tsx` — 3 типа indicator strips.
5. `grep "text-green-600\|text-red-600" components/prices/PriceCalculatorTable.tsx` — подсветка profit.
6. `grep "COLUMN_ORDER" components/prices/PriceCalculatorTable.tsx` — заголовки из pricing-math.
7. `grep "onRowClick" components/prices/PriceCalculatorTable.tsx` — prop присутствует для подключения модалки в 07-09.
8. `npx tsc --noEmit` без errors.
    </manual>
  </verify>
  <done>
- `PriceCalculatorTable.tsx` создан, exported types (PriceRow, WbCardRowGroup, ProductGroup)
- 4 sticky колонки с правильным накопительным left:
- Header sticky top-0 z-30
- Row click handler вызывает onRowClick prop
- Indicator strips по типу строки
- Подсветка прибыли зелёным/красным
- Empty state при отсутствии groups
- TypeScript компилируется
  </done>
</task>

</tasks>

<verification>
- 3 файла созданы: GlobalRatesBar, PromoTooltip, PriceCalculatorTable
- `npx tsc --noEmit` без ошибок для новых файлов
- GlobalRatesBar debounced save через updateAppSetting
- PriceCalculatorTable имеет все визуальные элементы из UI-SPEC
- Все strings на русском
</verification>

<success_criteria>
- План 07-08 (RSC page) может импортировать `<GlobalRatesBar>`, `<PriceCalculatorTable>` и передать ему собранные `groups: ProductGroup[]`
- План 07-09 (модалка) подключится через `onRowClick` prop
- Визуально: таблица с sticky колонками, rowSpan группировкой, indicator strips, подсветкой profit
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-07-SUMMARY.md`. Включить:
- 3 созданных компонента + signatures
- Известные limitations: некоторые ячейки = `—` (placeholder для колонок-inputs которые нужно подтянуть из данных PriceRow в плане 07-08)
- TODO для executor плана 07-08: расширить `PriceRow` interface дополнительными полями (sellerDiscountPct, wbDiscountPct, costPrice etc) и заполнить их при сборке groups
</output>
