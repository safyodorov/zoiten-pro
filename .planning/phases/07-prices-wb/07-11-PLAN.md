---
phase: 07-prices-wb
plan: 11
type: execute
wave: 9
depends_on:
  - 07-10
files_modified:
  - CLAUDE.md
  - README.md
autonomous: false
requirements:
  - PRICES-01
  - PRICES-02
  - PRICES-03
  - PRICES-04
  - PRICES-05
  - PRICES-06
  - PRICES-07
  - PRICES-08
  - PRICES-09
  - PRICES-10
  - PRICES-11
  - PRICES-12
  - PRICES-13
  - PRICES-14
  - PRICES-15
  - PRICES-16
must_haves:
  truths:
    - "`CLAUDE.md` обновлён секцией про Phase 7: pricing domain, 6 глобальных ставок, модель данных (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature), WB Promotions Calendar API правила"
    - "`README.md` обновлён: в разделах ERP отмечено `3. Управление ценами — полная реализация WB` вместо «заглушка»"
    - "Полный запуск тестов `npm run test` — GREEN (все suites: pricing-math, pricing-fallback, pricing-settings, wb-promotions-api, excel-auto-promo)"
    - "Production build `npm run build` — успешен, без warnings TypeScript"
    - "Миграция применена на VPS: `ssh root@85.198.97.89 \"cd /opt/zoiten-pro && git pull && npx prisma migrate deploy && bash deploy.sh\"`"
    - "Deploy на VPS успешен, `https://zoiten.pro/prices/wb` открывается и работает"
    - "Human verification финальная: полный flow end-to-end на проде"
  artifacts:
    - path: "CLAUDE.md"
      provides: "обновлён с Phase 7 pricing секцией"
      contains: "Управление ценами"
    - path: "README.md"
      provides: "обновлён с отмеченным Phase 7 как complete"
      contains: "Управление ценами"
  key_links:
    - from: "deploy.sh"
      to: "prisma migrate deploy"
      via: "bash script на VPS"
      pattern: "migrate deploy"
---

<objective>
Финальный план Phase 7: обновление документации (CLAUDE.md, README.md), финальная валидация всех тестов, production build, и deploy на VPS с применением миграции БД.

Purpose: закрытие фазы 7 — от разработки к production. После этого плана раздел «Управление ценами → WB» live на https://zoiten.pro/prices/wb.

Output:
- Обновлённая документация
- Все тесты GREEN
- Deploy успешен
- Human verification на проде
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-01-SUMMARY.md
@.planning/phases/07-prices-wb/07-02-SUMMARY.md
@.planning/phases/07-prices-wb/07-03-SUMMARY.md
@.planning/phases/07-prices-wb/07-04-SUMMARY.md
@.planning/phases/07-prices-wb/07-05-SUMMARY.md
@.planning/phases/07-prices-wb/07-06-SUMMARY.md
@.planning/phases/07-prices-wb/07-07-SUMMARY.md
@.planning/phases/07-prices-wb/07-08-SUMMARY.md
@.planning/phases/07-prices-wb/07-09-SUMMARY.md
@.planning/phases/07-prices-wb/07-10-SUMMARY.md
@CLAUDE.md
@README.md

<interfaces>
<!-- VPS deploy script (существует): -->

`/opt/zoiten-pro/deploy.sh` на VPS уже содержит:
1. `git pull` (или аналог)
2. `npm ci` (или `pnpm install`)
3. `npx prisma generate`
4. `npx prisma migrate deploy` ← применит нашу миграцию `20260409_prices_wb` автоматически
5. `npm run build`
6. `systemctl restart zoiten-erp`

Deploy команда: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`

<!-- WB API token scope на проде: -->

- Токен в `/etc/zoiten.pro.env` как `WB_API_TOKEN`
- Должен иметь scope «Цены и скидки» (bit 3) — это уже должно быть так, если smoke test в Wave 0 прошёл
- Если на проде токен другой и не имеет scope — перегенерировать через WB API Keys UI и обновить `/etc/zoiten.pro.env`

<!-- CLAUDE.md секция — куда добавлять: -->

Добавить новую секцию после «## Синхронизация с Wildberries» и перед «## VPS заметки»:

```markdown
## Управление ценами WB — Phase 7

### Домен

Раздел `/prices/wb` — онлайн-калькулятор юнит-экономики WB карточек. Показывает для каждого Product связанные WbCards с ценовыми строками: Текущая + Regular акции + Auto акции + Расчётные (1-3 слота). Клик по строке → модалка с realtime пересчётом 30 колонок расчёта.

### Модель данных (новые таблицы в Phase 7)

- **AppSetting** (KeyValue) — 6 глобальных ставок: wbWalletPct (2.0), wbAcquiringPct (2.7), wbJemPct (1.0), wbCreditPct (7.0), wbOverheadPct (6.0), wbTaxPct (8.0)
- **CalculatedPrice** — расчётные цены пользователя, uniqueness per (wbCardId, slot 1/2/3)
- **WbPromotion** — акции WB с id = promotionID из WB API
- **WbPromotionNomenclature** — связи nmId с акциями (regular через API, auto через Excel)

### Новые поля в существующих таблицах

- `Category.defaultDefectRatePct` — default процент брака per категория (fallback 2%)
- `Subcategory.defaultDrrPct` — default ДРР per подкатегория (fallback 10%)
- `Product.drrOverridePct`, `Product.defectRateOverridePct`, `Product.deliveryCostRub` — per-product overrides
- `WbCard.avgSalesSpeed7d` — средняя скорость продаж за 7 дней (из Sales API)

### Fallback chain для per-product параметров

`Product.override → Subcategory/Category.default → hardcoded`

Реализовано в `lib/pricing-math.ts`: функции `resolveDrrPct`, `resolveDefectRatePct`, `resolveDeliveryCostRub`.

### WB Promotions Calendar API

- Base URL: `https://dp-calendar-api.wildberries.ru` (верифицирован Wave 0)
- Rate limit: 10 req / 6 sec → 600ms паузы между запросами, sleep(6000) + retry на 429
- Endpoints: `/api/v1/calendar/promotions`, `/api/v1/calendar/promotions/details`, `/api/v1/calendar/promotions/nomenclatures`
- **Auto-акции:** API возвращает 422 на `/nomenclatures` — данные загружаются через Excel из кабинета WB (D-06)

### Pure function calculatePricing

`lib/pricing-math.ts` — единственная source of truth для расчёта юнит-экономики. Используется и на сервере (RSC page), и на клиенте (модалка с realtime). Pure, детерминированная, без зависимостей.

**Golden test:** `tests/pricing-math.test.ts` — nmId 800750522 → profit ≈ 567.68 ₽, ROI ≈ 26%, Re продаж ≈ 7%.

### Routes

- `/prices` → redirect на `/prices/wb`
- `/prices/wb` — основной раздел (RSC + клиентские компоненты)
- `/prices/ozon` — заглушка ComingSoon
- `POST /api/wb-promotions-sync` — синхронизация акций (MANAGE)
- `POST /api/wb-promotions-upload-excel` — загрузка Excel auto-акции (MANAGE)

### RBAC

- Read (`/prices/wb` rendering): `requireSection("PRICES")`
- Write (все server actions, sync, upload): `requireSection("PRICES", "MANAGE")`
- Все 7 server actions в `app/actions/pricing.ts` защищены

### Компоненты

- `GlobalRatesBar` — редактор 6 ставок с debounced save (500ms)
- `PriceCalculatorTable` — главная таблица с rowSpan + sticky колонки + indicator strips
- `PricingCalculatorDialog` — модалка юнит-экономики с realtime пересчётом
- `WbPromotionsSyncButton`, `WbAutoPromoUploadButton` — кнопки шапки
- `PromoTooltip` — wrapper shadcn Tooltip с description + advantages

### Testing

Phase 7 добавил vitest в проект. Запуск: `npm run test`. Покрытие:
- `pricing-math.test.ts` — golden test + zero guards
- `pricing-fallback.test.ts` — fallback chain
- `pricing-settings.test.ts` — Zod валидация
- `wb-promotions-api.test.ts` — mocked rate limit
- `excel-auto-promo.test.ts` — реальный fixture парсинг
```

<!-- README.md — где обновлять: -->

В разделе «Разделы ERP» изменить:

Было:
```
3. Управление ценами (заглушка)
```

Стало:
```
3. **Управление ценами** ✅ калькулятор юнит-экономики WB, синхронизация акций через API, загрузка Excel auto-акций, realtime модалка расчёта + заглушка Ozon
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить CLAUDE.md и README.md + финальная валидация тестов и build</name>
  <files>CLAUDE.md, README.md</files>
  <action>
**1. Обновить `CLAUDE.md`:**

Найти в CLAUDE.md раздел «## Синхронизация с Wildberries — ВАЖНАЯ СЕКЦИЯ» и после него (перед «## VPS заметки») добавить новую секцию **«## Управление ценами WB — Phase 7»** с содержимым из `<interfaces>` выше.

НЕ удалять существующие разделы, только ДОБАВИТЬ новый.

**2. Обновить `README.md`:**

Найти в README.md раздел «## Разделы ERP» (список нумерованных разделов) и обновить строку 3:

Было (приблизительно):
```
3. Управление ценами (заглушка)
```

Стало:
```
3. **Управление ценами** ✅ калькулятор юнит-экономики WB, синхронизация акций через API, загрузка Excel auto-акций, realtime модалка расчёта + заглушка Ozon
```

**3. Запустить финальную валидацию:**

```bash
# Все тесты должны быть GREEN
npm run test

# Production build должен пройти без TypeScript/lint ошибок
npm run build
```

Если `npm run test` имеет failures — смотреть какой test suite падает, возвращаться к соответствующему плану (07-02 для pricing-math, 07-04 для excel-auto-promo, 07-05 для pricing-settings, 07-03 для wb-promotions-api).

Если `npm run build` падает — смотреть какой файл имеет TypeScript error, исправлять в соответствующем компоненте.

**4. Git commit:**

```bash
cd C:/Claude/zoiten-pro
git add .
git status  # проверить что нет sensitive файлов
git commit -m "feat(prices): Phase 7 — Управление ценами WB

Раздел /prices/wb — калькулятор юнит-экономики с синхронизацией акций
через WB Promotions Calendar API и загрузкой Excel для auto-акций.

- 4 новые Prisma модели (AppSetting, CalculatedPrice, WbPromotion, WbPromotionNomenclature)
- 6 новых полей в Category/Subcategory/Product/WbCard
- Pure function calculatePricing с golden test (nmId 800750522 → profit 567.68 ₽)
- WB Promotions API rate limit compliant (600ms / 6000ms)
- RSC page с parallel data fetch + клиентский wrapper для модалки
- GlobalRatesBar (debounced save), PriceCalculatorTable (rowSpan + sticky), PricingCalculatorDialog (realtime)
- Addресует требования PRICES-01 .. PRICES-16

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```

**5. Push на origin (или оставить для деплоя через pull):**

```bash
git push origin main
```

Или если workflow через VPS pull — просто оставить локально коммит.

**Правила:**
- Все тексты в документации на русском.
- НЕ коммитить `.env` файлы, `/tests/fixtures/auto-promo-sample.xlsx` проверить что он не sensitive (Excel отчёт из кабинета WB содержит номенклатуры — для тестов приемлемо, но проверить что нет персональных данных).
- Commit message детальный — описывает что добавлено на high level.
  </action>
  <verify>
    <automated>npm run test && npm run build 2>&1 | tail -30</automated>
    <manual>
1. `grep "Управление ценами WB — Phase 7" CLAUDE.md` — новая секция добавлена.
2. `grep "калькулятор юнит-экономики WB" README.md` — README обновлён.
3. `npm run test` → все тесты GREEN (минимум 15+ passed).
4. `npm run build` → без ошибок.
5. `git log -1` — последний коммит содержит Phase 7 changes.
    </manual>
  </verify>
  <done>
- CLAUDE.md содержит новую секцию Phase 7
- README.md отмечает раздел как complete
- `npm run test` GREEN (все 5 suites)
- `npm run build` успешен
- Git commit создан
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy на VPS и финальная end-to-end верификация на проде</name>
  <what-built>
- Phase 7 полностью реализована локально
- Документация обновлена
- Тесты GREEN
- Production build успешен
- Всё готово к деплою на VPS
  </what-built>
  <how-to-verify>
1. **Push изменений на origin** (если не сделано):
   ```bash
   git push origin main
   ```

2. **Deploy на VPS:**
   ```bash
   ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
   ```

   Deploy script должен:
   - Сделать git pull
   - `npm ci`
   - `npx prisma generate`
   - `npx prisma migrate deploy` ← применит `20260409_prices_wb` миграцию
   - `npm run build`
   - `systemctl restart zoiten-erp`

3. **Проверить что миграция применилась на VPS:**
   ```bash
   ssh root@85.198.97.89 "cd /opt/zoiten-pro && psql -U zoiten -d zoiten_erp -c '\\dt' | grep -E 'AppSetting|CalculatedPrice|WbPromotion'"
   ```
   Должны быть видны 4 новые таблицы.

   ```bash
   ssh root@85.198.97.89 "cd /opt/zoiten-pro && psql -U zoiten -d zoiten_erp -c 'SELECT key, value FROM \"AppSetting\";'"
   ```
   Должны быть 6 ключей с дефолтными значениями (2.0, 2.7, 1.0, 7.0, 6.0, 8.0).

4. **Проверить что WB_API_TOKEN scope включает «Цены и скидки»:**
   ```bash
   ssh root@85.198.97.89 "curl -s 'https://dp-calendar-api.wildberries.ru/api/v1/calendar/promotions?startDateTime=2026-04-09T00:00:00Z&endDateTime=2026-04-10T00:00:00Z&allPromo=true&limit=1' -H \"Authorization: \$WB_API_TOKEN\" | head -c 200"
   ```
   Должен вернуть JSON с `data.promotions`. Если 401/403 — перегенерировать токен с правильным scope на проде, обновить `/etc/zoiten.pro.env`, перезапустить `systemctl restart zoiten-erp`.

5. **Открыть prod:**
   - `https://zoiten.pro/prices/wb` (залогиниться как MANAGER или SUPERADMIN)

6. **Полный end-to-end flow на проде:**
   - Страница открывается, Alert «Акции не синхронизированы» виден
   - Нажать «Синхронизировать с WB» → карточки обновляются, `avgSalesSpeed7d` заполняется
   - Нажать «Синхронизировать акции» → ждать 30-90 сек → toast success
   - Alert исчезает, в таблице появляются regular акционные строки (синие)
   - Если есть auto-акции в списке — загрузить Excel через «Загрузить отчёт auto-акции»
   - Клик на ценовую строку → открывается модалка
   - Изменить ДРР с 10 на 15 → Прибыль обновляется realtime
   - Сохранить в слот 1 → toast success → новая расчётная строка (янтарная) появляется в таблице
   - Обновить страницу → расчётная строка сохранилась
   - Изменить Кошелёк WB с 2.0 на 2.5 → toast success → все расчёты пересчитались

7. **Проверить RBAC на проде:**
   - Залогиниться как VIEWER (создать пользователя если нужно)
   - Открыть `/prices/wb` — таблица видна (read ok)
   - Попытка изменить ставку → 403 → toast error «Недостаточно прав»

8. **Подтвердить готовность фазы:**
   - Все 16 требований PRICES-01 .. PRICES-16 проверены вручную
   - Production работает стабильно
   - Миграция БД применена

**Если любой шаг fails — опишите проблему, executor исправит.**
  </how-to-verify>
  <resume-signal>
Введите "approved" если всё работает на проде. Иначе опишите проблемы.
  </resume-signal>
</task>

</tasks>

<verification>
- Документация обновлена (CLAUDE.md, README.md)
- `npm run test` GREEN
- `npm run build` успешен
- Git commit создан
- Deploy на VPS успешен
- Миграция 20260409_prices_wb применена на проде
- End-to-end flow работает на https://zoiten.pro/prices/wb
</verification>

<success_criteria>
- Phase 7 complete: все 16 требований PRICES-01..PRICES-16 реализованы
- Production live: https://zoiten.pro/prices/wb работает
- Тесты GREEN, build успешен
- Документация отражает реальное состояние проекта
- STATE.md обновлён (в SUMMARY плана)
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-11-SUMMARY.md` + **главный phase SUMMARY** `.planning/phases/07-prices-wb/07-SUMMARY.md` содержащий:
- Обзор всей фазы 7 (11 планов, waves 0-9)
- Список всех реализованных требований PRICES-01..PRICES-16 со статусом
- Созданные файлы (26+ файлов, 4 миграции модели, 4 новые таблицы, 6 полей)
- Golden test результаты
- Deployment status на VPS
- Known issues / deferred items (ссылка на `<deferred>` в CONTEXT.md)
- Рекомендации для Phase 8 (если планируется): отправка цен в WB через Prices API, история расчётов, экспорт Excel, Ozon Pricing
</output>
