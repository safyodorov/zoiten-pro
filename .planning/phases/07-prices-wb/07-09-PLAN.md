---
phase: 07-prices-wb
plan: 09
type: execute
wave: 7
depends_on:
  - 07-05
  - 07-07
  - 07-08
files_modified:
  - components/prices/PricingCalculatorDialog.tsx
  - components/prices/PriceCalculatorTableWrapper.tsx
  - app/(dashboard)/prices/wb/page.tsx
autonomous: false
requirements:
  - PRICES-04
  - PRICES-07
  - PRICES-08
  - PRICES-09
  - PRICES-16
must_haves:
  truths:
    - "`PricingCalculatorDialog` — shadcn Dialog с 2-колоночным layout: inputs слева, realtime outputs справа"
    - "Использует `react-hook-form` + `zod` resolver для форм inputs"
    - "Realtime пересчёт через `useWatch` + `useMemo` → `calculatePricing` (latency < 100ms)"
    - "Footer содержит native `<select>` для выбора слота (1/2/3), input названия, кнопки «Отмена» и «Сохранить как расчётную цену»"
    - "Чекбоксы «только этот товар» для ДРР и Брак управляют вызовом `updateProductOverride` vs `updateSubcategoryDefault`/`updateCategoryDefault`"
    - "`PriceCalculatorTableWrapper` — клиентский компонент обёртка, хранит useState для открытой модалки и передаёт `onRowClick` в `<PriceCalculatorTable>`"
    - "`/prices/wb/page.tsx` обновлён — рендерит `<PriceCalculatorTableWrapper>` вместо прямого `<PriceCalculatorTable>`"
    - "После save — toast success + revalidation (страница перерисуется с новыми данными)"
    - "Checkpoint: human-verify полной интерактивности клика и сохранения"
  artifacts:
    - path: "components/prices/PricingCalculatorDialog.tsx"
      provides: "модалка юнит-экономики с realtime пересчётом и сохранением"
      min_lines: 200
    - path: "components/prices/PriceCalculatorTableWrapper.tsx"
      provides: "клиентский wrapper для PriceCalculatorTable + Dialog state"
      min_lines: 50
    - path: "app/(dashboard)/prices/wb/page.tsx"
      provides: "обновлён: рендерит Wrapper вместо прямого Table"
      min_lines: 150
  key_links:
    - from: "components/prices/PricingCalculatorDialog.tsx"
      to: "app/actions/pricing.ts saveCalculatedPrice"
      via: "onSubmit"
      pattern: "saveCalculatedPrice"
    - from: "components/prices/PricingCalculatorDialog.tsx"
      to: "lib/pricing-math.ts calculatePricing"
      via: "useMemo realtime compute"
      pattern: "calculatePricing"
    - from: "components/prices/PriceCalculatorTableWrapper.tsx"
      to: "components/prices/PriceCalculatorTable"
      via: "render с onRowClick prop"
      pattern: "PriceCalculatorTable"
    - from: "components/prices/PriceCalculatorTableWrapper.tsx"
      to: "components/prices/PricingCalculatorDialog"
      via: "conditional render при open state"
      pattern: "PricingCalculatorDialog"
---

<objective>
Создать модалку юнит-экономики `PricingCalculatorDialog` с realtime пересчётом и функционалом сохранения в `CalculatedPrice`. Обернуть `PriceCalculatorTable` в клиентский wrapper для управления state модалки.

Это последний крупный UI компонент — после него раздел функционально готов.

Purpose: пользователь кликает на любую ценовую строку → открывается модалка с inputs и live расчётом → меняет параметры → видит моментальные изменения → сохраняет как расчётную цену в слот 1/2/3.

Output:
- `PricingCalculatorDialog.tsx` — 2-колоночная модалка
- `PriceCalculatorTableWrapper.tsx` — клиентский wrapper со state модалки
- `/prices/wb/page.tsx` обновлён рендерить wrapper
- Human verification checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-UI-SPEC.md
@.planning/phases/07-prices-wb/07-02-SUMMARY.md
@.planning/phases/07-prices-wb/07-05-SUMMARY.md
@.planning/phases/07-prices-wb/07-07-SUMMARY.md
@.planning/phases/07-prices-wb/07-08-SUMMARY.md
@CLAUDE.md
@components/ui/dialog.tsx
@components/users/UserDialog.tsx
@components/prices/PriceCalculatorTable.tsx
@lib/pricing-math.ts
@app/actions/pricing.ts

<interfaces>
<!-- Из lib/pricing-math.ts: -->

```typescript
import { calculatePricing, type PricingInputs, type PricingOutputs } from "@/lib/pricing-math"
```

<!-- Из app/actions/pricing.ts: -->

```typescript
import {
  saveCalculatedPrice,
  updateProductOverride,
  updateSubcategoryDefault,
  updateCategoryDefault,
  updateProductDelivery,
} from "@/app/actions/pricing"
```

<!-- Из PriceCalculatorTable (план 07-07): -->

```typescript
import {
  PriceCalculatorTable,
  type PriceRow,
  type ProductGroup,
  type WbCardRowGroup,
} from "@/components/prices/PriceCalculatorTable"

// PriceCalculatorTable принимает prop:
// onRowClick?: (card: WbCardRowGroup["card"], row: PriceRow, productId: string) => void
```

<!-- Паттерн существующего UserDialog (для копирования shadcn Dialog + react-hook-form): -->

```tsx
"use client"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"

const schema = z.object({ ... })

function UserDialog({ open, onOpenChange, user }) {
  const form = useForm({ resolver: zodResolver(schema), defaultValues: {...} })
  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-xl max-h-[92vh] overflow-y-auto">
        ...
      </DialogContent>
    </Dialog>
  )
}
```

<!-- UI-SPEC для модалки (D-14, D-15): -->

- `<DialogContent className="sm:max-w-4xl max-h-[92vh] overflow-y-auto">` (ширина 4xl = 896px для 2-колоночного layout)
- Header:
  ```tsx
  <DialogTitle>Расчёт юнит-экономики: {wbCard.name}</DialogTitle>
  <DialogDescription>Артикул: {wbCard.nmId} · Исходная цена: {initialSellerPrice} ₽</DialogDescription>
  ```
- Body: `<div className="grid grid-cols-1 md:grid-cols-2 gap-6">`
  - Левая колонка: `<h3>Входные параметры</h3>` + inputs (Цена продавца до скидки, Скидка продавца, ДРР + checkbox, Брак + checkbox, Доставка, Себестоимость readonly, Скидка WB СПП readonly, Комиссия ИУ readonly, Кошелёк/Эквайринг/Джем/Кредит/Общие/Налог collapsible)
  - Правая колонка: `<h3>Результат расчёта</h3>` + 30 outputs (через useMemo)
- Footer:
  ```tsx
  <DialogFooter>
    <div className="flex items-center gap-3 w-full">
      <label>Сохранить в слот:</label>
      <select className="h-9 rounded border border-input bg-transparent px-2 text-sm">
        <option value="1">Слот 1</option>
        <option value="2">Слот 2</option>
        <option value="3">Слот 3</option>
      </select>
      <Input placeholder="Название (опционально)" />
      <Button variant="outline">Отмена</Button>
      <Button type="submit">Сохранить как расчётную цену</Button>
    </div>
  </DialogFooter>
  ```

<!-- Ключевые детали: -->

1. **Native `<select>` для слота** (не shadcn Select из CLAUDE.md — base-ui Select ломается).
2. **Realtime пересчёт:** `useWatch({control, name: [...]})` — следит за конкретными полями, не триггерит rerender при изменении других.
3. **Чекбоксы scope:** ДРР и Брак имеют чекбокс «только этот товар» — при submit решает, куда писать:
   - true → `updateProductOverride(productId, "drrOverridePct", value)` (или defectRateOverridePct)
   - false → `updateSubcategoryDefault(subcategoryId, value)` (ДРР) или `updateCategoryDefault(categoryId, value)` (брак) — с предупреждающим `toast.warning("Изменение применится ко всем товарам подкатегории")`
4. **Snapshot:** при submit собирается полный JSON снимок параметров и передаётся в `saveCalculatedPrice`:
   ```typescript
   const snapshot = {
     inputs: { priceBeforeDiscount, sellerDiscountPct, wbDiscountPct, clubDiscountPct, commFbwPct, costPrice, buyoutPct, drrPct, defectRatePct, deliveryCostRub, walletPct, acquiringPct, jemPct, creditPct, overheadPct, taxPct },
     outputs: calculatePricing(inputs), // debug snapshot
     savedAt: new Date().toISOString(),
   }
   ```

<!-- Types wrapper компонента: -->

```typescript
interface OpenDialogState {
  card: WbCardRowGroup["card"]
  row: PriceRow
  productId: string
  // Дополнительные контекстные поля для модалки — всё что нужно для inputs
  // (costPrice, commFbwPct, subcategoryId, categoryId, etc.)
  // EXECUTOR: план 07-08 должен передавать эти поля через расширенный PriceRow или
  // wrapper извлекает их из groups по productId+cardId перед открытием.
}
```

**EXECUTOR REFINEMENT:** самый простой подход — расширить `PriceRow` в `PriceCalculatorTable.tsx` дополнительными полями для модалки (`inputs: PricingInputs` — полный набор), чтобы wrapper не искал дополнительно. План 07-08 заполнит эти поля при сборке groups.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать PricingCalculatorDialog и PriceCalculatorTableWrapper</name>
  <files>components/prices/PricingCalculatorDialog.tsx, components/prices/PriceCalculatorTableWrapper.tsx</files>
  <action>
**1. Расширить `PriceRow` в `components/prices/PriceCalculatorTable.tsx`** — добавить поле `inputs: PricingInputs` (полные входные параметры, которые использовались для серверного расчёта). Это избавит wrapper от повторного поиска данных.

Открыть `components/prices/PriceCalculatorTable.tsx` и в interface `PriceRow` добавить:

```typescript
import type { PricingInputs } from "@/lib/pricing-math"

export interface PriceRow {
  id: string
  type: PriceRowType
  sellerPriceBeforeDiscount: number
  label: string
  promotionDescription?: string | null
  promotionAdvantages?: readonly string[] | null
  calculatedSlot?: 1 | 2 | 3
  computed: PricingOutputs
  /** Полный набор inputs использованных при серверном расчёте.
   *  Позволяет модалке начать с этих значений без повторного запроса БД. */
  inputs: PricingInputs
  /** Дополнительный контекст для модалки. */
  context: {
    productId: string
    subcategoryId: string | null
    categoryId: string | null
  }
}
```

**2. Обновить `app/(dashboard)/prices/wb/page.tsx`** — при сборке каждого priceRow заполнять `inputs` и `context`. Это ОДНА строчка в каждом push — передавать тот же объект `inputsForRow` что уже используется для `calculatePricing()`.

Пример (в сборке current row):
```typescript
const currentInputs: PricingInputs = {
  ...baseInputs,
  priceBeforeDiscount: currentSellerPrice,
  sellerDiscountPct: currentSellerDiscount,
}
priceRows.push({
  id: `${card.id}-current`,
  type: "current",
  sellerPriceBeforeDiscount: currentSellerPrice,
  label: "Текущая",
  computed: calculatePricing(currentInputs),
  inputs: currentInputs,
  context: {
    productId: product.id,
    subcategoryId: product.subcategoryId ?? null,
    categoryId: product.categoryId ?? null,
  },
})
```

Применить аналогично к regular/auto/calculated rows.

**3. Создать `components/prices/PricingCalculatorDialog.tsx`:**

```tsx
// components/prices/PricingCalculatorDialog.tsx
// Phase 7: Модалка юнит-экономики с realtime пересчётом и сохранением расчётной цены.
//
// UI-SPEC: 2-колоночный layout, native <select> для слота, realtime через useWatch.

"use client"

import { useMemo, useState, useTransition } from "react"
import { useForm, useWatch } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import { z } from "zod"
import { toast } from "sonner"
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Checkbox } from "@/components/ui/checkbox"
import { cn } from "@/lib/utils"
import { calculatePricing, type PricingInputs } from "@/lib/pricing-math"
import {
  saveCalculatedPrice,
  updateProductOverride,
  updateSubcategoryDefault,
  updateCategoryDefault,
  updateProductDelivery,
} from "@/app/actions/pricing"
import type { PriceRow } from "@/components/prices/PriceCalculatorTable"

const formSchema = z.object({
  priceBeforeDiscount: z.coerce.number().min(0),
  sellerDiscountPct: z.coerce.number().min(0).max(100),
  drrPct: z.coerce.number().min(0).max(100),
  defectRatePct: z.coerce.number().min(0).max(100),
  deliveryCostRub: z.coerce.number().min(0),
  // Scope flags
  drrScopeProduct: z.boolean(),
  defectScopeProduct: z.boolean(),
  // Save metadata
  slot: z.coerce.number().int().min(1).max(3),
  calculatedName: z.string().max(100),
})

type FormValues = z.infer<typeof formSchema>

interface PricingCalculatorDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  card: {
    id: string
    nmId: number
    name?: string
  }
  row: PriceRow
}

export function PricingCalculatorDialog({
  open,
  onOpenChange,
  card,
  row,
}: PricingCalculatorDialogProps) {
  const [isPending, startTransition] = useTransition()

  // Initial form values — из серверного inputs
  const initialSlot =
    row.type === "calculated" && row.calculatedSlot ? row.calculatedSlot : 1
  const initialName =
    row.type === "calculated" ? row.label : `Расчётная цена ${initialSlot}`

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      priceBeforeDiscount: row.inputs.priceBeforeDiscount,
      sellerDiscountPct: row.inputs.sellerDiscountPct,
      drrPct: row.inputs.drrPct,
      defectRatePct: row.inputs.defectRatePct,
      deliveryCostRub: row.inputs.deliveryCostRub,
      drrScopeProduct: true,
      defectScopeProduct: true,
      slot: initialSlot,
      calculatedName: initialName,
    },
  })

  // Realtime outputs через useWatch + useMemo
  const watched = useWatch({
    control: form.control,
    name: [
      "priceBeforeDiscount",
      "sellerDiscountPct",
      "drrPct",
      "defectRatePct",
      "deliveryCostRub",
    ],
  })

  const liveOutputs = useMemo(() => {
    const [pBefore, sDisc, drr, defect, delivery] = watched
    const inputs: PricingInputs = {
      ...row.inputs,
      priceBeforeDiscount: pBefore ?? 0,
      sellerDiscountPct: sDisc ?? 0,
      drrPct: drr ?? 0,
      defectRatePct: defect ?? 0,
      deliveryCostRub: delivery ?? 0,
    }
    return calculatePricing(inputs)
  }, [watched, row.inputs])

  const onSubmit = (values: FormValues) => {
    startTransition(async () => {
      try {
        // 1. Scope updates для ДРР
        if (values.drrPct !== row.inputs.drrPct) {
          if (values.drrScopeProduct) {
            await updateProductOverride({
              productId: row.context.productId,
              field: "drrOverridePct",
              value: values.drrPct,
            })
          } else if (row.context.subcategoryId) {
            await updateSubcategoryDefault(
              row.context.subcategoryId,
              values.drrPct,
            )
            toast.info(
              "ДРР обновлён для всех товаров подкатегории",
            )
          }
        }

        // 2. Scope updates для Брака
        if (values.defectRatePct !== row.inputs.defectRatePct) {
          if (values.defectScopeProduct) {
            await updateProductOverride({
              productId: row.context.productId,
              field: "defectRateOverridePct",
              value: values.defectRatePct,
            })
          } else if (row.context.categoryId) {
            await updateCategoryDefault(
              row.context.categoryId,
              values.defectRatePct,
            )
            toast.info(
              "Процент брака обновлён для всех товаров категории",
            )
          }
        }

        // 3. Доставка (всегда per-product)
        if (values.deliveryCostRub !== row.inputs.deliveryCostRub) {
          await updateProductDelivery(
            row.context.productId,
            values.deliveryCostRub,
          )
        }

        // 4. Сохранить расчётную цену
        const sellerPrice =
          values.priceBeforeDiscount * (1 - values.sellerDiscountPct / 100)
        const snapshot = {
          inputs: {
            ...row.inputs,
            priceBeforeDiscount: values.priceBeforeDiscount,
            sellerDiscountPct: values.sellerDiscountPct,
            drrPct: values.drrPct,
            defectRatePct: values.defectRatePct,
            deliveryCostRub: values.deliveryCostRub,
          },
          outputs: liveOutputs,
          savedAt: new Date().toISOString(),
        }

        const result = await saveCalculatedPrice({
          wbCardId: card.id,
          slot: values.slot,
          name:
            values.calculatedName || `Расчётная цена ${values.slot}`,
          sellerPrice,
          drrPct: values.drrPct,
          defectRatePct: values.defectRatePct,
          deliveryCostRub: values.deliveryCostRub,
          snapshot: snapshot as unknown as Record<string, unknown>,
        })

        if (result.ok) {
          toast.success(`Расчётная цена «${values.calculatedName}» сохранена`)
          onOpenChange(false)
        } else {
          toast.error(result.error || "Не удалось сохранить расчёт")
        }
      } catch (e) {
        toast.error((e as Error).message || "Ошибка сохранения")
      }
    })
  }

  const fmtMoney = (n: number) =>
    Number.isFinite(n)
      ? n.toLocaleString("ru-RU", { minimumFractionDigits: 2, maximumFractionDigits: 2 })
      : "—"
  const fmtPct = (n: number) =>
    Number.isFinite(n) ? `${Math.round(n * 10) / 10}%` : "—"

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-4xl max-h-[92vh] overflow-y-auto">
        <DialogHeader>
          <DialogTitle className="text-base font-medium">
            Расчёт юнит-экономики: {card.name ?? "Карточка"}
          </DialogTitle>
          <DialogDescription>
            Артикул: {card.nmId} · Исходная цена:{" "}
            {fmtMoney(row.inputs.priceBeforeDiscount)} ₽
          </DialogDescription>
        </DialogHeader>

        <form onSubmit={form.handleSubmit(onSubmit)}>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {/* Левая колонка — Inputs */}
            <div className="space-y-4">
              <h3 className="text-sm font-medium text-muted-foreground uppercase tracking-wide">
                Входные параметры
              </h3>

              <div className="flex flex-col gap-1">
                <Label htmlFor="priceBeforeDiscount">
                  Цена продавца до скидки, ₽
                </Label>
                <Input
                  id="priceBeforeDiscount"
                  type="number"
                  step="0.01"
                  className="h-9"
                  {...form.register("priceBeforeDiscount")}
                />
              </div>

              <div className="flex flex-col gap-1">
                <Label htmlFor="sellerDiscountPct">Скидка продавца, %</Label>
                <Input
                  id="sellerDiscountPct"
                  type="number"
                  step="0.1"
                  min="0"
                  max="100"
                  className="h-9"
                  {...form.register("sellerDiscountPct")}
                />
              </div>

              <div className="flex flex-col gap-1">
                <Label htmlFor="drrPct">ДРР, %</Label>
                <Input
                  id="drrPct"
                  type="number"
                  step="0.1"
                  min="0"
                  max="100"
                  className="h-9"
                  {...form.register("drrPct")}
                />
                <label className="flex items-center gap-2 text-xs text-muted-foreground mt-1">
                  <Checkbox
                    checked={form.watch("drrScopeProduct")}
                    onCheckedChange={(c) =>
                      form.setValue("drrScopeProduct", c === true)
                    }
                  />
                  только этот товар
                </label>
              </div>

              <div className="flex flex-col gap-1">
                <Label htmlFor="defectRatePct">Процент брака, %</Label>
                <Input
                  id="defectRatePct"
                  type="number"
                  step="0.1"
                  min="0"
                  max="100"
                  className="h-9"
                  {...form.register("defectRatePct")}
                />
                <label className="flex items-center gap-2 text-xs text-muted-foreground mt-1">
                  <Checkbox
                    checked={form.watch("defectScopeProduct")}
                    onCheckedChange={(c) =>
                      form.setValue("defectScopeProduct", c === true)
                    }
                  />
                  только этот товар
                </label>
              </div>

              <div className="flex flex-col gap-1">
                <Label htmlFor="deliveryCostRub">Доставка на маркетплейс, ₽</Label>
                <Input
                  id="deliveryCostRub"
                  type="number"
                  step="0.01"
                  min="0"
                  className="h-9"
                  {...form.register("deliveryCostRub")}
                />
              </div>

              <div className="pt-4 border-t space-y-2 text-xs text-muted-foreground">
                <div>Себестоимость: {fmtMoney(row.inputs.costPrice)} ₽</div>
                <div>Скидка WB (СПП): {row.inputs.wbDiscountPct}%</div>
                <div>Комиссия ИУ FBW: {row.inputs.commFbwPct}%</div>
                <div>Глобальные ставки применяются автоматически</div>
              </div>
            </div>

            {/* Правая колонка — Outputs */}
            <div className="space-y-3 md:border-l md:pl-6">
              <h3 className="text-sm font-medium text-muted-foreground uppercase tracking-wide">
                Результат расчёта
              </h3>

              <dl className="space-y-1 text-xs tabular-nums">
                <OutputRow label="Цена продавца" value={fmtMoney(liveOutputs.sellerPrice)} />
                <OutputRow label="Цена со скидкой WB" value={fmtMoney(liveOutputs.priceAfterWbDiscount)} />
                <OutputRow label="Цена с кошельком" value={fmtMoney(liveOutputs.priceAfterWallet)} />
                <OutputRow label="Кошелёк" value={fmtMoney(liveOutputs.walletAmount)} />
                <OutputRow label="Эквайринг" value={fmtMoney(liveOutputs.acquiringAmount)} />
                <OutputRow label="Комиссия" value={fmtMoney(liveOutputs.commissionAmount)} />
                <OutputRow label="ДРР" value={fmtMoney(liveOutputs.drrAmount)} />
                <OutputRow label="Джем" value={fmtMoney(liveOutputs.jemAmount)} />
                <OutputRow label="К перечислению" value={fmtMoney(liveOutputs.transferAmount)} />
                <OutputRow label="Брак" value={fmtMoney(liveOutputs.defectAmount)} />
                <OutputRow label="Кредит" value={fmtMoney(liveOutputs.creditAmount)} />
                <OutputRow label="Общие" value={fmtMoney(liveOutputs.overheadAmount)} />
                <OutputRow label="Налог" value={fmtMoney(liveOutputs.taxAmount)} />
              </dl>

              {/* Highlighted card — profit/re/roi */}
              <div className="mt-4 bg-muted/50 p-3 rounded-md space-y-2">
                <OutputRow
                  label="Прибыль"
                  value={`${fmtMoney(liveOutputs.profit)} ₽`}
                  className={cn(
                    "text-base font-medium",
                    liveOutputs.profit >= 0
                      ? "text-green-600 dark:text-green-500"
                      : "text-red-600 dark:text-red-500",
                  )}
                />
                <OutputRow
                  label="Re продаж"
                  value={fmtPct(liveOutputs.returnOnSalesPct)}
                  className={cn(
                    "text-base font-medium",
                    liveOutputs.returnOnSalesPct >= 0
                      ? "text-green-600 dark:text-green-500"
                      : "text-red-600 dark:text-red-500",
                  )}
                />
                <OutputRow
                  label="ROI"
                  value={fmtPct(liveOutputs.roiPct)}
                  className={cn(
                    "text-base font-medium",
                    liveOutputs.roiPct >= 0
                      ? "text-green-600 dark:text-green-500"
                      : "text-red-600 dark:text-red-500",
                  )}
                />
              </div>
            </div>
          </div>

          <DialogFooter className="mt-6">
            <div className="flex items-center gap-3 w-full flex-wrap">
              <label className="text-sm">Сохранить в слот:</label>
              <select
                className="h-9 rounded border border-input bg-transparent px-2 text-sm"
                value={form.watch("slot")}
                onChange={(e) => form.setValue("slot", Number(e.target.value))}
              >
                <option value={1}>Слот 1</option>
                <option value={2}>Слот 2</option>
                <option value={3}>Слот 3</option>
              </select>
              <Input
                placeholder="Название (опционально)"
                className="h-9 flex-1 min-w-[200px]"
                {...form.register("calculatedName")}
              />
              <Button
                type="button"
                variant="outline"
                onClick={() => onOpenChange(false)}
                disabled={isPending}
              >
                Отмена
              </Button>
              <Button type="submit" disabled={isPending}>
                {isPending ? "Сохранение…" : "Сохранить как расчётную цену"}
              </Button>
            </div>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}

function OutputRow({
  label,
  value,
  className,
}: {
  label: string
  value: string
  className?: string
}) {
  return (
    <div className={cn("flex justify-between gap-4", className)}>
      <dt className="text-muted-foreground">{label}</dt>
      <dd className="text-right tabular-nums">{value}</dd>
    </div>
  )
}
```

**4. Создать `components/prices/PriceCalculatorTableWrapper.tsx`:**

```tsx
// components/prices/PriceCalculatorTableWrapper.tsx
// Phase 7: клиентская обёртка над PriceCalculatorTable — управляет state модалки.

"use client"

import { useState } from "react"
import {
  PriceCalculatorTable,
  type ProductGroup,
  type PriceRow,
  type WbCardRowGroup,
} from "@/components/prices/PriceCalculatorTable"
import { PricingCalculatorDialog } from "@/components/prices/PricingCalculatorDialog"

interface PriceCalculatorTableWrapperProps {
  groups: ProductGroup[]
}

interface DialogState {
  card: WbCardRowGroup["card"] & { name?: string }
  row: PriceRow
}

export function PriceCalculatorTableWrapper({
  groups,
}: PriceCalculatorTableWrapperProps) {
  const [dialog, setDialog] = useState<DialogState | null>(null)

  const handleRowClick = (
    card: WbCardRowGroup["card"],
    row: PriceRow,
    productId: string,
  ) => {
    // Найти Product name для DialogTitle
    const group = groups.find((g) => g.product.id === productId)
    const productName = group?.product.name ?? "Карточка"
    setDialog({ card: { ...card, name: productName }, row })
  }

  return (
    <>
      <PriceCalculatorTable groups={groups} onRowClick={handleRowClick} />
      {dialog && (
        <PricingCalculatorDialog
          open={true}
          onOpenChange={(open) => {
            if (!open) setDialog(null)
          }}
          card={dialog.card}
          row={dialog.row}
        />
      )}
    </>
  )
}
```

**5. Обновить `app/(dashboard)/prices/wb/page.tsx`** — заменить импорт и использование:

```tsx
// В импортах:
import { PriceCalculatorTableWrapper } from "@/components/prices/PriceCalculatorTableWrapper"

// В JSX вместо:
<PriceCalculatorTable groups={groups} />
// Стало:
<PriceCalculatorTableWrapper groups={groups} />
```

Также убрать прямой импорт `PriceCalculatorTable` из page.tsx (он импортируется только через Wrapper теперь). Но оставить импорт типов (ProductGroup, PriceRow и т.д.) если они там использовались для local builders.

**6. Проверить компиляцию:**
```bash
npm run build
```

**Правила:**
- `"use client"` на обоих компонентах.
- `useWatch` используется для realtime (НЕ `watch()` без аргументов — тот триггерит rerender на каждое нажатие).
- `useMemo` для `calculatePricing` — мемоизируем на основе `watched` и `row.inputs`.
- Native `<select>` для слота (CLAUDE.md convention).
- Toast success/error после каждого server action.
- `useTransition` для submit — показывает spinner и disable кнопок пока server action не завершится.
- Checkbox из `@/components/ui/checkbox` — уже существует в проекте (shadcn/base-ui).
  </action>
  <verify>
    <automated>test -f components/prices/PricingCalculatorDialog.tsx && test -f components/prices/PriceCalculatorTableWrapper.tsx && npm run build 2>&1 | tail -20</automated>
    <manual>
1. `grep "useWatch" components/prices/PricingCalculatorDialog.tsx` — realtime hook.
2. `grep "useMemo.*calculatePricing" components/prices/PricingCalculatorDialog.tsx` — memoized recompute.
3. `grep "native.*select\|<select" components/prices/PricingCalculatorDialog.tsx` — нативный select для слота.
4. `grep "saveCalculatedPrice" components/prices/PricingCalculatorDialog.tsx` — server action вызывается.
5. `grep "PricingCalculatorDialog" components/prices/PriceCalculatorTableWrapper.tsx` — модалка подключена.
6. `grep "PriceCalculatorTableWrapper" "app/(dashboard)/prices/wb/page.tsx"` — wrapper используется вместо прямого Table.
7. `npm run build` — без ошибок.
    </manual>
  </verify>
  <done>
- `PricingCalculatorDialog.tsx` создан с 2-колоночным layout, useWatch+useMemo, native select
- `PriceCalculatorTableWrapper.tsx` создан — управляет DialogState через useState
- `/prices/wb/page.tsx` обновлён — рендерит Wrapper
- `PriceRow` interface расширен полями `inputs` и `context`
- `/prices/wb/page.tsx` передаёт эти поля при сборке priceRows
- `npm run build` успешно
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification модалки и end-to-end flow</name>
  <what-built>
- Модалка `PricingCalculatorDialog` с 2 колонками, realtime пересчётом, кнопкой сохранения
- `PriceCalculatorTableWrapper` для управления state модалки
- Клик по строке таблицы → модалка открывается с правильными initial values
- Изменение inputs в модалке → правые outputs обновляются мгновенно
- Сохранение → toast success → модалка закрывается → страница revalidate-ит
  </what-built>
  <how-to-verify>
1. **Запустить dev server и открыть раздел:**
   - `npm run dev`
   - Открыть `http://localhost:3001/prices/wb` (после логина как MANAGER или SUPERADMIN)

2. **Проверить таблицу:**
   - Видна таблица с группировкой по Product (rowSpan Фото+Сводка работает)
   - Sticky колонки остаются при горизонтальном скролле вправо
   - Indicator strips: синяя полоса на regular акциях, фиолетовая на auto, янтарная на расчётных
   - Прибыль/Re продаж/ROI подсвечены зелёным (положительные) или красным (отрицательные)
   - Клик на любую ценовую строку → открывается модалка

3. **Проверить модалку:**
   - 2 колонки: Входные параметры слева, Результат расчёта справа
   - Изменить «Цена продавца до скидки» → правые значения обновляются мгновенно (< 100ms)
   - Изменить «ДРР» → Прибыль уменьшается/увеличивается в реальном времени
   - Прибыль/Re/ROI в выделенной карточке внизу правой колонки
   - Native `<select>` для слота работает (1/2/3)
   - Кнопка «Отмена» закрывает модалку

4. **Проверить сохранение:**
   - Ввести название «Тест» в поле названия
   - Выбрать слот 1
   - Нажать «Сохранить как расчётную цену»
   - Ожидать toast «Расчётная цена «Тест» сохранена»
   - Модалка закрывается, страница перерисовывается с новой расчётной строкой
   - Строка появляется в таблице с янтарной полосой и названием «Тест»

5. **Проверить scope чекбоксов:**
   - Открыть модалку
   - Изменить ДРР на 15%, снять чекбокс «только этот товар»
   - Сохранить
   - Ожидать toast.info «ДРР обновлён для всех товаров подкатегории»
   - Проверить в БД: `SELECT * FROM "Subcategory" WHERE "defaultDrrPct" = 15`

6. **Проверить дальтонизм-safety:**
   - В колонках Re продаж и ROI видны префиксы `+` или `−` (не только цвет)

7. **Проверить ставки:**
   - Изменить значение «Кошелёк WB» в GlobalRatesBar с 2.0 на 2.5
   - Ожидать toast «Ставки сохранены»
   - Обновить страницу (F5) → значение сохранилось, все расчёты пересчитаны с новой ставкой

**Если любой из пунктов fails — опишите проблему, executor исправит.**
  </how-to-verify>
  <resume-signal>
Введите "approved" если всё работает. Иначе опишите проблемы (например "клик по строке не открывает модалку, консоль: ...").
  </resume-signal>
</task>

</tasks>

<verification>
- `PricingCalculatorDialog` работает с realtime пересчётом
- `PriceCalculatorTableWrapper` управляет state модалки
- Сохранение в CalculatedPrice работает
- Человеческая верификация пройдена
- `npm run build` без ошибок
</verification>

<success_criteria>
- Клик по любой строке таблицы открывает модалку с правильными данными
- Realtime пересчёт latency < 100ms
- Сохранение расчётной цены → появляется новая строка в таблице
- Чекбоксы scope работают (Product override vs Subcategory/Category default)
- Ставки сохраняются между сессиями
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-09-SUMMARY.md`. Включить:
- PricingCalculatorDialog signature
- PriceCalculatorTableWrapper signature
- Измененные поля PriceRow interface
- Результаты human verification
- Известные caveats (если обнаружены)
</output>
