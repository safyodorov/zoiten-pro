---
phase: 07-prices-wb
plan: 10
type: execute
wave: 8
depends_on:
  - 07-04
  - 07-08
  - 07-09
files_modified:
  - components/prices/WbPromotionsSyncButton.tsx
  - components/prices/WbAutoPromoUploadButton.tsx
  - app/(dashboard)/prices/wb/page.tsx
autonomous: false
requirements:
  - PRICES-10
  - PRICES-11
  - PRICES-14
must_haves:
  truths:
    - "`WbPromotionsSyncButton` — кнопка с иконкой `Calendar` (lucide), label «Синхронизировать акции», `isPending` state через `useTransition`, вызывает `POST /api/wb-promotions-sync`"
    - "`WbAutoPromoUploadButton` — кнопка с иконкой `Upload`, открывает Dialog с native `<select>` auto-акций + file input + submit, вызывает `POST /api/wb-promotions-upload-excel` через multipart"
    - "Обе кнопки используют `sonner` toast для success/error + `router.refresh()` после успеха"
    - "Обе кнопки variant=\"outline\" size=\"default\", соответствуют паттерну `WbSyncButton`"
    - "`/prices/wb/page.tsx` обновлён: импортирует и рендерит новые кнопки + `<Alert>` для empty state акций (если promotions.length === 0)"
    - "RBAC: обе кнопки disabled если user не имеет PRICES/MANAGE (проверяется на уровне API route, UI просто получит 403 → toast.error)"
    - "Human verification: реальный sync + реальный Excel upload успешны"
  artifacts:
    - path: "components/prices/WbPromotionsSyncButton.tsx"
      provides: "кнопка синхронизации акций с progress indication"
      min_lines: 40
    - path: "components/prices/WbAutoPromoUploadButton.tsx"
      provides: "кнопка + Dialog для загрузки Excel auto-акции"
      min_lines: 100
    - path: "app/(dashboard)/prices/wb/page.tsx"
      provides: "обновлён с wiring новых кнопок + Alert"
      min_lines: 160
  key_links:
    - from: "components/prices/WbPromotionsSyncButton.tsx"
      to: "POST /api/wb-promotions-sync"
      via: "fetch"
      pattern: "wb-promotions-sync"
    - from: "components/prices/WbAutoPromoUploadButton.tsx"
      to: "POST /api/wb-promotions-upload-excel"
      via: "fetch multipart"
      pattern: "wb-promotions-upload-excel"
    - from: "app/(dashboard)/prices/wb/page.tsx"
      to: "components/prices/WbPromotionsSyncButton + WbAutoPromoUploadButton"
      via: "import + render"
      pattern: "WbPromotionsSyncButton|WbAutoPromoUploadButton"
---

<objective>
Создать две новые кнопки-триггера для раздела управления ценами WB:
1. `WbPromotionsSyncButton` — синхронизация акций через `/api/wb-promotions-sync`
2. `WbAutoPromoUploadButton` — загрузка Excel auto-акции через `/api/wb-promotions-upload-excel` (с nested Dialog для выбора акции и файла)

Обновить `/prices/wb/page.tsx`: добавить рендер новых кнопок в шапке и `<Alert>` empty state если акций не синхронизировано.

Purpose: полный UI для синхронизации и загрузки данных акций.

Output:
- 2 клиентских компонента кнопок
- Обновлённая RSC page с wiring
- Human verification реального sync + upload
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-UI-SPEC.md
@.planning/phases/07-prices-wb/07-04-SUMMARY.md
@.planning/phases/07-prices-wb/07-08-SUMMARY.md
@.planning/phases/07-prices-wb/07-09-SUMMARY.md
@CLAUDE.md
@components/cards/WbSyncButton.tsx
@components/cards/WbUploadIuButton.tsx
@components/ui/alert.tsx

<interfaces>
<!-- Паттерн WbSyncButton (существующий, для копирования): -->

```tsx
"use client"
import { useTransition } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { RefreshCw } from "lucide-react"
import { toast } from "sonner"

export function WbSyncButton() {
  const router = useRouter()
  const [isPending, startTransition] = useTransition()

  const handleClick = () => {
    startTransition(async () => {
      try {
        const res = await fetch("/api/wb-sync", { method: "POST" })
        const data = await res.json()
        if (!res.ok) {
          throw new Error(data.error || "Ошибка")
        }
        toast.success(`Синхронизировано ${data.count} карточек`)
        router.refresh()
      } catch (e) {
        toast.error((e as Error).message)
      }
    })
  }

  return (
    <Button variant="outline" onClick={handleClick} disabled={isPending}>
      <RefreshCw className={isPending ? "animate-spin mr-2" : "mr-2"} />
      {isPending ? "Синхронизация…" : "Синхронизировать с WB"}
    </Button>
  )
}
```

<!-- Паттерн WbUploadIuButton (существующий, для копирования Excel upload): -->

```tsx
"use client"
import { useRef, useTransition } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { Upload } from "lucide-react"
import { toast } from "sonner"

export function WbUploadIuButton() {
  const inputRef = useRef<HTMLInputElement>(null)
  const [isPending, startTransition] = useTransition()
  const router = useRouter()

  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0]
    if (!file) return
    startTransition(async () => {
      const formData = new FormData()
      formData.append("file", file)
      try {
        const res = await fetch("/api/wb-commission-iu", { method: "POST", body: formData })
        const data = await res.json()
        if (!res.ok) throw new Error(data.error)
        toast.success(`Загружено ${data.count}`)
        router.refresh()
      } catch (e) {
        toast.error((e as Error).message)
      }
      if (inputRef.current) inputRef.current.value = ""
    })
  }

  return (
    <>
      <input ref={inputRef} type="file" accept=".xlsx" className="hidden" onChange={handleFileChange} />
      <Button variant="outline" onClick={() => inputRef.current?.click()} disabled={isPending}>
        <Upload className="mr-2" />
        {isPending ? "Загрузка…" : "Загрузить ИУ"}
      </Button>
    </>
  )
}
```

<!-- UI-SPEC для WbAutoPromoUploadButton (сложнее — нужен Dialog для выбора акции): -->

Кнопка открывает Dialog:
- Title: «Загрузка отчёта auto-акции WB»
- Body:
  - Native `<select>` с auto-акциями (список из prop `autoPromotions: Array<{id, name}>`)
  - `<input type="file" accept=".xlsx">` для файла
  - Кнопки «Отмена» и «Загрузить» (submit)
- При submit → `new FormData` с `file` и `promotionId` → `fetch("/api/wb-promotions-upload-excel", {method, body})`
- После успеха → toast.success + router.refresh + close dialog

Props:
```typescript
interface WbAutoPromoUploadButtonProps {
  autoPromotions: Array<{ id: number; name: string }>
}
```

Если `autoPromotions.length === 0` — кнопка видна, но при клике Dialog показывает disabled submit + подсказку «Сначала синхронизируйте акции».

<!-- Alert empty state для promotions: -->

В RSC page.tsx, если `promotions.length === 0`, показать перед таблицей:

```tsx
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Info } from "lucide-react"

{promotions.length === 0 && (
  <Alert>
    <Info className="h-4 w-4" />
    <AlertTitle>Акции не синхронизированы</AlertTitle>
    <AlertDescription>
      Нажмите «Синхронизировать акции», чтобы загрузить текущие и будущие акции WB на 60 дней вперёд.
    </AlertDescription>
  </Alert>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать WbPromotionsSyncButton и WbAutoPromoUploadButton</name>
  <files>components/prices/WbPromotionsSyncButton.tsx, components/prices/WbAutoPromoUploadButton.tsx</files>
  <action>
**1. Создать `components/prices/WbPromotionsSyncButton.tsx`:**

```tsx
// components/prices/WbPromotionsSyncButton.tsx
// Phase 7: Кнопка синхронизации акций WB.

"use client"

import { useTransition } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { Calendar, RefreshCw } from "lucide-react"
import { toast } from "sonner"

export function WbPromotionsSyncButton() {
  const router = useRouter()
  const [isPending, startTransition] = useTransition()

  const handleClick = () => {
    startTransition(async () => {
      const loadingToast = toast.loading("Синхронизация акций…")

      try {
        const res = await fetch("/api/wb-promotions-sync", {
          method: "POST",
        })
        const data = await res.json()

        toast.dismiss(loadingToast)

        if (!res.ok) {
          throw new Error(
            data.error ||
              "Не удалось синхронизировать акции. Попробуйте ещё раз через минуту (WB API rate limit).",
          )
        }

        toast.success(
          `Синхронизировано ${data.synced ?? 0} акций, ${data.nomenclatures ?? 0} номенклатур`,
        )
        router.refresh()
      } catch (e) {
        toast.dismiss(loadingToast)
        toast.error((e as Error).message)
      }
    })
  }

  return (
    <Button
      variant="outline"
      onClick={handleClick}
      disabled={isPending}
      className="gap-2"
    >
      {isPending ? (
        <RefreshCw className="h-4 w-4 animate-spin" />
      ) : (
        <Calendar className="h-4 w-4" />
      )}
      {isPending ? "Синхронизация…" : "Синхронизировать акции"}
    </Button>
  )
}
```

**2. Создать `components/prices/WbAutoPromoUploadButton.tsx`:**

```tsx
// components/prices/WbAutoPromoUploadButton.tsx
// Phase 7: Кнопка + Dialog для загрузки Excel отчёта auto-акции.
// Native <select> для выбора акции из списка (CLAUDE.md convention).

"use client"

import { useRef, useState, useTransition } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { Label } from "@/components/ui/label"
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog"
import { Upload } from "lucide-react"
import { toast } from "sonner"

interface AutoPromotion {
  id: number
  name: string
}

interface WbAutoPromoUploadButtonProps {
  autoPromotions: AutoPromotion[]
}

export function WbAutoPromoUploadButton({
  autoPromotions,
}: WbAutoPromoUploadButtonProps) {
  const router = useRouter()
  const [open, setOpen] = useState(false)
  const [selectedId, setSelectedId] = useState<string>(
    autoPromotions.length > 0 ? String(autoPromotions[0].id) : "",
  )
  const [file, setFile] = useState<File | null>(null)
  const fileInputRef = useRef<HTMLInputElement>(null)
  const [isPending, startTransition] = useTransition()

  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setFile(e.target.files?.[0] ?? null)
  }

  const handleSubmit = () => {
    if (!file) {
      toast.error("Выберите файл")
      return
    }
    if (!selectedId) {
      toast.error("Выберите акцию")
      return
    }

    startTransition(async () => {
      const formData = new FormData()
      formData.append("file", file)
      formData.append("promotionId", selectedId)

      try {
        const res = await fetch("/api/wb-promotions-upload-excel", {
          method: "POST",
          body: formData,
        })
        const data = await res.json()

        if (!res.ok) {
          throw new Error(data.error || "Ошибка загрузки")
        }

        toast.success(
          `Загружено ${data.imported} строк в акцию «${data.promotionName}»`,
        )
        setOpen(false)
        setFile(null)
        if (fileInputRef.current) fileInputRef.current.value = ""
        router.refresh()
      } catch (e) {
        toast.error(
          (e as Error).message ||
            "Не удалось распознать Excel. Проверьте формат файла.",
        )
      }
    })
  }

  const disabled = autoPromotions.length === 0

  return (
    <>
      <Button
        variant="outline"
        onClick={() => setOpen(true)}
        className="gap-2"
      >
        <Upload className="h-4 w-4" />
        Загрузить отчёт auto-акции
      </Button>

      <Dialog open={open} onOpenChange={setOpen}>
        <DialogContent className="sm:max-w-md">
          <DialogHeader>
            <DialogTitle>Загрузка отчёта auto-акции WB</DialogTitle>
            <DialogDescription>
              {disabled
                ? "Сначала синхронизируйте акции через кнопку «Синхронизировать акции»."
                : "Выберите auto-акцию и загрузите Excel из кабинета WB."}
            </DialogDescription>
          </DialogHeader>

          <div className="space-y-4 py-2">
            <div className="flex flex-col gap-1">
              <Label htmlFor="promotion-select">Auto-акция</Label>
              <select
                id="promotion-select"
                className="h-9 rounded border border-input bg-transparent px-2 text-sm"
                value={selectedId}
                onChange={(e) => setSelectedId(e.target.value)}
                disabled={disabled}
              >
                {autoPromotions.length === 0 && (
                  <option value="">— нет доступных акций —</option>
                )}
                {autoPromotions.map((p) => (
                  <option key={p.id} value={p.id}>
                    {p.name}
                  </option>
                ))}
              </select>
            </div>

            <div className="flex flex-col gap-1">
              <Label htmlFor="file-input">Файл Excel из кабинета WB</Label>
              <input
                id="file-input"
                ref={fileInputRef}
                type="file"
                accept=".xlsx,.xls"
                onChange={handleFileChange}
                disabled={disabled}
                className="text-sm"
              />
            </div>
          </div>

          <DialogFooter>
            <Button
              variant="outline"
              onClick={() => setOpen(false)}
              disabled={isPending}
            >
              Отмена
            </Button>
            <Button
              onClick={handleSubmit}
              disabled={disabled || isPending || !file}
            >
              {isPending ? "Загрузка…" : "Загрузить"}
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </>
  )
}
```

**3. Проверить TypeScript:**
```bash
npx tsc --noEmit
```

**Правила:**
- Обе кнопки — client components.
- `useTransition` для async, `toast.loading` + `toast.dismiss` для long-running sync.
- Native `<select>` в dialog (CLAUDE.md).
- Ошибки на русском с контекстом (rate limit, формат файла).
- `router.refresh()` после успеха — RSC страница перерисуется с новыми данными.
  </action>
  <verify>
    <automated>test -f components/prices/WbPromotionsSyncButton.tsx && test -f components/prices/WbAutoPromoUploadButton.tsx && npx tsc --noEmit 2>&1 | grep -E "WbPromotionsSyncButton|WbAutoPromoUploadButton" | head -5</automated>
    <manual>
1. `grep "wb-promotions-sync" components/prices/WbPromotionsSyncButton.tsx` — endpoint.
2. `grep "wb-promotions-upload-excel" components/prices/WbAutoPromoUploadButton.tsx` — endpoint.
3. `grep "<select" components/prices/WbAutoPromoUploadButton.tsx` — native select используется.
4. `grep "useTransition" components/prices/WbPromotionsSyncButton.tsx components/prices/WbAutoPromoUploadButton.tsx` — обе используют.
5. `npx tsc --noEmit` без ошибок.
    </manual>
  </verify>
  <done>
- Оба компонента кнопок созданы
- `WbPromotionsSyncButton` вызывает `/api/wb-promotions-sync` с toast.loading/dismiss
- `WbAutoPromoUploadButton` открывает Dialog с native select + file input
- Native `<select>` используется (не shadcn Select)
- TypeScript компилируется
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить app/(dashboard)/prices/wb/page.tsx с wiring новых кнопок и Alert</name>
  <files>app/(dashboard)/prices/wb/page.tsx</files>
  <action>
Открыть `app/(dashboard)/prices/wb/page.tsx` (созданный в плане 07-08, обновлённый в 07-09 для Wrapper).

**1. Добавить импорты:**
```tsx
import { WbPromotionsSyncButton } from "@/components/prices/WbPromotionsSyncButton"
import { WbAutoPromoUploadButton } from "@/components/prices/WbAutoPromoUploadButton"
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Info } from "lucide-react"
```

**2. Удалить TODO комментарии** из JSX (которые план 07-08 оставил).

**3. Обновить рендер шапки** (заменить существующий TODO-блок):

```tsx
{/* Кнопки шапки */}
<div className="flex flex-wrap gap-2">
  <WbSyncButton />
  <WbSyncSppButton />
  <WbPromotionsSyncButton />
  <WbAutoPromoUploadButton
    autoPromotions={promotions
      .filter((p) => p.type === "auto")
      .map((p) => ({ id: p.id, name: p.name }))}
  />
</div>

{/* Empty state для promotions */}
{promotions.length === 0 && (
  <Alert>
    <Info className="h-4 w-4" />
    <AlertTitle>Акции не синхронизированы</AlertTitle>
    <AlertDescription>
      Нажмите «Синхронизировать акции», чтобы загрузить текущие и будущие акции
      WB на 60 дней вперёд.
    </AlertDescription>
  </Alert>
)}
```

**4. Проверить что все импорты присутствуют и build успешен:**
```bash
npm run build
```

**Примечания:**
- `WbAutoPromoUploadButton` получает только `id` и `name` (не весь объект WbPromotion) — это минимальный props, достаточно для выбора в select.
- `<Alert>` — стандартный shadcn компонент, уже существует в проекте (из Phase 1/2).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <manual>
1. `grep "WbPromotionsSyncButton\|WbAutoPromoUploadButton" "app/(dashboard)/prices/wb/page.tsx"` — импорты и рендер.
2. `grep "Alert.*promotions.length === 0\|promotions.length === 0" "app/(dashboard)/prices/wb/page.tsx"` — empty state logic.
3. `grep "autoPromotions={" "app/(dashboard)/prices/wb/page.tsx"` — prop передача.
4. `grep -c "TODO" "app/(dashboard)/prices/wb/page.tsx"` — 0 (все TODO удалены).
5. `npm run build` успешен.
    </manual>
  </verify>
  <done>
- `/prices/wb/page.tsx` импортирует 2 новые кнопки + Alert + Info icon
- Кнопки рендерятся в шапке после WbSyncButton/WbSyncSppButton
- Empty state Alert показывается когда promotions.length === 0
- TODO удалены из файла
- `npm run build` успешен
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification реального sync + Excel upload</name>
  <what-built>
- Кнопка «Синхронизировать акции» — вызывает `/api/wb-promotions-sync`
- Кнопка «Загрузить отчёт auto-акции» — Dialog с выбором акции и файла, вызывает `/api/wb-promotions-upload-excel`
- Alert «Акции не синхронизированы» при пустом списке promotions
- Вся шапка раздела готова: 4 кнопки + GlobalRatesBar
  </what-built>
  <how-to-verify>
1. **Проверка UI шапки:**
   - Открыть `http://localhost:3001/prices/wb` (dev сервер)
   - При первом заходе: Alert «Акции не синхронизированы» виден над таблицей
   - Видны 4 кнопки в шапке: «Синхронизировать с WB», «Скидка WB», «Синхронизировать акции», «Загрузить отчёт auto-акции»

2. **Реальная синхронизация акций:**
   - Нажать «Синхронизировать акции»
   - Ожидать toast «Синхронизация акций…» с spinner
   - Подождать 30-90 секунд (WB API + rate limit)
   - Ожидать toast success: «Синхронизировано N акций, M номенклатур» (N должно быть 50-100)
   - Страница перерисовывается, Alert исчезает
   - Если в таблице есть товары с nmId, которые попали в regular-акции — они теперь показывают синюю акционную строку с названием акции

3. **Проверка rate limit:**
   - Открыть dev tools network tab перед sync
   - Наблюдать запросы `https://dp-calendar-api.wildberries.ru/...` — должны быть с паузами 600ms
   - Если видно 429 — retry должен сработать автоматически

4. **Excel upload для auto-акции:**
   - Нажать «Загрузить отчёт auto-акции»
   - Dialog: выбрать auto-акцию из dropdown (будут видны после sync выше, у которых type=auto)
   - Выбрать файл `tests/fixtures/auto-promo-sample.xlsx` или реальный из Downloads
   - Нажать «Загрузить»
   - Ожидать toast success: «Загружено N строк в акцию «{name}»»
   - Dialog закрывается, страница refreshes
   - Если какие-то nmId из Excel совпали с товарами в таблице — они показывают **фиолетовую** auto-строку

5. **Tooltip на названии акции:**
   - Навести курсор на название акции в таблице
   - Ожидать tooltip с description и bulleted advantages

6. **Проверка БД (опционально):**
   - `psql -U zoiten -d zoiten_erp`
   - `SELECT COUNT(*) FROM "WbPromotion";` — должно быть > 0
   - `SELECT COUNT(*) FROM "WbPromotionNomenclature";` — > 0 (только для regular + Excel uploads)

7. **Negative test — rate limit:**
   - Быстро нажать «Синхронизировать акции» 2 раза подряд — второй должен показать «Синхронизация…» и ждать (disabled), не стартовать параллельный вызов

**Если любой шаг fails — опишите проблему.**
  </how-to-verify>
  <resume-signal>
Введите "approved" если sync и upload работают. Иначе опишите проблему.
  </resume-signal>
</task>

</tasks>

<verification>
- Обе кнопки созданы и интегрированы в `/prices/wb/page.tsx`
- Alert empty state показывается корректно
- `npm run build` успешен
- Human verification: реальный sync и upload успешны
</verification>

<success_criteria>
- После синхронизации акций таблица показывает акционные строки с tooltip
- После Excel upload auto-акций таблица показывает фиолетовые строки
- Все 4 кнопки работают и корректно отображают pending state
- План 07-11 может перейти к финальной документации и деплою
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-10-SUMMARY.md`. Включить:
- 2 созданных компонента
- Статус реального sync (сколько акций загружено)
- Статус Excel upload (сколько строк импортировано)
- Известные edge cases (если обнаружены)
</output>
