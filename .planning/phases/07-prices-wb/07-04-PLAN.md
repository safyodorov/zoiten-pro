---
phase: 07-prices-wb
plan: 04
type: execute
wave: 3
depends_on:
  - 07-03
files_modified:
  - app/api/wb-promotions-sync/route.ts
  - app/api/wb-promotions-upload-excel/route.ts
autonomous: true
requirements:
  - PRICES-10
  - PRICES-11
must_haves:
  truths:
    - "`POST /api/wb-promotions-sync` синхронизирует акции WB в окне [today, today+60d] с правильным rate limit handling"
    - "Защищён через `requireSection(\"PRICES\", \"MANAGE\")`"
    - "Upsert-ит акции в `WbPromotion`, детали через batched `fetchPromotionDetails`, номенклатуры через `fetchPromotionNomenclatures` только для regular type"
    - "Cleanup акций с `endDateTime < today - 7 days` выполняется в конце sync (Cascade удалит WbPromotionNomenclature)"
    - "`POST /api/wb-promotions-upload-excel` принимает multipart (file + promotionId), парсит Excel по индексам колонок A=0/F=5/L=11/M=12/T=19/U=20"
    - "Валидация: проверяет что promotionId существует и `type = \"auto\"`, иначе 400"
    - "Upsert WbPromotionNomenclature через удаление старых + createMany (транзакционно)"
    - "Оба endpoint'а возвращают JSON: {synced, nomenclatures, deleted} для sync и {imported} для upload"
    - "`tests/excel-auto-promo.test.ts` GREEN (реальный fixture парсится корректно)"
  artifacts:
    - path: "app/api/wb-promotions-sync/route.ts"
      provides: "POST endpoint синхронизации акций 60 дней"
      min_lines: 80
    - path: "app/api/wb-promotions-upload-excel/route.ts"
      provides: "POST multipart endpoint загрузки Excel auto-акции"
      min_lines: 80
  key_links:
    - from: "app/api/wb-promotions-sync/route.ts"
      to: "lib/wb-api.ts fetchAllPromotions/fetchPromotionDetails/fetchPromotionNomenclatures"
      via: "import + sequential calls with rate limit"
      pattern: "fetchAllPromotions"
    - from: "app/api/wb-promotions-sync/route.ts"
      to: "prisma.wbPromotion"
      via: "upsert + cleanup via deleteMany"
      pattern: "prisma.wbPromotion.upsert"
    - from: "app/api/wb-promotions-upload-excel/route.ts"
      to: "xlsx package"
      via: "XLSX.read + sheet_to_json with header:1"
      pattern: "XLSX.read"
    - from: "app/api/wb-promotions-upload-excel/route.ts"
      to: "prisma.wbPromotionNomenclature"
      via: "deleteMany + createMany in transaction"
      pattern: "prisma.\\$transaction"
---

<objective>
Создать две HTTP API routes:
1. `POST /api/wb-promotions-sync` — синхронизация акций WB из Promotions Calendar API (окно 60 дней)
2. `POST /api/wb-promotions-upload-excel` — загрузка Excel отчёта из кабинета WB для auto-акций

Оба используют паттерны из существующих routes проекта (`/api/wb-sync`, `/api/wb-commission-iu`) и функции из `lib/wb-api.ts` (plan 07-03).

Purpose: серверный слой для кнопок «Синхронизировать акции» и «Загрузить отчёт auto-акции» (UI создаётся в плане 07-10).

Output:
- Два новых route файла
- `tests/excel-auto-promo.test.ts` GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-03-SUMMARY.md
@CLAUDE.md
@lib/wb-api.ts
@app/api/wb-commission-iu/route.ts
@tests/excel-auto-promo.test.ts

<interfaces>
<!-- Функции из lib/wb-api.ts (добавлены в плане 07-03): -->

```typescript
export async function fetchAllPromotions(startDate: Date, endDate: Date): Promise<WbPromotionRaw[]>
export async function fetchPromotionDetails(ids: number[]): Promise<WbPromotionDetailsRaw[]>
export async function fetchPromotionNomenclatures(promotionId: number): Promise<WbPromotionNomenclatureRaw[]>

interface WbPromotionRaw {
  id: number
  name: string
  description?: string
  advantages?: string[]
  startDateTime: string
  endDateTime: string
  type: string  // "auto" | "regular"
}

interface WbPromotionDetailsRaw {
  id: number
  description?: string
  advantages?: string[]
  ranging?: unknown[]
}

interface WbPromotionNomenclatureRaw {
  nmID: number
  planPrice?: number
  planDiscount?: number
  inAction?: boolean
}
```

<!-- Prisma модели (созданы в плане 07-01): -->

```typescript
prisma.wbPromotion.upsert({
  where: { id: number },
  create: { id, name, description, advantages, startDateTime, endDateTime, type, source: "API", lastSyncedAt, rangingJson? },
  update: { ... }
})
prisma.wbPromotionNomenclature.deleteMany({ where: { promotionId } })
prisma.wbPromotionNomenclature.createMany({ data: [...], skipDuplicates: true })
prisma.wbPromotion.deleteMany({ where: { endDateTime: { lt: cutoffDate } } })  // cleanup
```

<!-- RBAC helper: -->

```typescript
import { requireSection } from "@/lib/rbac"
await requireSection("PRICES", "MANAGE")  // throws if no permission
```

<!-- Паттерн существующего Excel upload /api/wb-commission-iu/route.ts: -->

```typescript
import * as XLSX from "xlsx"
import { NextRequest, NextResponse } from "next/server"

export const runtime = "nodejs"

export async function POST(req: NextRequest) {
  await requireSection("...", "MANAGE")
  const formData = await req.formData()
  const file = formData.get("file") as File | null
  if (!file) return NextResponse.json({ error: "..." }, { status: 400 })

  const buf = Buffer.from(await file.arrayBuffer())
  const wb = XLSX.read(buf, { type: "buffer" })
  const sheet = wb.Sheets[wb.SheetNames[0]]
  const rows = XLSX.utils.sheet_to_json<(string | number)[]>(sheet, { header: 1 })

  // parse rows by index (NOT by column name — кириллица, BOM, whitespace issues)
  // ...
}
```

<!-- D-06 Excel column indices (0-indexed): -->

- A=0: "Товар уже участвует в акции" ("Да"/"Нет") → `inAction: boolean`
- F=5: "Артикул WB" → `nmId: number`
- L=11: "Плановая цена для акции" → `planPrice: number | null`
- M=12: "Текущая розничная цена" → `currentPrice: number | null`
- T=19: "Загружаемая скидка для участия в акции" → `planDiscount: number | null`
- U=20: "Статус" → `status: string | null`

<!-- Тест tests/excel-auto-promo.test.ts (уже существует из Wave 0): -->

Проверяет:
1. Fixture `tests/fixtures/auto-promo-sample.xlsx` существует и парсится
2. Строки читаются по правильным индексам — nmId из row[5] валиден
3. Поле inAction корректно распознаёт "Да"/"Нет"

Для теста excel parser может быть вынесен в отдельную функцию `parseAutoPromoExcel(buf): Array<ParsedRow>` и импортироваться в route. Это позволит unit-тестировать парсер без HTTP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать app/api/wb-promotions-sync/route.ts</name>
  <files>app/api/wb-promotions-sync/route.ts</files>
  <action>
**1. Создать директорию и файл:**
```bash
mkdir -p app/api/wb-promotions-sync
touch app/api/wb-promotions-sync/route.ts
```

**2. Написать содержимое:**

```typescript
// app/api/wb-promotions-sync/route.ts
// Phase 7 (D-05): Синхронизация акций WB из Promotions Calendar API.
// Окно: [today, today + 60 days]. Rate limit compliant (через lib/wb-api.ts).
// Cleanup: удаляет акции с endDateTime < today - 7 дней (Cascade удалит nomenclatures).

import { NextResponse } from "next/server"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"
import {
  fetchAllPromotions,
  fetchPromotionDetails,
  fetchPromotionNomenclatures,
} from "@/lib/wb-api"

export const runtime = "nodejs"
export const maxDuration = 300 // 5 минут — sync может быть медленным из-за rate limit

export async function POST() {
  // RBAC: write action → требуется MANAGE
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return NextResponse.json(
      { error: "Недостаточно прав для синхронизации акций" },
      { status: 403 },
    )
  }

  try {
    const now = new Date()
    const endWindow = new Date(Date.now() + 60 * 24 * 60 * 60 * 1000)

    // 1. Получить список всех акций в окне 60 дней
    const raw = await fetchAllPromotions(now, endWindow)

    // 2. Upsert акций в WbPromotion
    for (const p of raw) {
      await prisma.wbPromotion.upsert({
        where: { id: p.id },
        create: {
          id: p.id,
          name: p.name,
          description: p.description ?? null,
          advantages: p.advantages ?? [],
          startDateTime: new Date(p.startDateTime),
          endDateTime: new Date(p.endDateTime),
          type: p.type,
          source: "API",
          lastSyncedAt: new Date(),
        },
        update: {
          name: p.name,
          description: p.description ?? null,
          advantages: p.advantages ?? [],
          startDateTime: new Date(p.startDateTime),
          endDateTime: new Date(p.endDateTime),
          type: p.type,
          lastSyncedAt: new Date(),
        },
      })
    }

    // 3. Получить детали акций батчами по 10 ID
    const ids = raw.map((p) => p.id)
    const details = await fetchPromotionDetails(ids)
    for (const d of details) {
      await prisma.wbPromotion.update({
        where: { id: d.id },
        data: {
          description: d.description ?? undefined,
          advantages: d.advantages ?? undefined,
          rangingJson: (d.ranging ?? null) as unknown as
            | Record<string, unknown>
            | null,
        },
      })
    }

    // 4. Номенклатуры — ТОЛЬКО для regular-акций
    // Auto-акции заполняются через Excel upload (/api/wb-promotions-upload-excel, D-06)
    let nomTotal = 0
    for (const p of raw.filter((pp) => pp.type !== "auto")) {
      const noms = await fetchPromotionNomenclatures(p.id)
      // Удалить старые номенклатуры этой акции и вставить новые
      await prisma.wbPromotionNomenclature.deleteMany({
        where: { promotionId: p.id },
      })
      if (noms.length > 0) {
        await prisma.wbPromotionNomenclature.createMany({
          data: noms.map((n) => ({
            promotionId: p.id,
            nmId: n.nmID,
            inAction: n.inAction ?? false,
            planPrice: n.planPrice ?? null,
            planDiscount: n.planDiscount ?? null,
          })),
          skipDuplicates: true,
        })
      }
      nomTotal += noms.length
    }

    // 5. Cleanup — удалить акции закончившиеся > 7 дней назад
    const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000)
    const deleted = await prisma.wbPromotion.deleteMany({
      where: { endDateTime: { lt: cutoff } },
    })

    return NextResponse.json({
      synced: raw.length,
      nomenclatures: nomTotal,
      deleted: deleted.count,
    })
  } catch (e) {
    console.error("WB promotions sync error:", e)
    return NextResponse.json(
      {
        error: (e as Error).message || "Ошибка синхронизации акций",
      },
      { status: 500 },
    )
  }
}
```

**3. Проверить TypeScript компилируется:**
```bash
npx tsc --noEmit
```

**Правила:**
- `runtime = "nodejs"` обязательно (не Edge — prisma не работает на Edge).
- `maxDuration = 300` — 5 минут, поскольку sync 83+ акций с rate limit 600ms между запросами = минимум 50+ секунд, plus nomenclatures 50+ × 600ms = еще 30+ секунд.
- RBAC check в начале — write action.
- Try/catch глобальный — чтобы любая ошибка (в fetch, prisma, validation) возвращала 500 с русским сообщением.
- Error messages на русском — они показываются пользователю через toast.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "wb-promotions-sync"</automated>
    <manual>
1. `cat app/api/wb-promotions-sync/route.ts | head -20` — файл существует.
2. `grep "requireSection" app/api/wb-promotions-sync/route.ts` — RBAC check есть.
3. `grep "fetchAllPromotions\|fetchPromotionDetails\|fetchPromotionNomenclatures" app/api/wb-promotions-sync/route.ts` — все 3 вызова есть.
4. `grep "export const maxDuration" app/api/wb-promotions-sync/route.ts` — 300 seconds.
5. `npx tsc --noEmit` — нет errors.
    </manual>
  </verify>
  <done>
- `app/api/wb-promotions-sync/route.ts` существует
- Защищён через `requireSection("PRICES", "MANAGE")`
- Вызывает все 3 WB API функции из lib/wb-api.ts в правильном порядке
- Upsert + cleanup + JSON response
- TypeScript компилируется
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать app/api/wb-promotions-upload-excel/route.ts + unit-тест парсера</name>
  <files>app/api/wb-promotions-upload-excel/route.ts</files>
  <action>
**1. Создать директорию и файл:**
```bash
mkdir -p app/api/wb-promotions-upload-excel
touch app/api/wb-promotions-upload-excel/route.ts
```

**2. Написать содержимое:**

```typescript
// app/api/wb-promotions-upload-excel/route.ts
// Phase 7 (D-06): Загрузка Excel отчёта из кабинета WB для auto-акций.
// Причина: WB API не даёт nomenclatures для auto-акций (422), поэтому данные берутся из Excel.
// Парсер читает колонки по индексам (НЕ по именам — кириллица, BOM, whitespace issues).
//
// Колонки (0-indexed):
// A=0: "Товар уже участвует в акции" (Да/Нет) → inAction
// F=5: "Артикул WB" → nmId
// L=11: "Плановая цена для акции" → planPrice
// M=12: "Текущая розничная цена" → currentPrice
// T=19: "Загружаемая скидка для участия в акции" → planDiscount
// U=20: "Статус" → status

import { NextRequest, NextResponse } from "next/server"
import * as XLSX from "xlsx"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"

export const runtime = "nodejs"
export const maxDuration = 60

/** Результат парсинга одной строки Excel. Exported для unit-тестов. */
export interface ParsedAutoPromoRow {
  nmId: number
  inAction: boolean
  planPrice: number | null
  currentPrice: number | null
  planDiscount: number | null
  status: string | null
}

/** Парсит Excel buffer в массив ParsedAutoPromoRow.
 *  Пропускает строки с невалидным nmId или пустые.
 *  Exported чтобы тестировать без HTTP request.
 */
export function parseAutoPromoExcel(buf: Buffer): ParsedAutoPromoRow[] {
  const wb = XLSX.read(buf, { type: "buffer" })
  const sheet = wb.Sheets[wb.SheetNames[0]]
  const rows = XLSX.utils.sheet_to_json<(string | number | undefined)[]>(
    sheet,
    { header: 1 },
  )

  const result: ParsedAutoPromoRow[] = []

  // Пропускаем первую строку (заголовки)
  for (let i = 1; i < rows.length; i++) {
    const r = rows[i]
    if (!r || r.length === 0) continue

    const nmIdRaw = r[5]
    if (nmIdRaw == null) continue
    const nmId = parseInt(String(nmIdRaw), 10)
    if (Number.isNaN(nmId) || nmId <= 0) continue

    const inActionRaw = String(r[0] ?? "").trim().toLowerCase()
    const planPriceRaw = r[11]
    const currentPriceRaw = r[12]
    const planDiscountRaw = r[19]
    const statusRaw = r[20]

    result.push({
      nmId,
      inAction: inActionRaw === "да" || inActionRaw === "yes",
      planPrice:
        planPriceRaw != null && planPriceRaw !== ""
          ? parseFloat(String(planPriceRaw)) || null
          : null,
      currentPrice:
        currentPriceRaw != null && currentPriceRaw !== ""
          ? parseFloat(String(currentPriceRaw)) || null
          : null,
      planDiscount:
        planDiscountRaw != null && planDiscountRaw !== ""
          ? parseFloat(String(planDiscountRaw)) || null
          : null,
      status: statusRaw != null ? String(statusRaw).trim() || null : null,
    })
  }

  return result
}

export async function POST(req: NextRequest) {
  // RBAC: write action → MANAGE
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return NextResponse.json(
      { error: "Недостаточно прав для загрузки отчёта" },
      { status: 403 },
    )
  }

  const formData = await req.formData()
  const file = formData.get("file") as File | null
  const promotionIdRaw = formData.get("promotionId") as string | null

  if (!file) {
    return NextResponse.json({ error: "Файл не указан" }, { status: 400 })
  }
  if (!promotionIdRaw) {
    return NextResponse.json(
      { error: "ID акции не указан" },
      { status: 400 },
    )
  }

  const promotionId = parseInt(promotionIdRaw, 10)
  if (Number.isNaN(promotionId) || promotionId <= 0) {
    return NextResponse.json(
      { error: "Неверный ID акции" },
      { status: 400 },
    )
  }

  // Проверка: акция существует и type = "auto"
  const promo = await prisma.wbPromotion.findUnique({
    where: { id: promotionId },
  })
  if (!promo) {
    return NextResponse.json(
      {
        error:
          "Акция не найдена. Сначала синхронизируйте акции через кнопку «Синхронизировать акции».",
      },
      { status: 404 },
    )
  }
  if (promo.type !== "auto") {
    return NextResponse.json(
      {
        error:
          "Excel-загрузка доступна только для auto-акций. Для regular-акций данные синхронизируются через API автоматически.",
      },
      { status: 400 },
    )
  }

  try {
    const buf = Buffer.from(await file.arrayBuffer())
    const parsed = parseAutoPromoExcel(buf)

    if (parsed.length === 0) {
      return NextResponse.json(
        {
          error:
            "Не удалось распознать строки в файле. Проверьте формат — ожидается отчёт из кабинета WB по auto-акции.",
        },
        { status: 400 },
      )
    }

    // Upsert: удаляем старые номенклатуры этой акции, вставляем новые (транзакционно)
    await prisma.$transaction([
      prisma.wbPromotionNomenclature.deleteMany({
        where: { promotionId },
      }),
      prisma.wbPromotionNomenclature.createMany({
        data: parsed.map((p) => ({
          promotionId,
          nmId: p.nmId,
          inAction: p.inAction,
          planPrice: p.planPrice,
          currentPrice: p.currentPrice,
          planDiscount: p.planDiscount,
          status: p.status,
        })),
        skipDuplicates: true,
      }),
      prisma.wbPromotion.update({
        where: { id: promotionId },
        data: {
          lastSyncedAt: new Date(),
          source: "EXCEL",
        },
      }),
    ])

    return NextResponse.json({
      imported: parsed.length,
      promotionName: promo.name,
    })
  } catch (e) {
    console.error("Auto promo Excel upload error:", e)
    return NextResponse.json(
      {
        error:
          (e as Error).message ||
          "Не удалось распознать Excel. Проверьте формат файла.",
      },
      { status: 500 },
    )
  }
}
```

**3. Обновить `tests/excel-auto-promo.test.ts` для проверки exported `parseAutoPromoExcel` функции.** Если тест из Wave 0 импортирует только xlsx напрямую — добавить additional assertion через импорт `parseAutoPromoExcel`:

```typescript
// В конец tests/excel-auto-promo.test.ts добавить:
import { parseAutoPromoExcel } from "@/app/api/wb-promotions-upload-excel/route"
import fs from "fs"
import path from "path"

describe("parseAutoPromoExcel — real fixture", () => {
  it("парсит fixture в массив ParsedAutoPromoRow", () => {
    const fixturePath = path.resolve(
      process.cwd(),
      "tests/fixtures/auto-promo-sample.xlsx",
    )
    const buf = fs.readFileSync(fixturePath)
    const rows = parseAutoPromoExcel(buf)

    expect(rows.length).toBeGreaterThan(0)
    expect(rows[0].nmId).toBeGreaterThan(0)
    expect(typeof rows[0].inAction).toBe("boolean")
  })
})
```

**4. Запустить тест:**
```bash
npm run test -- tests/excel-auto-promo.test.ts
```
GREEN ожидается.

**Правила:**
- `parseAutoPromoExcel` — pure function, exported для тестов. Не смешивает file upload с парсингом.
- Парсинг по индексам, НЕ по именам колонок (кириллица + BOM).
- `nmIdRaw` проверка: `parseInt(nmIdRaw) → NaN` → skip row (робастность).
- Транзакция `$transaction([...])` обеспечивает атомарность delete+createMany+update.
- Error messages на русском для пользователя.
  </action>
  <verify>
    <automated>npm run test -- tests/excel-auto-promo.test.ts</automated>
    <manual>
1. `cat app/api/wb-promotions-upload-excel/route.ts | head -10` — файл существует.
2. `grep "export function parseAutoPromoExcel" app/api/wb-promotions-upload-excel/route.ts` — функция exported.
3. `grep "prisma.\\$transaction" app/api/wb-promotions-upload-excel/route.ts` — транзакция есть.
4. `grep "requireSection" app/api/wb-promotions-upload-excel/route.ts` — RBAC есть.
5. `npm run test -- tests/excel-auto-promo.test.ts` → GREEN.
    </manual>
  </verify>
  <done>
- `app/api/wb-promotions-upload-excel/route.ts` существует с exported `parseAutoPromoExcel`
- Multipart (file + promotionId) парсится, валидация `promo.type === "auto"`
- Транзакционный upsert (delete + createMany + update)
- `tests/excel-auto-promo.test.ts` GREEN
- Error messages на русском
  </done>
</task>

</tasks>

<verification>
- Оба route файла существуют и компилируются
- `npm run test` → `tests/excel-auto-promo.test.ts` GREEN
- RBAC проверки на месте
- Rate limit handling через функции из lib/wb-api.ts
</verification>

<success_criteria>
- План 07-10 (UI кнопки) может вызывать `fetch("/api/wb-promotions-sync", { method: "POST" })` и `fetch("/api/wb-promotions-upload-excel", { method: "POST", body: formData })`
- При первом вызове sync — загружаются 83+ акций из WB Promotions Calendar за ~60-90 секунд
- Excel upload валидирует что акция существует и type=auto, парсит 6 колонок
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-04-SUMMARY.md`. Включить:
- Два созданных endpoint'а с примером curl для manual verification:
  - `curl -X POST https://zoiten.pro/api/wb-promotions-sync` (с cookie session)
  - `curl -X POST https://zoiten.pro/api/wb-promotions-upload-excel -F file=@sample.xlsx -F promotionId=2287`
- Ожидаемый response shape
- Известные edge cases (422 auto → silent return, 429 retry, cleanup стратегия)
</output>
