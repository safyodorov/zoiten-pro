---
phase: 07-prices-wb
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - lib/wb-api.ts
  - app/api/wb-sync/route.ts
autonomous: true
requirements:
  - PRICES-10
  - PRICES-12
must_haves:
  truths:
    - "`lib/wb-api.ts` экспортирует 4 новые функции: `fetchAllPromotions`, `fetchPromotionDetails`, `fetchPromotionNomenclatures`, `fetchAvgSalesSpeed7d`"
    - "Константа `PROMO_API` зафиксирована как верифицированный base URL WB Promotions Calendar (из 07-WAVE0-NOTES.md)"
    - "`fetchAllPromotions` делает pagination через offset/limit с паузой 600ms между запросами"
    - "`fetchPromotionDetails` батчит по 10 ID за раз с 600ms паузами"
    - "`fetchPromotionNomenclatures` обрабатывает 422 (auto-акции) через silent return `[]`"
    - "Все 3 функции обрабатывают 429 через `sleep(6000)` + retry"
    - "`fetchAvgSalesSpeed7d(nmIds[])` вызывает WB Statistics Sales API за 7 дней, возвращает `Map<nmId, avgPerDay>`"
    - "`app/api/wb-sync/route.ts` вызывает `fetchAvgSalesSpeed7d` и записывает результат в `WbCard.avgSalesSpeed7d`"
    - "`tests/wb-promotions-api.test.ts` GREEN — mocked rate limit + 429 retry"
  artifacts:
    - path: "lib/wb-api.ts"
      provides: "существующие функции + 4 новые + PROMO_API константа"
      contains: "fetchAllPromotions"
    - path: "app/api/wb-sync/route.ts"
      provides: "существующая sync логика + avgSalesSpeed7d update"
      contains: "fetchAvgSalesSpeed7d"
  key_links:
    - from: "lib/wb-api.ts"
      to: "WB Promotions Calendar API"
      via: "fetch с Authorization header и паузами"
      pattern: "dp-calendar-api.wildberries.ru|discounts-prices-api.wildberries.ru"
    - from: "app/api/wb-sync/route.ts"
      to: "WbCard.avgSalesSpeed7d"
      via: "prisma.wbCard.update при синхронизации"
      pattern: "avgSalesSpeed7d"
    - from: "tests/wb-promotions-api.test.ts"
      to: "lib/wb-api.ts fetchAllPromotions"
      via: "mocked fetch test"
      pattern: "fetchAllPromotions"
---

<objective>
Расширить `lib/wb-api.ts` функциями для работы с WB Promotions Calendar API (синхронизация акций) и Statistics Sales API (средняя скорость продаж за 7 дней). Интегрировать `fetchAvgSalesSpeed7d` в существующий `/api/wb-sync/route.ts`.

Purpose: обеспечить серверный слой для синхронизации акций (используется в плане 07-04) и автоматического обновления скорости продаж при регулярной синхронизации WB карточек.

**КРИТИЧНО:** Rate limit handling — WB Promotions API режет 10 req / 6 sec, поэтому все функции должны делать паузы 600ms между запросами и обрабатывать 429 через `sleep(6000)` + retry.

Output:
- `lib/wb-api.ts` с 4 новыми функциями (ДОБАВКА, не рефактор существующих)
- `app/api/wb-sync/route.ts` обновлён для записи avgSalesSpeed7d
- `tests/wb-promotions-api.test.ts` GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-WAVE0-NOTES.md
@.planning/phases/07-prices-wb/07-01-SUMMARY.md
@CLAUDE.md
@lib/wb-api.ts
@app/api/wb-sync/route.ts
@tests/wb-promotions-api.test.ts

<interfaces>
<!-- Существующие паттерны в lib/wb-api.ts: -->

- `getToken()` — читает `process.env.WB_API_TOKEN`, валидирует что токен существует
- `fetch(url, { headers: { Authorization: token } })` — стандартный паттерн seller API вызова
- `execSync("curl ...")` — ТОЛЬКО для публичного `card.wb.ru/v4/detail` (TLS блокировка Node.js fetch), НЕ для Promotions API

<!-- WB Promotions Calendar API endpoints: -->

Base URL (из 07-WAVE0-NOTES.md, верифицирован smoke test'ом в Wave 0):
- `https://dp-calendar-api.wildberries.ru` ИЛИ `https://discounts-prices-api.wildberries.ru`
- **EXECUTOR:** читает 07-WAVE0-NOTES.md секция 3 и фиксирует правильный URL как константу `PROMO_API` в начале функций.

Endpoints:
1. `GET /api/v1/calendar/promotions?startDateTime=ISO&endDateTime=ISO&allPromo=true&limit=N&offset=N`
   - Возвращает список акций: `{data: {promotions: [{id, name, type, startDateTime, endDateTime, description?, advantages?}]}}`
   - Pagination через limit/offset (макс limit=100)
   - Rate limit: 10 req / 6 sec

2. `GET /api/v1/calendar/promotions/details?promotionIDs=1,2,3` (макс 10 ID за раз)
   - Возвращает детали: `{data: {promotions: [{id, description, advantages, ranging}]}}`

3. `GET /api/v1/calendar/promotions/nomenclatures?promotionID=X&inAction=false&limit=1000`
   - ТОЛЬКО для `type="regular"`. Для `type="auto"` возвращает 422 — нужно silent return `[]`.
   - Возвращает: `{data: {nomenclatures: [{nmID, planPrice, planDiscount, inAction?}]}}`

<!-- WB Statistics Sales API (для avgSalesSpeed7d): -->

- `GET https://statistics-api.wildberries.ru/api/v1/supplier/sales?dateFrom={ISO_7_days_ago}`
- Возвращает массив продаж: `[{nmId, date, ...}, ...]`
- Агрегация: `count(sales by nmId) / 7 = avgPerDay`

<!-- Текущий /api/wb-sync/route.ts структура: -->

- Последовательно вызывает: `fetchCards`, `fetchAllPrices`, `fetchStandardCommissions`, `fetchStocks`, `fetchBuyoutPercent`, `fetchWbDiscounts`
- Upsert'ит WbCard с полями: `priceBeforeDiscount`, `sellerDiscount`, `price`, `stockQty`, `buyoutPercent`, `commFbwStd`, `discountWb`, и т.д.
- **НУЖНО ДОБАВИТЬ:** вызов `fetchAvgSalesSpeed7d(nmIds)` → Map → передача в upsert как новое поле `avgSalesSpeed7d`

<!-- Mocked test из Wave 0 tests/wb-promotions-api.test.ts: -->

Тест проверяет:
1. `fetchAllPromotions` делает pagination до пустого ответа (минимум 2 fetch'а)
2. `fetchAllPromotions` на 429 делает `sleep(6000)` и retry один раз, потом продолжает
3. `vi.useFakeTimers()` используется для контроля sleep'ов

<!-- sleep helper (может быть добавлен в lib/wb-api.ts если ещё нет): -->

```typescript
function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms))
}
```

**КРИТИЧНЫЕ ПРАВИЛА (pitfalls из RESEARCH.md):**
- НЕ использовать `execSync('curl ...')` для Promotions API — только `fetch`.
- Обрабатывать 429 через sleep(6000) + retry, НЕ пропускать.
- Фильтровать `p.type !== "auto"` перед `fetchPromotionNomenclatures` (или silent return [] на 422).
- Проверка existing `maxDuration` в route.ts — 60 сек может быть мало для sync, увеличить до 300.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить fetchAllPromotions/fetchPromotionDetails/fetchPromotionNomenclatures в lib/wb-api.ts</name>
  <files>lib/wb-api.ts</files>
  <action>
**1. Прочитать `07-WAVE0-NOTES.md` секция 3** и зафиксировать правильный `PROMO_API` base URL.

**2. Прочитать текущий `lib/wb-api.ts`** чтобы понять структуру: где определены константы, как используется `getToken()`, есть ли уже `sleep()` helper.

**3. ДОБАВИТЬ в конец файла `lib/wb-api.ts`** (не трогая существующие функции):

```typescript
// ──────────────────────────────────────────────────────────────────
// Phase 7: WB Promotions Calendar API
// ──────────────────────────────────────────────────────────────────
//
// Endpoints для синхронизации акций WB (D-04, D-05).
// Rate limit: 10 req / 6 sec → паузы 600ms между запросами, sleep(6000) + retry на 429.
//
// Base URL верифицирован smoke test'ом в Wave 0 (см. 07-WAVE0-NOTES.md).

/** Base URL WB Promotions Calendar API. Верифицирован в Wave 0. */
const PROMO_API = "https://dp-calendar-api.wildberries.ru"
// EXECUTOR: если Wave 0 smoke test показал другой URL — заменить.
// Альтернатива: "https://discounts-prices-api.wildberries.ru"

/** Пауза между запросами — 10 req/6sec = 600ms interval. */
const PROMO_RATE_DELAY_MS = 600

/** Sleep при 429 — полный rate window + буфер. */
const PROMO_429_BACKOFF_MS = 6000

/** Raw структура акции из WB API (минимальная, для upsert в WbPromotion). */
export interface WbPromotionRaw {
  id: number
  name: string
  description?: string
  advantages?: string[]
  startDateTime: string  // RFC3339
  endDateTime: string
  type: string           // "auto" | "regular" | others
}

/** Raw детали акции (advantages, description, ranging). */
export interface WbPromotionDetailsRaw {
  id: number
  name?: string
  description?: string
  advantages?: string[]
  ranging?: unknown[]
}

/** Raw номенклатура в акции (только для regular). */
export interface WbPromotionNomenclatureRaw {
  nmID: number
  price?: number
  planPrice?: number
  discount?: number
  planDiscount?: number
  inAction?: boolean
}

// Helper: pause (used for rate limiting)
function sleep(ms: number): Promise<void> {
  return new Promise((resolve) => setTimeout(resolve, ms))
}

/** Получить список всех акций в окне [startDate, endDate].
 *  Обрабатывает pagination через offset/limit=100.
 *  Rate limit: 600ms между запросами, sleep(6000) на 429 + retry один раз.
 */
export async function fetchAllPromotions(
  startDate: Date,
  endDate: Date,
): Promise<WbPromotionRaw[]> {
  const token = getToken()
  const all: WbPromotionRaw[] = []
  let offset = 0
  const limit = 100
  let retried429 = false

  while (true) {
    const url =
      `${PROMO_API}/api/v1/calendar/promotions` +
      `?startDateTime=${encodeURIComponent(startDate.toISOString())}` +
      `&endDateTime=${encodeURIComponent(endDate.toISOString())}` +
      `&allPromo=true&limit=${limit}&offset=${offset}`

    const res = await fetch(url, {
      headers: { Authorization: token },
    })

    if (res.status === 429) {
      if (retried429) {
        throw new Error("WB Promotions API rate limit: 429 после retry")
      }
      retried429 = true
      await sleep(PROMO_429_BACKOFF_MS)
      continue
    }
    retried429 = false

    if (!res.ok) {
      const body = await res.text()
      throw new Error(`WB Promotions API ${res.status}: ${body}`)
    }

    const data = await res.json()
    const items =
      (data?.data?.promotions ??
        data?.promotions ??
        []) as WbPromotionRaw[]

    if (items.length === 0) break
    all.push(...items)

    if (items.length < limit) break

    offset += items.length
    await sleep(PROMO_RATE_DELAY_MS)
  }

  return all
}

/** Получить детали акций батчами по 10 ID.
 *  Возвращает массив деталей (в порядке неопределённый — склеить по id на стороне caller'а).
 */
export async function fetchPromotionDetails(
  ids: number[],
): Promise<WbPromotionDetailsRaw[]> {
  if (ids.length === 0) return []
  const token = getToken()
  const details: WbPromotionDetailsRaw[] = []

  for (let i = 0; i < ids.length; i += 10) {
    const batch = ids.slice(i, i + 10)
    const url =
      `${PROMO_API}/api/v1/calendar/promotions/details` +
      `?promotionIDs=${batch.join(",")}`

    let attempt = 0
    while (true) {
      const res = await fetch(url, {
        headers: { Authorization: token },
      })

      if (res.status === 429) {
        if (attempt >= 1) {
          throw new Error("WB Promotions details: 429 после retry")
        }
        attempt++
        await sleep(PROMO_429_BACKOFF_MS)
        continue
      }

      if (!res.ok) {
        const body = await res.text()
        throw new Error(`WB Promotions details ${res.status}: ${body}`)
      }

      const data = await res.json()
      const items =
        (data?.data?.promotions ??
          data?.promotions ??
          []) as WbPromotionDetailsRaw[]
      details.push(...items)
      break
    }

    if (i + 10 < ids.length) {
      await sleep(PROMO_RATE_DELAY_MS)
    }
  }

  return details
}

/** Получить номенклатуры ОДНОЙ акции (только regular — для auto вернёт пустой массив из-за 422).
 *  silent return `[]` на 422 и retry на 429.
 */
export async function fetchPromotionNomenclatures(
  promotionId: number,
): Promise<WbPromotionNomenclatureRaw[]> {
  const token = getToken()
  const url =
    `${PROMO_API}/api/v1/calendar/promotions/nomenclatures` +
    `?promotionID=${promotionId}&inAction=false&limit=1000`

  let attempt = 0
  while (true) {
    const res = await fetch(url, { headers: { Authorization: token } })

    if (res.status === 422) {
      // Auto-акция — silent return (D-06: auto через Excel)
      return []
    }

    if (res.status === 429) {
      if (attempt >= 1) {
        throw new Error(`WB nomenclatures ${promotionId}: 429 после retry`)
      }
      attempt++
      await sleep(PROMO_429_BACKOFF_MS)
      continue
    }

    if (!res.ok) {
      const body = await res.text()
      throw new Error(`WB nomenclatures ${promotionId} ${res.status}: ${body}`)
    }

    const data = await res.json()
    const items =
      (data?.data?.nomenclatures ??
        data?.nomenclatures ??
        []) as WbPromotionNomenclatureRaw[]
    return items
  }
}

/** Получить среднюю скорость продаж за последние 7 дней.
 *  Возвращает Map<nmId, avgPerDay> (sales за 7 дней / 7).
 *
 *  Источник: WB Statistics Sales API /api/v1/supplier/sales.
 *  D-09: записывается в WbCard.avgSalesSpeed7d при sync.
 */
export async function fetchAvgSalesSpeed7d(
  nmIds: number[],
): Promise<Map<number, number>> {
  const token = getToken()
  const result = new Map<number, number>()
  if (nmIds.length === 0) return result

  const dateFrom = new Date()
  dateFrom.setDate(dateFrom.getDate() - 7)
  const dateFromIso = dateFrom.toISOString()

  const url =
    `https://statistics-api.wildberries.ru/api/v1/supplier/sales` +
    `?dateFrom=${encodeURIComponent(dateFromIso)}`

  const res = await fetch(url, { headers: { Authorization: token } })

  if (res.status === 429) {
    await sleep(60_000) // Statistics API даёт 1 req/min
    return fetchAvgSalesSpeed7d(nmIds)
  }

  if (!res.ok) {
    console.error(`WB Sales API ${res.status}`)
    return result // degraded mode — поле останется null
  }

  const sales = (await res.json()) as Array<{ nmId?: number; nm_id?: number }>

  // Подсчитать количество продаж per nmId
  const counts = new Map<number, number>()
  for (const s of sales) {
    const nm = s.nmId ?? s.nm_id
    if (nm == null) continue
    counts.set(nm, (counts.get(nm) ?? 0) + 1)
  }

  // Вернуть только те nmId, которые в запросе
  const requestedSet = new Set(nmIds)
  for (const [nmId, count] of counts) {
    if (requestedSet.has(nmId)) {
      result.set(nmId, count / 7)
    }
  }

  return result
}
```

**4. Запустить mocked тесты rate limit:**
```bash
npm run test -- tests/wb-promotions-api.test.ts
```
Тесты должны быть GREEN (тесты из Wave 0 проверяют что fetch вызывается с паузами).

**5. Если тест на 429 retry падает:** проверить что в `fetchAllPromotions` используется `await sleep(PROMO_429_BACKOFF_MS)` перед `continue`, а не до `const res = await fetch(...)`. Тест с `vi.useFakeTimers` + `vi.advanceTimersByTimeAsync(6100)` должен поймать один повтор.

**Правила:**
- НЕ трогать существующие функции в `lib/wb-api.ts` — только ДОБАВКА в конце файла.
- НЕ использовать `execSync('curl')` — обычный `fetch`.
- Комментарии на русском, код identifiers на английском.
- `PROMO_API` как константа — один source of truth для base URL.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-promotions-api.test.ts</automated>
    <manual>
1. `grep -c "export async function fetch" lib/wb-api.ts` — должно содержать как минимум 4 новые функции plus существующие.
2. `grep "PROMO_API" lib/wb-api.ts` — константа зафиксирована в начале добавленного блока.
3. `grep "fetchAllPromotions\|fetchPromotionDetails\|fetchPromotionNomenclatures\|fetchAvgSalesSpeed7d" lib/wb-api.ts` — все 4 функции есть.
4. `npm run test -- tests/wb-promotions-api.test.ts` → GREEN (2 теста pass).
    </manual>
  </verify>
  <done>
- `lib/wb-api.ts` содержит 4 новые exported функции
- `PROMO_API` константа с верифицированным URL
- Rate limit handling через `sleep(600)` + `sleep(6000)` на 429
- `fetchPromotionNomenclatures` возвращает `[]` на 422 (auto-акции)
- `fetchAvgSalesSpeed7d` возвращает Map через Statistics Sales API
- `tests/wb-promotions-api.test.ts` GREEN
- Существующие функции нетронуты (НЕ рефактор)
  </done>
</task>

<task type="auto">
  <name>Task 2: Интегрировать fetchAvgSalesSpeed7d в /api/wb-sync/route.ts</name>
  <files>app/api/wb-sync/route.ts</files>
  <action>
Прочитать текущий `app/api/wb-sync/route.ts` — это существующий endpoint полной синхронизации WB карточек. Он последовательно вызывает `fetchCards`, `fetchAllPrices`, `fetchStandardCommissions`, `fetchStocks`, `fetchBuyoutPercent`, `fetchWbDiscounts` и upsert'ит WbCard с этими данными.

**Добавить ДВЕ вещи:**

**1. Импорт `fetchAvgSalesSpeed7d`** в начале файла (там где импортируются другие fetch функции):
```typescript
import {
  // ... существующие ...
  fetchAvgSalesSpeed7d,
} from "@/lib/wb-api"
```

**2. Вызов функции и обновление WbCard:**

Найти место где собраны `nmIds` (массив всех nmId обрабатываемых карточек — он уже должен существовать в этом route). Добавить после всех других sync вызовов, перед upsert:

```typescript
// Phase 7 (D-09): средняя скорость продаж за 7 дней
let salesSpeedMap = new Map<number, number>()
try {
  salesSpeedMap = await fetchAvgSalesSpeed7d(nmIds)
} catch (e) {
  console.error("fetchAvgSalesSpeed7d failed:", e)
  // Continue без этого поля — degraded mode
}
```

**Затем** в upsert/update блоке каждой WbCard добавить поле:
```typescript
avgSalesSpeed7d: salesSpeedMap.get(card.nmId) ?? null,
```

**Важно:** Эта добавка должна быть аккуратной — НЕ ломать существующий контракт route.ts. Если существующий код использует `for (const card of cards)` паттерн с `prisma.wbCard.upsert` — добавить поле в оба `create` и `update` объекты. Если используется `createMany` — добавить в data array.

**3. Увеличить maxDuration** если сейчас < 120:
```typescript
export const maxDuration = 180 // 3 минуты — учитывает добавку Sales API
```

**Правила:**
- НЕ удалять существующие sync шаги.
- НЕ менять порядок вызовов (Prices после Content, Stocks после Prices, и т.д. — паттерн уже работает).
- НЕ выбрасывать ошибку если `fetchAvgSalesSpeed7d` падает — degraded mode (поле станет null, но основной sync завершается).
- Добавка должна быть **аддитивной** — существующий функционал не должен сломаться.
  </action>
  <verify>
    <automated>grep -c "fetchAvgSalesSpeed7d\|avgSalesSpeed7d" app/api/wb-sync/route.ts</automated>
    <manual>
1. `grep "fetchAvgSalesSpeed7d" app/api/wb-sync/route.ts` — вызов существует.
2. `grep "avgSalesSpeed7d" app/api/wb-sync/route.ts` — поле добавлено в upsert.
3. `npm run build` (или `npx tsc --noEmit`) — TypeScript компилируется без ошибок.
4. Ручная проверка: существующие sync шаги (`fetchAllPrices`, `fetchStocks`, etc.) **не удалены** и **не изменены**.
    </manual>
  </verify>
  <done>
- `app/api/wb-sync/route.ts` вызывает `fetchAvgSalesSpeed7d(nmIds)` после существующих sync шагов
- Результат сохраняется в Map, используется в upsert каждой WbCard как `avgSalesSpeed7d` field
- Ошибка fetch не ломает sync (try/catch вокруг вызова, degraded mode)
- TypeScript компилируется
- Существующая функциональность нетронута
  </done>
</task>

</tasks>

<verification>
- 4 новые функции в `lib/wb-api.ts` работают
- `/api/wb-sync/route.ts` обновляет `WbCard.avgSalesSpeed7d` при следующей синхронизации
- mocked тесты rate limit GREEN
- TypeScript компилируется без ошибок
</verification>

<success_criteria>
- План 07-04 (API routes `/api/wb-promotions-sync` и `/api/wb-promotions-upload-excel`) может использовать функции `fetchAllPromotions`, `fetchPromotionDetails`, `fetchPromotionNomenclatures` из `lib/wb-api.ts`
- При следующей ручной синхронизации `/api/wb-sync` через кнопку «Синхронизировать с WB» — поле `WbCard.avgSalesSpeed7d` заполняется автоматически
- План 07-07 (PriceCalculatorTable) может читать `wbCard.avgSalesSpeed7d` и показывать в колонке Сводка
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-03-SUMMARY.md`. Включить:
- Список 4 новых функций с signatures
- Зафиксированный PROMO_API URL (финальный, после Wave 0 verification)
- Rate limit стратегия (600ms / 6000ms)
- Как /api/wb-sync теперь обновляет avgSalesSpeed7d
- Примечание что существующие WbCards получат значение только после следующей полной синхронизации
</output>
