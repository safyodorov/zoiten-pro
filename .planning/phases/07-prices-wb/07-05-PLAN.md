---
phase: 07-prices-wb
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - app/actions/pricing.ts
autonomous: true
requirements:
  - PRICES-06
  - PRICES-08
  - PRICES-09
  - PRICES-14
must_haves:
  truths:
    - "`app/actions/pricing.ts` экспортирует все server actions для Phase 7 (updateAppSetting, getPricingSettings, saveCalculatedPrice, updateProductOverride, updateSubcategoryDefault, updateCategoryDefault, updateProductDelivery)"
    - "Все actions защищены `requireSection(\"PRICES\", \"MANAGE\")`, кроме `getPricingSettings` который использует `requireSection(\"PRICES\")` (VIEW достаточно)"
    - "Все actions используют Zod валидацию с понятными русскими сообщениями об ошибках"
    - "`appSettingValueSchema` принимает только строки с числом 0-100 с десятыми"
    - "`saveCalculatedPrice` делает upsert по `@@unique([wbCardId, slot])` и сохраняет `snapshot: Json`"
    - "`updateProductOverride` работает с 3 полями: drrOverridePct, defectRateOverridePct, deliveryCostRub"
    - "`revalidatePath(\"/prices/wb\")` вызывается после каждой write action (кроме getPricingSettings)"
    - "`tests/pricing-settings.test.ts` GREEN (Zod валидация)"
  artifacts:
    - path: "app/actions/pricing.ts"
      provides: "все server actions для Phase 7 + exported Zod schemas + isValidAppSettingKey"
      min_lines: 200
      exports:
        - "updateAppSetting"
        - "getPricingSettings"
        - "saveCalculatedPrice"
        - "updateProductOverride"
        - "updateSubcategoryDefault"
        - "updateCategoryDefault"
        - "updateProductDelivery"
        - "appSettingValueSchema"
        - "isValidAppSettingKey"
  key_links:
    - from: "app/actions/pricing.ts"
      to: "prisma.appSetting"
      via: "upsert для updateAppSetting"
      pattern: "prisma.appSetting.upsert"
    - from: "app/actions/pricing.ts"
      to: "prisma.calculatedPrice"
      via: "upsert по @@unique"
      pattern: "prisma.calculatedPrice.upsert"
    - from: "app/actions/pricing.ts"
      to: "lib/rbac.ts requireSection"
      via: "guard в каждом action"
      pattern: "requireSection\\(\"PRICES\""
    - from: "tests/pricing-settings.test.ts"
      to: "app/actions/pricing.ts"
      via: "import appSettingValueSchema и isValidAppSettingKey"
      pattern: "appSettingValueSchema"
---

<objective>
Создать единственный файл `app/actions/pricing.ts` со всеми серверными actions для Phase 7. Это централизованное место для:
- Чтения/обновления глобальных ставок `AppSetting`
- Сохранения расчётных цен `CalculatedPrice`
- Обновления override'ов (Product/Subcategory/Category defaults) — для чекбокса «только этот товар» из модалки
- Обновления стоимости доставки на товар

Всё с Zod валидацией и RBAC защитой.

Purpose: серверный слой для модалки `PricingCalculatorDialog` (план 07-09) и для `GlobalRatesBar` (план 07-07).

Output:
- `app/actions/pricing.ts` со всеми 7 actions
- `tests/pricing-settings.test.ts` GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-prices-wb/07-CONTEXT.md
@.planning/phases/07-prices-wb/07-RESEARCH.md
@.planning/phases/07-prices-wb/07-01-SUMMARY.md
@.planning/phases/07-prices-wb/07-02-SUMMARY.md
@CLAUDE.md
@app/actions/users.ts
@tests/pricing-settings.test.ts

<interfaces>
<!-- Prisma модели из плана 07-01: -->

```typescript
prisma.appSetting.upsert({ where: { key }, create: {...}, update: {...} })
prisma.calculatedPrice.upsert({ where: { wbCardId_slot: { wbCardId, slot } }, create: {...}, update: {...} })
prisma.product.update({ where: { id }, data: { drrOverridePct, defectRateOverridePct, deliveryCostRub } })
prisma.subcategory.update({ where: { id }, data: { defaultDrrPct } })
prisma.category.update({ where: { id }, data: { defaultDefectRatePct } })
```

<!-- RBAC helper: -->

```typescript
import { requireSection } from "@/lib/rbac"
await requireSection("PRICES", "MANAGE")  // write
await requireSection("PRICES")             // read
```

<!-- Тест из Wave 0 tests/pricing-settings.test.ts определяет контракт: -->

```typescript
import { appSettingValueSchema, isValidAppSettingKey } from "@/app/actions/pricing"

// appSettingValueSchema принимает: "2.0", "2.5", "0", "99.9"
// Отклоняет: "101", "200", "-1", "-0.5", "abc", ""

// isValidAppSettingKey принимает 6 ключей:
// wbWalletPct, wbAcquiringPct, wbJemPct, wbCreditPct, wbOverheadPct, wbTaxPct
// Отклоняет: "unknown", "wbEvilKey"
```

<!-- Паттерн существующих server actions (из app/actions/users.ts, app/actions/products.ts): -->

```typescript
"use server"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"
import { revalidatePath } from "next/cache"
import { z } from "zod"

const mySchema = z.object({...})

export async function updateThing(input: z.infer<typeof mySchema>) {
  await requireSection("SECTION", "MANAGE")
  const parsed = mySchema.safeParse(input)
  if (!parsed.success) {
    return { ok: false, error: parsed.error.issues[0].message }
  }
  try {
    await prisma.model.update({ ... })
    revalidatePath("/path")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}
```

<!-- CalculatedPrice snapshot: -->

`snapshot: Json` — должен содержать полный слепок параметров на момент сохранения, включая:
- Входные: priceBeforeDiscount, sellerDiscountPct, wbDiscountPct, clubDiscountPct, commFbwPct
- Глобальные ставки из AppSetting на момент сохранения (wbWalletPct, wbAcquiringPct, wbJemPct, wbCreditPct, wbOverheadPct, wbTaxPct)
- Resolved per-product: drrPct (с учётом fallback), defectRatePct, deliveryCostRub, costPrice
- Выходные (для debug): профит, ROI, Re продаж (опционально)

Для валидации - `z.record(z.any())` достаточно (frontend отправляет готовый snapshot объект).

<!-- D-09 Override scope logic: -->

- Чекбокс «только этот товар» (в модалке для ДРР и Брак):
  - true → `updateProductOverride(productId, field, value)` — обновляет Product.drrOverridePct или Product.defectRateOverridePct
  - false → `updateSubcategoryDefault(subcategoryId, value)` (для ДРР) или `updateCategoryDefault(categoryId, value)` (для брака) — обновляет всю подкатегорию/категорию
- Доставка — всегда Product.deliveryCostRub (нет чекбокса, см. D-14)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать app/actions/pricing.ts со всеми 7 actions + Zod схемами</name>
  <files>app/actions/pricing.ts</files>
  <action>
**1. Создать файл `app/actions/pricing.ts`:**

```typescript
// app/actions/pricing.ts
// Phase 7: Server actions для управления ценами WB.
//
// Экспортирует:
// - updateAppSetting: обновить одну глобальную ставку (debounced из GlobalRatesBar)
// - getPricingSettings: прочитать все глобальные ставки (для init)
// - saveCalculatedPrice: upsert расчётной цены в слот 1/2/3
// - updateProductOverride: Product.drrOverridePct / defectRateOverridePct (чекбокс «только этот товар»)
// - updateSubcategoryDefault: Subcategory.defaultDrrPct (для всех товаров подкатегории)
// - updateCategoryDefault: Category.defaultDefectRatePct (для всех товаров категории)
// - updateProductDelivery: Product.deliveryCostRub (всегда per-product, без scope выбора)

"use server"

import { z } from "zod"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"
import { revalidatePath } from "next/cache"

// ──────────────────────────────────────────────────────────────────
// Константы и whitelisted ключи
// ──────────────────────────────────────────────────────────────────

/** Whitelisted ключи AppSetting для Phase 7. Защита от injection неизвестных ключей. */
const APP_SETTING_KEYS = [
  "wbWalletPct",
  "wbAcquiringPct",
  "wbJemPct",
  "wbCreditPct",
  "wbOverheadPct",
  "wbTaxPct",
] as const

export type AppSettingKey = (typeof APP_SETTING_KEYS)[number]

/** Проверка валидности ключа AppSetting. Exported для тестов и защиты action'ов. */
export function isValidAppSettingKey(key: string): key is AppSettingKey {
  return (APP_SETTING_KEYS as readonly string[]).includes(key)
}

// ──────────────────────────────────────────────────────────────────
// Zod схемы
// ──────────────────────────────────────────────────────────────────

/** Zod схема для value глобальной ставки.
 *  Строка → парсится как число → валидируется [0, 100] с десятыми.
 *  Exported для тестов tests/pricing-settings.test.ts.
 */
export const appSettingValueSchema = z
  .string()
  .min(1, "Значение не может быть пустым")
  .refine((s) => !Number.isNaN(parseFloat(s)), "Значение должно быть числом")
  .refine((s) => {
    const n = parseFloat(s)
    return n >= 0 && n <= 100
  }, "Значение должно быть в диапазоне [0, 100]")

/** Zod схема для slot (1|2|3). */
const slotSchema = z.number().int().min(1).max(3)

/** Zod схема для сохранения расчётной цены. */
const saveCalculatedPriceSchema = z.object({
  wbCardId: z.string().min(1, "wbCardId обязателен"),
  slot: slotSchema,
  name: z.string().min(1, "Название обязательно").max(100, "Название слишком длинное"),
  sellerPrice: z.number().positive("Цена продавца должна быть > 0"),
  drrPct: z.number().min(0).max(100).nullable().optional(),
  defectRatePct: z.number().min(0).max(100).nullable().optional(),
  deliveryCostRub: z.number().min(0).nullable().optional(),
  snapshot: z.record(z.any()), // JSON полный слепок параметров
})

/** Zod схема для обновления product override полей. */
const updateProductOverrideSchema = z.object({
  productId: z.string().min(1),
  field: z.enum(["drrOverridePct", "defectRateOverridePct"]),
  value: z.number().min(0).max(100).nullable(),
})

// ──────────────────────────────────────────────────────────────────
// Result type
// ──────────────────────────────────────────────────────────────────

type ActionResult<T = undefined> =
  | { ok: true; data?: T }
  | { ok: false; error: string }

// ──────────────────────────────────────────────────────────────────
// Actions
// ──────────────────────────────────────────────────────────────────

/** Получить все 6 глобальных ставок.
 *  Возвращает Record<key, number> (value парсится в number).
 *  Lazy-seed: если ключи отсутствуют (первый вызов после миграции не сработал) — создаёт дефолты.
 */
export async function getPricingSettings(): Promise<
  ActionResult<Record<AppSettingKey, number>>
> {
  try {
    await requireSection("PRICES")
  } catch {
    return { ok: false, error: "Нет доступа к разделу «Управление ценами»" }
  }

  try {
    const rows = await prisma.appSetting.findMany({
      where: { key: { in: [...APP_SETTING_KEYS] } },
    })

    const result: Record<string, number> = {}
    for (const row of rows) {
      result[row.key] = parseFloat(row.value) || 0
    }

    // Lazy-seed отсутствующих (safeguard — миграция должна была заполнить)
    const defaults: Record<AppSettingKey, number> = {
      wbWalletPct: 2.0,
      wbAcquiringPct: 2.7,
      wbJemPct: 1.0,
      wbCreditPct: 7.0,
      wbOverheadPct: 6.0,
      wbTaxPct: 8.0,
    }
    for (const key of APP_SETTING_KEYS) {
      if (result[key] == null) {
        result[key] = defaults[key]
        // Upsert в background не нужен — миграция это исправит; здесь просто fallback.
      }
    }

    return { ok: true, data: result as Record<AppSettingKey, number> }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Обновить одну глобальную ставку.
 *  Debounced из GlobalRatesBar (500ms), Zod валидация, upsert.
 */
export async function updateAppSetting(
  key: string,
  value: string,
): Promise<ActionResult> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав для изменения ставок" }
  }

  if (!isValidAppSettingKey(key)) {
    return { ok: false, error: `Неизвестный ключ настройки: ${key}` }
  }

  const parsed = appSettingValueSchema.safeParse(value)
  if (!parsed.success) {
    return { ok: false, error: parsed.error.issues[0].message }
  }

  try {
    // Нормализуем — сохраняем с одним знаком после запятой
    const normalized = (Math.round(parseFloat(parsed.data) * 10) / 10).toFixed(1)

    await prisma.appSetting.upsert({
      where: { key },
      create: { key, value: normalized, updatedAt: new Date() },
      update: { value: normalized, updatedAt: new Date() },
    })

    revalidatePath("/prices/wb")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Сохранить расчётную цену в слот (1/2/3) с полным snapshot параметров.
 *  Upsert по @@unique([wbCardId, slot]) — перезапись того же слота идемпотентна.
 */
export async function saveCalculatedPrice(
  input: z.infer<typeof saveCalculatedPriceSchema>,
): Promise<ActionResult<{ id: string }>> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав для сохранения расчётной цены" }
  }

  const parsed = saveCalculatedPriceSchema.safeParse(input)
  if (!parsed.success) {
    return { ok: false, error: parsed.error.issues[0].message }
  }

  try {
    const saved = await prisma.calculatedPrice.upsert({
      where: {
        wbCardId_slot: {
          wbCardId: parsed.data.wbCardId,
          slot: parsed.data.slot,
        },
      },
      create: {
        wbCardId: parsed.data.wbCardId,
        slot: parsed.data.slot,
        name: parsed.data.name,
        sellerPrice: parsed.data.sellerPrice,
        drrPct: parsed.data.drrPct ?? null,
        defectRatePct: parsed.data.defectRatePct ?? null,
        deliveryCostRub: parsed.data.deliveryCostRub ?? null,
        snapshot: parsed.data.snapshot,
      },
      update: {
        name: parsed.data.name,
        sellerPrice: parsed.data.sellerPrice,
        drrPct: parsed.data.drrPct ?? null,
        defectRatePct: parsed.data.defectRatePct ?? null,
        deliveryCostRub: parsed.data.deliveryCostRub ?? null,
        snapshot: parsed.data.snapshot,
      },
    })

    revalidatePath("/prices/wb")
    return { ok: true, data: { id: saved.id } }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Обновить Product.drrOverridePct или Product.defectRateOverridePct.
 *  Используется когда в модалке чекбокс «только этот товар» = true.
 *  value = null → очистка override (будет использоваться Subcategory/Category default).
 */
export async function updateProductOverride(
  input: z.infer<typeof updateProductOverrideSchema>,
): Promise<ActionResult> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав" }
  }

  const parsed = updateProductOverrideSchema.safeParse(input)
  if (!parsed.success) {
    return { ok: false, error: parsed.error.issues[0].message }
  }

  try {
    const data: Record<string, number | null> = {}
    data[parsed.data.field] = parsed.data.value

    await prisma.product.update({
      where: { id: parsed.data.productId },
      data,
    })

    revalidatePath("/prices/wb")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Обновить Subcategory.defaultDrrPct — влияет на ВСЕ товары подкатегории,
 *  у которых не задан Product.drrOverridePct.
 *  Используется когда в модалке чекбокс «только этот товар» = false (ДРР).
 */
export async function updateSubcategoryDefault(
  subcategoryId: string,
  value: number | null,
): Promise<ActionResult> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав" }
  }

  if (value != null && (value < 0 || value > 100)) {
    return { ok: false, error: "Значение должно быть в диапазоне [0, 100]" }
  }

  try {
    await prisma.subcategory.update({
      where: { id: subcategoryId },
      data: { defaultDrrPct: value },
    })
    revalidatePath("/prices/wb")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Обновить Category.defaultDefectRatePct — влияет на ВСЕ товары категории,
 *  у которых не задан Product.defectRateOverridePct.
 *  Используется когда в модалке чекбокс «только этот товар» = false (Брак).
 */
export async function updateCategoryDefault(
  categoryId: string,
  value: number | null,
): Promise<ActionResult> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав" }
  }

  if (value != null && (value < 0 || value > 100)) {
    return { ok: false, error: "Значение должно быть в диапазоне [0, 100]" }
  }

  try {
    await prisma.category.update({
      where: { id: categoryId },
      data: { defaultDefectRatePct: value },
    })
    revalidatePath("/prices/wb")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}

/** Обновить Product.deliveryCostRub. Всегда per-product (D-14).
 *  value = null → использовать hardcoded 30₽.
 */
export async function updateProductDelivery(
  productId: string,
  value: number | null,
): Promise<ActionResult> {
  try {
    await requireSection("PRICES", "MANAGE")
  } catch {
    return { ok: false, error: "Недостаточно прав" }
  }

  if (value != null && value < 0) {
    return { ok: false, error: "Стоимость доставки не может быть отрицательной" }
  }

  try {
    await prisma.product.update({
      where: { id: productId },
      data: { deliveryCostRub: value },
    })
    revalidatePath("/prices/wb")
    return { ok: true }
  } catch (e) {
    return { ok: false, error: (e as Error).message }
  }
}
```

**2. Запустить тесты Zod:**
```bash
npm run test -- tests/pricing-settings.test.ts
```
Ожидается GREEN — тесты проверяют `appSettingValueSchema` и `isValidAppSettingKey`.

**3. Проверить TypeScript компилируется:**
```bash
npx tsc --noEmit
```

**Правила:**
- `"use server"` в начале файла — все exported async функции становятся server actions.
- RBAC check **в первой строке каждой функции**, перед чем-либо еще.
- Zod валидация — `safeParse`, не `parse` (не throw, возвращать `{ ok: false, error }`).
- `revalidatePath("/prices/wb")` после каждой write action.
- `ActionResult<T>` тип — стандартный pattern проекта (проверить в `app/actions/users.ts` для consistency).
- Error messages на русском.
- Комментарии на русском, identifiers на английском.
  </action>
  <verify>
    <automated>npm run test -- tests/pricing-settings.test.ts && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
1. `cat app/actions/pricing.ts | head -20` — `"use server"` присутствует.
2. `grep -c "export async function\|export function\|export const" app/actions/pricing.ts` — минимум 9 exports (7 actions + 2 exported utilities).
3. `grep "requireSection" app/actions/pricing.ts | wc -l` — 7 (по одному на каждое action).
4. `grep "revalidatePath" app/actions/pricing.ts | wc -l` — 6 (все кроме getPricingSettings).
5. `npm run test -- tests/pricing-settings.test.ts` → GREEN (все 10+ assertions pass).
6. `npx tsc --noEmit` — без errors.
    </manual>
  </verify>
  <done>
- `app/actions/pricing.ts` создан с 7 actions + 2 exported utilities (appSettingValueSchema, isValidAppSettingKey)
- Все actions защищены `requireSection`
- Zod валидация везде
- `tests/pricing-settings.test.ts` GREEN
- TypeScript компилируется
- `revalidatePath("/prices/wb")` в каждом write action
  </done>
</task>

</tasks>

<verification>
- 7 server actions экспортированы
- `appSettingValueSchema` и `isValidAppSettingKey` exported для тестов
- `tests/pricing-settings.test.ts` GREEN
- `npx tsc --noEmit` без ошибок
- `"use server"` directive в начале файла
</verification>

<success_criteria>
- План 07-07 (GlobalRatesBar) может импортировать `updateAppSetting` и вызывать с debounce 500ms
- План 07-09 (модалка) может импортировать `saveCalculatedPrice`, `updateProductOverride`, `updateSubcategoryDefault`, `updateCategoryDefault`, `updateProductDelivery`
- План 07-08 (RSC page) может импортировать `getPricingSettings` для получения 6 ставок
- VIEWER user получает FORBIDDEN на write actions; MANAGER/SUPERADMIN — работают
</success_criteria>

<output>
После завершения создать `.planning/phases/07-prices-wb/07-05-SUMMARY.md`. Включить:
- Список 7 actions с signatures
- Примеры вызова из клиента
- RBAC matrix (какие actions требуют MANAGE vs VIEW)
- Примечание что `getPricingSettings` используется не только для init, но и для fallback seed
</output>
