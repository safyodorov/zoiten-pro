---
phase: 12-customer-messenger
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - app/(dashboard)/support/new/page.tsx
  - components/support/NewMessengerTicketForm.tsx
  - components/support/customers/MergeCustomerDialog.tsx
  - app/(dashboard)/support/customers/[customerId]/page.tsx
  - app/(dashboard)/support/[ticketId]/page.tsx
  - app/(dashboard)/support/page.tsx
autonomous: false
requirements:
  - SUP-34
  - SUP-35
must_haves:
  truths:
    - "app/(dashboard)/support/new/page.tsx — RSC с requireSection('SUPPORT','MANAGE'), шапка 'Новый тикет (MESSENGER)', рендерит NewMessengerTicketForm с пустыми defaults"
    - "NewMessengerTicketForm — client RHF + zodResolver: native <select> messengerType (TELEGRAM/WHATSAPP/OTHER), input customerName [1..200], input messengerContact [3..100], input nmId optional integer positive, textarea text [1..10000]. На submit → createManualMessengerTicket → router.push('/support/[ticketId]')"
    - "MergeCustomerDialog — client кнопка 'Связать с другим' + модалка в профиле: search existing Customer (переиспользует searchCustomers), исключает current customerId из списка, AlertDialog confirmation перед merge (shadcn/base-ui), вызов mergeCustomers → router.push('/support/customers/[targetId]')"
    - "Профиль /support/customers/[customerId] — добавлен блок MergeCustomerDialog справа от NoteEditor, только при ticketsCount > 0 (или всегда, для MVP)"
    - "/support/[ticketId]/page.tsx: если ticket.channel === 'MESSENGER' — ReplyPanel/ChatReplyPanel/ReturnActionsPanel НЕ рендерятся, вместо них — read-only hint 'Канал внешний — ответьте покупателю в Telegram/WhatsApp'"
    - "Кнопка '+ Новый тикет' в шапке /support/page.tsx справа от SupportSyncButton → Link на /support/new"
    - "SupportTicketCard показывает messengerType бейдж (Telegram/WhatsApp/другое) для MESSENGER канала рядом с channelLabel 'Мессенджер'"
    - "UAT чек-лист выполнен на VPS: deploy + migration применяется чисто, 7 пунктов UAT проходят (Chat auto-link, manual link, MESSENGER create, merge, ReplyPanel скрыт, профиль, регрессия)"
  artifacts:
    - path: "app/(dashboard)/support/new/page.tsx"
      provides: "RSC страница формы ручного MESSENGER тикета"
      contains: "requireSection"
    - path: "components/support/NewMessengerTicketForm.tsx"
      provides: "Client форма RHF+Zod для MESSENGER тикета"
      exports: ["NewMessengerTicketForm"]
    - path: "components/support/customers/MergeCustomerDialog.tsx"
      provides: "Client модалка merge двух Customer с confirmation"
      exports: ["MergeCustomerDialog"]
  key_links:
    - from: "components/support/NewMessengerTicketForm.tsx"
      to: "app/actions/support.ts createManualMessengerTicket"
      via: "RHF onSubmit → server action → router.push"
      pattern: "createManualMessengerTicket"
    - from: "components/support/customers/MergeCustomerDialog.tsx"
      to: "app/actions/support.ts mergeCustomers + searchCustomers"
      via: "модалка поиска + confirmation + server action"
      pattern: "mergeCustomers"
    - from: "app/(dashboard)/support/customers/[customerId]/page.tsx"
      to: "components/support/customers/MergeCustomerDialog"
      via: "рендерится в aside рядом с NoteEditor"
      pattern: "MergeCustomerDialog"
    - from: "app/(dashboard)/support/page.tsx"
      to: "/support/new"
      via: "Link в шапке ленты — '+ Новый тикет'"
      pattern: "/support/new"
    - from: "app/(dashboard)/support/[ticketId]/page.tsx"
      to: "channel === \"MESSENGER\""
      via: "conditional render — скрытие ReplyPanel для MESSENGER"
      pattern: "MESSENGER"
---

<objective>
Phase 12 Plan 03 — финальный UI слой + deploy + UAT:

1. **`app/(dashboard)/support/new/page.tsx`** — RSC страница формы ручного MESSENGER тикета (требует SUPPORT+MANAGE).
2. **`NewMessengerTicketForm`** — client форма (RHF + Zod): messengerType (native select), customerName, messengerContact, nmId (optional), text. Submit → `createManualMessengerTicket` → redirect на `/support/[ticketId]`.
3. **`MergeCustomerDialog`** — client модалка в профиле покупателя: search target Customer (переиспользует searchCustomers из Plan 12-02), confirmation перед merge, `mergeCustomers` → redirect на `/support/customers/[targetId]`.
4. **Интеграция в `/support/customers/[customerId]/page.tsx`** — кнопка MergeCustomerDialog в aside.
5. **Изменения в `/support/[ticketId]/page.tsx`** — для `channel === "MESSENGER"` скрыть ReplyPanel/ChatReplyPanel/ReturnActionsPanel, показать read-only hint.
6. **Кнопка «+ Новый тикет»** в шапке `/support/page.tsx` → Link на `/support/new`.
7. **MESSENGER бейдж** в SupportTicketCard для различения Telegram/WhatsApp/другое.
8. **Deploy на VPS** — bash deploy.sh с новой миграцией 20260418_phase12_customer_messenger.
9. **Human UAT** — 7 пунктов чек-листа покрывающих все 4 SUP-требования.

Purpose: Менеджер получает полный MVP функционал — создаёт MESSENGER тикеты вручную, сливает дубликаты покупателей, видит все тикеты в ленте с правильной индикацией канала. Phase 12 complete.

Output: 1 новая RSC страница, 2 новых клиентских компонента, 3 модифицированных файла, deploy, UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-customer-messenger/12-RESEARCH.md
@.planning/phases/12-customer-messenger/12-VALIDATION.md
@.planning/phases/12-customer-messenger/12-01-PLAN.md
@.planning/phases/12-customer-messenger/12-02-PLAN.md
@.planning/phases/11-templates-appeals/11-03-PLAN.md
@.planning/phases/09-returns/09-04-PLAN.md
@CLAUDE.md
@app/(dashboard)/support/page.tsx
@app/(dashboard)/support/[ticketId]/page.tsx
@app/(dashboard)/support/customers/[customerId]/page.tsx
@components/support/SupportTicketCard.tsx
@components/ui/dialog.tsx
@components/support/templates/TemplateForm.tsx

<interfaces>
**Из Plan 12-01 (app/actions/support.ts):**
```typescript
export async function createManualMessengerTicket(input): Promise<ActionResult & { ticketId?: string }>
// input: { messengerType: "TELEGRAM"|"WHATSAPP"|"OTHER", customerId: string|null, customerName: string|null, messengerContact: string, text: string, nmId: number|null }

export async function mergeCustomers(input: { sourceId, targetId }): Promise<ActionResult & { ticketsMoved?: number }>
```

**Из Plan 12-02 (app/actions/support.ts):**
```typescript
export async function searchCustomers(query): Promise<{ok:true, customers:Array<{id, name, phone, wbUserId}>} | {ok:false, error}>
```

**Паттерн Form (Phase 11-03 TemplateForm):**
```typescript
"use client"
import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import { z } from "zod"
import { useTransition } from "react"
import { useRouter } from "next/navigation"
import { toast } from "sonner"

const schema = z.object({ ... })

export function Form({ defaultValues }) {
  const router = useRouter()
  const [isPending, startTransition] = useTransition()
  const form = useForm({ resolver: zodResolver(schema), defaultValues })
  const onSubmit = form.handleSubmit((v) => { startTransition(async () => { const r = await action(v); if (r.ok) router.push(...); else toast.error(...) }) })
  return <form onSubmit={onSubmit}>...</form>
}
```

**Native select pattern (CLAUDE.md):**
```tsx
<select
  {...form.register("messengerType")}
  className="w-full h-9 rounded-md border bg-transparent px-2"
>
  <option value="TELEGRAM">Telegram</option>
  <option value="WHATSAPP">WhatsApp</option>
  <option value="OTHER">Другое</option>
</select>
```

**Deploy паттерн (Phase 9/10/11 — deploy.sh):**
```bash
ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
```
deploy.sh запускает `git pull && npm ci && npm run build && npx prisma migrate deploy && systemctl restart zoiten-erp`.

**Messenger type labels:**
- TELEGRAM → "Telegram"
- WHATSAPP → "WhatsApp"
- OTHER → "Другое"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: /support/new + NewMessengerTicketForm + кнопка «+ Новый тикет» в ленте</name>
  <files>
    app/(dashboard)/support/new/page.tsx,
    components/support/NewMessengerTicketForm.tsx,
    app/(dashboard)/support/page.tsx
  </files>
  <read_first>
    - components/support/templates/TemplateForm.tsx (паттерн RHF + zodResolver + native select + useTransition + router.push)
    - app/(dashboard)/support/page.tsx (шапка — добавить кнопку «+ Новый тикет» справа от SupportSyncButton)
    - app/actions/support.ts (Plan 12-01 — createManualMessengerTicket сигнатура)
    - CLAUDE.md (русский, native select, RBAC SUPPORT+MANAGE для write)
    - .planning/phases/11-templates-appeals/11-03-PLAN.md (паттерн Plan 11-03 Task 1 Шаг D — TemplateForm как образец)
  </read_first>
  <action>
    **Шаг A — создать `app/(dashboard)/support/new/page.tsx` (RSC):**

    ```typescript
    import Link from "next/link"
    import { ChevronLeft } from "lucide-react"
    import { requireSection } from "@/lib/rbac"
    import { NewMessengerTicketForm } from "@/components/support/NewMessengerTicketForm"

    export const dynamic = "force-dynamic"

    export default async function NewMessengerTicketPage() {
      await requireSection("SUPPORT", "MANAGE")

      return (
        <div className="p-6 max-w-2xl space-y-4">
          <Link
            href="/support"
            className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground"
          >
            <ChevronLeft className="h-4 w-4 mr-1" />
            Назад к ленте
          </Link>
          <div>
            <h1 className="text-xl font-semibold">Новый тикет (MESSENGER)</h1>
            <p className="text-sm text-muted-foreground mt-1">
              Для обращений из Telegram/WhatsApp/других каналов вне Wildberries
            </p>
          </div>
          <NewMessengerTicketForm />
        </div>
      )
    }
    ```

    **Шаг B — создать `components/support/NewMessengerTicketForm.tsx` (client):**

    ```typescript
    "use client"

    import { useForm } from "react-hook-form"
    import { zodResolver } from "@hookform/resolvers/zod"
    import { z } from "zod"
    import { useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { toast } from "sonner"
    import { Button } from "@/components/ui/button"
    import { createManualMessengerTicket } from "@/app/actions/support"

    const schema = z.object({
      messengerType: z.enum(["TELEGRAM", "WHATSAPP", "OTHER"]),
      customerName: z.string().trim().min(1, "Имя обязательно").max(200),
      messengerContact: z.string().trim().min(3, "Минимум 3 символа").max(100),
      nmId: z
        .union([z.number().int().positive(), z.nan(), z.literal("")])
        .transform((v) => (typeof v === "number" && Number.isFinite(v) ? v : null))
        .nullable(),
      text: z.string().trim().min(1, "Текст обязателен").max(10000),
    })

    type FormInput = z.input<typeof schema>
    type FormOutput = z.output<typeof schema>

    export function NewMessengerTicketForm() {
      const router = useRouter()
      const [isPending, startTransition] = useTransition()

      const form = useForm<FormInput>({
        resolver: zodResolver(schema) as never,
        defaultValues: {
          messengerType: "TELEGRAM",
          customerName: "",
          messengerContact: "",
          nmId: null as unknown as FormInput["nmId"],
          text: "",
        },
      })

      const onSubmit = form.handleSubmit((raw) => {
        const parsed = schema.safeParse(raw)
        if (!parsed.success) {
          toast.error(parsed.error.issues[0]?.message ?? "Ошибка формы")
          return
        }
        const values: FormOutput = parsed.data
        startTransition(async () => {
          const res = await createManualMessengerTicket({
            messengerType: values.messengerType,
            customerId: null,                        // Phase 12 MVP — всегда создаём нового Customer
            customerName: values.customerName,
            messengerContact: values.messengerContact,
            text: values.text,
            nmId: values.nmId,
          })
          if (res.ok && res.ticketId) {
            toast.success("Тикет создан")
            router.push(`/support/${res.ticketId}`)
          } else if (!res.ok) {
            toast.error(res.error)
          }
        })
      })

      return (
        <form onSubmit={onSubmit} className="space-y-4">
          <div>
            <label className="text-sm font-medium" htmlFor="messengerType">
              Канал *
            </label>
            <select
              id="messengerType"
              {...form.register("messengerType")}
              className="w-full h-9 rounded-md border bg-transparent px-2 mt-1"
              disabled={isPending}
            >
              <option value="TELEGRAM">Telegram</option>
              <option value="WHATSAPP">WhatsApp</option>
              <option value="OTHER">Другое</option>
            </select>
          </div>

          <div>
            <label className="text-sm font-medium" htmlFor="customerName">
              Имя покупателя *
            </label>
            <input
              id="customerName"
              type="text"
              {...form.register("customerName")}
              placeholder="Иван Петров"
              className="w-full h-9 rounded-md border bg-transparent px-2 mt-1"
              disabled={isPending}
              maxLength={200}
            />
            {form.formState.errors.customerName && (
              <p className="text-xs text-red-600 mt-1">
                {form.formState.errors.customerName.message}
              </p>
            )}
          </div>

          <div>
            <label className="text-sm font-medium" htmlFor="messengerContact">
              Контакт *
            </label>
            <input
              id="messengerContact"
              type="text"
              {...form.register("messengerContact")}
              placeholder="@username или +79991234567"
              className="w-full h-9 rounded-md border bg-transparent px-2 mt-1"
              disabled={isPending}
              maxLength={100}
            />
            {form.formState.errors.messengerContact && (
              <p className="text-xs text-red-600 mt-1">
                {form.formState.errors.messengerContact.message}
              </p>
            )}
          </div>

          <div>
            <label className="text-sm font-medium" htmlFor="nmId">
              Артикул WB (необязательно)
            </label>
            <input
              id="nmId"
              type="number"
              {...form.register("nmId", {
                setValueAs: (v) => {
                  if (v === "" || v === null || v === undefined) return null
                  const n = Number(v)
                  return Number.isFinite(n) && n > 0 ? n : null
                },
              })}
              placeholder="12345678"
              className="w-full h-9 rounded-md border bg-transparent px-2 mt-1"
              disabled={isPending}
            />
          </div>

          <div>
            <label className="text-sm font-medium" htmlFor="text">
              Текст обращения *
            </label>
            <textarea
              id="text"
              {...form.register("text")}
              rows={6}
              placeholder="Содержание обращения покупателя..."
              className="w-full rounded-md border bg-transparent p-2 mt-1 text-sm resize-y"
              disabled={isPending}
              maxLength={10000}
            />
            {form.formState.errors.text && (
              <p className="text-xs text-red-600 mt-1">
                {form.formState.errors.text.message}
              </p>
            )}
          </div>

          <div className="flex justify-end gap-2 pt-2">
            <Button
              type="button"
              variant="outline"
              onClick={() => router.push("/support")}
              disabled={isPending}
            >
              Отмена
            </Button>
            <Button type="submit" disabled={isPending}>
              {isPending ? "Создание..." : "Создать тикет"}
            </Button>
          </div>
        </form>
      )
    }
    ```

    **Шаг C — обновить `app/(dashboard)/support/page.tsx`** (шапка — добавить кнопку «+ Новый тикет»):

    Найти блок шапки:
    ```tsx
    <div className="flex items-center justify-between">
      <h1 className="text-xl font-semibold">Служба поддержки</h1>
      <SupportSyncButton />
    </div>
    ```
    Заменить на:
    ```tsx
    <div className="flex items-center justify-between">
      <h1 className="text-xl font-semibold">Служба поддержки</h1>
      <div className="flex items-center gap-2">
        <Link
          href="/support/new"
          className="inline-flex items-center h-9 rounded-md border px-3 text-sm hover:bg-muted"
        >
          <Plus className="w-4 h-4 mr-1" />
          Новый тикет
        </Link>
        <SupportSyncButton />
      </div>
    </div>
    ```
    Добавить импорты:
    ```typescript
    import Link from "next/link"
    import { Plus } from "lucide-react"
    ```

    **Шаг D — verification:**
    ```
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "app/(dashboard)/support/new/page.tsx" && test -f components/support/NewMessengerTicketForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "app/(dashboard)/support/new/page.tsx"` exit 0
    - `grep "requireSection(\"SUPPORT\", \"MANAGE\")" "app/(dashboard)/support/new/page.tsx"` находит
    - `test -f components/support/NewMessengerTicketForm.tsx` exit 0
    - `grep "use client" components/support/NewMessengerTicketForm.tsx` находит
    - `grep "zodResolver\\|useForm" components/support/NewMessengerTicketForm.tsx | wc -l` >= 2
    - `grep "createManualMessengerTicket" components/support/NewMessengerTicketForm.tsx` находит
    - `grep "<option value=\"TELEGRAM\"\\|<option value=\"WHATSAPP\"\\|<option value=\"OTHER\"" components/support/NewMessengerTicketForm.tsx | wc -l` >= 3
    - `grep "router.push(\`/support/\\\${" components/support/NewMessengerTicketForm.tsx` находит redirect
    - `grep "Новый тикет\\|/support/new" "app/(dashboard)/support/page.tsx"` находит кнопку
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    /support/new — RSC страница с RBAC MANAGE; NewMessengerTicketForm — client RHF+Zod с native select messengerType, валидирует все поля, вызывает createManualMessengerTicket, редиректит на созданный тикет; кнопка «+ Новый тикет» в шапке ленты. tsc проходит.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: MergeCustomerDialog + интеграция в профиль + MESSENGER hint в диалоге + messengerType бейдж в ленте</name>
  <files>
    components/support/customers/MergeCustomerDialog.tsx,
    app/(dashboard)/support/customers/[customerId]/page.tsx,
    app/(dashboard)/support/[ticketId]/page.tsx,
    components/support/SupportTicketCard.tsx
  </files>
  <read_first>
    - components/support/customers/LinkCustomerButton.tsx (Plan 12-02 — паттерн модалки с search existing Customer через searchCustomers)
    - components/ui/dialog.tsx (shadcn Dialog wrapper — переиспользуется)
    - app/(dashboard)/support/customers/[customerId]/page.tsx (Plan 12-02 — добавить MergeCustomerDialog рядом с NoteEditor)
    - app/(dashboard)/support/[ticketId]/page.tsx (conditional render ReplyPanel/ChatReplyPanel/ReturnActionsPanel — нужно добавить guard для MESSENGER чтобы все ТРИ были скрыты)
    - components/support/SupportTicketCard.tsx (Plan 12-02 — client component; расширить channel === 'MESSENGER' → показать messengerType бейдж)
    - app/actions/support.ts (mergeCustomers + searchCustomers сигнатуры)
    - .planning/phases/12-customer-messenger/12-RESEARCH.md (§Risk 3 — merge undo предупреждение)
  </read_first>
  <behavior>
    **MergeCustomerDialog (client):**
    - Props: `{ currentCustomerId: string, currentCustomerName: string | null, ticketsCount: number }`
    - Кнопка «Связать с другим покупателем» (иконка Merge из lucide)
    - Клик → Dialog:
      - Шаг 1 (search): input поиск + список results (searchCustomers), ИСКЛЮЧАЯ current customerId. При клике на target — переход на Шаг 2.
      - Шаг 2 (confirm): AlertDialog-like блок:
        - «Вы уверены? Профиль "{currentName}" будет **удалён**. {N} тикет(а) будет перенесено к "{targetName}". Заметка будет потеряна. **Операция необратима.**»
        - Кнопки «Отмена» / «Объединить»
      - При confirm → mergeCustomers({ sourceId: currentCustomerId, targetId }) → toast success с ticketsMoved → router.push(`/support/customers/${targetId}`)
    - Показывает state (isPending) через disabled кнопок.

    **Профиль customers/[id]/page.tsx integration:**
    - Импорт `MergeCustomerDialog`.
    - В aside ПОСЛЕ `<NoteEditor ... />` добавить:
      ```tsx
      <div className="rounded-lg border p-4">
        <MergeCustomerDialog
          currentCustomerId={customer.id}
          currentCustomerName={customer.name}
          ticketsCount={customer.tickets.length}
        />
      </div>
      ```

    **Диалог /support/[ticketId]/page.tsx MESSENGER conditional:**
    - Текущие conditional рендеры (упрощённо):
      - `canReply` (FEEDBACK|QUESTION) → ReplyPanel
      - `isChat` (CHAT) → ChatReplyPanel
      - `isReturn` (RETURN) → ReturnActionsPanel
      - fallback: "Канал не поддерживает ответ"
    - Phase 12: добавить переменную `isMessenger = channel === "MESSENGER"` и отдельный блок:
      ```tsx
      {isMessenger && (
        <div className="border-t p-3 text-xs text-muted-foreground text-center">
          Канал внешний — ответьте покупателю в{" "}
          {ticket.messengerType === "TELEGRAM" ? "Telegram" :
           ticket.messengerType === "WHATSAPP" ? "WhatsApp" : "мессенджере"}
          {ticket.messengerContact && <>: <code>{ticket.messengerContact}</code></>}
        </div>
      )}
      ```
    - Fallback ИСПРАВИТЬ: `{!canReply && !isChat && !isReturn && !isMessenger && <div>...</div>}` — чтобы не дублировать сообщение.
    - Добавить `messengerType` и `messengerContact` в select/include ticket выше (через expand существующего include с `messengerType: true, messengerContact: true`).

    **SupportTicketCard messengerType бейдж:**
    - В type `SupportTicketCardProps.ticket` добавить `messengerType: "TELEGRAM" | "WHATSAPP" | "OTHER" | null`
    - В JSX где рендерится `{channelLabelMap[ticket.channel]}` (под иконкой канала) — для MESSENGER показать двойной лейбл:
      ```tsx
      <span className="text-[10px] uppercase">
        {channelLabelMap[ticket.channel]}
      </span>
      {ticket.channel === "MESSENGER" && ticket.messengerType && (
        <span className="text-[9px] uppercase text-muted-foreground mt-0.5">
          {ticket.messengerType === "TELEGRAM" ? "TG" :
           ticket.messengerType === "WHATSAPP" ? "WA" : "Др."}
        </span>
      )}
      ```
    - Обновить также `/support/page.tsx` select (добавить `messengerType: true`).
  </behavior>
  <action>
    **Шаг A — создать `components/support/customers/MergeCustomerDialog.tsx`:**

    ```typescript
    "use client"

    import { useState, useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { Merge, AlertTriangle } from "lucide-react"
    import { toast } from "sonner"
    import { Button } from "@/components/ui/button"
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
    } from "@/components/ui/dialog"
    import {
      mergeCustomers,
      searchCustomers,
    } from "@/app/actions/support"

    interface SearchResult {
      id: string
      name: string | null
      phone: string | null
      wbUserId: string | null
    }

    type Step = "search" | "confirm"

    export function MergeCustomerDialog({
      currentCustomerId,
      currentCustomerName,
      ticketsCount,
    }: {
      currentCustomerId: string
      currentCustomerName: string | null
      ticketsCount: number
    }) {
      const router = useRouter()
      const [open, setOpen] = useState(false)
      const [step, setStep] = useState<Step>("search")
      const [query, setQuery] = useState("")
      const [results, setResults] = useState<SearchResult[]>([])
      const [target, setTarget] = useState<SearchResult | null>(null)
      const [isPending, startTransition] = useTransition()

      function onQueryChange(v: string) {
        setQuery(v)
        if (v.trim().length < 2) {
          setResults([])
          return
        }
        startTransition(async () => {
          const res = await searchCustomers(v)
          if (res.ok) {
            // Исключаем текущего покупателя из списка
            setResults(res.customers.filter((c) => c.id !== currentCustomerId))
          } else {
            toast.error(res.error)
          }
        })
      }

      function onPickTarget(c: SearchResult) {
        setTarget(c)
        setStep("confirm")
      }

      function onConfirmMerge() {
        if (!target) return
        startTransition(async () => {
          const res = await mergeCustomers({
            sourceId: currentCustomerId,
            targetId: target.id,
          })
          if (res.ok) {
            toast.success(
              res.ticketsMoved !== undefined
                ? `Перенесено ${res.ticketsMoved} тикет(ов)`
                : "Покупатели объединены"
            )
            setOpen(false)
            router.push(`/support/customers/${target.id}`)
          } else {
            toast.error(res.error)
          }
        })
      }

      function onClose() {
        setOpen(false)
        // Сбросим state только после анимации закрытия
        setTimeout(() => {
          setStep("search")
          setQuery("")
          setResults([])
          setTarget(null)
        }, 200)
      }

      return (
        <>
          <div className="space-y-2">
            <h3 className="text-sm font-semibold">Дубликат?</h3>
            <p className="text-xs text-muted-foreground">
              Если это тот же покупатель что в другом профиле — объедините.
            </p>
            <Button
              variant="outline"
              size="sm"
              onClick={() => setOpen(true)}
              className="w-full"
            >
              <Merge className="w-4 h-4 mr-1" />
              Связать с другим
            </Button>
          </div>

          <Dialog open={open} onOpenChange={(v) => (v ? setOpen(true) : onClose())}>
            <DialogContent className="max-w-md">
              <DialogHeader>
                <DialogTitle>
                  {step === "search" ? "Выбрать целевого покупателя" : "Подтвердите объединение"}
                </DialogTitle>
              </DialogHeader>

              {step === "search" && (
                <div className="space-y-2">
                  <p className="text-xs text-muted-foreground">
                    Все тикеты {currentCustomerName ? `"${currentCustomerName}"` : "этого покупателя"} будут перенесены к выбранному.
                  </p>
                  <input
                    type="text"
                    value={query}
                    onChange={(e) => onQueryChange(e.target.value)}
                    placeholder="Поиск по имени или телефону (от 2 символов)"
                    className="w-full rounded-md border px-3 py-2 text-sm"
                    autoFocus
                  />
                  <div className="max-h-[300px] overflow-y-auto">
                    {query.trim().length >= 2 && results.length === 0 && !isPending && (
                      <div className="text-sm text-muted-foreground py-4 text-center">
                        Не найдено (текущий покупатель исключён из списка)
                      </div>
                    )}
                    <ul className="space-y-1">
                      {results.map((c) => (
                        <li key={c.id}>
                          <button
                            type="button"
                            onClick={() => onPickTarget(c)}
                            disabled={isPending}
                            className="w-full text-left rounded border px-3 py-2 hover:bg-muted"
                          >
                            <div className="text-sm font-medium">
                              {c.name ?? "Без имени"}
                            </div>
                            {c.phone && (
                              <div className="text-xs text-muted-foreground">{c.phone}</div>
                            )}
                            {c.wbUserId && (
                              <div className="text-[10px] text-muted-foreground font-mono">
                                {c.wbUserId}
                              </div>
                            )}
                          </button>
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>
              )}

              {step === "confirm" && target && (
                <div className="space-y-3">
                  <div className="rounded-md border border-amber-300 bg-amber-50 dark:bg-amber-900/20 p-3 flex gap-2">
                    <AlertTriangle className="w-5 h-5 text-amber-600 shrink-0 mt-0.5" />
                    <div className="text-sm space-y-2">
                      <p className="font-medium">Операция необратима.</p>
                      <ul className="text-xs space-y-1">
                        <li>
                          Профиль <strong>{currentCustomerName ?? "текущего покупателя"}</strong> будет удалён
                        </li>
                        <li>
                          <strong>{ticketsCount}</strong> тикет(ов) перенесётся к{" "}
                          <strong>{target.name ?? "Без имени"}</strong>
                        </li>
                        <li>Внутренняя заметка будет потеряна</li>
                      </ul>
                    </div>
                  </div>
                  <div className="flex justify-end gap-2 pt-2">
                    <Button
                      variant="outline"
                      onClick={() => setStep("search")}
                      disabled={isPending}
                    >
                      Назад
                    </Button>
                    <Button
                      variant="destructive"
                      onClick={onConfirmMerge}
                      disabled={isPending}
                    >
                      {isPending ? "Объединение..." : "Объединить"}
                    </Button>
                  </div>
                </div>
              )}
            </DialogContent>
          </Dialog>
        </>
      )
    }
    ```

    **Шаг B — обновить `app/(dashboard)/support/customers/[customerId]/page.tsx`** — добавить MergeCustomerDialog в aside:

    Найти блок в `<aside>` после `<NoteEditor .../>`:
    ```tsx
    </aside>
    ```
    Перед закрытием aside добавить:
    ```tsx
    <div className="rounded-lg border p-4">
      <MergeCustomerDialog
        currentCustomerId={customer.id}
        currentCustomerName={customer.name}
        ticketsCount={customer.tickets.length}
      />
    </div>
    ```

    Добавить импорт:
    ```typescript
    import { MergeCustomerDialog } from "@/components/support/customers/MergeCustomerDialog"
    ```

    **Шаг C — обновить `app/(dashboard)/support/[ticketId]/page.tsx`** — MESSENGER conditional:

    1. В `prisma.supportTicket.findUnique` include/select — добавить (если используется plain include, добавить в ticket scalar defaults; если select — явно):

       Проверить: текущий код использует `include: { customer, assignedTo, messages, appealRecord }` — scalar поля (включая messengerType/messengerContact) возвращаются автоматически. Если нужен explicit select — добавить поля. После prisma generate (next build) новые поля появятся в типе автоматически.

    2. Добавить переменную:
       ```typescript
       const isMessenger = ticket.channel === "MESSENGER"
       ```

    3. В JSX центральной колонки перед fallback `!canReply && !isChat && !isReturn`:
       ```tsx
       {isMessenger && (
         <div className="border-t p-3 text-xs text-muted-foreground text-center">
           Канал внешний — ответьте покупателю в{" "}
           {ticket.messengerType === "TELEGRAM"
             ? "Telegram"
             : ticket.messengerType === "WHATSAPP"
             ? "WhatsApp"
             : "мессенджере"}
           {ticket.messengerContact && (
             <>: <code className="bg-muted px-1 rounded">{ticket.messengerContact}</code></>
           )}
         </div>
       )}
       {!canReply && !isChat && !isReturn && !isMessenger && (
         <div className="border-t p-3 text-xs text-muted-foreground text-center">
           Канал «{ticket.channel}» не поддерживает ответ через интерфейс.
         </div>
       )}
       ```

    **Шаг D — обновить `components/support/SupportTicketCard.tsx`** — добавить messengerType бейдж:

    1. Добавить в `SupportTicketCardProps.ticket` новое поле:
       ```typescript
       messengerType: "TELEGRAM" | "WHATSAPP" | "OTHER" | null
       ```

    2. В блоке иконка канала (flex-shrink-0) заменить:
       ```tsx
       <div className="flex-shrink-0 flex flex-col items-center gap-1 text-muted-foreground">
         <Icon className="w-5 h-5" />
         <span className="text-[10px] uppercase">
           {channelLabelMap[ticket.channel]}
         </span>
         {ticket.channel === "MESSENGER" && ticket.messengerType && (
           <span className="text-[9px] uppercase text-muted-foreground font-medium">
             {ticket.messengerType === "TELEGRAM"
               ? "Tg"
               : ticket.messengerType === "WHATSAPP"
               ? "Wa"
               : "Др"}
           </span>
         )}
       </div>
       ```

    **Шаг E — обновить `app/(dashboard)/support/page.tsx`** select:

    В `prisma.supportTicket.findMany({ select: { ... } })` добавить:
    ```typescript
    messengerType: true,
    ```

    И передать в SupportTicketCard ticket prop (уже автоматически, т.к. spread).

    **Шаг F — verification:**
    ```
    npx tsc --noEmit
    npm run test
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test && test -f components/support/customers/MergeCustomerDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/customers/MergeCustomerDialog.tsx` exit 0
    - `grep "export function MergeCustomerDialog" components/support/customers/MergeCustomerDialog.tsx` находит
    - `grep "mergeCustomers\\|searchCustomers" components/support/customers/MergeCustomerDialog.tsx | wc -l` >= 2
    - `grep "необратим\\|удалён\\|удален" components/support/customers/MergeCustomerDialog.tsx` находит warning
    - `grep "MergeCustomerDialog" "app/(dashboard)/support/customers/[customerId]/page.tsx"` находит import+render
    - `grep "isMessenger\\|MESSENGER\\|messengerType" "app/(dashboard)/support/[ticketId]/page.tsx" | wc -l` >= 3
    - `grep "!canReply && !isChat && !isReturn && !isMessenger" "app/(dashboard)/support/[ticketId]/page.tsx"` находит правильный fallback
    - `grep "messengerType" components/support/SupportTicketCard.tsx | wc -l` >= 2
    - `grep "messengerType: true\\|messengerType:true" "app/(dashboard)/support/page.tsx"` находит select
    - `npx tsc --noEmit` exit 0
    - `npm run test` exit 0 (регрессия clean)
    - `npm run build` exit 0 (следующий шаг — deploy требует build clean)
  </acceptance_criteria>
  <done>
    MergeCustomerDialog — 2-шаговая модалка (search → confirm) с alerting warning; профиль /support/customers/[id] содержит блок MergeCustomerDialog в aside; /support/[ticketId] для MESSENGER скрывает все reply panels и показывает hint с контактом; SupportTicketCard показывает Tg/Wa/Др бейдж для MESSENGER; /support лента select включает messengerType; tsc + test + build проходят.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 3: Deploy Phase 12 на VPS + Human UAT (7 пунктов)</name>
  <files>
    deploy.sh,
    prisma/migrations/20260418_phase12_customer_messenger/migration.sql
  </files>
  <what-built>
    - Phase 12 полный комплект: миграция + syncChats auto-upsert Customer + 6 server actions (link/createForTicket/updateNote/merge/manualMessenger/searchCustomers) + lib/customer-aggregations + RSC страница профиля /support/customers/[id] + 7 новых клиентских компонентов + форма /support/new + MESSENGER hint в диалоге + messengerType бейдж в ленте
    - Все unit tests GREEN: customer-sync-chat (4+), customer-actions (8+), messenger-ticket (5+), merge-customers (5+), customer-profile-page (10+) — суммарно 30+ новых тестов
    - SQL backfill существующих CHAT тикетов (customerId=NULL → create Customer через `chat:` namespacing + set customerId)
  </what-built>
  <how-to-verify>
    **Перед UAT — execute-phase выполнит deploy:**

    1. Commit всех изменений Plan 12-01/02/03 (автоматически после предыдущих задач)
    2. На VPS: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — прогнать migration deploy + rebuild + systemctl restart
    3. Проверить статус: `ssh root@85.198.97.89 "systemctl status zoiten-erp.service"` — active (running)
    4. Проверить применение миграции: `ssh root@85.198.97.89 "sudo -u postgres psql zoiten_erp -c \\\"SELECT typname FROM pg_type WHERE typname = 'MessengerType';\\\""` → MessengerType exists
    5. Проверить SQL backfill применился: `ssh root@85.198.97.89 "sudo -u postgres psql zoiten_erp -c \\\"SELECT COUNT(*) FROM \\\\\\\"Customer\\\\\\\" WHERE \\\\\\\"wbUserId\\\\\\\" LIKE 'chat:%';\\\""` → число N > 0 (если были CHAT тикеты)
    6. `curl -sI https://zoiten.pro/support/customers/test` → 307/200 (redirect login если не залогинен)

    **Human UAT checklist (7 пунктов, критичные для Phase 12):**

    Подготовка:
    - [ ] Залогинен суперадмином (sergey.fyodorov@gmail.com) с SUPPORT+MANAGE
    - [ ] Хотя бы один существующий CHAT тикет в /support для проверки auto-linking

    **Пункт 1 — Customer auto-create для CHAT при sync (SUP-32):**
    - [ ] Открыть `/support` → найти CHAT тикет → клик → в диалоге справа видна секция «Покупатель» с именем (Link на профиль)
    - [ ] Клик на имя → `/support/customers/{id}` открывается → Customer.wbUserId начинается с `chat:`
    - [ ] (Auto-sync проверка) через 5 минут `journalctl -u zoiten-erp.service -f | grep -i "syncChats"` → без ошибок, Customer продолжают быть auto-linked

    **Пункт 2 — Manual link Customer для FEEDBACK/QUESTION/RETURN (SUP-32 manual):**
    - [ ] Найти FEEDBACK или QUESTION тикет с `customerId=null` → открыть диалог
    - [ ] В TicketSidePanel в блоке «Покупатель» видна кнопка «Связать с покупателем» (UserPlus icon)
    - [ ] Клик → модалка с табами «Найти существующего» / «Создать нового»
    - [ ] Mode "Найти существующего": поиск «И» → появляется список существующих Customer → клик → toast «Покупатель связан» → TicketSidePanel показывает Link на профиль
    - [ ] ⚠ Открыть другой ticket с `customerId=null` → нажать «Связать» → mode «Создать нового» → name «Тест UAT» + phone «+79990000001» → «Создать и связать» → toast «Покупатель создан и связан» → ticket показывает Link

    **Пункт 3 — Manual MESSENGER ticket create (SUP-34):**
    - [ ] В `/support` шапка содержит кнопку «+ Новый тикет» справа от «Синхронизировать»
    - [ ] Клик → `/support/new` открывается
    - [ ] Форма: выбрать «Telegram», customerName «UAT Тест», messengerContact «@uat_test», nmId пустой, text «Тестовое обращение через Telegram»
    - [ ] Submit → redirect на `/support/[новый_ticket_id]`
    - [ ] Диалог нового тикета: channel = MESSENGER, ReplyPanel/ChatReplyPanel/ReturnActionsPanel НЕ рендерятся
    - [ ] Вместо них видна плашка «Канал внешний — ответьте покупателю в Telegram: `@uat_test`»
    - [ ] В ленте `/support` новый тикет виден с иконкой «Мессенджер» + бейдж «Tg» под ней
    - [ ] В TicketSidePanel customer = «UAT Тест» как Link

    **Пункт 4 — Merge дубликатов (SUP-35):**
    - [ ] Создать второго Customer через «Связать с другим» → «Создать нового» → name «UAT Дубликат» (в любом тикете)
    - [ ] Открыть профиль первого UAT Customer (из пункта 2/3) — `/support/customers/{id}`
    - [ ] В aside ПОСЛЕ блока «Заметка» — блок «Дубликат?» с кнопкой «Связать с другим»
    - [ ] Клик → модалка → поиск «UAT» → видно «UAT Дубликат» (current UAT Тест исключён из списка)
    - [ ] Клик на «UAT Дубликат» → Шаг 2: warning «Операция необратима, профиль UAT Тест будет удалён, N тикет(ов) перенесётся к UAT Дубликат»
    - [ ] «Объединить» → toast «Перенесено N тикет(ов)» → redirect на `/support/customers/{дубликата}`
    - [ ] В профиле UAT Дубликат видны перенесённые тикеты
    - [ ] Попытка перейти на старый URL `/support/customers/{id UAT Тест}` → 404 (Customer удалён)

    **Пункт 5 — ReplyPanel для MESSENGER скрыт (SUP-34 contract):**
    - [ ] Открыть MESSENGER тикет (создан в пункте 3 или merge-перенесённый) → диалог
    - [ ] В центральной колонке под хронологией сообщений: **нет** textarea ответа
    - [ ] **Нет** кнопки «Отправить», «Шаблон», «Обжаловать»
    - [ ] Есть только плашка с указанием канала (Telegram/WhatsApp/мессенджере) + `messengerContact`

    **Пункт 6 — Профиль aggregates корректны (SUP-33):**
    - [ ] Открыть `/support/customers/{UAT Дубликат}` (после merge)
    - [ ] CustomerInfoCard: имя, phone (если есть), createdAt в Moscow TZ
    - [ ] ChannelStats: 5 каналов с числами, avg rating блок если есть FEEDBACK с rating
    - [ ] NoteEditor: ввести «UAT проверка заметки» → через 500ms toast «Заметка сохранена» → refresh page → текст сохранился
    - [ ] TicketsTable: все тикеты в DESC по createdAt, клик → открывает соответствующий тикет
    - [ ] Counts в ChannelStats = длине списка tickets в TicketsTable

    **Пункт 7 — Регрессия Phase 7/8/9/10/11:**
    - [ ] `/support` лента работает как раньше, фильтры не сломались
    - [ ] FEEDBACK тикет открывается, ReplyPanel с textarea + Шаблон + Отправить + Обжаловать (если FEEDBACK)
    - [ ] CHAT тикет открывается, ChatReplyPanel работает
    - [ ] RETURN тикет открывается, ReturnActionsPanel работает
    - [ ] `/support/returns` таблица работает
    - [ ] `/support/templates` + создание/редактирование работают
    - [ ] `/support/auto-reply` работает
    - [ ] `/prices/wb` работает
    - [ ] `/products` работает, фото открываются

    **Cleanup (необязательно — можно оставить тестовые Customer):**
    - Удалить тестовых Customer «UAT Тест»/«UAT Дубликат» можно через SQL: `DELETE FROM "Customer" WHERE name LIKE 'UAT %'` (tickets останутся с customerId=NULL через onDelete:SetNull).
  </how-to-verify>
  <resume-signal>
    Пользователь отправляет:
    - "approved" — все 7 пунктов OK, Phase 12 complete, ROADMAP обновить
    - "issues: [список проблем]" — зафиксировать gap-ы для `--gaps` replan
    - "partial: [что работает / что нет]" — создать gap-closure plan
  </resume-signal>
  <action>
    Deploy и human UAT частично автоматизируются. execute-phase делает:

    1. git commit (автоматически после задач 1-2).
    2. Deploy на VPS:
       ```bash
       ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
       ```
       deploy.sh выполнит `git pull && npm ci && npm run build && npx prisma migrate deploy && systemctl restart zoiten-erp`.
    3. Verify migration applied:
       ```bash
       ssh root@85.198.97.89 "sudo -u postgres psql zoiten_erp -c \"SELECT typname FROM pg_type WHERE typname = 'MessengerType';\""
       ```
    4. Verify service active:
       ```bash
       ssh root@85.198.97.89 "systemctl is-active zoiten-erp.service"
       ```
    5. Вывести пользователю UAT checklist из <how-to-verify>.
    6. Ждать resume-signal.

    **Если deploy упадёт** на миграции (например из-за backfill ошибки на production данных):
    - Проверить логи: `ssh root@85.198.97.89 "journalctl -u zoiten-erp.service -n 100"`
    - Если `gen_random_uuid()` не доступен в текущей БД: применить `CREATE EXTENSION IF NOT EXISTS pgcrypto;` через psql, повторить `npx prisma migrate deploy`.
    - Если backfill конфликтует (existing Customer с таким же `chat:...` wbUserId): ON CONFLICT уже обработан, но если лог показывает другую ошибку — исследовать перед переходом к UAT.
  </action>
  <verify>
    <automated>ssh root@85.198.97.89 "systemctl is-active zoiten-erp.service"</automated>
  </verify>
  <done>
    Deploy пройден (service active, миграция применена, enum MessengerType существует в БД), пользователь отправил "approved" после 7 пунктов UAT, либо зафиксировал gap-ы для replan. При "approved" — Phase 12 марк COMPLETE в ROADMAP.md + SUP-32/SUP-33/SUP-34/SUP-35 → Complete в REQUIREMENTS.md traceability table.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач (включая human UAT):
1. `npx prisma validate` exit 0
2. `npx tsc --noEmit` exit 0
3. `npm run build` exit 0
4. `npm run test` exit 0 (полная suite, Phase 7-12 GREEN)
5. `grep -c "export async function" app/actions/support.ts` >= 14 (Phase 8 6-7 + Phase 9 3 + Phase 12 5 + searchCustomers из 12-02)
6. `ls components/support/customers/` содержит 7 файлов: CustomerInfoCard, ChannelStats, NoteEditor, TicketsTable, LinkCustomerButton, MergeCustomerDialog + возможно index
7. Human UAT passed (resume-signal "approved")
8. VPS: `systemctl status zoiten-erp.service` = active, migration 20260418_phase12_customer_messenger применена
</verification>

<success_criteria>
Plan 12-03 завершён когда:
- /support/new страница + NewMessengerTicketForm работают — создают MESSENGER тикет с INBOUND message и опциональным Customer
- Кнопка «+ Новый тикет» в шапке ленты → /support/new
- MergeCustomerDialog 2-шаговая модалка (search → confirm warning) в профиле покупателя
- /support/[ticketId] для MESSENGER скрывает все reply panels и показывает hint
- SupportTicketCard показывает Tg/Wa/Др бейдж для MESSENGER под channelLabel
- Deploy на VPS прошёл: миграция 20260418_phase12_customer_messenger применена, service restarted
- Human UAT passed: 7 пунктов проверены (Chat auto-link, manual link, MESSENGER create, merge, ReplyPanel скрыт, профиль, регрессия)
- Phase 12 COMPLETE → ROADMAP.md обновлён, REQUIREMENTS.md traceability SUP-32/33/34/35 → Complete
- Commit `feat(12-03): /support/new форма MESSENGER + MergeCustomerDialog + MESSENGER hint + deploy`
</success_criteria>

<output>
После завершения создать `.planning/phases/12-customer-messenger/12-03-SUMMARY.md`:
- Новая RSC страница /support/new — описание формы
- NewMessengerTicketForm — RHF+Zod форма контракт
- MergeCustomerDialog — 2-шаговый flow (search → confirm)
- Изменения /support/[ticketId] (MESSENGER hint)
- Изменения SupportTicketCard (messengerType бейдж)
- Deploy info: миграция 20260418_phase12_customer_messenger применена на VPS, service активен
- UAT results: 7 пунктов покрыты, результаты
- Phase 12 COMPLETE marker:
  - ROADMAP.md Phase 12 → Complete, `- [x] Phase 12` в списке v1.1 phases
  - REQUIREMENTS.md SUP-32 (hybrid), SUP-33, SUP-34, SUP-35 → Complete в traceability
  - Progress table: 12. Профиль покупателя + Мессенджеры → 3/3 Complete
- Deferred (v1.2+): Sidebar «Покупатели» список, AutoMerge fuzzy, Merge undo, Messenger media upload
- Validation Map update: Plan 12-03 tasks GREEN (UAT выполнен)
</output>
