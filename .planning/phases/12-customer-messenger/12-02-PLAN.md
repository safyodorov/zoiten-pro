---
phase: 12-customer-messenger
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - app/(dashboard)/support/customers/[customerId]/page.tsx
  - components/support/customers/CustomerInfoCard.tsx
  - components/support/customers/ChannelStats.tsx
  - components/support/customers/NoteEditor.tsx
  - components/support/customers/TicketsTable.tsx
  - components/support/customers/LinkCustomerButton.tsx
  - app/(dashboard)/support/[ticketId]/page.tsx
  - components/support/TicketSidePanel.tsx
  - components/support/SupportTicketCard.tsx
  - tests/customer-profile-page.test.ts
autonomous: true
requirements:
  - SUP-32
  - SUP-33
must_haves:
  truths:
    - "app/(dashboard)/support/customers/[customerId]/page.tsx — RSC с requireSection('SUPPORT'), загружает Customer.tickets (include messages[0], assignedTo), notFound() при отсутствии, 2-колоночный grid layout"
    - "CustomerInfoCard отображает: имя (fallback 'Покупатель'), phone (если есть), wbUserId с badge 'chat:' при наличии префикса, createdAt в Europe/Moscow"
    - "ChannelStats использует lib/customer-aggregations.countTicketsByChannel + averageFeedbackRating; badges для 5 каналов (FEEDBACK/QUESTION/CHAT/RETURN/MESSENGER) + avg rating блок для FEEDBACK"
    - "NoteEditor — debounced client textarea (500ms), вызывает updateCustomerNote через useTransition, toast на success/error. Lost-update disclaimer в tooltip"
    - "TicketsTable — client таблица (канал с иконкой, дата, nmId, превью ticket.previewText, статус badge, ссылка 'Открыть /support/[id]'); DESC по createdAt; rowClick → Link"
    - "LinkCustomerButton (в TicketSidePanel) — рендерится ТОЛЬКО если customerId=null И channel !== 'CHAT'. Открывает модалку: native input поиск existing Customer (debounced server action searchCustomers) + «Создать нового» inline форма (name+phone)"
    - "SupportTicketCard: имя покупателя кликабельно — Link на /support/customers/[id] если ticket.customerId есть; sticky click остановлен preventDefault для вложенного Link"
    - "TicketSidePanel: если customerId существует — показывает имя Customer из join; если channel !== 'CHAT' и customerId=null — рендерит LinkCustomerButton"
    - "tests/customer-profile-page.test.ts расширен (поверх Plan 12-01 stubs) — 5+ тестов покрывают page-level behavior через helper exports"
    - "Страница /support/customers/[customerId] использует force-dynamic (revalidatePath работает), RBAC requireSection('SUPPORT') для read, RSC загружает Customer + tickets в одном запросе"
  artifacts:
    - path: "app/(dashboard)/support/customers/[customerId]/page.tsx"
      provides: "RSC страница профиля покупателя — agregates + tickets table"
      contains: "requireSection"
    - path: "components/support/customers/CustomerInfoCard.tsx"
      provides: "Блок имя/phone/wbUserId/createdAt"
      exports: ["CustomerInfoCard"]
    - path: "components/support/customers/ChannelStats.tsx"
      provides: "Счётчики тикетов по каналам + средний рейтинг"
      exports: ["ChannelStats"]
    - path: "components/support/customers/NoteEditor.tsx"
      provides: "Debounced textarea для Customer.note"
      exports: ["NoteEditor"]
    - path: "components/support/customers/TicketsTable.tsx"
      provides: "Список тикетов покупателя с иконкой канала + ссылка"
      exports: ["TicketsTable"]
    - path: "components/support/customers/LinkCustomerButton.tsx"
      provides: "Client кнопка + модалка связывания тикета с Customer"
      exports: ["LinkCustomerButton"]
    - path: "components/support/TicketSidePanel.tsx"
      provides: "Расширенная TicketSidePanel с интеграцией LinkCustomerButton и отображением customer.name"
      contains: "LinkCustomerButton"
    - path: "components/support/SupportTicketCard.tsx"
      provides: "Карточка тикета с кликабельным именем покупателя"
      contains: "customers/"
  key_links:
    - from: "app/(dashboard)/support/customers/[customerId]/page.tsx"
      to: "lib/customer-aggregations.ts"
      via: "import countTicketsByChannel + averageFeedbackRating для RSC aggregation"
      pattern: "customer-aggregations"
    - from: "components/support/customers/NoteEditor.tsx"
      to: "app/actions/support.ts updateCustomerNote"
      via: "debounced useEffect + useTransition"
      pattern: "updateCustomerNote"
    - from: "components/support/customers/LinkCustomerButton.tsx"
      to: "app/actions/support.ts linkTicketToCustomer + createCustomerForTicket"
      via: "onClick → server action + toast"
      pattern: "linkTicketToCustomer\\|createCustomerForTicket"
    - from: "components/support/TicketSidePanel.tsx"
      to: "components/support/customers/LinkCustomerButton"
      via: "conditional render — customerId null && channel !== CHAT"
      pattern: "LinkCustomerButton"
    - from: "components/support/SupportTicketCard.tsx"
      to: "/support/customers/[id]"
      via: "Link href если ticket.customer.id доступен"
      pattern: "customers/"
    - from: "app/(dashboard)/support/[ticketId]/page.tsx"
      to: "TicketSidePanel"
      via: "передаёт customerId + channel в props"
      pattern: "customerId"
---

<objective>
Phase 12 Plan 02 — UI слой профиля покупателя + линковка:

1. **RSC `/support/customers/[customerId]/page.tsx`** — Customer + tickets load (с messages.take:1 для preview, assignedTo), агрегаты через `lib/customer-aggregations.ts`, 2-колоночный layout (left: CustomerInfoCard + ChannelStats + NoteEditor; right: TicketsTable).
2. **5 клиентских компонента** в `components/support/customers/`:
   - `CustomerInfoCard` — name/phone/wbUserId/createdAt
   - `ChannelStats` — 5 badges + avg rating
   - `NoteEditor` — debounced save
   - `TicketsTable` — хронология тикетов
   - `LinkCustomerButton` — модалка для TicketSidePanel
3. **Модификация существующих компонентов**:
   - `TicketSidePanel.tsx` — добавить customerId, channel props; рендерить LinkCustomerButton если `customerId===null && channel !== 'CHAT'`; показывать customer.name в карточке
   - `SupportTicketCard.tsx` — customer.id → Link в имени покупателя (внутри Link.href на /support/[id]) → вложенный Link конфликтует. Решение: переработать — имя покупателя → обычный <span>, а Link на profile показывать отдельно через icon-button
4. **`[ticketId]/page.tsx`** — передать `ticket.customerId` и `ticket.channel` в TicketSidePanel.
5. **Расширить `tests/customer-profile-page.test.ts`** — добавить сценарии со всеми каналами + пустой Customer.

Purpose: Менеджер открывает `/support/customers/[id]`, видит всю историю покупателя, редактирует внутреннюю заметку. Для FEEDBACK/QUESTION/RETURN тикетов без Customer — в диалоге тикета кнопка «Связать с покупателем» открывает модалку выбора/создания.

Output: 1 новая RSC страница, 5 новых клиентских компонента, 3 модифицированных файла, 1 расширенный test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-customer-messenger/12-RESEARCH.md
@.planning/phases/12-customer-messenger/12-VALIDATION.md
@.planning/phases/12-customer-messenger/12-01-PLAN.md
@.planning/phases/11-templates-appeals/11-03-PLAN.md
@CLAUDE.md
@app/(dashboard)/support/[ticketId]/page.tsx
@app/(dashboard)/support/page.tsx
@components/support/TicketSidePanel.tsx
@components/support/SupportTicketCard.tsx
@components/ui/dialog.tsx
@lib/customer-aggregations.ts
@lib/prisma.ts
@lib/rbac.ts

<interfaces>
**Из Plan 12-01 (lib/customer-aggregations.ts):**
```typescript
export interface TicketForAggregation { channel: TicketChannel; rating: number | null }
export function countTicketsByChannel(tickets: TicketForAggregation[]): Record<TicketChannel, number>
export function averageFeedbackRating(tickets: TicketForAggregation[]): number | null
```

**Из Plan 12-01 (app/actions/support.ts):**
```typescript
export async function linkTicketToCustomer(ticketId: string, customerId: string): Promise<ActionResult>
export async function createCustomerForTicket(ticketId: string, input: { name: string; phone?: string | null }): Promise<ActionResult & { customerId?: string }>
export async function updateCustomerNote(customerId: string, note: string): Promise<ActionResult>
```

**Prisma Customer (существующая модель schema.prisma):**
```prisma
model Customer {
  id        String          @id @default(cuid())
  wbUserId  String?         @unique       // 'chat:...' или настоящий wbUserId
  phone     String?
  name      String?
  note      String?         @db.Text
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt
  tickets   SupportTicket[]
}
```

**Паттерн debounced save** (из components/prices/GlobalRatesBar.tsx Phase 7):
```typescript
const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
function onChange(value: string) {
  setLocalValue(value)
  if (timerRef.current) clearTimeout(timerRef.current)
  timerRef.current = setTimeout(() => {
    startTransition(async () => {
      const res = await saveAction(value)
      if (res.ok) toast.success("Сохранено")
      else toast.error(res.error)
    })
  }, 500)
}
```

**Паттерн TicketSidePanel** (components/support/TicketSidePanel.tsx) — уже принимает channel, status, assignedToId, users, дат. Phase 12 расширение: добавить props `customerId: string | null`, `customerName: string | null`, `ticketId` уже есть.

**Существующий layout [ticketId]/page.tsx** (строка 132+):
```tsx
<div className="flex-1 grid grid-cols-1 lg:grid-cols-[280px_1fr_280px] min-h-0">
  <aside>... Покупатель + Товар ...</aside>
  <div>... SupportDialog + ReplyPanel/etc ...</div>
  <div><TicketSidePanel ... /></div>
</div>
```
В aside уже отображается `ticket.customer?.name ?? "Покупатель"` (строка 138). Phase 12 — добавить Link если есть customer.id.

**Паттерн RSC page с searchParams и requireSection** (из app/(dashboard)/support/templates/page.tsx Phase 11):
```typescript
export const dynamic = "force-dynamic"
export default async function Page({ params }: { params: Promise<{ customerId: string }> }) {
  await requireSection("SUPPORT")
  const { customerId } = await params
  const data = await prisma.customer.findUnique(...)
  if (!data) notFound()
  return <...>
}
```

**shadcn Dialog** (components/ui/dialog.tsx — base-ui wrapper):
```tsx
<Dialog open={open} onOpenChange={setOpen}>
  <DialogContent>
    <DialogHeader><DialogTitle>...</DialogTitle></DialogHeader>
    ...
  </DialogContent>
</Dialog>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: RSC страница /support/customers/[customerId] + 4 базовых компонента</name>
  <files>
    app/(dashboard)/support/customers/[customerId]/page.tsx,
    components/support/customers/CustomerInfoCard.tsx,
    components/support/customers/ChannelStats.tsx,
    components/support/customers/NoteEditor.tsx,
    components/support/customers/TicketsTable.tsx
  </files>
  <read_first>
    - lib/customer-aggregations.ts (Plan 12-01 — countTicketsByChannel + averageFeedbackRating)
    - components/support/SupportTicketCard.tsx (channelIconMap + channelLabelMap + statusBorderMap — переиспользовать mapping)
    - app/(dashboard)/support/[ticketId]/page.tsx (паттерн Prisma include для customer+tickets+messages)
    - app/(dashboard)/support/templates/page.tsx (паттерн RSC page + requireSection + force-dynamic)
    - components/prices/GlobalRatesBar.tsx (паттерн debounced save через useRef + useTransition)
    - CLAUDE.md (русский, Moscow timezone, native select)
    - .planning/phases/12-customer-messenger/12-RESEARCH.md (§Customer profile page layout строки 411-459)
  </read_first>
  <action>
    **Шаг A — создать `app/(dashboard)/support/customers/[customerId]/page.tsx` (RSC):**

    ```typescript
    import { notFound } from "next/navigation"
    import Link from "next/link"
    import { ChevronLeft } from "lucide-react"
    import { requireSection } from "@/lib/rbac"
    import { prisma } from "@/lib/prisma"
    import {
      countTicketsByChannel,
      averageFeedbackRating,
    } from "@/lib/customer-aggregations"
    import { CustomerInfoCard } from "@/components/support/customers/CustomerInfoCard"
    import { ChannelStats } from "@/components/support/customers/ChannelStats"
    import { NoteEditor } from "@/components/support/customers/NoteEditor"
    import { TicketsTable } from "@/components/support/customers/TicketsTable"

    export const dynamic = "force-dynamic"

    export default async function CustomerProfilePage({
      params,
    }: {
      params: Promise<{ customerId: string }>
    }) {
      await requireSection("SUPPORT")
      const { customerId } = await params

      const customer = await prisma.customer.findUnique({
        where: { id: customerId },
        include: {
          tickets: {
            orderBy: { createdAt: "desc" },
            select: {
              id: true,
              channel: true,
              status: true,
              nmId: true,
              rating: true,
              previewText: true,
              createdAt: true,
              assignedTo: {
                select: { id: true, name: true, firstName: true, lastName: true },
              },
            },
          },
        },
      })
      if (!customer) notFound()

      const byChannel = countTicketsByChannel(
        customer.tickets.map((t) => ({ channel: t.channel, rating: t.rating }))
      )
      const avgRating = averageFeedbackRating(
        customer.tickets.map((t) => ({ channel: t.channel, rating: t.rating }))
      )

      return (
        <div className="p-6 space-y-4">
          <div className="flex items-center justify-between">
            <Link
              href="/support"
              className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground"
            >
              <ChevronLeft className="h-4 w-4 mr-1" />
              Назад к ленте
            </Link>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-[320px_1fr] gap-6">
            <aside className="space-y-4">
              <CustomerInfoCard
                name={customer.name}
                phone={customer.phone}
                wbUserId={customer.wbUserId}
                createdAt={customer.createdAt}
              />
              <ChannelStats byChannel={byChannel} avgRating={avgRating} />
              <NoteEditor
                customerId={customer.id}
                initialNote={customer.note ?? ""}
              />
            </aside>
            <section>
              <h2 className="text-lg font-semibold mb-3">
                История обращений ({customer.tickets.length})
              </h2>
              <TicketsTable tickets={customer.tickets} />
            </section>
          </div>
        </div>
      )
    }
    ```

    **Шаг B — создать `components/support/customers/CustomerInfoCard.tsx`:**

    ```typescript
    // Server component — чистый рендер, без интерактивности.
    import { User, Phone, Calendar, Hash } from "lucide-react"

    export interface CustomerInfoCardProps {
      name: string | null
      phone: string | null
      wbUserId: string | null
      createdAt: Date
    }

    function formatDate(d: Date): string {
      return new Intl.DateTimeFormat("ru-RU", {
        day: "2-digit", month: "2-digit", year: "numeric",
        hour: "2-digit", minute: "2-digit",
        timeZone: "Europe/Moscow",
      }).format(d)
    }

    export function CustomerInfoCard({ name, phone, wbUserId, createdAt }: CustomerInfoCardProps) {
      const isChatCustomer = wbUserId?.startsWith("chat:") ?? false
      return (
        <section className="rounded-lg border p-4 space-y-2">
          <h2 className="text-base font-semibold flex items-center gap-2">
            <User className="w-4 h-4" />
            {name ?? "Покупатель"}
          </h2>
          {phone && (
            <div className="flex items-center gap-2 text-sm text-muted-foreground">
              <Phone className="w-3.5 h-3.5" />
              <span>{phone}</span>
            </div>
          )}
          {wbUserId && (
            <div className="flex items-center gap-2 text-xs text-muted-foreground">
              <Hash className="w-3.5 h-3.5" />
              <code className="bg-muted px-1 rounded">{wbUserId}</code>
              {isChatCustomer && (
                <span className="text-[10px] uppercase bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-100 px-1 rounded">
                  WB Chat
                </span>
              )}
            </div>
          )}
          <div className="flex items-center gap-2 text-xs text-muted-foreground">
            <Calendar className="w-3.5 h-3.5" />
            <span>Создан {formatDate(createdAt)}</span>
          </div>
        </section>
      )
    }
    ```

    **Шаг C — создать `components/support/customers/ChannelStats.tsx`:**

    ```typescript
    // Server component.
    import type { TicketChannel } from "@prisma/client"
    import { MessageSquare, HelpCircle, MessageCircle, RotateCw, Inbox, Star } from "lucide-react"

    export interface ChannelStatsProps {
      byChannel: Record<TicketChannel, number>
      avgRating: number | null
    }

    const CHANNEL_CONFIG: Record<TicketChannel, { label: string; Icon: typeof MessageSquare }> = {
      FEEDBACK: { label: "Отзывы", Icon: MessageSquare },
      QUESTION: { label: "Вопросы", Icon: HelpCircle },
      CHAT: { label: "Чаты", Icon: MessageCircle },
      RETURN: { label: "Возвраты", Icon: RotateCw },
      MESSENGER: { label: "Мессенджер", Icon: Inbox },
    }

    export function ChannelStats({ byChannel, avgRating }: ChannelStatsProps) {
      const channels: TicketChannel[] = ["FEEDBACK", "QUESTION", "CHAT", "RETURN", "MESSENGER"]
      return (
        <section className="rounded-lg border p-4 space-y-3">
          <h3 className="text-sm font-semibold">По каналам</h3>
          <ul className="space-y-1">
            {channels.map((ch) => {
              const cfg = CHANNEL_CONFIG[ch]
              return (
                <li key={ch} className="flex items-center justify-between text-sm">
                  <span className="flex items-center gap-2 text-muted-foreground">
                    <cfg.Icon className="w-3.5 h-3.5" />
                    {cfg.label}
                  </span>
                  <span className="font-medium tabular-nums">{byChannel[ch]}</span>
                </li>
              )
            })}
          </ul>
          {avgRating !== null && (
            <div className="pt-2 border-t flex items-center justify-between text-sm">
              <span className="flex items-center gap-2 text-muted-foreground">
                <Star className="w-3.5 h-3.5 fill-amber-400 text-amber-400" />
                Средний рейтинг
              </span>
              <span className="font-medium">{avgRating.toFixed(2)}</span>
            </div>
          )}
        </section>
      )
    }
    ```

    **Шаг D — создать `components/support/customers/NoteEditor.tsx`:**

    ```typescript
    "use client"

    import { useRef, useState, useTransition } from "react"
    import { toast } from "sonner"
    import { updateCustomerNote } from "@/app/actions/support"

    export function NoteEditor({
      customerId,
      initialNote,
    }: {
      customerId: string
      initialNote: string
    }) {
      const [value, setValue] = useState(initialNote)
      const [isPending, startTransition] = useTransition()
      const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

      function handleChange(next: string) {
        setValue(next)
        if (timerRef.current) clearTimeout(timerRef.current)
        timerRef.current = setTimeout(() => {
          startTransition(async () => {
            const res = await updateCustomerNote(customerId, next)
            if (!res.ok) toast.error(res.error)
            else toast.success("Заметка сохранена", { duration: 1500 })
          })
        }, 500)
      }

      return (
        <section className="rounded-lg border p-4 space-y-2">
          <div className="flex items-center justify-between">
            <h3 className="text-sm font-semibold">Заметка</h3>
            <span
              className="text-[10px] text-muted-foreground"
              title="Сохраняется автоматически. При конфликте победит последнее изменение."
            >
              Автосохранение
            </span>
          </div>
          <textarea
            value={value}
            onChange={(e) => handleChange(e.target.value)}
            placeholder="Внутренняя заметка о покупателе..."
            rows={6}
            maxLength={5000}
            className="w-full rounded border bg-transparent p-2 text-sm resize-none"
            disabled={isPending}
          />
          <div className="flex justify-end text-[10px] text-muted-foreground">
            {value.length}/5000
            {isPending && <span className="ml-2">сохраняю...</span>}
          </div>
        </section>
      )
    }
    ```

    **Шаг E — создать `components/support/customers/TicketsTable.tsx`:**

    ```typescript
    // Client component (Link + hover).
    import Link from "next/link"
    import {
      MessageSquare, HelpCircle, MessageCircle, RotateCw, Inbox, Star,
    } from "lucide-react"
    import type { TicketChannel, TicketStatus } from "@prisma/client"

    const channelIconMap: Record<TicketChannel, typeof MessageSquare> = {
      FEEDBACK: MessageSquare,
      QUESTION: HelpCircle,
      CHAT: MessageCircle,
      RETURN: RotateCw,
      MESSENGER: Inbox,
    }
    const channelLabelMap: Record<TicketChannel, string> = {
      FEEDBACK: "Отзыв", QUESTION: "Вопрос", CHAT: "Чат", RETURN: "Возврат", MESSENGER: "Мессенджер",
    }
    const statusLabelMap: Record<TicketStatus, string> = {
      NEW: "Новый", IN_PROGRESS: "В работе", ANSWERED: "Отвечен", CLOSED: "Закрыт", APPEALED: "Обжалование",
    }
    const statusColorMap: Record<TicketStatus, string> = {
      NEW: "text-red-600", IN_PROGRESS: "text-yellow-600", ANSWERED: "text-green-600",
      CLOSED: "text-gray-500", APPEALED: "text-purple-600",
    }

    export interface TicketRow {
      id: string
      channel: TicketChannel
      status: TicketStatus
      nmId: number | null
      rating: number | null
      previewText: string | null
      createdAt: Date
      assignedTo: {
        id: string
        name: string
        firstName: string | null
        lastName: string | null
      } | null
    }

    function formatDate(d: Date): string {
      return new Intl.DateTimeFormat("ru-RU", {
        day: "2-digit", month: "2-digit", year: "numeric",
        hour: "2-digit", minute: "2-digit",
        timeZone: "Europe/Moscow",
      }).format(d)
    }

    function assigneeName(a: TicketRow["assignedTo"]): string {
      if (!a) return "—"
      const full = [a.firstName, a.lastName].filter(Boolean).join(" ").trim()
      return full || a.name || "—"
    }

    export function TicketsTable({ tickets }: { tickets: TicketRow[] }) {
      if (tickets.length === 0) {
        return (
          <div className="rounded-lg border p-8 text-center text-muted-foreground">
            У этого покупателя нет обращений
          </div>
        )
      }
      return (
        <ul className="space-y-2">
          {tickets.map((t) => {
            const Icon = channelIconMap[t.channel]
            return (
              <li key={t.id}>
                <Link
                  href={`/support/${t.id}`}
                  className="flex items-start gap-3 rounded-lg border p-3 hover:bg-muted transition-colors"
                >
                  <div className="flex flex-col items-center w-16 shrink-0">
                    <Icon className="w-4 h-4 text-muted-foreground" />
                    <span className="text-[10px] uppercase text-muted-foreground mt-1">
                      {channelLabelMap[t.channel]}
                    </span>
                    {t.channel === "FEEDBACK" && t.rating !== null && (
                      <span className="flex items-center gap-0.5 mt-1 text-amber-500">
                        {Array.from({ length: t.rating }).map((_, i) => (
                          <Star key={i} className="w-2.5 h-2.5 fill-current" />
                        ))}
                      </span>
                    )}
                  </div>
                  <div className="flex-1 min-w-0 space-y-1">
                    <div className="flex items-center gap-2 text-sm">
                      <span className={`text-xs font-medium ${statusColorMap[t.status]}`}>
                        {statusLabelMap[t.status]}
                      </span>
                      {t.nmId && (
                        <span className="text-xs text-muted-foreground">
                          Артикул {t.nmId}
                        </span>
                      )}
                      <span className="text-xs text-muted-foreground ml-auto">
                        {formatDate(t.createdAt)}
                      </span>
                    </div>
                    <p className="text-sm text-muted-foreground line-clamp-2">
                      {t.previewText ?? <span className="italic">нет текста</span>}
                    </p>
                    <div className="text-xs text-muted-foreground">
                      Менеджер: {assigneeName(t.assignedTo)}
                    </div>
                  </div>
                </Link>
              </li>
            )
          })}
        </ul>
      )
    }
    ```

    **Шаг F — verification:**
    ```
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "app/(dashboard)/support/customers/[customerId]/page.tsx" && test -f components/support/customers/CustomerInfoCard.tsx && test -f components/support/customers/ChannelStats.tsx && test -f components/support/customers/NoteEditor.tsx && test -f components/support/customers/TicketsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "app/(dashboard)/support/customers/[customerId]/page.tsx"` exit 0
    - `grep "requireSection(\"SUPPORT\")" "app/(dashboard)/support/customers/[customerId]/page.tsx"` находит
    - `grep "countTicketsByChannel\\|averageFeedbackRating" "app/(dashboard)/support/customers/[customerId]/page.tsx" | wc -l` >= 2
    - `grep "notFound()" "app/(dashboard)/support/customers/[customerId]/page.tsx"` находит
    - `grep "export const dynamic = \"force-dynamic\"" "app/(dashboard)/support/customers/[customerId]/page.tsx"` находит
    - `test -f components/support/customers/CustomerInfoCard.tsx` exit 0
    - `grep "export function CustomerInfoCard" components/support/customers/CustomerInfoCard.tsx` находит
    - `grep "chat:" components/support/customers/CustomerInfoCard.tsx` находит (WB Chat badge)
    - `test -f components/support/customers/ChannelStats.tsx` exit 0
    - `grep "export function ChannelStats" components/support/customers/ChannelStats.tsx` находит
    - `grep "FEEDBACK\\|QUESTION\\|CHAT\\|RETURN\\|MESSENGER" components/support/customers/ChannelStats.tsx | wc -l` >= 5
    - `test -f components/support/customers/NoteEditor.tsx` exit 0
    - `grep "updateCustomerNote" components/support/customers/NoteEditor.tsx` находит
    - `grep "setTimeout\\|clearTimeout" components/support/customers/NoteEditor.tsx | wc -l` >= 2 (debounce)
    - `grep "use client" components/support/customers/NoteEditor.tsx` находит
    - `test -f components/support/customers/TicketsTable.tsx` exit 0
    - `grep "export function TicketsTable" components/support/customers/TicketsTable.tsx` находит
    - `grep "/support/\\\${t.id}\\|/support/\\\$\\{" components/support/customers/TicketsTable.tsx` находит Link
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    RSC страница профиля покупателя работает: load customer+tickets, aggregations через lib/customer-aggregations, 2-колоночный layout рендерит 4 новых компонента; debounced NoteEditor сохраняет через updateCustomerNote; TicketsTable с иконками и ссылками; tsc проходит.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: LinkCustomerButton + интеграция в TicketSidePanel + SupportTicketCard + [ticketId]/page.tsx</name>
  <files>
    components/support/customers/LinkCustomerButton.tsx,
    components/support/TicketSidePanel.tsx,
    components/support/SupportTicketCard.tsx,
    app/(dashboard)/support/[ticketId]/page.tsx
  </files>
  <read_first>
    - components/support/TicketSidePanel.tsx (существующая структура — расширяется props customerId/customerName, conditional render LinkCustomerButton)
    - components/support/SupportTicketCard.tsx (Link оборачивает всю карточку — нельзя вложить <Link>; решение — вынести имя покупателя в отдельный inline-элемент с e.stopPropagation() + router.push, либо рендерить имя как span и добавить отдельный icon-button справа)
    - components/ui/dialog.tsx (shadcn Dialog wrapper для модалки)
    - components/support/templates/TemplatePickerModal.tsx (паттерн модалки с поиском из Phase 11-03 — похожий подход для customer search)
    - app/(dashboard)/support/[ticketId]/page.tsx (передача customerId/customerName в TicketSidePanel)
    - app/actions/support.ts (Plan 12-01 — linkTicketToCustomer, createCustomerForTicket)
    - .planning/phases/12-customer-messenger/12-RESEARCH.md (§Pattern 2 строки 158-170 — manual link flow, §Open Questions #3 — customer selector list)
  </read_first>
  <behavior>
    **LinkCustomerButton behavior:**
    - Client component, принимает `ticketId`, `ticketChannel` (для guard), рендерится только в TicketSidePanel когда `customerId===null && ticketChannel !== "CHAT"`
    - Кнопка «Связать с покупателем» (UserPlus icon)
    - Клик → открывает Dialog с 2 режимами (tab-like через state):
      - **Mode «existing»**: native input поиск (debounced 300ms) — при запросе > 2 символов загружает список Customer через новый server action `searchCustomers(query)`. Список максимум 20. Клик на Customer → linkTicketToCustomer → toast + router.refresh + закрыть модалку.
      - **Mode «new»**: форма с name + phone inputs → createCustomerForTicket → toast + router.refresh + закрыть модалку.
    - Переключение между режимами — native `<select>` или две кнопки-таба

    **Новый server action `searchCustomers`** (добавить в app/actions/support.ts Plan 12-02; не план 12-01 — там уже коммичен):
    - Принимает `query: string` (trim, если < 2 символов — return [])
    - requireSection("SUPPORT") (read-only доступно VIEWER)
    - `prisma.customer.findMany({ where: { OR: [{ name: { contains: query, mode: 'insensitive' } }, { phone: { contains: query } }] }, take: 20, orderBy: { updatedAt: 'desc' } })`
    - Возвращает `{ id, name, phone, wbUserId }[]`

    **TicketSidePanel расширение:**
    - Новые props: `customerId: string | null`, `customerName: string | null`
    - Перед существующим блоком статуса — добавить секцию «Покупатель»:
      - если `customerId` есть: Link на `/support/customers/${customerId}` с именем
      - если `customerId===null`:
        - Если `channel !== "CHAT"`: рендерить LinkCustomerButton
        - Если `channel === "CHAT"`: показать «—» (CHAT auto-links, но возможны legacy до миграции)

    **SupportTicketCard — кликабельное имя (SUP-33 требование):**
    Текущая карточка: весь div обёрнут `<Link>`. Вложенный Link невозможен. Решение:
    - Добавить в `SupportTicketCardProps.ticket` новое поле `customer: { id: string; name: string | null } | null`
    - В JSX: где сейчас `<span className="font-medium">Покупатель</span>` — если `ticket.customer` есть, рендерить inline-элемент `<a href={...}>` (внутри Link это работает — браузер не вкладывает anchor в anchor, но React рендерит OK; НО клик всё равно пойдёт в outer Link). Правильное решение — onClick + preventDefault + router.push:

    ```tsx
    {ticket.customer ? (
      <a
        href={`/support/customers/${ticket.customer.id}`}
        onClick={(e) => { e.preventDefault(); e.stopPropagation(); router.push(`/support/customers/${ticket.customer!.id}`) }}
        className="font-medium hover:underline cursor-pointer"
      >
        {ticket.customer.name ?? "Покупатель"}
      </a>
    ) : (
      <span className="font-medium">
        {ticket.customerNameSnapshot ?? "Покупатель"}
      </span>
    )}
    ```

    Превращает SupportTicketCard в client component (из-за useRouter). Альтернатива: оставить как span но сделать отдельную кнопку-иконку → чище для code review.

    **Выбранный подход (проще и без конвертации в client):** добавить маленькую icon-кнопку «ExternalLink» (lucide) справа от имени, которая является Link на профиль. Но это требует остановки event propagation от outer Link. Решение: реализовать SupportTicketCard как client component с useRouter (минимальный overhead, карточка и так использует Link — переписать через useRouter.push).

    **Финальное решение (проще):** Phase 12 плановые изменения минимальны — сделать SupportTicketCard client component с useRouter + onClick на outer div + inline a/button для имени с stopPropagation.

    **Расширение [ticketId]/page.tsx:**
    - Передать в TicketSidePanel новые props:
    ```tsx
    <TicketSidePanel
      ticketId={ticket.id}
      channel={ticket.channel}
      status={ticket.status}
      assignedToId={ticket.assignedToId}
      users={supportUsers}
      createdAt={ticket.createdAt}
      lastMessageAt={ticket.lastMessageAt}
      appealRecord={ticket.appealRecord}
      customerId={ticket.customerId}
      customerName={ticket.customer?.name ?? null}
    />
    ```
    - В aside (левая колонка) заменить plain `<p>{ticket.customer?.name ?? "Покупатель"}</p>` на Link если customerId есть:
    ```tsx
    <section>
      <h3 className="text-xs uppercase text-muted-foreground mb-2">Покупатель</h3>
      {ticket.customerId ? (
        <Link href={`/support/customers/${ticket.customerId}`} className="text-sm hover:underline">
          {ticket.customer?.name ?? "Покупатель"}
        </Link>
      ) : (
        <p className="text-sm">{ticket.customer?.name ?? ticket.customerNameSnapshot ?? "Покупатель"}</p>
      )}
      {ticket.customer?.wbUserId && (
        <p className="text-xs text-muted-foreground">WB ID: {ticket.customer.wbUserId}</p>
      )}
    </section>
    ```
  </behavior>
  <action>
    **Шаг A — добавить `searchCustomers` в `app/actions/support.ts`** (в конец файла, после 12-01 actions):

    ```typescript
    // Phase 12 Plan 02 — search для LinkCustomerButton модалки
    export async function searchCustomers(
      query: string
    ): Promise<{ ok: true; customers: Array<{ id: string; name: string | null; phone: string | null; wbUserId: string | null }> } | { ok: false; error: string }> {
      try {
        await requireSection("SUPPORT")
        const q = query.trim()
        if (q.length < 2) return { ok: true, customers: [] }
        const customers = await prisma.customer.findMany({
          where: {
            OR: [
              { name: { contains: q, mode: "insensitive" } },
              { phone: { contains: q } },
            ],
          },
          select: { id: true, name: true, phone: true, wbUserId: true },
          take: 20,
          orderBy: { updatedAt: "desc" },
        })
        return { ok: true, customers }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }
    ```

    **Шаг B — создать `components/support/customers/LinkCustomerButton.tsx`:**

    ```typescript
    "use client"

    import { useState, useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { UserPlus } from "lucide-react"
    import { toast } from "sonner"
    import { Button } from "@/components/ui/button"
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
    } from "@/components/ui/dialog"
    import {
      linkTicketToCustomer,
      createCustomerForTicket,
      searchCustomers,
    } from "@/app/actions/support"

    type Mode = "existing" | "new"

    interface SearchResult {
      id: string
      name: string | null
      phone: string | null
      wbUserId: string | null
    }

    export function LinkCustomerButton({
      ticketId,
    }: {
      ticketId: string
    }) {
      const router = useRouter()
      const [open, setOpen] = useState(false)
      const [mode, setMode] = useState<Mode>("existing")
      const [isPending, startTransition] = useTransition()
      const [query, setQuery] = useState("")
      const [results, setResults] = useState<SearchResult[]>([])
      const [newName, setNewName] = useState("")
      const [newPhone, setNewPhone] = useState("")

      function onQueryChange(v: string) {
        setQuery(v)
        if (v.trim().length < 2) {
          setResults([])
          return
        }
        startTransition(async () => {
          const res = await searchCustomers(v)
          if (res.ok) setResults(res.customers)
          else toast.error(res.error)
        })
      }

      function onPickExisting(customerId: string) {
        startTransition(async () => {
          const res = await linkTicketToCustomer(ticketId, customerId)
          if (res.ok) {
            toast.success("Покупатель связан")
            setOpen(false)
            router.refresh()
          } else {
            toast.error(res.error)
          }
        })
      }

      function onCreateNew() {
        if (newName.trim().length < 1) {
          toast.error("Имя обязательно")
          return
        }
        startTransition(async () => {
          const res = await createCustomerForTicket(ticketId, {
            name: newName.trim(),
            phone: newPhone.trim() || null,
          })
          if (res.ok) {
            toast.success("Покупатель создан и связан")
            setOpen(false)
            setNewName("")
            setNewPhone("")
            router.refresh()
          } else {
            toast.error(res.error)
          }
        })
      }

      return (
        <>
          <Button
            variant="outline"
            size="sm"
            onClick={() => setOpen(true)}
            className="w-full"
          >
            <UserPlus className="w-4 h-4 mr-1" />
            Связать с покупателем
          </Button>
          <Dialog open={open} onOpenChange={setOpen}>
            <DialogContent className="max-w-md">
              <DialogHeader>
                <DialogTitle>Связать тикет с покупателем</DialogTitle>
              </DialogHeader>
              <div className="flex gap-2 border-b pb-2">
                <button
                  type="button"
                  onClick={() => setMode("existing")}
                  className={`text-sm px-3 py-1 rounded ${mode === "existing" ? "bg-primary text-primary-foreground" : "hover:bg-muted"}`}
                >
                  Найти существующего
                </button>
                <button
                  type="button"
                  onClick={() => setMode("new")}
                  className={`text-sm px-3 py-1 rounded ${mode === "new" ? "bg-primary text-primary-foreground" : "hover:bg-muted"}`}
                >
                  Создать нового
                </button>
              </div>
              {mode === "existing" && (
                <div className="space-y-2">
                  <input
                    type="text"
                    value={query}
                    onChange={(e) => onQueryChange(e.target.value)}
                    placeholder="Поиск по имени или телефону (от 2 символов)"
                    className="w-full rounded-md border px-3 py-2 text-sm"
                    autoFocus
                  />
                  <div className="max-h-[300px] overflow-y-auto">
                    {query.trim().length >= 2 && results.length === 0 && !isPending && (
                      <div className="text-sm text-muted-foreground py-4 text-center">
                        Не найдено
                      </div>
                    )}
                    <ul className="space-y-1">
                      {results.map((c) => (
                        <li key={c.id}>
                          <button
                            type="button"
                            onClick={() => onPickExisting(c.id)}
                            disabled={isPending}
                            className="w-full text-left rounded border px-3 py-2 hover:bg-muted"
                          >
                            <div className="text-sm font-medium">
                              {c.name ?? "Без имени"}
                            </div>
                            {c.phone && (
                              <div className="text-xs text-muted-foreground">
                                {c.phone}
                              </div>
                            )}
                            {c.wbUserId && (
                              <div className="text-[10px] text-muted-foreground font-mono">
                                {c.wbUserId}
                              </div>
                            )}
                          </button>
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>
              )}
              {mode === "new" && (
                <div className="space-y-3">
                  <div>
                    <label className="text-xs text-muted-foreground">Имя *</label>
                    <input
                      type="text"
                      value={newName}
                      onChange={(e) => setNewName(e.target.value)}
                      placeholder="Иван Петров"
                      className="w-full rounded-md border px-3 py-2 text-sm mt-1"
                      maxLength={200}
                    />
                  </div>
                  <div>
                    <label className="text-xs text-muted-foreground">Телефон</label>
                    <input
                      type="text"
                      value={newPhone}
                      onChange={(e) => setNewPhone(e.target.value)}
                      placeholder="+79991234567"
                      className="w-full rounded-md border px-3 py-2 text-sm mt-1"
                      maxLength={20}
                    />
                  </div>
                  <div className="flex justify-end gap-2">
                    <Button variant="outline" onClick={() => setOpen(false)} disabled={isPending}>
                      Отмена
                    </Button>
                    <Button onClick={onCreateNew} disabled={isPending || newName.trim().length < 1}>
                      Создать и связать
                    </Button>
                  </div>
                </div>
              )}
            </DialogContent>
          </Dialog>
        </>
      )
    }
    ```

    **Шаг C — обновить `components/support/TicketSidePanel.tsx`:**

    Добавить в interface Props:
    ```typescript
    customerId: string | null
    customerName: string | null
    ```

    В компоненте (перед существующим блоком «Статус») добавить секцию «Покупатель»:

    ```typescript
    import Link from "next/link"
    import { LinkCustomerButton } from "@/components/support/customers/LinkCustomerButton"
    // ...

    export function TicketSidePanel({
      ticketId, channel, status, assignedToId, users, createdAt, lastMessageAt, appealRecord,
      customerId, customerName,
    }: Props) {
      // ...
      return (
        <aside className="space-y-4 text-sm">
          {/* Phase 12: секция покупателя */}
          <div>
            <label className="block text-xs text-muted-foreground mb-1">Покупатель</label>
            {customerId ? (
              <Link
                href={`/support/customers/${customerId}`}
                className="text-sm hover:underline"
              >
                {customerName ?? "Покупатель"} →
              </Link>
            ) : channel === "CHAT" ? (
              <p className="text-xs text-muted-foreground italic">
                Для CHAT канала покупатель будет связан автоматически при следующей синхронизации
              </p>
            ) : (
              <LinkCustomerButton ticketId={ticketId} />
            )}
          </div>

          {/* existing APPEALED panel */}
          {status === "APPEALED" && appealRecord && (
            <AppealStatusPanel ... />
          )}
          {/* existing Status + Менеджер + meta blocks — NO CHANGE */}
        </aside>
      )
    }
    ```

    **Шаг D — обновить `components/support/SupportTicketCard.tsx`:**

    Конвертировать в client component (`"use client"` в начале) + useRouter для inline click-on-customer-name. Добавить `customer` в ticket prop:

    ```typescript
    "use client"

    import { useRouter } from "next/navigation"
    import Link from "next/link"
    // ... existing imports ...

    export interface SupportTicketCardProps {
      ticket: {
        id: string
        channel: TicketChannel
        status: TicketStatus
        nmId: number | null
        rating: number | null
        previewText: string | null
        createdAt: Date
        appealStatus?: AppealStatus | null
        assignedTo: { id: string; name: string; firstName: string | null; lastName: string | null } | null
        // Phase 12:
        customer: { id: string; name: string | null } | null
        customerNameSnapshot: string | null
      }
      wbCard: { nmId: number; photoUrl: string | null; title: string | null } | null
    }

    export function SupportTicketCard({ ticket, wbCard }: SupportTicketCardProps) {
      const router = useRouter()
      const Icon = channelIconMap[ticket.channel]

      function onCustomerClick(e: React.MouseEvent) {
        if (!ticket.customer) return
        e.preventDefault()
        e.stopPropagation()
        router.push(`/support/customers/${ticket.customer.id}`)
      }

      const customerLabel =
        ticket.customer?.name ?? ticket.customerNameSnapshot ?? "Покупатель"

      return (
        <Link href={`/support/${ticket.id}`} className={/* existing ... */}>
          {/* existing icon + photo + left flex ... */}
          <div className="flex-1 min-w-0 space-y-1">
            <div className="flex items-center gap-2 text-sm">
              {ticket.customer ? (
                <a
                  href={`/support/customers/${ticket.customer.id}`}
                  onClick={onCustomerClick}
                  className="font-medium hover:underline cursor-pointer"
                >
                  {customerLabel}
                </a>
              ) : (
                <span className="font-medium">{customerLabel}</span>
              )}
              {ticket.channel === "FEEDBACK" && ticket.rating !== null && (
                <span className="flex items-center gap-0.5 text-amber-500">
                  {Array.from({ length: ticket.rating }).map((_, i) => (
                    <Star key={i} className="w-3.5 h-3.5 fill-current" />
                  ))}
                </span>
              )}
              {/* existing status + appealStatus — NO CHANGE */}
            </div>
            {/* existing preview + footer — NO CHANGE */}
          </div>
        </Link>
      )
    }
    ```

    **Шаг E — обновить `app/(dashboard)/support/[ticketId]/page.tsx`:**

    В `<TicketSidePanel>` вызове добавить props:
    ```tsx
    <TicketSidePanel
      ticketId={ticket.id}
      channel={ticket.channel}
      status={ticket.status}
      assignedToId={ticket.assignedToId}
      users={supportUsers}
      createdAt={ticket.createdAt}
      lastMessageAt={ticket.lastMessageAt}
      appealRecord={ticket.appealRecord}
      customerId={ticket.customerId}
      customerName={ticket.customer?.name ?? null}
    />
    ```

    Заменить в aside блок «Покупатель» (строки 134-144):
    ```tsx
    <section>
      <h3 className="text-xs uppercase text-muted-foreground mb-2">Покупатель</h3>
      {ticket.customerId ? (
        <Link
          href={`/support/customers/${ticket.customerId}`}
          className="text-sm hover:underline"
        >
          {ticket.customer?.name ?? "Покупатель"} →
        </Link>
      ) : (
        <p className="text-sm">
          {ticket.customer?.name ?? ticket.customerNameSnapshot ?? "Покупатель"}
        </p>
      )}
      {ticket.customer?.wbUserId && (
        <p className="text-xs text-muted-foreground">
          WB ID: {ticket.customer.wbUserId}
        </p>
      )}
    </section>
    ```

    Также обновить лента `app/(dashboard)/support/page.tsx` — добавить в select tickets:
    ```typescript
    select: {
      // existing fields ...
      customer: { select: { id: true, name: true } },
      customerNameSnapshot: true,
    },
    ```
    И передать в `<SupportTicketCard ticket={...} />` — новая форма включает эти два поля.

    **Шаг F — verification:**
    ```
    npx tsc --noEmit
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/support/customers/LinkCustomerButton.tsx && grep -q "LinkCustomerButton" components/support/TicketSidePanel.tsx && grep -q "customerId" "app/(dashboard)/support/[ticketId]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/customers/LinkCustomerButton.tsx` exit 0
    - `grep "export function LinkCustomerButton" components/support/customers/LinkCustomerButton.tsx` находит
    - `grep "linkTicketToCustomer\\|createCustomerForTicket\\|searchCustomers" components/support/customers/LinkCustomerButton.tsx | wc -l` >= 3
    - `grep "use client" components/support/customers/LinkCustomerButton.tsx` находит
    - `grep "export async function searchCustomers" app/actions/support.ts` находит
    - `grep "LinkCustomerButton" components/support/TicketSidePanel.tsx` находит
    - `grep "customerId" components/support/TicketSidePanel.tsx | wc -l` >= 2
    - `grep "channel === \"CHAT\"\\|channel === \"CHAT\"" components/support/TicketSidePanel.tsx` находит guard
    - `grep "use client" components/support/SupportTicketCard.tsx` находит
    - `grep "customers/" components/support/SupportTicketCard.tsx` находит ссылку на профиль
    - `grep "customer:" components/support/SupportTicketCard.tsx` находит новое поле в props
    - `grep "customerId={ticket.customerId}" "app/(dashboard)/support/[ticketId]/page.tsx"` находит передачу props
    - `grep "customers/\\\${ticket.customerId}\\|/support/customers" "app/(dashboard)/support/[ticketId]/page.tsx"` находит Link в aside
    - `grep "customer:" "app/(dashboard)/support/page.tsx"` находит select customer в ленте
    - `npx tsc --noEmit` exit 0
    - `npm run test` exit 0 (регрессия clean)
  </acceptance_criteria>
  <done>
    LinkCustomerButton — client модалка search/create; searchCustomers server action добавлен в app/actions/support.ts; TicketSidePanel показывает покупателя (Link если customerId, LinkCustomerButton если null && !CHAT); SupportTicketCard — client component с кликабельным именем покупателя; [ticketId]/page.tsx передаёт customerId+customerName; лента /support select теперь включает customer + customerNameSnapshot. tsc проходит, регрессия clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Расширить tests/customer-profile-page.test.ts — 5+ тестов page-level aggregates</name>
  <files>
    tests/customer-profile-page.test.ts
  </files>
  <read_first>
    - tests/customer-profile-page.test.ts (Plan 12-01 stubs: countTicketsByChannel + averageFeedbackRating тесты)
    - lib/customer-aggregations.ts (функции под тест)
    - .planning/phases/12-customer-messenger/12-VALIDATION.md (Wave 0 requirement: customer-profile-page.test.ts GREEN)
  </read_first>
  <action>
    **Дополнить `tests/customer-profile-page.test.ts`** (добавить describe блоки к существующим из Plan 12-01; минимум 5 новых тестов — суммарно 10+):

    ```typescript
    // ДОПОЛНЕНИЕ к существующим тестам Plan 12-01 — конкретные сценарии профиля

    describe("countTicketsByChannel — дополнительные сценарии Plan 12-02", () => {
      it("Customer со всеми 5 каналами — корректная раскладка", () => {
        const tickets = [
          { channel: "FEEDBACK" as const, rating: 5 },
          { channel: "FEEDBACK" as const, rating: 4 },
          { channel: "QUESTION" as const, rating: null },
          { channel: "QUESTION" as const, rating: null },
          { channel: "QUESTION" as const, rating: null },
          { channel: "CHAT" as const, rating: null },
          { channel: "RETURN" as const, rating: null },
          { channel: "RETURN" as const, rating: null },
          { channel: "MESSENGER" as const, rating: null },
        ]
        const result = countTicketsByChannel(tickets)
        expect(result).toEqual({
          FEEDBACK: 2, QUESTION: 3, CHAT: 1, RETURN: 2, MESSENGER: 1,
        })
      })

      it("Customer только с CHAT (типичный auto-linked случай)", () => {
        const tickets = [
          { channel: "CHAT" as const, rating: null },
          { channel: "CHAT" as const, rating: null },
          { channel: "CHAT" as const, rating: null },
        ]
        const result = countTicketsByChannel(tickets)
        expect(result.CHAT).toBe(3)
        expect(result.FEEDBACK).toBe(0)
      })
    })

    describe("averageFeedbackRating — дополнительные сценарии Plan 12-02", () => {
      it("Customer с отличным рейтингом 5.0", () => {
        const tickets = [
          { channel: "FEEDBACK" as const, rating: 5 },
          { channel: "FEEDBACK" as const, rating: 5 },
          { channel: "FEEDBACK" as const, rating: 5 },
        ]
        expect(averageFeedbackRating(tickets)).toBe(5)
      })

      it("Customer с плохим рейтингом (mix каналов не влияет)", () => {
        const tickets = [
          { channel: "FEEDBACK" as const, rating: 1 },
          { channel: "FEEDBACK" as const, rating: 2 },
          { channel: "QUESTION" as const, rating: null }, // игнорируется
          { channel: "RETURN" as const, rating: null },   // игнорируется
        ]
        expect(averageFeedbackRating(tickets)).toBe(1.5)
      })

      it("Customer без FEEDBACK — null", () => {
        const tickets = [
          { channel: "CHAT" as const, rating: null },
          { channel: "MESSENGER" as const, rating: null },
        ]
        expect(averageFeedbackRating(tickets)).toBeNull()
      })
    })
    ```

    **Запуск:** `npm run test -- tests/customer-profile-page.test.ts` → суммарно 10+ GREEN (включая 5+ из Plan 12-01 + 5+ новых).

    **Примечание:** Page-level integration test (RSC render) в vitest сложен из-за Next.js серверных импортов (prisma, auth). Обходим это — тестируем только pure helpers, которые RSC использует. UAT в Plan 12-03 покрывает реальный рендер профиля в браузере.
  </action>
  <verify>
    <automated>npm run test -- tests/customer-profile-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/customer-profile-page.test.ts` >= 10
    - `grep -c "describe(" tests/customer-profile-page.test.ts` >= 4
    - `grep -c "it.skip" tests/customer-profile-page.test.ts` == 0
    - `grep "countTicketsByChannel\\|averageFeedbackRating" tests/customer-profile-page.test.ts | wc -l` >= 4
    - `grep "MESSENGER\\|CHAT\\|RETURN\\|FEEDBACK\\|QUESTION" tests/customer-profile-page.test.ts | wc -l` >= 10
    - `npm run test -- tests/customer-profile-page.test.ts` exit 0 (10+ GREEN)
    - `npm run test` exit 0 (полная suite)
  </acceptance_criteria>
  <done>
    tests/customer-profile-page.test.ts расширен до 10+ GREEN тестов покрывая все 5 каналов и edge-cases aggregation helpers; полная suite не регрессирует.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач:
1. `npx tsc --noEmit` exit 0
2. `npm run test` exit 0 (Phase 12-02 + регрессия Phase 7-11)
3. `test -f "app/(dashboard)/support/customers/[customerId]/page.tsx"`
4. `ls components/support/customers/ | wc -l` >= 5
5. Страница `/support/customers/[id]` рендерится без ошибок (tsc-уровень)
6. TicketSidePanel содержит LinkCustomerButton + customerId prop
7. SupportTicketCard — client component с router.push на профиль
8. Validation Map (12-VALIDATION.md) outcome: Plan 12-02 GREEN
</verification>

<success_criteria>
Plan 12-02 завершён когда:
- /support/customers/[customerId] RSC страница работает: load customer+tickets, aggregations, 2-колоночный layout
- 5 клиентских компонента созданы: CustomerInfoCard, ChannelStats, NoteEditor (debounced), TicketsTable, LinkCustomerButton (модалка search+create)
- searchCustomers server action добавлен в app/actions/support.ts
- TicketSidePanel расширен: секция «Покупатель» с Link или LinkCustomerButton (guard: CHAT auto-linked)
- SupportTicketCard — client component, имя покупателя → router.push на профиль
- [ticketId]/page.tsx передаёт customerId/customerName; aside Link на профиль
- лента /support select теперь включает customer + customerNameSnapshot
- tests/customer-profile-page.test.ts — 10+ GREEN тестов
- Commit `feat(12-02): профиль покупателя + LinkCustomerButton + кликабельное имя в ленте`
</success_criteria>

<output>
После завершения создать `.planning/phases/12-customer-messenger/12-02-SUMMARY.md`:
- Новая страница `/support/customers/[customerId]` — описание layout
- Список 5 клиентских компонент + их экспорты
- searchCustomers server action — контракт для Plan 12-03
- Изменения TicketSidePanel + SupportTicketCard (client conversion)
- Изменения [ticketId]/page.tsx (select в Prisma)
- Интерфейсы для Plan 12-03:
  - `/support/customers/[id]` доступен из диалога и ленты — Plan 12-03 добавит MergeCustomerDialog как кнопку в профиле
  - createManualMessengerTicket (Plan 12-01) доступен для /support/new страницы
- Validation Map update: Plan 12-02 tasks GREEN
</output>
