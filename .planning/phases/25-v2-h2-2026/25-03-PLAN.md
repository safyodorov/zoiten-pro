---
phase: 25-v2-h2-2026
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/sales-plan/data.ts
  - scripts/bootstrap-sales-plan-monthly.ts
autonomous: true
requirements: [SP-02, SP-14]
must_haves:
  truths:
    - "loadSalesPlanInputs(db) собирает SalesPlanInputs (товары + иерархия + monthLevels + dayOverrides + arrivals + baseline/avgPrice/buyoutPct из funnel + seed)"
    - "loadFactDaily(db, from, to) возвращает company-level и product-level факт из WbCardFunnelDaily на лету"
    - "bootstrap-скрипт переносит baselineOverrides/priceOverrides → SalesPlanMonthLevel и leadTimes → leadTimes2 идемпотентно"
    - "loadSalesPlanInputs использует DI PrismaClient (не глобальный import) и PRODUCT_HIERARCHY_ORDER_BY"
  artifacts:
    - path: "lib/sales-plan/data.ts"
      provides: "loadSalesPlanInputs, loadFactDaily (Prisma-loader, DI)"
      exports: ["loadSalesPlanInputs", "loadFactDaily"]
    - path: "scripts/bootstrap-sales-plan-monthly.ts"
      provides: "миграция старых overrides → monthLevels + leadTimes2"
      contains: "salesPlanMonthLevel"
  key_links:
    - from: "lib/sales-plan/data.ts"
      to: "lib/sales-plan/engine.ts (типы SalesPlanInputs/ProductPlanInput)"
      via: "loader производит вход для computeSalesPlan"
      pattern: "SalesPlanInputs"
    - from: "lib/sales-plan/data.ts"
      to: "WbCardFunnelDaily"
      via: "company-level + product-level факт через MarketplaceArticle"
      pattern: "wbCardFunnelDaily"
---

<objective>
Этап 1 (Фундамент), часть 3: Prisma-загрузчик `lib/sales-plan/data.ts` (мост между БД и pure-движком) + bootstrap-скрипт миграции старых overrides. Невидимый деплой — данные и движок в проде, старый UI работает без изменений.

Purpose: `loadSalesPlanInputs` собирает всё, что нужно `computeSalesPlan` (товары, иерархия, monthLevels, dayOverrides, приходы через resolveArrivalBatches, baseline/avgPrice/% выкупа из funnel по settled-окну). `loadFactDaily` даёт факт двумя разрезами (company-level для ИУ, product-level для строк товаров). Bootstrap переносит существующие пользовательские настройки в новую модель, чтобы план не стартовал с нуля.
Output: `lib/sales-plan/data.ts` + `scripts/bootstrap-sales-plan-monthly.ts`; запуск скрипта на проде после деплоя.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md

<interfaces>
Из lib/sales-plan/engine.ts (Wave 1, готово): SalesPlanInputs, ProductPlanInput, ModelParams, ArrivalBatch.
Из lib/sales-plan/arrivals.ts (Wave 1, готово): resolveArrivalBatches(input): ArrivalBatch[].
Prisma-модели (Wave 1, готово): SalesPlanMonthLevel, SalesPlanDayOverride, VirtualPurchase, Purchase.plannedArrivalDate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/sales-plan/data.ts — loadSalesPlanInputs + loadFactDaily (DI)</name>
  <files>lib/sales-plan/data.ts</files>
  <read_first>
    - lib/sales-forecast.ts (строки 1-15 импорты; 219-265 запрос товаров + MarketplaceArticle join; 280-358 funnel-запрос company/product-level, settled-окно [today−37;today−7], baseline last-7d, seed [today−3;today−1], 4-уровневая % выкупа own→legacy→subcategory→global)
    - lib/stock-data.ts (DI-паттерн: функция принимает prisma инстанс; productionBreakdown per-закупка qtyRemaining)
    - lib/sales-plan/engine.ts, arrivals.ts, types.ts (Wave 1 — типы и resolveArrivalBatches для сборки arrivals)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §3 (переиспользуемое из sales-forecast), §5 (два разреза факта), §3.4 (arrivals в loader)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок `lib/sales-plan/data.ts` (219-271)
  </read_first>
  <action>
Создай `lib/sales-plan/data.ts` с DI-паттерном (функции принимают `db: PrismaClient`, не импортируют глобальный prisma):

`export async function loadSalesPlanInputs(db: PrismaClient, params: { today, horizonFrom, horizonTo } & ModelParams): Promise<SalesPlanInputs>`:
- Запрос активных товаров: `db.product.findMany({ where: { deletedAt: null }, include: { brand: { include: { direction: true } }, category: true, subcategory: true, articles: { where: { marketplaceId: wbMarketplace.id }, select: { article: true } }, salesPlanMonthLevels: true, salesPlanDayOverrides: true, virtualPurchases: { where: { status: { in: ["SUGGESTED","ACCEPTED"] } } } }, orderBy: PRODUCT_HIERARCHY_ORDER_BY })`.
- Собери nmIds per товар из articles (nmId = parseInt(article)).
- Funnel: один запрос `db.wbCardFunnelDaily.findMany` по всем nmId за окно [today−37; today−1]. Из него per товар: `baselineOrdersPerDay` = avg ordersCount last-7d [today−7;today−1]; `avgPriceRub` = buyoutsSumRub/buyoutsCount settled-окна [today−37;today−7] с fallback на WbCard.price; `buyoutPct` + `buyoutSource` по 4-уровневой цепочке own→legacy→subcategory→global (перенеси из sales-forecast); `seedOrders` = ordersCount по дням [today−3;today−1].
- `stockNow` = Σ WbCard.stockQty (по nmId товара) + ivanovoStock (Product.ivanovoStock если есть).
- Приходы: загрузи реальные закупки товара (Purchase PLANNED|ACTIVE с items+stages+plannedArrivalDate + SupplierProductLink.leadTimeDays) + legacy ProductIncoming + виртуальные (уже в include). Прогони `resolveArrivalBatches` per товар → arrivals[].
- monthLevels[] = из salesPlanMonthLevels (month ISO, targetOrdersPerDay/priceRub/buyoutPct nullable). dayOverrides = Record<dateIso, ordersPerDay> из salesPlanDayOverrides.
- Верни `SalesPlanInputs { today, horizonFrom, horizonTo, deliveryDays, returnDays, wbInboundLagDays, products: ProductPlanInput[] }`. Всё сериализуемо (Date→ISO string, Decimal→number).

`export async function loadFactDaily(db: PrismaClient, from: string, to: string): Promise<{ company: Map<dateIso, {buyoutsRub, ordersRub, buyoutsUnits}>, byProduct: Map<productId, Map<dateIso, {...}>>, settledThroughIso: string }>`:
- (а) company-level: SUM по ВСЕМ nmId (включая 73 непривязанных) GROUP BY date — для сравнения с ИУ.
- (б) product-level: через MarketplaceArticle join (канонический паттерн sales-forecast:219-265) — nmId → productId.
- settle-лаг: settledThroughIso = today − 7 (дни свежее помечаются unsettled на стороне потребителя).
Импорты: `import type { PrismaClient } from "@prisma/client"`, `import { PRODUCT_HIERARCHY_ORDER_BY } from "@/lib/product-order"`, типы из `./types`, `resolveArrivalBatches` из `./arrivals`. Ноль импортов React/Next.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "sales-plan/data" | head; echo "tsc-check-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function loadSalesPlanInputs\|export async function loadFactDaily" lib/sales-plan/data.ts` === 2
    - `grep -c "PRODUCT_HIERARCHY_ORDER_BY" lib/sales-plan/data.ts` ≥ 1
    - `grep -c "resolveArrivalBatches" lib/sales-plan/data.ts` ≥ 1
    - `grep -c "wbCardFunnelDaily" lib/sales-plan/data.ts` ≥ 1
    - loadFactDaily возвращает и company, и byProduct: `grep -c "company\|byProduct" lib/sales-plan/data.ts` ≥ 2
    - `grep -c "from \"react\|from \"next" lib/sales-plan/data.ts` === 0
    - `npx tsc --noEmit` не выдаёт ошибок про lib/sales-plan/data.ts
  </acceptance_criteria>
  <done>data.ts: loadSalesPlanInputs производит вход для движка, loadFactDaily даёт факт в двух разрезах; DI PrismaClient, типобезопасно.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/bootstrap-sales-plan-monthly.ts — миграция старых overrides</name>
  <files>scripts/bootstrap-sales-plan-monthly.ts</files>
  <read_first>
    - scripts/bootstrap-balance-snapshot.ts (строки 44-60 — DI PrismaClient напрямую, main/disconnect, idempotent транзакция deleteMany+createMany)
    - app/actions/sales-plan.ts (ключи salesPlan.baselineOverrides/priceOverrides/leadTimes — что переносим)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §2.7 (bootstrap: семантика plannedSalesPerDay «target ПОСЛЕ прихода», leadTimes→leadTimes2), §9 этап 1
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок `scripts/bootstrap-sales-plan-monthly.ts` (373-408)
  </read_first>
  <action>
Создай `scripts/bootstrap-sales-plan-monthly.ts` (запуск `npx tsx scripts/...`, `import { PrismaClient } from "@prisma/client"` напрямую — НЕ из @/lib):
- Прочитай AppSetting `salesPlan.baselineOverrides` (JSON `Record<productId, number>`), `salesPlan.priceOverrides`, `salesPlan.leadTimes`, `salesPlan.horizon`.
- Горизонт-месяцы: 2026-07-01 … 2026-12-01 (6 первых чисел месяцев).
- Для каждого товара с baselineOverride: определи месяц прихода `ProductIncoming.expectedDate` (если есть). Для месяцев ДО месяца expectedDate сеется `baselineOverride ?? null`; для месяцев ОТ месяца expectedDate и позже — `plannedSalesPerDay ?? baselineOverride ?? null` (учёт семантики «target после прихода», §2.7 — убирает завышение плана до прихода для товаров с остатком). Товар без ProductIncoming — `baselineOverride` на все месяцы.
- priceOverrides → SalesPlanMonthLevel.priceRub на все месяцы горизонта.
- Идемпотентная транзакция: `tx.salesPlanMonthLevel.deleteMany({ where: { month: { in: horizonMonths } } })` + `tx.salesPlanMonthLevel.createMany({ data: rows })` (skipDuplicates).
- Перенос leadTimes → leadTimes2: если `salesPlan.leadTimes` существует и ≠ {3,3}, upsert `salesPlan.leadTimes2` этим значением (не теряем пользовательские deliveryDays/returnDays).
- `main().catch(e => { console.error(e); process.exit(1) })` + `await prisma.$disconnect()`.
- НЕ удаляй старые ключи (это делает Wave 6 зачистка).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx tsc --noEmit scripts/bootstrap-sales-plan-monthly.ts 2>&1 | grep -iE "error TS" | head; echo "check-done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new PrismaClient()" scripts/bootstrap-sales-plan-monthly.ts` ≥ 1
    - `grep -c "salesPlanMonthLevel" scripts/bootstrap-sales-plan-monthly.ts` ≥ 1
    - `grep -c "leadTimes2" scripts/bootstrap-sales-plan-monthly.ts` ≥ 1
    - `grep -c "plannedSalesPerDay\|expectedDate" scripts/bootstrap-sales-plan-monthly.ts` ≥ 1 (учёт семантики прихода)
    - `grep -c "\\$disconnect\|process.exit" scripts/bootstrap-sales-plan-monthly.ts` ≥ 1
    - `grep -c "from \"@/lib" scripts/bootstrap-sales-plan-monthly.ts` === 0 (standalone tsx-скрипт)
  </acceptance_criteria>
  <done>bootstrap-скрипт: baselineOverrides/priceOverrides → monthLevels (с семантикой прихода), leadTimes → leadTimes2; идемпотентен; запускается через tsx.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — data.ts и bootstrap типобезопасны на новых Prisma-моделях.
- data.ts — ноль React/Next импортов (loader-слой), DI PrismaClient.
- bootstrap — standalone, идемпотентный, учитывает семантику plannedSalesPerDay.
- Существующий `/sales-plan` (старый UI) продолжает работать — data.ts и bootstrap ничего в старом коде не трогают.
</verification>

<success_criteria>
Этап 1 «Фундамент» завершён (25-01+25-02+25-03): схема + движок + loader + bootstrap. Данные и движок в проде невидимо, старый UI жив. Bootstrap запускается на проде после деплоя (перенос старых overrides). Основа для Этапа 2 (Товары UI).
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-03-SUMMARY.md`. Отметь прод-задачу: запустить `npx tsx scripts/bootstrap-sales-plan-monthly.ts` на VPS после деплоя миграции (`set -a; . /etc/zoiten.pro.env; set +a; npx tsx ...`).
</output>
