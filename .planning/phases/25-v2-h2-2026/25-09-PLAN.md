---
phase: 25-v2-h2-2026
plan: 09
type: execute
wave: 8
depends_on: [08]
files_modified:
  - lib/sales-plan/pdds-feed.ts
  - app/actions/sales-plan.ts
  - app/(dashboard)/sales-plan/page.tsx
  - components/layout/nav-items.ts
  - components/sales-plan/SalesForecastTable.tsx
  - components/sales-plan/SalesForecastSummary.tsx
  - components/sales-plan/SalesForecastDailyChart.tsx
  - components/sales-plan/SalesForecastEndDate.tsx
  - components/sales-plan/ProductForecastDialog.tsx
  - app/api/wb-funnel-backfill/route.ts
  - CLAUDE.md
autonomous: false
requirements: [SP-12, SP-13]
must_haves:
  truths:
    - "lib/sales-plan/pdds-feed.ts: getPlannedRevenueSeries + getPlannedVirtualPayments (live-сверка статусов, forward-fill курса) + buildVirtualPurchasePayments (pure DEPOSIT/BALANCE)"
    - "Анти-двойной счёт: CONVERTED/DISMISSED-после-фиксации исключаются из виртуальных платежей по live-статусу"
    - "Зачистка: SalesForecast*-компоненты, ProductForecastDialog, IU_REMAINING_RUB, DEFAULT_END_DATE, старые AppSetting-ключи удалены"
    - "/purchase-plan и /procurement/plan деприкейтнуты (сняты из sidebar)"
    - "Прод: миграция + bootstrap применены, все 3 таба работают, npm run test зелёный"
  artifacts:
    - path: "lib/sales-plan/pdds-feed.ts"
      provides: "контракт ПДДС для следующей фазы"
      exports: ["getPlannedRevenueSeries", "getPlannedVirtualPayments", "buildVirtualPurchasePayments"]
  key_links:
    - from: "lib/sales-plan/pdds-feed.ts (getPlannedVirtualPayments)"
      to: "live VirtualPurchase.status"
      via: "сверка vpIds из paramsJson (CONVERTED/DISMISSED исключить)"
      pattern: "CONVERTED|status"
    - from: "getPlannedVirtualPayments"
      to: "getRateForDate (lib/balance-data.ts)"
      via: "forward-fill курса CNY→RUB"
      pattern: "getRateForDate|forward"
---

<objective>
Этап 6 (ПДДС-feed + зачистка): контракт `lib/sales-plan/pdds-feed.ts` для следующей фазы (ПДДС `/finance/cashflow`) + удаление старого кода + деприкейт временных разделов + финальный деплой + UAT. TDD — доводит Wave-0-стаб `sales-plan-pdds-feed` до GREEN.

Purpose: SP-12 — притоки (дневной ряд плановых выкупов от версии) + оттоки (плановые платежи VP с live-сверкой статусов, анти-двойной счёт, forward-fill курса). SP-13 (зачистка) — убрать дубли (SalesForecast*, IU_REMAINING_RUB, DEFAULT_END_DATE, старые ключи), деприкейт /purchase-plan. Кодовая база без легаси.
Output: pdds-feed.ts + удаление 5 компонентов + деприкейт nav + deploy + UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Из lib/procurement-math.ts: computeDepositDueDate (createdAt+3), computeBalanceDueDate (deposit+leadTime) — для платежей VP.
Из lib/balance-data.ts: getRateForDate (Prisma-coupled forward-fill курса).
Prisma-модели (Wave 1): SalesPlanVersion (paramsJson с VP-снапшотами), SalesPlanVersionDay, VirtualPurchase (live status).
tests/sales-plan-pdds-feed.test.ts (Wave 0 контракт).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/sales-plan/pdds-feed.ts (стаб → GREEN) — pure-ядро + loader</name>
  <files>lib/sales-plan/pdds-feed.ts</files>
  <behavior>
    - Test: DEPOSIT dueDate = orderDate + 3 (computeDepositDueDate)
    - Test: BALANCE dueDate = deposit + leadTime (computeBalanceDueDate)
    - Test: depositPct/balancePct fallback 30/70
    - Test: amount в валюте закупки (CNY), pure-ядро не конвертирует в ₽
  </behavior>
  <read_first>
    - lib/procurement-math.ts (строки 15-30 computeDepositDueDate/computeBalanceDueDate — переиспользовать)
    - lib/balance-data.ts (getRateForDate — forward-fill курса, Prisma-coupled — только в loader-обёртке)
    - tests/sales-plan-pdds-feed.test.ts (Wave 0 контракт)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §8 (контракт ПДДС: getPlannedRevenueSeries, buildVirtualPurchasePayments, getPlannedVirtualPayments, анти-двойной счёт правило сверки, forward-fill курса)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок pdds-feed (275-311)
    - .planning/phases/25-v2-h2-2026/CRITIC-VERDICT.md (дыра №1: live-сверка статусов CONVERTED; дыра №10: forward-fill курса на будущие dueDate)
  </read_first>
  <action>
Создай `lib/sales-plan/pdds-feed.ts` — разделение pure-ядро / loader-обёртка:

**PURE-ядро (экспорт, без Prisma):**
- `buildVirtualPurchasePayments(vpSnapshot): Array<{ type: "DEPOSIT"|"BALANCE"; dueDate: string; amount: number; currency: string }>`: DEPOSIT dueDate = orderDate + 3 (computeDepositDueDate); BALANCE = deposit + leadTime (computeBalanceDueDate); amount = qty × unitPrice × depositPct|balancePct (fallback 30/70); валюта закупки (CNY), НЕ конвертируется. Импортируй computeDepositDueDate/computeBalanceDueDate из procurement-math (они pure).

**LOADER-обёртка (async, Prisma-coupled):**
- `getPlannedRevenueSeries(db, versionId): Promise<Array<{ date, buyoutsRub, buyoutsUnits, byProduct: [...] }>>`: дневной ряд плановых выкупов из SalesPlanVersionDay (связка productId→nmId сохранена; net-to-seller ПДДС считает сам).
- `getPlannedVirtualPayments(db, versionId): Promise<...>`: (1) прочитай VP-снапшоты из `version.paramsJson`; (2) **сверь их id с live VirtualPurchase.status**: `CONVERTED` → платежи исключаются (заняты реальными PurchasePayment созданной Purchase); `DISMISSED`-после-фиксации → тоже исключаются (+ флаг «версия устарела»); id не найден live (авто-SUGGESTED удалена регенерацией) → считать по snapshot-данным; (3) для каждого активного — `buildVirtualPurchasePayments`; (4) **forward-fill курса**: конвертация amount CNY→₽ через `getRateForDate(dueDate)` — последний известный курс ≤ dueDate (для будущих дат = последний известный курс вообще). Верни платежи с флагом «версия устарела» если есть расхождения статусов.
Ноль React/Next; pure-ядро без Prisma (loader принимает db: PrismaClient).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/sales-plan-pdds-feed.test.ts 2>&1 | grep -iE "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-pdds-feed.test.ts` — все PASS (GREEN)
    - `grep -c "export function buildVirtualPurchasePayments\|export async function getPlannedRevenueSeries\|export async function getPlannedVirtualPayments" lib/sales-plan/pdds-feed.ts` === 3
    - live-сверка статусов: `grep -c "CONVERTED\|status" lib/sales-plan/pdds-feed.ts` ≥ 2
    - forward-fill курса: `grep -c "getRateForDate" lib/sales-plan/pdds-feed.ts` ≥ 1
    - pure-ядро использует procurement-math: `grep -c "computeDepositDueDate\|computeBalanceDueDate" lib/sales-plan/pdds-feed.ts` ≥ 2
    - `grep -c "from \"react\|from \"next" lib/sales-plan/pdds-feed.ts` === 0
  </acceptance_criteria>
  <done>pdds-feed.ts: pure-ядро (платежи VP) + loader (притоки, оттоки с live-сверкой CONVERTED/DISMISSED + forward-fill курса); Wave-0-стаб GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Зачистка — удаление SalesForecast*/ProductForecastDialog/IU_REMAINING_RUB/DEFAULT_END_DATE/старых ключей + деприкейт nav + backfill-патч</name>
  <files>components/sales-plan/SalesForecastTable.tsx, components/sales-plan/SalesForecastSummary.tsx, components/sales-plan/SalesForecastDailyChart.tsx, components/sales-plan/SalesForecastEndDate.tsx, components/sales-plan/ProductForecastDialog.tsx, app/actions/sales-plan.ts, app/(dashboard)/sales-plan/page.tsx, components/layout/nav-items.ts, app/api/wb-funnel-backfill/route.ts</files>
  <read_first>
    - components/sales-plan/SalesForecast*.tsx, ProductForecastDialog.tsx (удаляемые файлы — сначала grep всех импортов чтобы не оставить orphan-ссылок)
    - app/actions/sales-plan.ts (старые saveBaselineOverrides/savePriceOverrides/saveLeadTimes/clearBaselineOverrides + ключи salesPlan.baselineOverrides/priceOverrides/leadTimes — удалить)
    - components/layout/nav-items.ts (строки 49-50 /procurement/plan + /purchase-plan — деприкейт)
    - app/api/wb-funnel-backfill/route.ts (MAX_DAYS=30 — снять лимит на произвольные from/to)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §9 этап 6 (список зачистки), §7.6 (удаляемые компоненты)
    - .planning/phases/25-v2-h2-2026/25-VALIDATION.md
  </read_first>
  <action>
1. Удали файлы (rm): `components/sales-plan/SalesForecastTable.tsx`, `SalesForecastSummary.tsx`, `SalesForecastDailyChart.tsx`, `SalesForecastEndDate.tsx`, `ProductForecastDialog.tsx`. Перед удалением: `grep -rn "SalesForecast\|ProductForecastDialog" app components --include=*.tsx --include=*.ts` — убедись, что все импорты уже мигрированы на новые компоненты (Waves 4-6). Если остались ссылки — исправь.

2. `app/actions/sales-plan.ts`: удали старые actions `saveBaselineOverrides`, `savePriceOverrides`, `saveLeadTimes`, `clearBaselineOverrides` (заменены новыми Wave 3) + константы `BASELINE_KEY`/`PRICE_KEY`/`LEAD_TIMES_KEY`. Новые actions остаются.

3. Убери хардкоды: `grep -rn "IU_REMAINING_RUB\|DEFAULT_END_DATE" app lib components` → удали все определения и использования (заменены iuTargets/horizon из AppSetting).

4. Старые AppSetting-ключи: добавь одноразовую очистку в bootstrap-скрипт ИЛИ отдельный SQL-комментарий в SUMMARY (`DELETE FROM "AppSetting" WHERE key IN ('salesPlan.baselineOverrides','salesPlan.priceOverrides','salesPlan.leadTimes')` — выполнить на проде ПОСЛЕ подтверждения, что новый UI работает). НЕ выполняй автоматически в миграции (данные могут понадобиться для отката).

5. `components/layout/nav-items.ts`: деприкейт `/procurement/plan` (строка 49) и `/purchase-plan` (строка 50) — закомментируй или удали пункты sidebar (после недели параллельной работы — уточни с пользователем перед фактическим снятием; в этом плане — подготовь изменение, финальное снятие подтверждается на UAT). Роуты остаются доступны по прямой ссылке (только из sidebar убираются).

6. `app/api/wb-funnel-backfill/route.ts`: сними лимит `MAX_DAYS=30` от today — разреши произвольные from/to (fetchFunnelDaily умеет окно ≤31д; разбивай длинный диапазон на чанки ≤31д). Страховка от даунтайма крона на длинном горизонте.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -rc "IU_REMAINING_RUB\|DEFAULT_END_DATE" app lib components 2>/dev/null | grep -v ":0" | head; grep -rc "SalesForecastTable\|ProductForecastDialog" app components --include=*.tsx --include=*.ts 2>/dev/null | grep -v ":0" | head; npx tsc --noEmit 2>&1 | grep -iE "error TS" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - удалённые файлы отсутствуют: `test ! -f components/sales-plan/SalesForecastTable.tsx && test ! -f components/sales-plan/ProductForecastDialog.tsx` (exit 0)
    - нет orphan-ссылок: `grep -rc "SalesForecastTable\|ProductForecastDialog\|SalesForecastSummary" app components --include=*.tsx --include=*.ts | grep -v ":0" | wc -l` === 0
    - хардкоды удалены: `grep -rc "IU_REMAINING_RUB\|DEFAULT_END_DATE" app lib components | grep -v ":0" | wc -l` === 0
    - старые actions удалены: `grep -c "export async function saveBaselineOverrides\|export async function clearBaselineOverrides" app/actions/sales-plan.ts` === 0
    - backfill-патч: `grep -c "MAX_DAYS" app/api/wb-funnel-backfill/route.ts` === 0 (лимит снят) ИЛИ произвольные from/to поддержаны
    - `npx tsc --noEmit` — ноль ошибок (нет битых импортов после удаления)
  </acceptance_criteria>
  <done>SalesForecast*/ProductForecastDialog удалены, IU_REMAINING_RUB/DEFAULT_END_DATE/старые actions+ключи вычищены, nav деприкейт подготовлен, backfill без лимита. Кодовая база без дублей.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Финальный деплой + прод-задачи + end-to-end UAT + обновление CLAUDE.md</name>
  <what-built>
Этап 6: контракт ПДДС (pdds-feed.ts с live-сверкой статусов + forward-fill курса), зачистка легаси (SalesForecast*, IU_REMAINING_RUB, DEFAULT_END_DATE, старые actions/ключи), деприкейт /purchase-plan+/procurement/plan, backfill без лимита. Вся фаза 25 (план продаж v2) готова к финальному деплою.
  </what-built>
  <how-to-verify>
0. Обнови CLAUDE.md: добавь секцию про раздел «План продаж v2» (три таба /sales-plan{,/products,/purchases}, движок lib/sales-plan/, версии, ИУ, виртуальные закупки, pdds-feed контракт) по образцу секции Phase 7.
1. Деплой по CLAUDE.md: `git add -A && git commit` → `git push origin main` → `ssh root@85.198.97.89 "cd /opt/zoiten-pro && nohup bash deploy.sh > /var/log/zoiten-deploy.log 2>&1 &"` → следи за логом до `==> Done` → `curl https://zoiten.pro` → 200. Перед деплоем `df -h /` (≥5GB).
2. Прод-задачи (если ещё не выполнены в предыдущих этапах): (а) миграция `20260705_sales_plan_v2` применена (`prisma migrate deploy` в deploy.sh); (б) `npx tsx scripts/bootstrap-sales-plan-monthly.ts` запущен на VPS (`set -a; . /etc/zoiten.pro.env; set +a; npx tsx ...`) — старые overrides перенесены; (в) UserSectionRole по SALES проверены (у кого VIEW → выдать MANAGE + перелогин); (г) `plannedArrivalDate` заполнены по открытым закупкам (постепенно, +45 приемлем).
3. End-to-end UAT на проде (https://zoiten.pro/sales-plan):
   - **Сводный**: три ряда план/факт/ИУ по бакетам, KPI «Отставание от ИУ», строка «Вне плана», график факт/план/ИУ.
   - **Товары**: редактирование уровней (bulk «Пересчитать план»), модалка «Дни» realtime «Сток(расч)», приходы 📦/◇/⚠, «Масштабировать месяц».
   - **Пора заказывать**: предложения, подтвердить/отклонить одну, конвертировать одну → префилл PurchaseModal.
   - **Версии**: зафиксировать, переключиться на версию (read-only баннер), вернуться к черновику.
   - **RBAC**: VIEW-пользователь не может писать (write отвергается).
   - **Старый UI отсутствует**: /sales-plan показывает новую матрицу, нет SalesForecastTable.
4. Подтверди снятие /purchase-plan+/procurement/plan из sidebar (после недели параллельной работы — если рано, оставь роуты, но пункты sidebar скрой).
  </how-to-verify>
  <resume-signal>Напиши «approved» после успешного end-to-end UAT на проде, или опиши проблемы (напр. bootstrap не перенёс overrides, факт не сходится с ИУ, конвертация не префиллит, RBAC не блокирует VIEW).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version paramsJson → pdds-feed | Immutable снапшот VP; live-статусы могут расходиться |
| CNY amount → ₽ | forward-fill курса на будущие dueDate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Tampering | двойной отток ПДДС при convert-после-фиксации | mitigate | getPlannedVirtualPayments сверяет vpIds из paramsJson с live status — CONVERTED/DISMISSED исключаются (§8) |
| T-25-09 | Information Disclosure | старые AppSetting-ключи после зачистки | accept | DELETE выполняется вручную на проде ПОСЛЕ подтверждения нового UI (возможность отката) |
| T-25-10 | Denial of Service | backfill без лимита на длинный диапазон | mitigate | разбивка на чанки ≤31д (fetchFunnelDaily окно ≤31д) |
</threat_model>

<verification>
- `npm run test` — ВСЯ sales-plan суита (7 файлов) GREEN, включая pdds-feed.
- `npx tsc --noEmit` — ноль ошибок (зачистка не оставила битых импортов).
- pdds-feed: pure-ядро + loader с live-сверкой + forward-fill.
- Легаси удалён (SalesForecast*, IU_REMAINING_RUB, DEFAULT_END_DATE, старые actions/ключи).
- Прод: 3 таба работают, миграция+bootstrap применены, curl 200.
</verification>

<success_criteria>
Этап 6 завершён (SP-12 контракт ПДДС + SP-13 зачистка): pdds-feed для следующей фазы, кодовая база без дублей, деприкейт временных разделов. Фаза 25 «План продаж v2» полностью в проде — три ряда (план/факт/ИУ), помесячные уровни с дневной детализацией, приходы, виртуальные закупки, версионирование, контракт ПДДС.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-09-SUMMARY.md`. Зафиксируй: дату финального деплоя, результаты bootstrap на проде, факт очистки старых AppSetting-ключей, статус снятия /purchase-plan из sidebar.
</output>
