---
phase: 25-v2-h2-2026
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - app/actions/sales-plan.ts
  - app/actions/procurement.ts
  - app/(dashboard)/procurement/purchases/[id]/page.tsx
  - components/procurement/PurchaseDetailActions.tsx
autonomous: true
requirements: [SP-04, SP-05, SP-13]
must_haves:
  truths:
    - "saveMonthLevels/scaleMonthLevels/saveDayOverrides/saveProductPlanParams/saveModelParams — все требуют SALES MANAGE (фикс дыры VIEW-write)"
    - "getProductPlanDays (read) возвращает дни месяца + сериализуемый ProductPlanInput для клиентского realtime-пересчёта"
    - "scaleMonthLevels материализует baseline×factor для товаров с targetOrdersPerDay=null"
    - "Поле «Плановая дата прихода» редактируется в карточке закупки под PROCUREMENT MANAGE"
    - "Все write-actions делают revalidatePath трёх роутов /sales-plan{,/products,/purchases}"
  artifacts:
    - path: "app/actions/sales-plan.ts"
      provides: "saveMonthLevels, scaleMonthLevels, saveDayOverrides, saveProductPlanParams, saveModelParams, getProductPlanDays"
      exports: ["saveMonthLevels", "scaleMonthLevels", "saveDayOverrides", "saveProductPlanParams", "saveModelParams", "getProductPlanDays"]
    - path: "app/actions/procurement.ts"
      provides: "savePlannedArrivalDate (PROCUREMENT MANAGE)"
      contains: "plannedArrivalDate"
  key_links:
    - from: "app/actions/sales-plan.ts (getProductPlanDays)"
      to: "lib/sales-plan/engine.ts + data.ts"
      via: "loader + simulateProductPlan → сериализуемый вход для клиента"
      pattern: "loadSalesPlanInputs|computeSalesPlan"
    - from: "app/actions/sales-plan.ts (все write)"
      to: "requireSection(SALES, MANAGE)"
      via: "RBAC-фикс"
      pattern: "requireSection.*SALES.*MANAGE"
---

<objective>
Этап 2 (Товары), часть 1: server actions редактирования плана + read-action дневных данных + поле «Плановая дата прихода» в карточке закупки. Все write-actions поднимаются до `SALES MANAGE` (фикс дыры: старые `saveBaselineOverrides` требовали лишь VIEW).

Purpose: actions — контракт, на который опирается Товары-UI (25-04). getProductPlanDays возвращает не только дни месяца, но и полный сериализуемый `ProductPlanInput` (arrivals/seed/monthLevels/dayOverrides/параметры) для realtime-пересчёта «Сток(расч)» на клиенте локальным запуском pure `simulateProductPlan`. plannedArrivalDate замыкает resolver дат приходов на реальные закупки.
Output: переработанный `app/actions/sales-plan.ts` + `savePlannedArrivalDate` в procurement + поле в карточке закупки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Из lib/sales-plan/data.ts (Wave 2): loadSalesPlanInputs(db, params), loadFactDaily(db, from, to).
Из lib/sales-plan/engine.ts (Wave 1): computeSalesPlan, ProductPlanInput, PlanDayRow, SalesPlanResult.
Prisma-модели (Wave 1): SalesPlanMonthLevel, SalesPlanDayOverride, Purchase.plannedArrivalDate.
Текущий app/actions/sales-plan.ts: setGlobalJson (строки 21-32) — копировать; saveBaselineOverrides/savePriceOverrides/saveLeadTimes/clearBaselineOverrides — старые (VIEW), остаются до Wave 6 зачистки (не удалять сейчас).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/actions/sales-plan.ts — новые write-actions (SALES MANAGE) + getProductPlanDays</name>
  <files>app/actions/sales-plan.ts</files>
  <read_first>
    - app/actions/sales-plan.ts (текущий — setGlobalJson строки 21-32 копировать; паттерн ActionResult; НЕ удалять старые actions)
    - app/actions/pricing.ts (строки 100-118 — паттерн action с MANAGE + Zod safeParse + try/catch + revalidatePath)
    - lib/sales-plan/data.ts, engine.ts (loader + движок для getProductPlanDays)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.3 (scaleMonthLevels материализация baseline, ProductPlanDialog getProductPlanDays), §7.5 (RBAC), §7.6 (список actions)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки `app/actions/sales-plan.ts` (315-370), «Обработка ошибок» (911-933), «AppSetting JSON» (993-1016)
  </read_first>
  <action>
В `app/actions/sales-plan.ts` (добавь новые, старые оставь до Wave 6). Все write — `await requireSection("SALES", "MANAGE")`, Zod safeParse, try/catch с `console.error`, revalidatePath трёх роутов `/sales-plan`, `/sales-plan/products`, `/sales-plan/purchases`. Тип `ActionResult = { ok: true } | { ok: false; error: string }`.

1. `saveMonthLevels(payload: Array<{ productId, month, targetOrdersPerDay: number|null, priceRub: number|null, buyoutPct: number|null }>)`: upsert per (productId, month) в SalesPlanMonthLevel; если все три поля null — deleteMany (нет уровня). Zod: массив, month regex YYYY-MM-01, числа nullable ≥0.

2. `scaleMonthLevels(payload: { month: string, factor: number, productIds?: string[] })`: top-down подгон под ИУ. Для товаров с существующим `targetOrdersPerDay` → `× factor`. Для товаров с `targetOrdersPerDay=null` (после bootstrap большинство) — **материализуй baseline**: вычисли baseline из funnel (через loadSalesPlanInputs или отдельный запрос avg last-7d) и запиши `baselineOrdersPerDay × factor` в SalesPlanMonthLevel (снапшот на момент масштабирования, §7.3). Day-overrides месяца НЕ трогай. Верни `{ ok: true, materializedCount, scaledCount }` для диалога подтверждения.

3. `saveDayOverrides(payload: { productId, overrides: Record<dateIso, number|null> })`: upsert per (productId, date); null → delete. Zod: date regex YYYY-MM-DD, ordersPerDay ≥0.

4. `saveProductPlanParams(payload: { productId, month, priceRub: number|null, buyoutPct: number|null })`: обновляет только priceRub/buyoutPct месячного уровня (вкладка «Параметры» модалки), targetOrdersPerDay не трогает.

5. `saveModelParams(payload: { defaultLeadTimeDays?, safetyStockDays?, vpCoverDays?, transitDays?, wbInboundLagDays?, deliveryDays?, returnDays? })`: пишет в AppSetting-ключи `salesPlan.defaultLeadTimeDays`, `salesPlan.safetyStockDays`, `salesPlan.vpCoverDays`, `salesPlan.transitDays`, `salesPlan.wbInboundLagDays`, `salesPlan.leadTimes2` (deliveryDays/returnDays) через setGlobalJson/upsert. Zod: числа int в границах (0..365).

6. `getProductPlanDays(productId: string, month: string, versionId?: string)` — **read-action** (`requireSection("SALES")` без MANAGE): загрузи `loadSalesPlanInputs(prisma, params)`, найди `ProductPlanInput` этого товара, прогони движок для полного дневного ряда, отфильтруй дни месяца. Верни `{ ok: true, days: PlanDayRow[], productInput: ProductPlanInput }` — productInput сериализуемый (для клиентского realtime). Если versionId задан — комментарий-TODO: дни из SalesPlanVersionDay (read-only, Wave 7); сейчас всегда из драфта.

Все actions используют `import { prisma } from "@/lib/prisma"` и передают его в loader (DI). Цепочку `regenerateVirtualPurchases` в пересчёт добавит Wave 6 — сейчас комментарий-TODO в saveMonthLevels/saveDayOverrides.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c 'requireSection("SALES", "MANAGE")' app/actions/sales-plan.ts; npx tsc --noEmit 2>&1 | grep -i "actions/sales-plan" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function saveMonthLevels\|export async function scaleMonthLevels\|export async function saveDayOverrides\|export async function saveProductPlanParams\|export async function saveModelParams\|export async function getProductPlanDays" app/actions/sales-plan.ts` === 6
    - `grep -c 'requireSection("SALES", "MANAGE")' app/actions/sales-plan.ts` ≥ 5 (все 5 write-actions)
    - getProductPlanDays использует read-level RBAC (requireSection("SALES") без MANAGE) — присутствует строка
    - `grep -c "loadSalesPlanInputs\|computeSalesPlan" app/actions/sales-plan.ts` ≥ 1 (getProductPlanDays гоняет движок)
    - scaleMonthLevels материализует baseline: `grep -ci "materialized\|baseline" app/actions/sales-plan.ts` ≥ 1
    - каждый write делает revalidatePath 3 роутов: `grep -c 'revalidatePath("/sales-plan/products")' app/actions/sales-plan.ts` ≥ 5
    - `npx tsc --noEmit` без ошибок про actions/sales-plan.ts
  </acceptance_criteria>
  <done>6 новых actions; все write под SALES MANAGE; getProductPlanDays отдаёт дни + сериализуемый вход для клиентского realtime; старые actions не тронуты.</done>
</task>

<task type="auto">
  <name>Task 2: savePlannedArrivalDate + поле «Плановая дата прихода» в карточке закупки</name>
  <files>app/actions/procurement.ts, app/(dashboard)/procurement/purchases/[id]/page.tsx, components/procurement/PurchaseDetailActions.tsx</files>
  <read_first>
    - app/actions/procurement.ts (паттерн action PROCUREMENT MANAGE + Zod + revalidatePath)
    - app/(dashboard)/procurement/purchases/[id]/page.tsx (структура карточки закупки — куда встроить поле; RSC → PurchaseDetailActions client wrapper)
    - components/procurement/PurchaseDetailActions.tsx (client-обёртка — паттерн inline-редактирования, чтобы detail page остался RSC)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.5 (plannedArrivalDate под PROCUREMENT MANAGE, prefill createdAt+leadTimeDays), §3.4 (это приоритетный источник дат)
    - .planning/phases/25-v2-h2-2026/CRITIC-VERDICT.md (подтверждено: Purchase.plannedArrivalDate добавлено в Wave 1)
  </read_first>
  <action>
1. В `app/actions/procurement.ts` добавь `savePlannedArrivalDate(payload: { purchaseId: string, date: string | null }): Promise<ActionResult>`:
   - `await requireSection("PROCUREMENT", "MANAGE")`.
   - Zod: date `null | YYYY-MM-DD regex`.
   - `prisma.purchase.update({ where: { id }, data: { plannedArrivalDate: date === null ? null : new Date(date + "T00:00:00Z") } })`.
   - `revalidatePath("/procurement/purchases")` + `revalidatePath(\`/procurement/purchases/${purchaseId}\`)` + `revalidatePath("/sales-plan/products")` + `revalidatePath("/sales-plan")` (resolver дат перечитывается).
   - try/catch, ActionResult.

2. В карточке закупки добавь поле «Плановая дата прихода в Иваново» — native `<input type="date">` (client, через PurchaseDetailActions или новый маленький client-компонент; detail page остаётся RSC). Значение = `purchase.plannedArrivalDate` (ISO). При пустом — placeholder/хинт показывает расчётный prefill `createdAt + leadTimeDays` (из SupplierProductLink, fallback 45) как «расчётно: DD.MM.YYYY». onBlur/onChange → `savePlannedArrivalDate`. disabled если не canManage (PROCUREMENT). Подпись-хинт: «приоритетный источник дат для плана продаж; без неё — эвристика createdAt+45». Соблюдай CLAUDE.md: native input, русский язык, MSK-время, revalidatePath.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "savePlannedArrivalDate" app/actions/procurement.ts; npx tsc --noEmit 2>&1 | grep -i "procurement" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function savePlannedArrivalDate" app/actions/procurement.ts` === 1
    - `grep -c 'requireSection("PROCUREMENT", "MANAGE")' app/actions/procurement.ts` (savePlannedArrivalDate) ≥ 1 в новой функции
    - `grep -rc "plannedArrivalDate" "app/(dashboard)/procurement/purchases/[id]/page.tsx" components/procurement/PurchaseDetailActions.tsx` — присутствует минимум в одном файле карточки
    - savePlannedArrivalDate делает revalidatePath /sales-plan/products: `grep -c 'revalidatePath("/sales-plan/products")' app/actions/procurement.ts` ≥ 1
    - `npx tsc --noEmit` без новых ошибок про procurement
  </acceptance_criteria>
  <done>savePlannedArrivalDate под PROCUREMENT MANAGE; поле даты в карточке закупки редактируемо с prefill-хинтом; resolver дат в плане продаж перечитывается при сохранении.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | Пользователь через UI шлёт payload редактирования плана / даты прихода |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | write-actions sales-plan | mitigate | `requireSection("SALES","MANAGE")` на всех write (фикс текущей дыры VIEW-write, SP-13) |
| T-25-02 | Tampering | payload month/date/factor | mitigate | Zod safeParse — regex YYYY-MM(-DD), числа в границах; невалид → отказ до Prisma |
| T-25-03 | Elevation of Privilege | savePlannedArrivalDate | mitigate | `requireSection("PROCUREMENT","MANAGE")` — только закупщик правит даты закупок |
| T-25-04 | Information Disclosure | getProductPlanDays | accept | read под SALES VIEW — план продаж виден всем с доступом к секции (внутренняя ERP) |
</threat_model>

<verification>
- `npx tsc --noEmit` — все actions типобезопасны на новых Prisma-моделях + движке.
- 6 новых sales-plan actions, 5 write под MANAGE, getProductPlanDays под VIEW.
- savePlannedArrivalDate под PROCUREMENT MANAGE, revalidatePath замыкает план продаж.
- Старые actions (saveBaselineOverrides и др.) не тронуты — удаляются в Wave 6.
</verification>

<success_criteria>
Контракт actions Товары-таба готов: редактирование уровней/дней/параметров/модели (все SALES MANAGE), getProductPlanDays для realtime-модалки, plannedArrivalDate в карточке закупки. 25-04 (Товары UI) строится поверх этих actions.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-05-SUMMARY.md`. Отметь прод-задачу этапа 2: проверить UserSectionRole по SALES перед деплоем (у кого VIEW → write отвалится; выдать MANAGE + перелогин, JWT не самообновляется).
</output>
