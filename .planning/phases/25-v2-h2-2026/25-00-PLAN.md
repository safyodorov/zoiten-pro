---
phase: 25-v2-h2-2026
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/sales-plan-engine.test.ts
  - tests/sales-plan-arrivals.test.ts
  - tests/sales-plan-iu.test.ts
  - tests/date-buckets.test.ts
  - tests/sales-plan-plan-fact.test.ts
  - tests/sales-plan-virtual.test.ts
  - tests/sales-plan-pdds-feed.test.ts
autonomous: true
requirements: [SP-02, SP-03]
must_haves:
  truths:
    - "7 тест-файлов существуют и падают (RED) с осмысленными describe/it"
    - "Golden-якорь ИУ 438 068 120 ₽ зафиксирован в sales-plan-iu.test.ts"
    - "npm run test обнаруживает 7 новых файлов (не пустые, не skip)"
  artifacts:
    - path: "tests/sales-plan-iu.test.ts"
      provides: "Golden ИУ = 438 068 120 ₽"
      contains: "438_068_120"
    - path: "tests/sales-plan-engine.test.ts"
      provides: "Golden engine стаб"
      contains: "computeSalesPlan"
    - path: "tests/date-buckets.test.ts"
      provides: "quarter/halfyear/year бакеты стаб"
      contains: "2026-Q3"
  key_links:
    - from: "tests/*.test.ts"
      to: "lib/sales-plan/*, lib/date-buckets.ts"
      via: "import (RED — модули ещё не существуют)"
      pattern: "from \"@/lib/sales-plan"
---

<objective>
Wave 0: создать 7 vitest-файлов как RED-стабы (тесты падают, потому что `lib/sales-plan/*` и `lib/date-buckets.ts` ещё не написаны). Это тестовая инфраструктура фазы 25 — по мере реализации движка (Waves 1, 3, 4, 6) стабы доводятся до GREEN.

Purpose: зафиксировать контракт движка (сигнатуры, golden-значения) ДО реализации — Nyquist-compliance (каждый расчётный task имеет `<automated>` команду). Golden-якорь ИУ 438 068 120 ₽ и engine-инварианты — регрессионные защиты.
Output: 7 файлов в `tests/`, каждый импортирует ещё-не-существующий модуль → `npm run test` показывает RED для новых файлов, остальной suite зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-VALIDATION.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md

<interfaces>
<!-- Контракты, которые стабы фиксируют. Реализуются в Waves 1/3/4/6. -->
Из lib/sales-plan/engine.ts (Wave 1):
```typescript
export function computeSalesPlan(inputs: SalesPlanInputs): SalesPlanResult
// SalesPlanInputs: { today, horizonFrom, horizonTo, deliveryDays, returnDays, wbInboundLagDays, products: ProductPlanInput[] }
// ProductPlanInput: { productId, sku, name, nmIds, stockNow, baselineOrdersPerDay, buyoutPct, buyoutSource, avgPriceRub, monthLevels[], dayOverrides{}, arrivals[], seedOrders{} }
// SalesPlanResult: { products: ProductPlanResult[], companyDaily[], companyMonthly[] }
// ProductPlanResult: { productId, days: PlanDayRow[], monthTotals[], firstStockoutDate, lostUnitsToStockout, lostRubToStockout }
// PlanDayRow: { date, ordersUnits, buyoutsUnits, buyoutsRub, ordersRub, stockEnd, rateRequested }
```
Из lib/sales-plan/arrivals.ts (Wave 1):
```typescript
export function resolveArrivalBatches(input): ArrivalBatch[]
// ArrivalBatch: { date, qty, source: "purchase"|"virtual"|"incoming-legacy", refId, dateSource: "manual"|"transit-eta"|"leadtime-eta"|"legacy-expected" }
```
Из lib/sales-plan/iu.ts (Wave 1):
```typescript
export function iuTotalForRange(from: string, to: string, targets: IuTarget[]): number
export function iuSeriesForRange(from, to, targets): Array<{ date: string; cumulative: number }>
// IuTarget: { from: string; to: string; dailyRub: number }
```
Из lib/date-buckets.ts (Wave 1):
```typescript
export type Granularity = "day"|"week"|"month"|"quarter"|"halfyear"|"year"
export function bucketKey(date: Date, granularity: Granularity): string
export function bucketLabel(key: string, granularity: Granularity): string
```
Из lib/sales-plan/plan-fact.ts (Wave 3):
```typescript
export function buildPlanFactReport(input): PlanFactReport
```
Из lib/sales-plan/virtual-purchases.ts (Wave 4):
```typescript
export function suggestVirtualPurchases(input): VpSuggestion[]
```
Из lib/sales-plan/pdds-feed.ts (Wave 6):
```typescript
export function buildVirtualPurchasePayments(vpSnapshot): Array<{ type, dueDate, amount, currency }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED-стабы движка — engine, arrivals, iu, date-buckets</name>
  <files>tests/sales-plan-engine.test.ts, tests/sales-plan-arrivals.test.ts, tests/sales-plan-iu.test.ts, tests/date-buckets.test.ts</files>
  <read_first>
    - tests/pricing-math.test.ts (канонический образец структуры vitest golden-теста — импорт, describe/it, toBeCloseTo)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §3.1 (типы), §3.5 (список тестов), §Validation Architecture
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки `tests/sales-plan-engine.test.ts` (строки 813-858) и `tests/date-buckets.test.ts` (строки 862-882) — готовые скелеты
  </read_first>
  <action>
Создай 4 vitest-файла. Каждый импортирует ещё-не-существующий модуль (RED через компиляцию/резолв).

1. `tests/sales-plan-engine.test.ts` — `import { computeSalesPlan, type SalesPlanInputs } from "@/lib/sales-plan/engine"`. Собери `goldenInputs` дословно из 25-PATTERNS.md строки 822-844 (today "2026-07-01", horizonFrom/To "2026-07-01"/"2026-08-31", deliveryDays 3, returnDays 3, wbInboundLagDays 0; 1 товар stockNow 100, baselineOrdersPerDay 10, buyoutPct 0.8, avgPriceRub 5000; monthLevels июль targetOrdersPerDay 12 priceRub 5000 buyoutPct 0.8, август 15/5500/0.8; dayOverrides {"2026-07-15": 20}; arrivals [{date "2026-07-20", qty 500, source "purchase", refId "p1", dateSource "manual"}, {date "2026-08-10", qty 300, source "virtual", refId "vp1", dateSource "manual"}]; seedOrders {"2026-06-28":10,"2026-06-29":12,"2026-06-30":11}). Тесты `it`: (a) T+3 выкупы — заказ 2026-07-01 → выкуп 2026-07-04 = `toBeCloseTo(12*0.8, 2)`; (b) day override 2026-07-15 rateRequested === 20; (c) ступенька — 2026-08-01 rateRequested === 15 (не 12); (d) сток-лимит — `orders[d] <= stockEnd[d-1]` для всех дней; (e) zero-guard — при stockNow 0 orders === 0 до первого прихода. Не мокай — вызывай реальную (ещё несуществующую) функцию.

2. `tests/sales-plan-arrivals.test.ts` — `import { resolveArrivalBatches } from "@/lib/sales-plan/arrivals"`. Тесты (по §3.4 RESEARCH): (a) уровень 1 `plannedArrivalDate` приоритет → dateSource "manual"; (b) уровень 2 TRANSIT.date + transitDays(20) → dateSource "transit-eta"; (c) TRANSIT.qty=0 или date=null → пропуск на уровень 3; (d) частичный TRANSIT (qty < qtyRemaining) → сплит на 2 партии (transit-eta + leadtime-eta); (e) уровень 3 `createdAt + 45` → dateSource "leadtime-eta"; (f) уровень 4 legacy `ProductIncoming.expectedDate` → "legacy-expected"; (g) уровень 5 null → партия не создаётся. Формируй минимальные входные объекты по типам ArrivalBatch.

3. `tests/sales-plan-iu.test.ts` — `import { iuTotalForRange, iuSeriesForRange } from "@/lib/sales-plan/iu"`. КРИТИЧНЫЙ golden: `expect(iuTotalForRange("2026-07-01","2026-12-31",[{from:"2026-07-01",to:"2026-12-31",dailyRub:2_380_805}])).toBe(438_068_120)`. Плюс: 184 дня в диапазоне; границы (from==to → 1 день === 2_380_805); мульти-периоды (2 target в массиве суммируются); iuSeriesForRange последний cumulative === 438_068_120.

4. `tests/date-buckets.test.ts` — `import { bucketKey, bucketLabel, type Granularity } from "@/lib/date-buckets"`. Тесты дословно из 25-PATTERNS.md строки 870-880: quarter 2026-07-15 → "2026-Q3"; halfyear → "2026-H2"; year → "2026"; плюс регресс day/week/month (перенос из loan-math): month 2026-07-15 → "2026-07"; bucketLabel "2026-Q3" quarter → "Q3 2026".
  </action>
  <verify>
    <automated>npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-arrivals.test.ts tests/sales-plan-iu.test.ts tests/date-buckets.test.ts 2>&1 | grep -iE "Cannot find module|failed|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/sales-plan-iu.test.ts` существует и содержит `438_068_120`: `grep -c "438_068_120" tests/sales-plan-iu.test.ts` ≥ 1
    - Все 4 файла импортируют несуществующие модули → vitest падает с «Cannot find module '@/lib/...'» (это ожидаемый RED)
    - `grep -c "computeSalesPlan" tests/sales-plan-engine.test.ts` ≥ 1
    - `grep -c "2026-Q3" tests/date-buckets.test.ts` ≥ 1
  </acceptance_criteria>
  <done>4 RED-стаба созданы, каждый падает на резолве модуля движка, golden-якорь ИУ зафиксирован.</done>
</task>

<task type="auto">
  <name>Task 2: RED-стабы plan-fact, virtual, pdds-feed</name>
  <files>tests/sales-plan-plan-fact.test.ts, tests/sales-plan-virtual.test.ts, tests/sales-plan-pdds-feed.test.ts</files>
  <read_first>
    - tests/pricing-math.test.ts (структура)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §4 (virtual-purchases), §6.2 (plan-fact формулы), §8 (pdds-feed), §3.5 (список тестов)
    - .planning/phases/25-v2-h2-2026/25-VALIDATION.md (per-task verification map — SP-07/10 plan-fact, SP-08 virtual, SP-09/12 pdds-feed)
  </read_first>
  <action>
Создай 3 vitest-файла как RED-стабы:

1. `tests/sales-plan-plan-fact.test.ts` — `import { buildPlanFactReport } from "@/lib/sales-plan/plan-fact"`. Тесты (§6.2): (a) бакетирование day/week/month/quarter/halfyear/year — группировка versionDays по bucketKey; (b) deviation ₽ = факт − план, % = (факт/план − 1)×100; (c) pro-rata — незавершённый бакет (содержит today) считает план только за дни ≤ вчера; (d) factSettled — дни > today−7 помечены unsettled; (e) строка «Вне плана» — company-level факт минус product-level = разница, суммы сходятся. Минимальные фикстуры дневных строк плана/факта.

2. `tests/sales-plan-virtual.test.ts` — `import { suggestVirtualPurchases } from "@/lib/sales-plan/virtual-purchases"`. Тесты (§4.1): (a) триггер — пробой страхового запаса `projectedStock(d) < safetyStockDays(14) × rate(d)` создаёт предложение; (b) qty на покрытие 60 дней; (c) clamp — orderDate = max(today, breach − leadTime); (d) итеративный roll-forward — товару может понадобиться 2-3 предложения за проход; (e) minQty — qty < 10 → стоп.

3. `tests/sales-plan-pdds-feed.test.ts` — `import { buildVirtualPurchasePayments } from "@/lib/sales-plan/pdds-feed"`. Тесты pure-ядра (§8, только НЕ-async функция; loader-функции тестируются вручную UAT): (a) DEPOSIT dueDate = orderDate + 3; (b) BALANCE dueDate = deposit + leadTime; (c) depositPct/balancePct fallback 30/70; (d) amount в валюте закупки (CNY), не конвертируется в pure-ядре. Vp-снапшот минимальный.
  </action>
  <verify>
    <automated>npx vitest run tests/sales-plan-plan-fact.test.ts tests/sales-plan-virtual.test.ts tests/sales-plan-pdds-feed.test.ts 2>&1 | grep -iE "Cannot find module|failed" | head</automated>
  </verify>
  <acceptance_criteria>
    - 3 файла существуют: `ls tests/sales-plan-plan-fact.test.ts tests/sales-plan-virtual.test.ts tests/sales-plan-pdds-feed.test.ts` без ошибок
    - `grep -c "buildPlanFactReport" tests/sales-plan-plan-fact.test.ts` ≥ 1
    - `grep -c "suggestVirtualPurchases" tests/sales-plan-virtual.test.ts` ≥ 1
    - `grep -c "buildVirtualPurchasePayments" tests/sales-plan-pdds-feed.test.ts` ≥ 1
    - vitest падает на резолве `@/lib/sales-plan/*` (ожидаемый RED)
  </acceptance_criteria>
  <done>3 RED-стаба созданы; вся Wave 0 тест-инфраструктура зафиксирована; существующий suite (`npm run test` для прочих файлов) не сломан.</done>
</task>

</tasks>

<verification>
- 7 новых тест-файлов существуют в `tests/`.
- Каждый падает на резолве несуществующего модуля движка (RED) — это корректное состояние Wave 0.
- Существующие тесты (pricing-math, balance-math и др.) не затронуты — `npx vitest run tests/pricing-math.test.ts` зелёный.
- Golden-якорь `438_068_120` присутствует в sales-plan-iu.test.ts.
</verification>

<success_criteria>
Wave 0 завершена: контракты движка зафиксированы в 7 RED-тестах, golden ИУ = 438 068 120 ₽, engine/arrivals/date-buckets инварианты описаны. Waves 1/3/4/6 доводят их до GREEN.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-00-SUMMARY.md`
</output>
