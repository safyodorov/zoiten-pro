---
phase: 25-v2-h2-2026
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260705_sales_plan_v2/migration.sql
autonomous: true
requirements: [SP-01]
must_haves:
  truths:
    - "Миграция создаёт 5 таблиц + enum + Purchase.plannedArrivalDate + сид AppSetting"
    - "prisma/schema.prisma содержит все 5 моделей с back-relations Product/Supplier/User"
    - "npx prisma generate проходит без ошибок; npx prisma migrate deploy применяет миграцию локально"
    - "Старый sales-plan (lib/sales-forecast.ts, AppSetting salesPlan.baselineOverrides) не сломан — миграция аддитивна"
  artifacts:
    - path: "prisma/migrations/20260705_sales_plan_v2/migration.sql"
      provides: "DDL всех новых таблиц + enum + ALTER Purchase + сид AppSetting"
      contains: "CREATE TABLE \"SalesPlanMonthLevel\""
    - path: "prisma/schema.prisma"
      provides: "Prisma-модели SalesPlanMonthLevel/DayOverride/VirtualPurchase/Version/VersionDay + Purchase.plannedArrivalDate"
      contains: "model SalesPlanVersionDay"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260705_sales_plan_v2/migration.sql"
      via: "модели ⇄ DDL синхронны (одно поле = одна колонка)"
      pattern: "VirtualPurchaseStatus"
    - from: "Product / Supplier / User"
      to: "новые модели"
      via: "back-relations"
      pattern: "salesPlanMonthLevels|virtualPurchases|salesPlanVersions"
---

<objective>
[BLOCKING] Этап 1 (Фундамент), часть 1: рукописная аддитивная миграция `20260705_sales_plan_v2` со всеми новыми таблицами модели данных плана продаж v2 (§2 RESEARCH) + синхронное обновление `prisma/schema.prisma`.

Purpose: заложить схему БД, на которой строятся движок (Wave 1), data-loader (Wave 1), UI (Waves 2-5), pdds-feed (Wave 6). Проект использует manual SQL миграции + `prisma migrate deploy` (НЕ `prisma db push`) — миграция пишется руками, применяется локально `migrate deploy`, на проде через deploy.sh.
Output: `migration.sql` + обновлённый `schema.prisma`, `prisma generate` регенерирует клиент с новыми типами.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@.planning/phases/25-v2-h2-2026/CRITIC-VERDICT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma-модели SalesPlan* + VirtualPurchase + back-relations + Purchase.plannedArrivalDate</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (модели Purchase, Supplier, Product, User, AppSetting, FinanceStockSnapshot ~строка 1813 — паттерн productId-без-FK + денорм sku/name; WbCardFunnelDaily ~1104 — паттерн no-FK)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §2.1-2.5 (полные prisma-блоки моделей с полями и комментариями)
    - .planning/phases/25-v2-h2-2026/CRITIC-VERDICT.md (дыра 13а: Supplier back-relation virtualPurchases обязателен для Prisma)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок «Снапшот immutable без FK» (строки 1020-1038)
  </read_first>
  <action>
Добавь в `prisma/schema.prisma` (в конец, с docs-комментарием «Phase 25: План продаж v2 (2026-07)») ДОСЛОВНО модели из §2 RESEARCH:

1. `enum VirtualPurchaseStatus { SUGGESTED ACCEPTED DISMISSED CONVERTED }` (§2.3).

2. `model SalesPlanMonthLevel` (§2.1): id cuid; productId + `product Product @relation(onDelete: Cascade)`; `month DateTime @db.Date`; `targetOrdersPerDay Float?`; `priceRub Float?`; `buyoutPct Float?`; comment String?; updatedAt @updatedAt; updatedBy String?; `@@unique([productId, month])`; `@@index([month])`.

3. `model SalesPlanDayOverride` (§2.2): id cuid; productId + relation Cascade; `date DateTime @db.Date`; `ordersPerDay Float` (абсолютная ставка, не дельта); updatedAt; updatedBy String?; `@@unique([productId, date])`; `@@index([date])`.

4. `model VirtualPurchase` (§2.3): id cuid; productId + relation Cascade; `supplierId String?` + `supplier Supplier? @relation(onDelete: SetNull)`; `qty Int`; `orderDate DateTime @db.Date`; `expectedArrivalDate DateTime @db.Date`; `leadTimeDaysUsed Int?`; `unitPrice Decimal? @db.Decimal(14,4)`; `currency String @default("CNY")`; `source String @default("auto")`; `status VirtualPurchaseStatus @default(SUGGESTED)`; `convertedPurchaseId String?` (БЕЗ FK); comment String?; createdAt @default(now()); updatedAt @updatedAt; `@@index([productId, expectedArrivalDate])`; `@@index([status])`.

5. `model SalesPlanVersion` (§2.4): id cuid; `label String`; `kind String @default("user")`; `horizonFrom DateTime @db.Date`; `horizonTo DateTime @db.Date`; `paramsJson Json`; note String?; `createdById String?` + `createdBy User? @relation(onDelete: SetNull)`; createdAt @default(now()); `days SalesPlanVersionDay[]`; `@@index([createdAt])`.

6. `model SalesPlanVersionDay` (§2.4, паттерн FinanceStockSnapshot — БЕЗ @relation на Product): `id BigInt @id @default(autoincrement())`; versionId + `version SalesPlanVersion @relation(onDelete: Cascade)`; `productId String` (БЕЗ @relation!); `sku String`; `name String`; `date DateTime @db.Date`; поля Float: planOrdersUnits, planOrdersRub, planBuyoutsUnits, planBuyoutsRub, priceUsed, buyoutPctUsed, stockEndUnits; `@@unique([versionId, productId, date])`; `@@index([versionId, date])`.

Back-relations (§2.5): в `model Product` добавь `salesPlanMonthLevels SalesPlanMonthLevel[]`, `salesPlanDayOverrides SalesPlanDayOverride[]`, `virtualPurchases VirtualPurchase[]`; в `model Supplier` добавь `virtualPurchases VirtualPurchase[]`; в `model User` добавь `salesPlanVersions SalesPlanVersion[]`.

В `model Purchase` добавь новое поле: `plannedArrivalDate DateTime? @db.Date` с комментарием «ПЛАНОВАЯ дата прихода в Иваново. Приоритетный источник дат для плана продаж (resolver §3.4)».

НЕ трогай `ProductIncoming`, production-sync, старые AppSetting-ключи — миграция строго аддитивна.

Запусти `npx prisma format` затем `npx prisma generate`.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx prisma validate && npx prisma generate 2>&1 | grep -iE "Generated|error" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model SalesPlanMonthLevel\|model SalesPlanDayOverride\|model VirtualPurchase\|model SalesPlanVersion\|model SalesPlanVersionDay" prisma/schema.prisma` === 5
    - `grep -c "enum VirtualPurchaseStatus" prisma/schema.prisma` === 1
    - `grep -c "plannedArrivalDate" prisma/schema.prisma` ≥ 1
    - Product/Supplier/User имеют back-relations: `grep -c "salesPlanMonthLevels\|virtualPurchases\|salesPlanVersions" prisma/schema.prisma` ≥ 5 (3 в Product + 1 Supplier + 1 User)
    - SalesPlanVersionDay использует BigInt PK и НЕ имеет `product Product @relation`: `grep -A20 "model SalesPlanVersionDay" prisma/schema.prisma` содержит `BigInt` и НЕ содержит `product  Product @relation`
    - `npx prisma validate` проходит (exit 0)
  </acceptance_criteria>
  <done>Все 5 моделей + enum + Purchase.plannedArrivalDate + 5 back-relations в schema.prisma; prisma generate успешен.</done>
</task>

<task type="auto">
  <name>Task 2: Рукописная миграция migration.sql + локальное применение + сид AppSetting</name>
  <files>prisma/migrations/20260705_sales_plan_v2/migration.sql</files>
  <read_first>
    - prisma/migrations/20260702_phase24_finance/migration.sql (образец аддитивной рукописной миграции: CREATE TABLE / CREATE TYPE / ALTER / INSERT ON CONFLICT)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §2.6 (AppSetting-ключи и дефолты) + §2.7 (шаблон migration.sql)
    - prisma/schema.prisma (модели из Task 1 — источник точных типов колонок)
  </read_first>
  <action>
Создай `prisma/migrations/20260705_sales_plan_v2/migration.sql` — DDL, точно соответствующий моделям из Task 1:

- `CREATE TYPE "VirtualPurchaseStatus" AS ENUM ('SUGGESTED','ACCEPTED','DISMISSED','CONVERTED');`
- `CREATE TABLE "SalesPlanMonthLevel"` (id TEXT PK, "productId" TEXT NOT NULL, month DATE NOT NULL, "targetOrdersPerDay" DOUBLE PRECISION, "priceRub" DOUBLE PRECISION, "buyoutPct" DOUBLE PRECISION, comment TEXT, "updatedAt" TIMESTAMP(3) NOT NULL, "updatedBy" TEXT) + FK productId → Product(id) ON DELETE CASCADE + `CREATE UNIQUE INDEX` на (productId, month) + index на (month).
- `CREATE TABLE "SalesPlanDayOverride"` (id, productId, date DATE NOT NULL, "ordersPerDay" DOUBLE PRECISION NOT NULL, updatedAt, updatedBy) + FK Cascade + unique (productId, date) + index (date).
- `CREATE TABLE "VirtualPurchase"` (id, productId NOT NULL, supplierId TEXT NULL, qty INTEGER NOT NULL, "orderDate" DATE NOT NULL, "expectedArrivalDate" DATE NOT NULL, "leadTimeDaysUsed" INTEGER, "unitPrice" DECIMAL(14,4), currency TEXT NOT NULL DEFAULT 'CNY', source TEXT NOT NULL DEFAULT 'auto', status "VirtualPurchaseStatus" NOT NULL DEFAULT 'SUGGESTED', "convertedPurchaseId" TEXT, comment TEXT, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL) + FK productId → Product ON DELETE CASCADE + FK supplierId → Supplier ON DELETE SET NULL + index (productId, expectedArrivalDate) + index (status).
- `CREATE TABLE "SalesPlanVersion"` (id, label TEXT NOT NULL, kind TEXT NOT NULL DEFAULT 'user', "horizonFrom" DATE NOT NULL, "horizonTo" DATE NOT NULL, "paramsJson" JSONB NOT NULL, note TEXT, "createdById" TEXT, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP) + FK createdById → User ON DELETE SET NULL + index (createdAt).
- `CREATE TABLE "SalesPlanVersionDay"` (id BIGSERIAL PK, "versionId" TEXT NOT NULL, "productId" TEXT NOT NULL, sku TEXT NOT NULL, name TEXT NOT NULL, date DATE NOT NULL, DOUBLE PRECISION колонки planOrdersUnits/planOrdersRub/planBuyoutsUnits/planBuyoutsRub/priceUsed/buyoutPctUsed/stockEndUnits NOT NULL) + FK versionId → SalesPlanVersion ON DELETE CASCADE + unique (versionId, productId, date) + index (versionId, date). Никакого FK на Product (no-FK паттерн).
- `ALTER TABLE "Purchase" ADD COLUMN "plannedArrivalDate" DATE;`
- Сид AppSetting (§2.6) через ON CONFLICT DO NOTHING:
```sql
INSERT INTO "AppSetting"(key, value, "updatedAt") VALUES
  ('salesPlan.iuTargets', '[{"from":"2026-07-01","to":"2026-12-31","dailyRub":2380805}]', now()),
  ('salesPlan.horizon',   '{"from":"2026-07-01","to":"2026-12-31"}', now()),
  ('salesPlan.iuMetric',  '"buyouts"', now()),
  ('salesPlan.leadTimes2', '{"deliveryDays":3,"returnDays":3}', now()),
  ('salesPlan.wbInboundLagDays', '0', now()),
  ('salesPlan.transitDays', '20', now()),
  ('salesPlan.defaultLeadTimeDays', '45', now()),
  ('salesPlan.safetyStockDays', '14', now()),
  ('salesPlan.vpCoverDays', '60', now())
ON CONFLICT (key) DO NOTHING;
```
(Проверь фактическую схему AppSetting: колонки key/value/updatedAt — сверь с существующими INSERT в 20260702_phase24_finance или в other migrations; подгони список колонок под реальную структуру таблицы AppSetting.)

Примени миграцию локально: `npx prisma migrate deploy`. Если локальной PostgreSQL нет — задокументируй в SUMMARY что миграция pending для VPS (паттерн Phase 14/9: миграция применяется через deploy.sh на проде), но `migration.sql` синтаксически валиден и модели в schema.prisma совпадают. НЕ применяй `prisma db push` и `prisma migrate dev` (сбрасывают данные).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && test -f prisma/migrations/20260705_sales_plan_v2/migration.sql && grep -cE "CREATE TABLE \"SalesPlan|CREATE TABLE \"VirtualPurchase\"|CREATE TYPE \"VirtualPurchaseStatus\"|ALTER TABLE \"Purchase\"" prisma/migrations/20260705_sales_plan_v2/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - Файл `prisma/migrations/20260705_sales_plan_v2/migration.sql` существует
    - `grep -c "CREATE TABLE" prisma/migrations/20260705_sales_plan_v2/migration.sql` === 5
    - `grep -c "CREATE TYPE \"VirtualPurchaseStatus\"" migration.sql` === 1
    - `grep -c "ALTER TABLE \"Purchase\" ADD COLUMN \"plannedArrivalDate\"" migration.sql` === 1
    - `grep -c "salesPlan.iuTargets\|salesPlan.horizon\|salesPlan.iuMetric\|salesPlan.safetyStockDays\|salesPlan.vpCoverDays" migration.sql` === 5 (все 9 ключей присутствуют; проверяем минимум 5 маркеров)
    - `grep -c "BIGSERIAL\|bigserial" migration.sql` ≥ 1 (SalesPlanVersionDay PK)
    - `grep -c "ON CONFLICT (key) DO NOTHING" migration.sql` ≥ 1
    - `grep -c "ON DELETE CASCADE" migration.sql` ≥ 4 (MonthLevel, DayOverride, VirtualPurchase.product, VersionDay.version)
  </acceptance_criteria>
  <done>migration.sql создан, синтаксически валиден, DDL совпадает с schema.prisma, сид 9 AppSetting-ключей идемпотентен. Применён локально или помечен pending для VPS deploy.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` — схема валидна.
- `npx prisma generate` — клиент регенерирован, типы `SalesPlanMonthLevel`, `VirtualPurchase` и т.д. доступны.
- migration.sql покрывает 5 таблиц + enum + ALTER Purchase + 9 AppSetting-ключей.
- Старые модели/данные не тронуты (аддитивность): `grep -c "DROP\|ALTER TABLE.*DROP" migration.sql` === 0.
</verification>

<success_criteria>
Схема БД плана продаж v2 в проекте: 5 новых таблиц, enum, plannedArrivalDate, 5 back-relations, 9 сидовых AppSetting-ключей. Prisma-клиент регенерирован. Основа для всех последующих волн готова. Старый sales-plan работает без изменений.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-01-SUMMARY.md` (укажи, применена ли миграция локально или pending для VPS).
</output>
