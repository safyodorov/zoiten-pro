---
phase: 25-v2-h2-2026
plan: 08
type: execute
wave: 7
depends_on: [06, 07]
files_modified:
  - app/actions/sales-plan.ts
  - lib/sales-plan/plan-fact.ts
  - components/sales-plan/PlanVersionBar.tsx
  - components/sales-plan/FixPlanVersionDialog.tsx
  - app/(dashboard)/sales-plan/page.tsx
  - app/(dashboard)/sales-plan/products/page.tsx
autonomous: false
requirements: [SP-11]
must_haves:
  truths:
    - "fixSalesPlanVersion материализует дневной ряд драфта в immutable SalesPlanVersion + SalesPlanVersionDay (чанки 5000, обе метрики)"
    - "Активная версия (salesPlan.activeVersionId) — baseline для план/факт по умолчанию"
    - "Прошлое версии не переписывается: дни <today копируются из активной; unconstrained-номинал первой версии помечен"
    - "PlanVersionBar: селектор версий (native select) + «Зафиксировать план» + read-only режим (?version=) + «дрейф» через compareVersions"
    - "paramsJson содержит снапшот активных VirtualPurchase (для ПДДС Wave 8)"
  artifacts:
    - path: "app/actions/sales-plan.ts"
      provides: "fixSalesPlanVersion, setActiveSalesPlanVersion, renamePlanVersion, deleteSalesPlanVersion"
      exports: ["fixSalesPlanVersion", "setActiveSalesPlanVersion", "renamePlanVersion", "deleteSalesPlanVersion"]
    - path: "components/sales-plan/PlanVersionBar.tsx"
      provides: "Селектор версий + фиксация + read-only баннер"
      contains: "Зафиксировать план"
    - path: "lib/sales-plan/plan-fact.ts"
      provides: "compareVersions (дрейф)"
      contains: "compareVersions"
  key_links:
    - from: "fixSalesPlanVersion"
      to: "SalesPlanVersionDay createMany чанками"
      via: "материализация драфт-ряда, дни <today из активной версии"
      pattern: "createMany|5000"
    - from: "app/(dashboard)/sales-plan/page.tsx"
      to: "salesPlan.activeVersionId → SalesPlanVersionDay"
      via: "план/факт против активной версии"
      pattern: "activeVersionId|SalesPlanVersionDay"
---

<objective>
Этап 5 (Версионирование): фиксация плана в immutable-снапшот, активная версия как baseline план/факт, read-only просмотр версий, «дрейф» черновика. ⚠ Первую версию фиксировать в день деплоя этого этапа (минимизация unconstrained-зоны прошлого — §6.1).

Purpose: план/факт по умолчанию меряется против зафиксированной версии — фундамент честного контроля (SP-11). Версия immutable (только create/rename/delete), покрывает 01.07–31.12 целиком (дни <today копируются из активной, симуляция стартует от today). paramsJson содержит снапшот активных VirtualPurchase — потребляется ПДДС (Wave 8).
Output: 4 версионных action + compareVersions + PlanVersionBar + FixPlanVersionDialog + переключение план/факт на активную версию во всех табах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Prisma-модели (Wave 1): SalesPlanVersion (label, kind, horizonFrom/To, paramsJson, note, createdById), SalesPlanVersionDay (BigInt PK, productId no-FK, sku/name денорм, planOrders*, planBuyouts*, priceUsed, buyoutPctUsed, stockEndUnits).
Из lib/sales-plan/{engine,data,plan-fact}.ts (Waves 1/2/5): computeSalesPlan, loadSalesPlanInputs, buildPlanFactReport.
Из app/actions/sales-plan.ts (Waves 3/6): setGlobalJson, VP-actions.
Из components/sales-plan/PlanFactControls.tsx, SalesPlanTabs.tsx (Waves 4/5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Версионные actions + compareVersions (дрейф)</name>
  <files>app/actions/sales-plan.ts, lib/sales-plan/plan-fact.ts</files>
  <read_first>
    - app/actions/sales-plan.ts (Waves 3/6 — setGlobalJson, ActionResult, паттерн MANAGE)
    - lib/sales-plan/{data,engine}.ts (loadSalesPlanInputs + computeSalesPlan для материализации ряда)
    - prisma/schema.prisma (SalesPlanVersion/SalesPlanVersionDay — поля для createMany)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §6.1 (семантика: чанки 5000, immutable, прошлое из активной, unconstrained-первая версия, paramsJson содержит VP-снапшоты), §2.4 (структура paramsJson)
  </read_first>
  <action>
В `app/actions/sales-plan.ts` (все SALES MANAGE, revalidatePath 3 роутов):

1. `fixSalesPlanVersion(payload: { label?: string, note?: string })`: транзакция:
   - `loadSalesPlanInputs(prisma)` + `computeSalesPlan` → дневной ряд драфта [today…horizonTo].
   - Дни `< today`: копируются из активной версии (`salesPlan.activeVersionId` → её SalesPlanVersionDay); если активной нет (первая версия) — unconstrained номинал `rate × buyoutPct × price` из monthLevels (стоки на 01.07 не сохранены). Версия покрывает 01.07–31.12 целиком.
   - Создай header `SalesPlanVersion` (label ?? «План от DD.MM.YYYY», kind "user", horizonFrom/To, createdById = текущий user, `paramsJson` = снапшот: параметры модели + monthLevels/dayOverrides digest + активные VirtualPurchase (id, productId, qty, orderDate, expectedArrivalDate, unitPrice, currency, depositPct/balancePct, supplierId — самодостаточные копии для ПДДС) + iuTargets).
   - `createMany` строк `SalesPlanVersionDay` чанками по 5000 (обе метрики: planOrdersUnits/Rub + planBuyoutsUnits/Rub + priceUsed + buyoutPctUsed + stockEndUnits; sku/name денормализованы). Zero-строки не пишутся.
   - Установи новую версию активной (`salesPlan.activeVersionId`). Верни `{ ok: true, versionId }`.
   - Immutable: НЕТ action на UPDATE строк.

2. `setActiveSalesPlanVersion(id)`: setGlobalJson `salesPlan.activeVersionId`.
3. `renamePlanVersion(id, label)`: update только label.
4. `deleteSalesPlanVersion(id)`: delete (каскад days через FK); если удаляли активную — сбросить activeVersionId. Требует подтверждения на UI.

В `lib/sales-plan/plan-fact.ts` добавь pure `compareVersions(versionA_days, versionB_days)`: «дрейф» — суммарное отклонение план_B − план_A по горизонту (₽ и %) для показа «насколько текущие правки уводят от зафиксированного». Тест не требуется отдельным стабом (покрыт вручную), но функция pure.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "fixSalesPlanVersion\|setActiveSalesPlanVersion\|renamePlanVersion\|deleteSalesPlanVersion" app/actions/sales-plan.ts; grep -c "compareVersions\|5000\|createMany" app/actions/sales-plan.ts lib/sales-plan/plan-fact.ts; npx tsc --noEmit 2>&1 | grep -iE "actions/sales-plan|plan-fact" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function fixSalesPlanVersion\|export async function setActiveSalesPlanVersion\|export async function renamePlanVersion\|export async function deleteSalesPlanVersion" app/actions/sales-plan.ts` === 4
    - fixSalesPlanVersion чанки: `grep -c "5000\|chunk" app/actions/sales-plan.ts` ≥ 1
    - paramsJson содержит VP-снапшоты: `grep -ci "paramsJson\|virtualPurchase" app/actions/sales-plan.ts` ≥ 1
    - `grep -c "export function compareVersions" lib/sales-plan/plan-fact.ts` === 1
    - все версионные write под MANAGE: рядом `requireSection("SALES", "MANAGE")`
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>fixSalesPlanVersion (чанки 5000, immutable, прошлое из активной, VP-снапшот в paramsJson), set/rename/delete, compareVersions (дрейф).</done>
</task>

<task type="auto">
  <name>Task 2: PlanVersionBar + FixPlanVersionDialog + переключение план/факт на активную версию</name>
  <files>components/sales-plan/PlanVersionBar.tsx, components/sales-plan/FixPlanVersionDialog.tsx, app/(dashboard)/sales-plan/page.tsx, app/(dashboard)/sales-plan/products/page.tsx</files>
  <read_first>
    - components/credits/ScheduleControls.tsx (URL-state паттерн)
    - components/procurement/PurchaseModal.tsx (образец FixPlanVersionDialog)
    - app/(dashboard)/sales-plan/page.tsx (Wave 5 — переключить план на активную версию), products/page.tsx (Wave 4 — read-only при ?version=)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.1 (PlanVersionBar native select + «Зафиксировать план» + read-only баннер), §6.1 (сшивка ряда: <today версия, ≥today версия/драфт), §6.2 (сшивка «наш план»)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки PlanVersionBar (740-769), FixPlanVersionDialog (31)
  </read_first>
  <action>
1. `components/sales-plan/PlanVersionBar.tsx` — client. native `<select>` версий (НЕ base-ui): «Рабочий план (черновик)» + версии по убыванию даты; смена → `?version=` в URL (draft → delete param). Кнопка «Зафиксировать план» (Lock icon, только canManage + черновик) → открывает FixPlanVersionDialog. При просмотре версии (`versionId` в URL) — amber-баннер «Просмотр версии «{label}». Редактирование недоступно. [Вернуться к рабочему плану]». Проп `readOnly = !canManage || Boolean(versionId)` — единый флаг, прокидывается в инпуты всех табов. Показывай «дрейф» (compareVersions черновик vs активная), если есть правки.

2. `components/sales-plan/FixPlanVersionDialog.tsx` — client, модалка (образец PurchaseModal): поля label (default «План от DD.MM.YYYY»), note (textarea), сводка «что фиксируется» (N товаров, горизонт, N активных VP). Сабмит → `fixSalesPlanVersion` → редирект на `?version=<newId>` + toast.

3. `app/(dashboard)/sales-plan/page.tsx` (Сводный) — переключи источник плана: если `salesPlan.activeVersionId` есть → план из `SalesPlanVersionDay` активной (или выбранной `?version=`); иначе — драфт (номинал, бейдж «номинал»). Дни `<today` — версия; `≥today` — версия либо live-драфт по селектору. Прокинь `<PlanVersionBar versions={} activeVersionId={} currentVersionId={} canManage={} />`.

4. `app/(dashboard)/sales-plan/products/page.tsx` (Товары) — `readOnly = mode !== "edit" || !canManage || Boolean(version)`; при `?version=` рендерит версию read-only (getProductPlanDays с versionId читает SalesPlanVersionDay). Прокинь PlanVersionBar. Группа «Архивные товары» при фильтрации версии — hard-purged товары (нет live Product) по денорм sku/name (§2.4): без фильтров входят в суммы; при фильтрах исключаются с notice «N архивных строк вне фильтра».
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "Зафиксировать план\|version\|readOnly" components/sales-plan/PlanVersionBar.tsx; grep -c "fixSalesPlanVersion" components/sales-plan/FixPlanVersionDialog.tsx; grep -c "activeVersionId\|SalesPlanVersionDay\|version" "app/(dashboard)/sales-plan/page.tsx"; npx tsc --noEmit 2>&1 | grep -iE "PlanVersionBar|FixPlanVersion|sales-plan/page|sales-plan/products" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - PlanVersionBar native select: `grep -c "<select" components/sales-plan/PlanVersionBar.tsx` ≥ 1
    - PlanVersionBar read-only баннер: `grep -ci "readOnly\|Просмотр версии\|amber" components/sales-plan/PlanVersionBar.tsx` ≥ 1
    - FixPlanVersionDialog: `grep -c "fixSalesPlanVersion" components/sales-plan/FixPlanVersionDialog.tsx` ≥ 1
    - page переключён на активную версию: `grep -c "activeVersionId\|SalesPlanVersionDay" "app/(dashboard)/sales-plan/page.tsx"` ≥ 1
    - products read-only при version: `grep -c "version\|readOnly" "app/(dashboard)/sales-plan/products/page.tsx"` ≥ 1
    - `npx tsc --noEmit` без ошибок; `npm run test` вся суита кроме pdds-feed зелёная
  </acceptance_criteria>
  <done>PlanVersionBar (селектор + фиксация + read-only баннер + дрейф); FixPlanVersionDialog; план/факт переключён на активную версию; read-only просмотр версий во всех табах.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT фиксации версии + фиксация первой версии в проде</name>
  <what-built>
Версионирование: fixSalesPlanVersion (immutable снапшот дневного ряда, чанки 5000, обе метрики, VP в paramsJson), активная версия как baseline план/факт, read-only просмотр (?version=), «дрейф» черновика, set/rename/delete версии. План/факт всех табов переключён на активную версию.
  </what-built>
  <how-to-verify>
1. Убедись что этапы 3-4-5 (waves 5-6-7) задеплоены плотной серией (CLAUDE.md: push → nohup deploy.sh → curl 200) и prod отвечает 200 на https://zoiten.pro/sales-plan.
2. Открой `/sales-plan/products` (черновик), внеси/подтверди план — убедись что план осмысленный.
3. Нажми «Зафиксировать план» → FixPlanVersionDialog (label, note, сводка) → зафиксируй. **Это первая версия — фиксируй в день деплоя этапа 5** (минимизация unconstrained-зоны прошлого, §6.1).
4. Проверь: селектор версий показывает новую версию, URL стал `?version=<id>`, amber-баннер «Просмотр версии», инпуты disabled.
5. Вернись к рабочему плану — редактирование снова доступно; «дрейф» показывает 0 сразу после фиксации.
6. `/sales-plan` (Сводный): план/факт теперь против активной версии; прошедшие дни без бейджа «номинал» (закреплены версией).
7. Проверь read-only: переключись на версию в Товарах — данные версии, редактирование заблокировано.
  </how-to-verify>
  <resume-signal>Напиши «approved» или опиши проблемы (напр. дрейф ≠ 0 сразу после фиксации, версия неполная, read-only не блокирует).</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` — вся sales-plan суита кроме pdds-feed (Wave 8) зелёная.
- `npx tsc --noEmit` — версионные actions + UI типобезопасны.
- fixSalesPlanVersion immutable, чанки 5000, прошлое из активной, VP-снапшот в paramsJson.
- План/факт всех табов против активной версии; read-only режим работает.
- Первая версия зафиксирована в проде в день деплоя этапа 5 (UAT).
</verification>

<success_criteria>
Этап 5 «Версионирование» завершён (SP-11): фиксация плана, активная версия baseline, read-only просмотр, дрейф. Требование 7 (фиксация) закрыто. Первая версия зафиксирована в день деплоя. unconstrained-зона прошлого минимизирована.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-08-SUMMARY.md`. Зафиксируй дату первой версии в проде.
</output>
