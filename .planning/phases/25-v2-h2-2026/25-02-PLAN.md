---
phase: 25-v2-h2-2026
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/date-buckets.ts
  - lib/loan-math.ts
  - lib/sales-plan/types.ts
  - lib/sales-plan/dates.ts
  - lib/sales-plan/iu.ts
  - lib/sales-plan/arrivals.ts
  - lib/sales-plan/engine.ts
autonomous: true
requirements: [SP-02, SP-03, SP-05, SP-06]
must_haves:
  truths:
    - "iuTotalForRange('2026-07-01','2026-12-31') === 438 068 120 ₽ (golden GREEN)"
    - "computeSalesPlan симулирует дневной ряд с T+3 выкупами, T+6 возвратами, сток-лимитом (golden GREEN)"
    - "resolveArrivalBatches проходит все 5 уровней fallback + сплит частичного TRANSIT + createdAt+45 default"
    - "bucketKey/bucketLabel поддерживают quarter/halfyear/year; loan-math переключён на общий модуль без регресса"
    - "Все pure-функции без импортов Prisma/React/Next (клиентобезопасны для realtime-модалки)"
  artifacts:
    - path: "lib/sales-plan/engine.ts"
      provides: "computeSalesPlan, simulateProductPlan (PURE)"
      exports: ["computeSalesPlan"]
    - path: "lib/sales-plan/iu.ts"
      provides: "iuTotalForRange, iuSeriesForRange"
      exports: ["iuTotalForRange", "iuSeriesForRange"]
    - path: "lib/sales-plan/arrivals.ts"
      provides: "resolveArrivalBatches"
      exports: ["resolveArrivalBatches"]
    - path: "lib/date-buckets.ts"
      provides: "bucketKey/bucketLabel + Granularity (day..year)"
      exports: ["bucketKey", "bucketLabel"]
  key_links:
    - from: "lib/loan-math.ts"
      to: "lib/date-buckets.ts"
      via: "import { bucketKey, bucketLabel, Granularity }"
      pattern: "from \"@/lib/date-buckets\""
    - from: "lib/sales-plan/engine.ts"
      to: "lib/sales-forecast.ts (алгоритм simulateProduct)"
      via: "перенос проверенной механики T+3/T+6/сток-лимит"
      pattern: "simulateProductPlan"
---

<objective>
Этап 1 (Фундамент), часть 2: pure-ядро движка плана продаж. TDD — доводим до GREEN 4 из 7 Wave-0-стабов: `sales-plan-engine`, `sales-plan-arrivals`, `sales-plan-iu`, `date-buckets`.

Purpose: детерминированный движок без Prisma (образец `lib/pricing-math.ts`) — используется и на сервере (RSC рендер), и на клиенте (realtime-пересчёт «Сток(расч)» в ProductPlanDialog). Механика симуляции переносится из проверенного `lib/sales-forecast.ts` (baseline, T+3 выкупы, T+6 возвраты, сток-лимит) с эволюцией: массив приходов вместо singleton, ставка из month/day override без ramp-хака.
Output: `lib/date-buckets.ts` + `lib/sales-plan/{types,dates,iu,arrivals,engine}.ts`; loan-math переключён на общий bucket-модуль.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md

<interfaces>
Wave-0 тесты определяют контракт (см. tests/sales-plan-engine.test.ts, tests/sales-plan-iu.test.ts, tests/sales-plan-arrivals.test.ts, tests/date-buckets.test.ts — уже созданы в 25-00).
Ключевые типы из §3.1 RESEARCH: SalesPlanInputs, ProductPlanInput, PlanDayRow, ProductPlanResult, ArrivalBatch, SalesPlanResult.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/date-buckets.ts (вынос из loan-math + quarter/halfyear/year) + переключение loan-math</name>
  <files>lib/date-buckets.ts, lib/loan-math.ts</files>
  <behavior>
    - Test: bucketKey(2026-07-15, "quarter") === "2026-Q3"
    - Test: bucketKey(2026-07-15, "halfyear") === "2026-H2"
    - Test: bucketKey(2026-07-15, "year") === "2026"
    - Test: регресс — bucketKey(2026-07-15, "month") === "2026-07" (перенос из loan-math без изменения)
    - Test: bucketLabel("2026-Q3","quarter") === "Q3 2026"
    - Test: loan-math по-прежнему компилируется и его тесты зелёные после переключения импорта
  </behavior>
  <read_first>
    - lib/loan-math.ts (строки 8, 48-86 getIsoWeek, 182-237 bucketKey/bucketLabel — источник для копирования)
    - tests/date-buckets.test.ts (Wave 0 стаб — контракт)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок `lib/date-buckets.ts` (строки 146-173 — готовый паттерн расширения)
  </read_first>
  <action>
Создай `lib/date-buckets.ts`: перенеси ИЗ `lib/loan-math.ts` целиком `getIsoWeek` (строки 55-86) и `bucketKey`/`bucketLabel` (182-237), добавь:
- `export type Granularity = "day" | "week" | "month" | "quarter" | "halfyear" | "year"` (заменяет локальный `LoanGranularity`).
- В `bucketKey` кейсы: `quarter` → `${y}-Q${Math.ceil((date.getUTCMonth()+1)/3)}`; `halfyear` → `${y}-H${date.getUTCMonth() < 6 ? 1 : 2}`; `year` → `${y}` (используй UTC-геттеры как в оригинале).
- В `bucketLabel` кейсы: `quarter` → `Q${key.split("-Q")[1]} ${key.split("-")[0]}`; `halfyear` → `H${key.split("-H")[1]} ${key.split("-")[0]}`; `year` → `key`.
Экспортируй `bucketKey`, `bucketLabel`, `Granularity`, `getIsoWeek` (если loan-math его использует наружу — иначе оставь локальной).

В `lib/loan-math.ts`: удали локальные `bucketKey`/`bucketLabel`/`getIsoWeek`/`LoanGranularity`, замени на `import { bucketKey, bucketLabel, type Granularity } from "@/lib/date-buckets"`. Если где-то использовался тип `LoanGranularity` — замени на `Granularity`. Оставь re-export `export type { Granularity } from "@/lib/date-buckets"` если внешние потребители loan-math импортировали LoanGranularity (проверь grep по проекту: `grep -rn "LoanGranularity" --include=*.ts --include=*.tsx`; исправь все места).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/date-buckets.test.ts tests/loan-math.test.ts 2>&1 | grep -iE "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/date-buckets.test.ts` — все тесты PASS (GREEN)
    - `grep -c "from \"@/lib/date-buckets\"" lib/loan-math.ts` ≥ 1 (loan-math переключён)
    - `grep -v '^//' lib/loan-math.ts | grep -c "^export function bucketKey"` === 0 (локальная реализация удалена из loan-math)
    - `grep -rn "LoanGranularity" lib components app --include=*.ts --include=*.tsx | grep -v "date-buckets" | wc -l` === 0 (нет orphan-ссылок)
    - существующие loan-math тесты (если есть tests/loan-math.test.ts) зелёные — регресса нет
  </acceptance_criteria>
  <done>lib/date-buckets.ts с 6 бакетами; loan-math переключён без регресса; date-buckets.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: types.ts + dates.ts + iu.ts (golden ИУ GREEN)</name>
  <files>lib/sales-plan/types.ts, lib/sales-plan/dates.ts, lib/sales-plan/iu.ts</files>
  <behavior>
    - Test: iuTotalForRange("2026-07-01","2026-12-31",[{from:"2026-07-01",to:"2026-12-31",dailyRub:2_380_805}]) === 438_068_120
    - Test: from==to → 1 день === 2_380_805
    - Test: мульти-период — 2 target в массиве суммируются по пересечению с диапазоном
    - Test: iuSeriesForRange последний cumulative === 438_068_120
  </behavior>
  <read_first>
    - lib/sales-forecast.ts (строки 1-15 импорты, 78-162 типы, date helpers addDays/getMskTodayIso — источник для dates.ts)
    - lib/pricing-math.ts (строки 31-95 — паттерн разделения export interface входов/выходов)
    - tests/sales-plan-iu.test.ts (Wave 0 golden контракт)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §3.1 (типы), §2.6 (IuTarget формат)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки `lib/sales-plan/types.ts` (107-126), `lib/sales-plan/iu.ts` (197-215)
  </read_first>
  <action>
1. `lib/sales-plan/types.ts` — все публичные интерфейсы движка из §3.1 RESEARCH (дословно поля): `SalesPlanInputs`, `ProductPlanInput` (с полями иерархии brand/direction/category/subcategory для сортировки, nmIds, stockNow, baselineOrdersPerDay, buyoutPct, buyoutSource, avgPriceRub, monthLevels[], dayOverrides, arrivals[], seedOrders), `PlanDayRow`, `ProductPlanResult`, `SalesPlanResult` (products[] + companyDaily[] + companyMonthly[]), `ArrivalBatch`, union-типы `ArrivalDateSource = "manual"|"transit-eta"|"leadtime-eta"|"legacy-expected"`, `BuyoutSource = "own"|"legacy"|"subcategory"|"global"`, `IuTarget = { from: string; to: string; dailyRub: number }`, `ModelParams` (deliveryDays, returnDays, wbInboundLagDays, safetyStockDays, vpCoverDays, defaultLeadTimeDays, transitDays). Все поля сериализуемые (string/number/boolean/null — без Date/Decimal). Ноль импортов Prisma/React/Next.

2. `lib/sales-plan/dates.ts` — перенеси из `lib/sales-forecast.ts` date-хелперы: `addDays(iso: string, n: number): string`, `getMskTodayIso(): string`, `daysBetween`, `eachDayIso(from, to): string[]`, `parseIsoUtc`. Pure, без Prisma. Экспортируй.

3. `lib/sales-plan/iu.ts` — pure, iuTargets передаются снаружи:
```typescript
export function iuTotalForRange(from: string, to: string, targets: IuTarget[]): number
export function iuSeriesForRange(from: string, to: string, targets: IuTarget[]): Array<{ date: string; cumulative: number }>
```
`iuTotalForRange` = Σ по дням диапазона `dailyRub` того target, чей период `[t.from; t.to]` содержит день (0 если нет). `iuSeriesForRange` — накопительный ряд по дням. Используй `eachDayIso` из dates.ts. Golden: 184 дня × 2_380_805 = 438_068_120.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/sales-plan-iu.test.ts 2>&1 | grep -iE "passed|failed|438" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-iu.test.ts` — все PASS, включая `=== 438_068_120`
    - `grep -c "export function iuTotalForRange\|export function iuSeriesForRange" lib/sales-plan/iu.ts` === 2
    - `grep -c "import.*prisma\|from \"@prisma\|from \"react\|from \"next" lib/sales-plan/iu.ts lib/sales-plan/types.ts lib/sales-plan/dates.ts` === 0 (pure)
    - `grep -c "export interface SalesPlanInputs\|export interface ProductPlanInput\|export interface PlanDayRow\|export interface ProductPlanResult" lib/sales-plan/types.ts` === 4
  </acceptance_criteria>
  <done>types/dates/iu созданы; sales-plan-iu.test.ts GREEN с golden 438 068 120 ₽; всё pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: arrivals.ts + engine.ts (симуляция + golden GREEN)</name>
  <files>lib/sales-plan/arrivals.ts, lib/sales-plan/engine.ts</files>
  <behavior>
    - Test (arrivals): 5 уровней fallback → корректный dateSource-тег на каждой партии
    - Test (arrivals): частичный TRANSIT (qty<qtyRemaining) → сплит на 2 партии (transit-eta + leadtime-eta)
    - Test (arrivals): TRANSIT.date=null → пропуск на уровень 3 (leadtime-eta createdAt+45)
    - Test (engine): T+3 выкуп — заказ 2026-07-01 → buyoutsUnits(2026-07-04) = 12×0.8
    - Test (engine): day override 2026-07-15 → rateRequested === 20
    - Test (engine): ступенька — 2026-08-01 rateRequested === 15
    - Test (engine): сток-лимит orders[d] ≤ stockEnd[d-1]; zero-guard stockNow=0 → orders=0 до прихода
  </behavior>
  <read_first>
    - lib/sales-forecast.ts (функция computeForecast / simulateProduct — ПРОВЕРЕННЫЙ алгоритм: baseline, T+3 выкупы, T+6 возвраты, clamp stock ≥ 0, seed-заказы [today−3;today−1])
    - lib/stock-data.ts (строки 134-175 productionBreakdown — формула qtyRemaining = quantity − stage(WAREHOUSE).qty per закупка)
    - tests/sales-plan-engine.test.ts, tests/sales-plan-arrivals.test.ts (Wave 0 контракты)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §3.2 (детализация месяц→день), §3.3 (дневная симуляция), §3.4 (resolveArrivalBatches fallback-цепочка)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки `lib/sales-plan/engine.ts` (58-103), `lib/sales-plan/arrivals.ts` (130-142)
  </read_first>
  <action>
1. `lib/sales-plan/arrivals.ts` — pure (НЕ async): `resolveArrivalBatches(input): ArrivalBatch[]`. Принимает уже загруженные данные (закупки с этапами, виртуальные закупки, legacy ProductIncoming, параметры transitDays/defaultLeadTimeDays/wbInboundLagDays). Реализуй fallback-цепочку §3.4:
   - Per реальная закупка (PLANNED|ACTIVE): `qtyRemaining = max(0, item.quantity − stage(WAREHOUSE).qty)`; если ≤0 — пропуск. Дата по уровням: (1) `plannedArrivalDate` → "manual", вся qtyRemaining; (2) `stage(TRANSIT)` ТОЛЬКО если `TRANSIT.qty>0 И TRANSIT.date≠null`: TRANSIT-часть `min(TRANSIT.qty, qtyRemaining)` по `TRANSIT.date + transitDays` → "transit-eta", остаток `qtyRemaining − TRANSIT.qty` (если >0) отдельной партией на уровень 3 → "leadtime-eta"; при TRANSIT.qty>0 но date=null — уровень пропущен целиком; (3) `createdAt + leadTimeDays(SupplierProductLink ?? defaultLeadTimeDays=45)` → "leadtime-eta"; (4) legacy `ProductIncoming.expectedDate` (только если ровно одна открытая закупка без дат) → "legacy-expected"; (5) null → партия не создаётся.
   - Per виртуальная закупка (SUGGESTED + ACCEPTED): `date = expectedArrivalDate`, source "virtual".
   - Ко всем датам: `+ wbInboundLagDays` (default 0). Приход в сток на `date + 1` — это делает engine, arrivals возвращает саму дату доступности.

2. `lib/sales-plan/engine.ts` — pure. `computeSalesPlan(inputs: SalesPlanInputs): SalesPlanResult` + внутренняя `simulateProductPlan(product, params): ProductPlanResult`. Перенеси механику `lib/sales-forecast.ts:computeForecast` с изменениями §3.3:
   - `rateRequested(d) = dayOverrides[d] ?? monthLevel(d).targetOrdersPerDay ?? baselineOrdersPerDay` (§3.2); `priceRub(d) = monthLevel(d).priceRub ?? avgPriceRub`; `buyoutPct(d) = monthLevel(d).buyoutPct/100 ?? product.buyoutPct`.
   - Симуляция от today до horizonTo+deliveryDays+returnDays: `stock[d] = stock[d−1] − orders[d−1] + Σinflow(d) + returns(d)` (clamp ≥ 0); `orders[d] = min(rateRequested(d), stock[d])`; `buyouts[d+deliveryDays] += orders[d]×buyoutPct(d)` (T+3); `returns[d+deliveryDays+returnDays] += orders[d]×(1−buyoutPct(d))` (T+6); buyoutsRub по priceRub месяца дня выкупа.
   - inflow — массив `arrivals`, приход в сток на `arrival.date + 1`. seed-заказы `[today−3;today−1]` для выкупов первых дней. Дни `[horizonFrom; today−1]` НЕ симулируются (зона факта). Сохраняй `rateRequested` в PlanDayRow (топливо suggester'а Wave 4).
   - Ступенька между месяцами без ramp-up. Выход: `days` ровно [horizonFrom…horizonTo], monthTotals, firstStockoutDate, lostUnitsToStockout = Σ(rateRequested−ordersUnits), lostRubToStockout.
   - `computeSalesPlan`: прогон simulateProductPlan per товар + агрегация companyDaily (Σ per день) + companyMonthly.
   Zero Prisma-импортов. round через Math.round где нужно.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-arrivals.test.ts 2>&1 | grep -iE "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-engine.test.ts tests/sales-plan-arrivals.test.ts` — все PASS (GREEN)
    - `grep -c "export function computeSalesPlan" lib/sales-plan/engine.ts` === 1
    - `grep -c "export function resolveArrivalBatches" lib/sales-plan/arrivals.ts` === 1
    - `grep -c "import.*@/lib/prisma\|from \"@prisma\|from \"react\|from \"next" lib/sales-plan/engine.ts lib/sales-plan/arrivals.ts` === 0 (pure, клиентобезопасны)
    - engine поддерживает T+3/T+6/сток-лимит — тесты (a)-(e) golden зелёные
  </acceptance_criteria>
  <done>arrivals + engine созданы; 4 Wave-0-стаба (engine, arrivals, iu, date-buckets) GREEN; движок pure и готов к импорту в клиентский бандл (realtime-модалка).</done>
</task>

</tasks>

<verification>
- `npm run test` — 4 sales-plan-стаба (engine/arrivals/iu/date-buckets) зелёные; остальные 3 (plan-fact/virtual/pdds-feed) остаются RED (реализуются в Waves 3/4/6).
- Все файлы `lib/sales-plan/*` + `lib/date-buckets.ts` — ноль импортов Prisma/React/Next.
- loan-math переключён на общий bucket-модуль без регресса.
</verification>

<success_criteria>
Pure-ядро движка готово: computeSalesPlan (симуляция T+3/T+6/сток-лимит/ступенька), resolveArrivalBatches (5 уровней fallback), iuTotalForRange (golden 438 068 120 ₽), date-buckets (6 бакетов). Всё детерминированно и клиентобезопасно. Waves 2+ строят UI и data-loader поверх.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-02-SUMMARY.md`
</output>
