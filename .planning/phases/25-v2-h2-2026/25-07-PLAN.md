---
phase: 25-v2-h2-2026
plan: 07
type: execute
wave: 6
depends_on: [04, 05]
files_modified:
  - lib/sales-plan/virtual-purchases.ts
  - app/actions/sales-plan.ts
  - app/(dashboard)/sales-plan/purchases/page.tsx
  - components/sales-plan/VirtualPurchasesTable.tsx
  - components/sales-plan/VirtualPurchaseDialog.tsx
  - components/sales-plan/IncomingBadges.tsx
  - components/procurement/PurchaseModal.tsx
autonomous: true
requirements: [SP-08, SP-09]
must_haves:
  truths:
    - "suggestVirtualPurchases: триггер пробоя страхового запаса + qty на покрытие 60 дн + clamp orderDate к today + итеративный roll-forward"
    - "ИНВАРИАНТ «не прошлым числом»: orderDate виртуальной закупки ВСЕГДА ≥ today; expectedArrivalDate ВСЕГДА ≥ today + leadTimeDays (раньше стандартного цикла от сегодня товар не придёт) — на ВСЕХ путях: авто-генерация (clamp), ручное создание, updateVirtualPurchase (серверный clamp), UI date-picker (min-даты)"
    - "opt-out: SUGGESTED+ACCEPTED учитываются в arrivals сразу; DISMISSED исключён; регенерация не трогает ACCEPTED/DISMISSED/CONVERTED/manual"
    - "regenerateVirtualPurchases встроен в обе цепочки пересчёта (Товары bulk + модалка «Сохранить и пересчитать»)"
    - "convertVirtualPurchase → PurchaseModal префилл (plannedArrivalDate = VP.expectedArrivalDate), status=CONVERTED, CONVERTED исключён из arrivals"
    - "Таб «Пора заказывать» + бейджи ◇/⚠ в Товарах"
  artifacts:
    - path: "lib/sales-plan/virtual-purchases.ts"
      provides: "suggestVirtualPurchases (pure)"
      exports: ["suggestVirtualPurchases"]
    - path: "app/(dashboard)/sales-plan/purchases/page.tsx"
      provides: "RSC таб «Пора заказывать»"
      contains: "VirtualPurchasesTable"
    - path: "components/sales-plan/VirtualPurchasesTable.tsx"
      provides: "Таблица предложений + действия"
      contains: "Создать закупку"
  key_links:
    - from: "app/actions/sales-plan.ts (regenerateVirtualPurchases)"
      to: "цепочки пересчёта (bulk + модалка)"
      via: "deleteMany SUGGESTED auto + createMany новые"
      pattern: "regenerateVirtualPurchases"
    - from: "convertVirtualPurchase"
      to: "PurchaseModal префилл + status CONVERTED"
      via: "?create=1&from-virtual=<id>"
      pattern: "from-virtual|CONVERTED"
---

<objective>
Этап 4 (Виртуальные закупки): генератор «пора заказывать» (opt-out) + таб «Пора заказывать» + конвертация в реальную закупку с анти-двойным-счётом. TDD — доводит Wave-0-стаб `sales-plan-virtual` до GREEN.

Purpose: система предлагает что пора заказывать (SP-08), учитывает предложения в плане сразу («как если бы заказывали вовремя»), пользователь точечно отклоняет; конвертация в реальную Purchase с префиллом (SP-09). regenerateVirtualPurchases встраивается в ОБЕ цепочки пересчёта — иначе day-overrides рассинхронизируют предложения с планом (закрытая дыра критика №5).
Output: `lib/sales-plan/virtual-purchases.ts` + VP-actions в sales-plan.ts + таб purchases + 2 компонента + бейджи ◇/⚠ + префилл PurchaseModal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Из lib/sales-plan/engine.ts (Wave 1): computeSalesPlan/simulateProductPlan (топливо suggester — rateRequested в PlanDayRow).
Из lib/sales-plan/data.ts (Wave 2): loadSalesPlanInputs.
Prisma-модели (Wave 1): VirtualPurchase (status SUGGESTED/ACCEPTED/DISMISSED/CONVERTED, source auto/manual), SupplierProductLink (leadTimeDays, unitPrice, currency).
Из app/actions/sales-plan.ts (Wave 3): saveMonthLevels/saveDayOverrides (в них встраивается regenerateVirtualPurchases).
Из components/sales-plan/IncomingBadges.tsx (Wave 4): расширяется на ◇/⚠.
Из components/procurement/PurchaseModal.tsx: расширяется на from-virtual префилл.
tests/sales-plan-virtual.test.ts (Wave 0 контракт).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/sales-plan/virtual-purchases.ts (стаб → GREEN)</name>
  <files>lib/sales-plan/virtual-purchases.ts</files>
  <behavior>
    - Test: триггер — projectedStock(d) < safetyStockDays(14) × rate(d) создаёт предложение
    - Test: qty на покрытие vpCoverDays(60) + страховой запас − projectedStock(arrival)
    - Test: clamp — orderDate = max(today, breach − leadTime)
    - Test: итеративный roll-forward — товар может получить 2-3 предложения за проход (maxIterations 6)
    - Test: minQty — qty < 10 → стоп
    - Test: ИНВАРИАНТ — даже если breach в прошлом (breach − leadTime < today), orderDate = today (clamp), а expectedArrivalDate = today + leadTimeDays; НИКОГДА не возвращается orderDate/arrival прошлым числом
  </behavior>
  <read_first>
    - lib/sales-plan/engine.ts (simulateProductPlan — источник projectedStock/rateRequested; suggester гоняет симуляцию итеративно)
    - tests/sales-plan-virtual.test.ts (Wave 0 контракт)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §4.1 (алгоритм suggestVirtualPurchases — псевдокод), §4.2 (жизненный цикл opt-out)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок virtual-purchases (18)
  </read_first>
  <action>
Создай `lib/sales-plan/virtual-purchases.ts` — pure. `suggestVirtualPurchases(input): VpSuggestion[]` (алгоритм §4.1, итеративный roll-forward):
- Для каждого товара с SupplierProductLink (иначе → в список noSupplierLink для UI-предупреждения):
  - `arrivals := реальные партии + ACCEPTED/manual виртуальные` (авто-SUGGESTED исключены — пересоздаются).
  - Повторять до `maxIterationsPerProduct = 6`:
    - `result := simulateProductPlan(product, arrivals)`.
    - `breach := первый d, где projectedStock(d) < safetyStockDays × rate(d)` (страховой запас, НЕ ноль). Если breach == null или > horizonTo → стоп.
    - `orderDate := max(today, breach − leadTimeDays)` (clamp — заказ в прошлом невозможен).
    - `arrival := orderDate + leadTimeDays` (если > breach → флаг «поздно», план проседает).
    - `qty := ceil(Σ rate(d) за [arrival; min(arrival + vpCoverDays, horizonTo)] + safetyStockDays × rate(arrival) − projectedStock(arrival))`.
    - Если `qty < minQty(10)` → стоп. Иначе добавить VpSuggestion (productId, qty, orderDate, expectedArrivalDate=arrival, leadTimeDaysUsed, unitPrice из SupplierProductLink, isLate) + партию в arrivals; продолжить.
- Параметры: safetyStockDays=14, vpCoverDays=60, leadTimeDays = min(SupplierProductLink.leadTimeDays) ?? 45. Источник скорости — плановая rateRequested (НЕ WbCard.avgSalesSpeed7d).
- Верни `{ suggestions: VpSuggestion[], noSupplierLink: string[] }`. Pure, ноль Prisma/React/Next.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/sales-plan-virtual.test.ts 2>&1 | grep -iE "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-virtual.test.ts` — все PASS (GREEN)
    - `grep -c "export function suggestVirtualPurchases" lib/sales-plan/virtual-purchases.ts` === 1
    - триггер страхового запаса: `grep -ci "safetyStock\|breach" lib/sales-plan/virtual-purchases.ts` ≥ 1
    - итеративный roll-forward: `grep -ci "maxIteration\|iteration\|6" lib/sales-plan/virtual-purchases.ts` ≥ 1
    - инвариант «не прошлым числом»: `grep -ci "max(.*today\|Math.max" lib/sales-plan/virtual-purchases.ts` ≥ 1; тест sales-plan-virtual проверяет `orderDate >= today` и `expectedArrivalDate >= today + leadTimeDays` при breach в прошлом
    - `grep -c "from \"@/lib/prisma\|from \"react\|from \"next" lib/sales-plan/virtual-purchases.ts` === 0 (pure)
  </acceptance_criteria>
  <done>suggestVirtualPurchases pure с триггером страхового запаса, qty покрытия, clamp, roll-forward, minQty; Wave-0-стаб GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: VP server actions + встраивание regenerateVirtualPurchases в цепочки пересчёта</name>
  <files>app/actions/sales-plan.ts, components/procurement/PurchaseModal.tsx</files>
  <read_first>
    - app/actions/sales-plan.ts (Wave 3 — saveMonthLevels/saveDayOverrides с TODO-комментарием под regenerateVirtualPurchases; setGlobalJson; ActionResult)
    - app/actions/procurement.ts (createPurchase — для convertVirtualPurchase транзакции)
    - components/procurement/PurchaseModal.tsx (структура модалки — куда встроить from-virtual префилл)
    - lib/sales-plan/{virtual-purchases,data,engine}.ts
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §4.2 (жизненный цикл: регенерация транзакция, DISMISSED-подавление ±14 дней, конвертация), §7.6 (список VP-actions)
    - .planning/phases/25-v2-h2-2026/CRITIC-VERDICT.md (дыра №5: regenerateVirtualPurchases в цепочке saveDayOverrides обязателен)
  </read_first>
  <action>
В `app/actions/sales-plan.ts` добавь VP-actions (все SALES MANAGE, revalidatePath 3 роутов, кроме convert — +/procurement/purchases):

1. `regenerateVirtualPurchases(productIds?: string[])`: транзакция `tx.virtualPurchase.deleteMany({ where: { status: "SUGGESTED", source: "auto" } })` + вычисли предложения через `loadSalesPlanInputs` + `suggestVirtualPurchases` + `tx.virtualPurchase.createMany(новые SUGGESTED)`. ACCEPTED/DISMISSED/CONVERTED/manual неприкосновенны. DISMISSED с совпадающим `(productId, orderDate ± 14 дней)` подавляет повторное авто-предложение.
   **Встрой вызов regenerateVirtualPurchases в конец `saveMonthLevels` И `saveDayOverrides`** (замени TODO из Wave 3) — обе цепочки пересчёта («Пересчитать план (N)» + «Сохранить и пересчитать» модалки).

2. `acceptVirtualPurchase(id)`: SUGGESTED → ACCEPTED (переживает регенерацию).
3. `updateVirtualPurchase(id, { qty?, orderDate?, supplierId?, expectedArrivalDate?, unitPrice? })`: правка предложения → source остаётся, status ACCEPTED. **ИНВАРИАНТ «не прошлым числом» (серверный clamp — обязателен, т.к. клиент можно обойти): `orderDate = max(getMskTodayIso(), orderDate)`; `expectedArrivalDate = max(orderDate + leadTimeDays, expectedArrivalDate)` — виртуальная закупка НИКОГДА не размещается прошлым числом, приход не раньше стандартного цикла (`today + leadTimeDays`) от сегодня. Тот же clamp применяется при ручном создании VP (`source="manual"`). Иначе VP с прошлой датой прихода не участвует в симуляции (движок считает от today) и вводит план в заблуждение.**
4. `dismissVirtualPurchase(id)`: → DISMISSED (исключён из arrivals, план проседает, lostRub виден).
5. `convertVirtualPurchase(id)` — **SALES MANAGE + PROCUREMENT MANAGE**: возвращает `{ ok: true, redirectUrl: "/procurement/purchases?create=1&from-virtual=" + id }` (собственно создание Purchase — в PurchaseModal). После создания Purchase (в createPurchase) VP помечается `status=CONVERTED, convertedPurchaseId` — либо здесь через callback, либо createPurchase принимает fromVirtualId и обновляет VP в той же транзакции. Реализуй: convertVirtualPurchase только валидирует + отдаёт redirectUrl; финализация CONVERTED — в createPurchase при наличии fromVirtualId.

В `components/procurement/PurchaseModal.tsx`: если открыта с `?from-virtual=<id>` — префилл из VirtualPurchase (supplier, product, qty, unitPrice, `plannedArrivalDate = VP.expectedArrivalDate`); при сабмите createPurchase получает fromVirtualId → в транзакции создаёт Purchase + помечает VP CONVERTED (convertedPurchaseId). Анти-двойной счёт: CONVERTED исключён из arrivals (resolveArrivalBatches уже фильтрует SUGGESTED+ACCEPTED — CONVERTED не в этом множестве).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "regenerateVirtualPurchases\|acceptVirtualPurchase\|dismissVirtualPurchase\|convertVirtualPurchase\|updateVirtualPurchase" app/actions/sales-plan.ts; grep -c "from-virtual\|CONVERTED\|fromVirtual" components/procurement/PurchaseModal.tsx app/actions/procurement.ts 2>/dev/null; npx tsc --noEmit 2>&1 | grep -iE "actions/sales-plan|PurchaseModal" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function regenerateVirtualPurchases\|export async function acceptVirtualPurchase\|export async function updateVirtualPurchase\|export async function dismissVirtualPurchase\|export async function convertVirtualPurchase" app/actions/sales-plan.ts` === 5
    - regenerateVirtualPurchases вызывается в saveMonthLevels И saveDayOverrides: `grep -c "regenerateVirtualPurchases" app/actions/sales-plan.ts` ≥ 3 (определение + 2 вызова)
    - convertVirtualPurchase требует оба RBAC: рядом `requireSection("SALES", "MANAGE")` и `requireSection("PROCUREMENT", "MANAGE")`
    - инвариант «не прошлым числом» в updateVirtualPurchase (+ ручное создание): `grep -ci "max(.*today\|Math.max\|getMskTodayIso" app/actions/sales-plan.ts` ≥ 1 — orderDate и expectedArrivalDate не в прошлом
    - deleteMany только auto SUGGESTED: `grep -ci "SUGGESTED.*auto\|status.*SUGGESTED" app/actions/sales-plan.ts` ≥ 1
    - PurchaseModal from-virtual префилл: `grep -c "from-virtual\|fromVirtual\|CONVERTED" components/procurement/PurchaseModal.tsx app/actions/procurement.ts` ≥ 1
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>5 VP-actions; regenerateVirtualPurchases в обеих цепочках пересчёта; convertVirtualPurchase → PurchaseModal префилл + CONVERTED (анти-двойной счёт).</done>
</task>

<task type="auto">
  <name>Task 3: Таб «Пора заказывать» (page + VirtualPurchasesTable + Dialog) + бейджи ◇/⚠</name>
  <files>app/(dashboard)/sales-plan/purchases/page.tsx, components/sales-plan/VirtualPurchasesTable.tsx, components/sales-plan/VirtualPurchaseDialog.tsx, components/sales-plan/IncomingBadges.tsx</files>
  <read_first>
    - app/(dashboard)/procurement/purchases/page.tsx (образец RSC с searchParams-фильтрами + where)
    - components/procurement/PurchasesTable.tsx (таблица с действиями, чекбоксы)
    - components/procurement/PurchaseModal.tsx (образец VirtualPurchaseDialog)
    - components/sales-plan/IncomingBadges.tsx (Wave 4 — расширить на ◇/⚠)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.4 (VirtualPurchasesTable мокап + сегмент-фильтр + «Создать закупку»), §7.3 (бейджи IncomingBadges ◇ ACCEPTED / ⚠ SUGGESTED)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки purchases/page.tsx (484-511), VirtualPurchasesTable (786-797), VirtualPurchaseDialog (41)
  </read_first>
  <action>
1. `app/(dashboard)/sales-plan/purchases/page.tsx` — RSC. `await requireSection("SALES")`; canManage. Сегмент-фильтр `?status=suggested|accepted|dismissed|all` (default suggested) + каскадные фильтры. Загрузи `virtualPurchase.findMany({ where, include: { product, supplier }, orderBy: { orderDate: "asc" } })`. Для «Сток до» — прогони simulate плана per товар (первый stockout из плана, НЕ текущей скорости). Счётчики per статус. Рендер `<SalesPlanTabs urgentCount={N}/> <VirtualPurchasesTable rows={} canManage={}/>`.

2. `components/sales-plan/VirtualPurchasesTable.tsx` — client, sticky raw-HTML (CLAUDE.md, сплошной bg). Колонки: УКТ · Название · Сток · План шт/д · Сток до · Заказать до (просроченная красным, дефолтная сортировка) · Кол-во · Поставщик · Срок · Сумма · Действия. Действия: [Подтвердить] (acceptVirtualPurchase) · [Изменить] (VirtualPurchaseDialog) · [Убрать] (dismissVirtualPurchase); для ACCEPTED — [→ Создать закупку] `router.push("/procurement/purchases?create=1&from-virtual=" + id)`. Групповые чекбоксы → bulk-действия. Секция «Подтверждённые» отдельно. `<details>` «Как формируются предложения» внизу. Формат fmtRub/tabular-nums.

3. `components/sales-plan/VirtualPurchaseDialog.tsx` — client, модалка правки (образец PurchaseModal): qty, orderDate (`min`=today — дата в прошлом недоступна в пикере), supplierId (native select), expectedArrivalDate (`min`=orderDate + leadTimeDays — не раньше стандартного цикла от сегодня), unitPrice → updateVirtualPurchase. Клиентский min — UX-подсказка; жёсткий инвариант держит серверный clamp в updateVirtualPurchase.

4. `components/sales-plan/IncomingBadges.tsx` — расширь на виртуальные: `◇ DD.MM ×qty` ACCEPTED (dashed violet; popover правка/убрать) · `⚠ DD.MM ×qty (авто)` SUGGESTED (amber dashed; **учтена в плане**; действия подтвердить/изменить/убрать). Легенда обновляется. Реальные 📦 из Wave 4 сохраняются.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "VirtualPurchasesTable\|status" "app/(dashboard)/sales-plan/purchases/page.tsx"; grep -c "from-virtual\|acceptVirtualPurchase\|dismissVirtualPurchase" components/sales-plan/VirtualPurchasesTable.tsx; grep -c "SUGGESTED\|ACCEPTED\|Diamond\|AlertTriangle" components/sales-plan/IncomingBadges.tsx; npx tsc --noEmit 2>&1 | grep -iE "sales-plan/purchases|VirtualPurchase" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - 4 файла существуют/обновлены
    - page фильтр по статусу: `grep -c "suggested\|status" "app/(dashboard)/sales-plan/purchases/page.tsx"` ≥ 1
    - VirtualPurchasesTable: `grep -c "from-virtual\|Создать закупку" components/sales-plan/VirtualPurchasesTable.tsx` ≥ 1
    - VirtualPurchasesTable действия: `grep -c "acceptVirtualPurchase\|dismissVirtualPurchase" components/sales-plan/VirtualPurchasesTable.tsx` ≥ 2
    - НЕТ прозрачного sticky bg: `grep -c "bg-muted/[0-9]\|bg-background/[0-9]" components/sales-plan/VirtualPurchasesTable.tsx` === 0
    - IncomingBadges виртуальные бейджи: `grep -c "SUGGESTED\|ACCEPTED" components/sales-plan/IncomingBadges.tsx` ≥ 1
    - `npx tsc --noEmit` без ошибок; `npm run test` весь sales-plan suite (кроме pdds-feed — Wave 8) зелёный
  </acceptance_criteria>
  <done>Таб «Пора заказывать» с предложениями/действиями/конвертацией; бейджи ◇/⚠ в Товарах; opt-out работает (SUGGESTED учтены в плане).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → VP-actions | Пользователь подтверждает/правит/отклоняет/конвертирует виртуальные закупки |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Elevation of Privilege | VP write-actions | mitigate | `requireSection("SALES","MANAGE")`; convert — дополнительно `PROCUREMENT MANAGE` |
| T-25-06 | Tampering | двойной счёт при convert | mitigate | CONVERTED исключён из arrivals структурно; VP помечается CONVERTED в той же транзакции createPurchase |
| T-25-07 | Repudiation | регенерация затирает пользовательские VP | mitigate | deleteMany только `status=SUGGESTED, source=auto`; ACCEPTED/DISMISSED/manual неприкосновенны |
| T-25-08 | Tampering | VP размещена прошлым числом (ручная правка/manual) → нереалистичный приход, молчаливый no-op в симуляции | mitigate | инвариант orderDate ≥ today и expectedArrivalDate ≥ today+leadTime на всех путях: auto-clamp (Task 1) + серверный clamp в updateVirtualPurchase/manual create (Task 2) + min-даты в пикере (Task 3) |
</threat_model>

<verification>
- `npm run test` — sales-plan-virtual GREEN; вся суита кроме pdds-feed зелёная.
- `npx tsc --noEmit` — движок + actions + 4 UI типобезопасны.
- regenerateVirtualPurchases в обеих цепочках пересчёта (дыра критика №5 закрыта).
- ИНВАРИАНТ «не прошлым числом»: orderDate ≥ today и expectedArrivalDate ≥ today+leadTime — проверено в тесте (auto) и серверным clamp (manual/update); дата в прошлом недоступна в пикере.
- convertVirtualPurchase — двойной RBAC + анти-двойной счёт.
- Sticky без прозрачных bg.
</verification>

<success_criteria>
Этап 4 «Виртуальные закупки» завершён: генератор (SP-08, opt-out), таб «Пора заказывать», конвертация с анти-двойным-счётом (SP-09). План учитывает «как если бы заказывали вовремя». ⚠ Замерить на проде VPS 2GB полную цепочку «Пересчитать план» (симуляция 104 товаров + до 6 итераций suggester + revalidatePath 3 роутов — риск №11).
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-07-SUMMARY.md`. Отметь замер производительности цепочки пересчёта на проде.
</output>
