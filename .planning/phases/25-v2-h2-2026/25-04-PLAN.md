---
phase: 25-v2-h2-2026
plan: 04
type: execute
wave: 4
depends_on: [03, 05]
files_modified:
  - app/(dashboard)/sales-plan/products/page.tsx
  - components/sales-plan/SalesPlanTabs.tsx
  - components/sales-plan/SalesPlanFilters.tsx
  - components/sales-plan/ModelParamsBar.tsx
  - components/sales-plan/IncomingBadges.tsx
  - components/sales-plan/ProductPlanTable.tsx
  - components/sales-plan/ProductPlanCell.tsx
  - components/sales-plan/ProductPlanDialog.tsx
  - components/layout/section-titles.ts
autonomous: true
requirements: [SP-04, SP-05]
must_haves:
  truths:
    - "/sales-plan/products показывает помесячные уровни per товар (сток + Июл..Дек + Итог), sticky Фото/SKU/Название/Приходы"
    - "mode=edit рендерит инпуты (только MANAGE + черновик); bulk-drafts → «Пересчитать план (N)» → saveMonthLevels + router.refresh()"
    - "ProductPlanDialog вкладка «Дни»: getProductPlanDays lazy load + realtime «Сток(расч)» клиентским simulateProductPlan"
    - "IncomingBadges показывает реальные приходы 📦 (виртуальные ◇/⚠ добавит Wave 6)"
    - "SalesPlanTabs (3 таба, prefetch=false); section-titles для подроутов выше /^\\/sales-plan/"
  artifacts:
    - path: "app/(dashboard)/sales-plan/products/page.tsx"
      provides: "RSC Товары: loadSalesPlanInputs + computeSalesPlan + рендер таблицы"
      contains: "loadSalesPlanInputs"
    - path: "components/sales-plan/ProductPlanTable.tsx"
      provides: "Sticky-таблица уровней + bulk-drafts"
      contains: "Пересчитать план"
    - path: "components/sales-plan/ProductPlanDialog.tsx"
      provides: "Модалка Дни/Параметры/График с realtime"
      contains: "getProductPlanDays"
  key_links:
    - from: "ProductPlanTable.tsx"
      to: "app/actions/sales-plan.ts (saveMonthLevels)"
      via: "«Пересчитать план (N)» + router.refresh()"
      pattern: "saveMonthLevels"
    - from: "ProductPlanDialog.tsx"
      to: "lib/sales-plan/engine.ts (клиентский simulateProductPlan)"
      via: "realtime «Сток(расч)» без Prisma"
      pattern: "computeSalesPlan|simulateProductPlan"
    - from: "section-titles.ts"
      to: "/sales-plan/products, /sales-plan/purchases"
      via: "matches ВЫШЕ /^\\/sales-plan/"
      pattern: "/sales-plan/products"
---

<objective>
Этап 2 (Товары), часть 2: рабочий таб «Товары» `/sales-plan/products` — помесячные плановые уровни с редактированием, модалка правки по дням с realtime-пересчётом, приходы (реальные), панель параметров модели, каскадные фильтры, шапка-табы. Старый `/sales-plan` (Сводный) ещё жив как заглушка до Этапа 3.

Purpose: рабочий инструмент планирования per товар (SP-04). Таблица использует движок на сервере (RSC), модалка «Дни» — лениво грузит `getProductPlanDays` и пересчитывает «Сток(расч)» на клиенте pure-функцией. Bulk-drafts (паттерн текущего SalesForecastTable) — батчевое сохранение через «Пересчитать план (N)».
Output: страница + 7 компонентов sales-plan + проводка section-titles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Из app/actions/sales-plan.ts (Wave 3): saveMonthLevels, scaleMonthLevels, saveDayOverrides, saveProductPlanParams, saveModelParams, getProductPlanDays.
Из lib/sales-plan/data.ts (Wave 2): loadSalesPlanInputs. Из engine.ts (Wave 1): computeSalesPlan (клиентобезопасен для realtime).
Из lib/rbac.ts: requireSection("SALES"), getSectionRole("SALES") === "MANAGE" → canManage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SalesPlanTabs + SalesPlanFilters + ModelParamsBar + IncomingBadges + section-titles</name>
  <files>components/sales-plan/SalesPlanTabs.tsx, components/sales-plan/SalesPlanFilters.tsx, components/sales-plan/ModelParamsBar.tsx, components/sales-plan/IncomingBadges.tsx, components/layout/section-titles.ts</files>
  <read_first>
    - components/credits/CreditsTabs.tsx (образец табов: usePathname + startsWith + border-primary + prefetch=false)
    - components/sales-plan/SalesForecastFilters.tsx (каскадные фильтры — rename → SalesPlanFilters)
    - components/prices/wb/GlobalRatesBar.tsx (debounced-save глобальных параметров → ModelParamsBar)
    - components/layout/section-titles.ts (строка 32 /^\/sales-plan/ — matches подроутов вставить ВЫШЕ)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.1 (табы), §7.3 (ModelParamsBar, IncomingBadges легенда), §7.5 (каскад)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки SalesPlanTabs (515-560), ModelParamsBar (773-782), SalesPlanFilters (801-810), IncomingBadges (1084-1087)
  </read_first>
  <action>
1. `components/sales-plan/SalesPlanTabs.tsx` — client, копия структуры CreditsTabs. TABS = [{href "/sales-plan", label "Сводный"}, {href "/sales-plan/products", label "Товары"}, {href "/sales-plan/purchases", label "Пора заказывать"}]. isActive для "/sales-plan" — точное `pathname === "/sales-plan"`, для остальных — startsWith. Все `<Link prefetch={false}>`. Проп `urgentCount?: number` для бейджа «Пора заказывать» (пока не рендерится — заполнит Wave 6).

2. `components/sales-plan/SalesPlanFilters.tsx` — rename+перенос из SalesForecastFilters. Каскад Направление→Бренд→Категория→Подкатегория (CLAUDE.md паттерн: дочерние опции сужаются, невалидные выборы тихо вычищаются из URL). URL: `?directions=&brands=&categories=&subcategories=`. MultiSelectDropdown.

3. `components/sales-plan/ModelParamsBar.tsx` — client, collapsible (`<details>/<summary>`, без анимации). Поля (native input number): lead time fallback (defaultLeadTimeDays), страховой запас (safetyStockDays), покрытие VP (vpCoverDays), транзит (transitDays), лаг WB (wbInboundLagDays), deliveryDays, returnDays. Debounced save 500ms (useRef таймер per поле, паттерн GlobalRatesBar) → `saveModelParams` + `router.refresh()`. Проп `readOnly` — disable inputs при просмотре версии или VIEW-роли.

4. `components/sales-plan/IncomingBadges.tsx` — client (no-analog, shadcn Popover + Lucide Package/Diamond/AlertTriangle). Принимает `arrivals: ArrivalBatch[]`. В этом этапе рендерит ТОЛЬКО реальные приходы (`source === "purchase"|"incoming-legacy"`): `📦 DD.MM ×qty` + Popover (поставщик, этап из lib/purchase-stages.ts, dateSource-тег, ссылка на закупку `prefetch={false}`). Виртуальные ◇/⚠ — заглушка/скрыты (Wave 6 включит SUGGESTED+ACCEPTED). Легенда в футере таблицы — экспортируй `IncomingBadgesLegend`.

5. `components/layout/section-titles.ts` — добавь ВЫШЕ существующего `{ match: /^\/sales-plan/, title: "План продаж" }` (строка 32): `{ match: /^\/sales-plan\/products/, title: "План продаж — Товары" }` и `{ match: /^\/sales-plan\/purchases/, title: "План продаж — Пора заказывать" }` (порядок важен: специфичные matches раньше общего).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "/sales-plan/products\|/sales-plan/purchases" components/layout/section-titles.ts; grep -c "prefetch={false}" components/sales-plan/SalesPlanTabs.tsx; npx tsc --noEmit 2>&1 | grep -iE "SalesPlanTabs|ModelParamsBar|IncomingBadges|SalesPlanFilters|section-titles" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - 5 файлов существуют
    - `grep -c "prefetch={false}" components/sales-plan/SalesPlanTabs.tsx` ≥ 3 (все 3 таба)
    - section-titles: matches для обоих подроутов присутствуют ВЫШЕ общего `/^\/sales-plan/` — проверь порядок вручную (специфичные раньше)
    - ModelParamsBar debounced-save: `grep -c "saveModelParams\|setTimeout\|useRef" components/sales-plan/ModelParamsBar.tsx` ≥ 2
    - IncomingBadges НЕ рендерит SUGGESTED в этом этапе: `grep -c "Package\|Popover" components/sales-plan/IncomingBadges.tsx` ≥ 1
    - `npx tsc --noEmit` без ошибок про эти 5 файлов
  </acceptance_criteria>
  <done>Табы+фильтры+ModelParamsBar+IncomingBadges(реальные приходы)+section-titles готовы; каскад и prefetch=false соблюдены.</done>
</task>

<task type="auto">
  <name>Task 2: ProductPlanCell + ProductPlanTable (bulk-drafts + sticky)</name>
  <files>components/sales-plan/ProductPlanCell.tsx, components/sales-plan/ProductPlanTable.tsx</files>
  <read_first>
    - components/sales-plan/SalesForecastTable.tsx (bulk-drafts строки 107-200; fmtNum/fmtAdaptive/fmtRub 54-79 — копировать; sticky-структура)
    - components/cost/CostTable.tsx (CostCell строки 64-80 — inline-редактируемая ячейка)
    - components/credits/SummaryScheduleTable.tsx (строки 42-96 sticky-колонки, 117-160 raw-HTML thead — СПЛОШНОЙ bg на sticky!)
    - CLAUDE.md (Sticky data-таблицы — bg-background/bg-muted БЕЗ /NN; PRODUCT_HIERARCHY_ORDER_BY; prefetch=false)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.3 (ProductPlanTable мокап, ячейка compare/edit, bulk-drafts, «Масштабировать месяц»)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки ProductPlanTable (643-690), ProductPlanCell (694-715), «Форматирование чисел» (960-989), «Sticky-таблицы» (937-956)
  </read_first>
  <action>
1. `components/sales-plan/ProductPlanCell.tsx` — client, inline-редактируемая ячейка месяца (образец CostCell). Props: productId, month, value (number|null), baseline (number), readOnly, onChange(draft). Клик → показать native `<input>`, Enter/Escape → скрыть. placeholder = `авто ${baseline.toFixed(1)}`. Кнопка ✕ сбрасывает на null → placeholder. Подпись под инпутом: `≈ ${Math.round(effectiveRate * daysInMonth)} шт · ${fmtRub(monthTotal)}`. Маркер `•д` если у товара в этом месяце есть dayOverrides.

2. `components/sales-plan/ProductPlanTable.tsx` — client, переработка SalesForecastTable, сохрани паттерн bulk-drafts:
   - `drafts: Record<productId, Record<month, string>>` (двойной ключ). `pendingChangedCount` — счётчик изменившихся ячеек (сравнение с currentLevels, парсинг ",").
   - Кнопка «Пересчитать план (N)» → `startTransition(async () => { const r = await saveMonthLevels(payload); if(!r.ok){toast.error;return}; router.refresh(); setDrafts({}) })`. (regenerateVirtualPurchases в цепочку добавит Wave 6.)
   - «Отменить правки» → `setDrafts({})` локально.
   - Sticky-left колонки: Фото · SKU · Название · Приходы (`<IncomingBadges>`). Scroll: Сток · Июл…Дек · Итог ₽. Итоговая строка `sticky bottom-0 bg-muted` (СПЛОШНОЙ, без /NN). raw-HTML thead (не shadcn Table), `border-separate border-spacing-0`, каждая sticky-ячейка `bg-background`/`bg-muted` без прозрачности.
   - Режим `mode` (compare|edit) — проп; в edit рендерит ProductPlanCell (инпуты), только если readOnly=false.
   - Ячейка compare: `П <₽>` + для прошедших/текущего `Ф <₽>` + % отклонения цветом (>0 emerald, 0..−5% amber, <−5% red). `⚠` при сток-ограничении (tooltip).
   - Клик по строке товара (не по инпуту) → открыть ProductPlanDialog (передать productId).
   - Кнопка «Масштабировать месяц…» → диалог подтверждения → `scaleMonthLevels(month, factor)` → показать materializedCount/scaledCount.
   - Формат чисел fmtNum/fmtAdaptive/fmtRub — скопируй из SalesForecastTable. Сортировка через compareProductsByHierarchy (данные уже отсортированы в page).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "Пересчитать план\|saveMonthLevels\|drafts" components/sales-plan/ProductPlanTable.tsx; grep -c "bg-muted/\|bg-background/" components/sales-plan/ProductPlanTable.tsx; npx tsc --noEmit 2>&1 | grep -iE "ProductPlanTable|ProductPlanCell" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "saveMonthLevels" components/sales-plan/ProductPlanTable.tsx` ≥ 1
    - `grep -c "drafts" components/sales-plan/ProductPlanTable.tsx` ≥ 2 (bulk-drafts паттерн)
    - `grep -c "Пересчитать план" components/sales-plan/ProductPlanTable.tsx` ≥ 1
    - НЕТ прозрачного bg на sticky: `grep -c "bg-muted/[0-9]\|bg-background/[0-9]" components/sales-plan/ProductPlanTable.tsx` === 0 (CLAUDE.md sticky-правило)
    - `grep -c "scaleMonthLevels" components/sales-plan/ProductPlanTable.tsx` ≥ 1
    - ProductPlanCell: `grep -c "placeholder\|baseline" components/sales-plan/ProductPlanCell.tsx` ≥ 1
    - `npx tsc --noEmit` без ошибок про эти файлы
  </acceptance_criteria>
  <done>ProductPlanTable со sticky (сплошной bg), bulk-drafts, «Пересчитать план (N)», «Масштабировать месяц»; ProductPlanCell inline-редактирование с baseline-placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: ProductPlanDialog (realtime «Дни») + RSC page /sales-plan/products</name>
  <files>components/sales-plan/ProductPlanDialog.tsx, app/(dashboard)/sales-plan/products/page.tsx</files>
  <read_first>
    - components/sales-plan/ProductForecastDialog.tsx (строки 1-57 — паттерн Dialog + recharts + ChartContainer; переработка)
    - app/(dashboard)/prices/wb/page.tsx (RSC с большим payload — образец для products/page.tsx)
    - lib/sales-plan/engine.ts (computeSalesPlan/simulateProductPlan — клиентский realtime)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.3 (ProductPlanDialog вкладки Дни/Параметры/График, getProductPlanDays lazy, realtime «Сток(расч)», «Сохранить и пересчитать»)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки ProductPlanDialog (719-736), products/page.tsx (458-479)
  </read_first>
  <action>
1. `components/sales-plan/ProductPlanDialog.tsx` — client, max-w-4xl, замена ProductForecastDialog. Вкладки native-state (не URL): «Дни» (default) / «Параметры» / «График».
   - «Дни»: селектор месяца → при открытии/смене месяца лениво вызывает read-action `getProductPlanDays(productId, month, versionId?)` → получает `{ days, productInput }`. Таблица дней: Дата · План шт (native input, редактируемо если !readOnly) · План ₽ · Факт шт · Факт ₽ · Откл · Сток(расч) · События. **Realtime «Сток(расч)»**: при вводе в инпут дня — локально мержим правку в `productInput.dayOverrides` и вызываем клиентский pure `computeSalesPlan`/`simulateProductPlan` (импорт из engine, без Prisma) → пересчитываем колонку Сток(расч) без сервера. `*` = дневной override. События: приходы, «сток исчерпан».
   - Кнопка «Сохранить и пересчитать» → `saveDayOverrides(productId, overrides)` (regenerateVirtualPurchases добавит Wave 6) → закрыть + router.refresh(). disabled если readOnly.
   - «Параметры»: плановая цена ₽ (per месяц, редактируемо → saveProductPlanParams), % выкупа (read-only + источник own/legacy/subcategory/global), сток WB/Иваново.
   - «График»: bar-chart дней план vs факт + ReferenceLine приходов (recharts, паттерн ProductForecastDialog).

2. `app/(dashboard)/sales-plan/products/page.tsx` — RSC (образец prices/wb/page.tsx). `await requireSection("SALES")`; `canManage = (await getSectionRole("SALES")) === "MANAGE"`; searchParams `{ mode?, directions?, brands?, categories?, subcategories?, version? }`. `readOnly = mode !== "edit" || !canManage || Boolean(version)`. Загрузи `loadSalesPlanInputs(prisma, params)` (horizon/model-params из AppSetting) → `computeSalesPlan(inputs)`. Загрузи факт `loadFactDaily` для ячеек месяцев (прошедшие/текущий). Загрузи опции каскадных фильтров (brand/category/subcategory с FK-полями directionId/brandId/categoryId — CLAUDE.md). Отфильтруй по searchParams. Рендер: `<SalesPlanTabs /> <PlanVersionBar/>` (PlanVersionBar заглушка до Wave 7 — можно временно опустить или пустой) `<ModelParamsBar params={} readOnly={!canManage}/> <SalesPlanFilters .../> <ProductPlanTable products={planResult.products} fact={} mode={} readOnly={readOnly} canManage={canManage}/>`. Всё сериализуемо через RSC→client boundary (Decimal→number, Date→ISO).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "getProductPlanDays\|computeSalesPlan\|simulateProductPlan" components/sales-plan/ProductPlanDialog.tsx; grep -c "loadSalesPlanInputs\|requireSection" "app/(dashboard)/sales-plan/products/page.tsx"; npx tsc --noEmit 2>&1 | grep -iE "ProductPlanDialog|sales-plan/products" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getProductPlanDays" components/sales-plan/ProductPlanDialog.tsx` ≥ 1 (lazy load)
    - `grep -c "computeSalesPlan\|simulateProductPlan" components/sales-plan/ProductPlanDialog.tsx` ≥ 1 (клиентский realtime)
    - `grep -c "saveDayOverrides\|Сохранить и пересчитать" components/sales-plan/ProductPlanDialog.tsx` ≥ 1
    - page.tsx: `grep -c "loadSalesPlanInputs\|computeSalesPlan" "app/(dashboard)/sales-plan/products/page.tsx"` ≥ 1
    - page.tsx RBAC: `grep -c 'requireSection("SALES")\|getSectionRole' "app/(dashboard)/sales-plan/products/page.tsx"` ≥ 1
    - каскадные фильтры: page запрашивает FK-поля — `grep -c "directionId\|brandId\|categoryId" "app/(dashboard)/sales-plan/products/page.tsx"` ≥ 1
    - `npx tsc --noEmit` без ошибок; `npm run build` компилирует роут (или tsc-эквивалент)
  </acceptance_criteria>
  <done>ProductPlanDialog с realtime «Сток(расч)» (клиентский движок) и «Сохранить и пересчитать»; RSC page грузит движок + факт + каскадные фильтры; таб «Товары» рабочий.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — все 7 компонентов + page типобезопасны.
- ProductPlanDialog импортирует engine для клиентского realtime (движок клиентобезопасен — проверено Wave 1: ноль Prisma-импортов).
- Sticky-таблица без прозрачных bg (CLAUDE.md).
- section-titles: подроуты выше общего matcher.
- Старый `/sales-plan` (Сводный) не тронут — жив как заглушка до Этапа 3.
</verification>

<success_criteria>
Этап 2 «Товары» завершён (25-05+25-04): помесячный план per товар редактируется (bulk-drafts), симуляция с мульти-партийными приходами, факт per месяц в ячейках, модалка правки по дням с realtime-пересчётом стока, приходы (реальные), панель параметров модели, каскадные фильтры. SP-04, SP-05 закрыты. Прод-задачи: bootstrap-скрипт (25-03), проверка UserSectionRole SALES (25-05), заполнение plannedArrivalDate по открытым закупкам.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-04-SUMMARY.md`
</output>
