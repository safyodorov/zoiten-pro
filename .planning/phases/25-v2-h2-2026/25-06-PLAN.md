---
phase: 25-v2-h2-2026
plan: 06
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - lib/sales-plan/plan-fact.ts
  - app/(dashboard)/sales-plan/page.tsx
  - components/sales-plan/PlanFactControls.tsx
  - components/sales-plan/PlanFactSummaryCards.tsx
  - components/sales-plan/PlanFactChart.tsx
  - components/sales-plan/PlanFactMatrix.tsx
autonomous: true
requirements: [SP-06, SP-07, SP-10]
must_haves:
  truths:
    - "buildPlanFactReport бакетирует день/неделя/месяц/квартал/полугодие/год + pro-rata незавершённого бакета + deviation ₽/%"
    - "Три ряда: наш план (драфт/версия) / наш факт (funnel на лету) / ИУ (438 068 120 ₽)"
    - "Строка «Вне плана» = company-level факт − product-level (73 непривязанных nmId)"
    - "Колонка «Итог» = горизонт H2; year-бакет в движке для будущих лет"
    - "guard end ≥ today снят; day-разбивка ограничена окном 62 дня с notice"
  artifacts:
    - path: "lib/sales-plan/plan-fact.ts"
      provides: "buildPlanFactReport (pure)"
      exports: ["buildPlanFactReport"]
    - path: "app/(dashboard)/sales-plan/page.tsx"
      provides: "RSC Сводный: матрица + KPI + график"
      contains: "buildPlanFactReport"
    - path: "components/sales-plan/PlanFactMatrix.tsx"
      provides: "Sticky-матрица план/факт/ИУ по бакетам"
      contains: "Вне плана"
  key_links:
    - from: "lib/sales-plan/plan-fact.ts"
      to: "lib/date-buckets.ts (bucketKey 6 бакетов)"
      via: "бакетирование дневных рядов"
      pattern: "bucketKey"
    - from: "app/(dashboard)/sales-plan/page.tsx"
      to: "lib/sales-plan/{data,iu,plan-fact}.ts"
      via: "loadFactDaily + iuSeriesForRange + buildPlanFactReport"
      pattern: "loadFactDaily|iuTotalForRange|buildPlanFactReport"
---

<objective>
Этап 3 (Сводный): переработка `/sales-plan` в план/факт/ИУ-витрину. Pure `buildPlanFactReport` (TDD — доводит Wave-0-стаб `sales-plan-plan-fact` до GREEN) + Сводный UI (матрица, KPI, график). Снимается хардкод `IU_REMAINING_RUB`, guard `end ≥ today`.

Purpose: три ряда данных (SP-06), отклонение по бакетам с pro-rata (SP-07), факт из funnel двумя разрезами + строка «Вне плана» (SP-10). ИУ = 2 380 805 ₽/день по выкупам (iuMetric="buyouts"). До первой фиксации версии прошедшие дни = unconstrained номинал (бейдж «номинал» — §6.1).
Output: `lib/sales-plan/plan-fact.ts` + переработанный `page.tsx` + 4 компонента PlanFact*.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-v2-h2-2026/25-RESEARCH.md
@.planning/phases/25-v2-h2-2026/25-PATTERNS.md
@CLAUDE.md

<interfaces>
Из lib/date-buckets.ts (Wave 1): bucketKey, bucketLabel, Granularity (6 бакетов).
Из lib/sales-plan/iu.ts (Wave 1): iuTotalForRange, iuSeriesForRange.
Из lib/sales-plan/data.ts (Wave 2): loadFactDaily(db, from, to) → { company, byProduct, settledThroughIso }, loadSalesPlanInputs.
Из lib/sales-plan/engine.ts (Wave 1): computeSalesPlan (драфт-ряд плана до появления версий).
Из components/sales-plan/SalesPlanTabs.tsx (Wave 4): SalesPlanTabs.
tests/sales-plan-plan-fact.test.ts (Wave 0 стаб — контракт buildPlanFactReport).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/sales-plan/plan-fact.ts — buildPlanFactReport (стаб → GREEN)</name>
  <files>lib/sales-plan/plan-fact.ts</files>
  <behavior>
    - Test: бакетирование day/week/month/quarter/halfyear/year через bucketKey
    - Test: deviation ₽ = факт − план; % = (факт/план − 1)×100
    - Test: pro-rata — незавершённый бакет (содержит today) считает план только за дни ≤ вчера
    - Test: factSettled — дни > today−7 помечены unsettled
    - Test: строка «Вне плана» = company-level факт − product-level, суммы сходятся
  </behavior>
  <read_first>
    - lib/loan-math.ts / lib/date-buckets.ts (bucketKey/bucketLabel — бакетирование)
    - lib/sales-forecast.ts (агрегация — образец)
    - tests/sales-plan-plan-fact.test.ts (Wave 0 контракт)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §6.2 (формулы план/факт: pro-rata, FAC, ИУ-блок, накопительный итог), §5 (два разреза факта, «Вне плана»)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блок plan-fact (177-195)
  </read_first>
  <action>
Создай `lib/sales-plan/plan-fact.ts` — pure (Prisma передаётся снаружи как загруженные ряды). `buildPlanFactReport(input)`:
- Вход: `planDaily` (дневной ряд плана: date → planRub/planUnits — из версии или драфта), `factCompanyDaily` (Map date → buyoutsRub — company-level), `factProductDaily` (Map date → buyoutsRub — product-level сумма привязанных), `iuTargets`, `granularity: Granularity`, `from`, `to`, `today`, `cumulative: boolean`, `settledThroughIso`.
- Бакетирование: группируй дни по `bucketKey(date, granularity)`. Для каждого бакета: `план = Σ planRub`, `факт = Σ factCompanyDaily`, `отклонение ₽ = факт − план`, `% = (факт/план − 1)×100` (guard план=0).
- pro-rata: для текущего бакета (содержит today) — план считается только за дни `≤ вчера` (иначе месяц перманентно красный).
- factSettled: дни `> settledThroughIso` (= today−7) → флаг unsettled (потребитель приглушает).
- ИУ-блок: `iuCum(d) = iuSeriesForRange` из iu.ts; KPI `отставание от ИУ = факт_накоп − иу_накоп(вчера)`; требуемый run-rate = (438 068 120 − факт_накоп)/оставшиеся_дни; план vs ИУ на завершение.
- FAC: primary = факт_накоп + план_остатка; secondary = линейный run-rate.
- Строка «Вне плана»: `factCompanyDaily − factProductDaily` per бакет (73 непривязанных nmId) — суммы «Вне плана» + product-level = company-level всегда.
- Колонка «Итог» = year-бакет / весь горизонт. Верни структуру для матрицы (buckets[] с label/plan/fact/deviation/iu) + KPI-агрегаты + «Вне плана» ряд.
Zero Prisma/React/Next импортов. Импорт bucketKey из date-buckets, iu-функции из ./iu.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && npx vitest run tests/sales-plan-plan-fact.test.ts 2>&1 | grep -iE "passed|failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sales-plan-plan-fact.test.ts` — все PASS (GREEN)
    - `grep -c "export function buildPlanFactReport" lib/sales-plan/plan-fact.ts` === 1
    - `grep -c "bucketKey" lib/sales-plan/plan-fact.ts` ≥ 1
    - pro-rata реализован: `grep -ci "prorata\|pro-rata\|yesterday\|вчера" lib/sales-plan/plan-fact.ts` ≥ 1
    - строка «Вне плана»: `grep -ci "company\|byProduct\|вне плана\|unplanned" lib/sales-plan/plan-fact.ts` ≥ 1
    - `grep -c "from \"@/lib/prisma\|from \"react\|from \"next" lib/sales-plan/plan-fact.ts` === 0 (pure)
  </acceptance_criteria>
  <done>plan-fact.ts pure с бакетированием (6 бакетов), pro-rata, deviation, FAC, ИУ-блоком, «Вне плана»; Wave-0-стаб GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: PlanFactControls + PlanFactSummaryCards + PlanFactChart</name>
  <files>components/sales-plan/PlanFactControls.tsx, components/sales-plan/PlanFactSummaryCards.tsx, components/sales-plan/PlanFactChart.tsx</files>
  <read_first>
    - components/credits/ScheduleControls.tsx (URL-state, сегментированный переключатель, native input date, пресеты)
    - components/credits/LoanSummaryCards.tsx (KPI-карточки с цветом)
    - components/sales-plan/SalesForecastDailyChart.tsx (recharts bars/line — образец PlanFactChart)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.2 (тулбар PlanFactControls, KPI-карточки, PlanFactChart)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки PlanFactControls (564-596), PlanFactSummaryCards (33), PlanFactChart (34)
  </read_first>
  <action>
1. `components/sales-plan/PlanFactControls.tsx` — client, URL-state (паттерн ScheduleControls). Сегментированный переключатель разбивки: День|Неделя|Месяц|Квартал|Полугодие (без «год» — это колонка «Итог»). `?granularity=` (default month; day ограничен окном 62 дня → notice при выборе). Пресеты (кнопки): Тек. неделя / Тек. месяц / 3 мес / Полугодие — устанавливают from/to. native `<input type="date">` для from/to (clamp в горизонт; guard end≥today снят). native `<select>` метрики: Выкупы ₽ / Выкупы шт / Заказы ₽ / Заказы шт (`?metric=`). Чекбокс «Нарастающим итогом» (`?cumulative=1`). Всё через router.push с URLSearchParams.

2. `components/sales-plan/PlanFactSummaryCards.tsx` — 5 KPI-карточек (§7.2): Факт за период (+% от плана, emerald/amber/red), План за период (+версия), ИУ-план за период (+«факт = X% ИУ»), Прогноз на 31.12 (FAC vs ИУ 438.1М), **Отставание от ИУ нарастающим** (₽ + дни-эквивалент /2 380 805 — крупнейшая типографика, главная «тревожная лампочка»). Принимает готовые агрегаты из page (buildPlanFactReport). Цвет по знаку отклонения.

3. `components/sales-plan/PlanFactChart.tsx` — client, recharts. Факт — bars; План — ступенчатая line; ИУ — dashed line (в cumulative — наклонная прямая). ReferenceLine «сегодня». Последние ~5 дней факта приглушены (unsettled) + легенда «выкупы дозаполняются 3–7 дней». Данные — из page.
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "granularity\|cumulative" components/sales-plan/PlanFactControls.tsx; grep -c "Отставание\|ИУ" components/sales-plan/PlanFactSummaryCards.tsx; npx tsc --noEmit 2>&1 | grep -iE "PlanFactControls|PlanFactSummaryCards|PlanFactChart" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - 3 файла существуют
    - PlanFactControls URL-state: `grep -c "router.push\|URLSearchParams\|granularity" components/sales-plan/PlanFactControls.tsx` ≥ 2
    - PlanFactControls native select метрики (не base-ui): `grep -c "<select" components/sales-plan/PlanFactControls.tsx` ≥ 1
    - PlanFactSummaryCards: `grep -ci "Отставание от ИУ\|438" components/sales-plan/PlanFactSummaryCards.tsx` ≥ 1
    - PlanFactChart recharts bars+line: `grep -c "Bar\|Line\|ReferenceLine" components/sales-plan/PlanFactChart.tsx` ≥ 2
    - `npx tsc --noEmit` без ошибок про эти 3 файла
  </acceptance_criteria>
  <done>Тулбар (5 разбивок + пресеты + метрика + cumulative), 5 KPI-карточек (с «Отставание от ИУ»), график факт/план/ИУ.</done>
</task>

<task type="auto">
  <name>Task 3: PlanFactMatrix + RSC page /sales-plan (Сводный)</name>
  <files>components/sales-plan/PlanFactMatrix.tsx, app/(dashboard)/sales-plan/page.tsx</files>
  <read_first>
    - components/credits/SummaryScheduleTable.tsx (главный образец sticky-матрицы с периодными колонками; строки 42-96, 117-160)
    - app/(dashboard)/sales-plan/page.tsx (ТЕКУЩИЙ — переработка; извлечь переиспользуемое, но убрать IU_REMAINING_RUB и guard end≥today)
    - CLAUDE.md (sticky bg сплошной; native select; MSK; русский)
    - .planning/phases/25-v2-h2-2026/25-RESEARCH.md §7.2 (PlanFactMatrix мокап + строки план/факт/откл/ИУ + «Вне плана» + футнот метрико-зависимый), §6.2 (сшивка ряда план: <today версия, ≥today драфт)
    - .planning/phases/25-v2-h2-2026/25-PATTERNS.md блоки PlanFactMatrix (600-639), sales-plan/page.tsx (412-453)
  </read_first>
  <action>
1. `components/sales-plan/PlanFactMatrix.tsx` — client, sticky-таблица (образец SummaryScheduleTable). Единственная sticky-left колонка «Показатель» (width 240). Колонки-бакеты + «Итог». Строки: План ₽ · Факт ₽ · Отклонение ₽ · Отклонение % · (разделитель) · ИУ-план ₽ · Факт − ИУ ₽ · Выполнение ИУ % · ИУ нарастающим откл ₽. Свёрнутые группы «Штуки» / «Заказы ₽». raw-HTML thead (не shadcn Table), `border-separate border-spacing-0`, СПЛОШНОЙ bg на каждой sticky-ячейке (bg-background/bg-muted БЕЗ /NN — CLAUDE.md). Текущий бакет помечен `*` (факт по вчера, pro-rata). tabular-nums, fmtRub. **ИУ-строки при активных каскадных фильтрах СКРЫВАЮТСЯ** с notice «ИУ сравнивается только с полным фактом компании». Строка «Вне плана» и метрико-зависимый футнот (buyouts: «73 артикула, 2,4 М ₽/мес, 3,2% — [настроить → /cards/wb]»; orders: «13,2 М ₽/мес, ~6%»).

2. `app/(dashboard)/sales-plan/page.tsx` — RSC переработка. `await requireSection("SALES")`; canManage; searchParams `{ granularity?, from?, to?, metric?, cumulative?, version? }`. Убери `IU_REMAINING_RUB` и `DEFAULT_END_DATE` guard (end≥today снят — clamp в горизонт). Загрузи: horizon/iuTargets из AppSetting; `loadFactDaily(prisma, from, to)` (company + product-level); план — из активной версии (`salesPlan.activeVersionId` → SalesPlanVersionDay когда появятся Wave 7; сейчас драфт через `loadSalesPlanInputs` + `computeSalesPlan` companyDaily) — для дней <today до первой фиксации номинал из monthLevels (бейдж «номинал (без сток-лимита)»); `iuSeriesForRange`. Прогони `buildPlanFactReport`. Рендер: `<SalesPlanTabs/> <PlanVersionBar/>` (заглушка до Wave 7) `<PlanFactControls/> <PlanFactSummaryCards/> <PlanFactChart/> <PlanFactMatrix/> <SalesPlanFilters/>`. Каскадные фильтры влияют на план/факт product-level (ИУ-строки скрываются при фильтрах). day-разбивка ограничена окном 62 дня (notice если шире).
  </action>
  <verify>
    <automated>cd "c:/Users/serge/zoiten-pro" && grep -c "Вне плана\|bg-muted/\|bg-background/" components/sales-plan/PlanFactMatrix.tsx; grep -c "buildPlanFactReport\|loadFactDaily\|IU_REMAINING_RUB" "app/(dashboard)/sales-plan/page.tsx"; npx tsc --noEmit 2>&1 | grep -iE "PlanFactMatrix|sales-plan/page" | head; echo done</automated>
  </verify>
  <acceptance_criteria>
    - PlanFactMatrix: `grep -c "Вне плана" components/sales-plan/PlanFactMatrix.tsx` ≥ 1
    - НЕТ прозрачного bg на sticky: `grep -c "bg-muted/[0-9]\|bg-background/[0-9]" components/sales-plan/PlanFactMatrix.tsx` === 0
    - page использует движок+факт+ИУ: `grep -c "buildPlanFactReport" "app/(dashboard)/sales-plan/page.tsx"` ≥ 1 и `grep -c "loadFactDaily" "app/(dashboard)/sales-plan/page.tsx"` ≥ 1
    - хардкод удалён: `grep -c "IU_REMAINING_RUB\|DEFAULT_END_DATE" "app/(dashboard)/sales-plan/page.tsx"` === 0
    - `npm run test` — вся sales-plan test suite (кроме virtual/pdds — Waves 4/6) зелёная
    - `npx tsc --noEmit` без ошибок; роут компилируется
  </acceptance_criteria>
  <done>Сводный переработан: матрица (3 ряда + «Вне плана», sticky сплошной bg), KPI, график; IU_REMAINING_RUB/DEFAULT_END_DATE удалены; guard end≥today снят.</done>
</task>

</tasks>

<verification>
- `npm run test` — sales-plan-plan-fact GREEN; engine/arrivals/iu/date-buckets GREEN; virtual/pdds-feed остаются RED (Waves 6/8).
- `npx tsc --noEmit` — plan-fact + 4 компонента + page типобезопасны.
- Sticky-матрица без прозрачных bg.
- IU_REMAINING_RUB и DEFAULT_END_DATE guard удалены из Сводного.
- Три ряда (план-драфт/факт/ИУ) по всем бакетам + строка «Вне плана».
</verification>

<success_criteria>
Этап 3 «Сводный» завершён: три ряда данных (SP-06), план/факт с отклонением по 5 бакетам + «Итог» + pro-rata + FAC (SP-07), факт из funnel двумя разрезами + строка «Вне плана» (SP-10). ⚠ Этапы 3-5 деплоить плотной серией; первую версию фиксировать в день деплоя Этапа 5.
</success_criteria>

<output>
После завершения создай `.planning/phases/25-v2-h2-2026/25-06-SUMMARY.md`. Отметь: до первой фиксации версии (Wave 7) прошедшие дни = unconstrained номинал — бейдж «номинал (без сток-лимита)».
</output>
