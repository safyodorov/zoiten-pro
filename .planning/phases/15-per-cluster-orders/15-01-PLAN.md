---
phase: 15-per-cluster-orders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql
  - .planning/REQUIREMENTS.md
  - lib/wb-api.ts
  - tests/wb-orders-per-warehouse.test.ts
autonomous: true
requirements: [ORDERS-01, ORDERS-02, ORDERS-03]
must_haves:
  truths:
    - "В REQUIREMENTS.md v1.2 секции присутствуют требования ORDERS-01, ORDERS-02, ORDERS-03 с описанием"
    - "Prisma schema содержит модель WbCardWarehouseOrders с unique (wbCardId, warehouseId) + Cascade от WbCard и от WbWarehouse (SetNull или Cascade — решить)"
    - "lib/wb-api.ts экспортирует fetchOrdersPerWarehouse(nmIds: number[], periodDays?: number): Promise<Map<nmId, { avg: number, yesterday: number, perWarehouse: Map<warehouseName, number> }>>"
    - "Existing fetchAvgSalesSpeed7d либо помечена @deprecated с редиректом на fetchOrdersPerWarehouse, либо оставлена без изменений (не ломаем текущие вызовы)"
    - "tests/wb-orders-per-warehouse.test.ts — GREEN: 6+ кейсов (happy path, фильтрация isCancel, агрегация per warehouseName, пустой ответ, 429 retry, несуществующий nmId)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Model WbCardWarehouseOrders"
      contains: "model WbCardWarehouseOrders"
    - path: "prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql"
      provides: "SQL миграция CREATE TABLE + индексы"
      contains: "CREATE TABLE \"WbCardWarehouseOrders\""
    - path: ".planning/REQUIREMENTS.md"
      provides: "ORDERS-01..ORDERS-03 в v1.2"
      contains: "ORDERS-01"
    - path: "lib/wb-api.ts"
      provides: "fetchOrdersPerWarehouse + интерфейс WarehouseOrdersStats"
      contains: "export async function fetchOrdersPerWarehouse"
    - path: "tests/wb-orders-per-warehouse.test.ts"
      provides: "Vitest тесты парсинга + агрегации"
      contains: "fetchOrdersPerWarehouse"
  key_links:
    - from: "lib/wb-api.ts"
      to: "statistics-api.wildberries.ru/api/v1/supplier/orders"
      via: "fetch с header Authorization"
      pattern: "statistics-api\\.wildberries\\.ru.*supplier/orders"
    - from: "prisma/schema.prisma WbCardWarehouseOrders"
      to: "WbCard, WbWarehouse"
      via: "relation + onDelete Cascade"
      pattern: "onDelete: Cascade"
---

<objective>
Foundation для Phase 15: создать модель БД `WbCardWarehouseOrders` (миграция), добавить требования ORDERS-01/02/03 в REQUIREMENTS.md, реализовать в `lib/wb-api.ts` функцию `fetchOrdersPerWarehouse` которая делает ОДИН запрос к WB Statistics Orders API за 7 дней и возвращает одновременно: (a) aggregate avg7d + ordersYesterday per nmId (для backward compat — заменяет вызов `fetchAvgSalesSpeed7d`), (b) per-warehouse breakdown per nmId (для новой фичи Phase 15). Покрыть тестами. Автономно — без чекпоинтов.

Purpose: без схемы БД и API-функции нельзя интегрировать orders в wb-sync (Plan 15-02) и в UI (Plan 15-03). Один запрос к Orders API (rate limit ~1/мин) покрывает обе задачи — не делаем два вызова подряд.
Output: миграция применена локально (schema sync), TypeScript API, GREEN тесты, требования в REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@prisma/schema.prisma
@lib/wb-api.ts
@.planning/phases/14-stock/14-03-SUMMARY.md

<interfaces>
<!-- Ключевые контракты из существующего кода — executor не должен исследовать кодовую базу. -->

Существующая Prisma модель WbCardWarehouseStock (prisma/schema.prisma:798-810) — паттерн для WbCardWarehouseOrders:
```prisma
model WbCardWarehouseStock {
  id          String      @id @default(cuid())
  wbCardId    String
  wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
  warehouseId Int
  warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
  quantity    Int         @default(0)
  updatedAt   DateTime    @updatedAt

  @@unique([wbCardId, warehouseId])
  @@index([wbCardId])
  @@index([warehouseId])
}
```

Существующая функция fetchAvgSalesSpeed7d (lib/wb-api.ts:843-909) — источник алгоритма фильтрации isCancel и TZ-логики вчерашнего дня:
```typescript
// GET https://statistics-api.wildberries.ru/api/v1/supplier/orders?dateFrom=<ISO 7d ago>&flag=0
// Header: Authorization: <WB_API_TOKEN>
// Response: Array<{ nmId, warehouseName, isCancel, date, ... }>
// Логика: отфильтровать isCancel=true, сгруппировать по nmId, avg = count/7
// Yesterday: сравнение по префиксу date "YYYY-MM-DD" в Moscow TZ (UTC+3)
// Rate limit: 429 → sleep(60_000) + рекурсивный retry (однократный)
```

Существующий интерфейс OrdersStats (lib/wb-api.ts:729-734):
```typescript
export interface OrdersStats {
  avg7d: number      // заказы минус отмены за 7 дней / 7
  yesterday: number  // заказы минус отмены за вчерашний день (Moscow TZ)
}
```

Паттерн миграции Phase 14 STOCK-01 (prisma/migrations/20260422_*):
- CREATE TABLE с колонками id (TEXT), wbCardId (TEXT), warehouseId (INT), ordersCount (INT DEFAULT 0), periodDays (INT DEFAULT 7), updatedAt (TIMESTAMP)
- Unique index (wbCardId, warehouseId)
- FK с ON DELETE CASCADE на WbCard.id
- FK на WbWarehouse.id (без Cascade — auto-insert unknown)

Запуск тестов: `npm test -- wb-orders-per-warehouse` (vitest)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: REQUIREMENTS.md — добавить ORDERS-01/02/03 + Prisma schema миграция WbCardWarehouseOrders</name>
  <read_first>
    - .planning/REQUIREMENTS.md (полностью — понять структуру v1.2 секции)
    - prisma/schema.prisma (секция Phase 14: Stock Management, строки 786-810 — паттерн WbCardWarehouseStock)
    - prisma/migrations/ (найти последнюю миграцию Phase 14 для примера SQL формата и FK ON DELETE Cascade синтаксиса)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - prisma/schema.prisma
    - prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql
  </files>
  <behavior>
    - REQUIREMENTS.md после правки содержит в секции "## v1.2 Requirements — Управление остатками" новую подсекцию `### Orders Per-Warehouse (Phase 15)` с тремя требованиями:
      * ORDERS-01: WB Orders API за 7 дней синхронизируется в таблицу WbCardWarehouseOrders per-warehouse при /api/wb-sync. Unique (wbCardId, warehouseId). Cascade от WbCard.
      * ORDERS-02: /stock/wb колонка З каждого кластера (collapsed) = SUM(ordersCount кластера) / periodDays. При expand — per-warehouse З = ordersCount / periodDays. Об/Д пересчитывается от кластерной З.
      * ORDERS-03: scripts/wb-sync-stocks.js расширен секцией orders (синхронно со stocks). WbCard.avgSalesSpeed7d остаётся fallback когда per-warehouse данных нет.
    - В секции Traceability table добавлены 3 строки: `| ORDERS-01 | Phase 15 | Pending |` ... ORDERS-03
    - В футере файла добавлена строка `*Phase 15 added: 2026-04-22 | +3 requirements (ORDERS-01..ORDERS-03)*`
    - prisma/schema.prisma после правки содержит новую модель WbCardWarehouseOrders (после модели WbCardWarehouseStock, строки ~810+):
      * id String @id @default(cuid())
      * wbCardId String + relation WbCard onDelete: Cascade
      * warehouseId Int + relation WbWarehouse (без Cascade)
      * ordersCount Int @default(0)
      * periodDays Int @default(7)
      * updatedAt DateTime @updatedAt
      * @@unique([wbCardId, warehouseId])
      * @@index([wbCardId])
      * @@index([warehouseId])
    - В модели WbCard добавлен обратный relation: `warehouseOrders WbCardWarehouseOrders[]`
    - В модели WbWarehouse добавлен обратный relation: `orders WbCardWarehouseOrders[]`
    - prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql содержит:
      * CREATE TABLE "WbCardWarehouseOrders" с колонками + ограничениями
      * CREATE UNIQUE INDEX "WbCardWarehouseOrders_wbCardId_warehouseId_key"
      * CREATE INDEX на wbCardId и warehouseId
      * ALTER TABLE ADD CONSTRAINT FOREIGN KEY wbCardId REFERENCES "WbCard"("id") ON DELETE CASCADE
      * ALTER TABLE ADD CONSTRAINT FOREIGN KEY warehouseId REFERENCES "WbWarehouse"("id")
  </behavior>
  <action>
    1. Открыть .planning/REQUIREMENTS.md. Добавить подсекцию после блока STOCK-26..STOCK-29 (перед `## v2 Requirements`):

    ```markdown
    ### Orders Per-Warehouse (Phase 15)

    - [ ] **ORDERS-01**: Prisma миграция — модель `WbCardWarehouseOrders(id, wbCardId, warehouseId, ordersCount Int @default(0), periodDays Int @default(7), updatedAt)` с `@@unique([wbCardId, warehouseId])`, indexes на `wbCardId` и `warehouseId`, FK `wbCardId → WbCard.id ON DELETE CASCADE`, FK `warehouseId → WbWarehouse.id`. Обратные relations в `WbCard.warehouseOrders` и `WbWarehouse.orders`.
    - [ ] **ORDERS-02**: При `POST /api/wb-sync` параллельно stocks загружаются orders за 7 дней через `GET statistics-api.wildberries.ru/api/v1/supplier/orders?dateFrom=<7d ago>&flag=0`. `isCancel: true` ИСКЛЮЧАЮТСЯ. Clean-replace per `wbCardId` в транзакции: `deleteMany NOT IN incoming` + `upsert` per warehouse. Auto-insert неизвестных складов через `stableWarehouseIdFromName` + `needsClusterReview: true` (паттерн STOCK-10).
    - [ ] **ORDERS-03**: На `/stock/wb` колонка З каждого кластера (collapsed) = `SUM(ordersCount per warehouses of cluster) / periodDays`. При expand кластера — per-warehouse З = `ordersCount / periodDays`. Метрики Об/Д per-кластер пересчитываются от кластерной З (не от `card.avgSalesSpeed7d`) через существующую `calculateStockMetrics` из `lib/stock-math.ts`. `WbCard.avgSalesSpeed7d` остаётся fallback для nmId без per-warehouse данных и для Сводной колонки МП/З. `scripts/wb-sync-stocks.js` расширен секцией orders (идентичный паттерн stocks section).
    ```

    Также добавить в таблицу Traceability перед `---\n*Defined: 2026-04-05...` строки:
    ```
    | ORDERS-01 | Phase 15 | Pending |
    | ORDERS-02 | Phase 15 | Pending |
    | ORDERS-03 | Phase 15 | Pending |
    ```

    И добавить в футер новую строку:
    ```
    *Phase 15 added: 2026-04-22 | +3 requirements (ORDERS-01..ORDERS-03) | extends Phase 14 with per-warehouse orders*
    ```

    2. Открыть prisma/schema.prisma. В модель `WbCard` (после строки `warehouses  WbCardWarehouseStock[]`) добавить:
    ```prisma
      // Phase 15: Per-warehouse orders breakdown (ORDERS-01)
      warehouseOrders     WbCardWarehouseOrders[]
    ```

    В модель `WbWarehouse` (после `stocks WbCardWarehouseStock[]`) добавить:
    ```prisma
      orders             WbCardWarehouseOrders[]
    ```

    После модели `WbCardWarehouseStock` добавить:
    ```prisma
    // Phase 15: Per-warehouse orders (ORDERS-01)
    // Заказы минус отмены за periodDays (default 7 дней) per (wbCardId, warehouseId).
    // Обновляется при /api/wb-sync через fetchOrdersPerWarehouse + clean-replace per wbCardId.
    // Clean-replace: deleteMany NOT IN incoming + upsert по unique (wbCardId, warehouseId).
    model WbCardWarehouseOrders {
      id          String      @id @default(cuid())
      wbCardId    String
      wbCard      WbCard      @relation(fields: [wbCardId], references: [id], onDelete: Cascade)
      warehouseId Int
      warehouse   WbWarehouse @relation(fields: [warehouseId], references: [id])
      ordersCount Int         @default(0)
      periodDays  Int         @default(7)
      updatedAt   DateTime    @updatedAt

      @@unique([wbCardId, warehouseId])
      @@index([wbCardId])
      @@index([warehouseId])
    }
    ```

    3. Создать директорию и файл prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql:

    ```sql
    -- Phase 15 (ORDERS-01): WbCardWarehouseOrders — заказы per-warehouse за N дней
    CREATE TABLE "WbCardWarehouseOrders" (
      "id"          TEXT        NOT NULL,
      "wbCardId"    TEXT        NOT NULL,
      "warehouseId" INTEGER     NOT NULL,
      "ordersCount" INTEGER     NOT NULL DEFAULT 0,
      "periodDays"  INTEGER     NOT NULL DEFAULT 7,
      "updatedAt"   TIMESTAMP(3) NOT NULL,
      CONSTRAINT "WbCardWarehouseOrders_pkey" PRIMARY KEY ("id")
    );

    CREATE UNIQUE INDEX "WbCardWarehouseOrders_wbCardId_warehouseId_key"
      ON "WbCardWarehouseOrders"("wbCardId", "warehouseId");

    CREATE INDEX "WbCardWarehouseOrders_wbCardId_idx"
      ON "WbCardWarehouseOrders"("wbCardId");

    CREATE INDEX "WbCardWarehouseOrders_warehouseId_idx"
      ON "WbCardWarehouseOrders"("warehouseId");

    ALTER TABLE "WbCardWarehouseOrders"
      ADD CONSTRAINT "WbCardWarehouseOrders_wbCardId_fkey"
      FOREIGN KEY ("wbCardId") REFERENCES "WbCard"("id")
      ON DELETE CASCADE ON UPDATE CASCADE;

    ALTER TABLE "WbCardWarehouseOrders"
      ADD CONSTRAINT "WbCardWarehouseOrders_warehouseId_fkey"
      FOREIGN KEY ("warehouseId") REFERENCES "WbWarehouse"("id")
      ON DELETE RESTRICT ON UPDATE CASCADE;
    ```

    4. Запустить `npx prisma format` (для нормализации schema.prisma) и `npx prisma generate` (для обновления TypeScript клиента). Миграцию НЕ применять локально (нет локальной PG — паттерн Phase 14). Применится на VPS через deploy.sh.

    5. Убедиться что проект компилируется: `npx tsc --noEmit` должна пройти без ошибок после `prisma generate`.
  </action>
  <verify>
    <automated>
      grep -c "ORDERS-01" .planning/REQUIREMENTS.md | grep -v "^0$" &&
      grep -c "model WbCardWarehouseOrders" prisma/schema.prisma | grep -v "^0$" &&
      test -f prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql &&
      grep -c "warehouseOrders" prisma/schema.prisma | grep -v "^0$" &&
      npx prisma validate &&
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ORDERS-01" .planning/REQUIREMENTS.md` ≥ 2 (в описании + в Traceability)
    - `grep -c "ORDERS-02" .planning/REQUIREMENTS.md` ≥ 2
    - `grep -c "ORDERS-03" .planning/REQUIREMENTS.md` ≥ 2
    - `grep "model WbCardWarehouseOrders" prisma/schema.prisma` → найдено ровно 1 совпадение
    - `grep "warehouseOrders" prisma/schema.prisma` → найдено ≥ 2 совпадения (модель WbCard + сама таблица)
    - `grep "orders WbCardWarehouseOrders" prisma/schema.prisma` → найдено 1 совпадение (в WbWarehouse)
    - файл `prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql` существует
    - `grep "ON DELETE CASCADE" prisma/migrations/20260422_phase15_orders_per_warehouse/migration.sql` → найдено ≥ 1 совпадение (wbCardId FK)
    - `npx prisma validate` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Требования ORDERS-01/02/03 задокументированы, Prisma schema содержит модель WbCardWarehouseOrders с корректными relations и unique/cascade, SQL миграция создана в правильном формате (ready to apply на VPS), TypeScript компилируется.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/wb-api.ts — fetchOrdersPerWarehouse + vitest тесты</name>
  <read_first>
    - lib/wb-api.ts (полностью — понять существующий паттерн fetchAvgSalesSpeed7d строки 729-909)
    - tests/wb-stocks-per-warehouse.test.ts (паттерн mock fetch + groupBy тесты)
    - vitest.config.ts (убедиться в alias @ → корень)
  </read_first>
  <files>
    - lib/wb-api.ts
    - tests/wb-orders-per-warehouse.test.ts
  </files>
  <behavior>
    - Test 1 — happy path: fetchOrdersPerWarehouse([123, 456]) с mock response из 5 orders (3 для nmId=123 на 2 складах, 2 для nmId=456 на 1 складе, без isCancel) → возвращает Map из 2 nmId; nmId=123 → avg=3/periodDays, perWarehouse Map с 2 ключами; nmId=456 → avg=2/periodDays, perWarehouse с 1 ключом
    - Test 2 — фильтрация isCancel: 3 orders из которых 1 с isCancel=true → avg считает только 2, perWarehouse для отменённого склада содержит 0 или отсутствует
    - Test 3 — yesterday кейс: order с date="YYYY-MM-DD..." предыдущего MSK-дня попадает в yesterday, текущего — нет
    - Test 4 — пустой массив nmIds: fetchOrdersPerWarehouse([]) → пустой Map БЕЗ HTTP запроса (проверка через mock fetch.mock.calls.length === 0)
    - Test 5 — API 429 с успешным retry через 60 сек (mock setTimeout): первый вызов возвращает 429, второй — 200 с корректным JSON → функция возвращает данные, НЕ throw
    - Test 6 — неизвестный nmId: order с nmId которого нет в передаваемом списке → отсутствует в результирующем Map (фильтрация на клиенте)
    - Test 7 — periodDays параметр: fetchOrdersPerWarehouse([123], 14) → avg = count/14 (не count/7); dateFrom сдвигается на 14 дней назад
  </behavior>
  <action>
    1. В lib/wb-api.ts (после строки 909 — конец существующей `fetchAvgSalesSpeed7d`) добавить новый интерфейс и функцию:

    ```typescript
    /** Phase 15 (ORDERS-02): per-warehouse заказы за периодом.
     *
     *  Один запрос к Statistics Orders API возвращает ВСЕ заказы продавца за
     *  `periodDays` (default 7). Функция считает одновременно:
     *  - avg: заказы минус отмены / periodDays (backward compat с fetchAvgSalesSpeed7d)
     *  - yesterday: заказы за вчерашний день (Moscow TZ, YYYY-MM-DD префикс)
     *  - perWarehouse: Map<warehouseName, количество заказов минус отмены> для ORDERS-02
     *
     *  `isCancel: true` исключаются из ВСЕХ трёх счётчиков — "заказы минус отмены".
     *
     *  Rate limit WB Statistics API ~1 req/min. Один вызов = все данные.
     *  При 429 — sleep(60s) + один рекурсивный retry (паттерн fetchAvgSalesSpeed7d).
     *
     *  @param nmIds — фильтр по nmId (API возвращает все, фильтрация на клиенте).
     *  @param periodDays — окно в днях (default 7).
     *  @returns Map<nmId, OrdersWarehouseStats>. nmId без заказов отсутствует.
     */
    export interface OrdersWarehouseStats {
      /** Заказы минус отмены за periodDays / periodDays (шт/день). */
      avg: number
      /** Заказы минус отмены за вчерашний день (Moscow TZ), шт. */
      yesterday: number
      /** Заказы минус отмены per-warehouse по имени склада (warehouseName → count). */
      perWarehouse: Map<string, number>
      /** Окно в днях (для downstream расчётов). */
      periodDays: number
    }

    export async function fetchOrdersPerWarehouse(
      nmIds: number[],
      periodDays: number = 7,
    ): Promise<Map<number, OrdersWarehouseStats>> {
      const result = new Map<number, OrdersWarehouseStats>()
      if (nmIds.length === 0) return result

      const token = getToken()

      const dateFrom = new Date()
      dateFrom.setDate(dateFrom.getDate() - periodDays)
      const dateFromIso = dateFrom.toISOString()

      const url =
        `https://statistics-api.wildberries.ru/api/v1/supplier/orders` +
        `?dateFrom=${encodeURIComponent(dateFromIso)}&flag=0`

      const res = await fetch(url, { headers: { Authorization: token } })

      if (res.status === 429) {
        await sleep(60_000)
        return fetchOrdersPerWarehouse(nmIds, periodDays)
      }

      if (!res.ok) {
        console.error(`WB Orders API (fetchOrdersPerWarehouse) ${res.status}`)
        return result
      }

      const orders = (await res.json()) as Array<{
        nmId?: number
        nm_id?: number
        warehouseName?: string
        isCancel?: boolean
        date?: string
      }>
      if (!Array.isArray(orders)) return result

      // Окно "вчера" в Moscow TZ — идентично fetchAvgSalesSpeed7d
      const mskNow = new Date(Date.now() + 3 * 3600_000)
      const yy = mskNow.getUTCFullYear()
      const mm = String(mskNow.getUTCMonth() + 1).padStart(2, "0")
      const dd = String(mskNow.getUTCDate() - 1).padStart(2, "0")
      const yesterdayPrefix = `${yy}-${mm}-${dd}`

      const requested = new Set(nmIds)
      const totals = new Map<number, number>()
      const yesterdayCounts = new Map<number, number>()
      const perWarehouseMap = new Map<number, Map<string, number>>()

      for (const o of orders) {
        if (o.isCancel) continue
        const nm = o.nmId ?? o.nm_id
        if (nm == null || !requested.has(nm)) continue
        const wh = (o.warehouseName ?? "").trim()

        totals.set(nm, (totals.get(nm) ?? 0) + 1)
        if (o.date && o.date.startsWith(yesterdayPrefix)) {
          yesterdayCounts.set(nm, (yesterdayCounts.get(nm) ?? 0) + 1)
        }
        if (wh) {
          let perWh = perWarehouseMap.get(nm)
          if (!perWh) {
            perWh = new Map<string, number>()
            perWarehouseMap.set(nm, perWh)
          }
          perWh.set(wh, (perWh.get(wh) ?? 0) + 1)
        }
      }

      for (const nmId of requested) {
        const t = totals.get(nmId) ?? 0
        const y = yesterdayCounts.get(nmId) ?? 0
        const perWh = perWarehouseMap.get(nmId) ?? new Map<string, number>()
        if (t === 0 && y === 0 && perWh.size === 0) continue
        result.set(nmId, {
          avg: t / periodDays,
          yesterday: y,
          perWarehouse: perWh,
          periodDays,
        })
      }

      return result
    }
    ```

    2. НЕ удалять `fetchAvgSalesSpeed7d` — остаётся как есть (backward compat). В jsdoc добавить заметку:
    ```typescript
    // Phase 15 note: новый код должен использовать fetchOrdersPerWarehouse — она
    // возвращает per-warehouse breakdown вдобавок к avg/yesterday одним HTTP-запросом.
    ```

    3. Создать tests/wb-orders-per-warehouse.test.ts:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"
    import { fetchOrdersPerWarehouse } from "@/lib/wb-api"

    // @ts-expect-error - mocking global fetch
    global.fetch = vi.fn()

    describe("fetchOrdersPerWarehouse", () => {
      beforeEach(() => {
        vi.clearAllMocks()
        process.env.WB_API_TOKEN = "test-token"
      })

      it("пустой массив nmIds → пустой Map без HTTP", async () => {
        const result = await fetchOrdersPerWarehouse([])
        expect(result.size).toBe(0)
        expect(fetch).not.toHaveBeenCalled()
      })

      it("happy path — агрегация per warehouseName", async () => {
        const orders = [
          { nmId: 100, warehouseName: "Коледино", isCancel: false, date: "2025-04-15T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", isCancel: false, date: "2025-04-16T10:00:00" },
          { nmId: 100, warehouseName: "Казань", isCancel: false, date: "2025-04-17T10:00:00" },
          { nmId: 200, warehouseName: "Коледино", isCancel: false, date: "2025-04-18T10:00:00" },
        ]
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => orders,
        })

        const result = await fetchOrdersPerWarehouse([100, 200], 7)

        expect(result.size).toBe(2)
        const s100 = result.get(100)!
        expect(s100.avg).toBeCloseTo(3 / 7)
        expect(s100.perWarehouse.get("Коледино")).toBe(2)
        expect(s100.perWarehouse.get("Казань")).toBe(1)
        expect(s100.periodDays).toBe(7)

        const s200 = result.get(200)!
        expect(s200.avg).toBeCloseTo(1 / 7)
        expect(s200.perWarehouse.get("Коледино")).toBe(1)
      })

      it("isCancel=true исключаются из всех счётчиков", async () => {
        const orders = [
          { nmId: 100, warehouseName: "Коледино", isCancel: false, date: "2025-04-15T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", isCancel: true, date: "2025-04-16T10:00:00" },
          { nmId: 100, warehouseName: "Казань", isCancel: true, date: "2025-04-17T10:00:00" },
        ]
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => orders,
        })

        const result = await fetchOrdersPerWarehouse([100], 7)
        const s = result.get(100)!
        expect(s.avg).toBeCloseTo(1 / 7)
        expect(s.perWarehouse.get("Коледино")).toBe(1)
        expect(s.perWarehouse.has("Казань")).toBe(false)
      })

      it("нерелевантные nmId отфильтрованы", async () => {
        const orders = [
          { nmId: 100, warehouseName: "Коледино", isCancel: false, date: "2025-04-15T10:00:00" },
          { nmId: 999, warehouseName: "Коледино", isCancel: false, date: "2025-04-15T10:00:00" },
        ]
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => orders,
        })

        const result = await fetchOrdersPerWarehouse([100], 7)
        expect(result.size).toBe(1)
        expect(result.has(100)).toBe(true)
        expect(result.has(999)).toBe(false)
      })

      it("периодDays=14 → avg делится на 14", async () => {
        const orders = Array.from({ length: 14 }, (_, i) => ({
          nmId: 100,
          warehouseName: "Коледино",
          isCancel: false,
          date: `2025-04-${String(i + 1).padStart(2, "0")}T10:00:00`,
        }))
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => orders,
        })

        const result = await fetchOrdersPerWarehouse([100], 14)
        const s = result.get(100)!
        expect(s.avg).toBeCloseTo(14 / 14)
        expect(s.periodDays).toBe(14)

        // Проверим что URL содержит сдвинутый на 14 дней dateFrom
        const url = (fetch as ReturnType<typeof vi.fn>).mock.calls[0][0] as string
        expect(url).toContain("statistics-api.wildberries.ru/api/v1/supplier/orders")
        expect(url).toContain("dateFrom=")
      })

      it("HTTP !ok (non-429) → пустой Map, не throw", async () => {
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: false,
          status: 500,
          json: async () => ({}),
        })

        const result = await fetchOrdersPerWarehouse([100], 7)
        expect(result.size).toBe(0)
      })

      it("пустой warehouseName не попадает в perWarehouse", async () => {
        const orders = [
          { nmId: 100, warehouseName: "", isCancel: false, date: "2025-04-15T10:00:00" },
          { nmId: 100, warehouseName: "Коледино", isCancel: false, date: "2025-04-16T10:00:00" },
        ]
        ;(fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
          ok: true,
          status: 200,
          json: async () => orders,
        })

        const result = await fetchOrdersPerWarehouse([100], 7)
        const s = result.get(100)!
        expect(s.avg).toBeCloseTo(2 / 7) // avg считает оба
        expect(s.perWarehouse.size).toBe(1) // но в perWarehouse только непустой
        expect(s.perWarehouse.get("Коледино")).toBe(1)
      })
    })
    ```

    4. Запустить `npm test -- wb-orders-per-warehouse` → все 7 тестов GREEN.

    5. Запустить `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>
      grep -c "fetchOrdersPerWarehouse" lib/wb-api.ts | grep -v "^0$" &&
      grep -c "OrdersWarehouseStats" lib/wb-api.ts | grep -v "^0$" &&
      test -f tests/wb-orders-per-warehouse.test.ts &&
      npm test -- wb-orders-per-warehouse --run 2>&1 | grep -E "Tests.*7 passed|passed.*7" &&
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function fetchOrdersPerWarehouse" lib/wb-api.ts` = 1
    - `grep -c "export interface OrdersWarehouseStats" lib/wb-api.ts` = 1
    - `grep "perWarehouse" lib/wb-api.ts` → найдено ≥ 3 совпадения (интерфейс + заполнение Map + return)
    - `grep "isCancel" lib/wb-api.ts` → найдено ≥ 1 совпадение (в новой функции — фильтрация)
    - `grep "statistics-api.wildberries.ru/api/v1/supplier/orders" lib/wb-api.ts` → найдено ≥ 2 совпадения (старая + новая функция)
    - файл tests/wb-orders-per-warehouse.test.ts существует
    - `npm test -- wb-orders-per-warehouse --run` exit 0 с 7 passed
    - `npx tsc --noEmit` exit 0
    - Существующая fetchAvgSalesSpeed7d НЕ удалена: `grep -c "export async function fetchAvgSalesSpeed7d" lib/wb-api.ts` = 1
  </acceptance_criteria>
  <done>fetchOrdersPerWarehouse реализована с фильтрацией isCancel и per-warehouse агрегацией, 7 vitest GREEN, TypeScript компилируется, fetchAvgSalesSpeed7d backward compatibility сохранена.</done>
</task>

</tasks>

<verification>
- Требования ORDERS-01/02/03 задокументированы в REQUIREMENTS.md (в двух местах — описание + Traceability)
- Prisma schema корректна: `npx prisma validate` exit 0
- SQL миграция создана и содержит Cascade FK для wbCardId
- Новая функция fetchOrdersPerWarehouse + интерфейс экспортированы из lib/wb-api.ts
- 7 vitest тестов GREEN (`npm test -- wb-orders-per-warehouse --run`)
- `npx tsc --noEmit` exit 0
- Старая fetchAvgSalesSpeed7d НЕ сломана (существует и не модифицирована деструктивно)
</verification>

<success_criteria>
- 3 новых требования в REQUIREMENTS.md (ORDERS-01, ORDERS-02, ORDERS-03)
- 1 новая Prisma модель WbCardWarehouseOrders + 2 обратных relations
- 1 SQL миграция (pending для VPS deploy)
- 1 новая TS функция + интерфейс в lib/wb-api.ts
- 7+ passing vitest тестов
- TypeScript компилируется без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/15-per-cluster-orders/15-01-SUMMARY.md` с:
- Какие артефакты созданы (migration SQL, новая функция, тесты)
- Interfaces published для 15-02 и 15-03 (WbCardWarehouseOrders model, fetchOrdersPerWarehouse signature)
- Known stubs: миграция не применена локально, применится через deploy.sh
</output>
