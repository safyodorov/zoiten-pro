---
phase: 15-per-cluster-orders
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - lib/stock-wb-data.ts
  - components/stock/StockWbTable.tsx
autonomous: true
requirements: [ORDERS-02, ORDERS-03]
must_haves:
  truths:
    - "getStockWbData() возвращает per-nmId данные с per-warehouse orders разбивкой per-кластер"
    - "StockWbTable.tsx колонка З кластера (collapsed) = SUM(orders кластера) / periodDays, используется вместо card.avgSalesSpeed7d"
    - "При expand кластера per-warehouse колонка З = ordersCount / periodDays"
    - "Метрики Об/Д per-кластер пересчитываются через calculateStockMetrics с НОВЫМ ordersPerDay (не card.avgSalesSpeed7d)"
    - "Если для кластера/склада нет данных о заказах, но есть card.avgSalesSpeed7d — показываем fallback value в колонке З Сводной строке или МП колонке (card-level), per-cluster/per-warehouse показывают 0 или '—'"
    - "Визуально: существующая МП колонка З на card-row продолжает показывать card.avgSalesSpeed7d (fallback для nmId без per-warehouse данных)"
  artifacts:
    - path: "lib/stock-wb-data.ts"
      provides: "WbStockRow расширен orders полями, ClusterAggregate содержит ordersPerDay"
      contains: "ordersCount"
    - path: "components/stock/StockWbTable.tsx"
      provides: "Колонки З кластера/склада используют per-warehouse ordersPerDay"
      contains: "ordersPerDay"
  key_links:
    - from: "lib/stock-wb-data.ts"
      to: "prisma.wbCardWarehouseOrders (через include у wbCard.findMany)"
      via: "include: { warehouseOrders: { include: { warehouse: true } } }"
      pattern: "warehouseOrders"
    - from: "components/stock/StockWbTable.tsx"
      to: "calculateStockMetrics с кластерным ordersPerDay"
      via: "clusterData.ordersPerDay"
      pattern: "ordersPerDay:\\s*(clusterData|cluster|slot)"
---

<objective>
Обновить data-помощник `lib/stock-wb-data.ts` чтобы он подтягивал `WbCardWarehouseOrders` через Prisma include и считал per-кластерную скорость заказов (сумма ordersCount кластера / periodDays) + per-warehouse скорость для expanded view. Обновить `components/stock/StockWbTable.tsx` так, чтобы колонка **З** каждого кластера (collapsed) использовала кластерную скорость, per-warehouse колонка (expanded) — per-warehouse скорость, а метрики Об/Д пересчитывались через тот же `calculateStockMetrics` с новым `ordersPerDay`. `card.avgSalesSpeed7d` остаётся fallback для Сводной колонки МП и для случаев когда per-warehouse данных нет.

Purpose: после Plan 15-02 в БД есть per-warehouse orders, но UI продолжает использовать единое card.avgSalesSpeed7d для всех кластеров — это не даёт корректную картину (реально в одном кластере может быть 80% заказов, в другом 0). Отдельный план т.к. файлы не пересекаются с 15-02 (api/wb-sync vs lib/stock-wb-data).
Output: UI /stock/wb показывает реальные per-cluster скорости заказов, Об/Д пересчитываются корректно.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/stock-wb-data.ts
@components/stock/StockWbTable.tsx
@lib/stock-math.ts
@lib/wb-clusters.ts
@.planning/phases/15-per-cluster-orders/15-01-SUMMARY.md

<interfaces>
<!-- От 15-01: Prisma модель WbCardWarehouseOrders с relation warehouse -->

Prisma include для wbCard.findMany (как в существующем коде stock-wb-data.ts:79-86):
```typescript
const wbCards = await prisma.wbCard.findMany({
  where: { nmId: { in: wbNmIds } },
  include: {
    warehouses: { include: { warehouse: true } },     // существующее — stocks
    warehouseOrders: { include: { warehouse: true } }, // НОВОЕ — orders (Plan 15-01)
  },
})
```

Данные per card.warehouseOrders:
```typescript
Array<{
  id: string
  wbCardId: string
  warehouseId: number
  ordersCount: number
  periodDays: number
  updatedAt: Date
  warehouse: { id, name, cluster, shortCluster, needsClusterReview, ... }
}>
```

<!-- Существующие интерфейсы stock-wb-data.ts (расширяются): -->
```typescript
export interface WarehouseSlot {
  warehouseId: number
  warehouseName: string
  needsClusterReview: boolean
  quantity: number
  // Phase 15 — добавить:
  ordersCount: number              // 0 если нет данных
  ordersPerDay: number | null      // ordersCount / periodDays, null если нет
}

export interface ClusterAggregate {
  shortCluster: string
  totalStock: number | null
  warehouses: WarehouseSlot[]
  // Phase 15 — добавить:
  totalOrdersCount: number | null    // SUM по складам кластера, null если нет данных
  ordersPerDay: number | null        // totalOrdersCount / periodDays (единый periodDays всех складов)
}

export interface WbStockRow {
  // ...существующие
  // Phase 15 — добавить:
  periodDays: number | null          // общий periodDays для карточки (из первого orderRow)
}
```

<!-- Существующий StockWbTable.tsx — точки замены: -->

Collapsed cluster columns (строки 319-330) — сейчас:
```typescript
const clusterMetrics = calculateStockMetrics({
  stock: clusterData?.totalStock ?? null,
  ordersPerDay: card.avgSalesSpeed7d,        // ← ЗАМЕНЯЕТСЯ на clusterData.ordersPerDay
  turnoverNormDays,
})
const clusterThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)  // ← тоже
return [
  <StockCell ... value={clusterData?.totalStock ?? null} />,
  <StockCell ... value={card.avgSalesSpeed7d} />,         // ← ЗАМЕНЯЕТСЯ на clusterData.ordersPerDay
  <StockCell ... value={clusterMetrics.turnoverDays} />,
  <DeficitCell ... deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
]
```

Expanded per-warehouse columns (строки 298-315) — сейчас показывают только количество stock. Phase 15 добавляет отображение З per-warehouse (slot.ordersPerDay).

<!-- КРИТИЧНО про MP колонка: -->
МП колонка на card-row (строки 288-291) — это card-level metrics, должна ОСТАТЬСЯ с card.avgSalesSpeed7d (fallback если per-warehouse пустое). НЕ ТРОГАТЬ.

<!-- Fallback стратегия: -->
- Если card.warehouseOrders пустой массив (нет данных за 7 дней) → cluster.ordersPerDay = null для всех кластеров → Об/Д = null через calculateStockMetrics guard
- card.avgSalesSpeed7d остаётся в интерфейсе WbStockRow, используется в МП колонке
- В per-cluster/per-warehouse колонке З показываем null → "—" (не показываем card.avgSalesSpeed7d как fallback per-cluster, потому что это вводит в заблуждение — реально в кластере 0 заказов)

<!-- CLUSTER_ORDER (уже импортируется): -->
["ЦФО", "ЮГ", "Урал", "ПФО", "СЗО", "СФО", "Прочие"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: lib/stock-wb-data.ts — агрегация per-warehouse orders per-cluster</name>
  <read_first>
    - lib/stock-wb-data.ts (полностью — все 180 строк)
    - lib/wb-clusters.ts (CLUSTER_ORDER)
    - .planning/phases/15-per-cluster-orders/15-01-SUMMARY.md (модель WbCardWarehouseOrders)
  </read_first>
  <files>
    - lib/stock-wb-data.ts
  </files>
  <behavior>
    - Interface WarehouseSlot расширен полями: ordersCount (number, 0 default), ordersPerDay (number | null)
    - Interface ClusterAggregate расширен полями: totalOrdersCount (number | null), ordersPerDay (number | null)
    - Interface WbStockRow получает поле periodDays (number | null)
    - Prisma findMany wbCard расширен include.warehouseOrders
    - Функция строит для каждого WbCard две параллельные агрегации по кластерам: stocks (как было) + orders (новое). Per-warehouse мердж: в WarehouseSlot попадают И quantity (из stocks), И ordersCount (из orders). Если warehouse есть только в одной из таблиц — вторая метрика = 0 (или null для ordersPerDay если вообще нет WbCardWarehouseOrders записи).
    - clusterData.ordersPerDay = (sum(ordersCount всех складов кластера) / periodDays) если есть хотя бы одна запись orders в кластере, иначе null
    - Per-warehouse ordersPerDay = ordersCount / periodDays если запись orders существует, иначе null
    - Существующая структура stocks-агрегации не меняется
  </behavior>
  <action>
    1. Открыть lib/stock-wb-data.ts. Расширить интерфейсы:

    ```typescript
    export interface WarehouseSlot {
      warehouseId: number
      warehouseName: string
      needsClusterReview: boolean
      quantity: number
      // Phase 15 (ORDERS-03):
      ordersCount: number           // 0 если нет записи в WbCardWarehouseOrders
      ordersPerDay: number | null   // ordersCount / periodDays, null если нет записи
    }

    export interface ClusterAggregate {
      shortCluster: string
      totalStock: number | null
      warehouses: WarehouseSlot[]
      // Phase 15 (ORDERS-03):
      totalOrdersCount: number | null   // SUM ordersCount всех складов кластера; null если ни одного order-record
      ordersPerDay: number | null       // totalOrdersCount / periodDays
    }

    export interface WbStockRow {
      wbCardId: string
      nmId: number
      wbCardName: string | null
      avgSalesSpeed7d: number | null
      totalStock: number | null
      clusters: Record<ClusterShortName, ClusterAggregate>
      // Phase 15 (ORDERS-03):
      periodDays: number | null         // periodDays из первой WbCardWarehouseOrders записи (обычно 7)
    }
    ```

    2. Расширить Prisma include (в findMany wbCards, строка ~79):

    ```typescript
    const wbCards = wbNmIds.length > 0
      ? await prisma.wbCard.findMany({
          where: { nmId: { in: wbNmIds } },
          include: {
            warehouses: {
              include: { warehouse: true },
            },
            warehouseOrders: {
              include: { warehouse: true },
            },
          },
        })
      : []
    ```

    3. В цикле построения WbStockRow (строки ~124-165 — `const wbRows: WbStockRow[] = wbArticles.map((a) => { ... })`) изменить инициализацию clusters и agregate processing:

    Заменить блок инициализации clusters:
    ```typescript
    const clusters = {} as Record<ClusterShortName, ClusterAggregate>
    for (const shortCluster of CLUSTER_ORDER) {
      clusters[shortCluster] = {
        shortCluster,
        totalStock: null,
        totalOrdersCount: null,
        ordersPerDay: null,
        warehouses: [],
      }
    }
    ```

    Перед существующим циклом по card.warehouses добавить сбор map orders по warehouseId:
    ```typescript
    // Phase 15: Map<warehouseId, {ordersCount, periodDays}> для быстрого lookup
    const ordersByWarehouseId = new Map<number, { ordersCount: number; periodDays: number }>()
    let cardPeriodDays: number | null = null
    if (card) {
      for (const wo of card.warehouseOrders) {
        ordersByWarehouseId.set(wo.warehouseId, {
          ordersCount: wo.ordersCount,
          periodDays: wo.periodDays,
        })
        if (cardPeriodDays === null) cardPeriodDays = wo.periodDays
      }
    }
    ```

    4. Переработать цикл обработки складов для card — объединить stocks и orders в единый набор уникальных warehouseId:

    ```typescript
    let totalStock: number | null = null
    if (card) {
      // Собираем все уникальные warehouseId из stocks + orders
      const allWarehouseIds = new Set<number>()
      for (const ws of card.warehouses) allWarehouseIds.add(ws.warehouseId)
      for (const wo of card.warehouseOrders) allWarehouseIds.add(wo.warehouseId)

      // Lookup stocks и warehouse meta по warehouseId
      const stockByWarehouseId = new Map<number, typeof card.warehouses[number]>()
      for (const ws of card.warehouses) stockByWarehouseId.set(ws.warehouseId, ws)

      const warehouseMetaById = new Map<number, { name: string; shortCluster: string | null; needsClusterReview: boolean }>()
      for (const ws of card.warehouses) {
        warehouseMetaById.set(ws.warehouseId, {
          name: ws.warehouse?.name ?? `Склад ${ws.warehouseId}`,
          shortCluster: ws.warehouse?.shortCluster ?? null,
          needsClusterReview: ws.warehouse?.needsClusterReview ?? false,
        })
      }
      for (const wo of card.warehouseOrders) {
        if (!warehouseMetaById.has(wo.warehouseId)) {
          warehouseMetaById.set(wo.warehouseId, {
            name: wo.warehouse?.name ?? `Склад ${wo.warehouseId}`,
            shortCluster: wo.warehouse?.shortCluster ?? null,
            needsClusterReview: wo.warehouse?.needsClusterReview ?? false,
          })
        }
      }

      for (const warehouseId of allWarehouseIds) {
        const meta = warehouseMetaById.get(warehouseId)!
        const rawCluster = meta.shortCluster
        const shortCluster: ClusterShortName = (
          rawCluster && (CLUSTER_ORDER as readonly string[]).includes(rawCluster)
            ? rawCluster
            : "Прочие"
        ) as ClusterShortName

        const aggregate = clusters[shortCluster]!
        const stockEntry = stockByWarehouseId.get(warehouseId)
        const orderEntry = ordersByWarehouseId.get(warehouseId)

        const quantity = stockEntry?.quantity ?? 0
        const ordersCount = orderEntry?.ordersCount ?? 0
        const periodDays = orderEntry?.periodDays ?? null
        const ordersPerDay = orderEntry ? ordersCount / orderEntry.periodDays : null

        aggregate.warehouses.push({
          warehouseId,
          warehouseName: meta.name,
          needsClusterReview: meta.needsClusterReview,
          quantity,
          ordersCount,
          ordersPerDay,
        })

        // Stocks aggregation (только если в stocks была запись)
        if (stockEntry) {
          aggregate.totalStock = (aggregate.totalStock ?? 0) + quantity
          if (totalStock === null) totalStock = 0
          totalStock += quantity
        }

        // Orders aggregation
        if (orderEntry) {
          aggregate.totalOrdersCount = (aggregate.totalOrdersCount ?? 0) + ordersCount
          // ordersPerDay пересчитаем ниже, когда знаем total
        }
        void periodDays // unused но сохраняет символ от lint
      }

      // Второй проход: пересчёт ordersPerDay per-cluster
      for (const shortCluster of CLUSTER_ORDER) {
        const agg = clusters[shortCluster]!
        if (agg.totalOrdersCount !== null && cardPeriodDays !== null && cardPeriodDays > 0) {
          agg.ordersPerDay = agg.totalOrdersCount / cardPeriodDays
        }
      }
    }
    ```

    5. Расширить return object WbStockRow (строки ~157-164):

    ```typescript
    return {
      wbCardId: card?.id ?? `missing-${a.article}`,
      nmId: isNaN(nmId) ? 0 : nmId,
      wbCardName: card?.name ?? null,
      avgSalesSpeed7d: card?.avgSalesSpeed7d ?? null,
      totalStock,
      clusters,
      periodDays: cardPeriodDays,
    }
    ```

    6. Также обновить построение clusterWarehouses (строки 94-107) — включать warehouseId из warehouseOrders, чтобы колонки per-warehouse отображались даже для складов без stocks:

    ```typescript
    for (const card of wbCards) {
      // Из stocks
      for (const ws of card.warehouses) {
        const shortCluster = (ws.warehouse?.shortCluster as ClusterShortName | undefined) ?? "Прочие"
        const targetKey = (CLUSTER_ORDER as readonly string[]).includes(shortCluster) ? shortCluster : "Прочие"
        const map = clusterWarehousesMap[targetKey]!
        if (!map.has(ws.warehouseId)) {
          map.set(ws.warehouseId, {
            warehouseId: ws.warehouseId,
            warehouseName: ws.warehouse?.name ?? `Склад ${ws.warehouseId}`,
            needsClusterReview: ws.warehouse?.needsClusterReview ?? false,
          })
        }
      }
      // Phase 15: также из orders
      for (const wo of card.warehouseOrders) {
        const shortCluster = (wo.warehouse?.shortCluster as ClusterShortName | undefined) ?? "Прочие"
        const targetKey = (CLUSTER_ORDER as readonly string[]).includes(shortCluster) ? shortCluster : "Прочие"
        const map = clusterWarehousesMap[targetKey]!
        if (!map.has(wo.warehouseId)) {
          map.set(wo.warehouseId, {
            warehouseId: wo.warehouseId,
            warehouseName: wo.warehouse?.name ?? `Склад ${wo.warehouseId}`,
            needsClusterReview: wo.warehouse?.needsClusterReview ?? false,
          })
        }
      }
    }
    ```

    7. `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>
      grep -c "warehouseOrders" lib/stock-wb-data.ts | grep -v "^0$" &&
      grep -c "ordersCount" lib/stock-wb-data.ts | grep -v "^0$" &&
      grep -c "ordersPerDay" lib/stock-wb-data.ts | grep -v "^0$" &&
      grep -c "totalOrdersCount" lib/stock-wb-data.ts | grep -v "^0$" &&
      grep -c "periodDays" lib/stock-wb-data.ts | grep -v "^0$" &&
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "warehouseOrders:" lib/stock-wb-data.ts` (в Prisma include) → найдено 1 совпадение
    - `grep -c "ordersCount" lib/stock-wb-data.ts` ≥ 4 (interface WarehouseSlot + ClusterAggregate + цикл + возврат)
    - `grep -c "ordersPerDay" lib/stock-wb-data.ts` ≥ 5
    - `grep -c "totalOrdersCount" lib/stock-wb-data.ts` ≥ 3
    - `grep -c "cardPeriodDays" lib/stock-wb-data.ts` ≥ 2
    - `npx tsc --noEmit` exit 0
    - Существующие поля не удалены: `grep "totalStock: number | null" lib/stock-wb-data.ts` → найдено
  </acceptance_criteria>
  <done>getStockWbData возвращает per-кластер ordersPerDay вместе с per-warehouse breakdown, TypeScript компилируется, fallback когда WbCardWarehouseOrders пусто — все ordersPerDay = null.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: components/stock/StockWbTable.tsx — использовать per-cluster ordersPerDay для З/Об/Д</name>
  <read_first>
    - components/stock/StockWbTable.tsx (полностью)
    - lib/stock-math.ts (calculateStockMetrics + deficitThreshold подписи)
    - .planning/phases/15-per-cluster-orders/15-01-SUMMARY.md (проверить формат ClusterAggregate + WarehouseSlot)
  </read_first>
  <files>
    - components/stock/StockWbTable.tsx
  </files>
  <behavior>
    - В collapsed cluster view (4 sub-columns О/З/Об/Д): З показывает `clusterData.ordersPerDay` (не `card.avgSalesSpeed7d`). Об и Д считаются через `calculateStockMetrics` с `ordersPerDay: clusterData.ordersPerDay`. `deficitThreshold` тоже с `clusterData.ordersPerDay`.
    - В expanded cluster view (per-warehouse columns): каждая ячейка показывает `slot.ordersPerDay` (был quantity — теперь двухстрочно или через tooltip). Правильнее: показываем З per-warehouse = `slot.ordersPerDay`. Stock уже был виден в collapsed — expand фокусируется на per-warehouse metrics. НО чтобы не ломать существующий UX (сейчас expanded показывает quantity), ОБА значения нужны. Решение: в expanded view показываем `slot.ordersPerDay` (скорость заказов per-склад), а stock остаётся видимым через collapsed или в tooltip. НО CONTEXT-SPEC пришёл: "при expand — per-warehouse колонка показывает ordersCount / periodDays". Значит в expanded показываем ordersPerDay (а не quantity). Но для visual consistency — в tooltip или в subtitle показываем stock quantity как второстепенную инфу. Для v1 просто показываем ordersPerDay, stock-значение всегда доступно в collapsed view.
    - МП колонка на card-row (totalStock + avgSalesSpeed7d + metrics) — НЕ МЕНЯЕТСЯ (это card-level fallback через avgSalesSpeed7d).
    - Сводная строка (rowSpan, строки 220-271) — НЕ МЕНЯЕТСЯ (все placeholder null).
  </behavior>
  <action>
    1. Открыть components/stock/StockWbTable.tsx. Найти блок expanded (строки ~298-315) — сейчас:

    ```typescript
    if (isExpanded) {
      if (warehouses.length === 0) {
        return [<StockCell key={`${card.wbCardId}-${cluster}-empty`} value={null} />]
      }
      return warehouses.map((w, wIdx) => {
        const slot = clusterData?.warehouses.find((s) => s.warehouseId === w.warehouseId)
        return (
          <TableCell
            key={`${card.wbCardId}-${cluster}-${w.warehouseId}`}
            className={cn(
              "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right",
              wIdx === warehouses.length - 1 && "border-r"
            )}
          >
            {slot ? formatStockValue(slot.quantity) : <span className="text-muted-foreground">—</span>}
          </TableCell>
        )
      })
    }
    ```

    Заменить на (показывает ordersPerDay — per-warehouse З):
    ```typescript
    if (isExpanded) {
      if (warehouses.length === 0) {
        return [<StockCell key={`${card.wbCardId}-${cluster}-empty`} value={null} />]
      }
      return warehouses.map((w, wIdx) => {
        const slot = clusterData?.warehouses.find((s) => s.warehouseId === w.warehouseId)
        const ordersPerDay = slot?.ordersPerDay ?? null
        return (
          <TableCell
            key={`${card.wbCardId}-${cluster}-${w.warehouseId}`}
            className={cn(
              "px-2 py-1 h-8 text-xs leading-tight tabular-nums text-right",
              wIdx === warehouses.length - 1 && "border-r"
            )}
            title={slot ? `Остаток: ${formatStockValue(slot.quantity)} шт${slot.ordersCount ? ` · Заказов/${card.periodDays ?? 7}д: ${slot.ordersCount}` : ""}` : undefined}
          >
            {ordersPerDay !== null ? formatStockValue(ordersPerDay) : <span className="text-muted-foreground">—</span>}
          </TableCell>
        )
      })
    }
    ```

    2. Найти блок collapsed (строки ~319-330) — сейчас:

    ```typescript
    const clusterMetrics = calculateStockMetrics({
      stock: clusterData?.totalStock ?? null,
      ordersPerDay: card.avgSalesSpeed7d,
      turnoverNormDays,
    })
    const clusterThreshold = deficitThreshold(turnoverNormDays, card.avgSalesSpeed7d)
    return [
      <StockCell key={`${card.wbCardId}-${cluster}-o`} value={clusterData?.totalStock ?? null} />,
      <StockCell key={`${card.wbCardId}-${cluster}-z`} value={card.avgSalesSpeed7d} />,
      <StockCell key={`${card.wbCardId}-${cluster}-ob`} value={clusterMetrics.turnoverDays} />,
      <DeficitCell key={`${card.wbCardId}-${cluster}-d`} deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
    ]
    ```

    Заменить на:
    ```typescript
    const clusterOrdersPerDay = clusterData?.ordersPerDay ?? null
    const clusterMetrics = calculateStockMetrics({
      stock: clusterData?.totalStock ?? null,
      ordersPerDay: clusterOrdersPerDay,
      turnoverNormDays,
    })
    const clusterThreshold = deficitThreshold(turnoverNormDays, clusterOrdersPerDay)
    return [
      <StockCell key={`${card.wbCardId}-${cluster}-o`} value={clusterData?.totalStock ?? null} />,
      <StockCell key={`${card.wbCardId}-${cluster}-z`} value={clusterOrdersPerDay} />,
      <StockCell key={`${card.wbCardId}-${cluster}-ob`} value={clusterMetrics.turnoverDays} />,
      <DeficitCell key={`${card.wbCardId}-${cluster}-d`} deficit={clusterMetrics.deficit} threshold={clusterThreshold} />,
    ]
    ```

    3. МП колонка на card-row (строки ~287-291) НЕ менять — она показывает card-level avgSalesSpeed7d как fallback:

    ```typescript
    {/* МП О/З/Об/Д — card-level, avgSalesSpeed7d как fallback */}
    <StockCell value={card.totalStock} />
    <StockCell value={card.avgSalesSpeed7d} />
    <StockCell value={cardMetrics.turnoverDays} />
    <DeficitCell deficit={cardMetrics.deficit} threshold={cardThreshold} />
    ```

    4. `npx tsc --noEmit` → 0 ошибок.

    5. Запустить существующие тесты: `npm test -- --run` → не должно быть регрессий.
  </action>
  <verify>
    <automated>
      grep -c "clusterData?\.ordersPerDay\|clusterData\.ordersPerDay\|clusterOrdersPerDay" components/stock/StockWbTable.tsx | grep -v "^0$" &&
      grep -c "slot?\.ordersPerDay\|slot\.ordersPerDay" components/stock/StockWbTable.tsx | grep -v "^0$" &&
      npx tsc --noEmit &&
      npm test -- --run 2>&1 | grep -v "failed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clusterOrdersPerDay" components/stock/StockWbTable.tsx` ≥ 3 (decl + в calculateStockMetrics + в deficitThreshold + в StockCell)
    - `grep "ordersPerDay: clusterOrdersPerDay\|ordersPerDay: clusterData" components/stock/StockWbTable.tsx` → найдено ≥ 1 совпадение (в calculateStockMetrics)
    - `grep "slot.ordersPerDay\|slot?.ordersPerDay" components/stock/StockWbTable.tsx` → найдено ≥ 1 совпадение (expanded view)
    - `grep -c "card.avgSalesSpeed7d" components/stock/StockWbTable.tsx` = 3 (МП-колонка З value + card-level metrics в calculateStockMetrics + deficitThreshold). Для старых cluster-level вызовов = 0
    - В collapsed блоке нет `card.avgSalesSpeed7d` (используется только clusterOrdersPerDay): для этого ищем `ordersPerDay: card.avgSalesSpeed7d` — должно быть только 1 совпадение (в card-level metrics выше в коде)
    - `npx tsc --noEmit` exit 0
    - `npm test -- --run` exit 0 (без падений существующих тестов)
  </acceptance_criteria>
  <done>Collapsed cluster view показывает per-cluster З и корректные Об/Д; expanded view показывает per-warehouse З (ordersPerDay) с tooltip содержащим stock; МП card-level продолжает работать от avgSalesSpeed7d; TypeScript + тесты проходят.</done>
</task>

</tasks>

<verification>
- Визуальная проверка после deploy: кластеры с реальными заказами показывают ненулевую З, пустые кластеры — «—»
- Об/Д в коллапс-виде пересчитываются корректно от кластерной скорости (не единое card.avgSalesSpeed7d)
- Expand кластера показывает per-warehouse ordersPerDay
- МП колонка на card-row продолжает показывать card.avgSalesSpeed7d (fallback)
- `npx tsc --noEmit` exit 0
- Существующие vitest тесты не падают
</verification>

<success_criteria>
- lib/stock-wb-data.ts расширен: WarehouseSlot.ordersCount/ordersPerDay, ClusterAggregate.totalOrdersCount/ordersPerDay, WbStockRow.periodDays, Prisma include.warehouseOrders
- components/stock/StockWbTable.tsx collapsed view использует clusterData.ordersPerDay вместо card.avgSalesSpeed7d (для З + в calculateStockMetrics + в deficitThreshold)
- Expanded view показывает slot.ordersPerDay per-warehouse
- Card-level МП колонка сохраняет fallback через avgSalesSpeed7d
- TypeScript + существующие тесты GREEN
</success_criteria>

<output>
После завершения создать `.planning/phases/15-per-cluster-orders/15-03-SUMMARY.md` с:
- Описанием новых полей в интерфейсах
- UX изменениями (что видит пользователь)
- Integration notes: UI теперь работает с данными из WbCardWarehouseOrders, наполняемыми через Plan 15-02. UAT после deploy.
- Известные ограничения: per-warehouse expanded view показывает ТОЛЬКО ordersPerDay, stock quantity виден в tooltip (кнопка при hover)
</output>
