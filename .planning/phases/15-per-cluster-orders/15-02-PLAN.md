---
phase: 15-per-cluster-orders
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - app/api/wb-sync/route.ts
  - scripts/wb-sync-stocks.js
autonomous: true
requirements: [ORDERS-02, ORDERS-03]
must_haves:
  truths:
    - "POST /api/wb-sync после загрузки stocks per-warehouse также загружает orders per-warehouse и записывает в WbCardWarehouseOrders"
    - "Clean-replace per wbCardId в транзакции: deleteMany NOT IN incoming + upsert + auto-insert неизвестных складов через stableWarehouseIdFromName"
    - "Вызов fetchOrdersPerWarehouse ЗАМЕНЯЕТ предыдущий вызов fetchAvgSalesSpeed7d в /api/wb-sync (backward compat — поля WbCard.avgSalesSpeed7d и WbCard.ordersYesterday продолжают записываться из того же ответа)"
    - "scripts/wb-sync-stocks.js содержит новую секцию orders: curl → GET /supplier/orders → clean-replace в WbCardWarehouseOrders per wbCardId + auto-insert unknown warehouses"
    - "WB_API_TOKEN один — один вызов fetchOrdersPerWarehouse не дублирует запрос к Orders API (не делаем 2 HTTP подряд)"
  artifacts:
    - path: "app/api/wb-sync/route.ts"
      provides: "Секция orders per-warehouse + обновлённый блок avgSalesSpeed"
      contains: "fetchOrdersPerWarehouse"
    - path: "scripts/wb-sync-stocks.js"
      provides: "Orders section — curl + clean-replace для WbCardWarehouseOrders"
      contains: "WbCardWarehouseOrders"
  key_links:
    - from: "app/api/wb-sync/route.ts"
      to: "lib/wb-api.ts fetchOrdersPerWarehouse"
      via: "import + await call после fetchStocksPerWarehouse"
      pattern: "fetchOrdersPerWarehouse"
    - from: "app/api/wb-sync/route.ts"
      to: "prisma.wbCardWarehouseOrders"
      via: "tx.wbCardWarehouseOrders.upsert + deleteMany NOT IN"
      pattern: "wbCardWarehouseOrders\\.(upsert|deleteMany|findUnique)"
    - from: "scripts/wb-sync-stocks.js"
      to: "Statistics Orders API + prisma.wbCardWarehouseOrders"
      via: "curl + async transaction"
      pattern: "supplier/orders"
---

<objective>
Интеграция fetchOrdersPerWarehouse в POST /api/wb-sync: добавить секцию orders per-warehouse после существующей stocks per-warehouse секции с идентичной логикой clean-replace + auto-insert unknown warehouses (переиспользуем stableWarehouseIdFromName). Заменить вызов fetchAvgSalesSpeed7d на fetchOrdersPerWarehouse — один запрос к Orders API теперь покрывает обе задачи (card-level avg/yesterday + per-warehouse breakdown). Расширить scripts/wb-sync-stocks.js новой секцией orders — полный паттерн повторяет stocks section скрипта.

Purpose: WB Statistics Orders API имеет rate limit ~1 req/min — делать 2 отдельных запроса подряд нельзя. Один вызов fetchOrdersPerWarehouse покрывает все нужды, экономит rate limit budget.
Output: расширенный route handler + one-shot script способный оркестровать sync orders без UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/api/wb-sync/route.ts
@scripts/wb-sync-stocks.js
@lib/wb-api.ts
@.planning/phases/14-stock/14-03-SUMMARY.md
@.planning/phases/15-per-cluster-orders/15-01-SUMMARY.md

<interfaces>
<!-- От 15-01: новая функция и модель -->

Из lib/wb-api.ts (Plan 15-01):
```typescript
export interface OrdersWarehouseStats {
  avg: number                        // = ordersCount / periodDays
  yesterday: number
  perWarehouse: Map<string, number>  // warehouseName → ordersCount
  periodDays: number
}
export async function fetchOrdersPerWarehouse(
  nmIds: number[],
  periodDays?: number,  // default 7
): Promise<Map<number, OrdersWarehouseStats>>
```

Из prisma/schema.prisma (Plan 15-01):
```prisma
model WbCardWarehouseOrders {
  id          String   @default(cuid())
  wbCardId    String   // FK → WbCard, onDelete: Cascade
  warehouseId Int      // FK → WbWarehouse
  ordersCount Int      @default(0)
  periodDays  Int      @default(7)
  updatedAt   DateTime @updatedAt

  @@unique([wbCardId, warehouseId])
}
```

Prisma клиент имя: `prisma.wbCardWarehouseOrders` (camelCase из PascalCase model name).

<!-- Существующий паттерн для copy — ИЗ app/api/wb-sync/route.ts (строки 189-282) -->

Pattern stocks-per-warehouse clean-replace (пример для копии):
```typescript
await prisma.$transaction(
  async (tx) => {
    for (const [nmId, warehouseItems] of stocksPerWarehouse.entries()) {
      const card = await tx.wbCard.findUnique({ where: { nmId }, select: { id: true } })
      if (!card) continue

      const incomingWarehouseIds: number[] = []
      for (const item of warehouseItems) {
        const existingByName = await tx.wbWarehouse.findFirst({ where: { name: item.warehouseName }, select: { id: true } })
        let warehouseId: number
        if (existingByName) warehouseId = existingByName.id
        else {
          warehouseId = stableWarehouseIdFromName(item.warehouseName)
          console.warn(`[wb-sync] Auto-insert неизвестный склад...`)
          await tx.wbWarehouse.create({
            data: {
              id: warehouseId,
              name: item.warehouseName,
              cluster: "Прочие склады",
              shortCluster: "Прочие",
              isActive: true,
              needsClusterReview: true,
            },
          })
        }
        incomingWarehouseIds.push(warehouseId)
        await tx.wbCardWarehouseStock.upsert({
          where: { wbCardId_warehouseId: { wbCardId: card.id, warehouseId } },
          create: { wbCardId: card.id, warehouseId, quantity: item.quantity },
          update: { quantity: item.quantity },
        })
      }

      if (incomingWarehouseIds.length > 0) {
        await tx.wbCardWarehouseStock.deleteMany({
          where: { wbCardId: card.id, NOT: { warehouseId: { in: incomingWarehouseIds } } },
        })
      }
    }
  },
  { timeout: 60_000 },
)
```

Существующий helper stableWarehouseIdFromName(name) в wb-sync/route.ts:28-35 — НЕ переопределять, использовать как есть.

<!-- Текущий вызов в route.ts (строки 85-93) который меняется: -->
```typescript
let ordersStatsMap = new Map<number, { avg7d: number; yesterday: number }>()
try {
  ordersStatsMap = await fetchAvgSalesSpeed7d(nmIds)  // ← ЗАМЕНЯЕТСЯ на fetchOrdersPerWarehouse
} catch (e) {
  console.error("fetchAvgSalesSpeed7d failed:", e)
}
```

И использование строки 110-112:
```typescript
const ordersStats = ordersStatsMap.get(card.nmId)
const avgSalesSpeed7d = ordersStats?.avg7d ?? null
const ordersYesterday = ordersStats?.yesterday ?? null
```

<!-- Структура scripts/wb-sync-stocks.js — полный существующий паттерн паст 1 в 1 для orders -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: /api/wb-sync/route.ts — заменить fetchAvgSalesSpeed7d + добавить clean-replace для WbCardWarehouseOrders</name>
  <read_first>
    - app/api/wb-sync/route.ts (полностью — понять текущий flow)
    - .planning/phases/15-per-cluster-orders/15-01-SUMMARY.md (подтверждение готовности fetchOrdersPerWarehouse и модели)
    - lib/wb-api.ts (проверить подпись fetchOrdersPerWarehouse и OrdersWarehouseStats)
  </read_first>
  <files>
    - app/api/wb-sync/route.ts
  </files>
  <behavior>
    - Существующий вызов fetchAvgSalesSpeed7d(nmIds) ЗАМЕНЁН на fetchOrdersPerWarehouse(nmIds, 7). Остальной код чтения ordersStats сохранён: card.avgSalesSpeed7d и card.ordersYesterday продолжают писаться в БД из полей OrdersWarehouseStats.avg и OrdersWarehouseStats.yesterday соответственно.
    - После существующего блока clean-replace stocksPerWarehouse (строки ~190-282) добавлен НОВЫЙ блок clean-replace ordersPerWarehouse с идентичной логикой: find wbCard by nmId → per-warehouse lookup by name → auto-insert через stableWarehouseIdFromName если нет → upsert WbCardWarehouseOrders → deleteMany NOT IN incoming.
    - Новая секция обёрнута в try/catch (degraded mode — если транзакция упадёт, ошибка попадает в errors массив, sync не ломается).
    - Response /api/wb-sync расширен полем `warehouseOrdersUpdated: <N>` — сколько wbCards обновлено per-warehouse orders.
    - Rate limit защита: в одном sync делаем ровно ОДИН запрос к Orders API (через fetchOrdersPerWarehouse), не два.
  </behavior>
  <action>
    1. Открыть app/api/wb-sync/route.ts. Изменить import (строки 9-20):
    - УДАЛИТЬ из импорта: `fetchAvgSalesSpeed7d`
    - ДОБАВИТЬ в импорт: `fetchOrdersPerWarehouse, type OrdersWarehouseStats`

    Итого:
    ```typescript
    import {
      fetchAllCards,
      parseCard,
      fetchAllPrices,
      fetchWbDiscounts,
      fetchStandardCommissions,
      fetchStocks,
      fetchBuyoutPercent,
      fetchOrdersPerWarehouse,
      type OrdersWarehouseStats,
      fetchStocksPerWarehouse,
      type WarehouseStockItem,
    } from "@/lib/wb-api"
    ```

    2. Заменить блок (строки 82-93 "Phase 7 (D-09) ...") на:

    ```typescript
    // 8. Phase 7 (D-09) + Phase 15 (ORDERS-02): заказы за 7 дней — per-card avg/yesterday + per-warehouse breakdown.
    // Один запрос к Orders API (rate limit ~1 req/min) покрывает обе задачи.
    // Degraded mode — если Orders API недоступен, поля останутся null, per-warehouse блок пропускается.
    let ordersPerWarehouseMap = new Map<number, OrdersWarehouseStats>()
    try {
      ordersPerWarehouseMap = await fetchOrdersPerWarehouse(nmIds, 7)
    } catch (e) {
      console.error("fetchOrdersPerWarehouse failed:", e)
    }
    ```

    3. Найти блок использования ordersStatsMap (строки 110-112) и заменить на чтение из нового Map:

    ```typescript
    const ordersStats = ordersPerWarehouseMap.get(card.nmId)
    const avgSalesSpeed7d = ordersStats?.avg ?? null
    const ordersYesterday = ordersStats?.yesterday ?? null
    ```

    4. Добавить НОВЫЙ блок clean-replace сразу ПОСЛЕ существующего блока stocksPerWarehouse transaction (после строки ~282, перед `return NextResponse.json(...)`):

    ```typescript
    // Phase 15 (ORDERS-02): Clean-replace per-warehouse orders
    let warehouseOrdersUpdated = 0
    if (ordersPerWarehouseMap.size > 0) {
      try {
        await prisma.$transaction(
          async (tx) => {
            for (const [nmId, stats] of ordersPerWarehouseMap.entries()) {
              const card = await tx.wbCard.findUnique({
                where: { nmId },
                select: { id: true },
              })
              if (!card) continue

              const incomingWarehouseIds: number[] = []

              for (const [warehouseName, ordersCount] of stats.perWarehouse.entries()) {
                if (!warehouseName) continue

                // Lookup by name FIRST — seed+stocks section уже могли создать этот склад
                const existingByName = await tx.wbWarehouse.findFirst({
                  where: { name: warehouseName },
                  select: { id: true },
                })

                let warehouseId: number
                if (existingByName) {
                  warehouseId = existingByName.id
                } else {
                  warehouseId = stableWarehouseIdFromName(warehouseName)
                  console.warn(
                    `[wb-sync orders] Auto-insert неизвестный склад: id=${warehouseId} name="${warehouseName}"`,
                  )
                  await tx.wbWarehouse.create({
                    data: {
                      id: warehouseId,
                      name: warehouseName,
                      cluster: "Прочие склады",
                      shortCluster: "Прочие",
                      isActive: true,
                      needsClusterReview: true,
                    },
                  })
                }

                incomingWarehouseIds.push(warehouseId)

                await tx.wbCardWarehouseOrders.upsert({
                  where: {
                    wbCardId_warehouseId: {
                      wbCardId: card.id,
                      warehouseId,
                    },
                  },
                  create: {
                    wbCardId: card.id,
                    warehouseId,
                    ordersCount,
                    periodDays: stats.periodDays,
                  },
                  update: {
                    ordersCount,
                    periodDays: stats.periodDays,
                  },
                })
              }

              // Clean: удалить склады которых нет в текущем ответе API
              if (incomingWarehouseIds.length > 0) {
                await tx.wbCardWarehouseOrders.deleteMany({
                  where: {
                    wbCardId: card.id,
                    NOT: { warehouseId: { in: incomingWarehouseIds } },
                  },
                })
              } else {
                // Если кластеры пустые но card есть — обнуляем полностью (редкий кейс)
                await tx.wbCardWarehouseOrders.deleteMany({
                  where: { wbCardId: card.id },
                })
              }

              warehouseOrdersUpdated++
            }
          },
          { timeout: 60_000 },
        )
        console.log(
          `[wb-sync] Обновлено per-warehouse orders для ${warehouseOrdersUpdated} wbCards`,
        )
      } catch (e) {
        console.error("[wb-sync] Per-warehouse orders transaction failed:", e)
        errors.push(`per-warehouse-orders: ${(e as Error).message}`)
      }
    }
    ```

    5. Расширить объект response (в конце функции POST):
    ```typescript
    return NextResponse.json({
      synced,
      total: rawCards.length,
      pricesLoaded: priceMap.size,
      discountsLoaded: discountMap.size,
      warehouseStocksUpdated: stocksPerWarehouse.size,
      warehouseOrdersUpdated,
      errors: errors.length > 0 ? errors.slice(0, 10) : undefined,
    })
    ```

    6. `npx tsc --noEmit` → 0 ошибок.
  </action>
  <verify>
    <automated>
      grep -c "fetchOrdersPerWarehouse" app/api/wb-sync/route.ts | grep -v "^0$" &&
      grep -c "fetchAvgSalesSpeed7d" app/api/wb-sync/route.ts &&
      grep -c "wbCardWarehouseOrders\.upsert" app/api/wb-sync/route.ts | grep -v "^0$" &&
      grep -c "wbCardWarehouseOrders\.deleteMany" app/api/wb-sync/route.ts | grep -v "^0$" &&
      grep -c "warehouseOrdersUpdated" app/api/wb-sync/route.ts | grep -v "^0$" &&
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchOrdersPerWarehouse" app/api/wb-sync/route.ts` ≥ 2 (import + usage)
    - `grep -c "fetchAvgSalesSpeed7d" app/api/wb-sync/route.ts` = 0 (старый вызов удалён)
    - `grep "tx.wbCardWarehouseOrders.upsert" app/api/wb-sync/route.ts` → найдено 1 совпадение
    - `grep "tx.wbCardWarehouseOrders.deleteMany" app/api/wb-sync/route.ts` → найдено ≥ 1 совпадение
    - `grep "warehouseOrdersUpdated" app/api/wb-sync/route.ts` → найдено ≥ 2 совпадения (declare + response)
    - `grep "needsClusterReview: true" app/api/wb-sync/route.ts` → найдено ≥ 2 совпадения (stocks + orders section)
    - `npx tsc --noEmit` exit 0
    - Существующая fetchStocksPerWarehouse секция НЕ удалена: `grep -c "fetchStocksPerWarehouse" app/api/wb-sync/route.ts` ≥ 2
  </acceptance_criteria>
  <done>route.ts делает один вызов Orders API, заполняет и avg/yesterday поля WbCard и per-warehouse WbCardWarehouseOrders в транзакции; auto-insert неизвестных складов работает; response содержит warehouseOrdersUpdated.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: scripts/wb-sync-stocks.js — добавить секцию orders (паттерн stocks section)</name>
  <read_first>
    - scripts/wb-sync-stocks.js (полностью — все 143 строки, понять паттерн curl → prisma transaction)
    - .planning/phases/15-per-cluster-orders/15-01-SUMMARY.md (подтверждение модели WbCardWarehouseOrders)
  </read_first>
  <files>
    - scripts/wb-sync-stocks.js
  </files>
  <behavior>
    - Скрипт после существующей секции обработки stocks (завершается циклом `for (const [nmId, items] of byNmId)` на строках ~53-126) получает НОВУЮ секцию orders:
      * curl GET https://statistics-api.wildberries.ru/api/v1/supplier/orders?dateFrom=<7d ago>&flag=0
      * Парсинг JSON → фильтрация isCancel:true
      * Группировка по (nmId, warehouseName) → counts
      * Per-nmId транзакция: findUnique wbCard → per-warehouse lookup by name → auto-insert через stableWarehouseIdFromName → upsert WbCardWarehouseOrders(ordersCount, periodDays=7) → deleteMany NOT IN incoming
    - Скрипт завершается выводом суммарной статистики: matched nmIds, inserted orders records, auto-inserted warehouses.
    - `periodDays = 7` константа в начале секции.
    - Переиспользуется существующий `stableWarehouseIdFromName` из того же файла (уже определён).
    - Переиспользуется уже созданный prisma клиент (не создавать второй).
  </behavior>
  <action>
    1. Открыть scripts/wb-sync-stocks.js. После закрытия цикла stocks (перед `console.log(\`\n== Результат ==\`)`) добавить новую секцию orders. Конкретно — вставить перед строкой `console.log(\`\\n== Результат ==\`)`:

    ```javascript
      // ─── Phase 15 (ORDERS-02): orders per-warehouse ─────────────
      console.log(`\nFetching orders (7 days)...`)
      const PERIOD_DAYS = 7
      const dateFrom7 = new Date(Date.now() - PERIOD_DAYS * 24 * 3600 * 1000).toISOString()
      const cmdOrders = `curl -sS -H "Authorization: ${WB_API_TOKEN}" "https://statistics-api.wildberries.ru/api/v1/supplier/orders?dateFrom=${encodeURIComponent(dateFrom7)}&flag=0"`
      const rawOrders = execSync(cmdOrders, { encoding: "utf-8", maxBuffer: 200 * 1024 * 1024 })
      let orders
      try {
        orders = JSON.parse(rawOrders)
      } catch (e) {
        console.error("Orders API JSON parse failed — пропускаем секцию orders:", e.message)
        orders = null
      }

      let ordersMatched = 0
      let ordersInserted = 0
      let ordersUnknownWarehouses = 0
      const newOrdersWarehouses = new Set()

      if (Array.isArray(orders)) {
        console.log(`Received ${orders.length} order rows`)

        // Группируем по nmId → Map<warehouseName, count> (исключая isCancel)
        const ordersByNmId = new Map()
        for (const o of orders) {
          if (o.isCancel) continue
          const nm = o.nmId ?? o.nm_id
          const wh = (o.warehouseName || "").trim()
          if (!nm || !wh) continue
          if (!ordersByNmId.has(nm)) ordersByNmId.set(nm, new Map())
          const whMap = ordersByNmId.get(nm)
          whMap.set(wh, (whMap.get(wh) ?? 0) + 1)
        }
        console.log(`Unique nmIds with orders: ${ordersByNmId.size}`)

        for (const [nmId, whMap] of ordersByNmId) {
          const wbCardId = cardByNmId.get(nmId)
          if (!wbCardId) continue
          ordersMatched++

          await prisma.$transaction(async (tx) => {
            const incoming = new Set()

            for (const [warehouseName, ordersCount] of whMap) {
              let wh = await tx.wbWarehouse.findFirst({
                where: { name: warehouseName },
                select: { id: true },
              })

              let warehouseId
              if (wh) {
                warehouseId = wh.id
              } else {
                warehouseId = stableWarehouseIdFromName(warehouseName)
                try {
                  await tx.wbWarehouse.create({
                    data: {
                      id: warehouseId,
                      name: warehouseName,
                      cluster: "Прочие склады",
                      shortCluster: "Прочие",
                      isActive: true,
                      needsClusterReview: true,
                    },
                  })
                  newOrdersWarehouses.add(warehouseName)
                  ordersUnknownWarehouses++
                } catch (e) {
                  // race condition — берём существующий
                  const existing = await tx.wbWarehouse.findFirst({
                    where: { name: warehouseName },
                    select: { id: true },
                  })
                  if (existing) warehouseId = existing.id
                  else throw e
                }
              }

              incoming.add(warehouseId)

              const existing = await tx.wbCardWarehouseOrders.findUnique({
                where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
                select: { id: true },
              })

              if (existing) {
                await tx.wbCardWarehouseOrders.update({
                  where: { wbCardId_warehouseId: { wbCardId, warehouseId } },
                  data: { ordersCount, periodDays: PERIOD_DAYS },
                })
              } else {
                await tx.wbCardWarehouseOrders.create({
                  data: { wbCardId, warehouseId, ordersCount, periodDays: PERIOD_DAYS },
                })
                ordersInserted++
              }
            }

            // Clean: удалить склады которых нет в текущем ответе
            if (incoming.size > 0) {
              await tx.wbCardWarehouseOrders.deleteMany({
                where: {
                  wbCardId,
                  NOT: { warehouseId: { in: [...incoming] } },
                },
              })
            }
          })
        }
      }

    ```

    2. Расширить финальный summary блок (заменить существующий `console.log(\`\n== Результат ==\`)` и последующие строки) на:

    ```javascript
      console.log(`\n== Результат ==`)
      console.log(`[STOCKS] Matched nmIds: ${matched} / ${byNmId.size}`)
      console.log(`[STOCKS] Новых записей WbCardWarehouseStock: ${inserted}`)
      console.log(`[STOCKS] Unknown warehouses auto-inserted: ${unknownWarehouses}`)
      if (newWarehouses.size > 0) {
        console.log(`\n[STOCKS] Новые склады (needsClusterReview):`)
        newWarehouses.forEach((n) => console.log(`  - ${n}`))
      }
      console.log(`\n[ORDERS] Matched nmIds: ${ordersMatched}`)
      console.log(`[ORDERS] Новых записей WbCardWarehouseOrders: ${ordersInserted}`)
      console.log(`[ORDERS] Unknown warehouses auto-inserted: ${ordersUnknownWarehouses}`)
      if (newOrdersWarehouses.size > 0) {
        console.log(`\n[ORDERS] Новые склады (needsClusterReview):`)
        newOrdersWarehouses.forEach((n) => console.log(`  - ${n}`))
      }

      await prisma.$disconnect()
    ```

    3. Синтаксис-проверка: `node --check scripts/wb-sync-stocks.js` → 0 exit.
  </action>
  <verify>
    <automated>
      grep -c "wbCardWarehouseOrders" scripts/wb-sync-stocks.js | grep -v "^0$" &&
      grep -c "supplier/orders" scripts/wb-sync-stocks.js | grep -v "^0$" &&
      grep -c "isCancel" scripts/wb-sync-stocks.js | grep -v "^0$" &&
      grep -c "ordersCount" scripts/wb-sync-stocks.js | grep -v "^0$" &&
      grep -c "PERIOD_DAYS" scripts/wb-sync-stocks.js | grep -v "^0$" &&
      node --check scripts/wb-sync-stocks.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "tx.wbCardWarehouseOrders.findUnique" scripts/wb-sync-stocks.js` → найдено 1 совпадение
    - `grep "tx.wbCardWarehouseOrders.create" scripts/wb-sync-stocks.js` → найдено 1 совпадение
    - `grep "tx.wbCardWarehouseOrders.deleteMany" scripts/wb-sync-stocks.js` → найдено 1 совпадение
    - `grep "statistics-api.wildberries.ru/api/v1/supplier/orders" scripts/wb-sync-stocks.js` → найдено 1 совпадение
    - `grep "isCancel" scripts/wb-sync-stocks.js` → найдено ≥ 1 совпадение (фильтрация)
    - `grep "PERIOD_DAYS" scripts/wb-sync-stocks.js` → найдено ≥ 2 совпадения
    - `node --check scripts/wb-sync-stocks.js` exit 0 (нет синтаксических ошибок)
    - Существующая секция stocks НЕ удалена: `grep "wbCardWarehouseStock" scripts/wb-sync-stocks.js` → найдено ≥ 3 совпадения
    - Существующий единственный prisma client (`new PrismaClient()`) сохранён: `grep -c "new PrismaClient" scripts/wb-sync-stocks.js` = 1
  </acceptance_criteria>
  <done>Скрипт способен одним прогоном заполнить остатки И заказы per-warehouse без UI клика, auto-insert unknown warehouses работает для обеих секций, итоговый console.log разделяет статистику STOCKS vs ORDERS.</done>
</task>

</tasks>

<verification>
- `/api/wb-sync` POST делает ровно ОДИН вызов Orders API (через fetchOrdersPerWarehouse) — rate limit не истощается
- Per-warehouse orders записываются через clean-replace транзакцию
- Response содержит warehouseOrdersUpdated счётчик
- `node --check scripts/wb-sync-stocks.js` exit 0
- `npx tsc --noEmit` exit 0
- После deploy скрипт `node scripts/wb-sync-stocks.js` на VPS заполнит оба типа данных одним прогоном
</verification>

<success_criteria>
- route.ts использует fetchOrdersPerWarehouse (не fetchAvgSalesSpeed7d)
- Новый транзакционный блок clean-replace для WbCardWarehouseOrders интегрирован после stocks блока
- scripts/wb-sync-stocks.js расширен секцией orders с идентичным паттерном (lookup by name → auto-insert → upsert → clean)
- TypeScript компилируется, Node.js syntax check проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/15-per-cluster-orders/15-02-SUMMARY.md` с:
- Какие файлы изменены
- Response shape /api/wb-sync (для Plan 15-03)
- Known stubs: миграция БД применится на VPS через deploy.sh (из Plan 15-01)
- Integration note для Plan 15-03: данные в WbCardWarehouseOrders доступны через prisma.wbCardWarehouseOrders с include relation warehouse
</output>
