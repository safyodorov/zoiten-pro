---
phase: 21-credits
plan: 06
type: execute
wave: 3
depends_on: [02, 03]
autonomous: true
files_modified:
  - components/credits/LoanSummaryCards.tsx
  - components/credits/LoanScheduleTable.tsx
  - components/credits/LoanBalanceChart.tsx
  - app/(dashboard)/credits/[id]/page.tsx
requirements:
  - D-04  # computed агрегаты + остаток в строке
  - D-09  # computed статус
  - D-11  # RBAC
  - D-18  # детальная карточка: summary cards + таблица графика + line-chart
  - D-19  # ru-RU формат + МСК даты
must_haves:
  truths:
    - "GET /credits/[id] рендерит детальную карточку кредита (RSC)"
    - "Summary cards (паттерн SpendSummary): сумма кредита, погашено тела, уплачено процентов, текущий остаток, переплата, ставка/срок/даты + КРЕДИТОР (loan.lender.name)"
    - "Таблица графика: дата / тело / проценты / вычисленный остаток основного долга по каждой строке (computeSchedule)"
    - "Line-chart остатка основного долга во времени (recharts) — кривая balance из computeSchedule"
    - "404 (notFound) если loan не найден или deletedAt != null"
    - "Кредитор отображается из loan.lender.name (НЕ loan.bank), термин «Кредитор» (U-03)"
  artifacts:
    - path: "app/(dashboard)/credits/[id]/page.tsx"
      provides: "RSC детальная карточка"
      contains: "requireSection(\"CREDITS\")"
    - path: "components/credits/LoanScheduleTable.tsx"
      provides: "Таблица графика с computed остатком per row"
      contains: "balance"
    - path: "components/credits/LoanBalanceChart.tsx"
      provides: "recharts line-chart остатка"
      contains: "recharts"
  key_links:
    - from: "app/(dashboard)/credits/[id]/page.tsx"
      to: "lib/loan-math.ts computeSchedule"
      via: "RSC server compute"
      pattern: "computeSchedule|computeLoanAggregates"
    - from: "components/credits/LoanBalanceChart.tsx"
      to: "recharts LineChart"
      via: "balance time series"
      pattern: "LineChart|Line"
---

<objective>
Реализовать вторую UI-поверхность: детальная карточка кредита `/credits/[id]` (D-18) — summary cards + таблица графика с вычисленным остатком + line-chart кривой остатка основного долга.

Purpose: По клику из списка пользователь видит полную картину одного кредита: ключевые числа, детальный график тело/проценты с накопительным остатком, визуализацию погашения.

Output: 3 компонента + RSC страница [id].

**⚠ U-03:** в summary-картах и заголовке кредитор берётся из `loan.lender.name`, термин «Кредитор» (НЕ «Банк»). include использует `lender: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@components/ads/SpendSummary.tsx
@components/cards/WbCardOrdersChart.tsx
@lib/loan-math.ts
@CLAUDE.md
</context>

<interfaces>
lib/loan-math.ts (Plan 03):
- computeSchedule(amount, payments) -> ScheduleRow[] {date, principal, interest, balance}
- computeLoanAggregates(amount, payments) -> {totalPrincipalPaid, totalInterestPaid, currentBalance, overpayment}
- computeStatus(currentBalance) -> "active" | "paid"

Prisma модель (Plan 01): `Loan { lender Lender; lenderId; company Company; companyId }` — include `{ company: true, lender: true, payments }`.

recharts wrapper: components/ui/chart (ChartContainer, ChartTooltip, ChartTooltipContent, ChartConfig) — паттерн WbCardOrdersChart. var(--chart-1) для dark-aware цветов.
Форматирование дат ДД.ММ.ГГГГ; деньги ru-RU + ₽ справа (паттерн SpendSummary formatRub).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx + LoanSummaryCards</name>
  <read_first>
    - components/ads/SpendSummary.tsx (паттерн summary cards: grid, formatRub, бейджи, ₽ справа)
    - lib/loan-math.ts (computeLoanAggregates, computeSchedule, computeStatus)
    - app/(dashboard)/credits/page.tsx (если создан Plan 05 — паттерн requireSection + загрузка)
    - .planning/phases/21-credits/21-CONTEXT.md (D-18 п.1 список карточек, D-07 fallback даты, U-03 термин «Кредитор»)
  </read_first>
  <files>
    app/(dashboard)/credits/[id]/page.tsx
    components/credits/LoanSummaryCards.tsx
  </files>
  <action>
    app/(dashboard)/credits/[id]/page.tsx RSC:
    - await requireSection("CREDITS")
    - const { id } = await params (Next 15 async params)
    - prisma.loan.findFirst({ where: { id, deletedAt: null }, include: { company: true, lender: true, payments: { orderBy: { date: "asc" } } } })
    - если null -> notFound() (import from next/navigation)
    - const amount = Number(loan.amount); payments -> {date, principal: Number(p.principal), interest: Number(p.interest)}
    - const schedule = computeSchedule(amount, payments); const agg = computeLoanAggregates(amount, payments); const status = computeStatus(agg.currentBalance)
    - effectiveIssueDate = loan.issueDate ?? payments[0]?.date ?? null (D-07)
    - layout: заголовок (№ КД + орг + **кредитор (loan.lender.name)** + статус-бейдж + ссылка «← Назад к списку» на /credits) -> LoanSummaryCards -> LoanBalanceChart (передать schedule + amount) -> LoanScheduleTable (передать schedule).

    components/credits/LoanSummaryCards.tsx (server component — нет интерактива):
    - grid (паттерн SpendSummary: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3).
    - карточки (D-18 п.1): «Сумма кредита» (amount ₽), «Погашено тела» (totalPrincipalPaid ₽ + % от amount), «Уплачено процентов» (totalInterestPaid ₽), «Текущий остаток» (currentBalance ₽, выделенный — sky если active, emerald если paid), «Переплата» (overpayment = totalInterestPaid ₽), «Параметры» (**кредитор** + ставка 28,0% + срок 24 мес или «—» + дата выдачи ДД.ММ.ГГГГ; если дата из fallback — title «по первому платежу»).
    - формат денег: n.toLocaleString("ru-RU", {maximumFractionDigits:0}) + " ₽".
    - props: agg, amount, annualRatePct (Number), termMonths, effectiveIssueDate, issueDateIsFallback, status, lenderName, companyName.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "requireSection" "app/(dashboard)/credits/[id]/page.tsx" && grep -q "notFound" "app/(dashboard)/credits/[id]/page.tsx" && grep -q "computeSchedule" "app/(dashboard)/credits/[id]/page.tsx" && grep -q "lender" "app/(dashboard)/credits/[id]/page.tsx" && test -f components/credits/LoanSummaryCards.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx содержит requireSection("CREDITS") и notFound() для отсутствующего/удалённого loan
    - include использует `lender: true`; заголовок/карточки показывают loan.lender.name (НЕ bank)
    - использует computeSchedule + computeLoanAggregates + computeStatus
    - effectiveIssueDate = issueDate ?? first payment date (D-07)
    - LoanSummaryCards: карточки per D-18 п.1 + кредитор в «Параметры»; деньги ru-RU + ₽, ставка с 1 знаком, статус-цвет на остатке
    - НЕТ упоминаний loan.bank / bankName
    - npx tsc --noEmit без новых ошибок в этих файлах
  </acceptance_criteria>
  <done>RSC карточка + summary cards с computed агрегатами и кредитором</done>
</task>

<task type="auto">
  <name>Task 2: LoanScheduleTable (график с computed остатком)</name>
  <read_first>
    - lib/loan-math.ts (ScheduleRow shape — {date, principal, interest, balance})
    - CLAUDE.md «Sticky data-таблицы» (для длинного графика — sticky thead) + «Форматирование чисел» (ru-RU деньги)
    - components/employees/EmployeesTable.tsx (паттерн обычной таблицы)
  </read_first>
  <files>components/credits/LoanScheduleTable.tsx</files>
  <action>
    Компонент (можно server, без интерактива; если нужен sticky scroll — обернуть в overflow). Props: schedule: ScheduleRow[].

    Таблица (D-18 п.2): колонки Дата (ДД.ММ.ГГГГ) / Тело (ru-RU ₽) / Проценты (ru-RU ₽) / Остаток основного долга (balance, ru-RU ₽).
    - sticky thead паттерн CLAUDE.md (raw HTML table border-separate border-spacing-0, thead bg-background, th sticky top-0 z-20 bg-background border-b) — на случай длинного графика (24+ строк). НЕ shadcn TableHeader/TableRow в шапке.
    - строки в порядке дат (schedule уже отсортирован).
    - итоговая строка внизу: Σ тело, Σ проценты (опционально).
    - деньги: toLocaleString("ru-RU", {maximumFractionDigits:0}) + " ₽"; нулевые проценты — «0 ₽» или «—».
    - правое выравнивание числовых колонок (text-right tabular-nums).
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f components/credits/LoanScheduleTable.tsx && grep -q "balance" components/credits/LoanScheduleTable.tsx && grep -qi "ru-RU" components/credits/LoanScheduleTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LoanScheduleTable рендерит колонки Дата/Тело/Проценты/Остаток
    - остаток берётся из ScheduleRow.balance (computed, D-04)
    - sticky thead (border-separate + bg-background, НЕ shadcn TableHeader)
    - деньги ru-RU + ₽, числа выровнены вправо tabular-nums
    - npx tsc --noEmit без ошибок
  </acceptance_criteria>
  <done>Таблица графика с накопительным остатком per row</done>
</task>

<task type="auto">
  <name>Task 3: LoanBalanceChart (recharts line-chart остатка)</name>
  <read_first>
    - components/cards/WbCardOrdersChart.tsx (паттерн recharts + ChartContainer + ChartConfig + var(--chart-N))
    - components/ui/chart.tsx (ChartContainer / ChartTooltip API)
    - lib/loan-math.ts (ScheduleRow.balance + date)
  </read_first>
  <files>components/credits/LoanBalanceChart.tsx</files>
  <action>
    Клиентский компонент (recharts требует client — первая строка "use client"). Props: schedule: ScheduleRow[], amount: number (опционально для стартовой точки).

    - построить data points: { date: label ДД.ММ.ГГ, balance } из schedule (опционально добавить стартовую точку с balance = amount).
    - ChartContainer config={chartConfig} с LineChart data (recharts): XAxis dataKey="date", YAxis (₽ тысячами), CartesianGrid, Line type="monotone" dataKey="balance" stroke="var(--chart-1)" dot={{r:1.5}}, ChartTooltip content={<ChartTooltipContent />}.
    - chartConfig: { balance: { label: "Остаток тела (₽)", color: "var(--chart-1)" } }.
    - tooltip и оси — ru-RU тысячи (v.toLocaleString("ru-RU")).
    - card-shape обёртка (rounded-md border bg-card p-3), заголовок «Остаток основного долга», max-w-[720px].
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f components/credits/LoanBalanceChart.tsx && grep -q "recharts" components/credits/LoanBalanceChart.tsx && grep -q "use client" components/credits/LoanBalanceChart.tsx && grep -q "Line" components/credits/LoanBalanceChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LoanBalanceChart начинается с "use client"
    - импортирует из recharts (LineChart/Line) и components/ui/chart
    - dataKey="balance", stroke var(--chart-1)
    - оси/tooltip ru-RU тысячи
    - npx tsc --noEmit без ошибок
  </acceptance_criteria>
  <done>recharts line-chart кривой остатка основного долга</done>
</task>

</tasks>

<verification>
- /credits/[id] открывается, notFound для отсутствующего/удалённого
- Summary cards показывают корректные агрегаты (currentBalance == amount − Σprincipal) + кредитор
- График-таблица показывает накопительный остаток (последняя строка balance == currentBalance)
- Line-chart рисует убывающую кривую остатка
- Термин «Кредитор» (не «Банк») в карточке (U-03)
</verification>

<success_criteria>
- Вторая UI-поверхность (D-18) работает: клик из списка → полная карточка
- Агрегаты и остаток per row совпадают с computeLoanAggregates/computeSchedule
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-06-SUMMARY.md`
</output>
