---
phase: 21-credits
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/sections.ts
  - components/layout/section-titles.ts
  - components/layout/nav-items.ts
autonomous: true
requirements:
  - D-10  # ERP_SECTION.CREDITS routing + nav
  - D-11  # RBAC requireSection("CREDITS")
must_haves:
  truths:
    - "lib/sections.ts мапит /credits → CREDITS (middleware RBAC route guard работает)"
    - "components/layout/section-titles.ts возвращает заголовки для /credits, /credits/[id], /credits/schedule"
    - "components/layout/nav-items.ts содержит entry CREDITS с href /credits, иконкой Landmark"
    - "ICON_MAP содержит Landmark импорт из lucide-react"
  artifacts:
    - path: "lib/sections.ts"
      provides: "URL→section mapping для /credits"
      contains: "\"/credits\": \"CREDITS\""
    - path: "components/layout/nav-items.ts"
      provides: "Sidebar nav entry «Кредиты»"
      contains: "/credits"
  key_links:
    - from: "lib/sections.ts"
      to: "ERP_SECTION.CREDITS"
      via: "SECTION_PATHS map"
      pattern: "\"/credits\":\\s*\"CREDITS\""
    - from: "components/layout/nav-items.ts"
      to: "/credits route"
      via: "href + section"
      pattern: "section:\\s*\"CREDITS\""
---

<objective>
Подключить новый раздел `/credits` к инфраструктуре навигации и RBAC: URL→section mapping (middleware), заголовки раздела (Header), пункт в Sidebar (D-10, D-11).

Purpose: Без sections.ts mapping middleware вернёт 403/redirect на /credits. Без nav-items раздел unreachable из меню. Без section-titles Header пустой. Это «проводка» (wiring) между enum CREDITS (Plan 01) и UI (Plans 05-07).

Output: 3 правки existing файлов проводки.

**Claude's Discretion решения (зафиксированы):**
- Иконка Sidebar: **Landmark** (банк/учреждение).
- Позиция в Sidebar: после «План продаж» (`/sales-plan`).
- Сводный график — отдельный route `/credits/schedule` (НЕ таб на /credits); навигация между «Список | Сводный график» через `CreditsTabs` (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/sections.ts
@components/layout/section-titles.ts
@components/layout/nav-items.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/sections.ts + section-titles.ts</name>
  <read_first>
    - lib/sections.ts (текущий SECTION_PATHS — где вставить /credits)
    - components/layout/section-titles.ts (паттерн RegExp match, порядок specific→general)
  </read_first>
  <files>
    lib/sections.ts
    components/layout/section-titles.ts
  </files>
  <action>
    **lib/sections.ts** — добавить в объект `SECTION_PATHS` (после `"/ads": "ADS",`):
    ```typescript
    "/credits": "CREDITS",
    ```

    **components/layout/section-titles.ts** — добавить в массив `SECTION_TITLES` (более specific перед general; вставить рядом с разделами после /sales-plan):
    ```typescript
    { match: /^\/credits\/schedule/, title: "Кредиты — сводный график" },
    { match: /^\/credits\/[^/]+/, title: "Кредит" },
    { match: /^\/credits/, title: "Кредиты" },
    ```
    Порядок важен: `/credits/schedule` ДО `/credits/[^/]+` ДО `/credits` (иначе [id] перехватит schedule).
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "\"/credits\": \"CREDITS\"" lib/sections.ts && grep -q "Кредиты — сводный график" components/layout/section-titles.ts && grep -q "title: \"Кредиты\"" components/layout/section-titles.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"/credits": "CREDITS"' lib/sections.ts` → true
    - section-titles.ts содержит RegExp `/^\/credits\/schedule/` → "Кредиты — сводный график"
    - section-titles.ts содержит RegExp `/^\/credits\/[^/]+/` → "Кредит"
    - section-titles.ts содержит RegExp `/^\/credits/` → "Кредиты"
    - schedule-match расположен ВЫШЕ [id]-match ВЫШЕ /credits-match в массиве (порядок)
  </acceptance_criteria>
  <done>/credits мапится на CREDITS в sections.ts; 3 title-правила добавлены в правильном порядке</done>
</task>

<task type="auto">
  <name>Task 2: nav-items.ts — пункт «Кредиты» + иконка Landmark</name>
  <read_first>
    - components/layout/nav-items.ts (NAV_ITEMS массив + ICON_MAP + импорты lucide)
  </read_first>
  <files>components/layout/nav-items.ts</files>
  <action>
    **1) Импорт** — добавить `Landmark` в import из `"lucide-react"` (в существующий список импортов вверху файла).

    **2) NAV_ITEMS** — добавить entry после `{ section: "SALES", href: "/sales-plan", ... }`:
    ```typescript
    { section: "CREDITS", href: "/credits", label: "Кредиты", icon: "Landmark" },
    ```

    **3) ICON_MAP** — добавить `Landmark,` в объект ICON_MAP.

    НЕ добавлять `prefetch` тут — prefetch={false} применяется в NavLinks.tsx (уже глобально, production gotcha). НЕ менять existing entries.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "section: \"CREDITS\", href: \"/credits\"" components/layout/nav-items.ts && grep -q "Landmark" components/layout/nav-items.ts</automated>
  </verify>
  <acceptance_criteria>
    - nav-items.ts импорт содержит `Landmark` из lucide-react
    - NAV_ITEMS содержит `{ section: "CREDITS", href: "/credits", label: "Кредиты", icon: "Landmark" }`
    - ICON_MAP содержит ключ `Landmark`
    - entry расположен после SALES (`/sales-plan`)
    - `npx tsc --noEmit` не даёт новых ошибок в nav-items.ts (Landmark существует в lucide-react)
  </acceptance_criteria>
  <done>Пункт «Кредиты» с иконкой Landmark в Sidebar nav, ICON_MAP обновлён</done>
</task>

</tasks>

<verification>
- middleware RBAC: /credits защищён requireSection("CREDITS") (работает через sections.ts mapping)
- Sidebar показывает «Кредиты» (для SUPERADMIN или user с правом CREDITS)
- Header показывает корректный заголовок на /credits, /credits/[id], /credits/schedule
- RBAC напоминание: получателю прав через /admin/users нужен logout/login (JWT не самообновляется) — отметить в SUMMARY
- **КАВЕАТ ПОСЛЕДОВАТЕЛЬНОСТИ RBAC (важно для тестирования Wave 3):** этот план добавляет `"/credits": "CREDITS"` в sections.ts, но значение enum `ERP_SECTION.CREDITS` появляется в БД только после `prisma migrate deploy` на VPS (Plan 21-08). До применения миграции JWT-сессии не содержат CREDITS в sectionRoles, и middleware / `requireSection("CREDITS")` вернёт **403 на /credits — это ОЖИДАЕМО, не баг**. Проводка корректна; guard активируется только после миграции. Не править wiring из-за локального 403 до миграции.
</verification>

<success_criteria>
- Раздел /credits reachable из меню и проходит RBAC guard
- Plans 05-07 могут рендерить страницы без 403
- **Каведат активации RBAC:** RBAC-guard для CREDITS активируется ТОЛЬКО после `prisma migrate deploy` на VPS (Plan 21-08). Локально, до применения миграции, 403 на /credits — ожидаемое поведение (enum CREDITS ещё не существует в БД, JWT не содержит CREDITS в sectionRoles). Тестирование Wave 3 НЕ должно трактовать этот 403 как дефект.
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-02-SUMMARY.md`
</output>
</content>
</invoke>
