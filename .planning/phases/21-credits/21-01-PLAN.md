---
phase: 21-credits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260609_phase21_credits/migration.sql
autonomous: true
requirements:
  - D-05  # Loan модель
  - D-06  # LoanPayment модель
  - D-08  # Lender справочник (переименован из Bank, U-03)
  - D-09  # Статус computed (нет поля в БД)
  - D-10  # ERP_SECTION.CREDITS
  - D-19  # Decimal precision (14,2 деньги / 6,3 ставка)
must_haves:
  truths:
    - "prisma/schema.prisma содержит 3 новые модели: Loan, LoanPayment, Lender (справочник «Кредитор», U-03 — НЕ Bank)"
    - "enum ERP_SECTION содержит значение CREDITS"
    - "Loan имеет soft delete (deletedAt DateTime?)"
    - "Loan.amount = Decimal(14,2), Loan.annualRatePct = Decimal(6,3)"
    - "LoanPayment.principal и interest = Decimal(14,2) @default(0), @@index([loanId, date])"
    - "Loan.companyId FK → Company onDelete Restrict; Loan.lenderId FK → Lender onDelete Restrict; LoanPayment.loanId FK → Loan onDelete Cascade"
    - "Company и Lender имеют back-relation loans Loan[]"
    - "Статус кредита НЕ хранится полем (computed по остатку, D-09) — в схеме нет поля status"
    - "Нигде в schema нет модели Bank или поля bankId (переименовано в Lender/lenderId, U-03)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Loan + LoanPayment + Lender модели + CREDITS в ERP_SECTION + back-relations"
      contains: "model Loan"
    - path: "prisma/migrations/20260609_phase21_credits/migration.sql"
      provides: "Manual SQL миграция: ALTER TYPE ERP_SECTION + 3 CREATE TABLE + indexes + FK"
      contains: "CREATE TABLE \"Loan\""
  key_links:
    - from: "prisma/schema.prisma:Loan.companyId"
      to: "Company.id"
      via: "FK onDelete Restrict"
      pattern: "company\\s+Company\\s+@relation"
    - from: "prisma/schema.prisma:Loan.lenderId"
      to: "Lender.id"
      via: "FK onDelete Restrict"
      pattern: "lender\\s+Lender\\s+@relation"
    - from: "prisma/schema.prisma:LoanPayment.loanId"
      to: "Loan.id"
      via: "FK onDelete Cascade"
      pattern: "loan\\s+Loan\\s+@relation.+Cascade"
    - from: "prisma/migrations/20260609_phase21_credits/migration.sql"
      to: "Postgres enum ERP_SECTION"
      via: "ALTER TYPE ... ADD VALUE 'CREDITS'"
      pattern: "ALTER TYPE \"ERP_SECTION\" ADD VALUE"
---

<objective>
Создать Prisma schema + manual SQL миграцию для Phase 21: модели `Loan` + `LoanPayment` + справочник `Lender` («Кредитор»), добавить `CREDITS` в enum `ERP_SECTION`. Реализует решения D-05/D-06/D-08/D-09/D-10/D-19 + переименование Bank→Lender (U-03).

Purpose: Foundation для всех последующих планов 21-02..21-08. Без неё нельзя писать server actions, seed, UI. Миграция пишется руками (нет локальной PG), применится через `prisma migrate deploy` на VPS в Plan 21-08.

Output: Изменённый `prisma/schema.prisma` + новая директория `prisma/migrations/20260609_phase21_credits/` с `migration.sql`.

**⚠ U-03 (переименование):** справочник кредиторов называется `Lender` (НЕ `Bank`); FK на нём — `Loan.lenderId`/`lender` (НЕ `bankId`/`bank`). Значения справочника: «Сбербанк», «JetLend». Слово «Сбербанк» — это значение поля `Lender.name`, его не переименовываем; переименовываем только сущность-справочник и FK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@prisma/schema.prisma
@CLAUDE.md
</context>

<interfaces>
<!-- Существующие сущности, на которые ссылается Phase 21 -->

From prisma/schema.prisma:
```prisma
enum ERP_SECTION {
  PRODUCTS
  PRICES
  WEEKLY_CARDS
  STOCK
  COST
  PROCUREMENT
  SALES
  SUPPORT
  EMPLOYEES
  ADS
  USER_MANAGEMENT
  // ── Phase 21 NEW: добавить CREDITS ──
}

model Company {
  id        String            @id @default(cuid())
  name      String            @unique
  employees EmployeeCompany[]
  createdAt DateTime          @default(now())
  // ── Phase 21 NEW back-relation: loans Loan[] ──
}
```

Существующий pattern Decimal precision (Phase 20):
```prisma
amount Decimal @db.Decimal(14, 2)
```

Существующий pattern soft delete (Product/Supplier):
```prisma
deletedAt DateTime?
@@index([deletedAt])
```

Существующий pattern справочника с sortOrder (Brand):
```prisma
model Brand { id String @id @default(cuid()); name String @unique; sortOrder Int @default(0); createdAt DateTime @default(now()); updatedAt DateTime @updatedAt }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Добавить CREDITS в ERP_SECTION + 3 модели (Loan/LoanPayment/Lender) в schema.prisma</name>
  <read_first>
    - prisma/schema.prisma (текущее состояние enum ERP_SECTION ~line 21-33; model Company ~line 494; где ставить новые блоки)
    - .planning/phases/21-credits/21-CONTEXT.md (D-05/D-06/D-08 + U-03 переименование Bank→Lender)
    - CLAUDE.md «Связи между таблицами БД» (паттерн naming)
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    **1) Добавить `CREDITS` в enum `ERP_SECTION`** (после `USER_MANAGEMENT`):
    ```prisma
    enum ERP_SECTION {
      PRODUCTS
      PRICES
      WEEKLY_CARDS
      STOCK
      COST
      PROCUREMENT
      SALES
      SUPPORT
      EMPLOYEES
      ADS
      USER_MANAGEMENT
      CREDITS
    }
    ```

    **2) Добавить 3 модели** в конец `prisma/schema.prisma` с разделителями `// ──...──` (блок Phase 21):

    `Lender` (D-08 + U-03, паттерн Brand; «Кредитор» — банки + краудлендинг):
    ```prisma
    model Lender {
      id        String   @id @default(cuid())
      name      String   @unique
      sortOrder Int      @default(0)
      loans     Loan[]
      createdAt DateTime @default(now())
      updatedAt DateTime @updatedAt
    }
    ```

    `Loan` (D-05, D-19, soft delete; FK lenderId per U-03):
    ```prisma
    model Loan {
      id             String        @id @default(cuid())
      contractNumber String        // № КД, free text, форматы разные ("№ 3702242101-23-2", "№21220")
      companyId      String
      company        Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
      lenderId       String
      lender         Lender        @relation(fields: [lenderId], references: [id], onDelete: Restrict)
      amount         Decimal       @db.Decimal(14, 2) // сумма кредита (тело)
      annualRatePct  Decimal       @db.Decimal(6, 3)  // годовая ставка %, напр. 28.000
      termMonths     Int?          // срок в месяцах, nullable
      issueDate      DateTime?     // дата выдачи — ручное поле, nullable (D-07)
      notes          String?       @db.Text
      payments       LoanPayment[]
      deletedAt      DateTime?
      createdAt      DateTime      @default(now())
      updatedAt      DateTime      @updatedAt

      @@index([deletedAt])
      @@index([companyId])
      @@index([lenderId])
    }
    ```

    `LoanPayment` (D-06):
    ```prisma
    model LoanPayment {
      id        String   @id @default(cuid())
      loanId    String
      loan      Loan     @relation(fields: [loanId], references: [id], onDelete: Cascade)
      date      DateTime @db.Date
      principal Decimal  @db.Decimal(14, 2) @default(0) // тело долга
      interest  Decimal  @db.Decimal(14, 2) @default(0) // проценты
      createdAt DateTime @default(now())

      @@index([loanId, date])
    }
    ```

    **3) Дополнить `model Company`** — добавить back-relation:
    ```prisma
    loans Loan[]
    ```

    **ВАЖНО:**
    - Справочник называется `Lender` (U-03), FK — `lenderId`/`lender`. НЕ создавать `Bank`/`bankId`.
    - Статус кредита НЕ хранится (D-09) — НЕ добавлять поле `status`. Вычисляется на лету из payments.
    - Остаток в строке LoanPayment НЕ хранится (D-06) — вычисляется при рендере.
    - НЕ трогать User, не трогать другие модели кроме Company (back-relation).
    - Per D-19: деньги `Decimal(14,2)`, ставка `Decimal(6,3)`.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -c "^model Loan\|^model LoanPayment\|^model Lender" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^model Loan" prisma/schema.prisma` → true
    - `grep -q "^model LoanPayment" prisma/schema.prisma` → true
    - `grep -q "^model Lender" prisma/schema.prisma` → true
    - schema НЕ содержит `model Bank` и НЕ содержит поля `bankId` (U-03 — переименовано)
    - `grep -A14 "enum ERP_SECTION" prisma/schema.prisma` содержит строку `CREDITS`
    - `grep -q "lenderId" prisma/schema.prisma` (FK на Lender) присутствует в блоке Loan
    - `grep -q "annualRatePct  Decimal       @db.Decimal(6, 3)" prisma/schema.prisma` (или с любым whitespace: `Decimal.*@db.Decimal\(6, 3\)`)
    - `grep -q "@@index(\[loanId, date\])" prisma/schema.prisma`
    - `grep -q "onDelete: Restrict" prisma/schema.prisma` присутствует в блоке Loan (companyId + lenderId)
    - schema НЕ содержит `status` поля в model Loan (D-09 computed)
    - `npx prisma format --schema=prisma/schema.prisma && npx prisma validate --schema=prisma/schema.prisma` проходит
  </acceptance_criteria>
  <done>3 модели (Lender/Loan/LoanPayment) + CREDITS в ERP_SECTION + Company.loans back-relation; нет следов Bank/bankId; prisma validate проходит</done>
</task>

<task type="auto">
  <name>Task 2: Manual SQL миграция</name>
  <read_first>
    - prisma/schema.prisma (после Task 1)
    - prisma/migrations/20260601_phase20_procurement/migration.sql (если есть) ИЛИ prisma/migrations/20260526_product_incoming/migration.sql — паттерн naming/format manual миграции
    - prisma/migrations/20260407_add_employees_section/migration.sql — паттерн ALTER TYPE ERP_SECTION ADD VALUE
    - .planning/phases/21-credits/21-CONTEXT.md (D-05/D-06/D-08 + U-03)
  </read_first>
  <files>prisma/migrations/20260609_phase21_credits/migration.sql</files>
  <action>
    Создать `prisma/migrations/20260609_phase21_credits/migration.sql` воспроизводящий schema из Task 1.

    **КРИТИЧНО — порядок и enum:**
    1. `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CREDITS';`
       (Postgres требует commit между ADD VALUE и использованием — но т.к. CREDITS нигде в этой миграции в DEFAULT не используется, это безопасно одной транзакцией; в проде `prisma migrate deploy` каждый файл = отдельная транзакция.)

    2. **CREATE TABLE "Lender"** (U-03 — НЕ "Bank"):
       ```sql
       CREATE TABLE "Lender" (
         "id" TEXT NOT NULL,
         "name" TEXT NOT NULL,
         "sortOrder" INTEGER NOT NULL DEFAULT 0,
         "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
         "updatedAt" TIMESTAMP(3) NOT NULL,
         CONSTRAINT "Lender_pkey" PRIMARY KEY ("id")
       );
       CREATE UNIQUE INDEX "Lender_name_key" ON "Lender"("name");
       ```

    3. **CREATE TABLE "Loan"**:
       - поля: id TEXT PK, contractNumber TEXT NOT NULL, companyId TEXT NOT NULL, lenderId TEXT NOT NULL, amount DECIMAL(14,2) NOT NULL, annualRatePct DECIMAL(6,3) NOT NULL, termMonths INTEGER, issueDate TIMESTAMP(3), notes TEXT, deletedAt TIMESTAMP(3), createdAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, updatedAt TIMESTAMP(3) NOT NULL
       - indexes: `Loan_deletedAt_idx`, `Loan_companyId_idx`, `Loan_lenderId_idx`
       - FK: `Loan_companyId_fkey` → "Company"("id") ON DELETE RESTRICT ON UPDATE CASCADE; `Loan_lenderId_fkey` → "Lender"("id") ON DELETE RESTRICT ON UPDATE CASCADE

    4. **CREATE TABLE "LoanPayment"**:
       - поля: id TEXT PK, loanId TEXT NOT NULL, date DATE NOT NULL, principal DECIMAL(14,2) NOT NULL DEFAULT 0, interest DECIMAL(14,2) NOT NULL DEFAULT 0, createdAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
       - index: `CREATE INDEX "LoanPayment_loanId_date_idx" ON "LoanPayment"("loanId", "date");`
       - FK: `LoanPayment_loanId_fkey` → "Loan"("id") ON DELETE CASCADE ON UPDATE CASCADE

    **НЕ запускать `prisma migrate dev` локально** (нет PG). Применится через `prisma migrate deploy` в deploy.sh на VPS (Plan 21-08).

    После создания проверить `npx prisma validate`.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f prisma/migrations/20260609_phase21_credits/migration.sql && grep -q "ALTER TYPE \"ERP_SECTION\" ADD VALUE" prisma/migrations/20260609_phase21_credits/migration.sql && grep -c "CREATE TABLE \"Lender\"\|CREATE TABLE \"Loan\"\|CREATE TABLE \"LoanPayment\"" prisma/migrations/20260609_phase21_credits/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `test -f prisma/migrations/20260609_phase21_credits/migration.sql` → true
    - migration.sql содержит `ALTER TYPE "ERP_SECTION" ADD VALUE` с `'CREDITS'`
    - migration.sql содержит `CREATE TABLE "Lender"`, `CREATE TABLE "Loan"`, `CREATE TABLE "LoanPayment"` (grep count = 3)
    - migration.sql НЕ содержит `CREATE TABLE "Bank"` и НЕ содержит столбца `bankId` (U-03)
    - содержит `lenderId` и FK `Loan_lenderId_fkey` → "Lender"
    - содержит `ON DELETE RESTRICT` (для Loan FK companyId + lenderId)
    - содержит `ON DELETE CASCADE` (для LoanPayment.loanId FK)
    - содержит `DECIMAL(14,2)` и `DECIMAL(6,3)`
    - содержит `CREATE INDEX "LoanPayment_loanId_date_idx"`
    - `npx prisma validate --schema=prisma/schema.prisma` проходит
  </acceptance_criteria>
  <done>migration.sql существует, ALTER TYPE CREDITS + 3 таблицы (Lender/Loan/LoanPayment) + indexes + FK с правильными onDelete; нет Bank/bankId; prisma validate проходит</done>
</task>

</tasks>

<verification>
- `npx prisma validate` проходит без ошибок
- `npx prisma format` не предлагает изменений
- grep подтверждает 3 модели (Lender/Loan/LoanPayment) + CREDITS в enum + Company.loans back-relation
- Все FK с правильным onDelete (Restrict для Company/Lender, Cascade для LoanPayment)
- Нет следов Bank/bankId в schema и миграции (U-03)
- Нет поля status в Loan (D-09 computed)
</verification>

<success_criteria>
- Schema готова для генерации Prisma client на VPS
- Plan 21-02/21-03/21-04 могут импортировать `import { Loan, LoanPayment, Lender } from "@prisma/client"` и использовать `requireSection("CREDITS")`
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-01-SUMMARY.md`
</output>
