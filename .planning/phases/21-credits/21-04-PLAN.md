---
phase: 21-credits
plan: 04
type: execute
wave: 2
depends_on: [01]
autonomous: false
files_modified:
  - scripts/seed-credits.ts
  - .planning/phases/21-credits/21-04-SEED-NOTES.md
requirements:
  - D-01  # Разовый seed (источник — папка Кредиты/, U-01)
  - D-07  # issueDate = null при seed
  - D-08  # Lender (Сбербанк/JetLend), U-03
  - D-19  # ставка → 28.000
must_haves:
  truths:
    - "scripts/seed-credits.ts читает ДЕТАЛЬНЫЕ графики из папки Кредиты/ (Сбербанк XLSX через `xlsx` + JetLend PDF через pdftotext), создаёт Lender + Loan + LoanPayment[] (U-01)"
    - "Метаданные кредитов (сумма/ставка/срок/кредитор/орг/№КД) берутся из Кредиты.xlsx Лист2; помесячные итоги Лист2 = контрольные суммы"
    - "JetLend PDF парсятся через execSync('pdftotext -layout ...'); числа русского формата (пробел=тысячи, запятая=десятичная)"
    - "Сбербанк XLSX парсятся через `xlsx`; числа формата 399,557.24 (запятая=тысячи, точка=десятичная)"
    - "Сбербанк XLSX = только хвост → история Сбера дополняется помесячно из Кредиты.xlsx Лист1/Лист2 (U-05)"
    - "Орг из Excel мапятся на существующие Company: Пеликан→ПЕЛИКАН ХЭППИ ТОЙС, Зойтен→ЗОЙТЕН, Сикрет Вэй→СИКРЕТ ВЭЙ, Дрим Лайн→ДРИМ ЛАЙН"
    - "Ставка 0.28 / «28%» в Excel → 28.000 в БД"
    - "Срок «24 месяца» → termMonths 24 (parse int)"
    - "issueDate = null для всех seed-кредитов (D-07)"
    - "Идемпотентность: повторный запуск не дублирует (delete-and-reseed Loan)"
    - "Сверка (checkpoint): накопленный остаток per-кредит == amount + помесячные per-org/Итого совпадают с Лист2; конкретные числа в 21-04-SEED-NOTES.md (не TBD)"
  artifacts:
    - path: "scripts/seed-credits.ts"
      provides: "Разовый seed: парсинг папки Кредиты/ (XLSX+PDF) + метаданные/сверка из Кредиты.xlsx"
      contains: "pdftotext"
    - path: ".planning/phases/21-credits/21-04-SEED-NOTES.md"
      provides: "Маппинг файлов→кредитов + структура Сбер XLSX / JetLend PDF + per-org контрольные суммы + результат сверки"
  key_links:
    - from: "scripts/seed-credits.ts"
      to: "prisma.loan + prisma.loanPayment"
      via: "create nested"
      pattern: "loanPayment"
    - from: "scripts/seed-credits.ts"
      to: "папка Кредиты/ (PDF + XLSX)"
      via: "pdftotext -layout + XLSX.readFile"
      pattern: "pdftotext|Кредиты"
    - from: "scripts/seed-credits.ts"
      to: "Company (существующие записи)"
      via: "findUnique по name (mapped)"
      pattern: "company\\.findUnique|companyMap"
---

<objective>
Создать разовый seed-скрипт `scripts/seed-credits.ts` для импорта текущих кредитов. **Источник строк графика — ДЕТАЛЬНЫЕ файлы папки `Кредиты/`** (U-01): Сбербанк XLSX (через npm `xlsx`) + JetLend PDF (через `pdftotext -layout`). Метаданные кредита (сумма/ставка/срок/кредитор/орг/№КД) и контрольные суммы — из `Кредиты.xlsx` Лист2. Завершается checkpoint'ом сверки накопленных остатков и per-org/Итого с Лист2.

Purpose: Наполнить базу реальными построчными графиками платежей без UI-парсера. Контрольная точка — суммы из seed должны совпадать с Лист2 per-org подытогами + Итого, а накопленный остаток per-кредит = amount (ground-truth verification).

Output: scripts/seed-credits.ts + 21-04-SEED-NOTES.md с маппингом файлов и сверкой.

**ВАЖНО про данные и окружение:**
- Папка `C:\Users\User\zoiten-pro\Кредиты\` (untracked) содержит **13 файлов**:
  - 2 XLSX (Сбербанк): `График_платежей_по_договору_3702242101_23_2_на_08_06_2026_г_.xlsx`, `График_платежей_по_договору_3702259264_23_2_на_08_06_2026_г_.xlsx`. Лист «График платежей»; стр.0 = «График платежей по договору № …» (источник № КД); стр.3 заголовки `Дата | К оплате, ₽ | Основной долг, ₽ | Проценты, ₽ | Остаток основного долга, ₽`; стр.4+ данные. Числа `399,557.24` (**запятая=тысячи, точка=десятичная**). Выгрузка «на 08.06.2026» = только ОСТАВШИЕСЯ платежи (эти 2 почти погашены, остаток 0) — U-05.
  - 11 PDF (JetLend): `schedule-38..41.pdf`, `schedule (57..63).pdf`. Договоры 20967, 20968, 21220, 21359, 21384, 21934, 21940, 25397, 28513, 29294, 29338. Парсинг `pdftotext -layout`; строки с датой `DD.MM.YYYY`; колонки «Основной долг»/«Проценты»/«Остаток основного долга». Числа `504 678,64` (**ПРОБЕЛ=тысячи, ЗАПЯТАЯ=десятичная — русский формат, иначе чем XLSX!**). PDF = полный график.
- `Кредиты.xlsx` (untracked) — метаданные + контрольные суммы (Лист2: заголовки колонок = № договоров, под ними сумма/ставка/срок, далее помесячные тело+проценты + per-org/Итого).
- **pdftotext подтверждён доступен** на dev-машине (`pdftotext -v` → version 4.00, /mingw64/bin). На VPS нужен `poppler-utils` (см. Plan 21-08 DEPLOY-NOTES).
- Dev-машина — **Windows**; Excel читать через npm `xlsx` (`^0.18.5`, уже в проекте, см. `app/api/wb-commission-iu/route.ts`). НЕ полагаться на Python.
- Скрипт запускается ОДИН раз вручную (`npx tsx scripts/seed-credits.ts`); на VPS — после deploy в Plan 21-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@prisma/schema.prisma
@app/api/wb-commission-iu/route.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Разобрать папку Кредиты/ + Кредиты.xlsx — зафиксировать структуру + контрольные суммы</name>
  <read_first>
    - .planning/phases/21-credits/21-CONTEXT.md (блок «⚠ ОБНОВЛЕНИЕ 2026-06-09» + canonical_refs «Источник данных» + U-01..U-05)
    - app/api/wb-commission-iu/route.ts (паттерн чтения Excel через npm `xlsx`: XLSX.read / sheet_to_json)
  </read_first>
  <read_first_samples>
    - C:/Users/User/zoiten-pro/Кредиты/График_платежей_по_договору_3702242101_23_2_на_08_06_2026_г_.xlsx (образец Сбербанк XLSX — лист «График платежей», заголовки на стр.3)
    - C:/Users/User/zoiten-pro/Кредиты/schedule-38.pdf (образец JetLend PDF — для проверки вывода pdftotext -layout)
    - C:/Users/User/zoiten-pro/Кредиты.xlsx (метаданные + контрольные суммы, Лист2)
  </read_first_samples>
  <files>.planning/phases/21-credits/21-04-SEED-NOTES.md</files>
  <action>
    Цель — ЗАФИКСИРОВАТЬ фактическую структуру всех источников и КОНКРЕТНЫЕ контрольные суммы, чтобы Task 2 писал парсер без догадок.

    **Шаг 0 — проверка инструментов (ОБЯЗАТЕЛЬНО):**
    ```bash
    pdftotext -v        # должен быть установлен (на dev — version 4.00). Если нет → blocker + инструкция (poppler-utils / poppler для Windows)
    ```
    Excel — через npm `xlsx` (без Python).

    **Шаг 1 — Сбербанк XLSX (папка Кредиты/):** для одного образца вывести листы + первые ~12 строк листа «График платежей»:
    ```bash
    npx tsx -e "import * as XLSX from 'xlsx'; const wb = XLSX.readFile('C:/Users/User/zoiten-pro/Кредиты/График_платежей_по_договору_3702242101_23_2_на_08_06_2026_г_.xlsx', { cellDates: true }); console.log('SHEETS:', wb.SheetNames); const ws = wb.Sheets['График платежей'] ?? wb.Sheets[wb.SheetNames[0]]; const rows = XLSX.utils.sheet_to_json(ws, { header: 1, defval: null }); rows.slice(0, 14).forEach((r,i)=>console.log(i, JSON.stringify(r)));"
    ```
    Зафиксировать: имя листа, индекс строки с «График платежей по договору № …» (стр.0 → извлечь № КД), индекс строки заголовков (ожид. стр.3), порядок колонок (Дата / К оплате / Основной долг / Проценты / Остаток основного долга), формат чисел (`399,557.24` — запятая=тысячи).

    **Шаг 2 — JetLend PDF (папка Кредиты/):** для одного образца вывести текст:
    ```bash
    pdftotext -layout "C:/Users/User/zoiten-pro/Кредиты/schedule-38.pdf" - | head -60
    ```
    Зафиксировать: где в шапке/тексте № договора (короткое число 20xxx–29xxx), какие строки содержат дату `DD.MM.YYYY`, порядок числовых колонок (Основной долг / Проценты / Остаток основного долга), формат чисел (`504 678,64` — пробел=тысячи, запятая=десятичная). Зафиксировать regex для строки платежа.

    **Шаг 3 — маппинг файлов → договоров:** перечислить все 13 файлов и сопоставить каждому № КД:
    - 2 XLSX → 3702242101-23-2, 3702259264-23-2 (Сбербанк, из стр.0)
    - 11 PDF → 20967, 20968, 21220, 21359, 21384, 21934, 21940, 25397, 28513, 29294, 29338 (JetLend, из имени файла / шапки PDF — указать, откуда брать №)

    **Шаг 4 — Кредиты.xlsx Лист2 (метаданные + контрольные суммы):**
    ```bash
    npx tsx -e "import * as XLSX from 'xlsx'; const wb = XLSX.readFile('C:/Users/User/zoiten-pro/Кредиты.xlsx', { cellDates: true }); console.log('SHEETS:', wb.SheetNames); for (const sh of wb.SheetNames) { const rows = XLSX.utils.sheet_to_json(wb.Sheets[sh], { header: 1, defval: null }); console.log('---', sh, rows.length, 'rows ---'); rows.slice(0, 16).forEach((r,i)=>console.log(i, JSON.stringify(r))); }"
    ```
    Зафиксировать: заголовки колонок = № договоров; под ними сумма / ставка (`28%` или 0.28) / срок (`24 месяца`); где помесячные основной долг + проценты; где per-org подытоги и Итого. Маппинг каждого № договора → орг → Lender (Сбербанк для 3702…; JetLend для числовых 20xxx–29xxx).

    Записать в `21-04-SEED-NOTES.md`:
    - Подтверждение, что `pdftotext` доступен (версия).
    - Структуру Сбербанк XLSX (лист, строки, колонки, формат чисел).
    - Структуру JetLend PDF (regex строки платежа, колонки, формат чисел, источник № договора).
    - Полный маппинг 13 файлов → № КД → орг → Lender (Сбербанк/JetLend).
    - Структуру Лист2 (где сумма/ставка/срок per договор; где помесячные тело/проценты; per-org/Итого).
    - **КОНКРЕТНЫЕ контрольные суммы из Лист2:** per-org Σ основной долг, per-org Σ проценты, общий Итого основной долг, общий Итого проценты. **Реальные числа — НЕ «TBD».**
    - Маппинг орг → Company.name.
    - U-05: для Сбера зафиксировать, покрывает ли детальный XLSX весь график; если нет (только хвост) — отметить, что историю Сбера надо дополнить из Лист1/Лист2 помесячно (одна строка LoanPayment на месяц; **зафиксировать выбор даты строки: конец месяца ИЛИ 1-е число** — выбрать и записать).

    Если файл/папка недоступны → записать blocker и поднять на checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f .planning/phases/21-credits/21-04-SEED-NOTES.md && grep -qi "итого\|per-org\|контрольн" .planning/phases/21-credits/21-04-SEED-NOTES.md && grep -qi "pdftotext\|JetLend" .planning/phases/21-credits/21-04-SEED-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - 21-04-SEED-NOTES.md существует
    - содержит подтверждение доступности pdftotext (версия)
    - содержит структуру Сбербанк XLSX (лист/строки/колонки/формат `399,557.24`)
    - содержит структуру JetLend PDF (regex строки, колонки, формат `504 678,64`, источник № договора)
    - содержит маппинг всех 13 файлов → № КД → орг → Lender (Сбербанк/JetLend)
    - содержит структуру Лист2 (сумма/ставка/срок + помесячные тело/проценты + per-org/Итого)
    - содержит **конкретные** контрольные суммы per-org + Итого (реальные числа, НЕ «TBD»)
    - содержит маппинг орг → Company.name
    - содержит решение по U-05 (дополнение истории Сбера из Лист1/Лист2 + выбор даты строки)
  </acceptance_criteria>
  <done>Структура папки Кредиты/ (XLSX+PDF) + Кредиты.xlsx Лист2 + маппинг файлов + конкретные контрольные суммы зафиксированы в SEED-NOTES.md</done>
</task>

<task type="auto">
  <name>Task 2: scripts/seed-credits.ts — парсинг папки Кредиты/ (XLSX+PDF) + метаданные/сверка из Лист2</name>
  <read_first>
    - .planning/phases/21-credits/21-04-SEED-NOTES.md (структура + маппинг файлов из Task 1)
    - prisma/schema.prisma (модели Loan/LoanPayment/Lender/Company)
    - app/api/wb-commission-iu/route.ts (паттерн чтения Excel через npm `xlsx` — XLSX.readFile / sheet_to_json)
    - scripts/ (любой существующий seed-скрипт как паттерн PrismaClient + tsx; напр. prisma/seed-wb-warehouses.ts если есть)
    - app/actions/credits.ts (если уже создан Plan 03 — переиспользовать round/parse логику; иначе inline)
  </read_first>
  <files>scripts/seed-credits.ts</files>
  <action>
    Создать `scripts/seed-credits.ts` — standalone Node/tsx скрипт с `new PrismaClient()`.

    Источники (per SEED-NOTES, U-01):
    - **Строки LoanPayment** — из детальных файлов папки `Кредиты/` (НЕ из помесячного Лист2 для тех кредитов, где есть детальный файл).
    - **Метаданные кредита** (amount, annualRatePct, termMonths, № КД, орг, Lender) — из `Кредиты.xlsx` Лист2.
    - **Контрольные суммы** — Лист2 (для финального вывода/сверки).

    Шаги:

    1. **Проверка pdftotext (ОБЯЗАТЕЛЬНО):** `execSync('pdftotext -v', { stdio: 'pipe' })` в try/catch. Если падает — кинуть внятную ошибку: «pdftotext не найден. Установите poppler-utils (Linux: apt-get install -y poppler-utils; Windows: poppler для Windows в PATH).» и остановиться.

    2. **Lender upsert (U-03):** `prisma.lender.upsert({where:{name}, create:{name, sortOrder}, update:{}})` для «Сбербанк» и «JetLend» (и других из Лист2, если есть).

    3. **Company lookup:** `prisma.company.findUnique({where:{name: MAPPED}})` по маппингу (Пеликан→ПЕЛИКАН ХЭППИ ТОЙС...). Если не найдена — warn + skip кредит (не падать).

    4. **Метаданные из Лист2:** прочитать `Кредиты.xlsx` через `XLSX.readFile(...,{cellDates:true})`; распарсить Лист2 в map по № КД: {amount, annualRatePct = rate→процент (0.28→28.000 или «28%»→28.000), termMonths = parseInt из «N месяцев» или null, орг, Lender}.

    5. **Идемпотентность:** в начале `prisma.loan.deleteMany({})` (Loan cascade удалит LoanPayment). Lender не трогаем — upsert.

    6. **Строки LoanPayment из детальных файлов:**
       - **JetLend PDF:** для каждого PDF в папке — `execSync('pdftotext -layout "<abs path>" -', {encoding:'utf8', maxBuffer: 10*1024*1024})`. Парсить строки с датой `DD.MM.YYYY` (regex). Из каждой брать «Основной долг» (principal) и «Проценты» (interest). **Числа русского формата:** убрать пробелы-разделители тысяч (`\s` и неразрывный пробел ` `), запятую→точку → Number. Сопоставить с кредитом по № договора (короткое число из имени файла / шапки PDF, per SEED-NOTES).
       - **Сбербанк XLSX:** для каждого XLSX в папке — `XLSX.readFile(...,{cellDates:true})`, лист «График платежей»; заголовки на стр.3, данные стр.4+. Из каждой строки брать «Основной долг» (principal) и «Проценты» (interest). **Числа формата `399,557.24`:** запятая=разделитель тысяч (убрать), точка=десятичная → Number. № КД — из стр.0 («…по договору № …»). Сопоставить с кредитом.
       - **U-05 — дополнение истории Сбера:** детальные Сбербанк XLSX дают только хвост. Проверить покрытие (накопленный Σprincipal по детальному файлу vs amount из Лист2). Если детальный график покрывает не весь срок — дополнить недостающую (раннюю) часть истории Сбера помесячными строками из `Кредиты.xlsx` Лист1/Лист2 (одна строка LoanPayment на месяц: principal/interest из Лист2; дата строки — per решение в SEED-NOTES). Не дублировать месяцы, уже покрытые детальным XLSX.

    7. **Создание Loan:** для каждого № КД из Лист2 — собрать payments (детальные + при необходимости дополнение по U-05), затем `prisma.loan.create({ data: { contractNumber, companyId, lenderId, amount, annualRatePct, termMonths, issueDate: null /*D-07*/, notes: null, payments: { create: payments } } })`.

    8. **Вывод для сверки:** в конце вывести в консоль кол-во созданных Loan; per-кредит: накопленный Σprincipal vs amount (должны совпадать в пределах копеек); per-org Σprincipal/Σinterest + Итого — для сверки с Лист2.

    Parse дат: JetLend `DD.MM.YYYY` → `new Date(Date.UTC(yyyy,mm-1,dd))`; Сбер XLSX даты → JS Date через `cellDates:true`.

    Комментарий вверху файла: «Разовый seed (D-01, U-01). Источник строк — папка Кредиты/ (Сбербанк XLSX через xlsx + JetLend PDF через pdftotext -layout); метаданные/сверка — Кредиты.xlsx Лист2. Требует pdftotext (poppler). Запуск: npx tsx scripts/seed-credits.ts. НЕ часть deploy.sh — запускается вручную один раз после миграции.»
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f scripts/seed-credits.ts && grep -q "PrismaClient" scripts/seed-credits.ts && grep -q "loanPayment\|payments:" scripts/seed-credits.ts && grep -q "pdftotext" scripts/seed-credits.ts && grep -q "prisma.lender\|lenderId" scripts/seed-credits.ts && grep -q "xlsx\|XLSX" scripts/seed-credits.ts && npx tsc --noEmit 2>&1 | grep -c "seed-credits" || true</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-credits.ts существует, использует PrismaClient
    - проверяет наличие pdftotext (execSync 'pdftotext -v' в try/catch + внятная ошибка с инструкцией установки poppler)
    - парсит JetLend PDF через `execSync('pdftotext -layout ...')` с разбором русского формата чисел (пробел=тысячи, запятая=десятичная)
    - парсит Сбербанк XLSX из папки через `xlsx` с разбором формата `399,557.24` (запятая=тысячи, точка=десятичная)
    - реализует U-05 (дополнение истории Сбера из Лист1/Лист2 при неполном покрытии детального XLSX)
    - метаданные (amount/ставка/срок) берутся из Кредиты.xlsx Лист2
    - Lender upsert по name (Сбербанк/JetLend), `prisma.lender` (НЕ prisma.bank)
    - Company lookup по mapped name
    - ставка → проценты (0.28→28.000 / «28%»→28.000)
    - termMonths через parseInt из текста «N месяцев»
    - issueDate = null (D-07)
    - идемпотентность (loan.deleteMany в начале)
    - payments создаются nested
    - выводит per-кредит остаток-vs-amount + per-org + Итого для сверки
    - `npx tsc --noEmit` без ошибок в seed-credits.ts
  </acceptance_criteria>
  <done>Seed парсит папку Кредиты/ (XLSX через xlsx + PDF через pdftotext), дополняет историю Сбера (U-05), берёт метаданные из Лист2, создаёт Lender/Loan/LoanPayment идемпотентно, печатает контрольные суммы</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Сверка seed-сумм — остатки per-кредит + per-org/Итого с Лист2</name>
  <files>.planning/phases/21-credits/21-04-SEED-NOTES.md</files>
  <action>
    HUMAN-VERIFY: агент запускает seed (или dry-run печать сумм если локальной PG нет) и предъявляет числа на сравнение: (1) накопленный Σprincipal каждого кредита == amount; (2) per-org Σ тело/проценты + Итого == Лист2.
  </action>
  <what-built>seed-credits.ts парсит папку Кредиты/ (Сбер XLSX + JetLend PDF), дополняет историю Сбера (U-05), печатает per-кредит остаток-vs-amount + per-org Σprincipal/Σinterest + Итого; SEED-NOTES.md содержит конкретные контрольные суммы из Лист2</what-built>
  <how-to-verify>
    1. Убедиться, что pdftotext установлен (`pdftotext -v`).
    2. Запустить `npx tsx scripts/seed-credits.ts` (после применения миграции Plan 01 локально ИЛИ это выполнится на VPS в Plan 08 — на этом этапе достаточно dry-run печати сумм без записи, если локальной PG нет).
    3. Проверить, что накопленный Σ тело каждого кредита == сумма кредита (amount) в пределах копеек (U-04 — сверка с колонкой «Остаток основного долга» должна сойтись в 0).
    4. Сравнить выведенные per-org Σ основной долг и Σ проценты + Итого с контрольными суммами из 21-04-SEED-NOTES.md (из Лист2).
    5. Если совпадает (с точностью до копеек / округления) → ввести "approved".
    6. Если расходится → описать расхождение (какой кредит/орг, principal или interest, на сколько) — вероятно ошибка парсинга формата чисел (русский vs XLSX), маппинга файла→договора, или неполное дополнение Сбера (U-05); скорректировать парсер.
  </how-to-verify>
  <resume-signal>Type "approved" если остатки per-кредит сходятся И per-org/Итого совпадают с Лист2, или опиши расхождение</resume-signal>
  <verify>
    <automated>test -f .planning/phases/21-credits/21-04-SEED-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - User подтвердил: накопленный остаток per-кредит == amount (U-04)
    - User подтвердил совпадение seed-сумм per-org/Итого с Лист2 Excel
  </acceptance_criteria>
  <done>User подтвердил что остатки per-кредит сходятся и seed-суммы per-org/Итого совпадают с Лист2</done>
</task>

</tasks>

<verification>
- 21-04-SEED-NOTES.md содержит структуру папки Кредиты/ + Лист2 + маппинг файлов + конкретные контрольные суммы (не TBD)
- seed-credits.ts парсит XLSX (через xlsx) + PDF (через pdftotext), дополняет историю Сбера (U-05), идемпотентен, tsc проходит
- User подтвердил сверку остатков per-кредит + per-org/Итого с Лист2
</verification>

<success_criteria>
- Реальные кредиты с ПОСТРОЧНЫМИ графиками (Сбер + JetLend) готовы к загрузке (на VPS в Plan 08)
- Контрольные суммы дают ground-truth для UAT сводного графика (Plan 07)
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-04-SUMMARY.md`
</output>
