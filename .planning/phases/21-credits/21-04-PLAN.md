---
phase: 21-credits
plan: 04
type: execute
wave: 2
depends_on: [01]
autonomous: false
files_modified:
  - scripts/seed-credits.ts
  - .planning/phases/21-credits/21-04-SEED-NOTES.md
requirements:
  - D-01  # Разовый seed из Кредиты.xlsx
  - D-07  # issueDate = null при seed
  - D-08  # Bank извлечение из "Сбербанк {Орг}"
  - D-19  # ставка 0.28 → 28.000
must_haves:
  truths:
    - "scripts/seed-credits.ts читает Кредиты.xlsx, создаёт записи Bank (upsert по name) + Loan + LoanPayment[]"
    - "Орг из Excel мапятся на существующие Company: Пеликан→ПЕЛИКАН ХЭППИ ТОЙС, Зойтен→ЗОЙТЕН, Сикрет Вэй→СИКРЕТ ВЭЙ, Дрим Лайн→ДРИМ ЛАЙН"
    - "Ставка 0.28 в Excel → 28.000 в БД (×100)"
    - "Срок «24 месяца» → termMonths 24 (parse int)"
    - "issueDate = null для всех seed-кредитов (D-07)"
    - "Идемпотентность: повторный запуск не дублирует (delete-and-reseed Loan)"
    - "Per-org суммы principal/interest из seed совпадают с Лист2 per-org + Итого (verification checkpoint)"
  artifacts:
    - path: "scripts/seed-credits.ts"
      provides: "Разовый seed-скрипт импорта ~25 кредитов из Excel"
      contains: "Кредиты.xlsx"
    - path: ".planning/phases/21-credits/21-04-SEED-NOTES.md"
      provides: "Маппинг колонок Excel + per-org контрольные суммы + результат сверки с Лист2"
  key_links:
    - from: "scripts/seed-credits.ts"
      to: "prisma.loan + prisma.loanPayment"
      via: "create nested"
      pattern: "loanPayment"
    - from: "scripts/seed-credits.ts"
      to: "Company (существующие записи)"
      via: "findUnique по name (mapped)"
      pattern: "company\\.findUnique|companyMap"
---

<objective>
Создать разовый seed-скрипт `scripts/seed-credits.ts` для импорта текущих ~25 кредитов из `Кредиты.xlsx` (D-01). Скрипт читает Лист2 (метаданные + помесячные тело+проценты) и Лист1 (дневной график), создаёт Bank + Loan + LoanPayment. Завершается checkpoint'ом сверки per-org/Итого сумм с Лист2.

Purpose: Наполнить базу реальными данными без UI-парсера (формат нестабильный). Контрольная точка — суммы из seed должны совпадать с Лист2 per-org подытогами + Итого (ground-truth verification).

Output: scripts/seed-credits.ts + 21-04-SEED-NOTES.md с маппингом и сверкой.

**ВАЖНО про данные:** `Кредиты.xlsx` лежит в `C:\Users\User\zoiten-pro\Кредиты.xlsx` (untracked, не в репо), cp1251-подобная кодировка. Dev-машина — **Windows**, поэтому `python3`/`openpyxl` могут быть НЕДОСТУПНЫ. Скрипт должен иметь рабочий путь и на Windows: проект уже использует npm-пакет **`xlsx` (^0.18.5)** для парсинга Excel (см. `app/api/wb-commission-iu/route.ts` → `import * as XLSX from "xlsx"`). Скрипт запускается ОДИН раз вручную (`npx tsx scripts/seed-credits.ts`); на VPS — после deploy в Plan 21-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@prisma/schema.prisma
@app/api/wb-commission-iu/route.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Разобрать Кредиты.xlsx — зафиксировать структуру + контрольные суммы</name>
  <read_first>
    - .planning/phases/21-credits/21-CONTEXT.md (canonical_refs «Источник данных» + specifics «Из анализа Кредиты.xlsx»)
    - app/api/wb-commission-iu/route.ts (паттерн чтения Excel через npm `xlsx`: XLSX.read / sheet_to_json)
  </read_first>
  <files>.planning/phases/21-credits/21-04-SEED-NOTES.md</files>
  <action>
    **Шаг 0 — проверка доступности парсера (ОБЯЗАТЕЛЬНО):** dev-машина Windows. Сначала проверить наличие Python+openpyxl, и если недоступно — переключиться на Node-путь (`xlsx` уже в проекте). НЕ полагаться только на Python.

    ```powershell
    # Проба Python (Windows-launcher py, затем python3):
    py -c "import openpyxl; print('openpyxl OK')" ; if ($LASTEXITCODE -ne 0) { python3 -c "import openpyxl; print('openpyxl OK')" }
    ```
    Если ОБЕ команды падают (нет python/openpyxl) → **использовать Node-путь** (предпочтительно на Windows; проект уже зависит от `xlsx`).

    **Путь A — Python openpyxl (если доступен):**
    ```bash
    py -c "
    import openpyxl, json
    wb = openpyxl.load_workbook(r'C:\Users\User\zoiten-pro\Кредиты.xlsx', data_only=True)
    print('SHEETS:', wb.sheetnames)
    for sh in wb.sheetnames:
        ws = wb[sh]
        print(f'--- {sh}: {ws.max_row} rows x {ws.max_column} cols ---')
        for r in range(1, min(ws.max_row, 12)+1):
            row = [ws.cell(r,c).value for c in range(1, min(ws.max_column, 20)+1)]
            print(r, row)
    "
    ```

    **Путь B — Node `xlsx` (Windows fallback, ИСПОЛЬЗОВАТЬ если Python недоступен):**
    ```bash
    npx tsx -e "import * as XLSX from 'xlsx'; const wb = XLSX.readFile('C:/Users/User/zoiten-pro/Кредиты.xlsx'); console.log('SHEETS:', wb.SheetNames); for (const sh of wb.SheetNames) { const rows = XLSX.utils.sheet_to_json(wb.Sheets[sh], { header: 1, defval: null }); console.log('---', sh, rows.length, 'rows ---'); rows.slice(0, 12).forEach((r, i) => console.log(i+1, JSON.stringify(r))); }"
    ```
    (`xlsx.readFile` сам справляется с кодировкой Excel; на Windows прямые слэши в пути работают.)

    Зафиксировать в `21-04-SEED-NOTES.md`:
    - **Каким путём (A/B) прочитан файл** — записать явно (для воспроизводимости).
    - Имена листов (Лист1 / Лист2 или фактические).
    - **Лист2 структура:** какие колонки = № КД / орг / банк («Сбербанк {Орг}») / сумма / ставка (0.xx) / срок («N месяцев»); где помесячные основной долг + проценты; где per-org подытоги и Итого.
    - **Лист1 структура:** дневной график тела долга, группировка колонок по орг, секции «Новые кредиты» / «Балансы».
    - **Контрольные суммы из Лист2** (КРИТИЧНО для verification): per-org Σ основной долг, per-org Σ проценты, общий Итого основной долг, общий Итого проценты. **Выписать КОНКРЕТНЫЕ ЧИСЛА — НЕ «TBD».**
    - Маппинг орг → Company.name (Пеликан→ПЕЛИКАН ХЭППИ ТОЙС и т.д.).
    - Список банков (вероятно «Сбербанк»; проверить № КД форматы 3702… / 8639… / 23xxx — указывают ли на разные банки).

    Если файл недоступен/повреждён → записать blocker и поднять на checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f .planning/phases/21-credits/21-04-SEED-NOTES.md && grep -qi "итого\|per-org\|контрольн" .planning/phases/21-credits/21-04-SEED-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - 21-04-SEED-NOTES.md существует
    - явно указан использованный путь чтения (Python openpyxl ИЛИ Node `xlsx`); если Python недоступен на Windows — executor обязан переключиться на Node-путь
    - содержит имена листов и структуру колонок Лист2 (№ КД / сумма / ставка / срок / банк / орг)
    - содержит **конкретные** контрольные суммы per-org + Итого (реальные числа из Лист2, НЕ «TBD»)
    - содержит маппинг орг → Company.name
  </acceptance_criteria>
  <done>Структура Excel + конкретные контрольные суммы зафиксированы в SEED-NOTES.md (прочитано через доступный путь — Python или Node xlsx)</done>
</task>

<task type="auto">
  <name>Task 2: scripts/seed-credits.ts</name>
  <read_first>
    - .planning/phases/21-credits/21-04-SEED-NOTES.md (структура из Task 1)
    - prisma/schema.prisma (модели Loan/LoanPayment/Bank/Company)
    - app/api/wb-commission-iu/route.ts (паттерн чтения Excel через npm `xlsx` — XLSX.readFile / sheet_to_json)
    - scripts/ (любой существующий seed-скрипт как паттерн — напр. prisma/seed-wb-warehouses.ts если есть, для стиля PrismaClient + tsx)
    - app/actions/credits.ts (если уже создан Plan 03 — переиспользовать round/parse логику; иначе inline)
  </read_first>
  <files>scripts/seed-credits.ts</files>
  <action>
    Создать `scripts/seed-credits.ts` — standalone Node/tsx скрипт с `new PrismaClient()`.

    **Чтение Excel — приоритет Node `xlsx` (Windows-совместимо):** использовать `import * as XLSX from "xlsx"` + `XLSX.readFile("C:/Users/User/zoiten-pro/Кредиты.xlsx")` + `XLSX.utils.sheet_to_json(ws, { header: 1, defval: null })` (паттерн из `app/api/wb-commission-iu/route.ts`). Это работает на Windows без Python. (Если в Task 1 executor подтвердил, что доступен только Python — допускается child_process python openpyxl → JSON в temp, но это НЕ default на Windows.)

    Шаги:
    1. Прочитать `Кредиты.xlsx` через `xlsx` (см. выше). НЕ требовать Python.
    2. **Bank upsert:** для каждого уникального банка `prisma.bank.upsert({where:{name}, create:{name, sortOrder}, update:{}})`. Извлечь имя банка из «Сбербанк {Орг}» (отрезать орг-суффикс) → «Сбербанк».
    3. **Company lookup:** `prisma.company.findUnique({where:{name: MAPPED}})` по маппингу (Пеликан→ПЕЛИКАН ХЭППИ ТОЙС...). Если не найдена — warn + skip кредит (не падать).
    4. **Идемпотентность:** в начале `prisma.loan.deleteMany({})` (Loan cascade удалит LoanPayment) — чистая переинициализация. Bank не трогаем — upsert. Допустимо т.к. seed разовый и кредиты только из Excel на этом этапе.
    5. **Для каждого кредита (Лист2):**
       - parse: contractNumber (№ КД), amount, annualRatePct = `rateRaw * 100` (0.28→28.000), termMonths = parseInt из «N месяцев» или null, companyId (из lookup), bankId (из upsert), issueDate = **null** (D-07), notes = null.
       - Собрать `payments`: из помесячных строк Лист2 (date + principal=основной долг + interest=проценты). Использовать помесячные Лист2 как primary (там и principal, и interest) — выбрать источник, который воспроизводит контрольные суммы Лист2.
       - `prisma.loan.create({ data: { ...скаляры, payments: { create: payments } } })`.
    6. В конце вывести в консоль: кол-во созданных Loan, суммарный Σprincipal и Σinterest per-org + Итого — для сверки с Лист2.

    parse чисел: учесть запятую как десятичный разделитель и пробелы-разделители тысяч (ru формат) → нормализовать в number. Даты Excel → JS Date (через `XLSX.SSF` или `cellDates: true` при чтении).

    Комментарий вверху файла: «Разовый seed (D-01). Запуск: npx tsx scripts/seed-credits.ts. Чтение Excel через npm `xlsx` (Windows-совместимо, без Python). НЕ часть deploy.sh — запускается вручную один раз после миграции.»
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f scripts/seed-credits.ts && grep -q "PrismaClient" scripts/seed-credits.ts && grep -q "loanPayment\|payments:" scripts/seed-credits.ts && grep -qi "Кредиты.xlsx" scripts/seed-credits.ts && grep -q "xlsx" scripts/seed-credits.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-credits.ts существует, использует PrismaClient
    - читает Кредиты.xlsx через npm `xlsx` (Windows-совместимо, без зависимости от Python/openpyxl)
    - Bank upsert по name (идемпотентно)
    - Company lookup по mapped name
    - ставка ×100 (0.28→28.000) присутствует в коде
    - termMonths через parseInt из текста «N месяцев»
    - issueDate = null
    - payments создаются nested
    - выводит per-org + Итого суммы в консоль для сверки
    - `npx tsc --noEmit` без ошибок в seed-credits.ts
  </acceptance_criteria>
  <done>Seed-скрипт парсит Excel через `xlsx`, создаёт Bank/Loan/LoanPayment идемпотентно, печатает контрольные суммы</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Сверка seed-сумм с Лист2 Excel</name>
  <files>.planning/phases/21-credits/21-04-SEED-NOTES.md</files>
  <action>
    HUMAN-VERIFY: пользователь сверяет выведенные seed-скриптом per-org/Итого суммы с контрольными суммами Лист2 из 21-04-SEED-NOTES.md. Агент запускает seed (или dry-run печать сумм если локальной PG нет) и предъявляет числа на сравнение.
  </action>
  <what-built>seed-credits.ts парсит Кредиты.xlsx (через npm `xlsx`) и печатает per-org Σprincipal/Σinterest + Итого; SEED-NOTES.md содержит конкретные контрольные суммы из Лист2</what-built>
  <how-to-verify>
    1. Запустить `npx tsx scripts/seed-credits.ts` (после применения миграции Plan 01 локально ИЛИ это выполнится на VPS в Plan 08 — на этом этапе достаточно dry-run печати сумм без записи, если локальной PG нет).
    2. Сравнить выведенные per-org Σ основной долг и Σ проценты + Итого с контрольными суммами из 21-04-SEED-NOTES.md (взятыми из Лист2).
    3. Если совпадает (с точностью до копеек / округления) → ввести "approved".
    4. Если расходится → описать расхождение (какая орг, principal или interest, на сколько) — вероятно ошибка маппинга колонок или источника (Лист1 vs Лист2); скорректировать парсер.
  </how-to-verify>
  <resume-signal>Type "approved" если суммы совпадают с Лист2, или опиши расхождение</resume-signal>
  <verify>
    <automated>test -f .planning/phases/21-credits/21-04-SEED-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - User подтвердил совпадение seed-сумм per-org/Итого с Лист2 Excel
  </acceptance_criteria>
  <done>User подтвердил что seed-суммы per-org/Итого совпадают с Лист2 Excel</done>
</task>

</tasks>

<verification>
- 21-04-SEED-NOTES.md содержит структуру + конкретные контрольные суммы (не TBD)
- seed-credits.ts читает Excel через `xlsx` (Windows-совместимо), идемпотентен, tsc проходит
- User подтвердил сверку per-org/Итого с Лист2
</verification>

<success_criteria>
- Реальные ~25 кредитов готовы к загрузке (на VPS в Plan 08)
- Контрольные суммы дают ground-truth для UAT сводного графика (Plan 07)
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-04-SUMMARY.md`
</output>
</content>
