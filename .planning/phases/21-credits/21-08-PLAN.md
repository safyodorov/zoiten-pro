---
phase: 21-credits
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
autonomous: false
files_modified:
  - components/settings/BanksTab.tsx
  - components/settings/SettingsTabs.tsx
  - app/(dashboard)/admin/settings/page.tsx
  - .planning/phases/21-credits/21-UAT.md
  - .planning/phases/21-credits/21-DEPLOY-NOTES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
requirements:
  - D-08  # Bank CRUD UI в /admin/settings
  - All Phase 21 — deploy + seed run + UAT
must_haves:
  truths:
    - "Раздел Bank управляется в /admin/settings (таб «Банки») — CRUD через actions из Plan 03"
    - "21-DEPLOY-NOTES.md описывает: deploy.sh (prisma migrate deploy применит CREDITS enum + 3 таблицы) + ОДНОРАЗОВЫЙ запуск npx tsx scripts/seed-credits.ts на VPS"
    - "21-UAT.md содержит 12+ пунктов чек-листа (3 UI-поверхности + сверка итогов с Лист2)"
    - "REQUIREMENTS.md обновлён: добавлен раздел Phase 21 Credits с decision IDs D-01..D-19 в Traceability"
    - "ROADMAP.md Phase 21 entry: Goal заполнен, count plans=8, plans list заполнен"
    - "Checkpoint: после deploy + seed юзер проходит UAT, в т.ч. сверку per-org/Итого сводного графика с Лист2"
  artifacts:
    - path: ".planning/phases/21-credits/21-UAT.md"
      provides: "Human UAT checklist 12+ пунктов"
      min_lines: 40
    - path: ".planning/phases/21-credits/21-DEPLOY-NOTES.md"
      provides: "Deploy + seed-run шаги"
    - path: "components/settings/BanksTab.tsx"
      provides: "CRUD Bank в настройках"
      contains: "createBank"
  key_links:
    - from: "components/settings/BanksTab.tsx"
      to: "app/actions/bank.ts"
      via: "server actions"
      pattern: "createBank|updateBank|deleteBank"
    - from: ".planning/phases/21-credits/21-DEPLOY-NOTES.md"
      to: "scripts/seed-credits.ts"
      via: "одноразовый запуск на VPS"
      pattern: "seed-credits"
---

<objective>
Финальный план Phase 21: UI управления справочником Bank в /admin/settings, документация (UAT + DEPLOY notes), обновление REQUIREMENTS/ROADMAP, deploy на VPS + одноразовый запуск seed, checkpoint UAT с пользователем.

Purpose: Bank CRUD из Plan 03 нужен UI вход. Без deploy + seed раздел пустой на проде. Без UAT нельзя подтвердить production readiness и корректность сводных итогов (сверка с Лист2). Без обновления REQUIREMENTS traceability ломается.

Output: 3 правки settings + 2 doc файла + правки REQUIREMENTS/ROADMAP + deploy + checkpoint UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@components/settings/BrandsTab.tsx
@components/settings/SettingsTabs.tsx
@app/actions/bank.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BanksTab + SettingsTabs интеграция</name>
  <read_first>
    - components/settings/BrandsTab.tsx (паттерн reference CRUD таба — create/edit/delete/reorder)
    - components/settings/SettingsTabs.tsx (как добавить новый таб)
    - app/(dashboard)/admin/settings/page.tsx (как грузятся данные табов + requireSuperadmin)
    - app/actions/bank.ts (createBank/updateBank/deleteBank/reorderBanks)
  </read_first>
  <files>
    components/settings/BanksTab.tsx
    components/settings/SettingsTabs.tsx
    app/(dashboard)/admin/settings/page.tsx
  </files>
  <action>
    **BanksTab.tsx** — паттерн BrandsTab (простой reference CRUD; решение D-08: простой CRUD без обязательного DnD, но sortOrder используется при выводе; reorder опционален — если BrandsTab имеет SortableList, переиспользовать, иначе простой список с inline edit + add + delete).
    - props: banks: {id, name, sortOrder}[].
    - add (CreatableCombobox/input + createBank), inline rename (updateBank), delete (deleteBank — покажет ошибку если есть связанные кредиты, FK Restrict).
    - опционально reorder через reorderBanks (если паттерн SortableList уже есть в settings).

    **SettingsTabs.tsx** — добавить таб «Банки» в список табов (после существующих reference-табов).

    **app/(dashboard)/admin/settings/page.tsx** — загрузить `prisma.bank.findMany({ orderBy: { sortOrder: "asc" } })`, передать в BanksTab через SettingsTabs.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f components/settings/BanksTab.tsx && grep -q "createBank\|updateBank\|deleteBank" components/settings/BanksTab.tsx && grep -qi "банк" components/settings/SettingsTabs.tsx && grep -q "bank.findMany" "app/(dashboard)/admin/settings/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - BanksTab использует bank actions (create/update/delete)
    - SettingsTabs содержит таб «Банки»
    - settings/page.tsx грузит banks через prisma.bank.findMany orderBy sortOrder
    - npx tsc --noEmit без ошибок
  </acceptance_criteria>
  <done>Bank CRUD доступен в /admin/settings</done>
</task>

<task type="auto">
  <name>Task 2: 21-UAT.md + 21-DEPLOY-NOTES.md + REQUIREMENTS + ROADMAP</name>
  <read_first>
    - .planning/phases/20-procurement/20-08-PLAN.md (паттерн UAT + DEPLOY notes структура)
    - .planning/REQUIREMENTS.md (формат раздела + Traceability таблица)
    - .planning/ROADMAP.md (Phase 21 entry ~line 464 — Goal + Plans placeholders)
    - .planning/phases/21-credits/21-04-SEED-NOTES.md (контрольные суммы Лист2 для UAT-сверки)
  </read_first>
  <files>
    .planning/phases/21-credits/21-UAT.md
    .planning/phases/21-credits/21-DEPLOY-NOTES.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
    **21-DEPLOY-NOTES.md:**
    - Шаг 1: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — deploy.sh выполнит `prisma migrate deploy` → применит миграцию 20260609_phase21_credits (ALTER TYPE ERP_SECTION ADD VALUE CREDITS + 3 таблицы).
    - Шаг 2 (одноразово): скопировать Кредиты.xlsx на VPS (или запустить seed локально против прод-БД через туннель — указать выбранный способ); затем `cd /opt/zoiten-pro && npx tsx scripts/seed-credits.ts`. НЕ часть deploy.sh.
    - Шаг 3: smoke — открыть https://zoiten.pro/credits.
    - Напоминание RBAC: получателю прав CREDITS через /admin/users нужен logout/login (JWT не самообновляется).

    **21-UAT.md** — 14 пунктов:
    (a) /credits открывается без ошибок, список показывает ~25 кредитов
    (b) колонки списка: орг/банк/№КД/сумма/ставка/срок/дата выдачи/остаток/статус
    (c) фильтры орг/банк/статус работают
    (d) кнопка «Добавить кредит» → модалка create → новый кредит появляется
    (e) edit кредита (включая правку строк графика) сохраняется
    (f) удаление кредита (soft delete) — пропадает из списка
    (g) клик по кредиту → /credits/[id], summary cards корректны (остаток = сумма − Σтело)
    (h) таблица графика показывает накопительный остаток; line-chart рисует кривую
    (i) /credits/schedule открывается, левый блок sticky при горизонтальном скролле
    (j) переключение день/неделя/месяц меняет период-колонки
    (k) каждый кредит = 2 строки (тело+проценты); per-org подытоги + Итого видны
    (l) **СВЕРКА с Лист2:** при месячной разбивке per-org Σ тело/проценты + Итого совпадают с Кредиты.xlsx Лист2 (контрольные суммы из 21-04-SEED-NOTES.md)
    (m) Bank CRUD в /admin/settings работает; удаление банка с кредитами блокируется
    (n) Sidebar показывает «Кредиты» (Landmark); Header заголовки корректны

    **REQUIREMENTS.md** — добавить раздел «## Phase 21 Requirements — Кредиты» с маппингом decision IDs (D-01..D-19) на реализацию + строки в Traceability таблицу (D-01..D-19 → Phase 21 → Complete).

    **ROADMAP.md** — Phase 21 entry: заполнить Goal (из CONTEXT domain), `**Plans:** 8 plans`, plans list:
    ```
    Plans:
    - [ ] 21-01-PLAN.md — Schema + миграция (Loan/LoanPayment/Bank + ERP_SECTION.CREDITS)
    - [ ] 21-02-PLAN.md — Проводка: sections.ts + section-titles + nav-items + RBAC
    - [ ] 21-03-PLAN.md — lib/loan-math + server actions (credits + bank)
    - [ ] 21-04-PLAN.md — Seed-скрипт из Кредиты.xlsx + сверка с Лист2
    - [ ] 21-05-PLAN.md — Список кредитов + фильтры + LoanModal CRUD
    - [ ] 21-06-PLAN.md — Детальная карточка (summary cards + график + line-chart)
    - [ ] 21-07-PLAN.md — Сводный горизонтальный график (день/неделя/месяц, по орг)
    - [ ] 21-08-PLAN.md — Bank settings + deploy + seed + UAT
    ```
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f .planning/phases/21-credits/21-UAT.md && test -f .planning/phases/21-credits/21-DEPLOY-NOTES.md && grep -q "seed-credits" .planning/phases/21-credits/21-DEPLOY-NOTES.md && grep -qi "Лист2\|лист 2" .planning/phases/21-credits/21-UAT.md && grep -q "Phase 21" .planning/REQUIREMENTS.md && grep -q "21-07-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 21-DEPLOY-NOTES.md содержит deploy.sh шаг + одноразовый seed-credits запуск + RBAC logout/login напоминание
    - 21-UAT.md содержит 12+ пунктов включая сверку с Лист2 (пункт l)
    - REQUIREMENTS.md содержит раздел Phase 21 + Traceability строки D-01..D-19
    - ROADMAP.md Phase 21: Goal заполнен (не placeholder), 8 plans, plans list
  </acceptance_criteria>
  <done>Документация UAT/DEPLOY + REQUIREMENTS/ROADMAP обновлены</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy + seed run + UAT на проде</name>
  <files>.planning/phases/21-credits/21-UAT.md</files>
  <action>
    Агент выполняет deploy на VPS + одноразовый seed (per 21-DEPLOY-NOTES.md), затем пользователь проходит UAT-чеклист 21-UAT.md (14 пунктов), включая ключевую сверку сводного графика с Лист2. Deploy полностью делегирован (не спрашивать подтверждения — memory feedback_deploy_delegation).
  </action>
  <what-built>Полный раздел /credits: список + карточка + сводный график; seed ~25 кредитов; Bank CRUD; nav/RBAC проводка</what-built>
  <how-to-verify>
    1. Агент выполняет deploy: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`.
    2. Агент выполняет одноразовый seed на VPS per 21-DEPLOY-NOTES.md (скопировать Кредиты.xlsx + npx tsx scripts/seed-credits.ts), фиксирует выведенные per-org/Итого суммы.
    3. Пользователь проходит 21-UAT.md (14 пунктов a..n) на https://zoiten.pro/credits.
    4. КЛЮЧЕВОЙ пункт (l): сводный график месячная разбивка — per-org Σ + Итого совпадают с Кредиты.xlsx Лист2.
    5. Если всё ок → "approved". Если есть проблемы → описать (какой пункт, что не так) → gap-closure план.
  </how-to-verify>
  <resume-signal>Type "approved" если все UAT-пункты пройдены, или опиши проблемы</resume-signal>
  <verify>
    <automated>test -f .planning/phases/21-credits/21-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Deploy выполнен, seed загрузил ~25 кредитов
    - Пользователь подтвердил UAT включая сверку сводного графика с Лист2
  </acceptance_criteria>
  <done>Deploy выполнен, seed загружен, пользователь подтвердил UAT включая сверку с Лист2</done>
</task>

</tasks>

<verification>
- Bank CRUD работает в /admin/settings
- deploy.sh применил миграцию; seed загрузил ~25 кредитов
- Все 3 UI-поверхности работают на проде
- Сводные итоги совпадают с Лист2
- REQUIREMENTS/ROADMAP обновлены
</verification>

<success_criteria>
- Phase 21 production-ready, подтверждён UAT
- Traceability D-01..D-19 зафиксирована
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-08-SUMMARY.md`
</output>
