---
phase: 21-credits
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
autonomous: false
files_modified:
  - components/settings/LendersTab.tsx
  - components/settings/SettingsTabs.tsx
  - app/(dashboard)/admin/settings/page.tsx
  - .planning/phases/21-credits/21-UAT.md
  - .planning/phases/21-credits/21-DEPLOY-NOTES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
requirements:
  - D-08  # Lender CRUD UI в /admin/settings (U-03)
  - All Phase 21 — deploy + seed run + UAT
must_haves:
  truths:
    - "Справочник Lender управляется в /admin/settings (таб «Кредиторы») — CRUD через actions из Plan 03 (U-03)"
    - "21-DEPLOY-NOTES.md описывает: deploy.sh (prisma migrate deploy применит CREDITS enum + 3 таблицы) + установку poppler-utils + доставку файлов папки Кредиты/ + ОДНОРАЗОВЫЙ запуск npx tsx scripts/seed-credits.ts"
    - "21-UAT.md содержит 14 пунктов чек-листа (3 UI-поверхности + сверка итогов с Лист2 с конкретными числами из SEED-NOTES)"
    - "REQUIREMENTS.md обновлён: добавлен раздел Phase 21 Credits с decision IDs D-01..D-19 + U-01..U-05 в Traceability"
    - "ROADMAP.md Phase 21 entry: Goal заполнен, count plans=8, plans list заполнен; отмечено переименование Bank→Lender и источник папки Кредиты/"
    - "Checkpoint: после deploy + seed юзер проходит UAT, в т.ч. сверку per-org/Итого сводного графика с Лист2"
  artifacts:
    - path: ".planning/phases/21-credits/21-UAT.md"
      provides: "Human UAT checklist 14 пунктов"
      min_lines: 40
    - path: ".planning/phases/21-credits/21-DEPLOY-NOTES.md"
      provides: "Deploy + poppler + доставка файлов + seed-run шаги"
    - path: "components/settings/LendersTab.tsx"
      provides: "CRUD Lender в настройках"
      contains: "createLender"
  key_links:
    - from: "components/settings/LendersTab.tsx"
      to: "app/actions/lender.ts"
      via: "server actions"
      pattern: "createLender|updateLender|deleteLender"
    - from: ".planning/phases/21-credits/21-DEPLOY-NOTES.md"
      to: "scripts/seed-credits.ts"
      via: "одноразовый запуск на VPS"
      pattern: "seed-credits"
---

<objective>
Финальный план Phase 21: UI управления справочником Lender («Кредиторы») в /admin/settings, документация (UAT + DEPLOY notes), обновление REQUIREMENTS/ROADMAP, deploy на VPS (с poppler-utils + доставкой файлов папки Кредиты/) + одноразовый запуск seed, checkpoint UAT с пользователем.

Purpose: Lender CRUD из Plan 03 нужен UI вход. Без deploy + seed раздел пустой на проде. Seed парсит JetLend PDF через pdftotext — на VPS нужен poppler-utils + файлы папки Кредиты/. Без UAT нельзя подтвердить production readiness и корректность сводных итогов (сверка с Лист2). Без обновления REQUIREMENTS traceability ломается.

Output: 3 правки settings + 2 doc файла + правки REQUIREMENTS/ROADMAP + deploy + checkpoint UAT.

**⚠ U-03:** таб называется «Кредиторы» (компонент LendersTab, НЕ BanksTab); использует `prisma.lender` + actions из app/actions/lender.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@components/settings/BrandsTab.tsx
@components/settings/SettingsTabs.tsx
@app/actions/lender.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LendersTab + SettingsTabs интеграция</name>
  <read_first>
    - components/settings/BrandsTab.tsx (паттерн reference CRUD таба — create/edit/delete/reorder)
    - components/settings/SettingsTabs.tsx (как добавить новый таб)
    - app/(dashboard)/admin/settings/page.tsx (как грузятся данные табов + requireSuperadmin)
    - app/actions/lender.ts (createLender/updateLender/deleteLender/reorderLenders)
  </read_first>
  <files>
    components/settings/LendersTab.tsx
    components/settings/SettingsTabs.tsx
    app/(dashboard)/admin/settings/page.tsx
  </files>
  <action>
    **LendersTab.tsx** — паттерн BrandsTab (простой reference CRUD; решение D-08: простой CRUD без обязательного DnD, но sortOrder используется при выводе; reorder опционален — если BrandsTab имеет SortableList, переиспользовать, иначе простой список с inline edit + add + delete).
    - props: lenders: {id, name, sortOrder}[].
    - add (CreatableCombobox/input + createLender), inline rename (updateLender), delete (deleteLender — покажет ошибку «Нельзя удалить кредитора с кредитами» если есть связанные кредиты, FK Restrict).
    - опционально reorder через reorderLenders (если паттерн SortableList уже есть в settings).

    **SettingsTabs.tsx** — добавить таб «Кредиторы» в список табов (после существующих reference-табов).

    **app/(dashboard)/admin/settings/page.tsx** — загрузить `prisma.lender.findMany({ orderBy: { sortOrder: "asc" } })`, передать в LendersTab через SettingsTabs.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f components/settings/LendersTab.tsx && grep -q "createLender\|updateLender\|deleteLender" components/settings/LendersTab.tsx && grep -qi "кредитор" components/settings/SettingsTabs.tsx && grep -q "lender.findMany" "app/(dashboard)/admin/settings/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - LendersTab использует lender actions (createLender/updateLender/deleteLender)
    - SettingsTabs содержит таб «Кредиторы» (НЕ «Банки», U-03)
    - settings/page.tsx грузит lenders через `prisma.lender.findMany` orderBy sortOrder
    - НЕТ упоминаний BanksTab / prisma.bank / createBank
    - npx tsc --noEmit без ошибок
  </acceptance_criteria>
  <done>Lender CRUD доступен в /admin/settings (таб «Кредиторы»)</done>
</task>

<task type="auto">
  <name>Task 2: 21-UAT.md + 21-DEPLOY-NOTES.md + REQUIREMENTS + ROADMAP</name>
  <read_first>
    - .planning/phases/20-procurement/20-08-PLAN.md (паттерн UAT + DEPLOY notes структура)
    - .planning/REQUIREMENTS.md (формат раздела + Traceability таблица)
    - .planning/ROADMAP.md (Phase 21 entry — Goal + Plans placeholders)
    - **.planning/phases/21-credits/21-04-SEED-NOTES.md (ОБЯЗАТЕЛЬНО — конкретные контрольные суммы per-org/Итого из Лист2; UAT-пункт сверки (l) должен цитировать ИМЕННО эти числа, не «TBD»)**
  </read_first>
  <files>
    .planning/phases/21-credits/21-UAT.md
    .planning/phases/21-credits/21-DEPLOY-NOTES.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
    **21-DEPLOY-NOTES.md:**
    - Шаг 1: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — deploy.sh выполнит `prisma migrate deploy` → применит миграцию 20260609_phase21_credits (ALTER TYPE ERP_SECTION ADD VALUE CREDITS + 3 таблицы Lender/Loan/LoanPayment).
    - Шаг 2 (предусловия seed на VPS):
      - **poppler-utils** (для pdftotext, которым парсятся JetLend PDF): `ssh root@85.198.97.89 "apt-get install -y poppler-utils"` (проверить `pdftotext -v`).
      - **Доставка untracked-файлов** (папка `Кредиты/` + `Кредиты.xlsx` НЕ в репозитории): скопировать на VPS через scp, напр. `scp -r "C:/Users/User/zoiten-pro/Кредиты" "C:/Users/User/zoiten-pro/Кредиты.xlsx" root@85.198.97.89:/opt/zoiten-pro/`. **Зафиксировать выбранный способ запуска seed** (вариант A или B):
        - **Вариант A (рекомендуется):** seed гонится ЛОКАЛЬНО (Windows, где файлы и pdftotext уже есть) против БД прода — задать `DATABASE_URL` прода (через SSH-туннель к Postgres или прямой коннект, если разрешён) и `npx tsx scripts/seed-credits.ts`. Не требует копирования файлов и установки poppler на VPS.
        - **Вариант B:** скопировать `Кредиты/` + `Кредиты.xlsx` на VPS + `apt-get install -y poppler-utils` + `cd /opt/zoiten-pro && npx tsx scripts/seed-credits.ts` на VPS.
      - В любом случае seed НЕ часть deploy.sh.
    - Шаг 3: smoke — открыть https://zoiten.pro/credits.
    - Напоминание RBAC: получателю прав CREDITS через /admin/users нужен logout/login (JWT не самообновляется). Также: до миграции на VPS `requireSection("CREDITS")` отдаёт 403 — нормально, активируется после migrate deploy (см. Plan 21-02 каведат).

    **21-UAT.md** — 14 пунктов:
    (a) /credits открывается без ошибок, список показывает кредиты (Сбербанк + JetLend)
    (b) колонки списка: орг/**кредитор**/№КД/сумма/ставка/срок/дата выдачи/остаток/статус
    (c) фильтры орг/кредитор/статус работают
    (d) кнопка «Добавить кредит» → модалка create (с выбором кредитора) → новый кредит появляется
    (e) edit кредита (включая правку строк графика) сохраняется
    (f) удаление кредита (soft delete) — пропадает из списка
    (g) клик по кредиту → /credits/[id], summary cards корректны (остаток = сумма − Σтело), кредитор показан
    (h) таблица графика показывает накопительный остаток; line-chart рисует кривую
    (i) /credits/schedule открывается, левый блок sticky при горизонтальном скролле, показывает кредитора
    (j) переключение день/неделя/месяц меняет период-колонки
    (k) каждый кредит = 2 строки (тело+проценты); per-org подытоги + Итого видны
    (l) **СВЕРКА с Лист2:** при месячной разбивке per-org Σ тело/проценты + Итого совпадают с Кредиты.xlsx Лист2. **Вписать в чек-лист конкретные ожидаемые числа per-org + Итого из 21-04-SEED-NOTES.md** (чтобы проверяющий сверял с фактическими, не «TBD»). Дополнительно: накопленный остаток каждого кредита == сумма кредита (U-04).
    (m) Lender CRUD в /admin/settings (таб «Кредиторы») работает; удаление кредитора с кредитами блокируется
    (n) Sidebar показывает «Кредиты» (Landmark); Header заголовки корректны

    **REQUIREMENTS.md** — добавить раздел «## Phase 21 Requirements — Кредиты» с маппингом decision IDs (D-01..D-19) + вводных U-01..U-05 (источник папки Кредиты/, переименование Bank→Lender) на реализацию + строки в Traceability таблицу (D-01..D-19 → Phase 21 → Complete).

    **ROADMAP.md** — Phase 21 entry: заполнить Goal (из CONTEXT domain; отметить переименование «Банк→Кредитор» (Lender) и источник детальных графиков из папки `Кредиты/`), `**Plans:** 8 plans`, plans list:
    ```
    Plans:
    - [ ] 21-01-PLAN.md — Schema + миграция (Loan/LoanPayment/Lender + ERP_SECTION.CREDITS)
    - [ ] 21-02-PLAN.md — Проводка: sections.ts + section-titles + nav-items + RBAC
    - [ ] 21-03-PLAN.md — lib/loan-math + vitest + server actions (credits + lender)
    - [ ] 21-04-PLAN.md — Seed из папки Кредиты/ (Сбер XLSX + JetLend PDF) + сверка с Лист2
    - [ ] 21-05-PLAN.md — Список кредитов + фильтры (орг/кредитор/статус) + LoanModal CRUD
    - [ ] 21-06-PLAN.md — Детальная карточка (summary cards + график + line-chart)
    - [ ] 21-07-PLAN.md — Сводный горизонтальный график (день/неделя/месяц, по орг)
    - [ ] 21-08-PLAN.md — Lender settings + deploy (poppler) + seed + UAT
    ```
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && test -f .planning/phases/21-credits/21-UAT.md && test -f .planning/phases/21-credits/21-DEPLOY-NOTES.md && grep -q "seed-credits" .planning/phases/21-credits/21-DEPLOY-NOTES.md && grep -qi "poppler" .planning/phases/21-credits/21-DEPLOY-NOTES.md && grep -qi "Лист2\|лист 2" .planning/phases/21-credits/21-UAT.md && grep -q "Phase 21" .planning/REQUIREMENTS.md && grep -q "21-07-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 21-DEPLOY-NOTES.md содержит deploy.sh шаг + установку poppler-utils + доставку untracked-файлов папки Кредиты/ + один из вариантов A/B запуска seed + RBAC logout/login напоминание
    - 21-UAT.md содержит 14 пунктов включая сверку с Лист2 (пункт l) с КОНКРЕТНЫМИ ожидаемыми числами из 21-04-SEED-NOTES.md (не «TBD») + проверку остаток-per-кредит==amount (U-04)
    - UAT термины «кредитор» (НЕ «банк»), таб «Кредиторы»
    - REQUIREMENTS.md содержит раздел Phase 21 + Traceability строки D-01..D-19 (+ упоминание U-01..U-05)
    - ROADMAP.md Phase 21: Goal заполнен (не placeholder, с пометкой Bank→Lender и источник папки), 8 plans, plans list
  </acceptance_criteria>
  <done>Документация UAT/DEPLOY (с poppler + доставкой файлов) + REQUIREMENTS/ROADMAP обновлены; UAT-сверка цитирует контрольные суммы из SEED-NOTES</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy + seed run + UAT на проде</name>
  <files>.planning/phases/21-credits/21-UAT.md</files>
  <action>
    Агент выполняет deploy на VPS + одноразовый seed (per 21-DEPLOY-NOTES.md — включая poppler-utils + доставку файлов папки Кредиты/), затем пользователь проходит UAT-чеклист 21-UAT.md (14 пунктов), включая ключевую сверку сводного графика с Лист2. Deploy полностью делегирован (не спрашивать подтверждения — memory feedback_deploy_delegation).
  </action>
  <what-built>Полный раздел /credits: список + карточка + сводный график; seed кредитов (Сбер XLSX + JetLend PDF); Lender CRUD; nav/RBAC проводка</what-built>
  <how-to-verify>
    1. Агент выполняет deploy: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`.
    2. Агент готовит окружение для seed per 21-DEPLOY-NOTES.md (poppler-utils + доставка `Кредиты/` + `Кредиты.xlsx`, либо локальный запуск против прод-БД), затем `npx tsx scripts/seed-credits.ts`, фиксирует выведенные per-org/Итого суммы + остаток-per-кредит.
    3. Пользователь проходит 21-UAT.md (14 пунктов a..n) на https://zoiten.pro/credits.
    4. КЛЮЧЕВОЙ пункт (l): сводный график месячная разбивка — per-org Σ + Итого совпадают с Кредиты.xlsx Лист2 (контрольные суммы из 21-04-SEED-NOTES.md); остаток каждого кредита == сумма кредита.
    5. Если всё ок → "approved". Если есть проблемы → описать (какой пункт, что не так) → gap-closure план.
  </how-to-verify>
  <resume-signal>Type "approved" если все UAT-пункты пройдены, или опиши проблемы</resume-signal>
  <verify>
    <automated>test -f .planning/phases/21-credits/21-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Deploy выполнен, seed загрузил кредиты (Сбер + JetLend) с построчными графиками
    - Пользователь подтвердил UAT включая сверку сводного графика с Лист2 + остаток-per-кредит==amount
  </acceptance_criteria>
  <done>Deploy выполнен, seed загружен, пользователь подтвердил UAT включая сверку с Лист2</done>
</task>

</tasks>

<verification>
- Lender CRUD работает в /admin/settings (таб «Кредиторы»)
- deploy.sh применил миграцию; poppler установлен/доступен; seed загрузил кредиты (Сбер + JetLend)
- Все 3 UI-поверхности работают на проде
- Сводные итоги совпадают с Лист2; остатки per-кредит сходятся
- REQUIREMENTS/ROADMAP обновлены
</verification>

<success_criteria>
- Phase 21 production-ready, подтверждён UAT
- Traceability D-01..D-19 + U-01..U-05 зафиксирована
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-08-SUMMARY.md`
</output>
