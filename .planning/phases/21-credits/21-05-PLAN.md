---
phase: 21-credits
plan: 05
type: execute
wave: 3
depends_on: [02, 03]
autonomous: true
files_modified:
  - lib/credits-data.ts
  - components/credits/CreditsTabs.tsx
  - components/credits/CreditsTable.tsx
  - components/credits/CreditsFilters.tsx
  - components/credits/LoanModal.tsx
  - app/(dashboard)/credits/page.tsx
requirements:
  - D-04  # computed агрегаты (остаток)
  - D-09  # computed статус
  - D-11  # RBAC requireSection CREDITS / MANAGE
  - D-12  # список кредитов sticky + фильтры + клик→карточка
  - D-19  # ru-RU формат
must_haves:
  truths:
    - "GET /credits рендерит список кредитов (RSC) с колонками: организация, КРЕДИТОР, № КД, сумма, ставка %, срок, дата выдачи, текущий остаток, статус"
    - "Таблица sticky (raw HTML <table border-separate>, НЕ shadcn <Table>) с sticky thead"
    - "Текущий остаток вычислен через computeLoanAggregates; статус через computeStatus (active=sky, paid=emerald)"
    - "Дата выдачи fallback на дату первого платежа если issueDate null (D-07, display-only)"
    - "Фильтры: организация / кредитор / статус (MultiSelectDropdown / select)"
    - "Клик по строке → переход на /credits/[id]"
    - "Кнопка «Добавить кредит» + LoanModal (create/edit) вызывает createLoan/updateLoan; удаление через deleteLoan"
    - "CreditsTabs показывает «Список | Сводный график» (/credits | /credits/schedule)"
    - "Данные кредитора берутся из loan.lender.name (НЕ loan.bank), термин в UI «Кредитор» (U-03)"
  artifacts:
    - path: "app/(dashboard)/credits/page.tsx"
      provides: "RSC страница списка кредитов"
      contains: "requireSection(\"CREDITS\")"
    - path: "components/credits/CreditsTable.tsx"
      provides: "Sticky таблица кредитов + клик строки"
      contains: "border-separate"
    - path: "components/credits/LoanModal.tsx"
      provides: "Форма create/edit кредита + nested график"
      contains: "createLoan"
    - path: "lib/credits-data.ts"
      provides: "Загрузка кредитов + агрегаты + сортировка"
      contains: "computeLoanAggregates"
  key_links:
    - from: "app/(dashboard)/credits/page.tsx"
      to: "lib/credits-data.ts"
      via: "loadCredits()"
      pattern: "loadCredits|credits-data"
    - from: "components/credits/CreditsTable.tsx"
      to: "/credits/[id]"
      via: "router.push / Link prefetch=false"
      pattern: "/credits/"
    - from: "components/credits/LoanModal.tsx"
      to: "app/actions/credits.ts"
      via: "server action"
      pattern: "createLoan|updateLoan"
---

<objective>
Реализовать первую UI-поверхность: список кредитов `/credits` (D-12) — RSC sticky-таблица с фильтрами, computed остаток/статус, клик→карточка, и CRUD через LoanModal. Плюс CreditsTabs (Список | Сводный график) и data helper lib/credits-data.ts.

Purpose: Точка входа раздела. Пользователь видит общий обзор и управляет кредитами (D-01 ручной ввод).

Output: 1 data lib + 5 компонентов/страниц.

**⚠ U-03:** колонка/фильтр — «Кредитор» (НЕ «Банк»); данные из `loan.lender.name`. Список кредиторов для фильтра/модалки — из `prisma.lender` (НЕ prisma.bank).

**Claude's Discretion:** сортировка по умолчанию = организация → кредитор → дата выдачи (nulls last); статус active=sky, paid=emerald.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-credits/21-CONTEXT.md
@components/employees/EmployeesTable.tsx
@components/employees/EmployeeModal.tsx
@components/cards/CardsTabs.tsx
@lib/loan-math.ts
@lib/rbac.ts
@app/actions/credits.ts
@CLAUDE.md
</context>

<interfaces>
```typescript
// lib/loan-math.ts (Plan 03)
export function computeLoanAggregates(amount: number, payments: PaymentInput[]): LoanAggregates // {currentBalance, ...}
export function computeStatus(currentBalance: number): "active" | "paid"

// app/actions/credits.ts (Plan 03)
createLoan(input): Promise<CreateResult>      // input.lenderId
updateLoan(input): Promise<ActionResult>      // input.lenderId
deleteLoan(id): Promise<ActionResult>

// lib/rbac.ts (существующий) — для определения прав в RSC БЕЗ try/catch:
export async function getSectionRole(section: string): Promise<"VIEW" | "MANAGE" | null>
// SUPERADMIN → "MANAGE"; иначе sectionRoles[section] ?? legacy fallback ?? null
```
Prisma модель (Plan 01): `Loan { lender Lender; lenderId }`, `Lender { id, name, sortOrder }`.
Существующий MultiSelectDropdown: `components/ui/MultiSelectDropdown` (Phase 9). Native <select> для простых dropdown (CLAUDE.md convention). CreatableCombobox для создаваемых (кредитор/орг выбираем из существующих — native select).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: lib/credits-data.ts + page.tsx + CreditsTabs</name>
  <read_first>
    - lib/loan-math.ts (computeLoanAggregates, computeStatus)
    - lib/rbac.ts (getSectionRole — НЕ try/catch для detection прав)
    - components/cards/CardsTabs.tsx (паттерн табов pathname.startsWith)
    - app/(dashboard)/ads/wb/page.tsx ИЛИ employees/page.tsx (паттерн RSC page + requireSection + загрузка + рендер компонентов)
    - .planning/phases/21-credits/21-CONTEXT.md (D-12 колонки, D-07 fallback даты, U-03 термин «Кредитор»)
  </read_first>
  <files>
    lib/credits-data.ts
    components/credits/CreditsTabs.tsx
    app/(dashboard)/credits/page.tsx
  </files>
  <action>
    **lib/credits-data.ts:**
    ```typescript
    import { prisma } from "@/lib/prisma"
    import { computeLoanAggregates, computeStatus, type LoanStatus } from "@/lib/loan-math"

    export interface CreditRow {
      id: string
      contractNumber: string
      companyName: string
      lenderName: string                // U-03: имя кредитора (loan.lender.name)
      amount: number
      annualRatePct: number
      termMonths: number | null
      issueDate: Date | null
      effectiveIssueDate: Date | null  // issueDate ?? первая дата платежа (D-07 display)
      currentBalance: number
      totalPrincipalPaid: number
      totalInterestPaid: number
      status: LoanStatus
    }
    ```
    `loadCredits(): Promise<CreditRow[]>`:
    - `prisma.loan.findMany({ where: { deletedAt: null }, include: { company: true, lender: true, payments: { orderBy: { date: "asc" } } } })`
    - для каждого: `Number(amount)`, payments → `{date, principal: Number, interest: Number}`; `agg = computeLoanAggregates(amount, payments)`; `status = computeStatus(agg.currentBalance)`; `effectiveIssueDate = issueDate ?? payments[0]?.date ?? null`; `lenderName = loan.lender.name`.
    - сортировка: companyName → lenderName → effectiveIssueDate (nulls last) → contractNumber. (Decimal из Prisma → Number().)
    Также экспортировать `loadLendersAndCompanies()` для фильтров и модалки: списки `{id, name}` кредиторов (`prisma.lender.findMany`, orderBy sortOrder) и Company (orderBy name).

    **components/credits/CreditsTabs.tsx:** копия CardsTabs паттерна, TABS = `[{href:"/credits", label:"Список"}, {href:"/credits/schedule", label:"Сводный график"}]`. ВАЖНО: active = `tab.href === "/credits/schedule" ? pathname.startsWith("/credits/schedule") : pathname === "/credits"` (чтобы /credits/schedule не подсвечивал обе вкладки).

    **app/(dashboard)/credits/page.tsx:** RSC.
    - `await requireSection("CREDITS")`
    - `const rows = await loadCredits(); const { lenders, companies } = await loadLendersAndCompanies()`
    - layout: `h-full flex flex-col` → шапка (CreditsTabs + кнопка «Добавить кредит» через LoanModal trigger + CreditsFilters) → `flex-1 min-h-0` контейнер с CreditsTable.
    - передать в CreditsTable отфильтрованные по searchParams строки (фильтрацию делать в page или в client filters — следовать паттерну /stock: фильтры через searchParams, page фильтрует rows перед передачей).
    - **canManage — БЕЗ try/catch (anti-pattern):** использовать существующий `getSectionRole` из `lib/rbac.ts`:
      ```typescript
      import { getSectionRole } from "@/lib/rbac"
      const canManage = (await getSectionRole("CREDITS")) === "MANAGE"
      ```
      (SUPERADMIN автоматически вернёт "MANAGE".) Передать `canManage` + `lenders` + `companies` в CreditsTable/CreditsFilters/LoanModal.
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "computeLoanAggregates" lib/credits-data.ts && grep -q "lender" lib/credits-data.ts && grep -q "requireSection(\"CREDITS\")" "app/(dashboard)/credits/page.tsx" && grep -q "getSectionRole(\"CREDITS\")" "app/(dashboard)/credits/page.tsx" && test -f components/credits/CreditsTabs.tsx && npx tsc --noEmit 2>&1 | grep -c "credits" || true</automated>
  </verify>
  <acceptance_criteria>
    - lib/credits-data.ts экспортирует loadCredits + CreditRow (с полем lenderName) + loadLendersAndCompanies
    - include использует `lender: true` (НЕ bank), `lenderName = loan.lender.name`
    - currentBalance/status вычислены через loan-math
    - effectiveIssueDate = issueDate ?? first payment date (D-07)
    - сортировка company→lender→issueDate
    - page.tsx содержит `requireSection("CREDITS")`
    - **canManage определяется через `(await getSectionRole("CREDITS")) === "MANAGE"`** (НЕ через try/catch requireSection MANAGE)
    - НЕТ упоминаний prisma.bank / loan.bank / bankName
    - CreditsTabs: только одна вкладка active на /credits и на /credits/schedule
    - `npx tsc --noEmit` без ошибок в этих файлах
  </acceptance_criteria>
  <done>Data helper + RSC page + табы готовы; список загружается с агрегатами и именем кредитора; canManage через getSectionRole</done>
</task>

<task type="auto">
  <name>Task 2: CreditsTable + CreditsFilters (sticky + клик строки)</name>
  <read_first>
    - components/employees/EmployeesTable.tsx (sticky таблица + клик строки + фильтры)
    - CLAUDE.md «Sticky data-таблицы (pattern)» (raw HTML table, border-separate, thead bg-background, sticky top-0 z-20)
    - components/products/ProductFilters.tsx (паттерн MultiSelect фильтров через URL searchParams)
    - lib/credits-data.ts (CreditRow shape — из Task 1)
  </read_first>
  <files>
    components/credits/CreditsTable.tsx
    components/credits/CreditsFilters.tsx
  </files>
  <action>
    **CreditsFilters.tsx** (`"use client"`): MultiSelectDropdown по организации + **кредитору** + select по статусу (active/paid/все). Через URL searchParams (router.push). Опции кредиторов/орг приходят props из page (`lenders`, `companies`). Каскад не требуется (орг и кредитор независимы).

    **CreditsTable.tsx** (`"use client"`): sticky-таблица по CLAUDE.md паттерну:
    - `<div className="overflow-auto h-full">` единственный scroll-контейнер
    - `<table className="w-full border-separate border-spacing-0">`
    - `<thead className="bg-background">` + `<tr>` (raw) с `<th>` cells: `sticky top-0 z-20 bg-background border-b`. НЕ использовать shadcn TableHeader/TableRow в шапке.
    - body: `<tbody>` + `<tr>` с `cursor-pointer hover:bg-muted/40 onClick={() => router.push(/credits/${id})}`.
    - Колонки (D-12): Организация, **Кредитор** (lenderName), № КД, Сумма (ru-RU + ₽ справа), Ставка % (`28,0%`), Срок (`24 мес` или «—»), Дата выдачи (effectiveIssueDate, формат ДД.ММ.ГГГГ; если из fallback — пометить курсивом/title «по первому платежу»), Текущий остаток (ru-RU ₽), Статус (бейдж: active→sky «Активен», paid→emerald «Погашён»).
    - формат денег: `n.toLocaleString("ru-RU", {minimumFractionDigits:0, maximumFractionDigits:0}) + " ₽"` (паттерн SpendSummary).
    - Действия: в последней колонке (если canManage) кнопки «Изменить» (открыть LoanModal с данными) и «Удалить» (confirm → deleteLoan) — onClick со stopPropagation чтобы не открывался [id].
    - Кнопка «Добавить кредит» рендерится в page шапке (LoanModal trigger), но Table может содержать edit-trigger.
    - Пустой список: показать дружелюбную заглушку «Кредитов пока нет — добавьте первый» (см. note в success_criteria про seed).
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "border-separate" components/credits/CreditsTable.tsx && grep -q "sticky top-0" components/credits/CreditsTable.tsx && grep -q "/credits/" components/credits/CreditsTable.tsx && grep -qi "кредитор\|lenderName" components/credits/CreditsTable.tsx && test -f components/credits/CreditsFilters.tsx && npx tsc --noEmit 2>&1 | grep -c "CreditsTable\|CreditsFilters" || true</automated>
  </verify>
  <acceptance_criteria>
    - CreditsTable использует `border-separate border-spacing-0` + `<thead className="bg-background">` + sticky `<th>` (НЕ shadcn TableHeader/TableRow в шапке)
    - колонка «Кредитор» отображает lenderName (заголовок «Кредитор», НЕ «Банк», U-03)
    - клик по строке → `/credits/${id}`
    - деньги форматируются через toLocaleString("ru-RU") + ₽ справа
    - статус бейдж: active=sky, paid=emerald
    - дата выдачи fallback помечена (title/курсив) когда из первого платежа
    - CreditsFilters: организация + кредитор + статус через searchParams
    - action-кнопки используют stopPropagation
    - пустой список показывает заглушку
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>Sticky список с фильтрами (орг/кредитор/статус) и кликом на карточку</done>
</task>

<task type="auto">
  <name>Task 3: LoanModal — форма create/edit + nested график</name>
  <read_first>
    - components/employees/EmployeeModal.tsx (multi-section форма + nested useFieldArray + RHF + zodResolver паттерн)
    - app/actions/credits.ts (createLoan/updateLoan сигнатуры + Zod схема, поле lenderId)
    - .planning/phases/21-credits/21-CONTEXT.md (D-05 поля кредита, D-06 поля платежа)
    - CLAUDE.md «Conventions» (native HTML select, RHF + zod 4.x — z.number() + valueAsNumber, НЕ z.coerce)
  </read_first>
  <files>components/credits/LoanModal.tsx</files>
  <action>
    `"use client"` модалка (shadcn Dialog). Props: `mode: "create" | "edit"`, `loan?` (для edit), `lenders`, `companies`, trigger.

    Поля формы (RHF + zodResolver, схема зеркалит LoanSchema из actions):
    - № КД (text), Организация (native <select> из companies), **Кредитор** (native <select> из lenders), Сумма (number input), Годовая ставка % (number, шаг 0.001 — поддержка 28.000), Срок мес (number, nullable — пустое→null), Дата выдачи (date input, nullable — D-07), Заметки (textarea).
    - **Nested график** (useFieldArray «payments»): таблица строк {Дата (date), Тело (number), Проценты (number)} + кнопки «Добавить строку» / удалить строку. Для ручного ввода (D-01). При edit — предзаполнено из loan.payments.
    - КРИТИЧНО (zod 4.x + RHF 7.72): использовать `z.number()` + `valueAsNumber: true` в register, НЕ `z.coerce.number()` (известная несовместимость, см. STATE decisions Phase 7/11). Для nullable числовых — transform пустой строки в null.
    - submit: mode create → `createLoan(values)`, edit → `updateLoan({id, ...values})` (values содержит lenderId). toast success/error. На успех — закрыть + router.refresh().
    - Кнопки сохранения disabled если !canManage (но обычно модалка показывается только при canManage).
    - НЕ реализовывать Excel-импорт в модалке (D-01 deferred).
  </action>
  <verify>
    <automated>cd C:/Users/User/zoiten-pro && grep -q "createLoan\|updateLoan" components/credits/LoanModal.tsx && grep -q "useFieldArray" components/credits/LoanModal.tsx && grep -q "lenderId\|lenders" components/credits/LoanModal.tsx && npx tsc --noEmit 2>&1 | grep -c "LoanModal" || true</automated>
  </verify>
  <acceptance_criteria>
    - LoanModal содержит все поля D-05 (№ КД, орг, кредитор, сумма, ставка, срок, дата выдачи, заметки)
    - селектор «Кредитор» из props `lenders` (native <select>), отправляет lenderId (НЕ bankId)
    - nested график через useFieldArray («payments»: дата/тело/проценты + add/remove)
    - native <select> для орг и кредитор (НЕ base-ui Select)
    - использует z.number() + valueAsNumber (НЕ z.coerce)
    - вызывает createLoan (create) / updateLoan (edit)
    - ставка input поддерживает 3 знака (step 0.001)
    - дата выдачи nullable
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>Модалка create/edit с ручным вводом кредита (включая выбор кредитора) и графика, под RHF+zod</done>
</task>

</tasks>

<verification>
- /credits открывается, показывает список с computed остатком/статусом
- Фильтры орг/кредитор/статус работают
- Клик строки → /credits/[id]
- LoanModal создаёт/редактирует кредит (с выбором кредитора); deleteLoan делает soft delete
- Sticky thead не мерцает при scroll (raw HTML паттерн)
- Термин «Кредитор» (не «Банк») везде в UI (U-03)
</verification>

<success_criteria>
- Первая UI-поверхность (D-12) работает end-to-end
- Plan 06 (карточка) reachable по клику; Plan 07 (сводный график) reachable по табу
- **Данные:** список пуст до запуска seed (Plan 21-04 seed-скрипт выполняется на VPS в Plan 21-08). На этапе Wave 3 (локальная разработка) тестировать UI, создавая 1-2 кредита через сам LoanModal (модалка — легитимная CRUD-точка входа, D-01 ручной ввод). Пустой список на dev до seed — ожидаемо, не дефект.
</success_criteria>

<output>
After completion, create `.planning/phases/21-credits/21-05-SUMMARY.md`
</output>
