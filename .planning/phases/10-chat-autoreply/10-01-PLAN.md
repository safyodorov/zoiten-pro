---
phase: 10-chat-autoreply
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/wb-support-api.ts
  - tests/fixtures/wb-chat-chats-sample.json
  - tests/fixtures/wb-chat-events-sample.json
  - tests/wb-chat-api.test.ts
  - tests/support-sync-chats.test.ts
  - tests/auto-reply-cron.test.ts
  - tests/chat-reply-panel.test.ts
  - tests/auto-reply-settings.test.ts
autonomous: true
requirements:
  - SUP-21
must_haves:
  truths:
    - "prisma/schema.prisma содержит модель AutoReplyConfig (singleton с id=\"default\"), расширения SupportTicket (+2 поля chatReplySign, customerNameSnapshot), SupportMessage (+1 поле wbEventId @unique), enum MediaType расширен DOCUMENT"
    - "lib/wb-support-api.ts экспортирует типы Chat/ChatEvent/ChatMessage/ChatAttachment и 5 методов: pingChat, listChats, getChatEvents, sendChatMessage (multipart), downloadChatAttachment"
    - "getChatToken() читает WB_CHAT_TOKEN с fallback на WB_API_TOKEN (паттерн Phase 9 getReturnsToken); callChatApi использует callApi(CHAT_API, getChatToken(), ...)"
    - "tests/wb-chat-api.test.ts GREEN — покрывает URL/headers/parsing для listChats + getChatEvents (cursor next), multipart FormData для sendChatMessage, 429 retry с X-Ratelimit-Retry, 401/403 русские ошибки"
    - "tests/fixtures содержат canonical JSON образцы /chats и /events из 10-RESEARCH.md"
    - "4 Wave 0 stub-файла (support-sync-chats, auto-reply-cron, chat-reply-panel, auto-reply-settings) существуют как it.skip placeholders"
    - "npm run test 0 failed — Phase 7/8/9 regression не сломан рефакторингом callApi"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "AutoReplyConfig + 3 поля в existing моделях + DOCUMENT в enum MediaType"
      contains: "model AutoReplyConfig"
    - path: "lib/wb-support-api.ts"
      provides: "WB Buyer Chat API клиент (5 методов + типы + 3-й токен)"
      exports:
        - pingChat
        - listChats
        - getChatEvents
        - sendChatMessage
        - downloadChatAttachment
    - path: "tests/wb-chat-api.test.ts"
      provides: "Unit-тесты клиента (mock fetch + FormData assertions)"
      contains: "describe"
    - path: "tests/fixtures/wb-chat-chats-sample.json"
      provides: "Canonical /chats response для reuse"
      contains: "chatID"
    - path: "tests/fixtures/wb-chat-events-sample.json"
      provides: "Canonical /events response с next cursor"
      contains: "eventID"
    - path: "tests/support-sync-chats.test.ts"
      provides: "Wave 0 stub для Plan 10-02"
      contains: "it.skip"
    - path: "tests/auto-reply-cron.test.ts"
      provides: "Wave 0 stub для Plan 10-02"
      contains: "it.skip"
    - path: "tests/chat-reply-panel.test.ts"
      provides: "Wave 0 stub для Plan 10-03"
      contains: "it.skip"
    - path: "tests/auto-reply-settings.test.ts"
      provides: "Wave 0 stub для Plan 10-04"
      contains: "it.skip"
  key_links:
    - from: "lib/wb-support-api.ts listChats / getChatEvents / sendChatMessage"
      to: "https://buyer-chat-api.wildberries.ru"
      via: "callChatApi → callApi(CHAT_API, getChatToken(), ...)"
      pattern: "buyer-chat-api\\.wildberries\\.ru"
    - from: "prisma/schema.prisma AutoReplyConfig"
      to: "User (updatedBy)"
      via: "@relation(\"AutoReplyUpdater\")"
      pattern: "AutoReplyUpdater"
    - from: "prisma/schema.prisma SupportMessage.wbEventId"
      to: "@unique constraint"
      via: "идемпотентный upsert для Plan 10-02 syncChats"
      pattern: "wbEventId\\s+String\\?\\s+@unique"
---

<objective>
Phase 10 Plan 01 — foundation для чата + автоответов:

1. **Prisma миграция** `phase10_chat_autoreply`:
   - Новая модель `AutoReplyConfig` (singleton — id = `"default"`), 7 полей + relation на User.
   - Расширение `SupportTicket`: `chatReplySign: String?`, `customerNameSnapshot: String?`.
   - Расширение `SupportMessage`: `wbEventId: String? @unique` (для идемпотентного cursor-based sync).
   - Расширение enum `MediaType`: добавить `DOCUMENT` (для PDF из chat attachments).
   - Relation User: `autoReplyUpdates  AutoReplyConfig[]  @relation("AutoReplyUpdater")`.
   - Seed в миграции: `INSERT INTO "AutoReplyConfig" ... ON CONFLICT DO NOTHING`.

2. **Расширение `lib/wb-support-api.ts`** — WB Buyer Chat API клиент (5 методов через существующий `callApi` wrapper):
   - Константа `CHAT_API = "https://buyer-chat-api.wildberries.ru"`.
   - `getChatToken()` — читает `WB_CHAT_TOKEN`, fallback на `WB_API_TOKEN`.
   - `callChatApi(path, init, attempt = 0)` — обёртка над `callApi(CHAT_API, getChatToken(), ...)`.
   - Типы: `ChatGoodCard`, `ChatLastMessage`, `Chat`, `ChatAttachmentImage`, `ChatAttachmentFile`, `ChatAttachments`, `ChatMessage`, `ChatEvent`, `ChatEventsResult`.
   - Методы: `pingChat()`, `listChats()`, `getChatEvents(next?)`, `sendChatMessage({replySign, message, files})`, `downloadChatAttachment(downloadId)`.
   - 403 scope hint в `callApi`: расширить branch для `buyer-chat-api` → `bit 9 Buyers chat (WB_CHAT_TOKEN)`.

3. **Wave 0 deliverables** (все MUST exist + npm run test passes):
   - `tests/wb-chat-api.test.ts` GREEN — ~10 тестов (URL/headers, parsing listChats, getChatEvents cursor, sendChatMessage multipart FormData, 429 retry, 401/403 ошибки).
   - `tests/fixtures/wb-chat-chats-sample.json` + `wb-chat-events-sample.json` — canonical образцы из 10-RESEARCH.md §3-4.
   - 4 stub-файла (`support-sync-chats`, `auto-reply-cron`, `chat-reply-panel`, `auto-reply-settings`) с `it.skip` placeholders.

Purpose: Database schema + API client — foundation для Plan 10-02 (sync + autoreply), 10-03 (UI multipart), 10-04 (settings). Client изолирован в lib/ через unit-тесты против mock fetch (паттерн Phase 8/9 `callApi(baseUrl, token, ...)`). Wave 0 гарантирует Nyquist compliance для downstream планов.

Output: Обновлённая `prisma/schema.prisma` + миграция `<ts>_phase10_chat_autoreply/`, расширенный `lib/wb-support-api.ts`, 5 новых test-файлов, 2 JSON fixtures.

**⚠ VPS pre-flight (BLOCKER для Plan 10-04 deploy, НЕ блокирует Plan 10-01):**
Добавить `WB_CHAT_TOKEN` в `/etc/zoiten.pro.env` перед execute-phase (scope bit 9 "Чат с покупателями" в seller.wildberries.ru). Без токена sync/autoreply работать не будут, но код компилируется и тесты проходят (используется test-token в vitest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-chat-autoreply/10-RESEARCH.md
@.planning/phases/10-chat-autoreply/10-VALIDATION.md
@.planning/phases/08-support-mvp/08-01-SUMMARY.md
@.planning/phases/09-returns/09-01-SUMMARY.md
@CLAUDE.md
@prisma/schema.prisma
@lib/wb-support-api.ts
@lib/prisma.ts
@lib/rbac.ts
@tests/wb-support-api.test.ts
@tests/wb-returns-api.test.ts

<interfaces>
**Существующие (Phase 8 + Phase 9) в `prisma/schema.prisma`:**
```prisma
enum TicketChannel { FEEDBACK  QUESTION  CHAT  RETURN  MESSENGER }  // CHAT уже есть!
enum MediaType    { IMAGE  VIDEO }  // Phase 10 расширит DOCUMENT

model SupportTicket {
  id            String           @id @default(cuid())
  channel       TicketChannel
  wbExternalId  String?
  nmId          Int?
  status        TicketStatus     @default(NEW)
  // Phase 9 ...wbClaimStatus / wbActions / returnState / ... и пр.
  @@unique([channel, wbExternalId])
  // Phase 10 добавит: chatReplySign String?  customerNameSnapshot String?
}

model SupportMessage {
  id          String         @id @default(cuid())
  ticketId    String
  ticket      SupportTicket  @relation(...)
  direction   Direction
  text        String?        @db.Text
  authorId    String?
  isAutoReply Boolean        @default(false)  // уже есть в Phase 8!
  wbSentAt    DateTime?
  // Phase 10 добавит: wbEventId String? @unique
}

model User {
  id                  String              @id @default(cuid())
  assignedTickets     SupportTicket[]     @relation("SupportAssignee")
  authoredMessages    SupportMessage[]    @relation("SupportAuthor")
  returnDecisions     ReturnDecision[]    @relation("ReturnDecider")
  // Phase 10 добавит: autoReplyUpdates  AutoReplyConfig[]  @relation("AutoReplyUpdater")
}
```

**Существующие в `lib/wb-support-api.ts` (Phase 8 + Phase 9):**
```typescript
const FEEDBACKS_API = "https://feedbacks-api.wildberries.ru"
const RETURNS_API   = "https://returns-api.wildberries.ru"
const RATE_LIMIT_FALLBACK_MS = 6000

function getToken(): string                // WB_API_TOKEN (bit 5/7)
function getReturnsToken(): string         // WB_RETURNS_TOKEN, fallback WB_API_TOKEN (bit 11)

async function callApi(baseUrl, token, path, init, attempt = 0): Promise<Response>
async function callWb(path, init, attempt = 0): Promise<Response>           // FEEDBACKS_API
async function callReturnsApi(path, init, attempt = 0): Promise<Response>   // RETURNS_API

// В callApi 403 branch (Phase 9):
// const scopeHint = baseUrl.includes("returns-api") ? "bit 11 Buyers Returns (WB_RETURNS_TOKEN)" : "bit 5"
// throw new Error(`Нет доступа — проверьте scope токена (${scopeHint})`)
// Phase 10 РАСШИРИТ branch для buyer-chat-api.

export async function listFeedbacks, listQuestions, replyFeedback, editFeedbackAnswer, replyQuestion
export async function listReturns, approveReturn, rejectReturn, reconsiderReturn
```

**WB Buyer Chat API (VERIFIED в 10-RESEARCH.md §2-7):**
- Base: `https://buyer-chat-api.wildberries.ru`
- Auth: `Authorization: ${WB_CHAT_TOKEN}` (без Bearer)
- Rate limit: 10 req / 10 sec + 10 burst / sec → 1000ms пауза между batches, 429 retry через `X-Ratelimit-Retry` header fallback 6000ms
- Endpoints (5):
  - `GET /ping` — health (200 = scope OK)
  - `GET /api/v1/seller/chats` → `{ result: Chat[], errors: null }`
  - `GET /api/v1/seller/events?next={ms}` → `{ result: {next, events, totalEvents, newestEventTime, oldestEventTime}, errors: null }`
  - `POST /api/v1/seller/message` — multipart/form-data с polями `replySign`, `message?`, `file*` (array; JPEG/PNG/PDF; ≤5MB each; ≤30MB total)
  - `GET /api/v1/seller/download/{id}` — бинарь файла (требует auth header)

**Canonical /chats response (из 10-RESEARCH.md §3):**
```json
{
  "result": [
    {
      "chatID": "9e1b3f80-...uuid-string",
      "replySign": "base64signature==",
      "clientName": "Иван П.",
      "goodCard": { "nmID": 123456789, "price": 1990, "size": "M" },
      "lastMessage": { "text": "Здравствуйте, где инструкция?", "addTimestamp": 1713355200 }
    }
  ],
  "errors": null
}
```

**Canonical /events response (из 10-RESEARCH.md §4):**
```json
{
  "result": {
    "next": 1713355200123,
    "newestEventTime": "2026-04-17T12:00:00Z",
    "oldestEventTime": "2026-04-14T08:30:00Z",
    "totalEvents": 42,
    "events": [
      {
        "chatID": "9e1b3f80-...",
        "eventID": "evt-abc123",
        "eventType": "message",
        "isNewChat": false,
        "message": {
          "text": "Добрый день",
          "attachments": {
            "goodCard": { "nmID": 123456789, "price": 1990, "size": "M" },
            "files":  [ { "downloadID": "file-xyz", "fileName": "doc.pdf", "fileSize": 12345 } ],
            "images": [ { "downloadID": "img-abc", "fileName": "photo.jpg", "width": 800, "height": 600 } ]
          }
        },
        "addTimestamp": 1713355123456,
        "sender": "client",
        "clientName": "Иван П."
      }
    ]
  },
  "errors": null
}
```

**Vitest pattern (из `tests/wb-returns-api.test.ts` Phase 9):**
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
beforeEach(() => {
  process.env.WB_API_TOKEN = "test-token"
  process.env.WB_CHAT_TOKEN = "test-chat-token"
  vi.stubGlobal("fetch", vi.fn())
  vi.resetModules()
})
afterEach(() => { vi.unstubAllGlobals() })
function mockResponse(body, status = 200, headers = {}): Response { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: Prisma миграция — AutoReplyConfig + 3 поля в existing моделях + DOCUMENT в MediaType</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (ПОЛНАЯ — чтобы найти блоки `model User`, `model SupportTicket`, `model SupportMessage`, `enum MediaType` и не сломать Phase 8/9 relations)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «Data Model» строки 342-410 — финальная схема AutoReplyConfig, seed SQL)
    - .planning/phases/09-returns/09-01-PLAN.md (паттерн расширения SupportTicket 8 nullable-полями без breaking changes)
    - CLAUDE.md (раздел «Связи между таблицами БД» — onDelete convention, именованные relations)
  </read_first>
  <action>
    Открыть `prisma/schema.prisma` и внести 4 точечные изменения без ломки Phase 7/8/9.

    **1. Расширить `enum MediaType`** — найти блок и добавить DOCUMENT:
    ```prisma
    enum MediaType {
      IMAGE
      VIDEO
      DOCUMENT   // Phase 10 — PDF из chat attachments.files[]
    }
    ```

    **2. Расширить `model SupportTicket`** — найти блок и добавить **2 новых поля** (перед `@@unique([channel, wbExternalId])`, рядом с Phase 9 полями):
    ```prisma
      // ── Phase 10: CHAT канал ──
      chatReplySign         String?   // replySign из WB Chat API для sendMessage (ротируется, обновляем при каждом sync)
      customerNameSnapshot  String?   // clientName из WB (Customer-линковка — Phase 12)
    ```

    **3. Расширить `model SupportMessage`** — найти блок и добавить поле перед закрывающей `}`:
    ```prisma
      // ── Phase 10: CHAT канал ──
      wbEventId   String?   @unique  // eventID из WB Chat events для идемпотентного upsert. Nullable — Phase 8/9 legacy messages не имеют.
    ```

    **4. Добавить `model AutoReplyConfig` в конец файла** (после `ReturnDecision`):
    ```prisma
    // ──────────────────────────────────────────────────────────────
    // Phase 10: Чат + Автоответы
    // ──────────────────────────────────────────────────────────────

    // Singleton — id = "default". Локальная feature ERP (WB API не имеет endpoint для автоответов).
    // Cron /api/cron/support-sync-chat (5 мин) читает конфиг и отправляет OUTBOUND сообщения с isAutoReply=true
    // вне рабочих часов (workDays + workdayStart/End по timezone).
    model AutoReplyConfig {
      id            String    @id                                           // всегда "default"
      isEnabled     Boolean   @default(false)
      workdayStart  String    @default("09:00")                              // "HH:MM" в timezone
      workdayEnd    String    @default("18:00")                              // "HH:MM" в timezone
      workDays      Int[]     @default([1, 2, 3, 4, 5])                      // ISO 8601: 1=Mon, 7=Sun
      messageText   String    @db.Text @default("Здравствуйте, {имя_покупателя}! Спасибо за обращение по товару «{название_товара}». Мы ответим в рабочее время.")
      timezone      String    @default("Europe/Moscow")
      updatedById   String?
      updatedBy     User?     @relation("AutoReplyUpdater", fields: [updatedById], references: [id], onDelete: SetNull)
      updatedAt     DateTime  @updatedAt
    }
    ```

    **5. Расширить `model User`** — найти блок и **после** существующей `returnDecisions  ReturnDecision[] @relation("ReturnDecider")` добавить:
    ```prisma
      autoReplyUpdates  AutoReplyConfig[]  @relation("AutoReplyUpdater")  // Phase 10 — кто последний редактировал автоответ
    ```

    **6. Запустить форматирование + валидацию:**
    ```bash
    npx prisma format
    npx prisma validate
    ```

    **7. Генерировать миграцию SQL вручную** (локальной БД нет — паттерн Phase 1/8/9):
    Создать файл `prisma/migrations/<yyyymmddhhmmss>_phase10_chat_autoreply/migration.sql` c содержимым:

    ```sql
    -- Phase 10: Chat + AutoReply

    -- 1. Enum MediaType расширение
    ALTER TYPE "MediaType" ADD VALUE 'DOCUMENT';

    -- 2. SupportTicket: 2 новых поля (nullable — backward compat)
    ALTER TABLE "SupportTicket" ADD COLUMN "chatReplySign" TEXT;
    ALTER TABLE "SupportTicket" ADD COLUMN "customerNameSnapshot" TEXT;

    -- 3. SupportMessage: wbEventId + unique index
    ALTER TABLE "SupportMessage" ADD COLUMN "wbEventId" TEXT;
    CREATE UNIQUE INDEX "SupportMessage_wbEventId_key" ON "SupportMessage"("wbEventId");

    -- 4. AutoReplyConfig singleton
    CREATE TABLE "AutoReplyConfig" (
      "id"            TEXT NOT NULL,
      "isEnabled"     BOOLEAN NOT NULL DEFAULT false,
      "workdayStart"  TEXT NOT NULL DEFAULT '09:00',
      "workdayEnd"    TEXT NOT NULL DEFAULT '18:00',
      "workDays"      INTEGER[] DEFAULT ARRAY[1,2,3,4,5]::INTEGER[],
      "messageText"   TEXT NOT NULL DEFAULT 'Здравствуйте, {имя_покупателя}! Спасибо за обращение по товару «{название_товара}». Мы ответим в рабочее время.',
      "timezone"      TEXT NOT NULL DEFAULT 'Europe/Moscow',
      "updatedById"   TEXT,
      "updatedAt"     TIMESTAMP(3) NOT NULL,
      CONSTRAINT "AutoReplyConfig_pkey" PRIMARY KEY ("id")
    );

    ALTER TABLE "AutoReplyConfig" ADD CONSTRAINT "AutoReplyConfig_updatedById_fkey"
      FOREIGN KEY ("updatedById") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE;

    -- 5. Seed singleton row (ON CONFLICT DO NOTHING — идемпотентно)
    INSERT INTO "AutoReplyConfig" ("id", "isEnabled", "workdayStart", "workdayEnd", "workDays", "messageText", "timezone", "updatedAt")
    VALUES (
      'default',
      false,
      '09:00',
      '18:00',
      ARRAY[1,2,3,4,5]::INTEGER[],
      'Здравствуйте, {имя_покупателя}! Спасибо за обращение по товару «{название_товара}». Мы ответим в рабочее время.',
      'Europe/Moscow',
      NOW()
    )
    ON CONFLICT ("id") DO NOTHING;
    ```

    Применение `prisma migrate deploy` — в Plan 10-04 deploy-задаче на VPS.

    **⚠ КРИТИЧНО:**
    - НЕ трогать Phase 8 модели SupportTicket/SupportMessage кроме добавления новых nullable-полей (backward compat).
    - НЕ удалять existing значения enum MediaType (IMAGE, VIDEO) — только добавить DOCUMENT.
    - Проверить, что `npx prisma generate` успешен (Plan 10-02/03/04 используют типы).
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma format --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model AutoReplyConfig\b" prisma/schema.prisma` == 1
    - `grep "chatReplySign\s*String?" prisma/schema.prisma` находит строку
    - `grep "customerNameSnapshot\s*String?" prisma/schema.prisma` находит строку
    - `grep "wbEventId\s*String?\s*@unique" prisma/schema.prisma` находит строку
    - `grep "DOCUMENT" prisma/schema.prisma` находит строку (в enum MediaType)
    - `grep -c "AutoReplyUpdater" prisma/schema.prisma` >= 2 (forward + reverse)
    - `grep "id = \"default\"" prisma/schema.prisma` НЕ находит (это комментарий концепции, не код)
    - `grep '@@unique(\[channel, wbExternalId\])' prisma/schema.prisma` всё ещё находит (Phase 8 не сломали)
    - `grep "ReturnDecider" prisma/schema.prisma | wc -l` >= 2 (Phase 9 не сломали)
    - `grep -c "model SupportTicket" prisma/schema.prisma` == 1
    - `grep -c "model SupportMessage" prisma/schema.prisma` == 1
    - `ls prisma/migrations/ | grep phase10_chat_autoreply` exit 0
    - `grep "CREATE TABLE \"AutoReplyConfig\"" prisma/migrations/*phase10_chat_autoreply/migration.sql` находит строку
    - `grep "INSERT INTO \"AutoReplyConfig\"" prisma/migrations/*phase10_chat_autoreply/migration.sql` находит строку
    - `grep "ADD VALUE 'DOCUMENT'" prisma/migrations/*phase10_chat_autoreply/migration.sql` находит строку
    - `npx prisma validate` exit 0
    - `npx prisma format --check` exit 0
  </acceptance_criteria>
  <done>
    prisma/schema.prisma содержит AutoReplyConfig, 2 поля в SupportTicket, 1 поле в SupportMessage с @unique, DOCUMENT в enum MediaType, relation AutoReplyUpdater в User; миграция `<ts>_phase10_chat_autoreply/migration.sql` создана с ALTER TABLE + CREATE TABLE + seed INSERT ON CONFLICT; `npx prisma validate` и `npx prisma format --check` pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/wb-support-api.ts — WB Buyer Chat API клиент (5 методов + типы) + fixtures + GREEN test</name>
  <files>lib/wb-support-api.ts, tests/wb-chat-api.test.ts, tests/fixtures/wb-chat-chats-sample.json, tests/fixtures/wb-chat-events-sample.json</files>
  <read_first>
    - lib/wb-support-api.ts (существующий — паттерн callApi(baseUrl, token, ...), getReturnsToken fallback)
    - tests/wb-returns-api.test.ts (Phase 9 паттерн vitest mock fetch + env token)
    - tests/wb-support-api.test.ts (Phase 8 паттерн mockResponse helper)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (разделы «WB Buyer Chat API» §2-8, «Multipart Upload», «Token Architecture»)
    - .planning/phases/10-chat-autoreply/10-VALIDATION.md (раздел «Wave 0 Requirements»)
    - CLAUDE.md (раздел «WB API токен» — Authorization без Bearer)
  </read_first>
  <behavior>
    - `getChatToken()` возвращает `WB_CHAT_TOKEN ?? WB_API_TOKEN` (паттерн getReturnsToken Phase 9); throw если оба не заданы
    - `callChatApi(path, init, attempt = 0)` делегирует `callApi(CHAT_API, getChatToken(), path, init, attempt)`
    - `callApi` 403 branch расширен: `baseUrl.includes("buyer-chat-api")` → `"bit 9 Buyers chat (WB_CHAT_TOKEN)"`
    - `pingChat()` → GET `/ping` → return `{ ok: true }` при 200
    - `listChats()` → GET `/api/v1/seller/chats` → extract `json.result` → return `Chat[]`
    - `getChatEvents(next?)` → GET `/api/v1/seller/events?next={ms}` (без query если next undefined) → return `{ events: ChatEvent[], next: number, totalEvents: number }`
    - `sendChatMessage({replySign, message?, files?})`:
      - Build `FormData()`: append `replySign`, `message` (если передан), каждый файл как `file`
      - POST multipart — **НЕ** ставить Content-Type (fetch/FormData сам выставляет `multipart/form-data; boundary=...`)
      - **Примечание по реализации:** callApi ставит header `Content-Type: application/json` — для multipart нужен отдельный путь. Добавить в callChatApi специальный helper `callChatApiMultipart(path, formData)` ИЛИ передавать `contentType: false` в init. Рекомендую: в callApi проверять `init.body instanceof FormData` → пропускать JSON Content-Type header.
    - `downloadChatAttachment(downloadId)` → GET `/api/v1/seller/download/{id}` → return `Buffer` (через `Buffer.from(await res.arrayBuffer())`)
    - 429 с `X-Ratelimit-Retry` → sleep(retry*1000) → retry 1 раз; без header → sleep(6000ms) → retry 1 раз (существующий callApi)
    - 401 → русская ошибка «Неверный токен WB API»
    - 403 от `buyer-chat-api` → «Нет доступа — проверьте scope токена (bit 9 Buyers chat (WB_CHAT_TOKEN))»
    - TypeScript types: `ChatGoodCard`, `Chat`, `ChatLastMessage`, `ChatAttachmentImage`, `ChatAttachmentFile`, `ChatAttachments`, `ChatMessage`, `ChatEvent`, `ChatEventsResult`, `SendChatMessageInput`
  </behavior>
  <action>
    **Шаг A — расширить `lib/wb-support-api.ts`:**

    1. Добавить константу рядом с `FEEDBACKS_API`/`RETURNS_API`:
    ```typescript
    const CHAT_API = "https://buyer-chat-api.wildberries.ru"
    ```

    2. Добавить third token-helper после `getReturnsToken`:
    ```typescript
    // Токен Phase 10 (Buyers Chat) — scope bit 9 "Чат с покупателями".
    // Fallback на WB_API_TOKEN для dev/test (паттерн Phase 9 getReturnsToken).
    function getChatToken(): string {
      const token = process.env.WB_CHAT_TOKEN ?? process.env.WB_API_TOKEN
      if (!token) throw new Error("WB_CHAT_TOKEN или WB_API_TOKEN не настроен")
      return token
    }
    ```

    3. **Модифицировать `callApi`** — две точечные правки:
       a) В 403 branch — расширить scopeHint:
       ```typescript
       if (res.status === 403) {
         const scopeHint = baseUrl.includes("returns-api")
           ? "bit 11 Buyers Returns (WB_RETURNS_TOKEN)"
           : baseUrl.includes("buyer-chat-api")
             ? "bit 9 Buyers chat (WB_CHAT_TOKEN)"
             : "bit 5"
         throw new Error(`Нет доступа — проверьте scope токена (${scopeHint})`)
       }
       ```
       b) В headers merge — не добавлять `Content-Type: application/json` если body=FormData (fetch сам поставит boundary):
       ```typescript
       const isFormData = init.body instanceof FormData
       const res = await fetch(`${baseUrl}${path}`, {
         ...init,
         headers: {
           Authorization: token,
           ...(isFormData ? {} : { "Content-Type": "application/json" }),
           ...(init.headers ?? {}),
         },
       })
       ```
       **КРИТИЧНО:** существующие Phase 8/9 вызовы callApi используют `JSON.stringify` — они НЕ FormData, поведение не меняется.

    4. Добавить wrapper после `callReturnsApi`:
    ```typescript
    // Chat API — использует WB_CHAT_TOKEN (scope bit 9 Buyers chat)
    async function callChatApi(path: string, init: RequestInit, attempt = 0): Promise<Response> {
      return callApi(CHAT_API, getChatToken(), path, init, attempt)
    }
    ```

    5. Добавить Chat типы и методы в конец файла:
    ```typescript
    // ── WB Buyer Chat API (Phase 10) ─────────────────────────────

    export interface ChatGoodCard {
      nmID: number
      price?: number
      size?: string
    }

    export interface ChatLastMessage {
      text: string
      addTimestamp: number   // Unix seconds
    }

    export interface Chat {
      chatID: string           // UUID — используем как SupportTicket.wbExternalId
      replySign: string        // обязательно для sendMessage; храним в SupportTicket.chatReplySign
      clientName: string
      goodCard?: ChatGoodCard
      lastMessage?: ChatLastMessage
    }

    export interface ChatAttachmentImage {
      downloadID: string
      fileName: string
      width?: number
      height?: number
    }

    export interface ChatAttachmentFile {
      downloadID: string
      fileName: string
      fileSize?: number
    }

    export interface ChatAttachments {
      goodCard?: ChatGoodCard
      images?: ChatAttachmentImage[]
      files?: ChatAttachmentFile[]
    }

    export interface ChatMessage {
      text?: string
      attachments?: ChatAttachments
    }

    export interface ChatEvent {
      chatID: string
      eventID: string          // уникален глобально — используем как SupportMessage.wbEventId
      eventType: string        // "message"
      isNewChat: boolean
      message?: ChatMessage
      addTimestamp: number     // Unix ms
      sender: "client" | "seller"
      clientName?: string
    }

    export interface ChatEventsResult {
      events: ChatEvent[]
      next: number
      totalEvents: number
      newestEventTime?: string
      oldestEventTime?: string
    }

    export interface SendChatMessageInput {
      replySign: string        // ≤255 символов
      message?: string         // ≤1000 символов
      files?: Array<{ name: string; data: Buffer | Blob; contentType: string }>
    }

    // ── Chat API methods ─────────────────────────────────────────

    export async function pingChat(): Promise<{ ok: true }> {
      await callChatApi("/ping", { method: "GET" })
      return { ok: true }
    }

    export async function listChats(): Promise<Chat[]> {
      const res = await callChatApi("/api/v1/seller/chats", { method: "GET" })
      const json = (await res.json()) as { result?: Chat[]; errors?: unknown }
      return json.result ?? []
    }

    export async function getChatEvents(next?: number): Promise<ChatEventsResult> {
      const path = next !== undefined
        ? `/api/v1/seller/events?next=${encodeURIComponent(String(next))}`
        : "/api/v1/seller/events"
      const res = await callChatApi(path, { method: "GET" })
      const json = (await res.json()) as {
        result?: {
          events?: ChatEvent[]
          next?: number
          totalEvents?: number
          newestEventTime?: string
          oldestEventTime?: string
        }
        errors?: unknown
      }
      return {
        events: json.result?.events ?? [],
        next: json.result?.next ?? 0,
        totalEvents: json.result?.totalEvents ?? 0,
        newestEventTime: json.result?.newestEventTime,
        oldestEventTime: json.result?.oldestEventTime,
      }
    }

    export async function sendChatMessage(input: SendChatMessageInput): Promise<{ ok: true; addTime?: number; chatID?: string }> {
      if (input.replySign.length > 255) {
        throw new Error("replySign превышает 255 символов")
      }
      if (input.message && input.message.length > 1000) {
        throw new Error("Сообщение превышает 1000 символов")
      }
      const form = new FormData()
      form.append("replySign", input.replySign)
      if (input.message) form.append("message", input.message)
      let totalBytes = 0
      for (const f of input.files ?? []) {
        // Buffer → Blob для FormData (node 20 FormData принимает Blob)
        const blob = f.data instanceof Blob ? f.data : new Blob([f.data], { type: f.contentType })
        if (blob.size > 5 * 1024 * 1024) {
          throw new Error(`Файл ${f.name} больше 5 МБ`)
        }
        totalBytes += blob.size
        form.append("file", blob, f.name)
      }
      if (totalBytes > 30 * 1024 * 1024) {
        throw new Error("Суммарный размер файлов больше 30 МБ")
      }
      const res = await callChatApi("/api/v1/seller/message", { method: "POST", body: form })
      const json = (await res.json()) as { result?: { addTime?: number; chatID?: string }; errors?: unknown }
      return { ok: true, addTime: json.result?.addTime, chatID: json.result?.chatID }
    }

    export async function downloadChatAttachment(downloadId: string): Promise<Buffer> {
      const res = await callChatApi(`/api/v1/seller/download/${encodeURIComponent(downloadId)}`, { method: "GET" })
      const arr = await res.arrayBuffer()
      return Buffer.from(arr)
    }
    ```

    **Шаг B — создать `tests/fixtures/wb-chat-chats-sample.json`:**
    ```json
    {
      "result": [
        {
          "chatID": "9e1b3f80-a8d6-4c7f-b9aa-8e11f2c4dd01",
          "replySign": "base64signature==",
          "clientName": "Иван П.",
          "goodCard": { "nmID": 123456789, "price": 1990, "size": "M" },
          "lastMessage": { "text": "Здравствуйте, где инструкция?", "addTimestamp": 1713355200 }
        },
        {
          "chatID": "1a2b3c4d-5e6f-7089-90ab-cdef12345678",
          "replySign": "anothersignature==",
          "clientName": "Мария С.",
          "goodCard": { "nmID": 987654321, "price": 2490, "size": "L" },
          "lastMessage": { "text": "Спасибо!", "addTimestamp": 1713441600 }
        }
      ],
      "errors": null
    }
    ```

    **Шаг C — создать `tests/fixtures/wb-chat-events-sample.json`:**
    ```json
    {
      "result": {
        "next": 1713355200123,
        "newestEventTime": "2026-04-17T12:00:00Z",
        "oldestEventTime": "2026-04-14T08:30:00Z",
        "totalEvents": 2,
        "events": [
          {
            "chatID": "9e1b3f80-a8d6-4c7f-b9aa-8e11f2c4dd01",
            "eventID": "evt-abc123",
            "eventType": "message",
            "isNewChat": false,
            "message": {
              "text": "Добрый день",
              "attachments": {
                "goodCard": { "nmID": 123456789, "price": 1990, "size": "M" },
                "files": [ { "downloadID": "file-xyz", "fileName": "doc.pdf", "fileSize": 12345 } ],
                "images": [ { "downloadID": "img-abc", "fileName": "photo.jpg", "width": 800, "height": 600 } ]
              }
            },
            "addTimestamp": 1713355123456,
            "sender": "client",
            "clientName": "Иван П."
          },
          {
            "chatID": "9e1b3f80-a8d6-4c7f-b9aa-8e11f2c4dd01",
            "eventID": "evt-def456",
            "eventType": "message",
            "isNewChat": true,
            "message": { "text": "Здравствуйте, где инструкция?" },
            "addTimestamp": 1713355180000,
            "sender": "client",
            "clientName": "Иван П."
          }
        ]
      },
      "errors": null
    }
    ```

    **Шаг D — создать `tests/wb-chat-api.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"
    import chatsSample from "./fixtures/wb-chat-chats-sample.json"
    import eventsSample from "./fixtures/wb-chat-events-sample.json"

    const ORIGINAL_API = process.env.WB_API_TOKEN
    const ORIGINAL_CHAT = process.env.WB_CHAT_TOKEN

    beforeEach(() => {
      process.env.WB_API_TOKEN = "test-token"
      process.env.WB_CHAT_TOKEN = "test-chat-token"
      vi.stubGlobal("fetch", vi.fn())
      vi.resetModules()
    })

    afterEach(() => {
      process.env.WB_API_TOKEN = ORIGINAL_API
      process.env.WB_CHAT_TOKEN = ORIGINAL_CHAT
      vi.unstubAllGlobals()
    })

    function mockResponse(body: unknown, status = 200, headers: Record<string, string> = {}): Response {
      return new Response(JSON.stringify(body), {
        status,
        headers: { "Content-Type": "application/json", ...headers },
      })
    }

    describe("pingChat", () => {
      it("GET /ping с Authorization: WB_CHAT_TOKEN", async () => {
        const { pingChat } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await pingChat()
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toBe("https://buyer-chat-api.wildberries.ru/ping")
        expect(call[1].headers.Authorization).toBe("test-chat-token")
      })
    })

    describe("listChats", () => {
      it("GET /api/v1/seller/chats и парсит result[]", async () => {
        const { listChats } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse(chatsSample))
        const res = await listChats()
        expect(res).toHaveLength(2)
        expect(res[0].chatID).toBe("9e1b3f80-a8d6-4c7f-b9aa-8e11f2c4dd01")
        expect(res[0].replySign).toBe("base64signature==")
        expect(res[0].clientName).toBe("Иван П.")
        expect(res[0].goodCard?.nmID).toBe(123456789)
      })
    })

    describe("getChatEvents", () => {
      it("без next — GET без query", async () => {
        const { getChatEvents } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse(eventsSample))
        await getChatEvents()
        const url = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0][0] as string
        expect(url).toBe("https://buyer-chat-api.wildberries.ru/api/v1/seller/events")
      })

      it("с next cursor — GET с query next=X", async () => {
        const { getChatEvents } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse(eventsSample))
        await getChatEvents(1713355200123)
        const url = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0][0] as string
        expect(url).toContain("next=1713355200123")
      })

      it("парсит events + next + totalEvents из fixture", async () => {
        const { getChatEvents } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse(eventsSample))
        const res = await getChatEvents()
        expect(res.events).toHaveLength(2)
        expect(res.events[0].eventID).toBe("evt-abc123")
        expect(res.events[0].sender).toBe("client")
        expect(res.events[1].isNewChat).toBe(true)
        expect(res.next).toBe(1713355200123)
        expect(res.totalEvents).toBe(2)
      })
    })

    describe("sendChatMessage", () => {
      it("POST multipart с replySign + message, body — FormData, Content-Type НЕ application/json", async () => {
        const { sendChatMessage } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
          mockResponse({ result: { addTime: 1234567890, chatID: "9e1b3f80" } })
        )
        await sendChatMessage({ replySign: "sign", message: "Здравствуйте!" })
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("/api/v1/seller/message")
        expect(call[1].method).toBe("POST")
        expect(call[1].body).toBeInstanceOf(FormData)
        // Authorization есть, Content-Type НЕ application/json (fetch поставит multipart/form-data; boundary=...)
        expect(call[1].headers.Authorization).toBe("test-chat-token")
        expect(call[1].headers["Content-Type"]).toBeUndefined()
        const fd = call[1].body as FormData
        expect(fd.get("replySign")).toBe("sign")
        expect(fd.get("message")).toBe("Здравствуйте!")
      })

      it("прикрепляет файлы через form.append('file', blob, name)", async () => {
        const { sendChatMessage } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({ result: {} }))
        const data = Buffer.from([1, 2, 3, 4, 5])
        await sendChatMessage({
          replySign: "sign",
          message: "With file",
          files: [{ name: "photo.jpg", data, contentType: "image/jpeg" }],
        })
        const fd = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0][1].body as FormData
        const file = fd.get("file") as File | null
        expect(file).toBeTruthy()
        expect(file?.name).toBe("photo.jpg")
      })

      it("throws если replySign > 255 символов", async () => {
        const { sendChatMessage } = await import("@/lib/wb-support-api")
        await expect(sendChatMessage({ replySign: "x".repeat(256) })).rejects.toThrow("255")
      })

      it("throws если message > 1000 символов", async () => {
        const { sendChatMessage } = await import("@/lib/wb-support-api")
        await expect(sendChatMessage({ replySign: "s", message: "x".repeat(1001) })).rejects.toThrow("1000")
      })

      it("throws если файл больше 5 МБ", async () => {
        const { sendChatMessage } = await import("@/lib/wb-support-api")
        const big = Buffer.alloc(5 * 1024 * 1024 + 1, 0)
        await expect(
          sendChatMessage({ replySign: "s", files: [{ name: "big.jpg", data: big, contentType: "image/jpeg" }] })
        ).rejects.toThrow("больше 5")
      })
    })

    describe("downloadChatAttachment", () => {
      it("GET /api/v1/seller/download/{id} → Buffer", async () => {
        const { downloadChatAttachment } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
          new Response(new Uint8Array([7, 8, 9]), { status: 200 })
        )
        const buf = await downloadChatAttachment("file-xyz")
        const url = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0][0] as string
        expect(url).toContain("/api/v1/seller/download/file-xyz")
        expect(Buffer.isBuffer(buf)).toBe(true)
        expect(buf.length).toBe(3)
      })
    })

    describe("Chat API — 429 + 401/403 errors", () => {
      it("ретраит при 429 с X-Ratelimit-Retry", async () => {
        const { listChats } = await import("@/lib/wb-support-api")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock.mockResolvedValueOnce(mockResponse({}, 429, { "X-Ratelimit-Retry": "1" }))
        fetchMock.mockResolvedValueOnce(mockResponse({ result: [] }))
        const res = await listChats()
        expect(res).toEqual([])
        expect(fetchMock).toHaveBeenCalledTimes(2)
      }, 10000)

      it("бросает русскую ошибку на 401", async () => {
        const { listChats } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}, 401))
        await expect(listChats()).rejects.toThrow("Неверный токен WB API")
      })

      it("бросает ошибку с 'bit 9 Buyers chat' на 403 от buyer-chat-api", async () => {
        const { listChats } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}, 403))
        await expect(listChats()).rejects.toThrow("bit 9")
      })
    })
    ```

    **Шаг E — запустить тесты:** `npm run test -- tests/wb-chat-api.test.ts` — ожидается >= 12 GREEN тестов.

    Также проверить regression: `npm run test -- tests/wb-support-api.test.ts tests/wb-returns-api.test.ts` — Phase 8/9 не сломано рефакторингом callApi (FormData branch активируется только для FormData body).
  </action>
  <verify>
    <automated>npm run test -- tests/wb-chat-api.test.ts tests/wb-support-api.test.ts tests/wb-returns-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buyer-chat-api.wildberries.ru" lib/wb-support-api.ts` >= 1
    - `grep -c "WB_CHAT_TOKEN" lib/wb-support-api.ts` >= 1
    - `grep -c "export async function listChats" lib/wb-support-api.ts` == 1
    - `grep -c "export async function getChatEvents" lib/wb-support-api.ts` == 1
    - `grep -c "export async function sendChatMessage" lib/wb-support-api.ts` == 1
    - `grep -c "export async function pingChat" lib/wb-support-api.ts` == 1
    - `grep -c "export async function downloadChatAttachment" lib/wb-support-api.ts` == 1
    - `grep -c "export interface Chat" lib/wb-support-api.ts` >= 1
    - `grep -c "export interface ChatEvent" lib/wb-support-api.ts` >= 1
    - `grep "bit 9" lib/wb-support-api.ts` находит строку (403 handling)
    - `grep "FormData" lib/wb-support-api.ts` находит строку (multipart branch)
    - `test -f tests/fixtures/wb-chat-chats-sample.json` exit 0
    - `test -f tests/fixtures/wb-chat-events-sample.json` exit 0
    - `grep "9e1b3f80" tests/fixtures/wb-chat-chats-sample.json` находит строку
    - `grep "evt-abc123" tests/fixtures/wb-chat-events-sample.json` находит строку
    - `test -f tests/wb-chat-api.test.ts` exit 0
    - `npm run test -- tests/wb-chat-api.test.ts` exit 0 (минимум 12 GREEN)
    - `npm run test -- tests/wb-support-api.test.ts` exit 0 (Phase 8 regression)
    - `npm run test -- tests/wb-returns-api.test.ts` exit 0 (Phase 9 regression)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/wb-support-api.ts расширен 5 методами Chat API + 8+ типов; callApi поддерживает FormData (не ставит JSON Content-Type); 2 JSON fixture + test-файл с >= 12 GREEN тестами; Phase 8/9 regression pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Wave 0 — 4 stub-теста для downstream планов (10-02 sync+cron, 10-03 panel, 10-04 settings)</name>
  <files>tests/support-sync-chats.test.ts, tests/auto-reply-cron.test.ts, tests/chat-reply-panel.test.ts, tests/auto-reply-settings.test.ts</files>
  <read_first>
    - .planning/phases/10-chat-autoreply/10-VALIDATION.md (раздел «Wave 0 Requirements»)
    - tests/support-sync-returns.test.ts (Phase 9 паттерн stub)
    - tests/return-actions.test.ts (Phase 9 паттерн stub)
  </read_first>
  <action>
    Создать 4 stub-файла с `it.skip` placeholder-ами. Каждый явно ссылается на downstream-план и описывает тесты, которые downstream план должен реализовать.

    **1. `tests/support-sync-chats.test.ts`** (Plan 10-02):
    ```typescript
    import { describe, it } from "vitest"

    describe("support-sync-chats (Wave 0 stub — Plan 10-02 will implement)", () => {
      it.skip("syncChats() Phase B: обновляет chatReplySign + customerNameSnapshot + previewText для всех chats", () => {
        // Реализация: Plan 10-02 задача 1 — upsert по @@unique([channel, wbExternalId])
      })
      it.skip("syncChats() Phase A: создаёт SupportMessage INBOUND по wbEventId unique — идемпотентно", () => {
        // Реализация: Plan 10-02 — повторный sync не создаёт дубликаты
      })
      it.skip("syncChats(): isNewChat=true создаёт тикет если не найден", () => {
        // Реализация: Plan 10-02
      })
      it.skip("syncChats(): sender=client → direction=INBOUND; sender=seller → direction=OUTBOUND", () => {
        // Реализация: Plan 10-02
      })
      it.skip("syncChats(): attachments.images/files → SupportMedia IMAGE/DOCUMENT с expiresAt = createdAt + 1 год", () => {
        // Реализация: Plan 10-02
      })
      it.skip("syncChats(): обновляет AppSetting.support.chat.lastEventNext после каждого tick", () => {
        // Реализация: Plan 10-02
      })
    })
    ```

    **2. `tests/auto-reply-cron.test.ts`** (Plan 10-02):
    ```typescript
    import { describe, it } from "vitest"

    describe("auto-reply-cron (Wave 0 stub — Plan 10-02 will implement)", () => {
      it.skip("runAutoReplies(): isEnabled=false → sent=0 (skip полностью)", () => {
        // Реализация: Plan 10-02 задача 2 — lib/auto-reply.ts
      })
      it.skip("runAutoReplies(): внутри рабочих часов (workDays+workdayStart/End по Europe/Moscow) → sent=0", () => {
        // Реализация: Plan 10-02 — isWithinWorkingHours helper
      })
      it.skip("runAutoReplies(): вне рабочих часов + isEnabled=true → отправляет ответ с isAutoReply=true", () => {
        // Реализация: Plan 10-02
      })
      it.skip("runAutoReplies(): {имя_покупателя} заменяется на ticket.customerNameSnapshot, fallback 'покупатель'", () => {
        // Реализация: Plan 10-02
      })
      it.skip("runAutoReplies(): {название_товара} заменяется на WbCard.name по nmId, fallback 'товар'", () => {
        // Реализация: Plan 10-02
      })
      it.skip("runAutoReplies(): dedup — не отправляет повторно если был OUTBOUND isAutoReply=true за 24ч на том же ticket", () => {
        // Реализация: Plan 10-02 — защита от спама
      })
      it.skip("GET /api/cron/support-sync-chat: 401 без x-cron-secret; 200 + syncChats()+runAutoReplies() при валидном", () => {
        // Реализация: Plan 10-02 задача 3 — cron endpoint
      })
    })
    ```

    **3. `tests/chat-reply-panel.test.ts`** (Plan 10-03):
    ```typescript
    import { describe, it } from "vitest"

    describe("chat-reply-panel (Wave 0 stub — Plan 10-03 will implement)", () => {
      it.skip("ChatReplyPanel: textarea maxLength=1000, счётчик символов", () => {
        // Реализация: Plan 10-03 задача 1
      })
      it.skip("ChatReplyPanel: file input accept='image/jpeg,image/png,application/pdf' multiple", () => {
        // Реализация: Plan 10-03
      })
      it.skip("ChatReplyPanel: клиентская валидация — файл > 5 МБ отклоняется с toast", () => {
        // Реализация: Plan 10-03
      })
      it.skip("ChatReplyPanel: клиентская валидация — сумма файлов > 30 МБ отклоняется с toast", () => {
        // Реализация: Plan 10-03
      })
      it.skip("sendChatMessageAction: требует requireSection('SUPPORT','MANAGE'), VIEWER получает reject", () => {
        // Реализация: Plan 10-03 задача 2 — server action в app/actions/support.ts
      })
      it.skip("sendChatMessageAction: создаёт OUTBOUND SupportMessage + SupportMedia per file + revalidatePath", () => {
        // Реализация: Plan 10-03
      })
      it.skip("sendChatMessageAction: reject если ticket.channel !== 'CHAT' или chatReplySign == null", () => {
        // Реализация: Plan 10-03
      })
    })
    ```

    **4. `tests/auto-reply-settings.test.ts`** (Plan 10-04):
    ```typescript
    import { describe, it } from "vitest"

    describe("auto-reply-settings (Wave 0 stub — Plan 10-04 will implement)", () => {
      it.skip("saveAutoReplyConfig: Zod valid payload → upsert AutoReplyConfig{id:'default'} + updatedById", () => {
        // Реализация: Plan 10-04 задача 2 — server action
      })
      it.skip("saveAutoReplyConfig: Zod invalid workdayStart (не HH:MM) → reject", () => {
        // Реализация: Plan 10-04 — regex ^\\d{2}:\\d{2}$
      })
      it.skip("saveAutoReplyConfig: Zod invalid workDays (не в 0..6) → reject", () => {
        // Реализация: Plan 10-04 — z.array(z.number().min(0).max(6))
      })
      it.skip("saveAutoReplyConfig: messageText length 1..1000 enforced", () => {
        // Реализация: Plan 10-04
      })
      it.skip("saveAutoReplyConfig: RBAC SUPPORT,MANAGE — VIEWER получает reject", () => {
        // Реализация: Plan 10-04
      })
    })
    ```

    **Запуск:** `npm run test` — все stubs помечены как skipped, 0 failed.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/support-sync-chats.test.ts` exit 0
    - `test -f tests/auto-reply-cron.test.ts` exit 0
    - `test -f tests/chat-reply-panel.test.ts` exit 0
    - `test -f tests/auto-reply-settings.test.ts` exit 0
    - `grep -c "it.skip" tests/support-sync-chats.test.ts` >= 6
    - `grep -c "it.skip" tests/auto-reply-cron.test.ts` >= 7
    - `grep -c "it.skip" tests/chat-reply-panel.test.ts` >= 7
    - `grep -c "it.skip" tests/auto-reply-settings.test.ts` >= 5
    - `grep "Plan 10-02" tests/support-sync-chats.test.ts` находит ссылку
    - `grep "Plan 10-02" tests/auto-reply-cron.test.ts` находит ссылку
    - `grep "Plan 10-03" tests/chat-reply-panel.test.ts` находит ссылку
    - `grep "Plan 10-04" tests/auto-reply-settings.test.ts` находит ссылку
    - `npm run test` exit 0 — 0 failed (Phase 7/8/9 + wb-chat-api.test.ts GREEN + 4 новых stubs SKIPPED)
  </acceptance_criteria>
  <done>
    4 stub-файла созданы с `it.skip` placeholder-ами (6+7+7+5 stubs с reference на downstream планы); `npm run test` exit 0; Phase 7/8/9 regression pass.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npx prisma validate` exit 0
2. `npx prisma format --check` exit 0
3. `ls prisma/migrations/*phase10_chat_autoreply/migration.sql` exit 0
4. `npm run test -- tests/wb-chat-api.test.ts` exit 0 (≥ 12 GREEN)
5. `npm run test` exit 0 (Phase 7/8/9 regression + новые GREEN + 4 stubs SKIPPED, 0 failed)
6. `npx tsc --noEmit` exit 0
7. `grep -c "export async function " lib/wb-support-api.ts` >= 14 (5 Phase 8 + 4 Phase 9 + 5 Phase 10)
</verification>

<success_criteria>
Plan 10-01 завершён когда:
- Prisma schema содержит AutoReplyConfig + 2 поля в SupportTicket + 1 поле в SupportMessage (@unique) + DOCUMENT в enum MediaType + relation AutoReplyUpdater в User
- Миграция `<ts>_phase10_chat_autoreply/migration.sql` создана с ALTER TABLE + CREATE TABLE + INSERT ON CONFLICT (pending для VPS deploy)
- lib/wb-support-api.ts экспортирует 5 новых Chat методов + 8+ типов; callApi поддерживает FormData (не ставит JSON Content-Type когда body=FormData); 403 scope hint для buyer-chat-api = "bit 9 Buyers chat"
- 2 fixture JSON файла созданы
- tests/wb-chat-api.test.ts — минимум 12 GREEN test cases
- 4 stub-файла созданы с it.skip + ссылками на 10-02/10-03/10-04
- `npm run test` 0 failed (Phase 7/8/9 + новые Phase 10 GREEN + stubs SKIPPED)
- Commit: `feat(10-01): Prisma AutoReplyConfig + WB Chat API клиент + Wave 0 stubs`
</success_criteria>

<output>
После завершения создать `.planning/phases/10-chat-autoreply/10-01-SUMMARY.md` с:
- Изменения schema.prisma: AutoReplyConfig + расширения SupportTicket/SupportMessage + DOCUMENT в MediaType
- Миграция `<ts>_phase10_chat_autoreply/migration.sql` — применена локально или pending (для Plan 10-04 deploy)
- Новые методы lib/wb-support-api.ts: pingChat/listChats/getChatEvents/sendChatMessage/downloadChatAttachment + типы
- Рефакторинг callApi: FormData branch (не ставит JSON Content-Type) + 403 scope hint для buyer-chat-api
- 5 тест-файлов (1 GREEN + 4 stubs)
- 2 fixtures: wb-chat-chats-sample.json, wb-chat-events-sample.json
- Deploy задача для Plan 10-04: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && npx prisma migrate deploy"` + добавить WB_CHAT_TOKEN в /etc/zoiten.pro.env
- Wave 0 VPS pre-flight (ожидает пользователя): curl -sI https://buyer-chat-api.wildberries.ru/ping с новым WB_CHAT_TOKEN → 200
</output>
</content>
</invoke>