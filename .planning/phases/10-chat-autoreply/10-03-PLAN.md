---
phase: 10-chat-autoreply
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - components/support/ChatReplyPanel.tsx
  - app/actions/support.ts
  - app/(dashboard)/support/[ticketId]/page.tsx
  - components/support/SupportDialog.tsx
  - tests/chat-reply-panel.test.ts
autonomous: true
requirements:
  - SUP-22
  - SUP-25
must_haves:
  truths:
    - "components/support/ChatReplyPanel.tsx — client-компонент с textarea (maxLength=1000) + file input (multiple, accept='image/jpeg,image/png,application/pdf') + клиентская валидация (каждый файл ≤5MB, сумма ≤30MB) + Send кнопка disabled при пустом text+files"
    - "app/actions/support.ts экспортирует sendChatMessageAction(formData: FormData) — server action с requireSection('SUPPORT','MANAGE'); reject если ticket.channel !== 'CHAT'; проверка chatReplySign !== null; создаёт OUTBOUND SupportMessage + SupportMedia per file; вызывает lib/wb-support-api sendChatMessage; revalidatePath /support и /support/[ticketId]; возвращает { ok: true } или { ok: false, error }"
    - "app/(dashboard)/support/[ticketId]/page.tsx — conditional: channel === 'CHAT' → рендерит <ChatReplyPanel /> (заменяя ReplyPanel); channel === 'RETURN' → остаётся <ReturnActionsPanel /> (Phase 9); channel FEEDBACK/QUESTION → остаётся <ReplyPanel /> (Phase 8)"
    - "components/support/SupportDialog.tsx — для message.isAutoReply=true рендерит badge/иконку Bot рядом с меткой направления (INBOUND/OUTBOUND)"
    - "tests/chat-reply-panel.test.ts GREEN — минимум 7 тестов (textarea limit, file validation ≤5MB/≤30MB, MIME filter, RBAC VIEWER reject, OUTBOUND + SupportMedia создание, revalidatePath, channel check)"
    - "Client-side валидация НЕ предотвращает server-side re-validation (server action повторяет проверки размеров для security)"
  artifacts:
    - path: "components/support/ChatReplyPanel.tsx"
      provides: "Client multipart upload panel для CHAT канала"
      exports:
        - default (ChatReplyPanel)
    - path: "app/actions/support.ts"
      provides: "sendChatMessageAction — multipart WB send + OUTBOUND persistence"
      exports:
        - sendChatMessageAction
    - path: "app/(dashboard)/support/[ticketId]/page.tsx"
      provides: "Conditional render панелей по каналу"
    - path: "components/support/SupportDialog.tsx"
      provides: "Bot badge для isAutoReply=true сообщений"
    - path: "tests/chat-reply-panel.test.ts"
      provides: "Integration тесты sendChatMessageAction + клиентской валидации"
      contains: "describe"
  key_links:
    - from: "components/support/ChatReplyPanel.tsx"
      to: "app/actions/support.ts sendChatMessageAction"
      via: "import + FormData submit"
      pattern: "sendChatMessageAction"
    - from: "app/actions/support.ts sendChatMessageAction"
      to: "lib/wb-support-api.ts sendChatMessage"
      via: "import"
      pattern: "from\\s+\"@/lib/wb-support-api\""
    - from: "app/actions/support.ts sendChatMessageAction"
      to: "requireSection(\"SUPPORT\", \"MANAGE\")"
      via: "RBAC guard"
      pattern: "requireSection\\(\"SUPPORT\",\\s*\"MANAGE\"\\)"
    - from: "app/(dashboard)/support/[ticketId]/page.tsx"
      to: "components/support/ChatReplyPanel"
      via: "conditional channel === 'CHAT'"
      pattern: "ChatReplyPanel"
    - from: "components/support/SupportDialog.tsx"
      to: "message.isAutoReply"
      via: "Bot icon + visual marker"
      pattern: "isAutoReply"
---

<objective>
Phase 10 Plan 03 — UI для отправки сообщений в чат + иконка автоответа:

1. **`components/support/ChatReplyPanel.tsx`** (новый client component):
   - Sticky нижняя панель: textarea (maxLength=1000) + file input (hidden, триггер через кнопку `<Paperclip />`) + Send.
   - Accepted MIME: `image/jpeg,image/png,application/pdf`.
   - Клиентская валидация: размер каждого файла ≤ 5 МБ; суммарный ≤ 30 МБ; невалидный файл → toast error + не добавляется в `files[]`.
   - Preview: список выбранных файлов с кнопкой удаления (×) и размером в KB.
   - Счётчик символов textarea (1000 max).
   - Submit через FormData → `sendChatMessageAction(formData)`; toast.success/error; clear state.
   - Disabled если `!replySign` или pending или empty (нет text и files).
   - Props: `{ ticketId: string; replySign: string | null }`.

2. **`app/actions/support.ts` — добавить `sendChatMessageAction`**:
   - `"use server"`, `requireSection("SUPPORT", "MANAGE")`, `getSessionUserId()` helper (уже есть в Phase 8).
   - Парсинг FormData: `ticketId`, `text`, `files` (as File[]).
   - Server-side validation (повтор клиентской): text ≤ 1000, файл ≤ 5 МБ, сумма ≤ 30 МБ.
   - Find ticket: проверка `ticket.channel === "CHAT"` + `ticket.chatReplySign !== null` (иначе reject).
   - **Порядок операций (WB-first, защита от частичного состояния):**
     1. `sendChatMessage({replySign, message, files: [{name, data: Buffer.from(await f.arrayBuffer()), contentType: f.type}]})` — если WB throws → return `{ok: false, error}`, локально ничего не меняем.
     2. После успеха WB: create OUTBOUND `SupportMessage` с `authorId=userId, isAutoReply=false` + per file create `SupportMedia` с `type=IMAGE|DOCUMENT`, `localPath = /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}` (файл сохраняем локально **после** успешного WB send — WB уже принял, не потеряем).
     3. Update `SupportTicket`: `status = "ANSWERED"`, `lastMessageAt = now`.
     4. `revalidatePath("/support")`, `revalidatePath("/support/" + ticketId)`.

3. **Модификация `app/(dashboard)/support/[ticketId]/page.tsx`**:
   - Conditional rendering: `const isChat = ticket.channel === "CHAT"` + рендер `<ChatReplyPanel ticketId={ticket.id} replySign={ticket.chatReplySign} />` когда isChat.
   - Также передать `chatReplySign` в select (ранее не выбиралось — добавить в Prisma query).
   - Phase 8 ReplyPanel (FEEDBACK/QUESTION) и Phase 9 ReturnActionsPanel (RETURN) — НЕ ТРОГАТЬ.
   - Для нераспознанных каналов (MESSENGER — Phase 12) fallback сообщение.

4. **Модификация `components/support/SupportDialog.tsx`**:
   - Для каждого message, если `message.isAutoReply === true` — рендерить inline badge с иконкой `Bot` из lucide-react + title="Автоответ" (russian).
   - Расположение: рядом с меткой направления (INBOUND/OUTBOUND) или в заголовке bubble.

5. **Тесты** (заменяет Wave 0 stub `tests/chat-reply-panel.test.ts`):
   - Integration тесты `sendChatMessageAction` с mock Prisma + mock WB API + mock next/cache revalidatePath.
   - Тесты клиентской валидации можно опустить (hard to test without jsdom setup — держим на UAT).
   - Минимум 7 GREEN: happy path, channel guard, chatReplySign guard, text/file validation, RBAC, revalidatePath, WB-first (WB error = нет записи).

**Purpose:** Менеджер отправляет сообщения в чат WB прямо из ERP-диалога (текст + фото/PDF). Визуально отличает автоответы (🤖) от ручных ответов (SUP-25). WB-first transaction order (из Phase 9) защищает от неконсистентного состояния БД при падении WB.

**Output:** 1 новый клиентский компонент, 1 новый server action, 2 модификации (route page + SupportDialog), 1 наполненный integration-тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-chat-autoreply/10-RESEARCH.md
@.planning/phases/10-chat-autoreply/10-VALIDATION.md
@.planning/phases/10-chat-autoreply/10-01-SUMMARY.md
@.planning/phases/10-chat-autoreply/10-02-SUMMARY.md
@.planning/phases/08-support-mvp/08-04-SUMMARY.md
@.planning/phases/09-returns/09-04-SUMMARY.md
@CLAUDE.md
@app/actions/support.ts
@app/(dashboard)/support/[ticketId]/page.tsx
@components/support/ReplyPanel.tsx
@components/support/ReturnActionsPanel.tsx
@components/support/SupportDialog.tsx
@lib/wb-support-api.ts
@lib/rbac.ts
@lib/prisma.ts

<interfaces>
**Новые lib методы Plan 10-01:**
```typescript
export async function sendChatMessage(input: {
  replySign: string
  message?: string
  files?: Array<{ name: string; data: Buffer | Blob; contentType: string }>
}): Promise<{ ok: true; addTime?: number; chatID?: string }>
```

**Prisma SupportTicket (Plan 10-01 расширение):**
```prisma
chatReplySign         String?
customerNameSnapshot  String?
// + Phase 8 поля: channel, wbExternalId, nmId, status, etc.
```

**Prisma SupportMessage:**
```prisma
direction   Direction     // INBOUND | OUTBOUND
text        String?
authorId    String?
isAutoReply Boolean       @default(false)
wbEventId   String?       @unique  // для OUTBOUND через UI — null (мы не знаем eventID до sync cycle)
wbSentAt    DateTime?
```

**Prisma SupportMedia:**
```prisma
type        MediaType     // IMAGE | VIDEO | DOCUMENT
wbUrl       String
localPath   String?
expiresAt   DateTime      // createdAt + 1 год
```

**Существующий helper getSessionUserId() в app/actions/support.ts (Phase 8):**
```typescript
async function getSessionUserId(): Promise<string | null> {
  const session = await auth()
  return session?.user?.id ?? null
}
```

**Существующий ReplyPanel (Phase 8 — не трогаем):**
```tsx
// components/support/ReplyPanel.tsx
export function ReplyPanel({ ticketId, disabled }: { ticketId: string; disabled?: boolean })
```
Используется для FEEDBACK/QUESTION. Phase 10 ДОБАВЛЯЕТ ChatReplyPanel рядом, не заменяет.

**Существующий ReturnActionsPanel (Phase 9 — не трогаем):**
```tsx
export function ReturnActionsPanel(props: {...})
```
Conditional для channel === "RETURN".

**ActionResult тип (уже есть в app/actions/support.ts):**
```typescript
export type ActionResult = { ok: true } | { ok: false; error: string }
```

**Паттерн app/(dashboard)/support/[ticketId]/page.tsx (Phase 9 conditional):**
```tsx
{ticket.channel === "FEEDBACK" || ticket.channel === "QUESTION" ? (
  <ReplyPanel ticketId={ticket.id} disabled={ticket.status === "CLOSED"} />
) : ticket.channel === "RETURN" ? (
  <ReturnActionsPanel ... />
) : (
  <div>Канал не поддерживает ответ</div>
)}
```

**SupportDialog bubble рендер (упрощённо, из Phase 8):**
```tsx
{messages.map((m) => (
  <div key={m.id} className={m.direction === "INBOUND" ? "self-start" : "self-end"}>
    <div>{m.text}</div>
    <div className="text-xs text-muted-foreground">
      {m.direction === "OUTBOUND" ? "Вы" : "Покупатель"} • {formatTime(m.sentAt)}
    </div>
  </div>
))}
```

Phase 10 расширит: если `m.isAutoReply` → inline Bot icon + title="Автоответ".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: sendChatMessageAction в app/actions/support.ts + test GREEN</name>
  <files>app/actions/support.ts, tests/chat-reply-panel.test.ts</files>
  <read_first>
    - app/actions/support.ts (Phase 8/9 — паттерн replyToTicket, approveReturn: requireSection + getSessionUserId + try/catch + revalidatePath)
    - lib/wb-support-api.ts (Plan 10-01 — sendChatMessage контракт)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «Multipart Upload — Server action» строки 621-691 — эталонный код)
    - tests/return-actions.test.ts (Phase 9 паттерн integration test RBAC + WB-first + Prisma mock)
    - tests/chat-reply-panel.test.ts (Wave 0 stub Plan 10-01 — заменить)
    - CLAUDE.md (раздел «UPLOAD_DIR» — путь /var/www/zoiten-uploads/support/)
  </read_first>
  <behavior>
    - `sendChatMessageAction(formData: FormData): Promise<ActionResult>`:
      1. `"use server"`, `await requireSection("SUPPORT", "MANAGE")`, `const userId = await getSessionUserId()`; reject если !userId
      2. Парсинг: `ticketId = formData.get("ticketId") as string`, `text = (formData.get("text") as string) ?? ""`, `files = formData.getAll("files") as File[]` (filter out empty File — может содержать "" если пусто)
      3. Empty check: `!text.trim() && files.length === 0` → `{ok:false, error:"Пустое сообщение"}`
      4. Text validation: `text.length > 1000` → reject
      5. File validation: per file `f.size > 5*1024*1024` → reject с именем; total > 30*1024*1024 → reject
      6. MIME validation: accept только `image/jpeg`, `image/png`, `application/pdf` (любой другой → reject)
      7. Find ticket: `prisma.supportTicket.findUnique({where: {id}, select: {id, channel, chatReplySign, wbExternalId}})` → !ticket reject; channel !== "CHAT" reject; !chatReplySign reject
      8. **WB-first:**
         - `const wbFiles = await Promise.all(files.map(async (f) => ({name: f.name, data: Buffer.from(await f.arrayBuffer()), contentType: f.type})))`
         - `try { await sendChatMessage({replySign, message: text || undefined, files: wbFiles}) } catch (err) { return {ok:false, error: "WB: " + err.message} }`
      9. После успеха WB — персистентность:
         - `const msg = await prisma.supportMessage.create({data: {ticketId, direction:"OUTBOUND", text: text || null, authorId: userId, isAutoReply: false, wbSentAt: new Date(), sentAt: new Date()}})`
         - Сохранить локальные копии файлов + create SupportMedia per file:
           - `const dir = path.join(UPLOAD_DIR, "support", ticket.id, msg.id); await fs.mkdir(dir, {recursive:true})`
           - для каждого файла: `const localPath = path.join(dir, sanitize(f.name)); await fs.writeFile(localPath, Buffer.from(await f.arrayBuffer()))` (повторный arrayBuffer НЕ работает — File уже прочитан в wbFiles; сохранить Buffer из wbFiles ИЛИ read заново из оригинального File — уточнить: File stream можно читать повторно через blob())
           - `prisma.supportMedia.create({data: {messageId: msg.id, type: f.type === "application/pdf" ? "DOCUMENT" : "IMAGE", wbUrl: "", localPath, sizeBytes, expiresAt: +1 год}})`
         - **Simpler approach:** сохранить Buffer в переменную `wbFiles[i].data` (уже есть) → writeFile в fs после Prisma create
         - `prisma.supportTicket.update({where: {id}, data: {status: "ANSWERED", lastMessageAt: new Date()}})`
      10. `revalidatePath("/support"); revalidatePath("/support/" + ticketId); return {ok:true}`
  </behavior>
  <action>
    **Шаг A — добавить в `app/actions/support.ts` после существующих actions:**

    ```typescript
    // В начало файла добавить импорты (если отсутствуют):
    import { sendChatMessage } from "@/lib/wb-support-api"
    import { promises as fs } from "node:fs"
    import path from "node:path"
    import type { MediaType } from "@prisma/client"

    const UPLOAD_DIR = process.env.UPLOAD_DIR || "/var/www/zoiten-uploads"
    const MAX_FILE_BYTES = 5 * 1024 * 1024
    const MAX_TOTAL_BYTES = 30 * 1024 * 1024
    const ALLOWED_MIME = new Set(["image/jpeg", "image/png", "application/pdf"])

    function sanitizeChatFilename(name: string): string {
      return name.replace(/[^a-zA-Z0-9.\-_]/g, "_").slice(-128) || `file_${Date.now()}`
    }

    function mimeToMediaType(mime: string): MediaType {
      return mime === "application/pdf" ? "DOCUMENT" : "IMAGE"
    }

    export async function sendChatMessageAction(formData: FormData): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const ticketId = formData.get("ticketId") as string
        if (!ticketId) return { ok: false, error: "ticketId обязателен" }

        const text = ((formData.get("text") as string) ?? "").trim()
        const files = (formData.getAll("files") as unknown[]).filter(
          (f): f is File => f instanceof File && f.size > 0
        )

        if (!text && files.length === 0) return { ok: false, error: "Пустое сообщение" }
        if (text.length > 1000) return { ok: false, error: "Текст превышает 1000 символов" }

        let totalBytes = 0
        for (const f of files) {
          if (!ALLOWED_MIME.has(f.type)) {
            return { ok: false, error: `Недопустимый формат: ${f.name} (${f.type})` }
          }
          if (f.size > MAX_FILE_BYTES) {
            return { ok: false, error: `Файл ${f.name} больше 5 МБ` }
          }
          totalBytes += f.size
        }
        if (totalBytes > MAX_TOTAL_BYTES) {
          return { ok: false, error: "Суммарный размер файлов больше 30 МБ" }
        }

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: ticketId },
          select: {
            id: true,
            channel: true,
            chatReplySign: true,
            wbExternalId: true,
          },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (ticket.channel !== "CHAT") {
          return { ok: false, error: "Канал тикета не CHAT" }
        }
        if (!ticket.chatReplySign) {
          return {
            ok: false,
            error: "Нет replySign для чата — запустите синхронизацию",
          }
        }

        // Prebuild буферы (File stream — однократное чтение)
        const wbFiles = await Promise.all(
          files.map(async (f) => ({
            name: f.name,
            data: Buffer.from(await f.arrayBuffer()),
            contentType: f.type,
          }))
        )

        // ── WB-first: если WB упадёт — ничего локально не меняем ──
        try {
          await sendChatMessage({
            replySign: ticket.chatReplySign,
            message: text || undefined,
            files: wbFiles.length > 0 ? wbFiles : undefined,
          })
        } catch (err) {
          const msg = err instanceof Error ? err.message : "Ошибка WB API"
          return { ok: false, error: `WB: ${msg}` }
        }

        // ── После успеха: локальная запись ──
        const now = new Date()
        const yearMs = 365 * 24 * 60 * 60 * 1000

        const msg = await prisma.supportMessage.create({
          data: {
            ticketId: ticket.id,
            direction: "OUTBOUND",
            text: text || null,
            authorId: userId,
            isAutoReply: false,
            wbSentAt: now,
            sentAt: now,
          },
        })

        if (wbFiles.length > 0) {
          const dir = path.join(UPLOAD_DIR, "support", ticket.id, msg.id)
          await fs.mkdir(dir, { recursive: true })
          for (const f of wbFiles) {
            const localPath = path.join(dir, sanitizeChatFilename(f.name))
            await fs.writeFile(localPath, f.data)
            await prisma.supportMedia.create({
              data: {
                messageId: msg.id,
                type: mimeToMediaType(f.contentType),
                wbUrl: "",
                localPath,
                sizeBytes: f.data.length,
                expiresAt: new Date(Date.now() + yearMs),
              },
            })
          }
        }

        await prisma.supportTicket.update({
          where: { id: ticket.id },
          data: { status: "ANSWERED", lastMessageAt: now },
        })

        revalidatePath("/support")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return {
          ok: false,
          error: err instanceof Error ? err.message : "Ошибка отправки",
        }
      }
    }
    ```

    **Шаг B — заменить `tests/chat-reply-panel.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    const state = {
      tickets: [] as any[],
      messages: [] as any[],
      media: [] as any[],
    }
    const sendChatMessageMock = vi.fn()
    const revalidateMock = vi.fn()
    const requireSectionMock = vi.fn()
    const authMock = vi.fn()

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        supportTicket: {
          findUnique: async ({ where }: any) => state.tickets.find((t) => t.id === where.id) ?? null,
          update: async ({ where, data }: any) => {
            const t = state.tickets.find((x) => x.id === where.id)
            Object.assign(t, data)
            return t
          },
        },
        supportMessage: {
          create: async ({ data }: any) => {
            const m = { id: `M${state.messages.length + 1}`, ...data }
            state.messages.push(m)
            return m
          },
        },
        supportMedia: {
          create: async ({ data }: any) => {
            const md = { id: `MD${state.media.length + 1}`, ...data }
            state.media.push(md)
            return md
          },
        },
      },
    }))

    vi.mock("@/lib/wb-support-api", () => ({
      sendChatMessage: sendChatMessageMock,
    }))

    vi.mock("@/lib/rbac", () => ({
      requireSection: requireSectionMock,
    }))

    vi.mock("@/lib/auth", () => ({
      auth: authMock,
    }))

    vi.mock("next/cache", () => ({
      revalidatePath: revalidateMock,
    }))

    vi.mock("node:fs", () => ({
      promises: {
        mkdir: vi.fn().mockResolvedValue(undefined),
        writeFile: vi.fn().mockResolvedValue(undefined),
      },
    }))

    function makeFile(content: Uint8Array, name: string, type: string): File {
      return new File([content], name, { type })
    }

    beforeEach(() => {
      state.tickets = [
        { id: "T1", channel: "CHAT", chatReplySign: "sig1", wbExternalId: "C1" },
      ]
      state.messages = []
      state.media = []
      sendChatMessageMock.mockReset()
      sendChatMessageMock.mockResolvedValue({ ok: true })
      revalidateMock.mockReset()
      requireSectionMock.mockReset()
      requireSectionMock.mockResolvedValue(undefined)
      authMock.mockReset()
      authMock.mockResolvedValue({ user: { id: "U1" } })
    })
    afterEach(() => vi.resetAllMocks())

    describe("sendChatMessageAction — happy path", () => {
      it("создаёт OUTBOUND + вызывает sendChatMessage + revalidatePath", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "Здравствуйте, ответ менеджера")
        const res = await sendChatMessageAction(fd)
        expect(res).toEqual({ ok: true })
        expect(sendChatMessageMock).toHaveBeenCalledWith(
          expect.objectContaining({ replySign: "sig1", message: "Здравствуйте, ответ менеджера" })
        )
        expect(state.messages).toHaveLength(1)
        expect(state.messages[0].direction).toBe("OUTBOUND")
        expect(state.messages[0].isAutoReply).toBe(false)
        expect(state.messages[0].authorId).toBe("U1")
        expect(revalidateMock).toHaveBeenCalledWith("/support")
        expect(revalidateMock).toHaveBeenCalledWith("/support/T1")
      })

      it("создаёт SupportMedia IMAGE для JPEG и DOCUMENT для PDF", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "с файлами")
        fd.append("files", makeFile(new Uint8Array([1,2,3]), "photo.jpg", "image/jpeg"))
        fd.append("files", makeFile(new Uint8Array([4,5,6]), "doc.pdf", "application/pdf"))
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(true)
        expect(state.media).toHaveLength(2)
        expect(state.media.find((m) => m.type === "IMAGE")).toBeDefined()
        expect(state.media.find((m) => m.type === "DOCUMENT")).toBeDefined()
      })

      it("обновляет ticket.status на ANSWERED + lastMessageAt", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "ответ")
        await sendChatMessageAction(fd)
        expect(state.tickets[0].status).toBe("ANSWERED")
        expect(state.tickets[0].lastMessageAt).toBeInstanceOf(Date)
      })
    })

    describe("sendChatMessageAction — validation", () => {
      it("reject: пустое сообщение (нет text и files)", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "")
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("Пустое")
      })

      it("reject: text > 1000 символов", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "x".repeat(1001))
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("1000")
      })

      it("reject: недопустимый MIME (text/plain)", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "text")
        fd.append("files", makeFile(new Uint8Array([1]), "script.txt", "text/plain"))
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("формат")
      })

      it("reject: файл > 5 МБ", async () => {
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        const big = new Uint8Array(5 * 1024 * 1024 + 1)
        fd.append("files", makeFile(big, "big.jpg", "image/jpeg"))
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("5 МБ")
      })
    })

    describe("sendChatMessageAction — guards", () => {
      it("reject: тикет не CHAT", async () => {
        state.tickets[0].channel = "FEEDBACK"
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "ответ")
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("CHAT")
      })

      it("reject: chatReplySign отсутствует", async () => {
        state.tickets[0].chatReplySign = null
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "ответ")
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("replySign")
      })

      it("reject: VIEWER (requireSection throws)", async () => {
        requireSectionMock.mockRejectedValueOnce(new Error("Недостаточно прав"))
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "ответ")
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("Недостаточно")
      })

      it("WB-first: если WB throws — OUTBOUND message НЕ создаётся", async () => {
        sendChatMessageMock.mockRejectedValueOnce(new Error("WB 503"))
        const { sendChatMessageAction } = await import("@/app/actions/support")
        const fd = new FormData()
        fd.set("ticketId", "T1")
        fd.set("text", "ответ")
        const res = await sendChatMessageAction(fd)
        expect(res.ok).toBe(false)
        expect(state.messages).toHaveLength(0)
        expect(state.tickets[0].status).not.toBe("ANSWERED")
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/chat-reply-panel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function sendChatMessageAction" app/actions/support.ts` == 1
    - `grep "requireSection.*SUPPORT.*MANAGE" app/actions/support.ts` находит >= 1 вхождения (уже было в Phase 8/9)
    - `grep "channel !== \"CHAT\"" app/actions/support.ts` находит строку
    - `grep "chatReplySign" app/actions/support.ts` находит строку
    - `grep "sendChatMessage" app/actions/support.ts` находит строку
    - `grep "isAutoReply:\s*false" app/actions/support.ts` находит строку
    - `grep "MAX_FILE_BYTES" app/actions/support.ts` находит строку
    - `grep 'DOCUMENT' app/actions/support.ts` находит строку
    - `grep 'revalidatePath.*/support/' app/actions/support.ts` находит строку
    - `npm run test -- tests/chat-reply-panel.test.ts` exit 0 (≥ 10 тестов GREEN)
    - `npm run test -- tests/return-actions.test.ts tests/support-actions.test.ts` exit 0 (Phase 8/9 regression)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    sendChatMessageAction экспортируется с RBAC + channel guard + chatReplySign guard + Zod-like валидацией размеров + MIME + WB-first transaction order; ≥ 10 integration-тестов GREEN; Phase 8/9 regression pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: ChatReplyPanel client component + conditional render в /support/[ticketId]/page.tsx + Bot badge в SupportDialog</name>
  <files>components/support/ChatReplyPanel.tsx, app/(dashboard)/support/[ticketId]/page.tsx, components/support/SupportDialog.tsx</files>
  <read_first>
    - components/support/ReplyPanel.tsx (Phase 8 — паттерн client textarea + startTransition + toast)
    - components/support/ReturnActionsPanel.tsx (Phase 9 — паттерн sticky панель + conditional)
    - components/support/SupportDialog.tsx (текущий рендер bubble — куда вставить Bot badge)
    - app/(dashboard)/support/[ticketId]/page.tsx (существующий conditional rendering Phase 8/9)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «ChatReplyPanel.tsx» строки 742-791 — эталонный JSX)
    - lib/wb-support-api.ts (sendChatMessage контракт — для понимания accept types)
    - CLAUDE.md (раздел «Select: native HTML» — но здесь не select, а file input)
  </read_first>
  <action>
    **Шаг A — создать `components/support/ChatReplyPanel.tsx`:**

    ```tsx
    "use client"

    import { useRef, useState, useTransition } from "react"
    import { toast } from "sonner"
    import { Paperclip, Send, X, Loader2 } from "lucide-react"
    import { Button } from "@/components/ui/button"
    import { sendChatMessageAction } from "@/app/actions/support"

    const MAX_FILE_BYTES = 5 * 1024 * 1024
    const MAX_TOTAL_BYTES = 30 * 1024 * 1024
    const ALLOWED_MIME = ["image/jpeg", "image/png", "application/pdf"]

    interface ChatReplyPanelProps {
      ticketId: string
      replySign: string | null
    }

    export function ChatReplyPanel({ ticketId, replySign }: ChatReplyPanelProps) {
      const [text, setText] = useState("")
      const [files, setFiles] = useState<File[]>([])
      const fileInputRef = useRef<HTMLInputElement>(null)
      const [isPending, startTransition] = useTransition()

      const disabled = !replySign || isPending || (!text.trim() && files.length === 0)

      function onFileSelect(e: React.ChangeEvent<HTMLInputElement>) {
        const picked = Array.from(e.target.files ?? [])
        const next: File[] = [...files]
        let totalBytes = next.reduce((sum, f) => sum + f.size, 0)
        for (const f of picked) {
          if (!ALLOWED_MIME.includes(f.type)) {
            toast.error(`Недопустимый формат: ${f.name}`)
            continue
          }
          if (f.size > MAX_FILE_BYTES) {
            toast.error(`${f.name} больше 5 МБ`)
            continue
          }
          if (totalBytes + f.size > MAX_TOTAL_BYTES) {
            toast.error("Суммарный размер больше 30 МБ")
            break
          }
          next.push(f)
          totalBytes += f.size
        }
        setFiles(next)
        // reset input value чтобы можно было выбрать тот же файл повторно
        if (fileInputRef.current) fileInputRef.current.value = ""
      }

      function removeFile(idx: number) {
        setFiles((prev) => prev.filter((_, i) => i !== idx))
      }

      function onSubmit() {
        const fd = new FormData()
        fd.set("ticketId", ticketId)
        fd.set("text", text)
        for (const f of files) fd.append("files", f)
        startTransition(async () => {
          const res = await sendChatMessageAction(fd)
          if (res.ok) {
            toast.success("Сообщение отправлено")
            setText("")
            setFiles([])
          } else {
            toast.error(res.error)
          }
        })
      }

      return (
        <div className="sticky bottom-0 border-t bg-background p-3 space-y-2">
          {files.length > 0 && (
            <div className="flex flex-wrap gap-2">
              {files.map((f, i) => (
                <div
                  key={i}
                  className="flex items-center gap-1 bg-muted px-2 py-1 rounded text-xs"
                >
                  <span>
                    {f.name} ({(f.size / 1024).toFixed(0)} КБ)
                  </span>
                  <button
                    type="button"
                    onClick={() => removeFile(i)}
                    className="ml-1 hover:text-destructive"
                    title="Удалить"
                  >
                    <X className="h-3 w-3" />
                  </button>
                </div>
              ))}
            </div>
          )}
          <div className="flex items-end gap-2">
            <input
              ref={fileInputRef}
              type="file"
              multiple
              accept={ALLOWED_MIME.join(",")}
              className="hidden"
              onChange={onFileSelect}
            />
            <Button
              type="button"
              variant="outline"
              size="sm"
              onClick={() => fileInputRef.current?.click()}
              disabled={isPending || !replySign}
              title="Прикрепить JPEG / PNG / PDF"
            >
              <Paperclip className="h-4 w-4" />
            </Button>
            <textarea
              value={text}
              onChange={(e) => setText(e.target.value)}
              maxLength={1000}
              rows={2}
              disabled={isPending || !replySign}
              placeholder={replySign ? "Сообщение (до 1000 символов)..." : "Нет replySign — запустите синхронизацию"}
              className="flex-1 resize-none rounded border px-2 py-1 text-sm focus:outline-none focus:ring-1 focus:ring-primary"
            />
            <div className="flex flex-col items-end gap-1">
              <span className="text-[10px] text-muted-foreground">
                {text.length}/1000
              </span>
              <Button type="button" size="sm" onClick={onSubmit} disabled={disabled}>
                {isPending ? (
                  <Loader2 className="h-4 w-4 animate-spin" />
                ) : (
                  <>
                    <Send className="h-4 w-4 mr-1" /> Отправить
                  </>
                )}
              </Button>
            </div>
          </div>
          {!replySign && (
            <p className="text-xs text-destructive">
              Нет replySign — запустите синхронизацию чата
            </p>
          )}
        </div>
      )
    }

    export default ChatReplyPanel
    ```

    **Шаг B — модифицировать `app/(dashboard)/support/[ticketId]/page.tsx`:**

    1. В `prisma.supportTicket.findUnique({..., select: {...}})` добавить в select: `chatReplySign: true`, `customerNameSnapshot: true`.
    2. Добавить импорт: `import { ChatReplyPanel } from "@/components/support/ChatReplyPanel"`.
    3. В JSX conditional block — расширить паттерн Phase 9:
    ```tsx
    {ticket.channel === "FEEDBACK" || ticket.channel === "QUESTION" ? (
      <ReplyPanel ticketId={ticket.id} disabled={ticket.status === "CLOSED"} />
    ) : ticket.channel === "RETURN" ? (
      <ReturnActionsPanel {...existingProps} />
    ) : ticket.channel === "CHAT" ? (
      <ChatReplyPanel ticketId={ticket.id} replySign={ticket.chatReplySign} />
    ) : (
      <div className="p-3 text-sm text-muted-foreground">
        Канал «{ticket.channel}» не поддерживает ответ.
      </div>
    )}
    ```

    **Шаг C — модифицировать `components/support/SupportDialog.tsx`:**

    Для каждого message в хронологии — если `message.isAutoReply === true`, рендерить inline badge:

    ```tsx
    import { Bot } from "lucide-react"
    // ...
    // Внутри map messages, рядом с меткой направления:
    {message.isAutoReply && (
      <span
        className="inline-flex items-center gap-1 text-[10px] text-muted-foreground"
        title="Автоответ"
      >
        <Bot className="h-3 w-3" />
        Автоответ
      </span>
    )}
    ```

    Конкретное расположение — внутри блока мета-информации bubble (где уже рендерится «Вы / Покупатель • время»). Паттерн Phase 8: `<div className="text-xs text-muted-foreground">...</div>` — добавить туда `isAutoReply` флаг.

    **⚠ Не ломать Phase 8/9:**
    - Phase 8 FEEDBACK/QUESTION сообщения `isAutoReply: false` (default) → badge не рендерится.
    - Phase 10 autoReply OUTBOUND → badge рендерится.
    - Phase 10 ручной OUTBOUND (через ChatReplyPanel) → `isAutoReply: false` → badge не рендерится.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/ChatReplyPanel.tsx` exit 0
    - `grep '"use client"' components/support/ChatReplyPanel.tsx` находит строку
    - `grep "sendChatMessageAction" components/support/ChatReplyPanel.tsx` находит строку
    - `grep 'accept=' components/support/ChatReplyPanel.tsx` находит image/jpeg + image/png + application/pdf
    - `grep 'MAX_FILE_BYTES\|5\s*\*\s*1024\s*\*\s*1024' components/support/ChatReplyPanel.tsx` находит 5 МБ limit
    - `grep "ChatReplyPanel" app/\(dashboard\)/support/\[ticketId\]/page.tsx` находит импорт + render
    - `grep "channel === \"CHAT\"" app/\(dashboard\)/support/\[ticketId\]/page.tsx` находит conditional
    - `grep "chatReplySign" app/\(dashboard\)/support/\[ticketId\]/page.tsx` находит select
    - `grep -c "ReplyPanel" app/\(dashboard\)/support/\[ticketId\]/page.tsx` >= 1 (Phase 8 не удалён)
    - `grep -c "ReturnActionsPanel" app/\(dashboard\)/support/\[ticketId\]/page.tsx` >= 1 (Phase 9 не удалён)
    - `grep "isAutoReply" components/support/SupportDialog.tsx` находит строку
    - `grep "Bot" components/support/SupportDialog.tsx` находит строку
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0 (prod build компилируется)
  </acceptance_criteria>
  <done>
    ChatReplyPanel — client multipart panel с клиентской валидацией размеров/MIME + preview + счётчик + disabled когда !replySign; /support/[ticketId]/page.tsx conditional расширен channel==="CHAT"; SupportDialog отображает Bot badge для isAutoReply сообщений; Phase 8 ReplyPanel и Phase 9 ReturnActionsPanel не сломаны; prod build GREEN.
  </done>
</task>

</tasks>

<verification>
После 2 задач:
1. `npx tsc --noEmit` exit 0
2. `npm run build` exit 0
3. `npm run test` exit 0 (Phase 7/8/9 + Phase 10 regression)
4. `grep -c "export.*ChatReplyPanel" components/support/ChatReplyPanel.tsx` >= 1
5. `grep -c "export async function sendChatMessageAction" app/actions/support.ts` == 1
6. `grep -c "isAutoReply" components/support/SupportDialog.tsx` >= 1
</verification>

<success_criteria>
Plan 10-03 завершён когда:
- app/actions/support.ts экспортирует sendChatMessageAction с полной валидацией + WB-first + SupportMedia create + revalidatePath
- components/support/ChatReplyPanel.tsx — функциональный клиентский компонент с multipart FormData, preview, счётчиком
- app/(dashboard)/support/[ticketId]/page.tsx conditional расширен CHAT → ChatReplyPanel (Phase 8/9 panels сохранены)
- components/support/SupportDialog.tsx отображает Bot badge для message.isAutoReply=true
- tests/chat-reply-panel.test.ts — ≥ 10 GREEN integration-тестов
- `npm run test` + `npm run build` exit 0
- Commit: `feat(10-03): ChatReplyPanel + sendChatMessageAction + Bot badge для isAutoReply`
</success_criteria>

<output>
Создать `.planning/phases/10-chat-autoreply/10-03-SUMMARY.md` с:
- Новый server action: sendChatMessageAction — сигнатура, validation, WB-first transaction order
- Новый клиентский компонент: ChatReplyPanel — props, клиентские лимиты (5 МБ/30 МБ), accept MIME
- Модификации: SupportDialog (Bot badge), /support/[ticketId]/page.tsx (conditional render)
- Файлы медиа: /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename} — формат пути идентичен Phase 8 downloadMediaBatch
- Backward compat: FEEDBACK/QUESTION продолжают использовать ReplyPanel; RETURN → ReturnActionsPanel
</output>
</content>
</invoke>