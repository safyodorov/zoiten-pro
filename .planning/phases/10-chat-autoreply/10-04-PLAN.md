---
phase: 10-chat-autoreply
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - app/(dashboard)/support/auto-reply/page.tsx
  - components/support/AutoReplyForm.tsx
  - app/actions/support.ts
  - lib/pricing-schemas.ts
  - components/layout/nav-items.ts
  - tests/auto-reply-settings.test.ts
  - deploy.sh
autonomous: false
requirements:
  - SUP-07
  - SUP-23
  - SUP-24
must_haves:
  truths:
    - "app/(dashboard)/support/auto-reply/page.tsx — RSC, requireSection('SUPPORT'), читает AutoReplyConfig id='default' и рендерит AutoReplyForm"
    - "components/support/AutoReplyForm.tsx — client component: toggle isEnabled + 7 чекбоксов дней (Пн-Вс, ISO 1..7) + 2 input type='time' + textarea messageText + select timezone + кнопка 'Сохранить' (НЕ 'Синхронизировать с WB' — WB API не имеет endpoint)"
    - "app/actions/support.ts экспортирует saveAutoReplyConfig(formData): Zod валидация (workdayStart/End HH:MM, workDays 1..7, messageText 1..1000); upsert AutoReplyConfig{id:'default'} с updatedById; revalidatePath('/support/auto-reply'); RBAC SUPPORT+MANAGE"
    - "Sidebar — в components/layout/nav-items.ts добавлен пункт 'Автоответ' (icon Bot) между 'Возвраты' и 'Сотрудники'"
    - "Под подсказкой textarea — список доступных переменных {имя_покупателя}, {название_товара}"
    - "tests/auto-reply-settings.test.ts GREEN — минимум 6 тестов (happy path upsert, Zod workdayStart regex, workDays range, messageText length, RBAC VIEWER reject, updatedById записывается)"
    - "deploy.sh расширен командами: проверка WB_CHAT_TOKEN в /etc/zoiten.pro.env; prisma migrate deploy; systemd-timer или crontab добавляет /api/cron/support-sync-chat 5 мин"
    - "Human UAT checkpoint пройден — оператор протестировал настройки, получил реальное сообщение покупателя, проверил автоответ вне рабочих часов"
  artifacts:
    - path: "app/(dashboard)/support/auto-reply/page.tsx"
      provides: "RSC страница настроек автоответа"
      exports:
        - default
    - path: "components/support/AutoReplyForm.tsx"
      provides: "Client форма редактирования AutoReplyConfig"
      exports:
        - AutoReplyForm
    - path: "app/actions/support.ts"
      provides: "saveAutoReplyConfig server action"
      exports:
        - saveAutoReplyConfig
    - path: "lib/pricing-schemas.ts"
      provides: "autoReplyConfigSchema Zod (добавляется к существующему файлу)"
      exports:
        - autoReplyConfigSchema
    - path: "components/layout/nav-items.ts"
      provides: "Sidebar — пункт 'Автоответ'"
    - path: "tests/auto-reply-settings.test.ts"
      provides: "Integration тесты saveAutoReplyConfig + Zod"
      contains: "describe"
    - path: "deploy.sh"
      provides: "Deploy runbook включает systemd timer для 5-мин cron"
  key_links:
    - from: "app/(dashboard)/support/auto-reply/page.tsx"
      to: "requireSection(\"SUPPORT\")"
      via: "RBAC read guard"
      pattern: "requireSection\\(\"SUPPORT\"\\)"
    - from: "app/actions/support.ts saveAutoReplyConfig"
      to: "requireSection(\"SUPPORT\", \"MANAGE\")"
      via: "RBAC write guard"
      pattern: "requireSection\\(\"SUPPORT\",\\s*\"MANAGE\"\\)"
    - from: "app/actions/support.ts saveAutoReplyConfig"
      to: "prisma.autoReplyConfig.upsert({where: {id: \"default\"}})"
      via: "singleton upsert + updatedById"
      pattern: "id:\\s*\"default\""
    - from: "components/support/AutoReplyForm.tsx"
      to: "app/actions/support.ts saveAutoReplyConfig"
      via: "FormData submit"
      pattern: "saveAutoReplyConfig"
    - from: "components/layout/nav-items.ts"
      to: "/support/auto-reply"
      via: "NAV_ITEMS entry"
      pattern: "/support/auto-reply"
---

<objective>
Phase 10 Plan 04 — настройки автоответа + deploy + UAT:

1. **`app/(dashboard)/support/auto-reply/page.tsx`** (RSC):
   - `await requireSection("SUPPORT")` (read).
   - `const config = await prisma.autoReplyConfig.findUnique({where: {id: "default"}})` — fallback к defaults если null (должен быть засеен в Plan 10-01 миграции).
   - Render `<AutoReplyForm config={config} />`.
   - Заголовок: «Автоответ в чате».

2. **`components/support/AutoReplyForm.tsx`** (client):
   - Поля: `isEnabled` (toggle/checkbox), `workDays` (7 native checkboxes Пн-Вс, mapping 1..7), `workdayStart` / `workdayEnd` (native `<input type="time">`), `messageText` (textarea ≤1000 + подсказка переменных), `timezone` (native `<select>` с опциями Europe/Moscow default + Europe/Berlin + UTC).
   - Submit через FormData → `saveAutoReplyConfig` (имя кнопки — «Сохранить», НЕ «Синхронизировать с WB» — D-01 из user_decisions).
   - Счётчик символов textarea.
   - Подсказка переменных под textarea: `Доступны: {имя_покупателя}, {название_товара}`.

3. **`app/actions/support.ts` — добавить `saveAutoReplyConfig`:**
   - `"use server"`, `requireSection("SUPPORT", "MANAGE")`, `getSessionUserId()`.
   - Парсинг FormData + Zod через `autoReplyConfigSchema` (в lib/pricing-schemas.ts).
   - `upsert({where:{id:"default"}, create:{...parsed, id:"default", updatedById:userId}, update:{...parsed, updatedById:userId}})`.
   - `revalidatePath("/support/auto-reply")`.
   - Return `ActionResult`.

4. **`lib/pricing-schemas.ts` — добавить `autoReplyConfigSchema`:**
   - **Важное ограничение проекта** (STATE.md decision из Phase 7): Zod схемы хранятся в `lib/pricing-schemas.ts`, НЕ в `"use server"` файлах (vitest не может загрузить auth chain из server actions).
   - Добавляем в существующий файл `lib/pricing-schemas.ts`:
   ```typescript
   export const autoReplyConfigSchema = z.object({
     isEnabled: z.boolean(),
     workdayStart: z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/, "Формат HH:MM"),
     workdayEnd: z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/, "Формат HH:MM"),
     workDays: z.array(z.number().int().min(1).max(7)).max(7),
     messageText: z.string().min(1).max(1000),
     timezone: z.string().min(1).max(64),
   })
   export type AutoReplyConfigInput = z.infer<typeof autoReplyConfigSchema>
   ```

5. **Sidebar — `components/layout/nav-items.ts`:**
   - Добавить `{ section: "SUPPORT", href: "/support/auto-reply", label: "Автоответ", icon: "Bot" }` **после** `/support/returns`, **перед** `/employees`.
   - Добавить `Bot` из lucide-react в ICON_MAP.

6. **Deploy — `deploy.sh` расширение** (новая секция):
   - Сообщение: «Проверка WB_CHAT_TOKEN в /etc/zoiten.pro.env» — grep + warning если отсутствует (не fail, дать пользователю знать).
   - `npx prisma migrate deploy` — уже есть в deploy.sh Phase 6, здесь просто убедиться что Plan 10-01 миграция применится.
   - Добавить systemd timer ИЛИ crontab entry для `/api/cron/support-sync-chat` каждые 5 мин:
     ```
     # crontab:
     */5 * * * * curl -s -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/support-sync-chat > /dev/null 2>&1
     ```
   - Обновить DEPLOY.md runbook (если есть) с инструкцией добавить строку в crontab после деплоя.

7. **Test — заменить Wave 0 stub `tests/auto-reply-settings.test.ts`:**
   - Integration тесты `saveAutoReplyConfig` с mock Prisma + mock auth + mock requireSection.
   - Минимум 6 тестов: happy path, Zod invalid workdayStart, Zod invalid workDays range, Zod too short messageText, RBAC reject, updatedById записывается.

8. **Human UAT checkpoint** (Task 4):
   - Deploy на VPS.
   - Инструкция оператору:
     - Открыть `/support/auto-reply` → заполнить настройки → нажать «Сохранить» → увидеть toast «Сохранено».
     - Перезагрузить страницу — значения сохранились.
     - Проверить sidebar badge «Автоответ» появился.
     - Подождать 5 минут → `journalctl -u zoiten-erp -f | grep "support-sync-chat"` → увидеть cron tick.
     - Опционально: попросить покупателя написать в чат вне рабочих часов → через ≤5 мин получить автоответ с корректными переменными `{имя_покупателя}` / `{название_товара}`.
   - Resume-signal: «approved» или описание проблем.

**Purpose:** UX-слой настроек автоответа + production deploy + верификация end-to-end. SUP-24 реализован как **локальная** ERP-feature (кнопка «Сохранить», НЕ «Синхронизировать с WB» — WB API не имеет endpoint для auto-reply config). Sidebar добавляет «Автоответ» как второй sub-пункт Support (после «Возвраты»).

**Output:** 1 новая RSC-страница + 1 новый клиентский компонент + 1 server action + 1 модификация схем + 1 sidebar entry + 1 наполненный тест + расширенный deploy.sh + human UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-chat-autoreply/10-RESEARCH.md
@.planning/phases/10-chat-autoreply/10-VALIDATION.md
@.planning/phases/10-chat-autoreply/10-01-SUMMARY.md
@.planning/phases/10-chat-autoreply/10-02-SUMMARY.md
@.planning/phases/10-chat-autoreply/10-03-SUMMARY.md
@.planning/phases/09-returns/09-04-SUMMARY.md
@CLAUDE.md
@DEPLOY.md
@deploy.sh
@app/actions/support.ts
@app/actions/pricing.ts
@lib/pricing-schemas.ts
@lib/rbac.ts
@lib/prisma.ts
@components/layout/nav-items.ts
@components/support/ReturnActionsPanel.tsx
@prisma/schema.prisma

<interfaces>
**Новая Prisma модель (Plan 10-01):**
```prisma
model AutoReplyConfig {
  id            String    @id                    // "default"
  isEnabled     Boolean   @default(false)
  workdayStart  String    @default("09:00")
  workdayEnd    String    @default("18:00")
  workDays      Int[]     @default([1,2,3,4,5])  // ISO 1=Mon..7=Sun
  messageText   String    @db.Text
  timezone      String    @default("Europe/Moscow")
  updatedById   String?
  updatedAt     DateTime  @updatedAt
  updatedBy     User?     @relation("AutoReplyUpdater", fields: [updatedById], references: [id], onDelete: SetNull)
}
```

**User.autoReplyUpdates** relation — обратное поле (Plan 10-01).

**ActionResult (app/actions/support.ts Phase 8):**
```typescript
export type ActionResult = { ok: true } | { ok: false; error: string }
```

**getSessionUserId helper (Phase 8 в app/actions/support.ts):**
```typescript
async function getSessionUserId(): Promise<string | null>
```

**requireSection (lib/rbac.ts):**
```typescript
await requireSection("SUPPORT")            // read
await requireSection("SUPPORT", "MANAGE")  // write
```

**lib/pricing-schemas.ts (существующий Phase 7):**
```typescript
import { z } from "zod"
export const globalRatesSchema = z.object({...})
export const calculatedPriceSchema = z.object({...})
// + другие Phase 7 схемы — их не трогаем
```
Добавляем `autoReplyConfigSchema` в конец.

**NAV_ITEMS pattern (components/layout/nav-items.ts):**
```typescript
{ section: "SUPPORT", href: "/support/returns", label: "Возвраты", icon: "PackageX" },
// Добавить НИЖЕ этой строки:
{ section: "SUPPORT", href: "/support/auto-reply", label: "Автоответ", icon: "Bot" },
{ section: "EMPLOYEES", ... }  // существующая
```

ICON_MAP — добавить `Bot: Bot` + импорт `Bot` из lucide-react.

**deploy.sh (Phase 6 base):**
- Содержит `npx prisma migrate deploy`, `pm2 reload` (или systemd restart).
- Phase 9 deploy task добавил проверку `WB_RETURNS_TOKEN` (паттерн).
- Phase 10 ДОЛЖЕН добавить проверку `WB_CHAT_TOKEN` + crontab-строку для 5-мин cron.

**Native time inputs — браузер отдаёт значение в формате "HH:MM" (валидно для regex `^([01]\d|2[0-3]):[0-5]\d$`).**

**ISO day of week:**
- JS `getDay()`: 0=Sun..6=Sat
- ISO 8601: 1=Mon..7=Sun
- В UI показываем Пн-Вс в ISO порядке, маппим на `workDays: Int[]` 1..7.

**Пример isWithinWorkingHours (Plan 10-02) — для понимания:**
```typescript
const jsDay = tzDate.getDay()
const isoDay = jsDay === 0 ? 7 : jsDay
if (!config.workDays.includes(isoDay)) return false
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: saveAutoReplyConfig server action + Zod схема + test GREEN</name>
  <files>app/actions/support.ts, lib/pricing-schemas.ts, tests/auto-reply-settings.test.ts</files>
  <read_first>
    - lib/pricing-schemas.ts (Phase 7 — добавить autoReplyConfigSchema в конец файла, не ломать существующие схемы)
    - app/actions/support.ts (Phase 8/9/10 — паттерн server action с requireSection + getSessionUserId + revalidatePath)
    - tests/pricing-settings.test.ts (Phase 7 — паттерн Zod + upsert test)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «`/support/auto-reply` server action» строки 820-845 — эталонный код)
    - tests/auto-reply-settings.test.ts (Wave 0 stub — заменить)
    - CLAUDE.md (раздел «Conventions» — Zod валидация, Moscow timezone)
  </read_first>
  <behavior>
    - `autoReplyConfigSchema`: Zod с полями isEnabled (boolean), workdayStart/End (regex HH:MM 00:00-23:59), workDays (array 1..7, max 7 items), messageText (min 1 max 1000), timezone (min 1 max 64)
    - `saveAutoReplyConfig(formData)`:
      1. requireSection SUPPORT,MANAGE
      2. getSessionUserId — reject если null
      3. Парсинг FormData в объект:
         - `isEnabled = formData.get("isEnabled") === "true"` (или "on" для native checkbox)
         - `workdayStart = String(formData.get("workdayStart"))`
         - `workdayEnd = String(formData.get("workdayEnd"))`
         - `workDays = formData.getAll("workDays").map(v => Number.parseInt(String(v), 10))`
         - `messageText = String(formData.get("messageText"))`
         - `timezone = String(formData.get("timezone") ?? "Europe/Moscow")`
      4. `parsed = autoReplyConfigSchema.safeParse(data)` — reject если !parsed.success (error.issues[0].message)
      5. upsert: `prisma.autoReplyConfig.upsert({where: {id: "default"}, create: {id: "default", ...parsed.data, updatedById: userId}, update: {...parsed.data, updatedById: userId}})`
      6. revalidatePath("/support/auto-reply"); revalidatePath("/support")
      7. return { ok: true }
  </behavior>
  <action>
    **Шаг A — добавить в `lib/pricing-schemas.ts` (конец файла):**

    ```typescript
    // ──────────────────────────────────────────────────────────────
    // Phase 10: AutoReplyConfig
    // ──────────────────────────────────────────────────────────────

    export const autoReplyConfigSchema = z.object({
      isEnabled: z.boolean(),
      workdayStart: z.string().regex(
        /^([01]\d|2[0-3]):[0-5]\d$/,
        "workdayStart должен быть в формате HH:MM"
      ),
      workdayEnd: z.string().regex(
        /^([01]\d|2[0-3]):[0-5]\d$/,
        "workdayEnd должен быть в формате HH:MM"
      ),
      workDays: z
        .array(z.number().int().min(1, "День недели — 1..7 (ISO 8601)").max(7, "День недели — 1..7 (ISO 8601)"))
        .max(7, "Максимум 7 дней")
        .default([]),
      messageText: z
        .string()
        .min(1, "Текст не может быть пустым")
        .max(1000, "Максимум 1000 символов"),
      timezone: z.string().min(1).max(64).default("Europe/Moscow"),
    })

    export type AutoReplyConfigInput = z.infer<typeof autoReplyConfigSchema>
    ```

    **Шаг B — добавить в `app/actions/support.ts` (рядом с sendChatMessageAction):**

    ```typescript
    import { autoReplyConfigSchema } from "@/lib/pricing-schemas"

    export async function saveAutoReplyConfig(formData: FormData): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const raw = {
          isEnabled: formData.get("isEnabled") === "true" || formData.get("isEnabled") === "on",
          workdayStart: String(formData.get("workdayStart") ?? ""),
          workdayEnd: String(formData.get("workdayEnd") ?? ""),
          workDays: formData
            .getAll("workDays")
            .map((v) => Number.parseInt(String(v), 10))
            .filter((n) => Number.isFinite(n)),
          messageText: String(formData.get("messageText") ?? ""),
          timezone: String(formData.get("timezone") ?? "Europe/Moscow"),
        }

        const parsed = autoReplyConfigSchema.safeParse(raw)
        if (!parsed.success) {
          return { ok: false, error: parsed.error.issues[0]?.message ?? "Невалидные данные" }
        }

        await prisma.autoReplyConfig.upsert({
          where: { id: "default" },
          create: { id: "default", ...parsed.data, updatedById: userId },
          update: { ...parsed.data, updatedById: userId },
        })

        revalidatePath("/support/auto-reply")
        revalidatePath("/support")
        return { ok: true }
      } catch (err) {
        return {
          ok: false,
          error: err instanceof Error ? err.message : "Ошибка сохранения",
        }
      }
    }
    ```

    **Шаг C — заменить `tests/auto-reply-settings.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    const state = {
      configs: [] as any[],
    }
    const upsertMock = vi.fn(async ({ where, create, update }: any) => {
      const existing = state.configs.find((c) => c.id === where.id)
      if (existing) {
        Object.assign(existing, update)
        return existing
      }
      const c = { ...create }
      state.configs.push(c)
      return c
    })
    const requireSectionMock = vi.fn()
    const authMock = vi.fn()
    const revalidateMock = vi.fn()

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        autoReplyConfig: { upsert: upsertMock },
      },
    }))

    vi.mock("@/lib/rbac", () => ({
      requireSection: requireSectionMock,
    }))

    vi.mock("@/lib/auth", () => ({
      auth: authMock,
    }))

    vi.mock("next/cache", () => ({
      revalidatePath: revalidateMock,
    }))

    beforeEach(() => {
      state.configs = []
      upsertMock.mockClear()
      requireSectionMock.mockReset()
      requireSectionMock.mockResolvedValue(undefined)
      authMock.mockReset()
      authMock.mockResolvedValue({ user: { id: "U1" } })
      revalidateMock.mockReset()
    })
    afterEach(() => vi.resetAllMocks())

    function validFormData(overrides: Partial<Record<string, any>> = {}): FormData {
      const fd = new FormData()
      fd.set("isEnabled", overrides.isEnabled ?? "true")
      fd.set("workdayStart", (overrides.workdayStart as string) ?? "09:00")
      fd.set("workdayEnd", (overrides.workdayEnd as string) ?? "18:00")
      const days = (overrides.workDays as number[]) ?? [1, 2, 3, 4, 5]
      for (const d of days) fd.append("workDays", String(d))
      fd.set("messageText", (overrides.messageText as string) ?? "Привет!")
      fd.set("timezone", (overrides.timezone as string) ?? "Europe/Moscow")
      return fd
    }

    describe("saveAutoReplyConfig — happy path", () => {
      it("upsert AutoReplyConfig с id='default' + updatedById", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData())
        expect(res).toEqual({ ok: true })
        expect(upsertMock).toHaveBeenCalledWith(
          expect.objectContaining({
            where: { id: "default" },
            create: expect.objectContaining({ id: "default", updatedById: "U1", isEnabled: true }),
            update: expect.objectContaining({ updatedById: "U1" }),
          })
        )
        expect(revalidateMock).toHaveBeenCalledWith("/support/auto-reply")
      })

      it("сохраняет workDays как массив number[]", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        await saveAutoReplyConfig(validFormData({ workDays: [2, 4, 6] }))
        const call = upsertMock.mock.calls[0][0]
        expect(call.create.workDays).toEqual([2, 4, 6])
      })
    })

    describe("saveAutoReplyConfig — Zod validation", () => {
      it("reject: workdayStart не HH:MM", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData({ workdayStart: "25:99" }))
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("HH:MM")
      })

      it("reject: workDays вне 1..7", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData({ workDays: [0, 8] }))
        expect(res.ok).toBe(false)
        expect((res as any).error).toMatch(/1\.\.7|ISO/i)
      })

      it("reject: messageText пустой", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData({ messageText: "" }))
        expect(res.ok).toBe(false)
        expect((res as any).error).toMatch(/пуст|empty/i)
      })

      it("reject: messageText > 1000 символов", async () => {
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData({ messageText: "x".repeat(1001) }))
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("1000")
      })
    })

    describe("saveAutoReplyConfig — RBAC", () => {
      it("reject: VIEWER без MANAGE (requireSection throws)", async () => {
        requireSectionMock.mockRejectedValueOnce(new Error("Недостаточно прав"))
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData())
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("Недостаточно")
        expect(upsertMock).not.toHaveBeenCalled()
      })

      it("reject: нет user.id в сессии", async () => {
        authMock.mockResolvedValueOnce({ user: null })
        const { saveAutoReplyConfig } = await import("@/app/actions/support")
        const res = await saveAutoReplyConfig(validFormData())
        expect(res.ok).toBe(false)
        expect((res as any).error).toContain("user.id")
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/auto-reply-settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const autoReplyConfigSchema" lib/pricing-schemas.ts` находит строку
    - `grep "export async function saveAutoReplyConfig" app/actions/support.ts` находит строку
    - `grep 'id: "default"' app/actions/support.ts` находит строку
    - `grep "autoReplyConfigSchema" app/actions/support.ts` находит импорт
    - `grep 'revalidatePath("/support/auto-reply")' app/actions/support.ts` находит строку
    - `grep -c "it(" tests/auto-reply-settings.test.ts` >= 6
    - `npm run test -- tests/auto-reply-settings.test.ts` exit 0 (≥8 GREEN)
    - `npm run test` exit 0 (Phase 7 pricing-settings.test.ts regression)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    autoReplyConfigSchema в lib/pricing-schemas.ts; saveAutoReplyConfig в app/actions/support.ts с singleton upsert + RBAC; ≥ 8 integration-тестов GREEN; Phase 7 pricing regression pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: RSC /support/auto-reply/page.tsx + AutoReplyForm client + Sidebar nav-item</name>
  <files>app/(dashboard)/support/auto-reply/page.tsx, components/support/AutoReplyForm.tsx, components/layout/nav-items.ts</files>
  <read_first>
    - app/(dashboard)/support/returns/page.tsx (Phase 9 — паттерн RSC для /support/* с requireSection)
    - components/support/ReturnsFilters.tsx или другой client form (паттерн native inputs без base-ui Select)
    - components/prices/GlobalRatesBar.tsx (Phase 7 — паттерн debounced save, но здесь без debounce — разовое «Сохранить»)
    - components/layout/nav-items.ts (существующий — паттерн добавления entry)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «`/support/auto-reply` страница настроек» строки 794-817)
    - CLAUDE.md (раздел «Conventions» — native `<select>`, русский, Moscow timezone)
  </read_first>
  <action>
    **Шаг A — создать `app/(dashboard)/support/auto-reply/page.tsx`:**

    ```tsx
    import { requireSection } from "@/lib/rbac"
    import { prisma } from "@/lib/prisma"
    import { AutoReplyForm } from "@/components/support/AutoReplyForm"

    export const dynamic = "force-dynamic"

    export default async function AutoReplyPage() {
      await requireSection("SUPPORT")

      const config = await prisma.autoReplyConfig.findUnique({
        where: { id: "default" },
      })

      return (
        <div className="max-w-2xl space-y-4 p-4">
          <div>
            <h2 className="text-lg font-semibold">Автоответ в чате</h2>
            <p className="text-sm text-muted-foreground">
              Вне рабочих часов — новым сообщениям покупателей в WB-чате уходит заранее
              подготовленный ответ. Локальная функция ERP (не требует настройки в WB).
            </p>
          </div>
          <AutoReplyForm config={config} />
        </div>
      )
    }
    ```

    **Шаг B — создать `components/support/AutoReplyForm.tsx`:**

    ```tsx
    "use client"

    import { useState, useTransition } from "react"
    import { toast } from "sonner"
    import { Save, Loader2 } from "lucide-react"
    import { Button } from "@/components/ui/button"
    import { saveAutoReplyConfig } from "@/app/actions/support"
    import type { AutoReplyConfig } from "@prisma/client"

    const DAY_LABELS: Array<{ iso: number; label: string }> = [
      { iso: 1, label: "Пн" },
      { iso: 2, label: "Вт" },
      { iso: 3, label: "Ср" },
      { iso: 4, label: "Чт" },
      { iso: 5, label: "Пт" },
      { iso: 6, label: "Сб" },
      { iso: 7, label: "Вс" },
    ]

    const TIMEZONES = [
      { value: "Europe/Moscow", label: "Москва (GMT+3)" },
      { value: "Europe/Kaliningrad", label: "Калининград (GMT+2)" },
      { value: "Asia/Yekaterinburg", label: "Екатеринбург (GMT+5)" },
      { value: "UTC", label: "UTC" },
    ]

    interface Props {
      config: AutoReplyConfig | null
    }

    export function AutoReplyForm({ config }: Props) {
      const [isEnabled, setIsEnabled] = useState(config?.isEnabled ?? false)
      const [workdayStart, setWorkdayStart] = useState(config?.workdayStart ?? "09:00")
      const [workdayEnd, setWorkdayEnd] = useState(config?.workdayEnd ?? "18:00")
      const [workDays, setWorkDays] = useState<number[]>(config?.workDays ?? [1, 2, 3, 4, 5])
      const [messageText, setMessageText] = useState(
        config?.messageText ??
          "Здравствуйте, {имя_покупателя}! Спасибо за обращение по товару «{название_товара}». Мы ответим в рабочее время."
      )
      const [timezone, setTimezone] = useState(config?.timezone ?? "Europe/Moscow")
      const [isPending, startTransition] = useTransition()

      function toggleDay(iso: number) {
        setWorkDays((prev) =>
          prev.includes(iso) ? prev.filter((d) => d !== iso) : [...prev, iso].sort((a, b) => a - b)
        )
      }

      function onSubmit(e: React.FormEvent) {
        e.preventDefault()
        const fd = new FormData()
        fd.set("isEnabled", String(isEnabled))
        fd.set("workdayStart", workdayStart)
        fd.set("workdayEnd", workdayEnd)
        for (const d of workDays) fd.append("workDays", String(d))
        fd.set("messageText", messageText)
        fd.set("timezone", timezone)

        startTransition(async () => {
          const res = await saveAutoReplyConfig(fd)
          if (res.ok) toast.success("Настройки сохранены")
          else toast.error(res.error)
        })
      }

      return (
        <form onSubmit={onSubmit} className="space-y-5 rounded-lg border p-4 bg-card">
          <label className="flex items-center gap-2">
            <input
              type="checkbox"
              checked={isEnabled}
              onChange={(e) => setIsEnabled(e.target.checked)}
              className="h-4 w-4"
            />
            <span className="text-sm font-medium">Включить автоответ</span>
          </label>

          <div className="space-y-1">
            <span className="text-sm font-medium">Рабочие дни</span>
            <div className="flex flex-wrap gap-2">
              {DAY_LABELS.map(({ iso, label }) => (
                <label
                  key={iso}
                  className={`flex items-center gap-1 px-2 py-1 rounded border cursor-pointer text-sm ${
                    workDays.includes(iso) ? "bg-primary/10 border-primary" : ""
                  }`}
                >
                  <input
                    type="checkbox"
                    checked={workDays.includes(iso)}
                    onChange={() => toggleDay(iso)}
                    className="h-3 w-3"
                  />
                  {label}
                </label>
              ))}
            </div>
          </div>

          <div className="grid grid-cols-2 gap-3">
            <label className="space-y-1">
              <span className="text-sm font-medium">Начало рабочего дня</span>
              <input
                type="time"
                value={workdayStart}
                onChange={(e) => setWorkdayStart(e.target.value)}
                className="w-full rounded border px-2 py-1 text-sm"
                required
              />
            </label>
            <label className="space-y-1">
              <span className="text-sm font-medium">Конец рабочего дня</span>
              <input
                type="time"
                value={workdayEnd}
                onChange={(e) => setWorkdayEnd(e.target.value)}
                className="w-full rounded border px-2 py-1 text-sm"
                required
              />
            </label>
          </div>

          <label className="space-y-1 block">
            <span className="text-sm font-medium">Часовой пояс</span>
            <select
              value={timezone}
              onChange={(e) => setTimezone(e.target.value)}
              className="w-full rounded border px-2 py-1 text-sm"
            >
              {TIMEZONES.map((tz) => (
                <option key={tz.value} value={tz.value}>
                  {tz.label}
                </option>
              ))}
            </select>
          </label>

          <label className="space-y-1 block">
            <span className="text-sm font-medium">
              Текст автоответа ({messageText.length}/1000)
            </span>
            <textarea
              value={messageText}
              onChange={(e) => setMessageText(e.target.value)}
              maxLength={1000}
              rows={5}
              required
              className="w-full rounded border px-2 py-1 text-sm resize-none"
            />
            <p className="text-xs text-muted-foreground">
              Доступны переменные:{" "}
              <code className="px-1 rounded bg-muted">{"{имя_покупателя}"}</code>,{" "}
              <code className="px-1 rounded bg-muted">{"{название_товара}"}</code>
            </p>
          </label>

          <div className="flex justify-end">
            <Button type="submit" disabled={isPending}>
              {isPending ? (
                <Loader2 className="h-4 w-4 animate-spin mr-1" />
              ) : (
                <Save className="h-4 w-4 mr-1" />
              )}
              Сохранить
            </Button>
          </div>
        </form>
      )
    }
    ```

    **Шаг C — модифицировать `components/layout/nav-items.ts`:**

    1. Добавить импорт `Bot` из lucide-react в список импортов.
    2. Добавить в NAV_ITEMS **после** `{ section: "SUPPORT", href: "/support/returns", ... }` и **до** `{ section: "EMPLOYEES", href: "/employees", ... }`:
    ```typescript
    { section: "SUPPORT", href: "/support/auto-reply", label: "Автоответ", icon: "Bot" },
    ```
    3. Добавить в ICON_MAP: `Bot,`.

    Результат (образно):
    ```typescript
    import { ..., Headphones, PackageX, Bot, UserCheck, ... } from "lucide-react"

    export const NAV_ITEMS: NavItem[] = [
      // ...
      { section: "SUPPORT", href: "/support", label: "Служба поддержки", icon: "Headphones" },
      { section: "SUPPORT", href: "/support/returns", label: "Возвраты", icon: "PackageX" },
      { section: "SUPPORT", href: "/support/auto-reply", label: "Автоответ", icon: "Bot" },
      { section: "EMPLOYEES", href: "/employees", label: "Сотрудники", icon: "UserCheck" },
      // ...
    ]

    export const ICON_MAP: Record<string, React.ComponentType<{ className?: string }>> = {
      // ... existing
      Bot,
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/\(dashboard\)/support/auto-reply/page.tsx` exit 0
    - `grep 'requireSection("SUPPORT")' app/\(dashboard\)/support/auto-reply/page.tsx` находит строку
    - `grep 'id: "default"' app/\(dashboard\)/support/auto-reply/page.tsx` находит строку
    - `grep "AutoReplyForm" app/\(dashboard\)/support/auto-reply/page.tsx` находит импорт + render
    - `test -f components/support/AutoReplyForm.tsx` exit 0
    - `grep '"use client"' components/support/AutoReplyForm.tsx` находит строку
    - `grep "saveAutoReplyConfig" components/support/AutoReplyForm.tsx` находит импорт
    - `grep "type=\"time\"" components/support/AutoReplyForm.tsx` находит 2 строки (start + end)
    - `grep '{имя_покупателя}' components/support/AutoReplyForm.tsx` находит (подсказка переменных)
    - `grep '{название_товара}' components/support/AutoReplyForm.tsx` находит
    - `grep "Сохранить" components/support/AutoReplyForm.tsx` находит (кнопка НЕ «Синхронизировать с WB»)
    - `grep "/support/auto-reply" components/layout/nav-items.ts` находит NAV_ITEMS entry
    - `grep "Bot" components/layout/nav-items.ts` находит в импортах И в ICON_MAP
    - `npx tsc --noEmit` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    /support/auto-reply RSC страница рендерит AutoReplyForm из загруженного config; AutoReplyForm — client форма с native inputs (toggle + 7 day checkboxes + 2 time + textarea + select timezone); кнопка «Сохранить» (не «Синхронизировать с WB»); sidebar «Автоответ» добавлен под «Возвраты»; prod build GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Deploy на VPS — WB_CHAT_TOKEN проверка + prisma migrate + crontab 5-мин</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh (текущий скрипт Phase 6/8/9 — паттерн bash + prisma migrate deploy + systemctl reload)
    - DEPLOY.md (если есть — runbook)
    - app/api/cron/support-sync-chat/route.ts (Plan 10-02 — endpoint)
    - .planning/phases/09-returns/09-04-SUMMARY.md (Phase 9 deploy паттерн WB_RETURNS_TOKEN)
  </read_first>
  <action>
    Модифицировать `deploy.sh` (или создать если его нет) — добавить Phase 10 секцию.

    **Содержание расширения:**

    ```bash
    # ── Phase 10: WB_CHAT_TOKEN проверка (warning only, не fail) ──
    if ! grep -q "^WB_CHAT_TOKEN=" /etc/zoiten.pro.env 2>/dev/null; then
      echo "⚠ WARNING: WB_CHAT_TOKEN не найден в /etc/zoiten.pro.env"
      echo "  Синхронизация чата не будет работать без этого токена."
      echo "  Выпустите токен в seller.wildberries.ru → Настройки → Доступ к API"
      echo "  → scope 'Чат с покупателями' (bit 9)"
      echo "  Затем: echo 'WB_CHAT_TOKEN=<token>' >> /etc/zoiten.pro.env"
      echo "         systemctl restart zoiten-erp.service"
    else
      echo "✓ WB_CHAT_TOKEN присутствует в /etc/zoiten.pro.env"
    fi

    # ── Phase 10: Crontab для support-sync-chat (каждые 5 минут) ──
    CRON_LINE="*/5 * * * * curl -s -H \"x-cron-secret: \$CRON_SECRET\" http://localhost:3001/api/cron/support-sync-chat > /dev/null 2>&1"

    if ! crontab -l 2>/dev/null | grep -q "support-sync-chat"; then
      echo "Добавляю crontab-строку для support-sync-chat (5 мин)"
      (crontab -l 2>/dev/null; echo "$CRON_LINE") | crontab -
    else
      echo "✓ support-sync-chat уже в crontab"
    fi
    ```

    **Интеграция в deploy.sh:**
    - Вставить **после** `npx prisma migrate deploy` (чтобы миграция Plan 10-01 применилась первой).
    - **Перед** `systemctl restart zoiten-erp.service` (чтобы env var подхватился после перезапуска).

    **Если deploy.sh использует рабочую директорию /opt/zoiten-pro:**
    ```bash
    cd /opt/zoiten-pro
    # ... existing
    npx prisma migrate deploy
    # ── Phase 10 добавление здесь ──
    # ... existing systemctl restart
    ```

    **Актуальный deploy flow:**
    1. git pull
    2. npm ci
    3. npx prisma generate
    4. npx prisma migrate deploy
    5. npm run build
    6. **НОВОЕ**: Phase 10 env check + crontab
    7. systemctl restart zoiten-erp.service

    Человек (Sergey) запустит deploy.sh через SSH после ручной пуш-команды:
    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    ```

    Это часть Task 4 (human UAT) — оператор выполняет команду.

    Update DEPLOY.md (если существует) — добавить секцию:
    - «Phase 10 prerequisite: добавить WB_CHAT_TOKEN в /etc/zoiten.pro.env»
    - Проверка: `curl -sI https://buyer-chat-api.wildberries.ru/ping -H "Authorization: $WB_CHAT_TOKEN"` → 200 OK
  </action>
  <verify>
    <automated>grep -q "WB_CHAT_TOKEN" deploy.sh && grep -q "support-sync-chat" deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep "WB_CHAT_TOKEN" deploy.sh` находит строку
    - `grep "support-sync-chat" deploy.sh` находит строку
    - `grep "\\*/5 \\* \\* \\* \\*" deploy.sh` находит crontab 5-min pattern
    - `grep "prisma migrate deploy" deploy.sh` всё ещё присутствует (не сломали Phase 6 deploy)
    - `bash -n deploy.sh` exit 0 (синтаксис корректен)
  </acceptance_criteria>
  <done>
    deploy.sh расширен проверкой WB_CHAT_TOKEN (warning-only, не fail) + crontab для /api/cron/support-sync-chat 5 мин; синтаксис bash корректен; Phase 6/8/9 команды deploy не сломаны.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Human UAT — deploy + end-to-end верификация</name>
  <what-built>
    - Phase 10 Plans 10-01, 10-02, 10-03, 10-04 завершены (код, тесты, миграция, sidebar)
    - VPS deploy с Plan 10-01 миграцией применённой
    - /etc/zoiten.pro.env содержит WB_CHAT_TOKEN
    - crontab содержит строку для /api/cron/support-sync-chat
    - systemd zoiten-erp.service перезапущен
  </what-built>
  <how-to-verify>
    **Шаг 1 — Подготовка токена (ОДНОРАЗОВО, до Task 4):**
    1. Зайти на `https://seller.wildberries.ru/supplier-settings/access-to-api`
    2. Создать токен с scope **«Чат с покупателями» (bit 9)**
    3. SSH: `ssh root@85.198.97.89`
    4. `echo 'WB_CHAT_TOKEN=<скопированный_токен>' >> /etc/zoiten.pro.env`
    5. Проверка scope: `source /etc/zoiten.pro.env && curl -sI https://buyer-chat-api.wildberries.ru/ping -H "Authorization: $WB_CHAT_TOKEN"`
       - Ожидаемо: `HTTP/2 200`
       - Если 401/403 → токен плох, перевыпустить

    **Шаг 2 — Deploy:**
    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    ```
    Ожидаемо в логах:
    - `✓ WB_CHAT_TOKEN присутствует в /etc/zoiten.pro.env`
    - `Применены миграции: ... phase10_chat_autoreply`
    - `Добавляю crontab-строку для support-sync-chat`
    - `systemctl restart zoiten-erp.service` ОК

    Проверить service status: `ssh root@85.198.97.89 "systemctl status zoiten-erp.service"` → active (running)

    **Шаг 3 — UI sanity checks:**

    3.1. Открыть `https://zoiten.pro/support/auto-reply`:
         - Страница загружается без 500
         - Видна форма: toggle, 7 дней Пн-Вс, 2 time inputs, textarea, select timezone, кнопка «Сохранить»
         - Нет кнопки «Синхронизировать с WB» (reformulated per user_decisions)
         - Подсказка `{имя_покупателя}`, `{название_товара}` под textarea
         - Sidebar: пункт «Автоответ» (Bot icon) виден под «Возвраты»

    3.2. Настроить и сохранить:
         - isEnabled: ON
         - Дни: Пн-Пт (1..5)
         - Часы: 09:00 — 18:00
         - TZ: Europe/Moscow
         - Текст: «Здравствуйте, {имя_покупателя}! Мы ответим в рабочее время. Товар: {название_товара}»
         - Нажать «Сохранить» → toast «Настройки сохранены»
         - Reload страницы → значения сохранились ✓

    3.3. Открыть `https://zoiten.pro/support`:
         - Если есть CHAT-тикеты (cron уже подтянул) — они видны как карточки канала CHAT
         - Открыть один CHAT тикет `/support/{id}` → вместо ReplyPanel видна ChatReplyPanel (textarea + paperclip + Send)
         - Ввести текст → нажать Send → toast «Сообщение отправлено» (проверка позже на WB стороне)

    **Шаг 4 — Cron проверка:**

    4.1. Подождать 5 минут после деплоя.

    4.2. Проверить логи: `ssh root@85.198.97.89 "journalctl -u zoiten-erp.service -f --since '10 min ago' | grep -i chat"`
         - Ожидаемо: строки `syncChats` или `runAutoReplies` (если есть INBOUND CHAT за последние 24ч)

    4.3. `ssh root@85.198.97.89 "crontab -l | grep support-sync-chat"` → строка видна

    **Шаг 5 — End-to-end autoreply (если есть тест-покупатель):**

    5.1. В нерабочее время (например, 23:00 МСК или Сб) попросить покупателя написать в чат WB по тест-товару.

    5.2. Проверить через ≤5 мин:
         - В `/support` появился новый CHAT-тикет.
         - В `/support/{id}` — INBOUND от покупателя + OUTBOUND автоответ с 🤖 иконкой.
         - Переменные подставлены: `{имя_покупателя}` → реальное имя, `{название_товара}` → название товара.
         - WB-мобильное приложение покупателя — сообщение от продавца доставлено.

    5.3. Если Шаг 5 не доступен (нет тест-покупателя) — пропустить и отметить как "UAT deferred to production traffic".

    **Шаг 6 — Regression:**
         - `/support` (лента с FEEDBACK/QUESTION/RETURN) работает как до Phase 10 ✓
         - `/support/returns` (Phase 9) не сломано ✓
         - `/cards/wb` (Phase 7) не сломано ✓
         - Sidebar пункты: Возвраты + Автоответ + всё старое присутствует ✓
  </how-to-verify>
  <files>N/A (checkpoint — user interaction, нет файловых изменений)</files>
  <action>Оператор выполняет шаги 1-6 из how-to-verify на VPS и в браузере. Claude не модифицирует файлы.</action>
  <verify>
    <automated>MISSING — human verification required (VPS deploy log + live UI + 5-min cron tick + optional e2e autoreply)</automated>
  </verify>
  <done>
    Оператор вернул resume-signal 'approved' (либо 'approved с deferred e2e' если тест-покупатель недоступен); Phase 7/8/9 разделы не сломаны (regression Step 6 pass).
  </done>
  <resume-signal>
    Type "approved" if всё работает ИЛИ опишите проблемы (url + шаг + ошибка).
    Если Шаг 5 deferred — укажите: "approved (E2E autoreply deferred to production traffic)".
  </resume-signal>
</task>

</tasks>

<verification>
После 4 задач:
1. `npm run test` exit 0 (Phase 7/8/9 + Phase 10 GREEN)
2. `npm run build` exit 0
3. `grep "WB_CHAT_TOKEN" deploy.sh` exit 0
4. `grep "support-sync-chat" deploy.sh` exit 0
5. Deploy успешен (systemctl active)
6. Human UAT checkpoint approved (либо approved с deferred e2e)
</verification>

<success_criteria>
Plan 10-04 завершён когда:
- autoReplyConfigSchema Zod в lib/pricing-schemas.ts
- saveAutoReplyConfig в app/actions/support.ts с singleton upsert + RBAC + Zod
- /support/auto-reply RSC страница с AutoReplyForm client (кнопка «Сохранить», НЕ «Синхронизировать с WB»)
- Sidebar «Автоответ» (Bot icon) под «Возвраты»
- ≥ 8 GREEN тестов auto-reply-settings
- deploy.sh расширен WB_CHAT_TOKEN check + crontab 5-min
- Deploy на VPS успешен; systemctl active
- Human UAT checkpoint approved
- Commit: `feat(10-04): AutoReplyForm + saveAutoReplyConfig + deploy cron 5-min`
</success_criteria>

<output>
Создать `.planning/phases/10-chat-autoreply/10-04-SUMMARY.md` с:
- Новая Zod схема lib/pricing-schemas.ts: autoReplyConfigSchema + AutoReplyConfigInput
- Новый server action: saveAutoReplyConfig — upsert AutoReplyConfig{id:'default'} + updatedById
- Новая страница: /support/auto-reply (RSC) + AutoReplyForm (client)
- Sidebar entry: «Автоответ» под «Возвраты» (Bot icon)
- Deploy изменения: deploy.sh + WB_CHAT_TOKEN warning + crontab 5-min
- Human UAT result (approved / с deferred частями)
- VPS state: токен + миграция + cron активны
</output>
</content>
</invoke>