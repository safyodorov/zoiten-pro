---
phase: 10-chat-autoreply
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - lib/support-sync.ts
  - lib/auto-reply.ts
  - app/api/cron/support-sync-chat/route.ts
  - app/api/support-sync/route.ts
  - tests/support-sync-chats.test.ts
  - tests/auto-reply-cron.test.ts
autonomous: true
requirements:
  - SUP-07
  - SUP-21
  - SUP-25
must_haves:
  truths:
    - "lib/support-sync.ts экспортирует syncChats(): Phase B = GET /chats обновляет chatReplySign/customerNameSnapshot/previewText; Phase A = cursor-based GET /events с AppSetting lastEventNext; upsert SupportTicket по @@unique([channel=CHAT, wbExternalId=chatID]); upsert SupportMessage по wbEventId @unique; sender=client→INBOUND, sender=seller→OUTBOUND; attachments → SupportMedia (IMAGE для images, DOCUMENT для files)"
    - "lib/auto-reply.ts экспортирует runAutoReplies(): читает AutoReplyConfig id='default'; skip если isEnabled=false или внутри рабочих часов; подстановка {имя_покупателя} и {название_товара}; dedup per chat 24h; отправляет через sendChatMessage c isAutoReply=true"
    - "GET /api/cron/support-sync-chat защищён x-cron-secret, вызывает syncChats() + runAutoReplies() последовательно, возвращает объединённый результат"
    - "POST /api/support-sync (существующий Phase 8/9) расширен вызовом syncChats() + runAutoReplies() после syncSupport + syncReturns — ручная кнопка покрывает все 3 канала"
    - "syncChats идемпотентен: повторный вызов с тем же wbEventId не создаёт дубликаты (проверено integration-тестом)"
    - "runAutoReplies не отправляет OUTBOUND если AutoReplyConfig отсутствует в БД (fresh install) — graceful skip"
    - "tests/support-sync-chats.test.ts GREEN — минимум 6 тестов (Phase B upsert, Phase A идемпотентность, isNewChat, sender mapping, attachments, AppSetting cursor)"
    - "tests/auto-reply-cron.test.ts GREEN — минимум 7 тестов (disabled skip, workhours skip, happy path send, переменные подстановка, dedup, cron 401, cron 200)"
  artifacts:
    - path: "lib/support-sync.ts"
      provides: "syncChats() дополняет существующие syncSupport + syncReturns"
      exports:
        - syncChats
        - syncSupport
        - syncReturns
    - path: "lib/auto-reply.ts"
      provides: "runAutoReplies() + isWithinWorkingHours() helper"
      exports:
        - runAutoReplies
        - isWithinWorkingHours
    - path: "app/api/cron/support-sync-chat/route.ts"
      provides: "Cron endpoint (5 мин, CRON_SECRET)"
      exports:
        - GET
    - path: "app/api/support-sync/route.ts"
      provides: "Ручной POST расширен chat+autoreply результатами (backward compat через spread)"
    - path: "tests/support-sync-chats.test.ts"
      provides: "Integration тесты syncChats (mock @/lib/wb-support-api + mock @/lib/prisma)"
      contains: "describe"
    - path: "tests/auto-reply-cron.test.ts"
      provides: "Integration тесты runAutoReplies + cron route"
      contains: "describe"
  key_links:
    - from: "lib/support-sync.ts syncChats"
      to: "lib/wb-support-api.ts listChats / getChatEvents / downloadChatAttachment"
      via: "import"
      pattern: "from\\s+\"@/lib/wb-support-api\""
    - from: "lib/support-sync.ts syncChats"
      to: "prisma.supportTicket.upsert({channel_wbExternalId: {channel:'CHAT', wbExternalId: chat.chatID}})"
      via: "composite unique"
      pattern: "channel_wbExternalId"
    - from: "lib/support-sync.ts syncChats"
      to: "prisma.supportMessage.findUnique({where: {wbEventId}})"
      via: "@unique идемпотентность"
      pattern: "wbEventId"
    - from: "lib/auto-reply.ts runAutoReplies"
      to: "lib/wb-support-api.ts sendChatMessage"
      via: "import + isAutoReply=true mark"
      pattern: "isAutoReply:\\s*true"
    - from: "app/api/cron/support-sync-chat/route.ts"
      to: "process.env.CRON_SECRET via x-cron-secret header"
      via: "header check"
      pattern: "x-cron-secret"
---

<objective>
Phase 10 Plan 02 — синхронизация чата + локальный автоответ:

1. **lib/support-sync.ts — `syncChats()`** (новая функция рядом с syncSupport + syncReturns):
   - Phase B (каждый tick): `listChats()` → upsert SupportTicket channel=CHAT по chatID; обновить `chatReplySign`, `customerNameSnapshot`, `previewText`, `lastMessageAt`; set `nmId` из `goodCard.nmID`.
   - Phase A (каждый tick): `getChatEvents(AppSetting.support.chat.lastEventNext)` → upsert SupportMessage по `wbEventId` (идемпотентно); `sender="client"→INBOUND`, `sender="seller"→OUTBOUND`; attachments.images[] → SupportMedia `IMAGE`, attachments.files[] → SupportMedia `DOCUMENT`; download через `downloadChatAttachment` в `/var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}`.
   - Обновление AppSetting `support.chat.lastEventNext` после последнего события.
   - Возвращает `{ newChats, newMessages, mediaDownloaded, errors }`.

2. **lib/auto-reply.ts — `runAutoReplies()`** (новый файл):
   - Читает `AutoReplyConfig` (findUnique id='default'); skip если null или `isEnabled=false`.
   - Helper `isWithinWorkingHours(config)`: по `config.timezone` (Europe/Moscow) вычисляет ISO day 1..7, проверяет включение в `config.workDays`, сравнивает `nowMinutes` vs `startMinutes/endMinutes`.
   - Внутри рабочих часов → skip (sent=0).
   - Вне рабочих часов: найти все INBOUND CHAT сообщения за последние 24ч без последующего OUTBOUND + без OUTBOUND `isAutoReply=true` за 24ч на том же ticket (dedup per chat).
   - Для каждого кандидата: подстановка `{имя_покупателя}` ← `ticket.customerNameSnapshot` (fallback `"покупатель"`), `{название_товара}` ← `WbCard.name` по `ticket.nmId` (fallback `"товар"`); `sendChatMessage({replySign: ticket.chatReplySign, message: text})`; запись OUTBOUND SupportMessage `{authorId:null, isAutoReply:true}`.
   - Возвращает `{ sent, errors }`.

3. **app/api/cron/support-sync-chat/route.ts** (новый cron endpoint, 5 мин):
   - Паттерн Phase 8/9: `x-cron-secret` header vs `process.env.CRON_SECRET`, 401 иначе.
   - Вызов: `syncChats()` → затем `runAutoReplies()` → return JSON `{ok, syncChats: {...}, autoReply: {...}}`.

4. **app/api/support-sync/route.ts** (расширение существующего ручного endpoint):
   - После `syncSupport + syncReturns` вызвать `syncChats` + `runAutoReplies` (sequentially, не падает если хотя бы один успешен).
   - Расширить JSON ответ полями `chat: SyncChatsResult`, `autoReply: AutoReplyResult`.
   - Backward compat: Phase 8 SupportSyncButton читает `feedbacksSynced/questionsSynced/mediaSaved` напрямую из spread — НЕ трогаем order.

5. **Integration tests** (заменяют Wave 0 stubs):
   - `tests/support-sync-chats.test.ts` (≥6 GREEN) — mock @/lib/wb-support-api + in-memory @/lib/prisma (паттерн tests/support-sync.test.ts Phase 8).
   - `tests/auto-reply-cron.test.ts` (≥7 GREEN) — mock время + mock Prisma + mock sendChatMessage; также тест cron route 401/200.

**Purpose:** Полная автономная обработка чата и автоответа без участия менеджера. Идемпотентность через `wbEventId @unique` (WB Chat API cursor-based events). Локальный autoreply (WB API не имеет endpoint, ERP feature — ключевое решение research). Отдельный 5-мин cron от 15-мин (SUP-07 dop требует частоту 5 мин для чата).

**Output:** 2 новых lib-файла (`auto-reply.ts`, расширение `support-sync.ts`), 1 новый route (`support-sync-chat`), 1 модификация существующего route (`support-sync`), 2 наполненных тест-файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-chat-autoreply/10-RESEARCH.md
@.planning/phases/10-chat-autoreply/10-VALIDATION.md
@.planning/phases/10-chat-autoreply/10-01-SUMMARY.md
@.planning/phases/08-support-mvp/08-02-SUMMARY.md
@.planning/phases/09-returns/09-02-SUMMARY.md
@CLAUDE.md
@lib/wb-support-api.ts
@lib/support-sync.ts
@lib/support-media.ts
@lib/prisma.ts
@lib/rbac.ts
@app/api/cron/support-sync-reviews/route.ts
@app/api/cron/purge-deleted/route.ts
@app/api/support-sync/route.ts
@tests/support-sync.test.ts

<interfaces>
**Новое из Plan 10-01 (lib/wb-support-api.ts):**
```typescript
export interface Chat { chatID: string; replySign: string; clientName: string; goodCard?: ChatGoodCard; lastMessage?: ChatLastMessage }
export interface ChatEvent { chatID: string; eventID: string; eventType: string; isNewChat: boolean; message?: ChatMessage; addTimestamp: number; sender: "client"|"seller"; clientName?: string }
export interface ChatEventsResult { events: ChatEvent[]; next: number; totalEvents: number; newestEventTime?: string; oldestEventTime?: string }
export interface SendChatMessageInput { replySign: string; message?: string; files?: Array<{name, data, contentType}> }

export async function listChats(): Promise<Chat[]>
export async function getChatEvents(next?: number): Promise<ChatEventsResult>
export async function sendChatMessage(input: SendChatMessageInput): Promise<{ok: true; addTime?: number; chatID?: string}>
export async function downloadChatAttachment(downloadId: string): Promise<Buffer>
```

**Новое из Plan 10-01 (prisma/schema.prisma):**
```prisma
model SupportTicket {
  // ... Phase 8/9
  chatReplySign         String?
  customerNameSnapshot  String?
}

model SupportMessage {
  // ... Phase 8
  wbEventId  String?  @unique  // идемпотентность для syncChats
}

enum MediaType { IMAGE  VIDEO  DOCUMENT }  // DOCUMENT для PDF из chat attachments

model AutoReplyConfig {
  id            String    @id                    // "default"
  isEnabled     Boolean   @default(false)
  workdayStart  String    @default("09:00")      // HH:MM
  workdayEnd    String    @default("18:00")
  workDays      Int[]     @default([1,2,3,4,5])  // ISO 8601: 1=Mon, 7=Sun
  messageText   String    @db.Text
  timezone      String    @default("Europe/Moscow")
  updatedById   String?
  updatedAt     DateTime  @updatedAt
}
```

**Существующие lib/support-sync.ts (Phase 8+9):**
```typescript
export interface SyncResult { feedbacksSynced, questionsSynced, mediaSaved, errors }
export async function syncSupport(opts): Promise<SyncResult>
export async function syncReturns(): Promise<SyncReturnsResult>
```

**Паттерн cron (из app/api/cron/support-sync-reviews/route.ts):**
```typescript
const secret = req.headers.get("x-cron-secret")
if (!secret || secret !== process.env.CRON_SECRET) return NextResponse.json({error:"Не авторизован"}, {status:401})
const support = await syncSupport({ isAnswered: false })
const returns = await syncReturns()
return NextResponse.json({ ok: true, support, returns })
```

**Паттерн ручного POST /api/support-sync (Phase 9 backward compat):**
Response body spread: `{ok: true, ...supportResult, support: supportResult, returns: returnsResult}`. Phase 10 расширит: `{...spread, chat: chatResult, autoReply: autoReplyResult}`.

**AppSetting model (существующий — Phase 7):**
```prisma
model AppSetting { key String @id  value String  updatedAt DateTime @updatedAt  updatedBy String? }
```
Используется как KV store. Для `support.chat.lastEventNext` — value = string number (ms timestamp).

**downloadChatAttachment vs downloadMedia:**
- Phase 8 `downloadMediaBatch(items)` из `lib/support-media.ts` — берёт `wbUrl`, делает `fetch(wbUrl)`, пишет в `/var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}`.
- Phase 10 отличается: WB Chat attachments приходят с `downloadID` (не URL) → нужен auth header → используем `downloadChatAttachment(downloadId)` из Plan 10-01 и пишем Buffer через fs.writeFile (паттерн downloadMedia но с Buffer вместо fetch).
- **Решение:** добавить helper `downloadChatMediaBatch(items)` в `lib/support-sync.ts` (или расширить `lib/support-media.ts`); items format: `{downloadId, ticketId, messageId, fileName}`. Рекомендуем inline в `syncChats` для простоты (minimal abstraction) ИЛИ новый exported helper в `lib/support-media.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: lib/support-sync.ts — syncChats() + integration test GREEN</name>
  <files>lib/support-sync.ts, tests/support-sync-chats.test.ts</files>
  <read_first>
    - lib/support-sync.ts (существующий syncSupport + syncReturns — паттерн idempotent upsert через @@unique + transactions)
    - lib/support-media.ts (downloadMediaBatch — reuse не получится из-за downloadID vs wbUrl; смотреть как шаблон)
    - lib/wb-support-api.ts (Plan 10-01 — listChats, getChatEvents, downloadChatAttachment, типы Chat/ChatEvent)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «Sync Strategy» строки 411-513 — эталонный псевдокод + «Incremental sync strategy» §8)
    - tests/support-sync.test.ts (Phase 8 паттерн integration test — mock prisma state + mock WB API)
    - tests/support-sync-returns.test.ts (Phase 9 stub — заменяемый паттерн)
    - CLAUDE.md (раздел «UPLOAD_DIR» — путь /var/www/zoiten-uploads/support/)
  </read_first>
  <behavior>
    - `syncChats(): Promise<SyncChatsResult>` где `SyncChatsResult = { newChats, newMessages, mediaDownloaded, errors: string[] }`
    - Phase B: `listChats()` → для каждого chat `prisma.supportTicket.upsert` через `@@unique([channel, wbExternalId])`:
      - create: channel="CHAT", wbExternalId=chatID, chatReplySign, customerNameSnapshot=clientName, nmId=goodCard?.nmID ?? null, previewText=(lastMessage?.text)?.slice(0, 140) ?? null, lastMessageAt = lastMessage ? new Date(lastMessage.addTimestamp * 1000) : null, status="NEW"
      - update: chatReplySign (ротируется), customerNameSnapshot, previewText, lastMessageAt (только если больше текущего — иначе undefined)
    - Phase A: в цикле `while (true)`:
      - `const { events, next, totalEvents } = await getChatEvents(cursor)` где cursor = AppSetting.support.chat.lastEventNext (as number) при первом вызове, затем newNext
      - break если `events.length === 0`
      - per event:
        - existing = `prisma.supportMessage.findUnique({where: {wbEventId: event.eventID}})` → continue если найден (идемпотентность)
        - ticket = `findUnique({where: {channel_wbExternalId: {channel: "CHAT", wbExternalId: event.chatID}}})` → если null и isNewChat=true → skip с errors.push (Phase B должен был создать) ИЛИ fallback-create
        - direction = event.sender === "client" ? "INBOUND" : "OUTBOUND"
        - text = event.message?.text ?? null
        - msg = `supportMessage.create({data: {ticketId, direction, text, authorId:null, wbEventId: event.eventID, wbSentAt: new Date(event.addTimestamp), isAutoReply: false}})` (isAutoReply всегда false — INBOUND от покупателя, OUTBOUND через API маркируется отдельно в runAutoReplies)
        - Attachments:
          - images[] → `supportMedia.create({messageId, type:"IMAGE", wbUrl: "DOWNLOAD_ID:" + img.downloadID, expiresAt: new Date(Date.now() + 365*24*3600*1000)})`
          - files[] → `supportMedia.create({type:"DOCUMENT", ...})`
        - queue: `{downloadId, ticketId, messageId, fileName}` для batch download после цикла
      - break если `events.length < BATCH_SIZE` (пагинация WB, последняя страница)
      - cursor = next; `await sleep(1000)` между страницами (rate limit 10/10s)
    - После цикла: `setAppSetting("support.chat.lastEventNext", String(cursor))` (через upsert AppSetting)
    - Download queue: для каждого `{downloadId, ticketId, messageId, fileName}`:
      - `try { buffer = await downloadChatAttachment(downloadId); fs.mkdir(`/var/www/zoiten-uploads/support/${ticketId}/${messageId}/`, {recursive:true}); fs.writeFile(localPath, buffer); prisma.supportMedia.updateMany({where: {messageId, wbUrl: "DOWNLOAD_ID:" + downloadId}, data: {localPath, sizeBytes: buffer.length}}) } catch (err) { errors.push(...) }`
    - Ratelimit-safe: `await sleep(1000)` после каждого download в batch (10 req/10s WB limit)
    - В случае исключений на верхнем уровне (listChats 403 например) — errors.push, не throw (cron не должен падать)
  </behavior>
  <action>
    **Шаг A — расширить `lib/support-sync.ts`:**

    Добавить после существующего `syncReturns` (перед EOF):

    ```typescript
    // ──────────────────────────────────────────────────────────────
    // Phase 10: syncChats — WB Buyer Chat API → SupportTicket/Message/Media
    // ──────────────────────────────────────────────────────────────

    import {
      // ... existing imports
      listChats,
      getChatEvents,
      downloadChatAttachment,
      type Chat,
      type ChatEvent,
    } from "@/lib/wb-support-api"
    import { promises as fs } from "node:fs"
    import path from "node:path"

    const CHAT_PAUSE_MS = 1000
    const CHAT_MAX_PAGES = 20
    const UPLOAD_DIR = process.env.UPLOAD_DIR || "/var/www/zoiten-uploads"
    const LAST_EVENT_NEXT_KEY = "support.chat.lastEventNext"

    export interface SyncChatsResult {
      newChats: number
      newMessages: number
      mediaDownloaded: number
      errors: string[]
    }

    interface PendingDownload {
      downloadId: string
      ticketId: string
      messageId: string
      fileName: string
    }

    function sanitizeFilename(name: string): string {
      return name.replace(/[^a-zA-Z0-9.\-_]/g, "_").slice(-128) || `file_${Date.now()}`
    }

    async function sleep(ms: number): Promise<void> {
      return new Promise((r) => setTimeout(r, ms))
    }

    async function getLastEventNext(): Promise<number | undefined> {
      const row = await prisma.appSetting.findUnique({
        where: { key: LAST_EVENT_NEXT_KEY },
      })
      if (!row?.value) return undefined
      const n = Number.parseInt(row.value, 10)
      return Number.isFinite(n) ? n : undefined
    }

    async function setLastEventNext(next: number): Promise<void> {
      await prisma.appSetting.upsert({
        where: { key: LAST_EVENT_NEXT_KEY },
        create: { key: LAST_EVENT_NEXT_KEY, value: String(next) },
        update: { value: String(next) },
      })
    }

    export async function syncChats(): Promise<SyncChatsResult> {
      const result: SyncChatsResult = {
        newChats: 0,
        newMessages: 0,
        mediaDownloaded: 0,
        errors: [],
      }
      const pendingDownloads: PendingDownload[] = []

      // ── Phase B: обновить replySign / customerNameSnapshot / previewText ──
      let chats: Chat[] = []
      try {
        chats = await listChats()
      } catch (err) {
        result.errors.push(`listChats: ${err instanceof Error ? err.message : "unknown"}`)
        return result
      }

      for (const chat of chats) {
        try {
          const existing = await prisma.supportTicket.findUnique({
            where: { channel_wbExternalId: { channel: "CHAT", wbExternalId: chat.chatID } },
          })
          const lastMessageAt = chat.lastMessage
            ? new Date(chat.lastMessage.addTimestamp * 1000)
            : null
          const previewText = chat.lastMessage?.text?.slice(0, 140) ?? null

          if (existing) {
            await prisma.supportTicket.update({
              where: { id: existing.id },
              data: {
                chatReplySign: chat.replySign,
                customerNameSnapshot: chat.clientName,
                previewText: previewText ?? undefined,
                lastMessageAt: lastMessageAt ?? undefined,
                nmId: chat.goodCard?.nmID ?? existing.nmId,
              },
            })
          } else {
            await prisma.supportTicket.create({
              data: {
                channel: "CHAT",
                wbExternalId: chat.chatID,
                chatReplySign: chat.replySign,
                customerNameSnapshot: chat.clientName,
                nmId: chat.goodCard?.nmID ?? null,
                previewText: previewText,
                lastMessageAt,
                status: "NEW",
              },
            })
            result.newChats++
          }
        } catch (err) {
          result.errors.push(`chat ${chat.chatID}: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      // ── Phase A: инкрементальный pull events ──
      let cursor = await getLastEventNext()
      let pagesProcessed = 0
      let latestNext = cursor ?? 0

      while (pagesProcessed < CHAT_MAX_PAGES) {
        let page
        try {
          page = await getChatEvents(cursor)
        } catch (err) {
          result.errors.push(`getChatEvents: ${err instanceof Error ? err.message : "unknown"}`)
          break
        }
        if (page.events.length === 0) break

        for (const event of page.events) {
          try {
            const existingMsg = await prisma.supportMessage.findUnique({
              where: { wbEventId: event.eventID },
            })
            if (existingMsg) continue

            let ticket = await prisma.supportTicket.findUnique({
              where: { channel_wbExternalId: { channel: "CHAT", wbExternalId: event.chatID } },
            })
            if (!ticket && event.isNewChat) {
              ticket = await prisma.supportTicket.create({
                data: {
                  channel: "CHAT",
                  wbExternalId: event.chatID,
                  customerNameSnapshot: event.clientName ?? null,
                  status: "NEW",
                  lastMessageAt: new Date(event.addTimestamp),
                  previewText: event.message?.text?.slice(0, 140) ?? null,
                },
              })
              result.newChats++
            }
            if (!ticket) {
              result.errors.push(`event ${event.eventID}: chat ${event.chatID} не найден после Phase B`)
              continue
            }

            const direction = event.sender === "client" ? "INBOUND" : "OUTBOUND"
            const msg = await prisma.supportMessage.create({
              data: {
                ticketId: ticket.id,
                direction,
                text: event.message?.text ?? null,
                authorId: null,
                wbEventId: event.eventID,
                wbSentAt: new Date(event.addTimestamp),
                isAutoReply: false,
              },
            })
            result.newMessages++

            const yearMs = 365 * 24 * 60 * 60 * 1000
            for (const img of event.message?.attachments?.images ?? []) {
              await prisma.supportMedia.create({
                data: {
                  messageId: msg.id,
                  type: "IMAGE",
                  wbUrl: `DOWNLOAD_ID:${img.downloadID}`,
                  expiresAt: new Date(Date.now() + yearMs),
                },
              })
              pendingDownloads.push({
                downloadId: img.downloadID,
                ticketId: ticket.id,
                messageId: msg.id,
                fileName: img.fileName,
              })
            }
            for (const f of event.message?.attachments?.files ?? []) {
              await prisma.supportMedia.create({
                data: {
                  messageId: msg.id,
                  type: "DOCUMENT",
                  wbUrl: `DOWNLOAD_ID:${f.downloadID}`,
                  expiresAt: new Date(Date.now() + yearMs),
                },
              })
              pendingDownloads.push({
                downloadId: f.downloadID,
                ticketId: ticket.id,
                messageId: msg.id,
                fileName: f.fileName,
              })
            }
          } catch (err) {
            result.errors.push(`event ${event.eventID}: ${err instanceof Error ? err.message : "unknown"}`)
          }
        }

        latestNext = page.next || latestNext
        if (page.events.length < 100) break // WB default batch, последняя страница
        cursor = page.next
        pagesProcessed++
        await sleep(CHAT_PAUSE_MS)
      }

      // Обновить cursor
      if (latestNext > 0) {
        try {
          await setLastEventNext(latestNext)
        } catch (err) {
          result.errors.push(`lastEventNext: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      // ── Download queue ──
      for (const dl of pendingDownloads) {
        try {
          const buffer = await downloadChatAttachment(dl.downloadId)
          const dir = path.join(UPLOAD_DIR, "support", dl.ticketId, dl.messageId)
          await fs.mkdir(dir, { recursive: true })
          const filename = sanitizeFilename(dl.fileName)
          const localPath = path.join(dir, filename)
          await fs.writeFile(localPath, buffer)
          await prisma.supportMedia.updateMany({
            where: { messageId: dl.messageId, wbUrl: `DOWNLOAD_ID:${dl.downloadId}` },
            data: { localPath, sizeBytes: buffer.length },
          })
          result.mediaDownloaded++
          await sleep(CHAT_PAUSE_MS)
        } catch (err) {
          result.errors.push(`download ${dl.downloadId}: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      return result
    }
    ```

    **Шаг B — заменить `tests/support-sync-chats.test.ts`:**

    Integration-тест с in-memory mock Prisma + mock WB API (паттерн Phase 8 `tests/support-sync.test.ts`).

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    // In-memory Prisma state
    const state = {
      tickets: [] as any[],
      messages: [] as any[],
      media: [] as any[],
      settings: new Map<string, string>(),
    }

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        supportTicket: {
          findUnique: async ({ where }: any) => {
            const cw = where.channel_wbExternalId
            if (cw) {
              return state.tickets.find((t) => t.channel === cw.channel && t.wbExternalId === cw.wbExternalId) ?? null
            }
            return state.tickets.find((t) => t.id === where.id) ?? null
          },
          create: async ({ data }: any) => {
            const t = { id: `T${state.tickets.length + 1}`, ...data }
            state.tickets.push(t)
            return t
          },
          update: async ({ where, data }: any) => {
            const t = state.tickets.find((x) => x.id === where.id)
            Object.assign(t, data)
            return t
          },
        },
        supportMessage: {
          findUnique: async ({ where }: any) =>
            state.messages.find((m) => m.wbEventId === where.wbEventId) ?? null,
          create: async ({ data }: any) => {
            const m = { id: `M${state.messages.length + 1}`, ...data }
            state.messages.push(m)
            return m
          },
        },
        supportMedia: {
          create: async ({ data }: any) => {
            const md = { id: `MD${state.media.length + 1}`, ...data }
            state.media.push(md)
            return md
          },
          updateMany: async ({ where, data }: any) => {
            let count = 0
            for (const md of state.media) {
              if (md.messageId === where.messageId && md.wbUrl === where.wbUrl) {
                Object.assign(md, data)
                count++
              }
            }
            return { count }
          },
        },
        appSetting: {
          findUnique: async ({ where }: any) =>
            state.settings.has(where.key) ? { key: where.key, value: state.settings.get(where.key) } : null,
          upsert: async ({ where, create, update }: any) => {
            state.settings.set(where.key, (update?.value ?? create?.value) as string)
            return { key: where.key, value: state.settings.get(where.key) }
          },
        },
      },
    }))

    vi.mock("@/lib/wb-support-api", async () => {
      const actual = await vi.importActual<any>("@/lib/wb-support-api")
      return {
        ...actual,
        listChats: vi.fn(),
        getChatEvents: vi.fn(),
        downloadChatAttachment: vi.fn(),
      }
    })

    vi.mock("node:fs", () => ({
      promises: {
        mkdir: vi.fn().mockResolvedValue(undefined),
        writeFile: vi.fn().mockResolvedValue(undefined),
      },
    }))

    beforeEach(() => {
      state.tickets = []
      state.messages = []
      state.media = []
      state.settings.clear()
    })
    afterEach(() => vi.resetAllMocks())

    describe("syncChats — Phase B (chats)", () => {
      it("создаёт новый SupportTicket channel=CHAT с chatReplySign + customerNameSnapshot", async () => {
        const api = await import("@/lib/wb-support-api") as any
        api.listChats.mockResolvedValueOnce([
          {
            chatID: "CHAT-1",
            replySign: "sig1",
            clientName: "Иван",
            goodCard: { nmID: 100 },
            lastMessage: { text: "Hi", addTimestamp: 1713355200 },
          },
        ])
        api.getChatEvents.mockResolvedValueOnce({ events: [], next: 0, totalEvents: 0 })
        const { syncChats } = await import("@/lib/support-sync")
        const res = await syncChats()
        expect(res.newChats).toBe(1)
        expect(state.tickets[0].channel).toBe("CHAT")
        expect(state.tickets[0].wbExternalId).toBe("CHAT-1")
        expect(state.tickets[0].chatReplySign).toBe("sig1")
        expect(state.tickets[0].customerNameSnapshot).toBe("Иван")
        expect(state.tickets[0].nmId).toBe(100)
        expect(state.tickets[0].previewText).toBe("Hi")
      })

      it("при повторном вызове обновляет chatReplySign (ротация signature)", async () => {
        const api = await import("@/lib/wb-support-api") as any
        state.tickets.push({
          id: "T-existing",
          channel: "CHAT",
          wbExternalId: "CHAT-2",
          chatReplySign: "old-sig",
          customerNameSnapshot: "Мария",
        })
        api.listChats.mockResolvedValueOnce([
          { chatID: "CHAT-2", replySign: "new-sig", clientName: "Мария", lastMessage: { text: "A", addTimestamp: 1 } },
        ])
        api.getChatEvents.mockResolvedValueOnce({ events: [], next: 0, totalEvents: 0 })
        const { syncChats } = await import("@/lib/support-sync")
        await syncChats()
        expect(state.tickets[0].chatReplySign).toBe("new-sig")
      })
    })

    describe("syncChats — Phase A (events)", () => {
      it("sender=client → INBOUND, sender=seller → OUTBOUND", async () => {
        const api = await import("@/lib/wb-support-api") as any
        state.tickets.push({ id: "T1", channel: "CHAT", wbExternalId: "C1" })
        api.listChats.mockResolvedValueOnce([])
        api.getChatEvents.mockResolvedValueOnce({
          events: [
            { chatID: "C1", eventID: "E1", eventType: "message", isNewChat: false, sender: "client", message: { text: "in" }, addTimestamp: 1 },
            { chatID: "C1", eventID: "E2", eventType: "message", isNewChat: false, sender: "seller", message: { text: "out" }, addTimestamp: 2 },
          ],
          next: 0,
          totalEvents: 2,
        })
        const { syncChats } = await import("@/lib/support-sync")
        const res = await syncChats()
        expect(res.newMessages).toBe(2)
        expect(state.messages.find((m) => m.wbEventId === "E1")?.direction).toBe("INBOUND")
        expect(state.messages.find((m) => m.wbEventId === "E2")?.direction).toBe("OUTBOUND")
      })

      it("идемпотентен: повторный sync с тем же wbEventId не создаёт дубликаты", async () => {
        const api = await import("@/lib/wb-support-api") as any
        state.tickets.push({ id: "T1", channel: "CHAT", wbExternalId: "C1" })
        const events = [{ chatID: "C1", eventID: "E-DUP", eventType: "message", isNewChat: false, sender: "client", message: { text: "x" }, addTimestamp: 1 }]
        api.listChats.mockResolvedValue([])
        api.getChatEvents
          .mockResolvedValueOnce({ events, next: 0, totalEvents: 1 })
          .mockResolvedValueOnce({ events, next: 0, totalEvents: 1 })
        const { syncChats } = await import("@/lib/support-sync")
        await syncChats()
        await syncChats()
        expect(state.messages).toHaveLength(1)
      })

      it("isNewChat=true + ticket отсутствует → создаёт тикет", async () => {
        const api = await import("@/lib/wb-support-api") as any
        api.listChats.mockResolvedValueOnce([])
        api.getChatEvents.mockResolvedValueOnce({
          events: [
            { chatID: "NEW-C", eventID: "E-NEW", eventType: "message", isNewChat: true, sender: "client", clientName: "Petra", message: { text: "first" }, addTimestamp: 1 },
          ],
          next: 0,
          totalEvents: 1,
        })
        const { syncChats } = await import("@/lib/support-sync")
        const res = await syncChats()
        expect(res.newChats).toBe(1)
        expect(state.tickets[0].wbExternalId).toBe("NEW-C")
        expect(res.newMessages).toBe(1)
      })

      it("attachments.images → SupportMedia IMAGE; attachments.files → DOCUMENT", async () => {
        const api = await import("@/lib/wb-support-api") as any
        state.tickets.push({ id: "T1", channel: "CHAT", wbExternalId: "C1" })
        api.listChats.mockResolvedValueOnce([])
        api.getChatEvents.mockResolvedValueOnce({
          events: [
            {
              chatID: "C1", eventID: "E-MEDIA", eventType: "message", isNewChat: false, sender: "client", addTimestamp: 1,
              message: {
                text: "media",
                attachments: {
                  images: [{ downloadID: "img-1", fileName: "photo.jpg" }],
                  files: [{ downloadID: "f-1", fileName: "doc.pdf" }],
                },
              },
            },
          ],
          next: 0, totalEvents: 1,
        })
        api.downloadChatAttachment.mockResolvedValue(Buffer.from([1, 2, 3]))
        const { syncChats } = await import("@/lib/support-sync")
        await syncChats()
        expect(state.media.filter((m) => m.type === "IMAGE")).toHaveLength(1)
        expect(state.media.filter((m) => m.type === "DOCUMENT")).toHaveLength(1)
      }, 15000)

      it("обновляет AppSetting.support.chat.lastEventNext после tick", async () => {
        const api = await import("@/lib/wb-support-api") as any
        state.tickets.push({ id: "T1", channel: "CHAT", wbExternalId: "C1" })
        api.listChats.mockResolvedValueOnce([])
        api.getChatEvents.mockResolvedValueOnce({
          events: [{ chatID: "C1", eventID: "E-N", eventType: "message", isNewChat: false, sender: "client", message: { text: "x" }, addTimestamp: 1 }],
          next: 1713999999999,
          totalEvents: 1,
        })
        const { syncChats } = await import("@/lib/support-sync")
        await syncChats()
        expect(state.settings.get("support.chat.lastEventNext")).toBe("1713999999999")
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-sync-chats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function syncChats" lib/support-sync.ts` == 1
    - `grep "channel: \"CHAT\"" lib/support-sync.ts` находит строку
    - `grep "chatReplySign" lib/support-sync.ts` находит строку
    - `grep "wbEventId" lib/support-sync.ts` находит строку
    - `grep "support.chat.lastEventNext" lib/support-sync.ts` находит строку
    - `grep "DOCUMENT" lib/support-sync.ts` находит строку (MediaType для files)
    - `grep "downloadChatAttachment" lib/support-sync.ts` находит строку
    - `grep "isNewChat" lib/support-sync.ts` находит строку
    - `test -f tests/support-sync-chats.test.ts` exit 0
    - `grep -c "it(" tests/support-sync-chats.test.ts` >= 6
    - `npm run test -- tests/support-sync-chats.test.ts` exit 0 (≥6 GREEN)
    - `npm run test -- tests/support-sync.test.ts tests/support-sync-returns.test.ts` exit 0 (Phase 8/9 regression)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/support-sync.ts экспортирует syncChats (Phase B + Phase A + download queue); AppSetting cursor persist; идемпотентность через wbEventId; ≥6 integration-тестов GREEN; Phase 8/9 regression pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/auto-reply.ts + cron route + test GREEN</name>
  <files>lib/auto-reply.ts, app/api/cron/support-sync-chat/route.ts, tests/auto-reply-cron.test.ts</files>
  <read_first>
    - lib/support-sync.ts (Task 1 — syncChats готов, auto-reply вызывается после)
    - lib/wb-support-api.ts (Plan 10-01 — sendChatMessage)
    - app/api/cron/support-sync-reviews/route.ts (эталонный cron паттерн)
    - .planning/phases/10-chat-autoreply/10-RESEARCH.md (раздел «runAutoReplies() in lib/auto-reply.ts» строки 515-606 — эталонный код с isWithinWorkingHours helper)
    - tests/support-cron.test.ts (Phase 8 паттерн cron test)
    - tests/auto-reply-cron.test.ts (Wave 0 stub из Plan 10-01 — заменить)
  </read_first>
  <behavior>
    - `isWithinWorkingHours(config): boolean` — чистая функция:
      - Построить Date в timezone config.timezone (через `toLocaleString("en-US", {timeZone})`)
      - dayOfWeek = 1..7 по ISO (1=Mon..7=Sun); JS getDay(): 0=Sun..6=Sat → маппинг: js===0 ? 7 : js
      - Если !config.workDays.includes(dayOfWeek) → return false
      - Парсить workdayStart/End как "HH:MM" → minutes; nowMinutes = hours*60 + minutes; return nowMinutes >= startMinutes && nowMinutes < endMinutes
    - `runAutoReplies(): Promise<{sent: number; errors: string[]}>`:
      - const config = `prisma.autoReplyConfig.findUnique({where: {id: "default"}})`
      - if (!config || !config.isEnabled) return {sent:0, errors:[]}
      - if (isWithinWorkingHours(config)) return {sent:0, errors:[]}
      - candidates: raw SQL или Prisma-with-filter — найти SupportMessage где direction=INBOUND, ticket.channel=CHAT, ticket.chatReplySign IS NOT NULL, createdAt/sentAt >= 24ч назад, НЕТ OUTBOUND message в том же ticket с sentAt > thisInbound.sentAt, НЕТ OUTBOUND с isAutoReply=true в том же ticket за последние 24ч
      - Для упрощения: JS-фильтр — получить все INBOUND за 24ч где ticket.channel=CHAT + chatReplySign != null, затем per-ticket: `messages = await prisma.supportMessage.findMany({where: {ticketId, sentAt: {gte: lastAutoReplyCutoff}}, orderBy: {sentAt: "desc"}})` и проверить `!messages.some(m => m.direction === "OUTBOUND")` AND `!messages.some(m => m.isAutoReply === true)` (последний — dedup 24h)
      - Per candidate ticket (group INBOUND по ticket, 1 раз per ticket):
        - wbCard = ticket.nmId ? `prisma.wbCard.findUnique({where: {nmId: ticket.nmId}})` : null
        - text = config.messageText
            .replace(/\{имя_покупателя\}/g, ticket.customerNameSnapshot ?? "покупатель")
            .replace(/\{название_товара\}/g, wbCard?.name ?? "товар")
        - try sendChatMessage({replySign: ticket.chatReplySign!, message: text})
        - supportMessage.create({direction:"OUTBOUND", text, authorId:null, isAutoReply:true, wbSentAt: now, ticketId})
        - sent++
      - catch per ticket: errors.push
    - `GET /api/cron/support-sync-chat`: x-cron-secret check; try { chat = await syncChats(); autoReply = await runAutoReplies(); return NextResponse.json({ok:true, chat, autoReply}) }; catch throw 500
  </behavior>
  <action>
    **Шаг A — создать `lib/auto-reply.ts`:**

    ```typescript
    // lib/auto-reply.ts
    // Phase 10: ERP-local auto-reply. WB Chat API не имеет endpoint для auto-reply,
    // эта логика — чисто локальная: cron 5 мин проверяет AutoReplyConfig и отправляет
    // OUTBOUND сообщение с isAutoReply=true вне рабочих часов.

    import { prisma } from "@/lib/prisma"
    import { sendChatMessage } from "@/lib/wb-support-api"
    import type { AutoReplyConfig } from "@prisma/client"

    const DAY_MS = 24 * 60 * 60 * 1000

    export function isWithinWorkingHours(config: AutoReplyConfig, now: Date = new Date()): boolean {
      // Build date in config.timezone
      const tzString = now.toLocaleString("en-US", { timeZone: config.timezone })
      const tzDate = new Date(tzString)

      // ISO 8601 day of week: 1=Mon..7=Sun
      const jsDay = tzDate.getDay() // 0..6 (0=Sun)
      const isoDay = jsDay === 0 ? 7 : jsDay

      if (!config.workDays.includes(isoDay)) return false

      const [startH, startM] = config.workdayStart.split(":").map(Number)
      const [endH, endM] = config.workdayEnd.split(":").map(Number)
      const startMinutes = startH * 60 + startM
      const endMinutes = endH * 60 + endM
      const nowMinutes = tzDate.getHours() * 60 + tzDate.getMinutes()

      return nowMinutes >= startMinutes && nowMinutes < endMinutes
    }

    export interface AutoReplyResult {
      sent: number
      skipped: number
      errors: string[]
    }

    export async function runAutoReplies(now: Date = new Date()): Promise<AutoReplyResult> {
      const result: AutoReplyResult = { sent: 0, skipped: 0, errors: [] }

      const config = await prisma.autoReplyConfig.findUnique({ where: { id: "default" } })
      if (!config) {
        result.errors.push("AutoReplyConfig не инициализирован (ожидается seed 'default')")
        return result
      }
      if (!config.isEnabled) {
        result.skipped = 1
        return result
      }
      if (isWithinWorkingHours(config, now)) {
        result.skipped = 1
        return result
      }

      const cutoff = new Date(now.getTime() - DAY_MS)

      // Кандидаты: CHAT тикеты с chatReplySign и хотя бы одним INBOUND за 24ч
      const tickets = await prisma.supportTicket.findMany({
        where: {
          channel: "CHAT",
          chatReplySign: { not: null },
          lastMessageAt: { gte: cutoff },
        },
        select: {
          id: true,
          nmId: true,
          chatReplySign: true,
          customerNameSnapshot: true,
        },
      })

      for (const ticket of tickets) {
        try {
          if (!ticket.chatReplySign) continue

          // Последние сообщения ticket за 24ч для dedup + ответ-проверки
          const recent = await prisma.supportMessage.findMany({
            where: { ticketId: ticket.id, sentAt: { gte: cutoff } },
            orderBy: { sentAt: "desc" },
          })
          if (recent.length === 0) continue

          const lastInbound = recent.find((m) => m.direction === "INBOUND")
          if (!lastInbound) continue

          // Есть ли OUTBOUND после последнего INBOUND? → не нужен автоответ
          const hasOutboundAfter = recent.some(
            (m) => m.direction === "OUTBOUND" && m.sentAt > lastInbound.sentAt
          )
          if (hasOutboundAfter) continue

          // Dedup: уже отправляли autoReply за 24ч на этом ticket?
          const hadAutoReply = recent.some((m) => m.isAutoReply === true)
          if (hadAutoReply) continue

          // Подстановка переменных
          const wbCard = ticket.nmId
            ? await prisma.wbCard.findUnique({ where: { nmId: ticket.nmId }, select: { name: true } })
            : null
          const text = config.messageText
            .replace(/\{имя_покупателя\}/g, ticket.customerNameSnapshot ?? "покупатель")
            .replace(/\{название_товара\}/g, wbCard?.name ?? "товар")

          try {
            await sendChatMessage({ replySign: ticket.chatReplySign, message: text })
          } catch (err) {
            result.errors.push(
              `ticket ${ticket.id}: sendChatMessage ${err instanceof Error ? err.message : "unknown"}`
            )
            continue
          }

          await prisma.supportMessage.create({
            data: {
              ticketId: ticket.id,
              direction: "OUTBOUND",
              text,
              authorId: null,
              isAutoReply: true,
              wbSentAt: now,
              sentAt: now,
            },
          })
          result.sent++
        } catch (err) {
          result.errors.push(
            `ticket ${ticket.id}: ${err instanceof Error ? err.message : "unknown"}`
          )
        }
      }

      return result
    }
    ```

    **Шаг B — создать `app/api/cron/support-sync-chat/route.ts`:**

    ```typescript
    // GET /api/cron/support-sync-chat — cron синхронизация чата (5 мин).
    // Защищён x-cron-secret. Вызывает syncChats() + runAutoReplies().
    import { NextRequest, NextResponse } from "next/server"
    import { syncChats } from "@/lib/support-sync"
    import { runAutoReplies } from "@/lib/auto-reply"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }
      try {
        const chat = await syncChats()
        const autoReply = await runAutoReplies()
        return NextResponse.json({ ok: true, chat, autoReply })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка синхронизации чата"
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    **Шаг C — заменить `tests/auto-reply-cron.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    const state = {
      config: null as any,
      tickets: [] as any[],
      messages: [] as any[],
      wbCards: [] as any[],
    }

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        autoReplyConfig: {
          findUnique: async () => state.config,
        },
        supportTicket: {
          findMany: async ({ where }: any) =>
            state.tickets.filter((t) =>
              t.channel === where.channel &&
              (!where.chatReplySign || t.chatReplySign !== null) &&
              (!where.lastMessageAt?.gte || (t.lastMessageAt && t.lastMessageAt >= where.lastMessageAt.gte))
            ),
        },
        supportMessage: {
          findMany: async ({ where }: any) =>
            state.messages
              .filter((m) => m.ticketId === where.ticketId && (!where.sentAt?.gte || m.sentAt >= where.sentAt.gte))
              .sort((a, b) => b.sentAt.getTime() - a.sentAt.getTime()),
          create: async ({ data }: any) => {
            const m = { id: `M${state.messages.length + 1}`, ...data }
            state.messages.push(m)
            return m
          },
        },
        wbCard: {
          findUnique: async ({ where }: any) => state.wbCards.find((c) => c.nmId === where.nmId) ?? null,
        },
      },
    }))

    const sendChatMessageMock = vi.fn()
    vi.mock("@/lib/wb-support-api", () => ({
      sendChatMessage: sendChatMessageMock,
    }))

    beforeEach(() => {
      state.config = null
      state.tickets = []
      state.messages = []
      state.wbCards = []
      sendChatMessageMock.mockReset()
      sendChatMessageMock.mockResolvedValue({ ok: true })
      process.env.CRON_SECRET = "test-cron"
    })

    afterEach(() => vi.resetAllMocks())

    describe("runAutoReplies — guards", () => {
      it("isEnabled=false → skipped", async () => {
        state.config = { id: "default", isEnabled: false, workDays: [1,2,3,4,5], workdayStart: "09:00", workdayEnd: "18:00", messageText: "Hi {имя_покупателя}", timezone: "Europe/Moscow" }
        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(0)
        expect(res.skipped).toBe(1)
      })

      it("внутри рабочих часов → skipped", async () => {
        state.config = {
          id: "default", isEnabled: true, workDays: [1,2,3,4,5,6,7], workdayStart: "00:00", workdayEnd: "23:59",
          messageText: "Hi", timezone: "Europe/Moscow",
        }
        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(0)
        expect(res.skipped).toBe(1)
      })

      it("AutoReplyConfig отсутствует → errors + sent=0", async () => {
        state.config = null
        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(0)
        expect(res.errors.length).toBe(1)
      })
    })

    describe("runAutoReplies — happy path (вне рабочих часов)", () => {
      it("отправляет OUTBOUND isAutoReply=true + подстановка {имя_покупателя}/{название_товара}", async () => {
        state.config = {
          id: "default", isEnabled: true, workDays: [],  // пустой = всегда вне рабочих
          workdayStart: "09:00", workdayEnd: "18:00",
          messageText: "Привет, {имя_покупателя}! Товар: {название_товара}",
          timezone: "Europe/Moscow",
        }
        state.tickets.push({
          id: "T1", channel: "CHAT", chatReplySign: "sig1", customerNameSnapshot: "Иван", nmId: 100, lastMessageAt: new Date(),
        })
        state.messages.push({ id: "M1", ticketId: "T1", direction: "INBOUND", text: "?", sentAt: new Date(), isAutoReply: false })
        state.wbCards.push({ nmId: 100, name: "Кабель HDMI" })

        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(1)
        expect(sendChatMessageMock).toHaveBeenCalledWith(expect.objectContaining({
          replySign: "sig1",
          message: "Привет, Иван! Товар: Кабель HDMI",
        }))
        const outbound = state.messages.find((m) => m.direction === "OUTBOUND")
        expect(outbound?.isAutoReply).toBe(true)
      })

      it("fallback 'покупатель' если customerNameSnapshot=null; 'товар' если nmId=null", async () => {
        state.config = {
          id: "default", isEnabled: true, workDays: [],
          workdayStart: "09:00", workdayEnd: "18:00",
          messageText: "{имя_покупателя}, {название_товара}",
          timezone: "Europe/Moscow",
        }
        state.tickets.push({
          id: "T1", channel: "CHAT", chatReplySign: "sig", customerNameSnapshot: null, nmId: null, lastMessageAt: new Date(),
        })
        state.messages.push({ id: "M1", ticketId: "T1", direction: "INBOUND", sentAt: new Date(), isAutoReply: false })

        const { runAutoReplies } = await import("@/lib/auto-reply")
        await runAutoReplies()
        expect(sendChatMessageMock).toHaveBeenCalledWith(expect.objectContaining({
          message: "покупатель, товар",
        }))
      })

      it("dedup: не отправляет если уже был OUTBOUND isAutoReply=true за 24ч", async () => {
        state.config = {
          id: "default", isEnabled: true, workDays: [],
          workdayStart: "09:00", workdayEnd: "18:00",
          messageText: "auto", timezone: "Europe/Moscow",
        }
        state.tickets.push({ id: "T1", channel: "CHAT", chatReplySign: "sig", customerNameSnapshot: "x", nmId: null, lastMessageAt: new Date() })
        state.messages.push({ id: "M1", ticketId: "T1", direction: "INBOUND", sentAt: new Date(Date.now() - 3600_000), isAutoReply: false })
        state.messages.push({ id: "M2", ticketId: "T1", direction: "OUTBOUND", sentAt: new Date(Date.now() - 1000), isAutoReply: true })

        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(0)
        expect(sendChatMessageMock).not.toHaveBeenCalled()
      })

      it("skip если OUTBOUND (manual reply) был после INBOUND — менеджер уже ответил", async () => {
        state.config = {
          id: "default", isEnabled: true, workDays: [],
          workdayStart: "09:00", workdayEnd: "18:00",
          messageText: "auto", timezone: "Europe/Moscow",
        }
        state.tickets.push({ id: "T1", channel: "CHAT", chatReplySign: "sig", customerNameSnapshot: "x", nmId: null, lastMessageAt: new Date() })
        state.messages.push({ id: "M1", ticketId: "T1", direction: "INBOUND", sentAt: new Date(Date.now() - 3600_000), isAutoReply: false })
        state.messages.push({ id: "M2", ticketId: "T1", direction: "OUTBOUND", sentAt: new Date(Date.now() - 1000), isAutoReply: false })

        const { runAutoReplies } = await import("@/lib/auto-reply")
        const res = await runAutoReplies()
        expect(res.sent).toBe(0)
      })
    })

    describe("cron /api/cron/support-sync-chat", () => {
      function mockReq(headers: Record<string, string> = {}) {
        return { headers: { get: (k: string) => headers[k.toLowerCase()] ?? null } } as any
      }

      it("возвращает 401 без x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-sync-chat/route")
        const res = await GET(mockReq({}))
        expect(res.status).toBe(401)
      })

      it("возвращает 200 с { ok, chat, autoReply } при валидном секрете", async () => {
        // Stub syncChats через mock support-sync
        vi.doMock("@/lib/support-sync", () => ({
          syncChats: vi.fn().mockResolvedValue({ newChats: 0, newMessages: 0, mediaDownloaded: 0, errors: [] }),
        }))
        state.config = null // runAutoReplies вернёт errors (нет конфига), но не 500
        const { GET } = await import("@/app/api/cron/support-sync-chat/route")
        const res = await GET(mockReq({ "x-cron-secret": "test-cron" }))
        expect(res.status).toBe(200)
        const body = await res.json()
        expect(body.ok).toBe(true)
        expect(body.chat).toBeDefined()
        expect(body.autoReply).toBeDefined()
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/auto-reply-cron.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function runAutoReplies" lib/auto-reply.ts` == 1
    - `grep -c "export function isWithinWorkingHours" lib/auto-reply.ts` == 1
    - `grep "AutoReplyConfig" lib/auto-reply.ts` находит строку
    - `grep '{имя_покупателя}' lib/auto-reply.ts` находит строку
    - `grep '{название_товара}' lib/auto-reply.ts` находит строку
    - `grep 'isAutoReply:\s*true' lib/auto-reply.ts` находит строку
    - `grep 'id: "default"' lib/auto-reply.ts` находит строку
    - `test -f app/api/cron/support-sync-chat/route.ts` exit 0
    - `grep "x-cron-secret" app/api/cron/support-sync-chat/route.ts` находит строку
    - `grep "syncChats" app/api/cron/support-sync-chat/route.ts` находит строку
    - `grep "runAutoReplies" app/api/cron/support-sync-chat/route.ts` находит строку
    - `npm run test -- tests/auto-reply-cron.test.ts` exit 0 (≥7 GREEN)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/auto-reply.ts экспортирует runAutoReplies + isWithinWorkingHours с dedup 24h + timezone-aware working hours; новый cron endpoint 5 мин защищён x-cron-secret; ≥7 integration-тестов GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Расширить POST /api/support-sync (ручная кнопка) — chat + autoReply в backward-compat ответе</name>
  <files>app/api/support-sync/route.ts</files>
  <read_first>
    - app/api/support-sync/route.ts (существующий Phase 8/9 — backward compat паттерн spread)
    - app/api/cron/support-sync-chat/route.ts (Task 2 — эталонный call syncChats + runAutoReplies)
    - lib/support-sync.ts (Task 1 — syncChats)
    - lib/auto-reply.ts (Task 2 — runAutoReplies)
    - .planning/phases/09-returns/09-02-SUMMARY.md (backward-compat pattern: spread supportResult ПЕРВЫМ)
  </read_first>
  <action>
    Модифицировать существующий `app/api/support-sync/route.ts`:

    - Импортировать `syncChats` и `runAutoReplies`.
    - После успешного `syncSupport + syncReturns` вызвать `syncChats` → затем `runAutoReplies` (последовательно, НЕ в Promise.all — rate-limit safety).
    - Возвращаемый JSON расширить:
    ```typescript
    const support = await syncSupport({ isAnswered: false })
    const returns = await syncReturns()
    const chat = await syncChats()
    const autoReply = await runAutoReplies()

    return NextResponse.json({
      ok: true,
      ...support,        // backward compat: feedbacksSynced, questionsSynced, mediaSaved, errors (Phase 8 SupportSyncButton читает эти поля)
      support,
      returns,
      chat,
      autoReply,
    })
    ```

    **⚠ КРИТИЧНО:** порядок spread из Phase 9 SUMMARY: `...support` ПЕРВЫМ → нельзя ломать Phase 8 клиента. Объединённые `errors` — backward compat через spread вернёт `errors` из support; для детализации клиент может читать `returns.errors`, `chat.errors`, `autoReply.errors`.

    **RBAC не меняем** — уже `requireSection("SUPPORT", "MANAGE")` в Phase 8.

    **Обработка ошибок chat/autoReply:** если syncChats или runAutoReplies кидает исключение (WB 403, network), **не ломать общий 200** — обернуть в try/catch:
    ```typescript
    let chat, autoReply
    try {
      chat = await syncChats()
    } catch (err) {
      chat = { newChats: 0, newMessages: 0, mediaDownloaded: 0, errors: [err instanceof Error ? err.message : "syncChats failure"] }
    }
    try {
      autoReply = await runAutoReplies()
    } catch (err) {
      autoReply = { sent: 0, skipped: 0, errors: [err instanceof Error ? err.message : "runAutoReplies failure"] }
    }
    ```

    Это защита от "партийного" падения (Feedbacks OK, Chat 403): пользователь видит частичный результат в toast, а не общий 500.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "syncChats" app/api/support-sync/route.ts && grep -q "runAutoReplies" app/api/support-sync/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "syncChats" app/api/support-sync/route.ts` находит строку
    - `grep "runAutoReplies" app/api/support-sync/route.ts` находит строку
    - `grep "chat" app/api/support-sync/route.ts` находит строку (поле ответа)
    - `grep "autoReply" app/api/support-sync/route.ts` находит строку
    - `grep "requireSection.*SUPPORT.*MANAGE" app/api/support-sync/route.ts` НЕ удалено (Phase 8 RBAC на месте)
    - `grep "...support" app/api/support-sync/route.ts` находит строку (backward compat spread)
    - `npx tsc --noEmit` exit 0
    - `npm run test` exit 0 (полная suite: Phase 7/8/9 + Phase 10 GREEN)
  </acceptance_criteria>
  <done>
    POST /api/support-sync расширен вызовом syncChats + runAutoReplies в try/catch (партийное падение не ломает 200); JSON ответ содержит chat + autoReply поля; backward compat spread порядка сохранён; full suite GREEN.
  </done>
</task>

</tasks>

<verification>
После всех 3 задач:
1. `npx tsc --noEmit` exit 0
2. `npm run test` exit 0 (wb-chat-api + support-sync-chats + auto-reply-cron GREEN; Phase 7/8/9 regression GREEN; остальные stubs SKIPPED)
3. `ls app/api/cron/support-sync-chat/route.ts` exit 0
4. `grep -c "export async function " lib/support-sync.ts` >= 3 (syncSupport + syncReturns + syncChats)
5. `grep -c "export async function " lib/auto-reply.ts` >= 1 (runAutoReplies)
6. `npm run build` exit 0 (компилируется с новыми route.ts)
</verification>

<success_criteria>
Plan 10-02 завершён когда:
- lib/support-sync.ts экспортирует syncChats с Phase B (upsert chats) + Phase A (cursor events + идемпотентность по wbEventId) + download queue
- lib/auto-reply.ts экспортирует runAutoReplies (disabled skip / workhours skip / dedup 24h / substitution / sendChatMessage) + isWithinWorkingHours helper
- GET /api/cron/support-sync-chat защищён x-cron-secret; вызывает syncChats + runAutoReplies; возвращает объединённый JSON
- POST /api/support-sync расширен chat+autoReply в try/catch (backward compat)
- ≥6 GREEN тестов в tests/support-sync-chats.test.ts, ≥7 GREEN в tests/auto-reply-cron.test.ts
- Полный suite 0 failed
- Commit: `feat(10-02): syncChats + runAutoReplies + cron support-sync-chat (5 мин)`
</success_criteria>

<output>
Создать `.planning/phases/10-chat-autoreply/10-02-SUMMARY.md` с:
- Новая функция lib/support-sync.ts: syncChats + helpers (getLastEventNext/setLastEventNext через AppSetting)
- Новый модуль lib/auto-reply.ts: runAutoReplies + isWithinWorkingHours + контракт
- Новый endpoint: GET /api/cron/support-sync-chat (5 мин) — путь, секрет header, JSON структура
- Модификация POST /api/support-sync: backward compat spread + новые поля chat, autoReply
- Cron расписание (для Plan 10-04 deploy): `*/5 * * * * curl -s -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/support-sync-chat`
- AppSetting ключ "support.chat.lastEventNext" — cursor для инкрементального sync
</output>
</content>
</invoke>