---
phase: 05-ui-module-stubs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/ComingSoon.tsx
  - app/(dashboard)/prices/page.tsx
  - app/(dashboard)/weekly/page.tsx
  - app/(dashboard)/inventory/page.tsx
  - app/(dashboard)/batches/page.tsx
  - app/(dashboard)/purchase-plan/page.tsx
  - app/(dashboard)/sales-plan/page.tsx
  - app/(dashboard)/support/page.tsx
autonomous: true
requirements:
  - STUB-01
  - STUB-02
  - STUB-03
  - STUB-04
  - STUB-05
  - STUB-06
  - SUPP-01
  - SUPP-02

must_haves:
  truths:
    - "Authenticated user can navigate to /prices, /weekly, /inventory, /batches, /purchase-plan, /sales-plan and see a 'coming soon' placeholder"
    - "Each stub page is protected by requireSection() for its respective ERP section"
    - "Authenticated user can navigate to /support and see a support placeholder page"
    - "/support is protected by requireSection('SUPPORT')"
    - "Unauthenticated requests to stub pages redirect to /login via middleware"
  artifacts:
    - path: "components/ui/ComingSoon.tsx"
      provides: "Reusable coming soon component with section name and В разработке message"
      exports: ["ComingSoon"]
    - path: "app/(dashboard)/prices/page.tsx"
      provides: "Управление ценами stub page"
      contains: "requireSection(\"PRICES\")"
    - path: "app/(dashboard)/weekly/page.tsx"
      provides: "Недельные карточки stub page"
      contains: "requireSection(\"WEEKLY_CARDS\")"
    - path: "app/(dashboard)/inventory/page.tsx"
      provides: "Управление остатками stub page"
      contains: "requireSection(\"STOCK\")"
    - path: "app/(dashboard)/batches/page.tsx"
      provides: "Себестоимость партий stub page"
      contains: "requireSection(\"COST\")"
    - path: "app/(dashboard)/purchase-plan/page.tsx"
      provides: "План закупок stub page"
      contains: "requireSection(\"PROCUREMENT\")"
    - path: "app/(dashboard)/sales-plan/page.tsx"
      provides: "План продаж stub page"
      contains: "requireSection(\"SALES\")"
    - path: "app/(dashboard)/support/page.tsx"
      provides: "Служба поддержки placeholder page"
      contains: "requireSection(\"SUPPORT\")"
  key_links:
    - from: "app/(dashboard)/prices/page.tsx"
      to: "lib/rbac.ts"
      via: "requireSection import"
    - from: "app/(dashboard)/support/page.tsx"
      to: "lib/rbac.ts"
      via: "requireSection(\"SUPPORT\")"
    - from: "all stub pages"
      to: "components/ui/ComingSoon.tsx"
      via: "ComingSoon component import"
---

<objective>
Create a reusable ComingSoon component and 7 protected stub pages for all planned ERP modules plus support.

Purpose: All ERP sections are navigable placeholders — the product feels complete and production-ready even though future modules are not yet built.
Output: ComingSoon component + 6 module stub pages + 1 support placeholder page, all under (dashboard) layout with RBAC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-module-stubs/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/rbac.ts:
```typescript
// Throws "UNAUTHORIZED" if not logged in, "FORBIDDEN" if no section access
export async function requireSection(section: string): Promise<void>
```

From lib/sections.ts (URL → section mapping used by middleware):
```typescript
export const SECTION_PATHS = {
  "/products": "PRODUCTS",
  "/prices": "PRICES",
  "/weekly": "WEEKLY_CARDS",
  "/inventory": "STOCK",
  "/batches": "COST",
  "/purchase-plan": "PROCUREMENT",
  "/sales-plan": "SALES",
  "/support": "SUPPORT",
} as const
```

Existing page pattern (from app/(dashboard)/products/page.tsx pattern):
```typescript
// Server Component — uses requireSection for RBAC
import { requireSection } from "@/lib/rbac"

export default async function SomePage() {
  await requireSection("SECTION_NAME")
  // ...render
}
```

Dashboard layout (app/(dashboard)/layout.tsx):
- Wraps all (dashboard) pages in Sidebar + Header
- Handles its own auth redirect if session missing
- Pages don't need to add their own auth check beyond requireSection()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ComingSoon component</name>
  <files>components/ui/ComingSoon.tsx</files>
  <action>
Create `components/ui/ComingSoon.tsx` as a simple Server Component (no "use client" needed — no motion here):

```typescript
// components/ui/ComingSoon.tsx
// Reusable placeholder for ERP modules not yet implemented (per D-08, D-09)
import { Clock } from "lucide-react"

interface ComingSoonProps {
  sectionName: string
  description?: string
}

export function ComingSoon({ sectionName, description }: ComingSoonProps) {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] text-center gap-6">
      <div className="rounded-full bg-gray-100 p-6">
        <Clock className="w-12 h-12 text-gray-400" />
      </div>
      <div className="space-y-2">
        <h1 className="text-2xl font-semibold text-gray-900">{sectionName}</h1>
        <p className="text-lg text-gray-500">В разработке</p>
        {description && (
          <p className="text-sm text-gray-400 max-w-md mx-auto">{description}</p>
        )}
      </div>
    </div>
  )
}
```

This matches the dashboard's light theme (not dark like the landing page).
  </action>
  <verify>
    <automated>PATH=/usr/local/bin:$PATH npx tsc --noEmit 2>&1 | grep "ComingSoon" | head -10</automated>
  </verify>
  <done>components/ui/ComingSoon.tsx exists, exports ComingSoon, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 module stub pages and support placeholder</name>
  <files>
    app/(dashboard)/prices/page.tsx
    app/(dashboard)/weekly/page.tsx
    app/(dashboard)/inventory/page.tsx
    app/(dashboard)/batches/page.tsx
    app/(dashboard)/purchase-plan/page.tsx
    app/(dashboard)/sales-plan/page.tsx
    app/(dashboard)/support/page.tsx
  </files>
  <action>
Create all 7 pages. Each module stub follows the exact same pattern with the correct section name and RBAC section string.

**URL-to-section mapping** (must match lib/sections.ts exactly):
- `/prices` → `"PRICES"` → label "Управление ценами"
- `/weekly` → `"WEEKLY_CARDS"` → label "Недельные карточки"
- `/inventory` → `"STOCK"` → label "Управление остатками"
- `/batches` → `"COST"` → label "Себестоимость партий"
- `/purchase-plan` → `"PROCUREMENT"` → label "План закупок"
- `/sales-plan` → `"SALES"` → label "План продаж"

**app/(dashboard)/prices/page.tsx:**
```typescript
import { requireSection } from "@/lib/rbac"
import { ComingSoon } from "@/components/ui/ComingSoon"

export default async function PricesPage() {
  await requireSection("PRICES")
  return <ComingSoon sectionName="Управление ценами" />
}
```

Repeat this exact pattern for each of the 6 stub pages, substituting the correct section string and label.

**app/(dashboard)/support/page.tsx** (SUPP-01, SUPP-02 — per D-13, D-14: placeholder until ai-cs-zoiten is deployed):
```typescript
import { requireSection } from "@/lib/rbac"
import { ExternalLink } from "lucide-react"

export default async function SupportPage() {
  await requireSection("SUPPORT")
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] text-center gap-6">
      <div className="space-y-3">
        <h1 className="text-2xl font-semibold text-gray-900">Служба поддержки</h1>
        <p className="text-gray-500 max-w-md mx-auto">
          Интеграция AI-бота поддержки в процессе. Модуль будет доступен после
          деплоя сервиса ai-cs-zoiten.
        </p>
        <a
          href="https://github.com/safyodorov/ai-cs-zoiten"
          target="_blank"
          rel="noopener noreferrer"
          className="inline-flex items-center gap-2 text-sm text-blue-600 hover:underline mt-2"
        >
          <ExternalLink className="w-4 h-4" />
          safyodorov/ai-cs-zoiten на GitHub
        </a>
      </div>
    </div>
  )
}
```

Create directories as needed. Next.js App Router creates routes from directory structure under app/(dashboard)/.
  </action>
  <verify>
    <automated>PATH=/usr/local/bin:$PATH npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 7 pages exist; each stub calls requireSection() with the correct section string matching lib/sections.ts; TypeScript compiles without errors; build succeeds</done>
</task>

</tasks>

<verification>
TypeScript compilation passes: `PATH=/usr/local/bin:$PATH npx tsc --noEmit`
Build succeeds: `PATH=/usr/local/bin:$PATH npm run build 2>&1 | tail -20`
All 7 page files exist under app/(dashboard)/.
Each stub page has the correct requireSection() call.
</verification>

<success_criteria>
- ComingSoon component exists with sectionName prop and "В разработке" message
- 6 module stub pages created at correct routes matching SECTION_PATHS in lib/sections.ts
- /support page shows support placeholder with GitHub link
- All pages call requireSection() with the correct ERP_SECTION string
- Unauthenticated access to any stub page redirects to /login (via middleware — no extra code needed)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-module-stubs/05-02-SUMMARY.md`
</output>
