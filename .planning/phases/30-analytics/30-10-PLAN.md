---
phase: 30-analytics
plan: 10
type: execute
wave: 2
depends_on: [30-03]
files_modified:
  - components/analytics/tabs/CardStatsTab.tsx
  - components/analytics/tabs/QueryStatsTab.tsx
autonomous: true
requirements: [ANL-09, ANL-10]
must_haves:
  truths:
    - "На «Статистика карточки» выбор N метрик строит N графиков динамики в каждой строке"
    - "Слева — компактный столбец средних в фикс. порядке (показы, CTR, конв. в корзину, конв. в заказ, заказы, сумма)"
    - "На «Статистика запросов» рендерится тепловая карта запрос×день; средняя позиция игнорирует дни-прочерки"
    - "По умолчанию 5 запросов + скролл к остальным; цвет ячейки: топ зелёный, глубоко красный, отсутствие — прочерк"
  artifacts:
    - path: "components/analytics/tabs/CardStatsTab.tsx"
      provides: "Вкладка графиков динамики (recharts, одна метрика = один график)"
      min_lines: 60
    - path: "components/analytics/tabs/QueryStatsTab.tsx"
      provides: "Вкладка тепловой карты позиций (кастомный div/grid)"
      min_lines: 60
  key_links:
    - from: "components/analytics/tabs/CardStatsTab.tsx"
      to: "lib/analytics/types.ts"
      via: "props SkuPayload[] + MetricKey"
      pattern: "SkuPayload"
---

<objective>
Реализовать две «сложные» вкладки дашборда как чистые презентационные клиентские компоненты, принимающие `SkuPayload[]` (ANL-09, ANL-10). «Статистика карточки»: столбец средних (фикс. порядок) + панель выбора метрик (URL searchParams) + «одна метрика = один график» динамики по дням (recharts, паттерн CashflowChart) с hover-значением. «Статистика запросов»: кастомная тепловая карта «запрос × день» на div/grid (без новой библиотеки — RESEARCH), цветовая шкала по глубине позиции, бейдж средней позиции (игнор прочерков), 5 запросов по умолчанию + скролл.

Purpose: Interface-first — компоненты строятся против контракта SkuPayload (30-03), не исследуя кодовую базу; параллелятся с data/clients.
Output: `components/analytics/tabs/CardStatsTab.tsx`, `components/analytics/tabs/QueryStatsTab.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-TZ-source.md
@lib/analytics/types.ts

<!-- Паттерны графиков recharts (тики fill var(--muted-foreground)) -->
@components/finance/CashflowChart.tsx
@components/ui/chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CardStatsTab — столбец средних + панель метрик + графики динамики</name>
  <files>components/analytics/tabs/CardStatsTab.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.9 — точный список метрик, фикс. порядок средних, «одна метрика = один график», ось X = даты, hover)
    - .planning/phases/30-analytics/30-TZ-source.md (§4 «Статистика карточки»)
    - components/finance/CashflowChart.tsx (recharts LineChart, XAxis даты, тики fill var(--muted-foreground), Tooltip)
    - components/sales-plan/PlanFactControls.tsx (URL searchParams через useSearchParams/router.push, native select/сегм. кнопки)
    - lib/analytics/types.ts (SkuPayload.funnel, funnelDays, priceDays, MetricKey)
  </read_first>
  <action>
    Клиентский компонент (`"use client"`) `CardStatsTab({ skus }: { skus: SkuPayload[] })`. Sticky-таблица (1 строка = 1 SKU). Слева по строке — компактный столбец средних значений в ФИКСИРОВАННОМ порядке: показы, CTR, конв. в корзину, конв. в заказ, заказы, сумма (из `funnel`). Сверху таблицы — панель выбора метрик (показы, CTR, конв. в корзину, конв. в заказ, заказы, сумма, выкуп, цена — БЕЗ позиций); выбор хранится в URL searchParams (`metrics=views,ctr,...`, паттерн PlanFactControls). Для каждой ВЫБРАННОЙ метрики — отдельный recharts LineChart в каждой строке (данные из `funnelDays`/`priceDays`), ось X = даты периода, правая шкала значений с делениями, Tooltip с точным значением за день (паттерн CashflowChart, тики `fill var(--muted-foreground)`). N метрик → N графиков в строке. Sticky-заголовок: `bg-background` БЕЗ `/NN` opacity (CLAUDE.md §471). НЕ вызывать client-функции из RSC (компонент client — ок).
  </action>
  <verify>
    <automated>test -f components/analytics/tabs/CardStatsTab.tsx && grep -q "SkuPayload" components/analytics/tabs/CardStatsTab.tsx && grep -Eq "recharts|LineChart" components/analytics/tabs/CardStatsTab.tsx && ! grep -Eq "bg-(background|muted)/[0-9]" components/analytics/tabs/CardStatsTab.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "CardStatsTab" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Компонент принимает `skus: SkuPayload[]`, рендерит столбец средних в фикс. порядке (показы, CTR, конв. в корзину, конв. в заказ, заказы, сумма).
    - Панель метрик управляет числом графиков через URL searchParams; используется recharts LineChart с осью X = даты и Tooltip.
    - Позиции в панели метрик ОТСУТСТВУЮТ (req.9).
    - Нет полупрозрачных `bg-*/NN` на sticky-ячейках; tsc не выдаёт ошибок по файлу.
  </acceptance_criteria>
  <done>CardStatsTab: столбец средних + панель метрик + N графиков динамики на recharts; hover-значения; без sticky-opacity-бага.</done>
</task>

<task type="auto">
  <name>Task 2: QueryStatsTab — тепловая карта позиций (запросы × дни)</name>
  <files>components/analytics/tabs/QueryStatsTab.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.10 — матрица запрос×день, бейдж средней позиции по дням присутствия, 5 запросов по умолч. + скролл, цветовая шкала)
    - .planning/phases/30-analytics/30-TZ-source.md (§4 «Статистика запросов»)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Package Legitimacy Audit — тепловую карту делать кастомным div/grid, БЕЗ новой библиотеки)
    - lib/analytics/types.ts (SkuPayload.queries: QueryPositionSeries[], PositionDay, QueryPositionSeries.avgPosition)
  </read_first>
  <action>
    Клиентский компонент (`"use client"`) `QueryStatsTab({ skus }: { skus: SkuPayload[] })`. По строке (1 SKU): артикул+бренд слева, справа — тепловая карта «запрос × день» на CSS-grid (div/grid, БЕЗ сторонней библиотеки). Строки матрицы = запросы SKU (`queries`), столбцы = дни периода (подписанные даты). У каждого запроса слева — цветной бейдж средней позиции (`avgPosition` из движка — уже считается по дням присутствия, 30-03); при `avgPosition===null` — прочерк. Цвет ячейки кодирует глубину позиции: топ (малое число) — зелёный, глубоко (большое) — красный; день отсутствия (`organic===null`) — прочерк (нейтральный, не красный, не входит в среднее). По умолчанию видно 5 запросов, остальные — вертикальный скролл ВНУТРИ строки (`max-h` + `overflow-y-auto`). Sticky-заголовок дат: `bg-background` БЕЗ `/NN` opacity.
  </action>
  <verify>
    <automated>test -f components/analytics/tabs/QueryStatsTab.tsx && grep -q "SkuPayload" components/analytics/tabs/QueryStatsTab.tsx && grep -Eq "grid|overflow-y-auto" components/analytics/tabs/QueryStatsTab.tsx && ! grep -Eq "bg-(background|muted)/[0-9]" components/analytics/tabs/QueryStatsTab.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "QueryStatsTab" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Компонент рендерит матрицу запрос×день (CSS-grid), подписи дат, бейдж avgPosition слева у каждого запроса.
    - Цветовая шкала: топ→зелёный, глубоко→красный; отсутствие → прочерк.
    - По умолчанию 5 запросов видны, остальные доступны скроллом внутри строки (`overflow-y-auto`).
    - Новая npm-библиотека НЕ добавлена (кастомный div/grid); нет sticky-opacity-бага.
  </acceptance_criteria>
  <human-check>
    После сборки дашборда (30-11): открыть «Статистика запросов» на реальном прогоне — визуально проверить читаемость матрицы, корректность цветовой шкалы и скролл к запросам >5 (R10 частично manual, VALIDATION.md).
  </human-check>
  <done>QueryStatsTab: тепловая карта запрос×день на кастомном grid, бейдж средней позиции (игнор прочерков), 5 по умолч. + скролл, цветовая шкала по глубине.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client-компонент | payloadJson (данные конкурентов) передаётся в браузер как props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Information Disclosure | данные прогона в DOM | accept | внутренний ERP, доступ гейтится RBAC (30-13); данные конкурентов не PII |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | тепловая карта — кастомный div/grid, БЕЗ новой библиотеки (RESEARCH) |
</threat_model>

<verification>
- Оба файла существуют, принимают SkuPayload[], tsc без ошибок по ним.
- Нет полупрозрачных sticky-ячеек.
- Новой npm-зависимости нет.
</verification>

<success_criteria>
Вкладки «Статистика карточки» (N метрик → N графиков) и «Статистика запросов» (тепловая карта, средняя без прочерков, 5+скролл) реализованы против контракта SkuPayload. ANL-09/ANL-10 (UI) готовы к сборке в контейнер (30-11).
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-10-SUMMARY.md` — экспортируемые имена компонентов (их импортирует контейнер 30-11).
</output>
