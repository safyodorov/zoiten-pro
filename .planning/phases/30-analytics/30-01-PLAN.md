---
phase: 30-analytics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/30-analytics/30-WAVE0-NOTES.md
  - tests/fixtures/analytics-detail-sample.json
  - tests/fixtures/analytics-card-sample.json
autonomous: false
requirements: [ANL-03, ANL-04]
user_setup:
  - service: mpstats
    why: "Реальный MPSTATS-токен нужен для curl-верификации точных эндпоинтов позиций/запросов (RESEARCH Open Q#1 — блокирующий unknown)"
    env_vars:
      - name: MPSTATS_TOKEN_SPIKE
        source: "MPSTATS кабинет → Аккаунт → блок «API токен»"
must_haves:
  truths:
    - "Точные MPSTATS-эндпоинты (позиции organic/ad + список запросов с частотностью) подтверждены curl-запросом с реальным токеном"
    - "Актуальная карта vol→basket-host и реальная форма полей card.json подтверждены на реальном nmID"
    - "Есть ≥1 реальный образец detail-JSON для golden-тестов парсера и движка"
  artifacts:
    - path: ".planning/phases/30-analytics/30-WAVE0-NOTES.md"
      provides: "Подтверждённые пути MPSTATS + карта basket-host + форма card.json"
      contains: "X-Mpstats-TOKEN"
    - path: "tests/fixtures/analytics-detail-sample.json"
      provides: "Образец detail-файла «Сравнение карточек» для R1/R2"
    - path: "tests/fixtures/analytics-card-sample.json"
      provides: "Образец basket-CDN card.json для R4"
  key_links:
    - from: "30-WAVE0-NOTES.md"
      to: "lib/analytics/mpstats.ts (план 30-05)"
      via: "документированные пути эндпоинтов"
      pattern: "positions|keywords|search"
---

<objective>
Разрешить ДВА блокирующих внешних unknown ДО написания продакшен-кода клиентов (RESEARCH §Common Pitfalls #1, #4; §Open Questions #1, #3, #4). Спайк с реальным MPSTATS-токеном пользователя фиксирует точные пути API; проверка card.json на реальном nmID фиксирует актуальную карту basket-host и форму полей; сохраняются реальные фикстуры для golden-тестов парсера/движка.

Purpose: Без этого спайка `mpstats.ts` (ANL-03/R3) и `wb-card-scan.ts` (ANL-04/R4) писались бы вслепую — прямой перенос неподтверждённых предположений в код (повтор антипаттерна Phase 07 PROMO_API).
Output: `30-WAVE0-NOTES.md` + 2 фикстуры в `tests/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-RESEARCH.md
@.planning/phases/30-analytics/30-VALIDATION.md
@.planning/phases/30-analytics/30-TZ-source.md

<!-- Прецедент формата Wave 0 заметок: -->
@.planning/phases/07-prices-wb/07-WAVE0-NOTES.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Получить от пользователя MPSTATS-токен и реальный detail-JSON</name>
  <what-built>Спайк требует двух пользовательских входов, которые Claude не может добыть сам: (1) персональный MPSTATS-токен (заголовок X-Mpstats-TOKEN), (2) ≥1 реальный файл detail из отчёта WB «Сравнение карточек» (перехваченный DevTools → Network → запрос `detail`).</what-built>
  <read_first>
    - .planning/phases/30-analytics/30-RESEARCH.md (§Open Questions #1, #4 — что именно нужно и почему)
    - .planning/phases/30-analytics/30-TZ-source.md (§2.1, §2.2 — структура файла и источник токена)
  </read_first>
  <how-to-verify>
    1. Предоставьте MPSTATS-токен (кабинет → Аккаунт → «API токен»). ВАЖНО: токен НЕ коммитить, использовать только для curl во время спайка.
    2. Приложите ≥1 реальный файл detail (JSON) из отчёта «Сравнение карточек» (5 SKU внутри).
    3. Если под рукой есть — приложите ссылку на 2-3 реальных конкурентных nmID из целевой ниши (для проверки card.json / basket-host).
  </how-to-verify>
  <acceptance_criteria>
    - Пользователь предоставил непустой MPSTATS-токен (для curl-проверки эндпоинтов).
    - Пользователь предоставил ≥1 файл detail с полем `data.salesFunnel.byDay` (или подтвердил, что структура ТЗ §2.1 актуальна).
  </acceptance_criteria>
  <resume-signal>Токен передан + файл detail приложен → «продолжай»</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Curl-верификация MPSTATS-эндпоинтов позиций и запросов</name>
  <files>.planning/phases/30-analytics/30-WAVE0-NOTES.md</files>
  <read_first>
    - .planning/phases/30-analytics/30-RESEARCH.md (§MPSTATS Client Skeleton — base URL `https://mpstats.io/api/wb`, заголовок `X-Mpstats-TOKEN`, конвенция `/get/item/{id}/{report}?d1=&d2=`; §Assumptions Log A1-A2)
    - .planning/phases/07-prices-wb/07-WAVE0-NOTES.md (образец структуры заметок)
  </read_first>
  <action>
    С реальным токеном пользователя выполнить curl (не Node fetch) к кандидат-эндпоинтам MPSTATS для ОДНОГО реального nmID из detail-файла, окно дат = период `byDay` файла (d1/d2 в формате YYYY-MM-DD). Проверить кандидатов для (a) позиций organic/ad по дням с рекламными параметрами (CPM/тип размещения/позиция буста) — пробовать `/get/item/{id}/positions`, `/get/item/{id}/by_keywords`, `/get/item/{id}/keywords`, `/get/item/{id}/search_queries`; (b) списка запросов ниши с частотностью — искать эндпоинт, отдающий запросы + поле частотности (frequency/count). Если публичные пути не отвечают — открыть раздел API в кабинете MPSTATS пользователя (персональная Swagger/Postman-дока). Задокументировать в `30-WAVE0-NOTES.md`: рабочий путь каждого эндпоинта, метод, параметры (d1/d2/пагинация), реальную JSON-форму ответа (имена полей organic-позиции, ad-позиции, CPM, тип, буст, дата, частотность запроса), поведение при 429/исчерпании лимита. Для КАЖДОГО реально работающего эндпоинта записать отдельной строкой машиночитаемый маркер `CONFIRMED: <METHOD> <path>` (например `CONFIRMED: GET /get/item/{id}/keywords`); неудачные попытки помечать `FAILED:`/`UNCONFIRMED:` — они НЕ должны проходить как успех. Токен НЕ записывать в заметки (маскировать как `<TOKEN>`).
  </action>
  <verify>
    <automated>test -f .planning/phases/30-analytics/30-WAVE0-NOTES.md && grep -qi "X-Mpstats-TOKEN" .planning/phases/30-analytics/30-WAVE0-NOTES.md && grep -q "CONFIRMED:" .planning/phases/30-analytics/30-WAVE0-NOTES.md && grep -A2 "CONFIRMED:" .planning/phases/30-analytics/30-WAVE0-NOTES.md | grep -Eqi "position|keyword|search" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `30-WAVE0-NOTES.md` содержит секцию «MPSTATS endpoints», где КАЖДЫЙ реально работающий путь для позиций (organic/ad) и для списка запросов с частотностью помечен маркером `CONFIRMED:`; неудачные попытки помечены `FAILED:`/`UNCONFIRMED:` (файл только с неудачными попытками НЕ проходит verify).
    - Для каждого эндпоинта записана реальная форма ответа (конкретные имена полей), достаточная, чтобы план 30-05 написал типы без догадок.
    - Токен нигде в файле не записан открытым текстом (маскирован).
  </acceptance_criteria>
  <done>Пути MPSTATS для позиций и запросов подтверждены реальным curl либо кабинетной докой; форма ответа задокументирована; путь для фильтра «частотность > 500» определён.</done>
</task>

<task type="auto">
  <name>Task 3: Проверка card.json (vol→host, форма полей) + сохранение фикстур</name>
  <files>.planning/phases/30-analytics/30-WAVE0-NOTES.md, tests/fixtures/analytics-card-sample.json, tests/fixtures/analytics-detail-sample.json</files>
  <read_first>
    - .planning/phases/30-analytics/30-RESEARCH.md (§Basket-CDN URL Construction — формула vol=nmId/100000, part=nmId/1000, таблица BASKET_RANGES; §Common Pitfalls #3, #4; §Assumptions Log A3, A4)
    - lib/wb-storefront-feedbacks.ts (заметка: basket-хосты обычно НЕ блокируют native fetch, в отличие от card.wb.ru)
  </read_first>
  <action>
    Для реального nmID (из detail-файла или предоставленного пользователем) построить URL `https://basket-{host}.wbbasket.ru/vol{vol}/part{part}/{nmId}/info/ru/card.json` и проверить доступность: сначала native `fetch`, при 403/блокировке — curl (зафиксировать, какой транспорт работает — подтвердить/опровергнуть A3). Подобрать реальный `host` для 2-3 nmID из разных диапазонов vol → зафиксировать актуальную карту `vol→host` (обновить таблицу RESEARCH на дату исполнения). Сохранить полученный card.json как `tests/fixtures/analytics-card-sample.json`. В `30-WAVE0-NOTES.md` записать: рабочий транспорт (fetch/curl), путь к первым 5 фото листинга внутри card.json (поле/схема URL превью), путь к характеристикам (options/grouped), актуальную карту vol→host + дату среза, поведение при 404 (нужно ли fallback-пробирование соседних хостов). Отдельно: сохранить предоставленный пользователем detail-файл как `tests/fixtures/analytics-detail-sample.json` и сверить его реальные имена полей `byDay[]`/`commonParams[]`/`searchQueries[]` с ТЗ §2.1 — расхождения зафиксировать в заметках (A5).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/analytics-detail-sample.json','utf8')); JSON.parse(require('fs').readFileSync('tests/fixtures/analytics-card-sample.json','utf8')); console.log('OK')" && grep -Eqi "vol|basket|card.json" .planning/phases/30-analytics/30-WAVE0-NOTES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/analytics-detail-sample.json` — валидный JSON, содержит `data.salesFunnel.byDay` (или задокументировано реальное отличие структуры).
    - `tests/fixtures/analytics-card-sample.json` — валидный JSON реального card.json с массивом фото и характеристиками.
    - `30-WAVE0-NOTES.md` фиксирует: рабочий транспорт для card.json (fetch vs curl), путь к фото и характеристикам, актуальную карту vol→host с датой, стратегию 404-fallback.
    - Расхождения реальных имён полей detail-JSON с ТЗ §2.1 явно перечислены (или подтверждено совпадение).
  </acceptance_criteria>
  <done>Форма card.json и карта basket-host подтверждены на реальном nmID; обе фикстуры сохранены и валидны; расхождения detail-JSON зафиксированы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-процесс → MPSTATS API | реальный токен пользователя пересекает границу при curl |
| dev-процесс → basket-CDN | произвольный nmID формирует внешний URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | MPSTATS-токен в заметках/логах | mitigate | токен маскируется как `<TOKEN>` в 30-WAVE0-NOTES.md; не коммитить реальное значение |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | фаза не устанавливает внешних пакетов (RESEARCH §Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `30-WAVE0-NOTES.md` существует и содержит подтверждённые MPSTATS-пути + карту basket-host.
- Обе фикстуры — валидный JSON, парсятся node.
- Токен не записан открытым текстом.
</verification>

<success_criteria>
Оба блокирующих unknown (MPSTATS-пути, card.json/basket-host) разрешены реальной проверкой; фикстуры для R1/R2/R4 готовы. Планы 30-04/30-05/30-06 могут писать код без догадок.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-01-SUMMARY.md` when done — включить подтверждённые пути MPSTATS и карту vol→host (их будут читать планы 30-05/30-06).
</output>
