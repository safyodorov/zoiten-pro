---
phase: 30-analytics
plan: 09
type: execute
wave: 5
depends_on: [30-08, 30-11, 30-12, 30-02]
files_modified:
  - app/(dashboard)/analytics/page.tsx
  - app/(dashboard)/analytics/upload/page.tsx
  - components/analytics/AnalyticsUploadForm.tsx
  - components/analytics/AnalyticsTokenBar.tsx
  - components/analytics/NicheRunStatusPoller.tsx
autonomous: true
requirements: [ANL-05]
must_haves:
  truths:
    - "Список сохранённых прогонов: ≥2 разных прогона, можно открыть прошлый без пересбора"
    - "Форма загрузки 6 файлов → превью 30 SKU → кнопка «Начать сбор» (заблокирована при активном прогоне)"
    - "MPSTATS-токен вводится в шапке (type=password, маскируется при повторном открытии)"
    - "Poller опрашивает статус COLLECTING → READY/PARTIAL/FAILED и редиректит на прогон"
    - "Зависшие COLLECTING (updatedAt старше ~15 мин) помечаются с кнопкой «Пометить как FAILED»"
  artifacts:
    - path: "app/(dashboard)/analytics/page.tsx"
      provides: "RSC-список прогонов (история)"
      min_lines: 30
    - path: "components/analytics/AnalyticsTokenBar.tsx"
      provides: "Ввод MPSTATS-токена (GlobalRatesBar-паттерн, password)"
    - path: "components/analytics/NicheRunStatusPoller.tsx"
      provides: "Polling статуса прогона"
  key_links:
    - from: "components/analytics/AnalyticsTokenBar.tsx"
      to: "app/actions/analytics.ts saveMpstatsToken"
      via: "debounced save (MANAGE)"
      pattern: "saveMpstatsToken"
---

<objective>
Реализовать UI-оболочку раздела (ANL-05 история, D-01 токен, D-02 запуск/прогресс): список сохранённых прогонов (история, открытие прошлого без пересбора, ≥2 прогона, индикатор «завис» для COLLECTING старше ~15 мин); страница загрузки 6 файлов с превью 30 SKU и кнопкой «Начать сбор» (заблокирована при активном прогоне); шапка ввода MPSTATS-токена (паттерн `GlobalRatesBar`, `type=password`, маскировка); компонент polling статуса. Кнопка выгрузки PDF (ссылка на pdf-route 30-12) и ссылки на дашборд-прогоны (30-11).

Purpose: Дать пользователю точки входа: история, запуск, прогресс, токен.
Output: список-страница, upload-страница, upload-форма, token-bar, status-poller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-CONTEXT.md
@.planning/phases/30-analytics/30-RESEARCH.md

<!-- Паттерн debounced KV-шапки (для токена) -->
@components/prices/GlobalRatesBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSC-список прогонов + индикатор «завис»</name>
  <files>app/(dashboard)/analytics/page.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.5 acceptance — ≥2 прогона, открыть прошлый, новая запись при пересборе)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 2 «Важная деталь надёжности» — «завис» для COLLECTING старше N минут; §Common Pitfalls #2)
    - lib/rbac.ts (requireSection/getSectionRole); CLAUDE.md §601 (<Link prefetch={false}> в списках)
  </read_first>
  <action>
    RSC `app/(dashboard)/analytics/page.tsx`: `requireSection("ANALYTICS","VIEW")`; `prisma.nicheRun.findMany({orderBy:{createdAt:"desc"}})`; таблица прогонов (дата, период dateFrom–dateTo, статус, skuCount, пометка неполноты для PARTIAL); каждая строка — `<Link href="/analytics/runs/{id}" prefetch={false}>`; кнопка «Новый прогон» → `/analytics/upload`; для записей `COLLECTING` с `updatedAt` старше ~15 мин — визуальный бейдж «завис» + кнопка (server action) «Пометить как FAILED» (обновляет status; MANAGE); ссылка «PDF» на `/api/analytics/runs/{id}/pdf` для READY/PARTIAL — со списка порядок ПО УМОЛЧАНИЮ = revenue (без query `sort`). ПРИМЕЧАНИЕ: приёмка req.6/req.11 «порядок PDF = выбранная на экране сортировка» закрывается кнопкой «Скачать PDF» ВНУТРИ дашборда (план 30-11, читает активный `?sort=`); ссылка со списка — быстрый доступ с дефолтным порядком.
  </action>
  <verify>
    <automated>test -f "app/(dashboard)/analytics/page.tsx" && grep -q "requireSection" "app/(dashboard)/analytics/page.tsx" && grep -q "nicheRun.findMany" "app/(dashboard)/analytics/page.tsx" && grep -q "prefetch={false}" "app/(dashboard)/analytics/page.tsx" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "analytics/page" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Список показывает все прогоны (desc), открывает прошлый по ссылке без пересбора; PARTIAL помечен.
    - COLLECTING старше ~15 мин помечен «завис» + доступна ручная пометка FAILED.
    - Ссылки-строки используют `prefetch={false}`; гейтится VIEW; tsc чист.
  </acceptance_criteria>
  <done>Список прогонов с историей, открытием прошлого, PDF-ссылкой и индикатором «завис».</done>
</task>

<task type="auto">
  <name>Task 2: upload-страница + AnalyticsUploadForm + NicheRunStatusPoller</name>
  <files>app/(dashboard)/analytics/upload/page.tsx, components/analytics/AnalyticsUploadForm.tsx, components/analytics/NicheRunStatusPoller.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.1 UX, req.5)
    - .planning/phases/30-analytics/30-CONTEXT.md (D-02 фоновый прогон + прогресс)
    - app/api/analytics/upload/route.ts (форма ответа превью — 30-08); app/actions/analytics.ts (startNicheRun — 30-08)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Security Domain — accept=".json" + проверка file.size на клиенте; дизейбл кнопки при активном прогоне)
  </read_first>
  <action>
    RSC `upload/page.tsx` (requireSection VIEW) с клиентским `AnalyticsUploadForm`: `<input type="file" accept=".json" multiple>` (ровно 6; клиентская проверка `file.size` до отправки — анти-DoS); POST в `/api/analytics/upload`; отобразить превью 30 SKU (или ошибку 400); кнопка «Начать сбор» вызывает `startNicheRun` (дизейблить, если ответ сообщает об активном прогоне, T-30-04); после запуска — смонтировать `NicheRunStatusPoller` с полученным runId. `NicheRunStatusPoller.tsx` (`"use client"`): каждые ~2-3с `GET /api/analytics/runs/{id}/status`, показывать `progressNote`; при READY/PARTIAL — `router.push("/analytics/runs/{id}")`; при FAILED — показать errorMessage. Требует ввода токена (если нет — направить к шапке AnalyticsTokenBar).
  </action>
  <verify>
    <automated>test -f "app/(dashboard)/analytics/upload/page.tsx" && test -f components/analytics/AnalyticsUploadForm.tsx && test -f components/analytics/NicheRunStatusPoller.tsx && grep -q "startNicheRun" components/analytics/AnalyticsUploadForm.tsx && grep -Eq "accept=\".json\"|accept='.json'" components/analytics/AnalyticsUploadForm.tsx && grep -q "status" components/analytics/NicheRunStatusPoller.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "UploadForm|StatusPoller|analytics/upload" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Форма принимает ровно 6 .json, проверяет размер на клиенте, показывает превью 30 SKU или ошибку.
    - «Начать сбор» вызывает startNicheRun и блокируется при активном прогоне.
    - Poller опрашивает status, показывает прогресс, редиректит на прогон при READY/PARTIAL; tsc чист.
  </acceptance_criteria>
  <human-check>
    Полный прогон вручную: загрузить 6 реальных файлов → превью 30 → «Начать сбор» → наблюдать прогресс → открыть готовый дашборд (D-02 UX, R8/R9 визуально).
  </human-check>
  <done>Upload-поток: 6 файлов → превью → фоновый запуск → polling прогресса → редирект на дашборд.</done>
</task>

<task type="auto">
  <name>Task 3: AnalyticsTokenBar — ввод MPSTATS-токена (password, debounced)</name>
  <files>components/analytics/AnalyticsTokenBar.tsx</files>
  <read_first>
    - components/prices/GlobalRatesBar.tsx (образец 1:1 — debounce 500ms + useTransition + sonner toast + router.refresh)
    - .planning/phases/30-analytics/30-CONTEXT.md (D-01 — токен в шапке раздела)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Security Domain — input type=password, маска •••• при повторном открытии)
    - app/actions/analytics.ts (saveMpstatsToken — 30-08)
  </read_first>
  <action>
    Клиентский `AnalyticsTokenBar.tsx` по образцу `GlobalRatesBar.tsx`: ОДНО поле `<input type="password">` для MPSTATS-токена; debounced (~500ms) вызов `saveMpstatsToken` (server action, MANAGE) + toast + `router.refresh()`; при повторном открытии — НЕ показывать реальное значение, placeholder `••••••••` (маскировка, T-30-01). Разместить в шапке раздела (страница списка/upload). Показывать поле только пользователю с MANAGE (getSectionRole).
  </action>
  <verify>
    <automated>test -f components/analytics/AnalyticsTokenBar.tsx && grep -q 'type="password"' components/analytics/AnalyticsTokenBar.tsx && grep -q "saveMpstatsToken" components/analytics/AnalyticsTokenBar.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "TokenBar" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Поле токена `type="password"`; debounced save через saveMpstatsToken; toast + router.refresh.
    - Реальное значение токена не отображается при повторном открытии (маска/placeholder).
    - tsc чист по файлу.
  </acceptance_criteria>
  <done>Шапка ввода MPSTATS-токена по паттерну GlobalRatesBar с маскировкой (password).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер → сервер | ввод токена, загрузка файлов, запуск |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | поле MPSTATS-токена | mitigate | input type=password + маска •••• при повторном открытии; поле только для MANAGE |
| T-30-03 | Denial of Service | загрузка файлов | mitigate | клиентская проверка file.size + accept=".json" до отправки |
| T-30-04 | Resource Exhaustion | кнопка «Начать сбор» | mitigate | дизейбл при активном прогоне (сервер тоже блокирует — 30-08) |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- Все 5 файлов существуют; tsc чист.
- Токен — password; кнопка запуска блокируется при активном прогоне; poller работает.
</verification>

<success_criteria>
UI-оболочка раздела готова: история прогонов (≥2, открытие прошлого, «завис»), загрузка 6 файлов → превью → фоновый запуск → прогресс → дашборд, ввод токена (password). ANL-05 UI + D-01/D-02 UX выполнены.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-09-SUMMARY.md` — маршруты раздела (/analytics, /analytics/upload) для RBAC-регистрации (30-13).
</output>
