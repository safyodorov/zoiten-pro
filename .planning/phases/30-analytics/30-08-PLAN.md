---
phase: 30-analytics
plan: 08
type: execute
wave: 4
depends_on: [30-04, 30-07, 30-02]
files_modified:
  - app/api/analytics/upload/route.ts
  - app/actions/analytics.ts
  - app/api/analytics/runs/[id]/status/route.ts
autonomous: true
requirements: [ANL-01, ANL-05]
must_haves:
  truths:
    - "POST /api/analytics/upload валидирует 6 файлов и возвращает превью 30 SKU или 400 с явным сообщением"
    - "startNicheRun создаёт NicheRun(PENDING) и запускает сбор через after() (не блокирует ответ)"
    - "Запуск заблокирован, пока есть активный прогон (PENDING/COLLECTING) — защита лимита MPSTATS"
    - "saveMpstatsToken пишет AppSetting analytics.mpstatsToken (requireSection MANAGE)"
    - "GET .../status отдаёт status + progressNote для polling"
  artifacts:
    - path: "app/api/analytics/upload/route.ts"
      provides: "Парсинг+валидация 6 файлов (VIEW)"
      exports: ["POST"]
    - path: "app/actions/analytics.ts"
      provides: "startNicheRun (after()) + saveMpstatsToken (MANAGE)"
      exports: ["startNicheRun", "saveMpstatsToken"]
    - path: "app/api/analytics/runs/[id]/status/route.ts"
      provides: "Статус прогона для polling (VIEW)"
      exports: ["GET"]
  key_links:
    - from: "app/actions/analytics.ts startNicheRun"
      to: "lib/analytics/collector.ts collectNicheRun"
      via: "after(() => collectNicheRun(runId))"
      pattern: "after\\("
---

<objective>
Слой API/оркестрации (D-01, D-02): upload-эндпоинт валидирует 6 файлов (реюз `extractTop30` из 30-04) и возвращает превью 30 SKU; server action `startNicheRun` создаёт `NicheRun(PENDING)` и запускает `collectNicheRun` через `after()` из `next/server` (фоново, не блокирует HTTP — RESEARCH Pattern 2); `saveMpstatsToken` пишет токен в AppSetting KV; status-route отдаёт статус для polling. Защита лимита: запуск блокируется, пока есть незавершённый прогон (PENDING/COLLECTING). Лимит размера файла (анти-DoS).

Purpose: Связать парсер, персистентность и фоновый сбор в исполняемый поток «загрузка → запуск → прогресс».
Output: upload route, `app/actions/analytics.ts`, status route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-CONTEXT.md
@.planning/phases/30-analytics/30-RESEARCH.md
@lib/analytics/types.ts

<interfaces>
<!-- lib/rbac.ts — requireSection(section, minRole) throws UNAUTHORIZED/FORBIDDEN -->
export async function requireSection(section: string, minRole?: "VIEW"|"MANAGE"): Promise<void>
<!-- AppSetting KV upsert (колонки key/value/updatedAt, НЕТ createdAt) — ON CONFLICT(key) DO UPDATE -->
<!-- RESEARCH §Pattern 2 — startNicheRun с after(); §Pattern 4 — analytics.mpstatsToken upsert -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/analytics/upload — валидация 6 файлов + превью 30 SKU</name>
  <files>app/api/analytics/upload/route.ts</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.1 acceptance)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Security Domain — лимит размера файла, nginx client_max_body_size 5m)
    - lib/analytics/data.ts (extractTop30 — сигнатура из 30-04)
    - lib/rbac.ts (requireSection)
    - app/api/procurement/inspection/report-generate/route.ts (образец route-handler с requireSection + try/catch)
  </read_first>
  <action>
    `POST(req)`: `requireSection("ANALYTICS","VIEW")` (try/catch → 401/403); принять multipart с 6 файлами; проверить количество (===6) и размер каждого (лимит ~5МБ, анти-DoS — ДО JSON.parse); распарсить каждый JSON и передать в `extractTop30`; при ошибке валидации/дубликата/периода — вернуть 400 с явным человекочитаемым сообщением (текст ошибки из data.ts); при успехе — вернуть JSON превью `{skus: [{nmId, brand, mainPhoto}...30], dateFrom, dateTo}`. НЕ создавать NicheRun здесь (создаётся в startNicheRun после подтверждения).
  </action>
  <verify>
    <automated>test -f app/api/analytics/upload/route.ts && grep -q "requireSection" app/api/analytics/upload/route.ts && grep -q "extractTop30" app/api/analytics/upload/route.ts && grep -q "ANALYTICS" app/api/analytics/upload/route.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "analytics/upload" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Route вызывает `requireSection("ANALYTICS","VIEW")` в try/catch (401/403 при ошибке).
    - При невалидных/дублирующих файлах или несовпадении периода — HTTP 400 с явным сообщением.
    - При 6 валидных файлах — 200 + превью ровно 30 SKU + dateFrom/dateTo.
    - Есть проверка размера файла до парсинга; tsc чист по файлу.
  </acceptance_criteria>
  <done>upload-route валидирует 6 файлов и отдаёт превью 30 SKU либо 400 с сообщением; лимит размера соблюдён.</done>
</task>

<task type="auto">
  <name>Task 2: startNicheRun (after()) + saveMpstatsToken — server actions</name>
  <files>app/actions/analytics.ts</files>
  <read_first>
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 2 — startNicheRun+after() полный листинг; §Pattern 4 — AppSetting upsert analytics.mpstatsToken; §Common Pitfalls #2 — after() не переживает рестарт)
    - .planning/phases/30-analytics/30-CONTEXT.md (D-01 токен в AppSetting; D-02 фоновый прогон; D-03 лимит)
    - lib/analytics/collector.ts (collectNicheRun — сигнатура из 30-07)
    - lib/rbac.ts (requireSection); CLAUDE.md (Server Actions: "use server" + requireSection + try/catch + revalidatePath + zod)
  </read_first>
  <action>
    Файл с `"use server"`. `startNicheRun(input)`: `requireSection("ANALYTICS","MANAGE")`; zod-валидация input (30 nmID, dateFrom/dateTo, byDay); ПРОВЕРИТЬ отсутствие активного прогона (`prisma.nicheRun.findFirst({where:{status:{in:["PENDING","COLLECTING"]}}})`) — если есть, вернуть `{ok:false, error:"уже идёт сбор"}` (защита лимита MPSTATS, T-30-04); прочитать токен из AppSetting (`analytics.mpstatsToken`) — если пуст, вернуть ошибку «укажите MPSTATS-токен»; `prisma.nicheRun.create({status:"PENDING", dateFrom, dateTo, skuCount:30, createdById})`; `after(async () => { try { await collectNicheRun(run.id, {...input, mpstatsToken}) } catch { update FAILED } })` из `next/server`; вернуть `{ok:true, runId}`; `revalidatePath("/analytics")`. `saveMpstatsToken(token)`: `requireSection("ANALYTICS","MANAGE")`; zod (непустая строка); `prisma.appSetting.upsert({where:{key:"analytics.mpstatsToken"}, create:{key:"analytics.mpstatsToken", value:token}, update:{value:token}})`; `revalidatePath("/analytics")`.
  </action>
  <verify>
    <automated>test -f app/actions/analytics.ts && grep -q '"use server"' app/actions/analytics.ts && grep -q "after(" app/actions/analytics.ts && grep -q "collectNicheRun" app/actions/analytics.ts && grep -q "MANAGE" app/actions/analytics.ts && grep -q "analytics.mpstatsToken" app/actions/analytics.ts && grep -Eq "PENDING|COLLECTING" app/actions/analytics.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "actions/analytics" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `startNicheRun` вызывает `requireSection("ANALYTICS","MANAGE")`, создаёт NicheRun PENDING, запускает `collectNicheRun` через `after()` из `next/server`, возвращает runId немедленно.
    - Активный прогон (PENDING/COLLECTING) блокирует новый запуск (T-30-04); пустой токен блокирует запуск.
    - `saveMpstatsToken` upsert-ит `analytics.mpstatsToken` в AppSetting (MANAGE); tsc чист.
  </acceptance_criteria>
  <done>startNicheRun запускает фоновый сбор через after() с защитой от параллельных запусков; saveMpstatsToken пишет токен в KV.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/analytics/runs/[id]/status — статус для polling</name>
  <files>app/api/analytics/runs/[id]/status/route.ts</files>
  <read_first>
    - .planning/phases/30-analytics/30-CONTEXT.md (D-02 — polling статуса, прогресс «собрано X/30»)
    - lib/rbac.ts (requireSection)
    - app/api/analytics/upload/route.ts (образец route-handler с requireSection из Task 1)
  </read_first>
  <action>
    `GET(req, {params})`: `requireSection("ANALYTICS","VIEW")` (try/catch → 401/403); прочитать `prisma.nicheRun.findUnique({where:{id}, select:{status, progressNote, incompleteSkus, errorMessage, updatedAt}})`; вернуть JSON `{status, progressNote, incompleteSkus, errorMessage}`. Если запись отсутствует — 404. Легковесный эндпоинт (без payloadJson — его читает RSC-страница).
  </action>
  <verify>
    <automated>test -f "app/api/analytics/runs/[id]/status/route.ts" && grep -q "requireSection" "app/api/analytics/runs/[id]/status/route.ts" && grep -q "nicheRun" "app/api/analytics/runs/[id]/status/route.ts" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "status/route" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GET возвращает `{status, progressNote}` для существующего runId; 404 для несуществующего; 401/403 без гранта.
    - Эндпоинт не отдаёт payloadJson (только статус-поля); tsc чист.
  </acceptance_criteria>
  <done>status-route отдаёт статус + прогресс для polling; гейтится VIEW.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| клиент → сервер | загрузка файлов, запуск сбора, ввод токена |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Denial of Service | upload JSON-файлов | mitigate | проверка размера файла ДО JSON.parse (≤5МБ); nginx client_max_body_size 5m |
| T-30-04 | Resource Exhaustion (лимит MPSTATS) | параллельные клики «Начать сбор» | mitigate | startNicheRun блокирует запуск при активном PENDING/COLLECTING прогоне |
| T-30-06 | Elevation of Privilege | upload/actions/status | mitigate | requireSection ANALYTICS: VIEW на чтение/upload, MANAGE на запуск/токен |
| T-30-05 | Information Disclosure | токен plaintext в AppSetting | accept | прецедент WbApiToken; внутренний ERP, доверенный доступ к БД (D-01) |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- upload/actions/status существуют, каждый вызывает requireSection; tsc чист по файлам.
- startNicheRun использует after() + блокировку активного прогона.
</verification>

<success_criteria>
Поток «загрузка 6 файлов → превью 30 SKU → запуск фонового сбора → polling статуса» исполним; токен пишется в KV; запуск защищён от параллельных кликов и требует MANAGE. ANL-01 wiring + ANL-05 запуск готовы; D-01/D-02 выполнены.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-08-SUMMARY.md` — форма ответа upload-превью и статус-эндпоинта (их потребляет UI 30-09).
</output>
