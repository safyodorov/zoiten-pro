---
phase: 30-analytics
plan: 06
type: tdd
wave: 2
depends_on: [30-03, 30-01]
files_modified:
  - lib/analytics/wb-card-scan.ts
  - tests/analytics-wb-card-scan.test.ts
autonomous: true
requirements: [ANL-04]
must_haves:
  truths:
    - "По nmID возвращаются первые 5 фото листинга + характеристики (из card.json, per-nmId basket-CDN)"
    - "Цена/СПП/рейтинг конкурентов берутся ПЕРВИЧНО из detail-JSON (D-04); card.wb.ru — только для сверки"
    - "Сверка card.wb.ru идёт ОДНИМ батч-вызовом на все nmId (≤20/запрос → 2 батча), НЕ per-SKU, БЕЗ Sales-API fallback"
    - "nmID валидируется как положительное целое < 2^31 перед построением URL (анти-SSRF)"
    - "При 404 ожидаемого basket-host пробуются соседние хосты (fallback)"
  artifacts:
    - path: "lib/analytics/wb-card-scan.ts"
      provides: "scanCardMedia (card.json фото+характеристики) + verifyPricesBatch (батч card.wb.ru, best-effort)"
      exports: ["scanCardMedia", "verifyPricesBatch", "cardJsonUrl"]
    - path: "tests/analytics-wb-card-scan.test.ts"
      provides: "Тест на фикстуре card.json + мок транспорта; батч-вызов без per-SKU"
  key_links:
    - from: "lib/analytics/wb-card-scan.ts verifyPricesBatch"
      to: "lib/wb-api.ts (fetchWbDiscounts)"
      via: "ОДИН батч-вызов на все nmId (реюз), без Sales-API ветки"
      pattern: "wb-api"
---

<objective>
Реализовать скан карточек конкурентов (ANL-04) через ПЕРЕИСПОЛЬЗОВАНИЕ существующего механизма (SPEC req.4 — не писать новый транспорт). basket-CDN `card.json` даёт первые 5 фото листинга + характеристики (D-04, per-nmId — basket-хосты без rate-limit). Цена/СПП/рейтинг конкурентов ПЕРВИЧНО берутся из detail-JSON (D-04: medianPrice×0.97, commonParams.nmRating/feedbacksCount); `card.wb.ru/cards/v4/detail` — только для СВЕРКИ и вызывается ОДНИМ батч-вызовом (`fetchWbDiscounts` внутренне батчит ≤20/запрос → 30 nmId = 2 батча), НЕ per-SKU и БЕЗ активации Sales-API fallback. Анти-SSRF: nmID только из провалидированного detail-JSON, проверка положительного целого < 2^31.

Purpose: Собрать листинг-данные без изобретения нового скана И без риска WB Statistics-API rate-limit/IP-бана (см. HIGH-3 ниже).
Output: `lib/analytics/wb-card-scan.ts`, `tests/analytics-wb-card-scan.test.ts`.

**Wave 0 gating (MEDIUM-1):** актуальная карта vol→host, рабочий транспорт card.json (fetch vs curl) и форма полей фото/характеристик — LOW/MEDIUM-confidence unknowns (RESEARCH §Open Questions #3, §Assumptions Log A3–A4), РАЗРЕШАЮТСЯ в Wave 0 (план 30-01). Значения в RESEARCH до Wave 0 — предположения; финализировать по `30-WAVE0-NOTES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-WAVE0-NOTES.md
@.planning/phases/30-analytics/30-RESEARCH.md
@tests/fixtures/analytics-card-sample.json
@lib/analytics/types.ts

<interfaces>
<!-- Переиспользуемый curl-механизм — lib/wb-api.ts:408 -->
<!-- fetchWbDiscounts ВНУТРЕННЕ батчит nmIds по 20 (for i+=20) через curl card.wb.ru v4;
     Step-2 Sales-API fallback (statistics-api.wildberries.ru, rate-limit 5/мин, БЕЗ retry) активируется при v4Failed -->
export async function fetchWbDiscounts(
  nmIds: number[],
  sellerPriceMap?: Map<number, PriceData>,
  storefrontOut?: StorefrontRatingsOut
): Promise<Map<number, number>>
export interface StorefrontRatingsOut { ratings: Map<number, number>; feedbacks: Map<number, number> }
<!-- curl-паттерн (execSync) внутри fetchWbDiscounts — НЕ переписывать, ВЫЗЫВАТЬ батчем -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cardJsonUrl + scanCardMedia (фото+характеристики) с анти-SSRF и fallback</name>
  <files>lib/analytics/wb-card-scan.ts, tests/analytics-wb-card-scan.test.ts</files>
  <behavior>
    - cardJsonUrl(nmId): валидный nmId → корректный URL basket-{host}.wbbasket.ru/vol{vol}/part{part}/{nmId}/info/ru/card.json (host по карте Wave0).
    - cardJsonUrl(отрицательный / нечисловой / ≥2^31): throw (анти-SSRF).
    - scanCardMedia(nmId) на фикстуре: возвращает listingPhotos (≥1, цель 5) + characteristics {name,value}[].
    - 404 ожидаемого host → пробуется соседний host (host±1) прежде чем признать провал.
  </behavior>
  <read_first>
    - .planning/phases/30-analytics/30-WAVE0-NOTES.md (актуальная карта vol→host, рабочий транспорт fetch/curl, путь к фото и характеристикам в card.json, стратегия 404-fallback)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Basket-CDN URL Construction; §Pattern 5 — реюз fetchWbDiscounts; §Common Pitfalls #3, #4; §Security Domain SSRF)
    - lib/wb-api.ts (fetchWbDiscounts line 408 — сигнатура; curl-паттерн execSync line 427)
    - tests/fixtures/analytics-card-sample.json (форма фото/характеристик)
    - lib/analytics/types.ts (SkuPayload — listingPhotos, characteristics)
  </read_first>
  <action>
    RED→GREEN. Тест `tests/analytics-wb-card-scan.test.ts`: мок транспорта (fetch или execSync — по Wave0) возвращает фикстуру card.json; кейсы: 5 фото + характеристики извлечены; cardJsonUrl бросает на невалидном nmID; 404 → fallback на соседний host. Затем в `wb-card-scan.ts`: константа `BASKET_RANGES` (актуальная карта из Wave0 + дата среза в комментарии) и `basketHostForVol(vol)`; `cardJsonUrl(nmId)` (validate `Number.isInteger(nmId) && nmId>0 && nmId<2**31` иначе throw; vol=⌊nmId/100000⌋, part=⌊nmId/1000⌋); `fetchCardJson(nmId)` — транспорт по Wave0 (fetch, при блокировке — реюз curl-подхода из wb-api.ts), при 404 пробировать host±1; `scanCardMedia(nmId): Promise<{listingPhotos: string[], characteristics: {name,value}[]}>` — извлечь первые 5 фото листинга + характеристики (options/grouped) из card.json. Рейтинг/цену здесь НЕ дёргать (первично из detail-JSON — см. Task 2).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-wb-card-scan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Тест: scanCardMedia на фикстуре → listingPhotos.length ≥ 1 (цель 5) + непустой characteristics[].
    - Тест: cardJsonUrl(-1) и cardJsonUrl(2**31) бросают исключение.
    - Тест: первый host 404 → выполнен запрос к соседнему host.
    - Новый curl-транспорт к card.wb.ru НЕ добавлен (реюз через Task 2).
  </acceptance_criteria>
  <done>scanCardMedia отдаёт 5 фото + характеристики из card.json; nmID-валидация и 404-fallback покрыты тестом.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verifyPricesBatch — ОДИН батч-вызов card.wb.ru (реюз, без Sales-API hammering)</name>
  <files>lib/analytics/wb-card-scan.ts, tests/analytics-wb-card-scan.test.ts</files>
  <behavior>
    - verifyPricesBatch(nmIds: number[]) вызывает fetchWbDiscounts ОДИН раз на весь массив (внутреннее батчирование ≤20/запрос), НЕ в цикле per-SKU.
    - При недоступности v4 — возвращает пустую/частичную Map без падения и БЕЗ per-SKU ретраев (detail-JSON — источник истины для цены/рейтинга).
    - Тест подтверждает: fetchWbDiscounts вызван ровно 1 раз для 30 nmId (spy/mock), а не 30 раз.
  </behavior>
  <read_first>
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 5; §Security Domain; CLAUDE.md «WB API rate-limit защиты» — Statistics-API 5/мин)
    - .planning/phases/30-analytics/30-CONTEXT.md (D-04 — цена/рейтинг первично из detail-JSON; card.wb.ru — для сверки)
    - lib/wb-api.ts (fetchWbDiscounts — внутреннее ≤20-батчирование + Step-2 Sales-API fallback)
    - lib/analytics/wb-card-scan.ts (cardJsonUrl из Task 1)
  </read_first>
  <action>
    RED→GREEN. Тест: замокать `fetchWbDiscounts` (vi.mock lib/wb-api), вызвать `verifyPricesBatch([...30 nmId])`, проверить, что мок вызван РОВНО 1 раз (не per-SKU) и с полным массивом; при отклонённом промисе — verifyPricesBatch не бросает, возвращает пустую Map. Затем в `wb-card-scan.ts`: `verifyPricesBatch(nmIds: number[]): Promise<Map<number, {sppDiscount?: number, rating?: number, feedbacks?: number}>>` — ОДИН вызов `fetchWbDiscounts(nmIds, undefined, storefrontOut)` (внутренне батчит ≤20/запрос — для 30 это 2 curl-батча v4), собрать СПП + storefront rating/feedbacks; обернуть в try/catch → при ошибке вернуть частичный/пустой результат (best-effort, НЕ ретраить per-SKU, НЕ инициировать доп. обращения к Sales-API). НЕ вызывать per-SKU в цикле. Пометить в комментарии: цена/рейтинг конкурентов первичны из detail-JSON, этот вызов — сверка (D-04).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-wb-card-scan.test.ts && grep -c "wb-api" lib/analytics/wb-card-scan.ts | grep -vqx 0 && ! grep -Eq "for .*fetchWbDiscounts|nmIds\.map\(.*fetchWbDiscounts" lib/analytics/wb-card-scan.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `verifyPricesBatch` вызывает `fetchWbDiscounts` ОДИН раз на весь массив nmId (тест-spy подтверждает 1 вызов, не 30); реюз lib/wb-api.ts (SPEC req.4).
    - Нет per-SKU цикла вокруг fetchWbDiscounts; при ошибке — best-effort пустой результат, без per-SKU ретраев и без дополнительного долбления Sales-API.
    - Комментарий фиксирует: цена/рейтинг первичны из detail-JSON, card.wb.ru — сверка.
  </acceptance_criteria>
  <done>verifyPricesBatch делает единственный батч-вызов card.wb.ru (реюз), исключая per-SKU-амплификацию и Sales-API rate-limit; цена/рейтинг первичны из detail-JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сервер → basket-CDN / card.wb.ru / statistics-api.wildberries.ru | произвольный nmID формирует внешний URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering / Elevation (SSRF) | cardJsonUrl(nmId) | mitigate | nmID валидируется как положительное целое < 2^31; источник — только провалидированный detail-JSON (30-04) |
| T-30-10 | Availability | устаревание карты vol→host | mitigate | карта из Wave0 с датой среза + 404-fallback на соседние host |
| T-30-16 | Denial of Service (WB Statistics-API rate-limit / IP-бан) | реюз fetchWbDiscounts | mitigate (было high) | НЕ per-SKU: ОДИН батч-вызов на все nmId (внутреннее ≤20-батчирование); цена/рейтинг первичны из detail-JSON; при сбое v4 — best-effort без per-SKU Sales-API ретраев (CLAUDE.md «WB rate-limit защиты») |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет; реюз существующего curl (lib/wb-api.ts) |
</threat_model>

<verification>
- `npx vitest run tests/analytics-wb-card-scan.test.ts` зелёный (вкл. spy: fetchWbDiscounts вызван 1 раз для 30 nmId).
- Реюз lib/wb-api.ts подтверждён grep; нет per-SKU цикла вокруг fetchWbDiscounts.
</verification>

<success_criteria>
По nmID собираются 5 фото листинга + характеристики через card.json; цена/рейтинг первичны из detail-JSON, сверка card.wb.ru — единственным батч-вызовом (без Statistics-API rate-limit риска); SSRF-валидация и fallback покрыты. ANL-04 выполнен.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-06-SUMMARY.md` — сигнатуры scanCardMedia/verifyPricesBatch (их вызывает коллектор 30-07: media per-SKU, verifyPricesBatch — ОДИН раз на все 30).
</output>
