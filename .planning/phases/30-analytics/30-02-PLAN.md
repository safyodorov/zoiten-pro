---
phase: 30-analytics
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260713_phase30_analytics/migration.sql
autonomous: true
requirements: [ANL-05, ANL-12]
must_haves:
  truths:
    - "В enum ERP_SECTION есть значение ANALYTICS"
    - "Существует таблица NicheRun с payloadJson (JSONB) и статус-машиной"
    - "prisma generate проходит — типы NicheRun/NicheRunStatus доступны коду"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "enum ERP_SECTION += ANALYTICS; model NicheRun; enum NicheRunStatus"
      contains: "model NicheRun"
    - path: "prisma/migrations/20260713_phase30_analytics/migration.sql"
      provides: "Рукописная миграция (ALTER TYPE + CREATE TABLE)"
      contains: "ALTER TYPE"
  key_links:
    - from: "prisma/schema.prisma NicheRun"
      to: "User"
      via: "createdBy relation + back-relation nicheRuns"
      pattern: "nicheRuns\\s+NicheRun"
---

<objective>
[BLOCKING] Заложить схему БД фазы: добавить значение `ERP_SECTION.ANALYTICS` (для RBAC-раздела, ANL-12) и модель `NicheRun` с иммутабельным `payloadJson` + статус-машиной (для персистентности прогонов, ANL-05). РУКОПИСНАЯ миграция по прецеденту Phase 23/25/27 — НЕ `prisma db push`. Этот план ДОЛЖЕН исполниться до любого кода, зависящего от нового enum/модели (30-07, 30-08, 30-11, 30-12, 30-13).

Purpose: Snapshot-паттерн (RESEARCH Pattern 1) требует индекс-таблицу + JSONB-колонку; RBAC-раздел требует enum-значение.
Output: обновлённый `schema.prisma` + рукописная миграция; `prisma generate` зелёный.

ПЛАНИРОВАНИЕ ТОЛЬКО: применение в проде (`npx prisma migrate deploy`) НЕ выполняется в этом цикле.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-RESEARCH.md
@.planning/phases/30-analytics/30-CONTEXT.md

<interfaces>
<!-- Прецедент модели-снапшота (mirror this) — prisma/schema.prisma:1255 -->
model WeeklyFinReportSnapshot {
  id          String   @id @default(cuid())
  weekStart   DateTime @db.Date @unique
  fixedAt     DateTime @default(now())
  fixedById   String?
  fixedBy     User?    @relation(fields: [fixedById], references: [id], onDelete: SetNull)
  payloadJson Json
}
<!-- User модель уже содержит back-relation weeklyFinReportSnapshots WeeklyFinReportSnapshot[] (line 120) -->
<!-- Прецедент enum ERP_SECTION (line 21) + миграция ALTER TYPE ADD VALUE — prisma/migrations/20260610_phase23_cash/migration.sql -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema.prisma — enum ANALYTICS + NicheRunStatus + модель NicheRun</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (enum ERP_SECTION line 21-37; model User line 88-120 — куда добавить back-relation; model WeeklyFinReportSnapshot line 1255 — образец)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 1 — draft Prisma-модели NicheRun + enum NicheRunStatus)
  </read_first>
  <action>
    В `enum ERP_SECTION` добавить значение `ANALYTICS` (после `FINANCE`). Добавить `enum NicheRunStatus { PENDING COLLECTING READY PARTIAL FAILED }`. Добавить `model NicheRun` с полями: `id String @id @default(cuid())`, `createdAt DateTime @default(now())`, `createdById String?`, `createdBy User? @relation(fields:[createdById], references:[id], onDelete: SetNull)`, `dateFrom DateTime @db.Date`, `dateTo DateTime @db.Date`, `status NicheRunStatus @default(PENDING)`, `skuCount Int @default(0)`, `progressNote String?` (для polling-прогресса «MPSTATS Y/30»), `incompleteSkus Json?` (список [{nmId, reason}] для PARTIAL, ANL-07), `errorMessage String?` (для FAILED), `payloadJson Json?` (NicheRunPayload — null пока не READY/PARTIAL), `updatedAt DateTime @updatedAt`. В `model User` добавить back-relation `nicheRuns NicheRun[]` (по образцу `weeklyFinReportSnapshots`). Индекс `@@index([status])` на NicheRun (для запроса «активный прогон» и «завис» в UI).
  </action>
  <verify>
    <automated>grep -q "ANALYTICS" prisma/schema.prisma && grep -q "model NicheRun" prisma/schema.prisma && grep -q "enum NicheRunStatus" prisma/schema.prisma && grep -q "nicheRuns\s*NicheRun" prisma/schema.prisma && npx prisma validate && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ANALYTICS prisma/schema.prisma` ≥ 1 внутри блока enum ERP_SECTION.
    - `model NicheRun` содержит поля `payloadJson Json?`, `status NicheRunStatus`, `incompleteSkus Json?`, `progressNote String?`, `dateFrom`/`dateTo @db.Date`.
    - `model User` содержит back-relation `nicheRuns NicheRun[]`.
    - `npx prisma validate` завершается без ошибок.
  </acceptance_criteria>
  <done>schema.prisma расширен enum ANALYTICS + NicheRunStatus + NicheRun (с back-relation на User); prisma validate зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: Рукописная миграция + prisma generate</name>
  <files>prisma/migrations/20260713_phase30_analytics/migration.sql</files>
  <read_first>
    - prisma/migrations/20260610_phase23_cash/migration.sql (образец: `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CASH';` + CREATE TYPE + CREATE TABLE в одной миграции)
    - prisma/migrations/20260710_weekly_finreport_snapshot/migration.sql (образец CREATE TABLE + FK → User ON DELETE SET NULL)
  </read_first>
  <action>
    Создать `prisma/migrations/20260713_phase30_analytics/migration.sql` с (в порядке): (1) `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'ANALYTICS';` (значение НЕ используется в этой же миграции — безопасно, как в phase23); (2) `CREATE TYPE "NicheRunStatus" AS ENUM ('PENDING','COLLECTING','READY','PARTIAL','FAILED');`; (3) `CREATE TABLE "NicheRun" (...)` со всеми колонками из Task 1 (id TEXT PK, createdAt/updatedAt TIMESTAMP(3), createdById TEXT null, dateFrom/dateTo DATE, status "NicheRunStatus" DEFAULT 'PENDING', skuCount INTEGER DEFAULT 0, progressNote/errorMessage TEXT null, incompleteSkus/payloadJson JSONB null); (4) `CREATE INDEX "NicheRun_status_idx" ON "NicheRun"("status");` (5) FK `NicheRun_createdById_fkey` → `User"("id") ON DELETE SET NULL ON UPDATE CASCADE`. В шапке комментарий: «Phase 30 analytics — применяется через prisma migrate deploy на VPS (execute-фаза, отдельно по сигналу)». Затем выполнить `npx prisma generate` и убедиться, что типы `NicheRun`/`NicheRunStatus` доступны.
  </action>
  <verify>
    <automated>grep -q 'ADD VALUE IF NOT EXISTS .ANALYTICS.' prisma/migrations/20260713_phase30_analytics/migration.sql && grep -q 'CREATE TABLE "NicheRun"' prisma/migrations/20260713_phase30_analytics/migration.sql && grep -q 'NicheRunStatus' prisma/migrations/20260713_phase30_analytics/migration.sql && npx prisma generate && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Файл миграции содержит `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'ANALYTICS';`, `CREATE TYPE "NicheRunStatus"`, `CREATE TABLE "NicheRun"` с колонкой `payloadJson` JSONB, FK на User `ON DELETE SET NULL`.
    - `npx prisma generate` завершается успешно; тип `NicheRun` доступен из `@prisma/client`.
    - Значение ANALYTICS НЕ используется в самой миграции (нет вставок с ним) — избегаем PG-ограничения «new enum value in same tx».
  </acceptance_criteria>
  <done>Рукописная миграция создана по прецеденту phase23/weekly-snapshot; prisma generate зелёный. Применение в проде — отдельно (не в этом цикле).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| приложение → Postgres | новая таблица NicheRun хранит payloadJson (данные конкурентов) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | enum ERP_SECTION.ANALYTICS | mitigate | значение enum — фундамент RBAC-гейтинга; реальный гейтинг в 30-13 (sections.ts/requireSection) |
| T-30-07 | Tampering | миграция ALTER TYPE в транзакции | mitigate | ANALYTICS не используется в той же миграции (PG-ограничение); IF NOT EXISTS для идемпотентности |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- `npx prisma validate` и `npx prisma generate` зелёные.
- Миграция содержит ALTER TYPE ANALYTICS + CREATE TABLE NicheRun.
</verification>

<success_criteria>
enum ANALYTICS и модель NicheRun существуют в схеме; рукописная миграция готова; клиент Prisma сгенерирован. НЕ деплоится в этом цикле.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-02-SUMMARY.md` — указать финальные имена полей NicheRun (их читают 30-07, 30-08, 30-11, 30-12).
</output>
