---
phase: 30-analytics
plan: 13
type: execute
wave: 6
depends_on: [30-08, 30-09, 30-11, 30-12, 30-02]
files_modified:
  - lib/sections.ts
  - components/layout/nav-items.ts
  - lib/section-labels.ts
autonomous: true
requirements: [ANL-12]
must_haves:
  truths:
    - "Маршрут /analytics зарегистрирован → ANALYTICS в SECTION_PATHS (edge-safe middleware gate)"
    - "Раздел появляется в навигации (nav-items) и в списке грантов пользователя (section-labels)"
    - "Каждый analytics server action / route вызывает requireSection (VIEW чтение / MANAGE запись)"
    - "Неавторизованный по разделу пользователь получает 403; SUPERADMIN и грант ANALYTICS видят раздел"
  artifacts:
    - path: "lib/sections.ts"
      provides: "SECTION_PATHS += /analytics → ANALYTICS"
      contains: "/analytics"
    - path: "components/layout/nav-items.ts"
      provides: "Пункт навигации ANALYTICS"
      contains: "ANALYTICS"
    - path: "lib/section-labels.ts"
      provides: "Русский лейбл раздела ANALYTICS в списке грантов"
      contains: "ANALYTICS"
  key_links:
    - from: "lib/sections.ts"
      to: "middleware.ts"
      via: "SECTION_PATHS читается Edge-middleware по startsWith"
      pattern: "/analytics"
---

<objective>
Включить RBAC-раздел `ANALYTICS` (ANL-12): зарегистрировать маршрут `/analytics` в `lib/sections.ts` (edge-safe карта для `middleware.ts`), добавить пункт навигации в `components/layout/nav-items.ts` и русский лейбл в `lib/section-labels.ts` (для чекбоксов грантов на `/admin/users`). Значение `ERP_SECTION.ANALYTICS` уже добавлено миграцией (30-02); гейтинг server actions/routes через `requireSection` реализован в планах 30-08/30-09/30-11/30-12. Этот план — финальное «включение» раздела (делается последним, чтобы раздел появлялся только когда весь функционал готов).

Purpose: read — просмотр дашборда/истории; write (MANAGE) — загрузка файлов / запуск сбора / токен.
Output: обновлённые sections.ts, nav-items.ts, section-labels.ts.

ПЛАНИРОВАНИЕ ТОЛЬКО: прод-смок (curl 403) — при деплое (отдельно, не в этом цикле).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-CONTEXT.md

<interfaces>
<!-- lib/sections.ts — SECTION_PATHS: Record<urlPrefix, ERP_SECTION> (edge-safe, без @prisma/client) -->
<!-- components/layout/nav-items.ts — NAV_ITEMS: {section, href, label, icon}[] + ICON_MAP -->
<!-- lib/section-labels.ts — SECTION_OPTIONS: {value, label}[] (гранты на /admin/users) -->
<!-- middleware.ts — Object.entries(SECTION_PATHS).find(([prefix]) => pathname.startsWith(prefix)) — авто-подхват -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Регистрация раздела — sections.ts + nav-items.ts + section-labels.ts</name>
  <files>lib/sections.ts, components/layout/nav-items.ts, lib/section-labels.ts</files>
  <read_first>
    - lib/sections.ts (SECTION_PATHS — куда добавить `"/analytics": "ANALYTICS"`)
    - components/layout/nav-items.ts (NAV_ITEMS + ICON_MAP — добавить пункт; иконка "BarChart3" уже импортирована и в ICON_MAP; НЕ использовать "LineChart" — занята FINANCE_MODELS)
    - lib/section-labels.ts (SECTION_OPTIONS — добавить `{value:"ANALYTICS", label:"Аналитика"}`)
    - middleware.ts (подтвердить авто-подхват SECTION_PATHS — правки middleware НЕ требуются)
  </read_first>
  <action>
    В `lib/sections.ts` добавить в `SECTION_PATHS` запись `"/analytics": "ANALYTICS"` (edge-safe, без импорта prisma). В `components/layout/nav-items.ts` добавить в `NAV_ITEMS` пункт `{ section: "ANALYTICS", href: "/analytics", label: "Аналитика", icon: "BarChart3" }` (НЕ "LineChart" — она занята FINANCE_MODELS; "BarChart3" уже присутствует в ICON_MAP). В `lib/section-labels.ts` добавить в `SECTION_OPTIONS` `{ value: "ANALYTICS", label: "Аналитика" }`. middleware.ts НЕ править (он читает SECTION_PATHS через startsWith автоматически).
  </action>
  <verify>
    <automated>grep -q '"/analytics": *"ANALYTICS"' lib/sections.ts && grep -q '"ANALYTICS"' components/layout/nav-items.ts && grep -q 'href: *"/analytics"' components/layout/nav-items.ts && grep -q 'value: *"ANALYTICS"' lib/section-labels.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "sections.ts|nav-items|section-labels" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SECTION_PATHS` содержит `"/analytics": "ANALYTICS"`; файл остаётся edge-safe (без @prisma/client).
    - `NAV_ITEMS` содержит пункт ANALYTICS href `/analytics` с валидной иконкой из ICON_MAP.
    - `SECTION_OPTIONS` содержит `{value:"ANALYTICS", label:"Аналитика"}`.
    - tsc чист по всем трём файлам.
  </acceptance_criteria>
  <done>Раздел ANALYTICS зарегистрирован в маршрутах, навигации и списке грантов; middleware подхватывает /analytics автоматически.</done>
</task>

<task type="auto">
  <name>Task 2: Аудит гейтинга — requireSection на всех analytics-эндпоинтах</name>
  <files>lib/sections.ts</files>
  <read_first>
    - app/actions/analytics.ts, app/api/analytics/upload/route.ts, app/api/analytics/runs/[id]/status/route.ts, app/api/analytics/runs/[id]/pdf/route.ts (проверить наличие requireSection)
    - app/(dashboard)/analytics/page.tsx, app/(dashboard)/analytics/upload/page.tsx, app/(dashboard)/analytics/runs/[id]/page.tsx (RSC — requireSection VIEW)
    - .planning/phases/30-analytics/30-SPEC.md (req.12 acceptance — 403 без гранта; запуск требует MANAGE)
  </read_first>
  <action>
    Провести аудит: подтвердить, что КАЖДЫЙ analytics-эндпоинт вызывает `requireSection("ANALYTICS", …)` — read-пути (VIEW): RSC-страницы + status/pdf/upload GET; write-пути (MANAGE): startNicheRun / saveMpstatsToken / пометка FAILED. Если какой-то путь не гейтится — исправить в соответствующем файле (это единственная правка вне трёх файлов Task 1; допускается точечно). Зафиксировать в SUMMARY карту путь→минимальная роль.
  </action>
  <verify>
    <automated>grep -Rq 'requireSection("ANALYTICS"' app/actions/analytics.ts && grep -Rlq 'requireSection' app/api/analytics && for p in "app/(dashboard)/analytics/page.tsx" "app/(dashboard)/analytics/upload/page.tsx" "app/(dashboard)/analytics/runs/[id]/page.tsx"; do grep -q "requireSection" "$p" || { echo "UNGATED $p"; exit 1; }; done && grep -Rq 'requireSection("ANALYTICS", *"MANAGE")' app/actions/analytics.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Каждая analytics RSC-страница и каждый route/action вызывает `requireSection("ANALYTICS", …)`.
    - Write-операции (startNicheRun, saveMpstatsToken, пометка FAILED) требуют `MANAGE`; read — `VIEW`.
    - Нет ungated analytics-путей (grep-аудит зелёный).
  </acceptance_criteria>
  <human-check>
    При деплое (отдельно, не в этом цикле): curl `/analytics` без гранта → 403; логин SUPERADMIN и пользователь с грантом ANALYTICS → раздел виден; запуск сбора без MANAGE → отказ (R12 manual smoke, VALIDATION.md / DEPLOY.md).
  </human-check>
  <done>Все analytics-пути гейтятся requireSection (VIEW чтение / MANAGE запись); карта путь→роль зафиксирована.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| анонимный/невалидный пользователь → /analytics | Edge middleware + server-action RBAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | доступ к /analytics | mitigate | SECTION_PATHS → middleware Edge-guard + requireSection("ANALYTICS", VIEW/MANAGE) на каждом action/route (двухслойный гейтинг проекта) |
| T-30-15 | Elevation of Privilege | write без MANAGE | mitigate | запуск сбора/токен/пометка FAILED требуют MANAGE; аудит-греп в Task 2 |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- /analytics в SECTION_PATHS + nav-items + section-labels; tsc чист.
- Аудит: все analytics-пути вызывают requireSection; write → MANAGE.
- Прод-смок 403 — при деплое (не в этом цикле).
</verification>

<success_criteria>
Раздел ANALYTICS включён в RBAC/навигацию; /analytics гейтится middleware + requireSection; read=VIEW, write=MANAGE; SUPERADMIN и грант ANALYTICS видят раздел. ANL-12 выполнен.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-13-SUMMARY.md` — карта путь→минимальная роль + напоминание о прод-смоке 403 при будущем деплое.
</output>
