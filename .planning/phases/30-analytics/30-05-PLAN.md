---
phase: 30-analytics
plan: 05
type: tdd
wave: 2
depends_on: [30-03, 30-01]
files_modified:
  - lib/analytics/mpstats.ts
  - tests/analytics-mpstats.test.ts
autonomous: true
requirements: [ANL-03]
must_haves:
  truths:
    - "Клиент шлёт заголовок X-Mpstats-TOKEN, окно дат = период файлов (d1/d2)"
    - "Позиции по дням разделены на organic и ad (с CPM/типом/бустом)"
    - "Запросы с частотностью ≤ 500 отфильтрованы"
    - "Лимит-ошибка (429/исчерпание тарифа) обрабатывается без падения (MpstatsRateLimitError)"
  artifacts:
    - path: "lib/analytics/mpstats.ts"
      provides: "MPSTATS-клиент: позиции per SKU + список запросов ниши с частотностью"
      exports: ["fetchPositions", "fetchNicheQueries", "MpstatsRateLimitError"]
    - path: "tests/analytics-mpstats.test.ts"
      provides: "Контрактный тест на мок-ответах (форма из Wave 0)"
  key_links:
    - from: "lib/analytics/mpstats.ts"
      to: "MPSTATS API"
      via: "fetch с X-Mpstats-TOKEN по путям из 30-WAVE0-NOTES.md"
      pattern: "X-Mpstats-TOKEN"
---

<objective>
Реализовать MPSTATS-клиент (ANL-03) по ПОДТВЕРЖДЁННЫМ в Wave 0 путям: по каждому SKU — дневной ряд позиций, разделённый на органические и рекламные (с параметрами рекламы CPM/тип размещения/позиция буста), окно дат = период `byDay` файлов; список запросов ниши по SKU с фильтром частотности > 500. Устойчивость к лимитам (D-03): 429/исчерпание тарифа → `MpstatsRateLimitError`, без падения всего прогона.

Purpose: Токен не покидает сервер; клиент DI-friendly для контрактных тестов на мок-ответах.
Output: `lib/analytics/mpstats.ts`, `tests/analytics-mpstats.test.ts`.

ЗАВИСИТ ОТ 30-01: пути эндпоинтов и форма ответа берутся из `30-WAVE0-NOTES.md`. НЕ писать пути «по догадке» — использовать подтверждённые (антипаттерн Phase 07 PROMO_API).

**Wave 0 gating:** RESEARCH §Open Questions #1 (точные MPSTATS-эндпоинты позиций/запросов) и §Assumptions Log A1–A2 — это LOW-confidence unknowns, РАЗРЕШАЮТСЯ в Wave 0 (план 30-01). Любые пути/поля, упомянутые в RESEARCH/этом плане до Wave 0, — ПРЕДПОЛОЖЕНИЯ; финальные значения — только из `30-WAVE0-NOTES.md` (маркеры `CONFIRMED:`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-WAVE0-NOTES.md
@.planning/phases/30-analytics/30-RESEARCH.md
@lib/analytics/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mpstats.ts — клиент позиций/запросов + обработка лимита + контрактный тест</name>
  <files>lib/analytics/mpstats.ts, tests/analytics-mpstats.test.ts</files>
  <behavior>
    - fetchPositions(nmId, d1, d2, token): по мок-ответу возвращает PositionDay[] с разделением organic/ad; ad содержит cpm/placementType/boostPosition.
    - fetchNicheQueries(nmId, token): возвращает QueryPositionSeries[]; запросы с frequency ≤ 500 отфильтрованы.
    - При status 429 → выброшен MpstatsRateLimitError (не generic Error, не падение процесса).
    - Все запросы уходят с заголовком X-Mpstats-TOKEN и параметрами d1/d2 (окно = период файлов).
    - Пагинация запросов внутри SKU обрабатывается (несколько страниц склеиваются).
  </behavior>
  <read_first>
    - .planning/phases/30-analytics/30-WAVE0-NOTES.md (ПОДТВЕРЖДЁННЫЕ пути + реальная форма ответа — источник истины)
    - .planning/phases/30-analytics/30-RESEARCH.md (§MPSTATS Client Skeleton — mpstatsFetch, MpstatsRateLimitError; §Common Pitfalls #1)
    - .planning/phases/30-analytics/30-SPEC.md (req.3 acceptance)
    - lib/analytics/types.ts (PositionDay, QueryPositionSeries)
  </read_first>
  <action>
    RED→GREEN. Тест `tests/analytics-mpstats.test.ts` мокает глобальный `fetch` (vi.stubGlobal), отдаёт зафиксированные в Wave0 формы ответа; кейсы: разделение organic/ad, фильтр частотности >500, 429→MpstatsRateLimitError, наличие заголовка X-Mpstats-TOKEN и d1/d2. Затем в `mpstats.ts`: `MPSTATS_BASE` из Wave0; `mpstatsFetch<T>(path, token)` (заголовок X-Mpstats-TOKEN; status 429 → throw MpstatsRateLimitError; !ok → Error с телом); `fetchPositions(nmId, d1, d2, token): Promise<PositionDay[]>` (маппинг реального ответа на organic/ad + CPM/тип/буст, дни отсутствия → organic:null/ad:null); `fetchNicheQueries(nmId, token): Promise<QueryPositionSeries[]>` (пагинация; фильтр `frequency > 500`); `class MpstatsRateLimitError extends Error`. Токен принимается параметром (не читает БД — DI). Прогонять до зелёного.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-mpstats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Тест: мок-ответ → PositionDay[] с organic и ad (ad с cpm/placementType/boostPosition).
    - Тест: запрос с frequency 400 отфильтрован, с frequency 600 — оставлен.
    - Тест: fetch со status 429 → выброшен `MpstatsRateLimitError`.
    - `grep -c "X-Mpstats-TOKEN" lib/analytics/mpstats.ts` ≥ 1; клиент принимает token параметром (не импортирует prisma).
  </acceptance_criteria>
  <done>MPSTATS-клиент реализован по подтверждённым путям; позиции organic/ad, фильтр частотности, лимит-ошибка — покрыты контрактным тестом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сервер → MPSTATS API | токен пользователя + nmID пересекают границу |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Denial of Service (лимит тарифа) | fetchPositions/fetchNicheQueries | mitigate | 429 → MpstatsRateLimitError без падения; коллектор (30-07) ловит и деградирует по правилу полноты |
| T-30-01 | Information Disclosure | MPSTATS-токен | mitigate | токен только параметр функции, не логируется; клиент серверный |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- `npx vitest run tests/analytics-mpstats.test.ts` зелёный.
- `npm test` не ломает существующие файлы.
</verification>

<success_criteria>
Клиент MPSTATS отдаёт дневные позиции organic/ad и запросы с частотностью >500 по подтверждённым путям; окно дат = период файлов; лимит-ошибка не роняет прогон. ANL-03 покрыт.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-05-SUMMARY.md` — сигнатуры fetchPositions/fetchNicheQueries (их вызывает коллектор 30-07).
</output>
