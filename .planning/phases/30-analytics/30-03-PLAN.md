---
phase: 30-analytics
plan: 03
type: tdd
wave: 1
depends_on: [30-01]
files_modified:
  - lib/analytics/types.ts
  - lib/analytics/engine.ts
  - tests/analytics-engine.test.ts
autonomous: true
requirements: [ANL-02, ANL-06, ANL-07, ANL-10]
must_haves:
  truths:
    - "aggregateFunnel: объёмы (показы/заказы/сумма) = месячное значение ÷ 30 (КОНСТАНТА 30, не число дней n)"
    - "aggregateFunnel считает конверсии/выкуп «от сумм» (Σ/Σ по byDay); цена = средняя за период × 0.97"
    - "Конверсия клик→заказ «от сумм» совпадает с произведением клик→корзина × корзина→заказ"
    - "evaluateCompleteness: сбой в топ-10 по выручке → FAILED; сбой в 11–30 → PARTIAL"
    - "sortSkus даёт одинаковый порядок по выручке / по конв. клик→заказ (единый источник сортировки)"
    - "averagePositionByQuery игнорирует дни-прочерки (не входят в среднее, не штрафуют)"
  artifacts:
    - path: "lib/analytics/types.ts"
      provides: "Контракты payload для всех downstream-планов (data/mpstats/collector/snapshot/UI/pdf)"
      contains: "NicheRunPayload"
    - path: "lib/analytics/engine.ts"
      provides: "Чистый движок метрик/сортировки/полноты/средней позиции"
      exports: ["aggregateFunnel", "sortSkus", "evaluateCompleteness", "averagePositionByQuery"]
    - path: "tests/analytics-engine.test.ts"
      provides: "Golden-тесты правил ТЗ §3 + правила полноты (фикстура n≠30)"
  key_links:
    - from: "lib/analytics/engine.ts"
      to: "lib/analytics/types.ts"
      via: "import типов (без импорта prisma/сети)"
      pattern: "from \"\\./types\""
---

<objective>
Определить КОНТРАКТЫ (`types.ts`) и чистый ДВИЖОК (`engine.ts`) фазы. Interface-first: `types.ts` — единый источник форм payload, который потребляют все downstream-планы (парсер, клиенты, коллектор, снапшот, вкладки UI, PDF). Движок реализует правила ТЗ §3: **объёмы (показы/заказы/сумма) = месячное значение ÷ 30 (константа 30)**, конверсии/выкуп «от сумм» (Σ/Σ), цена −3%; единую сортировку (ANL-06); правило полноты по рангу выручки (ANL-07); среднюю позицию с игнором прочерков (ANL-10). Всё — pure-функции без Prisma/fetch, покрытые golden-тестами (паттерн `lib/finance-cashflow/engine.ts`).

Purpose: Убрать «scavenger hunt» — downstream получает контракты в коде, а не исследует кодовую базу.
Output: `lib/analytics/types.ts`, `lib/analytics/engine.ts`, `tests/analytics-engine.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-RESEARCH.md
@.planning/phases/30-analytics/30-WAVE0-NOTES.md
@tests/fixtures/analytics-detail-sample.json

<!-- Паттерн pure-движка + golden-тестов -->
@lib/sales-plan/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — контракты payload фазы (вкл. месячные тоталы)</name>
  <files>lib/analytics/types.ts</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.2 — «показы/заказы/сумма = месяц ÷ 30»; ТЗ §3)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Recommended Project Structure; §Pattern 1 — NicheRunPayload draft; §Code Examples — FunnelDayRaw/FunnelAggregate)
    - .planning/phases/30-analytics/30-WAVE0-NOTES.md (реальные имена полей MPSTATS-позиций и card.json — чтобы типы отражали реальность)
    - tests/fixtures/analytics-detail-sample.json (реальные имена полей byDay/byMonth/commonParams)
  </read_first>
  <action>
    Определить и экспортировать типы (без реализации): `FunnelDayRaw` (nmId, dt, viewCount, openCard, addToCart, orders, ordersSum, buyoutCount, medianPrice — имена сверить с фикстурой/Wave0); `FunnelMonthTotals` (viewCount, orders, ordersSum — МЕСЯЧНЫЕ тоталы, источник объёмов «÷30»; из `data.salesFunnel.byMonth`); `FunnelAggregate` (viewsPerDay, ordersPerDay, ordersSumPerDay, ctr, clickToCart, cartToOrder, clickToOrder, buyoutPct, medianPriceWallet); `PositionDay` (dt, organic: number|null, ad: {position, cpm, placementType, boostPosition}|null); `QueryPositionSeries` (query, frequency, days: PositionDay[], avgPosition: number|null); `SkuPayload` (nmId, brand, seller, subject, rating, feedbacksCount, mainPhoto, listingPhotos: string[5], characteristics: {name,value}[], funnel: FunnelAggregate, funnelDays: FunnelDayRaw[], priceDays: {dt,value}[], queries: QueryPositionSeries[], revenue: number, complete: boolean, incompleteReasons?: string[]); `NicheRunPayload` (version: 1, dateFrom, dateTo, skus: SkuPayload[]); `SortMode = "revenue" | "clickToOrder"`; `SkuCompletenessInput` и `CompletenessResult` (status: "OK"|"PARTIAL"|"FAILED", failedInTop10: number[], failedIn11to30: number[]); константы `NICHE_RUN_SNAPSHOT_VERSION = 1` и `DAYS_IN_MONTH = 30` (делитель объёмов по ТЗ §3). Метрики для вкладки «Статистика карточки» — union строк `MetricKey` (views, ctr, clickToCart, clickToOrder, orders, ordersSum, buyout, price — без позиций).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "analytics/engine\|analytics/data\|analytics/mpstats\|analytics/wb-card-scan\|analytics/collector\|analytics/snapshot\|analytics/pdf" | grep -c "error TS" | grep -qx 0 && grep -q "NicheRunPayload" lib/analytics/types.ts && grep -q "FunnelMonthTotals" lib/analytics/types.ts && grep -q "DAYS_IN_MONTH" lib/analytics/types.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Экспортированы все типы: FunnelDayRaw, FunnelMonthTotals, FunnelAggregate, PositionDay, QueryPositionSeries, SkuPayload, NicheRunPayload, SortMode, MetricKey, CompletenessResult, SkuCompletenessInput.
    - Экспортированы константы `NICHE_RUN_SNAPSHOT_VERSION = 1` и `DAYS_IN_MONTH = 30`.
    - Файл НЕ импортирует prisma/сеть (чистые типы). `SkuPayload` содержит поля для ВСЕХ 5 вкладок (общая, листинг, характеристики, статистика карточки, статистика запросов).
  </acceptance_criteria>
  <done>types.ts экспортирует полный набор контрактов payload (вкл. FunnelMonthTotals + DAYS_IN_MONTH); downstream-планы могут импортировать без исследования кодовой базы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engine.ts — движок метрик/сортировки/полноты + golden-тесты (n≠30)</name>
  <files>lib/analytics/engine.ts, tests/analytics-engine.test.ts</files>
  <behavior>
    - aggregateFunnel(days, monthly): viewsPerDay == (monthly.viewCount) / 30 (КОНСТАНТА 30); ordersPerDay == monthly.orders/30; ordersSumPerDay == monthly.ordersSum/30. Если monthly не передан → месячное значение = Σ(byDay), затем ÷ 30 (тоже константа 30, НЕ ÷ n).
    - Golden-фикстура использует n=28 дней: тест ПАДАЕТ, если делить на n (28), и ПРОХОДИТ при делении на 30 (проверяет именно константу).
    - Конверсии/выкуп «от сумм» (Σ/Σ по byDay), при разных объёмах по дням ≠ среднему дневных процентов (тест ловит «от сумм»); clickToOrder == clickToCart × cartToOrder (epsilon 1e-9).
    - medianPriceWallet == avg(medianPrice за период) × 0.97 (цена — СРЕДНЯЯ за период, НЕ ÷30).
    - evaluateCompleteness: сбой у SKU ранга ≤10 по выручке → FAILED (+ nmId в failedInTop10); ранг 11–30 → PARTIAL (+ failedIn11to30); всё собрано → OK.
    - sortSkus("revenue") по убыванию revenue; sortSkus("clickToOrder") по убыванию funnel.clickToOrder; тай-брейк по nmId.
    - averagePositionByQuery: дни organic===null исключаются; все null → null (нет штрафа).
  </behavior>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.2 «месяц ÷ 30» + «от сумм»; req.6; req.7; req.10)
    - .planning/phases/30-analytics/30-TZ-source.md (§3 Правила расчёта — «месячное значение ÷ 30»; «медианная цена — средняя за период»)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Code Examples — aggregateFunnel/evaluateCompleteness наброски; §Common Pitfalls #6 «от сумм»). ВНИМАНИЕ: набросок в RESEARCH делит объёмы на `n` — это НЕВЕРНО, использовать константу 30 (SPEC req.2), не копировать `/ n` для объёмов.
    - lib/analytics/types.ts (контракты Task 1: FunnelMonthTotals, DAYS_IN_MONTH)
    - lib/sales-plan/engine.ts (паттерн DI-движка); tests/pricing-math.test.ts (паттерн golden-теста)
  </read_first>
  <action>
    RED→GREEN→REFACTOR. Сначала `tests/analytics-engine.test.ts` с golden-фикстурами: (1) фикстура с n=28 днями и известным monthly-тоталом — проверить, что viewsPerDay == monthly.viewCount/30 (тест должен упасть при делении на 28); (2) день с большим объёмом смещает конверсию — ловит «от сумм» vs «среднее процентов»; (3) clickToOrder == clickToCart×cartToOrder; (4) цена ×0.97. Затем реализовать в `engine.ts` (импорт типов + DAYS_IN_MONTH из `./types`, БЕЗ prisma/fetch): `aggregateFunnel(days: FunnelDayRaw[], monthly?: FunnelMonthTotals): FunnelAggregate` — объёмы: `mViews = monthly?.viewCount ?? Σ(days.viewCount)`, `viewsPerDay = mViews / DAYS_IN_MONTH`; аналогично orders/ordersSum (делитель ВСЕГДА DAYS_IN_MONTH=30, НЕ days.length); конверсии/выкуп «от сумм» по byDay (CTR=Σopen/Σview, clickToCart=Σcart/Σopen, cartToOrder=Σorders/Σcart, clickToOrder=Σorders/Σopen, buyoutPct=Σbuyout/Σorders; защита от деления на 0 → 0); `medianPriceWallet = avg(days.medianPrice) × 0.97` (средняя за период — Σ/n дней, это цена, НЕ объём); `sortSkus(skus, mode: SortMode)` (стабильная сортировка, тай-брейк nmId); `evaluateCompleteness(skus)` (ВСЕГДА ранжировать по revenue desc; провал в топ-10 → FAILED, 11–30 → PARTIAL); `averagePositionByQuery(days: PositionDay[]): number|null` (среднее organic только по дням присутствия). Прогонять тест до зелёного.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/analytics-engine.test.ts` — все тесты зелёные.
    - Тест на фикстуре n=28 подтверждает деление объёмов на КОНСТАНТУ 30, а не на n (при подмене делителя на days.length тест падает).
    - Тест явно проверяет: clickToOrder «от сумм» == clickToCart×cartToOrder; цена ×0.97; FAILED при сбое в топ-10; PARTIAL при сбое в 11–30; average игнорирует прочерки.
    - `engine.ts` не импортирует `@/lib/prisma` и не делает fetch; объёмы делятся на DAYS_IN_MONTH, не на days.length.
  </acceptance_criteria>
  <done>Движок реализован pure-функциями: объёмы = месяц÷30 (константа), конверсии/выкуп «от сумм», цена ×0.97, сортировка/полнота/средняя позиция; golden-тест с n=28 фиксирует деление на 30.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| нет внешних границ | чистые функции, детерминированы, без сети/БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering (integrity) | расчёт конверсий и объёмов | mitigate | golden-тест фиксирует «от сумм» (Pitfall #6) И деление объёмов на константу 30 (фикстура n=28) — исключает две регрессии сразу |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- `npx vitest run tests/analytics-engine.test.ts` зелёный (вкл. фикстуру n=28 для деления на 30).
- `npm test` не ломает существующие 100+ файлов.
- engine.ts без prisma/fetch; объёмы ÷ DAYS_IN_MONTH.
</verification>

<success_criteria>
Контракты payload определены; движок метрик/сортировки/полноты/средней позиции реализован и покрыт golden-тестами; правила ТЗ §3 (месяц÷30, «от сумм», цена ×0.97) falsifiable-проверены.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-03-SUMMARY.md` — перечислить экспортируемые типы SkuPayload/NicheRunPayload/FunnelMonthTotals + сигнатуру aggregateFunnel(days, monthly) (их импортируют 30-04..30-12).
</output>
