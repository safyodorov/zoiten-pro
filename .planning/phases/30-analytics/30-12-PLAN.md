---
phase: 30-analytics
plan: 12
type: tdd
wave: 4
depends_on: [30-03, 30-07, 30-02]
files_modified:
  - lib/analytics/pdf.ts
  - app/api/analytics/runs/[id]/pdf/route.ts
  - tests/analytics-pdf.test.ts
autonomous: true
requirements: [ANL-11]
must_haves:
  truths:
    - "PDF генерируется без ошибки для сохранённого прогона"
    - "Содержит сводную таблицу (30 строк: артикул, бренд, выручка/мес, конв.клик→заказ)"
    - "По-SKU блоки: артикул+бренд, выручка/мес, конв.клик→заказ, 5 фото листинга, график цены, график конверсий"
    - "Порядок SKU в PDF = текущая сортировка на экране"
  artifacts:
    - path: "lib/analytics/pdf.ts"
      provides: "renderNicheRunPdf(payload, sortMode) — pdfkit, линии из series"
      exports: ["renderNicheRunPdf"]
    - path: "app/api/analytics/runs/[id]/pdf/route.ts"
      provides: "GET — стрим PDF (VIEW), порядок из query sort"
      exports: ["GET"]
    - path: "tests/analytics-pdf.test.ts"
      provides: "Integration: реальный рендер в Buffer, PDF magic bytes + ненулевой размер"
  key_links:
    - from: "app/api/analytics/runs/[id]/pdf/route.ts"
      to: "lib/analytics/pdf.ts renderNicheRunPdf"
      via: "стрим Buffer в ответ"
      pattern: "renderNicheRunPdf"
---

<objective>
Реализовать PDF-выгрузку (ANL-11): `pdf.ts:renderNicheRunPdf(payload, sortMode)` строит PDF через `pdfkit` (уже в проекте) — (a) сводная таблица 30 строк (артикул, бренд, суммарная выручка/мес, конв.клик→заказ = клик→корзина × корзина→заказ) + (b) по-SKU блоки (артикул+бренд, выручка/мес, конв.клик→заказ, 5 фото листинга, график цены, график конверсий в корзину/заказ). Графики рисуются напрямую примитивами `moveTo/lineTo/stroke` из series (RESEARCH Pattern 7, БЕЗ headless-браузера/recharts на сервере). Кириллица через регистрацию `DejaVuSans.ttf` с graceful fallback. Фото сжимаются `compressToBudget` (sharp). Порядок = `sortSkus(payload.skus, sortMode)`.

Purpose: Отчуждаемый отчёт из того же снапшота; порядок совпадает с экраном.
Output: `lib/analytics/pdf.ts`, pdf-route, `tests/analytics-pdf.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-RESEARCH.md
@lib/analytics/types.ts

<!-- Прецедент pdfkit + Cyrillic-шрифт + compressToBudget -->
@app/api/procurement/inspection/report-generate/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pdf.ts — renderNicheRunPdf (сводная таблица + по-SKU блоки + графики) + тест</name>
  <files>lib/analytics/pdf.ts, tests/analytics-pdf.test.ts</files>
  <behavior>
    - renderNicheRunPdf(payload, "revenue") → Buffer, начинается с PDF magic bytes (%PDF), ненулевой размер.
    - Порядок блоков соответствует sortSkus(payload.skus, sortMode).
    - drawLineChart с series.length<2 не падает.
  </behavior>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.11 acceptance — точный состав сводной таблицы и по-SKU блока)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 7 — drawLineChart moveTo/lineTo/stroke; fill+stroke gotcha; §Common Pitfalls #5 Cyrillic)
    - app/api/procurement/inspection/report-generate/route.ts (регистрация DejaVuSans FONT_REG/FONT_BOLD + existsSync fallback; compressToBudget через sharp; new PDFDocument)
    - lib/analytics/types.ts (NicheRunPayload, SkuPayload — funnel, priceDays, listingPhotos); lib/analytics/engine.ts (sortSkus)
  </read_first>
  <action>
    RED→GREEN. Тест `tests/analytics-pdf.test.ts` (integration): собрать синтетический `NicheRunPayload` (2-3 SKU достаточно), вызвать `renderNicheRunPdf` → проверить Buffer начинается с `%PDF` и `.length > 0`; проверить, что порядок SKU соответствует sortSkus. Затем `pdf.ts`: `renderNicheRunPdf(payload: NicheRunPayload, sortMode: SortMode): Promise<Buffer>` — `new PDFDocument`, регистрация DejaVuSans (существующий путь + existsSync fallback на Helvetica, паттерн report-generate), собрать Buffer из потока; `sortSkus` для порядка; секция (a) сводная таблица 30 строк (артикул, бренд, выручка/мес, конв.клик→заказ); секция (b) по каждому SKU блок: артикул+бренд, выручка/мес, конв.клик→заказ, до 5 фото листинга (скачать+`compressToBudget` под байт-бюджет, sharp), `drawLineChart` цены (priceDays) и конверсий (в корзину/в заказ по дням); helper `drawLineChart(doc, series, box)` через `moveTo/lineTo/stroke` (учесть fill+stroke gotcha — RESEARCH). Не использовать recharts/headless.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-pdf.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Тест: renderNicheRunPdf возвращает Buffer, начинается с `%PDF`, размер > 0.
    - Тест: порядок SKU в выводе соответствует sortSkus(sortMode).
    - `pdf.ts` регистрирует DejaVu-шрифт с fallback; рисует графики через moveTo/lineTo/stroke; не импортирует recharts/puppeteer (`grep -c "recharts\|puppeteer" lib/analytics/pdf.ts` == 0).
  </acceptance_criteria>
  <done>renderNicheRunPdf строит сводную таблицу + по-SKU блоки с 5 фото и графиками через pdfkit; порядок = сортировка; кириллица через DejaVu; тест magic-bytes зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/analytics/runs/[id]/pdf — стрим PDF (VIEW), порядок из query</name>
  <files>app/api/analytics/runs/[id]/pdf/route.ts</files>
  <read_first>
    - lib/analytics/pdf.ts (renderNicheRunPdf из Task 1); lib/analytics/snapshot.ts (parseNicheRunPayload)
    - lib/rbac.ts (requireSection); app/api/procurement/inspection/report-generate/route.ts (образец стрима PDF-ответа с заголовками)
  </read_first>
  <action>
    `GET(req, {params})`: `requireSection("ANALYTICS","VIEW")` (try/catch → 401/403); `prisma.nicheRun.findUnique` по id; `parseNicheRunPayload(payloadJson)` (если null — 404/409 «прогон не готов»); `sortMode` из query (`?sort=revenue|clickToOrder`, дефолт revenue — совпадает с экраном); `renderNicheRunPdf(payload, sortMode)`; вернуть с `Content-Type: application/pdf` + `Content-Disposition: attachment; filename="niche-run-<id>.pdf"`.
  </action>
  <verify>
    <automated>test -f "app/api/analytics/runs/[id]/pdf/route.ts" && grep -q "renderNicheRunPdf" "app/api/analytics/runs/[id]/pdf/route.ts" && grep -q "requireSection" "app/api/analytics/runs/[id]/pdf/route.ts" && grep -q "application/pdf" "app/api/analytics/runs/[id]/pdf/route.ts" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "pdf/route" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GET гейтится requireSection VIEW; читает снапшот; при не-готовом прогоне — 404/409.
    - Отдаёт `application/pdf` с attachment-именем; порядок берётся из query `sort` (revenue|clickToOrder), дефолт revenue — pdf-route ОБЯЗАН сортировать соответственно (req.6/req.11).
    - Приёмка «порядок PDF = экранная сортировка» обеспечивается тем, что кнопка «Скачать PDF» в дашборде (30-11) передаёт активный `?sort=` в этот эндпоинт.
    - tsc чист по файлу.
  </acceptance_criteria>
  <done>pdf-route стримит PDF из снапшота с порядком из query; гейтится VIEW.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сервер → basket-CDN (фото для PDF) | скачивание изображений по URL из снапшота |
| клиент → сервер (запрос PDF) | runId + sort пересекают границу |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | pdf-route | mitigate | requireSection("ANALYTICS","VIEW") до рендера |
| T-30-14 | Denial of Service | скачивание/сжатие фото в PDF | mitigate | compressToBudget (sharp) ограничивает байт-бюджет; фото-URL — из провалидированного снапшота |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | pdfkit/sharp уже в package.json; новых пакетов нет |
</threat_model>

<verification>
- `npx vitest run tests/analytics-pdf.test.ts` зелёный (PDF magic bytes + порядок).
- pdf-route отдаёт application/pdf, гейтится VIEW; tsc чист.
</verification>

<success_criteria>
PDF генерируется из снапшота: сводная таблица 30 строк + по-SKU блоки (5 фото + график цены + графики конверсий); порядок = сортировка; кириллица корректна. ANL-11 выполнен.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-12-SUMMARY.md` — путь pdf-эндпоинта и параметр sort (для кнопки выгрузки в UI 30-11/30-09).
</output>
