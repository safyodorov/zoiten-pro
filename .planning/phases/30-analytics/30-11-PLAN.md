---
phase: 30-analytics
plan: 11
type: execute
wave: 4
depends_on: [30-10, 30-07, 30-02]
files_modified:
  - app/(dashboard)/analytics/runs/[id]/page.tsx
  - components/analytics/SortToggle.tsx
  - components/analytics/PdfExportButton.tsx
  - components/analytics/tabs/OverviewTab.tsx
  - components/analytics/tabs/ListingTab.tsx
  - components/analytics/tabs/CharacteristicsTab.tsx
autonomous: true
requirements: [ANL-06, ANL-08]
must_haves:
  truths:
    - "RSC-страница читает NicheRun.payloadJson и рендерит полноэкранный дашборд из 5 вкладок"
    - "«Общая информация» открыта по умолчанию; во всех вкладках видны все 30 SKU"
    - "Переключатель сортировки (выручка / конв. клик→заказ) применяется одинаково во всех 5 вкладках"
    - "Кнопка «Скачать PDF» в дашборде передаёт активный ?sort= → порядок PDF совпадает с экраном (req.6/req.11)"
    - "Каждая вкладка содержит поля из ТЗ §4"
  artifacts:
    - path: "app/(dashboard)/analytics/runs/[id]/page.tsx"
      provides: "RSC-контейнер дашборда: чтение снапшота + навигация по 5 вкладкам"
      min_lines: 40
    - path: "components/analytics/SortToggle.tsx"
      provides: "Единый переключатель сортировки (URL searchParams)"
    - path: "components/analytics/PdfExportButton.tsx"
      provides: "Кнопка «Скачать PDF» с href, включающим активный ?sort="
    - path: "components/analytics/tabs/OverviewTab.tsx"
      provides: "Вкладка «Общая информация» (по умолчанию)"
  key_links:
    - from: "app/(dashboard)/analytics/runs/[id]/page.tsx"
      to: "lib/analytics/snapshot.ts parseNicheRunPayload"
      via: "чтение NicheRun.payloadJson"
      pattern: "parseNicheRunPayload"
    - from: "components/analytics/PdfExportButton.tsx"
      to: "/api/analytics/runs/[id]/pdf"
      via: "href с активным ?sort= из URL searchParams"
      pattern: "pdf\\?sort="
---

<objective>
Собрать полноэкранный дашборд (ANL-08): RSC-страница `runs/[id]/page.tsx` читает `NicheRun.payloadJson` (через `parseNicheRunPayload`), применяет сортировку и передаёт `SkuPayload[]` в 5 вкладок. Три «простые» вкладки (Общая/Листинг/Характеристики) реализуются здесь; две «сложные» (CardStats/QueryStats) импортируются из 30-10. Единая сортировка (ANL-06) — `SortToggle` через URL searchParams (паттерн PlanFactControls), применяется идентично во всех вкладках. Кнопка «Скачать PDF» (`PdfExportButton`) читает АКТИВНЫЙ `?sort=` и строит ссылку на pdf-route с тем же порядком — закрывает приёмку req.6/req.11 «порядок PDF = выбранная на экране сортировка». «Общая информация» — по умолчанию; все 30 строк видны; sticky-таблицы БЕЗ opacity-бага.

Purpose: Финальная сборка дашборда + единая сортировка, распространяющаяся и на PDF.
Output: RSC-страница + SortToggle + PdfExportButton + 3 вкладки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-TZ-source.md
@lib/analytics/types.ts

<interfaces>
<!-- Импортируются из 30-10 (уже существуют к этой волне) -->
components/analytics/tabs/CardStatsTab.tsx  → CardStatsTab({ skus: SkuPayload[] })
components/analytics/tabs/QueryStatsTab.tsx → QueryStatsTab({ skus: SkuPayload[] })
<!-- lib/analytics/engine.ts — sortSkus(skus, mode: SortMode) из 30-03 -->
<!-- lib/analytics/snapshot.ts — parseNicheRunPayload(json) из 30-07 -->
<!-- pdf-route (30-12, та же волна): GET /api/analytics/runs/{id}/pdf?sort=revenue|clickToOrder — PdfExportButton строит href-строку (без импорта) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSC-страница runs/[id] + SortToggle + PdfExportButton (единая сортировка → PDF)</name>
  <files>app/(dashboard)/analytics/runs/[id]/page.tsx, components/analytics/SortToggle.tsx, components/analytics/PdfExportButton.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.6 сортировка на всех вкладках И в PDF; req.8 5 вкладок, «Общая» по умолч.; req.11 порядок PDF = сортировка)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 6 — sticky-table + URL searchParams; CLAUDE.md §471 sticky)
    - components/sales-plan/PlanFactControls.tsx (URL searchParams, native select/сегм.кнопки — НЕ base-ui Select)
    - lib/analytics/snapshot.ts (parseNicheRunPayload); lib/analytics/engine.ts (sortSkus, SortMode)
    - lib/rbac.ts (getSectionRole/requireSection для RSC)
  </read_first>
  <action>
    RSC `page.tsx` (`app/(dashboard)/analytics/runs/[id]/page.tsx`): читает `searchParams` (tab, sort, metrics); `requireSection("ANALYTICS","VIEW")`; `prisma.nicheRun.findUnique` по id; `parseNicheRunPayload(payloadJson)` (если null/не READY|PARTIAL — показать статус/ошибку); применить `sortSkus(payload.skus, sort ?? "revenue")` ОДИН раз и передать отсортированный массив во ВСЕ вкладки (единый порядок, req.6); при статусе PARTIAL — показать пометку неполноты (`incompleteSkus`). Навигация по 5 вкладкам (Общая[default]/Листинг/Характеристики/Статистика карточки/Статистика запросов) — через `tab` в URL; полноэкранная раскладка. Клиентский `SortToggle.tsx` — сегментированные кнопки «По выручке» / «По конверсии клик→заказ», пишет `sort` в URL (useSearchParams/router.push, паттерн PlanFactControls). Клиентский `PdfExportButton.tsx` (`"use client"`) — читает активный `sort` из `useSearchParams` (дефолт revenue) и рендерит ссылку `href={`/api/analytics/runs/${id}/pdf?sort=${sort}`}` (обычный `<a>`/скачивание, БЕЗ импорта pdf-route — только строка URL), так порядок PDF всегда = текущей экранной сортировке. Разместить SortToggle + PdfExportButton в шапке дашборда. Если строки становятся `<Link>` — `prefetch={false}` (CLAUDE.md §601).
  </action>
  <verify>
    <automated>test -f "app/(dashboard)/analytics/runs/[id]/page.tsx" && grep -q "parseNicheRunPayload" "app/(dashboard)/analytics/runs/[id]/page.tsx" && grep -q "sortSkus" "app/(dashboard)/analytics/runs/[id]/page.tsx" && grep -q "requireSection" "app/(dashboard)/analytics/runs/[id]/page.tsx" && test -f components/analytics/SortToggle.tsx && test -f components/analytics/PdfExportButton.tsx && grep -Eq "pdf\?sort=" components/analytics/PdfExportButton.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "analytics/runs|SortToggle|PdfExportButton" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - RSC читает payloadJson через parseNicheRunPayload, гейтится requireSection VIEW.
    - `sortSkus` вызывается ОДИН раз, результат передаётся во все 5 вкладок (единый порядок).
    - `PdfExportButton` строит href на pdf-route с активным `?sort=` (порядок PDF = экран, req.6/req.11); SortToggle пишет sort в URL.
    - Навигация по 5 вкладкам; `tab` по умолчанию = «Общая»; PARTIAL показывает пометку неполноты; tsc чист.
  </acceptance_criteria>
  <done>RSC-контейнер рендерит 5 вкладок из снапшота с единой сортировкой через URL; кнопка PDF наследует активный порядок; PARTIAL-пометка видна.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewTab + ListingTab + CharacteristicsTab (все 30 строк, поля ТЗ §4)</name>
  <files>components/analytics/tabs/OverviewTab.tsx, components/analytics/tabs/ListingTab.tsx, components/analytics/tabs/CharacteristicsTab.tsx</files>
  <read_first>
    - .planning/phases/30-analytics/30-TZ-source.md (§4 — точные поля каждой вкладки)
    - .planning/phases/30-analytics/30-SPEC.md (req.8)
    - .planning/phases/30-analytics/30-RESEARCH.md (CLAUDE.md §471 sticky — сплошной bg-background/bg-muted, БЕЗ /NN)
    - lib/analytics/types.ts (SkuPayload)
  </read_first>
  <action>
    Три клиентских компонента, каждый `({ skus }: { skus: SkuPayload[] })`, sticky-таблица (1 строка = 1 SKU, все 30 видны). `OverviewTab` (по умолч.): фото + бренд + артикул + продавец, рейтинг + число отзывов, средняя цена/мес (medianPriceWallet), сумма заказов/мес, конв. клик→заказ. `ListingTab`: артикул+бренд, узкие колонки выручки и конв.клик→заказ, крупно 5 фото листинга (`listingPhotos`, фото приоритетны по площади). `CharacteristicsTab`: артикул+бренд, выручка+конв.клик→заказ, блок характеристик (`characteristics`). Sticky-заголовки: сплошной `bg-background`/`bg-muted` БЕЗ `/NN` opacity (CLAUDE.md §471). Порядок строк = порядок переданного массива (уже отсортирован контейнером).
  </action>
  <verify>
    <automated>for f in OverviewTab ListingTab CharacteristicsTab; do test -f "components/analytics/tabs/$f.tsx" && grep -q "SkuPayload" "components/analytics/tabs/$f.tsx" && ! grep -Eq "bg-(background|muted)/[0-9]" "components/analytics/tabs/$f.tsx" || { echo "FAIL $f"; exit 1; }; done && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -Ec "OverviewTab|ListingTab|CharacteristicsTab" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - OverviewTab содержит: фото, бренд, артикул, продавец, рейтинг+отзывы, средняя цена/мес, сумма заказов/мес, конв.клик→заказ.
    - ListingTab: крупно 5 фото + узкие колонки выручки/конверсии.
    - CharacteristicsTab: блок характеристик + выручка/конверсия.
    - Все три принимают SkuPayload[] и рендерят все 30 строк; нет полупрозрачных sticky-ячеек; tsc чист.
  </acceptance_criteria>
  <human-check>
    После сборки: открыть `/analytics/runs/<id>`, переключить 5 вкладок — проверить, что все 30 строк видны, «Общая» открыта по умолчанию, поля соответствуют ТЗ §4, смена сортировки меняет порядок идентично на всех вкладках И в скачанном PDF (R8 manual, VALIDATION.md).
  </human-check>
  <done>Три простые вкладки рендерят все 30 SKU с полями ТЗ §4; sticky без opacity-бага; порядок = единая сортировка.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → браузер | payloadJson прогона передаётся клиентским вкладкам |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Elevation of Privilege | RSC-страница прогона | mitigate | requireSection("ANALYTICS","VIEW") в page.tsx до чтения снапшота |
| T-30-13 | Integrity (UI) | sticky-таблицы | mitigate | сплошной bg-background/bg-muted без /NN (grep-гейт) — предотвращает повторяющийся баг проекта |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- RSC-страница + SortToggle + PdfExportButton + 3 вкладки существуют; tsc чист; нет sticky-opacity.
- sortSkus вызывается один раз в контейнере (единый порядок); PdfExportButton передаёт активный ?sort=.
</verification>

<success_criteria>
Полноэкранный дашборд из 5 вкладок рендерится из снапшота; «Общая» по умолчанию; единая сортировка применяется во всех вкладках И в PDF (кнопка наследует активный ?sort=); все 30 SKU видны. ANL-06/ANL-08 выполнены, req.11 порядок закрыт.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-11-SUMMARY.md` — путь дашборд-страницы, параметры URL (sort/tab/metrics) и что PDF-кнопка наследует ?sort= (для 30-12/30-09).
</output>
