---
phase: 30-analytics
plan: 07
type: execute
wave: 3
depends_on: [30-02, 30-03, 30-04, 30-05, 30-06]
files_modified:
  - lib/analytics/snapshot.ts
  - lib/analytics/collector.ts
  - tests/analytics-snapshot.test.ts
autonomous: true
requirements: [ANL-05, ANL-07]
must_haves:
  truths:
    - "Прогон сохраняется как immutable payloadJson + индекс-запись NicheRun"
    - "Коллектор собирает по 30 SKU: MPSTATS-позиции + card.json медиа + агрегат воронки (месяц÷30)"
    - "Сверка цен card.wb.ru — ОДИН батч-вызов verifyPricesBatch на все 30 nmId, НЕ per-SKU (анти-rate-limit)"
    - "Правило полноты: сбой в топ-10 по выручке → FAILED (payload не пишется); в 11–30 → PARTIAL (payload + incompleteSkus)"
    - "429/лимит MPSTATS не роняет весь прогон (деградация по правилу полноты)"
  artifacts:
    - path: "lib/analytics/snapshot.ts"
      provides: "buildNicheRunPayload/parseNicheRunPayload (version guard)"
      exports: ["buildNicheRunPayload", "parseNicheRunPayload"]
    - path: "lib/analytics/collector.ts"
      provides: "collectNicheRun — оркестратор сбора + запись статуса/payload"
      exports: ["collectNicheRun"]
    - path: "tests/analytics-snapshot.test.ts"
      provides: "Тест build/parse payload (version guard, null-fallback)"
  key_links:
    - from: "lib/analytics/collector.ts"
      to: "prisma.nicheRun"
      via: "update статуса/payloadJson"
      pattern: "nicheRun\\.(update|create)"
---

<objective>
Реализовать персистентность прогона (ANL-05) и оркестратор сбора с правилом полноты (ANL-07). `snapshot.ts` — иммутабельный payload (паттерн `lib/finance-weekly/snapshot.ts`: version guard + null-fallback). `collector.ts` — `collectNicheRun(runId, input)`: по 30 SKU собрать позиции (MPSTATS 30-05), медиа `scanCardMedia` (card.json 30-06), агрегировать воронку из файлов (движок 30-03, объёмы = месяц÷30 через monthlyTotals), СВЕРКУ цен `verifyPricesBatch` — ОДНИМ батч-вызовом на все 30 nmId (НЕ per-SKU, анти-rate-limit), оценить полноту (evaluateCompleteness), записать в `NicheRun`. Сбой в топ-10 по выручке → `FAILED` (payloadJson НЕ пишется); в 11–30 → `PARTIAL` (payload + `incompleteSkus`); иначе `READY`. Лимит MPSTATS (D-03) ловится, не роняет прогон.

Purpose: Дорогой прогон считается один раз и рендерится из снапшота; повторный прогон = новая запись.
Output: `lib/analytics/snapshot.ts`, `lib/analytics/collector.ts`, `tests/analytics-snapshot.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-analytics/30-SPEC.md
@.planning/phases/30-analytics/30-RESEARCH.md
@lib/analytics/types.ts

<!-- Паттерн снапшота: version guard + null-fallback -->
@lib/finance-weekly/snapshot.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snapshot.ts — build/parse payload + version guard + тест</name>
  <files>lib/analytics/snapshot.ts, tests/analytics-snapshot.test.ts</files>
  <behavior>
    - buildNicheRunPayload(skus, dateFrom, dateTo) → NicheRunPayload с version = NICHE_RUN_SNAPSHOT_VERSION.
    - parseNicheRunPayload(валидный json) → payload; при неверной version → null; при не-объекте/без skus[] → null.
  </behavior>
  <read_first>
    - lib/finance-weekly/snapshot.ts (buildWeeklySnapshotPayload / parseWeeklySnapshotPayload — version guard + null-fallback, образец 1:1)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 1 — NicheRunPayload, NICHE_RUN_SNAPSHOT_VERSION)
    - lib/analytics/types.ts (NicheRunPayload, SkuPayload, NICHE_RUN_SNAPSHOT_VERSION)
  </read_first>
  <action>
    RED→GREEN. Тест `tests/analytics-snapshot.test.ts`: build→parse round-trip; parse при version=999 → null; parse при `{}` → null. Затем `snapshot.ts` (импорт типов, БЕЗ prisma): `buildNicheRunPayload(skus: SkuPayload[], dateFrom: string, dateTo: string): NicheRunPayload` (version из константы); `parseNicheRunPayload(json: unknown): NicheRunPayload | null` (guard: object, version === NICHE_RUN_SNAPSHOT_VERSION, Array.isArray(skus) — иначе null). По образцу finance-weekly.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Тест round-trip зелёный; parse на неверной version → null; parse на не-объекте → null.
    - `snapshot.ts` не импортирует prisma (чистый build/parse).
  </acceptance_criteria>
  <done>snapshot.ts реализует build/parse с version-guard по образцу finance-weekly; тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: collector.ts — оркестратор сбора + правило полноты + статус-машина</name>
  <files>lib/analytics/collector.ts</files>
  <read_first>
    - .planning/phases/30-analytics/30-SPEC.md (req.5, req.7 — персистентность + правило полноты)
    - .planning/phases/30-analytics/30-RESEARCH.md (§Pattern 2 — collectNicheRun схема; §Code Examples — evaluateCompleteness; D-03 лимиты; CLAUDE.md «WB API rate-limit защиты»)
    - .planning/phases/30-analytics/30-CONTEXT.md (D-02 статус-машина PENDING→COLLECTING→READY|PARTIAL|FAILED; D-03; D-04 цена первично из detail-JSON)
    - lib/analytics/engine.ts (aggregateFunnel(days, monthly), evaluateCompleteness, averagePositionByQuery)
    - lib/analytics/mpstats.ts (fetchPositions, fetchNicheQueries, MpstatsRateLimitError)
    - lib/analytics/wb-card-scan.ts (scanCardMedia — per-SKU; verifyPricesBatch — ОДИН вызов на все nmId)
    - lib/analytics/snapshot.ts (buildNicheRunPayload)
  </read_first>
  <action>
    `collectNicheRun(runId, input: {skus, byDayByNmId, monthlyTotalsByNmId, commonParamsByNmId, dateFrom, dateTo, mpstatsToken})`: (1) `prisma.nicheRun.update` status → COLLECTING; (2) ОДИН РАЗ до/вне SKU-цикла вызвать `verifyPricesBatch(input.skus)` (все 30 nmId одним батчем — сверка СПП/рейтинга; НЕ вызывать per-SKU, анти-rate-limit T-30-16); (3) для каждого из 30 SKU УМЕРЕННЫМ параллелизмом (D-03 — не долбить лимит; последовательно/маленькими батчами): позиции `fetchPositions` + запросы `fetchNicheQueries` (обернуть try/catch на `MpstatsRateLimitError` — пометить SKU без позиций, НЕ падать), медиа `scanCardMedia` (card.json, per-SKU — basket без rate-limit), агрегат `aggregateFunnel(byDayByNmId[nmId], monthlyTotalsByNmId[nmId])` (объёмы месяц÷30), `averagePositionByQuery` на каждый запрос; цена/рейтинг — первично из commonParams/detail-JSON (D-04), сверка из результата verifyPricesBatch; обновлять `progressNote` («MPSTATS Y/30, карточки Z/30»); (4) собрать `SkuCompletenessInput[]` (revenue = ordersSum за период; hasFunnel/hasPhotos/hasCharacteristics/hasPositions), вызвать `evaluateCompleteness`; (5) FAILED → update status FAILED + errorMessage (список проблемных топ-10), payloadJson НЕ писать; PARTIAL → `buildNicheRunPayload` + update status PARTIAL + `incompleteSkus` + payloadJson; OK → status READY + payloadJson. Импортировать `prisma` из `@/lib/prisma`.
  </action>
  <verify>
    <automated>test -f lib/analytics/collector.ts && grep -q "collectNicheRun" lib/analytics/collector.ts && grep -q "evaluateCompleteness" lib/analytics/collector.ts && grep -q "verifyPricesBatch" lib/analytics/collector.ts && ! grep -Eq "for .*verifyPricesBatch|map\(.*verifyPricesBatch" lib/analytics/collector.ts && grep -Eq "MpstatsRateLimitError|catch" lib/analytics/collector.ts && grep -q "COLLECTING" lib/analytics/collector.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "collector" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `collectNicheRun` переводит статус COLLECTING → READY|PARTIAL|FAILED в зависимости от evaluateCompleteness.
    - `verifyPricesBatch` вызывается ОДИН раз на все 30 nmId (вне SKU-цикла); `scanCardMedia`/`fetchPositions`/`fetchNicheQueries` — в цикле per-SKU.
    - `aggregateFunnel` получает monthlyTotals (объёмы месяц÷30); цена/рейтинг первично из detail-JSON.
    - При FAILED payloadJson не пишется; при PARTIAL — payloadJson + incompleteSkus; при READY — payloadJson. `MpstatsRateLimitError`/ошибки одного SKU ловятся, не роняют прогон; tsc чист.
  </acceptance_criteria>
  <done>collectNicheRun собирает 30 SKU из трёх источников (сверка цен — одним батчем), применяет правило полноты по рангу выручки и пишет иммутабельный снапшот; лимиты MPSTATS/WB не роняют прогон и не долбят rate-limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| фоновый процесс → MPSTATS/basket/card.wb.ru/Postgres | сбор данных конкурентов и запись снапшота |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Denial of Service (лимит тарифа MPSTATS) | цикл сбора по 30 SKU | mitigate | умеренный параллелизм (D-03); MpstatsRateLimitError ловится, прогон деградирует по правилу полноты, не падает |
| T-30-16 | Denial of Service (WB Statistics-API rate-limit / IP-бан) | сверка цен card.wb.ru | mitigate (было high) | `verifyPricesBatch` вызывается ОДИН раз на все 30 nmId (вне цикла), НЕ per-SKU; без Sales-API per-SKU ретраев |
| T-30-12 | Integrity | правило полноты топ-10 | mitigate | evaluateCompleteness ранжирует по выручке; сбой в топ-10 → FAILED (payload не пишется) |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | новых пакетов нет |
</threat_model>

<verification>
- `npx vitest run tests/analytics-snapshot.test.ts` зелёный.
- collector.ts: статус-машина + evaluateCompleteness + verifyPricesBatch (1 вызов, вне цикла) + try/catch на лимит; tsc чист.
</verification>

<success_criteria>
Прогон сохраняется как immutable payloadJson; коллектор собирает 30 SKU (сверка цен — одним батчем, объёмы месяц÷30), применяет правило полноты (топ-10 блокирует, 11–30 помечает); лимиты MPSTATS/WB не роняют прогон. ANL-05/ANL-07 core готовы.
</success_criteria>

<output>
Create `.planning/phases/30-analytics/30-07-SUMMARY.md` — сигнатуры collectNicheRun/parseNicheRunPayload (их вызывают 30-08 startNicheRun и 30-11/30-12 рендер).
</output>
