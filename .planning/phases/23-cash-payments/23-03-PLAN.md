---
phase: 23-cash-payments
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - lib/cash-import/types.ts
  - lib/cash-import/normalize.ts
  - lib/cash-import/categorize.ts
  - lib/cash-import/parse.ts
  - lib/cash-import/fingerprint.ts
  - lib/cash-import/persist.ts
  - lib/cash-import/index.ts
  - tests/cash-import.test.ts
  - scripts/import-cash-budget.ts
requirements: [CASH-04, CASH-05, CASH-06, CASH-07]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Чистые функции категоризации/нормализации/парсинга НЕ импортируют next-auth/prisma-singleton (vitest-safe)"
    - "categorize: первое совпадение по УПОРЯДОЧЕННОМУ rule-list (lowercase substring), match-order ≠ display sortOrder, fallback Прочее"
    - "«Пополнение кассы» (аванс на склад / юля фонд / на выкупы маши) матчится ДО «Зарплата/авансы» (аванс)"
    - "Excel-серийник→дата и RU-сумма парсятся (переиспользование excelSerialToDate/parseBalanceAmount)"
    - "responsible нормализуется (strip 1+ инициалов, фикс опечаток, ё→е ТОЛЬКО для lookup-ключа — value сохраняет ё), пусто→Иванова"
    - "fingerprint sha256 детерминирован → идемпотентный импорт"
    - "vitest golden тесты проходят (категоризация спорных назначений + аванс-shadow + Королёва ё-preserve + два инициала)"
    - "scripts/import-cash-budget.ts читает Офис Бюджет.xlsx, фильтр 2024-2026, preflight Иванова, persist через PrismaClient-arg"
  artifacts:
    - path: "lib/cash-import/categorize.ts"
      provides: "categorize(purpose) → имя категории по упорядоченному rule-list (first-match, Прочее fallback); CATEGORY_DISPLAY_ORDER отдельно"
      contains: "Прочее"
    - path: "lib/cash-import/parse.ts"
      provides: "parseYulyaSheet + parsePavelSheet → ParsedCashEntry[]"
      contains: "parseYulyaSheet"
    - path: "lib/cash-import/fingerprint.ts"
      provides: "computeCashFingerprint (sha256)"
      contains: "createHash"
    - path: "lib/cash-import/persist.ts"
      provides: "persistCashEntries(prisma, entries) — PrismaClient-arg, upsert категорий + employee-match + createMany skipDuplicates"
      contains: "PrismaClient"
    - path: "tests/cash-import.test.ts"
      provides: "Golden тесты categorize + аванс-shadow + normalizeResponsibleSurname (ё-preserve, 2 инициала) + fingerprint + parse"
      contains: "describe"
    - path: "scripts/import-cash-budget.ts"
      provides: "Разовый импорт Офис Бюджет.xlsx + preflight Иванова"
      contains: "persistCashEntries"
  key_links:
    - from: "categorize.ts"
      to: "CashCategory.name (23-01 сид)"
      via: "возвращает ровно те же 24 имени"
      pattern: "Грузчики/разнорабочие"
    - from: "scripts/import-cash-budget.ts"
      to: "lib/cash-import/persist.ts"
      via: "persistCashEntries(prisma, entries)"
      pattern: "persistCashEntries"
    - from: "persist.ts"
      to: "CashEntry.fingerprint @unique"
      via: "createMany skipDuplicates"
      pattern: "skipDuplicates"
---

<objective>
Создать пакет чистых функций `lib/cash-import/` (parse листов Юля+Павел, нормализация даты/суммы/подразделения/ответственного, авто-категоризация по УПОРЯДОЧЕННОМУ keyword rule-list, fingerprint-дедуп) + persist-пайплайн с PrismaClient-аргументом (vitest-safe, как `lib/bank-import/persist.ts`) + vitest golden-тесты на спорных назначениях + разовый скрипт `scripts/import-cash-budget.ts`.

Purpose: Ядро импорта 4742+395 строк наличной кассы. Зеркалит архитектуру Phase 22 bank-import. Идёт параллельно с 23-02 (непересекающиеся файлы).
Output: 7 файлов в lib/cash-import/ + тест + скрипт.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cash-payments/23-CONTEXT.md

<interfaces>
<!-- Переиспользуемые pure helpers из Phase 22 + точные паттерны. -->

lib/bank-import/normalize.ts (vitest-safe, без next-auth/prisma) экспортирует УЖЕ ГОТОВЫЕ:
  - excelSerialToDate(serial: number): Date | null  (1900 date system, range 30000-80000)
  - parseDateCell(v): Date | null  (DD.MM.YYYY → fallback excel serial)
  - parseBalanceAmount(s): number | null  (RU/US "217 568,45" / "33,201.97" → number)
  - normalizePurpose(s): string  (trim+collapse ws+lowercase)
  → ПЕРЕИСПОЛЬЗОВАТЬ через import из "../bank-import/normalize" (НЕ дублировать).

lib/bank-import/fingerprint.ts (образец): createHash("sha256").update(fields.join("|")).digest("hex").
lib/bank-import/persist.ts (образец PrismaClient-arg): `import type { PrismaClient } from "@prisma/client"`; функция принимает prisma первым аргом; upsert справочников + кеш Map + createMany({ data, skipDuplicates: true }); НЕ импортирует next-auth/lib/prisma. Скрипт передаёт свой PrismaClient.

vitest: tests/**/*.test.ts, environment node. Запуск `npm run test`. КРИТИЧНО (Phase 7 урок): тестируемый код НЕ импортирует next-auth/Prisma-singleton/next/* — иначе vitest падает на транзитивном импорте. lib/cash-import/{normalize,categorize,parse,fingerprint,types}.ts — чистые. persist.ts импортирует только `import type { PrismaClient }` (type-only, ок).

CashDirection enum (23-01): INCOME | EXPENSE. CashEntry поля (23-01): date, direction, amount, department?, categoryId?, purpose, responsibleEmployeeId?, responsibleNameRaw?, comment?, source, fingerprint?.

Источник: "Офис Бюджет.xlsx" в корне проекта (untracked → .gitignore; scp на VPS в 23-05).
Лист «Юля» колонки (header row 0, данные с row 1): Дата=0 | Приходы=1 | Расходы=2 | Подразделение=3 | (пусто)=4 | Назначение=5 | Ответственный=6.
Лист «Павел» колонки (header row 0): Дата=0 | Назначение=1 | Сумма=2. Все = EXPENSE.
Лист «Микроволновка на склад» — ИГНОРИРОВАТЬ.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types + normalize + categorize (упорядоченный rule-list, match-order ≠ display) + fingerprint + tests RED→GREEN</name>
  <read_first>
    - lib/bank-import/normalize.ts (excelSerialToDate, parseDateCell, parseBalanceAmount, normalizePurpose — переиспользовать)
    - lib/bank-import/fingerprint.ts (sha256 паттерн)
    - tests/excel-auto-promo.test.ts (vitest fixture/golden паттерн)
    - .planning/phases/23-cash-payments/23-CONTEXT.md строки 33-74 (таксономия 24 + keyword-map + нормализация подразделения/ответственного)
  </read_first>
  <behavior>
    categorize (first-match по УПОРЯДОЧЕННОМУ rule-list, lowercase substring, Прочее fallback):
    - "оплата грузчики 5 чел" → "Грузчики/разнорабочие"
    - "заправка картриджей" → "Канцелярия/оргтехника" (картридж ловится ДО такси — порядок!)
    - "выкуп аэрогриль" → "Выкупы товаров"
    - "юля фонд пополнение" → "Пополнение кассы"
    - "аванс на склад" → "Пополнение кассы"  (КРИТИЧНО: «Пополнение кассы» матчится ДО «Зарплата/авансы», иначе "аванс" в «Зарплата» затеняет сотни строк Павла)
    - "аванс" → "Зарплата/авансы"  (чистый «аванс» без «на склад» → зарплата)
    - "юля фонд" → "Пополнение кассы"
    - "такси до склада" → "Такси/транспорт"
    - "вода для кулера" → "Вода"
    - "что-то непонятное xyz" → "Прочее" (fallback)
    normalizeResponsibleSurname:
    - "Иванова Н." → "Иванова" (strip одного инициала)
    - "Иванова Н. В." → "Иванова" (strip ДВУХ инициалов — regex /(\s+[А-ЯЁ]\.?)+\s*$/u)
    - "Федоров" → "Фёдоров" (опечатка — фикс через SURNAME_FIXES)
    - "Чихова" → "Чижова"; "Легоставева"/"Легоставеа" → "Легостаева"
    - "Королёва" → "Королёва" (ё СОХРАНЯЕТСЯ в value; ё→е применяется ТОЛЬКО к lookup-ключу для SURNAME_FIXES)
    - "" / null → "Иванова" (default)
    computeCashFingerprint: одинаковый вход → одинаковый hash; разный amount/date/purpose → разный.
    normalizeDepartment: ""→null; "офис+ склад"→"офис+склад"; "Офис"→"офис" (trim, lowercase варианты офис/склад/маркетинг/такси).
  </behavior>
  <action>
**`lib/cash-import/types.ts`**:
```typescript
export type CashDir = "INCOME" | "EXPENSE"
export interface ParsedCashEntry {
  sheet: "yulya" | "pavel"
  date: Date
  direction: CashDir
  amount: number
  department: string | null
  purpose: string
  responsibleNameRaw: string   // нормализованная фамилия (пусто→"Иванова")
  categoryName: string         // одно из 24 имён категорий
  source: "budget-yulya" | "budget-pavel"
}
```

**`lib/cash-import/normalize.ts`** — переэкспорт + новые cash-специфичные. НЕ дублировать excelSerialToDate/parseBalanceAmount — реэкспорт:
```typescript
export { excelSerialToDate, parseDateCell, parseBalanceAmount, normalizePurpose } from "../bank-import/normalize"

export function normalizeDepartment(v: string | null | undefined): string | null {
  if (v == null) return null
  const s = String(v).trim().toLowerCase().replace(/\s+/g, " ")
  if (s === "") return null
  // нормализовать «офис+ склад» / «офис +склад» → «офис+склад»
  return s.replace(/\s*\+\s*/g, "+")
}

// Ключи lookup-таблицы — в lowercase + ё→е (нормализованный ключ). Значения — каноничный вид (с ё).
const SURNAME_FIXES: Record<string, string> = {
  "федоров": "Фёдоров", "федорова": "Фёдорова",
  "чихова": "Чижова",
  "легоставева": "Легостаева", "легоставеа": "Легостаева",
}
export function normalizeResponsibleSurname(v: string | null | undefined): string {
  let s = String(v ?? "").trim()
  if (s === "") return "Иванова"                              // пусто → Иванова (default)
  // strip ОДНОГО ИЛИ НЕСКОЛЬКИХ trailing инициалов: «Иванова Н.» / «Иванова Н. В.» / «Иванов А.Б.»
  s = s.replace(/(\s+[А-ЯЁ]\.?)+\s*$/u, "").trim()
  // КРИТИЧНО (WARNING 2): ё→е применяем ТОЛЬКО к ключу для SURNAME_FIXES,
  // НЕ к возвращаемому значению (responsibleNameRaw должен сохранять ё, напр. «Королёва»).
  const key = s.toLowerCase().replace(/ё/g, "е")
  if (SURNAME_FIXES[key]) return SURNAME_FIXES[key]
  // Не в таблице фиксов — возвращаем оригинал (с сохранением ё), capitalize первой буквы.
  return s.charAt(0).toUpperCase() + s.slice(1)
}
```
ПРИМЕЧАНИЕ по ё: в БД Employee.lastName может быть с ё (Фёдоров, Королёва). responsibleNameRaw ВСЕГДА сохраняет ё (value не трогаем). Матч в persist.ts делается ё-insensitive (обе стороны к «е»). Здесь ё→е только для lookup-ключа SURNAME_FIXES, чтобы «Федоров»/«Фёдоров» оба попадали в фикс.

**`lib/cash-import/categorize.ts`** — УПОРЯДОЧЕННЫЙ rule-list. **match-order НЕЗАВИСИМ от display sortOrder.** Порядок матчинга специально отличается от нумерации категорий 1..24: «Пополнение кассы» проверяется ПЕРЕД «Зарплата/авансы», чтобы "аванс на склад" не затенялось ключом "аванс". sortOrder 1..24 (для DISPLAY/сида 23-01) — отдельная константа:
```typescript
// === ПОРЯДОК МАТЧИНГА (CATEGORY_MATCH_RULES) ≠ ПОРЯДОК ОТОБРАЖЕНИЯ (CATEGORY_DISPLAY_ORDER) ===
// Матчинг — first-match по этому списку. Дисплей-сортировка категорий 1..24 — в 23-01 сиде.
// КРИТИЧНО (BLOCKER): «Пополнение кассы» (аванс на склад / юля фонд / на выкупы маши)
// идёт ВЫШЕ «Зарплата/авансы» (аванс), иначе "аванс на склад" (сотни строк Павла)
// ошибочно попадёт в «Зарплата/авансы» из-за substring "аванс".
const CATEGORY_MATCH_RULES: [string, string[]][] = [
  ["Грузчики/разнорабочие", ["грузчик", "разнорабоч", "такелаж"]],
  ["Упаковка", ["упаковщиц", "упаковк", "фасовщиц", "фасовк"]],
  ["Выкупы товаров", ["выкуп", "самовыкуп"]],
  ["Образцы/Китай", ["образц", "китай", "инспекц", "1688", "кабинет", "переводчик"]],
  ["Курьеры/доставка", ["курьер", "доставка", "сдэк", "cdek", "отправк", "почт"]],
  ["Фриланс/подрядчики", ["воркзилла", "кворк", "kwork", "фриланс", "дизайнер", "верстальщик", "программист", "блогер"]],
  // «Пополнение кассы» ВЫШЕ «Зарплата/авансы» — критично для "аванс на склад" (см. BLOCKER выше)
  ["Пополнение кассы", ["юля фонд", "аванс на склад", "на выкупы маши", "на расходы", "пополнение"]],
  ["Зарплата/авансы", ["аванс", "зарплат", "зп ", "оклад", "премия", "расчёт сотр"]],
  ["Вода", ["вода"]],
  ["Продукты/кухня", ["конфет", "чай", "кофе", "печень", "продукт", "сахар", "молоко", "вкусняшк", "сладк"]],
  ["Связь", ["связь", "телефон", "интернет", "мтс", "билайн", "мегафон", "симк", "тариф"]],
  ["Такси/транспорт", ["такси", "каршеринг", "бензин", "гсм", "проезд"]],
  ["Канцелярия/оргтехника", ["картридж", "бумаг", "канцеляр", "тонер", "печать", "ручк", "файл"]],
  ["Клининг/уборка", ["уборщиц", "клининг", "уборк"]],
  ["Реклама/маркетинг", ["реклам", "продвижен", "маркетинг", "таргет", "инст"]],
  ["Подбор персонала", ["ваканс", "авито", "hh", "объявлен", "резюме", "собеседован"]],
  ["Маркировка/ЧЗ", ["чз", "честный знак", "маркировк", "gs1", "штрихкод", "штрих-код"]],
  ["Подарки/цветы", ["цветы", "подарок", "подарк", "букет"]],
  ["Кэшбек", ["кэшбек", "кешбэк", "кэшбэк", "cashback"]],
  ["Аренда", ["аренда", "южные ворота"]],
  ["Налоги/банк/сборы", ["налог", "ндс", "госпошлин", "штраф", "комиссия банк", "пошлин"]],
  ["Управленческий учёт/услуги", ["управленческий учет", "бухгалтер", "юрист", "услуг", "надежда гаврилова"]],
  ["Возврат займов", ["возврат кредита", "возврат займа", "погашение займа", "займ"]],
]
export const FALLBACK_CATEGORY = "Прочее"

// Порядок ОТОБРАЖЕНИЯ категорий (sortOrder 1..24 в 23-01 сиде) — для справки/консистентности.
// НЕ используется в categorize(); матчинг идёт по CATEGORY_MATCH_RULES.
export const CATEGORY_DISPLAY_ORDER: string[] = [
  "Грузчики/разнорабочие", "Упаковка", "Выкупы товаров", "Образцы/Китай",
  "Курьеры/доставка", "Фриланс/подрядчики", "Зарплата/авансы", "Вода",
  "Продукты/кухня", "Связь", "Такси/транспорт", "Канцелярия/оргтехника",
  "Клининг/уборка", "Реклама/маркетинг", "Подбор персонала", "Маркировка/ЧЗ",
  "Подарки/цветы", "Кэшбек", "Аренда", "Налоги/банк/сборы",
  "Управленческий учёт/услуги", "Возврат займов", "Пополнение кассы", "Прочее",
]

export function categorize(purpose: string | null | undefined): string {
  const p = String(purpose ?? "").toLowerCase()
  for (const [name, keywords] of CATEGORY_MATCH_RULES) {
    for (const kw of keywords) {
      if (p.includes(kw)) return name
    }
  }
  return FALLBACK_CATEGORY
}
```
ВАЖНО: имена ТОЧНО совпадают с сидом 23-01 (24 имени). «Прочее» = fallback. **Порядок матчинга (CATEGORY_MATCH_RULES) ≠ порядок отображения (CATEGORY_DISPLAY_ORDER / sortOrder 1..24).** «Пополнение кассы» намеренно поднята в матчинге выше «Зарплата/авансы».

**`lib/cash-import/fingerprint.ts`** (паттерн bank-import):
```typescript
import { createHash } from "crypto"
import { normalizePurpose } from "../bank-import/normalize"
import type { ParsedCashEntry } from "./types"

export function computeCashFingerprint(e: ParsedCashEntry): string {
  const fields = [
    e.sheet,
    e.date.toISOString().slice(0, 10),
    e.direction,
    e.amount.toFixed(2),
    normalizePurpose(e.purpose),
    e.responsibleNameRaw,
  ]
  return createHash("sha256").update(fields.join("|")).digest("hex")
}
```

**`tests/cash-import.test.ts`** — golden тесты (RED перед реализацией → GREEN):
```typescript
import { describe, it, expect } from "vitest"
import { categorize, FALLBACK_CATEGORY } from "@/lib/cash-import/categorize"
import { normalizeResponsibleSurname, normalizeDepartment } from "@/lib/cash-import/normalize"
import { computeCashFingerprint } from "@/lib/cash-import/fingerprint"

describe("categorize (first-match по rule-list, Прочее fallback)", () => {
  it("грузчики → Грузчики/разнорабочие", () => expect(categorize("оплата грузчики 5 чел")).toBe("Грузчики/разнорабочие"))
  it("заправка картриджей → Канцелярия (НЕ Такси)", () => expect(categorize("заправка картриджей")).toBe("Канцелярия/оргтехника"))
  it("выкуп аэрогриль → Выкупы товаров", () => expect(categorize("выкуп аэрогриль")).toBe("Выкупы товаров"))
  // BLOCKER: «аванс на склад» НЕ должно затеняться ключом "аванс" в «Зарплата/авансы»
  it("аванс на склад → Пополнение кассы (НЕ Зарплата)", () => expect(categorize("аванс на склад")).toBe("Пополнение кассы"))
  it("чистый аванс → Зарплата/авансы", () => expect(categorize("аванс")).toBe("Зарплата/авансы"))
  it("юля фонд → Пополнение кассы", () => expect(categorize("юля фонд")).toBe("Пополнение кассы"))
  it("такси → Такси/транспорт", () => expect(categorize("такси до склада")).toBe("Такси/транспорт"))
  it("неизвестное → Прочее", () => expect(categorize("zzz непонятное")).toBe(FALLBACK_CATEGORY))
})
describe("normalizeResponsibleSurname", () => {
  it("strip один инициал", () => expect(normalizeResponsibleSurname("Иванова Н.")).toBe("Иванова"))
  it("strip два инициала", () => expect(normalizeResponsibleSurname("Иванова Н. В.")).toBe("Иванова"))
  it("пусто → Иванова", () => expect(normalizeResponsibleSurname("")).toBe("Иванова"))
  it("опечатка Федоров → Фёдоров", () => expect(normalizeResponsibleSurname("Федоров")).toBe("Фёдоров"))
  // WARNING 2: ё в фамилии вне таблицы фиксов СОХРАНЯЕТСЯ
  it("Королёва сохраняет ё", () => expect(normalizeResponsibleSurname("Королёва")).toBe("Королёва"))
})
describe("normalizeDepartment", () => {
  it("пусто → null", () => expect(normalizeDepartment("")).toBe(null))
  it("офис+ склад → офис+склад", () => expect(normalizeDepartment("офис+ склад")).toBe("офис+склад"))
})
describe("computeCashFingerprint", () => {
  const base = { sheet: "yulya" as const, date: new Date("2025-03-01"), direction: "EXPENSE" as const, amount: 5000, department: null, purpose: "грузчики", responsibleNameRaw: "Иванова", categoryName: "Грузчики/разнорабочие", source: "budget-yulya" as const }
  it("детерминирован", () => expect(computeCashFingerprint(base)).toBe(computeCashFingerprint(base)))
  it("разный amount → разный hash", () => expect(computeCashFingerprint(base)).not.toBe(computeCashFingerprint({ ...base, amount: 6000 })))
})
```
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx vitest run tests/cash-import.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` (или vitest run tests/cash-import.test.ts) — все cash-import тесты PASS, exit 0
    - Тесты включают: `аванс на склад → Пополнение кассы`, `аванс → Зарплата/авансы`, `юля фонд → Пополнение кассы`, `Королёва → Королёва`, `Иванова Н. В. → Иванова`
    - `grep "Прочее" lib/cash-import/categorize.ts` находит (FALLBACK_CATEGORY)
    - `grep -n "Пополнение кассы" lib/cash-import/categorize.ts` показывает строку В CATEGORY_MATCH_RULES ВЫШЕ `"Зарплата/авансы"` (порядок first-match — пополнение перед зарплатой)
    - `grep "картридж" lib/cash-import/categorize.ts` находит ВЫШЕ `"такси"` (порядок first-match) — Канцелярия перед Такси
    - `grep "replace(/ё/g, \"е\")" lib/cash-import/normalize.ts` присутствует ТОЛЬКО при вычислении `key` (не применяется к return value) — `grep -n "ё" lib/cash-import/normalize.ts` показывает что SURNAME_FIXES values содержат ё (Фёдоров)
    - `grep "from \"../bank-import/normalize\"" lib/cash-import/normalize.ts` находит (переиспользование, НЕ дублирование excelSerialToDate)
    - `grep "createHash" lib/cash-import/fingerprint.ts` находит
    - lib/cash-import/{types,normalize,categorize,fingerprint}.ts НЕ импортируют next-auth/@/lib/prisma/next: `grep -rE "next-auth|@/lib/prisma|next/" lib/cash-import/types.ts lib/cash-import/normalize.ts lib/cash-import/categorize.ts lib/cash-import/fingerprint.ts` → пусто
  </acceptance_criteria>
  <done>Категоризация (24 имени, упорядоченный match-rule-list с «Пополнение кассы» выше «Зарплата/авансы», Прочее fallback), нормализация ответственного (ё-preserve, strip 1+ инициалов, фиксы опечаток) и подразделения, fingerprint — реализованы и протестированы GREEN; чисто от next-auth.</done>
</task>

<task type="auto">
  <name>Task 2: parse (Юля+Павел) + persist (PrismaClient-arg) + index + scripts/import-cash-budget.ts (preflight Иванова)</name>
  <read_first>
    - lib/bank-import/persist.ts (ТОЧНЫЙ образец: PrismaClient-arg, upsert справочников + Map-кеш + createMany skipDuplicates, без next-auth)
    - lib/bank-import/index.ts (re-export паттерн)
    - .planning/phases/22-bank-accounts/22-05-PLAN.md Task 3 (scripts/import-bank-statements.ts — образец разового скрипта: PrismaClient, fs.readFileSync, XLSX.read, sheet_to_json header:1)
    - lib/cash-import/{categorize,normalize,fingerprint,types}.ts (из Task 1)
  </read_first>
  <action>
**`lib/cash-import/parse.ts`** — парсинг двух листов из XLSX.WorkBook (raw:true для серийных дат):
```typescript
import * as XLSX from "xlsx"
import { parseDateCell, parseBalanceAmount, normalizeDepartment, normalizeResponsibleSurname } from "./normalize"
import { categorize } from "./categorize"
import type { ParsedCashEntry } from "./types"

const MIN_YEAR = 2024, MAX_YEAR = 2026

function rowsOf(wb: XLSX.WorkBook, sheetName: string): (string | number | null)[][] {
  const sheet = wb.Sheets[sheetName]
  if (!sheet) return []
  return XLSX.utils.sheet_to_json(sheet, { header: 1, defval: null, raw: true })
}

/** Лист «Юля»: Дата=0 | Приходы=1 | Расходы=2 | Подразделение=3 | (пусто)=4 | Назначение=5 | Ответственный=6 */
export function parseYulyaSheet(wb: XLSX.WorkBook): ParsedCashEntry[] {
  const rows = rowsOf(wb, "Юля")
  const out: ParsedCashEntry[] = []
  for (let i = 1; i < rows.length; i++) {
    const r = rows[i]
    const date = parseDateCell(r[0] as never)
    if (!date) continue                                   // битые даты — пропуск
    const y = date.getUTCFullYear(); if (y < MIN_YEAR || y > MAX_YEAR) continue
    const income = parseBalanceAmount(r[1] as never)
    const expense = parseBalanceAmount(r[2] as never)
    let direction: "INCOME" | "EXPENSE"; let amount: number
    if (expense && expense > 0) { direction = "EXPENSE"; amount = expense }
    else if (income && income > 0) { direction = "INCOME"; amount = income }
    else continue                                          // нет суммы — пропуск
    const purpose = String(r[5] ?? "").trim()
    const department = normalizeDepartment(r[3] as never)
    const responsibleNameRaw = normalizeResponsibleSurname(r[6] as never)
    out.push({ sheet: "yulya", date, direction, amount, department, purpose, responsibleNameRaw, categoryName: categorize(purpose), source: "budget-yulya" })
  }
  return out
}

/** Лист «Павел»: Дата=0 | Назначение=1 | Сумма=2. Всё = EXPENSE. Ответственный пусто → Иванова. */
export function parsePavelSheet(wb: XLSX.WorkBook): ParsedCashEntry[] {
  const rows = rowsOf(wb, "Павел")
  const out: ParsedCashEntry[] = []
  for (let i = 1; i < rows.length; i++) {
    const r = rows[i]
    const date = parseDateCell(r[0] as never)
    if (!date) continue
    const y = date.getUTCFullYear(); if (y < MIN_YEAR || y > MAX_YEAR) continue
    const amount = parseBalanceAmount(r[2] as never)
    if (!amount || amount <= 0) continue
    const purpose = String(r[1] ?? "").trim()
    out.push({ sheet: "pavel", date, direction: "EXPENSE", amount, department: null, purpose, responsibleNameRaw: normalizeResponsibleSurname(null), categoryName: categorize(purpose), source: "budget-pavel" })
  }
  return out
}

export function parseBudget(wb: XLSX.WorkBook): ParsedCashEntry[] {
  return [...parseYulyaSheet(wb), ...parsePavelSheet(wb)]   // «Микроволновка на склад» игнорируется
}
```

**`lib/cash-import/persist.ts`** — PrismaClient-arg (зеркало bank-import/persist.ts). НЕ импортировать next-auth/lib/prisma:
```typescript
import type { PrismaClient } from "@prisma/client"
import { computeCashFingerprint } from "./fingerprint"
import type { ParsedCashEntry } from "./types"

export interface CashPersistResult { imported: number; skipped: number; total: number }

function eqSurname(a: string, b: string): boolean {
  const n = (s: string) => s.toLowerCase().replace(/ё/g, "е").trim()
  return n(a) === n(b)
}

export async function persistCashEntries(prisma: PrismaClient, entries: ParsedCashEntry[]): Promise<CashPersistResult> {
  if (entries.length === 0) return { imported: 0, skipped: 0, total: 0 }

  // 1. Кеш категорий по имени (категории засеяны миграцией 23-01; upsert на случай отсутствия)
  const catCache = new Map<string, string>()
  const names = new Set(entries.map((e) => e.categoryName))
  for (const name of names) {
    const cat = await prisma.cashCategory.upsert({ where: { name }, update: {}, create: { name, sortOrder: 0 } })
    catCache.set(name, cat.id)
  }

  // 2. Сопоставление ответственного → Employee по lastName (ё-insensitive). Не совпало → null.
  const employees = await prisma.employee.findMany({ select: { id: true, lastName: true } })
  const empCache = new Map<string, string | null>()  // responsibleNameRaw → employeeId|null
  function matchEmployee(raw: string): string | null {
    if (empCache.has(raw)) return empCache.get(raw)!
    const found = employees.find((e) => eqSurname(e.lastName, raw)) ?? null
    const id = found?.id ?? null
    empCache.set(raw, id)
    return id
  }

  // 3. Готовим rows + intra-batch дедуп по fingerprint
  const seen = new Set<string>()
  const rows: Array<{
    date: Date; direction: "INCOME" | "EXPENSE"; amount: number; department: string | null
    categoryId: string | null; purpose: string; responsibleEmployeeId: string | null
    responsibleNameRaw: string; source: string; fingerprint: string
  }> = []
  for (const e of entries) {
    const fingerprint = computeCashFingerprint(e)
    if (seen.has(fingerprint)) continue
    seen.add(fingerprint)
    rows.push({
      date: e.date, direction: e.direction, amount: e.amount, department: e.department,
      categoryId: catCache.get(e.categoryName) ?? null, purpose: e.purpose,
      responsibleEmployeeId: matchEmployee(e.responsibleNameRaw),
      responsibleNameRaw: e.responsibleNameRaw, source: e.source, fingerprint,
    })
  }

  // 4. createMany skipDuplicates (идемпотентность через fingerprint @unique)
  const res = await prisma.cashEntry.createMany({ data: rows, skipDuplicates: true })
  return { imported: res.count, skipped: entries.length - res.count, total: entries.length }
}
```

**`lib/cash-import/index.ts`** — re-export:
```typescript
export * from "./types"
export { categorize, FALLBACK_CATEGORY, CATEGORY_DISPLAY_ORDER } from "./categorize"
export { normalizeDepartment, normalizeResponsibleSurname, excelSerialToDate, parseDateCell, parseBalanceAmount } from "./normalize"
export { computeCashFingerprint } from "./fingerprint"
export { parseYulyaSheet, parsePavelSheet, parseBudget } from "./parse"
export { persistCashEntries } from "./persist"
```

**`scripts/import-cash-budget.ts`** (tsx, разовый прогон; образец scripts/import-bank-statements.ts). С preflight-проверкой наличия Employee Иванова:
```typescript
import { PrismaClient } from "@prisma/client"
import * as XLSX from "xlsx"
import fs from "fs"
import path from "path"
import { parseBudget } from "../lib/cash-import/parse"
import { persistCashEntries } from "../lib/cash-import/persist"

const prisma = new PrismaClient()
async function main() {
  // WARNING 3: preflight — Employee Иванова используется как default ответственный.
  // Если её нет в справочнике, записи с пустым ответственным получат responsibleEmployeeId=null.
  const iv = await prisma.employee.findFirst({ where: { lastName: "Иванова" } })
  if (!iv) console.warn("⚠ Employee Иванова не найдена — записи с пустым ответственным получат responsibleEmployeeId=null (responsibleNameRaw сохранится)")
  else console.log(`✓ Default ответственный найден: Иванова (id=${iv.id})`)

  const file = path.resolve(process.cwd(), "Офис Бюджет.xlsx")
  const wb = XLSX.read(fs.readFileSync(file), { type: "buffer", raw: true })
  const entries = parseBudget(wb)
  console.log(`Распознано записей: ${entries.length} (Юля+Павел, 2024-2026)`)
  const r = await persistCashEntries(prisma, entries)
  console.log(`total=${r.total} imported=${r.imported} skipped=${r.skipped}`)
}
main().then(() => prisma.$disconnect()).catch(async (e) => { console.error(e); await prisma.$disconnect(); process.exit(1) })
```
Запуск (документировать в SUMMARY): локально `npx tsx scripts/import-cash-budget.ts`; на VPS `set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/import-cash-budget.ts`. Идемпотентен (повторный запуск → imported 0). Файл «Офис Бюджет.xlsx» untracked — НЕ коммитить; убедиться что он в .gitignore (добавить если нет).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "cash-import|import-cash-budget" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "parseYulyaSheet" lib/cash-import/parse.ts` находит + колонки Юля (r[1] приход, r[2] расход, r[5] purpose, r[6] responsible)
    - `grep "parsePavelSheet" lib/cash-import/parse.ts` находит + `EXPENSE` для всех + r[2] amount
    - `grep "MIN_YEAR = 2024" lib/cash-import/parse.ts` И `MAX_YEAR = 2026` находит (фильтр годов)
    - `grep "PrismaClient" lib/cash-import/persist.ts` находит (type-only import); `grep "skipDuplicates" lib/cash-import/persist.ts` находит
    - persist.ts НЕ импортирует next-auth/lib/prisma: `grep -E "next-auth|@/lib/prisma" lib/cash-import/persist.ts` → пусто
    - `grep "persistCashEntries" scripts/import-cash-budget.ts` И `grep "Офис Бюджет.xlsx" scripts/import-cash-budget.ts` находят
    - WARNING 3: `grep -E "Иванова не найдена|findFirst.*Иванова|lastName: \"Иванова\"" scripts/import-cash-budget.ts` находит preflight Ивановой
    - `grep "Микроволновка"` НЕ требуется (игнорируется — parseBudget читает только Юля+Павел)
    - tsc чистый по cash-import + скрипту
  </acceptance_criteria>
  <done>parse Юля+Павел (фильтр 2024-2026, серийные даты, направление по колонкам), persist с employee-match (default Иванова, ё-insensitive) + categoryId по имени + createMany skipDuplicates; скрипт делает preflight Ивановой и делегирует в persistCashEntries.</done>
</task>

</tasks>

<verification>
- vitest cash-import тесты GREEN (категоризация спорных кейсов: картридж→Канцелярия, выкуп→Выкупы, юля фонд→Пополнение, аванс на склад→Пополнение, аванс→Зарплата)
- excelSerialToDate/parseBalanceAmount/normalizePurpose переиспользованы из bank-import (не дублированы)
- responsible: пусто→Иванова, ё СОХРАНЯЕТСЯ в value (Королёва), ё→е только для lookup-ключа, strip 1+ инициалов
- match-order ≠ display sortOrder («Пополнение кассы» выше «Зарплата/авансы» в матчинге)
- fingerprint sha256 детерминирован
- persist — PrismaClient-arg, vitest-safe (без next-auth)
- скрипт читает Офис Бюджет.xlsx, preflight Иванова, фильтр 2024-2026, делегирует persist
</verification>

<success_criteria>
- lib/cash-import/ — полный пакет чистых функций + persist
- Golden тесты проходят (включая аванс-shadow, Королёва ё-preserve, два инициала)
- scripts/import-cash-budget.ts готов к прогону на VPS (23-05), логирует preflight Ивановой
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-payments/23-03-SUMMARY.md`.
</output>
</content>
