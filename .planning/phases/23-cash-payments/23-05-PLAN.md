---
phase: 23-cash-payments
plan: 05
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified:
  - .gitignore
requirements: [CASH-11]
autonomous: false
user_setup: []
must_haves:
  truths:
    - "Миграция 20260610_phase23_cash применена на проде (prisma migrate deploy)"
    - "Офис Бюджет.xlsx загружен на VPS и импортирован (Юля+Павел 2024-2026)"
    - "Повторный прогон импорта идемпотентен (imported=0)"
    - "Иванова Юлия имеет UserSectionRole(CASH, MANAGE)"
    - "/cash на проде показывает импортированные операции + работает форма добавления"
  artifacts:
    - path: ".gitignore"
      provides: "Игнор Офис Бюджет.xlsx (реальные данные, untracked)"
      contains: "Офис Бюджет"
  key_links:
    - from: "VPS scp"
      to: "scripts/import-cash-budget.ts"
      via: "npx tsx прогон на проде"
      pattern: "import-cash-budget"
    - from: "Employee Иванова Юлия"
      to: "UserSectionRole CASH MANAGE"
      via: "найти Employee→User→provision"
      pattern: "CASH"
---

<objective>
Развернуть раздел кассы на проде: применить миграцию (`prisma migrate deploy` через deploy.sh), залить `Офис Бюджет.xlsx` через scp, прогнать `scripts/import-cash-budget.ts`, провизионировать RBAC для **Ивановой Юлии** (CASH MANAGE), провести UAT. Добавить `.xlsx` источник в .gitignore.

Purpose: Финальный план — данные в проде + доступ ведущему раздела. Зависит от всех предыдущих. Checkpoint (autonomous:false) — UAT с пользователем.
Output: данные в проде + RBAC + .gitignore + UAT-подтверждение.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cash-payments/23-CONTEXT.md

<interfaces>
<!-- Deploy/VPS/RBAC паттерны. -->

Deploy: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — deploy.sh запускает `prisma migrate deploy` → применит 20260610_phase23_cash. Пользователь делегирует deploy (memory feedback_deploy_delegation — не спрашивать подтверждение перед push+deploy).

VPS: /opt/zoiten-pro, env /etc/zoiten.pro.env. Прогон скрипта: `set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/import-cash-budget.ts`.

scp источника: `scp "Офис Бюджет.xlsx" root@85.198.97.89:/opt/zoiten-pro/` (как Выписки/Кредиты в прошлых фазах). Файл untracked — НЕ коммитить.

RBAC provision (memory feedback_zoiten_new_section_rbac): после добавления раздела ОБЯЗАТЕЛЬНО выдать UserSectionRole, иначе все кроме SUPERADMIN → 403. Найти Employee «Иванова Юлия» → её User (Employee.user 1:1) → INSERT/upsert UserSectionRole(userId, section='CASH', role='MANAGE'). Через /admin/users тумблер ИЛИ SQL. Получатель перелогинивается или ждёт ~60с (JWT refresh, fix d30cde6). UserSectionRole @@unique([userId, section]).

Категории засеяны миграцией (23-01) — отдельный сид не нужен; persist.ts (23-03) upsert'ит на случай отсутствия.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore — игнор источника Офис Бюджет.xlsx</name>
  <read_first>
    - .gitignore (текущее содержимое — проверить нет ли уже строки для xlsx-источников)
  </read_first>
  <action>
Добавить в `.gitignore` строку для источника наличной кассы (реальные финансовые данные, untracked):
```
# Phase 23: источник кассы (реальные данные — только scp на VPS)
Офис Бюджет.xlsx
```
Если в .gitignore уже игнорируются Выписки/ или Кредиты/ по образцу — добавить рядом. НЕ коммитить сам .xlsx.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && node -e "const s=require('fs').readFileSync('.gitignore','utf8'); process.exit(s.includes('Офис Бюджет') ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Офис Бюджет" .gitignore` находит
    - `git status` не показывает «Офис Бюджет.xlsx» как кандидат на коммит (untracked + ignored)
  </acceptance_criteria>
  <done>Источник кассы игнорируется git.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — deploy миграции, импорт данных на VPS, provision RBAC Ивановой, проверка /cash</name>
  <action>
    Агент сначала автоматизирует deploy + scp + импорт + provision RBAC (см. how-to-verify), затем пауза для ручной проверки пользователем.
  </action>
  <what-built>
    Полный раздел /cash: схема+миграция+сид 24 категорий (23-01), проводка раздела 6 точек + RBAC-тумблер (23-02), парсер+категоризатор+fingerprint+скрипт импорта (23-03), server actions + удобная форма + sticky-таблица с итогами/балансом+фильтры (23-04).
  </what-built>
  <how-to-verify>
    Автоматизировано агентом ДО checkpoint:
    1. **Deploy**: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — применит `20260610_phase23_cash` через `prisma migrate deploy` (создаст таблицы + засеет 24 категории).
    2. **scp источника**: `scp "Офис Бюджет.xlsx" root@85.198.97.89:/opt/zoiten-pro/`.
    3. **Импорт**: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/import-cash-budget.ts"` — вывести total/imported/skipped (ожидаемо ~4000+ записей за 2024-2026).
    4. **Provision RBAC**: найти Employee «Иванова Юлия» и её User; выдать UserSectionRole(CASH, MANAGE). Через SQL на VPS:
       ```sql
       INSERT INTO "UserSectionRole" (id, "userId", section, role)
       SELECT gen_random_uuid(), u.id, 'CASH', 'MANAGE'
       FROM "User" u JOIN "Employee" e ON u."employeeId" = e.id
       WHERE e."lastName" = 'Иванова' AND e."firstName" = 'Юлия'
       ON CONFLICT ("userId", section) DO UPDATE SET role = 'MANAGE';
       ```
       (суперадмину доступ не нужен — bypass). Если Employee «Иванова Юлия» имеет другое имя/нет User — спросить пользователя кому именно выдать CASH MANAGE. Получатель перелогинивается или ждёт ~60с (JWT refresh).

    Пользователь проверяет вручную:
    1. Открыть https://zoiten.pro/cash — видна таблица операций кассы со всеми колонками + блок итогов (Приход/Расход/Баланс).
    2. Фильтры: выбрать год 2025 → только 2025; Направление=Расход → только EXPENSE; подразделение; категория; ответственный; поиск по назначению.
    3. Проверить авто-категоризацию на нескольких строках (грузчики→Грузчики, картриджи→Канцелярия, выкуп→Выкупы, юля фонд→Пополнение кассы).
    4. Проверить ответственного: пустые в файле → Иванова; нераспознанные фамилии → показывается responsibleNameRaw.
    5. **Форма добавления** (приоритет): «Добавить операцию» → дата=сегодня, направление=Расход, ответственный=Иванова preselected; заполнить → сохранить → запись появилась, баланс пересчитался.
    6. Inline: изменить категорию/комментарий операции → перезагрузить → сохранилось.
    7. Войти под учёткой Ивановой Юлии (или проверить что у неё есть MANAGE) → /cash доступен, форма доступна.
    8. Идемпотентность: повторно прогнать `npx tsx scripts/import-cash-budget.ts` → imported=0.
  </how-to-verify>
  <acceptance_criteria>
    - `prisma migrate deploy` применил 20260610_phase23_cash без ошибок (deploy.sh лог)
    - `SELECT count(*) FROM "CashCategory"` ≥ 24 (категории засеяны)
    - `SELECT count(*) FROM "CashEntry"` > 3000 (импорт 2024-2026 Юля+Павел)
    - Повторный прогон скрипта → imported=0 (идемпотентность через fingerprint)
    - `SELECT * FROM "UserSectionRole" WHERE section='CASH'` — есть строка MANAGE для User Ивановой Юлии (или назначенного пользователя); шаг «спросить кому выдать CASH» выполнен если Иванова не найдена
    - /cash на проде открывается, таблица непустая, итоги показаны
    - Форма добавления работает (дата=сегодня, ответственный=Иванова), запись создаётся
    - Inline category/comment persist после refresh
  </acceptance_criteria>
  <resume-signal>Напишите «approved» или опишите проблемы (неверная категоризация конкретных назначений, дубли, ошибки сумм/дат, нет доступа у Ивановой).</resume-signal>
</task>

</tasks>

<verification>
- Миграция применена на проде; 24 категории + импорт данных
- Идемпотентность подтверждена (повторный импорт imported=0)
- RBAC CASH MANAGE выдан Ивановой Юлии (UserSectionRole)
- /cash на проде: таблица + итоги + форма + фильтры работают
</verification>

<success_criteria>
- Касса группы компаний развёрнута в проде с историческими данными 2024-2026
- Иванова Юлия может вести раздел (MANAGE)
- Удобная форма ручного добавления работает
- Phase 23 complete
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-payments/23-05-SUMMARY.md`.
Phase 23 complete — обновить STATE.md (Phase 23 done) и ROADMAP Phase 23 чекбоксы планов.
</output>
</content>
