---
phase: 23-cash-payments
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - lib/sections.ts
  - components/layout/section-titles.ts
  - components/layout/nav-items.ts
  - lib/section-labels.ts
  - app/(dashboard)/cash/page.tsx
requirements: [CASH-03]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Маршрут /cash защищён RBAC (middleware видит SECTION_PATHS[/cash]=CASH)"
    - "В Header показывается «Наличные расчёты» на /cash"
    - "В Sidebar есть пункт «Наличные расчёты» с иконкой Wallet"
    - "В /admin/users есть тумблер VIEW/MANAGE для раздела CASH (section-labels)"
    - "Стаб /cash рендерится без ошибок (requireSection CASH)"
  artifacts:
    - path: "lib/sections.ts"
      provides: "SECTION_PATHS[/cash]=CASH (middleware route guard)"
      contains: "\"/cash\": \"CASH\""
    - path: "lib/section-labels.ts"
      provides: "SECTION_OPTIONS запись CASH (тумблер прав)"
      contains: "value: \"CASH\""
    - path: "components/layout/nav-items.ts"
      provides: "NAV_ITEMS пункт CASH + Wallet в ICON_MAP"
      contains: "section: \"CASH\""
    - path: "components/layout/section-titles.ts"
      provides: "Заголовок Header для /cash"
      contains: "/cash"
    - path: "app/(dashboard)/cash/page.tsx"
      provides: "Стаб RSC с requireSection(CASH) — заменяется в 23-04"
      contains: "requireSection(\"CASH\")"
  key_links:
    - from: "middleware.ts"
      to: "SECTION_PATHS"
      via: "lib/sections.ts route guard"
      pattern: "\"/cash\""
    - from: "/admin/users тумблер"
      to: "SECTION_OPTIONS"
      via: "lib/section-labels.ts"
      pattern: "CASH"
---

<objective>
Провести новый раздел `/cash` через 6-точечный чеклист ERP_SECTION из CLAUDE.md (маршрут-гард, заголовок Header, пункт Sidebar, тумблер прав в /admin/users) + создать стаб-страницу `/cash` с `requireSection("CASH")`. Стаб заменяется полноценной таблицей в 23-04.

Purpose: Без всех 6 точек раздел будет частично сломан (особенно §5 lib/section-labels.ts — чаще всего забывают, без неё нельзя выдать доступ руками). Эта проводка изолирована от парсера (23-03) → может идти параллельно в Wave 2.
Output: 4 файла проводки + стаб-страница.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cash-payments/23-CONTEXT.md

<interfaces>
<!-- ТОЧНЫЕ текущие contents файлов проводки (извлечено из кодовой базы). -->

lib/sections.ts: объект SECTION_PATHS, последняя запись `"/bank": "BANK",` перед `} as const satisfies ...`. Edge-safe, без @prisma/client.

lib/section-labels.ts: SECTION_OPTIONS массив, есть запись `{ value: "BANK", label: "Банковские счета" },` перед SUPPORT.

components/layout/nav-items.ts: import-блок из lucide-react (есть Landmark, Building2 — добавить Wallet); NAV_ITEMS массив (есть `{ section: "BANK", href: "/bank", label: "Банковские счета", icon: "Building2" },`); ICON_MAP объект (добавить Wallet).

components/layout/section-titles.ts: SECTION_TITLES массив, есть `{ match: /^\/bank/, title: "Банковские счета" },`. Порядок: более специфичные паттерны раньше.

Стаб-страница: образец ComingSoon отсутствует для cash — простой RSC с requireSection + заголовком, заменится в 23-04. requireSection из @/lib/rbac (throw UNAUTHORIZED/FORBIDDEN).

memory/feedback_zoiten_new_section_rbac: после добавления раздела ОБЯЗАТЕЛЬНО провизионить UserSectionRole пользователям — это делается в 23-05 (Иванова Юлия MANAGE), здесь только enum/проводка.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6-точечная проводка — sections.ts + section-labels.ts + nav-items.ts + section-titles.ts</name>
  <read_first>
    - lib/sections.ts (SECTION_PATHS — добавить /cash после /bank)
    - lib/section-labels.ts (SECTION_OPTIONS — добавить CASH после BANK)
    - components/layout/nav-items.ts (import Wallet, NAV_ITEMS пункт, ICON_MAP)
    - components/layout/section-titles.ts (SECTION_TITLES — добавить /cash)
  </read_first>
  <action>
**§2 `lib/sections.ts`** — добавить в SECTION_PATHS после `"/bank": "BANK",`:
```typescript
  "/cash": "CASH",
```

**§5 `lib/section-labels.ts`** (ЧАЩЕ ВСЕГО ЗАБЫВАЮТ — без неё нельзя выдать доступ руками) — добавить в SECTION_OPTIONS после записи BANK:
```typescript
  { value: "CASH",            label: "Наличные расчёты" },
```

**§4 `components/layout/nav-items.ts`**:
  - В import-блоке из "lucide-react" добавить `Wallet,` (рядом с Building2).
  - В NAV_ITEMS после записи BANK добавить:
```typescript
  { section: "CASH", href: "/cash", label: "Наличные расчёты", icon: "Wallet" },
```
  - В ICON_MAP добавить `Wallet,`.

**§3 `components/layout/section-titles.ts`** — добавить в SECTION_TITLES после записи `{ match: /^\/bank/, title: "Банковские счета" },`:
```typescript
  { match: /^\/cash/, title: "Наличные расчёты" },
```

(§1 enum ERP_SECTION.CASH уже сделан в 23-01. §6 карточка дашборда — опционально, пропускаем.)
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sections|section-labels|nav-items|section-titles" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"/cash": "CASH"' lib/sections.ts` находит
    - `grep 'value: "CASH"' lib/section-labels.ts` находит (тумблер прав — критично)
    - `grep 'section: "CASH"' components/layout/nav-items.ts` находит + `grep "Wallet" components/layout/nav-items.ts` находит в import И ICON_MAP (≥2 вхождения)
    - `grep '/cash' components/layout/section-titles.ts` находит с title "Наличные расчёты"
    - tsc чистый по этим 4 файлам
  </acceptance_criteria>
  <done>Все 5 точек чеклиста (кроме enum=23-01 и опц. дашборда) проведены: маршрут-гард, заголовок, пункт sidebar с Wallet, тумблер прав.</done>
</task>

<task type="auto">
  <name>Task 2: Стаб-страница /cash (RSC, requireSection CASH)</name>
  <read_first>
    - app/(dashboard)/bank/page.tsx (образец RSC с requireSection — но это полная страница; для стаба нужен минимум)
    - lib/rbac.ts (requireSection сигнатура)
  </read_first>
  <action>
Создать `app/(dashboard)/cash/page.tsx` — минимальный стаб RSC (заменится полноценной таблицей в 23-04):
```typescript
import { requireSection } from "@/lib/rbac"

export default async function CashPage() {
  await requireSection("CASH")
  return (
    <div className="h-full flex flex-col gap-3">
      <div className="text-sm text-muted-foreground">
        Касса группы компаний — раздел в разработке (таблица и форма добавятся в 23-04).
      </div>
    </div>
  )
}
```
Стаб нужен чтобы маршрут `/cash` существовал и проходил RBAC до завершения 23-04. НЕ добавлять данные/таблицу здесь.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "cash/page" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `app/(dashboard)/cash/page.tsx` существует
    - `grep 'requireSection("CASH")' "app/(dashboard)/cash/page.tsx"` находит
    - tsc чистый по cash/page
  </acceptance_criteria>
  <done>Маршрут /cash существует, защищён requireSection(CASH), рендерит плейсхолдер.</done>
</task>

</tasks>

<verification>
- Все 6 точек чеклиста покрыты (enum в 23-01, остальные здесь); §5 section-labels проверена явно
- tsc чистый
- /cash маршрут защищён RBAC
</verification>

<success_criteria>
- Раздел /cash виден в Sidebar (Wallet), Header показывает «Наличные расчёты»
- Тумблер VIEW/MANAGE для CASH доступен в /admin/users
- Стаб-страница проходит RBAC
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-payments/23-02-SUMMARY.md`.
</output>
</content>
