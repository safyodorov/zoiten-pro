---
phase: 23-cash-payments
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - app/actions/cash.ts
  - lib/cash-labels.ts
  - components/cash/CashEntryForm.tsx
  - components/cash/CashFilters.tsx
  - components/cash/CashTable.tsx
  - app/(dashboard)/cash/page.tsx
requirements: [CASH-08, CASH-09, CASH-10]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Пользователь открывает /cash и видит sticky-таблицу операций кассы с итогами приход/расход/баланс"
    - "Кнопка «Добавить операцию» открывает удобную форму (дата=сегодня, направление, сумма, подразделение, категория, назначение, ответственный=Иванова, комментарий)"
    - "createCashEntry создаёт запись, после — список обновляется"
    - "Фильтры год/подразделение/категория/ответственный/направление + поиск по назначению сужают список"
    - "Inline-редактирование категории/комментария сохраняется (MANAGE)"
    - "При усечении (rows.length === 1000 из totalCount > 1000) пользователь видит индикатор «Показаны первые 1000 из N — уточните фильтры»"
  artifacts:
    - path: "app/actions/cash.ts"
      provides: "createCashEntry/updateCashEntry/deleteCashEntry/categorizeCashEntry/updateCashComment"
      contains: "requireSection(\"CASH\", \"MANAGE\")"
    - path: "components/cash/CashEntryForm.tsx"
      provides: "Диалог удобного ручного ввода (приоритет)"
      contains: "createCashEntry"
    - path: "components/cash/CashTable.tsx"
      provides: "Sticky-таблица + inline category/comment + итоги + индикатор усечения"
      contains: "border-separate border-spacing-0"
    - path: "components/cash/CashFilters.tsx"
      provides: "URL-driven фильтры год/подразделение/категория/ответственный/направление + поиск"
      contains: "useSearchParams"
    - path: "app/(dashboard)/cash/page.tsx"
      provides: "RSC: requireSection + where-builder + итоги + count(totalCount) + render"
      contains: "cashEntry.findMany"
  key_links:
    - from: "CashEntryForm"
      to: "createCashEntry"
      via: "server action + router.refresh"
      pattern: "createCashEntry"
    - from: "CashTable inline cells"
      to: "categorizeCashEntry / updateCashComment"
      via: "useTransition"
      pattern: "categorizeCashEntry"
    - from: "page.tsx"
      to: "prisma.cashEntry"
      via: "findMany с where + include category/responsibleEmployee + groupBy для итогов + count для totalCount"
      pattern: "cashEntry.findMany"
---

<objective>
Создать server actions кассы (`app/actions/cash.ts`), **удобную форму ручного добавления операции** (главный приоритет фазы) и read-only sticky-таблицу `/cash` с фильтрами и итогами (приход/расход/баланс). Заменить стаб 23-02.

Purpose: Завершает пользовательскую функциональность раздела. Зависит от схемы (23-01), проводки (23-02), категорий из БД (23-03 их использует, но опции категорий тут читаются из CashCategory напрямую). Зеркалит паттерны Phase 22 bank (server actions credits.ts, sticky CreditsTable/BankTransactionsTable, filters BankFilters).
Output: server actions + labels + 3 client-компонента + RSC page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cash-payments/23-CONTEXT.md
@.planning/phases/22-bank-accounts/22-05-PLAN.md

<interfaces>
<!-- Точные паттерны Phase 21/22 (извлечены из кодовой базы). Зеркалить, не изобретать. -->

Server actions (app/actions/credits.ts образец):
  "use server" + requireSection("CASH","MANAGE") + zod parse + try/catch + handleAuthError + revalidatePath("/cash").
  handleAuthError: e.message "UNAUTHORIZED"→"Не авторизован", "FORBIDDEN"→"Нет доступа".
  ActionResult = { ok:true } | { ok:false; error:string }; CreateResult = { ok:true; id } | { ok:false; error }.
  zod 4.x + RHF: использовать z.number() с valueAsNumber (НЕ z.coerce.number — несовместимо с RHF 7.72), zodResolver as any (паттерн Phase 21 LoanModal).

requireSection/getSectionRole из @/lib/rbac. getSectionRole("CASH")==="MANAGE".

Sticky-table CLAUDE.md (BankTransactionsTable образец): <div overflow-auto h-full> → <table border-separate border-spacing-0> → <thead bg-background> → <tr> прямой HTML → <th sticky top-0 z-20 bg-background border-b>. Body shadcn TableBody/TableRow/TableCell. СПЛОШНОЙ bg-background (НЕ /80) на sticky. Контейнер flex-1 min-h-0.

Inline CategoryCell (BankTransactionsTable образец): native <select> + useState(value) + useTransition; onChange → setValue + startTransition(categorizeCashEntry(id,next)); !ok → toast.error + откат. CommentCell: <input> + onBlur commit + Enter/Esc + откат.

URL-фильтры (BankFilters образец): useSearchParams + router.push + buildUrl(overrides); MultiSelectDropdown inline-копия с Checkbox для multi-value; native <select> для одиночных; debounced search (300ms useRef timer). НЕ localStorage.

Dialog: shadcn @base-ui dialog (components/ui/dialog.tsx). Native <select> для всех select (CLAUDE.md, НЕ base-ui Select). toast из sonner. Время Moscow — дата хранится @db.Date.

CashEntry поля (23-01): date, direction (INCOME/EXPENSE), amount Decimal, department?, categoryId?, purpose, responsibleEmployeeId?, responsibleNameRaw?, comment?, source ('manual' для ручных), fingerprint? (null для ручных — НЕ участвуют в дедупе).
CashCategory: id, name, sortOrder. Employee: id, lastName, firstName.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/actions/cash.ts + lib/cash-labels.ts</name>
  <read_first>
    - app/actions/credits.ts (ТОЧНЫЙ образец server actions: "use server", requireSection MANAGE, zod, handleAuthError, parseDate, revalidatePath, P2025)
    - app/actions/bank.ts (categorizeTx/updateTxComment — образец inline-мутаций)
    - lib/bank-labels.ts (образец pure labels файла без server-импортов)
    - .planning/phases/23-cash-payments/23-CONTEXT.md строки 136-142 (UI/RBAC спека формы)
  </read_first>
  <action>
**`lib/cash-labels.ts`** (pure, импортируется в client):
```typescript
export const DIRECTION_LABELS: Record<string, string> = { INCOME: "Приход", EXPENSE: "Расход" }
export const DIRECTION_OPTIONS = [
  { value: "EXPENSE", label: "Расход" },  // расход default (касса в основном расходы)
  { value: "INCOME", label: "Приход" },
]
```

**`app/actions/cash.ts`** ("use server"). Зеркало credits.ts:
```typescript
"use server"
import { requireSection } from "@/lib/rbac"
import { prisma } from "@/lib/prisma"
import { z } from "zod"
import { revalidatePath } from "next/cache"

type ActionResult = { ok: true } | { ok: false; error: string }
type CreateResult = { ok: true; id: string } | { ok: false; error: string }

function handleAuthError(e: unknown) {
  if (e instanceof Error) {
    if (e.message === "UNAUTHORIZED") return { ok: false as const, error: "Не авторизован" }
    if (e.message === "FORBIDDEN") return { ok: false as const, error: "Нет доступа" }
  }
  return null
}

const EntrySchema = z.object({
  date: z.string().min(1),                          // ISO YYYY-MM-DD
  direction: z.enum(["INCOME", "EXPENSE"]),
  amount: z.number().positive(),
  department: z.string().max(100).nullable().optional(),
  categoryId: z.string().nullable().optional(),
  purpose: z.string().min(1, "Укажите назначение").max(2000),
  responsibleEmployeeId: z.string().nullable().optional(),
  comment: z.string().max(2000).nullable().optional(),
})
const UpdateEntrySchema = EntrySchema.extend({ id: z.string().min(1) })
```
Экспортировать:
- `createCashEntry(data)` → requireSection MANAGE; parse; prisma.cashEntry.create({ data: { ...parsed, date: new Date(parsed.date), source: "manual", fingerprint: null, responsibleNameRaw: null } }); revalidatePath("/cash"); return { ok:true, id }.
- `updateCashEntry(data)` → requireSection MANAGE; UpdateEntrySchema; prisma.cashEntry.update({ where:{id}, data }); revalidatePath.
- `deleteCashEntry(id)` → requireSection MANAGE; prisma.cashEntry.delete; revalidatePath; P2025 → "Запись не найдена".
- `categorizeCashEntry(id, categoryId | null)` → requireSection MANAGE; update categoryId (пустая строка → null); revalidatePath.
- `updateCashComment(id, comment)` → requireSection MANAGE; update comment (пусто → null); revalidatePath.
Все обёрнуты try/catch + handleAuthError + console.error + return { ok:false, error:"Ошибка сервера" }.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "actions/cash|cash-labels" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'requireSection("CASH", "MANAGE")' app/actions/cash.ts` находит (≥3 раза — все мутации)
    - 5 экспортов: `grep -E "export async function (createCashEntry|updateCashEntry|deleteCashEntry|categorizeCashEntry|updateCashComment)" app/actions/cash.ts` → 5 совпадений
    - `grep 'revalidatePath("/cash")' app/actions/cash.ts` находит
    - `grep 'source: "manual"' app/actions/cash.ts` находит (ручные записи)
    - `grep "handleAuthError" app/actions/cash.ts` находит
    - `lib/cash-labels.ts` содержит DIRECTION_LABELS + DIRECTION_OPTIONS; EXPENSE default первым
    - tsc чистый
  </acceptance_criteria>
  <done>5 server actions защищены MANAGE; ручные записи source=manual/fingerprint=null; cash-labels готов для client.</done>
</task>

<task type="auto">
  <name>Task 2: CashEntryForm (удобный диалог — ПРИОРИТЕТ) + CashFilters (URL-driven)</name>
  <read_first>
    - components/credits/CreditModal или components/bank/BankImportButton (образец Dialog client-компонента с формой + router.refresh)
    - components/bank/BankFilters.tsx (URL-driven фильтры: useSearchParams, buildUrl, MultiSelectDropdown inline, native select, debounced search)
    - app/actions/cash.ts (createCashEntry — из Task 1)
    - lib/cash-labels.ts (DIRECTION_OPTIONS)
  </read_first>
  <action>
**`components/cash/CashEntryForm.tsx`** ("use client") — диалог удобного ручного ввода (ГЛАВНЫЙ ПРИОРИТЕТ — «удобно добавлять»):
  - Props: `categories: {id,name}[]`, `employees: {id, lastName, firstName}[]`, `departments: string[]` (distinct непустые из БД для подсказок).
  - Кнопка-триггер «Добавить операцию» (variant default, иконка Plus) → shadcn Dialog.
  - Поля формы (useState или react-hook-form; форма короткая — допустим простой useState):
    1. **Дата** — `<input type="date">`, default = сегодня (`new Date().toISOString().slice(0,10)`).
    2. **Направление** — native `<select>` из DIRECTION_OPTIONS, default "EXPENSE".
    3. **Сумма** — `<input type="number" step="0.01" min="0">`.
    4. **Подразделение** — native `<select>` с опциями из departments + пустая опция «—» (либо свободный datalist; для v1 select с известными значениями офис/склад/маркетинг/такси + «—»).
    5. **Категория** — native `<select>` из categories (по sortOrder) + пустая «Без категории».
    6. **Назначение** — `<textarea>` (обязательное).
    7. **Ответственный** — native `<select>` из employees (отображать «Фамилия Имя»), **default = Иванова** (найти employee с lastName совпадающим «Иванова» — preselect его id; если нет — первый/пусто).
    8. **Комментарий** — `<textarea>` опционально.
  - submit → `createCashEntry({ date, direction, amount: Number(amount), department: department||null, categoryId: categoryId||null, purpose, responsibleEmployeeId: responsibleEmployeeId||null, comment: comment||null })`.
  - На `ok` → toast.success("Операция добавлена") + закрыть диалог + сбросить форму (date→сегодня, direction→EXPENSE, остальное пусто, responsible→Иванова) + `router.refresh()`.
  - На `!ok` → toast.error(result.error). Валидация клиента: amount>0, purpose непусто (disabled submit иначе).

**`components/cash/CashFilters.tsx`** ("use client") — URL-driven (зеркало BankFilters):
  - Props: `categories: {id,name}[]`, `employees: {id,lastName,firstName}[]`, `departments: string[]`, `years: number[]`.
  - useSearchParams + router.push + buildUrl → "/cash".
  - Фильтры:
    - **Год** — native `<select>` (Все + years DESC) → param `year`.
    - **Направление** — native `<select>` (Все + Приход/Расход) → param `direction`.
    - **Подразделение** — native `<select>` (Все + departments) → param `department`.
    - **Категория** — MultiSelectDropdown (inline-копия с Checkbox, как BankFilters) → param `categories=id1,id2`.
    - **Ответственный** — MultiSelectDropdown → param `responsibles=id1,id2`.
    - **Поиск** по назначению — `<input type="search">` debounced 300ms → param `search`.
    - Кнопка «Сбросить» если есть активные.
  - Каскад не нужен (нет иерархии товаров).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CashEntryForm|CashFilters" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createCashEntry" components/cash/CashEntryForm.tsx` находит + `router.refresh` + `toast`
    - `grep 'type="date"' components/cash/CashEntryForm.tsx` находит + default сегодня (`new Date().toISOString().slice(0,10)`)
    - `grep -i "иванова" components/cash/CashEntryForm.tsx` находит (default ответственный) ИЛИ комментарий поясняющий preselect Ивановой
    - CashEntryForm имеет все 8 полей: date, direction, amount, department, category, purpose, responsible, comment (проверить наличие `<textarea` для purpose и `<select` для direction/category/responsible)
    - `grep "<select" components/cash/CashEntryForm.tsx` находит (native, НЕ base-ui Select)
    - `grep "useSearchParams" components/cash/CashFilters.tsx` + `router.push` находят
    - CashFilters содержит фильтры year/direction/department + MultiSelect categories/responsibles + search: `grep -E "year|direction|department|categories|responsibles|search" components/cash/CashFilters.tsx`
    - tsc чистый по обоим
  </acceptance_criteria>
  <done>Удобная форма добавления (8 полей, дата=сегодня, направление=Расход, ответственный=Иванова) + URL-фильтры год/направление/подразделение/категория/ответственный/поиск.</done>
</task>

<task type="auto">
  <name>Task 3: CashTable (sticky + inline + итоги + индикатор усечения) + RSC page /cash (where-builder + итоги + totalCount + render)</name>
  <read_first>
    - components/bank/BankTransactionsTable.tsx (ТОЧНЫЙ sticky-шаблон + inline CategoryCell/CommentCell + canManage gating + BankTxRow flat-type)
    - app/(dashboard)/bank/page.tsx (RSC: requireSection, getSectionRole, await searchParams, Prisma where-builder, Promise.all, Decimal→Number, date→ISO, flex layout)
    - app/(dashboard)/cash/page.tsx (стаб из 23-02 — заменить)
    - app/actions/cash.ts (categorizeCashEntry/updateCashComment — из Task 1)
  </read_first>
  <action>
**`components/cash/CashTable.tsx`** ("use client") — зеркало BankTransactionsTable:
  - Тип `CashRow` flat сериализуемый: `{ id, date: string, direction, amount: number, department: string|null, categoryId: string|null, categoryName: string|null, purpose, responsibleName: string|null, comment: string|null }`.
  - Props: `rows: CashRow[]`, `categories: {id,name}[]`, `canManage: boolean`, `totals: { income: number; expense: number; balance: number }`, **`totalCount: number`** (полное число записей по where, до take:1000 — для индикатора усечения).
  - **Блок итогов сверху** (над таблицей): Приход (зелёный) `totals.income.toLocaleString("ru-RU",{minimumFractionDigits:2})` ₽ · Расход (красный) totals.expense ₽ · **Баланс** `totals.balance` ₽ (income−expense; цвет по знаку). Это «Остаток/итоги» из CONTEXT.
  - **WARNING 1 — индикатор усечения:** в шапке блока (рядом с итогами/счётчиком) при `rows.length === 1000 && totalCount > 1000` рендерить предупреждение (muted/amber text): «Показаны первые 1000 из {totalCount.toLocaleString("ru-RU")} — уточните фильтры». Если `rows.length < 1000` или `totalCount <= 1000` — не показывать. take:1000 в page.tsx сохраняется; здесь только surfacing.
  - Sticky-таблица СТРОГО CLAUDE.md (как BankTransactionsTable): `<div overflow-auto h-full rounded-lg border>` → `<table border-separate border-spacing-0 text-sm>` → `<thead bg-background>` → `<tr>` → `<th sticky top-0 z-20 bg-background border-b ...>`. Сплошной bg-background на sticky.
  - Колонки: Дата | Направление (бейдж зелёный/красный, DIRECTION_LABELS) | Сумма (зелёная CREDIT/красная — по INCOME/EXPENSE; toLocaleString ru-RU 2dp) | Подразделение | Категория (inline CategoryCell — native select из categories + «Без категории»; canManage) | Назначение (truncate max-w + title) | Ответственный | Комментарий (inline CommentCell — input onBlur; canManage).
  - **CategoryCell**: native `<select value={categoryId ?? ""}>` опции categories + пустая «Без категории» → onChange startTransition(categorizeCashEntry(id, next || null)); !ok → toast + откат. !canManage → `<span>{categoryName ?? "—"}</span>`.
  - **CommentCell**: как BankTransactionsTable CommentCell → updateCashComment(id, value).
  - Пустой rows → плейсхолдер «Нет операций. Добавьте операцию или импортируйте бюджет.».

**`app/(dashboard)/cash/page.tsx`** — заменить стаб:
```typescript
import { requireSection, getSectionRole } from "@/lib/rbac"
import { prisma } from "@/lib/prisma"
import { Prisma } from "@prisma/client"
import { CashEntryForm } from "@/components/cash/CashEntryForm"
import { CashFilters } from "@/components/cash/CashFilters"
import { CashTable } from "@/components/cash/CashTable"
import type { CashRow } from "@/components/cash/CashTable"
```
  - `await requireSection("CASH")`; `canManage = (await getSectionRole("CASH")) === "MANAGE"`.
  - `const sp = await searchParams` (Promise; типы: year, direction, department, categories, responsibles, search).
  - where-builder (Prisma.CashEntryWhereInput):
    - year → `date: { gte: new Date(Date.UTC(y,0,1)), lt: new Date(Date.UTC(y+1,0,1)) }`
    - direction → `direction`
    - department → `department`
    - categories (split ",") → `categoryId: { in }`
    - responsibles (split ",") → `responsibleEmployeeId: { in }`
    - search → `purpose: { contains: search, mode: "insensitive" }`
  - Promise.all:
    - `cashEntry.findMany({ where, include: { category: true, responsibleEmployee: true }, orderBy: { date: "desc" }, take: 1000 })`
    - **`totalCount = await prisma.cashEntry.count({ where })`** (полное число по тому же where — для индикатора усечения; WARNING 1)
    - `cashCategory.findMany({ select:{id,name}, orderBy:{ sortOrder:"asc" } })`
    - `employee.findMany({ select:{id,lastName,firstName}, orderBy:{ lastName:"asc" } })`
    - departments: `cashEntry.findMany({ where:{ department:{ not:null } }, select:{ department:true }, distinct:["department"] })` → map+filter
    - years: `cashEntry.findMany({ select:{ date:true }, orderBy:{ date:"desc" } })` → distinct getUTCFullYear (или groupBy — допустим JS distinct)
    - **итоги (по тому же where, чтобы реагировали на фильтры)**: `cashEntry.groupBy({ by:["direction"], where, _sum:{ amount:true } })` → income = sum INCOME, expense = sum EXPENSE, balance = income−expense (Number(Decimal)).
  - Маппить findMany в `CashRow[]`: date→ISO slice(0,10); amount→Number; categoryName = e.category?.name ?? null; responsibleName = e.responsibleEmployee ? `${lastName} ${firstName}` : e.responsibleNameRaw ?? null (показываем raw если нет FK-матча).
  - Layout: `<div className="h-full flex flex-col gap-3">` → header row (`<div>Операций: {totalCount}</div>` + `{canManage && <CashEntryForm categories employees departments />}`) → `<CashFilters categories employees departments years />` → `<div className="flex-1 min-h-0"><CashTable rows categories canManage totals totalCount={totalCount} /></div>`.
  - **Передать `totalCount` в `<CashTable>`** — иначе индикатор усечения не сработает.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CashTable|cash/page" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "border-separate border-spacing-0" components/cash/CashTable.tsx` находит (sticky pattern)
    - `grep "sticky top-0 z-20 bg-background" components/cash/CashTable.tsx` находит; НЕТ `bg-background/` на sticky: `grep "bg-background/" components/cash/CashTable.tsx` → пусто
    - `grep "categorizeCashEntry" components/cash/CashTable.tsx` + `useTransition` находят
    - `grep -iE "баланс|итог|приход" components/cash/CashTable.tsx` находит (блок итогов)
    - WARNING 1: `grep "totalCount" components/cash/CashTable.tsx` находит (prop) + `grep -E "Показаны первые 1000|первые 1000 из" components/cash/CashTable.tsx` находит (индикатор усечения)
    - `grep '<select' components/cash/CashTable.tsx` находит (inline native CategoryCell)
    - `grep 'requireSection("CASH")' "app/(dashboard)/cash/page.tsx"` + `getSectionRole("CASH")` находят
    - `grep "cashEntry.findMany" "app/(dashboard)/cash/page.tsx"` + `orderBy` date desc находят
    - WARNING 1: `grep "cashEntry.count" "app/(dashboard)/cash/page.tsx"` находит (totalCount) + `grep "totalCount={totalCount}" "app/(dashboard)/cash/page.tsx"` ИЛИ `totalCount` передаётся в CashTable
    - `grep "cashEntry.groupBy" "app/(dashboard)/cash/page.tsx"` находит (итоги по direction) ИЛИ JS-агрегация income/expense
    - `grep "flex-1 min-h-0" "app/(dashboard)/cash/page.tsx"` находит
    - `grep 'mode: "insensitive"' "app/(dashboard)/cash/page.tsx"` находит (поиск)
    - where-builder покрывает year/direction/department/categories/responsibles/search
    - tsc чистый
  </acceptance_criteria>
  <done>/cash рендерит реальные операции кассы со sticky-таблицей, итогами приход/расход/баланс, индикатором усечения при rows.length===1000 (из totalCount), inline-категоризацией+комментарием, кнопкой удобного добавления; 6 фильтров+поиск работают через URL.</done>
</task>

</tasks>

<verification>
- tsc чистый для actions + labels + 3 компонента + page
- Sticky-таблица по CLAUDE.md (opaque bg, border-separate, flex-1 min-h-0)
- Форма добавления — приоритет: 8 полей, дата=сегодня, направление=Расход, ответственный=Иванова
- Итоги приход/расход/баланс реагируют на фильтры (groupBy по where)
- totalCount через count(where) → индикатор «Показаны первые 1000 из N» при усечении
- Inline category/comment + canManage gating
- Все мутации requireSection CASH MANAGE
</verification>

<success_criteria>
- /cash — рабочая таблица кассы с балансом
- Удобная форма ручного ввода (главный приоритет)
- Фильтры год/подразделение/категория/ответственный/направление + поиск
- Индикатор усечения при >1000 записях (take:1000 + totalCount)
- Inline-редактирование категории и комментария
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-payments/23-04-SUMMARY.md`.
</output>
</content>
