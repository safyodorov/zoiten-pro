---
phase: 23-cash-payments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260610_phase23_cash/migration.sql
requirements: [CASH-01, CASH-02]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Схема Prisma содержит CashEntry, CashCategory, enum CashDirection, Employee.cashEntries relation, ERP_SECTION.CASH"
    - "Миграция применима через prisma migrate deploy (manual SQL, без migrate dev)"
    - "Миграция сидит 24 строки CashCategory с sortOrder 1..24 (Прочее = 24)"
    - "prisma generate проходит без ошибок"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Модели CashEntry/CashCategory + enum CashDirection + ERP_SECTION.CASH"
      contains: "model CashEntry"
    - path: "prisma/migrations/20260610_phase23_cash/migration.sql"
      provides: "Raw SQL: ALTER TYPE + CREATE TYPE + 2 CREATE TABLE + FK + 24 INSERT CashCategory"
      contains: "ALTER TYPE \"ERP_SECTION\" ADD VALUE IF NOT EXISTS 'CASH'"
  key_links:
    - from: "CashEntry.categoryId"
      to: "CashCategory.id"
      via: "FK onDelete SetNull"
      pattern: "CashEntry_categoryId_fkey"
    - from: "CashEntry.responsibleEmployeeId"
      to: "Employee.id"
      via: "FK onDelete SetNull"
      pattern: "CashEntry_responsibleEmployeeId_fkey"
---

<objective>
Создать схему данных кассы группы компаний: модели `CashEntry` + `CashCategory`, enum `CashDirection { INCOME, EXPENSE }`, добавить `CASH` в enum `ERP_SECTION`, back-relation `Employee.cashEntries`. Написать ручную SQL-миграцию (как делается в проекте — без локальной PostgreSQL, применяется `prisma migrate deploy` на VPS) с CREATE TABLE + FK + сидингом 24 строк `CashCategory`.

Purpose: Фундамент для импорта (23-03), server actions/UI (23-04) и проводки раздела (23-02). Без схемы остальные планы не могут компилироваться.
Output: Изменения в `prisma/schema.prisma` + новый файл миграции `prisma/migrations/20260610_phase23_cash/migration.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cash-payments/23-CONTEXT.md

<interfaces>
<!-- Точные паттерны проекта, извлечённые из кодовой базы. Используй ИХ, не исследуй заново. -->

ERP_SECTION enum (prisma/schema.prisma строки 21-35) УЖЕ содержит BANK последним:
  PRODUCTS PRICES WEEKLY_CARDS STOCK COST PROCUREMENT SALES SUPPORT EMPLOYEES ADS USER_MANAGEMENT CREDITS BANK
  → добавить CASH в конец списка.

Employee model (строки 530-551) — добавить back-relation `cashEntries CashEntry[]` рядом с supplierBuyers/negotiationParticipations.

SQL-миграция: проект НЕ использует prisma migrate dev (нет локальной PG). Создаётся файл prisma/migrations/{date}_{name}/migration.sql вручную, применяется prisma migrate deploy на VPS (deploy.sh). Образцы синтаксиса:
- ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CASH';   (из 20260609_phase21_credits)
- CREATE TYPE "CashDirection" AS ENUM ('INCOME', 'EXPENSE');  (из 20260610_phase22_bank: CREATE TYPE ... AS ENUM)
- id: TEXT NOT NULL + CONSTRAINT "X_pkey" PRIMARY KEY ("id")
- @db.Date → DATE NOT NULL; деньги → DECIMAL(14, 2); @db.Text → TEXT; @default(now()) → TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP; @updatedAt → TIMESTAMP(3) NOT NULL (без default — Prisma управляет)
- @unique → CREATE UNIQUE INDEX "X_field_key" ON "X"("field");
- @@index([f]) → CREATE INDEX "X_f_idx" ON "X"("f");
- FK: ALTER TABLE "X" ADD CONSTRAINT "X_y_fkey" FOREIGN KEY ("y") REFERENCES "Y"("id") ON DELETE SET NULL ON UPDATE CASCADE;

Схема CashEntry/CashCategory — точно из 23-CONTEXT.md §Схема (строки 80-117). Decimal(14,2) для amount (паттерн Phase 21 денег; bank использует 18,2 но касса — рубли, 14,2 достаточно как в Credits).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema — CashDirection + CashCategory + CashEntry + ERP_SECTION.CASH + Employee relation</name>
  <read_first>
    - prisma/schema.prisma строки 21-35 (enum ERP_SECTION — добавить CASH), 530-551 (Employee — добавить cashEntries), и конец файла (куда добавить новые модели — после BankTransaction блока ~1567+)
    - .planning/phases/23-cash-payments/23-CONTEXT.md строки 80-117 (точная схема CashEntry/CashCategory)
    - prisma/schema.prisma строки 1567-1585 (model BankTransaction — образец стиля Decimal/@db.Date/@db.Text/relation/@@index)
  </read_first>
  <action>
В `prisma/schema.prisma`:

1. **enum ERP_SECTION** (строки 21-35) — добавить `CASH` после `BANK`:
```prisma
enum ERP_SECTION {
  ... CREDITS
  BANK
  CASH
}
```

2. **Новый enum** рядом с другими enum (например после AbcStatus или рядом с TxDirection):
```prisma
enum CashDirection {
  INCOME  // приход
  EXPENSE // расход
}
```

3. **model Employee** (строки 530-551) — добавить back-relation в блок Phase 20 back-relations:
```prisma
  // Phase 23: cash back-relation
  cashEntries CashEntry[]
```

4. **Новые модели** в конце файла (после BankTransaction-блока). Точно по 23-CONTEXT §Схема:
```prisma
model CashCategory {
  id        String      @id @default(cuid())
  name      String      @unique
  sortOrder Int         @default(0)
  entries   CashEntry[]
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
}

model CashEntry {
  id                    String        @id @default(cuid())
  date                  DateTime      @db.Date
  direction             CashDirection
  amount                Decimal       @db.Decimal(14, 2)
  department            String?
  categoryId            String?
  category              CashCategory? @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  purpose               String        @db.Text
  responsibleEmployeeId String?
  responsibleEmployee   Employee?     @relation(fields: [responsibleEmployeeId], references: [id], onDelete: SetNull)
  responsibleNameRaw    String?
  comment               String?       @db.Text
  source                String        @default("manual") // 'budget-yulya' | 'budget-pavel' | 'manual'
  fingerprint           String?       @unique
  createdAt             DateTime      @default(now())
  updatedAt             DateTime      @updatedAt

  @@index([date])
  @@index([categoryId])
  @@index([responsibleEmployeeId])
  @@index([direction])
}
```

После правки запустить `npx prisma generate` чтобы сгенерировать клиент (downstream-планы импортируют CashEntry/CashDirection типы).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "  CASH" prisma/schema.prisma` ≥ 1 (значение enum)
    - `grep "model CashEntry" prisma/schema.prisma` находит; `grep "model CashCategory" prisma/schema.prisma` находит
    - `grep "enum CashDirection" prisma/schema.prisma` находит с INCOME и EXPENSE
    - `grep "cashEntries CashEntry\[\]" prisma/schema.prisma` находит (Employee relation)
    - `grep "@db.Decimal(14, 2)" prisma/schema.prisma` находит в блоке CashEntry (amount)
    - `grep "fingerprint           String?       @unique" prisma/schema.prisma` ИЛИ `grep "fingerprint" prisma/schema.prisma` находит @unique
    - `npx prisma generate` завершается без ошибок (валидная схема)
  </acceptance_criteria>
  <done>Схема содержит обе модели + enum + relation + ERP_SECTION.CASH; prisma generate проходит; клиент сгенерирован.</done>
</task>

<task type="auto">
  <name>Task 2: Raw SQL миграция 20260610_phase23_cash (CREATE TABLE + FK + сид 24 CashCategory)</name>
  <read_first>
    - prisma/migrations/20260610_phase22_bank/migration.sql (ТОЧНЫЙ образец: ALTER TYPE, CREATE TYPE enum, CREATE TABLE с id TEXT/DATE/DECIMAL/TEXT, CREATE INDEX, ALTER TABLE ADD CONSTRAINT FK с ON DELETE SET NULL)
    - prisma/migrations/20260609_phase21_credits/migration.sql (образец ALTER TYPE ERP_SECTION ADD VALUE + Decimal(14,2))
    - .planning/phases/23-cash-payments/23-CONTEXT.md строки 33-64 (таксономия 24 категорий + порядок sortOrder для сида)
  </read_first>
  <action>
Создать `prisma/migrations/20260610_phase23_cash/migration.sql`. Структура (зеркало 22-bank):

```sql
-- Phase 23: Наличные расчёты — ERP_SECTION.CASH + CashDirection + CashCategory + CashEntry
-- Применяется через `prisma migrate deploy` на VPS (Plan 23-05).

-- 1. Новое значение enum ERP_SECTION
ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CASH';

-- 2. Новый enum направления
CREATE TYPE "CashDirection" AS ENUM ('INCOME', 'EXPENSE');

-- 3. CashCategory (справочник, редактируемый)
CREATE TABLE "CashCategory" (
  "id"        TEXT NOT NULL,
  "name"      TEXT NOT NULL,
  "sortOrder" INTEGER NOT NULL DEFAULT 0,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL,
  CONSTRAINT "CashCategory_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "CashCategory_name_key" ON "CashCategory"("name");

-- 4. CashEntry (приход/расход, fingerprint @unique дедуп, amount Decimal(14,2))
CREATE TABLE "CashEntry" (
  "id"                    TEXT NOT NULL,
  "date"                  DATE NOT NULL,
  "direction"             "CashDirection" NOT NULL,
  "amount"                DECIMAL(14, 2) NOT NULL,
  "department"            TEXT,
  "categoryId"            TEXT,
  "purpose"               TEXT NOT NULL,
  "responsibleEmployeeId" TEXT,
  "responsibleNameRaw"    TEXT,
  "comment"               TEXT,
  "source"                TEXT NOT NULL DEFAULT 'manual',
  "fingerprint"           TEXT,
  "createdAt"             TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt"             TIMESTAMP(3) NOT NULL,
  CONSTRAINT "CashEntry_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "CashEntry_fingerprint_key" ON "CashEntry"("fingerprint");
CREATE INDEX "CashEntry_date_idx" ON "CashEntry"("date");
CREATE INDEX "CashEntry_categoryId_idx" ON "CashEntry"("categoryId");
CREATE INDEX "CashEntry_responsibleEmployeeId_idx" ON "CashEntry"("responsibleEmployeeId");
CREATE INDEX "CashEntry_direction_idx" ON "CashEntry"("direction");

ALTER TABLE "CashEntry"
  ADD CONSTRAINT "CashEntry_categoryId_fkey"
  FOREIGN KEY ("categoryId") REFERENCES "CashCategory"("id")
  ON DELETE SET NULL ON UPDATE CASCADE;
ALTER TABLE "CashEntry"
  ADD CONSTRAINT "CashEntry_responsibleEmployeeId_fkey"
  FOREIGN KEY ("responsibleEmployeeId") REFERENCES "Employee"("id")
  ON DELETE SET NULL ON UPDATE CASCADE;

-- 5. Сид 24 категорий (sortOrder 1..24, Прочее = 24). Идемпотентно — ON CONFLICT DO NOTHING.
INSERT INTO "CashCategory" ("id", "name", "sortOrder", "updatedAt") VALUES
  (gen_random_uuid(), 'Грузчики/разнорабочие',         1,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Упаковка',                       2,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Выкупы товаров',                 3,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Образцы/Китай',                  4,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Курьеры/доставка',               5,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Фриланс/подрядчики',             6,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Зарплата/авансы',                7,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Вода',                           8,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Продукты/кухня',                 9,  CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Связь',                          10, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Такси/транспорт',                11, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Канцелярия/оргтехника',          12, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Клининг/уборка',                 13, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Реклама/маркетинг',              14, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Подбор персонала',               15, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Маркировка/ЧЗ',                  16, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Подарки/цветы',                  17, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Кэшбек',                         18, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Аренда',                         19, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Налоги/банк/сборы',              20, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Управленческий учёт/услуги',     21, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Возврат займов',                 22, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Пополнение кассы',               23, CURRENT_TIMESTAMP),
  (gen_random_uuid(), 'Прочее',                         24, CURRENT_TIMESTAMP)
ON CONFLICT ("name") DO NOTHING;
```

ВАЖНО:
- `gen_random_uuid()` доступен в проде (pgcrypto использовался в Phase 12 миграции). Если нет — fallback на md5(random()::text). Используем gen_random_uuid().
- Имена категорий ТОЧНО совпадают с массивом, который categorize.ts (23-03) возвращает — это критично для маппинга categoryId по имени при импорте. Список имён (24): Грузчики/разнорабочие, Упаковка, Выкупы товаров, Образцы/Китай, Курьеры/доставка, Фриланс/подрядчики, Зарплата/авансы, Вода, Продукты/кухня, Связь, Такси/транспорт, Канцелярия/оргтехника, Клининг/уборка, Реклама/маркетинг, Подбор персонала, Маркировка/ЧЗ, Подарки/цветы, Кэшбек, Аренда, Налоги/банк/сборы, Управленческий учёт/услуги, Возврат займов, Пополнение кассы, Прочее.
- НЕ запускать миграцию локально (нет PG) — она применится в 23-05 на VPS через deploy.sh.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && node -e "const s=require('fs').readFileSync('prisma/migrations/20260610_phase23_cash/migration.sql','utf8'); const n=(s.match(/gen_random_uuid\(\)/g)||[]).length; console.log('inserts:', n); process.exit(s.includes(\"ADD VALUE IF NOT EXISTS 'CASH'\") && s.includes('CREATE TABLE \"CashEntry\"') && s.includes('CREATE TABLE \"CashCategory\"') && n===24 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `prisma/migrations/20260610_phase23_cash/migration.sql` существует
    - содержит `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CASH'`
    - содержит `CREATE TYPE "CashDirection" AS ENUM ('INCOME', 'EXPENSE')`
    - содержит `CREATE TABLE "CashCategory"` и `CREATE TABLE "CashEntry"`
    - содержит оба FK: `CashEntry_categoryId_fkey` и `CashEntry_responsibleEmployeeId_fkey` с `ON DELETE SET NULL`
    - содержит `CREATE UNIQUE INDEX "CashEntry_fingerprint_key"`
    - ровно 24 `gen_random_uuid()` (24 INSERT строки) + `ON CONFLICT ("name") DO NOTHING`
    - имя 'Прочее' имеет sortOrder 24 (последнее)
  </acceptance_criteria>
  <done>SQL-миграция применима через migrate deploy, создаёт обе таблицы + FK + сидит 24 категории идемпотентно.</done>
</task>

</tasks>

<verification>
- prisma generate проходит (валидная схема)
- Миграция содержит все DDL + 24 INSERT с правильными именами/sortOrder
- Имена категорий идентичны выходу categorize.ts (23-03) — связка categoryId по имени
- НЕ применяется локально — deploy на VPS в 23-05
</verification>

<success_criteria>
- Схема: CashEntry + CashCategory + CashDirection + Employee.cashEntries + ERP_SECTION.CASH
- Миграция готова к prisma migrate deploy
- 24 категории засеяны (Прочее последняя)
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-payments/23-01-SUMMARY.md`.
</output>
</content>
