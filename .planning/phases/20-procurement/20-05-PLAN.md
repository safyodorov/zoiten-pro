---
phase: 20-procurement
plan: 05
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - lib/supplier-primary.ts
  - app/actions/suppliers.ts
  - app/(dashboard)/procurement/page.tsx
  - app/(dashboard)/procurement/suppliers/page.tsx
  - app/(dashboard)/procurement/suppliers/[id]/page.tsx
  - components/procurement/SuppliersTable.tsx
  - components/procurement/ProcurementFilters.tsx
  - components/procurement/SupplierModal.tsx
  - components/procurement/SupplierContactsTab.tsx
  - components/procurement/SupplierProductsTab.tsx
  - components/procurement/NegotiationsTab.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-10, D-11, D-12, D-13, D-14, D-15, D-17, D-18, D-20]
must_haves:
  truths:
    - "/procurement redirects to /procurement/suppliers"
    - "Supplier list is a sticky raw-HTML table with cascading filters Закупщик/Бренд/Категория/Подкатегория"
    - "Buyer selector shows frequently-used buyers (distinct buyerEmployeeId) on top, then full employee search"
    - "Supplier detail page has tabs Контакты / Товары / Переговоры"
    - "Only one isPrimary=true per (supplierId, type) is ever persisted"
    - "NegotiationParticipant requires exactly one of employee / supplierContact / custom"
    - "Soft-deleted suppliers are excluded from active lists"
  artifacts:
    - path: "lib/supplier-primary.ts"
      provides: "resolvePrimaryWrites pure helper (GREEN for supplier-actions test)"
      exports: ["resolvePrimaryWrites"]
    - path: "app/actions/suppliers.ts"
      provides: "CRUD Supplier + nested contacts/links/negotiations + soft delete"
      exports: ["createSupplier", "updateSupplier", "softDeleteSupplier", "saveSupplierProductLinks", "saveNegotiation"]
    - path: "components/procurement/SuppliersTable.tsx"
      provides: "sticky list table"
      contains: "overflow-auto"
  key_links:
    - from: "app/actions/suppliers.ts"
      to: "lib/supplier-primary.ts"
      via: "resolvePrimaryWrites before upserting contacts"
      pattern: "resolvePrimaryWrites"
    - from: "components/procurement/SupplierModal.tsx"
      to: "app/actions/suppliers.ts"
      via: "createSupplier/updateSupplier"
      pattern: "createSupplier|updateSupplier"
    - from: "app/(dashboard)/procurement/suppliers/page.tsx"
      to: "Supplier table"
      via: "prisma.supplier.findMany where deletedAt null"
      pattern: "deletedAt: null"
---

<objective>
Build the Suppliers subsection: server actions (CRUD with nested contacts, product links, negotiations; isPrimary + polymorphic-participant enforcement; soft delete), the `/procurement` redirect, the supplier list with cascading filters, and the supplier detail page with three tabs. Turns the 20-00 supplier-actions test GREEN via a pure `lib/supplier-primary.ts` helper.

Purpose: Suppliers are the reference data Purchases consume.
Output: 1 lib helper + 1 actions file + 3 pages + 6 components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-procurement/20-CONTEXT.md
@.planning/phases/20-procurement/20-RESEARCH.md
@CLAUDE.md
@tests/supplier-actions.test.ts
<interfaces>
Reusable, confirmed paths:
- CreatableCombobox: components/combobox/CreatableCombobox.tsx
- MultiSelectDropdown: components/ui/multi-select-dropdown.tsx
- Dialog: components/ui/dialog.tsx
- PRODUCT_HIERARCHY_ORDER_BY + compareProductsByHierarchy: lib/product-order.ts
- requireSection: lib/rbac.ts
- ProductFilters cascade canonical: components/products/ProductFilters.tsx
- Employee CRUD template: app/actions/employees.ts (createEmployee/updateEmployee/deleteEmployee + nested upsert/deleteMany-notIn)
- Modal multi-section form template: components/employees/EmployeeModal.tsx
- Decimal→client: convert pct/unitPrice via .toNumber() before passing to client (RESEARCH Pattern 9)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/supplier-primary.ts + app/actions/suppliers.ts</name>
  <read_first>
    - tests/supplier-actions.test.ts (RED — implement resolvePrimaryWrites to satisfy it)
    - app/actions/employees.ts:137-330 (createEmployee/updateEmployee nested upsert + deleteMany({notIn: keepIds}) inside $transaction; ActionResult/CreateResult type)
    - app/actions/products.ts (soft-delete server action: deletedAt = new Date(); revalidatePath)
    - 20-RESEARCH.md §"Pattern 3: Nested CRUD" (lines 184-215) + §"Pitfall 4 isPrimary" (472-475) + §"Pitfall 6 polymorphic participant" (482-485)
    - 20-CONTEXT.md D-01..D-04 (lines 35-72) — exact field lists
    - lib/rbac.ts (requireSection signature)
  </read_first>
  <files>lib/supplier-primary.ts, app/actions/suppliers.ts</files>
  <action>
    1. lib/supplier-primary.ts — pure helper (NO imports). `resolvePrimaryWrites(contacts: Array<{id?:string, supplierId:string, type:"SUPPLIER_MANAGER"|"SUPPLIER_BOSS", isPrimary:boolean}>)`: group by (supplierId,type); within each group, if any flagged isPrimary=true keep ONLY the last-flagged as primary (others → false); if none flagged, all false. Return the same array with corrected isPrimary. Guarantees ≤1 primary per group (D-02). This satisfies tests/supplier-actions.test.ts.

    2. app/actions/suppliers.ts — `"use server"`. All write actions start with `await requireSection("PROCUREMENT","MANAGE")`, wrap in try/catch, `revalidatePath("/procurement/suppliers")` (+ `/procurement/suppliers/[id]` via revalidatePath of the detail path). Export:
       - `createSupplier(input)` / `updateSupplier(id, input)`: `$transaction` upsert Supplier (nameForeign, nameEnglish, buyerEmployeeId, cooperationSummary). For nested contacts: run `resolvePrimaryWrites` on the incoming contacts, then deleteMany({supplierId, id notIn keepIds}) + upsert each (D-02; preferredContactCustom required if preferredContact==="OTHER" — Zod refine).
       - `saveSupplierProductLinks(supplierId, links[])`: upsert SupplierProductLink rows; productId nullable (productNameFallback used when null); decimals (unitPrice 14,4; depositPct/balancePct/deferralPct 5,2). deleteMany notIn keepIds.
       - `saveNegotiation(supplierId, input)` / `deleteNegotiation(id)`: upsert Negotiation + NegotiationProduct (M:N) + NegotiationParticipant. Enforce exactly-one-of (employeeId | supplierContactId | customName+customRole) per participant — `if ([employeeId, supplierContactId, customName].filter(Boolean).length !== 1) throw`. (D-04)
       - `softDeleteSupplier(id)`: set deletedAt = new Date(); do NOT cascade to children; Purchase rows untouched (D-20). `restoreSupplier(id)` optional (deletedAt=null).
       Use Zod schemas in this file (server-action local) for input validation; `z.number()` not `z.coerce`.
  </action>
  <verify>
    <automated>npm run test -- --run tests/supplier-actions.test.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "suppliers.ts|supplier-primary.ts" || echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - lib/supplier-primary.ts exports resolvePrimaryWrites, contains NO `import`
    - tests/supplier-actions.test.ts exits 0 (GREEN)
    - app/actions/suppliers.ts exports createSupplier, updateSupplier, softDeleteSupplier, saveSupplierProductLinks, saveNegotiation
    - app/actions/suppliers.ts contains `requireSection("PROCUREMENT", "MANAGE")` and `resolvePrimaryWrites`
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Supplier CRUD + nested writes implemented; isPrimary helper GREEN; polymorphic participant enforced; soft delete in place.</done>
</task>

<task type="auto">
  <name>Task 2: /procurement redirect + suppliers list page + filters + table</name>
  <read_first>
    - app/(dashboard)/credits/page.tsx — RSC list page + filters + table assembly pattern (Phase 21)
    - components/products/ProductFilters.tsx — cascading filter (Направление→Бренд→Категория→Подкатегория) canonical; reuse cascade logic for Закупщик/Бренд/Категория/Подкатегория
    - components/employees/EmployeesTable.tsx — sticky raw-HTML table pattern
    - CLAUDE.md §"Sticky data-таблицы (pattern)" — raw <table>, opaque bg-background on sticky cells; §"Каскадные фильтры"
    - lib/product-order.ts (compareProductsByHierarchy for product references)
    - 20-CONTEXT.md D-12, D-13, D-18 + Claude's Discretion (suppliers sort buyer ASC)
  </read_first>
  <files>app/(dashboard)/procurement/page.tsx, app/(dashboard)/procurement/suppliers/page.tsx, components/procurement/ProcurementFilters.tsx, components/procurement/SuppliersTable.tsx</files>
  <action>
    1. app/(dashboard)/procurement/page.tsx: RSC that `redirect("/procurement/suppliers")` (next/navigation). Renders nothing itself (Pitfall: don't render).
    2. app/(dashboard)/procurement/suppliers/page.tsx: RSC. `await requireSection("PROCUREMENT")`. Query suppliers `where:{deletedAt:null}` include buyer (Employee), productLinks→product (brand/category/subcategory for filter), contacts (for primary contact preview). Sort buyer name ASC (Discretion). Compute `frequentBuyerIds` via distinct buyerEmployeeId (D-01, RESEARCH Pattern 7) and pass to modal. Convert all Decimal to number before passing to client. Read filters from searchParams (закупщикIds/брендIds/категорияIds/подкатегорияIds) and apply in `where` (buyer via buyerEmployeeId in; brand/category via productLinks.some.product relation). Render <ProcurementFilters> + <SuppliersTable> + a «Новый поставщик» button opening <SupplierModal>.
    3. components/procurement/ProcurementFilters.tsx: client. Cascading MultiSelectDropdown Закупщик / Бренд / Категория / Подкатегория, URL-driven, child outputs narrow under parent + silently drop invalid children on parent change (copy ProductFilters.tsx cascade logic). (D-13)
    4. components/procurement/SuppliersTable.tsx: client. Sticky raw-HTML table (NOT shadcn <Table>): `<div className="overflow-auto h-full">` → `<table className="w-full border-separate border-spacing-0">` → `<thead className="bg-background">` with sticky th `sticky top-0 z-20 bg-background border-b`. Columns: nameEnglish / nameForeign / Закупщик / кол-во товаров / primary-контакт / created. Row click → `/procurement/suppliers/[id]`. Use `<Link prefetch={false}>` (perf gotcha). Solid bg-background/bg-muted on any sticky cell (no /40 alpha).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "procurement/page|suppliers/page|ProcurementFilters|SuppliersTable" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - app/(dashboard)/procurement/page.tsx contains `redirect("/procurement/suppliers")`
    - app/(dashboard)/procurement/suppliers/page.tsx contains `requireSection("PROCUREMENT")` and `deletedAt: null`
    - SuppliersTable.tsx contains `overflow-auto` and `border-separate` and `prefetch={false}` and NO `bg-muted/` alpha on sticky cells
    - ProcurementFilters.tsx contains MultiSelectDropdown usage for 4 filters
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Redirect works; suppliers list renders sticky table with cascading filters; rows link to detail.</done>
</task>

<task type="auto">
  <name>Task 3: Supplier modal + detail page tabs (Контакты / Товары / Переговоры)</name>
  <read_first>
    - components/employees/EmployeeModal.tsx — multi-section dialog form with useFieldArray for nested arrays (contacts pattern); zodResolver as any (Zod4 compat)
    - components/products/ProductForm.tsx — useFieldArray for nested arrays
    - components/combobox/CreatableCombobox.tsx — buyer/product picker
    - app/(dashboard)/credits/[id]/page.tsx — RSC detail page pattern; components/credits/CreditsTabs.tsx — tabs pattern (base-ui data-selected)
    - 20-CONTEXT.md D-02 (ContactMethod order WECHAT/PHONE/ALIBABA/OTHER; OTHER→custom required), D-03, D-04, D-15 (tabs/accordion + auto-resize textarea), specifics lines 225-236 (E.164 phone, cargo/white, exclusivity)
  </read_first>
  <files>app/(dashboard)/procurement/suppliers/[id]/page.tsx, components/procurement/SupplierModal.tsx, components/procurement/SupplierContactsTab.tsx, components/procurement/SupplierProductsTab.tsx, components/procurement/NegotiationsTab.tsx</files>
  <action>
    1. SupplierModal.tsx: client dialog (EmployeeModal pattern). Fields: nameForeign, nameEnglish (UTF-8, no transliteration), buyer selector (CreatableCombobox; frequentBuyerIds rendered on top per D-01), cooperationSummary (auto-resize textarea). Calls createSupplier/updateSupplier. zodResolver(...) as any.
    2. app/(dashboard)/procurement/suppliers/[id]/page.tsx: RSC. requireSection("PROCUREMENT"). Load supplier with contacts, productLinks→product, negotiations→products+participants. Decimal→number. Render header (edit button → SupplierModal, delete → softDeleteSupplier) + tabs.
    3. SupplierContactsTab.tsx: client. List + add/edit contacts. type select (SUPPLIER_MANAGER/SUPPLIER_BOSS), name, phone (E.164 validation regex `^\+[1-9]\d{6,14}$`), preferredContact native <select> in order WECHAT/PHONE/ALIBABA/OTHER; if OTHER → show required preferredContactCustom input; description; isPrimary checkbox (UI hints only one primary per type — server enforces). Save via updateSupplier (contacts array).
    4. SupplierProductsTab.tsx: client. List + add/edit SupplierProductLink. product picker (CreatableCombobox; if not found, productNameFallback text). Per-link fields: leadTimeDays + comment, unitPrice + currency, deliveryType native select (CARGO/WHITE) + comment, exclusivityStatus checkbox + terms, depositPct/balancePct/deferralPct + deferralTerms, inspectionCity/inspectionAddress/inspectionMapUrl (URL only — no lat/lng per resolved decision). Save via saveSupplierProductLinks.
    5. NegotiationsTab.tsx: client. List + add/edit Negotiation: date, goals, summary (nullable), products multi-select (NegotiationProduct), participants list — each participant is one of: employee (CreatableCombobox), supplierContact (select from this supplier's contacts), or custom name+role. Save via saveNegotiation; client mirrors the exactly-one-of-three rule the server enforces.
    All money/percent display: ru-RU locale, thousands space, currency code right (D-18).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "suppliers/\[id\]|SupplierModal|SupplierContactsTab|SupplierProductsTab|NegotiationsTab" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - SupplierModal.tsx calls createSupplier or updateSupplier and renders buyer CreatableCombobox
    - SupplierContactsTab.tsx contains a preferredContact select with options WECHAT/PHONE/ALIBABA/OTHER and conditional preferredContactCustom
    - SupplierProductsTab.tsx contains deliveryType CARGO/WHITE select and inspectionMapUrl field (and NO lat/lng fields)
    - NegotiationsTab.tsx contains participant add with employee/supplierContact/custom branches
    - app/(dashboard)/procurement/suppliers/[id]/page.tsx contains requireSection("PROCUREMENT") and renders 3 tabs
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Detail page renders 3 tabs; modal CRUD works; all D-02/D-03/D-04 fields editable; OTHER-contact custom required.</done>
</task>

</tasks>

<verification>
- tests/supplier-actions.test.ts GREEN
- tsc clean for all edited files
- Manual (20-07 UAT): create supplier, add 2 manager contacts both primary → only one stays primary; add product link with unit price; add negotiation with custom participant
</verification>

<success_criteria>
- D-01/02/03/04/10/11/12/13/14/15/20 implemented for the Suppliers subsection
- Soft delete preserves contacts/links/negotiations and never archives Purchases
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-05-SUMMARY.md`
</output>
