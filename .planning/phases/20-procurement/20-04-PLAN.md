---
phase: 20-procurement
plan: 04
type: execute
wave: 2
depends_on: [20-00, 20-01]
files_modified:
  - lib/cbr-rates.ts
  - app/api/cbr-rate-sync/route.ts
  - app/api/cron/dispatch/route.ts
  - prisma/seed.ts
files_modified_note: "seed.ts only if it is where AppSetting defaults live; otherwise seed via INSERT ON CONFLICT in migration or settings UI"
autonomous: true
requirements: [D-09]
must_haves:
  truths:
    - "fetchCbrRates() parses CBR daily_json.js via plain Node fetch (no curl)"
    - "ratePerUnit = Value / Nominal"
    - "getLatestRate(code) returns most-recent stored rate as fallback when today is missing"
    - "GET /api/cbr-rate-sync upserts all Valute rows into CurrencyRate and updates cbrRateSyncLastRun"
    - "Dispatcher fires cbr-rate-sync at cbrRateSyncCronTime (default 12:00 MSK), forward-only"
    - "tests/cbr-rates.test.ts is GREEN"
  artifacts:
    - path: "lib/cbr-rates.ts"
      provides: "fetchCbrRates, ratePerUnit, getLatestRate"
      exports: ["fetchCbrRates", "ratePerUnit", "getLatestRate"]
    - path: "app/api/cbr-rate-sync/route.ts"
      provides: "GET cron handler upserting CurrencyRate"
      exports: ["GET"]
  key_links:
    - from: "app/api/cron/dispatch/route.ts"
      to: "app/api/cbr-rate-sync/route.ts"
      via: "dynamic import + shouldFireCron branch"
      pattern: "cbr-rate-sync"
    - from: "app/api/cbr-rate-sync/route.ts"
      to: "CurrencyRate table"
      via: "prisma.currencyRate.upsert"
      pattern: "currencyRate.upsert"
---

<objective>
Implement CBR currency-rate sync (D-09): pure-ish `lib/cbr-rates.ts`, a cron route handler, and a dispatcher branch firing at 12:00 MSK (forward-only, no backfill). Turns the 20-00 cbr-rates RED test GREEN.

Purpose: Purchases display foreign-currency amounts converted to RUB via the latest CBR rate.
Output: lib/cbr-rates.ts, app/api/cbr-rate-sync/route.ts, dispatcher branch, 2 new AppSetting keys; tests/cbr-rates.test.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-procurement/20-RESEARCH.md
@tests/cbr-rates.test.ts
@app/api/cron/dispatch/route.ts
@lib/wb-cron-schedule.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/cbr-rates.ts (GREEN the test)</name>
  <read_first>
    - tests/cbr-rates.test.ts (RED test from 20-00 — implement to satisfy)
    - 20-RESEARCH.md §"Pattern 4: CBR Rates Integration" (lines 217-264) — copy CbrValute/CbrResponse interfaces + function bodies verbatim
    - 20-RESEARCH.md §"CBR Rates — Detailed Integration Plan" (lines 499-535) — live response shape, rateToRub = Value/Nominal
    - lib/prisma.ts — PrismaClient type for getLatestRate signature
  </read_first>
  <files>lib/cbr-rates.ts</files>
  <behavior>
    - ratePerUnit({Nominal:10, Value:8.1}) → 0.81; ({Nominal:1, Value:73.2644}) → 73.2644
    - fetchCbrRates(): fetch("https://www.cbr-xml-daily.ru/daily_json.js", {cache:"no-store"}); !res.ok → throw "CBR fetch failed: {status}"; returns parsed CbrResponse
    - getLatestRate(code, prismaClient): currencyRate.findFirst({where:{code}, orderBy:{date:"desc"}}) → {rateToRub, date} | null
  </behavior>
  <action>
    Create lib/cbr-rates.ts. Export interfaces `CbrValute`, `CbrResponse` and functions `fetchCbrRates`, `ratePerUnit`, `getLatestRate` exactly as in RESEARCH Pattern 4 (lines 217-264). Plain Node fetch — NO curl/execSync (CBR has no TLS-fingerprint block, unlike WB v4). `getLatestRate(code: string, prismaClient: PrismaClient)` typed against `@prisma/client`. Run the Wave 0 test until GREEN.
  </action>
  <verify>
    <automated>npm run test -- --run tests/cbr-rates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/cbr-rates.ts exports fetchCbrRates, ratePerUnit, getLatestRate
    - File contains "cbr-xml-daily.ru/daily_json.js" and does NOT contain "execSync" or "curl"
    - `npm run test -- --run tests/cbr-rates.test.ts` exits 0
  </acceptance_criteria>
  <done>cbr-rates helper implemented with plain fetch; test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: cbr-rate-sync route + dispatcher branch + AppSetting defaults</name>
  <read_first>
    - app/api/cron/dispatch/route.ts (full — settings.findMany key.in array; advUpdSyncTime block lines ~52-100 as the template for the new branch; shouldFireCron import line ~10-13)
    - 20-RESEARCH.md §"Pattern 5: Dispatcher Extension" (lines 266-289) + §"CBR Rate Sync Route" (lines 713-761) — copy handler verbatim
    - lib/wb-cron-schedule.ts (shouldFireCron signature + getMskTodayString)
    - prisma/seed.ts (how AppSetting defaults are seeded — `INSERT ... ON CONFLICT DO NOTHING` / upsert pattern); if a seed-AppSetting block exists, add the two keys there
    - MEMORY.md reference_cron_dispatcher.md — single timer + dispatcher pattern (no new systemd unit)
  </read_first>
  <files>app/api/cbr-rate-sync/route.ts, app/api/cron/dispatch/route.ts, prisma/seed.ts</files>
  <action>
    1. Create app/api/cbr-rate-sync/route.ts: GET handler, `runtime="nodejs"`, `maxDuration=60`. Guard `x-cron-secret === process.env.CRON_SECRET` else 403. Call fetchCbrRates(); parse `rateDate = new Date(data.Date)`; loop `Object.values(data.Valute)` and `prisma.currencyRate.upsert({ where:{ date_code:{date:rateDate, code:valute.CharCode} }, create:{...}, update:{...} })` with rateToRub = ratePerUnit(valute). Then upsert AppSetting `cbrRateSyncLastRun = getMskTodayString()`. Return JSON {ok, upserted, rateDate}. Copy verbatim from RESEARCH lines 713-761. (Pitfall 5: if data.Date < today, still upsert idempotently — @@unique([date,code]) makes it safe.)
    2. app/api/cron/dispatch/route.ts: add `"cbrRateSyncCronTime"` and `"cbrRateSyncLastRun"` to the settings key.in array; add a branch (mirror the adv-upd block) after the last existing branch: read `cbrTime = settings.cbrRateSyncCronTime ?? "12:00"`, `cbrLastRun = settings.cbrRateSyncLastRun ?? null`, gate with `shouldFireCron({...})`, dynamic-import `../cbr-rate-sync/route` GET, push `cbr:${status}` / `cbr:error`. Forward-only — no historical backfill.
    3. prisma/seed.ts: add AppSetting seed for `cbrRateSyncCronTime` default `"12:00"` (and optionally `cbrRateSyncLastRun` not seeded — left null) using the existing ON CONFLICT DO NOTHING / upsert idiom. If seed.ts has no AppSetting section, instead append an `INSERT INTO "AppSetting" ... ON CONFLICT DO NOTHING` to the 20-01 migration.sql — but prefer seed.ts to match Phase 7 AppSetting seeding.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "cbr-rate-sync|dispatch/route|cbr-rates" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/cbr-rate-sync/route.ts exists, exports GET, contains "currencyRate.upsert" and "x-cron-secret"
    - app/api/cron/dispatch/route.ts contains "cbrRateSyncCronTime" and a dynamic import of "../cbr-rate-sync/route"
    - prisma/seed.ts (or migration.sql) contains "cbrRateSyncCronTime" with default "12:00"
    - tsc reports no new errors in edited files
  </acceptance_criteria>
  <done>Cron route upserts rates; dispatcher fires it at 12:00 MSK; defaults seeded.</done>
</task>

</tasks>

<verification>
- tests/cbr-rates.test.ts GREEN
- tsc clean for edited files
- Manual (deferred to 20-07 UAT): trigger dispatcher with x-cron-secret → CurrencyRate rows inserted + cbrRateSyncLastRun set
</verification>

<success_criteria>
- D-09 fully implemented: parsing, fallback helper, cron route, dispatcher branch, forward-only
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-04-SUMMARY.md`
</output>
