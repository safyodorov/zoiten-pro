---
phase: 20-procurement
plan: 03
type: tdd
wave: 2
depends_on: [20-00]
files_modified:
  - lib/procurement-math.ts
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "Deposit due date = purchase createdAt + 3 calendar days"
    - "Balance due date = deposit due date + leadTimeDays"
    - "Editing percent recomputes amount; editing amount recomputes percent (no precision drift)"
    - "Purchase total = Σ(quantity × unitPrice)"
    - "tests/procurement-math.test.ts is GREEN"
  artifacts:
    - path: "lib/procurement-math.ts"
      provides: "Pure deposit/balance/amount math for createPurchase + modal recompute"
      exports: ["computeDepositDueDate", "computeBalanceDueDate", "recomputeAmountFromPercent", "recomputePercentFromAmount", "computePurchaseTotal"]
  key_links:
    - from: "lib/procurement-math.ts"
      to: "tests/procurement-math.test.ts"
      via: "satisfies the Wave 0 golden test"
      pattern: "export function computeDepositDueDate"
---

<objective>
Implement the pure procurement math helper that turns the 20-00 RED test GREEN. This is the single source of truth for deposit/balance dates and percent↔amount recompute, used by both `createPurchase` (server, 20-06) and the multi-payment modal (client, 20-06).

Purpose: Pure, deterministic, import-free module usable server- and client-side (mirrors lib/loan-math.ts / lib/pricing-math.ts).
Output: lib/procurement-math.ts; tests/procurement-math.test.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-procurement/20-RESEARCH.md
@tests/procurement-math.test.ts
</context>

<feature>
  <name>Procurement payment math</name>
  <files>lib/procurement-math.ts, tests/procurement-math.test.ts</files>
  <read_first>
    - tests/procurement-math.test.ts (the RED test from 20-00 — implement to satisfy it exactly)
    - 20-RESEARCH.md §"Pattern 6: Pure Payment Math Helper" (lines 291-341) — copy function bodies verbatim
    - lib/loan-math.ts — project's pure-helper style (no imports, named exports, JSDoc)
    - lib/pricing-math.ts — Math.round(n*100)/100 rounding pattern (avoids float drift)
  </read_first>
  <behavior>
    - computeDepositDueDate(createdAt: Date): Date → createdAt + 3 calendar days (mutate a copy, setDate(getDate()+3)). "2026-06-09" → "2026-06-12"; "2026-06-29" → "2026-07-02".
    - computeBalanceDueDate(depositDueDate: Date, leadTimeDays: number): Date → depositDueDate + leadTimeDays. ("2026-06-12", 30) → "2026-07-12".
    - recomputeAmountFromPercent(totalAmount, percent): number → Math.round(totalAmount * percent) / 100. (5000,30)→1500; (3000,33.33)≈999.9.
    - recomputePercentFromAmount(totalAmount, amount): number → if totalAmount===0 return 0; else Math.round((amount/totalAmount)*10000)/100. (5000,1500)→30.
    - computePurchaseTotal(items: {quantity:number, unitPrice:number}[]): number → Σ quantity*unitPrice.
  </behavior>
  <implementation>
    Create lib/procurement-math.ts with the 5 named exports above, bodies copied verbatim from RESEARCH.md Pattern 6 (lines 291-341). Pure functions only — NO imports, NO Prisma, NO Next. Add JSDoc in Russian matching loan-math.ts style. Run the Wave 0 test in --run mode until GREEN. Commit RED→GREEN per TDD (test already exists from 20-00, so this is the GREEN step + optional REFACTOR).
  </implementation>
  <verify>
    <automated>npm run test -- --run tests/procurement-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/procurement-math.ts exists and exports computeDepositDueDate, computeBalanceDueDate, recomputeAmountFromPercent, recomputePercentFromAmount, computePurchaseTotal
    - File contains NO `import` statement (pure module)
    - `npm run test -- --run tests/procurement-math.test.ts` exits 0 (all cases GREEN)
  </acceptance_criteria>
</feature>

<verification>
- tests/procurement-math.test.ts GREEN
- lib/procurement-math.ts is import-free (client+server safe)
</verification>

<success_criteria>
- D-08 formulas implemented and pinned by golden test
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-03-SUMMARY.md`
</output>
