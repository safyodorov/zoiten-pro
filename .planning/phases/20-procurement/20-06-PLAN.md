---
phase: 20-procurement
plan: 06
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03, 20-04]
files_modified:
  - app/actions/purchases.ts
  - app/(dashboard)/procurement/purchases/page.tsx
  - app/(dashboard)/procurement/purchases/[id]/page.tsx
  - components/procurement/PurchasesTable.tsx
  - components/procurement/PurchaseFilters.tsx
  - components/procurement/PurchaseModal.tsx
  - components/procurement/PurchasePaymentsCard.tsx
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-10, D-11, D-12, D-13, D-16, D-17, D-18, D-19, D-21]
must_haves:
  truths:
    - "Creating a purchase auto-generates exactly one Deposit + one Balance via procurement-math (dates + amounts)"
    - "One supplier + one currency per purchase (CNY default)"
    - "Item unitPrice prefills from SupplierProductLink but is editable per purchase"
    - "Editing percent recomputes amount and vice versa, WITHOUT mutating Supplier"
    - "User can add Депозит 2.., Баланс 2.. via add buttons"
    - "Purchase list filters by Статус/Период/Поставщик/Закупщик with status color coding"
    - "OVERDUE is computed live (dueDate < now AND paidDate IS NULL); PAID set manually"
    - "Hard delete allowed only when status === PLANNED"
  artifacts:
    - path: "app/actions/purchases.ts"
      provides: "createPurchase (auto-payments), updatePurchase, payment CRUD, deletePurchase(PLANNED-only)"
      exports: ["createPurchase", "updatePurchase", "deletePurchase", "savePurchasePayments", "markPaymentPaid"]
    - path: "components/procurement/PurchasePaymentsCard.tsx"
      provides: "multi-payment editor with percent↔amount recompute"
      contains: "recomputeAmountFromPercent"
  key_links:
    - from: "app/actions/purchases.ts"
      to: "lib/procurement-math.ts"
      via: "computeDepositDueDate/computeBalanceDueDate/recomputeAmountFromPercent/computePurchaseTotal"
      pattern: "computeDepositDueDate|computePurchaseTotal"
    - from: "components/procurement/PurchaseModal.tsx"
      to: "SupplierProductLink.unitPrice"
      via: "prefill item unit price"
      pattern: "unitPrice"
---

<objective>
Build the Purchases subsection: server actions (createPurchase auto-generating deposit+balance via procurement-math, multi-payment CRUD, status lifecycle, PLANNED-only hard delete), the purchase list with filters + status colors, and the detail page with a multi-payment editor supporting percent↔amount recompute and adding Депозит N / Баланс N.

Purpose: Closes the procurement cycle — track purchases and their payment schedules.
Output: 1 actions file + 2 pages + 4 components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-procurement/20-CONTEXT.md
@.planning/phases/20-procurement/20-RESEARCH.md
@CLAUDE.md
@lib/procurement-math.ts
<interfaces>
- procurement-math (20-03): computeDepositDueDate, computeBalanceDueDate, recomputeAmountFromPercent, recomputePercentFromAmount, computePurchaseTotal
- getLatestRate(code, prisma) from lib/cbr-rates.ts (20-04) — for RUB conversion display
- requireSection from lib/rbac.ts
- CreatableCombobox: components/combobox/CreatableCombobox.tsx; MultiSelectDropdown: components/ui/multi-select-dropdown.tsx; Dialog: components/ui/dialog.tsx
- Status colors (Discretion): PLANNED grey, ACTIVE blue, COMPLETED emerald; PaymentStatus PLANNED grey, PAID emerald, OVERDUE red
- Decimal→client: .toNumber() before crossing RSC→client (RESEARCH Pattern 9)
- getMskTodayString: lib/wb-cron-schedule.ts (display MSK, D-19)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/actions/purchases.ts (createPurchase auto-payments + lifecycle)</name>
  <read_first>
    - 20-RESEARCH.md §"Server Action — createPurchase with auto-payments" (lines 622-711) — copy the transaction structure verbatim
    - lib/procurement-math.ts (20-03 — the functions to call)
    - app/actions/employees.ts:137-330 ($transaction nested upsert + deleteMany notIn pattern)
    - app/actions/products.ts (delete server action; ActionResult type)
    - 20-CONTEXT.md D-05/D-06/D-07/D-08 (lines 75-100) + D-21 (line 143)
    - 20-RESEARCH.md §"Open Questions #2" (lines 787-792) — OVERDUE computed at read time, not cached
    - lib/rbac.ts
  </read_first>
  <files>app/actions/purchases.ts</files>
  <action>
    `"use server"`. Every write: `await requireSection("PROCUREMENT","MANAGE")`, try/catch, `revalidatePath("/procurement/purchases")` + `revalidatePath("/procurement/suppliers")` (Pitfall 8). Export:
    - `createPurchase(input)`: `$transaction` (copy RESEARCH lines 622-711). Create Purchase (status PLANNED, currency input.currency ?? "CNY", supplierId — one supplier D-07, options/logistics fields). Create PurchaseItem rows (productId, quantity, unitPrice Decimal(14,4) — prefilled value passed from client, editable). Compute total via `computePurchaseTotal(items.map(...Number(unitPrice)))`. Resolve depositPct/balancePct/leadTimeDays from input (which the client sourced from SupplierProductLink of the first/selected item; fall back to defaults 30/70/45). depositDue = computeDepositDueDate(purchase.createdAt); balanceDue = computeBalanceDueDate(depositDue, leadTimeDays). createMany TWO PurchasePayment rows: DEPOSIT ordinal 1 (percent=depositPct, amount=recomputeAmountFromPercent(total, depositPct), dueDate=depositDue, status PLANNED) + BALANCE ordinal 1 (balancePct, balanceDue). currency = purchase currency. Return {ok, id}.
    - `updatePurchase(id, input)`: update Purchase scalar fields + status (PLANNED|ACTIVE|COMPLETED) + items (deleteMany notIn + upsert). Recompute payment amounts is NOT automatic — payments are edited separately (D-08: user controls). Do NOT mutate Supplier.
    - `savePurchasePayments(purchaseId, payments[])`: upsert PurchasePayment rows (type DEPOSIT|BALANCE, ordinal, percent?, amount, currency, dueDate, paidDate?, comment). deleteMany notIn keepIds. This is where added Депозит 2 / Баланс 2 land. percent/amount values come pre-reconciled from the client (but server may also recompute amount from percent against current total if percent provided + amount omitted — use recomputeAmountFromPercent). NEVER write back to Supplier/SupplierProductLink.
    - `markPaymentPaid(paymentId, paidDate)`: set status=PAID, paidDate (manual — Defaults #7).
    - `deletePurchase(id)`: load purchase; if status !== "PLANNED" → return {ok:false, error:"Удалять можно только планируемые закупки"} (D-21); else hard delete (cascade PurchaseItem + PurchasePayment via FK).
    Zod validation local; `z.number()` not coerce.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "purchases.ts" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - app/actions/purchases.ts exports createPurchase, updatePurchase, deletePurchase, savePurchasePayments, markPaymentPaid
    - createPurchase calls computeDepositDueDate, computeBalanceDueDate, recomputeAmountFromPercent, computePurchaseTotal
    - deletePurchase contains a status !== "PLANNED" guard
    - file contains `requireSection("PROCUREMENT", "MANAGE")`
    - tsc reports no new errors in this file
  </acceptance_criteria>
  <done>Purchase CRUD with auto deposit+balance, manual extra payments, PAID manual, PLANNED-only hard delete; no Supplier mutation.</done>
</task>

<task type="auto">
  <name>Task 2: purchases list page + filters + sticky table (status colors)</name>
  <read_first>
    - app/(dashboard)/credits/page.tsx — RSC list + filters + table assembly (Phase 21)
    - components/credits/CreditsFilters.tsx + CreditsTable.tsx — status color + filter pattern
    - components/products/ProductFilters.tsx — filter cascade reference
    - CLAUDE.md §"Sticky data-таблицы" (raw HTML, opaque bg) ; §"Форматирование чисел"
    - components/ads/TopSpendingCampaigns.tsx — status badge color pattern
    - 20-CONTEXT.md D-13 (purchases filters Статус/Период/Поставщик/Закупщик), Discretion (purchases sort createdAt DESC; status colors)
    - 20-RESEARCH.md §"Open Questions #2" (OVERDUE live at read time)
  </read_first>
  <files>app/(dashboard)/procurement/purchases/page.tsx, components/procurement/PurchaseFilters.tsx, components/procurement/PurchasesTable.tsx</files>
  <action>
    1. purchases/page.tsx: RSC. requireSection("PROCUREMENT"). Query purchases include supplier(+buyer), items(+product), payments. Sort createdAt DESC. Filters from searchParams: статус (PurchaseStatus multi), период (dateFrom/dateTo on createdAt), поставщик (supplierId in), закупщик (supplier.buyerEmployeeId in). For each purchase compute total (Σ items), paid sum, and live payment OVERDUE flag (any payment dueDate < now && !paidDate && status!==PAID). Convert Decimal→number. Render <PurchaseFilters> + <PurchasesTable> + «Новая закупка» button → <PurchaseModal>.
    2. PurchaseFilters.tsx: client. Статус MultiSelect, период date inputs, Поставщик MultiSelect, Закупщик MultiSelect — URL-driven (D-13).
    3. PurchasesTable.tsx: client. Sticky raw-HTML table. Columns: дата создания (MSK) / поставщик / закупщик / валюта / сумма / статус (color badge: PLANNED grey / ACTIVE blue / COMPLETED emerald) / ближайший платёж dueDate / OVERDUE indicator (red badge if any overdue). Row click → /procurement/purchases/[id]. `prefetch={false}`. Solid bg on sticky cells. ru-RU number format, currency code right (D-18).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "purchases/page|PurchaseFilters|PurchasesTable" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - purchases/page.tsx contains requireSection("PROCUREMENT") and sorts by createdAt desc
    - PurchasesTable.tsx contains `overflow-auto`, `border-separate`, `prefetch={false}`, status badge classes (emerald/blue/grey), and NO `bg-muted/` alpha on sticky cells
    - PurchaseFilters.tsx contains 4 filter controls (status/period/supplier/buyer)
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Purchase list renders with filters and status/overdue color coding; rows link to detail.</done>
</task>

<task type="auto">
  <name>Task 3: PurchaseModal + detail page + multi-payment editor</name>
  <read_first>
    - components/employees/EmployeeModal.tsx — multi-section dialog + useFieldArray (items array); zodResolver as any
    - components/credits/LoanModal.tsx — nested inline rows editor (schedule rows) — closest analog to multi-payment editor
    - lib/procurement-math.ts — recomputeAmountFromPercent/recomputePercentFromAmount for live recompute
    - app/(dashboard)/credits/[id]/page.tsx — RSC detail page pattern
    - lib/cbr-rates.ts getLatestRate — RUB conversion display
    - 20-CONTEXT.md D-06 (unitPrice prefill), D-08 (auto + add payments, percent↔amount), D-16 (vertical card list, inline edit, add buttons), D-21
  </read_first>
  <files>app/(dashboard)/procurement/purchases/[id]/page.tsx, components/procurement/PurchaseModal.tsx, components/procurement/PurchasePaymentsCard.tsx</files>
  <action>
    1. PurchaseModal.tsx: client create/edit dialog. Fields: supplier (single CreatableCombobox — one per purchase D-07), currency native select (default CNY, applies to whole purchase — one currency D-05), optionsDescription/optionsExtraCost, logisticsCost/logisticsComment, status select (edit mode). Items: useFieldArray — each row: product picker, quantity, unitPrice. On product select, PREFILL unitPrice from that supplier's SupplierProductLink.unitPrice (passed in as a productId→link map prop), but field stays editable (D-06). On create, also pass depositPct/balancePct/leadTimeDays resolved from the selected items' SupplierProductLink to createPurchase. Calls createPurchase/updatePurchase.
    2. purchases/[id]/page.tsx: RSC. requireSection("PROCUREMENT"). Load purchase + supplier + items(+product) + payments. Compute total, getLatestRate(currency) for RUB display, Decimal→number. Render header (edit → PurchaseModal; delete → deletePurchase, disabled unless status PLANNED with tooltip), items table, and <PurchasePaymentsCard>.
    3. PurchasePaymentsCard.tsx: client (D-16). Vertical list of payment cards grouped DEPOSIT then BALANCE, ordered by ordinal. Each card: type label (Депозит N / Баланс N), inline-editable percent + amount + dueDate + paidDate + comment + status badge. LIVE recompute: editing percent → amount = recomputeAmountFromPercent(total, percent); editing amount → percent = recomputePercentFromAmount(total, amount) (client-side, instant). «Отметить оплаченным» button → markPaymentPaid. «Добавить депозит» / «Добавить баланс» buttons add a new card with next ordinal. Save all via savePurchasePayments. Display amounts ru-RU + currency code + RUB equivalent (amount × rateToRub) when currency != RUB. NEVER write to Supplier — note in a comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "purchases/\[id\]|PurchaseModal|PurchasePaymentsCard" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - PurchaseModal.tsx calls createPurchase or updatePurchase, renders a single supplier picker and a currency select defaulting to CNY, and prefills item unitPrice from SupplierProductLink
    - PurchasePaymentsCard.tsx imports/uses recomputeAmountFromPercent and recomputePercentFromAmount and has «Добавить депозит»/«Добавить баланс» buttons
    - purchases/[id]/page.tsx contains requireSection("PROCUREMENT") and disables delete unless status PLANNED
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Detail page shows items + multi-payment editor with live percent↔amount recompute and add-payment buttons; create auto-generates deposit+balance; delete gated to PLANNED.</done>
</task>

</tasks>

<verification>
- tsc clean for all edited files
- npm run test (procurement-math still GREEN — consumed by createPurchase)
- Manual (20-07 UAT): create purchase with 1 item → 1 deposit (+3d) + 1 balance (+leadTime) appear with correct amounts; edit deposit percent → amount updates; supplier link unchanged; ACTIVE purchase delete blocked
</verification>

<success_criteria>
- D-05/06/07/08/16/21 implemented for the Purchases subsection
- procurement-math is the single source of payment math (server + client)
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-06-SUMMARY.md`
</output>
