---
phase: 20-procurement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/procurement-math.test.ts
  - tests/cbr-rates.test.ts
  - tests/supplier-actions.test.ts
autonomous: true
requirements: [D-02, D-08, D-09]
must_haves:
  truths:
    - "Three RED test files exist and fail because their target modules do not exist yet"
    - "Tests encode the D-08 deposit/balance date + percent↔amount formulas as executable expectations"
    - "Test for D-09 CBR parsing/fallback exists with mocked fetch"
    - "Test for D-02 isPrimary constraint exists with a mock $transaction"
  artifacts:
    - path: "tests/procurement-math.test.ts"
      provides: "Golden expectations for lib/procurement-math.ts (D-08)"
      contains: "computeDepositDueDate"
    - path: "tests/cbr-rates.test.ts"
      provides: "Expectations for lib/cbr-rates.ts (D-09)"
      contains: "ratePerUnit"
    - path: "tests/supplier-actions.test.ts"
      provides: "Expectation for isPrimary enforcement (D-02)"
      contains: "isPrimary"
  key_links:
    - from: "tests/procurement-math.test.ts"
      to: "lib/procurement-math.ts"
      via: "import from @/lib/procurement-math"
      pattern: "@/lib/procurement-math"
---

<objective>
Create the Wave 0 RED test stubs per 20-VALIDATION.md so that the formulas and contracts for Phase 20 are pinned BEFORE implementation. These tests MUST fail initially (target modules do not exist), turning GREEN in later waves (20-03 procurement-math, 20-04 cbr-rates, 20-05 supplier-actions).

Purpose: Establish Nyquist feedback sampling — every downstream task's `<verify>` references one of these test files.
Output: 3 test files under `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-procurement/20-VALIDATION.md
@.planning/phases/20-procurement/20-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: procurement-math RED test (D-08)</name>
  <read_first>
    - 20-RESEARCH.md §"Procurement Math — Golden Test Setup" (lines 564-620) — copy the test expectations verbatim
    - 20-RESEARCH.md §"Pattern 6: Pure Payment Math Helper" (lines 291-341) — the function signatures under test
    - tests/loan-math.test.ts — canonical vitest golden-test pattern in this project (import from @/lib/..., describe/it/expect, --run flag)
    - vitest.config.ts — confirm `@` alias → project root
  </read_first>
  <files>tests/procurement-math.test.ts</files>
  <action>
    Create `tests/procurement-math.test.ts`. Import from `@/lib/procurement-math`:
    `computeDepositDueDate`, `computeBalanceDueDate`, `recomputeAmountFromPercent`, `recomputePercentFromAmount`, `computePurchaseTotal`.
    Encode these golden cases (copy from RESEARCH.md lines 564-620 verbatim):
    - computeDepositDueDate(new Date("2026-06-09T10:00:00Z")) → ISO date slice "2026-06-12" (createdAt + 3 calendar days, D-08)
    - computeDepositDueDate(new Date("2026-06-29T10:00:00Z")) → "2026-07-02" (month boundary)
    - computeBalanceDueDate(new Date("2026-06-12"), 30) → "2026-07-12" (depositDue + leadTimeDays, D-08)
    - recomputeAmountFromPercent(5000, 30) → 1500 (amount = total × pct / 100)
    - recomputeAmountFromPercent(3000, 33.33) → toBeCloseTo(999.9, 1)
    - recomputePercentFromAmount(5000, 1500) → 30
    - recomputePercentFromAmount(0, 100) → 0 (guard: total=0)
    - computePurchaseTotal([{quantity:10, unitPrice:500}]) → 5000
    Do NOT create lib/procurement-math.ts — this test MUST be RED.
  </action>
  <verify>
    <automated>npm run test -- --run tests/procurement-math.test.ts; if [ $? -ne 0 ]; then echo "RED-as-expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - tests/procurement-math.test.ts exists
    - File contains string "computeDepositDueDate" and "recomputePercentFromAmount"
    - File contains import string "@/lib/procurement-math"
    - `npm run test -- --run tests/procurement-math.test.ts` fails (module not found) — RED confirmed
  </acceptance_criteria>
  <done>Test file exists, references all 5 functions, fails because @/lib/procurement-math does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cbr-rates RED test (D-09)</name>
  <read_first>
    - 20-RESEARCH.md §"Pattern 4: CBR Rates Integration" (lines 217-264) — CbrValute/CbrResponse shapes, ratePerUnit = Value/Nominal, getLatestRate fallback
    - 20-RESEARCH.md §"CBR Rates — Detailed Integration Plan" (lines 499-535) — verified live response shape
    - tests/loan-math.test.ts — vitest pattern; tests/wb-promotions-api.test.ts (if present) for vi.fn() mock-fetch pattern
  </read_first>
  <files>tests/cbr-rates.test.ts</files>
  <action>
    Create `tests/cbr-rates.test.ts`. Import from `@/lib/cbr-rates`: `fetchCbrRates`, `ratePerUnit`, `getLatestRate`.
    Cases:
    - ratePerUnit({CharCode:"CNY", Nominal:10, Value:8.1, ...}) → 0.81 (Value/Nominal, D-09)
    - ratePerUnit({CharCode:"USD", Nominal:1, Value:73.2644, ...}) → 73.2644
    - fetchCbrRates(): mock global.fetch (vi.stubGlobal("fetch", vi.fn())) to resolve a CbrResponse with Date + Valute{CNY,USD}; assert returned object has .Date and .Valute.CNY.CharCode === "CNY"
    - fetchCbrRates() when res.ok=false → rejects with "CBR fetch failed"
    - getLatestRate("CNY", mockPrisma) where mockPrisma.currencyRate.findFirst resolves {rateToRub, date} → returns that row (fallback to latest stored, D-09)
    - getLatestRate("XXX", mockPrisma) where findFirst resolves null → returns null
    Use a minimal mock prisma object: `{ currencyRate: { findFirst: vi.fn() } }`.
    Do NOT create lib/cbr-rates.ts — this test MUST be RED.
  </action>
  <verify>
    <automated>npm run test -- --run tests/cbr-rates.test.ts; if [ $? -ne 0 ]; then echo "RED-as-expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - tests/cbr-rates.test.ts exists
    - File contains string "ratePerUnit" and "getLatestRate"
    - File contains import string "@/lib/cbr-rates"
    - `npm run test -- --run tests/cbr-rates.test.ts` fails — RED confirmed
  </acceptance_criteria>
  <done>Test file exists, references fetchCbrRates/ratePerUnit/getLatestRate, fails because @/lib/cbr-rates does not exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: supplier-actions isPrimary RED test (D-02)</name>
  <read_first>
    - 20-RESEARCH.md §"Pattern 3: Nested CRUD" (lines 184-215) — the isPrimary updateMany-before-upsert pattern under test
    - 20-RESEARCH.md §"Pitfall 4: isPrimary Constraint Race Condition" (lines 472-475)
    - app/actions/employees.ts — nested-CRUD server action structure (the file 20-05 will mirror); note its export names
    - tests/loan-math.test.ts + any existing tests/*-actions test for the dual-mode $transaction mock pattern (callback receives tx === prismaMock)
  </read_first>
  <files>tests/supplier-actions.test.ts</files>
  <action>
    Create `tests/supplier-actions.test.ts`. This pins D-02: only one isPrimary=true per (supplierId, type).
    Because app/actions/suppliers.ts pulls the auth chain (next-auth), DO NOT import the server action directly. Instead, extract the enforcement into a PURE helper that 20-05 will create in `lib/supplier-primary.ts` and import THAT:
    `import { resolvePrimaryWrites } from "@/lib/supplier-primary"`.
    Define the contract: `resolvePrimaryWrites(contacts: Array<{id?: string, supplierId: string, type: "SUPPLIER_MANAGER"|"SUPPLIER_BOSS", isPrimary: boolean}>)` returns, per (supplierId,type) group, at most ONE isPrimary=true (the last-wins among those flagged true; if none flagged, none primary). Cases:
    - Two SUPPLIER_MANAGER both isPrimary=true → result keeps exactly one isPrimary=true for that group
    - One MANAGER primary + one BOSS primary (same supplier) → both remain primary (different type groups)
    - No contact flagged → zero primary in result
    - assert the helper never returns >1 isPrimary per (supplierId,type) via a count assertion
    Do NOT create lib/supplier-primary.ts — this test MUST be RED.
  </action>
  <verify>
    <automated>npm run test -- --run tests/supplier-actions.test.ts; if [ $? -ne 0 ]; then echo "RED-as-expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - tests/supplier-actions.test.ts exists
    - File contains string "isPrimary" and "resolvePrimaryWrites"
    - File contains import string "@/lib/supplier-primary"
    - `npm run test -- --run tests/supplier-actions.test.ts` fails — RED confirmed
  </acceptance_criteria>
  <done>Test file exists, encodes D-02 one-primary-per-(supplier,type) rule against a pure helper, fails because the helper does not exist.</done>
</task>

</tasks>

<verification>
- All 3 test files exist under tests/
- Each fails (RED) because its target module is not yet created — this is the expected Wave 0 state
- `npm run test` shows 3 new failing suites (and existing suites unaffected)
</verification>

<success_criteria>
- tests/procurement-math.test.ts, tests/cbr-rates.test.ts, tests/supplier-actions.test.ts created
- Each references the exact import path its implementation will live at
- Formulas/contracts (D-08, D-09, D-02) encoded as executable expectations
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-00-SUMMARY.md`
</output>
