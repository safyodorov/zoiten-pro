---
phase: 20-procurement
plan: 07
type: execute
wave: 4
depends_on: [20-02, 20-04, 20-05, 20-06]
files_modified:
  - app/(dashboard)/procurement/plan/page.tsx
  - components/procurement/ProcurementPlanTable.tsx
  - lib/procurement-plan-data.ts
  - DEPLOY.md
autonomous: false
requirements: [D-10, D-11, D-12, D-18, D-19]
must_haves:
  truths:
    - "/procurement/plan renders a fresh forecast MVP without touching the existing /purchase-plan (temp) page"
    - "Plan shows products with deficit + their leadTimeDays from SupplierProductLink + a computed order-by date"
    - "No new DB writes from the plan page in v1 (read-only forecast)"
    - "Production deploy applies the Phase 20 migration via prisma migrate deploy"
    - "PROCUREMENT access provisioned to relevant non-SUPERADMIN users after deploy"
  artifacts:
    - path: "app/(dashboard)/procurement/plan/page.tsx"
      provides: "Fresh plan MVP RSC page"
      contains: "requireSection(\"PROCUREMENT\")"
    - path: "lib/procurement-plan-data.ts"
      provides: "Pure-ish forecast assembly helper"
      exports: ["buildProcurementPlanRows"]
  key_links:
    - from: "app/(dashboard)/procurement/plan/page.tsx"
      to: "SupplierProductLink.leadTimeDays + stock deficit"
      via: "buildProcurementPlanRows"
      pattern: "leadTimeDays"
---

<objective>
Ship the fresh `/procurement/plan` MVP (read-only deficit/lead-time forecast, per RESEARCH Open Question #1 recommendation), then deploy Phase 20 to production and run human UAT. The existing `/purchase-plan` (now «временный») stays untouched.

Purpose: Final subsection + production go-live + verification.
Output: 1 plan page + 1 table + 1 data helper + DEPLOY.md update + UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-procurement/20-CONTEXT.md
@.planning/phases/20-procurement/20-RESEARCH.md
@CLAUDE.md
@DEPLOY.md
<interfaces>
- buildProcurementPlanRows consumes stock deficit + SupplierProductLink.leadTimeDays
- Stock data helpers: lib/stock-data.ts (Product-level deficit) — read-only source for the forecast
- compareProductsByHierarchy: lib/product-order.ts
- getMskTodayString: lib/wb-cron-schedule.ts
- requireSection: lib/rbac.ts
- DO NOT modify: app/(dashboard)/purchase-plan/page.tsx, app/actions/procurement.ts, model ProductIncoming, components/procurement/ProcurementTable.tsx + ProcurementSearchInput.tsx (these belong to the temp plan — RESEARCH lines 421-434)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /procurement/plan MVP (read-only forecast)</name>
  <read_first>
    - 20-RESEARCH.md §"Open Questions #1" (lines 782-786) — recommended MVP scope: products with deficit + leadTimeDays + computed order-by date, no DB writes
    - 20-RESEARCH.md §"Existing Purchase-Plan — What Don't Touch Means" (lines 421-434) — files to leave alone
    - lib/stock-data.ts — Product-level deficit source (read-only); lib/stock-math.ts calculateStockMetrics
    - app/(dashboard)/stock/page.tsx — RSC data-assembly reference for deficit
    - lib/product-order.ts (compareProductsByHierarchy)
    - CLAUDE.md §"Sticky data-таблицы", §"Форматирование чисел" (Math.trunc for Д)
  </read_first>
  <files>lib/procurement-plan-data.ts, app/(dashboard)/procurement/plan/page.tsx, components/procurement/ProcurementPlanTable.tsx</files>
  <action>
    1. lib/procurement-plan-data.ts: export `buildProcurementPlanRows({ products })` (pure-ish — accepts already-fetched data, no Prisma import if feasible; else keep DB calls minimal). For each Product with deficit Д>0 (from stock data), produce a row: { sku, name, brand/category, deficit Д (Math.trunc), leadTimeDays (from the product's primary SupplierProductLink — pick the link with lowest leadTimeDays or the first non-null), supplierName, orderByDate = today (recommended order date = today if deficit), deliveryEta = today + leadTimeDays }. Compute deliveryEta from getMskTodayString-based date + leadTimeDays. NO writes.
    2. app/(dashboard)/procurement/plan/page.tsx: RSC. requireSection("PROCUREMENT"). Fetch products with deficit + their SupplierProductLink(leadTimeDays, supplier.nameEnglish). Sort via compareProductsByHierarchy. Call buildProcurementPlanRows. Render <ProcurementPlanTable>. This is a NEW page — do not import anything from the temp /purchase-plan.
    3. components/procurement/ProcurementPlanTable.tsx: client. Sticky raw-HTML table. Columns: товар (sku+name) / Д дефицит / поставщик / срок готовности (дней) / ETA доставки (МСК). `prefetch={false}`, solid sticky bg. Empty state «Нет товаров с дефицитом, привязанных к поставщикам». ru-RU formatting (D-18).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "procurement/plan/page|ProcurementPlanTable|procurement-plan-data" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - lib/procurement-plan-data.ts exports buildProcurementPlanRows and references leadTimeDays
    - app/(dashboard)/procurement/plan/page.tsx contains requireSection("PROCUREMENT") and does NOT import from app/(dashboard)/purchase-plan or app/actions/procurement
    - ProcurementPlanTable.tsx contains `overflow-auto` + `prefetch={false}` + no `bg-muted/` alpha on sticky cells
    - grep confirms app/(dashboard)/purchase-plan/page.tsx and app/actions/procurement.ts are UNCHANGED (not in this plan's diff)
    - tsc reports no new errors in edited files
  </acceptance_criteria>
  <done>Fresh read-only plan MVP renders deficit + lead-time forecast; temp plan untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Full type-check + suite + DEPLOY.md update + deploy to VPS</name>
  <read_first>
    - DEPLOY.md — VPS deploy runbook; deploy.sh location and `prisma migrate deploy` step
    - 20-RESEARCH.md §"New Section Checklist" step 7 (line 450) — RBAC provisioning note
    - MEMORY.md feedback_zoiten_new_section_rbac.md + feedback_deploy_delegation.md (user delegates deploy; provision UserSectionRole for non-SUPERADMIN)
    - CLAUDE.md §"VPS заметки" (ssh deploy command) + §"Production performance gotchas" (git add -A for new files)
  </read_first>
  <files>DEPLOY.md</files>
  <action>
    1. Run full validation locally: `npm run test` (all GREEN incl. procurement-math, cbr-rates, supplier-actions) and `npx tsc --noEmit` (clean).
    2. DEPLOY.md: add a short Phase 20 section documenting: migration `20260609_phase20_procurement` applied via `prisma migrate deploy`; new AppSetting `cbrRateSyncCronTime` default "12:00"; CBR cron runs via existing dispatcher (no new systemd unit); RBAC provisioning note — after deploy, grant PROCUREMENT VIEW/MANAGE to the procurement team via /admin/users (SUPERADMIN bypass works automatically; others need explicit UserSectionRole — they must relogin or wait ~60s for JWT refresh). No CBR backfill (forward-only).
    3. Commit (git add -A — new files) and deploy: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`. Verify the migration applied (no error in deploy output) and the service restarts.
    4. Manually trigger CBR sync once on prod to seed initial rates: `curl -H "x-cron-secret: $CRON_SECRET" https://zoiten.pro/api/cbr-rate-sync` (or via dispatcher) → confirm CurrencyRate rows inserted.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 (all suites GREEN)
    - `npx tsc --noEmit` exits 0
    - DEPLOY.md contains "20260609_phase20_procurement" and "cbrRateSyncCronTime" and a PROCUREMENT RBAC provisioning note
    - deploy.sh ran on VPS without migration error (report deploy output in SUMMARY)
  </acceptance_criteria>
  <done>Full suite + tsc green; DEPLOY.md updated; deployed to prod; CBR rates seeded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — procurement end-to-end</name>
  <files>none</files>
  <action>Pause for human verification. Present the what-built summary and walk the user through the how-to-verify checklist. Do not proceed until the user types "approved" or reports issues.</action>
  <what-built>
    Full Phase 20: Suppliers (CRUD, contacts, product links, negotiations), Purchases (auto deposit+balance, multi-payment, status colors), CBR rates cron, fresh /procurement/plan, nav group, RBAC.
  </what-built>
  <how-to-verify>
    On https://zoiten.pro (logged in as a user with PROCUREMENT MANAGE):
    1. Sidebar shows group with «Поставщики», «Закупки», «План закупок»; old item now «План закупок (временный)».
    2. /procurement redirects to /procurement/suppliers.
    3. Create a supplier (nameForeign китайский, nameEnglish, buyer). Add 2 manager contacts both marked primary → after save only ONE manager is primary. Add a product link with unit price + deliveryType + depositPct/balancePct + inspection URL. Add a negotiation with a custom participant.
    4. Cascading filters on /procurement/suppliers narrow correctly (Закупщик→Бренд→Категория→Подкатегория).
    5. Create a purchase for that supplier with 1 item (unit price prefilled from the link, editable). Confirm exactly 1 Депозит (dueDate = today+3) + 1 Баланс (= deposit + срок готовности) auto-created with correct amounts.
    6. Edit deposit percent → amount recomputes live; edit an amount → percent recomputes. Re-open the supplier product link → its values are UNCHANGED (no Supplier mutation).
    7. Add «Депозит 2» and «Баланс 2»; mark a payment paid. An overdue payment shows red.
    8. Try to delete an ACTIVE purchase → blocked; a PLANNED purchase → deletes.
    9. /procurement/plan shows deficit products with lead-time + ETA (read-only).
    10. Currency amounts show RUB equivalent (CBR rate present).
    11. A non-SUPERADMIN without PROCUREMENT grant is redirected/403 on /procurement/*; after grant + relogin they get access.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step failed + observed behavior)</resume-signal>
</task>

</tasks>

<verification>
- npm run test + tsc clean
- Production deploy successful, migration applied, CBR rates seeded
- Human UAT 11-point checklist passed
</verification>

<success_criteria>
- /procurement/plan MVP shipped without touching temp plan
- Phase 20 live in production with RBAC provisioned
- All D-01..D-21 observable in production
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-07-SUMMARY.md`
</output>
