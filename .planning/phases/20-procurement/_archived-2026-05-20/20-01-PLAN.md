---
phase: 20-procurement
plan: 01
type: execute
wave: 2
depends_on: [W0]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260601_phase20_procurement/migration.sql
autonomous: true
requirements:
  - D-01  # Supplier модель
  - D-02  # SupplierContact + ContactMethod enum
  - D-03  # SupplierProductLink + per-product параметры
  - D-04  # Negotiation + NegotiationProduct + NegotiationParticipant
  - D-05  # Purchase + PurchaseStatus enum
  - D-06  # PurchaseItem
  - D-07  # V1 single-supplier per Purchase
  - D-08  # PurchasePayment + PaymentStatus enum + DepositSlot ordinal
  - D-09  # CurrencyRate
  - D-17  # Decimal precision
  - D-20  # Soft delete для Supplier
  - D-21  # Hard delete Purchase только для PLANNED
must_haves:
  truths:
    - "prisma/schema.prisma содержит 10 новых моделей (Supplier, SupplierContact, SupplierProductLink, Negotiation, NegotiationProduct, NegotiationParticipant, Purchase, PurchaseItem, PurchasePayment, CurrencyRate)"
    - "5 новых enums (ContactMethod, ContactType, DeliveryType, PurchaseStatus, PaymentStatus)"
    - "Supplier имеет soft delete (deletedAt)"
    - "Все денежные поля используют Decimal с правильной precision (14,2 / 14,4 / 5,2 / 14,6)"
    - "SupplierProductLink имеет partial unique (supplierId, productId WHERE productId NOT NULL) через manual SQL"
    - "Все relations с правильным onDelete (Cascade / SetNull / Restrict per CONTEXT.md decisions)"
    - "Employee.buyerSuppliers back-relation добавлен (для buyerEmployeeId FK)"
    - "User модель не трогается (нет новых back-relations кроме как через Employee)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "10 новых моделей + 5 enums + back-relations"
      contains: "model Supplier"
    - path: "prisma/migrations/20260601_phase20_procurement/migration.sql"
      provides: "Manual SQL миграция со всеми таблицами + indexes + FK constraints + partial unique"
      contains: "CREATE TABLE \"Supplier\""
  key_links:
    - from: "prisma/schema.prisma:Supplier.buyerEmployeeId"
      to: "Employee.id"
      via: "FK onDelete: SetNull"
      pattern: "buyerEmployee\\s+Employee\\?"
    - from: "prisma/schema.prisma:Purchase.supplierId"
      to: "Supplier.id"
      via: "FK onDelete: Restrict (закупка хранит history)"
      pattern: "supplier\\s+Supplier\\s+@relation"
    - from: "prisma/migrations/20260601_phase20_procurement/migration.sql"
      to: "Postgres unique partial index"
      via: "CREATE UNIQUE INDEX ... WHERE \"productId\" IS NOT NULL"
      pattern: "CREATE UNIQUE INDEX.+WHERE"
---

<objective>
Создать Prisma schema + manual SQL миграцию для Phase 20: 10 моделей и 5 enums реализующих БД Поставщики (Supplier, SupplierContact, SupplierProductLink, Negotiation*, NegotiationProduct, NegotiationParticipant) + БД Закупки (Purchase, PurchaseItem, PurchasePayment) + Currency rates (CurrencyRate).

Purpose: Foundation для всех последующих планов 20-02 .. 20-08. Без неё ничего нельзя писать. Migration пишется руками (нет локальной PG), будет применена deploy.sh на VPS через `prisma migrate deploy`.

Output: Изменённый `prisma/schema.prisma` + новая директория `prisma/migrations/20260601_phase20_procurement/` с `migration.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-procurement/20-CONTEXT.md
@.planning/phases/20-procurement/20-W0-NOTES.md
@prisma/schema.prisma
@CLAUDE.md
</context>

<interfaces>
<!-- Существующие модели на которые ссылается Phase 20 -->

From prisma/schema.prisma:
```prisma
model Employee {
  id           String   @id @default(cuid())
  lastName     String
  firstName    String
  middleName   String?
  // ... existing fields
  // ── Phase 20 NEW back-relations (добавить в этот блок) ──
  suppliersAsBuyer  Supplier[]              @relation("SupplierBuyer")
  negotiationParts  NegotiationParticipant[] @relation("NegotiationEmployee")
}

model Product {
  id          String @id @default(cuid())
  sku         String @unique
  // ... existing fields
  // ── Phase 20 NEW back-relations ──
  supplierLinks    SupplierProductLink[]
  purchaseItems    PurchaseItem[]
  negotiationProds NegotiationProduct[]
}
```

Существующий pattern Decimal precision (Phase 19):
```prisma
model WbAdvertSpendRow {
  updSum Decimal @db.Decimal(12, 2)
}
```

Существующий pattern manual migration (Phase 16):
```sql
-- prisma/migrations/<date>_<name>/migration.sql
ALTER TABLE "X" ADD COLUMN "Y" ...
CREATE UNIQUE INDEX "..." ON ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Добавить 5 enums + 10 моделей в schema.prisma</name>
  <read_first>
    - prisma/schema.prisma (текущее состояние — где ставить новые блоки, какие имена back-relations свободны)
    - .planning/phases/20-procurement/20-CONTEXT.md (D-01..D-21 — конкретные поля)
    - CLAUDE.md «Связи между таблицами БД» (паттерн naming)
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    Добавить в конец `prisma/schema.prisma` (после WbAdvertSpendRow) блок Phase 20 с разделителями `// ──...──`:

    **1) Enums** (5 новых):

    ```prisma
    enum ContactMethod {
      WECHAT
      PHONE
      ALIBABA
      OTHER
    }

    enum ContactType {
      SUPPLIER_MANAGER
      SUPPLIER_BOSS
    }

    enum DeliveryType {
      CARGO
      WHITE
    }

    enum PurchaseStatus {
      PLANNED
      ACTIVE
      COMPLETED
    }

    enum PaymentStatus {
      PLANNED
      PAID
      OVERDUE
    }

    enum PaymentType {
      DEPOSIT
      BALANCE
    }
    ```

    **2) Модели** (10 новых, в этом порядке для читабельности):

    `Supplier` (D-01, D-20):
    ```prisma
    model Supplier {
      id                  String                @id @default(cuid())
      nameForeign         String                // оригинальное имя (китайский/etc)
      nameEnglish         String                // англ. имя для search
      buyerEmployeeId     String?
      buyerEmployee       Employee?             @relation("SupplierBuyer", fields: [buyerEmployeeId], references: [id], onDelete: SetNull)
      cooperationSummary  String?               @db.Text
      contacts            SupplierContact[]
      productLinks        SupplierProductLink[]
      negotiations        Negotiation[]
      purchases           Purchase[]
      deletedAt           DateTime?
      createdAt           DateTime              @default(now())
      updatedAt           DateTime              @updatedAt

      @@index([deletedAt])
      @@index([buyerEmployeeId])
    }
    ```

    `SupplierContact` (D-02):
    ```prisma
    model SupplierContact {
      id                     String         @id @default(cuid())
      supplierId             String
      supplier               Supplier       @relation(fields: [supplierId], references: [id], onDelete: Cascade)
      type                   ContactType
      name                   String         // utf-8, китайские иероглифы OK
      phone                  String?        // E.164 format
      preferredContact       ContactMethod
      preferredContactCustom String?        // обязателен при preferredContact=OTHER (валидация в server action)
      description            String?        @db.Text
      isPrimary              Boolean        @default(false)
      createdAt              DateTime       @default(now())
      updatedAt              DateTime       @updatedAt

      @@index([supplierId])
    }
    ```

    `SupplierProductLink` (D-03):
    ```prisma
    model SupplierProductLink {
      id                   String        @id @default(cuid())
      supplierId           String
      supplier             Supplier      @relation(fields: [supplierId], references: [id], onDelete: Cascade)
      productId            String?
      product              Product?      @relation(fields: [productId], references: [id], onDelete: SetNull)
      productNameFallback  String?       // если productId=null, текстовое имя

      leadTimeDays         Int?
      leadTimeComment      String?
      unitPrice            Decimal?      @db.Decimal(14, 4)
      currency             String?       // ISO 4217: CNY/USD/RUB/...

      deliveryType         DeliveryType?
      deliveryComment      String?

      exclusivityStatus    Boolean       @default(false)
      exclusivityTerms     String?       @db.Text

      depositPct           Decimal?      @db.Decimal(5, 2)
      balancePct           Decimal?      @db.Decimal(5, 2)
      deferralPct          Decimal?      @db.Decimal(5, 2)
      deferralTerms        String?       @db.Text

      inspectionCity       String?
      inspectionAddress    String?
      inspectionMapUrl     String?

      createdAt            DateTime      @default(now())
      updatedAt            DateTime      @updatedAt

      @@index([supplierId])
      @@index([productId])
      // partial unique (supplierId, productId) WHERE productId IS NOT NULL → создаётся в manual SQL
    }
    ```

    `Negotiation` (D-04):
    ```prisma
    model Negotiation {
      id           String                   @id @default(cuid())
      supplierId   String
      supplier     Supplier                 @relation(fields: [supplierId], references: [id], onDelete: Cascade)
      date         DateTime
      goals        String                   @db.Text
      summary      String?                  @db.Text  // null если ещё не прошли
      products     NegotiationProduct[]
      participants NegotiationParticipant[]
      createdAt    DateTime                 @default(now())
      updatedAt    DateTime                 @updatedAt

      @@index([supplierId])
      @@index([date])
    }
    ```

    `NegotiationProduct` (D-04 M:N):
    ```prisma
    model NegotiationProduct {
      id            String      @id @default(cuid())
      negotiationId String
      negotiation   Negotiation @relation(fields: [negotiationId], references: [id], onDelete: Cascade)
      productId     String
      product       Product     @relation(fields: [productId], references: [id], onDelete: Cascade)

      @@unique([negotiationId, productId])
      @@index([productId])
    }
    ```

    `NegotiationParticipant` (D-04 polymorphic):
    ```prisma
    model NegotiationParticipant {
      id                String           @id @default(cuid())
      negotiationId     String
      negotiation       Negotiation      @relation(fields: [negotiationId], references: [id], onDelete: Cascade)
      employeeId        String?
      employee          Employee?        @relation("NegotiationEmployee", fields: [employeeId], references: [id], onDelete: SetNull)
      supplierContactId String?
      supplierContact   SupplierContact? @relation(fields: [supplierContactId], references: [id], onDelete: SetNull)
      customName        String?
      customRole        String?
      // server action валидирует: ровно одно из (employeeId, supplierContactId, customName+customRole)
      createdAt         DateTime         @default(now())

      @@index([negotiationId])
    }
    ```

    Также добавить relation SupplierContact.negotiationParts:
    ```prisma
    // В SupplierContact:
    negotiationParts NegotiationParticipant[]
    ```

    `Purchase` (D-05, D-07):
    ```prisma
    model Purchase {
      id                  String            @id @default(cuid())
      status              PurchaseStatus    @default(PLANNED)
      currency            String            @default("CNY")
      supplierId          String
      supplier            Supplier          @relation(fields: [supplierId], references: [id], onDelete: Restrict)
      items               PurchaseItem[]
      payments            PurchasePayment[]

      optionsDescription  String?           @db.Text
      optionsExtraCost    Decimal?          @db.Decimal(14, 2)
      logisticsCost       Decimal?          @db.Decimal(14, 2)
      logisticsComment    String?           @db.Text

      createdAt           DateTime          @default(now())
      updatedAt           DateTime          @updatedAt

      @@index([status])
      @@index([supplierId])
      @@index([createdAt])
    }
    ```

    `PurchaseItem` (D-06):
    ```prisma
    model PurchaseItem {
      id         String   @id @default(cuid())
      purchaseId String
      purchase   Purchase @relation(fields: [purchaseId], references: [id], onDelete: Cascade)
      productId  String
      product    Product  @relation(fields: [productId], references: [id], onDelete: Restrict)
      quantity   Int
      unitPrice  Decimal  @db.Decimal(14, 4)

      @@index([purchaseId])
      @@index([productId])
    }
    ```

    `PurchasePayment` (D-08):
    ```prisma
    model PurchasePayment {
      id         String        @id @default(cuid())
      purchaseId String
      purchase   Purchase      @relation(fields: [purchaseId], references: [id], onDelete: Cascade)
      type       PaymentType   // DEPOSIT | BALANCE
      ordinal    Int           // 1, 2, 3, ... per (purchaseId, type)
      percent    Decimal?      @db.Decimal(5, 2)
      amount     Decimal       @db.Decimal(14, 2)
      currency   String        // ISO 4217, обычно совпадает с Purchase.currency
      dueDate    DateTime
      paidDate   DateTime?
      status     PaymentStatus @default(PLANNED)
      comment    String?       @db.Text
      createdAt  DateTime      @default(now())
      updatedAt  DateTime      @updatedAt

      @@unique([purchaseId, type, ordinal])
      @@index([purchaseId])
      @@index([status])
      @@index([dueDate])
    }
    ```

    `CurrencyRate` (D-09):
    ```prisma
    model CurrencyRate {
      id        String   @id @default(cuid())
      date      DateTime @db.Date
      code      String   // ISO 4217: "CNY", "USD", "EUR", ...
      nominal   Int      @default(1)
      rateToRub Decimal  @db.Decimal(14, 6)
      syncedAt  DateTime @default(now())

      @@unique([date, code])
      @@index([code])
      @@index([date])
    }
    ```

    **3) Дополнить existing models**:

    В `model Employee` — добавить back-relations:
    ```prisma
    suppliersAsBuyer    Supplier[]              @relation("SupplierBuyer")
    negotiationParts    NegotiationParticipant[] @relation("NegotiationEmployee")
    ```

    В `model Product` — добавить back-relations:
    ```prisma
    supplierLinks      SupplierProductLink[]
    purchaseItems      PurchaseItem[]
    negotiationProds   NegotiationProduct[]
    ```

    **ВАЖНО:** НЕ менять User, не трогать ERP_SECTION enum (PROCUREMENT уже есть). Не вводить новые back-relations на User — Закупщик это Employee (User opt-link через Employee.user).

    Per D-17: денежные `Decimal(14, 2)`, unit prices `(14, 4)`, percents `(5, 2)`, rate to RUB `(14, 6)`.
  </action>
  <verify>
    <automated>grep -c "^model Supplier\|^model SupplierContact\|^model SupplierProductLink\|^model Negotiation\|^model NegotiationProduct\|^model NegotiationParticipant\|^model Purchase\|^model PurchaseItem\|^model PurchasePayment\|^model CurrencyRate" prisma/schema.prisma</automated>
  </verify>
  <done>В schema.prisma присутствуют все 10 моделей (grep ≥10) и все 5 enums (ContactMethod, ContactType, DeliveryType, PurchaseStatus, PaymentStatus, PaymentType — итого 6 enums). Employee.suppliersAsBuyer и Product.supplierLinks back-relations добавлены.</done>
</task>

<task type="auto">
  <name>Task 2: Manual SQL миграция</name>
  <read_first>
    - prisma/schema.prisma (после изменений Task 1)
    - prisma/migrations/ (последние миграции — паттерн naming, format)
    - .planning/phases/20-procurement/20-CONTEXT.md (D-03 — partial unique через manual SQL)
  </read_first>
  <files>
    prisma/migrations/20260601_phase20_procurement/migration.sql
  </files>
  <action>
    Создать `prisma/migrations/20260601_phase20_procurement/migration.sql` с SQL воспроизводящим schema из Task 1:

    1. **CREATE TYPE** для 6 enums (ContactMethod, ContactType, DeliveryType, PurchaseStatus, PaymentStatus, PaymentType)
    2. **CREATE TABLE "Supplier"** — все поля + DEFAULT now() для createdAt, индексы (deletedAt, buyerEmployeeId), FK на Employee с ON DELETE SET NULL
    3. **CREATE TABLE "SupplierContact"** — FK supplierId ON DELETE CASCADE, индекс supplierId
    4. **CREATE TABLE "SupplierProductLink"** — все 17 полей (per-product параметры), FK supplierId CASCADE, productId SET NULL, индексы supplierId/productId
    5. **Partial unique index** (КРИТИЧНО для D-03):
       ```sql
       CREATE UNIQUE INDEX "SupplierProductLink_supplierId_productId_key"
         ON "SupplierProductLink"("supplierId", "productId")
         WHERE "productId" IS NOT NULL;
       ```
    6. **CREATE TABLE "Negotiation"** — FK supplierId CASCADE, индексы supplierId/date
    7. **CREATE TABLE "NegotiationProduct"** — FK negotiationId CASCADE, productId CASCADE, UNIQUE (negotiationId, productId)
    8. **CREATE TABLE "NegotiationParticipant"** — FK negotiationId CASCADE, employeeId SET NULL, supplierContactId SET NULL, индекс negotiationId
    9. **CREATE TABLE "Purchase"** — FK supplierId ON DELETE RESTRICT, индексы status/supplierId/createdAt, DEFAULT 'PLANNED' for status, DEFAULT 'CNY' for currency
    10. **CREATE TABLE "PurchaseItem"** — FK purchaseId CASCADE, productId RESTRICT, индексы purchaseId/productId
    11. **CREATE TABLE "PurchasePayment"** — FK purchaseId CASCADE, UNIQUE (purchaseId, type, ordinal), индексы status/dueDate, DEFAULT 'PLANNED' для status
    12. **CREATE TABLE "CurrencyRate"** — UNIQUE (date, code), индексы code/date, nominal DEFAULT 1

    Для Decimal: использовать `DECIMAL(14, 2)` / `DECIMAL(14, 4)` / `DECIMAL(5, 2)` / `DECIMAL(14, 6)` как указано в schema.

    Использовать строковые поля с DEFAULT (например `"createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`) и `@updatedAt` → `"updatedAt" TIMESTAMP(3) NOT NULL` (Prisma сам обновляет на app level).

    Шаблон одной таблицы (для образца, остальное по аналогии):

    ```sql
    -- Phase 20: Procurement schema migration

    -- Enums
    CREATE TYPE "ContactMethod" AS ENUM ('WECHAT', 'PHONE', 'ALIBABA', 'OTHER');
    CREATE TYPE "ContactType" AS ENUM ('SUPPLIER_MANAGER', 'SUPPLIER_BOSS');
    CREATE TYPE "DeliveryType" AS ENUM ('CARGO', 'WHITE');
    CREATE TYPE "PurchaseStatus" AS ENUM ('PLANNED', 'ACTIVE', 'COMPLETED');
    CREATE TYPE "PaymentStatus" AS ENUM ('PLANNED', 'PAID', 'OVERDUE');
    CREATE TYPE "PaymentType" AS ENUM ('DEPOSIT', 'BALANCE');

    -- Suppliers
    CREATE TABLE "Supplier" (
      "id" TEXT NOT NULL,
      "nameForeign" TEXT NOT NULL,
      "nameEnglish" TEXT NOT NULL,
      "buyerEmployeeId" TEXT,
      "cooperationSummary" TEXT,
      "deletedAt" TIMESTAMP(3),
      "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      "updatedAt" TIMESTAMP(3) NOT NULL,
      CONSTRAINT "Supplier_pkey" PRIMARY KEY ("id")
    );

    CREATE INDEX "Supplier_deletedAt_idx" ON "Supplier"("deletedAt");
    CREATE INDEX "Supplier_buyerEmployeeId_idx" ON "Supplier"("buyerEmployeeId");

    ALTER TABLE "Supplier" ADD CONSTRAINT "Supplier_buyerEmployeeId_fkey"
      FOREIGN KEY ("buyerEmployeeId") REFERENCES "Employee"("id")
      ON DELETE SET NULL ON UPDATE CASCADE;

    -- ... остальные 9 таблиц по аналогии
    ```

    **НЕ запускать `prisma migrate dev` локально** (нет PG). Миграция применится автоматически через `prisma migrate deploy` в deploy.sh на VPS (Plan 20-08).

    После создания migration.sql проверить что `npx prisma format && npx prisma validate` проходит (валидация schema).
  </action>
  <verify>
    <automated>test -f prisma/migrations/20260601_phase20_procurement/migration.sql && grep -c "CREATE TABLE \"Supplier\"\|CREATE TABLE \"SupplierContact\"\|CREATE TABLE \"SupplierProductLink\"\|CREATE TABLE \"Negotiation\"\|CREATE TABLE \"NegotiationProduct\"\|CREATE TABLE \"NegotiationParticipant\"\|CREATE TABLE \"Purchase\"\|CREATE TABLE \"PurchaseItem\"\|CREATE TABLE \"PurchasePayment\"\|CREATE TABLE \"CurrencyRate\"" prisma/migrations/20260601_phase20_procurement/migration.sql && grep -q "WHERE \"productId\" IS NOT NULL" prisma/migrations/20260601_phase20_procurement/migration.sql && cd C:/Users/User/zoiten-pro && npx prisma format --schema=prisma/schema.prisma && npx prisma validate --schema=prisma/schema.prisma</automated>
  </verify>
  <done>migration.sql существует, 10 таблиц + 6 enums + partial unique для SupplierProductLink созданы; prisma validate проходит</done>
</task>

</tasks>

<verification>
- `npx prisma validate` проходит без ошибок
- `npx prisma format` не предлагает изменений
- grep подтверждает 10 моделей + 6 enums + partial unique
- Все FK с правильными onDelete действиями (Cascade/SetNull/Restrict per CONTEXT.md)
</verification>

<success_criteria>
- Schema готова для генерации Prisma client на VPS
- Migration воспроизводимо применяется через `prisma migrate deploy`
- Plan 20-02/20-03 могут импортировать `import { Supplier, Purchase, ... } from "@prisma/client"`
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-01-SUMMARY.md`
</output>
