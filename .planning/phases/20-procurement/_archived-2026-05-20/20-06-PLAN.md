---
phase: 20-procurement
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - app/(dashboard)/procurement/purchases/page.tsx
  - app/(dashboard)/procurement/purchases/[id]/page.tsx
  - components/procurement/PurchasesTable.tsx
  - components/procurement/PurchasesFilters.tsx
  - components/procurement/PurchaseModal.tsx
  - components/procurement/PurchaseItemsEditor.tsx
  - components/procurement/PurchasePaymentsEditor.tsx
  - components/procurement/PurchaseStatusBadge.tsx
  - components/procurement/PaymentStatusBadge.tsx
  - components/procurement/CreatePurchaseButton.tsx
autonomous: true
requirements:
  - D-10  # /procurement/purchases routes
  - D-11  # RBAC read
  - D-12  # Sticky table
  - D-13  # Каскадные фильтры (status, period, supplier, buyer)
  - D-14  # CRUD через modal
  - D-16  # Multi-payment UI: вертикальный список карточек с inline edit
  - D-18  # Формат денег ru-RU + currency
  - D-19  # Даты МСК
must_haves:
  truths:
    - "/procurement/purchases — sticky-table закупок с фильтрами Status / Период / Поставщик / Закупщик"
    - "Status colors: PLANNED=серый, ACTIVE=синий, COMPLETED=emerald (Claude's discretion)"
    - "PaymentStatus colors: PLANNED=серый, PAID=emerald, OVERDUE=красный"
    - "Клик по строке → /procurement/purchases/[id] с PurchaseItemsEditor + PurchasePaymentsEditor"
    - "PurchaseModal — multi-section form для create/edit Purchase (supplier dropdown, currency native select, items table, options/logistics, overrides для deposit/balance)"
    - "PurchasePaymentsEditor — вертикальный список карточек (D-16) с inline edit percent+amount+date, кнопки «Добавить депозит» / «Добавить баланс»"
    - "Конверсия sum в RUB через getLatestRate(currency) — показывается рядом с currency amount"
    - "RBAC PROCUREMENT для view + MANAGE actions"
    - "deletePurchase кнопка disabled если status != PLANNED (UI enforcement дублирует server-side D-21)"
    - "markPaymentPaid через clickable badge / icon с date picker (optional default=today)"
  artifacts:
    - path: "app/(dashboard)/procurement/purchases/page.tsx"
      provides: "RSC list — таблица с фильтрами + кнопка «+ Закупка»"
      min_lines: 80
    - path: "app/(dashboard)/procurement/purchases/[id]/page.tsx"
      provides: "RSC detail — info / items / payments + status update buttons"
      min_lines: 80
    - path: "components/procurement/PurchaseModal.tsx"
      provides: "Multi-section form для create/edit"
    - path: "components/procurement/PurchasePaymentsEditor.tsx"
      provides: "D-16 — вертикальный список карточек с inline edit + add-payment buttons"
    - path: "components/procurement/PurchaseStatusBadge.tsx"
      provides: "Color-coded badge для PurchaseStatus с pickers для transition"
    - path: "components/procurement/PaymentStatusBadge.tsx"
      provides: "Color-coded badge для PaymentStatus + кнопка markPaid"
  key_links:
    - from: "components/procurement/PurchaseModal.tsx"
      to: "app/actions/purchases.ts:createPurchase, updatePurchase"
      via: "import + form submit"
      pattern: "createPurchase\\(|updatePurchase\\("
    - from: "components/procurement/PurchasePaymentsEditor.tsx"
      to: "app/actions/purchases.ts:addPurchasePayment, updatePurchasePayment, deletePurchasePayment, markPaymentPaid, markPaymentUnpaid"
      via: "import + button onClick handlers"
      pattern: "addPurchasePayment\\(|markPaymentPaid\\("
    - from: "app/(dashboard)/procurement/purchases/[id]/page.tsx"
      to: "app/actions/purchases.ts:recomputeOverduePaymentsForPurchase"
      via: "await перед render"
      pattern: "recomputeOverduePaymentsForPurchase\\("
---

<objective>
UI слой для БД Закупки. Список + детальная страница с inline-управлением payments. Sticky table pattern. Status badges с правильными цветами.

Purpose: Закрыть UX-loop для закупок. Юзер создаёт закупку — видит автоматические Депозит + Баланс с computed dates — может добавить Депозит 2/Баланс 2 — отметить как оплаченные — видеть OVERDUE флаги.

Output: 10 файлов (2 страницы + 8 компонентов).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-procurement/20-CONTEXT.md
@app/actions/purchases.ts
@app/actions/suppliers.ts
@lib/purchase-payments.ts
@lib/cbr-rates.ts
@components/procurement/SuppliersTable.tsx
@components/employees/EmployeeModal.tsx
@CLAUDE.md
</context>

<interfaces>
Server actions (Plan 20-05):

```typescript
// app/actions/purchases.ts
createPurchase, updatePurchase, deletePurchase, updatePurchaseStatus,
addPurchasePayment, updatePurchasePayment, deletePurchasePayment,
markPaymentPaid, markPaymentUnpaid, recomputeOverduePaymentsForPurchase
```

CBR rates lookup (Plan 20-02):

```typescript
// lib/cbr-rates.ts
getLatestRate(code: string, asOf?: Date): Promise<CurrencyRate | null>
// rateInRub = Number(rateToRub) / nominal
```

Pure helpers (Plan 20-05):

```typescript
// lib/purchase-payments.ts
computePaymentAmount, computeDepositDueDate, computeBalanceDueDate, recomputeOverdueStatus
```

Sticky table pattern: CLAUDE.md (mandatory).
ru-RU number format: `new Intl.NumberFormat('ru-RU', { maximumFractionDigits: 2 }).format(n)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: List page + filters + table + status badges</name>
  <read_first>
    - components/procurement/SuppliersTable.tsx (sticky table pattern, после 20-04)
    - components/procurement/SuppliersFilters.tsx (паттерн MultiSelect, каскад)
    - app/(dashboard)/procurement/suppliers/page.tsx (RSC pattern, после 20-04)
    - .planning/phases/20-procurement/20-CONTEXT.md (D-12, D-13, D-18, D-19 + Claude's discretion цвета)
  </read_first>
  <files>
    app/(dashboard)/procurement/purchases/page.tsx
    components/procurement/PurchasesTable.tsx
    components/procurement/PurchasesFilters.tsx
    components/procurement/PurchaseStatusBadge.tsx
    components/procurement/PaymentStatusBadge.tsx
    components/procurement/CreatePurchaseButton.tsx
  </files>
  <action>
    A) Создать PurchaseStatusBadge.tsx client component с COLORS map: PLANNED → "bg-muted text-muted-foreground border-muted-foreground/20", ACTIVE → "bg-blue-500/15 text-blue-700 dark:text-blue-300 border-blue-500/30", COMPLETED → "bg-emerald-500/15 text-emerald-700 dark:text-emerald-300 border-emerald-500/30". LABELS: "Планируется" / "В процессе" / "Завершена".

    B) Создать PaymentStatusBadge.tsx аналогично: PLANNED серый "Планируется", PAID emerald "Оплачено", OVERDUE красный "Просрочено".

    C) app/(dashboard)/procurement/purchases/page.tsx (RSC):
    - await requireSection("PROCUREMENT")
    - Парсить searchParams: statuses (CSV PurchaseStatus[]), supplierIds, buyerIds, from (ISO date), to
    - Build where: status:{in:statuses}, supplierId:{in:supplierIds}, supplier.buyerEmployeeId:{in:buyerIds}, createdAt:{gte:from, lte:to}
    - Promise.all загрузка: purchases (с include supplier+buyerEmployee, items+product, payments orderBy [type asc, ordinal asc]), suppliers (для filter dropdown), buyers
    - orderBy purchases: { createdAt: "desc" } (Claude's discretion)
    - Render: filters + CreatePurchaseButton + PurchasesTable в flex-col h-full контейнере

    D) PurchasesFilters.tsx (client) — MultiSelect для status (3 опции PLANNED/ACTIVE/COMPLETED с локализацией), supplier (nameEnglish + nameForeign), buyer (lastName firstName) + 2 native input type="date" для period from/to. Все через URL searchParams + router.replace.

    E) PurchasesTable.tsx (client) — sticky table per CLAUDE.md pattern (raw HTML table с overflow-auto h-full, border-separate border-spacing-0, thead bg-background, sticky top-0):
    Колонки:
    - Дата создания (formatMskDate)
    - Статус (PurchaseStatusBadge)
    - Поставщик (nameEnglish сверху + Закупщик внизу мелким)
    - Товаров (count items)
    - Сумма (sum items.quantity*items.unitPrice + currency, формат ru-RU)
    - Оплачено (sum payments.amount WHERE status=PAID)
    - К оплате (sum payments.amount WHERE status IN (PLANNED, OVERDUE), если есть OVERDUE → красный)
    - Действия (Delete icon — disabled если status != PLANNED)

    Click on row (td кроме action cell) → router.push(`/procurement/purchases/${p.id}`).

    F) CreatePurchaseButton.tsx — кнопка «+ Закупка», открывает PurchaseModal в create mode (передаёт suppliers list через prop).
  </action>
  <verify>
    <automated>test -f "app/(dashboard)/procurement/purchases/page.tsx" && test -f components/procurement/PurchasesTable.tsx && test -f components/procurement/PurchaseStatusBadge.tsx && test -f components/procurement/PaymentStatusBadge.tsx && grep -q "emerald" components/procurement/PurchaseStatusBadge.tsx && grep -q "OVERDUE" components/procurement/PaymentStatusBadge.tsx && grep -q "requireSection(\"PROCUREMENT\")" "app/(dashboard)/procurement/purchases/page.tsx"</automated>
  </verify>
  <done>List page рендерится через RSC; sticky table; status badges с правильными цветами; фильтры через URL</done>
</task>

<task type="auto">
  <name>Task 2: Detail page + PurchaseModal + items/payments editors</name>
  <read_first>
    - app/(dashboard)/procurement/suppliers/[id]/page.tsx (RSC detail pattern, после 20-04)
    - components/procurement/SupplierModal.tsx (multi-section form pattern, после 20-04)
    - components/employees/EmployeeModal.tsx (form patterns, sub-list editors)
    - lib/purchase-payments.ts (computePaymentAmount для realtime пересчёта в форме)
    - .planning/phases/20-procurement/20-CONTEXT.md (D-14, D-16)
  </read_first>
  <files>
    app/(dashboard)/procurement/purchases/[id]/page.tsx
    components/procurement/PurchaseModal.tsx
    components/procurement/PurchaseItemsEditor.tsx
    components/procurement/PurchasePaymentsEditor.tsx
  </files>
  <action>
    A) app/(dashboard)/procurement/purchases/[id]/page.tsx (RSC):

    - await requireSection("PROCUREMENT")
    - await recomputeOverduePaymentsForPurchase(id) — обновить OVERDUE статусы перед render
    - prisma.purchase.findUnique({where:{id}, include: supplier+buyerEmployee+productLinks (для default percents), items+product, payments orderBy [type asc, ordinal asc]})
    - notFound() если purchase null
    - Параллельно загрузить currentRates: для каждой уникальной currency в payments — getLatestRate(currency)
    - Render структура:
      * Header: PurchaseStatusBadge + supplier name + кнопки status transition (PLANNED → ACTIVE → COMPLETED)
      * Section «Информация»: read-only fields + button «Редактировать» открывает PurchaseModal в edit
      * Section «Товары»: PurchaseItemsEditor (можно реализовать как read-only table, edit через модалку)
      * Section «Платежи»: PurchasePaymentsEditor (inline edit)
      * Footer (sticky): кнопка Delete (disabled если status != PLANNED)
      * Sum в RUB через CurrencyRate.rateToRub / nominal

    B) PurchaseModal.tsx (client) — Dialog с multi-section form:
    - Section «Основное»: supplier native select (передаётся в props), currency native select (CNY/USD/EUR/RUB/...)
    - Section «Товары»: list edit — добавление товаров через CreatableCombobox по products. Per row: product, quantity, unitPrice. Show running total per item + grand total
    - Section «Опции и логистика»: optionsDescription textarea, optionsExtraCost input, logisticsCost input, logisticsComment textarea
    - Section «Платежи (создание)»: only при create mode — показать вычисленные defaults (depositPct, balancePct, leadTime из выбранного supplier через client-side fetch SupplierProductLink) с возможностью override (4 input: depositPct, balancePct, depositDueDate, balanceDueDate). При edit mode эта секция скрыта (платежи правят через PurchasePaymentsEditor)
    - Footer: Cancel / Delete (только edit + status=PLANNED) / Save

    Save вызывает createPurchase или updatePurchase + toast + onOpenChange(false) + router.refresh().

    C) PurchaseItemsEditor.tsx (client, опционально separate component) — render items таблицей с edit-icon → открыть PurchaseModal на секции «Товары».

    Или embed напрямую в page как read-only table + кнопка «Редактировать товары» открывает PurchaseModal.

    D) PurchasePaymentsEditor.tsx (client) per D-16 — вертикальный список карточек:

    - Группировка по type: «Депозит» секция + «Баланс» секция (SectionDivider pattern из EmployeeModal)
    - Каждая карточка платежа:
      * Header: «Депозит N» (где N=ordinal) + PaymentStatusBadge
      * Body grid: percent input + amount input + dueDate input + comment input
      * При изменении percent → amount пересчитывается auto (computePaymentAmount(percent, itemsTotal))
      * При изменении amount → percent пересчитывается (amount/itemsTotal*100)
      * Кнопка «Оплачено» / «Отменить оплату» (markPaymentPaid/markPaymentUnpaid)
      * Date-picker для paidDate (default = today)
      * Кнопка delete (×)
      * Debounced save (500ms) через updatePurchasePayment
    - Кнопки внизу: «+ Добавить депозит» / «+ Добавить баланс» → addPurchasePayment с auto-increment ordinal

    Total amount footer: ITEMS_TOTAL + EXTRA_COST + LOGISTICS = PURCHASE_TOTAL (ru-RU + currency).
    Sum paid / Sum planned / Sum overdue with цветами.

    Все client components используют sonner toast + router.refresh().
  </action>
  <verify>
    <automated>test -f "app/(dashboard)/procurement/purchases/[id]/page.tsx" && test -f components/procurement/PurchaseModal.tsx && test -f components/procurement/PurchasePaymentsEditor.tsx && grep -q "recomputeOverduePaymentsForPurchase" "app/(dashboard)/procurement/purchases/[id]/page.tsx" && grep -q "createPurchase\|updatePurchase" components/procurement/PurchaseModal.tsx && grep -q "addPurchasePayment\|markPaymentPaid" components/procurement/PurchasePaymentsEditor.tsx</automated>
  </verify>
  <done>Detail page рендерится; PurchaseModal позволяет create/edit с auto-payment defaults; PurchasePaymentsEditor inline-CRUD работает с D-16 вертикальным layout</done>
</task>

</tasks>

<verification>
- `npm run build` (Next.js compile + TypeScript) проходит
- Manual smoke (после deploy): /procurement/purchases открывается; create flow создаёт Purchase с 2 payments
- Manual smoke: edit Purchase обновляет items; PurchasePaymentsEditor добавляет Депозит 2; markPaid меняет badge на emerald
</verification>

<success_criteria>
- Юзер создаёт Purchase через UI с auto-расчётом deposit/balance
- Multi-payment схема D-16 работает в UI
- Status transitions через badges (PurchaseStatus + PaymentStatus)
- Currency conversion в RUB виден рядом с amount (через CBR rates)
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-06-SUMMARY.md`
</output>
