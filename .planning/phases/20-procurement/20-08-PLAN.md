---
phase: 20-procurement
plan: 08
type: execute
wave: 6
depends_on: [02, 04, 06, 07]
files_modified:
  - components/layout/nav-items.ts
  - components/landing/variants/GlassmorphismLanding.tsx
  - app/(dashboard)/dashboard/page.tsx
  - .planning/phases/20-procurement/20-UAT.md
  - .planning/phases/20-procurement/20-DEPLOY-NOTES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - D-10  # Sidebar nav (Procurement)
  - All Phase 20 — UAT + deploy
must_haves:
  truths:
    - "Sidebar содержит «Управление закупками» с правильной иконкой (Truck) между «Себестоимость партий» и «План продаж» (per Claude's discretion в CONTEXT.md)"
    - "Landing page / Dashboard содержат карточку «Закупки» с описанием раздела"
    - "REQUIREMENTS.md обновлён: добавлен раздел Phase 20 Procurement с decision IDs D-01..D-21 в Traceability"
    - "ROADMAP.md Phase 20 entry обновлён: count plans=8, plans list заполнен с brief objectives"
    - "20-UAT.md содержит 12+ пунктов чек-листа для пользователя на проде"
    - "20-DEPLOY-NOTES.md описывает шаги deploy: prisma migrate deploy + crontab dispatch UNCHANGED (cbr-rate-sync через dispatcher) + smoke test после deploy"
    - "Checkpoint: после deploy юзер проходит UAT и подтверждает все 12+ пунктов"
  artifacts:
    - path: "components/layout/nav-items.ts"
      provides: "Sidebar nav entry «Управление закупками» с правильной позицией"
      contains: "PROCUREMENT"
    - path: ".planning/phases/20-procurement/20-UAT.md"
      provides: "Human UAT checklist с 12+ пунктами"
      min_lines: 50
    - path: ".planning/phases/20-procurement/20-DEPLOY-NOTES.md"
      provides: "Deploy notes — migration, cron, smoke"
  key_links:
    - from: "components/layout/nav-items.ts"
      to: "ERP_SECTION.PROCUREMENT"
      via: "section property"
      pattern: "section:\\s*\"PROCUREMENT\""
    - from: "components/layout/nav-items.ts"
      to: "/procurement (default route)"
      via: "href"
      pattern: "href:\\s*\"/procurement\""
---

<objective>
Финальный план Phase 20: добавление в Sidebar nav, обновление landing/dashboard карточек, документация (UAT + DEPLOY notes), обновление REQUIREMENTS/ROADMAP. Checkpoint UAT с пользователем после deploy.

Purpose: Без sidebar item раздел unreachable. Без UAT checklist нельзя подтвердить production readiness. Без update REQUIREMENTS.md traceability ломается.

Output: 4 правки existing файлов + 2 новых doc файла + checkpoint UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-procurement/20-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@components/layout/nav-items.ts
@components/landing/variants/GlassmorphismLanding.tsx
@app/(dashboard)/dashboard/page.tsx
@CLAUDE.md
</context>

<interfaces>
nav-items.ts pattern:
```typescript
export const NAV_ITEMS = [
  { href: "/products", label: "Товары", icon: "Package", section: "PRODUCTS" },
  // ...
  { href: "/batches", label: "Себестоимость партий", icon: "Coins", section: "COST" },
  // NEW: Insert here per CONTEXT.md Claude's Discretion
  { href: "/procurement", label: "Управление закупками", icon: "Truck", section: "PROCUREMENT" },
  { href: "/sales-plan", label: "План продаж", icon: "...", section: "SALES" },
]
```

deploy.sh уже существует — Phase 20 ничего там не меняет (только prisma migrate deploy подхватит новую миграцию автоматически).
Cron infrastructure (systemd zoiten-cron-dispatch.timer) уже работает — Plan 20-02 добавил branch в dispatch/route.ts, и cbrRateSyncCronTime seed'ит миграция.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar nav + landing + dashboard cards + docs</name>
  <read_first>
    - components/layout/nav-items.ts (точная структура NAV_ITEMS — позиции, icon names, section IDs)
    - components/landing/variants/GlassmorphismLanding.tsx (как добавлены карточки разделов)
    - app/(dashboard)/dashboard/page.tsx (паттерн dashboard cards)
    - .planning/REQUIREMENTS.md (раздел Traceability + куда добавлять Phase 20)
    - .planning/ROADMAP.md (Phase 20 entry в Backlog — обновить status to Planned + добавить Plans list)
  </read_first>
  <files>
    components/layout/nav-items.ts
    components/landing/variants/GlassmorphismLanding.tsx
    app/(dashboard)/dashboard/page.tsx
    .planning/phases/20-procurement/20-UAT.md
    .planning/phases/20-procurement/20-DEPLOY-NOTES.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
    A) components/layout/nav-items.ts — добавить entry:

    - Импорт `Truck` icon из lucide-react (если ещё не импортирован).
    - Insert новый объект между entry для `/batches` (COST) и `/sales-plan` (SALES, либо где сейчас находится):
      ```typescript
      { href: "/procurement", label: "Управление закупками", icon: Truck, section: "PROCUREMENT" }
      ```
    - Если в проекте текущая заглушка раздела (Pending: проверить — в файле может быть entry с section: "PROCUREMENT" на route типа /procurement-plan-old). Если есть — заменить на правильное.

    B) components/landing/variants/GlassmorphismLanding.tsx — добавить карточку «Закупки» в array карточек разделов:
    ```tsx
    { title: "Закупки", description: "Поставщики, закупки, план — мульти-валютные платежи с курсами ЦБ РФ", icon: Truck, href: "/procurement" }
    ```
    (Уточнить структуру по существующим карточкам и встроить аналогично — sortOrder зависит от текущего распорядка.)

    C) app/(dashboard)/dashboard/page.tsx — если есть карточки разделов на dashboard, добавить «Управление закупками» (тот же style как существующие entries).

    D) Создать .planning/phases/20-procurement/20-UAT.md с 12+ пунктами:

    ```markdown
    # Phase 20 UAT Checklist

    **Дата деплоя:** ___
    **Тестировщик:** Сергей

    После `bash deploy.sh` на проде:

    ## Поставщики

    1. [ ] Открыть https://zoiten.pro/procurement — должно редиректнуть на /procurement/suppliers
    2. [ ] Sidebar содержит «Управление закупками» с иконкой грузовика
    3. [ ] /procurement/suppliers открывается, таблица пустая или со списком, фильтры работают
    4. [ ] Создать первого поставщика через «+ Поставщик»: nameForeign + nameEnglish + buyer
    5. [ ] Открыть detail-страницу поставщика, добавить контакт SUPPLIER_MANAGER с WeChat
    6. [ ] Добавить второй контакт с preferredContact=OTHER → должен потребовать заполнить «Свой вариант»
    7. [ ] Установить первый контакт как «Основной» — у второго звезда должна сняться
    8. [ ] Tab «Товары»: связать поставщика с одним из существующих Product, заполнить leadTimeDays=45, depositPct=30, balancePct=70, unitPrice=10.50 CNY
    9. [ ] Tab «Переговоры»: создать запись с date=сегодня, goals=«Тестовое», добавить participant=сотрудник из ERP

    ## Закупки

    10. [ ] Открыть /procurement/purchases, нажать «+ Закупка»
    11. [ ] Выбрать поставщика из шага 4, добавить 1 товар (quantity=100, unitPrice=10.50), currency=CNY → Save
    12. [ ] Открыть detail-страницу закупки — должны быть автоматически созданные Депозит 1 (dueDate=сегодня+3, amount=315 CNY = 30% от 1050) и Баланс 1 (dueDate=Депозит+45=сегодня+48, amount=735 CNY = 70%)
    13. [ ] Конверсия в RUB должна показаться рядом с amount (через CBR rates)
    14. [ ] Добавить «Депозит 2» через UI с custom percent — ordinal=2 в БД
    15. [ ] Mark Депозит 1 как «Оплачено» — badge меняется на emerald
    16. [ ] Изменить status закупки PLANNED → ACTIVE → COMPLETED (forward only)
    17. [ ] Попытаться удалить ACTIVE закупку — кнопка disabled (UI) + server возвращает error

    ## План закупок

    18. [ ] Открыть /procurement/plan — summary cards показывают актуальные числа
    19. [ ] Таблица per-месяц breakdown содержит созданную в шаге 11 закупку под её dueDate месяцем

    ## CBR + Cron

    20. [ ] curl -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/cbr-rate-sync → должен вернуть 200 с {ok:true, codes:[CNY,USD,EUR,...]}
    21. [ ] Проверить SELECT count(*) FROM "CurrencyRate" — >= 30 записей (валюты ЦБ РФ)
    22. [ ] AppSetting cbrRateSyncCronTime='12:00' зарегистрирован в БД

    ## RBAC

    23. [ ] Войти как VIEWER без PROCUREMENT → редирект на /unauthorized при попытке открыть /procurement/*
    24. [ ] VIEWER с PROCUREMENT view-only → может смотреть, но кнопки «+ Поставщик» / «Сохранить» вернут «Нет доступа»
    ```

    E) Создать .planning/phases/20-procurement/20-DEPLOY-NOTES.md:

    ```markdown
    # Phase 20 Deploy Notes

    ## Шаги

    1. Локально: убедиться что все unit-тесты GREEN: `npm test`
    2. Commit + push в main
    3. SSH на прод: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"`
    4. deploy.sh автоматически:
       - Pull latest from origin/main
       - npm ci
       - prisma generate
       - **prisma migrate deploy** — применит новую миграцию 20260601_phase20_procurement (10 tables + 6 enums + seed AppSetting cbrRateSyncCronTime)
       - next build
       - systemctl restart zoiten-erp
    5. После restart — manual smoke:
       - https://zoiten.pro/procurement — должно рендерить /procurement/suppliers
       - curl -H "x-cron-secret: $CRON_SECRET" https://zoiten.pro/api/cron/cbr-rate-sync — должно вернуть 200 + populate CurrencyRate
    6. Cron infrastructure НЕ меняется — systemd zoiten-cron-dispatch.timer уже бьёт раз в 5 минут, dispatcher теперь имеет cbr-rate-sync branch (в 12:00 МСК исполнится автоматически)

    ## Известные потенциальные грабли

    - **CBR endpoint** — если smoke fail на 503/timeout, проверить network access с VPS: `curl -v https://www.cbr-xml-daily.ru/daily_json.js`
    - **Миграция** — partial unique index (SupplierProductLink) требует Postgres ≥ 13; у нас PG 16 ✓
    - **Decimal precision** — UI парсит Decimal через Number(), убедиться что unitPrice = "10.5" rendering верно
    - **Currency conversion** — getLatestRate("RUB") вернёт null (RUB нет в ЦБ feed) — UI код должен default к rate=1 для RUB

    ## Rollback план

    Если что-то сломалось:
    1. `git revert <Phase 20 merge commit>` + push + deploy.sh
    2. ВНИМАНИЕ: prisma migrate deploy НЕ умеет автоматически rollback. Manual SQL для DROP TABLE:
       ```sql
       DROP TABLE "PurchasePayment", "PurchaseItem", "Purchase", "NegotiationParticipant", "NegotiationProduct", "Negotiation", "SupplierProductLink", "SupplierContact", "Supplier", "CurrencyRate" CASCADE;
       DROP TYPE "PaymentType", "PaymentStatus", "PurchaseStatus", "DeliveryType", "ContactType", "ContactMethod";
       DELETE FROM "AppSetting" WHERE "key" IN ('cbrRateSyncCronTime', 'cbrRateSyncLastRun');
       ```
    ```

    F) Дополнить .planning/REQUIREMENTS.md — добавить новый раздел перед Traceability:

    ```markdown
    ## v1.3 Requirements — Управление закупками (Phase 20)

    Requirements добавленные в Phase 20 (2026-05-20). Decisions D-01..D-21 в .planning/phases/20-procurement/20-CONTEXT.md.

    ### Поставщики

    - [ ] **PROCUREMENT-01**: БД Supplier с soft delete + relations к Employee (buyer), SupplierContact, SupplierProductLink, Negotiation, Purchase
    - [ ] **PROCUREMENT-02**: SupplierContact с type discriminator (SUPPLIER_MANAGER / SUPPLIER_BOSS), preferredContact (WeChat/Phone/Alibaba/OTHER), enforced single isPrimary per (supplier, type)
    - [ ] **PROCUREMENT-03**: SupplierProductLink с per-product параметрами (leadTimeDays, unitPrice + currency, deliveryType, exclusivity, депозит/баланс/отсрочка %, inspection address)
    - [ ] **PROCUREMENT-04**: Negotiation с participants (polymorphic: Employee | SupplierContact | custom)
    - [ ] **PROCUREMENT-05**: /procurement/suppliers — list + каскадные фильтры; /procurement/suppliers/[id] — detail с табами

    ### Закупки

    - [ ] **PROCUREMENT-06**: БД Purchase + PurchaseItem + PurchasePayment с PurchaseStatus (PLANNED/ACTIVE/COMPLETED), PaymentStatus (PLANNED/PAID/OVERDUE)
    - [ ] **PROCUREMENT-07**: Автоматический расчёт depositDueDate (createdAt+3d) + balanceDueDate (deposit.due + leadTimeDays) per D-08
    - [ ] **PROCUREMENT-08**: Multi-payment схема: можно добавлять Депозит 2/Баланс 2 с auto-increment ordinal
    - [ ] **PROCUREMENT-09**: /procurement/purchases — list со status badges + детальная страница с PurchasePaymentsEditor
    - [ ] **PROCUREMENT-10**: Hard delete Purchase запрещён для status != PLANNED

    ### Currency Rates

    - [ ] **PROCUREMENT-11**: CurrencyRate БД + lib/cbr-rates.ts (fetch + parse + upsert + getLatestRate)
    - [ ] **PROCUREMENT-12**: Daily cron в 12:00 МСК через dispatcher /api/cron/cbr-rate-sync

    ### План закупок

    - [ ] **PROCUREMENT-13**: /procurement/plan MVP с PlanSummaryCards + PlanForecastTable (aggregated PLANNED+ACTIVE payments по месяцам с conversion в RUB)

    ### UX & Infrastructure

    - [ ] **PROCUREMENT-14**: ProcurementTabs sticky nav между 3 sub-секциями
    - [ ] **PROCUREMENT-15**: Sidebar nav «Управление закупками» с иконкой Truck
    - [ ] **PROCUREMENT-16**: RBAC PROCUREMENT (view) + MANAGE (write) применён ко всем pages + actions
    ```

    Добавить эти 16 entries в Traceability table.

    G) Дополнить .planning/ROADMAP.md — Phase 20 entry в Backlog обновить:
    ```markdown
    ### Phase 20: Управление закупками — Поставщики, Закупки, План закупок

    **Goal:** (existing — unchanged)

    **Plans:** 8 plans
    Plans:
    - [ ] 20-W0-PLAN.md — CBR API smoke check + curl probe + user checkpoint
    - [ ] 20-01-PLAN.md — Prisma schema + manual SQL migration (10 моделей + 6 enums + partial unique)
    - [ ] 20-02-PLAN.md — lib/cbr-rates.ts + /api/cron/cbr-rate-sync + dispatcher branch + AppSetting seed
    - [ ] 20-03-PLAN.md — app/actions/suppliers.ts — CRUD Suppliers + Contacts + ProductLinks + Negotiations
    - [ ] 20-04-PLAN.md — UI Suppliers — list + detail + 5 client components
    - [ ] 20-05-PLAN.md — app/actions/purchases.ts — CRUD Purchases + Payments с auto-deposit/balance
    - [ ] 20-06-PLAN.md — UI Purchases — list + detail + PurchasePaymentsEditor (D-16)
    - [ ] 20-07-PLAN.md — /procurement/plan MVP + ProcurementTabs
    - [ ] 20-08-PLAN.md — Sidebar nav + landing + UAT + deploy
    ```
  </action>
  <verify>
    <automated>grep -q "PROCUREMENT" components/layout/nav-items.ts && grep -q "Truck\|Package" components/layout/nav-items.ts && test -f .planning/phases/20-procurement/20-UAT.md && test -f .planning/phases/20-procurement/20-DEPLOY-NOTES.md && grep -c "PROCUREMENT-0" .planning/REQUIREMENTS.md && grep -q "20-W0-PLAN\|20-01-PLAN\|20-08-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>Sidebar nav updated; landing+dashboard cards added; UAT.md и DEPLOY-NOTES.md созданы; REQUIREMENTS.md + ROADMAP.md обновлены с 16+ entries для Phase 20</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT после deploy</name>
  <files>.planning/phases/20-procurement/20-UAT.md</files>
  <action>HUMAN-ONLY: после deploy.sh пользователь проходит все 24 пункта UAT в браузере на https://zoiten.pro</action>
  <what-built>Phase 20 complete — все 8 plans выполнены, deploy через bash deploy.sh на VPS прошёл, миграция применена, cron работает</what-built>
  <how-to-verify>
    1. `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — deploy должен пройти без ошибок (prisma migrate deploy успешен)
    2. Открыть .planning/phases/20-procurement/20-UAT.md
    3. Пройти ВСЕ 24 пункта чек-листа в браузере на https://zoiten.pro
    4. Особое внимание:
       - Шаг 12 — расчёт deposit/balance должен совпадать с CONTEXT.md D-08
       - Шаг 13 — конверсия в RUB через CBR rates работает
       - Шаги 20-22 — CBR sync функционирует
       - Шаги 23-24 — RBAC enforced
    5. Если есть проблемы — описать в этом checkpoint, Plan регрессии в Phase 20 backlog
    6. Если всё ok — type "approved"
  </how-to-verify>
  <resume-signal>Type "approved" если все 24 пункта UAT прошли, либо опиши проблемы для regression plan</resume-signal>
  <verify>
    <automated>test -f .planning/phases/20-procurement/20-UAT.md</automated>
  </verify>
  <done>Все 24 пункта UAT отмечены пользователем; никаких blocker issues</done>
</task>

</tasks>

<verification>
- All file changes (nav-items, landing, dashboard, docs) committed
- Deploy.sh успешно прошёл
- UAT 24 пункта подтверждены пользователем
</verification>

<success_criteria>
- Phase 20 production-ready
- REQUIREMENTS.md содержит PROCUREMENT-01..16 + Traceability
- ROADMAP.md Phase 20 entry с финальным plan count и list
- User signed off через checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-08-SUMMARY.md`
</output>
