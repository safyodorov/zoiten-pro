---
phase: 20-procurement
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - lib/sections.ts
  - components/layout/section-titles.ts
  - components/layout/nav-items.ts
  - lib/section-labels.ts
  - app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements: [D-10, D-11]
must_haves:
  truths:
    - "Middleware protects all /procurement/* routes via PROCUREMENT section"
    - "Sidebar shows a 'Управление закупками' group: Поставщики, Закупки, План закупок"
    - "Old /purchase-plan nav label and header title become 'План закупок (временный)'"
    - "SECTION_OPTIONS PROCUREMENT label becomes 'Управление закупками' so admins can toggle VIEW/MANAGE"
    - "Header shows correct titles for all /procurement/* pages"
  artifacts:
    - path: "lib/sections.ts"
      provides: "/procurement → PROCUREMENT route guard"
      contains: "/procurement"
    - path: "components/layout/nav-items.ts"
      provides: "3 new PROCUREMENT nav items + renamed temp plan"
      contains: "/procurement/suppliers"
    - path: "components/layout/section-titles.ts"
      provides: "Header titles for procurement pages"
      contains: "Поставщики"
  key_links:
    - from: "middleware.ts"
      to: "PROCUREMENT section"
      via: "SECTION_PATHS /procurement prefix"
      pattern: "\"/procurement\": \"PROCUREMENT\""
---

<objective>
Wire the new `/procurement/*` routes into the app shell: middleware RBAC route guard, sidebar nav group, header titles, and the user-management VIEW/MANAGE toggle label. Rename the existing temporary plan to «План закупок (временный)». This is the CLAUDE.md "новый раздел" checklist applied to a section whose enum already exists.

Purpose: Routes must be RBAC-guarded and navigable before pages are built.
Output: 5 edited files. No new routes rendered yet (pages come in 20-05/06/07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-procurement/20-RESEARCH.md
@CLAUDE.md
<interfaces>
Current lib/sections.ts SECTION_PATHS already maps "/purchase-plan": "PROCUREMENT". Keep it.
Current nav-items.ts line 42: { section: "PROCUREMENT", href: "/purchase-plan", label: "План закупок", icon: "ShoppingCart" }
Current section-titles.ts line 23: { match: /^\/purchase-plan/, title: "План закупок" }
Current lib/section-labels.ts line 18: { value: "PROCUREMENT", label: "План закупок" }
nav-items.ts ICON_MAP already imports/exports: Package, ShoppingCart, Truck is NOT yet imported.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route guard + section labels + dashboard</name>
  <read_first>
    - lib/sections.ts (full file — SECTION_PATHS object, `satisfies Record<string,string>`)
    - lib/section-labels.ts:18 (PROCUREMENT entry to relabel)
    - 20-RESEARCH.md §"New Section Checklist" (lines 438-450) + §"Pitfall 2" (lines 462-465)
    - app/(dashboard)/dashboard/page.tsx — existing PROCUREMENT card (find by "/purchase-plan" or "закуп") to update label/href
    - CLAUDE.md §"⚠ Чеклист при добавлении нового раздела ERP" (step 5 — section-labels is most forgotten)
  </read_first>
  <files>lib/sections.ts, lib/section-labels.ts, app/(dashboard)/dashboard/page.tsx</files>
  <action>
    1. lib/sections.ts: add `"/procurement": "PROCUREMENT",` to SECTION_PATHS (keep existing `"/purchase-plan": "PROCUREMENT"`). Middleware uses prefix matching → this guards all /procurement/* sub-routes (D-10/D-11).
    2. lib/section-labels.ts: change the PROCUREMENT SECTION_OPTIONS entry label from `"План закупок"` to `"Управление закупками"`. This is what makes the VIEW/MANAGE toggle appear in /admin/users for this section (per CLAUDE.md step 5).
    3. app/(dashboard)/dashboard/page.tsx: locate the existing procurement dashboard card (currently linking to /purchase-plan). Update it to point at `/procurement/suppliers` with label «Управление закупками» / description «Поставщики, закупки, платежи». If there is also a separate «План закупок» card for the temp page, leave it but rename to «План закупок (временный)». Do NOT change unrelated cards.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sections.ts|section-labels.ts|dashboard/page" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - lib/sections.ts contains `"/procurement": "PROCUREMENT"`
    - lib/sections.ts still contains `"/purchase-plan": "PROCUREMENT"`
    - lib/section-labels.ts contains `label: "Управление закупками"` for value PROCUREMENT (grep `Управление закупками` matches in section-labels.ts)
    - app/(dashboard)/dashboard/page.tsx references "/procurement/suppliers"
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Route guard added, SECTION_OPTIONS relabeled, dashboard card points to procurement suppliers.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar nav group + header titles</name>
  <read_first>
    - components/layout/nav-items.ts (full file — NAV_ITEMS array + ICON_MAP; note Truck/PackageCheck not yet imported)
    - components/layout/section-titles.ts (full file — SECTION_TITLES array, "more specific first" ordering rule)
    - 20-RESEARCH.md §"Pattern 8: Existing nav mutation" (lines 362-375) — exact new items + position rule (after COST/Себестоимость, before SALES/План продаж)
  </read_first>
  <files>components/layout/nav-items.ts, components/layout/section-titles.ts</files>
  <action>
    nav-items.ts:
    1. Import `Truck` and `PackageCheck` from "lucide-react" (add to the import block) and add both to ICON_MAP.
    2. In NAV_ITEMS, INSERT after the COST entry (`/batches`) and before SALES (`/sales-plan`), three new items:
       `{ section: "PROCUREMENT", href: "/procurement/suppliers", label: "Поставщики", icon: "Truck" }`
       `{ section: "PROCUREMENT", href: "/procurement/purchases", label: "Закупки", icon: "PackageCheck" }`
       `{ section: "PROCUREMENT", href: "/procurement/plan", label: "План закупок", icon: "ShoppingCart" }`
    3. Change the EXISTING `/purchase-plan` item label from `"План закупок"` to `"План закупок (временный)"` (keep its href, icon, section). Leave it in its current position (it remains a separate visible item until data migration — Defaults #6).

    section-titles.ts: add entries BEFORE the existing `/purchase-plan` match (more-specific-first). Add:
       `{ match: /^\/procurement\/suppliers\/[^/]+/, title: "Поставщик" }`
       `{ match: /^\/procurement\/suppliers/, title: "Поставщики" }`
       `{ match: /^\/procurement\/purchases\/[^/]+/, title: "Закупка" }`
       `{ match: /^\/procurement\/purchases/, title: "Закупки" }`
       `{ match: /^\/procurement\/plan/, title: "План закупок" }`
       `{ match: /^\/procurement/, title: "Управление закупками" }`
    And change the existing `/purchase-plan` title to `"План закупок (временный)"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "nav-items.ts|section-titles.ts" || echo "no-type-errors-in-edited-files"</automated>
  </verify>
  <acceptance_criteria>
    - nav-items.ts imports `Truck` and `PackageCheck` and both appear in ICON_MAP
    - nav-items.ts contains hrefs "/procurement/suppliers", "/procurement/purchases", "/procurement/plan"
    - nav-items.ts contains label "План закупок (временный)"
    - section-titles.ts contains title "Поставщики" and "Закупки" and "Управление закупками"
    - section-titles.ts /purchase-plan title is "План закупок (временный)"
    - tsc reports no new errors in these files
  </acceptance_criteria>
  <done>Sidebar shows the procurement group; header resolves all procurement page titles; temp plan renamed everywhere.</done>
</task>

</tasks>

<verification>
- tsc has no new errors
- Visiting /procurement/* would be RBAC-guarded (SECTION_PATHS) — verified by 20-07 UAT
- Sidebar group and header titles present
</verification>

<success_criteria>
- All 5 CLAUDE.md new-section checklist files updated (enum already existed → schema step done in 20-01)
- VIEW/MANAGE toggle available in /admin/users for PROCUREMENT
- Temp plan visibly renamed, not deleted
</success_criteria>

<output>
After completion, create `.planning/phases/20-procurement/20-02-SUMMARY.md`
</output>
