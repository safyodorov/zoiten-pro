---
phase: 19-wb-ads
plan: "08"
type: execute
wave: 7
depends_on: ["05"]
files_modified:
  - components/landing/variants/GlassmorphismLanding.tsx
  - app/(dashboard)/dashboard/page.tsx
  - DEPLOY.md
  - .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md
autonomous: false
requirements: [LANDING-CARD, DASHBOARD-LINK, DEPLOY-DOC]
must_haves:
  truths:
    - "Landing page содержит блок (карточку) «Управление рекламой» (как у других ERP-разделов в landing)"
    - "Dashboard страница (post-login) содержит карточку «Управление рекламой» со ссылкой на /ads"
    - "DEPLOY.md обновлён инструкциями: WB_ADS_TOKEN setup + первый backfill (curl /api/wb-adv-backfill?days=30) + проверка cron dispatcher"
    - "19-DEPLOY-NOTES.md содержит post-deploy UAT checklist для Phase 19 (включая ночную проверку cron в 03:00 МСК)"
  artifacts:
    - path: "components/landing/variants/GlassmorphismLanding.tsx"
      provides: "Карточка раздела «Управление рекламой» в landing"
      contains: "Управление рекламой"
    - path: "app/(dashboard)/dashboard/page.tsx"
      provides: "Карточка-ссылка на /ads"
      contains: "/ads"
    - path: "DEPLOY.md"
      provides: "Step-by-step deploy инструкции для Phase 19 (миграция + token + backfill)"
      contains: "WB_ADS_TOKEN"
    - path: ".planning/phases/19-wb-ads/19-DEPLOY-NOTES.md"
      provides: "Phase-specific post-deploy UAT checklist"
      min_lines: 30
  key_links:
    - from: "DEPLOY.md"
      to: "VPS deploy.sh execution"
      via: "Документация шагов post-prisma-migrate-deploy"
      pattern: "WB_ADS_TOKEN|wb-adv-backfill"
---

<objective>
Финальная mile delivery — публикация раздела в landing/dashboard + документация deploy процесса для Phase 19. Без этих хвостов пользователь может пропустить кнопку «Получить токен», не запустить первый backfill, и в первый день после deploy раздел будет пустым.

Purpose: Хорошо документированный deploy экономит время troubleshoot'a в случае проблем. Landing card делает раздел discoverable для будущих сотрудников.

Output: 2 UI карточки (landing + dashboard), документация deploy, post-deploy UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@CLAUDE.md
@DEPLOY.md
@components/landing/variants/GlassmorphismLanding.tsx
@app/(dashboard)/dashboard/page.tsx
@components/layout/nav-items.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить карточку «Управление рекламой» в landing + dashboard</name>
  <read_first>
    - components/landing/variants/GlassmorphismLanding.tsx (паттерн карточек ERP разделов)
    - app/(dashboard)/dashboard/page.tsx (паттерн dashboard карточек)
    - components/layout/nav-items.ts (Megaphone иконка уже импортирована)
  </read_first>
  <files>components/landing/variants/GlassmorphismLanding.tsx, app/(dashboard)/dashboard/page.tsx</files>
  <action>
    1. **components/landing/variants/GlassmorphismLanding.tsx** — найти массив/секцию карточек ERP разделов (поиск через `grep -n "Товары\|/products" components/landing/variants/GlassmorphismLanding.tsx`). Добавить новую карточку «Управление рекламой» рядом с другими (например, после «Управление ценами»):
       - Иконка: `Megaphone` из lucide-react (или похожая)
       - href: `/ads`
       - label: «Управление рекламой»
       - короткое описание: «Аналитика расходов на рекламу Wildberries: ДРР, CPC, CTR per кампания и товар»

       Стиль и формат — точно как у соседних карточек. Если landing использует данные из nav-items.ts — возможно, уже всё автоматически подхватывается, тогда задача просто проверить, что карточка появилась.

    2. **app/(dashboard)/dashboard/page.tsx** — аналогично landing, найти grid карточек разделов и добавить ссылку на /ads с иконкой Megaphone.

    3. `npx tsc --noEmit` — 0 errors.

    NB: `npm run build` НЕ запускаем здесь — это будет финальный шаг в Task 3 перед UAT (full build перед deploy).
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax. Pattern `\|` is bash regex alternation in grep; PowerShell не поддерживает.</note>
    <automated>grep -q "Управление рекламой" components/landing/variants/GlassmorphismLanding.tsx && grep -qE "Управление рекламой|/ads" "app/(dashboard)/dashboard/page.tsx" && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Landing page содержит карточку «Управление рекламой» со ссылкой на /ads
    - Dashboard page содержит карточку-ссылку на /ads
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Раздел discoverable на landing и post-login dashboard</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Обновить DEPLOY.md инструкциями Phase 19 + создать 19-DEPLOY-NOTES.md с UAT</name>
  <read_first>
    - DEPLOY.md (полный документ — формат, стиль, существующие WB token инструкции)
    - .planning/quick/260515-m5o-cards-wb-expandable-row-4-7-wbcardorders/260515-m5o-SUMMARY.md (паттерн deploy инструкций для cron)
    - .planning/phases/19-wb-ads/19-RESEARCH.md
    - .planning/phases/19-wb-ads/19-W0-NOTES.md (для актуального bit scope, чтобы упомянуть в DEPLOY.md)
  </read_first>
  <files>DEPLOY.md, .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md</files>
  <action>
    1. **DEPLOY.md** — найти секцию про WB tokens (где описаны `WB_API_TOKEN`, `WB_RETURNS_TOKEN`, `WB_CHAT_TOKEN`). Добавить:
       - Описание `WB_ADS_TOKEN`: scope bit «Реклама/Продвижение» (точный номер — из 19-W0-NOTES.md), bootstrap из env, последующий replace через UI `/admin/settings`.
       - Инструкция по генерации в ЛК WB → Настройки → API-токены → выбрать только scope «Продвижение».
       - Пример строки в `/etc/zoiten.pro.env`:
         ```bash
         WB_ADS_TOKEN=eyJ...
         ```

    2. **DEPLOY.md** — секция про cron (где описан systemd zoiten-cron-dispatch.timer). Phase 19 НЕ добавляет новый .service/.timer — расширяется существующий dispatcher. Документировать:
       - Default time WB Advert sync: `03:00` МСК (через AppSetting `wbAdvSyncCronTime`).
       - При желании изменить — через SQL `UPDATE "AppSetting" SET value = '04:30' WHERE key = 'wbAdvSyncCronTime';`.

    3. **DEPLOY.md** — добавить шаг «Первый backfill Phase 19» после `prisma migrate deploy`:
       ```bash
       # После применения миграции 20260519_phase19_wb_advert
       curl -X POST -H "x-cron-secret: $CRON_SECRET" \
         "http://localhost:3001/api/wb-adv-backfill?days=30"
       # Ожидание: { ok: true, days: 30, campaigns: N, statsUpserted: M }
       # Длительность: ~2-5 мин (зависит от числа активных кампаний)
       ```

    4. Создать `.planning/phases/19-wb-ads/19-DEPLOY-NOTES.md` с UAT checklist для пользователя. Обязательные разделы:
       - **Migration** — `prisma migrate deploy` запущен; 4 таблицы появились в `\d` Postgres
       - **Token** — `/etc/zoiten.pro.env` содержит `WB_ADS_TOKEN`; в UI `/admin/settings` 4-я карточка показывает «Токен валиден» + scope chips
       - **Backfill** — `curl POST /api/wb-adv-backfill?days=30` отдал 200 с разумным count
       - **UI smoke** — `/ads/wb` рендерится; видны товары с агрегатами; expand с графиком работает
       - **RBAC** — суперадмин видит ✓; VIEWER без права ADS получает 403
       - **Cron (W11 fix — explicit ночная проверка)** — после 03:00 МСК следующей ночи:
         ```bash
         # На VPS:
         psql zoiten_erp -c "SELECT value FROM \"AppSetting\" WHERE key = 'wbAdvSyncLastRun';"
         # Ожидание: вчерашняя дата (YYYY-MM-DD)
         psql zoiten_erp -c "SELECT COUNT(*) FROM \"WbAdvertStatDaily\" WHERE date >= CURRENT_DATE - INTERVAL '2 days';"
         # Ожидание: > 0 (новые row'ы за последний день)
         journalctl -u zoiten-cron-dispatch.service --since "1 hour ago" | grep adv
         # Ожидание: строки `[dispatch] adv:200` или `adv:error` (для исследования)
         ```
       - **Rollback** — план откатить если что-то не так:
         - Остановить cron (флаг в AppSetting или вернуть старую версию dispatcher)
         - Прибить запись из ENUM нельзя (PostgreSQL не разрешает DROP VALUE) — но можно просто оставить ADS неиспользуемым в `requireSection`
         - Откатить 4 таблицы через `DROP TABLE "WbAdvert..."` если совсем плохо

    **I15 — Deferred functionality note:**
    В конце 19-DEPLOY-NOTES.md (или в retrospective секции, если есть) добавить:
    ```
    ## Out of scope / Deferred (v1.1+)

    Авто-кампании (АРК) НЕ синхронизируются в Phase 19. WB Advert API endpoint `/adv/v2/auto/*`
    зарезервирован для будущего, но в текущем `lib/wb-adv-api.ts` не обращён. Если у пользователя
    появятся АРК — нужен отдельный quick task: расширить fetchPromotionAdverts + fetchFullStats
    + добавить флаг is_auto в WbAdvertCampaign. Cron orchestrator уже идемпотентен и сможет
    включить АРК без миграционных дополнений (только расширение колонки `type` справочника).
    ```
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax.</note>
    <automated>grep -q "WB_ADS_TOKEN" DEPLOY.md && grep -q "wb-adv-backfill" DEPLOY.md && test -f .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md && grep -q "UAT" .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md && grep -q "Rollback" .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md && grep -qE "03:00|wbAdvSyncLastRun" .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md && grep -qE "АРК|Deferred|Out of scope" .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - DEPLOY.md содержит инструкции для WB_ADS_TOKEN setup + первого backfill
    - 19-DEPLOY-NOTES.md существует с разделами: Migration / Token / Backfill / UI smoke / RBAC / Cron / Rollback
    - Раздел Cron содержит explicit step «после 03:00 МСК следующей ночи проверить psql + journalctl»
    - В конце документа есть секция Deferred/Out of scope с упоминанием АРК (авто-кампаний)
    - Все шаги воспроизводимы пользователем самостоятельно (без задавания вопросов)
  </acceptance_criteria>
  <done>Документация deploy полная; пользователь может развернуть Phase 19 на проде по чеклисту; явный ночной UAT шаг для cron включён</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews + final build + deploy + smoke check production</name>
  <files>.planning/phases/19-wb-ads/19-DEPLOY-NOTES.md</files>
  <what-built>Все компоненты Phase 19: schema/token/api-client/cron/UI/chart/tests/landing/docs</what-built>
  <how-to-verify>
    1. **Final build check (выполняется ОДИН раз для всей Phase 19 — full build only here):**
       ```bash
       npm run build
       ```
       Ожидание: exits 0, нет TS ошибок, нет critical warnings.
    2. `git pull && bash deploy.sh` на VPS (или delegate Claude через ssh — см. memory deploy delegation)
    3. Пройти UAT checklist из `.planning/phases/19-wb-ads/19-DEPLOY-NOTES.md` пункты Migration → Token → Backfill → UI smoke → RBAC
    4. **На следующее утро** (после 03:00 МСК) — пройти UAT пункт Cron (psql + journalctl)
    5. Если есть проблемы — описать, что не так
    6. Если всё OK — подтвердить approval
  </how-to-verify>
  <action>Пользователь выполняет финальный `npm run build`, делает deploy на VPS, проходит UAT checklist из 19-DEPLOY-NOTES.md и подтверждает approval либо запускает gap closure (если что-то не работает на проде).</action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax.</note>
    <automated>npm run build 2>&1 | tail -10 && test -f .planning/phases/19-wb-ads/19-DEPLOY-NOTES.md</automated>
  </verify>
  <done>Final `npm run build` succeeded; UAT approved пользователем или сформирован список gap'ов для plan-revision</done>
  <resume-signal>Type "approved" если UAT прошёл (включая ночную проверку cron на следующее утро). Иначе опиши проблемы — потребуется gap closure plan-revision.</resume-signal>
</task>

</tasks>

<verification>
- `grep "WB_ADS_TOKEN\|wb-adv-backfill" DEPLOY.md` показывает hits
- `.planning/phases/19-wb-ads/19-DEPLOY-NOTES.md` существует с UAT checklist (включая ночной cron-step и АРК-deferred note)
- Landing и Dashboard содержат /ads карточки
- Final `npm run build` в Task 3 exits 0 — это единственный full build для Phase 19
</verification>

<success_criteria>
- Phase 19 готова к публикации на проде
- Документация позволяет пользователю развернуть и проверить раздел самостоятельно
- Rollback план задокументирован на случай проблем
- UAT checklist финализирует приёмку (включая ночную проверку cron в 03:00 МСК)
- Deferred functionality (АРК) явно отмечена для v1.1+
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-08-SUMMARY.md`
After all plans completed, optionally create `.planning/phases/19-wb-ads/19-RETRO.md` для cross-phase lessons.
</output>
</content>
</invoke>