---
phase: 19-wb-ads
plan: "03"
type: tdd
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/wb-adv-api.ts
  - lib/wb-cooldown.ts
  - tests/wb-adv-api.test.ts
autonomous: true
requirements: [API-PROMOTION-COUNT, API-PROMOTION-ADVERTS, API-FULLSTATS, API-BALANCE, API-RATE-LIMIT-PROTECTION]
must_haves:
  truths:
    - "lib/wb-adv-api.ts экспортирует функции fetchPromotionCount(), fetchFullStats(advertIds, dateRange), fetchBalance() — 3 функции (БЕЗ fetchPromotionAdverts — endpoint deprecated, см. 19-W0-NOTES)"
    - "Все функции используют await getWbToken('WB_ADS_TOKEN') (не process.env)"
    - "На 429 ответ функции бросают WbRateLimitError с retryAfterSec, как существующий retryFetch паттерн"
    - "fetchFullStats бьёт батчами ≤100 advertId с 1.1 сек паузой между батчами (1 req/sec лимит + 100ms буфер)"
    - "fetchPromotionCount соблюдает 5 req/sec rate (200ms sleep)"
    - "fetchFullStats обрабатывает null response (если по advertId/period нет данных)"
    - "WbAdvertStat включает поле canceled (W0 found in response)"
    - "WbAdvertBalance shape: {balance: int, net: int, currency: 'RUB'} БЕЗ bonus (W0)"
    - "Cooldown bus поддерживает новый bucket 'advert' (изолирован от WB_API_TOKEN buckets)"
  artifacts:
    - path: "lib/wb-adv-api.ts"
      provides: "Type-safe WB Advert API client (3 функции + типы responses)"
      min_lines: 180
      exports: ["fetchPromotionCount", "fetchFullStats", "fetchBalance", "WbAdvertCount", "WbAdvertStat", "WbAdvertBalance"]
    - path: "tests/wb-adv-api.test.ts"
      provides: "Unit-тесты: 200 OK + 429 retry + batch logic + scope mismatch"
      min_lines: 100
    - path: "lib/wb-cooldown.ts"
      provides: "WB_COOLDOWN_BUCKETS расширен значением 'advert' + resolveBucketFromUrl распознаёт advert-api"
      contains: "advert"
  key_links:
    - from: "lib/wb-adv-api.ts"
      to: "lib/wb-token.ts:getWbToken"
      via: "Все функции вызывают await getWbToken('WB_ADS_TOKEN')"
      pattern: "getWbToken.*WB_ADS_TOKEN"
    - from: "lib/wb-adv-api.ts"
      to: "lib/wb-cooldown.ts:resolveBucketFromUrl + setWbCooldownUntil"
      via: "On-429 пишет cooldown по bucket 'advert'"
      pattern: "advert"
---

<objective>
Создать `lib/wb-adv-api.ts` — type-safe WB Advert API client с 4 функциями: fetchPromotionCount, fetchPromotionAdverts, fetchFullStats, fetchBalance. Реализовать rate-limit защиту (429 backoff + cooldown bus) и batch-логику (fullstats ≤100 advertId, sleep 1с между батчами).

Purpose: Без этого слоя cron в Plan 19-04 не может разговаривать с WB. Изоляция в отдельном модуле (как `lib/wb-support-api.ts`) даёт type safety и упрощает мокинг в тестах.

Output: lib/wb-adv-api.ts + tests/wb-adv-api.test.ts. Cron Plan 19-04 импортирует 4 функции и не задумывается о rate limit'ах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@.planning/phases/19-wb-ads/19-W0-NOTES.md
@.planning/quick/260513-khv-per-endpoint-cooldown-locks-refactor-wbc/260513-khv-SUMMARY.md
@CLAUDE.md
@lib/wb-api.ts
@lib/wb-support-api.ts
@lib/wb-cooldown.ts
@lib/wb-token.ts
@tests/wb-fetch-rate-limit.test.ts

<interfaces>
<!-- Паттерны из существующих модулей. Tests/wb-fetch-rate-limit как образец 429 retry test. -->

From lib/wb-token.ts:
```typescript
export async function getWbToken(name: WbTokenName): Promise<string>
```

From lib/wb-api.ts:
```typescript
export class WbRateLimitError extends Error {
  retryAfterSec: number
  constructor(endpoint: string, retryAfterSec: number) {
    super(`WB rate limit: ${endpoint} (retry after ${retryAfterSec}s)`)
    this.retryAfterSec = retryAfterSec
  }
}
```

From lib/wb-cooldown.ts:
```typescript
export const WB_COOLDOWN_BUCKETS = [
  "statistics-stocks",
  "statistics-orders",
  "statistics-sales",
  "prices",
  "tariffs",
  "analytics",
  "content",
  "feedbacks",
  "questions",
] as const
export type WbCooldownBucket = (typeof WB_COOLDOWN_BUCKETS)[number]
export function resolveBucketFromUrl(url: string): WbCooldownBucket | null
export async function getWbCooldownSecondsRemaining(bucket: WbCooldownBucket): Promise<number>
export async function setWbCooldownUntil(bucket: WbCooldownBucket, retryAfterSec: number): Promise<void>
```

WB Advert API base URL: `https://advert-api.wildberries.ru`

**Phase 19 W0 (2026-05-19) RESOLVED** (см. memory/project_wb_advert_api.md и 19-W0-NOTES.md):

- ✅ `GET /adv/v1/promotion/count` → `{ adverts: Array<{ type, status, count, advert_list: Array<{ advertId, changeTime }> }> }` — двухуровневая структура, требует flatten
- ❌ `POST /adv/v1/promotion/adverts` → **404 deprecated**. WORKAROUND: deriving nmId targets из `fullstats.days[].apps[].nms[].nmId` (union). **НЕ создавать `fetchPromotionAdverts` функцию** — она удалена из API.
- ✅ `GET /adv/v3/fullstats?ids=N1,N2&beginDate=YYYY-MM-DD&endDate=YYYY-MM-DD` — **v3 GET с query params** (НЕ POST v2 с body!). Max 100 advertId, max 31 день. Returns `null` или массив. Shape (4-level nesting):
  ```
  Array<{
    advertId, atbs, canceled, clicks, cpc, cr, ctr, currency, orders, shks, sum, sum_price, views,
    boosterStats: Array<{ date, nm, avg_position }>,
    days: Array<{
      date: "YYYY-MM-DDTHH:MM:SSZ",
      atbs, clicks, cpc, cr, ctr, orders, shks, sum, sum_price, views,
      apps: Array<{
        appType, atbs, canceled, clicks, cpc, cr, ctr, orders, shks, sum, sum_price, views,
        nms: Array<{  // ПЛЮРАЛ! не "nm"
          nmId, name, atbs, canceled, clicks, cpc, cr, ctr, orders, shks, sum, sum_price, views
        }>
      }>
    }>
  }>
  ```
- ✅ `GET /adv/v1/balance` → `{ balance: int, net: int, currency: "RUB" }` — **поле `bonus` ОТСУТСТВУЕТ** (W0 проверка).

**Rate limits:** per-seller global limiter (НЕ per-token!). Per-endpoint: `/promotion/*` 5 req/sec, `/fullstats` 1 req/sec. Headers `x-ratelimit-{limit,retry,reset}`. Подробно: memory/project_wb_advert_api.md § Rate limits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить lib/wb-cooldown.ts bucket'ом 'advert' + resolveBucketFromUrl</name>
  <read_first>
    - lib/wb-cooldown.ts (особенно resolveBucketFromUrl)
    - tests/wb-cooldown.test.ts (текущий файл — содержит assertion `expect(WB_COOLDOWN_BUCKETS).toHaveLength(9)` или аналогичную; нужно обновить на 10)
    - .planning/quick/260513-khv-per-endpoint-cooldown-locks-refactor-wbc/260513-khv-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `resolveBucketFromUrl("https://advert-api.wildberries.ru/adv/v1/promotion/count")` === `"advert"`
    - Test 2: `resolveBucketFromUrl("https://advert-api.wildberries.ru/adv/v2/fullstats")` === `"advert"`
    - Test 3: `resolveBucketFromUrl("https://content-api.wildberries.ru/ping")` === `"content"` (regression — existing buckets не сломаны)
    - Test 4: `WB_COOLDOWN_BUCKETS` массив содержит `"advert"`
    - Test 5 (regression): `WB_COOLDOWN_BUCKETS.length` === 10 (было 9, стало 10)
  </behavior>
  <files>lib/wb-cooldown.ts, tests/wb-cooldown.test.ts (extend existing)</files>
  <action>
    1. В `lib/wb-cooldown.ts` расширить массив `WB_COOLDOWN_BUCKETS` значением `"advert"`:
       ```typescript
       export const WB_COOLDOWN_BUCKETS = [
         "statistics-stocks",
         "statistics-orders",
         "statistics-sales",
         "prices",
         "tariffs",
         "analytics",
         "content",
         "feedbacks",
         "questions",
         "advert",            // Phase 19 — WB_ADS_TOKEN scope
       ] as const
       ```

    2. В функции `resolveBucketFromUrl(url)` (примерно строка 58+) добавить ветку:
       ```typescript
       if (url.includes("advert-api.wildberries.ru")) return "advert"
       ```
       Поставить ДО других проверок или после — порядок не важен, домены не пересекаются.

    3. **КРИТИЧНО — обновить существующие assertions в `tests/wb-cooldown.test.ts`:**
       - Найти любое `expect(WB_COOLDOWN_BUCKETS).toHaveLength(9)` / `expect(WB_COOLDOWN_BUCKETS.length).toBe(9)` / `.toEqual([...9 items...])` и обновить до 10.
       - Если в файле есть массив-сравнение с full enumeration buckets — добавить `"advert"` в конец.
       - Если таких ассерций нет — не страшно, но grep'ом проверить (`grep -nE "toHaveLength|toBe\(9\)|length.*9" tests/wb-cooldown.test.ts`).

    4. Добавить новые тесты (Test 1-4 из behavior) — пять штук, включая explicit `expect(WB_COOLDOWN_BUCKETS).toContain("advert")` и `expect(WB_COOLDOWN_BUCKETS.length).toBe(10)`.

    Намеренно: bucket `advert` отделён от WB_API_TOKEN scope-ов. WB_ADS_TOKEN живёт по своим rate-limit правилам и не должен caнamage'ить WB_API_TOKEN при 429 (и vice versa).
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify command uses bash-only syntax.</note>
    <automated>npm run test -- tests/wb-cooldown.test.ts --run 2>&1 | tail -5 && grep -q '"advert"' lib/wb-cooldown.ts && ! grep -nE "toHaveLength\(9\)|length.*toBe\(9\)" tests/wb-cooldown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wb-cooldown.ts` массив `WB_COOLDOWN_BUCKETS` содержит `"advert"` (length = 10)
    - `resolveBucketFromUrl("https://advert-api.wildberries.ru/...")` возвращает `"advert"`
    - Существующие assertions в tests/wb-cooldown.test.ts обновлены с 9 → 10 (нет stale `toHaveLength(9)` или `toBe(9)` для bucket length)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Bucket 'advert' изолирован, 429 на Advert API не блокирует другие WB endpoint'ы; существующие assertions с count=9 обновлены</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать lib/wb-adv-api.ts + tests/wb-adv-api.test.ts</name>
  <read_first>
    - lib/wb-support-api.ts (паттерн callApi с baseUrl/token/path)
    - lib/wb-api.ts (wbFetch + WbRateLimitError pattern; особенно строки 698-770)
    - tests/wb-fetch-rate-limit.test.ts (паттерн mock fetch для 429 retry)
    - .planning/phases/19-wb-ads/19-W0-NOTES.md (реальный shape responses)
  </read_first>
  <behavior>
    - Test 1: `fetchPromotionCount()` вызывает `getWbToken("WB_ADS_TOKEN")` (через vi.mock) и GET к `advert-api.wildberries.ru/adv/v1/promotion/count`; flatten двухуровневой структуры в массив `{ advertId, type, status, changeTime }`.
    - Test 2: На 429 с `X-Ratelimit-Retry: 60` (WB header — не стандартный Retry-After!) бросает `WbRateLimitError` с `retryAfterSec === 60` (не повторяет автоматически — caller решает). Также проверять fallback на `Retry-After` если WB header отсутствует.
    - Test 3: `fetchFullStats([1, 2, ..., 250], { beginDate, endDate })` делает 3 батча (100/100/50) с sleep ≥1100ms между ними. Все 3 batch'a вызывают `fetch` с правильным URL (query params `?ids=N1,N2&beginDate=YYYY-MM-DD&endDate=YYYY-MM-DD`, метод GET). Возвращает плоский массив `{ advertId, date, nmId, name, appType, views, clicks, sum, canceled, ... }`.
    - Test 4: `fetchFullStats` обрабатывает `null` response (вернёт `[]` без падения).
    - Test 5: `fetchBalance()` GET к `/adv/v1/balance`, возвращает `{ balance: number, net: number, currency: string }` (БЕЗ bonus — он отсутствует в API).
    - Test 6: Cooldown bus check: если `getWbCooldownSecondsRemaining("advert")` > 0, fetchPromotionCount бросает `WbRateLimitError` без обращения к WB.
  </behavior>
  <files>lib/wb-adv-api.ts, tests/wb-adv-api.test.ts</files>
  <action>
    Создать `lib/wb-adv-api.ts` со следующей структурой:

    ```typescript
    // Phase 19: WB Advert API client. W0 verified 2026-05-19.
    // Endpoints: advert-api.wildberries.ru/adv/{v1,v3}/...
    //   - /adv/v1/promotion/count (GET)         — flatten двухуровневой структуры
    //   - /adv/v3/fullstats (GET с query params) — НЕ v2 POST!
    //   - /adv/v1/balance (GET)                 — нет поля bonus
    // Rate limits: /promotion/* 5 req/sec, /fullstats 1 req/sec batch ≤100,
    //   PLUS per-seller global limiter (общий для всех endpoints/токенов одного sid).
    // Cooldown bus: bucket 'advert' (изолирован от WB_API_TOKEN scope).
    // Token: WB_ADS_TOKEN scope bit 30 (W0 verified).

    import { getWbToken } from "@/lib/wb-token"
    import {
      getWbCooldownSecondsRemaining,
      setWbCooldownUntil,
    } from "@/lib/wb-cooldown"
    import { WbRateLimitError } from "@/lib/wb-api"

    const BASE_URL = "https://advert-api.wildberries.ru"
    const FULLSTATS_BATCH_SIZE = 100
    const PROMOTION_RATE_SLEEP_MS = 200      // 5 req/sec
    const FULLSTATS_RATE_SLEEP_MS = 1100     // 1 req/sec + 100ms буфер от per-seller limiter

    // ── Types ─────────────────────────────────────────────────────────

    export interface WbAdvertCount {
      advertId: number
      type: number
      status: number
      changeTime: string             // ISO с timezone offset, например "2026-05-19T13:18:01+03:00"
    }

    export interface WbAdvertStat {
      advertId: number
      date: string                   // YYYY-MM-DD (обрезаем ISO из API)
      nmId: number
      name: string | null            // название товара из API (для UI без JOIN'а с WbCard)
      appType: number
      views: number
      clicks: number
      ctr: number | null
      cpc: number | null
      sum: number
      atbs: number
      orders: number
      cr: number | null
      shks: number
      sumPrice: number
      canceled: number               // W0: technical cancels (товар недоступен/доставка failed). НЕ buyer refusals.
    }

    export interface WbAdvertBalance {
      balance: number
      net: number
      currency: string               // обычно "RUB"
    }

    // ── Helpers ───────────────────────────────────────────────────────

    function sleep(ms: number): Promise<void> {
      return new Promise(r => setTimeout(r, ms))
    }

    /** Прочитать retry-after из 429 response. WB Advert API использует
     *  кастомный заголовок `x-ratelimit-retry` (см. memory/project_wb_advert_api.md),
     *  с fallback на стандартный Retry-After. */
    function parseRetryAfter(res: Response): number {
      const wb = res.headers.get("x-ratelimit-retry")
      if (wb) return parseInt(wb, 10) || 60
      const std = res.headers.get("Retry-After")
      if (std) return parseInt(std, 10) || 60
      return 60
    }

    async function callAdvert(
      url: string,
      init: RequestInit = {},
    ): Promise<Response> {
      const cooldown = await getWbCooldownSecondsRemaining("advert")
      if (cooldown > 0) {
        throw new WbRateLimitError(`Advert API ${url}`, cooldown)
      }
      const token = await getWbToken("WB_ADS_TOKEN")
      const res = await fetch(url, {
        ...init,
        headers: {
          Authorization: token,
          ...(init.body && !(init.body instanceof FormData)
            ? { "Content-Type": "application/json" }
            : {}),
          ...init.headers,
        },
      })
      if (res.status === 429) {
        const retryAfter = parseRetryAfter(res)
        await setWbCooldownUntil("advert", retryAfter)
        throw new WbRateLimitError(`Advert API ${url}`, retryAfter)
      }
      if (!res.ok) {
        const text = await res.text().catch(() => "")
        throw new Error(`Advert API ${url} → HTTP ${res.status}: ${text}`)
      }
      return res
    }

    // ── Public API ────────────────────────────────────────────────────

    /** GET /adv/v1/promotion/count → плоский список (advertId, type, status, changeTime).
     *  W0: response — двухуровневая структура {adverts: [{type, status, count, advert_list}]}. */
    export async function fetchPromotionCount(): Promise<WbAdvertCount[]> {
      await sleep(PROMOTION_RATE_SLEEP_MS)
      const res = await callAdvert(`${BASE_URL}/adv/v1/promotion/count`)
      const data = await res.json() as {
        adverts: Array<{
          type: number
          status: number
          count: number
          advert_list: Array<{ advertId: number; changeTime: string }>
        }>
      }
      const out: WbAdvertCount[] = []
      for (const group of data.adverts ?? []) {
        for (const item of group.advert_list ?? []) {
          out.push({
            advertId: item.advertId,
            type: group.type,
            status: group.status,
            changeTime: item.changeTime,
          })
        }
      }
      return out
    }

    /** GET /adv/v3/fullstats?ids=...&beginDate=...&endDate=... — батчами по ≤100 advertId.
     *  W0: GET с query params (НЕ POST с body!). Response может быть null если по IDs/period нет данных.
     *  4-level nesting: campaign → day → app → nm (плюрал!) */
    export async function fetchFullStats(
      advertIds: number[],
      range: { beginDate: string; endDate: string }, // 'YYYY-MM-DD'
    ): Promise<WbAdvertStat[]> {
      const all: WbAdvertStat[] = []
      for (let i = 0; i < advertIds.length; i += FULLSTATS_BATCH_SIZE) {
        if (i > 0) await sleep(FULLSTATS_RATE_SLEEP_MS)
        const batch = advertIds.slice(i, i + FULLSTATS_BATCH_SIZE)
        const url = new URL(`${BASE_URL}/adv/v3/fullstats`)
        url.searchParams.set("ids", batch.join(","))
        url.searchParams.set("beginDate", range.beginDate)
        url.searchParams.set("endDate", range.endDate)
        const res = await callAdvert(url.toString())
        const raw = await res.json() as
          | Array<{
              advertId: number
              days?: Array<{
                date: string  // например "2026-04-19T00:00:00Z"
                apps?: Array<{
                  appType: number
                  nms?: Array<{
                    nmId: number
                    name?: string
                    views: number
                    clicks: number
                    ctr?: number
                    cpc?: number
                    sum: number
                    atbs: number
                    orders: number
                    cr?: number
                    shks: number
                    sum_price: number
                    canceled?: number
                  }>
                }>
              }>
            }>
          | null
        if (raw === null) continue   // W0: null = нет данных для этого батча/периода
        for (const camp of raw) {
          for (const day of camp.days ?? []) {
            const dateStr = day.date.slice(0, 10)
            for (const app of day.apps ?? []) {
              for (const nm of app.nms ?? []) {
                all.push({
                  advertId: camp.advertId,
                  date: dateStr,
                  nmId: nm.nmId,
                  name: nm.name ?? null,
                  appType: app.appType,
                  views: nm.views,
                  clicks: nm.clicks,
                  ctr: nm.ctr ?? null,
                  cpc: nm.cpc ?? null,
                  sum: nm.sum,
                  atbs: nm.atbs,
                  orders: nm.orders,
                  cr: nm.cr ?? null,
                  shks: nm.shks,
                  sumPrice: nm.sum_price,
                  canceled: nm.canceled ?? 0,
                })
              }
            }
          }
        }
      }
      return all
    }

    /** GET /adv/v1/balance → текущий баланс рекламного счёта.
     *  W0: response — {balance: int, net: int, currency: string}. Поля bonus НЕТ. */
    export async function fetchBalance(): Promise<WbAdvertBalance> {
      await sleep(PROMOTION_RATE_SLEEP_MS)
      const res = await callAdvert(`${BASE_URL}/adv/v1/balance`)
      const data = await res.json() as {
        balance: number
        net: number
        currency: string
      }
      return {
        balance: data.balance,
        net: data.net,
        currency: data.currency ?? "RUB",
      }
    }
    ```

    **Корректировка по W0-NOTES:** все три endpoint'а уже отражают реальный shape по 19-W0-NOTES + memory/project_wb_advert_api.md (verified 2026-05-19). fetchPromotionAdverts удалена — endpoint deprecated (404). nmId targets для WbAdvertTarget будут собираться в Plan 19-04 как union из fetchFullStats(...).map(r => r.nmId). Поле `name` в WbAdvertStat прокидывается из nms[].name — может пригодиться в UI без JOIN'а к WbCard.

    Создать `tests/wb-adv-api.test.ts` (vitest):

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    vi.mock("@/lib/wb-token", () => ({
      getWbToken: vi.fn(async () => "test-token"),
    }))
    vi.mock("@/lib/wb-cooldown", () => ({
      getWbCooldownSecondsRemaining: vi.fn(async () => 0),
      setWbCooldownUntil: vi.fn(async () => {}),
    }))

    const fetchMock = vi.fn()
    vi.stubGlobal("fetch", fetchMock)

    import {
      fetchPromotionCount,
      fetchFullStats,
      fetchBalance,
    } from "@/lib/wb-adv-api"
    import { WbRateLimitError } from "@/lib/wb-api"
    import { setWbCooldownUntil, getWbCooldownSecondsRemaining } from "@/lib/wb-cooldown"

    beforeEach(() => {
      fetchMock.mockReset()
      vi.mocked(setWbCooldownUntil).mockClear()
      vi.mocked(getWbCooldownSecondsRemaining).mockReset()
      vi.mocked(getWbCooldownSecondsRemaining).mockResolvedValue(0)
    })

    // Test 1
    it("fetchPromotionCount parses /promotion/count two-level structure", async () => {
      fetchMock.mockResolvedValueOnce(new Response(JSON.stringify({
        adverts: [
          { type: 9, status: 9, count: 2, advert_list: [
            { advertId: 1, changeTime: "2026-05-19T00:00:00+03:00" },
            { advertId: 2, changeTime: "2026-05-19T00:00:00+03:00" },
          ]},
        ],
      })))
      const res = await fetchPromotionCount()
      expect(res).toHaveLength(2)
      expect(res[0]).toMatchObject({ advertId: 1, type: 9, status: 9 })
    })

    // Test 2 — 429 + WB-specific x-ratelimit-retry header (preferred over Retry-After)
    it("on 429 reads x-ratelimit-retry and sets cooldown", async () => {
      fetchMock.mockResolvedValueOnce(new Response("rate-limited", {
        status: 429,
        headers: { "x-ratelimit-retry": "60" },
      }))
      await expect(fetchPromotionCount()).rejects.toBeInstanceOf(WbRateLimitError)
      expect(setWbCooldownUntil).toHaveBeenCalledWith("advert", 60)
    })

    // Test 3 — fullstats GET with query params, batching ≤100
    it("fetchFullStats sends GET batches of 100 with query params + sleep", async () => {
      vi.useFakeTimers()
      const advertIds = Array.from({ length: 250 }, (_, i) => i + 1)
      fetchMock.mockResolvedValue(new Response(JSON.stringify([])))
      const promise = fetchFullStats(advertIds, { beginDate: "2026-05-12", endDate: "2026-05-19" })
      await vi.runAllTimersAsync()
      await promise
      expect(fetchMock).toHaveBeenCalledTimes(3) // 100, 100, 50
      // Должен быть GET (без method или method: "GET")
      const firstInit = fetchMock.mock.calls[0][1] as RequestInit | undefined
      expect(firstInit?.method ?? "GET").toBe("GET")
      // URL должен содержать query params
      const firstUrl = fetchMock.mock.calls[0][0] as string
      expect(firstUrl).toContain("/adv/v3/fullstats")
      expect(firstUrl).toContain("beginDate=2026-05-12")
      expect(firstUrl).toContain("endDate=2026-05-19")
      expect(firstUrl).toContain("ids=1%2C2") // URL-encoded comma
      // Последний батч — 50 ids
      const lastUrl = fetchMock.mock.calls[2][0] as string
      const lastIds = new URL(lastUrl).searchParams.get("ids")!.split(",")
      expect(lastIds).toHaveLength(50)
      vi.useRealTimers()
    })

    // Test 4 — null response handling (W0: WB returns null when no data)
    it("fetchFullStats handles null response (no data for period)", async () => {
      fetchMock.mockResolvedValueOnce(new Response(JSON.stringify(null)))
      const res = await fetchFullStats([1], { beginDate: "2026-05-12", endDate: "2026-05-19" })
      expect(res).toEqual([])
    })

    // Test 4b — fullstats with real nested data (4-level: campaign → day → app → nms)
    it("fetchFullStats flattens 4-level nesting", async () => {
      fetchMock.mockResolvedValueOnce(new Response(JSON.stringify([
        {
          advertId: 100,
          days: [
            {
              date: "2026-05-15T00:00:00Z",
              apps: [
                {
                  appType: 32,
                  nms: [
                    { nmId: 999, name: "Test product", views: 100, clicks: 5,
                      sum: 75.5, atbs: 2, orders: 1, shks: 1, sum_price: 1500,
                      ctr: 5.0, cpc: 15.1, cr: 20.0, canceled: 0 },
                  ],
                },
              ],
            },
          ],
        },
      ])))
      const res = await fetchFullStats([100], { beginDate: "2026-05-15", endDate: "2026-05-15" })
      expect(res).toHaveLength(1)
      expect(res[0]).toMatchObject({
        advertId: 100, date: "2026-05-15", nmId: 999, name: "Test product",
        appType: 32, views: 100, clicks: 5, sum: 75.5, sumPrice: 1500, canceled: 0,
      })
    })

    // Test 5 — balance shape (W0: no bonus, has currency)
    it("fetchBalance parses /balance response without bonus", async () => {
      fetchMock.mockResolvedValueOnce(new Response(JSON.stringify({
        balance: 0, net: 2561471, currency: "RUB",
      })))
      const res = await fetchBalance()
      expect(res).toEqual({ balance: 0, net: 2561471, currency: "RUB" })
    })

    // Test 6 — cooldown bypass
    it("if cooldown active throws WbRateLimitError without fetch call", async () => {
      vi.mocked(getWbCooldownSecondsRemaining).mockReset()
      vi.mocked(getWbCooldownSecondsRemaining).mockResolvedValueOnce(300)
      vi.mocked(getWbCooldownSecondsRemaining).mockResolvedValue(0)
      await expect(fetchPromotionCount()).rejects.toBeInstanceOf(WbRateLimitError)
      expect(fetchMock).not.toHaveBeenCalled()
    })
    ```

    Запустить `npm run test -- tests/wb-adv-api.test.ts --run` — все 6 тестов должны быть GREEN.

    Запустить `npx tsc --noEmit` — 0 errors.
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify command uses bash-only syntax (&& chaining, grep).</note>
    <automated>npm run test -- tests/wb-adv-api.test.ts --run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -3 && grep -q "export async function fetchPromotionCount" lib/wb-adv-api.ts && grep -q "export async function fetchFullStats" lib/wb-adv-api.ts && grep -q "export async function fetchBalance" lib/wb-adv-api.ts && grep -q "export async function fetchPromotionAdverts" lib/wb-adv-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wb-adv-api.ts` экспортирует 3 функции: fetchPromotionCount, fetchFullStats, fetchBalance (БЕЗ fetchPromotionAdverts — endpoint deprecated)
    - Все функции вызывают `await getWbToken("WB_ADS_TOKEN")`
    - На 429 функции читают header `x-ratelimit-retry` (с fallback на `Retry-After`) и бросают WbRateLimitError + setWbCooldownUntil("advert", ...)
    - fetchFullStats — GET к `/adv/v3/fullstats` с query params `?ids=N1,N2&beginDate=YYYY-MM-DD&endDate=YYYY-MM-DD`, батчами по 100 с sleep ≥1100ms
    - fetchFullStats обрабатывает `null` response (вернёт `[]`)
    - WbAdvertStat включает поля `name: string | null` и `canceled: number`
    - WbAdvertBalance shape: `{balance, net, currency}` БЕЗ `bonus`
    - `tests/wb-adv-api.test.ts` содержит 7 тестов (1, 2, 3, 4, 4b, 5, 6) — все GREEN
    - Test 6 использует explicit `mockReset()` + chained `mockResolvedValueOnce(300)` + `mockResolvedValue(0)` (no race)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Type-safe client готов; Plan 19-04 cron может оркестрировать вызовы без знания HTTP/rate-limit деталей</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/wb-adv-api.test.ts tests/wb-cooldown.test.ts --run` exits 0
- `npx tsc --noEmit` exits 0
- `grep -c "export async function" lib/wb-adv-api.ts` ≥ 4
</verification>

<success_criteria>
- 4 функции wb-adv-api готовы и протестированы
- Rate-limit защита (cooldown bus + 429 handling) работает
- Batch logic для fullstats соответствует WB докам (≤100 + sleep 1с)
- TypeScript типы экспортированы для use в cron (Plan 19-04) и UI (Plan 19-05)
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-03-SUMMARY.md`
</output>
</content>
</invoke>