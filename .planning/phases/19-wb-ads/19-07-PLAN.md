---
phase: 19-wb-ads
plan: "07"
type: tdd
wave: 7
depends_on: ["03", "04", "05", "06"]
files_modified:
  - tests/wb-adv-api-rate-limit.test.ts
  - tests/wb-adv-fullstats-batch.test.ts
  - tests/wb-adv-period-range.test.ts
  - tests/wb-adv-aggregations-edge.test.ts
autonomous: true
requirements: [TEST-RATE-LIMIT, TEST-BATCH, TEST-PERIOD-RANGE, TEST-AGGREGATION-EDGE]
must_haves:
  truths:
    - "tests/wb-adv-api-rate-limit.test.ts проверяет 429 backoff + cooldown bus integration"
    - "tests/wb-adv-fullstats-batch.test.ts проверяет батч-логику ≤100 + sleep 1с"
    - "tests/wb-adv-period-range.test.ts проверяет date math (rolling 7/14/28 MSK без UTC drift)"
    - "tests/wb-adv-aggregations-edge.test.ts покрывает edge cases (divide by zero, NaN guard, пустые массивы)"
    - "Все новые тесты GREEN; пред'идущие тесты Phase 19 (wb-adv-api / wb-adv-cron / wb-advert-aggregations / wb-advert-chart-fill) не сломаны"
  artifacts:
    - path: "tests/wb-adv-api-rate-limit.test.ts"
      provides: "Углублённые тесты rate-limit поведения lib/wb-adv-api.ts"
      min_lines: 60
    - path: "tests/wb-adv-fullstats-batch.test.ts"
      provides: "Batch boundary тесты для fetchFullStats (99, 100, 101, 250 advertIds)"
      min_lines: 50
    - path: "tests/wb-adv-period-range.test.ts"
      provides: "Тесты date math: midnight MSK boundary, period=7/14/28, корректность 'yesterday'"
      min_lines: 50
    - path: "tests/wb-adv-aggregations-edge.test.ts"
      provides: "Edge cases для aggregateStats/groupByProduct/groupByCampaign (deep корректность)"
      min_lines: 60
  key_links:
    - from: "tests/wb-adv-*"
      to: "lib/wb-adv-api.ts, lib/wb-advert-aggregations.ts, lib/wb-advert-chart.ts"
      via: "Гарантирует регрессию-устойчивость трёх pure модулей"
      pattern: "from \"@/lib/wb-adv"
---

<objective>
Дополнить test coverage Phase 19. Базовые smoke-тесты уже созданы в Plans 19-03 (wb-adv-api), 19-04 (wb-adv-cron), 19-05 (wb-advert-aggregations), 19-06 (wb-advert-chart-fill). Этот план добавляет глубокие тесты edge cases для rate-limit поведения, batch boundaries, date math (MSK midnight), и формулы агрегаций (zero-guard, weighted averages).

Purpose: Phase 19 — это первая интеграция нового scope WB API в проект. Любая регрессия (особенно в rate-limit или date math) обнаружится только в продакшне с задержкой в дни. Глубокие unit-тесты дают safety net для будущих рефакторингов и для добавления auto-кампаний (АРК) в v1.1.

Output: 4 новых тест-файла, расширяющих coverage из Plans 19-03..06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@CLAUDE.md
@lib/wb-adv-api.ts
@lib/wb-advert-aggregations.ts
@lib/wb-advert-chart.ts
@tests/wb-orders-chart-msk.test.ts
@tests/wb-fetch-rate-limit.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rate-limit + batch boundary тесты для lib/wb-adv-api.ts</name>
  <read_first>
    - lib/wb-adv-api.ts
    - tests/wb-fetch-rate-limit.test.ts (паттерн 429 + retry + cooldown)
    - tests/wb-adv-api.test.ts (созданный в Plan 19-03)
  </read_first>
  <behavior>
    Rate limit (tests/wb-adv-api-rate-limit.test.ts):
    - Test 1: 429 без Retry-After header → default 60 sec в WbRateLimitError
    - Test 2: 429 с Retry-After: "30" → setWbCooldownUntil("advert", 30) вызван
    - Test 3: setWbCooldownUntil("advert", 60) → последующий fetchPromotionCount throws WbRateLimitError без HTTP call
    - Test 4: non-rate-limit error (500) → Error (не WbRateLimitError) — caller знает, что это не rate limit

    Batch (tests/wb-adv-fullstats-batch.test.ts):
    - Test 1: 99 advertIds → 1 батч из 99 (нет sleep)
    - Test 2: 100 advertIds → 1 батч из 100 (нет sleep)
    - Test 3: 101 advertIds → 2 батча (100 + 1) с 1 sleep 1с
    - Test 4: 250 advertIds → 3 батча (100+100+50) с 2 sleep'a
    - Test 5: 0 advertIds → 0 запросов (early return)
    - Test 6: batch body shape — каждый body = `[{id, interval}, ...]` корректной длины
  </behavior>
  <files>tests/wb-adv-api-rate-limit.test.ts, tests/wb-adv-fullstats-batch.test.ts</files>
  <action>
    1. Создать `tests/wb-adv-api-rate-limit.test.ts` (4 теста):
       - Mock'и: `vi.mock("@/lib/wb-token", () => ({ getWbToken: vi.fn(async () => "test") }))`
       - Mock'и: `vi.mock("@/lib/wb-cooldown", () => ({ getWbCooldownSecondsRemaining: vi.fn(), setWbCooldownUntil: vi.fn() }))`
       - `vi.stubGlobal("fetch", fetchMock)`
       - Сценарий 1: `fetchMock.mockResolvedValueOnce(new Response("rate", { status: 429 }))` (без headers); expect WbRateLimitError с retryAfterSec === 60.
       - Сценарий 2: same но с `headers: { "Retry-After": "30" }`; expect setWbCooldownUntil called with ("advert", 30).
       - Сценарий 3: `getWbCooldownSecondsRemaining.mockResolvedValueOnce(45)`; expect throws WbRateLimitError, fetchMock NOT called.
       - Сценарий 4: `fetchMock.mockResolvedValueOnce(new Response("err", { status: 500 }))`; expect throws plain Error (не WbRateLimitError).

    2. Создать `tests/wb-adv-fullstats-batch.test.ts` (6 тестов):
       - Те же mock'и как выше.
       - Использовать `vi.useFakeTimers()` + `vi.runAllTimersAsync()` для контроля sleep.
       - Для каждого размера N: создать array advertIds длиной N, вызвать fetchFullStats, проверить:
         - `fetchMock.mock.calls.length === expectedBatchCount`
         - Bodies: `JSON.parse(call[1].body).length` соответствует ожиданию (100, 100, 50 для N=250)
         - Каждый body element = `{ id: number, interval: { begin, end } }`
       - Для N=0: `fetchMock.mock.calls.length === 0`.

    Запустить тесты — все GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-adv-api-rate-limit.test.ts tests/wb-adv-fullstats-batch.test.ts --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/wb-adv-api-rate-limit.test.ts содержит 4 теста, все GREEN
    - tests/wb-adv-fullstats-batch.test.ts содержит 6 тестов, все GREEN
    - Тесты НЕ делают реальных HTTP вызовов (mock fetch)
  </acceptance_criteria>
  <done>Rate-limit и batch logic защищены unit-тестами</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Date math + aggregation edge case тесты</name>
  <read_first>
    - lib/wb-advert-aggregations.ts (getPeriodRange)
    - tests/wb-orders-chart-msk.test.ts (паттерн MSK midnight boundary тестов)
    - tests/wb-advert-aggregations.test.ts (созданный в Plan 19-05)
  </read_first>
  <behavior>
    Period range (tests/wb-adv-period-range.test.ts):
    - Test 1: getPeriodRange(7, now=2026-05-19T12:00 UTC) → end = 2026-05-19 (yesterday в MSK), begin = 2026-05-13 (7 days back)
    - Test 2: MSK midnight boundary (now=2026-05-19T20:00 UTC = 2026-05-19T23:00 MSK) → end = 2026-05-18 (vчера MSK)
    - Test 3: MSK midnight just past (now=2026-05-19T21:30 UTC = 2026-05-20T00:30 MSK) → end = 2026-05-19 (вчера MSK новые)
    - Test 4: days=14 → window 14 дней (end - 13)
    - Test 5: days=28 → window 28 дней
    - Test 6: days=1 → window 1 день (begin = end)

    Aggregation edge cases (tests/wb-adv-aggregations-edge.test.ts):
    - Test 1: aggregateStats — ровно одна row → totals = row values, ratios корректные
    - Test 2: aggregateStats — все строки с views=0/clicks=0 → ctr=null, cpc=null, cr=null
    - Test 3: aggregateStats — clicks > 0 но orders = 0 → cr = 0 (не null — деление 0/positive валидно)
    - Test 4: groupByCampaign — пустой массив → empty Map
    - Test 5: groupByCampaign — одна campaign 50 rows → Map size 1, агрегаты корректные
    - Test 6: groupByProduct — nmId без mapping → skip (не попадает в результат)
    - Test 7: groupByProduct — два nmId с одним productId → агрегаты суммируются в одну запись
  </behavior>
  <files>tests/wb-adv-period-range.test.ts, tests/wb-adv-aggregations-edge.test.ts</files>
  <action>
    1. Создать `tests/wb-adv-period-range.test.ts` (6 тестов):
       - Импорт `import { getPeriodRange } from "@/lib/wb-advert-aggregations"`
       - Для каждого теста передавать explicit `now: Date` (фиксированная UTC дата), проверять exact `{ begin, end }` строки.
       - Use `expect(getPeriodRange(7, new Date("2026-05-19T12:00:00Z")).end).toBe("2026-05-19")` и т.д.
       - MSK boundary: при 2026-05-19T21:30 UTC → 2026-05-20T00:30 MSK → end=2026-05-19 (yesterday).
       - При 2026-05-19T20:00 UTC → 2026-05-19T23:00 MSK → end=2026-05-18 (ещё не наступила midnight).

    2. Создать `tests/wb-adv-aggregations-edge.test.ts` (7 тестов):
       - Импорт `import { aggregateStats, groupByCampaign, groupByProduct } from "@/lib/wb-advert-aggregations"`
       - Helper `makeStatRow(overrides)` для удобства.
       - Pure тесты — никаких mock'ов.

    Запустить тесты — все GREEN.

    Финальный smoke: `npm run test --run` — все тесты Phase 19 (включая 19-03, 19-04, 19-05, 19-06, 19-07) проходят. Pre-existing failed tests на main (известные из Phase 16 SUMMARY — 41 failed) не должны меняться.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-adv-period-range.test.ts tests/wb-adv-aggregations-edge.test.ts --run 2>&1 | tail -5 && npm run test -- tests/wb-adv-api.test.ts tests/wb-adv-api-rate-limit.test.ts tests/wb-adv-fullstats-batch.test.ts tests/wb-advert-aggregations.test.ts tests/wb-advert-chart-fill.test.ts --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/wb-adv-period-range.test.ts — 6 тестов GREEN
    - tests/wb-adv-aggregations-edge.test.ts — 7 тестов GREEN
    - Все ранее написанные тесты Phase 19 (Plans 19-03..06) тоже проходят
  </acceptance_criteria>
  <done>Test coverage Phase 19 полный: ~30+ тестов, все pure module functions покрыты, rate-limit и date math защищены</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/wb-adv-*.test.ts tests/wb-advert-*.test.ts --run` exits 0
- `npx tsc --noEmit` exits 0
- Pre-existing failing tests на main (41 из Phase 16) — count не увеличился (не сломали main)
</verification>

<success_criteria>
- Phase 19 имеет comprehensive test coverage: API client + cron + aggregations + chart + date math
- Любой регрессия от будущих изменений будет поймана локально, не на проде
- Tests служат живой документацией поведения модулей
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-07-SUMMARY.md`
</output>
