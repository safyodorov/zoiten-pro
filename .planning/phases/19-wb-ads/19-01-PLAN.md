---
phase: 19-wb-ads
plan: "01"
type: execute
wave: 2
depends_on: ["W0"]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260519_phase19_wb_advert/migration.sql
autonomous: true
requirements: [SCHEMA-CAMPAIGN, SCHEMA-TARGET, SCHEMA-STATDAILY, SCHEMA-BALANCE, ERP_SECTION_ADS]
must_haves:
  truths:
    - "Prisma schema содержит 4 новые модели: WbAdvertCampaign, WbAdvertTarget, WbAdvertStatDaily, WbAdvertBalanceSnapshot"
    - "ERP_SECTION enum содержит значение ADS"
    - "Ручная миграция SQL валидна и при `prisma migrate deploy` создаёт все 4 таблицы + индексы + добавляет ADS в ENUM"
    - "Все 4 модели готовы к нормальному upsert через Prisma client (правильные @unique и compound indexes)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Модели WbAdvertCampaign/Target/StatDaily/BalanceSnapshot + ERP_SECTION.ADS"
      contains: "model WbAdvertCampaign"
    - path: "prisma/migrations/20260519_phase19_wb_advert/migration.sql"
      provides: "Ручная DDL миграция: CREATE TABLE x4 + индексы + ALTER TYPE для ENUM"
      min_lines: 60
  key_links:
    - from: "prisma/schema.prisma"
      to: "lib/wb-adv-api.ts (Plan 19-03), app/(dashboard)/ads/wb/page.tsx (Plan 19-05)"
      via: "Prisma client types — WbAdvertCampaign, WbAdvertStatDaily используются в WB API client и UI"
      pattern: "WbAdvertCampaign|WbAdvertStatDaily|WbAdvertBalanceSnapshot"
---

<objective>
Добавить в Prisma схему 4 новые модели для хранения рекламных данных WB + расширить ERP_SECTION enum значением ADS. Создать ручную SQL-миграцию (нет локальной PostgreSQL — применится на VPS через `prisma migrate deploy` в составе deploy.sh).

Purpose: Без таблиц нельзя писать API-клиент (Plan 19-03), cron (Plan 19-04), UI (Plan 19-05). Это фундамент всей Phase 19. ENUM ADS нужен для RBAC `requireSection("ADS")` во всех server actions и middleware.

Output: prisma/schema.prisma расширена; migration.sql создан и готов к deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@.planning/phases/19-wb-ads/19-W0-NOTES.md
@CLAUDE.md
@prisma/schema.prisma
@prisma/migrations/20260512_wb_api_token/migration.sql
@prisma/migrations/20260515_wb_card_orders_daily/migration.sql

<interfaces>
<!-- Существующие модели, на которые ссылаемся через nmId BEZ FK (паттерн проекта). -->

From prisma/schema.prisma:
```prisma
model WbCard {
  id     String @id @default(cuid())
  nmId   Int    @unique
  imtId  Int?
  // ... soft delete через deletedAt
}

model WbCardOrdersDaily {
  id   Int      @id @default(autoincrement())
  nmId Int      // БЕЗ FK на WbCard — исторические данные выживают при soft/hard delete
  date DateTime @db.Date
  qty  Int
  sellerPrice Int?
  buyerPrice  Int?
  @@unique([nmId, date])
  @@index([nmId])
  @@index([date])
}

enum ERP_SECTION {
  PRODUCTS
  PRICES
  WEEKLY_CARDS
  STOCK
  COST
  PROCUREMENT
  SALES
  SUPPORT
  EMPLOYEES
  USER_MANAGEMENT
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить 4 модели + ENUM ADS в prisma/schema.prisma</name>
  <read_first>
    - prisma/schema.prisma (особенно секция Enums + model WbCardOrdersDaily + model WbApiToken — паттерн комментариев и compound unique)
    - .planning/phases/19-wb-ads/19-RESEARCH.md (секция 4 «Предлагаемая модель данных» — черновик схемы)
    - .planning/phases/19-wb-ads/19-W0-NOTES.md (применить любые корректировки если есть)
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    1. В enum `ERP_SECTION` (около строки 21-32) добавить значение `ADS` (последним перед `USER_MANAGEMENT` или в конце — не критично, Prisma enum строки).

    2. В конце файла (после `model WbCardOrdersDaily`) добавить 4 модели согласно 19-RESEARCH.md секция 4, с корректировками из 19-W0-NOTES если есть. Канонический образец паттерна:

    ```prisma
    // ──────────────────────────────────────────────────────────────────
    // Phase 19: Управление рекламой WB. Источник данных — WB Advert API
    // (advert-api.wildberries.ru). Связь с нашими товарами через nmId БЕЗ FK
    // (паттерн проекта — WbCard может быть soft-delete, исторические данные выживают).
    // Daily cron /api/cron/wb-adv-sync в ~3:00 МСК + manual backfill.
    // ──────────────────────────────────────────────────────────────────

    model WbAdvertCampaign {
      advertId    Int       @id              // Primary key из WB (advertId)
      name        String?                     // Название кампании (часто пустое в WB)
      type        Int                         // Тип РК (4..9, актуальные см. 19-RESEARCH.md секция 2)
      status      Int                         // -1, 4, 7, 8, 9, 11 — справочник WB
      cpm         Int?                        // Текущая ставка ₽
      dailyBudget Int?                        // Дневной бюджет ₽
      startDate   DateTime?
      endDate     DateTime?
      changeTime  DateTime                    // Из API (момент последнего изменения)
      raw         Json?                       // Полный ответ /promotion/adverts для аудита
      createdAt   DateTime  @default(now())
      updatedAt   DateTime  @updatedAt
      targets     WbAdvertTarget[]
      stats       WbAdvertStatDaily[]
      @@index([status])
      @@index([type])
    }

    // M:N campaign ↔ nmId (один advertId может бить по нескольким товарам)
    // Phase 19 W0 update: /promotion/adverts (404) deprecated → targets деривируются
    // из fullstats.days[].apps[].nms[].nmId как union по всем дням ответа.
    // Заполняется в Plan 19-04 (cron) после fullstats sweep.
    model WbAdvertTarget {
      id        Int      @id @default(autoincrement())
      advertId  Int
      nmId      Int                            // ссылка на WbCard.nmId БЕЗ FK
      active    Boolean  @default(true)
      campaign  WbAdvertCampaign @relation(fields: [advertId], references: [advertId], onDelete: Cascade)
      createdAt DateTime @default(now())
      updatedAt DateTime @updatedAt
      @@unique([advertId, nmId])
      @@index([nmId])
    }

    // Дневная статистика per (campaign, date, nmId, platform).
    // appType: 32=наблюдалось эмпирически (web?). Точное соответствие platform ↔ appType
    // нужно подтвердить через несколько кампаний с разными платформами (Phase 19 follow-up).
    // Source: fullstats.days[].apps[].nms[].* (см. project_wb_advert_api memory).
    model WbAdvertStatDaily {
      id        Int      @id @default(autoincrement())
      advertId  Int
      date      DateTime @db.Date               // MSK day
      nmId      Int
      appType   Int                             // platform id из WB API
      views     Int      @default(0)
      clicks    Int      @default(0)
      ctr       Float?                          // %
      cpc       Float?                          // ₽
      sum       Float    @default(0)            // потрачено ₽
      atbs      Int      @default(0)            // add-to-basket
      orders    Int      @default(0)            // заказы из РК
      cr        Float?                          // конверсия в заказ %
      shks      Int      @default(0)            // штук заказано
      sumPrice  Float    @default(0)            // ₽ оборот по заказам РК
      canceled  Int      @default(0)            // Phase 19 W0: technical cancels (товар недоступен/доставка failed). НЕ buyer refusals.
      createdAt DateTime @default(now())
      updatedAt DateTime @updatedAt
      campaign  WbAdvertCampaign @relation(fields: [advertId], references: [advertId], onDelete: Cascade)
      @@unique([advertId, date, nmId, appType])
      @@index([date])
      @@index([nmId, date])
    }

    // Snapshot баланса рекламного счёта
    // Phase 19 W0 update: response — {balance: int, net: int, currency: "RUB"}, поле `bonus` ОТСУТСТВУЕТ в текущей версии API.
    // Если WB вернёт bonus в будущем — добавить `bonus Int?` отдельной миграцией.
    model WbAdvertBalanceSnapshot {
      id         Int      @id @default(autoincrement())
      capturedAt DateTime @default(now())
      balance    Int                           // основные средства ₽ (целое в API)
      net        Int                           // нетто ₽ (целое в API)
      currency   String   @default("RUB")      // Phase 19 W0: API возвращает "RUB"
      @@index([capturedAt])
    }
    ```

    Важно: shape `/balance` подтверждён в 19-W0-NOTES (Section 4). Поле `bonus` УБРАНО по W0 (отсутствует в response). Если W0 окажется обновлён с новыми находками — синхронизировать.

    3. Запустить `npx prisma format` чтобы привести в порядок отступы.

    4. Запустить `npx prisma generate` локально — НЕ применять миграцию (нет PG), просто проверить, что схема валидна.
  </action>
  <verify>
    <automated>npx prisma format && npx prisma validate && grep -q "model WbAdvertCampaign" prisma/schema.prisma && grep -q "model WbAdvertTarget" prisma/schema.prisma && grep -q "model WbAdvertStatDaily" prisma/schema.prisma && grep -q "model WbAdvertBalanceSnapshot" prisma/schema.prisma && grep -q "^\s*ADS\s*$" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` содержит `model WbAdvertCampaign`, `model WbAdvertTarget`, `model WbAdvertStatDaily`, `model WbAdvertBalanceSnapshot`
    - enum `ERP_SECTION` содержит `ADS`
    - `npx prisma validate` exits 0
    - `npx prisma generate` exits 0
    - `WbAdvertStatDaily.@@unique([advertId, date, nmId, appType])` присутствует (compound unique для idempotent upsert)
    - `WbAdvertTarget.@@unique([advertId, nmId])` присутствует
  </acceptance_criteria>
  <done>Prisma schema валидна и описывает все 4 новые таблицы + ADS scope</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Создать ручную SQL миграцию 20260519_phase19_wb_advert</name>
  <read_first>
    - prisma/migrations/20260512_wb_api_token/migration.sql (паттерн ручной миграции)
    - prisma/migrations/20260515_wb_card_orders_daily/migration.sql (паттерн @db.Date + compound unique)
    - prisma/schema.prisma (новые модели из Task 1 — единый источник истины)
  </read_first>
  <files>prisma/migrations/20260519_phase19_wb_advert/migration.sql</files>
  <action>
    Создать директорию `prisma/migrations/20260519_phase19_wb_advert/` (если её нет) и файл `migration.sql` со следующим содержимым (идемпотентные DDL — `IF NOT EXISTS` где можно, но `ALTER TYPE ENUM ADD VALUE` НЕ идемпотентно в PostgreSQL без `IF NOT EXISTS` (доступно с PG 12+), используем):

    ```sql
    -- Phase 19: WB Advert (управление рекламой WB)
    -- Created: 2026-05-19
    -- Manual migration — local PG отсутствует, применится на VPS через prisma migrate deploy

    -- 1. Расширить ERP_SECTION enum значением ADS
    ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'ADS';

    -- 2. WbAdvertCampaign — список кампаний (snapshot WB)
    CREATE TABLE IF NOT EXISTS "WbAdvertCampaign" (
      "advertId"     INTEGER PRIMARY KEY,
      "name"         TEXT,
      "type"         INTEGER NOT NULL,
      "status"       INTEGER NOT NULL,
      "cpm"          INTEGER,
      "dailyBudget"  INTEGER,
      "startDate"    TIMESTAMP(3),
      "endDate"      TIMESTAMP(3),
      "changeTime"   TIMESTAMP(3) NOT NULL,
      "raw"          JSONB,
      "createdAt"    TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      "updatedAt"    TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
    );

    CREATE INDEX IF NOT EXISTS "WbAdvertCampaign_status_idx" ON "WbAdvertCampaign"("status");
    CREATE INDEX IF NOT EXISTS "WbAdvertCampaign_type_idx" ON "WbAdvertCampaign"("type");

    -- 3. WbAdvertTarget — связь advertId ↔ nmId (M:N через unique compound)
    CREATE TABLE IF NOT EXISTS "WbAdvertTarget" (
      "id"        SERIAL PRIMARY KEY,
      "advertId"  INTEGER NOT NULL,
      "nmId"      INTEGER NOT NULL,
      "active"    BOOLEAN NOT NULL DEFAULT true,
      "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      CONSTRAINT "WbAdvertTarget_advertId_fkey" FOREIGN KEY ("advertId") REFERENCES "WbAdvertCampaign"("advertId") ON DELETE CASCADE ON UPDATE CASCADE
    );

    CREATE UNIQUE INDEX IF NOT EXISTS "WbAdvertTarget_advertId_nmId_key" ON "WbAdvertTarget"("advertId", "nmId");
    CREATE INDEX IF NOT EXISTS "WbAdvertTarget_nmId_idx" ON "WbAdvertTarget"("nmId");

    -- 4. WbAdvertStatDaily — дневная статистика per (advertId, date, nmId, appType)
    CREATE TABLE IF NOT EXISTS "WbAdvertStatDaily" (
      "id"        SERIAL PRIMARY KEY,
      "advertId"  INTEGER NOT NULL,
      "date"      DATE NOT NULL,
      "nmId"      INTEGER NOT NULL,
      "appType"   INTEGER NOT NULL,
      "views"     INTEGER NOT NULL DEFAULT 0,
      "clicks"    INTEGER NOT NULL DEFAULT 0,
      "ctr"       DOUBLE PRECISION,
      "cpc"       DOUBLE PRECISION,
      "sum"       DOUBLE PRECISION NOT NULL DEFAULT 0,
      "atbs"      INTEGER NOT NULL DEFAULT 0,
      "orders"    INTEGER NOT NULL DEFAULT 0,
      "cr"        DOUBLE PRECISION,
      "shks"      INTEGER NOT NULL DEFAULT 0,
      "sumPrice"  DOUBLE PRECISION NOT NULL DEFAULT 0,
      "canceled"  INTEGER NOT NULL DEFAULT 0,                          -- Phase 19 W0
      "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      CONSTRAINT "WbAdvertStatDaily_advertId_fkey" FOREIGN KEY ("advertId") REFERENCES "WbAdvertCampaign"("advertId") ON DELETE CASCADE ON UPDATE CASCADE
    );

    CREATE UNIQUE INDEX IF NOT EXISTS "WbAdvertStatDaily_advertId_date_nmId_appType_key" ON "WbAdvertStatDaily"("advertId", "date", "nmId", "appType");
    CREATE INDEX IF NOT EXISTS "WbAdvertStatDaily_date_idx" ON "WbAdvertStatDaily"("date");
    CREATE INDEX IF NOT EXISTS "WbAdvertStatDaily_nmId_date_idx" ON "WbAdvertStatDaily"("nmId", "date");

    -- 5. WbAdvertBalanceSnapshot — snapshot баланса (Phase 19 W0 shape)
    CREATE TABLE IF NOT EXISTS "WbAdvertBalanceSnapshot" (
      "id"         SERIAL PRIMARY KEY,
      "capturedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
      "balance"    INTEGER NOT NULL,
      "net"        INTEGER NOT NULL,
      "currency"   TEXT NOT NULL DEFAULT 'RUB'
    );

    CREATE INDEX IF NOT EXISTS "WbAdvertBalanceSnapshot_capturedAt_idx" ON "WbAdvertBalanceSnapshot"("capturedAt");
    ```

    Сравнить структуру `migration.sql` со structure, которую сгенерил бы Prisma по schema (можно через `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` для cross-check; но НЕ перезаписывать наш файл — diff может включать лишний DROP CONSTRAINT и т.п., наш вариант идемпотентен).

    Прочитать получившийся файл и убедиться, что все таблицы создаются, все индексы есть, FK правильные.
  </action>
  <verify>
    <automated>test -f prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q 'CREATE TABLE IF NOT EXISTS "WbAdvertCampaign"' prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q 'CREATE TABLE IF NOT EXISTS "WbAdvertTarget"' prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q 'CREATE TABLE IF NOT EXISTS "WbAdvertStatDaily"' prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q 'CREATE TABLE IF NOT EXISTS "WbAdvertBalanceSnapshot"' prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q "ADD VALUE IF NOT EXISTS 'ADS'" prisma/migrations/20260519_phase19_wb_advert/migration.sql && grep -q "advertId_date_nmId_appType_key" prisma/migrations/20260519_phase19_wb_advert/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - Файл `prisma/migrations/20260519_phase19_wb_advert/migration.sql` существует
    - SQL содержит CREATE TABLE для всех 4 моделей с `IF NOT EXISTS`
    - SQL содержит `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'ADS'`
    - Compound unique index `WbAdvertStatDaily_advertId_date_nmId_appType_key` присутствует
    - FK constraints с `ON DELETE CASCADE` для WbAdvertTarget и WbAdvertStatDaily
    - `npx prisma validate` всё ещё проходит (schema согласована)
  </acceptance_criteria>
  <done>Миграция готова к `prisma migrate deploy` на VPS. После деплоя 4 таблицы появятся в zoiten_erp, ENUM расширится</done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- `npx prisma generate` exits 0 (новые типы доступны в @prisma/client)
- grep подтверждает наличие всех 4 моделей в schema.prisma
- migration.sql валидный (не содержит синтаксических ошибок — можно проверить через `sqlfluff` или просто визуально)
- ENUM ADS виден в schema.prisma и в migration.sql
</verification>

<success_criteria>
- 4 модели описаны в Prisma schema и в SQL миграции
- ERP_SECTION enum расширен значением ADS
- Migration готова к применению на VPS через `bash deploy.sh` (вызывает `prisma migrate deploy`)
- Plan 19-02, 19-03, 19-04, 19-05 могут опираться на Prisma client типы
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-01-SUMMARY.md`
</output>
