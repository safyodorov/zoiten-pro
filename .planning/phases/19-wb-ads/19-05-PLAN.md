---
phase: 19-wb-ads
plan: "05"
type: execute
wave: 5
depends_on: ["01", "04"]
files_modified:
  - app/(dashboard)/ads/layout.tsx
  - app/(dashboard)/ads/page.tsx
  - app/(dashboard)/ads/wb/page.tsx
  - app/(dashboard)/ads/ozon/page.tsx
  - components/ads/AdsTabs.tsx
  - components/ads/AdsFilters.tsx
  - components/ads/AdsGroupByToggle.tsx
  - components/ads/AdvertCampaignsTable.tsx
  - lib/wb-advert-aggregations.ts
  - lib/rbac.ts
  - middleware.ts
  - lib/sections.ts
  - lib/section-labels.ts
  - components/layout/nav-items.ts
  - tests/wb-advert-aggregations.test.ts
autonomous: true
requirements: [UI-TABLE, UI-FILTERS, UI-PERIOD, RBAC-ADS, NAV-INTEGRATION, AGGREGATIONS, GROUP-BY-DIMENSIONS]
must_haves:
  truths:
    - "Маршрут /ads → redirect на /ads/wb; /ads/ozon — заглушка ComingSoon"
    - "Sidebar показывает пункт «Управление рекламой» с иконкой → /ads"
    - "/ads/wb рендерится для пользователя с ADS scope; для VIEWER без права — 403"
    - "Таблица /ads/wb показывает per-Product строки (rowSpan) со связанными WbAdvertCampaign + агрегированной статистикой за выбранный период"
    - "Колонки: Фото, Сводка (product), Тип РК, advertId/Name, Потрачено, Заказов РК, Оборот РК, ДРР, CPC, CTR, CR"
    - "Каскадные фильтры (Направление → Бренд → Категория → Подкатегория) + multi-select Тип РК + статус (active/paused/all) + период (7/14/28)"
    - "Toolbar содержит переключатель «Группировка» — 4 режима: По товару (default), По связке (imt), По кампании, По типу РК. URL param ?groupBy=product|imt|campaign|type"
    - "Каждый режим группировки агрегирует одни и те же отфильтрованные stats, но bucket'ит по другому ключу — данные не дублируются"
    - "Per-nmId, per-imtId, per-type агрегации доступны через pure helper'ы; per-nmId/per-imtId также рендерятся внутри product-группы в expand-панели (Plan 19-06)"
    - "ДРР, CPC, CTR, CR вычисляются на сервере через lib/wb-advert-aggregations.ts (pure helper)"
  artifacts:
    - path: "app/(dashboard)/ads/wb/page.tsx"
      provides: "RSC страница /ads/wb с таблицей + фильтрами + period filter + groupBy switch + nmIdToImtId/advertIdToType maps"
      min_lines: 200
    - path: "lib/wb-advert-aggregations.ts"
      provides: "Pure helpers: aggregateStats, groupByCampaign, groupByProduct, groupByNmId, groupByImtId, groupByType, getPeriodRange + EXPORTED type ProductCampaignGroup (с nmIdAgg и imtIdAgg)"
      min_lines: 140
    - path: "components/ads/AdvertCampaignsTable.tsx"
      provides: "Sticky data-таблица с per-product rowSpan по паттерну /prices/wb. Принимает discriminated union Row по groupBy. Импортирует ProductCampaignGroup и все Aggregated dims из lib/wb-advert-aggregations.ts"
      min_lines: 200
    - path: "components/ads/AdsGroupByToggle.tsx"
      provides: "Client-only селектор групировки (product/imt/campaign/type) — обновляет ?groupBy в URL"
      min_lines: 30
    - path: "lib/sections.ts"
      contains: "ADS"
      provides: "RBAC middleware распознаёт /ads/* как scope ADS"
    - path: "tests/wb-advert-aggregations.test.ts"
      provides: "9 тестов pure helpers: aggregateStats × 3, groupByCampaign × 1, groupByProduct × 1, getPeriodRange × 1, groupByNmId × 1, groupByImtId × 1, groupByType × 1"
      min_lines: 120
  key_links:
    - from: "app/(dashboard)/ads/wb/page.tsx"
      to: "prisma.wbAdvertCampaign, prisma.wbAdvertStatDaily, prisma.wbAdvertTarget, prisma.wbCard (imtId)"
      via: "RSC запрашивает все 4 таблицы за выбранный период; JOIN через nmId/MarketplaceArticle; WbCard.imtId загружается для построения nmIdToImtId Map"
      pattern: "wbAdvertCampaign|wbAdvertStatDaily|wbAdvertTarget|wbCard.*imtId"
    - from: "middleware.ts"
      to: "lib/sections.ts"
      via: "/ads/* → ADS маппинг для middleware route guard"
      pattern: "/ads.*ADS"
    - from: "components/ads/AdvertCampaignsTable.tsx"
      to: "lib/wb-advert-aggregations.ts"
      via: "ProductCampaignGroup тип экспортируется из lib и импортируется в Table — single source of truth, no drift между page.tsx и component"
      pattern: "ProductCampaignGroup"
    - from: "app/(dashboard)/ads/wb/page.tsx"
      to: "lib/wb-advert-aggregations.ts (groupByNmId / groupByImtId / groupByType)"
      via: "RSC вызывает все 5 group-* функций с одним и тем же отфильтрованным stats[] dataset + maps (nmIdToImtId, advertIdToType); результат передаётся в client component для рендера по выбранному groupBy"
      pattern: "groupByNmId|groupByImtId|groupByType"
---

<objective>
Создать раздел `/ads/wb` — view-only UI для рекламной аналитики WB. Использует данные, синхронизированные Plan 19-04. Это самый большой план в Phase 19 — он реализует таблицу per-Product с rowSpan по кампаниям, каскадные фильтры (как в /prices/wb), period selector (7/14/28), groupBy switch (4 режима), и pure helpers для расчёта ДРР/CPC/CTR/CR в **5 dimensions**: per Product / per nmId (карточка) / per imtId (связка) / per advertId (кампания) / per type (тип РК).

Purpose: User в исходном prompt прямо требовал три dimensions агрегации: «по типу РК, по карточкам товара и по их связкам». Без этих 3 measures DB не реализует обещанную пользовательскую ценность. Per-Product и per-campaign — наш текущий шаг; добавляем per-nmId summary + per-imtId summary + per-type summary, чтобы покрыть всё пространство пользовательских вопросов.

Output: Рабочий /ads/wb с каскадными фильтрами + 4-режимным groupBy switch + 5 dimensions агрегаций. После deploy + первого backfill (Plan 19-04) пользователь видит расходы по рекламе per товар / per nmId / per связка / per кампания / per тип.

**Density decision (по W7):** План имеет 3 задачи и ~15 modified файлов. Splitting в 19-05a + 19-05b отверг — основной риск (RBAC plumbing + skeleton routes — Task 1) полностью изолирован от aggregations (Task 2) и main page (Task 3), а cross-plan dependency на shared type `ProductCampaignGroup` усилил бы coupling. **Density accepted** по прецеденту Phase 17 (свойства+размеры+barcode с 12+ файлов в одном плане), Phase 18 (Product.name generator с 13 файлов). Дополнительные 3 group-* helper функции в Task 2 — это +30 строк pure кода и +3 теста, не увеличивает scope существенно. Если executor упрётся в context budget — Task 1 можно безопасно завершить и закоммитить отдельно от Task 2-3 (no shared state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@CLAUDE.md
@app/(dashboard)/prices/wb/page.tsx
@components/prices/PriceCalculatorTable.tsx
@components/prices/PricesFilters.tsx
@components/prices/PricesTabs.tsx
@components/products/ProductFilters.tsx
@lib/sections.ts
@lib/section-labels.ts
@lib/rbac.ts
@middleware.ts
@components/layout/nav-items.ts
@lib/product-order.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Расширить RBAC/sections + создать /ads layout/routes/tabs (skeleton)</name>
  <read_first>
    - lib/sections.ts
    - lib/section-labels.ts
    - middleware.ts
    - lib/rbac.ts
    - components/layout/nav-items.ts
    - app/(dashboard)/prices/page.tsx (redirect pattern)
    - components/prices/PricesTabs.tsx (tabs pattern)
    - app/(dashboard)/weekly/page.tsx (ComingSoon stub pattern)
  </read_first>
  <files>lib/sections.ts, lib/section-labels.ts, lib/rbac.ts, middleware.ts, components/layout/nav-items.ts, app/(dashboard)/ads/layout.tsx, app/(dashboard)/ads/page.tsx, app/(dashboard)/ads/ozon/page.tsx, components/ads/AdsTabs.tsx</files>
  <action>
    1. **lib/sections.ts** — добавить запись `"/ads": "ADS"` в `SECTION_PATHS`.

    2. **lib/section-labels.ts** — добавить `{ value: "ADS", label: "Управление рекламой" }` в `SECTION_OPTIONS` (после PRICES, чтобы порядок отображения был логичный).

    3. **lib/rbac.ts** — `requireSection(section: ERP_SECTION)` уже принимает enum литералы. После `npx prisma generate` (выполнен в Plan 19-01) TypeScript автоматически знает про ADS. Дополнительная правка не нужна, кроме случая если есть explicit allowlist — тогда расширить.

    4. **middleware.ts** — route guard. Если используется `SECTION_PATHS` из lib/sections.ts (Edge-safe) — после шага 1 всё работает автоматически.

    5. **components/layout/nav-items.ts**:
       - Импортировать `Megaphone` из `lucide-react`
       - В `NAV_ITEMS` добавить запись после PRICES: `{ section: "ADS", href: "/ads", label: "Управление рекламой", icon: "Megaphone" }`
       - В `ICON_MAP` добавить `Megaphone`

    6. **app/(dashboard)/ads/layout.tsx** — Server Component с RBAC:
       ```typescript
       import { requireSection } from "@/lib/rbac"
       export default async function AdsLayout({ children }: { children: React.ReactNode }) {
         await requireSection("ADS")
         return <>{children}</>
       }
       ```

    7. **app/(dashboard)/ads/page.tsx** — redirect:
       ```typescript
       import { redirect } from "next/navigation"
       export default function AdsRootPage() { redirect("/ads/wb") }
       ```

    8. **app/(dashboard)/ads/ozon/page.tsx** — ComingSoon stub (find existing ComingSoon component через `grep -r "ComingSoon" components/` если не помнишь путь).

    9. **components/ads/AdsTabs.tsx** — копировать паттерн `PricesTabs.tsx`, заменить pathnames на `/ads/wb` и `/ads/ozon`.

    10. Запустить `npx prisma generate` (если не сделано в Plan 19-01).
    11. Запустить `npx tsc --noEmit` — 0 errors.
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax (test -f, grep, &&).</note>
    <automated>grep -q '"/ads": "ADS"' lib/sections.ts && grep -q '"Управление рекламой"' lib/section-labels.ts && grep -q "Megaphone" components/layout/nav-items.ts && test -f "app/(dashboard)/ads/layout.tsx" && test -f "app/(dashboard)/ads/page.tsx" && test -f "app/(dashboard)/ads/ozon/page.tsx" && test -f components/ads/AdsTabs.tsx && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `lib/sections.ts` маппит `/ads` → `ADS`
    - `lib/section-labels.ts` содержит ADS опцию
    - `nav-items.ts` содержит NAV_ITEM с href `/ads` и иконкой Megaphone
    - 4 файла маршрутов созданы (layout, page, ozon/page, AdsTabs)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Скелет раздела готов: sidebar nav, route guard, layout, tabs работают; WB страница ещё пустая</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/wb-advert-aggregations.ts — pure helpers (5 dimensions) + EXPORTED ProductCampaignGroup type + 9 tests</name>
  <read_first>
    - .planning/phases/19-wb-ads/19-RESEARCH.md (формулы)
    - lib/customer-aggregations.ts (паттерн pure helpers)
    - lib/wb-orders-chart.ts (паттерн MSK date helpers)
  </read_first>
  <behavior>
    - Test 1: `aggregateStats([{views:100, clicks:5, sum:50, orders:1, sumPrice:1000, atbs:0, shks:0, advertId:1, nmId:1, appType:0, date:""}, ...])` → `{ totalSpent, totalOrders, totalRevenue, drr, cpc, ctr, cr }` с правильными значениями
    - Test 2: Empty array → all numbers 0, all ratios null (zero-guard, не NaN)
    - Test 3: views > 0 но clicks = 0 → cpc = null, ctr = 0, cr = null
    - Test 4: groupByCampaign — для двух advertId возвращает Map с 2 ключами
    - Test 5: groupByProduct — стрики с nmId не из переданного маппинга skipping'аются
    - Test 6: getPeriodRange(7, now) возвращает begin = today-7 (включая yesterday), end = today-1
    - **Test 7 (NEW): groupByNmId** — для 3 rows с одинаковым nmId=100 + 1 row с nmId=200 возвращает Map с 2 ключами; rows c одинаковым nmId aggregated через aggregateStats (sumSpent суммируется).
    - **Test 8 (NEW): groupByImtId** — для nmId=100 (imtId=500), nmId=101 (imtId=500), nmId=200 (imtId=600), nmId=300 (imtId=null) → Map с 2 ключами {500 → aggregated по nmId=100+101, 600 → aggregated по nmId=200}; nmId=300 пропущен (imtId=null).
    - **Test 9 (NEW): groupByType** — для advertId=1 (type=9), advertId=2 (type=9), advertId=3 (type=8) → Map с 2 ключами {9 → aggregated по advertId=1+2, 8 → aggregated по advertId=3}; rows с advertId не в map → пропущены.
  </behavior>
  <files>lib/wb-advert-aggregations.ts, tests/wb-advert-aggregations.test.ts</files>
  <action>
    Создать `lib/wb-advert-aggregations.ts`:

    ```typescript
    // Phase 19: Pure helpers для агрегации WB Advert stats.
    // Без Prisma/Next deps — testable в vitest.
    // Формулы:
    //   ДРР = sum_spent / sum_revenue * 100
    //   CPC = sum_spent / sum_clicks
    //   CTR = sum_clicks / sum_views * 100
    //   CR  = sum_orders / sum_clicks * 100

    export interface StatRow {
      advertId: number
      nmId: number
      appType: number
      date: string
      views: number
      clicks: number
      sum: number
      atbs: number
      orders: number
      shks: number
      sumPrice: number
    }

    export interface Aggregated {
      totalSpent: number
      totalOrders: number
      totalRevenue: number
      totalViews: number
      totalClicks: number
      drr: number | null
      cpc: number | null
      ctr: number | null
      cr: number | null
    }

    // ── Shared types для page.tsx + AdvertCampaignsTable (single source of truth) ──

    export interface ProductMeta {
      id: string
      name: string
      article: string | null
      sku: string
      photoUrl: string | null
      brand: { id: string; name: string; directionId: string | null } | null
      category: { id: string; name: string } | null
      subcategory: { id: string; name: string } | null
    }

    export interface CampaignRow {
      advertId: number
      name: string | null
      type: number
      status: number
      agg: Aggregated         // суммарно за период по этой кампании
    }

    export interface ProductCampaignGroup {
      product: ProductMeta
      productAgg: Aggregated   // суммарно по всем кампаниям продукта
      campaigns: CampaignRow[]
      // NEW (Phase 19 fix-iter 2): per-nmId / per-imtId агрегации внутри продукта
      // Заполняются в page.tsx и используются в Plan 19-06 (expand-панель)
      nmIdAgg?: Map<number, Aggregated>
      imtIdAgg?: Map<number, Aggregated>
      // imtId map per nmId — нужно компоненту чтобы группировать nmIds в imt-блоки
      nmIdToImtId?: Map<number, number | null>
      ordersCharts?: Array<{ nmId: number; data: unknown[] }>  // заполняется в Plan 19-06
    }

    // ── Pure functions ──

    export function aggregateStats(rows: StatRow[]): Aggregated {
      let totalSpent = 0, totalOrders = 0, totalRevenue = 0, totalViews = 0, totalClicks = 0
      for (const r of rows) {
        totalSpent += r.sum
        totalOrders += r.orders
        totalRevenue += r.sumPrice
        totalViews += r.views
        totalClicks += r.clicks
      }
      return {
        totalSpent, totalOrders, totalRevenue, totalViews, totalClicks,
        drr: totalRevenue > 0 ? (totalSpent / totalRevenue) * 100 : null,
        cpc: totalClicks > 0 ? totalSpent / totalClicks : null,
        ctr: totalViews > 0 ? (totalClicks / totalViews) * 100 : null,
        cr:  totalClicks > 0 ? (totalOrders / totalClicks) * 100 : null,
      }
    }

    export function groupByCampaign(rows: StatRow[]): Map<number, Aggregated> {
      const buckets = new Map<number, StatRow[]>()
      for (const r of rows) {
        const list = buckets.get(r.advertId) ?? []
        list.push(r); buckets.set(r.advertId, list)
      }
      const out = new Map<number, Aggregated>()
      for (const [id, list] of buckets) out.set(id, aggregateStats(list))
      return out
    }

    export function groupByProduct(
      rows: StatRow[],
      nmIdToProductId: Map<number, string>,
    ): Map<string, Aggregated> {
      const buckets = new Map<string, StatRow[]>()
      for (const r of rows) {
        const pid = nmIdToProductId.get(r.nmId)
        if (!pid) continue
        const list = buckets.get(pid) ?? []
        list.push(r); buckets.set(pid, list)
      }
      const out = new Map<string, Aggregated>()
      for (const [pid, list] of buckets) out.set(pid, aggregateStats(list))
      return out
    }

    /**
     * NEW (fix-iter 2): per-nmId summary.
     * Дефолтный bucket для строк рекламы по карточке товара.
     * Возвращает Map<nmId, Aggregated>.
     */
    export function groupByNmId(rows: StatRow[]): Map<number, Aggregated> {
      const buckets = new Map<number, StatRow[]>()
      for (const r of rows) {
        const list = buckets.get(r.nmId) ?? []
        list.push(r); buckets.set(r.nmId, list)
      }
      const out = new Map<number, Aggregated>()
      for (const [nm, list] of buckets) out.set(nm, aggregateStats(list))
      return out
    }

    /**
     * NEW (fix-iter 2): per-imtId summary («по связке»).
     * Объединяет nmIds одной WB-склейки.
     * @param rows  stat rows
     * @param nmIdToImtId  map nmId → imtId | null (загружается из WbCard в page.tsx)
     *
     * Поведение: rows c nmId, для которых imtId === null или нет в map'е — пропускаются.
     * Тем самым «связка» как dimension показывает ТОЛЬКО товары, реально склеенные на WB.
     */
    export function groupByImtId(
      rows: StatRow[],
      nmIdToImtId: Map<number, number | null>,
    ): Map<number, Aggregated> {
      const buckets = new Map<number, StatRow[]>()
      for (const r of rows) {
        const imt = nmIdToImtId.get(r.nmId)
        if (imt == null) continue
        const list = buckets.get(imt) ?? []
        list.push(r); buckets.set(imt, list)
      }
      const out = new Map<number, Aggregated>()
      for (const [imt, list] of buckets) out.set(imt, aggregateStats(list))
      return out
    }

    /**
     * NEW (fix-iter 2): per-campaign-type summary.
     * @param rows  stat rows
     * @param advertIdToType  map advertId → type number (из WbAdvertCampaign)
     *
     * Rows с advertId не в map'е — пропускаются.
     */
    export function groupByType(
      rows: StatRow[],
      advertIdToType: Map<number, number>,
    ): Map<number, Aggregated> {
      const buckets = new Map<number, StatRow[]>()
      for (const r of rows) {
        const t = advertIdToType.get(r.advertId)
        if (t == null) continue
        const list = buckets.get(t) ?? []
        list.push(r); buckets.set(t, list)
      }
      const out = new Map<number, Aggregated>()
      for (const [t, list] of buckets) out.set(t, aggregateStats(list))
      return out
    }

    /** Период [today-days .. today-1] MSK в формате YYYY-MM-DD. */
    export function getPeriodRange(days: number, now: Date = new Date()): { begin: string; end: string } {
      const mskMs = now.getTime() + 3 * 3600_000
      const todayMs = Math.floor(mskMs / 86400_000) * 86400_000
      const endMs = todayMs - 24 * 3600_000      // вчера
      const beginMs = endMs - (days - 1) * 24 * 3600_000
      return {
        begin: new Date(beginMs).toISOString().slice(0, 10),
        end: new Date(endMs).toISOString().slice(0, 10),
      }
    }
    ```

    **W9 fix:** Тип `ProductCampaignGroup` экспортируется ОТСЮДА (single source of truth). И `page.tsx`, и `AdvertCampaignsTable.tsx` должны его импортировать:
    ```typescript
    import type { ProductCampaignGroup } from "@/lib/wb-advert-aggregations"
    ```
    Это устраняет drift risk между серверной (page.tsx) и клиентской (компонент) сторонами.

    Создать `tests/wb-advert-aggregations.test.ts` — 9 тестов из behavior, vitest, no mocks (pure functions). Стандартный StatRow factory вверху файла для DRY:
    ```typescript
    const row = (overrides: Partial<StatRow> = {}): StatRow => ({
      advertId: 1, nmId: 100, appType: 0, date: "2026-05-15",
      views: 0, clicks: 0, sum: 0, atbs: 0, orders: 0, shks: 0, sumPrice: 0,
      ...overrides,
    })
    ```

    Запустить тесты — все 9 GREEN.
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax.</note>
    <automated>npm run test -- tests/wb-advert-aggregations.test.ts --run 2>&1 | tail -5 && grep -q "export function aggregateStats" lib/wb-advert-aggregations.ts && grep -q "export function groupByCampaign" lib/wb-advert-aggregations.ts && grep -q "export function groupByProduct" lib/wb-advert-aggregations.ts && grep -q "export function groupByNmId" lib/wb-advert-aggregations.ts && grep -q "export function groupByImtId" lib/wb-advert-aggregations.ts && grep -q "export function groupByType" lib/wb-advert-aggregations.ts && grep -q "export function getPeriodRange" lib/wb-advert-aggregations.ts && grep -q "export interface ProductCampaignGroup" lib/wb-advert-aggregations.ts && grep -q "nmIdAgg" lib/wb-advert-aggregations.ts && grep -q "imtIdAgg" lib/wb-advert-aggregations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wb-advert-aggregations.ts` экспортирует 7 функций: aggregateStats, groupByCampaign, groupByProduct, **groupByNmId, groupByImtId, groupByType**, getPeriodRange
    - `lib/wb-advert-aggregations.ts` экспортирует тип `ProductCampaignGroup` (с ProductMeta, CampaignRow подтипами + опциональные `nmIdAgg`, `imtIdAgg`, `nmIdToImtId` поля)
    - Zero-guard для всех ratio (null вместо NaN/Infinity)
    - Все 9 тестов GREEN
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pure helpers для 5 dimensions (product / nmId / imt / campaign / type) готовы; ProductCampaignGroup поддерживает per-product nmIdAgg + imtIdAgg для Plan 19-06</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: RSC /ads/wb page + AdsFilters + AdsGroupByToggle + AdvertCampaignsTable (5-dim aware)</name>
  <read_first>
    - app/(dashboard)/prices/wb/page.tsx (RSC паттерн с articleToProduct, фильтры, JOIN через MarketplaceArticle; КРИТИЧНО — строки 1-100 содержат каскадную фильтрацию, копируем)
    - components/prices/PriceCalculatorTable.tsx (sticky-таблица с rowSpan)
    - components/prices/PricesFilters.tsx (каскадные фильтры + MultiSelectDropdown)
    - components/products/ProductFilters.tsx (канонический образец каскадных фильтров)
    - lib/product-order.ts (PRODUCT_HIERARCHY_ORDER_BY)
    - CLAUDE.md (секция «Sticky data-таблицы», «Каскадные фильтры»)
  </read_first>
  <files>app/(dashboard)/ads/wb/page.tsx, components/ads/AdsFilters.tsx, components/ads/AdsGroupByToggle.tsx, components/ads/AdvertCampaignsTable.tsx</files>
  <action>
    1. **components/ads/AdsFilters.tsx** — client component с searchParams:
       - URL state: `direction`, `brand`, `category`, `subcategory`, `campaignType` (multi), `status` (active/paused/all), `period` (7/14/28).
       - MultiSelectDropdown для direction/brand/category/subcategory (каскад как в ProductFilters.tsx — при изменении родителя cleanup невалидных детей).
       - MultiSelectDropdown для campaignType (опции = labels из 19-RESEARCH.md секция 2 «Типы кампаний»: 4=каталог, 5=карточка, 6=поиск, 7=рекомендации, 8=единая ставка, 9=единая/ручная; уточнить с W0-NOTES).
       - Native `<select>` для status (active=9, paused=11, all).
       - Native `<select>` для period (7, 14, 28).
       - `router.push` на смену фильтра.

    2. **components/ads/AdsGroupByToggle.tsx** — NEW (fix-iter 2). Client-only селектор:
       ```typescript
       "use client"
       import { useRouter, useSearchParams, usePathname } from "next/navigation"
       const OPTIONS = [
         { value: "product",  label: "По товару" },
         { value: "imt",      label: "По связке" },
         { value: "campaign", label: "По кампании" },
         { value: "type",     label: "По типу РК" },
       ] as const

       export function AdsGroupByToggle() {
         const router = useRouter()
         const pathname = usePathname()
         const sp = useSearchParams()
         const current = sp.get("groupBy") ?? "product"
         const handle = (v: string) => {
           const next = new URLSearchParams(sp.toString())
           if (v === "product") next.delete("groupBy")
           else next.set("groupBy", v)
           router.push(`${pathname}?${next.toString()}`)
         }
         return (
           <div className="inline-flex rounded border bg-card">
             {OPTIONS.map(o => (
               <button key={o.value} onClick={() => handle(o.value)}
                 className={`px-3 py-1.5 text-sm ${current === o.value ? "bg-accent text-accent-foreground" : "hover:bg-muted"}`}>
                 {o.label}
               </button>
             ))}
           </div>
         )
       }
       ```

    3. **components/ads/AdvertCampaignsTable.tsx** — client component, sticky-таблица:
       - **W9 fix:** Импортировать `ProductCampaignGroup`, `Aggregated` из `@/lib/wb-advert-aggregations` (single source of truth, не дублировать local definition).
       - Принимает discriminated union prop `view`:
         ```typescript
         type TableView =
           | { groupBy: "product";  groups: ProductCampaignGroup[] }
           | { groupBy: "imt";      rows: Array<{ imtId: number; productNames: string[]; nmIds: number[]; agg: Aggregated }> }
           | { groupBy: "campaign"; rows: Array<{ advertId: number; name: string | null; type: number; status: number; agg: Aggregated }> }
           | { groupBy: "type";     rows: Array<{ type: number; campaignCount: number; agg: Aggregated }> }
         ```
       - Для `groupBy=product` — текущая логика (per-product rowSpan + кампании внутри).
       - Для остальных режимов — простая плоская таблица с кастомными колонками:
         - **imt**: Связка #imtId, Карточки (товары / nmIds count), Потрачено, Заказы, Оборот, ДРР, CPC, CTR, CR
         - **campaign**: advertId, Name, Тип РК, Статус, Потрачено, Заказы, Оборот, ДРР, CPC, CTR, CR
         - **type**: Тип РК (label из CAMPAIGN_TYPE_LABELS), Кол-во РК, Потрачено, Заказы, Оборот, ДРР, CPC, CTR, CR
       - Колонки product-режима: Фото (rowSpan), Сводка (rowSpan: name+article+sku), Тип РК, advertId / Name, Потрачено ₽, Заказов РК, Оборот ₽, ДРР %, CPC ₽, CTR %, CR %.
       - Структура — паттерн «Sticky data-таблицы» из CLAUDE.md (НЕ shadcn Table, `<div overflow-auto>` + `<table border-separate>`, `<thead bg-background>` со sticky top-0 z-20).
       - Per-product rowSpan через React.Fragment + Math.max(1, campaigns.length).
       - Если у Product 0 кампаний — одна строка с прочерками `—`.
       - Форматирование: денежные ₽ через `Intl.NumberFormat("ru-RU")`; ratio (ДРР/CTR/CR) с 1 знаком после запятой + `%`; null → `—`.
       - Тип РК через CAMPAIGN_TYPE_LABELS:
         ```typescript
         const CAMPAIGN_TYPE_LABELS: Record<number, string> = {
           4: "Каталог", 5: "Карточка", 6: "Поиск",
           7: "Рекомендации", 8: "Единая ставка", 9: "Единая/Ручная",
         }
         ```

    4. **app/(dashboard)/ads/wb/page.tsx** — RSC:

       ```typescript
       import { requireSection } from "@/lib/rbac"
       import { prisma } from "@/lib/prisma"
       import { AdsTabs } from "@/components/ads/AdsTabs"
       import { AdsFilters } from "@/components/ads/AdsFilters"
       import { AdsGroupByToggle } from "@/components/ads/AdsGroupByToggle"
       import { AdvertCampaignsTable } from "@/components/ads/AdvertCampaignsTable"
       import {
         getPeriodRange, groupByCampaign, groupByProduct, groupByNmId, groupByImtId, groupByType,
         aggregateStats,
       } from "@/lib/wb-advert-aggregations"
       import type { ProductCampaignGroup, Aggregated } from "@/lib/wb-advert-aggregations"
       import { compareProductsByHierarchy } from "@/lib/product-order"

       export default async function AdsWbPage({ searchParams }: { searchParams: Promise<Record<string, string | string[]>> }) {
         await requireSection("ADS")
         const sp = await searchParams

         // ── URL → filter state ──
         const asArr = (v: string | string[] | undefined): string[] =>
           v == null ? [] : Array.isArray(v) ? v : [v]
         const directionIds = asArr(sp.direction)
         const brandIds = asArr(sp.brand)
         const categoryIds = asArr(sp.category)
         const subcategoryIds = asArr(sp.subcategory)
         const typeFilter = asArr(sp.campaignType).map(Number).filter(n => !Number.isNaN(n))
         const statusFilter = (Array.isArray(sp.status) ? sp.status[0] : sp.status) ?? "active"
         const periodRaw = Number(Array.isArray(sp.period) ? sp.period[0] : sp.period) || 7
         const periodDays = [7, 14, 28].includes(periodRaw) ? periodRaw : 7
         const groupBy = (Array.isArray(sp.groupBy) ? sp.groupBy[0] : sp.groupBy) ?? "product"
         const validGroupBy = ["product", "imt", "campaign", "type"].includes(groupBy) ? groupBy : "product"

         // ── 1. Period range ──
         const { begin, end } = getPeriodRange(periodDays)
         const beginDate = new Date(begin + "T00:00:00Z")
         const endDate = new Date(end + "T00:00:00Z")

         // ── 2. Stats за период ──
         const stats = await prisma.wbAdvertStatDaily.findMany({
           where: { date: { gte: beginDate, lte: endDate } },
         })

         // ── 3. Кампании ──
         const campaignWhere: Record<string, unknown> = {}
         if (statusFilter === "active") campaignWhere.status = 9
         else if (statusFilter === "paused") campaignWhere.status = 11
         if (typeFilter.length > 0) campaignWhere.type = { in: typeFilter }
         const campaigns = await prisma.wbAdvertCampaign.findMany({
           where: campaignWhere,
           include: { targets: true },
         })

         // ── 3.5. NEW (fix-iter 2): advertId → type map для groupByType ──
         const advertIdToType = new Map<number, number>()
         for (const c of campaigns) advertIdToType.set(c.advertId, c.type)

         // ── 4. nmIds — все target'ы активных кампаний ──
         const allNmIds = new Set<number>()
         for (const c of campaigns) for (const t of c.targets) allNmIds.add(t.nmId)

         // ── 4.5. NEW (fix-iter 2): nmId → imtId map для groupByImtId ──
         // WbCard.imtId Int? (см. Plan 19-01 interfaces — модель WbCard содержит imtId).
         // imtId === null → товар не в склейке, в groupByImtId пропускается.
         const wbCards = await prisma.wbCard.findMany({
           where: { nmId: { in: Array.from(allNmIds) } },
           select: { nmId: true, imtId: true },
         })
         const nmIdToImtId = new Map<number, number | null>()
         for (const c of wbCards) nmIdToImtId.set(c.nmId, c.imtId ?? null)

         // ── 5. Каскадная Prisma where для Product (W8 fix — полный snippet) ──
         const productCascadeWhere: Record<string, unknown> = { deletedAt: null }
         if (subcategoryIds.length) productCascadeWhere.subcategoryId = { in: subcategoryIds }
         if (categoryIds.length)    productCascadeWhere.categoryId = { in: categoryIds }
         if (brandIds.length)       productCascadeWhere.brandId = { in: brandIds }
         if (directionIds.length)   productCascadeWhere.brand = { directionId: { in: directionIds } }

         const articles = await prisma.marketplaceArticle.findMany({
           where: {
             article: { in: Array.from(allNmIds).map(String) },
             marketplace: { slug: "wb" },
             product: productCascadeWhere,
           },
           include: {
             product: {
               include: {
                 brand: { include: { direction: true } },
                 category: true,
                 subcategory: true,
                 photos: { take: 1, orderBy: { sortOrder: "asc" } },
               },
             },
           },
         })

         const nmIdToProductId = new Map<number, string>()
         const productById = new Map<string, typeof articles[0]["product"]>()
         for (const a of articles) {
           const nm = parseInt(a.article, 10)
           if (Number.isNaN(nm)) continue
           if (!nmIdToProductId.has(nm)) nmIdToProductId.set(nm, a.product.id)
           productById.set(a.product.id, a.product)
         }

         // ── 6. Группировка stats (все 5 dimensions сразу — потом выбираем какие отдать клиенту) ──
         const aggByCampaign = groupByCampaign(stats)
         const aggByProduct = groupByProduct(stats, nmIdToProductId)
         const aggByNmId = groupByNmId(stats)
         const aggByImtId = groupByImtId(stats, nmIdToImtId)
         const aggByType = groupByType(stats, advertIdToType)

         // ── 7. ProductCampaignGroup[] (всегда строится для product-режима и для Plan 19-06 expand) ──
         const groups: ProductCampaignGroup[] = []
         for (const [productId, product] of productById) {
           const productNmIds = articles
             .filter(a => a.product.id === productId)
             .map(a => parseInt(a.article, 10))
             .filter(n => !Number.isNaN(n))
           const linkedCampaigns = campaigns.filter(c =>
             c.targets.some(t => productNmIds.includes(t.nmId)),
           )

           // NEW (fix-iter 2): per-product nmId/imt sub-aggs (используются в expand-панели Plan 19-06)
           const productNmIdAgg = new Map<number, Aggregated>()
           const productNmIdToImt = new Map<number, number | null>()
           for (const nm of productNmIds) {
             productNmIdAgg.set(nm, aggByNmId.get(nm) ?? aggregateStats([]))
             productNmIdToImt.set(nm, nmIdToImtId.get(nm) ?? null)
           }
           // imtIds данного продукта (unique non-null) → собрать sub-Map per imt
           const productImtIds = new Set<number>()
           for (const nm of productNmIds) {
             const imt = nmIdToImtId.get(nm)
             if (imt != null) productImtIds.add(imt)
           }
           const productImtIdAgg = new Map<number, Aggregated>()
           for (const imt of productImtIds) {
             productImtIdAgg.set(imt, aggByImtId.get(imt) ?? aggregateStats([]))
           }

           groups.push({
             product: {
               id: product.id, name: product.name, article: product.article, sku: product.sku,
               photoUrl: product.photos[0]?.url ?? null,
               brand: product.brand ? {
                 id: product.brand.id, name: product.brand.name, directionId: product.brand.directionId ?? null,
               } : null,
               category: product.category ? { id: product.category.id, name: product.category.name } : null,
               subcategory: product.subcategory ? { id: product.subcategory.id, name: product.subcategory.name } : null,
             },
             productAgg: aggByProduct.get(productId) ?? aggregateStats([]),
             campaigns: linkedCampaigns.map(c => ({
               advertId: c.advertId, name: c.name, type: c.type, status: c.status,
               agg: aggByCampaign.get(c.advertId) ?? aggregateStats([]),
             })),
             nmIdAgg: productNmIdAgg,
             imtIdAgg: productImtIdAgg,
             nmIdToImtId: productNmIdToImt,
           })
         }
         groups.sort((a, b) => compareProductsByHierarchy(a.product, b.product))

         // ── 7.5. NEW (fix-iter 2): построение flat rows для не-product режимов ──
         let view: React.ComponentProps<typeof AdvertCampaignsTable>["view"]
         if (validGroupBy === "imt") {
           // Per-imt rows
           const productNamesByImt = new Map<number, Set<string>>()
           const nmIdsByImt = new Map<number, Set<number>>()
           for (const [nm, imt] of nmIdToImtId) {
             if (imt == null) continue
             const pid = nmIdToProductId.get(nm)
             const pname = pid ? productById.get(pid)?.name : null
             if (pname) {
               if (!productNamesByImt.has(imt)) productNamesByImt.set(imt, new Set())
               productNamesByImt.get(imt)!.add(pname)
             }
             if (!nmIdsByImt.has(imt)) nmIdsByImt.set(imt, new Set())
             nmIdsByImt.get(imt)!.add(nm)
           }
           const imtRows = Array.from(aggByImtId.entries()).map(([imt, agg]) => ({
             imtId: imt,
             productNames: Array.from(productNamesByImt.get(imt) ?? []),
             nmIds: Array.from(nmIdsByImt.get(imt) ?? []),
             agg,
           })).sort((a, b) => b.agg.totalSpent - a.agg.totalSpent)
           view = { groupBy: "imt", rows: imtRows }
         } else if (validGroupBy === "campaign") {
           const campRows = campaigns.map(c => ({
             advertId: c.advertId, name: c.name, type: c.type, status: c.status,
             agg: aggByCampaign.get(c.advertId) ?? aggregateStats([]),
           })).sort((a, b) => b.agg.totalSpent - a.agg.totalSpent)
           view = { groupBy: "campaign", rows: campRows }
         } else if (validGroupBy === "type") {
           const countByType = new Map<number, number>()
           for (const c of campaigns) countByType.set(c.type, (countByType.get(c.type) ?? 0) + 1)
           const typeRows = Array.from(aggByType.entries()).map(([t, agg]) => ({
             type: t, campaignCount: countByType.get(t) ?? 0, agg,
           })).sort((a, b) => b.agg.totalSpent - a.agg.totalSpent)
           view = { groupBy: "type", rows: typeRows }
         } else {
           view = { groupBy: "product", groups }
         }

         // ── 8. Опции для AdsFilters ──
         const [directions, brands, categories, subcategories, distinctTypes] = await Promise.all([
           prisma.productDirection.findMany({ select: { id: true, name: true } }),
           prisma.brand.findMany({ select: { id: true, name: true, directionId: true } }),
           prisma.category.findMany({ select: { id: true, name: true, brandId: true } }),
           prisma.subcategory.findMany({ select: { id: true, name: true, categoryId: true } }),
           prisma.wbAdvertCampaign.findMany({ select: { type: true }, distinct: ["type"] }),
         ])

         return (
           <div>
             <AdsTabs />
             <div className="flex items-center gap-3 px-4 py-2">
               <AdsFilters
                 directions={directions} brands={brands}
                 categories={categories} subcategories={subcategories}
                 campaignTypes={distinctTypes.map(t => t.type)}
               />
               <AdsGroupByToggle />
             </div>
             <AdvertCampaignsTable view={view} />
           </div>
         )
       }
       ```

       **W8 закрыт:** каскадная фильтрация полностью раскрыта (см. секция «5. Каскадная Prisma where» — построение `productCascadeWhere` с 4 уровнями вложенности).

       **Scope fix-iter 2 закрыт:** добавлены 3 новые dimensions (nmId / imt / type), maps `nmIdToImtId` и `advertIdToType` подгружаются в RSC, view-shape передаётся discriminated union по `groupBy`.

    5. Запустить `npx tsc --noEmit` — 0 errors. **НЕ** запускать `npm run build` здесь — full Next.js build делается только в финальном Plan 19-08 перед deploy. Type-check достаточно для wave-5 валидации.
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify uses bash-only syntax (test -f, grep, &&, escaping for parens).</note>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && test -f "app/(dashboard)/ads/wb/page.tsx" && test -f components/ads/AdsFilters.tsx && test -f components/ads/AdsGroupByToggle.tsx && test -f components/ads/AdvertCampaignsTable.tsx && grep -q "compareProductsByHierarchy" "app/(dashboard)/ads/wb/page.tsx" && grep -q "wbAdvertStatDaily" "app/(dashboard)/ads/wb/page.tsx" && grep -q "productCascadeWhere\|directionId.*in" "app/(dashboard)/ads/wb/page.tsx" && grep -q "import type { ProductCampaignGroup" components/ads/AdvertCampaignsTable.tsx && grep -q "groupByImtId\|groupByNmId\|groupByType" "app/(dashboard)/ads/wb/page.tsx" && grep -q "nmIdToImtId\|advertIdToType" "app/(dashboard)/ads/wb/page.tsx" && grep -q "validGroupBy\|groupBy" "app/(dashboard)/ads/wb/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `/ads/wb/page.tsx` — RSC; запрашивает wbAdvertCampaign + wbAdvertStatDaily + marketplaceArticle + **wbCard (для imtId)**
    - Построены maps `nmIdToImtId` (из WbCard) и `advertIdToType` (из WbAdvertCampaign)
    - Вызываются все 5 group-* функций; результат складывается в discriminated union view по `?groupBy=product|imt|campaign|type`
    - Каждая `ProductCampaignGroup` содержит `nmIdAgg` + `imtIdAgg` + `nmIdToImtId` per-product sub-maps (для Plan 19-06 expand-панели)
    - `components/ads/AdsFilters.tsx` — client component с каскадными фильтрами + period selector
    - `components/ads/AdsGroupByToggle.tsx` — 4-button toggle, мутирует `?groupBy=` в URL
    - `components/ads/AdvertCampaignsTable.tsx` — принимает discriminated `view` prop, рендерит 4 layout-а; импортирует `ProductCampaignGroup` + `Aggregated` из `@/lib/wb-advert-aggregations`
    - Каскадная Prisma where реализована полностью (4 уровня — direction/brand/category/subcategory)
    - Sorting через `compareProductsByHierarchy` (глобальная иерархия товаров) для product-режима
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Страница /ads/wb рендерится с таблицей; фильтры работают; 4-режимный groupBy switch работает; per-Product/per-nmId/per-imt/per-campaign/per-type агрегации рассчитываются; sub-maps доступны в groups для Plan 19-06</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/wb-advert-aggregations.test.ts --run` exits 0 (9 тестов)
- `npx tsc --noEmit` exits 0
- После deploy: /ads/wb доступна суперадмину; для VIEWER без ADS scope — 403
- /ads/wb?groupBy=imt показывает таблицу со связками; /ads/wb?groupBy=type показывает агрегаты по типам РК
</verification>

<success_criteria>
- Раздел /ads/wb работает: каскадные фильтры, period selector, sticky-таблица с per-product rowSpan
- 4-режимный groupBy switch (по товару / по связке / по кампании / по типу РК) корректно переключает агрегацию
- Колонки Потрачено/Заказов/Оборот/ДРР/CPC/CTR/CR показывают корректные значения за выбранный период во всех 4 режимах
- Sidebar пункт «Управление рекламой» виден для пользователей с ADS scope
- Все pure helpers покрыты unit-тестами (9 тестов)
- ProductCampaignGroup — single source of truth (нет drift между page и component) + содержит per-product nmIdAgg / imtIdAgg для Plan 19-06
- 3 пользовательских dimensions из исходного prompt («по типу РК, по карточкам товара, по связкам») реализованы как pure aggregations и UI режимы
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-05-SUMMARY.md`
</output>
</content>
</invoke>