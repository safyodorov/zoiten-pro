---
phase: 19-wb-ads
plan: "02"
type: execute
wave: 2
depends_on: ["W0"]
files_modified:
  - lib/wb-token.ts
  - lib/wb-token-validate.ts
  - lib/wb-jwt.ts
  - app/actions/wb-tokens.ts
  - components/settings/WbTokensTab.tsx
  - tests/wb-jwt.test.ts
  - tests/wb-token-validate.test.ts
autonomous: true
requirements: [TOKEN-WB_ADS_TOKEN, TOKEN-VALIDATE-SCOPE-30, TOKEN-UI-CARD]
user_setup:
  - service: "Wildberries Advert API"
    why: "WB_ADS_TOKEN JWT нужно сгенерировать в ЛК WB → Настройки → API-токены с галочкой Продвижение"
    env_vars:
      - name: WB_ADS_TOKEN
        source: "ЛК WB → Настройки → API-токены → создать токен с scope Продвижение (для bootstrap из env). После Wave 3 будет редактироваться через UI."
must_haves:
  truths:
    - "lib/wb-token.ts экспортирует WB_TOKEN_NAMES с новым значением WB_ADS_TOKEN"
    - "lib/wb-token-validate.ts знает probe endpoint и required scope биты для WB_ADS_TOKEN"
    - "/admin/settings → WB API токены показывает 4-ю карточку «WB Реклама»"
    - "Bootstrap из env работает: getWbToken('WB_ADS_TOKEN') возвращает значение из process.env.WB_ADS_TOKEN при пустой БД"
  artifacts:
    - path: "lib/wb-token.ts"
      provides: "WbTokenName type расширен значением WB_ADS_TOKEN"
      contains: "WB_ADS_TOKEN"
    - path: "lib/wb-token-validate.ts"
      provides: "PROBE_ENDPOINTS + REQUIRED_SCOPE_BITS для WB_ADS_TOKEN"
      contains: "advert-api.wildberries.ru"
    - path: "app/actions/wb-tokens.ts"
      provides: "DISPLAY_NAMES + listWbTokens() возвращают карточку для WB_ADS_TOKEN"
      contains: "WB Реклама"
  key_links:
    - from: "lib/wb-token.ts"
      to: "lib/wb-adv-api.ts (Plan 19-03)"
      via: "Plan 19-03 вызывает getWbToken('WB_ADS_TOKEN') для авторизации всех вызовов Advert API"
      pattern: "getWbToken.*WB_ADS_TOKEN"
    - from: "components/settings/WbTokensTab.tsx"
      to: "Browser /admin/settings"
      via: "В UI появляется 4-я карточка с кнопкой «Заменить»"
      pattern: "WB Реклама"
---

<objective>
Расширить инфраструктуру WB API токенов (созданную в quick 260512-jxh) поддержкой нового токена WB_ADS_TOKEN. Это даст единый паттерн авторизации/replace UI/scope validation для рекламного API. Без этого Plan 19-03 не сможет получить токен через `getWbToken("WB_ADS_TOKEN")`.

Purpose: Без расширения WB_TOKEN_NAMES вся Phase 19 не будет иметь токена. UI карточка в /admin/settings даёт визуальный контроль (scope chips + срок действия), как у трёх существующих токенов.

Output: 4-я карточка «WB Реклама» в /admin/settings → WB API токены; getWbToken("WB_ADS_TOKEN") работает.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-wb-ads/19-RESEARCH.md
@.planning/phases/19-wb-ads/19-W0-NOTES.md
@.planning/quick/260512-jxh-wb-api-crud-api-ssh/260512-jxh-SUMMARY.md
@CLAUDE.md
@lib/wb-token.ts
@lib/wb-token-validate.ts
@lib/wb-jwt.ts
@app/actions/wb-tokens.ts
@components/settings/WbTokensTab.tsx

<interfaces>
<!-- Существующий source of truth (quick 260512-jxh). Расширяем минимально. -->

From lib/wb-token.ts:
```typescript
export const WB_TOKEN_NAMES = [
  "WB_API_TOKEN",
  "WB_RETURNS_TOKEN",
  "WB_CHAT_TOKEN",
] as const
export type WbTokenName = (typeof WB_TOKEN_NAMES)[number]
export async function getWbToken(name: WbTokenName): Promise<string>
export function invalidateWbTokenCache(name?: WbTokenName): void
```

From lib/wb-token-validate.ts:
```typescript
export const REQUIRED_SCOPE_BITS: Record<WbTokenName, number[]> = {
  WB_API_TOKEN: [1, 2, 3, 5, 6, 7],
  WB_RETURNS_TOKEN: [11],
  WB_CHAT_TOKEN: [9],
}
const PROBE_ENDPOINTS: Record<WbTokenName, string> = {
  WB_API_TOKEN: "https://content-api.wildberries.ru/ping",
  WB_RETURNS_TOKEN: "https://returns-api.wildberries.ru/api/v1/claims?is_archive=false&limit=1",
  WB_CHAT_TOKEN: "https://buyer-chat-api.wildberries.ru/api/v1/seller/events?next=0",
}
```

From app/actions/wb-tokens.ts:
```typescript
const DISPLAY_NAMES: Record<WbTokenName, string> = {
  WB_API_TOKEN: "WB Основной",
  WB_RETURNS_TOKEN: "WB Возвраты",
  WB_CHAT_TOKEN: "WB Чат",
}
```

From lib/wb-jwt.ts:
```typescript
export const WB_SCOPE_LABELS: Record<number, string> = {
  1: "Контент",
  2: "Аналитика",
  3: "Цены",
  5: "Отзывы",
  6: "Статистика",
  7: "Тарифы",
  9: "Чат",
  11: "Возвраты",
  // bit для «Реклама/Продвижение» — отсутствует, нужно добавить, реальный номер берём из W0-NOTES
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить WB_TOKEN_NAMES + REQUIRED_SCOPE_BITS + PROBE_ENDPOINTS + WB_SCOPE_LABELS значением WB_ADS_TOKEN</name>
  <read_first>
    - lib/wb-token.ts
    - lib/wb-token-validate.ts
    - lib/wb-jwt.ts
    - .planning/phases/19-wb-ads/19-W0-NOTES.md (КРИТИЧНО: bit для scope «Реклама/Продвижение» нужно взять из этого файла — он мог оказаться 30, 8, или иным; pattern в action/verify ниже использует placeholder <BIT_FROM_W0>)
  </read_first>
  <behavior>
    - Test 1: `WB_TOKEN_NAMES` экспортирует массив из 4 элементов, включая `"WB_ADS_TOKEN"`
    - Test 2: `REQUIRED_SCOPE_BITS.WB_ADS_TOKEN` содержит подтверждённый bit «Реклама/Продвижение» из W0-NOTES (НЕ хардкодить 30 в тесте — читать из импорта `REQUIRED_SCOPE_BITS.WB_ADS_TOKEN[0]`)
    - Test 3: `WB_SCOPE_LABELS[<BIT_FROM_W0>]` === `"Реклама"` (или «Продвижение» — точное русское имя из 19-W0-NOTES.md)
    - Test 4: `validateWbToken("WB_ADS_TOKEN", <jwt-без-bit>)` возвращает `{ ok: false, error: <с упоминанием Реклама/Продвижение> }` (skip probe call mock'ом fetch)
    - Test 5 (опционально): `validateWbToken("WB_ADS_TOKEN", <jwt-с-bit>)` с моком fetch на 200 возвращает `{ ok: true }`
  </behavior>
  <files>lib/wb-token.ts, lib/wb-token-validate.ts, lib/wb-jwt.ts, tests/wb-jwt.test.ts, tests/wb-token-validate.test.ts</files>
  <action>
    **ШАГ 0 (КРИТИЧНО — выполнить ПЕРВЫМ):** Прочитать `.planning/phases/19-wb-ads/19-W0-NOTES.md` секцию «JWT scope analysis». Извлечь:
    - `BIT_FROM_W0` — номер bit'а для scope «Реклама/Продвижение».
    - `LABEL_FROM_W0` — русское имя scope из WB UI.

    **Phase 19 W0 (2026-05-19) RESOLVED:**
    - `BIT_FROM_W0 = 30` — empirically verified (см. project_wb_advert_api memory). WB_API_TOKEN с bit 30 в scope успешно проходит `/promotion/count` 200; bit 4 (который раньше был в `WB_SCOPE_LABELS` как «Продвижение») в WB_API_TOKEN отсутствует, при этом Advert API работает — следовательно bit 4 НЕ нужен для Advert (старая label устарела).
    - `LABEL_FROM_W0 = "Продвижение"` — стандартный термин WB cabinet UI.
    - Также обязательно обновить `lib/wb-jwt.ts` `WB_SCOPE_LABELS`: УДАЛИТЬ старую запись `4: "Продвижение"` (помеченную в W0 как ошибочную) если она есть, ДОБАВИТЬ `30: "Продвижение"`.

    Если NOTES противоречит сам себе (например, в одном месте 30, в другом 8) — остановиться и спросить пользователя.

    1. **lib/wb-token.ts:** Расширить `WB_TOKEN_NAMES` массивом из 4 элементов, добавив `"WB_ADS_TOKEN"`:
       ```typescript
       export const WB_TOKEN_NAMES = [
         "WB_API_TOKEN",
         "WB_RETURNS_TOKEN",
         "WB_CHAT_TOKEN",
         "WB_ADS_TOKEN",      // Phase 19
       ] as const
       ```
       `WbTokenName` type автоматически подхватит. `bootstrapFromEnv()` уже работает generic'ом — читает `process.env[name]`, никаких изменений не нужно.

    2. **lib/wb-token-validate.ts:** Расширить `REQUIRED_SCOPE_BITS` и `PROBE_ENDPOINTS`:
       ```typescript
       export const REQUIRED_SCOPE_BITS: Record<WbTokenName, number[]> = {
         WB_API_TOKEN: [1, 2, 3, 5, 6, 7],
         WB_RETURNS_TOKEN: [11],
         WB_CHAT_TOKEN: [9],
         WB_ADS_TOKEN: [<BIT_FROM_W0>],   // Phase 19 — bit для scope «Реклама/Продвижение», подтверждённый в W0-NOTES
       }
       const PROBE_ENDPOINTS: Record<WbTokenName, string> = {
         WB_API_TOKEN: "https://content-api.wildberries.ru/ping",
         WB_RETURNS_TOKEN: "...",
         WB_CHAT_TOKEN: "...",
         WB_ADS_TOKEN: "https://advert-api.wildberries.ru/adv/v1/promotion/count",  // Phase 19
       }
       ```

       Probe endpoint /promotion/count выбран потому что:
       - Подтверждён живым в W0 smoke check
       - GET без body, минимальный response
       - При scope mismatch вернёт 401/403 (валидация поймает)

    3. **lib/wb-jwt.ts:** Добавить ярлык для подтверждённого bit в `WB_SCOPE_LABELS`:
       ```typescript
       export const WB_SCOPE_LABELS: Record<number, string> = {
         1: "Контент",
         2: "Аналитика",
         3: "Цены",
         5: "Отзывы",
         6: "Статистика",
         7: "Тарифы",
         9: "Чат",
         11: "Возвраты",
         <BIT_FROM_W0>: "<LABEL_FROM_W0>",       // Phase 19 — например `30: "Реклама"` или `8: "Продвижение"`
       }
       ```

    4. **Тесты** (расширить существующие):
       - `tests/wb-jwt.test.ts`: добавить assertion что `WB_SCOPE_LABELS[<BIT_FROM_W0>]` === строка, содержащая «Реклам» или «Продвижен» (regex match `/Реклам|Продвижен/`).
       - `tests/wb-token-validate.test.ts`: добавить тесты с моком `globalThis.fetch` (vi.fn) для WB_ADS_TOKEN. Не хардкодить bit — импортировать `REQUIRED_SCOPE_BITS` из lib и пользоваться `REQUIRED_SCOPE_BITS.WB_ADS_TOKEN[0]`:
         ```ts
         import { REQUIRED_SCOPE_BITS } from "@/lib/wb-token-validate"
         const adsBit = REQUIRED_SCOPE_BITS.WB_ADS_TOKEN[0]
         const okMask = 1 << adsBit
         ```
         - JWT с scopeBitmask=0 → возврат `{ok:false}` с упоминанием «Реклам/Продвижен» (regex)
         - JWT с scopeBitmask=okMask + mock fetch 200 → возврат `{ok:true}`
         - JWT с scopeBitmask=okMask + mock fetch 401 → возврат `{ok:false, error: содержит "401"}`
       Использовать `vi.mocked(fetch)` или `vi.spyOn(globalThis, "fetch")` — паттерн из tests/wb-token-cache.test.ts.

    NB: НЕ менять сигнатуры validateWbToken / getWbToken — добавляем только данные в Record'ах.
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — shell uses bash-only syntax (jq, grep, &&).</note>
    <automated>npm run test -- tests/wb-jwt.test.ts tests/wb-token-validate.test.ts --run 2>&1 | tail -5 && grep -q "WB_ADS_TOKEN" lib/wb-token.ts && grep -q "WB_ADS_TOKEN" lib/wb-token-validate.ts && ADS_BIT=$(node -e 'const {REQUIRED_SCOPE_BITS}=require("./lib/wb-token-validate.ts"); console.log(REQUIRED_SCOPE_BITS.WB_ADS_TOKEN[0])' 2>/dev/null || grep -oP 'WB_ADS_TOKEN:\s*\[\K[0-9]+' lib/wb-token-validate.ts | head -1) && echo "ads_bit=$ADS_BIT" && grep -E "^[[:space:]]*${ADS_BIT}:" lib/wb-jwt.ts | grep -qE "Реклам|Продвижен" && grep -qF "[${ADS_BIT}]" lib/wb-token-validate.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/wb-token.ts` содержит `"WB_ADS_TOKEN"` в `WB_TOKEN_NAMES`
    - `lib/wb-token-validate.ts` содержит запись `WB_ADS_TOKEN: [<bit>]` в `REQUIRED_SCOPE_BITS`, где `<bit>` — подтверждённый в `19-W0-NOTES.md` номер scope-бита «Реклама/Продвижение»
    - `lib/wb-token-validate.ts` содержит URL `advert-api.wildberries.ru/adv/v1/promotion/count` в PROBE_ENDPOINTS
    - `lib/wb-jwt.ts` содержит ярлык в `WB_SCOPE_LABELS` под тем же `<bit>` ключом, имя содержит «Реклам» или «Продвижен»
    - bit number в `WB_SCOPE_LABELS` совпадает с тем что зафиксирован в `19-W0-NOTES.md` (verify grep сверяет оба файла)
    - `npm run test -- tests/wb-jwt.test.ts tests/wb-token-validate.test.ts --run` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>WB_ADS_TOKEN теперь полноправный участник системы токенов: `getWbToken("WB_ADS_TOKEN")` работает, validateWbToken проверяет scope+probe</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Добавить DISPLAY_NAMES + UI карточку «WB Реклама» в /admin/settings</name>
  <read_first>
    - app/actions/wb-tokens.ts (особенно DISPLAY_NAMES и listWbTokens)
    - components/settings/WbTokensTab.tsx (общая структура — карточки рендерятся из массива tokens)
  </read_first>
  <files>app/actions/wb-tokens.ts, components/settings/WbTokensTab.tsx</files>
  <action>
    1. **app/actions/wb-tokens.ts:** Расширить `DISPLAY_NAMES`:
       ```typescript
       const DISPLAY_NAMES: Record<WbTokenName, string> = {
         WB_API_TOKEN: "WB Основной",
         WB_RETURNS_TOKEN: "WB Возвраты",
         WB_CHAT_TOKEN: "WB Чат",
         WB_ADS_TOKEN: "WB Реклама",   // Phase 19
       }
       ```

       `listWbTokens()` уже итерирует по `WB_TOKEN_NAMES`, поэтому 4-я карточка автоматически появится — нужно только переименовать (что уже сделано через DISPLAY_NAMES).

       `replaceWbToken()` уже generic — работает для любого `WbTokenName`. Никаких изменений не нужно.

    2. **components/settings/WbTokensTab.tsx:** Грид уже `lg:grid-cols-2 xl:grid-cols-3`. При 4 карточках на xl-экранах будет 3+1 — это нормально (или поменять на `xl:grid-cols-4` для симметрии). Поменять на:
       ```tsx
       <div className="grid gap-4 md:grid-cols-1 lg:grid-cols-2 xl:grid-cols-4">
       ```

    3. Запустить `npx tsc --noEmit` — должен пройти 0 errors. **НЕ** запускать `npm run build` здесь — full Next.js build делается только в финальном Plan 19-08 перед deploy. Type-check достаточно для wave-2 валидации.

    4. Никаких новых тестов не нужно — listWbTokens / replaceWbToken тестируется в tests/wb-tokens-actions.test.ts, и существующие тесты автоматически проверят 4-й токен (через `WB_TOKEN_NAMES` итерацию).
  </action>
  <verify>
    <note>Run via Bash tool (not PowerShell) — verify command uses bash-only syntax.</note>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -q '"WB Реклама"' app/actions/wb-tokens.ts && grep -q "xl:grid-cols-4" components/settings/WbTokensTab.tsx && npm run test -- tests/wb-tokens-actions.test.ts --run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app/actions/wb-tokens.ts` содержит `WB_ADS_TOKEN: "WB Реклама"` в `DISPLAY_NAMES`
    - `components/settings/WbTokensTab.tsx` grid обновлён на `xl:grid-cols-4`
    - `npx tsc --noEmit` exits 0
    - `npm run test -- tests/wb-tokens-actions.test.ts --run` exits 0 (существующие тесты всё ещё проходят с 4 токенами)
  </acceptance_criteria>
  <done>4-я карточка «WB Реклама» появилась в /admin/settings → WB API токены (когда токен отсутствует — «Токен не настроен», когда есть — scope chips «Реклама»)</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/wb-jwt.test.ts tests/wb-token-validate.test.ts tests/wb-tokens-actions.test.ts --run` exits 0
- `npx tsc --noEmit` exits 0
- `grep -c WB_ADS_TOKEN lib/wb-token.ts lib/wb-token-validate.ts app/actions/wb-tokens.ts` показывает ≥3 hits
- Bit number в `lib/wb-jwt.ts` WB_SCOPE_LABELS совпадает с зафиксированным в `19-W0-NOTES.md`
</verification>

<success_criteria>
- WB_ADS_TOKEN — 4-й полноправный токен в системе
- В UI /admin/settings → WB API токены 4-я карточка появляется
- Любой код (особенно Plan 19-03 lib/wb-adv-api.ts) может вызвать `await getWbToken("WB_ADS_TOKEN")` и получить значение из БД или env
</success_criteria>

<output>
After completion, create `.planning/phases/19-wb-ads/19-02-SUMMARY.md`
</output>
</content>
</invoke>