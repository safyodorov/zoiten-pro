---
phase: 22-bank-accounts
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - lib/sections.ts
  - components/layout/section-titles.ts
  - components/layout/nav-items.ts
  - lib/section-labels.ts
  - app/(dashboard)/bank/page.tsx
autonomous: true
requirements: [BANK-03]
must_haves:
  truths:
    - "Пункт «Банковские счета» виден в Sidebar и ведёт на /bank"
    - "Заголовок раздела отображается в Header при заходе на /bank"
    - "Раздел BANK доступен тумблером VIEW/MANAGE в /admin/users"
    - "Middleware защищает /bank по ERP_SECTION.BANK"
    - "Страница /bank рендерится для пользователя с доступом (заглушка до 22-05)"
  artifacts:
    - path: "lib/sections.ts"
      provides: "SECTION_PATHS['/bank'] = 'BANK' для middleware route guard"
      contains: "\"/bank\": \"BANK\""
    - path: "lib/section-labels.ts"
      provides: "SECTION_OPTIONS запись BANK для тумблера в /admin/users"
      contains: "value: \"BANK\""
    - path: "components/layout/nav-items.ts"
      provides: "NAV_ITEMS запись BANK + Building2 icon в ICON_MAP"
      contains: "section: \"BANK\""
    - path: "app/(dashboard)/bank/page.tsx"
      provides: "RSC заглушка с requireSection('BANK')"
      contains: "requireSection(\"BANK\")"
  key_links:
    - from: "middleware.ts"
      to: "ERP_SECTION.BANK"
      via: "lib/sections.ts SECTION_PATHS"
      pattern: "\"/bank\".*BANK"
    - from: "Sidebar"
      to: "/bank"
      via: "NAV_ITEMS + ICON_MAP Building2"
      pattern: "Building2"
---

<objective>
Провести новый раздел `/bank` через все 6 точек чеклиста CLAUDE.md для нового ERP_SECTION + создать RSC-заглушку страницы с RBAC-guard. Иконка Building2 (Landmark занят Credits).

Purpose: Сделать раздел доступным в навигации и управляемым через RBAC до того, как 22-05 наполнит его таблицей. Параллелится с 22-03 (чистая lib, не пересекается по файлам).
Output: 4 обновлённых конфиг-файла + новая страница `app/(dashboard)/bank/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-bank-accounts/22-CONTEXT.md
@.planning/phases/22-bank-accounts/22-RESEARCH.md

<interfaces>
<!-- Текущие значения конфиг-файлов — добавляем BANK по образцу CREDITS. -->

lib/sections.ts SECTION_PATHS (последняя запись `"/credits": "CREDITS",`) — добавить `"/bank": "BANK",`.

components/layout/section-titles.ts — образец:
```typescript
{ match: /^\/credits/, title: "Кредиты" },
{ match: /^\/employees/, title: "Сотрудники" },
```

components/layout/nav-items.ts — образец записи CREDITS + блок импортов lucide (Landmark уже есть; Building2 НЕТ — добавить):
```typescript
{ section: "CREDITS", href: "/credits", label: "Кредиты", icon: "Landmark" },
```
ICON_MAP — объект, ключи = строки иконок.

lib/section-labels.ts SECTION_OPTIONS — образец:
```typescript
{ value: "CREDITS",         label: "Кредиты" },
{ value: "SUPPORT",         label: "Служба поддержки" },
```

lib/rbac.ts — `requireSection(section, minRole?)` и `getSectionRole(section)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Провести 6-точечный чеклист ERP_SECTION для BANK</name>
  <read_first>
    - lib/sections.ts (SECTION_PATHS объект)
    - components/layout/section-titles.ts (массив match/title)
    - components/layout/nav-items.ts (NAV_ITEMS массив + блок импортов lucide-react + ICON_MAP объект)
    - lib/section-labels.ts (SECTION_OPTIONS массив — точка которую ЧАСТО ЗАБЫВАЮТ)
  </read_first>
  <action>
**Точка 1 (enum + миграция)** — УЖЕ сделано в плане 22-01 (ERP_SECTION.BANK + ALTER TYPE). В этом плане не трогать schema/migration.

**Точка 2 — `lib/sections.ts`:** в объекте `SECTION_PATHS` после строки `"/credits": "CREDITS",` добавить:
```typescript
  "/bank": "BANK",
```

**Точка 3 — `components/layout/section-titles.ts`:** в массиве правил (перед записью employees `{ match: /^\/employees/, ... }`) добавить:
```typescript
  { match: /^\/bank/, title: "Банковские счета" },
```

**Точка 4 — `components/layout/nav-items.ts`:**
  - В блоке импортов из `lucide-react` добавить `Building2,` (рядом с `Landmark,`).
  - В массиве `NAV_ITEMS` после записи CREDITS (`{ section: "CREDITS", href: "/credits", label: "Кредиты", icon: "Landmark" },`) добавить:
    ```typescript
    { section: "BANK", href: "/bank", label: "Банковские счета", icon: "Building2" },
    ```
  - В объекте `ICON_MAP` добавить ключ `Building2,` (рядом с `Landmark,`).

**Точка 5 — `lib/section-labels.ts` (⚠ КРИТИЧНО, часто забывают):** в массив `SECTION_OPTIONS` после записи CREDITS добавить:
```typescript
  { value: "BANK", label: "Банковские счета" },
```

**Точка 6 (карточка дашборда)** — опциональна, ПРОПУСТИТЬ в этом плане (раздел read-only, дашборд-карточка не требуется в scope этапа 1).

Все надписи на русском (CLAUDE.md convention).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sections|nav-items|section-labels|section-titles" || echo "NO TS ERRORS in section files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"/bank": "BANK"' lib/sections.ts` находит
    - `grep 'match: /\^\\/bank/' components/layout/section-titles.ts` (regex /^\/bank/) находит + title "Банковские счета"
    - `grep 'section: "BANK"' components/layout/nav-items.ts` находит + `icon: "Building2"`
    - `grep "Building2" components/layout/nav-items.ts` находит минимум 2 раза (импорт + ICON_MAP) + 1 в NAV_ITEMS = 3 совпадения
    - `grep 'value: "BANK"' lib/section-labels.ts` находит + label "Банковские счета"
    - tsc не выдаёт ошибок в этих 4 файлах
  </acceptance_criteria>
  <done>Все 5 обязательных точек чеклиста проведены; BANK виден в Sidebar, Header, /admin/users тумблере, защищён middleware.</done>
</task>

<task type="auto">
  <name>Task 2: Создать RSC-заглушку app/(dashboard)/bank/page.tsx</name>
  <read_first>
    - app/(dashboard)/credits/page.tsx (образец RSC page с requireSection + getSectionRole + flex layout)
    - lib/rbac.ts (сигнатуры requireSection / getSectionRole)
  </read_first>
  <action>
Создать `app/(dashboard)/bank/page.tsx` как минимальную RSC-заглушку (полноценная таблица — в 22-05). Назначение: дать рабочую защищённую точку входа для проверки RBAC/навигации в этой волне.

```typescript
import { requireSection } from "@/lib/rbac"

export default async function BankPage() {
  await requireSection("BANK")

  return (
    <div className="h-full flex flex-col gap-3">
      <div className="flex items-center justify-between">
        <h2 className="text-sm font-medium">Банковские операции</h2>
      </div>
      <div className="flex-1 min-h-0 flex items-center justify-center text-sm text-muted-foreground">
        Импорт выписок и таблица операций появятся здесь (этап 22-05).
      </div>
    </div>
  )
}
```

НЕ добавлять импорт выписок / таблицу — это scope 22-04/22-05. Только guard + плейсхолдер.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "bank/page" || echo "NO TS ERRORS in bank/page"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `app/(dashboard)/bank/page.tsx` существует
    - `grep 'requireSection("BANK")' app/(dashboard)/bank/page.tsx` находит
    - `grep "flex-1 min-h-0" app/(dashboard)/bank/page.tsx` находит (sticky-ready layout per CLAUDE.md)
    - tsc не выдаёт ошибок для bank/page
  </acceptance_criteria>
  <done>Заглушка /bank рендерится для пользователя с доступом BANK; неавторизованный/без доступа получает redirect через requireSection.</done>
</task>

</tasks>

<verification>
- tsc чистый для всех 5 затронутых файлов
- 6-точечный чеклист: точки 2-5 проведены (точка 1 в 22-01, точка 6 опц. пропущена)
- /bank защищён и навигируем
</verification>

<success_criteria>
- BANK присутствует в SECTION_PATHS, section-titles, NAV_ITEMS+ICON_MAP, SECTION_OPTIONS
- Building2 импортирован и замаплен
- /bank рендерит заглушку под RBAC guard
</success_criteria>

<output>
After completion, create `.planning/phases/22-bank-accounts/22-02-SUMMARY.md`.
Note: провизионирование UserSectionRole.BANK существующим пользователям выполняется в 22-05 (после deploy миграции) — спросить пользователя кому нужен доступ (memory: feedback_zoiten_new_section_rbac).
</output>
