---
phase: 22-bank-accounts
plan: 05
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03", "22-04"]
files_modified:
  - app/(dashboard)/bank/page.tsx
  - components/bank/BankTransactionsTable.tsx
  - components/bank/BankFilters.tsx
  - lib/bank-labels.ts
  - scripts/import-bank-statements.cjs
autonomous: false
requirements: [BANK-08, BANK-09, BANK-10]
user_setup: []
must_haves:
  truths:
    - "Пользователь открывает /bank и видит таблицу всех операций (sticky header)"
    - "Фильтры компания/счёт/банк/дата/направление/категория + поиск по назначению/контрагенту сужают список"
    - "Inline-select меняет категорию операции, изменение сохраняется"
    - "9 файлов из Выписки/ импортированы (через UI или scripts/import-bank-statements.cjs)"
    - "Повторный прогон импорта идемпотентен (дубли пропущены)"
  artifacts:
    - path: "app/(dashboard)/bank/page.tsx"
      provides: "RSC page: requireSection + data load + filters + sticky table"
      contains: "requireSection(\"BANK\")"
    - path: "components/bank/BankTransactionsTable.tsx"
      provides: "Sticky-таблица операций + inline CategoryCell"
      contains: "border-separate border-spacing-0"
    - path: "components/bank/BankFilters.tsx"
      provides: "URL-driven фильтры (company/account/bank/date/direction/category/search)"
      contains: "useSearchParams"
    - path: "scripts/import-bank-statements.cjs"
      provides: "Разовый локальный импорт 9 файлов из Выписки/"
      contains: "Выписки"
  key_links:
    - from: "BankTransactionsTable CategoryCell"
      to: "categorizeTx"
      via: "useTransition + server action"
      pattern: "categorizeTx"
    - from: "page.tsx"
      to: "prisma.bankTransaction"
      via: "findMany с where из фильтров + include account/counterparty"
      pattern: "bankTransaction.findMany"
    - from: "BankFilters"
      to: "URL searchParams"
      via: "router.push с params"
      pattern: "router.push"
---

<objective>
Заменить заглушку `/bank` (22-02) полноценной read-only RSC-страницей: sticky-таблица операций по паттерну проекта + URL-driven фильтры (компания/счёт/банк/дата/направление/категория + поиск по назначению/контрагенту) + inline-категоризация через native select. Создать `scripts/import-bank-statements.cjs` для разового локального импорта 9 файлов из `Выписки/`. Финальный UAT.

Purpose: Завершить этап 1 — пользователь видит и категоризирует реальные операции. Зависит от всех предыдущих планов.
Output: RSC page + 2 client-компонента + lib/bank-labels.ts + скрипт импорта + UAT-checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bank-accounts/22-CONTEXT.md
@.planning/phases/22-bank-accounts/22-RESEARCH.md

<interfaces>
<!-- Паттерны: RSC page RESEARCH §3.1; sticky table §3.2; filters §3.3; inline cat §8. -->

requireSection("BANK") / getSectionRole("BANK") из @/lib/rbac (getSectionRole возвращает "VIEW"|"MANAGE"|null; SUPERADMIN → "MANAGE").
categorizeTx(id, category) из @/app/actions/bank.ts (из 22-04) → { ok } | { ok:false, error }.
BankImportButton из @/components/bank/BankImportButton.tsx (из 22-04).

Sticky-table CLAUDE.md: НЕ shadcn Table/TableHeader/TableRow в шапке; <div overflow-auto h-full> → <table border-separate border-spacing-0> → <thead bg-background> → <tr> прямой HTML → <th sticky top-0 z-20 bg-background border-b>; body — shadcn TableBody/TableRow/TableCell OK. Контейнер таблицы flex-1 min-h-0. Сплошной bg-background (НЕ /80) на sticky.

TxCategory enum: UNCATEGORIZED, INTERNAL_TRANSFER, BANK_FEE, SUPPLIER_PAYMENT, INCOME, TAX, LOAN, OTHER.
TxDirection: DEBIT (расход), CREDIT (приход).

Источник: C:\Users\User\zoiten-pro\Выписки\ — untracked, 9 XLSX (2 ВТБ, 2 ПСБ, 5 Сбер), НЕ коммитить.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/bank-labels.ts + BankTransactionsTable (sticky + inline CategoryCell)</name>
  <read_first>
    - components/credits/CreditsTable.tsx (ТОЧНЫЙ sticky-table шаблон: overflow-auto, border-separate, thead bg-background, th sticky)
    - .planning/phases/22-bank-accounts/22-RESEARCH.md §8 (CategoryCell inline native select + useTransition)
    - app/actions/bank.ts (categorizeTx сигнатура — из 22-04)
  </read_first>
  <action>
**`lib/bank-labels.ts`** — русские метки enum (pure, без server-импортов, можно импортировать в client):
```typescript
export const CATEGORY_LABELS: Record<string, string> = {
  UNCATEGORIZED: "Без категории",
  INTERNAL_TRANSFER: "Внутренний перевод",
  BANK_FEE: "Комиссия банка",
  SUPPLIER_PAYMENT: "Оплата поставщику",
  INCOME: "Поступление выручки",
  TAX: "Налоги/сборы",
  LOAN: "Кредит/проценты",
  OTHER: "Прочее",
}
export const CATEGORY_OPTIONS = Object.entries(CATEGORY_LABELS).map(([value, label]) => ({ value, label }))
export const DIRECTION_LABELS: Record<string, string> = { DEBIT: "Расход", CREDIT: "Приход" }
```

**`components/bank/BankTransactionsTable.tsx`** ("use client"):
  - Props: `rows: BankTxRow[]`, `canManage: boolean`. Тип `BankTxRow` = плоский сериализуемый объект (Decimal→number на сервере): `{ id, date: string, direction, amount: number, currency, docNumber, operationType, purpose, counterpartyName, counterpartyInn, category, companyName, accountNumber, bankName }`.
  - Sticky-таблица СТРОГО по CLAUDE.md: `<div className="overflow-auto h-full rounded-lg border">` → `<table className="w-full border-separate border-spacing-0 text-sm">` → `<thead className="bg-background">` → `<tr>` (прямой HTML) → `<th className="sticky top-0 z-20 bg-background border-b px-3 py-2 text-left text-xs font-semibold text-muted-foreground whitespace-nowrap">`.
  - Колонки: Дата | Компания | Счёт | Банк | Направление | Сумма | Валюта | № док | Контрагент (имя + ИНН) | Назначение | Категория.
  - Body: shadcn `<TableBody>` + `<TableRow className="hover:bg-muted/40">` + `<TableCell>`.
  - Сумма: `amount.toLocaleString("ru-RU", { minimumFractionDigits: 2 })`; DEBIT — красноватый, CREDIT — зеленоватый текст (text-red-600 / text-green-600).
  - Дата: формат ru (`new Date(date).toLocaleDateString("ru-RU")`).
  - Назначение — `whitespace-nowrap max-w-[300px] truncate` + `title` для полного текста.
  - **CategoryCell** (вложенный компонент): native `<select>` (CLAUDE.md), useState(value) + useTransition; onChange → setValue + startTransition(categorizeTx(id, next)); на `!result.ok` → toast.error + откат value. Если `!canManage` → просто `<span>{CATEGORY_LABELS[current]}</span>`.
  - Если rows пустой → строка-плейсхолдер «Нет операций. Загрузите выписку.».
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "bank-labels|BankTransactionsTable" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/bank-labels.ts` существует; `grep "CATEGORY_OPTIONS" lib/bank-labels.ts` находит; 8 ключей в CATEGORY_LABELS
    - `components/bank/BankTransactionsTable.tsx` существует
    - `grep "border-separate border-spacing-0" components/bank/BankTransactionsTable.tsx` находит (sticky pattern)
    - `grep "sticky top-0 z-20 bg-background" components/bank/BankTransactionsTable.tsx` находит; НЕТ `bg-background/` (прозрачность) на sticky — `grep "bg-background/" components/bank/BankTransactionsTable.tsx` → пусто
    - `grep "categorizeTx" components/bank/BankTransactionsTable.tsx` находит + `useTransition`
    - `grep "<select" components/bank/BankTransactionsTable.tsx` находит (native select, НЕ base-ui)
    - tsc чистый
  </acceptance_criteria>
  <done>Sticky-таблица операций с inline-категоризацией; canManage gating; opaque bg на sticky cells.</done>
</task>

<task type="auto">
  <name>Task 2: BankFilters + RSC page /bank (data load + filters + table)</name>
  <read_first>
    - components/credits/CreditsFilters.tsx (URL-driven фильтры: useSearchParams + router.push + buildUrl + native select + MultiSelect inline)
    - app/(dashboard)/credits/page.tsx (RSC: requireSection, getSectionRole, await searchParams, Promise.all data load, flex layout)
    - app/(dashboard)/bank/page.tsx (заглушка из 22-02 — заменить)
  </read_first>
  <action>
**`components/bank/BankFilters.tsx`** ("use client") — URL-driven (CLAUDE.md: URL не localStorage):
  - Props: `companies: {id,name}[]`, `accounts: {id,number}[]`, `banks: {id,name}[]`.
  - `useSearchParams` + `useRouter`; `buildUrl(overrides)` строит `/bank?...`; пустое значение → params.delete.
  - MultiSelectDropdown (inline-копия с чекбоксами, как в CreditsFilters — НЕ импортировать из ui/) для компаний/счетов/банков (значения в URL `companies=id1,id2`).
  - Native `<select>` для Направление (Все/Расход(DEBIT)/Приход(CREDIT)) и Категория (Все + CATEGORY_OPTIONS из lib/bank-labels).
  - Два `<input type="date">` для dateFrom/dateTo.
  - `<input type="search">` debounced (300ms, useRef timer паттерн) для поиска по назначению/контрагенту → param `search`.

**`app/(dashboard)/bank/page.tsx`** — заменить заглушку:
```typescript
import { requireSection, getSectionRole } from "@/lib/rbac"
import { prisma } from "@/lib/prisma"
import { BankImportButton } from "@/components/bank/BankImportButton"
import { BankFilters } from "@/components/bank/BankFilters"
import { BankTransactionsTable } from "@/components/bank/BankTransactionsTable"
```
  - `await requireSection("BANK")`; `const canManage = (await getSectionRole("BANK")) === "MANAGE"`.
  - `const sp = await searchParams` (Next.js 15 Promise). Распарсить: companies/accounts/banks (split ","), direction, category, search, dateFrom, dateTo.
  - Построить `where: Prisma.BankTransactionWhereInput`:
    - companies → `account: { companyId: { in } }`
    - accounts → `accountId: { in }`
    - banks → `account: { bankId: { in } }` (мерж с companies-условием через account: {...})
    - direction → `direction`
    - category → `category`
    - dateFrom/dateTo → `date: { gte/lte: new Date(...) }`
    - search → `OR: [{ purpose: { contains, mode: "insensitive" } }, { counterpartyName: { contains, mode: "insensitive" } }]`
  - `Promise.all`: `bankTransaction.findMany({ where, include: { account: { include: { company: true, bank: true } }, counterparty: true }, orderBy: { date: "desc" }, take: 500 })` + filter options (`company.findMany`, `bankAccount.findMany`, `bank.findMany`).
  - Маппить в `BankTxRow[]` (Decimal `amount` → `Number(t.amount)`, date → ISO string, companyName/accountNumber/bankName из include).
  - Layout: `<div className="h-full flex flex-col gap-3">` → header row с `<h2>` + `{canManage && <BankImportButton />}` → `<BankFilters .../>` → `<div className="flex-1 min-h-0"><BankTransactionsTable rows={rows} canManage={canManage} /></div>`.
  - `take: 500` (read-only, без пагинации в v1; CLAUDE.md prefetch не релевантен — таблица не содержит Link).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "bank/page|BankFilters" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `components/bank/BankFilters.tsx` существует; `grep "useSearchParams" components/bank/BankFilters.tsx` находит + `router.push`
    - `grep "<select" components/bank/BankFilters.tsx` находит (native для direction/category)
    - `app/(dashboard)/bank/page.tsx` содержит `requireSection("BANK")` + `getSectionRole("BANK")`
    - `grep "bankTransaction.findMany" app/(dashboard)/bank/page.tsx` находит + `orderBy` date desc
    - `grep "Number(" app/(dashboard)/bank/page.tsx` находит (Decimal→number)
    - `grep "mode: \"insensitive\"" app/(dashboard)/bank/page.tsx` находит (поиск)
    - `grep "flex-1 min-h-0" app/(dashboard)/bank/page.tsx` находит
    - tsc чистый
  </acceptance_criteria>
  <done>/bank рендерит реальные операции с 6 фильтрами + поиском; canManage показывает кнопку импорта; inline-категоризация работает.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/import-bank-statements.cjs (разовый локальный импорт)</name>
  <read_first>
    - scripts/seed-credits.ts (образец разового seed-скрипта: PrismaClient, fs.readdirSync, XLSX.readFile)
    - lib/bank-import/index.ts (detectFormat/parseStatement/computeFingerprint — переиспользовать; .cjs не сможет импортировать TS напрямую — см. action)
    - app/api/bank-import/route.ts (логика upsert справочников + createMany — продублировать в скрипте ИЛИ вызвать общий helper)
  </read_first>
  <action>
Создать `scripts/import-bank-statements.cjs` — читает все .xlsx из `Выписки/` локально и прогоняет через тот же пайплайн что route.ts.

ВАЖНО: `lib/bank-import/` — TypeScript. .cjs не импортирует TS напрямую. Два варианта:
  - **Вариант A (предпочтительно):** сделать скрипт на `.ts` и запускать через `npx tsx scripts/import-bank-statements.ts` — тогда импорт `@/lib/bank-import` работает. Создать `scripts/import-bank-statements.ts` вместо .cjs (обновить files_modified в SUMMARY). tsx уже доступен (используется в проекте — seed-credits.ts запускается через tsx).
  - Использовать `import { PrismaClient } from "@prisma/client"`, `import * as XLSX from "xlsx"`, `import { detectFormat, parseStatement, computeFingerprint } from "../lib/bank-import"`.

Логика:
```typescript
const dir = path.resolve(process.cwd(), "Выписки")
const files = fs.readdirSync(dir).filter((f) => /\.(xlsx|xls)$/i.test(f))
for (const file of files) {
  const buffer = fs.readFileSync(path.join(dir, file))
  const probe = XLSX.read(buffer, { type: "buffer" })
  const format = detectFormat(file, probe)
  const workbook = format === "sber" ? XLSX.read(buffer, { type: "buffer", raw: false }) : probe
  const { transactions } = parseStatement(format, workbook)
  // тот же upsert Company/Bank/Account/Counterparty + createMany skipDuplicates + ImportBatch как в route.ts
  console.log(`${file} [${format}]: total=${transactions.length} imported=${count} skipped=${total-count}`)
}
```

Идемпотентность: через тот же `fingerprint @unique` + `createMany skipDuplicates`. Повторный запуск → imported 0.

Запуск (документировать в SUMMARY):
  - Локально (Windows): `npx tsx scripts/import-bank-statements.ts`
  - На VPS: `set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/import-bank-statements.ts`

НЕ коммитить файлы из Выписки/ (untracked, реальные данные).

**Чтобы избежать дублирования логики:** вынести upsert+write пайплайн из route.ts в общий helper `lib/bank-import/persist.ts` (`persistParsed(prisma, fileName, format, transactions, importedById?): Promise<{imported, skipped}>`), импортируемый и route.ts (22-04 уже написан — при необходимости рефактор route.ts на helper в этом плане) и скриптом. Если рефактор route.ts расширяет scope — допустимо продублировать логику в скрипте с пометкой в SUMMARY. Предпочтителен helper.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx tsc --noEmit -p tsconfig.json 2>&1 | grep "import-bank-statements" || echo "NO TS ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `scripts/import-bank-statements.ts` (или .cjs) существует
    - `grep "Выписки" scripts/import-bank-statements.ts` находит (читает из правильной папки)
    - `grep "detectFormat" scripts/import-bank-statements.ts` и `grep "skipDuplicates" scripts/import-bank-statements.ts` находят
    - `grep "raw: false" scripts/import-bank-statements.ts` находит (Сбер)
    - tsc чистый для скрипта
  </acceptance_criteria>
  <done>Скрипт читает 9 файлов из Выписки/, прогоняет через парсеры+дедуп, печатает per-file отчёт; идемпотентен.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — deploy миграции, импорт 9 файлов, проверка таблицы и идемпотентности</name>
  <action>
    Агент сначала автоматизирует deploy + provision RBAC + импорт (см. how-to-verify), затем пауза для ручной проверки пользователем.
  </action>
  <what-built>
    Полный раздел /bank: схема+миграция (22-01), проводка раздела (22-02), парсеры (22-03), импорт+категоризация (22-04), таблица+фильтры+скрипт (22-05).
  </what-built>
  <how-to-verify>
    Автоматизировано агентом ДО checkpoint (где возможно):
    1. Deploy на VPS: `ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"` — применит `20260610_phase22_bank` через `prisma migrate deploy`.
    2. Провизионировать BANK доступ суперадмину: спросить пользователя кому ещё нужен доступ; SQL `INSERT INTO "UserSectionRole" (...) VALUES (..., 'BANK', 'MANAGE')` или через /admin/users тумблер (memory feedback_zoiten_new_section_rbac — иначе все кроме SUPERADMIN получат 403; получатель перелогинивается).
    3. Импорт 9 файлов: либо локально `npx tsx scripts/import-bank-statements.ts`, либо через UI кнопку «Загрузить выписку» для каждого файла.

    Пользователь проверяет вручную:
    1. Открыть https://zoiten.pro/bank — видна таблица операций со всеми колонками.
    2. Проверить фильтры: выбрать компанию → список сужается; выбрать Направление=Приход → только CREDIT; диапазон дат; поиск по тексту назначения.
    3. Проверить ВТБ мультивалюту: есть строки с currency=CNY (если в выписках был CNY-счёт).
    4. Изменить категорию любой операции через select → перезагрузить страницу → категория сохранилась.
    5. Идемпотентность: повторно загрузить тот же файл → toast «импортировано 0, пропущено дублей N».
    6. Проверить корректность сумм/дат/контрагентов на нескольких операциях против исходного Excel.
  </how-to-verify>
  <acceptance_criteria>
    - `prisma migrate deploy` применил миграцию без ошибок (deploy.sh лог)
    - /bank открывается, таблица непустая после импорта
    - Фильтры и поиск сужают список
    - Категоризация persist после refresh
    - Повторный импорт даёт imported=0
  </acceptance_criteria>
  <resume-signal>Напишите «approved» или опишите проблемы (неверный парсинг конкретного формата, дубли, ошибки сумм/дат).</resume-signal>
</task>

</tasks>

<verification>
- tsc чистый для page + 2 компонента + labels + скрипт
- Sticky-таблица по CLAUDE.md (opaque bg, border-separate, flex-1 min-h-0)
- Фильтры URL-driven; поиск insensitive
- Импорт 9 файлов выполнен, идемпотентность подтверждена UAT
</verification>

<success_criteria>
- /bank — рабочая read-only таблица всех операций группы компаний
- 6 фильтров + поиск
- Inline-категоризация (TxCategory)
- 9 файлов импортированы; повторный импорт no-op
- Миграция применена на проде
</success_criteria>

<output>
After completion, create `.planning/phases/22-bank-accounts/22-05-SUMMARY.md`.
Phase 22 этап 1 complete — обновить STATE.md (Phase 22 done) и ROADMAP Phase 22 чекбоксы планов.
</output>
