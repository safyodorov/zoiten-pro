---
phase: 22-bank-accounts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260610_phase22_bank/migration.sql
autonomous: true
requirements: [BANK-01, BANK-02]
must_haves:
  truths:
    - "Схема содержит модели Bank, BankAccount, Counterparty, BankTransaction, ImportBatch с верными FK"
    - "Company расширена полями inn/kpp/ogrn/shortName; Lender имеет nullable bankId"
    - "ERP_SECTION enum содержит значение BANK"
    - "Миграция применяется через prisma migrate deploy без ручных правок"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Prisma модели Bank/BankAccount/Counterparty/BankTransaction/ImportBatch + enums TxDirection/TxCategory + ERP_SECTION.BANK + Company/Lender расширения"
      contains: "model BankTransaction"
    - path: "prisma/migrations/20260610_phase22_bank/migration.sql"
      provides: "Raw SQL миграция (ALTER TYPE + ALTER TABLE + 5 CREATE TABLE + FK + indexes)"
      contains: "ALTER TYPE \"ERP_SECTION\" ADD VALUE IF NOT EXISTS 'BANK'"
  key_links:
    - from: "BankTransaction.fingerprint"
      to: "unique constraint"
      via: "@unique → CREATE UNIQUE INDEX"
      pattern: "fingerprint.*@unique"
    - from: "BankAccount.companyId"
      to: "Company.id"
      via: "FK ON DELETE RESTRICT"
      pattern: "BankAccount_companyId_fkey"
---

<objective>
Расширить Prisma-схему банковским доменом: реквизиты в Company, новые модели Bank / BankAccount / Counterparty / BankTransaction / ImportBatch, enums TxDirection / TxCategory, nullable FK Lender→Bank и новое значение ERP_SECTION.BANK. Создать соответствующую raw SQL миграцию по образцу Phase 21.

Purpose: Фундамент БД для импорта выписок (22-04) и просмотра операций (22-05). Без схемы ни один последующий план не выполним.
Output: Обновлённый `prisma/schema.prisma` + новый `prisma/migrations/20260610_phase22_bank/migration.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bank-accounts/22-CONTEXT.md
@.planning/phases/22-bank-accounts/22-RESEARCH.md

<interfaces>
<!-- Текущие модели, которые расширяются. Из schema.prisma. -->

Текущий enum ERP_SECTION (строки 21–34) — добавляется BANK последним значением:
```prisma
enum ERP_SECTION { PRODUCTS PRICES WEEKLY_CARDS STOCK COST PROCUREMENT SALES SUPPORT EMPLOYEES ADS USER_MANAGEMENT CREDITS }
```

Текущая Company (строки 499–505):
```prisma
model Company {
  id        String            @id @default(cuid())
  name      String            @unique
  employees EmployeeCompany[]
  loans     Loan[]
  createdAt DateTime          @default(now())
}
```

Текущий Lender (строки 1231–1238):
```prisma
model Lender {
  id        String   @id @default(cuid())
  name      String   @unique
  sortOrder Int      @default(0)
  loans     Loan[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить schema.prisma банковским доменом</name>
  <read_first>
    - prisma/schema.prisma (целевой файл — enum ERP_SECTION строки 21-34, Company строки 499-505, Lender строки 1231-1238)
    - .planning/phases/22-bank-accounts/22-CONTEXT.md §«Предлагаемая схема» (строки 64-162) — verbatim source of truth для полей
  </read_first>
  <action>
В `prisma/schema.prisma`:

**1.1** В enum `ERP_SECTION` добавить `BANK` последним значением (после `CREDITS`).

**1.2** Добавить два новых enum (рядом с другими enum в начале файла):
```prisma
enum TxDirection { DEBIT CREDIT }
enum TxCategory {
  UNCATEGORIZED
  INTERNAL_TRANSFER
  BANK_FEE
  SUPPLIER_PAYMENT
  INCOME
  TAX
  LOAN
  OTHER
}
```

**1.3** Расширить `model Company` — добавить поля (существующие name/employees/loans/createdAt не трогать):
```prisma
  inn       String?  @unique
  kpp       String?
  ogrn      String?
  shortName String?
  accounts  BankAccount[]
```

**1.4** Расширить `model Lender` — добавить nullable FK на Bank (существующие поля не трогать):
```prisma
  bankId String?
  bank   Bank?   @relation(fields: [bankId], references: [id], onDelete: SetNull, onUpdate: Cascade)
```

**1.5** Добавить новые модели:
```prisma
model Bank {
  id        String        @id @default(cuid())
  bic       String        @unique
  name      String
  accounts  BankAccount[]
  lenders   Lender[]
  createdAt DateTime      @default(now())
}

model BankAccount {
  id           String            @id @default(cuid())
  number       String            @unique
  companyId    String
  company      Company           @relation(fields: [companyId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  bankId       String
  bank         Bank              @relation(fields: [bankId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  currency     String            @default("RUR")
  transactions BankTransaction[]
  createdAt    DateTime          @default(now())

  @@index([companyId])
  @@index([bankId])
}

model Counterparty {
  id           String            @id @default(cuid())
  inn          String?           @unique
  name         String
  transactions BankTransaction[]
  createdAt    DateTime          @default(now())
}

model BankTransaction {
  id                  String        @id @default(cuid())
  accountId           String
  account             BankAccount   @relation(fields: [accountId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  date                DateTime      @db.Date
  direction           TxDirection
  amount              Decimal       @db.Decimal(18, 2)
  currency            String
  docNumber           String?
  operationType       String?
  purpose             String        @db.Text
  counterpartyId      String?
  counterparty        Counterparty? @relation(fields: [counterpartyId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  counterpartyName    String?
  counterpartyInn     String?
  counterpartyBic     String?
  counterpartyAccount String?
  category            TxCategory?   @default(UNCATEGORIZED)
  fingerprint         String        @unique
  importBatchId       String?
  importBatch         ImportBatch?  @relation(fields: [importBatchId], references: [id], onDelete: SetNull, onUpdate: Cascade)
  sourceBank          String
  createdAt           DateTime      @default(now())

  @@index([accountId, date])
  @@index([category])
  @@index([counterpartyId])
  @@index([importBatchId])
}

model ImportBatch {
  id           String            @id @default(cuid())
  fileName     String
  sourceBank   String
  rowsTotal    Int
  rowsImported Int
  rowsSkipped  Int
  importedById String?
  createdAt    DateTime          @default(now())
  transactions BankTransaction[]
}
```

Запустить `npx prisma generate` для регенерации клиента (как Phase 21 D-264 — после добавления моделей клиент нужно регенерировать).
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model BankTransaction" prisma/schema.prisma` → 1
    - `grep "fingerprint  *String  *@unique" prisma/schema.prisma` находит строку
    - `grep "model Bank " prisma/schema.prisma` и `grep "model BankAccount" prisma/schema.prisma` и `grep "model Counterparty" prisma/schema.prisma` и `grep "model ImportBatch" prisma/schema.prisma` — все находят
    - `grep "enum TxDirection" prisma/schema.prisma` и `grep "enum TxCategory" prisma/schema.prisma` — находят
    - enum ERP_SECTION содержит строку `BANK` (grep "  BANK" между `enum ERP_SECTION` и закрывающей `}`)
    - Company содержит `inn       String?  @unique` (grep "inn .*@unique")
    - Lender содержит `bankId String?`
    - `npx prisma validate` exits 0
  </acceptance_criteria>
  <done>schema.prisma валиден, содержит 5 новых моделей + 2 enum + ERP_SECTION.BANK + Company/Lender расширения; prisma client регенерирован.</done>
</task>

<task type="auto">
  <name>Task 2: Создать raw SQL миграцию 20260610_phase22_bank</name>
  <read_first>
    - prisma/migrations/20260609_phase21_credits/migration.sql (ТОЧНЫЙ шаблон синтаксиса — ALTER TYPE, CREATE TABLE с TEXT/DECIMAL/DATE/TIMESTAMP(3), CREATE INDEX, ALTER TABLE ADD CONSTRAINT FOREIGN KEY)
    - prisma/schema.prisma (только что отредактированный — источник истины для полей/типов)
  </read_first>
  <action>
Создать файл `prisma/migrations/20260610_phase22_bank/migration.sql`. НЕ запускать локально (нет локальной PostgreSQL) — применится через `prisma migrate deploy` на VPS в плане 22-05.

Содержимое (порядок важен: enum → ALTER существующих таблиц → CREATE новых → FK):

```sql
-- Phase 22: Банковские счета — ERP_SECTION.BANK + Company реквизиты + Lender.bankId
-- + Bank, BankAccount, Counterparty, BankTransaction, ImportBatch + enums TxDirection/TxCategory
-- Применяется через `prisma migrate deploy` на VPS (Plan 22-05).

-- 1. Новое значение enum ERP_SECTION
ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'BANK';

-- 2. Новые enum
CREATE TYPE "TxDirection" AS ENUM ('DEBIT', 'CREDIT');
CREATE TYPE "TxCategory" AS ENUM ('UNCATEGORIZED', 'INTERNAL_TRANSFER', 'BANK_FEE', 'SUPPLIER_PAYMENT', 'INCOME', 'TAX', 'LOAN', 'OTHER');

-- 3. Реквизиты в Company
ALTER TABLE "Company" ADD COLUMN "inn" TEXT;
ALTER TABLE "Company" ADD COLUMN "kpp" TEXT;
ALTER TABLE "Company" ADD COLUMN "ogrn" TEXT;
ALTER TABLE "Company" ADD COLUMN "shortName" TEXT;
CREATE UNIQUE INDEX "Company_inn_key" ON "Company"("inn");

-- 4. Bank (по БИК)
CREATE TABLE "Bank" (
  "id"        TEXT NOT NULL,
  "bic"       TEXT NOT NULL,
  "name"      TEXT NOT NULL,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Bank_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "Bank_bic_key" ON "Bank"("bic");

-- 5. Lender.bankId (nullable FK на будущее)
ALTER TABLE "Lender" ADD COLUMN "bankId" TEXT;
ALTER TABLE "Lender"
  ADD CONSTRAINT "Lender_bankId_fkey"
  FOREIGN KEY ("bankId") REFERENCES "Bank"("id")
  ON DELETE SET NULL ON UPDATE CASCADE;

-- 6. BankAccount
CREATE TABLE "BankAccount" (
  "id"        TEXT NOT NULL,
  "number"    TEXT NOT NULL,
  "companyId" TEXT NOT NULL,
  "bankId"    TEXT NOT NULL,
  "currency"  TEXT NOT NULL DEFAULT 'RUR',
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "BankAccount_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "BankAccount_number_key" ON "BankAccount"("number");
CREATE INDEX "BankAccount_companyId_idx" ON "BankAccount"("companyId");
CREATE INDEX "BankAccount_bankId_idx" ON "BankAccount"("bankId");
ALTER TABLE "BankAccount"
  ADD CONSTRAINT "BankAccount_companyId_fkey"
  FOREIGN KEY ("companyId") REFERENCES "Company"("id")
  ON DELETE RESTRICT ON UPDATE CASCADE;
ALTER TABLE "BankAccount"
  ADD CONSTRAINT "BankAccount_bankId_fkey"
  FOREIGN KEY ("bankId") REFERENCES "Bank"("id")
  ON DELETE RESTRICT ON UPDATE CASCADE;

-- 7. Counterparty (дедуп по ИНН)
CREATE TABLE "Counterparty" (
  "id"        TEXT NOT NULL,
  "inn"       TEXT,
  "name"      TEXT NOT NULL,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "Counterparty_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "Counterparty_inn_key" ON "Counterparty"("inn");

-- 8. ImportBatch
CREATE TABLE "ImportBatch" (
  "id"           TEXT NOT NULL,
  "fileName"     TEXT NOT NULL,
  "sourceBank"   TEXT NOT NULL,
  "rowsTotal"    INTEGER NOT NULL,
  "rowsImported" INTEGER NOT NULL,
  "rowsSkipped"  INTEGER NOT NULL,
  "importedById" TEXT,
  "createdAt"    TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ImportBatch_pkey" PRIMARY KEY ("id")
);

-- 9. BankTransaction (fingerprint @unique — дедуп; amount Decimal(18,2))
CREATE TABLE "BankTransaction" (
  "id"                  TEXT NOT NULL,
  "accountId"           TEXT NOT NULL,
  "date"                DATE NOT NULL,
  "direction"           "TxDirection" NOT NULL,
  "amount"              DECIMAL(18, 2) NOT NULL,
  "currency"            TEXT NOT NULL,
  "docNumber"           TEXT,
  "operationType"       TEXT,
  "purpose"             TEXT NOT NULL,
  "counterpartyId"      TEXT,
  "counterpartyName"    TEXT,
  "counterpartyInn"     TEXT,
  "counterpartyBic"     TEXT,
  "counterpartyAccount" TEXT,
  "category"            "TxCategory" DEFAULT 'UNCATEGORIZED',
  "fingerprint"         TEXT NOT NULL,
  "importBatchId"       TEXT,
  "sourceBank"          TEXT NOT NULL,
  "createdAt"           TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "BankTransaction_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "BankTransaction_fingerprint_key" ON "BankTransaction"("fingerprint");
CREATE INDEX "BankTransaction_accountId_date_idx" ON "BankTransaction"("accountId", "date");
CREATE INDEX "BankTransaction_category_idx" ON "BankTransaction"("category");
CREATE INDEX "BankTransaction_counterpartyId_idx" ON "BankTransaction"("counterpartyId");
CREATE INDEX "BankTransaction_importBatchId_idx" ON "BankTransaction"("importBatchId");
ALTER TABLE "BankTransaction"
  ADD CONSTRAINT "BankTransaction_accountId_fkey"
  FOREIGN KEY ("accountId") REFERENCES "BankAccount"("id")
  ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE "BankTransaction"
  ADD CONSTRAINT "BankTransaction_counterpartyId_fkey"
  FOREIGN KEY ("counterpartyId") REFERENCES "Counterparty"("id")
  ON DELETE SET NULL ON UPDATE CASCADE;
ALTER TABLE "BankTransaction"
  ADD CONSTRAINT "BankTransaction_importBatchId_fkey"
  FOREIGN KEY ("importBatchId") REFERENCES "ImportBatch"("id")
  ON DELETE SET NULL ON UPDATE CASCADE;
```

ВАЖНО: `ALTER TYPE ... ADD VALUE` в PostgreSQL не может выполняться в той же транзакции, что и использование нового значения — но т.к. BANK здесь нигде в SQL не используется (только в RBAC-данных позже), это безопасно в одном файле.
  </action>
  <verify>
    <automated>cd c:/Users/User/zoiten-pro && node -e "const s=require('fs').readFileSync('prisma/migrations/20260610_phase22_bank/migration.sql','utf8'); ['ALTER TYPE \"ERP_SECTION\" ADD VALUE IF NOT EXISTS ','CREATE TABLE \"BankTransaction\"','BankTransaction_fingerprint_key','DECIMAL(18, 2)','TxDirection','BankAccount_companyId_fkey'].forEach(t=>{if(!s.includes(t))throw new Error('MISSING: '+t)}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Файл `prisma/migrations/20260610_phase22_bank/migration.sql` существует
    - Содержит `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'BANK'`
    - Содержит `CREATE TYPE "TxDirection" AS ENUM ('DEBIT', 'CREDIT')`
    - Содержит `CREATE TABLE "BankTransaction"` + `CREATE UNIQUE INDEX "BankTransaction_fingerprint_key"`
    - Содержит `DECIMAL(18, 2)` для amount
    - Содержит 5 FK constraint: BankAccount_companyId_fkey, BankAccount_bankId_fkey, BankTransaction_accountId_fkey (ON DELETE CASCADE), BankTransaction_counterpartyId_fkey (ON DELETE SET NULL), Lender_bankId_fkey
    - Содержит `ALTER TABLE "Company" ADD COLUMN "inn"` + `CREATE UNIQUE INDEX "Company_inn_key"`
    - node verify-команда печатает OK
  </acceptance_criteria>
  <done>migration.sql создан, синтаксически соответствует образцу Phase 21, покрывает все 5 таблиц + enum + ALTER Company/Lender + FK + indexes. Применение отложено до VPS deploy (22-05).</done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- migration.sql содержит все DDL для 5 таблиц + 2 enum + ERP_SECTION.BANK + Company/Lender ALTER
- Все FK с правильными onDelete (RESTRICT для account/company/bank ссылок защищающих историю; CASCADE для transaction→account; SET NULL для counterparty/importBatch/lender→bank)
</verification>

<success_criteria>
- schema.prisma валиден и содержит банковский домен целиком
- migration.sql готов к `prisma migrate deploy`
- prisma client регенерирован (типы BankTransaction/Bank/etc доступны для 22-04/22-05)
</success_criteria>

<output>
After completion, create `.planning/phases/22-bank-accounts/22-01-SUMMARY.md`
</output>
