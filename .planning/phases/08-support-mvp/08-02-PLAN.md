---
phase: 08-support-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/api/support-sync/route.ts
  - app/api/cron/support-sync-reviews/route.ts
  - app/api/cron/support-media-cleanup/route.ts
  - lib/support-sync.ts
  - lib/support-media.ts
  - tests/support-sync.test.ts
  - tests/support-cron.test.ts
  - tests/support-media-download.test.ts
  - tests/support-media-cleanup.test.ts
autonomous: true
requirements:
  - SUP-05
  - SUP-06
  - SUP-07
  - SUP-08
  - SUP-09
partial_requirements:
  - SUP-07  # Phase 8: cron только для отзывов/вопросов (15 мин). Chat cron 5 мин → Phase 10. Appeals cron 1 час → Phase 11.
must_haves:
  truths:
    - "POST /api/support-sync загружает новые отзывы и вопросы из WB, идемпотентен по (channel, wbExternalId)"
    - "Медиа-файлы (фото из photoLinks[].fullSize и видео из video.link) скачиваются локально в /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}"
    - "GET /api/cron/support-sync-reviews защищён заголовком x-cron-secret и вызывает общую функцию syncSupport()"
    - "GET /api/cron/support-media-cleanup удаляет SupportMedia где expiresAt < now() + соответствующие файлы с диска"
    - "После синхронизации ключ AppSetting.support.lastSyncedAt обновляется текущей датой"
    - "Cron-эндпоинты для чата (5 мин) и обжалований (1 час) в Phase 8 НЕ создаются — они появятся в Phase 10 и Phase 11 соответственно. В Phase 8 только /api/cron/support-sync-reviews (15 мин)."
  artifacts:
    - path: "app/api/support-sync/route.ts"
      provides: "POST endpoint ручной синхронизации (сессия + SUPPORT,MANAGE)"
      exports: ["POST"]
    - path: "app/api/cron/support-sync-reviews/route.ts"
      provides: "Cron endpoint (15 мин, CRON_SECRET)"
      exports: ["GET"]
    - path: "app/api/cron/support-media-cleanup/route.ts"
      provides: "Cron endpoint cleanup (раз в сутки, CRON_SECRET)"
      exports: ["GET"]
    - path: "lib/support-sync.ts"
      provides: "syncSupport() — переиспользуется ручной кнопкой и cron"
      exports: ["syncSupport"]
    - path: "lib/support-media.ts"
      provides: "downloadMedia/downloadMediaBatch"
      exports: ["downloadMedia", "downloadMediaBatch"]
  key_links:
    - from: "app/api/support-sync/route.ts"
      to: "lib/support-sync.ts syncSupport()"
      via: "import"
      pattern: "from\\s+\"@/lib/support-sync\""
    - from: "lib/support-sync.ts"
      to: "lib/wb-support-api.ts"
      via: "import listFeedbacks/listQuestions"
      pattern: "from\\s+\"@/lib/wb-support-api\""
    - from: "lib/support-sync.ts"
      to: "prisma.supportTicket.upsert"
      via: "composite unique channel_wbExternalId"
      pattern: "channel_wbExternalId"
    - from: "app/api/cron/support-sync-reviews/route.ts"
      to: "process.env.CRON_SECRET"
      via: "x-cron-secret header check"
      pattern: "x-cron-secret"
---

<objective>
Phase 8 Plan 02 — синхронизация и cron:
1. `lib/support-sync.ts` — общая функция `syncSupport()`: получает feedbacks и questions через lib/wb-support-api.ts, делает транзакционный upsert в БД по `(channel, wbExternalId)`, создаёт SupportMessage INBOUND (и OUTBOUND, если ответ от продавца уже был в WB), скачивает медиа локально.
2. `lib/support-media.ts` — `downloadMedia()` и `downloadMediaBatch()` с concurrency=5, retry=1.
3. `POST /api/support-sync/route.ts` — ручной вызов с RBAC.
4. `GET /api/cron/support-sync-reviews/route.ts` — cron раз в 15 минут, защищён CRON_SECRET (паттерн purge-deleted).
5. `GET /api/cron/support-media-cleanup/route.ts` — cron раз в сутки, удаляет просроченные SupportMedia (expiresAt < now()) + файлы.
6. Integration-тесты: `support-sync.test.ts`, `support-cron.test.ts`, `support-media-download.test.ts`, `support-media-cleanup.test.ts` (mock fetch + mock fs + real Prisma in-memory или via $transaction.rollback паттерн).

Purpose: Автономная подкачка отзывов/вопросов без участия менеджера. Идемпотентность через composite unique ключ. Разделение ручного POST и cron GET — ручной требует sessionUser, cron — CRON_SECRET. Общая логика в lib/ чтобы не дублировать.

Output: 3 route-файла, 2 lib-файла, 4 наполненных тест-файла (заменяют Wave 0 stubs из Plan 08-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-support-mvp/08-CONTEXT.md
@.planning/phases/08-support-mvp/08-RESEARCH.md
@.planning/phases/08-support-mvp/08-01-SUMMARY.md
@CLAUDE.md
@app/api/cron/purge-deleted/route.ts
@app/api/wb-sync/route.ts
@lib/wb-support-api.ts
@lib/wb-api.ts
@lib/prisma.ts
@lib/rbac.ts

<interfaces>
Из lib/wb-support-api.ts (создан в Plan 08-01):
```typescript
export interface Feedback {
  id: string; text: string; productValuation: number; createdDate: string
  state: string; answer: FeedbackAnswer | null
  productDetails: ProductDetails; photoLinks: PhotoLink[]; video: FeedbackVideo | null
}
export interface Question {
  id: string; text: string; createdDate: string; state: string
  answer: FeedbackAnswer | null; productDetails: ProductDetails
}
export async function listFeedbacks(p: ListParams): Promise<Feedback[]>
export async function listQuestions(p: ListParams): Promise<Question[]>
```

Prisma модели (созданы в Plan 08-01):
```prisma
model SupportTicket {
  channel TicketChannel; wbExternalId String?; nmId Int?
  rating Int?; status TicketStatus @default(NEW)
  lastMessageAt DateTime?; previewText String?
  @@unique([channel, wbExternalId])
}
model SupportMessage { direction Direction; text String?; wbSentAt DateTime? }
model SupportMedia { type MediaType; wbUrl String; localPath String?; expiresAt DateTime }
```

Паттерн cron (из app/api/cron/purge-deleted/route.ts):
```typescript
const secret = req.headers.get("x-cron-secret")
if (!secret || secret !== process.env.CRON_SECRET) return NextResponse.json({error:"Не авторизован"}, {status:401})
```

Паттерн sync (из app/api/wb-sync/route.ts): POST handler + session check + вызов lib-функции + возврат структурированного ответа `{ok, synced, errors}`.

Пути медиа: `/var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}` — `UPLOAD_DIR` env в проде, fallback `/tmp/zoiten-uploads` в dev.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: lib/support-media.ts — скачивание медиа с concurrency + тест</name>
  <files>lib/support-media.ts, tests/support-media-download.test.ts</files>
  <read_first>
    - .planning/phases/08-support-mvp/08-RESEARCH.md (раздел «Скачивание медиа» строки 535-566 — эталонный код)
    - CLAUDE.md (раздел «Architecture» — UPLOAD_DIR паттерн)
    - app/api/upload/route.ts (существующий паттерн загрузки файлов — если есть)
    - tests/support-media-download.test.ts (Wave 0 stub из Plan 08-01 — заменить содержимое)
  </read_first>
  <behavior>
    - downloadMedia(wbUrl, ticketId, messageId) → скачивает файл по URL, пишет в `${UPLOAD_DIR}/support/{ticketId}/{messageId}/{sanitized_filename}`, возвращает `{localPath, sizeBytes}`
    - Filename извлекается из `path.basename(new URL(wbUrl).pathname)` — санитизация
    - При fetch error throw Error с русским сообщением
    - downloadMediaBatch(items[]) → параллельно 5 одновременно, каждый fail не ломает другие, возвращает массив `{localPath?, error?, ...item}`
    - Retry=1 при первом network error (catch → wait 1сек → ещё попытка)
  </behavior>
  <action>
    **Создать `lib/support-media.ts`:**

    ```typescript
    // lib/support-media.ts
    // Скачивание медиа из WB в /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}

    import { promises as fs } from "node:fs"
    import path from "node:path"

    const DEFAULT_UPLOAD_DIR = process.env.UPLOAD_DIR || "/var/www/zoiten-uploads"

    export interface DownloadItem {
      wbUrl: string
      ticketId: string
      messageId: string
    }

    export interface DownloadResult extends DownloadItem {
      localPath?: string
      sizeBytes?: number
      error?: string
    }

    export async function downloadMedia(item: DownloadItem, attempt = 0): Promise<DownloadResult> {
      const dir = path.join(DEFAULT_UPLOAD_DIR, "support", item.ticketId, item.messageId)
      try {
        await fs.mkdir(dir, { recursive: true })
        const rawName = path.basename(new URL(item.wbUrl).pathname)
        // sanitize: только a-zA-Z0-9.-_
        const filename = rawName.replace(/[^a-zA-Z0-9.\-_]/g, "_").slice(-128) || `file_${Date.now()}`
        const localPath = path.join(dir, filename)

        const res = await fetch(item.wbUrl)
        if (!res.ok) throw new Error(`HTTP ${res.status}`)
        const buf = Buffer.from(await res.arrayBuffer())
        await fs.writeFile(localPath, buf)
        return { ...item, localPath, sizeBytes: buf.length }
      } catch (err) {
        if (attempt === 0) {
          await new Promise((r) => setTimeout(r, 1000))
          return downloadMedia(item, 1)
        }
        return { ...item, error: err instanceof Error ? err.message : "Ошибка скачивания медиа" }
      }
    }

    export async function downloadMediaBatch(items: DownloadItem[], concurrency = 5): Promise<DownloadResult[]> {
      const results: DownloadResult[] = []
      for (let i = 0; i < items.length; i += concurrency) {
        const batch = items.slice(i, i + concurrency)
        results.push(...await Promise.all(batch.map((it) => downloadMedia(it))))
      }
      return results
    }
    ```

    **Заменить содержимое `tests/support-media-download.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    vi.mock("node:fs", () => ({
      promises: {
        mkdir: vi.fn().mockResolvedValue(undefined),
        writeFile: vi.fn().mockResolvedValue(undefined),
      },
    }))

    beforeEach(() => {
      vi.stubGlobal("fetch", vi.fn())
      process.env.UPLOAD_DIR = "/tmp/test-uploads"
    })

    afterEach(() => {
      vi.unstubAllGlobals()
      vi.clearAllMocks()
    })

    describe("downloadMedia", () => {
      it("пишет файл в {UPLOAD_DIR}/support/{ticketId}/{messageId}/{sanitized}", async () => {
        const { downloadMedia } = await import("@/lib/support-media")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
          new Response(new Uint8Array([1, 2, 3]), { status: 200 })
        )
        const res = await downloadMedia({
          wbUrl: "https://feedback04.wbbasket.ru/abc/def/fs.webp",
          ticketId: "T1",
          messageId: "M1",
        })
        expect(res.localPath).toContain("/support/T1/M1/")
        expect(res.localPath).toMatch(/fs\.webp$/)
        expect(res.sizeBytes).toBe(3)
        expect(res.error).toBeUndefined()
      })

      it("ретраит 1 раз при fetch error, затем возвращает error", async () => {
        const { downloadMedia } = await import("@/lib/support-media")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock.mockRejectedValueOnce(new Error("network fail"))
        fetchMock.mockRejectedValueOnce(new Error("network fail again"))
        const res = await downloadMedia({ wbUrl: "https://x/y.jpg", ticketId: "T", messageId: "M" })
        expect(res.error).toBeDefined()
        expect(fetchMock).toHaveBeenCalledTimes(2)
      }, 10000)

      it("успешно завершается на 2-й попытке после первого fail", async () => {
        const { downloadMedia } = await import("@/lib/support-media")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock.mockRejectedValueOnce(new Error("first"))
        fetchMock.mockResolvedValueOnce(new Response(new Uint8Array([9]), { status: 200 }))
        const res = await downloadMedia({ wbUrl: "https://x/ok.jpg", ticketId: "T", messageId: "M" })
        expect(res.error).toBeUndefined()
        expect(res.sizeBytes).toBe(1)
      }, 10000)
    })

    describe("downloadMediaBatch", () => {
      it("обрабатывает пакеты concurrency=5, не ломается на одном failing", async () => {
        const { downloadMediaBatch } = await import("@/lib/support-media")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        // 3 успех, 1 fail (fail делает 2 попытки, итого 5 fetch)
        fetchMock
          .mockResolvedValueOnce(new Response(new Uint8Array([1]), { status: 200 }))
          .mockResolvedValueOnce(new Response(new Uint8Array([2]), { status: 200 }))
          .mockResolvedValueOnce(new Response(new Uint8Array([3]), { status: 200 }))
          .mockRejectedValueOnce(new Error("fail"))
          .mockRejectedValueOnce(new Error("fail"))
        const items = [
          { wbUrl: "https://x/1.jpg", ticketId: "T", messageId: "M1" },
          { wbUrl: "https://x/2.jpg", ticketId: "T", messageId: "M2" },
          { wbUrl: "https://x/3.jpg", ticketId: "T", messageId: "M3" },
          { wbUrl: "https://x/4.jpg", ticketId: "T", messageId: "M4" },
        ]
        const res = await downloadMediaBatch(items, 5)
        expect(res).toHaveLength(4)
        expect(res.filter((r) => r.localPath).length).toBe(3)
        expect(res.filter((r) => r.error).length).toBe(1)
      }, 10000)
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-media-download.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/support-media.ts` (exit 0)
    - `grep "export async function downloadMedia" lib/support-media.ts` находит строку
    - `grep "export async function downloadMediaBatch" lib/support-media.ts` находит строку
    - `grep "/support/" lib/support-media.ts` находит путь
    - `npm run test -- tests/support-media-download.test.ts` exit 0 (все 4 теста GREEN)
  </acceptance_criteria>
  <done>
    lib/support-media.ts экспортирует downloadMedia + downloadMediaBatch с retry и concurrency; тест-файл GREEN 4/4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/support-sync.ts + POST /api/support-sync + тест интеграции</name>
  <files>lib/support-sync.ts, app/api/support-sync/route.ts, tests/support-sync.test.ts</files>
  <read_first>
    - .planning/phases/08-support-mvp/08-RESEARCH.md (раздел «Архитектура синхронизации» строки 419-530 — эталонный код, idempotency таблица)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «specifics» — анонимизация покупателя, customerId всегда null в Phase 8)
    - lib/wb-support-api.ts (созданный в Plan 08-01)
    - lib/support-media.ts (созданный в задаче 1)
    - app/api/wb-sync/route.ts (паттерн route с sessionCheck + вызов lib-функции)
    - lib/rbac.ts (requireSection сигнатура)
    - tests/support-sync.test.ts (Wave 0 stub — заменить)
  </read_first>
  <behavior>
    - syncSupport() получает опции { isAnswered?: boolean; take?: number } (defaults: isAnswered=undefined — всё, take=feedbacks:5000/questions:10000)
    - Загружает feedbacks + questions через lib/wb-support-api.ts
    - Per-item: `prisma.$transaction` делает upsert SupportTicket по (channel, wbExternalId); create SupportMessage INBOUND если нет; create OUTBOUND если WB answer.text есть и нет локального OUTBOUND; upsert SupportMedia по wbUrl
    - После транзакций — downloadMediaBatch для всех SupportMedia где localPath=null
    - Обновляет SupportMedia.localPath и sizeBytes после успешной загрузки
    - Обновляет AppSetting ключ "support.lastSyncedAt" = new Date().toISOString()
    - Возвращает { feedbacksSynced: number; questionsSynced: number; mediaSaved: number; errors: string[] }
    - customerId **всегда null** в Phase 8 (WB Feedbacks/Questions не даёт wbUserId — см. 08-CONTEXT.md)
    - `rating` из Feedback → `productValuation`, nmId из productDetails.nmId, previewText = first 140 char text
    - POST /api/support-sync: RBAC requireSection("SUPPORT", "MANAGE") → syncSupport() → return JSON
  </behavior>
  <action>
    **Шаг A — создать `lib/support-sync.ts`:**

    ```typescript
    // lib/support-sync.ts
    // Синхронизация WB Feedbacks + Questions → SupportTicket/Message/Media.
    // Переиспользуется из POST /api/support-sync (ручной) и GET /api/cron/support-sync-reviews.

    import { prisma } from "@/lib/prisma"
    import { listFeedbacks, listQuestions, type Feedback, type Question } from "@/lib/wb-support-api"
    import { downloadMediaBatch, type DownloadItem } from "@/lib/support-media"

    const YEAR_MS = 365 * 24 * 60 * 60 * 1000

    export interface SyncResult {
      feedbacksSynced: number
      questionsSynced: number
      mediaSaved: number
      errors: string[]
    }

    export async function syncSupport(opts: { isAnswered?: boolean } = {}): Promise<SyncResult> {
      const errors: string[] = []
      let feedbacksSynced = 0
      let questionsSynced = 0

      // 1. Feedbacks — пагинация по skip, take=5000
      const feedbacks: Feedback[] = []
      for (let skip = 0; ; skip += 5000) {
        try {
          const batch = await listFeedbacks({ isAnswered: opts.isAnswered, take: 5000, skip })
          feedbacks.push(...batch)
          if (batch.length < 5000) break
        } catch (err) {
          errors.push(`Feedbacks skip=${skip}: ${err instanceof Error ? err.message : "unknown"}`)
          break
        }
      }

      // 2. Questions — пагинация по skip, take=10000
      const questions: Question[] = []
      for (let skip = 0; ; skip += 10000) {
        try {
          const batch = await listQuestions({ isAnswered: opts.isAnswered, take: 10000, skip })
          questions.push(...batch)
          if (batch.length < 10000) break
        } catch (err) {
          errors.push(`Questions skip=${skip}: ${err instanceof Error ? err.message : "unknown"}`)
          break
        }
      }

      const mediaToDownload: DownloadItem[] = []
      const mediaToUpdate: Array<{ wbUrl: string; messageId: string; ticketId: string }> = []

      // 3. Feedbacks → transaction per-item
      for (const fb of feedbacks) {
        try {
          const previewText = (fb.text ?? "").slice(0, 140)
          const wbCreatedAt = fb.createdDate ? new Date(fb.createdDate) : null

          await prisma.$transaction(async (tx) => {
            const ticket = await tx.supportTicket.upsert({
              where: { channel_wbExternalId: { channel: "FEEDBACK", wbExternalId: fb.id } },
              create: {
                channel: "FEEDBACK",
                wbExternalId: fb.id,
                customerId: null,
                nmId: fb.productDetails?.nmId ?? null,
                rating: fb.productValuation ?? null,
                previewText,
                lastMessageAt: wbCreatedAt,
                status: fb.answer?.text ? "ANSWERED" : "NEW",
              },
              update: {
                nmId: fb.productDetails?.nmId ?? null,
                rating: fb.productValuation ?? null,
                previewText,
              },
            })

            // INBOUND (оригинальный отзыв) — одна штука на тикет
            const inbound = await tx.supportMessage.findFirst({
              where: { ticketId: ticket.id, direction: "INBOUND" },
            })
            let inboundId: string
            if (!inbound) {
              const msg = await tx.supportMessage.create({
                data: {
                  ticketId: ticket.id,
                  direction: "INBOUND",
                  text: fb.text,
                  authorId: null,
                  wbSentAt: wbCreatedAt,
                },
              })
              inboundId = msg.id
              // Фото
              for (const photo of fb.photoLinks ?? []) {
                const expiresAt = new Date(Date.now() + YEAR_MS)
                const media = await tx.supportMedia.create({
                  data: { messageId: msg.id, type: "IMAGE", wbUrl: photo.fullSize, expiresAt },
                })
                mediaToDownload.push({ wbUrl: photo.fullSize, ticketId: ticket.id, messageId: msg.id })
                mediaToUpdate.push({ wbUrl: photo.fullSize, messageId: msg.id, ticketId: ticket.id })
              }
              // Видео
              if (fb.video?.link) {
                const expiresAt = new Date(Date.now() + YEAR_MS)
                await tx.supportMedia.create({
                  data: { messageId: msg.id, type: "VIDEO", wbUrl: fb.video.link, expiresAt },
                })
                mediaToDownload.push({ wbUrl: fb.video.link, ticketId: ticket.id, messageId: msg.id })
                mediaToUpdate.push({ wbUrl: fb.video.link, messageId: msg.id, ticketId: ticket.id })
              }
            } else {
              inboundId = inbound.id
            }

            // OUTBOUND (исторический ответ от продавца из WB)
            if (fb.answer?.text) {
              const outbound = await tx.supportMessage.findFirst({
                where: { ticketId: ticket.id, direction: "OUTBOUND" },
              })
              if (!outbound) {
                await tx.supportMessage.create({
                  data: {
                    ticketId: ticket.id,
                    direction: "OUTBOUND",
                    text: fb.answer.text,
                    authorId: null, // unknown — ответ мог быть из кабинета WB ранее
                    wbSentAt: fb.answer.createDate ? new Date(fb.answer.createDate) : null,
                  },
                })
              }
            }
          })
          feedbacksSynced++
        } catch (err) {
          errors.push(`Feedback ${fb.id}: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      // 4. Questions — аналогично (без медиа — WB Questions API не даёт photoLinks)
      for (const q of questions) {
        try {
          const previewText = (q.text ?? "").slice(0, 140)
          const wbCreatedAt = q.createdDate ? new Date(q.createdDate) : null
          await prisma.$transaction(async (tx) => {
            const ticket = await tx.supportTicket.upsert({
              where: { channel_wbExternalId: { channel: "QUESTION", wbExternalId: q.id } },
              create: {
                channel: "QUESTION",
                wbExternalId: q.id,
                customerId: null,
                nmId: q.productDetails?.nmId ?? null,
                previewText,
                lastMessageAt: wbCreatedAt,
                status: q.answer?.text ? "ANSWERED" : "NEW",
              },
              update: {
                nmId: q.productDetails?.nmId ?? null,
                previewText,
              },
            })
            const inbound = await tx.supportMessage.findFirst({ where: { ticketId: ticket.id, direction: "INBOUND" } })
            if (!inbound) {
              await tx.supportMessage.create({
                data: { ticketId: ticket.id, direction: "INBOUND", text: q.text, authorId: null, wbSentAt: wbCreatedAt },
              })
            }
            if (q.answer?.text) {
              const outbound = await tx.supportMessage.findFirst({ where: { ticketId: ticket.id, direction: "OUTBOUND" } })
              if (!outbound) {
                await tx.supportMessage.create({
                  data: {
                    ticketId: ticket.id, direction: "OUTBOUND", text: q.answer.text, authorId: null,
                    wbSentAt: q.answer.createDate ? new Date(q.answer.createDate) : null,
                  },
                })
              }
            }
          })
          questionsSynced++
        } catch (err) {
          errors.push(`Question ${q.id}: ${err instanceof Error ? err.message : "unknown"}`)
        }
      }

      // 5. Скачать медиа параллельно + обновить localPath в БД
      let mediaSaved = 0
      if (mediaToDownload.length > 0) {
        const downloadResults = await downloadMediaBatch(mediaToDownload, 5)
        for (const r of downloadResults) {
          if (r.localPath) {
            try {
              await prisma.supportMedia.updateMany({
                where: { wbUrl: r.wbUrl, messageId: r.messageId },
                data: { localPath: r.localPath, sizeBytes: r.sizeBytes },
              })
              mediaSaved++
            } catch (err) {
              errors.push(`Media update ${r.wbUrl}: ${err instanceof Error ? err.message : "unknown"}`)
            }
          } else if (r.error) {
            errors.push(`Media download ${r.wbUrl}: ${r.error}`)
          }
        }
      }

      // 6. AppSetting lastSyncedAt
      try {
        await prisma.appSetting.upsert({
          where: { key: "support.lastSyncedAt" },
          create: { key: "support.lastSyncedAt", value: new Date().toISOString() },
          update: { value: new Date().toISOString() },
        })
      } catch (err) {
        errors.push(`lastSyncedAt: ${err instanceof Error ? err.message : "unknown"}`)
      }

      return { feedbacksSynced, questionsSynced, mediaSaved, errors }
    }
    ```

    **Шаг B — создать `app/api/support-sync/route.ts`:**

    ```typescript
    // app/api/support-sync/route.ts
    // POST — ручная синхронизация, требует SUPPORT+MANAGE
    import { NextResponse } from "next/server"
    import { requireSection } from "@/lib/rbac"
    import { syncSupport } from "@/lib/support-sync"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function POST(): Promise<NextResponse> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const result = await syncSupport({ isAnswered: false })
        return NextResponse.json({ ok: true, ...result })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка синхронизации"
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    **Шаг C — заменить содержимое `tests/support-sync.test.ts`:**

    Integration-тест с моком lib/wb-support-api и lib/support-media (без real Prisma — `vi.mock("@/lib/prisma")` с in-memory data structure):

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    // Mock Prisma
    const prismaState = {
      tickets: [] as any[],
      messages: [] as any[],
      media: [] as any[],
      settings: [] as any[],
    }

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        $transaction: async (cb: any) => cb({
          supportTicket: {
            upsert: async ({ where, create, update }: any) => {
              const key = `${where.channel_wbExternalId.channel}_${where.channel_wbExternalId.wbExternalId}`
              const existing = prismaState.tickets.find((t) => `${t.channel}_${t.wbExternalId}` === key)
              if (existing) { Object.assign(existing, update); return existing }
              const t = { id: `T${prismaState.tickets.length + 1}`, ...create }
              prismaState.tickets.push(t)
              return t
            },
          },
          supportMessage: {
            findFirst: async ({ where }: any) => prismaState.messages.find((m) => m.ticketId === where.ticketId && m.direction === where.direction) ?? null,
            create: async ({ data }: any) => {
              const m = { id: `M${prismaState.messages.length + 1}`, ...data }
              prismaState.messages.push(m)
              return m
            },
          },
          supportMedia: {
            create: async ({ data }: any) => { prismaState.media.push({ id: `MD${prismaState.media.length+1}`, ...data }); return prismaState.media.at(-1) },
          },
        }),
        supportMedia: { updateMany: async () => ({ count: 0 }) },
        appSetting: { upsert: async () => ({}) },
      },
    }))

    // Mock WB API
    vi.mock("@/lib/wb-support-api", () => ({
      listFeedbacks: vi.fn(),
      listQuestions: vi.fn(),
    }))

    // Mock media
    vi.mock("@/lib/support-media", () => ({
      downloadMediaBatch: vi.fn().mockResolvedValue([]),
    }))

    beforeEach(() => {
      prismaState.tickets = []
      prismaState.messages = []
      prismaState.media = []
      prismaState.settings = []
    })

    afterEach(() => vi.clearAllMocks())

    describe("syncSupport", () => {
      it("делает upsert SupportTicket FEEDBACK с корректными полями", async () => {
        const { listFeedbacks, listQuestions } = await import("@/lib/wb-support-api") as any
        listFeedbacks.mockResolvedValueOnce([{
          id: "YX52", text: "Супер", productValuation: 5, createdDate: "2026-01-01T10:00:00Z",
          state: "wbRu", answer: null,
          productDetails: { imtId: 1, nmId: 123, productName: "X", supplierArticle: "", brandName: "" },
          photoLinks: [], video: null,
        }]).mockResolvedValueOnce([])
        listQuestions.mockResolvedValueOnce([])

        const { syncSupport } = await import("@/lib/support-sync")
        const res = await syncSupport({ isAnswered: false })

        expect(res.feedbacksSynced).toBe(1)
        expect(prismaState.tickets).toHaveLength(1)
        expect(prismaState.tickets[0].channel).toBe("FEEDBACK")
        expect(prismaState.tickets[0].wbExternalId).toBe("YX52")
        expect(prismaState.tickets[0].nmId).toBe(123)
        expect(prismaState.tickets[0].rating).toBe(5)
        expect(prismaState.tickets[0].customerId).toBeNull()
      })

      it("идемпотентен: повторный sync того же feedback не создаёт дубль", async () => {
        const { listFeedbacks, listQuestions } = await import("@/lib/wb-support-api") as any
        const fb = { id: "A1", text: "t", productValuation: 4, createdDate: "", state: "wbRu", answer: null,
          productDetails: { imtId: 1, nmId: 1, productName: "", supplierArticle: "", brandName: "" },
          photoLinks: [], video: null }
        listFeedbacks.mockResolvedValue([fb])
        listQuestions.mockResolvedValue([])

        const { syncSupport } = await import("@/lib/support-sync")
        await syncSupport()
        await syncSupport()
        expect(prismaState.tickets).toHaveLength(1)
      })

      it("создаёт OUTBOUND SupportMessage если WB answer.text есть", async () => {
        const { listFeedbacks, listQuestions } = await import("@/lib/wb-support-api") as any
        listFeedbacks.mockResolvedValueOnce([{
          id: "B1", text: "txt", productValuation: 5, createdDate: "",
          state: "wbRu",
          answer: { text: "Ответ", state: "wbRu", editable: true, createDate: "2026-01-02T00:00:00Z" },
          productDetails: { imtId: 1, nmId: 2, productName: "", supplierArticle: "", brandName: "" },
          photoLinks: [], video: null,
        }]).mockResolvedValueOnce([])
        listQuestions.mockResolvedValueOnce([])

        const { syncSupport } = await import("@/lib/support-sync")
        await syncSupport()
        expect(prismaState.messages.filter((m) => m.direction === "OUTBOUND")).toHaveLength(1)
        expect(prismaState.messages.find((m) => m.direction === "OUTBOUND")?.text).toBe("Ответ")
      })

      it("обрабатывает QUESTION: создаёт ticket QUESTION и INBOUND message", async () => {
        const { listFeedbacks, listQuestions } = await import("@/lib/wb-support-api") as any
        listFeedbacks.mockResolvedValueOnce([])
        listQuestions.mockResolvedValueOnce([{
          id: "Q1", text: "Вопрос?", createdDate: "2026-01-01T00:00:00Z", state: "wbRu", answer: null,
          productDetails: { imtId: 1, nmId: 99, productName: "", supplierArticle: "", brandName: "" },
        }]).mockResolvedValueOnce([])

        const { syncSupport } = await import("@/lib/support-sync")
        const res = await syncSupport()
        expect(res.questionsSynced).toBe(1)
        expect(prismaState.tickets[0].channel).toBe("QUESTION")
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/support-sync.ts` (exit 0)
    - `test -f app/api/support-sync/route.ts` (exit 0)
    - `grep "export async function syncSupport" lib/support-sync.ts` находит строку
    - `grep "channel_wbExternalId" lib/support-sync.ts` находит композитный ключ
    - `grep "customerId: null" lib/support-sync.ts` находит (Phase 8 всегда null)
    - `grep "requireSection.*SUPPORT.*MANAGE" app/api/support-sync/route.ts` находит RBAC
    - `npm run test -- tests/support-sync.test.ts` exit 0 (4 теста GREEN)
  </acceptance_criteria>
  <done>
    syncSupport() работает идемпотентно (проверено тестом повторного вызова), создаёт INBOUND/OUTBOUND SupportMessage, не создаёт Customer (customerId=null), POST /api/support-sync защищён RBAC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: Cron endpoints + cleanup + тесты</name>
  <files>
    app/api/cron/support-sync-reviews/route.ts,
    app/api/cron/support-media-cleanup/route.ts,
    tests/support-cron.test.ts,
    tests/support-media-cleanup.test.ts
  </files>
  <read_first>
    - app/api/cron/purge-deleted/route.ts (эталонный паттерн CRON_SECRET)
    - .planning/phases/08-support-mvp/08-RESEARCH.md (разделы «Cron GET /api/cron/support-sync-reviews» строки 482-520)
    - lib/support-sync.ts (созданный в задаче 2)
    - tests/support-cron.test.ts, tests/support-media-cleanup.test.ts (Wave 0 stubs — заменить)
  </read_first>
  <behavior>
    - GET /api/cron/support-sync-reviews: проверяет `x-cron-secret` header == process.env.CRON_SECRET; если нет → 401 `{error:"Не авторизован"}`; если OK → вызывает syncSupport({isAnswered:false}) и возвращает JSON результат
    - GET /api/cron/support-media-cleanup: аналогичная проверка CRON_SECRET; находит SupportMedia где expiresAt < now(); для каждого делает fs.unlink(localPath) с catch ENOENT; затем prisma.supportMedia.deleteMany
  </behavior>
  <action>
    **Шаг A — создать `app/api/cron/support-sync-reviews/route.ts`:**

    ```typescript
    // app/api/cron/support-sync-reviews/route.ts
    // GET — cron синхронизация (15 мин). Защищён x-cron-secret.
    import { NextRequest, NextResponse } from "next/server"
    import { syncSupport } from "@/lib/support-sync"

    export const runtime = "nodejs"
    export const maxDuration = 300

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }
      try {
        const result = await syncSupport({ isAnswered: false })
        return NextResponse.json({ ok: true, ...result })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка синхронизации"
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    **Шаг B — создать `app/api/cron/support-media-cleanup/route.ts`:**

    ```typescript
    // app/api/cron/support-media-cleanup/route.ts
    // GET — удаление просроченных медиа (раз в сутки). Защищён x-cron-secret.
    import { NextRequest, NextResponse } from "next/server"
    import { promises as fs } from "node:fs"
    import { prisma } from "@/lib/prisma"

    export const runtime = "nodejs"

    export async function GET(req: NextRequest): Promise<NextResponse> {
      const secret = req.headers.get("x-cron-secret")
      if (!secret || secret !== process.env.CRON_SECRET) {
        return NextResponse.json({ error: "Не авторизован" }, { status: 401 })
      }
      try {
        const now = new Date()
        const expired = await prisma.supportMedia.findMany({
          where: { expiresAt: { lt: now } },
          select: { id: true, localPath: true },
        })
        let filesDeleted = 0
        for (const m of expired) {
          if (m.localPath) {
            try {
              await fs.unlink(m.localPath)
              filesDeleted++
            } catch (err: any) {
              // игнорируем ENOENT
              if (err?.code !== "ENOENT") {
                console.error("support-media-cleanup unlink error:", err)
              }
            }
          }
        }
        const result = await prisma.supportMedia.deleteMany({
          where: { expiresAt: { lt: now } },
        })
        return NextResponse.json({ ok: true, rowsDeleted: result.count, filesDeleted })
      } catch (err) {
        const msg = err instanceof Error ? err.message : "Ошибка очистки"
        return NextResponse.json({ ok: false, error: msg }, { status: 500 })
      }
    }
    ```

    **Шаг C — заменить `tests/support-cron.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    vi.mock("@/lib/support-sync", () => ({
      syncSupport: vi.fn().mockResolvedValue({ feedbacksSynced: 0, questionsSynced: 0, mediaSaved: 0, errors: [] }),
    }))

    beforeEach(() => {
      process.env.CRON_SECRET = "test-secret"
    })
    afterEach(() => vi.clearAllMocks())

    function mockReq(headers: Record<string, string> = {}) {
      return { headers: { get: (k: string) => headers[k.toLowerCase()] ?? null } } as any
    }

    describe("support-sync-reviews cron", () => {
      it("возвращает 401 без x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-sync-reviews/route")
        const res = await GET(mockReq({}))
        expect(res.status).toBe(401)
      })
      it("возвращает 401 при неверном x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-sync-reviews/route")
        const res = await GET(mockReq({ "x-cron-secret": "wrong" }))
        expect(res.status).toBe(401)
      })
      it("возвращает 200 и вызывает syncSupport при валидном x-cron-secret", async () => {
        const { GET } = await import("@/app/api/cron/support-sync-reviews/route")
        const { syncSupport } = await import("@/lib/support-sync")
        const res = await GET(mockReq({ "x-cron-secret": "test-secret" }))
        expect(res.status).toBe(200)
        expect(syncSupport).toHaveBeenCalledWith({ isAnswered: false })
      })
    })
    ```

    **Шаг D — заменить `tests/support-media-cleanup.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    const unlinkMock = vi.fn()
    vi.mock("node:fs", () => ({
      promises: { unlink: unlinkMock },
    }))

    const findManyMock = vi.fn()
    const deleteManyMock = vi.fn()
    vi.mock("@/lib/prisma", () => ({
      prisma: {
        supportMedia: {
          findMany: findManyMock,
          deleteMany: deleteManyMock,
        },
      },
    }))

    beforeEach(() => {
      process.env.CRON_SECRET = "test-secret"
      unlinkMock.mockReset()
      findManyMock.mockReset()
      deleteManyMock.mockReset()
    })

    function mockReq(headers: Record<string, string> = {}) {
      return { headers: { get: (k: string) => headers[k.toLowerCase()] ?? null } } as any
    }

    describe("support-media-cleanup cron", () => {
      it("возвращает 401 без секрета", async () => {
        const { GET } = await import("@/app/api/cron/support-media-cleanup/route")
        const res = await GET(mockReq({}))
        expect(res.status).toBe(401)
      })
      it("удаляет файлы и записи SupportMedia где expiresAt < now()", async () => {
        findManyMock.mockResolvedValueOnce([
          { id: "M1", localPath: "/tmp/a.jpg" },
          { id: "M2", localPath: "/tmp/b.jpg" },
        ])
        unlinkMock.mockResolvedValue(undefined)
        deleteManyMock.mockResolvedValueOnce({ count: 2 })

        const { GET } = await import("@/app/api/cron/support-media-cleanup/route")
        const res = await GET(mockReq({ "x-cron-secret": "test-secret" }))
        const body = await res.json()
        expect(res.status).toBe(200)
        expect(body.rowsDeleted).toBe(2)
        expect(body.filesDeleted).toBe(2)
        expect(unlinkMock).toHaveBeenCalledTimes(2)
      })
      it("игнорирует ENOENT при fs.unlink", async () => {
        findManyMock.mockResolvedValueOnce([{ id: "M1", localPath: "/tmp/missing.jpg" }])
        const enoent = Object.assign(new Error("not found"), { code: "ENOENT" })
        unlinkMock.mockRejectedValueOnce(enoent)
        deleteManyMock.mockResolvedValueOnce({ count: 1 })

        const { GET } = await import("@/app/api/cron/support-media-cleanup/route")
        const res = await GET(mockReq({ "x-cron-secret": "test-secret" }))
        expect(res.status).toBe(200)
        const body = await res.json()
        expect(body.rowsDeleted).toBe(1)
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-cron.test.ts tests/support-media-cleanup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/api/cron/support-sync-reviews/route.ts` (exit 0)
    - `test -f app/api/cron/support-media-cleanup/route.ts` (exit 0)
    - `grep "x-cron-secret" app/api/cron/support-sync-reviews/route.ts` находит проверку
    - `grep "x-cron-secret" app/api/cron/support-media-cleanup/route.ts` находит проверку
    - `grep "expiresAt" app/api/cron/support-media-cleanup/route.ts` находит фильтр
    - `npm run test -- tests/support-cron.test.ts tests/support-media-cleanup.test.ts` exit 0 (6 тестов GREEN)
    - `npm run test` full suite — 0 failed
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Два cron-endpoint-а защищены x-cron-secret, sync-reviews делегирует syncSupport, media-cleanup удаляет просроченные медиа (файл + запись БД) с обработкой ENOENT; все 6 тестов GREEN; полный suite `npm run test` 0 failed.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npm run test` — 0 failed (wb-support-api + sync + cron + media-download + media-cleanup)
2. `npx tsc --noEmit` exit 0
3. `npm run build` exit 0 (компилируется с новыми route.ts)
4. `grep -c "export async function POST" app/api/support-sync/route.ts` == 1
5. `grep -c "export async function GET" app/api/cron/support-sync-reviews/route.ts` == 1
6. `grep -c "export async function GET" app/api/cron/support-media-cleanup/route.ts` == 1
</verification>

<success_criteria>
Plan 08-02 завершён когда:
- 3 route.ts файла созданы (POST sync + 2 cron GET)
- lib/support-sync.ts с идемпотентным upsert через `$transaction`, customerId=null всегда
- lib/support-media.ts с concurrency=5 + retry=1
- 4 теста GREEN (support-sync, support-cron, support-media-download, support-media-cleanup)
- Интеграция с lib/wb-support-api.ts (из Plan 08-01) корректная
- Commit: `feat(08-02): sync + cron поддержки с медиа-загрузкой`
</success_criteria>

<output>
Создать `.planning/phases/08-support-mvp/08-02-SUMMARY.md` с:
- Endpoints API: POST /api/support-sync, GET /api/cron/support-sync-reviews, GET /api/cron/support-media-cleanup
- Cron расписание для /etc/cron.d (Plan 08-04 добавит в deploy.sh):
  - `*/15 * * * * curl -s -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/support-sync-reviews`
  - `0 3 * * * curl -s -H "x-cron-secret: $CRON_SECRET" http://localhost:3001/api/cron/support-media-cleanup`
- AppSetting ключ "support.lastSyncedAt" — источник для Plan 08-03 UI (показ даты последней синхронизации опционально)
</output>
