---
phase: 08-support-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/(dashboard)/support/page.tsx
  - app/(dashboard)/layout.tsx
  - components/support/SupportTicketCard.tsx
  - components/support/SupportFilters.tsx
  - components/support/SupportPagination.tsx
  - components/layout/Sidebar.tsx
  - components/layout/NavLinks.tsx
  - components/layout/nav-items.ts
  - lib/support-badge.ts
  - tests/support-badge.test.ts
autonomous: true
requirements:
  - SUP-10
  - SUP-11
  - SUP-12
  - SUP-40
must_haves:
  truths:
    - "На /support рендерится лента тикетов (RSC) с цветными индикатор-полосами статуса и пагинацией по 20 штук"
    - "Фильтры (каналы, статусы, nmId, менеджер, dateFrom/dateTo, toggle «только неотвеченные») управляют запросом через searchParams"
    - "В sidebar у пункта «Служба поддержки» отображается badge с количеством SupportTicket.status=NEW"
    - "Старая заглушка /support (ссылка на ai-cs-zoiten GitHub) полностью ЗАМЕНЕНА на новую ленту"
    - "Пункт «Служба поддержки» в NAV_ITEMS остаётся с icon: Headphones и href: /support"
  artifacts:
    - path: "app/(dashboard)/support/page.tsx"
      provides: "RSC страница-лента тикетов с фильтрами и пагинацией"
      contains: "requireSection(\"SUPPORT\")"
    - path: "components/support/SupportTicketCard.tsx"
      provides: "Карточка тикета с индикатор-полосой + превью"
      exports: ["SupportTicketCard"]
    - path: "components/support/SupportFilters.tsx"
      provides: "MultiSelectDropdown + inputs через searchParams"
      exports: ["SupportFilters"]
    - path: "lib/support-badge.ts"
      provides: "getSupportBadgeCount() — count new tickets"
      exports: ["getSupportBadgeCount"]
  key_links:
    - from: "app/(dashboard)/support/page.tsx"
      to: "prisma.supportTicket.findMany"
      via: "where = buildWhere(searchParams)"
      pattern: "supportTicket\\.findMany"
    - from: "components/layout/Sidebar.tsx (или DashboardShell.tsx / NavLinks.tsx — зависит от реальной структуры layout)"
      to: "lib/support-badge.ts"
      via: "supportBadgeCount prop из layout.tsx"
      pattern: "supportBadgeCount"
    - from: "app/(dashboard)/layout.tsx"
      to: "lib/support-badge.ts"
      via: "await getSupportBadgeCount()"
      pattern: "getSupportBadgeCount"
---

<objective>
Phase 8 Plan 03 — лента `/support` + sidebar badge:
1. RSC страница `/support` с карточками тикетов, индикатор-полосами статуса (NEW красный, IN_PROGRESS жёлтый, ANSWERED зелёный, CLOSED серый, APPEALED фиолетовый), превью, рейтингом для FEEDBACK, ссылкой на WbCard.
2. `SupportTicketCard` — один компонент для одной карточки.
3. `SupportFilters` — inline MultiSelectDropdown + nmId input + date inputs + toggle «только неотвеченные» через searchParams (паттерн WbFilters.tsx).
4. Пагинация через searchParams (page, pageSize=20) — `SupportPagination` компонент.
5. Замена существующей заглушки `/support` (Phase 5 — ссылка на github.com/safyodorov/ai-cs-zoiten) на новую ленту.
6. Sidebar badge: `lib/support-badge.ts` → getSupportBadgeCount() → передаётся из `app/(dashboard)/layout.tsx` в Sidebar через prop.
7. nav-items.ts не меняем (пункт «Служба поддержки» + icon:"Headphones" уже есть); расширяем NavLinks если нужно для badge rendering.

Purpose: Первый UI поверх данных, установленных Plan 08-01/08-02. Независимо от диалога (Plan 08-04) — можно развивать параллельно.

Output: 1 обновлённая RSC-страница, 3 новых компонента support/, badge utility + тест, обновлённый layout + Sidebar + NavLinks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-support-mvp/08-CONTEXT.md
@.planning/phases/08-support-mvp/08-RESEARCH.md
@.planning/phases/08-support-mvp/08-01-SUMMARY.md
@.planning/phases/08-support-mvp/08-02-SUMMARY.md
@CLAUDE.md
@app/(dashboard)/support/page.tsx
@app/(dashboard)/layout.tsx
@components/layout/nav-items.ts
@components/layout/Sidebar.tsx
@components/cards/WbFilters.tsx
@lib/rbac.ts
@lib/prisma.ts

<interfaces>
Prisma модели (созданы в Plan 08-01):
```prisma
model SupportTicket {
  id String; channel TicketChannel; wbExternalId String?
  customerId String?; nmId Int?; status TicketStatus
  assignedToId String?; rating Int?; previewText String?
  createdAt DateTime; lastMessageAt DateTime?
  assignedTo User?; messages SupportMessage[]; customer Customer?
}
enum TicketChannel { FEEDBACK; QUESTION; CHAT; RETURN; MESSENGER }
enum TicketStatus { NEW; IN_PROGRESS; ANSWERED; CLOSED; APPEALED }
```

WbCard для получения фото по nmId (существующая модель):
```prisma
model WbCard { nmId Int @unique; photos Json?  /* ... */ }
```
(Формат фото и способ получения URL фото смотреть в существующем коде /cards/wb)

User фильтр для assignee: User где sectionRoles.some(section:"SUPPORT")
```typescript
prisma.user.findMany({
  where: { isActive: true, sectionRoles: { some: { section: "SUPPORT" } } },
  select: { id: true, name: true, firstName: true, lastName: true }
})
```

Паттерн MultiSelectDropdown: `components/cards/WbFilters.tsx` — inline component с useState+useRef, mount на popover вручную.

Navigation: components/layout/nav-items.ts — `{ section: "SUPPORT", href: "/support", label: "Служба поддержки", icon: "Headphones" }` уже есть.

Session layout: `app/(dashboard)/layout.tsx` — RSC, передаёт компоненты в DashboardShell; Sidebar рендерит NavLinks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: SupportFilters + SupportTicketCard + SupportPagination компоненты</name>
  <files>
    components/support/SupportTicketCard.tsx,
    components/support/SupportFilters.tsx,
    components/support/SupportPagination.tsx
  </files>
  <read_first>
    - components/cards/WbFilters.tsx (эталон MultiSelectDropdown + useSearchParams паттерн)
    - .planning/phases/08-support-mvp/08-RESEARCH.md (раздел «Карточка тикета — layout» строки 594-614, «Фильтры через searchParams» строки 617-625)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «specifics» — иконки каналов Lucide, превью 140 символов, имя покупателя "Покупатель")
    - CLAUDE.md (Conventions — русский язык, MultiSelectDropdown, native select)
  </read_first>
  <action>
    **Файл 1: `components/support/SupportTicketCard.tsx`** (Server Component — без "use client", чтобы вложенная Link/Image работали как RSC):

    ```tsx
    // components/support/SupportTicketCard.tsx
    import Link from "next/link"
    import { MessageSquare, HelpCircle, MessageCircle, RotateCw, Inbox, Star } from "lucide-react"
    import type { TicketChannel, TicketStatus } from "@prisma/client"

    const channelIconMap: Record<TicketChannel, typeof MessageSquare> = {
      FEEDBACK: MessageSquare,
      QUESTION: HelpCircle,
      CHAT: MessageCircle,
      RETURN: RotateCw,
      MESSENGER: Inbox,
    }

    const channelLabelMap: Record<TicketChannel, string> = {
      FEEDBACK: "Отзыв",
      QUESTION: "Вопрос",
      CHAT: "Чат",
      RETURN: "Возврат",
      MESSENGER: "Мессенджер",
    }

    const statusBorderMap: Record<TicketStatus, string> = {
      NEW: "border-l-red-500",
      IN_PROGRESS: "border-l-yellow-500",
      ANSWERED: "border-l-green-500",
      CLOSED: "border-l-gray-400",
      APPEALED: "border-l-purple-500",
    }

    const statusLabelMap: Record<TicketStatus, string> = {
      NEW: "Новый",
      IN_PROGRESS: "В работе",
      ANSWERED: "Отвечен",
      CLOSED: "Закрыт",
      APPEALED: "Обжалование",
    }

    export interface SupportTicketCardProps {
      ticket: {
        id: string
        channel: TicketChannel
        status: TicketStatus
        nmId: number | null
        rating: number | null
        previewText: string | null
        createdAt: Date
        assignedTo: { id: string; name: string; firstName: string | null; lastName: string | null } | null
      }
      wbCard: { nmId: number; photoUrl: string | null; title: string | null } | null
    }

    function formatDate(d: Date): string {
      return new Intl.DateTimeFormat("ru-RU", {
        day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit",
        timeZone: "Europe/Moscow",
      }).format(d)
    }

    function getAssigneeName(u: SupportTicketCardProps["ticket"]["assignedTo"]): string {
      if (!u) return "Не назначен"
      const full = [u.firstName, u.lastName].filter(Boolean).join(" ").trim()
      return full || u.name || "—"
    }

    export function SupportTicketCard({ ticket, wbCard }: SupportTicketCardProps) {
      const Icon = channelIconMap[ticket.channel]
      return (
        <Link
          href={`/support/${ticket.id}`}
          className={`flex gap-4 p-4 bg-white dark:bg-neutral-900 rounded-lg border border-l-4 hover:shadow-md transition-shadow ${statusBorderMap[ticket.status]}`}
        >
          <div className="flex-shrink-0 flex flex-col items-center gap-1 text-muted-foreground">
            <Icon className="w-5 h-5" />
            <span className="text-[10px] uppercase">{channelLabelMap[ticket.channel]}</span>
          </div>
          {wbCard?.photoUrl ? (
            // фото товара WB 60x80 (пропорция 3:4)
            // eslint-disable-next-line @next/next/no-img-element
            <img src={wbCard.photoUrl} alt={wbCard.title ?? ""} className="w-[60px] h-[80px] object-cover rounded" />
          ) : (
            <div className="w-[60px] h-[80px] rounded bg-muted flex items-center justify-center text-xs text-muted-foreground">
              {ticket.nmId ?? "—"}
            </div>
          )}
          <div className="flex-1 min-w-0 space-y-1">
            <div className="flex items-center gap-2 text-sm">
              <span className="font-medium">Покупатель</span>
              {ticket.channel === "FEEDBACK" && ticket.rating !== null && (
                <span className="flex items-center gap-0.5 text-amber-500">
                  {Array.from({ length: ticket.rating }).map((_, i) => (
                    <Star key={i} className="w-3.5 h-3.5 fill-current" />
                  ))}
                </span>
              )}
              <span className="text-xs text-muted-foreground ml-auto">{statusLabelMap[ticket.status]}</span>
            </div>
            <p className="text-sm text-muted-foreground line-clamp-2">
              {ticket.previewText ?? <span className="italic">нет текста</span>}
            </p>
            <div className="flex items-center justify-between text-xs text-muted-foreground pt-1">
              <span>{ticket.nmId ? `Артикул ${ticket.nmId}` : "Без артикула"}</span>
              <span>{formatDate(ticket.createdAt)}</span>
              <span>{getAssigneeName(ticket.assignedTo)}</span>
            </div>
          </div>
        </Link>
      )
    }
    ```

    **Файл 2: `components/support/SupportFilters.tsx`** (Client — MultiSelectDropdown inline, паттерн WbFilters):

    ```tsx
    "use client"
    import { useState, useRef, useEffect, useCallback } from "react"
    import { useRouter, useSearchParams, usePathname } from "next/navigation"
    import { Button } from "@/components/ui/button"
    import { Checkbox } from "@/components/ui/checkbox"
    import { ChevronDown, X } from "lucide-react"

    interface Option { value: string; label: string }
    interface Props {
      channelOptions: Option[]
      statusOptions: Option[]
      assigneeOptions: Option[]
    }

    function MultiSelectDropdown({ label, options, selected, onChange }: {
      label: string; options: Option[]; selected: string[]; onChange: (v: string[]) => void
    }) {
      const [open, setOpen] = useState(false)
      const ref = useRef<HTMLDivElement>(null)
      useEffect(() => {
        function click(e: MouseEvent) {
          if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false)
        }
        document.addEventListener("mousedown", click)
        return () => document.removeEventListener("mousedown", click)
      }, [])
      function toggle(v: string) {
        onChange(selected.includes(v) ? selected.filter((x) => x !== v) : [...selected, v])
      }
      const display = selected.length > 0 ? `${label} (${selected.length})` : label
      return (
        <div ref={ref} className="relative">
          <Button variant="outline" size="sm" onClick={() => setOpen(!open)}
            className={`gap-1.5 ${selected.length > 0 ? "border-primary text-primary" : ""}`}>
            {display}
            <ChevronDown className={`h-3.5 w-3.5 transition-transform ${open ? "rotate-180" : ""}`} />
          </Button>
          {open && (
            <div className="absolute top-full left-0 mt-1 z-50 min-w-[200px] max-h-[300px] overflow-y-auto rounded-md border bg-popover p-1 shadow-md">
              {options.map((o) => (
                <label key={o.value} className="flex items-center gap-2 px-2 py-1.5 rounded hover:bg-muted cursor-pointer text-sm">
                  <Checkbox checked={selected.includes(o.value)} onCheckedChange={() => toggle(o.value)} />
                  <span className="truncate">{o.label}</span>
                </label>
              ))}
              {options.length === 0 && <p className="px-2 py-1.5 text-xs text-muted-foreground">Нет данных</p>}
            </div>
          )}
        </div>
      )
    }

    export function SupportFilters({ channelOptions, statusOptions, assigneeOptions }: Props) {
      const router = useRouter()
      const pathname = usePathname()
      const sp = useSearchParams()

      const channels = (sp.get("channels") ?? "").split(",").filter(Boolean)
      const statuses = (sp.get("statuses") ?? "").split(",").filter(Boolean)
      const assignees = (sp.get("assignees") ?? "").split(",").filter(Boolean)
      const nmId = sp.get("nmId") ?? ""
      const dateFrom = sp.get("dateFrom") ?? ""
      const dateTo = sp.get("dateTo") ?? ""
      const unansweredOnly = sp.get("unanswered") === "1"

      const updateParam = useCallback((key: string, value: string | null) => {
        const next = new URLSearchParams(sp.toString())
        if (!value) next.delete(key); else next.set(key, value)
        next.delete("page") // reset pagination при смене фильтра
        router.push(`${pathname}?${next.toString()}`)
      }, [sp, pathname, router])

      return (
        <div className="flex flex-wrap items-center gap-2">
          <MultiSelectDropdown label="Канал" options={channelOptions} selected={channels}
            onChange={(v) => updateParam("channels", v.join(","))} />
          <MultiSelectDropdown label="Статус" options={statusOptions} selected={statuses}
            onChange={(v) => updateParam("statuses", v.join(","))} />
          <MultiSelectDropdown label="Менеджер" options={assigneeOptions} selected={assignees}
            onChange={(v) => updateParam("assignees", v.join(","))} />
          <input
            type="text" inputMode="numeric" placeholder="Артикул nmId" value={nmId}
            onChange={(e) => updateParam("nmId", e.target.value.replace(/\D/g, ""))}
            className="h-9 w-[140px] rounded-md border bg-transparent px-3 text-sm"
          />
          <input type="date" value={dateFrom} onChange={(e) => updateParam("dateFrom", e.target.value)}
            className="h-9 rounded-md border bg-transparent px-2 text-sm" />
          <input type="date" value={dateTo} onChange={(e) => updateParam("dateTo", e.target.value)}
            className="h-9 rounded-md border bg-transparent px-2 text-sm" />
          <label className="flex items-center gap-2 text-sm">
            <Checkbox checked={unansweredOnly} onCheckedChange={(c) => updateParam("unanswered", c ? "1" : null)} />
            Только неотвеченные
          </label>
          {(channels.length || statuses.length || assignees.length || nmId || dateFrom || dateTo || unansweredOnly) ? (
            <Button variant="ghost" size="sm" onClick={() => router.push(pathname)}>
              <X className="h-3.5 w-3.5 mr-1" />Сбросить
            </Button>
          ) : null}
        </div>
      )
    }
    ```

    **Файл 3: `components/support/SupportPagination.tsx`** (Client):

    ```tsx
    "use client"
    import Link from "next/link"
    import { useSearchParams, usePathname } from "next/navigation"
    import { Button } from "@/components/ui/button"
    import { ChevronLeft, ChevronRight } from "lucide-react"

    export function SupportPagination({ page, pageSize, total }: { page: number; pageSize: number; total: number }) {
      const sp = useSearchParams()
      const pathname = usePathname()
      const totalPages = Math.max(1, Math.ceil(total / pageSize))

      function href(p: number): string {
        const next = new URLSearchParams(sp.toString())
        next.set("page", String(p))
        return `${pathname}?${next.toString()}`
      }

      return (
        <div className="flex items-center justify-between pt-4 text-sm text-muted-foreground">
          <span>Всего: {total}</span>
          <div className="flex items-center gap-2">
            <Button asChild variant="outline" size="sm" disabled={page <= 1}>
              <Link href={href(Math.max(1, page - 1))}><ChevronLeft className="h-3.5 w-3.5" /></Link>
            </Button>
            <span>Стр. {page} из {totalPages}</span>
            <Button asChild variant="outline" size="sm" disabled={page >= totalPages}>
              <Link href={href(Math.min(totalPages, page + 1))}><ChevronRight className="h-3.5 w-3.5" /></Link>
            </Button>
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/SupportTicketCard.tsx` (exit 0)
    - `test -f components/support/SupportFilters.tsx` (exit 0)
    - `test -f components/support/SupportPagination.tsx` (exit 0)
    - `grep "border-l-red-500" components/support/SupportTicketCard.tsx` находит NEW индикатор
    - `grep "MessageSquare\|HelpCircle" components/support/SupportTicketCard.tsx` находит иконки каналов
    - `grep "useSearchParams" components/support/SupportFilters.tsx` находит паттерн
    - `grep "Только неотвеченные" components/support/SupportFilters.tsx` находит русскую надпись
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    3 компонента созданы с корректной типизацией и русским UI; tsc noEmit passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: RSC страница /support + замена заглушки + badge utility + тест</name>
  <files>
    app/(dashboard)/support/page.tsx,
    lib/support-badge.ts,
    tests/support-badge.test.ts
  </files>
  <read_first>
    - app/(dashboard)/support/page.tsx (СУЩЕСТВУЮЩАЯ ЗАГЛУШКА — будет полностью заменена, читать чтобы понимать что удаляем)
    - app/(dashboard)/cards/wb/page.tsx (если есть — эталон RSC страницы со списком + фильтрами + пагинацией)
    - app/actions/products.ts (паттерн server actions)
    - lib/rbac.ts (requireSection)
    - lib/prisma.ts
    - tests/support-badge.test.ts (Wave 0 stub — заменить)
    - .planning/phases/08-support-mvp/08-RESEARCH.md (раздел «Pagination» строки 630-641)
  </read_first>
  <behavior>
    - getSupportBadgeCount() возвращает число SupportTicket где status=NEW
    - Страница /support делает requireSection("SUPPORT")
    - Читает searchParams: channels, statuses, assignees, nmId, dateFrom, dateTo, unanswered, page, pageSize (defaults)
    - Построение where: channels→IN, statuses→IN, assignees→assignedToId IN, nmId→eq, dateFrom/dateTo→createdAt gte/lte, unanswered=1→status IN [NEW, IN_PROGRESS]
    - findMany с include: {assignedTo: {select: id, name, firstName, lastName}}, orderBy createdAt desc, skip/take
    - count для пагинации
    - Для каждого уникального nmId делает JOIN по WbCard → photoUrl + title (аналогично паттерну /cards/wb)
    - Рендер: кнопка Sync справа от заголовка (Plan 08-04 создаст компонент, пока render string «Синхронизировать» будет проставлен в Plan 08-04 — здесь оставить плейсхолдер-комментарий)
    - Если total=0: empty-state «Нет тикетов»
  </behavior>
  <action>
    **Файл 1: `lib/support-badge.ts`:**

    ```typescript
    // lib/support-badge.ts
    // Count of new tickets — used by Sidebar badge via (dashboard)/layout.tsx.
    import { prisma } from "@/lib/prisma"

    export async function getSupportBadgeCount(): Promise<number> {
      try {
        return await prisma.supportTicket.count({ where: { status: "NEW" } })
      } catch {
        // DB недоступна или миграция ещё не применена — не валим layout
        return 0
      }
    }
    ```

    **Файл 2: заменить `tests/support-badge.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    const countMock = vi.fn()
    vi.mock("@/lib/prisma", () => ({
      prisma: { supportTicket: { count: countMock } },
    }))

    beforeEach(() => countMock.mockReset())

    describe("getSupportBadgeCount", () => {
      it("возвращает количество тикетов со статусом NEW", async () => {
        countMock.mockResolvedValueOnce(7)
        const { getSupportBadgeCount } = await import("@/lib/support-badge")
        expect(await getSupportBadgeCount()).toBe(7)
        expect(countMock).toHaveBeenCalledWith({ where: { status: "NEW" } })
      })
      it("возвращает 0 если Prisma падает (например, миграция не применена)", async () => {
        countMock.mockRejectedValueOnce(new Error("DB offline"))
        const { getSupportBadgeCount } = await import("@/lib/support-badge")
        expect(await getSupportBadgeCount()).toBe(0)
      })
    })
    ```

    **Файл 3: ЗАМЕНИТЬ ПОЛНОСТЬЮ `app/(dashboard)/support/page.tsx`** (старое содержимое с GitHub ссылкой — удалить):

    ```tsx
    // app/(dashboard)/support/page.tsx
    // RSC — лента тикетов службы поддержки WB
    import { requireSection } from "@/lib/rbac"
    import { prisma } from "@/lib/prisma"
    import { SupportFilters } from "@/components/support/SupportFilters"
    import { SupportTicketCard } from "@/components/support/SupportTicketCard"
    import { SupportPagination } from "@/components/support/SupportPagination"
    import type { TicketChannel, TicketStatus } from "@prisma/client"

    const CHANNEL_OPTIONS = [
      { value: "FEEDBACK", label: "Отзывы" },
      { value: "QUESTION", label: "Вопросы" },
      { value: "CHAT", label: "Чаты" },
      { value: "RETURN", label: "Возвраты" },
      { value: "MESSENGER", label: "Мессенджер" },
    ]

    const STATUS_OPTIONS = [
      { value: "NEW", label: "Новый" },
      { value: "IN_PROGRESS", label: "В работе" },
      { value: "ANSWERED", label: "Отвечен" },
      { value: "CLOSED", label: "Закрыт" },
      { value: "APPEALED", label: "Обжалование" },
    ]

    function parseList<T extends string>(sp: string | string[] | undefined, valid: readonly T[]): T[] {
      if (!sp) return []
      const raw = Array.isArray(sp) ? sp.join(",") : sp
      return raw.split(",").filter((v): v is T => valid.includes(v as T))
    }

    export default async function SupportPage({
      searchParams,
    }: {
      searchParams: Promise<Record<string, string | string[] | undefined>>
    }) {
      await requireSection("SUPPORT")
      const sp = await searchParams

      const channels = parseList(sp.channels, ["FEEDBACK","QUESTION","CHAT","RETURN","MESSENGER"] as const)
      const statuses = parseList(sp.statuses, ["NEW","IN_PROGRESS","ANSWERED","CLOSED","APPEALED"] as const)
      const assignees = (Array.isArray(sp.assignees) ? sp.assignees.join(",") : sp.assignees ?? "").split(",").filter(Boolean)
      const nmId = sp.nmId ? Number(Array.isArray(sp.nmId) ? sp.nmId[0] : sp.nmId) : null
      const dateFrom = (Array.isArray(sp.dateFrom) ? sp.dateFrom[0] : sp.dateFrom) || null
      const dateTo = (Array.isArray(sp.dateTo) ? sp.dateTo[0] : sp.dateTo) || null
      const unansweredOnly = sp.unanswered === "1"
      const page = Math.max(1, Number(sp.page ?? 1))
      const pageSize = 20

      // assignee user options
      const supportUsers = await prisma.user.findMany({
        where: { isActive: true, sectionRoles: { some: { section: "SUPPORT" } } },
        select: { id: true, name: true, firstName: true, lastName: true },
        orderBy: [{ lastName: "asc" }, { firstName: "asc" }],
      })
      const assigneeOptions = supportUsers.map((u) => ({
        value: u.id,
        label: [u.firstName, u.lastName].filter(Boolean).join(" ").trim() || u.name || u.id.slice(-6),
      }))

      // Build where
      const where: Record<string, unknown> = {}
      if (channels.length) where.channel = { in: channels }
      if (statuses.length) {
        where.status = { in: statuses }
      } else if (unansweredOnly) {
        where.status = { in: ["NEW", "IN_PROGRESS"] as TicketStatus[] }
      }
      if (assignees.length) where.assignedToId = { in: assignees }
      if (nmId && !Number.isNaN(nmId)) where.nmId = nmId
      if (dateFrom || dateTo) {
        where.createdAt = {
          ...(dateFrom ? { gte: new Date(dateFrom) } : {}),
          ...(dateTo ? { lte: new Date(`${dateTo}T23:59:59`) } : {}),
        }
      }

      const [tickets, total] = await Promise.all([
        prisma.supportTicket.findMany({
          where,
          orderBy: { createdAt: "desc" },
          skip: (page - 1) * pageSize,
          take: pageSize,
          select: {
            id: true, channel: true, status: true, nmId: true, rating: true,
            previewText: true, createdAt: true,
            assignedTo: { select: { id: true, name: true, firstName: true, lastName: true } },
          },
        }),
        prisma.supportTicket.count({ where }),
      ])

      // WbCard JOIN по nmId (паттерн проекта — не FK)
      const nmIds = Array.from(new Set(tickets.map((t) => t.nmId).filter((n): n is number => n !== null)))
      const cards = nmIds.length > 0
        ? await prisma.wbCard.findMany({
            where: { nmId: { in: nmIds } },
            select: { nmId: true, title: true, photos: true },
          })
        : []
      const cardMap = new Map<number, { nmId: number; photoUrl: string | null; title: string | null }>()
      for (const c of cards) {
        // WbCard.photos — Json?, берём первый объект .c246x328 или .big
        const photos = Array.isArray(c.photos) ? c.photos : null
        const first = photos?.[0] as { c246x328?: string; big?: string } | undefined
        const photoUrl = first?.c246x328 ?? first?.big ?? null
        cardMap.set(c.nmId, { nmId: c.nmId, photoUrl, title: c.title })
      }

      return (
        <div className="p-6 space-y-4">
          <div className="flex items-center justify-between">
            <h1 className="text-xl font-semibold">Служба поддержки</h1>
            {/* SyncButton монтируется в Plan 08-04: <SupportSyncButton /> */}
          </div>
          <SupportFilters
            channelOptions={CHANNEL_OPTIONS}
            statusOptions={STATUS_OPTIONS}
            assigneeOptions={assigneeOptions}
          />
          {tickets.length === 0 ? (
            <div className="flex items-center justify-center min-h-[40vh] text-muted-foreground">
              Нет тикетов
            </div>
          ) : (
            <ul className="space-y-2">
              {tickets.map((t) => (
                <li key={t.id}>
                  <SupportTicketCard ticket={t} wbCard={t.nmId ? cardMap.get(t.nmId) ?? null : null} />
                </li>
              ))}
            </ul>
          )}
          <SupportPagination page={page} pageSize={pageSize} total={total} />
        </div>
      )
    }
    ```

    Проверка: `grep "github.com/safyodorov/ai-cs-zoiten" app/(dashboard)/support/page.tsx` должен быть ПУСТ (старая заглушка удалена).
  </action>
  <verify>
    <automated>npm run test -- tests/support-badge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/support-badge.ts` (exit 0)
    - `grep "export async function getSupportBadgeCount" lib/support-badge.ts` находит строку
    - `grep "status: \"NEW\"" lib/support-badge.ts` находит фильтр
    - `npm run test -- tests/support-badge.test.ts` exit 0 (2 теста GREEN)
    - `grep "github.com/safyodorov/ai-cs-zoiten" app/(dashboard)/support/page.tsx | wc -l` == 0 (старая заглушка удалена)
    - `grep "requireSection(\"SUPPORT\")" app/(dashboard)/support/page.tsx` находит RBAC
    - `grep "SupportTicketCard" app/(dashboard)/support/page.tsx` находит импорт
    - `grep "prisma.supportTicket.findMany" app/(dashboard)/support/page.tsx` находит запрос
  </acceptance_criteria>
  <done>
    lib/support-badge.ts + тест GREEN; `/support/page.tsx` полностью переписан на RSC-ленту тикетов (старая ссылка на ai-cs-zoiten удалена); фильтры и пагинация подключены через searchParams.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Sidebar badge — интеграция в layout + NavLinks</name>
  <files>
    app/(dashboard)/layout.tsx,
    components/layout/Sidebar.tsx,
    components/layout/NavLinks.tsx,
    components/layout/nav-items.ts
  </files>
  <read_first>
    - app/(dashboard)/layout.tsx (текущий RSC layout — проверить существование и структуру, паттерн передачи в DashboardShell)
    - components/layout/Sidebar.tsx (ПРОВЕРИТЬ СУЩЕСТВОВАНИЕ — может отсутствовать, вместо него DashboardShell + NavLinks. Если есть — понять как принимает props)
    - components/layout/DashboardShell.tsx (ПРОВЕРИТЬ СУЩЕСТВОВАНИЕ — из CLAUDE.md это RSC/client паттерн: RSC layout → DashboardShell client через ReactNode props)
    - components/layout/NavLinks.tsx (client — отрисовка пунктов меню)
    - components/layout/nav-items.ts (ПОДТВЕРДИТЬ текущую структуру NAV_ITEMS массива)
    - lib/support-badge.ts (созданный в задаче 2)
  </read_first>
  <action>
    **Шаг A — ОБЯЗАТЕЛЬНО ПРОВЕРИТЬ СТРУКТУРУ перед модификацией (explicit branching):**

    ```bash
    test -f components/layout/Sidebar.tsx && echo "SIDEBAR_EXISTS" || echo "SIDEBAR_MISSING"
    test -f components/layout/DashboardShell.tsx && echo "DASHBOARD_SHELL_EXISTS" || echo "DASHBOARD_SHELL_MISSING"
    ```

    **ВЕТВЛЕНИЕ (explicit, не интерпретировать):**
    - **ЕСЛИ `components/layout/Sidebar.tsx` НЕ существует** (SIDEBAR_MISSING) → модифицировать `DashboardShell.tsx` + `NavLinks.tsx` (по CLAUDE.md это layout-паттерн: RSC layout → DashboardShell client → Sidebar/NavLinks внутри).
    - **ЕСЛИ `Sidebar.tsx` существует** (SIDEBAR_EXISTS) → модифицировать его.
    - **В любом случае** проп `supportBadgeCount: number` пробрасывается из `app/(dashboard)/layout.tsx` через props вниз по цепочке (layout → DashboardShell → Sidebar/NavLinks).

    **Стратегия пропсов.** Возможные варианты:

    1. Layout-компонент уже принимает `badges: Record<string, number>` — просто добавить в layout.tsx.
    2. Layout-компонент не имеет badge-system — добавить проп `supportBadgeCount?: number` точечно (минимальное изменение).

    **Мы делаем Вариант 2** (MVP, не ломаем остальное): точечный prop `supportBadgeCount?: number` (единое имя на всех уровнях для grep-проверки) → финально в `NavLinks`.

    **Шаг B — обновить `components/layout/NavLinks.tsx`:**

    Найти рендер каждого `NavItem`. После основного label добавить conditional badge:

    Псевдо-diff (точная структура зависит от существующего кода):
    ```tsx
    // components/layout/NavLinks.tsx — добавить в props
    interface NavLinksProps {
      // ... существующие props
      supportBadgeCount?: number
    }

    // В рендере пункта меню, когда item.href === "/support":
    {item.href === "/support" && supportBadgeCount !== undefined && supportBadgeCount > 0 && (
      <span className="ml-auto inline-flex items-center justify-center min-w-[20px] h-5 px-1.5 rounded-full bg-red-500 text-white text-[11px] font-medium">
        {supportBadgeCount > 99 ? "99+" : supportBadgeCount}
      </span>
    )}
    ```

    Важно: если Sidebar свёрнут (collapsed, w-16), badge должен рендериться как маленькая точка в правом-верхнем углу иконки (или вообще скрываться — выбрать по существующему паттерну collapsed состояния).

    **Шаг C — обновить `components/layout/Sidebar.tsx`:**

    Добавить проп `supportBadgeCount?: number` и пробросить в `NavLinks`:
    ```tsx
    interface SidebarProps {
      // ...existing
      supportBadgeCount?: number
    }
    export function Sidebar({ /* ... */, supportBadgeCount }: SidebarProps) {
      // ...
      return (
        <aside>
          <NavLinks supportBadgeCount={supportBadgeCount} /* ...other props */ />
        </aside>
      )
    }
    ```

    **Шаг D — обновить `app/(dashboard)/layout.tsx`:**

    В самом начале (уже async RSC) вызвать getSupportBadgeCount() и передать в DashboardShell/Sidebar:
    ```tsx
    import { getSupportBadgeCount } from "@/lib/support-badge"

    export default async function DashboardLayout({ children }: { children: React.ReactNode }) {
      // ... existing auth/session
      const supportBadgeCount = await getSupportBadgeCount()
      return (
        <DashboardShell logoutForm={<LogoutForm />} supportBadgeCount={supportBadgeCount}>
          {children}
        </DashboardShell>
      )
    }
    ```

    Если `DashboardShell` не принимает `supportBadgeCount`, добавить:
    ```tsx
    // components/layout/DashboardShell.tsx (если существует)
    interface DashboardShellProps {
      children: React.ReactNode
      logoutForm: React.ReactNode
      supportBadgeCount?: number
    }
    export function DashboardShell({ children, logoutForm, supportBadgeCount }: DashboardShellProps) {
      // ... pass supportBadgeCount → Sidebar (или NavLinks напрямую, зависит от существующей структуры)
    }
    ```

    **Шаг E — nav-items.ts не меняем** (пункт уже с icon: "Headphones", href: "/support" — зафиксировано в текущем файле).

    **Шаг F — smoke-check:** `npm run build` должен успешно собрать проект.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "supportBadgeCount" components/layout/*.tsx | wc -l` >= 1 (проп встречается минимум в одном layout-компоненте: Sidebar.tsx ИЛИ DashboardShell.tsx ИЛИ NavLinks.tsx — зависит от веток Шага A)
    - `grep "supportBadgeCount" components/layout/NavLinks.tsx` находит использование (финальная точка рендера)
    - `grep "getSupportBadgeCount" app/(dashboard)/layout.tsx` находит вызов
    - `grep "HeadphonesIcon\|Headphones" components/layout/nav-items.ts` находит пункт SUPPORT (не сломан)
    - `npx tsc --noEmit` exit 0
    - (`npm run build` запускается в финальном `<verification>` блоке плана — не per-task, чтобы сохранить per-task feedback latency <30s)
  </acceptance_criteria>
  <done>
    layout.tsx (RSC) вызывает getSupportBadgeCount() и пробрасывает supportBadgeCount через существующую layout-цепочку (Sidebar ИЛИ DashboardShell → NavLinks); badge рендерится справа от пункта «Служба поддержки» если count > 0; `npx tsc --noEmit` проходит; `npm run build` проходит в финальной `<verification>` плана.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npm run test` full suite — 0 failed (включая support-badge GREEN)
2. `npm run build` exit 0
3. `npx tsc --noEmit` exit 0
4. `grep -c "github.com/safyodorov/ai-cs-zoiten" app/(dashboard)/support/page.tsx` == 0 (заглушка заменена)
5. Визуально на dev-сервере: открыть /support — видна лента (или empty state), фильтры работают, при status=NEW в БД — badge в sidebar показывает число
</verification>

<success_criteria>
Plan 08-03 завершён когда:
- /support полностью заменил заглушку (нет GitHub ссылки)
- 3 компонента support/ созданы (Card, Filters, Pagination) с русским языком
- Фильтры через searchParams: channels, statuses, assignees, nmId, dateFrom/dateTo, unanswered — работают
- Pagination 20/страницу
- Sidebar badge подсчитывает SupportTicket.status=NEW
- tests/support-badge.test.ts GREEN
- npm run build success
- Commit: `feat(08-03): лента /support + sidebar badge`
</success_criteria>

<output>
Создать `.planning/phases/08-support-mvp/08-03-SUMMARY.md` с:
- Список новых компонентов support/
- URL параметры ленты (channels, statuses, assignees, nmId, dateFrom, dateTo, unanswered, page)
- lib/support-badge.ts API
- Примечание для Plan 08-04: в `/support/page.tsx` на строке «{/* SyncButton монтируется в Plan 08-04 */}» — нужно импортировать и вставить SupportSyncButton
- UAT-задачи для HUMAN-UAT: открыть /support, проверить фильтры каждый по очереди, проверить badge после sync
</output>
