---
phase: 08-support-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/wb-support-api.ts
  - tests/wb-support-api.test.ts
  - tests/support-sync.test.ts
  - tests/support-cron.test.ts
  - tests/support-media-download.test.ts
  - tests/support-media-cleanup.test.ts
  - tests/support-badge.test.ts
  - tests/support-actions.test.ts
autonomous: true
requirements:
  - SUP-01
  - SUP-02
  - SUP-03
  - SUP-04
must_haves:
  truths:
    - "Prisma schema содержит 4 новые модели (SupportTicket, SupportMessage, SupportMedia, Customer) и 5 enum (TicketChannel, TicketStatus, AppealStatus, Direction, MediaType)"
    - "lib/wb-support-api.ts экспортирует методы listFeedbacks, replyFeedback, editFeedbackAnswer, listQuestions, replyQuestion и корректно обрабатывает 429 через X-Ratelimit-Retry"
    - "tests/wb-support-api.test.ts GREEN — покрывает endpoints, headers, парсинг responses и 429 retry"
    - "6 test stub-файлов существуют как пустые vitest-сюиты (не упадут на npm run test)"
    - "VPS-директория /var/www/zoiten-uploads/support/ готова к приёму медиа"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "4 новые модели поддержки + 5 enum + обратные relations на User"
      contains: "model SupportTicket"
    - path: "lib/wb-support-api.ts"
      provides: "Типизированный клиент WB Feedbacks + Questions API"
      exports: ["listFeedbacks", "replyFeedback", "editFeedbackAnswer", "listQuestions", "replyQuestion"]
    - path: "tests/wb-support-api.test.ts"
      provides: "Unit-тесты клиента (mock fetch)"
      contains: "describe"
    - path: "tests/support-sync.test.ts"
      provides: "Wave 0 stub для Plan 08-02"
    - path: "tests/support-cron.test.ts"
      provides: "Wave 0 stub для Plan 08-02"
    - path: "tests/support-media-download.test.ts"
      provides: "Wave 0 stub для Plan 08-02"
    - path: "tests/support-media-cleanup.test.ts"
      provides: "Wave 0 stub для Plan 08-02"
    - path: "tests/support-badge.test.ts"
      provides: "Wave 0 stub для Plan 08-03"
    - path: "tests/support-actions.test.ts"
      provides: "Wave 0 stub для Plan 08-04"
  key_links:
    - from: "lib/wb-support-api.ts"
      to: "https://feedbacks-api.wildberries.ru"
      via: "fetch с Authorization: WB_API_TOKEN"
      pattern: "feedbacks-api\\.wildberries\\.ru"
    - from: "prisma/schema.prisma SupportTicket"
      to: "User (assignedTo)"
      via: "@relation(\"SupportAssignee\")"
      pattern: "SupportAssignee"
    - from: "prisma/schema.prisma SupportMessage"
      to: "User (author)"
      via: "@relation(\"SupportAuthor\")"
      pattern: "SupportAuthor"
---

<objective>
Phase 8 Plan 01 — foundation плана поддержки:
1. Prisma миграция: 4 новые модели (SupportTicket, SupportMessage, SupportMedia, Customer) + 5 enum (TicketChannel, TicketStatus, AppealStatus, Direction, MediaType) с обратными relations на User и связью с WbCard через nmId (не FK, паттерн проекта).
2. lib/wb-support-api.ts — типизированный клиент для WB Feedbacks (list + POST/PATCH answer) и Questions (list + PATCH answer) с unit-тестами через vitest + mock fetch.
3. Wave 0: 6 пустых stub-файлов для downstream планов (08-02, 08-03, 08-04).
4. Подготовка VPS: `mkdir -p /var/www/zoiten-uploads/support/` + nginx alias (манёвр выносим в deployment-заметку для Plan 08-04).

Purpose: Фундамент для всей милстоуна v1.1 — схема поддерживает все 5 каналов (FEEDBACK/QUESTION/CHAT/RETURN/MESSENGER), чтобы не делать повторную миграцию в Phase 9-12. API-клиент с изоляцией в lib/ — паттерн wb-api.ts. Тесты-стабы обеспечивают Nyquist compliance для последующих планов.

Output: Обновлённая схема БД, новый модуль lib/wb-support-api.ts, 7 тест-файлов (1 полный + 6 stub), миграция prisma/migrations/<ts>_support_mvp/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-support-mvp/08-CONTEXT.md
@.planning/phases/08-support-mvp/08-RESEARCH.md
@.planning/phases/08-support-mvp/08-VALIDATION.md
@CLAUDE.md
@prisma/schema.prisma
@lib/wb-api.ts
@lib/prisma.ts
@lib/rbac.ts

<interfaces>
Из lib/prisma.ts:
```typescript
export const prisma: PrismaClient
```

Из lib/rbac.ts (уже существует):
```typescript
export async function requireSection(section: ERP_SECTION, minRole?: "VIEW" | "MANAGE"): Promise<Session>
```

Из prisma/schema.prisma существующие модели (к ним добавим обратные relations):
```prisma
model User {
  id    String @id
  email String @unique
  // добавить в Plan 08-01:
  // assignedTickets  SupportTicket[]  @relation("SupportAssignee")
  // authoredMessages SupportMessage[] @relation("SupportAuthor")
}
model WbCard {
  nmId Int @unique  // связь с SupportTicket через nmId (не FK)
}
```

Паттерн vitest в проекте (из Phase 7 — tests/wb-promotions-api.test.ts):
- import { describe, it, expect, vi, beforeEach } from "vitest"
- vi.stubGlobal("fetch", vi.fn()) для мока
- npm run test — запуск vitest run (не watch)

WB API endpoints (VERIFIED в 08-RESEARCH.md):
- Base URL: https://feedbacks-api.wildberries.ru
- Auth: header `Authorization: ${WB_API_TOKEN}` (без Bearer, паттерн проекта)
- GET /api/v1/feedbacks?isAnswered=false&take=5000&skip=0
- POST /api/v1/feedbacks/answer body {id: string, text: string}
- PATCH /api/v1/feedbacks/answer body {id: string, text: string}
- GET /api/v1/questions?isAnswered=false&take=10000&skip=0
- PATCH /api/v1/questions body {id: string, answer: {text: string}, state: "wbRu"}
- 429 handling: read header X-Ratelimit-Retry (в секундах) → sleep(value*1000) → retry 1 раз
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: Prisma миграция — 4 модели + 5 enum + обратные relations</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (текущая схема — смотреть существующие enum и модели User/WbCard, не ломать)
    - .planning/phases/08-support-mvp/08-RESEARCH.md (разделы «Prisma миграция» строки 272-417 — финальная схема, критические детали)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «Модель данных (Prisma schema)» и LOCKED decisions)
    - CLAUDE.md (раздел «Связи между таблицами БД» — паттерн именованных relations, onDelete)
  </read_first>
  <action>
    Открыть `prisma/schema.prisma` и добавить **в конец файла** (после всех существующих моделей):

    **1. Пять enum:**
    ```prisma
    enum TicketChannel {
      FEEDBACK   // отзыв WB (Phase 8)
      QUESTION   // вопрос WB (Phase 8)
      CHAT       // Phase 10
      RETURN     // Phase 9
      MESSENGER  // Phase 12
    }

    enum TicketStatus {
      NEW
      IN_PROGRESS
      ANSWERED
      CLOSED
      APPEALED   // Phase 11
    }

    enum AppealStatus {
      NONE
      PENDING
      APPROVED
      REJECTED
    }

    enum Direction {
      INBOUND
      OUTBOUND
    }

    enum MediaType {
      IMAGE
      VIDEO
    }
    ```

    **2. Модель Customer** (создаётся для Phase 12 — в Phase 8 записи не создаём, т.к. WB Feedbacks/Questions API не даёт wbUserId):
    ```prisma
    model Customer {
      id         String          @id @default(cuid())
      wbUserId   String?         @unique
      phone      String?
      name       String?
      note       String?         @db.Text
      createdAt  DateTime        @default(now())
      updatedAt  DateTime        @updatedAt
      tickets    SupportTicket[]
    }
    ```

    **3. Модель SupportTicket** — КРИТИЧНО `wbExternalId: String?` (не Int — WB возвращает cuid-like строки):
    ```prisma
    model SupportTicket {
      id             String           @id @default(cuid())
      channel        TicketChannel
      wbExternalId   String?          // String! 20-символьный Elasticsearch-подобный ID из WB
      customerId     String?
      customer       Customer?        @relation(fields: [customerId], references: [id], onDelete: SetNull)
      nmId           Int?             // связь с WbCard через nmId (не FK — паттерн проекта)
      status         TicketStatus     @default(NEW)
      assignedToId   String?
      assignedTo     User?            @relation("SupportAssignee", fields: [assignedToId], references: [id], onDelete: SetNull)
      rating         Int?             // productValuation 1-5 (только FEEDBACK)
      appealStatus   AppealStatus?    // Phase 11
      appealId       String?          // Phase 11
      lastMessageAt  DateTime?
      previewText    String?          @db.Text  // первые 140 символов последнего INBOUND
      createdAt      DateTime         @default(now())
      updatedAt      DateTime         @updatedAt
      resolvedAt     DateTime?
      messages       SupportMessage[]
      @@unique([channel, wbExternalId])
      @@index([status])
      @@index([channel])
      @@index([nmId])
      @@index([assignedToId])
      @@index([createdAt])
    }
    ```

    **4. Модель SupportMessage:**
    ```prisma
    model SupportMessage {
      id          String         @id @default(cuid())
      ticketId    String
      ticket      SupportTicket  @relation(fields: [ticketId], references: [id], onDelete: Cascade)
      direction   Direction
      text        String?        @db.Text
      authorId    String?        // null = покупатель (INBOUND) или WB system (OUTBOUND до того, как ответил менеджер)
      author      User?          @relation("SupportAuthor", fields: [authorId], references: [id], onDelete: SetNull)
      isAutoReply Boolean        @default(false)  // Phase 10
      media       SupportMedia[]
      sentAt      DateTime       @default(now())
      wbSentAt    DateTime?      // createdDate из WB API
      @@index([ticketId, sentAt])
    }
    ```

    **5. Модель SupportMedia:**
    ```prisma
    model SupportMedia {
      id         String         @id @default(cuid())
      messageId  String
      message    SupportMessage @relation(fields: [messageId], references: [id], onDelete: Cascade)
      type       MediaType
      wbUrl      String         @db.Text
      localPath  String?        @db.Text  // /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}
      sizeBytes  Int?
      createdAt  DateTime       @default(now())
      expiresAt  DateTime       // createdAt + 1 год
      @@index([expiresAt])
    }
    ```

    **6. ОБРАТНЫЕ RELATIONS в существующих моделях** (ОБЯЗАТЕЛЬНО, иначе Prisma fail на именованных relations):

    Найти в schema.prisma `model User` и **добавить внутрь** (в конец полей, перед `@@`-директивами если есть):
    ```prisma
      assignedTickets   SupportTicket[]  @relation("SupportAssignee")
      authoredMessages  SupportMessage[] @relation("SupportAuthor")
    ```

    **НЕ** добавлять обратный relation в `WbCard` — связь с SupportTicket идёт через nmId без FK (паттерн проекта, см. CLAUDE.md).

    **7. Запустить миграцию** (локально, затем на VPS отдельной задачей deploy):
    ```bash
    npx prisma format
    npx prisma validate
    npx prisma migrate dev --name support_mvp
    npx prisma generate
    ```

    Если локальная БД отсутствует (паттерн проекта, см. Phase 1 decision «Migration marked pending (no local PostgreSQL)»), то выполнить **только** `npx prisma format && npx prisma validate` + зафиксировать SQL из `prisma/migrations/<ts>_support_mvp/migration.sql` (сгенерировать через `npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-empty --script > /tmp/dryrun.sql` для ревью). Применение `migrate deploy` — в Plan 08-04 на VPS через `deploy.sh`.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma format --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^model SupportTicket\b" prisma/schema.prisma` == 1
    - `grep -c "^model SupportMessage\b" prisma/schema.prisma` == 1
    - `grep -c "^model SupportMedia\b" prisma/schema.prisma` == 1
    - `grep -c "^model Customer\b" prisma/schema.prisma` == 1
    - `grep -c "^enum TicketChannel\b" prisma/schema.prisma` == 1
    - `grep "wbExternalId\s*String?" prisma/schema.prisma` находит строку (String, не Int)
    - `grep "@@unique(\[channel, wbExternalId\])" prisma/schema.prisma` находит строку
    - `grep "SupportAssignee" prisma/schema.prisma | wc -l` >= 2 (forward + reverse relation)
    - `grep "SupportAuthor" prisma/schema.prisma | wc -l` >= 2
    - `npx prisma validate` exit 0
    - `npx prisma format --check` exit 0
  </acceptance_criteria>
  <done>
    prisma/schema.prisma содержит 4 новые модели, 5 новых enum, `wbExternalId: String?`, composite unique `@@unique([channel, wbExternalId])`, обратные relations в User; `npx prisma validate` и `npx prisma format --check` проходят без ошибок.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: lib/wb-support-api.ts — клиент WB Feedbacks + Questions API + unit-тесты</name>
  <files>lib/wb-support-api.ts, tests/wb-support-api.test.ts</files>
  <read_first>
    - .planning/phases/08-support-mvp/08-RESEARCH.md (разделы «WB Feedbacks + Questions API» строки 83-270 — VERIFIED endpoints, response schemas, rate limits)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «WB Feedbacks/Questions API»)
    - lib/wb-api.ts (паттерн клиента: getToken() + fetch + типизированные ответы)
    - CLAUDE.md (раздел «WB API токен» — Authorization без Bearer)
    - tests/wb-promotions-api.test.ts (если существует — паттерн vitest mock fetch)
    - vitest.config.ts (корневой конфиг — alias @)
  </read_first>
  <behavior>
    - listFeedbacks({isAnswered, take, skip, dateFrom?, dateTo?}) → массив Feedback, корректно строит URL `/api/v1/feedbacks?isAnswered=false&take=5000&skip=0`, шлёт header Authorization
    - listQuestions({isAnswered, take, skip}) → массив Question, строит URL `/api/v1/questions?...`, распаковывает из `{data: [...]}` если ответ обёрнут
    - replyFeedback(id, text) → POST `/api/v1/feedbacks/answer` body `{id, text}` → возвращает { ok: true } при 200-204
    - editFeedbackAnswer(id, text) → PATCH `/api/v1/feedbacks/answer` body `{id, text}` → возвращает { ok: true } при 200-204
    - replyQuestion(id, text) → PATCH `/api/v1/questions` body `{id, answer: {text}, state: "wbRu"}` → возвращает { ok: true } при 200-204
    - При 429 с header `X-Ratelimit-Retry: 5` → sleep(5000ms) → retry 1 раз → при втором 429 throw Error
    - При 429 без header → fallback sleep(6000ms) → retry 1 раз
    - При 401/403 → throw Error с русским сообщением «Неверный токен WB API» / «Нет доступа — проверьте scope токена (bit 5)»
    - При network error → throw Error с русским сообщением «Ошибка сети при обращении к WB API»
    - Все функции типизированы (exported TS types: Feedback, Question, Photo, Video, ProductDetails, Answer)
    - getToken() — повторяет паттерн wb-api.ts: `process.env.WB_API_TOKEN` + throw если пусто
  </behavior>
  <action>
    **Шаг A — создать `lib/wb-support-api.ts`:**

    Использовать паттерн `lib/wb-api.ts` (константы Base URL, getToken(), fetch + error handling).

    ```typescript
    // lib/wb-support-api.ts
    // Клиент WB Feedbacks + Questions API.
    // Scope токена: bit 5 (Отзывы). Не использует curl — Feedbacks/Questions не блокируются TLS fingerprint.

    const FEEDBACKS_API = "https://feedbacks-api.wildberries.ru"
    const RATE_LIMIT_FALLBACK_MS = 6000

    function getToken(): string {
      const token = process.env.WB_API_TOKEN
      if (!token) throw new Error("WB_API_TOKEN не настроен")
      return token
    }

    // ── Типы ответов ──────────────────────────────────────────────

    export interface PhotoLink { fullSize: string; miniSize: string }
    export interface FeedbackVideo { previewImage: string; link: string; durationSec: number }
    export interface ProductDetails { imtId: number; nmId: number; productName: string; supplierArticle: string; brandName: string; size?: string }
    export interface FeedbackAnswer { text: string; state: string; editable: boolean; createDate?: string }

    export interface Feedback {
      id: string
      text: string
      pros?: string
      cons?: string
      productValuation: number   // 1..5
      createdDate: string
      state: string              // "wbRu" | "none"
      answer: FeedbackAnswer | null
      productDetails: ProductDetails
      photoLinks: PhotoLink[]
      video: FeedbackVideo | null
    }

    export interface Question {
      id: string
      text: string
      createdDate: string
      state: string
      answer: FeedbackAnswer | null
      productDetails: ProductDetails
    }

    export interface ListParams { isAnswered?: boolean; take: number; skip: number; dateFrom?: number; dateTo?: number }

    // ── Внутренний helper с 429 retry ─────────────────────────────

    async function callWb(path: string, init: RequestInit, attempt = 0): Promise<Response> {
      const token = getToken()
      const res = await fetch(`${FEEDBACKS_API}${path}`, {
        ...init,
        headers: { Authorization: token, "Content-Type": "application/json", ...(init.headers ?? {}) },
      })
      if (res.status === 429 && attempt === 0) {
        const retry = Number(res.headers.get("X-Ratelimit-Retry")) || 0
        const waitMs = retry > 0 ? retry * 1000 : RATE_LIMIT_FALLBACK_MS
        await new Promise((r) => setTimeout(r, waitMs))
        return callWb(path, init, 1)
      }
      if (res.status === 401) throw new Error("Неверный токен WB API")
      if (res.status === 403) throw new Error("Нет доступа — проверьте scope токена (bit 5)")
      if (!res.ok) {
        const body = await res.text().catch(() => "")
        throw new Error(`WB API ${res.status}: ${body.slice(0, 200)}`)
      }
      return res
    }

    // ── Feedbacks ─────────────────────────────────────────────────

    export async function listFeedbacks(p: ListParams): Promise<Feedback[]> {
      const qs = new URLSearchParams()
      if (p.isAnswered !== undefined) qs.set("isAnswered", String(p.isAnswered))
      qs.set("take", String(p.take))
      qs.set("skip", String(p.skip))
      if (p.dateFrom) qs.set("dateFrom", String(p.dateFrom))
      if (p.dateTo) qs.set("dateTo", String(p.dateTo))
      qs.set("order", "dateDesc")
      const res = await callWb(`/api/v1/feedbacks?${qs}`, { method: "GET" })
      const json = await res.json() as { data?: { feedbacks?: Feedback[] }; feedbacks?: Feedback[] }
      return json.data?.feedbacks ?? json.feedbacks ?? []
    }

    export async function replyFeedback(id: string, text: string): Promise<{ ok: true }> {
      await callWb("/api/v1/feedbacks/answer", { method: "POST", body: JSON.stringify({ id, text }) })
      return { ok: true }
    }

    export async function editFeedbackAnswer(id: string, text: string): Promise<{ ok: true }> {
      await callWb("/api/v1/feedbacks/answer", { method: "PATCH", body: JSON.stringify({ id, text }) })
      return { ok: true }
    }

    // ── Questions ────────────────────────────────────────────────

    export async function listQuestions(p: ListParams): Promise<Question[]> {
      const qs = new URLSearchParams()
      if (p.isAnswered !== undefined) qs.set("isAnswered", String(p.isAnswered))
      qs.set("take", String(p.take))
      qs.set("skip", String(p.skip))
      const res = await callWb(`/api/v1/questions?${qs}`, { method: "GET" })
      const json = await res.json() as { data?: { questions?: Question[] }; questions?: Question[] }
      return json.data?.questions ?? json.questions ?? []
    }

    export async function replyQuestion(id: string, text: string): Promise<{ ok: true }> {
      await callWb("/api/v1/questions", {
        method: "PATCH",
        body: JSON.stringify({ id, answer: { text }, state: "wbRu" }),
      })
      return { ok: true }
    }
    ```

    **Шаг B — создать `tests/wb-support-api.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    const ORIGINAL_ENV = process.env.WB_API_TOKEN

    beforeEach(() => {
      process.env.WB_API_TOKEN = "test-token"
      vi.stubGlobal("fetch", vi.fn())
    })

    afterEach(() => {
      process.env.WB_API_TOKEN = ORIGINAL_ENV
      vi.unstubAllGlobals()
    })

    function mockResponse(body: unknown, status = 200, headers: Record<string,string> = {}): Response {
      return new Response(JSON.stringify(body), { status, headers: { "Content-Type": "application/json", ...headers } })
    }

    describe("listFeedbacks", () => {
      it("строит корректный URL с query params и шлёт Authorization header", async () => {
        const { listFeedbacks } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
          mockResponse({ data: { feedbacks: [] } })
        )
        await listFeedbacks({ isAnswered: false, take: 5000, skip: 0 })
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("/api/v1/feedbacks?")
        expect(call[0]).toContain("isAnswered=false")
        expect(call[0]).toContain("take=5000")
        expect(call[1].headers.Authorization).toBe("test-token")
      })

      it("парсит response в формате {data: {feedbacks: [...]}}", async () => {
        const { listFeedbacks } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(
          mockResponse({ data: { feedbacks: [{ id: "X", text: "t", productValuation: 5, createdDate: "", state: "wbRu", answer: null, productDetails: { imtId:1, nmId:2, productName:"", supplierArticle:"", brandName:"" }, photoLinks: [], video: null }] } })
        )
        const res = await listFeedbacks({ take: 10, skip: 0 })
        expect(res).toHaveLength(1)
        expect(res[0].id).toBe("X")
      })

      it("ретраит при 429 с заголовком X-Ratelimit-Retry", async () => {
        const { listFeedbacks } = await import("@/lib/wb-support-api")
        const fetchMock = globalThis.fetch as ReturnType<typeof vi.fn>
        fetchMock.mockResolvedValueOnce(mockResponse({}, 429, { "X-Ratelimit-Retry": "1" }))
        fetchMock.mockResolvedValueOnce(mockResponse({ data: { feedbacks: [] } }))
        const res = await listFeedbacks({ take: 10, skip: 0 })
        expect(res).toEqual([])
        expect(fetchMock).toHaveBeenCalledTimes(2)
      }, 10000)

      it("кидает русскую ошибку при 401", async () => {
        const { listFeedbacks } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}, 401))
        await expect(listFeedbacks({ take: 10, skip: 0 })).rejects.toThrow("Неверный токен WB API")
      })
    })

    describe("replyFeedback", () => {
      it("шлёт POST /api/v1/feedbacks/answer с body {id, text}", async () => {
        const { replyFeedback } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await replyFeedback("ABC", "Спасибо!")
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("/api/v1/feedbacks/answer")
        expect(call[1].method).toBe("POST")
        expect(JSON.parse(call[1].body)).toEqual({ id: "ABC", text: "Спасибо!" })
      })
    })

    describe("replyQuestion", () => {
      it("шлёт PATCH /api/v1/questions с body {id, answer:{text}, state:\"wbRu\"}", async () => {
        const { replyQuestion } = await import("@/lib/wb-support-api")
        ;(globalThis.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce(mockResponse({}))
        await replyQuestion("Q1", "Будет")
        const call = (globalThis.fetch as ReturnType<typeof vi.fn>).mock.calls[0]
        expect(call[0]).toContain("/api/v1/questions")
        expect(call[1].method).toBe("PATCH")
        expect(JSON.parse(call[1].body)).toEqual({ id: "Q1", answer: { text: "Будет" }, state: "wbRu" })
      })
    })
    ```

    **Запуск:** `npm run test -- tests/wb-support-api.test.ts` — должен быть GREEN.
  </action>
  <verify>
    <automated>npm run test -- tests/wb-support-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/wb-support-api.ts` (exit 0)
    - `grep -c "export async function listFeedbacks" lib/wb-support-api.ts` == 1
    - `grep -c "export async function replyFeedback" lib/wb-support-api.ts` == 1
    - `grep -c "export async function editFeedbackAnswer" lib/wb-support-api.ts` == 1
    - `grep -c "export async function listQuestions" lib/wb-support-api.ts` == 1
    - `grep -c "export async function replyQuestion" lib/wb-support-api.ts` == 1
    - `grep "X-Ratelimit-Retry" lib/wb-support-api.ts` находит обработку заголовка
    - `grep "feedbacks-api.wildberries.ru" lib/wb-support-api.ts` находит base URL
    - `test -f tests/wb-support-api.test.ts` (exit 0)
    - `npm run test -- tests/wb-support-api.test.ts` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    lib/wb-support-api.ts экспортирует 5 методов (listFeedbacks, replyFeedback, editFeedbackAnswer, listQuestions, replyQuestion) + типы; tests/wb-support-api.test.ts содержит как минимум 5 test cases (URL/headers, parsing, 429 retry, 401 error, reply body format); `npm run test -- tests/wb-support-api.test.ts` exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Wave 0 — 6 stub-файлов для downstream планов + VPS заметка</name>
  <files>
    tests/support-sync.test.ts,
    tests/support-cron.test.ts,
    tests/support-media-download.test.ts,
    tests/support-media-cleanup.test.ts,
    tests/support-badge.test.ts,
    tests/support-actions.test.ts
  </files>
  <read_first>
    - .planning/phases/08-support-mvp/08-VALIDATION.md (раздел «Wave 0 Requirements» — список 6 stub-файлов с описаниями)
    - tests/wb-support-api.test.ts (созданный в задаче 2 — паттерн vitest)
  </read_first>
  <action>
    Создать 6 stub-файлов (каждый с пустым placeholder-тестом, чтобы `npm run test` не падал).

    **Шаблон для каждого файла:**
    ```typescript
    import { describe, it } from "vitest"

    describe("<название> (Wave 0 stub — Plan 08-XX will implement)", () => {
      it.skip("placeholder — тесты добавит Plan 08-XX", () => {
        // Реализация в Plan 08-XX задача Y
      })
    })
    ```

    **Конкретные файлы (с правильными referrer-планами и описаниями):**

    **1. `tests/support-sync.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-sync (Wave 0 stub — Plan 08-02 will implement)", () => {
      it.skip("упорядочивает feedbacks и questions, идемпотентный upsert по (channel, wbExternalId)", () => {
        // Реализация: Plan 08-02 — integration-тесты с mock WB + real Prisma
      })
      it.skip("скачивает медиа локально и создаёт SupportMedia записи", () => {
        // Реализация: Plan 08-02
      })
    })
    ```

    **2. `tests/support-cron.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-cron (Wave 0 stub — Plan 08-02 will implement)", () => {
      it.skip("возвращает 401 если x-cron-secret header отсутствует", () => {
        // Реализация: Plan 08-02
      })
      it.skip("возвращает 401 если x-cron-secret не совпадает с CRON_SECRET", () => {
        // Реализация: Plan 08-02
      })
    })
    ```

    **3. `tests/support-media-download.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-media-download (Wave 0 stub — Plan 08-02 will implement)", () => {
      it.skip("скачивает медиа в /var/www/zoiten-uploads/support/{ticketId}/{messageId}/{filename}", () => {
        // Реализация: Plan 08-02
      })
      it.skip("retry при network error (max attempts=1)", () => {
        // Реализация: Plan 08-02
      })
    })
    ```

    **4. `tests/support-media-cleanup.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-media-cleanup (Wave 0 stub — Plan 08-02 will implement)", () => {
      it.skip("удаляет SupportMedia записи где expiresAt < now()", () => {
        // Реализация: Plan 08-02
      })
      it.skip("игнорирует ENOENT при fs.unlink", () => {
        // Реализация: Plan 08-02
      })
    })
    ```

    **5. `tests/support-badge.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-badge (Wave 0 stub — Plan 08-03 will implement)", () => {
      it.skip("count SupportTicket where status=NEW", () => {
        // Реализация: Plan 08-03
      })
    })
    ```

    **6. `tests/support-actions.test.ts`:**
    ```typescript
    import { describe, it } from "vitest"

    describe("support-actions (Wave 0 stub — Plan 08-04 will implement)", () => {
      it.skip("replyToTicket: шлёт ответ в WB API и создаёт OUTBOUND SupportMessage", () => {
        // Реализация: Plan 08-04
      })
      it.skip("assignTicket: обновляет assignedToId + revalidatePath", () => {
        // Реализация: Plan 08-04
      })
      it.skip("updateTicketStatus: переводит NEW → IN_PROGRESS → ANSWERED → CLOSED", () => {
        // Реализация: Plan 08-04
      })
    })
    ```

    **VPS заметка** (не отдельный файл, добавить в SUMMARY после выполнения):
    > Для Plan 08-04 потребуется на VPS:
    > ```bash
    > ssh root@85.198.97.89 "mkdir -p /var/www/zoiten-uploads/support/ && chown www-data:www-data /var/www/zoiten-uploads/support/"
    > ```
    > Эту команду выполнит Plan 08-04 в финальной задаче (deployment).

    **Запуск full suite для проверки:** `npm run test` — все stub-сюиты должны помечаться как skipped, не failed.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/support-sync.test.ts` (exit 0)
    - `test -f tests/support-cron.test.ts` (exit 0)
    - `test -f tests/support-media-download.test.ts` (exit 0)
    - `test -f tests/support-media-cleanup.test.ts` (exit 0)
    - `test -f tests/support-badge.test.ts` (exit 0)
    - `test -f tests/support-actions.test.ts` (exit 0)
    - `grep -l "it.skip" tests/support-sync.test.ts tests/support-cron.test.ts tests/support-media-download.test.ts tests/support-media-cleanup.test.ts tests/support-badge.test.ts tests/support-actions.test.ts | wc -l` == 6
    - `npm run test` exit 0 (все stub-ы skipped, не failed; тесты wb-support-api.test.ts — passed)
  </acceptance_criteria>
  <done>
    6 stub-файлов созданы с it.skip placeholder-ами, `npm run test` exit 0 (passed + skipped, 0 failed), ссылки на downstream-планы зафиксированы в describe-блоках.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npx prisma validate` exit 0
2. `npx prisma format --check` exit 0
3. `npm run test` exit 0 (wb-support-api.test.ts GREEN + 6 stubs SKIPPED, 0 FAILED)
4. `npx tsc --noEmit` exit 0 (типы lib/wb-support-api.ts корректны)
5. `grep -c "model SupportTicket" prisma/schema.prisma` == 1
6. `grep -c "export async function" lib/wb-support-api.ts` >= 5
</verification>

<success_criteria>
Plan 08-01 завершён когда:
- Prisma schema содержит 4 новые модели + 5 enum + обратные relations в User, `npx prisma validate` passes
- lib/wb-support-api.ts экспортирует listFeedbacks/replyFeedback/editFeedbackAnswer/listQuestions/replyQuestion с типами
- tests/wb-support-api.test.ts покрывает URL/headers, parsing, 429 retry, 401 error, reply body format — все GREEN
- 6 stub-файлов тестов созданы (it.skip), `npm run test` 0 failed
- Commit создан с сообщением `feat(08-01): БД + WB support API клиент + RBAC foundation`
</success_criteria>

<output>
После завершения создать `.planning/phases/08-support-mvp/08-01-SUMMARY.md` с:
- Изменения schema.prisma (список моделей/enum, миграция <ts>_support_mvp)
- Новые методы lib/wb-support-api.ts и их контракты
- Команда VPS для Plan 08-04: `mkdir -p /var/www/zoiten-uploads/support/`
- Задача для deploy-фазы: `npx prisma migrate deploy` на VPS
</output>
