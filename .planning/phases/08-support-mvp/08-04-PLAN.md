---
phase: 08-support-mvp
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/(dashboard)/support/[ticketId]/page.tsx
  - app/actions/support.ts
  - components/support/SupportDialog.tsx
  - components/support/ReplyPanel.tsx
  - components/support/SupportSyncButton.tsx
  - components/support/TicketSidePanel.tsx
  - app/(dashboard)/support/page.tsx
  - tests/support-actions.test.ts
autonomous: true
requirements:
  - SUP-13
  - SUP-14
  - SUP-15
  - SUP-16
partial_requirements:
  - SUP-14  # Phase 8: ответ только для каналов FEEDBACK и QUESTION. Кнопки возврата (Approve/Reject/Reconsider) → Phase 9. Отправка в чат → Phase 10. Кнопка обжалования → Phase 11.
must_haves:
  truths:
    - "Страница /support/[ticketId] открывает 3-колоночный диалог (покупатель+товар слева, хронология по центру, статус+менеджер+мета справа)"
    - "Менеджер вводит текст в textarea и жмёт «Отправить» → ответ уходит в WB API, создаётся OUTBOUND SupportMessage, статус тикета → ANSWERED"
    - "Менеджер меняет статус тикета через native <select>: NEW → IN_PROGRESS → ANSWERED → CLOSED"
    - "Менеджер назначает менеджера из списка пользователей с доступом SUPPORT"
    - "Кнопка «Синхронизировать» на /support и /support/[ticketId] вызывает POST /api/support-sync с toast loading/success/error"
    - "Server action replyToTicket работает ТОЛЬКО для каналов FEEDBACK и QUESTION. Для CHAT возвращает ошибку «Чат будет добавлен в Phase 10». Для RETURN и MESSENGER — «Возвраты и мессенджеры будут добавлены позже»."
  artifacts:
    - path: "app/(dashboard)/support/[ticketId]/page.tsx"
      provides: "RSC диалоговая страница"
      contains: "requireSection(\"SUPPORT\")"
    - path: "app/actions/support.ts"
      provides: "replyToTicket, assignTicket, updateTicketStatus server actions"
      exports: ["replyToTicket", "assignTicket", "updateTicketStatus"]
    - path: "components/support/SupportSyncButton.tsx"
      provides: "Клиентская кнопка sync с toast"
      exports: ["SupportSyncButton"]
    - path: "components/support/ReplyPanel.tsx"
      provides: "Sticky textarea + Send"
      exports: ["ReplyPanel"]
  key_links:
    - from: "app/actions/support.ts replyToTicket"
      to: "lib/wb-support-api.ts (replyFeedback/replyQuestion)"
      via: "import"
      pattern: "from\\s+\"@/lib/wb-support-api\""
    - from: "app/actions/support.ts"
      to: "requireSection(\"SUPPORT\", \"MANAGE\")"
      via: "RBAC guard в начале каждого действия"
      pattern: "requireSection\\(\"SUPPORT\",\\s*\"MANAGE\"\\)"
    - from: "components/support/SupportSyncButton.tsx"
      to: "POST /api/support-sync"
      via: "fetch с router.refresh() после"
      pattern: "/api/support-sync"
    - from: "app/(dashboard)/support/page.tsx"
      to: "components/support/SupportSyncButton.tsx"
      via: "import и вставка в header"
      pattern: "SupportSyncButton"
---

<objective>
Phase 8 Plan 04 — диалог + server actions + Sync-кнопка:
1. RSC страница `app/(dashboard)/support/[ticketId]/page.tsx` — 3-колоночный layout, хронологическая лента сообщений, превью медиа.
2. Server actions `app/actions/support.ts`: `replyToTicket(ticketId, text)`, `assignTicket(ticketId, userId|null)`, `updateTicketStatus(ticketId, status)` с `requireSection("SUPPORT", "MANAGE")`.
3. Клиентские компоненты: `SupportDialog` (3-col grid), `ReplyPanel` (sticky textarea + submit), `TicketSidePanel` (status/assign select), `SupportSyncButton` (toast loading/success/error).
4. Интеграция: `SupportSyncButton` монтируется на `/support` (шапка) и `/support/[ticketId]` (шапка диалога).
5. Integration-тесты server actions: `tests/support-actions.test.ts` (mock WB API + mock Prisma).
6. VPS deployment task (manual): `mkdir -p /var/www/zoiten-uploads/support/ && chown www-data:www-data /var/www/zoiten-uploads/support/` — в SUMMARY.

Purpose: Финальный UX-слой Phase 8 — менеджер может полностью работать с отзывом/вопросом без выхода из ERP. Все write-операции защищены RBAC и обёрнуты в revalidatePath для мгновенного обновления.

Output: 1 RSC-страница, 1 server actions файл, 4 новых клиент-компонента, 1 обновлённая /support/page.tsx (вставка SupportSyncButton), 1 заполненный тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-support-mvp/08-CONTEXT.md
@.planning/phases/08-support-mvp/08-RESEARCH.md
@.planning/phases/08-support-mvp/08-01-SUMMARY.md
@.planning/phases/08-support-mvp/08-02-SUMMARY.md
@CLAUDE.md
@lib/wb-support-api.ts
@lib/rbac.ts
@lib/prisma.ts
@app/actions/products.ts
@app/actions/users.ts

<interfaces>
Из lib/wb-support-api.ts (Plan 08-01):
```typescript
export async function replyFeedback(id: string, text: string): Promise<{ ok: true }>
export async function editFeedbackAnswer(id: string, text: string): Promise<{ ok: true }>
export async function replyQuestion(id: string, text: string): Promise<{ ok: true }>
```

Prisma (Plan 08-01):
```prisma
model SupportTicket {
  id String; channel TicketChannel; wbExternalId String?
  status TicketStatus; assignedToId String?
  messages SupportMessage[]; customer Customer?
  nmId Int?
}
model SupportMessage { direction Direction; text String?; authorId String?; media SupportMedia[]; sentAt DateTime; wbSentAt DateTime? }
model SupportMedia { type MediaType; wbUrl String; localPath String? }
```

Паттерн server actions (из app/actions/users.ts, app/actions/products.ts):
```typescript
"use server"
import { revalidatePath } from "next/cache"
import { prisma } from "@/lib/prisma"
import { requireSection } from "@/lib/rbac"

export async function myAction(input: InputShape): Promise<{ok: true} | {ok: false; error: string}> {
  try {
    const session = await requireSection("SUPPORT", "MANAGE")
    // ... prisma work
    revalidatePath("/support")
    return { ok: true }
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : "Ошибка операции" }
  }
}
```

Паттерн toast (проект использует sonner):
```typescript
import { toast } from "sonner"
const id = toast.loading("Синхронизация WB...")
try {
  const res = await fetch(...)
  toast.success("Готово", { id })
} catch { toast.error("Ошибка", { id }) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: app/actions/support.ts — 3 server actions + integration тест</name>
  <files>app/actions/support.ts, tests/support-actions.test.ts</files>
  <read_first>
    - app/actions/products.ts (паттерн requireSection + try/catch + revalidatePath)
    - app/actions/users.ts (паттерн return type ok/error)
    - lib/wb-support-api.ts (Plan 08-01 — сигнатуры reply функций)
    - lib/rbac.ts (requireSection)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «Ответ отправил — что потом» строки 208-218)
    - tests/support-actions.test.ts (Wave 0 stub — заменить)
    - lib/rbac.ts — проверить возвращаемый тип requireSection() (ожидается Session с user.id: string как обязательным)
    - lib/auth.ts — проверить callbacks jwt/session что id устанавливается из token
    - types/next-auth.d.ts — проверить augmentation что session.user.id: string (не string | undefined)
  </read_first>
  <behavior>
    - replyToTicket(ticketId: string, text: string):
      - RBAC SUPPORT+MANAGE
      - Находит тикет по id. Если не найден → ok:false "Тикет не найден"
      - Если text пустой/только пробелы → ok:false "Пустой ответ"
      - В зависимости от channel:
        - FEEDBACK → вызов replyFeedback(wbExternalId, text)
        - QUESTION → вызов replyQuestion(wbExternalId, text)
        - иначе → ok:false "Канал не поддерживает ответ в Phase 8"
      - При успехе: создать SupportMessage {direction:OUTBOUND, text, authorId:session.user.id, wbSentAt:now()}; обновить ticket {status:ANSWERED, resolvedAt:now(), lastMessageAt:now()}
      - revalidatePath("/support") и revalidatePath(`/support/${ticketId}`)
      - При ошибке WB API: не создавать message, не менять статус, возвращать ok:false с сообщением ошибки (русский)
    - assignTicket(ticketId, userId|null):
      - RBAC SUPPORT+MANAGE
      - Обновляет assignedToId (userId или null для снятия назначения)
      - revalidatePath обоих путей
    - updateTicketStatus(ticketId, status):
      - RBAC SUPPORT+MANAGE
      - Принимает только NEW | IN_PROGRESS | ANSWERED | CLOSED (APPEALED отдельно в Phase 11)
      - Обновляет status + resolvedAt = (status==ANSWERED||CLOSED) ? now() : null
      - revalidatePath
  </behavior>
  <action>
    **Шаг A — создать `app/actions/support.ts`:**

    ```typescript
    // app/actions/support.ts
    "use server"
    import { revalidatePath } from "next/cache"
    import { prisma } from "@/lib/prisma"
    import { requireSection } from "@/lib/rbac"
    import { replyFeedback, replyQuestion } from "@/lib/wb-support-api"
    import type { TicketStatus } from "@prisma/client"

    export type ActionResult = { ok: true } | { ok: false; error: string }

    const MANUAL_STATUSES: TicketStatus[] = ["NEW", "IN_PROGRESS", "ANSWERED", "CLOSED"]

    export async function replyToTicket(ticketId: string, text: string): Promise<ActionResult> {
      try {
        const session = await requireSection("SUPPORT", "MANAGE")
        if (!session.user.id) throw new Error("Session user.id отсутствует — проверь auth.config.ts")
        if (!text || !text.trim()) return { ok: false, error: "Пустой ответ" }

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: ticketId },
          select: { id: true, channel: true, wbExternalId: true },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (!ticket.wbExternalId) return { ok: false, error: "У тикета нет wbExternalId — ответ через WB невозможен" }

        // WB API call
        try {
          if (ticket.channel === "FEEDBACK") {
            await replyFeedback(ticket.wbExternalId, text)
          } else if (ticket.channel === "QUESTION") {
            await replyQuestion(ticket.wbExternalId, text)
          } else {
            return { ok: false, error: "Канал не поддерживает ответ в Phase 8" }
          }
        } catch (err) {
          const msg = err instanceof Error ? err.message : "Ошибка WB API"
          return { ok: false, error: `Ошибка WB API: ${msg}` }
        }

        // Local DB update
        const now = new Date()
        await prisma.$transaction([
          prisma.supportMessage.create({
            data: {
              ticketId: ticket.id,
              direction: "OUTBOUND",
              text,
              authorId: session.user.id,
              wbSentAt: now,
              sentAt: now,
            },
          }),
          prisma.supportTicket.update({
            where: { id: ticket.id },
            data: { status: "ANSWERED", resolvedAt: now, lastMessageAt: now },
          }),
        ])

        revalidatePath("/support")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка отправки ответа" }
      }
    }

    export async function assignTicket(ticketId: string, userId: string | null): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        await prisma.supportTicket.update({
          where: { id: ticketId },
          data: { assignedToId: userId, status: userId ? "IN_PROGRESS" : undefined },
        })
        revalidatePath("/support")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка назначения" }
      }
    }

    export async function updateTicketStatus(ticketId: string, status: TicketStatus): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        if (!MANUAL_STATUSES.includes(status)) {
          return { ok: false, error: "Этот статус нельзя установить вручную" }
        }
        const now = new Date()
        await prisma.supportTicket.update({
          where: { id: ticketId },
          data: {
            status,
            resolvedAt: status === "ANSWERED" || status === "CLOSED" ? now : null,
          },
        })
        revalidatePath("/support")
        revalidatePath(`/support/${ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка обновления статуса" }
      }
    }
    ```

    **Шаг B — заменить `tests/support-actions.test.ts`:**

    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"

    // Mock RBAC
    vi.mock("@/lib/rbac", () => ({
      requireSection: vi.fn().mockResolvedValue({ user: { id: "U1", name: "Test" } }),
    }))

    // Mock next/cache
    vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))

    // Mock WB API
    const replyFeedbackMock = vi.fn()
    const replyQuestionMock = vi.fn()
    vi.mock("@/lib/wb-support-api", () => ({
      replyFeedback: replyFeedbackMock,
      replyQuestion: replyQuestionMock,
    }))

    // Mock Prisma — stateful
    const ticketsState: any[] = []
    const messagesState: any[] = []
    const findUniqueMock = vi.fn()
    const updateMock = vi.fn()
    const createMessageMock = vi.fn()

    vi.mock("@/lib/prisma", () => ({
      prisma: {
        supportTicket: { findUnique: findUniqueMock, update: updateMock },
        supportMessage: { create: createMessageMock },
        $transaction: async (ops: any[]) => Promise.all(ops.map((op) => op)),
      },
    }))

    beforeEach(() => {
      ticketsState.length = 0
      messagesState.length = 0
      findUniqueMock.mockReset()
      updateMock.mockReset().mockResolvedValue({})
      createMessageMock.mockReset().mockResolvedValue({})
      replyFeedbackMock.mockReset().mockResolvedValue({ ok: true })
      replyQuestionMock.mockReset().mockResolvedValue({ ok: true })
    })
    afterEach(() => vi.clearAllMocks())

    describe("replyToTicket", () => {
      it("FEEDBACK: вызывает replyFeedback и создаёт OUTBOUND + обновляет status=ANSWERED", async () => {
        findUniqueMock.mockResolvedValueOnce({ id: "T1", channel: "FEEDBACK", wbExternalId: "WB1" })
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("T1", "Спасибо за отзыв!")
        expect(res).toEqual({ ok: true })
        expect(replyFeedbackMock).toHaveBeenCalledWith("WB1", "Спасибо за отзыв!")
        expect(createMessageMock).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ ticketId: "T1", direction: "OUTBOUND", text: "Спасибо за отзыв!", authorId: "U1" }),
        }))
      })

      it("QUESTION: вызывает replyQuestion", async () => {
        findUniqueMock.mockResolvedValueOnce({ id: "T2", channel: "QUESTION", wbExternalId: "WB2" })
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("T2", "Будет на следующей неделе")
        expect(res).toEqual({ ok: true })
        expect(replyQuestionMock).toHaveBeenCalledWith("WB2", "Будет на следующей неделе")
      })

      it("возвращает ошибку при пустом тексте", async () => {
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("T1", "   ")
        expect(res).toEqual({ ok: false, error: "Пустой ответ" })
        expect(replyFeedbackMock).not.toHaveBeenCalled()
      })

      it("возвращает ошибку если тикет не найден", async () => {
        findUniqueMock.mockResolvedValueOnce(null)
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("missing", "text")
        expect(res).toEqual({ ok: false, error: "Тикет не найден" })
      })

      it("при ошибке WB API не создаёт OUTBOUND message", async () => {
        findUniqueMock.mockResolvedValueOnce({ id: "T1", channel: "FEEDBACK", wbExternalId: "WB1" })
        replyFeedbackMock.mockRejectedValueOnce(new Error("WB API 500"))
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("T1", "text")
        expect(res.ok).toBe(false)
        expect(createMessageMock).not.toHaveBeenCalled()
      })

      it("не поддерживает CHAT/RETURN/MESSENGER в Phase 8", async () => {
        findUniqueMock.mockResolvedValueOnce({ id: "T1", channel: "CHAT", wbExternalId: "WB1" })
        const { replyToTicket } = await import("@/app/actions/support")
        const res = await replyToTicket("T1", "text")
        expect(res).toEqual({ ok: false, error: "Канал не поддерживает ответ в Phase 8" })
      })
    })

    describe("assignTicket", () => {
      it("обновляет assignedToId и status=IN_PROGRESS при назначении", async () => {
        const { assignTicket } = await import("@/app/actions/support")
        const res = await assignTicket("T1", "U2")
        expect(res).toEqual({ ok: true })
        expect(updateMock).toHaveBeenCalledWith(expect.objectContaining({
          where: { id: "T1" },
          data: expect.objectContaining({ assignedToId: "U2", status: "IN_PROGRESS" }),
        }))
      })
      it("null снимает назначение без изменения статуса", async () => {
        const { assignTicket } = await import("@/app/actions/support")
        const res = await assignTicket("T1", null)
        expect(res).toEqual({ ok: true })
        expect(updateMock).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ assignedToId: null, status: undefined }),
        }))
      })
    })

    describe("updateTicketStatus", () => {
      it("переводит статус NEW → IN_PROGRESS", async () => {
        const { updateTicketStatus } = await import("@/app/actions/support")
        const res = await updateTicketStatus("T1", "IN_PROGRESS")
        expect(res).toEqual({ ok: true })
        expect(updateMock).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ status: "IN_PROGRESS", resolvedAt: null }),
        }))
      })
      it("ANSWERED выставляет resolvedAt", async () => {
        const { updateTicketStatus } = await import("@/app/actions/support")
        await updateTicketStatus("T1", "ANSWERED")
        const call = updateMock.mock.calls[0][0]
        expect(call.data.status).toBe("ANSWERED")
        expect(call.data.resolvedAt).toBeInstanceOf(Date)
      })
      it("отклоняет APPEALED как manual-статус (резерв Phase 11)", async () => {
        const { updateTicketStatus } = await import("@/app/actions/support")
        const res = await updateTicketStatus("T1", "APPEALED" as any)
        expect(res).toEqual({ ok: false, error: "Этот статус нельзя установить вручную" })
      })
    })
    ```
  </action>
  <verify>
    <automated>npm run test -- tests/support-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/actions/support.ts` (exit 0)
    - `grep "\"use server\"" app/actions/support.ts` находит pragma
    - `grep "export async function replyToTicket" app/actions/support.ts` находит функцию
    - `grep "export async function assignTicket" app/actions/support.ts` находит функцию
    - `grep "export async function updateTicketStatus" app/actions/support.ts` находит функцию
    - `grep -c "requireSection(\"SUPPORT\", \"MANAGE\")" app/actions/support.ts` >= 3 (в каждой функции)
    - `grep "revalidatePath" app/actions/support.ts | wc -l` >= 3
    - `grep "replyFeedback\|replyQuestion" app/actions/support.ts` находит импорт/вызов WB API
    - `npm run test -- tests/support-actions.test.ts` exit 0 (все ~11 тестов GREEN)
  </acceptance_criteria>
  <done>
    3 server actions созданы с RBAC SUPPORT+MANAGE, обработкой ошибок (пустой ответ, тикет не найден, WB API fail, unsupported channel), revalidatePath; все тесты GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: Клиентские компоненты диалога — SupportDialog, ReplyPanel, TicketSidePanel, SupportSyncButton</name>
  <files>
    components/support/SupportDialog.tsx,
    components/support/ReplyPanel.tsx,
    components/support/TicketSidePanel.tsx,
    components/support/SupportSyncButton.tsx
  </files>
  <read_first>
    - components/cards/WbSyncButton.tsx (паттерн sync-кнопки с sonner toast)
    - app/actions/support.ts (созданный в задаче 1 — сигнатуры actions)
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «Диалог» строки 66-80, «Ответ отправил — что потом» 208-218)
    - CLAUDE.md (Conventions — native select, sonner)
  </read_first>
  <action>
    **Файл 1: `components/support/SupportSyncButton.tsx`** (клиентская кнопка sync):

    ```tsx
    "use client"
    import { useState } from "react"
    import { useRouter } from "next/navigation"
    import { Button } from "@/components/ui/button"
    import { RefreshCw } from "lucide-react"
    import { toast } from "sonner"

    export function SupportSyncButton() {
      const router = useRouter()
      const [loading, setLoading] = useState(false)

      async function onClick() {
        if (loading) return
        setLoading(true)
        const toastId = toast.loading("Синхронизация с WB...")
        try {
          const res = await fetch("/api/support-sync", { method: "POST" })
          const body = await res.json()
          if (!res.ok || !body.ok) {
            toast.error(body.error ?? "Ошибка синхронизации", { id: toastId })
          } else {
            const summary = `Отзывы: ${body.feedbacksSynced ?? 0}, вопросы: ${body.questionsSynced ?? 0}, медиа: ${body.mediaSaved ?? 0}`
            toast.success(`Готово. ${summary}`, { id: toastId })
            router.refresh()
          }
        } catch (err) {
          toast.error(err instanceof Error ? err.message : "Сеть недоступна", { id: toastId })
        } finally {
          setLoading(false)
        }
      }

      return (
        <Button onClick={onClick} disabled={loading} size="sm" variant="outline">
          <RefreshCw className={`h-4 w-4 mr-2 ${loading ? "animate-spin" : ""}`} />
          Синхронизировать
        </Button>
      )
    }
    ```

    **Файл 2: `components/support/ReplyPanel.tsx`** (sticky textarea + submit):

    ```tsx
    "use client"
    import { useState, useTransition } from "react"
    import { Button } from "@/components/ui/button"
    import { Send } from "lucide-react"
    import { toast } from "sonner"
    import { replyToTicket } from "@/app/actions/support"

    export function ReplyPanel({ ticketId, disabled }: { ticketId: string; disabled?: boolean }) {
      const [text, setText] = useState("")
      const [isPending, startTransition] = useTransition()

      function onSubmit() {
        const trimmed = text.trim()
        if (!trimmed) {
          toast.error("Пустой ответ")
          return
        }
        startTransition(async () => {
          const res = await replyToTicket(ticketId, trimmed)
          if (res.ok) {
            toast.success("Ответ отправлен")
            setText("")
          } else {
            toast.error(res.error)
          }
        })
      }

      return (
        <div className="sticky bottom-0 bg-white dark:bg-neutral-900 border-t p-3 flex items-end gap-2">
          <textarea
            value={text}
            onChange={(e) => setText(e.target.value)}
            placeholder="Введите ответ..."
            disabled={disabled || isPending}
            rows={3}
            className="flex-1 rounded-md border bg-transparent p-2 text-sm resize-none"
          />
          <Button onClick={onSubmit} disabled={disabled || isPending || !text.trim()} size="sm">
            <Send className="h-4 w-4 mr-1" />
            {isPending ? "Отправка..." : "Отправить"}
          </Button>
        </div>
      )
    }
    ```

    **Файл 3: `components/support/TicketSidePanel.tsx`** (правая колонка: native select для status + assignee):

    ```tsx
    "use client"
    import { useTransition } from "react"
    import { toast } from "sonner"
    import { assignTicket, updateTicketStatus } from "@/app/actions/support"
    import type { TicketStatus, TicketChannel } from "@prisma/client"

    interface User { id: string; name: string; firstName: string | null; lastName: string | null }
    interface Props {
      ticketId: string
      channel: TicketChannel
      status: TicketStatus
      assignedToId: string | null
      users: User[]
      createdAt: Date
      lastMessageAt: Date | null
    }

    const STATUS_LABELS: Record<TicketStatus, string> = {
      NEW: "Новый",
      IN_PROGRESS: "В работе",
      ANSWERED: "Отвечен",
      CLOSED: "Закрыт",
      APPEALED: "Обжалование",
    }
    const MANUAL_STATUSES: TicketStatus[] = ["NEW", "IN_PROGRESS", "ANSWERED", "CLOSED"]

    const CHANNEL_LABELS: Record<TicketChannel, string> = {
      FEEDBACK: "Отзыв", QUESTION: "Вопрос", CHAT: "Чат", RETURN: "Возврат", MESSENGER: "Мессенджер",
    }

    function fullName(u: User): string {
      const full = [u.firstName, u.lastName].filter(Boolean).join(" ").trim()
      return full || u.name || u.id.slice(-6)
    }

    function formatDate(d: Date | null): string {
      if (!d) return "—"
      return new Intl.DateTimeFormat("ru-RU", {
        day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit",
        timeZone: "Europe/Moscow",
      }).format(d)
    }

    export function TicketSidePanel({ ticketId, channel, status, assignedToId, users, createdAt, lastMessageAt }: Props) {
      const [isPending, startTransition] = useTransition()

      function onStatusChange(next: TicketStatus) {
        startTransition(async () => {
          const res = await updateTicketStatus(ticketId, next)
          if (res.ok) toast.success("Статус обновлён")
          else toast.error(res.error)
        })
      }
      function onAssigneeChange(val: string) {
        const userId = val === "" ? null : val
        startTransition(async () => {
          const res = await assignTicket(ticketId, userId)
          if (res.ok) toast.success(userId ? "Менеджер назначен" : "Назначение снято")
          else toast.error(res.error)
        })
      }

      return (
        <aside className="space-y-4 text-sm">
          <div>
            <label className="block text-xs text-muted-foreground mb-1">Статус</label>
            <select
              value={status}
              disabled={isPending}
              onChange={(e) => onStatusChange(e.target.value as TicketStatus)}
              className="w-full h-9 rounded-md border bg-transparent px-2"
            >
              {MANUAL_STATUSES.map((s) => <option key={s} value={s}>{STATUS_LABELS[s]}</option>)}
              {/* APPEALED рендерится только для чтения если текущий статус = APPEALED */}
              {status === "APPEALED" && <option value="APPEALED">Обжалование</option>}
            </select>
          </div>
          <div>
            <label className="block text-xs text-muted-foreground mb-1">Менеджер</label>
            <select
              value={assignedToId ?? ""}
              disabled={isPending}
              onChange={(e) => onAssigneeChange(e.target.value)}
              className="w-full h-9 rounded-md border bg-transparent px-2"
            >
              <option value="">— Не назначен —</option>
              {users.map((u) => <option key={u.id} value={u.id}>{fullName(u)}</option>)}
            </select>
          </div>
          <div className="pt-2 border-t space-y-2 text-xs text-muted-foreground">
            <div>Канал: <span className="text-foreground">{CHANNEL_LABELS[channel]}</span></div>
            <div>Создан: <span className="text-foreground">{formatDate(createdAt)}</span></div>
            <div>Последнее сообщение: <span className="text-foreground">{formatDate(lastMessageAt)}</span></div>
          </div>
        </aside>
      )
    }
    ```

    **Файл 4: `components/support/SupportDialog.tsx`** (центральная часть — хронологическая лента сообщений):

    ```tsx
    // components/support/SupportDialog.tsx — Server Component: сообщения из props (RSC data assembly)
    import type { Direction, MediaType } from "@prisma/client"

    interface Media { id: string; type: MediaType; wbUrl: string; localPath: string | null }
    interface Message {
      id: string; direction: Direction; text: string | null
      authorName: string | null // префильтрованное имя из RSC
      sentAt: Date; wbSentAt: Date | null
      media: Media[]
    }

    function formatTime(d: Date): string {
      return new Intl.DateTimeFormat("ru-RU", {
        day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit",
        timeZone: "Europe/Moscow",
      }).format(d)
    }

    function mediaSrc(m: Media): string {
      // nginx отдаёт /var/www/zoiten-uploads/support/ как /uploads/support/
      if (!m.localPath) return m.wbUrl
      const idx = m.localPath.indexOf("/zoiten-uploads/")
      if (idx === -1) return m.wbUrl
      return m.localPath.slice(idx + "/zoiten-uploads".length) // /support/T1/M1/file.jpg → /support/T1/M1/file.jpg  → via /uploads alias
        .replace(/^\//, "/uploads/")
    }

    export function SupportDialog({ messages }: { messages: Message[] }) {
      if (messages.length === 0) {
        return <div className="flex items-center justify-center h-full text-muted-foreground">Нет сообщений</div>
      }
      return (
        <div className="flex-1 overflow-y-auto p-4 space-y-3">
          {messages.map((m) => {
            const isOut = m.direction === "OUTBOUND"
            return (
              <div key={m.id} className={`flex ${isOut ? "justify-end" : "justify-start"}`}>
                <div className={`max-w-[70%] rounded-lg p-3 ${isOut ? "bg-primary text-primary-foreground" : "bg-muted"}`}>
                  <div className="text-[10px] uppercase opacity-70 mb-1">
                    {isOut ? (m.authorName ?? "Менеджер") : "Покупатель"} · {formatTime(m.wbSentAt ?? m.sentAt)}
                  </div>
                  {m.text && <p className="text-sm whitespace-pre-wrap">{m.text}</p>}
                  {m.media.length > 0 && (
                    <div className="mt-2 flex flex-wrap gap-2">
                      {m.media.map((md) => (
                        md.type === "IMAGE" ? (
                          // eslint-disable-next-line @next/next/no-img-element
                          <a key={md.id} href={mediaSrc(md)} target="_blank" rel="noreferrer">
                            <img src={mediaSrc(md)} alt="" className="max-w-[160px] max-h-[120px] rounded object-cover" />
                          </a>
                        ) : (
                          <a key={md.id} href={mediaSrc(md)} target="_blank" rel="noreferrer"
                             className="text-xs underline">Видео (откроется в новой вкладке)</a>
                        )
                      ))}
                    </div>
                  )}
                </div>
              </div>
            )
          })}
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/SupportSyncButton.tsx` (exit 0)
    - `test -f components/support/ReplyPanel.tsx` (exit 0)
    - `test -f components/support/TicketSidePanel.tsx` (exit 0)
    - `test -f components/support/SupportDialog.tsx` (exit 0)
    - `grep "/api/support-sync" components/support/SupportSyncButton.tsx` находит endpoint
    - `grep "replyToTicket" components/support/ReplyPanel.tsx` находит импорт action
    - `grep "assignTicket\|updateTicketStatus" components/support/TicketSidePanel.tsx` находит импорты
    - `grep "Синхронизация с WB" components/support/SupportSyncButton.tsx` находит русскую надпись
    - `grep "Пустой ответ" components/support/ReplyPanel.tsx` находит русскую валидацию
    - `grep "<select" components/support/TicketSidePanel.tsx` находит native select (не base-ui)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    4 клиент-компонента созданы, все импортируют server actions из app/actions/support.ts, используют sonner toast, native select, русский UI; tsc noEmit passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: RSC /support/[ticketId]/page.tsx + интеграция SupportSyncButton на /support</name>
  <files>
    app/(dashboard)/support/[ticketId]/page.tsx,
    app/(dashboard)/support/page.tsx
  </files>
  <read_first>
    - .planning/phases/08-support-mvp/08-CONTEXT.md (раздел «Диалог» строки 66-80 — 3-колоночный layout)
    - .planning/phases/08-support-mvp/08-RESEARCH.md (раздел «3-колоночный layout диалога» строки 645-649)
    - app/(dashboard)/support/page.tsx (обновлённая в Plan 08-03 — вставить SupportSyncButton в заголовок)
    - components/support/SupportDialog.tsx, ReplyPanel.tsx, TicketSidePanel.tsx, SupportSyncButton.tsx (из задачи 2)
    - lib/rbac.ts
  </read_first>
  <action>
    **Шаг A — создать `app/(dashboard)/support/[ticketId]/page.tsx`:**

    ```tsx
    // app/(dashboard)/support/[ticketId]/page.tsx
    // RSC — 3-колоночный диалог тикета
    import { notFound } from "next/navigation"
    import Link from "next/link"
    import { requireSection } from "@/lib/rbac"
    import { prisma } from "@/lib/prisma"
    import { SupportDialog } from "@/components/support/SupportDialog"
    import { ReplyPanel } from "@/components/support/ReplyPanel"
    import { TicketSidePanel } from "@/components/support/TicketSidePanel"
    import { SupportSyncButton } from "@/components/support/SupportSyncButton"
    import { ChevronLeft } from "lucide-react"

    export default async function TicketPage({ params }: { params: Promise<{ ticketId: string }> }) {
      await requireSection("SUPPORT")
      const { ticketId } = await params

      const ticket = await prisma.supportTicket.findUnique({
        where: { id: ticketId },
        include: {
          customer: true,
          assignedTo: { select: { id: true, name: true, firstName: true, lastName: true } },
          messages: {
            orderBy: { sentAt: "asc" },
            include: {
              author: { select: { id: true, name: true, firstName: true, lastName: true } },
              media: { select: { id: true, type: true, wbUrl: true, localPath: true } },
            },
          },
        },
      })
      if (!ticket) notFound()

      const supportUsers = await prisma.user.findMany({
        where: { isActive: true, sectionRoles: { some: { section: "SUPPORT" } } },
        select: { id: true, name: true, firstName: true, lastName: true },
        orderBy: [{ lastName: "asc" }, { firstName: "asc" }],
      })

      const wbCard = ticket.nmId
        ? await prisma.wbCard.findUnique({
            where: { nmId: ticket.nmId },
            select: { nmId: true, title: true, photos: true },
          })
        : null

      const photos = wbCard && Array.isArray(wbCard.photos) ? wbCard.photos : null
      const firstPhoto = photos?.[0] as { c246x328?: string; big?: string } | undefined
      const photoUrl = firstPhoto?.c246x328 ?? firstPhoto?.big ?? null

      // Префильтровать имена авторов
      function authorName(a: { name: string; firstName: string | null; lastName: string | null } | null): string | null {
        if (!a) return null
        const full = [a.firstName, a.lastName].filter(Boolean).join(" ").trim()
        return full || a.name || null
      }

      const messages = ticket.messages.map((m) => ({
        id: m.id,
        direction: m.direction,
        text: m.text,
        authorName: authorName(m.author),
        sentAt: m.sentAt,
        wbSentAt: m.wbSentAt,
        media: m.media,
      }))

      const canReply = ticket.channel === "FEEDBACK" || ticket.channel === "QUESTION"

      return (
        <div className="flex flex-col h-[calc(100vh-56px)]">
          <div className="flex items-center justify-between border-b px-4 py-3">
            <Link href="/support" className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground">
              <ChevronLeft className="h-4 w-4 mr-1" />Назад к ленте
            </Link>
            <SupportSyncButton />
          </div>
          <div className="flex-1 grid grid-cols-1 lg:grid-cols-[280px_1fr_280px] min-h-0">
            {/* Левая: покупатель + товар */}
            <aside className="border-r p-4 space-y-4 overflow-y-auto">
              <section>
                <h3 className="text-xs uppercase text-muted-foreground mb-2">Покупатель</h3>
                <p className="text-sm">{ticket.customer?.name ?? "Покупатель"}</p>
                {ticket.customer?.wbUserId && <p className="text-xs text-muted-foreground">WB ID: {ticket.customer.wbUserId}</p>}
              </section>
              {wbCard && (
                <section>
                  <h3 className="text-xs uppercase text-muted-foreground mb-2">Товар</h3>
                  {photoUrl && (
                    // eslint-disable-next-line @next/next/no-img-element
                    <img src={photoUrl} alt={wbCard.title ?? ""} className="w-full rounded mb-2 max-w-[180px]" />
                  )}
                  <p className="text-sm">{wbCard.title ?? "—"}</p>
                  <Link href={`/cards/wb?nmId=${wbCard.nmId}`} className="text-xs text-primary hover:underline">
                    Артикул {wbCard.nmId} →
                  </Link>
                </section>
              )}
            </aside>
            {/* Центр: хронология + sticky reply */}
            <div className="flex flex-col min-h-0">
              <SupportDialog messages={messages} />
              {canReply && <ReplyPanel ticketId={ticket.id} disabled={ticket.status === "CLOSED"} />}
              {!canReply && (
                <div className="border-t p-3 text-xs text-muted-foreground text-center">
                  Ответ через интерфейс доступен для каналов «Отзыв» и «Вопрос». Чат/возврат/мессенджер — в следующих фазах.
                </div>
              )}
            </div>
            {/* Правая: статус + менеджер + мета */}
            <div className="border-l p-4 overflow-y-auto">
              <TicketSidePanel
                ticketId={ticket.id}
                channel={ticket.channel}
                status={ticket.status}
                assignedToId={ticket.assignedToId}
                users={supportUsers}
                createdAt={ticket.createdAt}
                lastMessageAt={ticket.lastMessageAt}
              />
            </div>
          </div>
        </div>
      )
    }
    ```

    **Шаг B — обновить `app/(dashboard)/support/page.tsx`** (вставить SupportSyncButton в шапку):

    Найти строку `{/* SyncButton монтируется в Plan 08-04: <SupportSyncButton /> */}` (оставлено в Plan 08-03) и заменить на импорт + реальный компонент:

    ```tsx
    import { SupportSyncButton } from "@/components/support/SupportSyncButton"
    // ...
    <div className="flex items-center justify-between">
      <h1 className="text-xl font-semibold">Служба поддержки</h1>
      <SupportSyncButton />
    </div>
    ```

    **Шаг C — smoke проверка:** `npm run build` должен успешно собраться.

    **Шаг D — VPS deployment заметки** (вписать в SUMMARY, не исполнять автоматически):
    - Создать директорию: `ssh root@85.198.97.89 "mkdir -p /var/www/zoiten-uploads/support/ && chown -R www-data:www-data /var/www/zoiten-uploads/support/"`
    - Применить Prisma миграцию: на VPS `cd /opt/zoiten-pro && npx prisma migrate deploy`
    - Добавить в `/etc/cron.d/zoiten-support` (от root):
      ```
      */15 * * * * www-data curl -s -H "x-cron-secret: $(grep CRON_SECRET /etc/zoiten.pro.env | cut -d= -f2)" http://localhost:3001/api/cron/support-sync-reviews > /dev/null
      0 3 * * * www-data curl -s -H "x-cron-secret: $(grep CRON_SECRET /etc/zoiten.pro.env | cut -d= -f2)" http://localhost:3001/api/cron/support-media-cleanup > /dev/null
      ```
    - Проверить nginx alias для `/uploads/` уже покрывает `/var/www/zoiten-uploads/support/*` (существующий alias в Phase 6 работает с подпутями).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/(dashboard)/support/[ticketId]/page.tsx` (exit 0)
    - `grep "requireSection(\"SUPPORT\")" app/(dashboard)/support/[ticketId]/page.tsx` находит RBAC
    - `grep "SupportDialog\|ReplyPanel\|TicketSidePanel" app/(dashboard)/support/[ticketId]/page.tsx | wc -l` >= 3
    - `grep "lg:grid-cols-\[280px_1fr_280px\]" app/(dashboard)/support/[ticketId]/page.tsx` находит 3-col layout
    - `grep "SupportSyncButton" app/(dashboard)/support/page.tsx` находит (интегрирован)
    - `grep "/\* SyncButton монтируется" app/(dashboard)/support/page.tsx | wc -l` == 0 (плейсхолдер удалён)
    - `npx tsc --noEmit` exit 0
    - `npm run test` full suite — 0 failed
    - (`npm run build` запускается в финальном `<verification>` блоке плана — не per-task, чтобы сохранить per-task feedback latency <30s)
  </acceptance_criteria>
  <done>
    Страница /support/[ticketId] рендерит 3-колоночный диалог; SupportSyncButton интегрирован на ленту и диалог; `npx tsc --noEmit` passes; `npm run build` проходит в финальной `<verification>` плана.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npm run test` full suite — 0 failed (включая support-actions GREEN)
2. `npm run build` exit 0
3. `npx tsc --noEmit` exit 0
4. `grep "requireSection.*SUPPORT.*MANAGE" app/actions/support.ts | wc -l` >= 3
5. `grep "SupportSyncButton" app/(dashboard)/support/page.tsx` — интегрирован
6. `grep "github.com/safyodorov" app/(dashboard)/support/page.tsx | wc -l` == 0 (старая заглушка точно удалена после Plan 08-03, проверка повторно)
</verification>

<success_criteria>
Plan 08-04 завершён когда:
- app/actions/support.ts: 3 server actions GREEN, RBAC SUPPORT+MANAGE на каждой
- 4 клиент-компонента support/ созданы (SupportDialog, ReplyPanel, TicketSidePanel, SupportSyncButton)
- RSC страница /support/[ticketId]/page.tsx с 3-колоночным layout
- SupportSyncButton вставлен в /support и /support/[ticketId]
- tests/support-actions.test.ts GREEN (~11 тестов)
- `npm run build` passes
- VPS deployment instructions зафиксированы в SUMMARY
- Commit: `feat(08-04): диалог + server actions + sync button`
</success_criteria>

<output>
Создать `.planning/phases/08-support-mvp/08-04-SUMMARY.md` с:
- Server actions API и их контракты ({ok:true}|{ok:false;error:string})
- Список новых клиент-компонентов
- **VPS deployment tasks** (выполнить вручную после merge):
  ```bash
  # 1. Создать директорию медиа:
  ssh root@85.198.97.89 "mkdir -p /var/www/zoiten-uploads/support/ && chown -R www-data:www-data /var/www/zoiten-uploads/support/"
  # 2. Деплой + миграция:
  ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
  # 3. Добавить cron (от root):
  ssh root@85.198.97.89 "cat > /etc/cron.d/zoiten-support" # см. блок выше
  # 4. Проверить nginx — alias для /uploads/ (Phase 6) должен покрывать /uploads/support/*
  ```
- Ссылка на 10 HUMAN-UAT пунктов из 08-VALIDATION.md (Manual-Only Verifications)
- Напоминание: SUPP-01 и SUPP-02 (Phase 5) инвалидируются — пометить в PROJECT.md Key Decisions
</output>
