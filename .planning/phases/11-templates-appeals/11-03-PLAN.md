---
phase: 11-templates-appeals
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - app/(dashboard)/support/templates/page.tsx
  - app/(dashboard)/support/templates/new/page.tsx
  - app/(dashboard)/support/templates/[id]/edit/page.tsx
  - components/support/templates/TemplatesTable.tsx
  - components/support/templates/TemplatesFilters.tsx
  - components/support/templates/TemplateForm.tsx
  - components/support/templates/TemplatePickerModal.tsx
  - components/support/templates/TemplateImportButton.tsx
  - components/support/templates/TemplateExportButton.tsx
  - components/support/ReplyPanel.tsx
  - components/layout/nav-items.ts
  - tests/template-picker.test.ts
autonomous: true
requirements:
  - SUP-26
  - SUP-28
must_haves:
  truths:
    - "/support/templates RSC отображает таблицу шаблонов (Название, Канал, Тег, Товар/Общий, Активен, Обновлено, Действия) + кнопки «Новый», «Экспорт», «Импорт» в шапке"
    - "Страницы /support/templates/new и /support/templates/[id]/edit используют общий TemplateForm с react-hook-form + zodResolver + native <select> для channel"
    - "TemplatePickerModal открывается кнопкой «Шаблон» (FileText icon) в ReplyPanel и показывает 2 группы: «Для этого товара» (template.nmId === ticket.nmId) и «Общие» (template.nmId === null или !== ticket.nmId)"
    - "onPick вызывает substituteTemplateVars(template.text, {customerName, productName}) и подставляет в textarea родителя через prop callback"
    - "Только isActive=true шаблоны показываются в picker; фильтрация по channel соответствует каналу текущего тикета"
    - "TemplateExportButton скачивает JSON через a[download] blob URL; TemplateImportButton открывает file picker и отправляет содержимое в importTemplatesJson"
    - "Sidebar содержит пункт «Шаблоны ответов» под «Служба поддержки» с иконкой FileText (lucide)"
    - "ReplyPanel расширена новыми props (ticketNmId, customerName, productName, ticketChannel) для передачи контекста в picker — требуется обновление вызова в app/(dashboard)/support/[ticketId]/page.tsx"
    - "Phase 10 coordination: ChatReplyPanel (Phase 10, ещё не создан) должен получить аналогичную кнопку — это зафиксировано в SUMMARY как TODO для Phase 10 execute (backward dependency)"
    - "tests/template-picker.test.ts GREEN — минимум 6 test cases покрывают группировку, фильтрацию, substitution, isActive filter, пустое состояние"
  artifacts:
    - path: "app/(dashboard)/support/templates/page.tsx"
      provides: "RSC страница со списком шаблонов + фильтры + кнопки действий"
      contains: "requireSection"
    - path: "components/support/templates/TemplatePickerModal.tsx"
      provides: "Client модалка выбора шаблона с группировкой по nmId + substitution"
      exports: ["TemplatePickerModal"]
    - path: "components/support/templates/TemplateForm.tsx"
      provides: "Client форма create/edit с Zod + native <select>"
      exports: ["TemplateForm"]
    - path: "components/support/templates/TemplatesTable.tsx"
      provides: "Client таблица с фильтрами + inline действиями"
      exports: ["TemplatesTable"]
    - path: "components/support/templates/TemplateExportButton.tsx"
      provides: "Client кнопка — вызывает exportTemplatesJson и скачивает blob"
      exports: ["TemplateExportButton"]
    - path: "components/support/templates/TemplateImportButton.tsx"
      provides: "Client кнопка + file input → importTemplatesJson"
      exports: ["TemplateImportButton"]
    - path: "components/support/ReplyPanel.tsx"
      provides: "Расширенная ReplyPanel с кнопкой «Шаблон» и интеграцией TemplatePickerModal"
      contains: "TemplatePickerModal"
    - path: "components/layout/nav-items.ts"
      provides: "Sidebar пункт «Шаблоны ответов»"
      contains: "templates"
    - path: "tests/template-picker.test.ts"
      provides: "Unit тесты логики picker (группировка/фильтрация/substitution)"
      contains: "describe"
  key_links:
    - from: "components/support/templates/TemplatePickerModal.tsx"
      to: "lib/template-vars.ts substituteTemplateVars"
      via: "import + вызов в onPick до передачи в callback"
      pattern: "substituteTemplateVars"
    - from: "components/support/ReplyPanel.tsx"
      to: "components/support/templates/TemplatePickerModal.tsx"
      via: "import + render <TemplatePickerModal open={...} onPick={...} />"
      pattern: "TemplatePickerModal"
    - from: "components/support/templates/TemplateExportButton.tsx"
      to: "app/actions/templates.ts exportTemplatesJson"
      via: "server action call → blob URL download"
      pattern: "exportTemplatesJson"
    - from: "components/support/templates/TemplateImportButton.tsx"
      to: "app/actions/templates.ts importTemplatesJson"
      via: "file reader → server action → toast с result.added/updated/errors"
      pattern: "importTemplatesJson"
    - from: "app/(dashboard)/support/templates/page.tsx"
      to: "requireSection(\"SUPPORT\")"
      via: "RSC guard"
      pattern: "requireSection"
---

<objective>
Phase 11 Plan 03 — UI слой шаблонов:

1. **Страница `/support/templates`** (RSC) — таблица + фильтры + кнопки «Новый», «Экспорт», «Импорт».
2. **Формы create/edit** — отдельные страницы с react-hook-form + zodResolver + native `<select>` (per CLAUDE.md) + хинт про переменные `{имя_покупателя}/{название_товара}`.
3. **TemplatePickerModal** (client) — встраивается в ReplyPanel (FEEDBACK/QUESTION). Группировка: «Для этого товара» (template.nmId === ticket.nmId) vs «Общие» (null или другой nmId). Фильтр по каналу. Поиск по name/text/situationTag. onPick → substituteTemplateVars → подставить в textarea.
4. **Export/Import UI** — кнопки в шапке, скачивание JSON через blob URL, загрузка через file input → importTemplatesJson → toast с результатом.
5. **Sidebar** — добавить пункт «Шаблоны ответов» под «Служба поддержки».
6. **Replace Wave 0 stub** tests/template-picker.test.ts на 6+ GREEN test cases.

**Phase 10 coordination:** ChatReplyPanel (создаётся в Plan 10-03) должен получить аналогичную кнопку picker. Если Phase 11 execute ДО Phase 10 (вероятный сценарий, Phase 10 ещё Planned) — в SUMMARY фиксируется TODO для Plan 10-03: интегрировать TemplatePickerModal с `channel="CHAT"`. Бэкуард зависимость добавляется в Plan 10-03 при execute.

Purpose: Менеджер получает полноценный CRUD шаблонов в своём ERP и использует picker для быстрого ответа с авто-подстановкой имени покупателя/товара. WB sync заменён Export/Import JSON для переноса между инсталляциями.

Output: 1 RSC страница + 2 формы страницы, 6 новых client компонентов (TemplatesTable, TemplatesFilters, TemplateForm, TemplatePickerModal, TemplateExportButton, TemplateImportButton), 1 обновлённый ReplyPanel + 1 обновлённый nav-items, 1 test-файл (stub → GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-templates-appeals/11-RESEARCH.md
@.planning/phases/11-templates-appeals/11-VALIDATION.md
@.planning/phases/11-templates-appeals/11-01-PLAN.md
@.planning/phases/11-templates-appeals/11-02-PLAN.md
@CLAUDE.md
@components/support/ReplyPanel.tsx
@components/support/SupportFilters.tsx
@components/layout/nav-items.ts
@app/actions/templates.ts
@lib/template-vars.ts
@lib/prisma.ts
@lib/rbac.ts

<interfaces>
Из Plan 11-02 (app/actions/templates.ts):
```typescript
export async function createTemplate(input): Promise<ActionResult<{ id: string }>>
export async function updateTemplate(id, input): Promise<ActionResult>
export async function deleteTemplate(id): Promise<ActionResult>
export async function toggleTemplateActive(id): Promise<ActionResult<{ isActive: boolean }>>
export async function exportTemplatesJson(): Promise<ActionResult<{ json: string }>>
export async function importTemplatesJson(json): Promise<ActionResult<{ added, updated, errors }>>
```

Из Plan 11-01 (lib/template-vars.ts):
```typescript
export function substituteTemplateVars(text: string, ctx: { customerName?, productName? }): string
```

Текущий ReplyPanel (components/support/ReplyPanel.tsx) принимает только `{ ticketId, disabled }` — расширяется новыми props.

Prisma тип (из @prisma/client после Plan 11-01):
```typescript
type ResponseTemplate = {
  id: string; name: string; text: string;
  channel: "FEEDBACK"|"QUESTION"|"CHAT"|"RETURN"|"MESSENGER";
  situationTag: string|null; nmId: number|null; isActive: boolean;
  createdAt: Date; updatedAt: Date;
}
```

Паттерн проекта:
- Native `<select>` (CLAUDE.md) — НЕ base-ui Select
- MultiSelectDropdown inline для фильтров (паттерн из components/support/SupportFilters.tsx)
- Dialog из components/ui/dialog.tsx (shadcn base-ui wrapper)
- Icons: lucide-react
- Toast: sonner (паттерн из ReturnActionsPanel)

RSC page: `requireSection("SUPPORT")` для read-доступа.

Sidebar nav-items.ts — добавить новый item с iconName "FileText", href "/support/templates", section "SUPPORT".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Задача 1: Страницы /support/templates + TemplateForm + TemplatesTable + Export/Import + Sidebar</name>
  <files>
    app/(dashboard)/support/templates/page.tsx,
    app/(dashboard)/support/templates/new/page.tsx,
    app/(dashboard)/support/templates/[id]/edit/page.tsx,
    components/support/templates/TemplatesTable.tsx,
    components/support/templates/TemplatesFilters.tsx,
    components/support/templates/TemplateForm.tsx,
    components/support/templates/TemplateExportButton.tsx,
    components/support/templates/TemplateImportButton.tsx,
    components/layout/nav-items.ts
  </files>
  <read_first>
    - .planning/phases/11-templates-appeals/11-RESEARCH.md (§UI Architecture строки 344-410 — структура страниц + форма Zod)
    - .planning/phases/11-templates-appeals/11-02-PLAN.md (интерфейсы server actions)
    - components/support/SupportFilters.tsx (паттерн MultiSelectDropdown inline + searchParams)
    - components/support/ReplyPanel.tsx (паттерн client component + toast + useTransition)
    - components/layout/nav-items.ts (текущие items — добавить «Шаблоны ответов»)
    - components/ui/dialog.tsx (shadcn wrapper для модалок)
    - CLAUDE.md (native select, язык русский, RBAC)
    - app/(dashboard)/support/page.tsx (паттерн RSC + requireSection)
  </read_first>
  <action>
    **Шаг A — `app/(dashboard)/support/templates/page.tsx` (RSC):**

    ```typescript
    import { requireSection } from "@/lib/rbac"
    import { prisma } from "@/lib/prisma"
    import Link from "next/link"
    import { TemplatesTable } from "@/components/support/templates/TemplatesTable"
    import { TemplatesFilters } from "@/components/support/templates/TemplatesFilters"
    import { TemplateExportButton } from "@/components/support/templates/TemplateExportButton"
    import { TemplateImportButton } from "@/components/support/templates/TemplateImportButton"
    import { Button } from "@/components/ui/button"
    import { Plus } from "lucide-react"

    export const dynamic = "force-dynamic"

    export default async function TemplatesPage({
      searchParams,
    }: {
      searchParams: Promise<{ channel?: string; active?: string; q?: string }>
    }) {
      await requireSection("SUPPORT")
      const sp = await searchParams

      const channelFilter = sp.channel?.split(",").filter(Boolean) ?? []
      const activeFilter = sp.active // "all" | "active" | "inactive"
      const where: Record<string, unknown> = {}
      if (channelFilter.length > 0) where.channel = { in: channelFilter }
      if (activeFilter === "active") where.isActive = true
      if (activeFilter === "inactive") where.isActive = false
      if (sp.q) {
        where.OR = [
          { name: { contains: sp.q, mode: "insensitive" } },
          { text: { contains: sp.q, mode: "insensitive" } },
          { situationTag: { contains: sp.q, mode: "insensitive" } },
        ]
      }

      const templates = await prisma.responseTemplate.findMany({
        where,
        orderBy: [{ updatedAt: "desc" }],
      })

      return (
        <div className="p-6 space-y-4">
          <div className="flex items-center justify-between">
            <h1 className="text-2xl font-bold">Шаблоны ответов</h1>
            <div className="flex gap-2">
              <TemplateExportButton />
              <TemplateImportButton />
              <Link href="/support/templates/new">
                <Button size="sm"><Plus className="h-4 w-4 mr-1" /> Новый шаблон</Button>
              </Link>
            </div>
          </div>
          <TemplatesFilters />
          <TemplatesTable templates={templates} />
        </div>
      )
    }
    ```

    **Шаг B — `components/support/templates/TemplatesTable.tsx` (client):**

    Таблица с колонками (Название, Канал, Тег, Товар/Общий, Активен, Обновлено, Действия). Действия: Edit (Link), Toggle active (вызов toggleTemplateActive), Delete (confirm → deleteTemplate).

    Использовать `useTransition` для server actions, `toast` для feedback, `Link` для редактирования.

    Channel label mapping: FEEDBACK → "Отзыв", QUESTION → "Вопрос", CHAT → "Чат".
    Товар column: `template.nmId ? nmId : "Общий"`.
    Обновлено: `Intl.DateTimeFormat("ru-RU", { timeZone: "Europe/Moscow", ... })`.

    **Шаг C — `components/support/templates/TemplatesFilters.tsx` (client):**

    Inline MultiSelectDropdown (паттерн `components/support/SupportFilters.tsx`). Фильтры:
    - Канал: MultiSelect с опциями FEEDBACK/QUESTION/CHAT
    - Активность: native `<select>` all/active/inactive
    - Поиск: native `<input type="text" name="q">` с debounce через router.replace

    Использовать `searchParams` через `useRouter` + `usePathname` (паттерн SupportFilters).

    **Шаг D — `components/support/templates/TemplateForm.tsx` (client):**

    react-hook-form + zodResolver. Поля:
    - name (Input)
    - channel (native `<select>` с FEEDBACK/QUESTION/CHAT)
    - situationTag (Input, optional)
    - nmId (Input type="number", optional)
    - text (Textarea, обязательно) — под ним хинт: «Переменные: {имя_покупателя}, {название_товара} подставятся автоматически при выборе шаблона»
    - isActive (Checkbox)

    ```typescript
    "use client"
    import { useForm } from "react-hook-form"
    import { zodResolver } from "@hookform/resolvers/zod"
    import { z } from "zod"
    import { useTransition } from "react"
    import { useRouter } from "next/navigation"
    import { toast } from "sonner"
    import { createTemplate, updateTemplate } from "@/app/actions/templates"

    const schema = z.object({
      name: z.string().min(2).max(80),
      text: z.string().min(1).max(5000),
      channel: z.enum(["FEEDBACK", "QUESTION", "CHAT"] as const),
      situationTag: z.string().max(60).nullable().optional(),
      nmId: z.coerce.number().int().positive().nullable().optional(),
      isActive: z.boolean().default(true),
    })

    export function TemplateForm({
      id,
      defaultValues,
    }: {
      id?: string
      defaultValues?: Partial<z.infer<typeof schema>>
    }) {
      const router = useRouter()
      const [isPending, startTransition] = useTransition()
      const form = useForm({ resolver: zodResolver(schema), defaultValues })

      const onSubmit = form.handleSubmit((values) => {
        startTransition(async () => {
          const res = id ? await updateTemplate(id, values) : await createTemplate(values)
          if (res.ok) {
            toast.success(id ? "Шаблон обновлён" : "Шаблон создан")
            router.push("/support/templates")
          } else {
            toast.error(res.error)
          }
        })
      })

      return (
        <form onSubmit={onSubmit} className="space-y-4 max-w-2xl">
          {/* name, channel (native select), situationTag, nmId, text, isActive, buttons */}
        </form>
      )
    }
    ```

    **Шаг E — `app/(dashboard)/support/templates/new/page.tsx` + `[id]/edit/page.tsx`:**

    RSC wrappers: requireSection("SUPPORT", "MANAGE"), fetch template (edit) → render `<TemplateForm />`.

    **Шаг F — `components/support/templates/TemplateExportButton.tsx` (client):**

    ```typescript
    "use client"
    import { Button } from "@/components/ui/button"
    import { Download } from "lucide-react"
    import { useTransition } from "react"
    import { toast } from "sonner"
    import { exportTemplatesJson } from "@/app/actions/templates"

    export function TemplateExportButton() {
      const [isPending, startTransition] = useTransition()
      function onClick() {
        startTransition(async () => {
          const res = await exportTemplatesJson()
          if (!res.ok) { toast.error(res.error); return }
          const blob = new Blob([res.json], { type: "application/json" })
          const url = URL.createObjectURL(blob)
          const a = document.createElement("a")
          a.href = url
          a.download = `zoiten-templates-${new Date().toISOString().slice(0, 10)}.json`
          a.click()
          URL.revokeObjectURL(url)
          toast.success("Экспорт готов")
        })
      }
      return (
        <Button variant="outline" size="sm" onClick={onClick} disabled={isPending}>
          <Download className="h-4 w-4 mr-1" /> Экспорт
        </Button>
      )
    }
    ```

    **Шаг G — `components/support/templates/TemplateImportButton.tsx` (client):**

    File input скрытый (`<input type="file" accept=".json" hidden ref={...} />`) + кнопка. При выборе файла → read text → `importTemplatesJson(content)` → toast с added/updated/errors.

    ```typescript
    "use client"
    import { Button } from "@/components/ui/button"
    import { Upload } from "lucide-react"
    import { useRef, useTransition } from "react"
    import { toast } from "sonner"
    import { useRouter } from "next/navigation"
    import { importTemplatesJson } from "@/app/actions/templates"

    export function TemplateImportButton() {
      const router = useRouter()
      const inputRef = useRef<HTMLInputElement>(null)
      const [isPending, startTransition] = useTransition()

      function onChange(e: React.ChangeEvent<HTMLInputElement>) {
        const file = e.target.files?.[0]
        if (!file) return
        startTransition(async () => {
          const text = await file.text()
          const res = await importTemplatesJson(text)
          if (!res.ok) { toast.error(res.error); return }
          const msg = `Добавлено: ${res.added}, обновлено: ${res.updated}` + (res.errors.length ? `, ошибок: ${res.errors.length}` : "")
          res.errors.length ? toast.warning(msg) : toast.success(msg)
          router.refresh()
        })
        e.target.value = ""
      }

      return (
        <>
          <input type="file" accept=".json,application/json" ref={inputRef} onChange={onChange} className="hidden" />
          <Button variant="outline" size="sm" onClick={() => inputRef.current?.click()} disabled={isPending}>
            <Upload className="h-4 w-4 mr-1" /> Импорт
          </Button>
        </>
      )
    }
    ```

    **Шаг H — `components/layout/nav-items.ts`:**

    Найти раздел с существующими support-items (Все обращения / Возвраты) и **добавить** новый:

    ```typescript
    { href: "/support/templates", label: "Шаблоны ответов", iconName: "FileText", section: "SUPPORT" }
    ```

    Расположение: после «Возвраты», перед будущими «Автоответы»/«Статистика».
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f "app/(dashboard)/support/templates/page.tsx" && test -f components/support/templates/TemplateForm.tsx && test -f components/support/templates/TemplateExportButton.tsx && test -f components/support/templates/TemplateImportButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "app/(dashboard)/support/templates/page.tsx"` (exit 0)
    - `test -f "app/(dashboard)/support/templates/new/page.tsx"` (exit 0)
    - `test -f "app/(dashboard)/support/templates/[id]/edit/page.tsx"` (exit 0)
    - `test -f components/support/templates/TemplatesTable.tsx` (exit 0)
    - `test -f components/support/templates/TemplatesFilters.tsx` (exit 0)
    - `test -f components/support/templates/TemplateForm.tsx` (exit 0)
    - `test -f components/support/templates/TemplateExportButton.tsx` (exit 0)
    - `test -f components/support/templates/TemplateImportButton.tsx` (exit 0)
    - `grep "requireSection(\"SUPPORT\")" "app/(dashboard)/support/templates/page.tsx"` находит строку
    - `grep "exportTemplatesJson" components/support/templates/TemplateExportButton.tsx` находит строку
    - `grep "importTemplatesJson" components/support/templates/TemplateImportButton.tsx` находит строку
    - `grep "templates" components/layout/nav-items.ts` находит строку
    - `grep "FEEDBACK" components/support/templates/TemplateForm.tsx` находит валидацию channel
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    /support/templates доступен RSC-страницей с таблицей + фильтрами + кнопками Экспорт/Импорт/Новый; формы create/edit работают; Sidebar содержит «Шаблоны ответов»; typecheck проходит.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: TemplatePickerModal + интеграция в ReplyPanel + tests GREEN</name>
  <files>
    components/support/templates/TemplatePickerModal.tsx,
    components/support/ReplyPanel.tsx,
    tests/template-picker.test.ts
  </files>
  <read_first>
    - .planning/phases/11-templates-appeals/11-RESEARCH.md (§UI Architecture строки 404-456 + §Verified pattern строки 926-1017 — код ReplyPanel с picker)
    - components/support/ReplyPanel.tsx (текущая реализация — заменяем)
    - components/ui/dialog.tsx (shadcn wrapper)
    - lib/template-vars.ts (substituteTemplateVars из 11-01)
    - tests/template-picker.test.ts (stub — заменяется GREEN)
    - CLAUDE.md (русский, native input)
    - app/(dashboard)/support/[ticketId]/page.tsx (текущий вызов `<ReplyPanel ticketId={...} />` — обновляется с новыми props)
  </read_first>
  <behavior>
    **TemplatePickerModal**:
    - Props: `{ open, onOpenChange, ticketNmId: number|null, channel: "FEEDBACK"|"QUESTION"|"CHAT", customerName: string|null, productName: string|null, onPick: (substitutedText: string) => void }`
    - При `open=true` → RSC already provided templates? Нет — модалка сама делает client fetch через server action `listActiveTemplates(channel)` ИЛИ родитель передаёт все activeTemplates через props. Решение: родитель передаёт props `templates: ResponseTemplate[]` (pre-fetched в RSC).
    - Показывает только `isActive=true` и `channel === props.channel`.
    - Фильтр по тексту: `name.toLowerCase().includes(q) || text.toLowerCase().includes(q) || situationTag?.toLowerCase().includes(q)` — case insensitive.
    - Группировка: `forNmId = visible.filter(t => t.nmId === ticketNmId && ticketNmId !== null)`, `general = visible.filter(t => t.nmId === null || (ticketNmId !== null && t.nmId !== ticketNmId))`.
    - onPick(template): `substituteTemplateVars(template.text, { customerName, productName })` → передать через callback → закрыть модалку.
    - Если видимых шаблонов 0 — показать «Нет шаблонов для этого канала».

    **ReplyPanel расширение** (components/support/ReplyPanel.tsx):
    - Новые props: `ticketNmId`, `customerName`, `productName`, `ticketChannel` (типа FEEDBACK|QUESTION — RETURN/MESSENGER используют другие панели).
    - Также принимает `templates: ResponseTemplate[]` (pre-fetched в RSC).
    - Новая кнопка «Шаблон» (FileText icon) слева от «Отправить».
    - State `pickerOpen`, `onTemplatePick(substituted)` → `setText(substituted)`.

    **Обновление `app/(dashboard)/support/[ticketId]/page.tsx`**:
    - Добавить `prisma.responseTemplate.findMany({ where: { channel: ticket.channel, isActive: true } })` в data fetch.
    - Передать в ReplyPanel новые props: `ticketNmId={ticket.nmId}`, `customerName={customer?.name}` (фallback null), `productName={wbCard?.name}` (фallback null), `ticketChannel={ticket.channel}`, `templates={templates}`.

    **Phase 10 coordination (TODO в SUMMARY):**
    - ChatReplyPanel ещё не существует (Plan 10-03). Когда Phase 10 execute — Plan 10-03 задача должна добавить такую же кнопку «Шаблон» в ChatReplyPanel с `channel="CHAT"`. Это backward dependency — фиксируется в SUMMARY Phase 11-03 как TODO для Plan 10-03.
  </behavior>
  <action>
    **Шаг A — создать `components/support/templates/TemplatePickerModal.tsx`:**

    ```typescript
    "use client"

    import { useState, useMemo } from "react"
    import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"
    import { substituteTemplateVars } from "@/lib/template-vars"
    import type { ResponseTemplate } from "@prisma/client"

    export interface TemplatePickerModalProps {
      open: boolean
      onOpenChange: (open: boolean) => void
      templates: ResponseTemplate[]
      ticketNmId: number | null
      channel: "FEEDBACK" | "QUESTION" | "CHAT"
      customerName: string | null
      productName: string | null
      onPick: (substitutedText: string) => void
    }

    export function TemplatePickerModal({
      open, onOpenChange, templates, ticketNmId, channel, customerName, productName, onPick,
    }: TemplatePickerModalProps) {
      const [q, setQ] = useState("")

      const filtered = useMemo(() => {
        const query = q.trim().toLowerCase()
        return templates.filter((t) => {
          if (!t.isActive) return false
          if (t.channel !== channel) return false
          if (!query) return true
          return (
            t.name.toLowerCase().includes(query) ||
            t.text.toLowerCase().includes(query) ||
            (t.situationTag?.toLowerCase().includes(query) ?? false)
          )
        })
      }, [templates, channel, q])

      const forNmId = useMemo(
        () => (ticketNmId !== null ? filtered.filter((t) => t.nmId === ticketNmId) : []),
        [filtered, ticketNmId]
      )
      const general = useMemo(
        () => filtered.filter((t) => t.nmId === null || (ticketNmId !== null && t.nmId !== ticketNmId)),
        [filtered, ticketNmId]
      )

      function handlePick(template: ResponseTemplate) {
        const result = substituteTemplateVars(template.text, { customerName, productName })
        onPick(result)
        onOpenChange(false)
        setQ("")
      }

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="max-w-2xl">
            <DialogHeader>
              <DialogTitle>Выбрать шаблон</DialogTitle>
            </DialogHeader>
            <input
              type="text"
              placeholder="Поиск по названию, тексту или тегу..."
              value={q}
              onChange={(e) => setQ(e.target.value)}
              className="w-full rounded-md border px-3 py-2 text-sm"
              autoFocus
            />
            <div className="max-h-[400px] overflow-y-auto space-y-4 mt-2">
              {filtered.length === 0 && (
                <div className="text-sm text-muted-foreground py-8 text-center">
                  Нет шаблонов для этого канала
                </div>
              )}
              {forNmId.length > 0 && (
                <section>
                  <h3 className="text-sm font-medium mb-2">Для этого товара</h3>
                  <ul className="space-y-1">
                    {forNmId.map((t) => (
                      <li key={t.id}>
                        <button
                          type="button"
                          onClick={() => handlePick(t)}
                          className="w-full text-left rounded border px-3 py-2 hover:bg-muted"
                        >
                          <div className="font-medium text-sm">{t.name}</div>
                          {t.situationTag && <div className="text-xs text-muted-foreground">{t.situationTag}</div>}
                          <div className="text-xs mt-1 line-clamp-2">{t.text}</div>
                        </button>
                      </li>
                    ))}
                  </ul>
                </section>
              )}
              {general.length > 0 && (
                <section>
                  <h3 className="text-sm font-medium mb-2">Общие шаблоны</h3>
                  <ul className="space-y-1">
                    {general.map((t) => (
                      <li key={t.id}>
                        <button
                          type="button"
                          onClick={() => handlePick(t)}
                          className="w-full text-left rounded border px-3 py-2 hover:bg-muted"
                        >
                          <div className="font-medium text-sm">{t.name}</div>
                          {t.situationTag && <div className="text-xs text-muted-foreground">{t.situationTag}</div>}
                          <div className="text-xs mt-1 line-clamp-2">{t.text}</div>
                        </button>
                      </li>
                    ))}
                  </ul>
                </section>
              )}
            </div>
          </DialogContent>
        </Dialog>
      )
    }
    ```

    **Экспортируем pure helpers для тестов** (в том же файле):

    ```typescript
    // Экспортируется для unit-тестов (tests/template-picker.test.ts)
    export function groupTemplatesForPicker(
      templates: ResponseTemplate[],
      opts: { channel: ResponseTemplate["channel"]; ticketNmId: number | null; query?: string }
    ): { forNmId: ResponseTemplate[]; general: ResponseTemplate[] } {
      const query = opts.query?.trim().toLowerCase() ?? ""
      const filtered = templates.filter((t) => {
        if (!t.isActive) return false
        if (t.channel !== opts.channel) return false
        if (!query) return true
        return (
          t.name.toLowerCase().includes(query) ||
          t.text.toLowerCase().includes(query) ||
          (t.situationTag?.toLowerCase().includes(query) ?? false)
        )
      })
      const forNmId = opts.ticketNmId !== null ? filtered.filter((t) => t.nmId === opts.ticketNmId) : []
      const general = filtered.filter((t) => t.nmId === null || (opts.ticketNmId !== null && t.nmId !== opts.ticketNmId))
      return { forNmId, general }
    }
    ```

    **Шаг B — обновить `components/support/ReplyPanel.tsx`:**

    Заменить содержимое на:

    ```typescript
    "use client"

    import { useState, useTransition } from "react"
    import { Button } from "@/components/ui/button"
    import { Send, FileText } from "lucide-react"
    import { toast } from "sonner"
    import { replyToTicket } from "@/app/actions/support"
    import { TemplatePickerModal } from "@/components/support/templates/TemplatePickerModal"
    import type { ResponseTemplate } from "@prisma/client"

    export function ReplyPanel({
      ticketId,
      ticketNmId,
      ticketChannel,
      customerName,
      productName,
      templates,
      disabled,
    }: {
      ticketId: string
      ticketNmId: number | null
      ticketChannel: "FEEDBACK" | "QUESTION" | "CHAT"
      customerName: string | null
      productName: string | null
      templates: ResponseTemplate[]
      disabled?: boolean
    }) {
      const [text, setText] = useState("")
      const [pickerOpen, setPickerOpen] = useState(false)
      const [isPending, startTransition] = useTransition()

      function onSubmit() {
        const trimmed = text.trim()
        if (!trimmed) { toast.error("Пустой ответ"); return }
        startTransition(async () => {
          const res = await replyToTicket(ticketId, trimmed)
          if (res.ok) { toast.success("Ответ отправлен"); setText("") }
          else toast.error(res.error)
        })
      }

      return (
        <div className="sticky bottom-0 bg-white dark:bg-neutral-900 border-t p-3 flex items-end gap-2">
          <textarea
            value={text}
            onChange={(e) => setText(e.target.value)}
            placeholder="Введите ответ..."
            disabled={disabled || isPending}
            rows={3}
            className="flex-1 rounded-md border bg-transparent p-2 text-sm resize-none"
          />
          <div className="flex flex-col gap-1">
            <Button size="sm" variant="outline" onClick={() => setPickerOpen(true)} disabled={disabled}>
              <FileText className="h-4 w-4 mr-1" /> Шаблон
            </Button>
            <Button onClick={onSubmit} disabled={disabled || isPending || !text.trim()} size="sm">
              <Send className="h-4 w-4 mr-1" />
              {isPending ? "..." : "Отправить"}
            </Button>
          </div>
          <TemplatePickerModal
            open={pickerOpen}
            onOpenChange={setPickerOpen}
            templates={templates}
            ticketNmId={ticketNmId}
            channel={ticketChannel}
            customerName={customerName}
            productName={productName}
            onPick={(substituted) => setText(substituted)}
          />
        </div>
      )
    }
    ```

    **ВАЖНО:** Plan 11-04 добавит кнопку «Обжаловать» в эту же ReplyPanel. План 11-04 дополнит этот компонент, не заменяя полностью.

    **Обновление `app/(dashboard)/support/[ticketId]/page.tsx`** — найти вызов `<ReplyPanel ticketId={...} />` и расширить:
    ```tsx
    const templates = await prisma.responseTemplate.findMany({
      where: { channel: ticket.channel as "FEEDBACK"|"QUESTION"|"CHAT", isActive: true },
      orderBy: { updatedAt: "desc" },
    })
    // ...
    <ReplyPanel
      ticketId={ticket.id}
      ticketNmId={ticket.nmId}
      ticketChannel={ticket.channel as "FEEDBACK"|"QUESTION"|"CHAT"}
      customerName={ticket.customer?.name ?? null}
      productName={productNameFromWbCard ?? null}
      templates={templates}
    />
    ```

    Если ReplyPanel уже передаётся для FEEDBACK/QUESTION только (RETURN использует ReturnActionsPanel) — оставить текущую conditional логику.

    **Шаг C — заменить `tests/template-picker.test.ts` (stub → GREEN):**

    ```typescript
    import { describe, it, expect } from "vitest"
    import { groupTemplatesForPicker } from "@/components/support/templates/TemplatePickerModal"

    function t(overrides: Partial<{ id: string; name: string; text: string; channel: "FEEDBACK"|"QUESTION"|"CHAT"; situationTag: string | null; nmId: number | null; isActive: boolean }>) {
      return {
        id: overrides.id ?? "t-" + Math.random(),
        name: overrides.name ?? "Name",
        text: overrides.text ?? "Text",
        channel: overrides.channel ?? "FEEDBACK",
        situationTag: overrides.situationTag ?? null,
        nmId: overrides.nmId ?? null,
        isActive: overrides.isActive ?? true,
        createdAt: new Date(),
        updatedAt: new Date(),
        createdById: null,
        updatedById: null,
      }
    }

    describe("groupTemplatesForPicker", () => {
      it("отделяет шаблоны с nmId === ticketNmId от общих", () => {
        const templates = [
          t({ name: "Для кружки", nmId: 12345 }),
          t({ name: "Общий", nmId: null }),
          t({ name: "Для другого", nmId: 99999 }),
        ]
        const { forNmId, general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: 12345 })
        expect(forNmId.map(x => x.name)).toEqual(["Для кружки"])
        expect(general.map(x => x.name).sort()).toEqual(["Для другого", "Общий"])
      })

      it("при ticketNmId=null forNmId пустой, все остальные в general", () => {
        const templates = [t({ name: "A", nmId: null }), t({ name: "B", nmId: 555 })]
        const { forNmId, general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: null })
        expect(forNmId).toEqual([])
        expect(general).toHaveLength(2)
      })

      it("фильтрует по каналу", () => {
        const templates = [
          t({ name: "F1", channel: "FEEDBACK" }),
          t({ name: "Q1", channel: "QUESTION" }),
          t({ name: "C1", channel: "CHAT" }),
        ]
        const { general } = groupTemplatesForPicker(templates, { channel: "QUESTION", ticketNmId: null })
        expect(general.map(x => x.name)).toEqual(["Q1"])
      })

      it("исключает isActive=false", () => {
        const templates = [t({ name: "Active" }), t({ name: "Inactive", isActive: false })]
        const { general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: null })
        expect(general.map(x => x.name)).toEqual(["Active"])
      })

      it("фильтрация по query регистронезависимо", () => {
        const templates = [
          t({ name: "Привет", text: "Спасибо" }),
          t({ name: "Отказ", text: "К сожалению" }),
        ]
        const { general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: null, query: "ПРИвет" })
        expect(general).toHaveLength(1)
        expect(general[0].name).toBe("Привет")
      })

      it("query ищет по situationTag", () => {
        const templates = [t({ name: "N1", situationTag: "Положительный" })]
        const { general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: null, query: "положительный" })
        expect(general).toHaveLength(1)
      })

      it("пустой query возвращает все шаблоны канала", () => {
        const templates = [t({ name: "A" }), t({ name: "B" })]
        const { general } = groupTemplatesForPicker(templates, { channel: "FEEDBACK", ticketNmId: null, query: "" })
        expect(general).toHaveLength(2)
      })
    })
    ```

    **Запуск:** `npm run test -- tests/template-picker.test.ts` — минимум 7 passing.
  </action>
  <verify>
    <automated>npm run test -- tests/template-picker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/templates/TemplatePickerModal.tsx` (exit 0)
    - `grep "export function TemplatePickerModal" components/support/templates/TemplatePickerModal.tsx` находит строку
    - `grep "export function groupTemplatesForPicker" components/support/templates/TemplatePickerModal.tsx` находит строку
    - `grep "substituteTemplateVars" components/support/templates/TemplatePickerModal.tsx` находит вызов
    - `grep "TemplatePickerModal" components/support/ReplyPanel.tsx` находит импорт + render
    - `grep "ticketNmId" components/support/ReplyPanel.tsx` находит новый prop
    - `grep "FileText" components/support/ReplyPanel.tsx` находит иконку кнопки
    - `grep -c "it.skip" tests/template-picker.test.ts` == 0
    - `npm run test -- tests/template-picker.test.ts` exit 0 (минимум 7 passing)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    TemplatePickerModal создан + встроен в ReplyPanel, substituteTemplateVars вызывается при onPick, tests/template-picker.test.ts заменён с 7+ GREEN тестами группировки/фильтрации.
  </done>
</task>

</tasks>

<verification>
После всех задач:
1. `npx tsc --noEmit` exit 0
2. `npm run test` exit 0 (template-vars + response-templates + template-picker GREEN, appeal-actions skipped)
3. Страница `/support/templates` существует, требует requireSection("SUPPORT")
4. TemplatePickerModal встроен в ReplyPanel
5. Sidebar обновлён
6. Validation Map (11-VALIDATION.md) обновляется при outcome: 11-01, 11-02, 11-03 GREEN
</verification>

<success_criteria>
Plan 11-03 завершён когда:
- /support/templates: таблица + фильтры + Экспорт/Импорт/Новый работают
- Формы create/edit с Zod + native select
- TemplatePickerModal встроен в ReplyPanel, работает substitution через substituteTemplateVars
- Sidebar «Шаблоны ответов»
- 7+ GREEN тестов группировки/фильтрации
- TODO для Phase 10 execute зафиксирован в SUMMARY (интеграция picker в ChatReplyPanel)
- Commit `feat(11-03): страница шаблонов + picker modal + интеграция в ReplyPanel`
</success_criteria>

<output>
После завершения создать `.planning/phases/11-templates-appeals/11-03-SUMMARY.md`:
- Созданные файлы (9 новых + 2 обновлённых)
- Интеграционные точки для Plan 11-04 (ReplyPanel уже расширен — 11-04 добавляет кнопку «Обжаловать» рядом с «Шаблон»)
- **TODO Phase 10 execute**: «Plan 10-03 ChatReplyPanel должен интегрировать TemplatePickerModal с channel=\"CHAT\" (паттерн из ReplyPanel 11-03)»
- Validation Map update
</output>
