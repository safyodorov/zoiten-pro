---
phase: 11-templates-appeals
plan: 04
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-03
files_modified:
  - app/actions/appeals.ts
  - components/support/AppealModal.tsx
  - components/support/AppealStatusPanel.tsx
  - components/support/ReplyPanel.tsx
  - components/support/TicketSidePanel.tsx
  - components/support/SupportTicketCard.tsx
  - tests/appeal-actions.test.ts
autonomous: false
requirements:
  - SUP-14
  - SUP-29
  - SUP-31
must_haves:
  truths:
    - "app/actions/appeals.ts экспортирует 2 server actions: createAppeal(ticketId, reason, text) и updateAppealStatus(appealId, status) — оба с requireSection(\"SUPPORT\", \"MANAGE\")"
    - "createAppeal валидирует reason ∈ APPEAL_REASONS, text [10..1000] символов, channel === FEEDBACK, отсутствие существующего AppealRecord для ticketId, и в транзакции создаёт AppealRecord(PENDING) + обновляет ticket.status=APPEALED + appealStatus=PENDING + appealedAt=now + appealId=record.id"
    - "updateAppealStatus переводит PENDING → APPROVED|REJECTED (или обратно), auto-set appealResolvedAt + resolvedById, также обновляет ticket.appealStatus + ticket.appealResolvedAt"
    - "ReplyPanel расширена кнопкой «Обжаловать» (Flag icon) — условный render только для ticketChannel === FEEDBACK, открывает AppealModal"
    - "AppealModal: native <select> причины из APPEAL_REASONS (8 значений) + textarea text (counter 10/1000) + кнопка «Создать запись и открыть WB». Submit → createAppeal → при ok открывает window.open('https://seller.wildberries.ru/feedbacks-and-questions/') в новой вкладке + toast с инструкцией"
    - "TicketSidePanel: для ticket.status === APPEALED рендерится AppealStatusPanel с native <select> status (🕐 Ещё ожидание / ✅ Одобрено WB / ❌ Отклонено WB) + даты создания/разрешения"
    - "SupportTicketCard: inline бейдж рядом со статусом если ticket.appealStatus !== null: 🕐 «Обжалование» / ✅ «Обжалование одобрено» / ❌ «Отклонено WB»"
    - "SUP-30 НЕ реализуется — нет WB polling API (явно задокументировано в SUMMARY и objective); никакого cron endpoint, никакого appeal sync"
    - "tests/appeal-actions.test.ts GREEN — минимум 7 test cases покрывают happy path, RBAC, Zod отклонения, duplicate guard, channel guard, state transitions, resolvedAt auto-set"
    - "Task 4 (checkpoint:human-verify) — deploy + UAT на VPS: создание обжалования, ручной переключатель, индикатор в ленте, export/import JSON"
  artifacts:
    - path: "app/actions/appeals.ts"
      provides: "2 server actions для hybrid manual обжалований"
      exports: ["createAppeal", "updateAppealStatus"]
    - path: "components/support/AppealModal.tsx"
      provides: "Client модалка создания обжалования с jump-link на WB кабинет"
      exports: ["AppealModal"]
    - path: "components/support/AppealStatusPanel.tsx"
      provides: "Client блок в TicketSidePanel — ручной переключатель статуса обжалования"
      exports: ["AppealStatusPanel"]
    - path: "components/support/ReplyPanel.tsx"
      provides: "ReplyPanel с кнопкой «Обжаловать» для FEEDBACK (дополняет Plan 11-03)"
      contains: "AppealModal"
    - path: "components/support/TicketSidePanel.tsx"
      provides: "TicketSidePanel с условным AppealStatusPanel для APPEALED тикетов"
      contains: "AppealStatusPanel"
    - path: "components/support/SupportTicketCard.tsx"
      provides: "SupportTicketCard с inline indicator обжалования"
      contains: "appealStatus"
    - path: "tests/appeal-actions.test.ts"
      provides: "Unit тесты server actions с Prisma $transaction mock"
      contains: "describe"
  key_links:
    - from: "app/actions/appeals.ts createAppeal"
      to: "prisma.$transaction"
      via: "AppealRecord.create + supportTicket.update в одной транзакции"
      pattern: "\\$transaction"
    - from: "app/actions/appeals.ts"
      to: "lib/appeal-reasons.ts APPEAL_REASONS"
      via: "import + Zod validation z.enum(APPEAL_REASONS)"
      pattern: "APPEAL_REASONS"
    - from: "components/support/AppealModal.tsx"
      to: "app/actions/appeals.ts createAppeal"
      via: "client → server action + window.open WB cabinet URL"
      pattern: "createAppeal"
    - from: "components/support/AppealModal.tsx"
      to: "seller.wildberries.ru (внешний jump-link)"
      via: "window.open('https://seller.wildberries.ru/feedbacks-and-questions/')"
      pattern: "seller\\.wildberries\\.ru"
    - from: "components/support/AppealStatusPanel.tsx"
      to: "app/actions/appeals.ts updateAppealStatus"
      via: "onChange native select → server action + toast"
      pattern: "updateAppealStatus"
---

<objective>
Phase 11 Plan 04 — финальный UX-слой обжалований + UAT:

1. **app/actions/appeals.ts** — 2 server actions:
   - `createAppeal(ticketId, reason, text)` — Zod + duplicate guard + channel guard + транзакция (AppealRecord.create + SupportTicket.update)
   - `updateAppealStatus(appealId, status)` — PENDING ↔ APPROVED ↔ REJECTED с auto-set appealResolvedAt/resolvedById, синхронизация SupportTicket.appealStatus/appealResolvedAt

2. **AppealModal.tsx** (client) — native `<select>` причины (APPEAL_REASONS) + textarea (counter 10/1000) + submit: вызов createAppeal + открытие `seller.wildberries.ru/feedbacks-and-questions/` в новой вкладке + toast инструкция («Найдите отзыв в кабинете WB и подайте жалобу вручную»).

3. **ReplyPanel** (дополнение после Plan 11-03) — добавить кнопку «Обжаловать» (Flag icon) рядом с «Шаблон», видна только для `ticketChannel === "FEEDBACK"`.

4. **AppealStatusPanel.tsx** (client) — встраивается в TicketSidePanel для APPEALED тикетов: `<select>` с 3 опциями (🕐 Ещё ожидание / ✅ Одобрено WB / ❌ Отклонено WB) → `updateAppealStatus`.

5. **SupportTicketCard** — inline бейдж обжалования в ленте (`appealStatus` из тикета).

6. **tests/appeal-actions.test.ts** — замена Wave 0 stub на 7+ GREEN (Prisma mock + $transaction + RBAC).

7. **Checkpoint:human-verify** — deploy на VPS (prisma migrate deploy) + UAT: создание обжалования, ручное обновление статуса, индикатор в ленте, Export/Import JSON.

**КРИТИЧЕСКИ ВАЖНО — удалённые requirements:**
- **SUP-07 дополнение (cron обжалований 1 час)** — НЕ реализуется. Нет WB polling API (research §WB Report/Complaint API — СТАТУС ОТКЛЮЧЕНО, endpoint никогда не поддерживал GET статуса жалобы). Menedger меняет статус вручную.
- **SUP-30 (cron поллинг)** — НЕ реализуется по той же причине. Удалён из scope.

Эти удаления отражены в ROADMAP.md Phase 11 success criteria (обновление в рамках plan).

Purpose: Менеджер полностью закрывает петлю обжалования отзыва без WB API: создаёт локальную запись → jump-link в ЛК WB → вручную подаёт жалобу → возвращается в ERP → переключает статус. WB API отключён 2025-12-08 (research §WB Report/Complaint API — СТАТУС ОТКЛЮЧЕНО). Реализация 100% локальная, запись переживёт возможное восстановление WB API.

Output: 1 новый server actions файл, 2 новых client компонента (AppealModal, AppealStatusPanel), 3 обновлённых компонента (ReplyPanel + TicketSidePanel + SupportTicketCard), 1 обновлённый тест (stub → GREEN), 1 UAT checkpoint, обновлённый ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-templates-appeals/11-RESEARCH.md
@.planning/phases/11-templates-appeals/11-VALIDATION.md
@.planning/phases/11-templates-appeals/11-01-PLAN.md
@.planning/phases/11-templates-appeals/11-02-PLAN.md
@.planning/phases/11-templates-appeals/11-03-PLAN.md
@.planning/phases/09-returns/09-04-PLAN.md
@CLAUDE.md
@components/support/ReplyPanel.tsx
@components/support/TicketSidePanel.tsx
@components/support/SupportTicketCard.tsx
@app/actions/support.ts
@lib/prisma.ts
@lib/rbac.ts
@lib/appeal-reasons.ts

<interfaces>
Prisma AppealRecord (Plan 11-01):
```prisma
model AppealRecord {
  id               String  @id @default(cuid())
  ticketId         String  @unique
  reason           String
  text             String  @db.Text
  status           AppealStatus @default(PENDING)
  createdById      String
  resolvedById     String?
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  appealResolvedAt DateTime?
}
```

AppealStatus enum (существует с Phase 8): NONE | PENDING | APPROVED | REJECTED.

SupportTicket поля (существующие + новые из 11-01):
```prisma
status: TicketStatus         // NEW | IN_PROGRESS | ANSWERED | CLOSED | APPEALED
appealStatus: AppealStatus?  // существует
appealId: String?            // существует — в 11-04 получает значение AppealRecord.id
appealedAt: DateTime?        // Plan 11-01
appealResolvedAt: DateTime?  // Plan 11-01
```

APPEAL_REASONS (lib/appeal-reasons.ts из Plan 11-01):
```typescript
export const APPEAL_REASONS = [
  "Оскорбительные выражения, нецензурная лексика",
  "Реклама конкурентов или сторонних сервисов",
  ...
] as const
```

Плагин паттернов:
- `app/actions/support.ts` (Phase 8+9) — getSessionUserId, requireSection, try/catch, revalidatePath
- `components/support/ReplyPanel.tsx` (после Plan 11-03) — уже содержит кнопку «Шаблон», добавляем «Обжаловать»
- `components/support/TicketSidePanel.tsx` — MANUAL_STATUSES (не включает APPEALED) — не меняем логику, добавляем условный блок AppealStatusPanel
- `components/support/SupportTicketCard.tsx` — текущие бейджи статуса + новый бейдж обжалования

**WB кабинет jump-link:**
Базовый URL: `https://seller.wildberries.ru/feedbacks-and-questions/`
Deep-link с якорем отзыва: `https://seller.wildberries.ru/feedbacks-and-questions/#feedback-${wbExternalId}` (research §Open Questions Q6 — deep-link возможен, но не верифицирован; фallback — общая страница + toast инструкция).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: app/actions/appeals.ts — 2 server actions + tests GREEN</name>
  <files>
    app/actions/appeals.ts,
    tests/appeal-actions.test.ts
  </files>
  <read_first>
    - .planning/phases/11-templates-appeals/11-RESEARCH.md (§Code Examples строки 827-904 — createAppeal reference + §State Machine строки 596-640)
    - .planning/phases/11-templates-appeals/11-01-PLAN.md (схема AppealRecord)
    - lib/appeal-reasons.ts (APPEAL_REASONS из Plan 11-01)
    - app/actions/support.ts (паттерн action + $transaction)
    - tests/appeal-actions.test.ts (stub — заменяется GREEN)
    - tests/return-actions.test.ts (паттерн $transaction mock в Phase 9 — dual-mode callback+array)
  </read_first>
  <behavior>
    **createAppeal({ ticketId, reason, text })**:
    - requireSection("SUPPORT", "MANAGE")
    - Zod: reason ∈ APPEAL_REASONS (8 значений), text trim min 10 max 1000, ticketId cuid
    - Найти ticket с `include: { appealRecord: true }` (select минимум id, channel, status, appealRecord.id)
    - Guards (в порядке):
      1. ticket не найден → `{ ok: false, error: "Тикет не найден" }`
      2. ticket.channel !== "FEEDBACK" → «Обжаловать можно только отзывы»
      3. ticket.appealRecord существует → «Обжалование уже создано»
    - Транзакция ($transaction callback — чтобы получить record.id и обновить ticket.appealId):
      ```typescript
      const record = await prisma.$transaction(async (tx) => {
        const r = await tx.appealRecord.create({
          data: { ticketId, reason, text, status: "PENDING", createdById: userId },
        })
        await tx.supportTicket.update({
          where: { id: ticketId },
          data: {
            status: "APPEALED",
            appealStatus: "PENDING",
            appealedAt: new Date(),
            appealId: r.id,
          },
        })
        return r
      })
      ```
    - revalidatePath("/support") + revalidatePath(`/support/${ticketId}`)
    - return { ok: true, id: record.id }

    **updateAppealStatus({ appealId, status })**:
    - requireSection("SUPPORT", "MANAGE")
    - Zod: status ∈ ["PENDING", "APPROVED", "REJECTED"], appealId cuid
    - Найти AppealRecord → если not found → ошибка
    - `appealResolvedAt = status === "PENDING" ? null : new Date()`
    - `resolvedById = status === "PENDING" ? null : userId`
    - Транзакция:
      ```typescript
      await prisma.$transaction([
        prisma.appealRecord.update({
          where: { id: appealId },
          data: { status, appealResolvedAt, resolvedById },
        }),
        prisma.supportTicket.update({
          where: { id: record.ticketId },
          data: { appealStatus: status, appealResolvedAt },
        }),
      ])
      ```
    - revalidatePath
    - return { ok: true }

    **State machine** (research §State Machine):
    - PENDING → APPROVED | REJECTED — стандартный переход
    - APPROVED ↔ REJECTED напрямую — разрешён (менеджер исправил ошибку)
    - APPROVED | REJECTED → PENDING — разрешён (менеджер откатывает) → appealResolvedAt сбрасывается в null
  </behavior>
  <action>
    **Шаг A — создать `app/actions/appeals.ts`:**

    ```typescript
    "use server"

    // Server actions трекера обжалований отзывов.
    // WB API /api/v1/feedbacks/actions отключён 2025-12-08 — все операции локальные.
    // Менеджер создаёт запись + подаёт жалобу в ЛК WB вручную (hybrid manual).
    // SUP-30 (cron polling) НЕ реализован — нет WB API для опроса статуса.

    import { revalidatePath } from "next/cache"
    import { prisma } from "@/lib/prisma"
    import { requireSection } from "@/lib/rbac"
    import { auth } from "@/lib/auth"
    import { z } from "zod"
    import { APPEAL_REASONS } from "@/lib/appeal-reasons"
    import { Prisma } from "@prisma/client"

    export type ActionResult<T = void> =
      | ({ ok: true } & (T extends void ? {} : T))
      | { ok: false; error: string }

    async function getSessionUserId(): Promise<string | null> {
      const session = await auth()
      return session?.user?.id ?? null
    }

    const createAppealSchema = z.object({
      ticketId: z.string().cuid(),
      reason: z.enum(APPEAL_REASONS, {
        errorMap: () => ({ message: "Недопустимая причина обжалования" }),
      }),
      text: z.string().trim().min(10, "Минимум 10 символов").max(1000, "Максимум 1000 символов"),
    })

    export async function createAppeal(
      input: z.input<typeof createAppealSchema>
    ): Promise<ActionResult<{ id: string }>> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const data = createAppealSchema.parse(input)

        const ticket = await prisma.supportTicket.findUnique({
          where: { id: data.ticketId },
          select: {
            id: true,
            channel: true,
            status: true,
            appealRecord: { select: { id: true } },
          },
        })
        if (!ticket) return { ok: false, error: "Тикет не найден" }
        if (ticket.channel !== "FEEDBACK") return { ok: false, error: "Обжаловать можно только отзывы" }
        if (ticket.appealRecord) return { ok: false, error: "Обжалование уже создано" }

        const record = await prisma.$transaction(async (tx) => {
          const r = await tx.appealRecord.create({
            data: {
              ticketId: data.ticketId,
              reason: data.reason,
              text: data.text,
              status: "PENDING",
              createdById: userId,
            },
          })
          await tx.supportTicket.update({
            where: { id: data.ticketId },
            data: {
              status: "APPEALED",
              appealStatus: "PENDING",
              appealedAt: new Date(),
              appealId: r.id,
            },
          })
          return r
        })

        revalidatePath("/support")
        revalidatePath(`/support/${data.ticketId}`)
        return { ok: true, id: record.id }
      } catch (err) {
        if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === "P2002") {
          return { ok: false, error: "Обжалование уже создано" }
        }
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }

    const updateAppealStatusSchema = z.object({
      appealId: z.string().cuid(),
      status: z.enum(["PENDING", "APPROVED", "REJECTED"] as const),
    })

    export async function updateAppealStatus(
      input: z.input<typeof updateAppealStatusSchema>
    ): Promise<ActionResult> {
      try {
        await requireSection("SUPPORT", "MANAGE")
        const userId = await getSessionUserId()
        if (!userId) return { ok: false, error: "Сессия без user.id" }

        const data = updateAppealStatusSchema.parse(input)

        const record = await prisma.appealRecord.findUnique({
          where: { id: data.appealId },
          select: { id: true, ticketId: true },
        })
        if (!record) return { ok: false, error: "Запись обжалования не найдена" }

        const now = new Date()
        const resolved = data.status !== "PENDING"
        const resolvedAt: Date | null = resolved ? now : null
        const resolvedById: string | null = resolved ? userId : null

        await prisma.$transaction([
          prisma.appealRecord.update({
            where: { id: data.appealId },
            data: { status: data.status, appealResolvedAt: resolvedAt, resolvedById },
          }),
          prisma.supportTicket.update({
            where: { id: record.ticketId },
            data: { appealStatus: data.status, appealResolvedAt: resolvedAt },
          }),
        ])

        revalidatePath("/support")
        revalidatePath(`/support/${record.ticketId}`)
        return { ok: true }
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : "Ошибка" }
      }
    }
    ```

    **Шаг B — заменить `tests/appeal-actions.test.ts` (stub → GREEN):**

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest"

    vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))
    vi.mock("@/lib/rbac", () => ({ requireSection: vi.fn().mockResolvedValue({}) }))
    vi.mock("@/lib/auth", () => ({ auth: vi.fn().mockResolvedValue({ user: { id: "user-1" } }) }))

    const prismaMock = {
      supportTicket: { findUnique: vi.fn(), update: vi.fn() },
      appealRecord: { create: vi.fn(), findUnique: vi.fn(), update: vi.fn() },
      $transaction: vi.fn(),
    }
    vi.mock("@/lib/prisma", () => ({ prisma: prismaMock }))

    // Dual-mode $transaction mock (pattern из Phase 9): callback или array
    beforeEach(() => {
      vi.resetAllMocks()
      prismaMock.$transaction.mockImplementation(async (arg) => {
        if (typeof arg === "function") return arg(prismaMock)
        return Promise.all(arg)
      })
    })

    describe("createAppeal", () => {
      it("happy path: FEEDBACK без обжалования → AppealRecord + ticket.status=APPEALED", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        prismaMock.supportTicket.findUnique.mockResolvedValueOnce({
          id: "tk1", channel: "FEEDBACK", status: "NEW", appealRecord: null,
        })
        prismaMock.appealRecord.create.mockResolvedValueOnce({ id: "ap1" })
        prismaMock.supportTicket.update.mockResolvedValueOnce({})

        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Оскорбительные выражения, нецензурная лексика",
          text: "Отзыв содержит нецензурные выражения в адрес продавца",
        })
        expect(res).toEqual({ ok: true, id: "ap1" })
        expect(prismaMock.appealRecord.create).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ status: "PENDING", createdById: "user-1" }),
        }))
        expect(prismaMock.supportTicket.update).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ status: "APPEALED", appealStatus: "PENDING", appealId: "ap1" }),
        }))
      })

      it("отклоняет reason не из APPEAL_REASONS", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Произвольная причина" as never,
          text: "достаточно длинный текст",
        })
        expect(res.ok).toBe(false)
      })

      it("отклоняет text < 10 символов", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Другое",
          text: "короткий",
        })
        expect(res.ok).toBe(false)
        if (!res.ok) expect(res.error).toMatch(/Минимум 10/)
      })

      it("отклоняет text > 1000 символов", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Другое",
          text: "x".repeat(1001),
        })
        expect(res.ok).toBe(false)
      })

      it("отклоняет не-FEEDBACK канал", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        prismaMock.supportTicket.findUnique.mockResolvedValueOnce({
          id: "tk1", channel: "QUESTION", status: "NEW", appealRecord: null,
        })
        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Другое",
          text: "валидный текст обжалования",
        })
        expect(res).toEqual({ ok: false, error: "Обжаловать можно только отзывы" })
      })

      it("отклоняет duplicate — если appealRecord уже есть", async () => {
        const { createAppeal } = await import("@/app/actions/appeals")
        prismaMock.supportTicket.findUnique.mockResolvedValueOnce({
          id: "tk1", channel: "FEEDBACK", status: "APPEALED", appealRecord: { id: "existing" },
        })
        const res = await createAppeal({
          ticketId: "ckxyz00000000000000000001",
          reason: "Другое",
          text: "валидный текст обжалования",
        })
        expect(res).toEqual({ ok: false, error: "Обжалование уже создано" })
      })
    })

    describe("updateAppealStatus", () => {
      it("PENDING → APPROVED обновляет record + ticket с appealResolvedAt + resolvedById", async () => {
        const { updateAppealStatus } = await import("@/app/actions/appeals")
        prismaMock.appealRecord.findUnique.mockResolvedValueOnce({ id: "ap1", ticketId: "tk1" })
        prismaMock.appealRecord.update.mockResolvedValueOnce({})
        prismaMock.supportTicket.update.mockResolvedValueOnce({})

        const res = await updateAppealStatus({
          appealId: "ckxyz00000000000000000001",
          status: "APPROVED",
        })
        expect(res).toEqual({ ok: true })
        expect(prismaMock.appealRecord.update).toHaveBeenCalledWith(expect.objectContaining({
          data: expect.objectContaining({ status: "APPROVED", resolvedById: "user-1" }),
        }))
        const appealUpdate = prismaMock.appealRecord.update.mock.calls[0][0]
        expect(appealUpdate.data.appealResolvedAt).toBeInstanceOf(Date)
      })

      it("APPROVED → PENDING сбрасывает appealResolvedAt в null", async () => {
        const { updateAppealStatus } = await import("@/app/actions/appeals")
        prismaMock.appealRecord.findUnique.mockResolvedValueOnce({ id: "ap1", ticketId: "tk1" })
        prismaMock.appealRecord.update.mockResolvedValueOnce({})
        prismaMock.supportTicket.update.mockResolvedValueOnce({})

        await updateAppealStatus({
          appealId: "ckxyz00000000000000000001",
          status: "PENDING",
        })
        const appealUpdate = prismaMock.appealRecord.update.mock.calls[0][0]
        expect(appealUpdate.data.appealResolvedAt).toBeNull()
        expect(appealUpdate.data.resolvedById).toBeNull()
      })

      it("returns error если AppealRecord не найден", async () => {
        const { updateAppealStatus } = await import("@/app/actions/appeals")
        prismaMock.appealRecord.findUnique.mockResolvedValueOnce(null)
        const res = await updateAppealStatus({
          appealId: "ckxyz00000000000000000001",
          status: "APPROVED",
        })
        expect(res).toEqual({ ok: false, error: "Запись обжалования не найдена" })
      })
    })
    ```

    **Запуск:** `npm run test -- tests/appeal-actions.test.ts` — 9 passing.
  </action>
  <verify>
    <automated>npm run test -- tests/appeal-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/actions/appeals.ts` (exit 0)
    - `grep -c "export async function createAppeal" app/actions/appeals.ts` == 1
    - `grep -c "export async function updateAppealStatus" app/actions/appeals.ts` == 1
    - `grep -c "requireSection(\"SUPPORT\", \"MANAGE\")" app/actions/appeals.ts` >= 2
    - `grep "APPEAL_REASONS" app/actions/appeals.ts` находит импорт
    - `grep "\\$transaction" app/actions/appeals.ts | wc -l` >= 2
    - `grep "appealedAt" app/actions/appeals.ts` находит установку поля
    - `grep "appealResolvedAt" app/actions/appeals.ts` находит установку поля
    - `grep -c "it.skip" tests/appeal-actions.test.ts` == 0
    - `npm run test -- tests/appeal-actions.test.ts` exit 0 (минимум 7 passing)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    app/actions/appeals.ts экспортирует 2 server actions с RBAC + Zod + транзакция; tests/appeal-actions.test.ts заменён с 9 GREEN тестами (RBAC, Zod, happy paths, state transitions).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 2: AppealModal + AppealStatusPanel + интеграция в ReplyPanel, TicketSidePanel, SupportTicketCard</name>
  <files>
    components/support/AppealModal.tsx,
    components/support/AppealStatusPanel.tsx,
    components/support/ReplyPanel.tsx,
    components/support/TicketSidePanel.tsx,
    components/support/SupportTicketCard.tsx
  </files>
  <read_first>
    - .planning/phases/11-templates-appeals/11-RESEARCH.md (§UI Architecture строки 458-560 — AppealModal + AppealStatusPanel layout)
    - components/support/ReplyPanel.tsx (ПОСЛЕ Plan 11-03 — уже содержит кнопку «Шаблон»)
    - components/support/TicketSidePanel.tsx (текущий список статусов)
    - components/support/SupportTicketCard.tsx (текущие бейджи)
    - components/ui/dialog.tsx (shadcn wrapper)
    - lib/appeal-reasons.ts (APPEAL_REASONS 8 значений)
    - CLAUDE.md (русский, native select)
  </read_first>
  <action>
    **Шаг A — создать `components/support/AppealModal.tsx`:**

    ```typescript
    "use client"

    import { useState, useTransition } from "react"
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog"
    import { Button } from "@/components/ui/button"
    import { toast } from "sonner"
    import { createAppeal } from "@/app/actions/appeals"
    import { APPEAL_REASONS } from "@/lib/appeal-reasons"

    export interface AppealModalProps {
      open: boolean
      onOpenChange: (open: boolean) => void
      ticketId: string
      wbExternalId: string | null
    }

    export function AppealModal({ open, onOpenChange, ticketId, wbExternalId }: AppealModalProps) {
      const [reason, setReason] = useState<(typeof APPEAL_REASONS)[number] | "">("")
      const [text, setText] = useState("")
      const [isPending, startTransition] = useTransition()

      function onSubmit() {
        if (!reason) { toast.error("Выберите причину"); return }
        if (text.trim().length < 10) { toast.error("Текст от 10 символов"); return }
        if (text.length > 1000) { toast.error("Текст до 1000 символов"); return }

        startTransition(async () => {
          const res = await createAppeal({ ticketId, reason, text: text.trim() })
          if (!res.ok) { toast.error(res.error); return }
          toast.success("Запись обжалования создана. Подайте жалобу в ЛК WB.")
          // Open WB cabinet in new tab (deep-link если есть wbExternalId, иначе общая страница)
          const wbUrl = wbExternalId
            ? `https://seller.wildberries.ru/feedbacks-and-questions/#feedback-${wbExternalId}`
            : `https://seller.wildberries.ru/feedbacks-and-questions/`
          window.open(wbUrl, "_blank", "noopener")
          onOpenChange(false)
          setReason("")
          setText("")
        })
      }

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="max-w-xl">
            <DialogHeader>
              <DialogTitle>Обжаловать отзыв</DialogTitle>
            </DialogHeader>

            <div className="space-y-3">
              <div>
                <label className="text-sm font-medium">Причина</label>
                <select
                  value={reason}
                  onChange={(e) => setReason(e.target.value as typeof reason)}
                  className="w-full rounded-md border px-3 py-2 text-sm mt-1"
                  disabled={isPending}
                >
                  <option value="">— Выберите причину —</option>
                  {APPEAL_REASONS.map((r) => (
                    <option key={r} value={r}>{r}</option>
                  ))}
                </select>
              </div>

              <div>
                <label className="text-sm font-medium">
                  Комментарий <span className="text-xs text-muted-foreground">({text.length}/1000)</span>
                </label>
                <textarea
                  value={text}
                  onChange={(e) => setText(e.target.value)}
                  placeholder="Опишите причину обжалования подробно (10–1000 символов)..."
                  maxLength={1000}
                  rows={5}
                  disabled={isPending}
                  className="w-full rounded-md border px-3 py-2 text-sm mt-1 resize-none"
                />
              </div>

              <div className="rounded-md border border-orange-200 bg-orange-50 dark:bg-orange-950/20 p-3 text-sm">
                <p className="font-medium mb-1">Важно</p>
                <p>WB API для обжалования отключён с 08.12.2025. После сохранения:</p>
                <ol className="list-decimal pl-5 mt-1 space-y-0.5">
                  <li>Мы откроем ЛК Wildberries в новой вкладке.</li>
                  <li>Найдите отзыв и нажмите «Пожаловаться на отзыв».</li>
                  <li>Укажите ту же причину и текст.</li>
                  <li>Когда WB ответит — обновите статус в карточке тикета.</li>
                </ol>
              </div>
            </div>

            <DialogFooter>
              <Button variant="outline" onClick={() => onOpenChange(false)} disabled={isPending}>
                Отмена
              </Button>
              <Button onClick={onSubmit} disabled={isPending || !reason || text.trim().length < 10}>
                {isPending ? "Сохранение..." : "Создать запись и открыть WB"}
              </Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      )
    }
    ```

    **Шаг B — создать `components/support/AppealStatusPanel.tsx`:**

    ```typescript
    "use client"

    import { useTransition } from "react"
    import { toast } from "sonner"
    import { updateAppealStatus } from "@/app/actions/appeals"
    import type { AppealStatus } from "@prisma/client"

    export interface AppealStatusPanelProps {
      appealId: string
      currentStatus: AppealStatus
      reason: string
      createdAt: Date | string
      appealResolvedAt: Date | string | null
    }

    export function AppealStatusPanel({
      appealId, currentStatus, reason, createdAt, appealResolvedAt,
    }: AppealStatusPanelProps) {
      const [isPending, startTransition] = useTransition()

      function onChange(e: React.ChangeEvent<HTMLSelectElement>) {
        const next = e.target.value as "PENDING" | "APPROVED" | "REJECTED"
        if (next === currentStatus) return
        startTransition(async () => {
          const res = await updateAppealStatus({ appealId, status: next })
          if (res.ok) toast.success("Статус обжалования обновлён")
          else toast.error(res.error)
        })
      }

      const fmt = (d: Date | string | null) =>
        d
          ? new Intl.DateTimeFormat("ru-RU", { timeZone: "Europe/Moscow", dateStyle: "short", timeStyle: "short" }).format(new Date(d))
          : "—"

      return (
        <div className="rounded-md border border-purple-300 bg-purple-50 dark:border-purple-900 dark:bg-purple-950/20 p-3 space-y-2">
          <h3 className="text-sm font-medium">Статус обжалования</h3>
          <select
            value={currentStatus === "NONE" ? "PENDING" : currentStatus}
            onChange={onChange}
            disabled={isPending}
            className="w-full rounded-md border bg-background px-3 py-2 text-sm"
          >
            <option value="PENDING">🕐 Ещё ожидание</option>
            <option value="APPROVED">✅ Одобрено WB</option>
            <option value="REJECTED">❌ Отклонено WB</option>
          </select>
          <div className="text-xs text-muted-foreground space-y-0.5">
            <div><span className="font-medium">Причина:</span> {reason}</div>
            <div><span className="font-medium">Создано:</span> {fmt(createdAt)}</div>
            <div><span className="font-medium">Решено:</span> {fmt(appealResolvedAt)}</div>
          </div>
        </div>
      )
    }
    ```

    **Шаг C — обновить `components/support/ReplyPanel.tsx`:**

    Дополнить результат Plan 11-03: добавить кнопку «Обжаловать» (Flag icon) рядом с «Шаблон», условный render для `ticketChannel === "FEEDBACK"`.

    Добавить в props:
    - `wbExternalId: string | null` (для deep-link на WB)

    Добавить state:
    - `appealOpen` + `setAppealOpen`

    Импорт:
    - `import { Flag, FileText, Send } from "lucide-react"`
    - `import { AppealModal } from "@/components/support/AppealModal"`

    В JSX:
    ```tsx
    {ticketChannel === "FEEDBACK" && (
      <Button size="sm" variant="outline" onClick={() => setAppealOpen(true)} disabled={disabled}>
        <Flag className="h-4 w-4 mr-1" /> Обжаловать
      </Button>
    )}
    // ...
    <AppealModal
      open={appealOpen}
      onOpenChange={setAppealOpen}
      ticketId={ticketId}
      wbExternalId={wbExternalId}
    />
    ```

    Обновить вызов в `app/(dashboard)/support/[ticketId]/page.tsx` — передать `wbExternalId={ticket.wbExternalId}`.

    **Шаг D — обновить `components/support/TicketSidePanel.tsx`:**

    Добавить conditional render `AppealStatusPanel` для `ticket.status === "APPEALED"` + наличие `appealRecord`. Компонент должен получить `appealRecord` через prop (из RSC page) или pre-fetched в родителе.

    Сигнатура расширения prop:
    ```typescript
    appealRecord?: {
      id: string
      reason: string
      status: AppealStatus
      createdAt: Date
      appealResolvedAt: Date | null
    } | null
    ```

    В JSX (над существующими status/assignee блоками):
    ```tsx
    {ticket.status === "APPEALED" && appealRecord && (
      <AppealStatusPanel
        appealId={appealRecord.id}
        currentStatus={appealRecord.status}
        reason={appealRecord.reason}
        createdAt={appealRecord.createdAt}
        appealResolvedAt={appealRecord.appealResolvedAt}
      />
    )}
    ```

    Обновить `app/(dashboard)/support/[ticketId]/page.tsx` — добавить `include: { appealRecord: true }` в findUnique.

    **Шаг E — обновить `components/support/SupportTicketCard.tsx`:**

    Добавить inline бейдж рядом со статусом, если `ticket.appealStatus !== null && ticket.appealStatus !== "NONE"`:

    ```tsx
    {ticket.appealStatus && ticket.appealStatus !== "NONE" && (
      <span className="inline-flex items-center gap-1 rounded border px-1.5 py-0.5 text-xs">
        {ticket.appealStatus === "PENDING" && <>🕐 Обжалование</>}
        {ticket.appealStatus === "APPROVED" && <>✅ Обжалование одобрено</>}
        {ticket.appealStatus === "REJECTED" && <>❌ Отклонено WB</>}
      </span>
    )}
    ```

    Убедиться что `ticket.appealStatus` включается в select из списка тикетов (в `/support/page.tsx` data fetch — если ещё нет).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/support/AppealModal.tsx && test -f components/support/AppealStatusPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/support/AppealModal.tsx` (exit 0)
    - `test -f components/support/AppealStatusPanel.tsx` (exit 0)
    - `grep "APPEAL_REASONS" components/support/AppealModal.tsx` находит импорт
    - `grep "seller.wildberries.ru" components/support/AppealModal.tsx` находит jump-link
    - `grep "updateAppealStatus" components/support/AppealStatusPanel.tsx` находит импорт
    - `grep "Flag" components/support/ReplyPanel.tsx` находит иконку (новая кнопка «Обжаловать»)
    - `grep "AppealModal" components/support/ReplyPanel.tsx` находит импорт + render
    - `grep "ticketChannel === \"FEEDBACK\"" components/support/ReplyPanel.tsx` находит условный render
    - `grep "AppealStatusPanel" components/support/TicketSidePanel.tsx` находит импорт
    - `grep "appealStatus" components/support/SupportTicketCard.tsx` находит бейдж
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    AppealModal + AppealStatusPanel созданы; ReplyPanel дополнен кнопкой «Обжаловать» для FEEDBACK с открытием WB кабинета; TicketSidePanel условно показывает AppealStatusPanel для APPEALED тикетов; SupportTicketCard имеет inline бейдж обжалования.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Задача 3: Update ROADMAP.md + commit + deploy preparation</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (строки 223-234 — текущий Phase 11 блок с Success Criteria)
    - .planning/phases/11-templates-appeals/11-RESEARCH.md (§Executive Summary — reformulation justification)
  </read_first>
  <action>
    Обновить `.planning/ROADMAP.md` блок Phase 11:

    **1. Requirements** — убрать SUP-07/SUP-30 из Phase 11 line (оба удалены scope):

    БЫЛО:
    ```
    **Requirements**: SUP-07 (дополнение — cron обжалований 1 час), SUP-14 (дополнение — кнопка «Обжаловать» в диалоге FEEDBACK), SUP-26, SUP-27, SUP-28, SUP-29, SUP-30, SUP-31
    ```

    СТАНЕТ:
    ```
    **Requirements**: SUP-14 (дополнение — кнопка «Обжаловать» в диалоге FEEDBACK), SUP-26, SUP-27 (переформулирован — Export/Import JSON вместо WB sync), SUP-28, SUP-29 (переформулирован — hybrid manual через ЛК WB), SUP-31

    > **Scope change (2026-04-17):** SUP-07 дополнение (cron обжалований 1 час) и SUP-30 (cron поллинг статуса) УДАЛЕНЫ — WB API для обжалований отключён 2025-12-08, GET поллинга статуса никогда не существовало (research/11-RESEARCH.md §WB Report/Complaint API). SUP-27 переформулирован (WB templates API отключён 2025-11-19). SUP-29 — hybrid manual workflow.
    ```

    **2. Success Criteria** — обновить #2 и #4, удалить cron-упоминания:

    БЫЛО:
    ```
    **Success Criteria** (what must be TRUE):
      1. Менеджер открывает `/support/templates` → CRUD шаблонов с полями Название, Канал (FEEDBACK/QUESTION/CHAT), Тег ситуации, Товар/Общий, Активен
      2. Менеджер жмёт «Синхронизировать шаблоны» → локальные шаблоны подтягивают `wbTemplateId` из WB, новые можно публиковать, обновлять и удалять в WB
      3. При ответе в диалоге кнопка «Выбрать шаблон» открывает модалку поиска по тексту/тегу с группировкой (сначала шаблоны для текущего nmId, потом общие) → выбор подставляет текст в textarea
      4. Менеджер жмёт «Обжаловать отзыв» в диалоге FEEDBACK → выбирает причину + свободный текст → `ticket.status=APPEALED`, `appealStatus=PENDING`, запрос уходит в WB
      5. Cron раз в час обновляет статусы обжалований PENDING → APPROVED | REJECTED, индикатор в ленте и карточке меняется: 🕐 ожидание / ✅ одобрено / ❌ отклонено
    ```

    СТАНЕТ:
    ```
    **Success Criteria** (what must be TRUE):
      1. Менеджер открывает `/support/templates` → CRUD шаблонов с полями Название, Канал (FEEDBACK/QUESTION/CHAT), Тег ситуации, Товар/Общий, Активен
      2. Менеджер жмёт «Экспорт» → скачивается JSON всех активных шаблонов; «Импорт» → загружает JSON → upsert по @@unique([name, channel]) с отчётом {добавлено, обновлено, ошибки}. WB sync не реализуется — WB Templates API отключён 2025-11-19
      3. При ответе в диалоге кнопка «Выбрать шаблон» открывает модалку поиска по тексту/тегу с группировкой (сначала шаблоны для текущего nmId, потом общие) → выбор подставляет текст в textarea с подстановкой {имя_покупателя} и {название_товара}
      4. Менеджер жмёт «Обжаловать» в диалоге FEEDBACK → выбирает причину из справочника + текст → создаётся локальный AppealRecord(PENDING), ticket.status=APPEALED, открывается новая вкладка seller.wildberries.ru для ручной подачи жалобы. WB API отключён 2025-12-08 — автоматизация невозможна
      5. Менеджер вручную переключает статус обжалования (PENDING ↔ APPROVED ↔ REJECTED) в TicketSidePanel когда WB ответит в ЛК; индикатор в ленте показывает 🕐/✅/❌
    ```

    **3. Plans** (строка `**Plans**: TBD`) — обновить:

    БЫЛО:
    ```
    **Plans**: TBD
    ```

    СТАНЕТ:
    ```
    **Plans**: 4 plans
    Plans:
    - [ ] 11-01-PLAN.md — Foundation: Prisma миграция templates_appeals + appeal reasons + template vars + Wave 0 stubs
    - [ ] 11-02-PLAN.md — Server actions CRUD шаблонов + Export/Import JSON (заменяет WB sync)
    - [ ] 11-03-PLAN.md — UI страница /support/templates + TemplatePickerModal + интеграция в ReplyPanel + sidebar
    - [ ] 11-04-PLAN.md — Appeals hybrid manual + индикатор + UAT
    ```

    **4. Progress table** — обновить Phase 11 row:

    БЫЛО:
    ```
    | 11. Шаблоны + Обжалование | 0/TBD | Planned | |
    ```

    СТАНЕТ:
    ```
    | 11. Шаблоны + Обжалование (reformulated — local-only + hybrid manual) | 0/4 | Planned | |
    ```
  </action>
  <verify>
    <automated>grep -c "11-01-PLAN.md" .planning/ROADMAP.md && grep -c "Export/Import JSON" .planning/ROADMAP.md && grep -c "hybrid manual" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "11-01-PLAN.md" .planning/ROADMAP.md` находит строку
    - `grep "11-04-PLAN.md" .planning/ROADMAP.md` находит строку
    - `grep "Export/Import JSON" .planning/ROADMAP.md` находит строку (в success criteria #2)
    - `grep "hybrid manual" .planning/ROADMAP.md` находит строку
    - `grep "SUP-07" .planning/ROADMAP.md | grep -c "Phase 11"` == 0 (убрано из Phase 11 line)
    - `grep "4 plans" .planning/ROADMAP.md | grep -A1 "Phase 11"` присутствует
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 11 блок отражает reformulated scope: success criteria обновлены (Export/Import JSON вместо WB sync; hybrid manual вместо автоматики; удалены cron-упоминания), Requirements не содержит SUP-07/SUP-30, Plans перечислены.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Deploy на VPS + Human UAT</name>
  <files>(no files written in this task — deploy + manual verification только)</files>
  <what-built>
    Phase 11 полностью: Prisma миграция templates_appeals + 2 новые модели + CRUD шаблонов + Export/Import JSON + TemplatePickerModal в ReplyPanel + AppealModal + AppealStatusPanel + SupportTicketCard бейдж обжалования. SUP-30 cron удалён из scope (нет WB polling API). ROADMAP.md обновлён.
  </what-built>
  <action>
    Это checkpoint: ВСЕ предыдущие задачи закоммичены и задеплоены, менеджер проходит UAT checklist (см. how-to-verify) и подтверждает approved. При багах — описывает шаг UAT и ошибку для точечной доработки.
  </action>
  <how-to-verify>
    **Шаг 1 — Commit всех Phase 11 изменений:**
    ```bash
    cd /Users/macmini/zoiten.pro
    git add prisma/schema.prisma prisma/migrations/ \
      lib/appeal-reasons.ts lib/template-vars.ts \
      app/actions/templates.ts app/actions/appeals.ts \
      app/\(dashboard\)/support/templates/ \
      components/support/templates/ \
      components/support/AppealModal.tsx components/support/AppealStatusPanel.tsx \
      components/support/ReplyPanel.tsx components/support/TicketSidePanel.tsx components/support/SupportTicketCard.tsx \
      components/layout/nav-items.ts \
      tests/template-vars.test.ts tests/response-templates.test.ts \
      tests/template-picker.test.ts tests/appeal-actions.test.ts \
      .planning/ROADMAP.md
    git commit -m "feat(11): шаблоны ответов + hybrid обжалования (WB API отключён)"
    git push
    ```

    **Шаг 2 — Deploy на VPS:**
    ```bash
    ssh root@85.198.97.89 "cd /opt/zoiten-pro && bash deploy.sh"
    ```
    Ожидать: `npx prisma migrate deploy` применит миграцию templates_appeals; systemd zoiten-erp перезапустится; сайт zoiten.pro доступен.

    **Шаг 3 — UAT checklist (ручное прохождение менеджером):**

    1. **Templates CRUD (SUP-26):**
       - Открыть `https://zoiten.pro/support/templates`
       - Кнопка «Новый шаблон» → заполнить (name=«Спасибо 5★», channel=FEEDBACK, situationTag=«Положительный», text=«{имя_покупателя}, спасибо за отзыв о {название_товара}!»)
       - Создать ещё 2 FEEDBACK + 1 QUESTION + 1 CHAT
       - Проверить таблицу: 5 строк, все isActive=true
       - Toggle active на одном → статус меняется
       - Edit одного → сохраняется

    2. **Template picker + substitution (SUP-28):**
       - Открыть реальный FEEDBACK тикет `/support/{ticketId}`
       - Нажать кнопку «Шаблон» в ReplyPanel
       - Видны только FEEDBACK шаблоны, группировка «Для этого товара» + «Общие»
       - Выбрать шаблон — текст с `{имя_покупателя}` и `{название_товара}` подставляется substituted (имя «покупатель» если нет имени, название товара из WbCard)
       - Отправить ответ → сохраняется в SupportMessage

    3. **Export/Import JSON (SUP-27):**
       - Кнопка «Экспорт» → скачивается `zoiten-templates-YYYY-MM-DD.json`, открыть файл — валидный JSON с version:1, exportedAt, templates[]
       - Удалить 1 шаблон из БД (deleteTemplate)
       - Кнопка «Импорт» → выбрать скачанный файл
       - Toast: «Добавлено: 1, обновлено: 4» (или подобно)
       - Удалённый шаблон восстановлен

    4. **Appeal creation + WB jump (SUP-29, SUP-14):**
       - В реальном FEEDBACK тикете нажать «Обжаловать»
       - Модалка открывается: dropdown причин (8 значений) + textarea с counter
       - Выбрать причину «Оскорбительные выражения», написать текст
       - Кнопка «Создать запись и открыть WB» активна
       - Submit → toast успеха + открывается новая вкладка `seller.wildberries.ru/feedbacks-and-questions/` (возможно с якорем #feedback-...)
       - Тикет в БД: status=APPEALED, appealStatus=PENDING, appealId=record.id
       - В ERP диалоге: TicketSidePanel показывает AppealStatusPanel

    5. **Manual status toggle (SUP-29, SUP-31):**
       - В AppealStatusPanel переключить select: «✅ Одобрено WB»
       - Toast успеха; AppealRecord.status=APPROVED, appealResolvedAt=now, resolvedById=current user
       - Тикет: appealStatus=APPROVED, appealResolvedAt=now
       - Вернуться в ленту `/support` — карточка тикета показывает бейдж ✅ «Обжалование одобрено»
       - Переключить обратно на «🕐 Ещё ожидание» → appealResolvedAt сбрасывается в null

    6. **Sidebar + доступ:**
       - Sidebar содержит «Шаблоны ответов» под «Служба поддержки»
       - VIEWER (не MANAGE) открывает `/support/templates` — видит таблицу (requireSection SUPPORT), но кнопки Создать/Удалить/Import/Export выдают ошибку RBAC

    7. **SUP-30 удалён** (проверка отсутствия):
       - Нет `/api/cron/support-sync-appeals` endpoint'a
       - `crontab -l` на VPS не содержит строки для appeals cron
       - Это OK — задокументировано в ROADMAP
  </how-to-verify>
  <verify>Manual UAT — menedgerom approval через resume-signal</verify>
  <done>Все 7 UAT шагов passed; VPS deploy success; менеджер типает "approved"</done>
  <resume-signal>Type "approved" или опишите баги (кратко, со ссылкой на шаг UAT)</resume-signal>
</task>

</tasks>

<verification>
После всех задач:
1. `npm run test` exit 0 (все 4 тест-файла GREEN: template-vars, response-templates, template-picker, appeal-actions)
2. `npx tsc --noEmit` exit 0
3. `npx prisma validate` exit 0
4. Commit создан с полным diff Phase 11
5. VPS deploy успешен (systemd zoiten-erp active)
6. UAT checklist: все 7 шагов passed
7. ROADMAP.md Phase 11 reformulated scope видим
</verification>

<success_criteria>
Plan 11-04 завершён когда:
- app/actions/appeals.ts с 2 server actions + 9 GREEN тестов
- AppealModal + AppealStatusPanel созданы, интегрированы в ReplyPanel/TicketSidePanel/SupportTicketCard
- ROADMAP.md Phase 11 отражает reformulated scope (Export/Import JSON, hybrid manual, SUP-07/SUP-30 удалены)
- Phase 11 deployed на VPS, миграция templates_appeals применена
- UAT menedgerом passed (approved)
- Commit `feat(11-04): hybrid обжалования (AppealModal + jump WB cabinet) + UAT`
</success_criteria>

<output>
После завершения создать `.planning/phases/11-templates-appeals/11-04-SUMMARY.md`:
- Файлы созданные/обновлённые (7 файлов)
- Server actions контракты (createAppeal, updateAppealStatus)
- UI integration: ReplyPanel (кнопка «Обжаловать»), TicketSidePanel (AppealStatusPanel), SupportTicketCard (бейдж)
- WB cabinet jump URL pattern (deep-link если wbExternalId, иначе общая)
- **Scope removals документированные**: SUP-07 дополнение (cron 1 час) — НЕ реализован, WB polling API не существует; SUP-30 (cron sync) — НЕ реализован по той же причине
- **Phase 10 backward TODO reminder**: если Phase 10 ещё не execute — Plan 10-03 должен добавить TemplatePickerModal в ChatReplyPanel (паттерн из ReplyPanel Plan 11-03)
- Validation Map final: 11-VALIDATION.md per-task nyquist_compliant:true
</output>
