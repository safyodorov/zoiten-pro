---
phase: 27-abc
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/(dashboard)/sales-plan/products/page.tsx
  - components/sales-plan/ProductPlanTable.tsx
autonomous: true
requirements: [SP-18]

must_haves:
  truths:
    - "В строке товара /sales-plan «Товары» виден ABC-бейдж (A/B/C/«—») с цветом по классу"
    - "Клик по ABC меняет статус глобально (updateProductAbcStatus) с optimistic-обновлением"
    - "В строке товара виден тумблер «заказываем / не заказываем»"
    - "Для товара со статусом C тумблер визуально off + disabled + tooltip «Статус C — вне ассортимента»"
    - "Смена ABC/тумблера мгновенно отражается (useTransition + router.refresh), VP пересчитываются на сервере"
    - "Строка-итог tfoot остаётся выровненной: число ячеек tfoot = число колонок thead после добавления 2 колонок"
  artifacts:
    - path: "app/(dashboard)/sales-plan/products/page.tsx"
      provides: "Сериализация abcStatus/orderEnabled/effectiveOrderEnabled в tableProducts (через helper computeEffectiveOrderEnabled)"
      contains: "computeEffectiveOrderEnabled"
    - path: "components/sales-plan/ProductPlanTable.tsx"
      provides: "Колонки ABC-бейдж (инлайн-смена) + тумблер «заказываем» + вызовы actions + 2 пустых td в tfoot"
      contains: "updateProductAbcStatus"
  key_links:
    - from: "components/sales-plan/ProductPlanTable.tsx (ABC select onChange)"
      to: "app/actions/sales-plan.ts:updateProductAbcStatus"
      via: "startTransition + await action + router.refresh"
      pattern: "updateProductAbcStatus"
    - from: "components/sales-plan/ProductPlanTable.tsx (тумблер onChange)"
      to: "app/actions/sales-plan.ts:updateProductOrderEnabled"
      via: "startTransition + await action + router.refresh"
      pattern: "updateProductOrderEnabled"
---

<objective>
UI-часть SP-18: в матрице `/sales-plan «Товары»` вывести ABC-статус товара бейджем с инлайн-сменой (глобально, `updateProductAbcStatus`) и тумблер «заказываем / не заказываем» (`updateProductOrderEnabled`). Для статуса C тумблер принудительно off + disabled + tooltip.

Purpose: Пользователь меняет ABC и флаг «прямо в таблице, глобально» (D-1, D-2, D-5); при выключении «заказываем»/установке C сервер сразу гасит SUGGESTED-виртуальные закупки по товару (действия из 27-01 уже вызывают `regenerateVirtualPurchasesInternal([productId])`).

Output: сериализация `abcStatus`/`orderEnabled`/`effectiveOrderEnabled` в `tableProducts` (page.tsx, через общий helper `computeEffectiveOrderEnabled` из 27-01); две новые колонки в `ProductPlanTable` (ABC-бейдж + тумблер) с optimistic `useTransition` + `router.refresh`; синхронное выравнивание строки-итога `tfoot` (2 пустых `<td>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-abc/27-CONTEXT.md
@.planning/phases/27-abc/27-01-SUMMARY.md

<interfaces>
<!-- Контракты из 27-01 (уже реализованы) и кода UI. Использовать напрямую. -->

Из app/actions/sales-plan.ts (реализовано в 27-01):
```typescript
type ActionResult = { ok: true } | { ok: false; error: string }
export async function updateProductAbcStatus(productId: string, status: "A"|"B"|"C"|null): Promise<ActionResult>
export async function updateProductOrderEnabled(productId: string, enabled: boolean): Promise<ActionResult>
// Оба: SALES MANAGE + prisma.product.update + regenerateVirtualPurchasesInternal([productId]) + revalidate
```

Из lib/sales-plan/virtual-purchases.ts (helper из 27-01 — source of truth формулы гейта, pure, безопасен для импорта в RSC page.tsx):
```typescript
export function computeEffectiveOrderEnabled(
  abcStatus: "A"|"B"|"C"|null|undefined,
  orderEnabled: boolean|null|undefined,
): boolean   // return abcStatus !== "C" && (orderEnabled ?? true)
```

Из lib/sales-plan/types.ts (ProductPlanInput, обновлён 27-01): содержит `abcStatus?: "A"|"B"|"C"|null` и `orderEnabled?: boolean`.

Из app/(dashboard)/sales-plan/products/page.tsx:
```typescript
const canManage = (await getSectionRole("SALES")) === "MANAGE"   // ~стр. 76
const readOnly = mode !== "edit" || !canManage || Boolean(versionId)  // ~стр. 82
// tableProducts = filteredProducts.map((p) => ({ productId, sku, name, photoUrl,
//   stockNow, baselineOrdersPerDay, avgPriceRub, currentLevels, dayOverrideMonths,
//   arrivals, planResult }))   (~стр. 224-248)
// <ProductPlanTable products={tableProducts} months={MONTHS} mode={...}
//   readOnly={readOnly} factByProduct={factByProduct} today={today} />   (~стр. 301)
```

Из components/sales-plan/ProductPlanTable.tsx (проверено чтением):
```typescript
"use client"
import { useRouter } from "next/navigation"; import { toast } from "sonner"
import { useTransition } from "react"
const router = useRouter()
const [isPending, startTransition] = useTransition()   // ~стр. 176
interface ProductRow { productId; sku; name; photoUrl; stockNow; baselineOrdersPerDay;
  avgPriceRub; currentLevels; dayOverrideMonths; arrivals; planResult }   // ~стр. 53
// СТРУКТУРА КОЛОНОК (порядок, подтверждён чтением):
//   thead (стр. 407-454): Фото(sticky left:0 w:60) SKU(sticky left:60 w:90)
//     Название(sticky left:150 w:200) Приходы(sticky left:350 w:140)
//     → «Сток»(НЕ-sticky, text-right, стр. 438) → месяцы(map) → «Итог ₽»(стр. 453)
//   tbody (стр. 457+): те же ячейки; «Сток» ~стр. 528-531; месяцы ~стр. 532+
//   tfoot (стр. 643-676): <td colSpan={4}>Итого</td>(стр. 645-651)
//     + <td>сумма-Сток</td>(стр. 652-654) + месяцы map(стр. 655-671) + <td>Итог₽</td>(стр. 672-674)
// STICKY_TH / STICKY_TD константы ~стр. 82-83
// Образец optimistic-инлайн: кнопка Eraser (resetMonthLevelsToAuto) ~стр. 499-517:
//   startResetTransition(async () => { const r = await action(...);
//     if (!r.ok) { toast.error(...); return } toast.success(...); router.refresh() })
```

ABC-классы (образец из components/products/ProductsTable.tsx ~стр. 65):
```typescript
const ABC_CLASSES: Record<string, string> = {
  A: "bg-green-100 text-green-800",
  B: "bg-blue-100 text-blue-800",
  C: "bg-orange-100 text-orange-800",
}
```

CLAUDE.md conventions: native `<select>` (НЕ base-ui) для инлайн-смены статуса; sticky-ячейки — сплошной `bg-background`/`bg-muted` (без /NN alpha).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Сериализация abcStatus/orderEnabled/effectiveOrderEnabled в tableProducts</name>
  <files>app/(dashboard)/sales-plan/products/page.tsx</files>
  <read_first>
    - app/(dashboard)/sales-plan/products/page.tsx (~стр. 224-248 tableProducts.map; ~стр. 301 передача props в ProductPlanTable; ~стр. 76 canManage; ~стр. 82 readOnly; блок импортов сверху)
    - lib/sales-plan/types.ts (ProductPlanInput.abcStatus / orderEnabled — уже добавлены в 27-01)
    - lib/sales-plan/virtual-purchases.ts (computeEffectiveOrderEnabled — helper из 27-01)
  </read_first>
  <action>
    1. Импорт helper: добавить `import { computeEffectiveOrderEnabled } from "@/lib/sales-plan/virtual-purchases"` в блок импортов page.tsx (helper pure — RSC-безопасен).
    2. В `app/(dashboard)/sales-plan/products/page.tsx` в объекте `tableProducts.map((p) => ({ ... }))` (~стр. 226-247) добавить три поля (перед `planResult`):
       ```typescript
       abcStatus: p.abcStatus ?? null,
       orderEnabled: p.orderEnabled ?? true,
       effectiveOrderEnabled: computeEffectiveOrderEnabled(p.abcStatus, p.orderEnabled),
       ```
       `effectiveOrderEnabled` считается ЕДИНЫМ helper'ом (27-01), НЕ инлайн-формулой — источник истины один (закрывает WARNING-1). Значение передаётся клиенту для отрисовки состояния тумблера без повторного вычисления.
    3. В JSX `<ProductPlanTable ... />` (~стр. 301) добавить проп `canManage={canManage}` (тумблер/select должны быть disabled при отсутствии MANAGE; existing `readOnly` завязан на mode=edit, а ABC/флаг — глобальные и должны быть доступны из любого режима при MANAGE). Передать именно `canManage`, не `readOnly`.
    4. НЕ трогать логику фильтров/planResult/factByProduct.
  </action>
  <verify>
    <automated>grep -q 'effectiveOrderEnabled: computeEffectiveOrderEnabled(p.abcStatus, p.orderEnabled)' 'app/(dashboard)/sales-plan/products/page.tsx' && grep -q 'canManage={canManage}' 'app/(dashboard)/sales-plan/products/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'computeEffectiveOrderEnabled' 'app/(dashboard)/sales-plan/products/page.tsx'` >= 2 (импорт + вызов)
    - `grep -c '(p.abcStatus !== "C") && (p.orderEnabled' 'app/(dashboard)/sales-plan/products/page.tsx'` == 0 (инлайн-формула ЗАПРЕЩЕНА — только helper)
    - `grep -c 'abcStatus: p.abcStatus' 'app/(dashboard)/sales-plan/products/page.tsx'` == 1
    - `grep -c 'canManage={canManage}' 'app/(dashboard)/sales-plan/products/page.tsx'` == 1 (проп добавлен в ProductPlanTable)
  </acceptance_criteria>
  <done>tableProducts содержит abcStatus/orderEnabled/effectiveOrderEnabled (через общий helper из 27-01, инлайна нет); ProductPlanTable получает canManage. Ничего в фильтрах/planResult не сломано.</done>
</task>

<task type="auto">
  <name>Task 2: Колонки ABC-бейдж + тумблер «заказываем» + выравнивание tfoot в ProductPlanTable</name>
  <files>components/sales-plan/ProductPlanTable.tsx</files>
  <read_first>
    - components/sales-plan/ProductPlanTable.tsx: импорты 1-14; interface ProductRow ~стр. 53; interface ProductPlanTableProps ~стр. 71; useTransition ~стр. 176; STICKY_TH/STICKY_TD ~стр. 82; thead ~стр. 407-454 (колонка «Сток» ~стр. 438); tbody строка ~стр. 457; ячейка «Сток» ~стр. 528-531; образец optimistic Eraser ~стр. 499-517; **tfoot ~стр. 643-676** (Итого colSpan={4} стр. 646, сумма-Сток td стр. 652, месяцы map стр. 655, Итог₽ td стр. 672)
    - components/products/ProductsTable.tsx (~стр. 65 ABC_CLASSES бейдж-стили)
  </read_first>
  <action>
    ПОРЯДОК КОЛОНОК ПОСЛЕ ПРАВКИ (важно для выравнивания thead/tbody/tfoot):
    Фото | SKU | Название | Приходы | Сток | **ABC** | **Заказ** | месяцы… | Итог ₽.
    Обе новые колонки вставляются МЕЖДУ «Сток» и первым месяцем — синхронно в thead, tbody и tfoot.

    1. Импорты: добавить `import { updateProductAbcStatus, updateProductOrderEnabled } from "@/app/actions/sales-plan"` в существующий импорт actions (стр. 13).
    2. Локальная константа рядом с STICKY_TH (~стр. 82):
       ```typescript
       const ABC_CLASSES: Record<string, string> = {
         A: "bg-green-100 text-green-800",
         B: "bg-blue-100 text-blue-800",
         C: "bg-orange-100 text-orange-800",
       }
       ```
    3. `interface ProductRow` (~стр. 53) — добавить поля:
       ```typescript
       abcStatus: "A" | "B" | "C" | null
       orderEnabled: boolean
       effectiveOrderEnabled: boolean
       ```
    4. `interface ProductPlanTableProps` (~стр. 71) — добавить `canManage: boolean`; в деструктуризации пропсов (~стр. 167-174) добавить `canManage`.
    5. **thead** — после `<th>Сток</th>` (~стр. 438), ПЕРЕД `months.map` (~стр. 440), добавить ДВЕ не-sticky колонки (sticky left-offset'ы Фото/SKU/Название/Приходы НЕ трогать):
       ```jsx
       <th className={`${STICKY_TH} text-center border-r`} style={{ width: 56 }}>ABC</th>
       <th className={`${STICKY_TH} text-center border-r`} style={{ width: 90 }}>Заказ</th>
       ```
    6. **tbody** — после ячейки «Сток» (~стр. 528-531), ПЕРЕД `months.map` (~стр. 532), добавить ДВЕ ячейки. Обе с `onClick={(e) => e.stopPropagation()}` (клик не должен открывать диалог товара):
       - ABC (native `<select>` per CLAUDE.md, значения A/B/C/«—»=пустая строка):
         ```jsx
         <TableCell className="text-center px-1 border-r" onClick={(e) => e.stopPropagation()}>
           {canManage ? (
             <select
               value={p.abcStatus ?? ""}
               disabled={isPending}
               onChange={(e) => {
                 const v = e.target.value
                 const next = v === "" ? null : (v as "A" | "B" | "C")
                 startTransition(async () => {
                   const r = await updateProductAbcStatus(p.productId, next)
                   if (!r.ok) { toast.error(r.error || "Не удалось обновить ABC"); return }
                   router.refresh()
                 })
               }}
               className={`rounded px-1 py-0.5 text-xs font-semibold border ${p.abcStatus ? ABC_CLASSES[p.abcStatus] : "text-muted-foreground"}`}
             >
               <option value="">—</option>
               <option value="A">A</option>
               <option value="B">B</option>
               <option value="C">C</option>
             </select>
           ) : (
             <span className={`inline-flex items-center rounded px-2 py-0.5 text-xs font-semibold ${p.abcStatus ? ABC_CLASSES[p.abcStatus] : "text-muted-foreground"}`}>
               {p.abcStatus ?? "—"}
             </span>
           )}
         </TableCell>
         ```
       - Тумблер «заказываем» (checkbox; для C — checked=false + disabled + tooltip через `title`):
         ```jsx
         <TableCell className="text-center px-1 border-r" onClick={(e) => e.stopPropagation()}>
           <label
             className="inline-flex items-center gap-1 cursor-pointer text-xs"
             title={p.abcStatus === "C" ? "Статус C — вне ассортимента" : undefined}
           >
             <input
               type="checkbox"
               checked={p.effectiveOrderEnabled}
               disabled={!canManage || isPending || p.abcStatus === "C"}
               onChange={(e) => {
                 const enabled = e.target.checked
                 startTransition(async () => {
                   const r = await updateProductOrderEnabled(p.productId, enabled)
                   if (!r.ok) { toast.error(r.error || "Не удалось обновить флаг"); return }
                   router.refresh()
                 })
               }}
             />
             <span className={p.effectiveOrderEnabled ? "" : "text-muted-foreground"}>
               {p.effectiveOrderEnabled ? "заказ" : "нет"}
             </span>
           </label>
         </TableCell>
         ```
       Состояние тумблера рисуется по `effectiveOrderEnabled` (C → всегда unchecked+disabled, даже если orderEnabled=true в БД — «C форсит off»).
    7. **tfoot (ОБЯЗАТЕЛЬНО — иначе строка-итог съедет)**: две новые колонки вставлены МЕЖДУ «Сток» и месяцами, поэтому в tfoot (~стр. 643-676) после ячейки суммы-«Сток» (`<td>...</td>` ~стр. 652-654) и ПЕРЕД `months.map` (~стр. 655) добавить ДВЕ пустых ячейки:
       ```jsx
       <td className="border-r" />
       <td className="border-r" />
       ```
       НЕ менять `colSpan={4}` у «Итого» (стр. 646) и НЕ менять ячейку суммы-«Сток» — они соответствуют колонкам Фото/SKU/Название/Приходы и «Сток» соответственно. ABC+Заказ идут ПОСЛЕ «Сток», значит выравниваются двумя пустыми `<td>` именно тут. Итоговое число ячеек tfoot = число колонок thead (Итого-colSpan=4 покрывает 4 sticky-колонки, далее Сток + 2 пустых (ABC/Заказ) + N месяцев + Итог₽).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -q 'updateProductOrderEnabled(p.productId' components/sales-plan/ProductPlanTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'updateProductAbcStatus' components/sales-plan/ProductPlanTable.tsx` >= 2 (импорт + вызов)
    - `grep -c 'updateProductOrderEnabled' components/sales-plan/ProductPlanTable.tsx` >= 2
    - `grep -q 'disabled={!canManage || isPending || p.abcStatus === "C"}' components/sales-plan/ProductPlanTable.tsx` (C-тумблер disabled)
    - `grep -q 'Статус C — вне ассортимента' components/sales-plan/ProductPlanTable.tsx` (tooltip)
    - `grep -c 'e.stopPropagation()' components/sales-plan/ProductPlanTable.tsx` увеличилось на >= 2 (ABC + тумблер ячейки не открывают диалог)
    - `grep -c '<td className="border-r" />' components/sales-plan/ProductPlanTable.tsx` >= 2 (2 пустых ячейки-выравнивателя в tfoot под ABC+Заказ)
    - ВЫРАВНИВАНИЕ: число `<th>`/`<TableCell>`/`<td>` в thead, tbody-строке и tfoot совпадает (ручная сверка при чтении: colSpan={4}=Итого + Сток + 2 пустых + месяцы + Итог₽ = thead-колонки)
    - `npx tsc --noEmit` без ошибок
  </acceptance_criteria>
  <done>Две колонки (ABC native-select инлайн-смена + тумблер «заказываем») вставлены между «Сток» и месяцами синхронно в thead/tbody/tfoot; в tfoot добавлены 2 пустых `<td>` под ABC+Заказ (строка-итог выровнена); C→тумблер off+disabled+tooltip; optimistic useTransition+router.refresh; клики не открывают диалог; tsc чистый.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ProductPlanTable инлайн-select/checkbox) → server action | Правки ABC/флага мутируют глобальный Product-field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Elevation of Privilege | ProductPlanTable инлайн-контролы | mitigate | Клиентский `canManage`-гейт (select/checkbox disabled без MANAGE) — только UX; авторитетная проверка `requireSection("SALES","MANAGE")` в самих actions (27-01). Клиент не может обойти сервер |
| T-27-06 | Tampering | тумблер «заказываем» для C | mitigate | Состояние рисуется по `effectiveOrderEnabled` (считан общим helper'ом из 27-01 → C=unchecked); checkbox `disabled` при `abcStatus==='C'` → нельзя включить «заказываем» пока C (D-3). Сервер дополнительно даёт effective=false для C |
| T-27-07 | Information Disclosure | клик по ABC/тумблеру | accept | `stopPropagation` предотвращает открытие диалога товара при правке — UX, не безопасность; риск отсутствует |
</threat_model>

<verification>
- `npx tsc --noEmit` — типы ProductRow/props согласованы с page.tsx
- `npm run test` (регресс) — golden iu === 438_068_120, sales-plan-* Phase 25/26 не сломаны (UI-план кода движка не трогает)
- Выравнивание таблицы: строка-итог tfoot не съехала — 2 пустых `<td>` под ABC+Заказ; число ячеек tfoot = число колонок thead
- Визуально: ABC-бейдж цветной с инлайн-сменой A/B/C/«—»; тумблер «заказываем»; для C — тумблер off+disabled+tooltip
</verification>

<success_criteria>
- tableProducts (page.tsx) сериализует abcStatus/orderEnabled/effectiveOrderEnabled через общий helper `computeEffectiveOrderEnabled` (27-01) — инлайн-формула не дублируется
- В строке товара `/sales-plan «Товары»` — ABC-бейдж (native select, глобальная смена через updateProductAbcStatus) + тумблер «заказываем» (updateProductOrderEnabled)
- Для C тумблер off + disabled + tooltip «Статус C — вне ассортимента»
- Строка-итог tfoot выровнена: 2 пустых `<td>` под ABC+Заказ, число ячеек tfoot = число колонок thead
- Optimistic: useTransition + router.refresh; клик по контролам не открывает диалог товара
- Типы зелёные; движок и golden не тронуты
</success_criteria>

<output>
После завершения создать `.planning/phases/27-abc/27-02-SUMMARY.md`
</output>
