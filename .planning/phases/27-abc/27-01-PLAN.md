---
phase: 27-abc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260705_product_order_enabled/migration.sql
  - lib/sales-plan/types.ts
  - lib/sales-plan/virtual-purchases.ts
  - lib/sales-plan/data.ts
  - app/actions/sales-plan.ts
  - tests/sales-plan-order-gate.test.ts
autonomous: true
requirements: [SP-18, SP-19]

must_haves:
  truths:
    - "Товар со статусом C НЕ получает виртуальных закупок (effectiveOrderEnabled=false)"
    - "Товар с orderEnabled=false (A/B) НЕ получает виртуальных закупок"
    - "Товар A/B с orderEnabled=true получает виртуальные закупки как раньше (регрессии нет)"
    - "Существующие товары после миграции имеют orderEnabled=true (обратная совместимость)"
    - "updateProductAbcStatus и updateProductOrderEnabled пишут глобальные поля Product под SALES MANAGE и регенерируют VP"
  artifacts:
    - path: "prisma/migrations/20260705_product_order_enabled/migration.sql"
      provides: "ALTER TABLE Product ADD COLUMN orderEnabled DEFAULT true"
      contains: "orderEnabled"
    - path: "lib/sales-plan/virtual-purchases.ts"
      provides: "Гейт в suggestVirtualPurchases (skip effectiveOrderEnabled=false)"
      contains: "effectiveOrderEnabled"
    - path: "lib/sales-plan/data.ts"
      provides: "abcStatus + orderEnabled в ProductPlanInput"
      contains: "orderEnabled"
    - path: "app/actions/sales-plan.ts"
      provides: "updateProductAbcStatus + updateProductOrderEnabled + проброс effectiveOrderEnabled в vpProducts"
      contains: "updateProductOrderEnabled"
    - path: "tests/sales-plan-order-gate.test.ts"
      provides: "Тесты гейта: C/не-заказываем → 0, A/B заказываем → без изменений, C форсит off"
      contains: "effectiveOrderEnabled"
  key_links:
    - from: "app/actions/sales-plan.ts:regenerateVirtualPurchasesInternal (vpProducts)"
      to: "lib/sales-plan/virtual-purchases.ts:suggestVirtualPurchases"
      via: "effectiveOrderEnabled в VpProductInput"
      pattern: "effectiveOrderEnabled"
    - from: "lib/sales-plan/data.ts:loadSalesPlanInputs"
      to: "ProductPlanInput.abcStatus / orderEnabled"
      via: "product scalar-поля из findMany include"
      pattern: "orderEnabled"
---

<objective>
Фундамент фазы 27 (SP-19 + серверная часть SP-18): новое глобальное поле `Product.orderEnabled Boolean @default(true)`, гейт движка виртуальных закупок по эффективному флагу `effectiveOrderEnabled = (abcStatus !== 'C') && orderEnabled`, и два глобальных server actions для инлайн-смены ABC-статуса и флага «заказываем» из `/sales-plan «Товары»`.

Purpose: Товары, выведенные из ассортимента (статус C) или помеченные «не заказываем» (A/B + orderEnabled=false), больше НЕ получают предложений виртуальных закупок. Движок продаж (engine.ts) НЕ трогаем — без пополнений сток истощается сам (`orders=min(ставка,сток)`) → план будущих периодов = «распродаём остаток, потом 0» (D-4).

Output: Рукописная миграция + правки schema; `abcStatus`/`orderEnabled`/`effectiveOrderEnabled` в загрузчике и типах движка; skip в suggester; проброс в regenerate; 2 server actions (SALES MANAGE + регенерация VP + revalidate); vitest-тесты гейта.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-abc/27-CONTEXT.md

<interfaces>
<!-- Контракты, извлечённые из кода. Использовать напрямую, без исследования кодовой базы. -->

Из prisma/schema.prisma (~стр. 39, ~стр. 400):
```prisma
enum AbcStatus { A B C }

model Product {
  // ...
  label       String?    @db.VarChar(100)
  abcStatus   AbcStatus?          // существует, nullable
  // ДОБАВИТЬ: orderEnabled Boolean @default(true)
  availability Availability @default(IN_STOCK)
  // ...
}
```

Из lib/sales-plan/types.ts (ProductPlanInput ~стр. 45): сериализуемый (string/number/boolean/null, без Date/Decimal). Поля уже есть: productId, sku, name, brandId, ..., stockNow, baselineOrdersPerDay, buyoutPct, monthLevels, dayOverrides, arrivals, seedOrders.

Из lib/sales-plan/virtual-purchases.ts:
```typescript
export interface VpProductInput {
  productId: string
  sku: string
  name: string
  stockNow: number
  baselineOrdersPerDay: number
  leadTimeDays?: number
  monthLevels: Array<{ month: string; targetOrdersPerDay: number | null; priceRub?: number | null; buyoutPct?: number | null }>
  dayOverrides: Record<string, number>
  arrivals: Array<{ date: string; qty: number }>
  existingVirtualPurchases: VpArrivalInput[]
  unitPrice?: number | null
  // ДОБАВИТЬ: effectiveOrderEnabled?: boolean
}
export function suggestVirtualPurchases(input: VpSuggestInput): VpSuggestion[]
// цикл: for (const product of input.products) { ... } (~стр. 225)
```

Из lib/sales-plan/data.ts:
```typescript
// db.product.findMany({ where: { deletedAt: null }, include: {...} })  (~стр. 91)
// ВНИМАНИЕ: findMany использует include, НЕ select → скалярные поля Product
//   (abcStatus, orderEnabled) грузятся автоматически. Отдельный select НЕ нужен.
// productInputs.push({ productId: p.id, sku, name, ..., seedOrders })  (~стр. 443)
```

Из app/actions/sales-plan.ts:
```typescript
// vpProducts = inputs.products.map((p) => ({ productId: p.productId, sku, name, stockNow,
//   baselineOrdersPerDay, leadTimeDays, monthLevels, dayOverrides, arrivals,
//   existingVirtualPurchases, unitPrice }))  (~стр. 931)
// export async function regenerateVirtualPurchasesInternal(productIds?: string[]): Promise<void>  (~стр. 805)
// Паттерн write-action (saveMonthLevels ~стр. 127):
//   await requireSection("SALES", "MANAGE")
//   const parsed = Schema.safeParse(...); if (!parsed.success) return { ok:false, error:"Невалидные данные" }
//   try { ...prisma...; await regenerateVirtualPurchasesInternal([...]); revalidateSalesPlanPaths(); return { ok:true } }
//   catch (err) { console.error(...); return { ok:false, error:"..." } }
// type ActionResult = { ok: true } | { ok: false; error: string }
```

Из app/actions/products.ts (образец abcStatus zod):
```typescript
abcStatus: z.enum(["A", "B", "C"]).optional()   // в БД: parsed.abcStatus ?? null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Миграция Product.orderEnabled + prisma generate</name>
  <files>prisma/schema.prisma, prisma/migrations/20260705_product_order_enabled/migration.sql</files>
  <read_first>
    - prisma/schema.prisma (~стр. 39 enum AbcStatus, ~стр. 400 Product.abcStatus/availability — точка вставки нового поля)
    - Аналог рукописной аддитивной миграции: prisma/migrations/20260704_purchase_payment_amount_rub/migration.sql (одна строка ALTER TABLE ADD COLUMN)
  </read_first>
  <action>
    1. Создать каталог+файл `prisma/migrations/20260705_product_order_enabled/migration.sql` с ТОЧНЫМ содержимым:
       ```sql
       -- Phase 27: флаг «заказываем / не заказываем» (гейт виртуальных закупок)
       ALTER TABLE "Product" ADD COLUMN "orderEnabled" BOOLEAN NOT NULL DEFAULT true;
       ```
       DEFAULT true = обратная совместимость (существующие товары остаются «заказываем», D-3).
    2. В `prisma/schema.prisma` в модели `Product` сразу после строки `abcStatus AbcStatus?` (~стр. 400) добавить:
       ```prisma
       orderEnabled             Boolean                @default(true)
       ```
    3. Выполнить `npx prisma generate` (deploy.sh запускает только `migrate deploy`, НЕ generate — локальный клиент обязан знать `orderEnabled` для типобезопасности data.ts/actions). Миграция на прод применяется через `prisma migrate deploy` в deploy.sh — БЕЗ `prisma db push`.
  </action>
  <verify>
    <automated>grep -q 'ADD COLUMN "orderEnabled" BOOLEAN NOT NULL DEFAULT true' prisma/migrations/20260705_product_order_enabled/migration.sql && grep -q 'orderEnabled' prisma/schema.prisma && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADD COLUMN "orderEnabled"' prisma/migrations/20260705_product_order_enabled/migration.sql` == 1
    - `grep -c 'orderEnabled.*Boolean.*@default(true)' prisma/schema.prisma` == 1
    - `npx prisma validate` завершается без ошибок (клиент перегенерирован, `orderEnabled` в типах Product)
  </acceptance_criteria>
  <done>Миграция создана рукописным SQL (ADD COLUMN DEFAULT true), schema обновлена, `prisma generate` выполнен, `prisma validate` зелёный. Это BLOCKING — Task 2/3 требуют типа `Product.orderEnabled`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Гейт effectiveOrderEnabled в типах, загрузчике и suggester</name>
  <files>lib/sales-plan/types.ts, lib/sales-plan/virtual-purchases.ts, lib/sales-plan/data.ts, tests/sales-plan-order-gate.test.ts</files>
  <read_first>
    - lib/sales-plan/types.ts (ProductPlanInput ~стр. 45 — куда добавить поля)
    - lib/sales-plan/virtual-purchases.ts (VpProductInput ~стр. 38, цикл `for (const product of input.products)` ~стр. 225 — точка skip)
    - lib/sales-plan/data.ts (findMany include ~стр. 91; productInputs.push ~стр. 443)
    - Аналог тестов: tests/sales-plan-virtual.test.ts (makeProductInput fixture + DEFAULT_PARAMS)
  </read_first>
  <behavior>
    - Test «C форсит off»: чистая формула `(abcStatus === 'C') → effectiveOrderEnabled === false` даже при orderEnabled=true.
    - Test гейта в suggester: товар с `effectiveOrderEnabled: false` (сток мал, пробой есть, arrivals=[]) → 0 предложений.
    - Test регрессии: тот же товар с `effectiveOrderEnabled: true` (или undefined) → предложения как раньше (length > 0).
    - Test изоляции: в одном прогоне товар «заказываем» получает предложения, товар «не заказываем» — 0; первый не задет.
  </behavior>
  <action>
    1. `lib/sales-plan/types.ts` — в `ProductPlanInput` (~стр. 45) добавить два опциональных поля (после `seedOrders` или рядом с иерархией):
       ```typescript
       abcStatus?: "A" | "B" | "C" | null
       orderEnabled?: boolean            // глобальный флаг Product.orderEnabled
       ```
    2. `lib/sales-plan/virtual-purchases.ts`:
       - В `VpProductInput` (~стр. 38) добавить: `effectiveOrderEnabled?: boolean`.
       - В `suggestVirtualPurchases` в самом начале тела цикла `for (const product of input.products) {` (~стр. 225, ПЕРВОЙ строкой внутри) добавить гейт:
         ```typescript
         // Phase 27: гейт «заказываем». Товар выведен из ассортимента (C) или помечен
         // «не заказываем» → виртуальные закупки не считаются. undefined = совместимость (заказываем).
         if (product.effectiveOrderEnabled === false) continue
         ```
         Skip ДОЛЖЕН быть строго ПЕРВЫМ оператором цикла (до вычисления leadTimeDays/workArrivals), чтобы товар не попал ни в одну итерацию.
    3. `lib/sales-plan/data.ts`:
       - findMany (~стр. 91) уже использует `include` → скалярные `abcStatus`/`orderEnabled` грузятся сами, отдельный select добавлять НЕ нужно (в комментарии зафиксировать этот факт).
       - В `productInputs.push({...})` (~стр. 443) добавить два поля:
         ```typescript
         abcStatus: p.abcStatus ?? null,
         orderEnabled: p.orderEnabled,
         ```
    4. Создать `tests/sales-plan-order-gate.test.ts`. Импортировать `suggestVirtualPurchases`. Скопировать `DEFAULT_PARAMS` и `makeProductInput` из sales-plan-virtual.test.ts (fixture stockNow=30 → быстрый пробой). Написать describe-блоки под каждый case из <behavior>. Формулу «C форсит off» проверять как чистое выражение `(abc !== 'C') && orderEnabled` (объявить helper внутри теста ИЛИ импортировать общий, если создан) → при abc='C', orderEnabled=true ожидать false; при abc='A', orderEnabled=true → true; при abc='B', orderEnabled=false → false.
  </action>
  <verify>
    <automated>npm run test -- sales-plan-order-gate sales-plan-virtual sales-plan-iu sales-plan-engine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'effectiveOrderEnabled' lib/sales-plan/virtual-purchases.ts` >= 2 (интерфейс + гейт)
    - `grep -q 'if (product.effectiveOrderEnabled === false) continue' lib/sales-plan/virtual-purchases.ts`
    - `grep -c 'orderEnabled' lib/sales-plan/types.ts` >= 1 и `grep -c 'orderEnabled' lib/sales-plan/data.ts` >= 1
    - `npm run test -- sales-plan-order-gate` — все тесты гейта GREEN
    - `npm run test -- sales-plan-iu` — golden iu === 438_068_120 НЕ изменился; `sales-plan-engine`, `sales-plan-virtual` зелёные
  </acceptance_criteria>
  <done>Формула effectiveOrderEnabled применена в suggester (skip первым оператором цикла), поля прокинуты в types + data. Тесты гейта GREEN, golden и Phase 25/26 тесты не сломаны.</done>
</task>

<task type="auto">
  <name>Task 3: Проброс effectiveOrderEnabled в regenerate + 2 server actions</name>
  <files>app/actions/sales-plan.ts</files>
  <read_first>
    - app/actions/sales-plan.ts: заголовок 1-26 (импорты, ActionResult); saveMonthLevels ~стр. 127 (образец requireSection+try/catch+regenerate+revalidate); regenerateVirtualPurchasesInternal ~стр. 805; vpProducts map ~стр. 931; revalidateSalesPlanPaths ~стр. 51
    - app/actions/products.ts:51 (abcStatus zod enum образец)
  </read_first>
  <action>
    1. В `app/actions/sales-plan.ts` в `vpProducts = inputs.products.map((p) => ({ ... }))` (~стр. 931) добавить в объект поле (после `unitPrice`):
       ```typescript
       effectiveOrderEnabled: (p.abcStatus !== "C") && (p.orderEnabled ?? true),
       ```
       Формула ЕДИНАЯ (та же, что в тестах Task 2): C всегда false; A/B зависят от orderEnabled; undefined orderEnabled → true (совместимость до миграции). Это точка, где гейт применяется к реальному regenerate.
    2. Добавить два новых экспортируемых server action в конец файла (паттерн saveMonthLevels):
       ```typescript
       const AbcStatusSchema = z.object({
         productId: z.string().min(1),
         status: z.enum(["A", "B", "C"]).nullable(),
       })
       export async function updateProductAbcStatus(
         productId: string, status: "A" | "B" | "C" | null,
       ): Promise<ActionResult> {
         await requireSection("SALES", "MANAGE")
         const parsed = AbcStatusSchema.safeParse({ productId, status })
         if (!parsed.success) return { ok: false, error: "Невалидные данные" }
         try {
           await prisma.product.update({ where: { id: parsed.data.productId }, data: { abcStatus: parsed.data.status } })
           // C влияет на effectiveOrderEnabled → регенерируем VP по товару
           await regenerateVirtualPurchasesInternal([parsed.data.productId])
           revalidateSalesPlanPaths()
           revalidatePath("/products")
           return { ok: true }
         } catch (err) {
           console.error("[updateProductAbcStatus]", err)
           return { ok: false, error: "Не удалось обновить ABC-статус" }
         }
       }

       const OrderEnabledSchema = z.object({
         productId: z.string().min(1),
         enabled: z.boolean(),
       })
       export async function updateProductOrderEnabled(
         productId: string, enabled: boolean,
       ): Promise<ActionResult> {
         await requireSection("SALES", "MANAGE")
         const parsed = OrderEnabledSchema.safeParse({ productId, enabled })
         if (!parsed.success) return { ok: false, error: "Невалидные данные" }
         try {
           await prisma.product.update({ where: { id: parsed.data.productId }, data: { orderEnabled: parsed.data.enabled } })
           await regenerateVirtualPurchasesInternal([parsed.data.productId])
           revalidateSalesPlanPaths()
           revalidatePath("/products")
           return { ok: true }
         } catch (err) {
           console.error("[updateProductOrderEnabled]", err)
           return { ok: false, error: "Не удалось обновить флаг заказа" }
         }
       }
       ```
       `revalidatePath` уже импортирован (стр. 15). `revalidateSalesPlanPaths` определён (~стр. 51). `regenerateVirtualPurchasesInternal` определён в этом же файле.
    3. НЕ трогать engine.ts (D-4) и НЕ менять формулу движка.
  </action>
  <verify>
    <automated>npm run test -- sales-plan-iu sales-plan-virtual sales-plan-engine && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'effectiveOrderEnabled' app/actions/sales-plan.ts` >= 1 (в vpProducts map)
    - `grep -q 'export async function updateProductAbcStatus' app/actions/sales-plan.ts`
    - `grep -q 'export async function updateProductOrderEnabled' app/actions/sales-plan.ts`
    - `grep -c 'requireSection("SALES", "MANAGE")' app/actions/sales-plan.ts` увеличилось на 2 (оба новых action защищены)
    - `grep -c 'regenerateVirtualPurchasesInternal(\[' app/actions/sales-plan.ts` >= 2 (оба action регенерируют по productId)
    - `npx tsc --noEmit` без ошибок (типы Product.orderEnabled известны после Task 1)
    - golden `sales-plan-iu` === 438_068_120 не изменился
  </acceptance_criteria>
  <done>Гейт применён в regenerateVirtualPurchasesInternal через effectiveOrderEnabled в vpProducts. Два глобальных action (SALES MANAGE + zod + prisma.product.update + регенерация VP по productId + revalidate sales-plan/products) экспортированы. tsc чистый, golden не тронут.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (`/sales-plan «Товары»`) → server action | Инлайн-правки ABC/флага мутируют ГЛОБАЛЬНОЕ поле Product (D-5) |
| server action → suggester (pure) | effectiveOrderEnabled должен применяться в реальном regenerate, не только в тесте |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | suggestVirtualPurchases / regenerateVirtualPurchasesInternal | mitigate | ГЛАВНАЯ угроза (gate-leak): товар «не заказываем»/C получает VP. Единая формула `effectiveOrderEnabled = (abcStatus!=='C') && (orderEnabled ?? true)` — в vpProducts map (regenerate) И skip первым оператором цикла suggester. Тесты: C→0, «не заказываем»→0, «C форсит off», A/B заказываем→без изменений |
| T-27-02 | Elevation of Privilege | updateProductAbcStatus / updateProductOrderEnabled | mitigate | Оба action пишут глобальный Product-field → `requireSection("SALES","MANAGE")` первой строкой (SUPERADMIN bypass). Zod-валидация productId/status/enabled до prisma.update |
| T-27-03 | Tampering | prisma migration | mitigate | ADD COLUMN DEFAULT true (не NULL) → существующие товары не теряют «заказываем»; аддитивная, необратимых DROP нет |
| T-27-04 | Denial of Service | regenerateVirtualPurchasesInternal([productId]) | accept | Регенерация ограничена одним productId (не глобальная) — дёшево; частота = ручные клики пользователя, низкий риск |
</threat_model>

<verification>
- `npm run test` — все тесты зелёные, включая новый sales-plan-order-gate; golden iu === 438_068_120; sales-plan-engine/virtual/rollforward/distribute-forward Phase 25/26 не сломаны
- `npx prisma validate` — schema валидна
- `npx tsc --noEmit` — типы Product.orderEnabled известны, ошибок нет
- engine.ts НЕ изменён (`git diff --stat lib/sales-plan/engine.ts` пуст)
</verification>

<success_criteria>
- Миграция `20260705_product_order_enabled` создана рукописным SQL (ADD COLUMN DEFAULT true), schema обновлена, `prisma generate` выполнен
- `effectiveOrderEnabled = (abcStatus!=='C') && (orderEnabled ?? true)` применён и в suggester (skip), и в regenerate (vpProducts)
- Тесты гейта: C/«не заказываем» → 0 VP; A/B заказываем → без изменений; «C форсит off» → false
- updateProductAbcStatus + updateProductOrderEnabled: SALES MANAGE + zod + глобальный prisma.product.update + регенерация VP по productId + revalidate
- Golden iu не изменён; движок продаж (engine.ts) не тронут (D-4)
</success_criteria>

<output>
После завершения создать `.planning/phases/27-abc/27-01-SUMMARY.md`
</output>
