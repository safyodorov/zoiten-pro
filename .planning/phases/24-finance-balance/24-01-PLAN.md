---
phase: 24-finance-balance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260702_phase24_finance/migration.sql
  - lib/sections.ts
  - lib/section-labels.ts
  - components/layout/nav-items.ts
  - components/layout/section-titles.ts
  - app/(dashboard)/finance/balance/page.tsx
  - app/(dashboard)/finance/cashflow/page.tsx
  - app/(dashboard)/finance/pnl/page.tsx
  - components/finance/FinanceTabs.tsx
autonomous: true
requirements: [FIN-BAL-01, FIN-BAL-02, FIN-BAL-03, FIN-BAL-04, FIN-BAL-11]
must_haves:
  truths:
    - "Раздел «Финансовая отчётность» появляется в Sidebar и открывается по /finance/balance"
    - "Табы Баланс | ОДДС | ОПиУ переключаются; ОДДС и ОПиУ показывают заглушку «В разработке»"
    - "Раздел FINANCE управляется тумблером VIEW/MANAGE в /admin/users (SECTION_OPTIONS)"
    - "Публичный /finance-models НЕ ломается новым RBAC-префиксом /finance"
    - "Миграция создаёт 4 таблицы + 2 enum + ERP_SECTION.FINANCE + сид ставок налогов"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "4 модели снапшотов/справочников + enum FinanceStockLocation/FinanceAdjustmentType + ERP_SECTION.FINANCE"
      contains: "model FinanceStockSnapshot"
    - path: "prisma/migrations/20260702_phase24_finance/migration.sql"
      provides: "ALTER TYPE + CREATE TABLE + сид AppSetting"
      contains: "ALTER TYPE \"ERP_SECTION\" ADD VALUE"
    - path: "lib/sections.ts"
      provides: "RBAC route guard префикс /finance/"
      contains: "/finance/"
    - path: "lib/section-labels.ts"
      provides: "тумблер VIEW/MANAGE раздела в /admin/users"
      contains: "FINANCE"
    - path: "components/finance/FinanceTabs.tsx"
      provides: "таб-навигация Баланс|ОДДС|ОПиУ"
      contains: "/finance/cashflow"
  key_links:
    - from: "middleware.ts"
      to: "SECTION_PATHS[/finance/]"
      via: "pathname.startsWith prefix guard"
      pattern: "\"/finance/\""
    - from: "components/layout/nav-items.ts"
      to: "/finance/balance"
      via: "NAV_ITEMS запись section FINANCE"
      pattern: "section: \"FINANCE\""
---

<objective>
Заложить фундамент раздела «Финансовая отчётность»: (1) БД — 4 новые модели (снапшоты остатков и дебиторки, ручные корректировки, факт-корректировки налога) + 2 enum + `ERP_SECTION.FINANCE` + ручная миграция с сидом ставок налогов; (2) полный 6-точечный чеклист проводки раздела ERP из CLAUDE.md; (3) маршруты `/finance/{balance,cashflow,pnl}` + таб-навигация Баланс|ОДДС|ОПиУ (два последних — ComingSoon).

Purpose: Всё остальное в фазе (данные, cron, UI) зависит от Prisma-типов новых моделей и от существования раздела. Это Wave 1 foundation.
Output: schema + migration.sql + 6 файлов чеклиста + 3 страницы + FinanceTabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-CONTEXT.md
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Всё извлечено из кодовой базы — executor не должен искать. -->

КРИТИЧНО (Pitfall 7 + проверено): middleware.ts матчит `nextUrl.pathname.startsWith(prefix)`
по ключам SECTION_PATHS. Публичный раздел `/finance-models` начинается с `/finance` →
ключ `"/finance"` (без слэша) СЛОМАЕТ его RBAC. ИСПОЛЬЗОВАТЬ ключ С ТРЕЙЛИНГ-СЛЭШЕМ:
`"/finance/": "FINANCE"`. Тогда `/finance-models`.startsWith("/finance/") === false.

Рабочий шаблон миграции ALTER TYPE (prisma/migrations/20260610_phase23_cash/migration.sql):
```sql
ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'CASH';
```

Текущий enum ERP_SECTION (schema.prisma:21-36): PRODUCTS, PRICES, WEEKLY_CARDS, STOCK, COST,
PROCUREMENT, SALES, SUPPORT, EMPLOYEES, ADS, USER_MANAGEMENT, CREDITS, BANK, CASH.

AppSetting модель (schema.prisma:614): `key String @id`, `value String`, updatedAt, updatedBy.

ComingSoon (components/ui/ComingSoon.tsx): `<ComingSoon sectionName="..." description="..." />`.

FinanceTabs паттерн — копия CardsTabs (components/cards/CardsTabs.tsx):
"use client" + usePathname + pathname.startsWith(tab.href) → border-primary text-primary.

nav-items.ts занятые иконки: Landmark=CREDITS, Building2=BANK, Wallet=CASH, LineChart=FINANCE_MODELS.
Для FINANCE использовать `Scale` (lucide-react, весы = баланс) — добавить в import и ICON_MAP.

requireSection сигнатура (lib/rbac.ts): `requireSection("FINANCE")` (VIEW) / `requireSection("FINANCE","MANAGE")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma-модели + миграция + сид ставок</name>
  <read_first>
    - prisma/schema.prisma (строки 21-36 enum ERP_SECTION; 614-619 AppSetting; 1103-1125 WbCardFunnelDaily как образец daily-снапшота БЕЗ FK)
    - prisma/migrations/20260610_phase23_cash/migration.sql (рабочий шаблон ALTER TYPE + CREATE TABLE)
    - .planning/phases/24-finance-balance/24-RESEARCH.md (Pattern 1-4 — точные определения моделей)
  </read_first>
  <action>
В `prisma/schema.prisma` добавить (в конец файла, после CashEntry):

1. В `enum ERP_SECTION` добавить значение `FINANCE`.

2. Два новых enum:
```prisma
enum FinanceStockLocation {
  WB_WAREHOUSE
  WB_IN_WAY_TO_CLIENT
  WB_IN_WAY_FROM_CLIENT
  IVANOVO
}
enum FinanceAdjustmentType {
  ASSET
  LIABILITY
}
```

3. Четыре модели (БЕЗ @relation на Product/WbCard — переживают hard-purge, паттерн WbCardFunnelDaily):
```prisma
model FinanceStockSnapshot {
  id              String               @id @default(cuid())
  date            DateTime             @db.Date
  productId       String               // без @relation
  sku             String               // денормализовано для читаемости после удаления товара
  name            String               // денормализовано
  location        FinanceStockLocation
  qty             Int
  costPriceAtDate Decimal?             @db.Decimal(14, 2) // null = «без оценки» (D-11)
  valueRub        Decimal?             @db.Decimal(14, 2) // qty × costPriceAtDate, precomputed
  createdAt       DateTime             @default(now())
  @@unique([date, productId, location])
  @@index([date])
  @@index([productId])
}

model FinanceReceivablesSnapshot {
  id                    String   @id @default(cuid())
  date                  DateTime @unique @db.Date // singleton per date (D-05 консолидировано)
  balanceCurrentRub     Decimal  @db.Decimal(14, 2) // Balance API `current`
  balanceForWithdrawRub Decimal  @db.Decimal(14, 2) // Balance API `for_withdraw`
  weeklyTailRub         Decimal  @db.Decimal(14, 2) // Σ forPay незакрытой недели
  totalRub              Decimal  @db.Decimal(14, 2) // current + weeklyTail (D-14)
  rawJson               Json?
  createdAt             DateTime @default(now())
}

model FinanceManualAdjustment {
  id            String                @id @default(cuid())
  label         String
  type          FinanceAdjustmentType
  amountRub     Decimal               @db.Decimal(14, 2)
  effectiveFrom DateTime              @db.Date
  comment       String?
  createdById   String?
  createdAt     DateTime              @default(now())
  updatedAt     DateTime              @updatedAt
  deletedAt     DateTime?             // «действует до» = момент удаления (D-08)
  @@index([effectiveFrom])
  @@index([deletedAt])
}

model FinanceTaxPeriodActual {
  id                 String   @id @default(cuid())
  year               Int
  quarter            Int      // 1..4
  vatActualRub       Decimal? @db.Decimal(14, 2) // null = использовать расчёт
  incomeTaxActualRub Decimal? @db.Decimal(14, 2)
  updatedById        String?
  updatedAt          DateTime @updatedAt
  @@unique([year, quarter])
}
```

Создать `prisma/migrations/20260702_phase24_finance/migration.sql` вручную (локальной PG нет —
применится через deploy.sh, Plan 24-09). Содержимое:
- `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'FINANCE';`
- `CREATE TYPE "FinanceStockLocation" AS ENUM ('WB_WAREHOUSE','WB_IN_WAY_TO_CLIENT','WB_IN_WAY_FROM_CLIENT','IVANOVO');`
- `CREATE TYPE "FinanceAdjustmentType" AS ENUM ('ASSET','LIABILITY');`
- 4× `CREATE TABLE` c точными типами колонок как в schema (DATE, DECIMAL(14,2), TEXT, JSONB, TIMESTAMP(3)) + PK + все UNIQUE/INDEX из `@@unique`/`@@index`.
- Сид ставок и расписания cron:
```sql
INSERT INTO "AppSetting" ("key","value","updatedAt") VALUES
  ('finance.vatPct','7', CURRENT_TIMESTAMP),
  ('finance.incomeTaxPct','1', CURRENT_TIMESTAMP),
  ('financeBalanceSnapshotCronTime','06:00', CURRENT_TIMESTAMP)
ON CONFLICT ("key") DO NOTHING;
```

После правки schema выполнить `npx prisma generate` (обновить типы клиента).
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep "FINANCE" prisma/schema.prisma` находит значение в enum ERP_SECTION
    - `grep -E "model Finance(StockSnapshot|ReceivablesSnapshot|ManualAdjustment|TaxPeriodActual)" prisma/schema.prisma` → 4 совпадения
    - `grep "enum FinanceStockLocation" prisma/schema.prisma` находит; enum содержит IVANOVO
    - `npx prisma validate` завершается успехом (schema валидна)
    - migration.sql содержит `ALTER TYPE "ERP_SECTION" ADD VALUE IF NOT EXISTS 'FINANCE'` и 4 `CREATE TABLE`
    - migration.sql содержит INSERT с `finance.vatPct`, `finance.incomeTaxPct`, `financeBalanceSnapshotCronTime` и `ON CONFLICT ... DO NOTHING`
  </acceptance_criteria>
  <done>Schema валидна, миграция написана, клиент сгенерирован; сид ставок/расписания включён в миграцию.</done>
</task>

<task type="auto">
  <name>Task 2: 6-точечный чеклист проводки раздела ERP</name>
  <read_first>
    - lib/sections.ts (SECTION_PATHS — добавить /finance/)
    - lib/section-labels.ts (SECTION_OPTIONS — п.5 чеклиста, чаще всего забывают)
    - components/layout/nav-items.ts (NAV_ITEMS + ICON_MAP)
    - components/layout/section-titles.ts (регексы, специфичные выше общих)
    - middleware.ts (подтвердить startsWith-матчинг — обоснование трейлинг-слэша)
    - CLAUDE.md (§ «Чеклист при добавлении нового раздела ERP»)
  </read_first>
  <action>
Провести раздел FINANCE по чеклисту CLAUDE.md (п.1 схема — уже в Task 1):

**п.2 `lib/sections.ts`** — в объект `SECTION_PATHS` добавить строку `"/finance/": "FINANCE",`
(ОБЯЗАТЕЛЬНО трейлинг-слэш — иначе `startsWith` перехватит публичный `/finance-models`).

**п.3 `components/layout/section-titles.ts`** — добавить ТРИ записи ПЕРЕД существующей
`{ match: /^\/finance-models/, ... }` (строка ~38), порядок специфичные-первыми:
```ts
{ match: /^\/finance\/balance/, title: "Финансы — Баланс" },
{ match: /^\/finance\/cashflow/, title: "Финансы — ОДДС" },
{ match: /^\/finance\/pnl/, title: "Финансы — ОПиУ" },
```
(с слэшем после `finance` — не пересекаются с `/finance-models`).

**п.4 `components/layout/nav-items.ts`** — добавить в `NAV_ITEMS` (рядом с CASH/финансовыми):
`{ section: "FINANCE", href: "/finance/balance", label: "Финансовая отчётность", icon: "Scale" },`
+ добавить `Scale` в import из "lucide-react" И в `ICON_MAP`.

**п.5 `lib/section-labels.ts`** — в массив `SECTION_OPTIONS` добавить:
`{ value: "FINANCE", label: "Финансовая отчётность" },` (без этого нет тумблера VIEW/MANAGE в /admin/users).

`app/actions/users.ts` НЕ трогать (sectionRoles = z.record — принимает новый section).
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sections|nav-items|section-labels|section-titles" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"/finance/"' lib/sections.ts` находит ключ С трейлинг-слэшем (НЕ `"/finance"`)
    - `grep 'value: "FINANCE"' lib/section-labels.ts` находит (п.5 выполнен)
    - `grep 'section: "FINANCE"' components/layout/nav-items.ts` находит + `grep "Scale" components/layout/nav-items.ts` находит в import и ICON_MAP (2 совпадения)
    - `grep "finance\\\\/balance" components/layout/section-titles.ts` находит; записи finance стоят ВЫШЕ `/finance-models`
    - `grep '"/finance": ' lib/sections.ts` НЕ находит (нет ключа без слэша)
  </acceptance_criteria>
  <done>Все 6 точек чеклиста закрыты; /finance-models не задет; раздел управляем тумблером.</done>
</task>

<task type="auto">
  <name>Task 3: Маршруты /finance + FinanceTabs + заглушки ОДДС/ОПиУ</name>
  <read_first>
    - components/cards/CardsTabs.tsx (паттерн таб-навигации для копии)
    - components/ui/ComingSoon.tsx (props sectionName/description)
    - app/(dashboard)/cash/page.tsx (образец RSC-страницы с requireSection)
    - lib/rbac.ts (requireSection сигнатура)
  </read_first>
  <action>
1. `components/finance/FinanceTabs.tsx` — "use client", копия CardsTabs с массивом:
```ts
const TABS = [
  { href: "/finance/balance", label: "Баланс" },
  { href: "/finance/cashflow", label: "ОДДС" },
  { href: "/finance/pnl", label: "ОПиУ" },
]
```
Активный таб: `pathname.startsWith(tab.href)` → `border-primary text-primary`, иначе muted.

2. `app/(dashboard)/finance/balance/page.tsx` — RSC-СТАБ (полный отчёт придёт в Plan 24-07):
```tsx
import { requireSection } from "@/lib/rbac"
import { FinanceTabs } from "@/components/finance/FinanceTabs"
import { ComingSoon } from "@/components/ui/ComingSoon"

export const metadata = { title: "Финансы — Баланс — Zoiten ERP" }

export default async function FinanceBalancePage() {
  await requireSection("FINANCE")
  return (
    <div className="h-full flex flex-col gap-4">
      <FinanceTabs />
      <ComingSoon sectionName="Баланс" description="Отчёт в разработке (Phase 24)." />
    </div>
  )
}
```

3. `app/(dashboard)/finance/cashflow/page.tsx` — RSC: `await requireSection("FINANCE")` +
`<FinanceTabs />` + `<ComingSoon sectionName="ОДДС" description="Отчёт о движении денежных средств — следующая фаза." />`.

4. `app/(dashboard)/finance/pnl/page.tsx` — аналогично: `sectionName="ОПиУ"`,
`description="Отчёт о прибылях и убытках — будущая фаза."`.

Все три — с `requireSection("FINANCE")` (read-гейт). FinanceTabs рендерится на всех трёх.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "app/\(dashboard\)/finance|FinanceTabs" || echo "finance routes typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - Файлы существуют: `app/(dashboard)/finance/balance/page.tsx`, `.../cashflow/page.tsx`, `.../pnl/page.tsx`, `components/finance/FinanceTabs.tsx`
    - `grep -c "requireSection(\"FINANCE\")" app/(dashboard)/finance/*/page.tsx` → 3 файла содержат гейт
    - `grep "/finance/cashflow" components/finance/FinanceTabs.tsx` находит; FinanceTabs имеет "use client"
    - `grep "ComingSoon" app/(dashboard)/finance/cashflow/page.tsx` и `.../pnl/page.tsx` находят
    - `npx tsc --noEmit` не выдаёт ошибок в файлах finance
  </acceptance_criteria>
  <done>Раздел открывается; табы переключаются; ОДДС/ОПиУ = заглушки; баланс = временный ComingSoon до 24-07.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` OK; 4 модели + 2 enum + ERP_SECTION.FINANCE в schema
- migration.sql написана вручную (ALTER TYPE + 4 CREATE TABLE + сид ставок)
- 6 точек чеклиста закрыты; SECTION_PATHS ключ `/finance/` (не ломает /finance-models)
- 3 страницы + FinanceTabs; requireSection("FINANCE") на всех
</verification>

<success_criteria>
- Sidebar показывает «Финансовая отчётность» → /finance/balance
- Табы Баланс|ОДДС|ОПиУ работают; ОДДС/ОПиУ — ComingSoon
- Тумблер FINANCE VIEW/MANAGE доступен в /admin/users
- Foundation готов — остальные планы могут использовать Prisma-типы новых моделей
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-01-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ (параллельная разработка у пользователя).
</output>
