---
phase: 24-finance-balance
plan: 08
type: execute
wave: 4
depends_on: ["24-01", "24-07"]
files_modified:
  - lib/finance-balance-schemas.ts
  - app/actions/finance-balance.ts
  - tests/finance-balance-actions.test.ts
  - components/finance/RecalcButton.tsx
  - components/finance/ManualAdjustmentsModal.tsx
  - components/finance/TaxSettingsModal.tsx
  - app/(dashboard)/finance/balance/page.tsx
autonomous: true
requirements: [FIN-BAL-12, FIN-BAL-13, FIN-BAL-14]
must_haves:
  truths:
    - "«Пересчитать дату» переоценивает costPriceAtDate/valueRub снапшота по текущей ProductCost; qty НЕ меняется; Balance API НЕ вызывается (D-04)"
    - "CRUD ручных статей: создание/soft-delete + редактирование суммы ВЕРСИОНИРУЕТ (закрыть старую deletedAt=новый effectiveFrom + create новую), прошлое не переписывается (D-08/m8)"
    - "Ставки finance.vatPct/finance.incomeTaxPct редактируются (D-15)"
    - "Факт НДС/налога вводится per закрытый квартал; upsert по (year, quarter) (D-17)"
    - "Все write-операции защищены requireSection('FINANCE','MANAGE') + revalidatePath"
  artifacts:
    - path: "app/actions/finance-balance.ts"
      provides: "5 server actions раздела"
      exports: ["recalcBalanceDate", "saveFinanceAdjustment", "deleteFinanceAdjustment", "saveTaxRates", "saveTaxPeriodActual"]
    - path: "lib/finance-balance-schemas.ts"
      provides: "Zod-схемы (вне 'use server' — vitest-safe)"
      contains: "adjustmentSchema"
    - path: "tests/finance-balance-actions.test.ts"
      provides: "RBAC + recalc + upsert тесты с mocked prisma/auth"
      contains: "recalcBalanceDate"
    - path: "components/finance/ManualAdjustmentsModal.tsx"
      provides: "CRUD ручных статей в UI"
      contains: "saveFinanceAdjustment"
  key_links:
    - from: "app/actions/finance-balance.ts"
      to: "lib/rbac.ts"
      via: "requireSection('FINANCE','MANAGE') в каждом action"
      pattern: "requireSection\\(\"FINANCE\", \"MANAGE\"\\)"
    - from: "app/(dashboard)/finance/balance/page.tsx"
      to: "components/finance/{RecalcButton,ManualAdjustmentsModal,TaxSettingsModal}"
      via: "рендер в header-зоне (комментарий 24-08 из 24-07)"
      pattern: "RecalcButton"
    - from: "app/actions/finance-balance.ts recalcBalanceDate"
      to: "FinanceStockSnapshot"
      via: "update costPriceAtDate/valueRub по текущей ProductCost, qty untouched"
      pattern: "costPriceAtDate"
---

<objective>
Управляющий слой раздела: server actions (`recalcBalanceDate` D-04, CRUD `FinanceManualAdjustment`
D-08, `saveTaxRates` D-15, `saveTaxPeriodActual` D-17) + Zod-схемы в отдельном lib-файле
(vitest-safe, паттерн pricing-schemas) + тест RBAC/логики + три клиентских компонента
(RecalcButton, ManualAdjustmentsModal, TaxSettingsModal), встроенные в header-зону page.tsx из 24-07.

Purpose: Без этого слоя баланс read-only и не корректируем: нельзя переоценить прошлую дату,
добавить «Займы выданные», поменять ставки или внести факт бухгалтерии.
Output: actions + schemas + тест + 3 компонента + wiring в page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-CONTEXT.md
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Паттерны/схемы — извлечены. -->

Zod вне 'use server' (decision Phase 07): схемы в `lib/finance-balance-schemas.ts` — Next.js 15
'use server' файлы не экспортируют sync values + vitest не грузит auth chain.
Zod 4.x: `z.enum([...], { message: "..." })`; НЕ z.coerce с RHF.

Server action паттерн (app/actions/cost.ts / pricing.ts): "use server" → requireSection →
try/catch → revalidatePath("/finance/balance") → `{ ok: true } | { ok: false, error }`.

Модели (24-01): FinanceStockSnapshot { date, productId, qty, costPriceAtDate?, valueRub? };
FinanceManualAdjustment { label, type ASSET|LIABILITY, amountRub Decimal(14,2), effectiveFrom Date,
comment?, deletedAt? }; FinanceTaxPeriodActual { year, quarter, vatActualRub?, incomeTaxActualRub?,
@@unique([year, quarter]) }; AppSetting { key @id, value String } — ключи finance.vatPct / finance.incomeTaxPct.

⚠ Pitfall 6 (RESEARCH): recalcBalanceDate НЕ должен вызывать fetchAccountBalance — дебиторка
на прошлую дату не восстановима (Balance API отдаёт только «сейчас»); qty снапшота неизменяемы.
D-04 = ТОЛЬКО переоценка costPriceAtDate/valueRub по текущей ProductCost + revalidatePath
(live-статьи пересчитаются сами при рендере).

recalcBalanceDate алгоритм:
1. rows = financeStockSnapshot.findMany({ where: { date } , select: { id, productId, qty } })
2. costs = productCost.findMany({ where: { productId: { in: productIds } } }) → Map
3. for row: newCost = map.get(productId)?.costPrice ?? null;
   update({ where: { id }, data: { costPriceAtDate: newCost, valueRub: newCost!=null ? round2(qty*newCost) : null } })
   (батчами в $transaction).

Тест-паттерн actions (tests/*-actions.test.ts): vi.mock("@/lib/rbac") + vi.mock("@/lib/prisma")
+ vi.mock("next/cache", () => ({ revalidatePath: vi.fn() })); dual-mode $transaction mock
(decision Phase 09: callback + array, tx === prismaMock).

Client dialog паттерн: base-ui Dialog c render= prop (НЕ asChild) — components/ui/dialog.tsx,
образец CashEntryForm (Phase 23). Native <select> для type ASSET/LIABILITY (CLAUDE.md convention).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Схемы + server actions + тест</name>
  <read_first>
    - lib/pricing-schemas.ts (образец схем вне 'use server')
    - app/actions/pricing.ts (образец actions: requireSection MANAGE + revalidatePath + ActionResult)
    - tests/customer-actions.test.ts или tests/stock-actions.test.ts (mock rbac/prisma/next-cache, vi.hoisted)
    - prisma/schema.prisma (модели Finance* из 24-01)
    - .planning/phases/24-finance-balance/24-RESEARCH.md (Pitfall 6 — recalc НЕ дёргает Balance API)
  </read_first>
  <behavior>
    - recalcBalanceDate: qty не меняется; costPriceAtDate/valueRub обновлены по текущей ProductCost; fetchAccountBalance НЕ вызывался
    - saveFinanceAdjustment без MANAGE → { ok:false } (requireSection throws FORBIDDEN)
    - saveTaxPeriodActual: upsert по (year, quarter); quarter вне 1..4 → ok:false
    - saveTaxRates: пишет AppSetting finance.vatPct/finance.incomeTaxPct; отрицательные → ok:false
    - saveFinanceAdjustment редактирование суммы существующей статьи (m8): старая версия получает
      deletedAt = новый effectiveFrom, создаётся новая версия — прошлые балансы не переписываются
  </behavior>
  <action>
1. `lib/finance-balance-schemas.ts`:
```ts
import { z } from "zod"
export const adjustmentSchema = z.object({
  id: z.string().optional(),               // нет id → create
  label: z.string().min(1).max(200),
  type: z.enum(["ASSET", "LIABILITY"], { message: "Тип: актив или пассив" }),
  amountRub: z.number().finite(),
  effectiveFrom: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  comment: z.string().max(1000).optional().nullable(),
})
export const taxRatesSchema = z.object({ vatPct: z.number().min(0).max(100), incomeTaxPct: z.number().min(0).max(100) })
export const taxPeriodActualSchema = z.object({
  year: z.number().int().min(2024).max(2100),
  quarter: z.number().int().min(1).max(4),
  vatActualRub: z.number().finite().nullable(),
  incomeTaxActualRub: z.number().finite().nullable(),
})
```
2. `app/actions/finance-balance.ts` ("use server"), каждый action начинается с
`await requireSection("FINANCE", "MANAGE")`, try/catch, `revalidatePath("/finance/balance")`,
возврат `{ ok: true } | { ok: false; error: string }`:
- `recalcBalanceDate(dateStr: string)` — алгоритм из interfaces (Map costs → update rows в $transaction).
  НЕ импортировать wb-finance-api (Pitfall 6). Комментарий: «qty неизменяемы (D-04), дебиторка = снапшот».
- `saveFinanceAdjustment(input)` — parse adjustmentSchema.
  - НЕТ id → create c createdById из session (getCurrentUser).
  - ЕСТЬ id (m8 — НЕ мутировать прошлое ретроактивно): ручная статья версионируется «с даты» (D-08).
    Загрузить старую запись. Если меняются amountRub / type / effectiveFrom (финансово/временны́е поля):
    в $transaction — «закрыть» старую версию `update({ where:{id}, data:{ deletedAt: new Date(input.effectiveFrom) } })`
    (действовала до новой даты) + `create` НОВОЙ версии с новыми значениями (effectiveFrom = new Date(input.effectiveFrom)).
    Так прошлые балансы (asOf < новой effectiveFrom) продолжают видеть старую сумму через окно
    `effectiveFrom <= asOf AND (deletedAt=null OR deletedAt > asOf)` из 24-05 (без double-count на границе).
    Валидация: новый effectiveFrom должен быть >= старого effectiveFrom (нельзя версионировать раньше начала).
    Если меняются ТОЛЬКО label/comment (не влияют на суммы/даты) → допустимо in-place update без версии.
- `deleteFinanceAdjustment(id: string)` — soft delete: update({ data: { deletedAt: new Date() } }).
- `saveTaxRates(input)` — parse taxRatesSchema; 2× appSetting.upsert (finance.vatPct → String(vatPct), finance.incomeTaxPct).
- `saveTaxPeriodActual(input)` — parse; financeTaxPeriodActual.upsert({ where: { year_quarter: { year, quarter } }, ... }).
3. `tests/finance-balance-actions.test.ts` — кейсы из behavior; мокнуть `@/lib/rbac`
(requireSection: успех/throw FORBIDDEN per-тест), `@/lib/prisma` (dual-mode $transaction),
`next/cache`. Проверить что в recalc update вызывался БЕЗ поля qty в data.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx vitest run tests/finance-balance-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/finance-balance-actions.test.ts` — green
    - `grep -c "requireSection(\"FINANCE\", \"MANAGE\")" app/actions/finance-balance.ts` ≥ 5
    - `grep "wb-finance-api" app/actions/finance-balance.ts` НЕ находит (Pitfall 6)
    - `grep "revalidatePath(\"/finance/balance\")" app/actions/finance-balance.ts` находит
    - `grep "deletedAt" app/actions/finance-balance.ts` находит (soft delete D-08)
    - `grep -E "deletedAt: new Date|\$transaction" app/actions/finance-balance.ts` находит версионирование при правке суммы (m8 — не мутировать прошлое)
    - Тест m8 подтверждает: при правке amountRub старая версия закрыта deletedAt, создана новая (прошлое не переписано)
    - Тест recalc подтверждает: data update не содержит qty
  </acceptance_criteria>
  <done>5 actions защищены MANAGE, протестированы; recalc не трогает qty и API.</done>
</task>

<task type="auto">
  <name>Task 2: RecalcButton + ManualAdjustmentsModal + TaxSettingsModal + wiring в page</name>
  <read_first>
    - app/(dashboard)/finance/balance/page.tsx (header-зона с комментарием 24-08 из 24-07)
    - components/ui/dialog.tsx (base-ui Dialog, render= prop НЕ asChild)
    - components/cash/CashEntryForm.tsx (образец клиентской формы с server action + useTransition)
    - components/prices/GlobalRatesBar.tsx (образец инлайн-редактора ставок с debounce/refresh)
    - app/actions/finance-balance.ts (Task 1 — сигнатуры actions)
  </read_first>
  <action>
1. `components/finance/RecalcButton.tsx` — "use client": props `{ date: string }`; кнопка
«Пересчитать дату» (иконка RefreshCw) → useTransition → `recalcBalanceDate(date)` →
toast success/error (sonner) + `router.refresh()`. Disabled во время pending.
title: «Переоценка остатков по текущей себестоимости; количества не меняются».

2. `components/finance/ManualAdjustmentsModal.tsx` — "use client": props
`{ adjustments: Array<{id,label,type,amountRub,effectiveFrom,comment}> }` (сериализовано RSC).
Кнопка «Ручные статьи» → Dialog со списком активных статей (label, тип бейджем Актив/Пассив,
сумма fmtRub, effectiveFrom, кнопки ✎/🗑) + форма добавления/редактирования: label (input),
type (native select ASSET→«Актив»/LIABILITY→«Пассив»), amountRub (number input), effectiveFrom
(input type=date), comment (textarea). Submit → saveFinanceAdjustment; удалить →
deleteFinanceAdjustment (confirm). После успеха router.refresh().

3. `components/finance/TaxSettingsModal.tsx` — "use client": props
`{ vatPct: number; incomeTaxPct: number; actuals: Array<{year,quarter,vatActualRub,incomeTaxActualRub}> }`.
Кнопка «Налоги» → Dialog с двумя секциями: (а) Ставки — 2 number-инпута (НДС %, Налог на доходы %)
+ «Сохранить» → saveTaxRates; (б) Факт по кварталам (D-17) — селектор год/квартал (native select)
+ 2 инпута (Факт НДС ₽, Факт налог ₽, пустое = null → использовать расчёт) + «Сохранить» →
saveTaxPeriodActual. Подпись: «Факт закрытого квартала заменяет расчёт; текущий квартал всегда расчётный».

4. Wiring `app/(dashboard)/finance/balance/page.tsx`: в RSC догрузить props —
`financeManualAdjustment.findMany({ where: { deletedAt: null }, orderBy: { effectiveFrom: "desc" } })`,
`financeTaxPeriodActual.findMany()`, AppSetting finance.vatPct/incomeTaxPct (Number, дефолт 7/1).
Decimal→Number, Date→"YYYY-MM-DD" на границе. В header-зону (комментарий из 24-07) вставить:
`<RecalcButton date={dateStr} />`, `<ManualAdjustmentsModal adjustments={...} />`,
`<TaxSettingsModal vatPct={...} incomeTaxPct={...} actuals={...} />`.
Компоненты management показывать всем с VIEW (сами actions вернут FORBIDDEN без MANAGE) —
опционально скрыть через getSectionRole("FINANCE")==="MANAGE" (предпочтительно, паттерн проекта).
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "finance/balance|RecalcButton|ManualAdjustments|TaxSettings" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - Файлы созданы: RecalcButton.tsx, ManualAdjustmentsModal.tsx, TaxSettingsModal.tsx — все "use client"
    - `grep "recalcBalanceDate" components/finance/RecalcButton.tsx` находит
    - `grep "<select" components/finance/ManualAdjustmentsModal.tsx` находит (native select для типа)
    - `grep -E "RecalcButton|ManualAdjustmentsModal|TaxSettingsModal" app/(dashboard)/finance/balance/page.tsx` → 3 компонента отрендерены
    - `grep "render=" components/finance/ManualAdjustmentsModal.tsx` находит ИЛИ DialogTrigger без asChild (base-ui паттерн)
    - `npx tsc --noEmit` — нет ошибок
  </acceptance_criteria>
  <done>Пересчёт, ручные статьи и налоговые настройки доступны из UI; page подгружает их данные.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/finance-balance-actions.test.ts` — green
- `npm run test` — полный набор green
- Header /finance/balance содержит RecalcButton + Ручные статьи + Налоги
</verification>

<success_criteria>
- D-04: переоценка даты работает, qty неизменяемы, Balance API не дёргается
- D-08: ручные статьи создаются/правятся/снимаются (soft delete) и попадают в баланс
- D-15/D-17: ставки и факт per квартал редактируются и влияют на расчёт
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-08-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ.
</output>
