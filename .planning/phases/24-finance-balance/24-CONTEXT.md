# Phase 24: Финансовая отчётность — Баланс (управленческий учёт) — Context

**Gathered:** 2026-07-02
**Status:** Ready for planning
**⚠ ВАЖНО:** Пользователь запретил коммитить артефакты этой фазы до окончания параллельной разработки (указание 2026-07-02). Только планирование и проектирование.

<domain>
## Phase Boundary

Первый из трёх управленческих отчётов (Баланс → ОДДС → ОПиУ). Эта фаза делает ТОЛЬКО Баланс: консолидированный отчёт активы/пассивы/капитал по группе компаний, формируется на 01.07.2026 и далее на каждую дату (ежедневно). ОДДС и ОПиУ — отдельные будущие фазы, но архитектура раздела (таб-навигация, snapshot-инфраструктура) должна их предусматривать.

</domain>

<decisions>
## Implementation Decisions

### Историчность и снапшоты
- **D-01: Гибридная архитектура.** Ежедневные снапшоты ТОЛЬКО для статей без истории: (а) товарные остатки — новая snapshot-таблица с количеством И себестоимостью на дату; (б) дебиторка WB — снапшот ответа Balance API + хвост. Все остальные статьи (банк, касса, кредиты, авансы поставщикам, налоги) вычисляются НА ЛЕТУ на любую дату из транзакционных данных (`date <= X`).
- **D-02: Момент среза — «утром за вчера».** Cron ~06:00 МСК (после ночных WB-sync) фиксирует состояние на конец вчерашнего дня. «Баланс на 01.07» = состояние на конец дня 30.06 / начало 01.07 (как в бухгалтерии).
- **D-03: История с 01.07.2026, без ретроспективы.** Первый снапшот — баланс на 01.07. Прошлые даты не восстанавливаются.
- **D-04: Кнопка «Пересчитать дату».** Для выбранной даты: переоценивает зафиксированные количества остатков по обновлённой себестоимости + пересобирает ретро-вычислимые статьи (касса/кредиты/банк/авансы/налоги — они и так live, но если появится кэш — сброс). Количества в снапшоте неизменяемы (их не восстановить задним числом).

### Структура и консолидация
- **D-05: Консолидированный баланс** по всей группе компаний. Разрез по компаниям — deferred (касса, товары, закупки, дебиторка WB не привязаны к Company).
- **D-06: Вертикальная таблица:** Активы (разделы с подытогами) → Пассивы → строка «Капитал» = Активы − Пассивы (балансирующая статья).
- **D-07: Состав статей:**
  - АКТИВЫ: Денежные средства (банк — по счетам/валютам, касса) · Дебиторка WB · Запасы (склады WB / WB в пути к клиенту / WB в пути от клиента / склад Иваново / товар в пути из Китая) · Авансы поставщикам
  - ПАССИВЫ: Кредиты и займы · Налоговые обязательства
  - КАПИТАЛ: вычисляемая балансирующая строка
- **D-08: Ручные корректировочные статьи.** Пользователь может добавлять произвольные статьи (актив или пассив) с ручным вводом суммы, действующей с даты (например «Займы выданные», «Прочая дебиторка»). CRUD в UI раздела.
- **D-09: Две даты + дельта.** Выбранная дата + дата сравнения (default — начало месяца) + колонка изменения ₽/%.

### Оценка запасов и предоплат
- **D-10: Складские остатки (WB + Иваново): кол-во × текущая `ProductCost.costPrice` (₽).** Снапшот хранит и qty, и costPrice на момент среза; кнопка D-04 переоценивает qty по свежей себестоимости.
- **D-11: Товары без себестоимости** — отдельная строка «Без оценки: N товаров, M шт» с предупреждением и расшифровкой (списком товаров). Баланс явно показывает неполноту.
- **D-12: Закупки из Китая — по фактически уплаченному (кассовая оценка).** Все PAID-платежи по незавершённым закупкам (₽ по курсу на дату платежа): этапы до Отгрузки → статья «Авансы поставщикам»; Отгрузка / В пути (SHIPMENT, TRANSIT) → статья «Товар в пути из Китая». Граница по `currentStageOf()` из `lib/purchase-stages.ts`. Без кредиторки поставщикам и без валютной переоценки в v1.
- **D-13: WB «в пути к клиенту / от клиента»** (`WbCard.inWayToClient/inWayFromClient`) — включаются в Запасы отдельными подстроками, оцениваются по себестоимости (D-10).

### Налоги и дебиторка WB
- **D-14: Дебиторка WB — двухслойная схема:**
  1. Ежедневный снапшот `GET finance-api.wildberries.ru/api/v1/account/balance` → `current` + `for_withdraw` (официальный баланс по закрытым отчётам). Rate limit 1 req/мин.
  2. Хвост текущей недели: Σ `forPay` из `GET statistics-api.wildberries.ru/api/v1/supplier/sales` за незакрытую отчётную неделю (с понедельника).
  - Дебиторка на дату = balance.current + хвост. Требуется НОВЫЙ токен со scope «Финансы» (бит 13 официальной таблицы) — выпускает пользователь в ЛК WB; инфраструктура `WbApiToken` уже поддерживает. НЕ строить на `reportDetailByPeriod` — deprecated, удаляется 15.07.2026.
- **D-15: Налоговая база = выкупы WB по дням:** Σ `WbCardFunnelDaily.buyoutsSumRub` (данные с 01.04.2026, обновляются ежедневно). Ставки 7% НДС + 1% налог на доходы (УСН 15% Д−Р, бухгалтерия выводит на 1%) — хранить как редактируемые настройки (AppSetting), не хардкод.
- **D-16: Накопление обязательства — квартал минус уплаченное.** Обязательство на дату X = (7%+1%) × база с начала квартала до X − уплаченные налоги за этот период (BankTransaction `category=TAX` + аналогичная категория кассы) + незакрытые обязательства прошлых кварталов.
- **D-17: Корректировка бухгалтерии — факт per период перекрывает расчёт.** Форма ввода фактических сумм НДС и налога для закрытого периода (квартала); введённый факт заменяет расчётную цифру в балансе. Текущий (незакрытый) период всегда расчётный.

### Claude's Discretion
- **Гранулярность снапшота остатков:** рекомендуется per Product × локация (WB склады агрегированно / WB в пути к клиенту / WB в пути от клиента / Иваново) с полями qty + costPriceAtDate — даёт расшифровку статей и переоценку; точный дизайн таблиц — на планирование.
- **UI-размещение:** новый раздел «Финансы» `/finance/balance`, новый `ERP_SECTION.FINANCE`, полный чеклист из 6 пунктов CLAUDE.md (включая `SECTION_OPTIONS`!). Таб-навигация Баланс | ОДДС | ОПиУ (два последних — ComingSoon-заглушки под будущие фазы), паттерн `CardsTabs/PricesTabs`.
- **RBAC:** чтение = `requireSection("FINANCE")`, write-операции (пересчёт, корректировки, ручные статьи, настройки ставок) = `requireSection("FINANCE", "MANAGE")`.
- **Rate-limit защита Finance API:** cooldown-bucket по паттерну `lib/wb-cooldown.ts` (bucket 'finance'), retryFetch backoff.
- **Формат ₽:** ru-RU, пробел-тысячи, ₽; `Decimal(14,2)` для новых денежных полей (паттерн проекта); `Number(decimal)` на RSC→client границе.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Данные и helpers (существующий код)
- `prisma/schema.prisma` — модели: BankAccount (~:1677), BankTransaction (~:1707), CashEntry (~:1769), Loan (~:1277), LoanPayment (~:1301), Purchase (~:1466), PurchasePayment (~:1629), PurchaseItemStageProgress (~:1606), CurrencyRate (~:1651), ProductCost (~:446), WbCard.stockQty/inWayToClient/inWayFromClient (~:326-328), WbCardWarehouseStock (~:978), Product.ivanovoStock (~:410), ProductIncoming (~:1247), WbCardFunnelDaily (~:1103), WbApiToken, AppSetting
- `lib/loan-math.ts:130` — `computeLoanAggregates(amount, payments, asOf)` — остаток кредита на дату, ГОТОВ (критично передавать asOf)
- `lib/credits-data.ts:131` — `loadCreditsDashboard()` — образец агрегатного helper'а
- `lib/purchase-stages.ts:65` — `currentStageOf()` — этап закупки (граница аванс/в-пути, D-12)
- `lib/stock-data.ts:85`, `lib/stock-wb-data.ts` — текущие остатки (источник для снапшота)
- `app/(dashboard)/bank/page.tsx:140-300` + `components/bank/BankDashboard.tsx` — anchor-дата = MAX(balanceDate), остатки банка (прецедент «баланс на дату»)
- `app/(dashboard)/cash/page.tsx:155-164` — баланс кассы Σ INCOME − Σ EXPENSE
- `lib/wb-api.ts`, `lib/wb-cooldown.ts`, `lib/wb-jwt.ts` — паттерны WB API клиента, cooldown-локи, scope-биты (⚠ `wb-jwt.ts` scope-мэппинг расходится с официальной таблицей — см. RESEARCH)
- `CLAUDE.md` §«Чеклист при добавлении нового раздела ERP» — 6 обязательных пунктов для ERP_SECTION.FINANCE

### WB Finance API (результаты исследования 2026-07-02, вошло в discuss)
- `GET https://finance-api.wildberries.ru/api/v1/account/balance` — scope Финансы (бит 13), 1 req/мин, ответ `{currency, current, for_withdraw}` — прямая дебиторка по закрытым отчётам
- `GET https://statistics-api.wildberries.ru/api/v1/supplier/sales` — поле `forPay` — хвост незакрытой недели
- `POST https://finance-api.wildberries.ru/api/finance/v1/sales-reports/list|detailed` — новые отчёты реализации (с 15.04.2026, данные с 01.01.2025), поля `forPaySum`, `bankPaymentSum` — для будущей точной сверки (deferred)
- ⚠ `GET statistics-api…/api/v5/supplier/reportDetailByPeriod` — DEPRECATED, удаляется 15.07.2026 — НЕ ИСПОЛЬЗОВАТЬ
- Официальная таблица scope-битов: 1=Контент, 2=Аналитика, 3=Цены, 4=Маркетплейс, 5=Статистика, 6=Продвижение, 7=Вопросы/отзывы, 9=Чат, 10=Поставки, 11=Возвраты, 12=Документы, **13=Финансы**, 30=read-only
- Docs: https://dev.wildberries.ru/ru/openapi/financial-reports-and-accounting

</canonical_refs>

<code_context>
## Existing Code Insights

### Что считается на произвольную дату УЖЕ (данные есть)
- Кредиты: `computeLoanAggregates(…, asOf)` готов
- Касса: Σ по `date <= X` (helper дописать, логика тривиальна)
- Банк: `closingBalance` на anchor-дату ± транзакции (helper «остаток на дату» дописать)
- Авансы поставщикам: PAID-платежи по `paidDate` + этапы закупок (агрегат дописать)
- Выкупы WB (налоговая база): `WbCardFunnelDaily` daily с 01.04.2026

### Что существует только «сейчас» (поэтому снапшоты, D-01)
- Товарные остатки: WbCard.stockQty / WbCardWarehouseStock / ivanovoStock — перезаписываются при sync
- Себестоимость: ProductCost.costPrice — одно текущее значение, истории нет
- Дебиторка WB: отсутствует полностью (Balance API отдаёт только «сейчас»)

### Established Patterns
- Cron-диспетчер (systemd timers + `/api/cron/*` c `x-cron-secret`) — для ежедневного снапшота
- `WbApiToken` CRUD с JWT scope-валидацией — для нового токена «Финансы»
- Sticky raw-HTML таблицы, summary-карточки, строка Итого `bg-muted` (без прозрачности!), Decimal(14,2), `@db.Date`, МСК
- AppSetting KV — для ставок налогов (7%/1%) и настроек снапшота

### Integration Points
- `ERP_SECTION` enum + 6-точечный чеклист (nav, sections, titles, labels, RBAC)
- Cron-расписание: снапшот ПОСЛЕ ночных WB-sync (~06:00 МСК)
- Finance API — новый base URL в `lib/wb-api.ts` или отдельный модуль `lib/wb-finance-api.ts` (по образцу `lib/wb-support-api.ts`)

</code_context>

<specifics>
## Specific Ideas

- «Баланс на 01.07» — точка отсчёта всей отчётности; далее ОДДС (ежедневно) и ОПиУ (за период, в будущем — прогноз по плану продаж) в следующих фазах.
- Первый снапшот «на 01.07» (bootstrap): по официальной документации WB (вставлена пользователем 2026-07-02) остатки WB **по дням до 3 месяцев назад** доступны через отчёт `STOCK_HISTORY_DAILY_CSV` («Аналитика продавца CSV», токен категории Аналитика) — bootstrap WB-остатков на 30.06/01.07 делается ТОЧНО. Иваново и «в пути из Китая» — истории нет, берутся из ближайших доступных данных с пометкой. Ретро-вычислимые статьи (банк/касса/кредиты/авансы/налоги) на 01.07 считаются точно. Планировщику: однократный bootstrap-скрипт «снапшот на 01.07»; точный endpoint STOCK_HISTORY_DAILY_CSV подтвердить в Wave 0 (страница доков не предоставлена; механизм CSV-заданий уже есть в `lib/wb-api.ts:fetchBuyoutPercent`; учесть daily-cap Analytics 3/день).
- ⚠ Токен «Финансы» должен быть **Персональным или Сервисным** (не базовым): `finance/v1/sales-reports/*` на базовом недоступны, а Balance API на базовом = 1 запрос/сутки. Balance rate limit (перс./серв.) = 1 req/мин; ошибка 402 Payment Required — обрабатывать. Деньги в sales-reports — строки; daily reportId требует BigInt.
- Налоговая логика бизнеса: УСН 15% «доходы минус расходы», но бухгалтерия стабильно выводит разницу на ~1% от дохода; НДС 7%. Итого расчётное бремя 8% от выкупов — расчётная оценка, корректируемая фактом бухгалтерии (D-17).
- Пользователь должен выпустить новый WB-токен со scope «Финансы» до UAT дебиторки — включить в план как user-checkpoint.

</specifics>

<deferred>
## Deferred Ideas

- **ОДДС (отчёт о движении денежных средств)** — следующая фаза; `TxCategory` в банке уже создан под него (Phase 22)
- **ОПиУ (прибыли и убытки)** за период + прогноз на основе плана продаж — фаза после ОДДС
- **Разрез баланса по компаниям группы** — требует привязки кассы/закупок/товаров к Company
- **Точная дебиторка WB (слой 3):** еженедельная загрузка отчётов реализации `finance/v1/sales-reports` + сверка выплат с банковской выпиской (`bankPaymentSum` ↔ поступления от WB)
- **Кредиторка поставщикам + оценка «в пути» по контрактной стоимости** (кол-во × unitPrice × курс ЦБ) — v1 считает по уплаченному (D-12)
- **Валютная переоценка** (CNY остатки банка в ₽ по курсу ЦБ на дату баланса) — v1 показывает CNY отдельно, как в /bank
- **Начисленные-но-неуплаченные проценты по кредитам на дату** — v1 только по графику платежей
- **Исправление scope-мэппинга `lib/wb-jwt.ts`** под официальную таблицу битов (5/6/7/30 расходятся) — аккуратно, не сломать существующие проверки; можно отдельным quick task

</deferred>

---

*Phase: 24-finance-balance*
*Context gathered: 2026-07-02*
