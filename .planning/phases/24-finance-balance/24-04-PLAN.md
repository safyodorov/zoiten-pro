---
phase: 24-finance-balance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/balance-data.ts
  - tests/balance-data.test.ts
autonomous: true
requirements: [FIN-BAL-07]
must_haves:
  truths:
    - "getBankBalanceAsOf возвращает остаток счёта на произвольную дату (не только anchor)"
    - "Знаки корректны: CREDIT (+приход), DEBIT (−расход); границы интервала (X, anchor] строгие по X"
    - "getRateForDate возвращает курс ЦБ на дату (date <= X, desc); fallback на самый ранний с флагом"
    - "stageAsOf: этапы date<=X достигнуты; undated-этап (date=null) достигнут лишь на текущей дате (m7 паритет с /procurement); затем currentStageOf (D-12)"
  artifacts:
    - path: "lib/balance-data.ts"
      provides: "point-in-time хелперы баланса"
      exports: ["getBankBalanceAsOf", "getRateForDate", "stageAsOf"]
      min_lines: 60
    - path: "tests/balance-data.test.ts"
      provides: "тесты знаков/границ/fallback с mocked prisma"
      contains: "getBankBalanceAsOf"
  key_links:
    - from: "lib/balance-data.ts stageAsOf"
      to: "lib/purchase-stages.ts currentStageOf"
      via: "pre-filter date<=X → currentStageOf(reached)"
      pattern: "currentStageOf"
    - from: "tests/balance-data.test.ts"
      to: "lib/balance-data.ts"
      via: "import хелперов"
      pattern: "from \"@/lib/balance-data\""
---

<objective>
Три point-in-time хелпера в `lib/balance-data.ts` — заполняют дыры, которых НЕТ в проекте
(RESEARCH Pitfall 1/4): (1) `getBankBalanceAsOf` — остаток банковского счёта на любую дату
(в проекте есть только anchor = MAX(balanceDate)); (2) `getRateForDate` — курс ЦБ на дату
платежа (`getLatestRate` даёт только последний известный, не point-in-time); (3) `stageAsOf` —
текущий этап закупки на дату через pre-filter + существующий `currentStageOf` (D-12).

Purpose: Эти хелперы — фундамент агрегатора loadBalanceSheet (Plan 24-05, тот же файл). Их
поведение хрупкое (знаки/границы) → изолируем и тестируем отдельно. Wave 0 test requirement.
Output: lib/balance-data.ts (только хелперы) + tests/balance-data.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Реализации из RESEARCH «Code Examples» — verbatim. Схема подтверждена. -->

BankTransaction (schema.prisma:1707): `date DateTime @db.Date`, `direction TxDirection` (DEBIT=расход|CREDIT=приход),
`amount Decimal @db.Decimal(18,2)`, `accountId`.
BankAccount (schema.prisma:1677): `closingBalance Decimal?`, `balanceDate DateTime? @db.Date`.
CurrencyRate (schema.prisma:1649): `date @db.Date`, `code`, `rateToRub Decimal @db.Decimal(14,6)`, `@@unique([date, code])`.
currentStageOf (lib/purchase-stages.ts:65): `(reached: readonly string[]) => StageKey | null` — НЕ принимает дату.

getBankBalanceAsOf (RESEARCH Code Examples, verbatim):
```ts
export async function getBankBalanceAsOf(accountId: string, asOf: Date): Promise<number | null> {
  const account = await prisma.bankAccount.findUnique({
    where: { id: accountId }, select: { closingBalance: true, balanceDate: true },
  })
  if (!account?.closingBalance || !account.balanceDate) return null
  const closing = Number(account.closingBalance)
  const anchor = account.balanceDate
  if (asOf.getTime() >= anchor.getTime()) {
    const txs = await prisma.bankTransaction.findMany({
      where: { accountId, date: { gt: anchor, lte: asOf } }, select: { direction: true, amount: true },
    })
    const delta = txs.reduce((s, t) => s + (t.direction === "CREDIT" ? 1 : -1) * Number(t.amount), 0)
    return closing + delta
  }
  const txs = await prisma.bankTransaction.findMany({
    where: { accountId, date: { gt: asOf, lte: anchor } }, select: { direction: true, amount: true },
  })
  const delta = txs.reduce((s, t) => s + (t.direction === "CREDIT" ? 1 : -1) * Number(t.amount), 0)
  return closing - delta
}
```

getRateForDate (RESEARCH Pitfall 4 — новый point-in-time query + fallback):
```ts
export interface RateAsOf { rateToRub: number; date: Date; approximate: boolean }
export async function getRateForDate(code: string, asOf: Date): Promise<RateAsOf | null> {
  const exact = await prisma.currencyRate.findFirst({
    where: { code, date: { lte: asOf } }, orderBy: { date: "desc" },
  })
  if (exact) return { rateToRub: Number(exact.rateToRub), date: exact.date, approximate: false }
  // fallback: самый ранний доступный курс (курсы forward-only с 2026-06-09), с флагом approximate
  const earliest = await prisma.currencyRate.findFirst({ where: { code }, orderBy: { date: "asc" } })
  return earliest ? { rateToRub: Number(earliest.rateToRub), date: earliest.date, approximate: true } : null
}
```

stageAsOf (RESEARCH Code Examples + m7 — не терять этапы с date=null на текущей дате):
```ts
import { currentStageOf } from "@/lib/purchase-stages"
import { startOfDayMsk } from "@/lib/date-periods"
// m7: PurchaseItemStageProgress.date nullable. Этап с прогрессом, но БЕЗ даты — «достигнут,
// время неизвестно». Чтобы stageAsOf на ТЕКУЩЕЙ дате совпадал с /procurement currentStageOf
// (который дату игнорирует), считаем такой этап достигнутым, когда asOf «сегодня или позже».
// Для исторических дат undated-этап во времени разместить нельзя → не учитываем (не завышаем прошлое).
export function stageAsOf(
  stages: Array<{ stage: string; date: Date | null }>,
  asOf: Date,
  now: Date = new Date(),
): string | null {
  const asOfIsCurrent = asOf.getTime() >= startOfDayMsk(now).getTime()
  const reached = stages
    .filter((s) =>
      (s.date != null && s.date.getTime() <= asOf.getTime()) ||
      (s.date == null && asOfIsCurrent), // undated → достигнут только «сейчас» (m7, паритет с /procurement)
    )
    .map((s) => s.stage)
  return currentStageOf(reached)
}
```

Тест-мок prisma (паттерн tests/wb-adv-api.test.ts): vi.mock("@/lib/prisma", () => ({ prisma: { bankAccount: {...}, bankTransaction: {...}, currencyRate: {...} } })).
stageAsOf — pure, тестируется без мока (передаём массив напрямую).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/balance-data.ts — три point-in-time хелпера</name>
  <read_first>
    - lib/cbr-rates.ts (getLatestRate — понять чем getRateForDate отличается: point-in-time vs последний)
    - lib/purchase-stages.ts (currentStageOf, STAGE_ORDER)
    - app/(dashboard)/bank/page.tsx (строки 130-180 — anchor logic, знак direction)
    - prisma/schema.prisma (1677 BankAccount, 1707 BankTransaction, 1649 CurrencyRate)
  </read_first>
  <action>
Создать `lib/balance-data.ts` с `import { prisma } from "@/lib/prisma"`,
`import { currentStageOf } from "@/lib/purchase-stages"` и `import { startOfDayMsk } from "@/lib/date-periods"`
(нужен для m7-паритета stageAsOf на текущей дате). Реализовать `getBankBalanceAsOf`,
`RateAsOf` + `getRateForDate`, `stageAsOf` — ровно по коду из `<interfaces>` выше (verbatim).
Комментарий-шапка: «point-in-time хелперы; getBankBalanceAsOf — знак CREDIT=+ / DEBIT=−,
границы (X, anchor] строгие по X; getRateForDate — fallback на самый ранний курс с approximate=true
(курсы CurrencyRate forward-only с 2026-06-09)».
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "balance-data" || echo "balance-data typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export (async function getBankBalanceAsOf|async function getRateForDate|function stageAsOf)" lib/balance-data.ts` → 3 совпадения
    - `grep "direction === \"CREDIT\" ? 1 : -1" lib/balance-data.ts` находит (корректный знак)
    - `grep "orderBy: { date: \"desc\" }" lib/balance-data.ts` находит (point-in-time query)
    - `grep "currentStageOf" lib/balance-data.ts` находит (stageAsOf делегирует)
    - `npx tsc --noEmit` — нет ошибок в balance-data.ts
  </acceptance_criteria>
  <done>Три хелпера реализованы точно по RESEARCH; знаки/границы/fallback как задумано.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/balance-data.test.ts — знаки, границы, fallback</name>
  <read_first>
    - lib/balance-data.ts (созданный в Task 1 — контракт)
    - tests/wb-adv-api.test.ts (паттерн vi.mock("@/lib/prisma", ...) + vi.fn мок-методов)
  </read_first>
  <action>
Создать `tests/balance-data.test.ts`. Замокать `@/lib/prisma`. Кейсы:
- **getBankBalanceAsOf, asOf в прошлом**: closingBalance=1000, balanceDate=2026-07-10; tx между
  (asOf=2026-07-05, anchor]: один CREDIT 200, один DEBIT 50 → delta = 200−50 = 150 → результат
  closing − delta = 1000 − 150 = 850.
- **getBankBalanceAsOf, asOf >= anchor**: closingBalance=1000, balanceDate=2026-07-01, asOf=2026-07-10,
  tx (anchor, asOf]: CREDIT 300 → результат 1000 + 300 = 1300.
- **getBankBalanceAsOf, нет closingBalance** → null.
- **getRateForDate exact**: currencyRate.findFirst(date lte) вернул {rateToRub:12.5, date:2026-07-01}
  → { rateToRub:12.5, approximate:false }.
- **getRateForDate fallback**: первый findFirst → null, второй (earliest) → {rateToRub:11.0, date:2026-06-09}
  → { rateToRub:11.0, approximate:true }.
- **stageAsOf** (pure, без мока): stages=[{stage:"PRODUCTION",date:2026-06-01},{stage:"SHIPMENT",date:2026-07-05},{stage:"TRANSIT",date:null}];
  asOf=2026-06-15 (историческая) → "PRODUCTION" (SHIPMENT ещё не достигнут, TRANSIT undated → не учитывается на прошлой дате);
  asOf=2026-07-10 (историческая) → "SHIPMENT" (TRANSIT undated не поднимает этап в прошлом).
- **stageAsOf m7 — undated на текущей дате**: те же stages, `stageAsOf(stages, D, D)` где D=«сегодня»
  → "TRANSIT" (undated-этап TRANSIT достигнут на текущей дате, паритет с /procurement currentStageOf).
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx vitest run tests/balance-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/balance-data.test.ts` — все тесты green
    - Тест «asOf в прошлом» проверяет результат 850 (знак вычитания корректен)
    - Тест getRateForDate fallback проверяет `approximate: true`
    - Тест stageAsOf проверяет: историческую дату (PRODUCTION до SHIPMENT.date, SHIPMENT после) И
      undated-этап на текущей дате (m7 → TRANSIT, паритет с /procurement)
    - `grep "vi.mock(\"@/lib/prisma\"" tests/balance-data.test.ts` находит
  </acceptance_criteria>
  <done>Знаки/границы/fallback/pre-filter закреплены тестами.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/balance-data.test.ts` — green
- lib/balance-data.ts экспортирует 3 хелпера, готов к расширению loadBalanceSheet (24-05)
</verification>

<success_criteria>
- Остаток банка на дату, курс ЦБ на дату, этап закупки на дату — все считаются корректно
- Хрупкая логика знаков/границ покрыта тестами до сборки агрегатора
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-04-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ.
</output>
