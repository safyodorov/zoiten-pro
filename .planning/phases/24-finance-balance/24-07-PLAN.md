---
phase: 24-finance-balance
plan: 07
type: execute
wave: 3
depends_on: ["24-05"]
files_modified:
  - app/(dashboard)/finance/balance/page.tsx
  - components/finance/BalanceSheetTable.tsx
  - components/finance/BalanceDatePicker.tsx
autonomous: true
requirements: [FIN-BAL-10]
must_haves:
  truths:
    - "/finance/balance показывает вертикальную таблицу: Активы (разделы+подытоги) → Пассивы → Капитал (D-06)"
    - "Две даты: выбранная + сравнения (default начало месяца) + колонки дельты ₽ и % (D-09)"
    - "Строка «Без оценки: N товаров, M шт» с раскрываемой расшифровкой товаров (D-11)"
    - "Суммы ru-RU с пробелами-тысячами и ₽; подытоги на сплошном bg-muted (без /NN прозрачности)"
    - "Даты управляются через URL searchParams (shareable, back/forward работают)"
  artifacts:
    - path: "components/finance/BalanceSheetTable.tsx"
      provides: "вертикальная таблица баланса с дельтами"
      contains: "Капитал"
      min_lines: 100
    - path: "components/finance/BalanceDatePicker.tsx"
      provides: "клиентский выбор 2 дат через URL"
      contains: "useRouter"
    - path: "app/(dashboard)/finance/balance/page.tsx"
      provides: "RSC: loadBalanceSheet × 2 даты + рендер"
      contains: "loadBalanceSheet"
  key_links:
    - from: "app/(dashboard)/finance/balance/page.tsx"
      to: "lib/balance-data.ts"
      via: "loadBalanceSheet(date) + loadBalanceSheet(compareDate)"
      pattern: "loadBalanceSheet"
    - from: "app/(dashboard)/finance/balance/page.tsx"
      to: "lib/balance-math.ts"
      via: "computeDelta для колонок изменения"
      pattern: "computeDelta"
---

<objective>
UI отчёта: заменить ComingSoon-стаб `/finance/balance` (из 24-01) на реальный отчёт — RSC-страница
вызывает `loadBalanceSheet` для двух дат (выбранная + сравнения), считает дельты через
`computeDelta`, рендерит вертикальную таблицу Активы→Пассивы→Капитал с подытогами разделов,
строкой «Без оценки» (D-11) и колонками Δ₽/Δ% (D-09). Выбор дат — клиентский компонент, URL-driven.

Purpose: Видимый результат фазы — сам отчёт. Управляющие элементы (пересчёт, корректировки,
ставки, факт налога) добавит Plan 24-08 — эта страница строится так, чтобы 24-08 только добавил
компоненты в header-зону.
Output: page.tsx (реальный отчёт) + BalanceSheetTable + BalanceDatePicker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-CONTEXT.md

<interfaces>
<!-- Контракты из 24-05 + паттерны CLAUDE.md. -->

Из lib/balance-data.ts (Plan 24-05):
```ts
interface BalanceLine { key: string; label: string; amountRub: number; currency?: "RUB" | "CNY"; approximate?: boolean; note?: string }
interface BalanceGroup { key: string; label: string; lines: BalanceLine[]; subtotalRub: number }
interface BalanceSection { key: "assets"|"liabilities"; label: string; groups: BalanceGroup[]; totalRub: number }
interface UnvaluedStock { productCount: number; qtySum: number; products: Array<{sku:string;name:string;qty:number}> }
interface BalanceSheet { date: Date; assets: BalanceSection; liabilities: BalanceSection; capitalRub: number; unvaluedStock: UnvaluedStock }
async function loadBalanceSheet(asOf: Date): Promise<BalanceSheet>
```
Из lib/balance-math.ts (24-02): `computeDelta(current, compare): { abs: number; pct: number | null }`.
Из lib/date-periods.ts: `startOfMonthMsk(date)` существует, но ⚠ M5 — НЕ использовать
`.toISOString()` от него для строки сравнения: startOfMonthMsk возвращает Date в МСК-полночь
(`…-01T00:00:00+03:00`), а `.toISOString()` уводит в UTC → «30-е число ПРЕДЫДУЩЕГО месяца»
(в июле 2026 → 2026-06-30, до начала истории D-03). Строку сравнения формировать напрямую как
`date.slice(0,7) + "-01"` (YYYY-MM-01), БЕЗ toISOString.

Формат ₽: `new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 0 }).format(n) + " ₽"`
(пробел-тысячи). Дельта %: 1 знак после запятой, знак +/−, null → "—".

Sticky-таблица (CLAUDE.md § Sticky data-таблицы) — таблица баланса ВЕРТИКАЛЬНАЯ и небольшая
(~25 строк), горизонтальный skroll не нужен, но правила фона обязательны:
- Подытоги разделов и строка Капитал: `bg-muted` СПЛОШНОЙ (НЕ bg-muted/40!).
- Если делается sticky header: `<thead className="bg-background">` + прямые `<tr>`.
Строки: обычная `<table className="w-full border-separate border-spacing-0">` + TableBody/TableRow
из shadcn допустимы в body.

Колонки таблицы: Статья | На {дату} | На {дату сравнения} | Δ ₽ | Δ %.

URL-driven date picker (паттерн PeriodFilter Phase 13 / StatsTabs): "use client",
useSearchParams + useRouter.push(`/finance/balance?date=YYYY-MM-DD&compare=YYYY-MM-DD`),
native <input type="date"> (CLAUDE.md: native controls). Без локального state-каскада.

searchParams в Next 15: `searchParams: Promise<{ date?: string; compare?: string }>` — await.
Парсинг даты: `new Date(\`${s}T00:00:00+03:00\`)` при валидном YYYY-MM-DD, иначе fallback.
Default date = вчера МСК (последний снапшот per D-02); default compare = `date.slice(0,7) + "-01"`
(начало месяца выбранной даты, M5 — БЕЗ startOfMonthMsk().toISOString()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BalanceSheetTable — вертикальная таблица с подытогами и дельтами</name>
  <read_first>
    - lib/balance-data.ts (типы BalanceSheet/BalanceSection/BalanceLine — контракт props)
    - CLAUDE.md (§ Sticky data-таблицы — сплошной bg-muted на подытогах; § Форматирование чисел)
    - components/cost/CostTable.tsx или components/cash/CashTable.tsx (образец таблицы раздела)
  </read_first>
  <action>
Создать `components/finance/BalanceSheetTable.tsx` (server component — интерактивности нет,
кроме `<details>` для расшифровки «Без оценки»):

Props: `{ current: BalanceSheetSerialized; compare: BalanceSheetSerialized; currentLabel: string; compareLabel: string }`
— где Serialized = BalanceSheet c date:string (RSC-границы нет — компонент серверный, но
даты передавать строками для форматирования).

Рендер (5 колонок: Статья | На {currentLabel} | На {compareLabel} | Δ ₽ | Δ %):
1. Заголовок секции «АКТИВЫ» (строка bg-muted font-semibold).
2. Для каждой группы: строка-заголовок группы; строки статей (padding-left отступ,
   `approximate` → значок ⚠ с title=note; note → text-muted-foreground подпись);
   строка подытога группы `bg-muted` (СПЛОШНОЙ) «Итого {group.label}».
3. Строка «ИТОГО АКТИВЫ» (bg-muted font-semibold).
4. Аналогично «ПАССИВЫ» → «ИТОГО ПАССИВЫ».
5. Финальная строка «КАПИТАЛ» (bg-muted font-bold, значение capitalRub; отрицательный — text-red-600).
6. После таблицы, если unvaluedStock.productCount > 0: блок-предупреждение (amber) со строкой
   «Без оценки: {N} товаров, {M} шт» + `<details><summary>Показать товары</summary>` со списком
   {sku} {name} — {qty} шт (D-11).
7. CNY-строки (line.currency==="CNY", m4): показывать со значением и пометкой «справочно»
   (в рублёвые подытоги НЕ входят — subtotalRub уже их исключил в 24-05); дельту для них не считать.

Дельты: для каждой строки/подытога сопоставить по key со сравнением (Map по line.key /
group.key / section totals), `computeDelta(cur, cmp)` из `@/lib/balance-math`;
Δ₽ — fmtRub со знаком; Δ% — `pct===null ? "—" : (pct>0?"+":"")+pct.toFixed(1)+"%"`;
положительная дельта актива — text-emerald-600, отрицательная — text-red-600.
Строка есть только в одной из дат → сравнение 0.

Формат: `const fmtRub = (n: number) => new Intl.NumberFormat("ru-RU", { maximumFractionDigits: 0 }).format(n) + " ₽"`.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "BalanceSheetTable" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "КАПИТАЛ" components/finance/BalanceSheetTable.tsx` находит
    - `grep "computeDelta" components/finance/BalanceSheetTable.tsx` находит
    - `grep "bg-muted/" components/finance/BalanceSheetTable.tsx` НЕ находит (нет прозрачных фонов на подытогах)
    - `grep "Без оценки" components/finance/BalanceSheetTable.tsx` находит + `grep "details" находит (D-11 расшифровка)
    - `grep "ru-RU" components/finance/BalanceSheetTable.tsx` находит (формат тысяч)
    - `grep "\"use client\"" components/finance/BalanceSheetTable.tsx` НЕ находит (server component)
  </acceptance_criteria>
  <done>Таблица рендерит полный баланс с подытогами, капиталом, дельтами и D-11 блоком.</done>
</task>

<task type="auto">
  <name>Task 2: BalanceDatePicker (URL-driven) + сборка page.tsx</name>
  <read_first>
    - app/(dashboard)/finance/balance/page.tsx (текущий стаб из 24-01 — заменяем содержимое)
    - components/finance/FinanceTabs.tsx (сохраняем в layout страницы)
    - lib/date-periods.ts (startOfMonthMsk)
    - app/(dashboard)/support/stats/search-params.ts (паттерн парсинга searchParams; Next 15 — helpers НЕ экспортировать из page.tsx)
  </read_first>
  <action>
1. `components/finance/BalanceDatePicker.tsx` — "use client":
   props `{ date: string; compare: string }` (YYYY-MM-DD). Два native `<input type="date">`
   («Дата баланса», «Сравнить с») + при onChange — `router.push(\`/finance/balance?date=${d}&compare=${c}\`)`
   через useRouter (next/navigation). Никакого локального стейта кроме controlled values.

2. Переписать `app/(dashboard)/finance/balance/page.tsx`:
```tsx
import { requireSection } from "@/lib/rbac"
import { FinanceTabs } from "@/components/finance/FinanceTabs"
import { BalanceDatePicker } from "@/components/finance/BalanceDatePicker"
import { BalanceSheetTable } from "@/components/finance/BalanceSheetTable"
import { loadBalanceSheet } from "@/lib/balance-data"
// M5: compare-дату формируем как YYYY-MM-01 из строки (startOfMonthMsk().toISOString() даёт off-by-one)
```
- `await requireSection("FINANCE")`.
- `const sp = await searchParams` (тип Promise<{date?:string;compare?:string}>).
- date: валидный YYYY-MM-DD из sp.date или дефолт «вчера МСК»
  (`new Date(Date.now()+3*3600_000-24*3600_000).toISOString().split("T")[0]`).
- compare: sp.compare или `date.slice(0,7) + "-01"` (D-09 default — начало месяца выбранной даты;
  ⚠ M5 — НЕ `startOfMonthMsk(dateObj).toISOString()` — тот из-за UTC-сдвига даёт 30-е число прошлого
  месяца, до начала истории; если date = 1-е число, compare = сама date). Не опускаться ниже
  historyStart (01.07.2026): если compare < "2026-07-01" → compare = "2026-07-01".
- Парс объектов: `new Date(\`${s}T00:00:00+03:00\`)` → в loadBalanceSheet передавать НОРМАЛИЗОВАННУЮ
  дату снапшота `new Date(s)` (полночь UTC, консистентно с @db.Date записями cron 24-06).
- Параллельно: `const [current, compareSheet] = await Promise.all([loadBalanceSheet(...), loadBalanceSheet(...)])`.
- Рендер: FinanceTabs → строка заголовка с BalanceDatePicker (справа, `ml-auto`; зона для кнопок
  24-08 — оставить flex-контейнер `<div className="flex items-center gap-2">` с комментарием
  `{/* 24-08: RecalcButton / Adjustments / TaxRates здесь */}`) → BalanceSheetTable.
- Лейблы дат: `new Intl.DateTimeFormat("ru-RU").format(...)`.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "finance/balance|BalanceDatePicker" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "loadBalanceSheet" "app/(dashboard)/finance/balance/page.tsx"` → 2 вызова (или Promise.all с двумя)
    - `grep -E "slice\(0, ?7\)" "app/(dashboard)/finance/balance/page.tsx"` находит compare-default YYYY-MM-01 (M5)
    - `grep "startOfMonthMsk(dateObj).toISOString" "app/(dashboard)/finance/balance/page.tsx"` НЕ находит (M5 — off-by-one устранён)
    - `grep "ComingSoon" "app/(dashboard)/finance/balance/page.tsx"` НЕ находит (стаб заменён)
    - `grep "requireSection(\"FINANCE\")" "app/(dashboard)/finance/balance/page.tsx"` находит
    - `grep "type=\"date\"" components/finance/BalanceDatePicker.tsx` → 2 native инпута
    - `grep "router.push" components/finance/BalanceDatePicker.tsx` находит (URL-driven)
    - `npx tsc --noEmit` — нет ошибок в изменённых файлах
  </acceptance_criteria>
  <done>Отчёт открывается с двумя датами и дельтами; даты shareable через URL; зона для 24-08 готова.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` чисто; `npm run test` — регрессии нет
- /finance/balance рендерит Активы→Пассивы→Капитал, дельты, «Без оценки»
</verification>

<success_criteria>
- Вертикальный баланс с подытогами и балансирующим Капиталом (D-06)
- Две даты + дельта ₽/% (D-09), default сравнение — начало месяца
- «Без оценки: N товаров, M шт» с расшифровкой (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-07-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ.
</output>
