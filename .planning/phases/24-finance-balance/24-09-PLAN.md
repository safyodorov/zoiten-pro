---
phase: 24-finance-balance
plan: 09
type: execute
wave: 5
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05", "24-06", "24-07", "24-08"]
files_modified:
  - scripts/bootstrap-balance-snapshot.ts
autonomous: false
requirements: [FIN-BAL-15]
user_setup: []
must_haves:
  truths:
    - "bootstrap-скрипт создаёт снапшот на 01.07.2026: WB-остатки ТОЧНО из STOCK_HISTORY_DAILY_CSV (история до 3 мес), fallback — ближайшие данные с пометкой"
    - "Ретро-статьи (банк/касса/кредиты/авансы/налоги) на 01.07 считаются live — bootstrap их не трогает"
    - "Деплой ОТЛОЖЕН до окончания параллельной разработки пользователя (checkpoint фиксирует последовательность)"
    - "UAT-чеклист покрывает миграцию, токен, cron, дебиторку, баланс на 01.07"
  artifacts:
    - path: "scripts/bootstrap-balance-snapshot.ts"
      provides: "однократный bootstrap снапшота на 01.07 + сегодняшнего"
      contains: "STOCK_HISTORY_DAILY_CSV"
  key_links:
    - from: "scripts/bootstrap-balance-snapshot.ts"
      to: "lib/finance-snapshot.ts"
      via: "переиспользование computeStockSnapshotRows/runFinanceSnapshot"
      pattern: "finance-snapshot"
    - from: "deploy.sh (VPS)"
      to: "prisma/migrations/20260702_phase24_finance"
      via: "prisma migrate deploy"
      pattern: "phase24_finance"
---

<objective>
Финальный план: (1) однократный bootstrap-скрипт «снапшот на 01.07.2026» — WB-остатки ТОЧНО
через отчёт `STOCK_HISTORY_DAILY_CSV` (официально: остатки по дням до 3 месяцев назад, токен
«Аналитика»; endpoint подтвердить Wave 0-проверкой), Иваново/в-пути — из ближайших данных с
пометкой; (2) checkpoint деплоя: миграция + WB_FINANCE_TOKEN + smoke-curl Balance API + bootstrap
+ RBAC + UAT-чеклист.

⚠ **ДЕПЛОЙ ОТЛОЖЕН**: пользователь запретил коммитить/деплоить артефакты Phase 24 до окончания
параллельной разработки (указание 2026-07-02). Скрипт пишется сейчас; checkpoint исполняется
ТОЛЬКО после снятия запрета (пользователь скажет «можно деплоить»).

Purpose: Баланс на 01.07 — точка отсчёта всей отчётности (D-03). Без bootstrap первая дата
появится только «завтра после деплоя».
Output: scripts/bootstrap-balance-snapshot.ts + выполненный (позже) деплой-чеклист + UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-CONTEXT.md
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Bootstrap-механика + deploy-правила. -->

STOCK_HISTORY_DAILY_CSV (RESEARCH §«Официальная документация», HIGH):
- Официально: «остатки по дням за период до 3 месяцев от текущей даты — методы Аналитика
  продавца CSV, тип отчёта STOCK_HISTORY_DAILY_CSV», токен категории «Аналитика» (есть в WB_API_TOKEN).
- ⚠ Точный endpoint/параметры НЕ ПОДТВЕРЖДЕНЫ. Механизм CSV-заданий в проекте:
  lib/wb-api.ts:fetchBuyoutPercent (строки 273-385): POST
  `seller-analytics-api.wildberries.ru/api/v2/nm-report/downloads` (создать задание с id/reportType/params)
  → GET `...?downloadId=` (poll статуса) → GET `.../file/{id}` (ZIP c CSV). Для STOCK_HISTORY_DAILY_CSV
  ожидаемо тот же downloads-механизм с reportType="STOCK_HISTORY_DAILY_CSV" — ПРОВЕРИТЬ curl'ом
  (Wave 0 внутри checkpoint, т.к. прод/API до снятия запрета не трогаем).
- ⚠ Daily-cap Analytics: 3 отчёта/день, AppSetting('wbAnalyticsDailyCounter') (lib/wb-api.ts:16-45)
  — bootstrap потратит 1; не запускать в день, когда исчерпан.

Fallback (CONTEXT specifics): если STOCK_HISTORY_DAILY_CSV недоступен/не парсится — снапшот на
01.07 из ближайших текущих данных (computeStockSnapshotRows на «сейчас») с console.warn пометкой
«приближение». Иваново (Product.ivanovoStock) и «в пути из Китая» — истории нет в любом случае:
Иваново берётся текущим значением с пометкой; «в пути из Китая» — live-статья (D-12), bootstrap не нужен.
Дебиторка на 01.07 — Balance API отдаёт только «сейчас» → FinanceReceivablesSnapshot на 01.07
НЕ создаётся (строка в балансе покажет «нет снапшота на дату», честно).
Ретро-статьи (банк/касса/кредиты/авансы/налоги) на 01.07 live — точно, bootstrap не касается.

Переиспользование: lib/finance-snapshot.ts (24-06) — computeStockSnapshotRows (pure) для fallback
и как маппер qty→строки; себестоимость costPriceAtDate = ТЕКУЩАЯ ProductCost (D-10 — истории
себестоимости нет, это ожидаемое приближение, кнопка D-04 переоценит).

Скрипт-паттерн: scripts/import-cash-budget.ts / scripts/recompute-production.ts — standalone tsx,
собственный `new PrismaClient()`, запуск на VPS: `set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/bootstrap-balance-snapshot.ts`.

Deploy-правила (CLAUDE.md, ОБЯЗАТЕЛЬНО): коммит → push origin main → деплой ТОЛЬКО nohup:
`ssh root@85.198.97.89 "cd /opt/zoiten-pro && nohup bash deploy.sh > /var/log/zoiten-deploy.log 2>&1 &"`
→ следить лог до `==> Done` → `curl https://zoiten.pro` = 200. Перед деплоем `df -h /` ≥ 5GB.
deploy.sh выполняет prisma migrate deploy → применит 20260702_phase24_finance.

RBAC provision (паттерн 23-05): после деплоя выдать UserSectionRole(FINANCE, MANAGE) нужным
пользователям через /admin/users тумблер (появится из SECTION_OPTIONS) — спросить пользователя кому.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/bootstrap-balance-snapshot.ts (01.07 из истории CSV + fallback)</name>
  <read_first>
    - lib/finance-snapshot.ts (computeStockSnapshotRows, runFinanceSnapshot — переиспользовать)
    - lib/wb-api.ts (строки 16-45 daily-cap counter; 273-385 fetchBuyoutPercent — CSV downloads механизм: create→poll→ZIP)
    - scripts/recompute-production.ts или scripts/import-cash-budget.ts (standalone скрипт-паттерн, свой PrismaClient)
    - .planning/phases/24-finance-balance/24-RESEARCH.md (§«История остатков WB» — что подтверждено, что нет)
  </read_first>
  <action>
Создать `scripts/bootstrap-balance-snapshot.ts` (standalone tsx, свой `new PrismaClient()`):

CLI: `npx tsx scripts/bootstrap-balance-snapshot.ts [--date=2026-07-01] [--mode=csv|fallback]`
(дефолт date=2026-07-01, mode=csv).

**Режим csv (точный, D-03):**
1. Создать CSV-задание по механизму fetchBuyoutPercent (downloads API), но с
   `reportType: "STOCK_HISTORY_DAILY_CSV"` и params периода `{ startDate: date, endDate: date }`
   (точная форма params — уточняется по фактическому ответу API; ⚠ endpoint не подтверждён —
   при 400/404 вывести полный ответ WB и завершиться с кодом 2 → оператор переключается на fallback).
   Учесть daily-cap: перед созданием проверить AppSetting wbAnalyticsDailyCounter (< 3), инкрементировать.
2. Poll статуса → скачать ZIP → распаковать CSV (механизм/распаковка — как в fetchBuyoutPercent).
3. Распарсить строки: ожидаемые колонки nmId/размер/склад/дата/остаток (реальные заголовки
   залогировать и замапить по факту; агрегировать qty по nmId на дату).
4. Смаппить nmId→Product через MarketplaceArticle (паттерн stock-data: marketplace wb + parseInt);
   собрать строки WB_WAREHOUSE per product. inWayToClient/inWayFromClient истории нет →
   взять текущие WbCard.inWayToClient/inWayFromClient с console.warn «в пути — приближение (текущие значения)».
5. IVANOVO: текущий Product.ivanovoStock, console.warn «Иваново — приближение».
6. costPriceAtDate = текущая ProductCost.costPrice (console.warn «себестоимость текущая, D-04 переоценит»).
7. Запись: deleteMany({date}) + createMany (идемпотентно, как в runFinanceSnapshot).

**Режим fallback (приближение):** взять ТЕКУЩИЕ данные — повторить выборку из runFinanceSnapshot
(products+wbCards) → computeStockSnapshotRows → записать на --date c console.warn
«BOOTSTRAP-ПРИБЛИЖЕНИЕ: остатки на {date} = текущее состояние».

**Оба режима:** FinanceReceivablesSnapshot на прошлую дату НЕ создавать (Balance API — только
«сейчас»); в конце вывести summary: {date, mode, stockRows, warnings[]}.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "bootstrap-balance" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "STOCK_HISTORY_DAILY_CSV" scripts/bootstrap-balance-snapshot.ts` находит
    - `grep -E "mode=csv\|fallback|--mode" scripts/bootstrap-balance-snapshot.ts` находит оба режима
    - `grep "wbAnalyticsDailyCounter" scripts/bootstrap-balance-snapshot.ts` находит (daily-cap 3/день учтён)
    - `grep "financeReceivablesSnapshot" scripts/bootstrap-balance-snapshot.ts` НЕ находит создание за прошлую дату (или явный комментарий, что не создаётся)
    - `grep "deleteMany" scripts/bootstrap-balance-snapshot.ts` находит (идемпотентность)
    - `npx tsc --noEmit` — нет ошибок в скрипте
  </acceptance_criteria>
  <done>Bootstrap-скрипт готов к прогону на VPS в обоих режимах; идемпотентен; cap учтён.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — отложенный деплой + Wave 0 CSV-проверка + bootstrap + UAT</name>
  <what-built>
    Вся Phase 24: схема+миграция+раздел (24-01), balance-math (24-02), WB Finance клиент+токен
    (24-03), point-in-time хелперы (24-04), loadBalanceSheet (24-05), cron-снапшот 06:00 (24-06),
    UI отчёта (24-07), управление (24-08), bootstrap-скрипт (Task 1).
  </what-built>
  <how-to-verify>
    ⚠ **ЗАПУСКАТЬ ТОЛЬКО ПОСЛЕ снятия запрета на коммит/деплой** (параллельная разработка;
    пользователь явно скажет «можно»). До этого — план ждёт.

    Автоматизирует агент (по команде пользователя):
    1. `git fetch origin && git status -sb` (правило CLAUDE.md — проверить divergence), затем
       коммит фазы + `git push origin main`.
    2. `ssh root@85.198.97.89 "df -h /"` — ≥5GB; деплой ТОЛЬКО nohup:
       `ssh root@85.198.97.89 "cd /opt/zoiten-pro && nohup bash deploy.sh > /var/log/zoiten-deploy.log 2>&1 &"`
       → лог до `==> Done` → `curl -s -o /dev/null -w "%{http_code}" https://zoiten.pro` = 200.
       (deploy.sh применит миграцию 20260702_phase24_finance.)
    3. Токен: добавить WB_FINANCE_TOKEN (из checkpoint 24-03) через /admin/settings → WB токены
       (probe валидация) ИЛИ в /etc/zoiten.pro.env + restart.
    4. Smoke-curl с VPS: `curl -H "Authorization: $WB_FINANCE_TOKEN" https://finance-api.wildberries.ru/api/v1/account/balance`
       → JSON {currency, current, for_withdraw}. При 401/403 — токен/scope; при 402 — оплата WB API.
    5. **Wave 0 CSV**: пробное задание STOCK_HISTORY_DAILY_CSV (запуск bootstrap в режиме csv).
       Если endpoint отвечает 400/404 → перезапуск `--mode=fallback` (приближение допустимо per CONTEXT).
    6. Bootstrap: `set -a; . /etc/zoiten.pro.env; set +a; npx tsx scripts/bootstrap-balance-snapshot.ts --date=2026-07-01`
       → summary stockRows > 0. Затем прогнать текущий снапшот: `curl -H "x-cron-secret: $CRON_SECRET" https://zoiten.pro/api/cron/finance-snapshot`.
    7. RBAC: спросить пользователя, кому выдать FINANCE (MANAGE/VIEW), проставить в /admin/users.

    Пользователь проверяет вручную (UAT):
    1. /finance/balance открывается: Активы → Пассивы → Капитал; табы ОДДС/ОПиУ — заглушки.
    2. Баланс на 01.07.2026: выбрать дату 2026-07-01 — запасы из bootstrap; банк/касса/кредиты/
       авансы/налоги — live-значения; сверить ключевые статьи с бухгалтерией/выписками.
    3. Дебиторка WB на сегодня ≈ цифре в ЛК WB (current + хвост недели).
    4. Дельта: date=сегодня, compare=01.07 → колонки Δ₽/Δ% заполнены.
    5. «Без оценки»: если есть товары без себестоимости — строка N товаров/M шт + расшифровка.
    6. «Пересчитать дату» → costPrice обновились, qty не изменились.
    7. Ручные статьи: добавить «Займы выданные» (актив) → появились в балансе; удалить → исчезли с даты.
    8. Налоги: изменить ставку → расчёт изменился; внести факт за закрытый квартал → цифра заменилась.
    9. На следующее утро (после 06:00 МСК): новый снапшот записался
       (`SELECT count(*) FROM "FinanceStockSnapshot" WHERE date = CURRENT_DATE - 1` > 0).
  </how-to-verify>
  <acceptance_criteria>
    - Деплой прошёл по правилам CLAUDE.md (push → nohup → Done → curl 200); миграция применена
    - Smoke-curl Balance API вернул валидный JSON (числа current/for_withdraw)
    - Bootstrap создал FinanceStockSnapshot на 2026-07-01 (mode csv или задокументированный fallback)
    - Cron-снапшот пишется ежедневно (проверка утром следующего дня)
    - UserSectionRole(FINANCE) выдан назначенным пользователям
    - UAT-пункты 1-8 подтверждены пользователем
  </acceptance_criteria>
  <resume-signal>Напишите «approved» или опишите проблемы (расхождения статей с бухгалтерией, CSV endpoint, дебиторка ≠ ЛК WB, cron не сработал).</resume-signal>
</task>

</tasks>

<verification>
- Bootstrap-скрипт компилируется и идемпотентен (оба режима)
- Checkpoint исполняется только после снятия запрета на деплой
- UAT-чеклист покрывает все D-решения, видимые пользователю
</verification>

<success_criteria>
- Баланс на 01.07.2026 существует (точка отсчёта D-03), WB-остатки — точные (CSV) или явное приближение
- Прод: раздел развёрнут, cron пишет снапшоты, дебиторка живая, RBAC выдан
- Phase 24 complete после «approved»
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-09-SUMMARY.md`.
Phase 24 завершена — обновить STATE.md и чекбоксы ROADMAP (когда коммиты снова разрешены).
</output>
