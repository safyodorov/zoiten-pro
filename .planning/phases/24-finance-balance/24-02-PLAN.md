---
phase: 24-finance-balance
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/balance-math.ts
  - tests/balance-math.test.ts
autonomous: true
requirements: [FIN-BAL-05]
must_haves:
  truths:
    - "computeTaxLiability = accruedTotal − taxesPaidTotal (вычитание платежей ЕДИНОЖДЫ, B3); computeQuarterAccrual = (НДС%+налог%)×база_квартала БЕЗ вычитания"
    - "computeCapital возвращает Активы − Пассивы (балансирующая строка)"
    - "computeDelta возвращает abs и pct с guard на деление на ноль (compare===0 → pct=null)"
    - "Все функции pure, без импортов Prisma/Next — работают на сервере и клиенте"
  artifacts:
    - path: "lib/balance-math.ts"
      provides: "pure-функции налогового обязательства, капитала, дельты"
      exports: ["computeQuarterAccrual", "computeTaxLiability", "computeCapital", "computeDelta"]
      min_lines: 40
    - path: "tests/balance-math.test.ts"
      provides: "golden-тесты D-16/D-06/D-09"
      contains: "computeTaxLiability"
  key_links:
    - from: "tests/balance-math.test.ts"
      to: "lib/balance-math.ts"
      via: "import { computeTaxLiability, computeCapital, computeDelta }"
      pattern: "from \"@/lib/balance-math\""
---

<objective>
Pure-модуль `lib/balance-math.ts` — единственный источник истины для расчётов баланса:
начисление налога за квартал + налоговое обязательство (D-16), капитал = Активы−Пассивы (D-06),
дельта ₽/% между двумя датами (D-09). Полностью детерминированный, без зависимостей (паттерн `lib/loan-math.ts` /
`lib/pricing-math.ts`) → используется и в RSC-агрегаторе (сервер), и в realtime-пересчёте (клиент).

Purpose: Изолировать самую хрупкую математику фазы в тестируемый модуль. Wave 0 golden-test
requirement из 24-VALIDATION.md.
Output: lib/balance-math.ts + tests/balance-math.test.ts (green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Сигнатуры выведены из RESEARCH «Code Examples» + Pattern 4. -->

Образец pure-модуля с round2 (lib/loan-math.ts:38):
`export function round2(n: number): number { return Math.round(n * 100) / 100 }`

Образец golden-теста (tests/pricing-math.test.ts): describe/it + expect(...).toBeCloseTo(x, 2).

Целевые сигнатуры:
```ts
// D-16 начисление за ОДИН квартал — БЕЗ вычитания уплаченного (вычитание — глобально, ниже).
export function computeQuarterAccrual(buyoutsSumRub: number, vatPct: number, incomeTaxPct: number): number
// = round2((vatPct + incomeTaxPct) / 100 * buyoutsSumRub)

// B3/M4: обязательство = Σ начислений по кварталам (факт D-17 перекрывает начисление квартала)
//        − Σ ВСЕ уплаченные налоги [taxWindowStart, asOf]. Вычитание платежей — ЕДИНОЖДЫ,
//        вне ветвления факт/расчёт (иначе платежи внутри факт-квартала теряются → завышение, B3).
export interface TaxLiabilityInputs {
  accruedTotal: number     // Σ по кварталам [FinanceTaxPeriodActual факт ?? computeQuarterAccrual(base_q,…)]
  taxesPaidTotal: number   // Σ ВСЕ BankTransaction(TAX)+CashEntry('Налоги/банк/сборы') за [taxWindowStart, asOf]
}
export function computeTaxLiability(inputs: TaxLiabilityInputs): number
// = round2(inputs.accruedTotal − inputs.taxesPaidTotal)

export function computeCapital(totalAssets: number, totalLiabilities: number): number
// = round2(totalAssets − totalLiabilities)

export interface Delta { abs: number; pct: number | null }
export function computeDelta(current: number, compare: number): Delta
// abs = round2(current − compare); pct = compare === 0 ? null : round2((abs / Math.abs(compare)) * 100)
```
</interfaces>
</context>

<feature>
  <name>Balance pure math (D-16, D-06, D-09)</name>
  <files>lib/balance-math.ts, tests/balance-math.test.ts</files>
  <behavior>
    computeQuarterAccrual (D-16 начисление, БЕЗ вычитания уплаченного):
    - buyouts=1_000_000, vat=7, incomeTax=1 → 80_000
    - buyouts=0 → 0
    computeTaxLiability (accruedTotal − taxesPaidTotal, вычитание ЕДИНОЖДЫ):
    - Кейс 1 (golden): accruedTotal=90_000 (факт закрытого Q 50_000 + начисление текущего Q 40_000), taxesPaidTotal=30_000 → 60_000
    - Кейс 2 (нулевая база): accruedTotal=0, taxesPaidTotal=0 → 0
    - Кейс 3 (переплата уводит в минус): accruedTotal=8_000, taxesPaidTotal=50_000 → −42_000 (допустимо отрицательное)
    - Кейс 4 (B3 — факт за Q + платёж ВНУТРИ факт-квартала): accruedTotal=50_000 (факт закрытого Q),
      taxesPaidTotal=20_000 (платёж датирован внутри того же факт-квартала) → 30_000, НЕ 50_000
      (платёж вычтен глобально, не потерян в факт-ветке — обязательство не завышено)
    computeCapital:
    - assets=1000, liabilities=400 → 600
    - assets=400, liabilities=1000 → −600 (капитал может быть отрицательным)
    computeDelta:
    - current=150, compare=100 → { abs:50, pct:50 }
    - current=80, compare=100 → { abs:−20, pct:−20 }
    - current=50, compare=0 → { abs:50, pct:null } (guard деления на ноль)
    - compare отрицательное: current=−50, compare=−100 → { abs:50, pct:50 } (pct по |compare|)
  </behavior>
  <implementation>
    Скопировать round2 из loan-math (или импортировать — но модуль должен остаться pure;
    проще локально объявить round2 чтобы не тянуть loan-math). Реализовать 4 функции
    (computeQuarterAccrual, computeTaxLiability, computeCapital, computeDelta) ровно по формулам
    из interfaces. Округление до копеек round2 применять к финальным результатам.
    ⚠ computeTaxLiability вычитает платежи ЕДИНОЖДЫ (accruedTotal − taxesPaidTotal); вычитание
    внутри пер-квартального начисления запрещено (иначе платежи в факт-квартале теряются, B3).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): golden-тесты balance-math</name>
  <read_first>
    - tests/pricing-math.test.ts (структура golden-теста, toBeCloseTo)
    - .planning/phases/24-finance-balance/24-RESEARCH.md (Code Examples — computeTaxLiability формула)
  </read_first>
  <action>
Создать `tests/balance-math.test.ts` с импортом `import { computeQuarterAccrual, computeTaxLiability, computeCapital, computeDelta, type TaxLiabilityInputs } from "@/lib/balance-math"`.
Написать describe-блоки для всех кейсов из `<behavior>` выше (2 кейса quarterAccrual, 4 кейса
taxLiability включая B3-кейс «факт + платёж внутри квартала», 2 капитала, 4 дельты). Использовать
`expect(...).toBeCloseTo(value, 2)` для чисел и точное сравнение для `pct: null`. Тест должен
ПАДАТЬ (RED) — модуля ещё нет.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx vitest run tests/balance-math.test.ts 2>&1 | grep -qE "Cannot find module|failed|error" && echo "RED confirmed" || echo "RED expected"</automated>
  </verify>
  <acceptance_criteria>
    - Файл tests/balance-math.test.ts существует, импортирует из `@/lib/balance-math`
    - Содержит ≥ 12 `it(...)` кейсов (2 accrual + 4 tax + 2 capital + 4 delta)
    - `grep "toBeCloseTo(60000" tests/balance-math.test.ts` находит golden-кейс
    - `grep -E "toBeCloseTo\(30000" tests/balance-math.test.ts` находит B3-кейс (факт+платёж внутри квартала → не завышено)
    - `grep "null" tests/balance-math.test.ts` находит проверку guard деления на ноль
    - Запуск теста ПАДАЕТ (модуль ещё не реализован) — RED
  </acceptance_criteria>
  <done>Тесты описывают ожидаемое поведение и красные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): реализация lib/balance-math.ts</name>
  <read_first>
    - tests/balance-math.test.ts (созданный в Task 1 — контракт)
    - lib/loan-math.ts (образец round2 и стиля pure-модуля)
  </read_first>
  <action>
Создать `lib/balance-math.ts` без импортов Prisma/Next. Объявить локальный
`function round2(n: number): number { return Math.round(n * 100) / 100 }`. Реализовать
`computeQuarterAccrual`, `TaxLiabilityInputs`, `computeTaxLiability`, `computeCapital`, `Delta`,
`computeDelta` ровно по формулам из `<context><interfaces>`:
- computeQuarterAccrual: `round2((vatPct + incomeTaxPct)/100 * buyoutsSumRub)` (БЕЗ вычитания платежей)
- computeTaxLiability: `round2(inputs.accruedTotal - inputs.taxesPaidTotal)` (вычитание платежей ЕДИНОЖДЫ, B3)
- computeCapital: `round2(totalAssets - totalLiabilities)`
- computeDelta: `abs = round2(current - compare); pct = compare === 0 ? null : round2((abs / Math.abs(compare)) * 100)`
Прогнать тест до GREEN.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx vitest run tests/balance-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/balance-math.test.ts` — все тесты green
    - `grep -E "export function (computeQuarterAccrual|computeTaxLiability|computeCapital|computeDelta)" lib/balance-math.ts` → 4 совпадения
    - `grep "accruedTotal" lib/balance-math.ts` находит вычитание ЕДИНОЖДЫ (accruedTotal − taxesPaidTotal, B3)
    - `grep -E "import .* from \"@?prisma|next\"" lib/balance-math.ts` НЕ находит (модуль pure)
    - computeDelta(50, 0).pct === null (проверено тестом)
  </acceptance_criteria>
  <done>lib/balance-math.ts реализован, golden-тесты зелёные, модуль pure.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/balance-math.test.ts` — green
- lib/balance-math.ts экспортирует 3 функции, без Prisma/Next импортов
</verification>

<success_criteria>
- Налоговое обязательство (D-16), капитал (D-06), дельта (D-09) покрыты golden-тестами
- Модуль готов к переиспользованию в lib/balance-data.ts (Plan 24-05) и в клиентском пересчёте
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-02-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ.
</output>
