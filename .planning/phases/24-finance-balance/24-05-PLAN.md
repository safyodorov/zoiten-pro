---
phase: 24-finance-balance
plan: 05
type: execute
wave: 2
depends_on: ["24-01", "24-02", "24-04"]
files_modified:
  - lib/balance-data.ts
  - tests/balance-sheet.test.ts
autonomous: true
requirements: [FIN-BAL-08]
must_haves:
  truths:
    - "loadBalanceSheet(asOf) собирает Активы/Пассивы/Капитал на дату из всех источников"
    - "Денежные средства = Σ getBankBalanceAsOf(RUR-счета) + баланс кассы (INCOME−EXPENSE date<=X)"
    - "Кредиты = Σ computeLoanAggregates(...,asOf).currentBalance; ручные статьи по effectiveFrom<=X"
    - "Запасы = Σ FinanceStockSnapshot.valueRub по локациям; «без оценки» = отдельная строка (D-11)"
    - "Авансы/в пути из Китая = PAID-платежи с paidDate<=asOf по курсу на дату платежа; классификация по stageAsOf (WAREHOUSE исключён — уже в запасах, B2), НЕ по статусу (D-12)"
    - "Налоговое обязательство = Σ начислений по кварталам [factD-17 ?? computeQuarterAccrual] − Σ ВСЕ уплаченные налоги (вычитание ЕДИНОЖДЫ, B3); нижняя граница taxCalcStartQuarter (M4)"
    - "Капитал = computeCapital(Активы, Пассивы)"
  artifacts:
    - path: "lib/balance-data.ts"
      provides: "loadBalanceSheet агрегатор + типы BalanceSheet"
      exports: ["loadBalanceSheet", "BalanceSheet"]
      min_lines: 180
    - path: "tests/balance-sheet.test.ts"
      provides: "assembly-тест: капитал = активы − пассивы, строки статей"
      contains: "loadBalanceSheet"
  key_links:
    - from: "lib/balance-data.ts loadBalanceSheet"
      to: "lib/balance-math.ts computeTaxLiability/computeCapital"
      via: "import + вызов при сборке налогов и капитала"
      pattern: "computeTaxLiability"
    - from: "lib/balance-data.ts"
      to: "FinanceStockSnapshot / FinanceReceivablesSnapshot"
      via: "prisma.financeStockSnapshot.findMany({date}), prisma.financeReceivablesSnapshot.findUnique({date})"
      pattern: "financeStockSnapshot"
    - from: "lib/balance-data.ts"
      to: "lib/loan-math.ts computeLoanAggregates"
      via: "остаток кредитов на asOf"
      pattern: "computeLoanAggregates"
---

<objective>
`loadBalanceSheet(asOf)` — RSC-агрегатор, собирающий полный баланс на дату из всех источников
(D-05..D-16). Ретро-вычислимые статьи (банк/касса/кредиты/авансы/налоги) — на лету через
`date <= asOf`; снапшот-статьи (запасы, дебиторка WB) — из таблиц Plan 24-01, записанных cron'ом
(Plan 24-06). Использует хелперы Plan 24-04 (getBankBalanceAsOf/getRateForDate/stageAsOf) и
math Plan 24-02 (computeTaxLiability/computeCapital). Расширяет тот же файл `lib/balance-data.ts`.

Purpose: Ядро фазы — единственная функция сборки баланса, потребляется UI (24-07) и recalc (24-08).
Output: loadBalanceSheet + типы BalanceSheet в lib/balance-data.ts + assembly-тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-finance-balance/24-CONTEXT.md
@.planning/phases/24-finance-balance/24-RESEARCH.md

<interfaces>
<!-- Существующие сигнатуры + целевой тип. Executor не должен искать. -->

Хелперы из Plan 24-04 (уже в lib/balance-data.ts — переиспользовать):
getBankBalanceAsOf(accountId, asOf), getRateForDate(code, asOf)→{rateToRub,approximate}, stageAsOf(stages, asOf).
Math из Plan 24-02: computeQuarterAccrual(buyouts, vatPct, incomeTaxPct), computeTaxLiability({accruedTotal, taxesPaidTotal}), computeCapital(assets, liabilities).
computeLoanAggregates(amount, payments, asOf) (lib/loan-math.ts:130) → .currentBalance (передавать asOf!).
startOfQuarterMsk(asOf) (lib/date-periods.ts:41), currentStageOf (lib/purchase-stages.ts:65).

Prisma-модели (schema.prisma):
- BankAccount 1677: currency ("RUR"/"CNY"), closingBalance, balanceDate.
- CashEntry 1769: direction (INCOME/EXPENSE), amount, date, category(CashCategory.name).
- Loan 1277 / LoanPayment 1301: amount, payments[{date,principal,interest}], deletedAt.
- FinanceStockSnapshot (24-01): date, productId, sku, name, location(enum), qty, costPriceAtDate?, valueRub?.
- FinanceReceivablesSnapshot (24-01): date @unique, totalRub, balanceCurrentRub, weeklyTailRub.
- FinanceManualAdjustment (24-01): label, type(ASSET/LIABILITY), amountRub, effectiveFrom, deletedAt.
- FinanceTaxPeriodActual (24-01): year, quarter, vatActualRub?, incomeTaxActualRub?.
- Purchase 1466 (status PLANNED/ACTIVE/COMPLETED, currency) → items[] → stages[] ; payments[] (PurchasePayment: type, amount, currency, paidDate, status PAID).
- WbCardFunnelDaily 1103: nmId, date, buyoutsSumRub.
- BankTransaction 1707: category(TxCategory.TAX), amount, date. CashEntry category name 'Налоги/банк/сборы'.
- AppSetting 614: keys finance.vatPct, finance.incomeTaxPct (сид 7/1 из 24-01).

Целевой тип:
```ts
export interface BalanceLine { key: string; label: string; amountRub: number; currency?: "RUB" | "CNY"; approximate?: boolean; note?: string }
// m4: currency==="CNY" — справочная строка, В рублёвый subtotal/total НЕ включается (как /bank v1).
export interface BalanceGroup { key: string; label: string; lines: BalanceLine[]; subtotalRub: number }
export interface BalanceSection { key: "assets" | "liabilities"; label: string; groups: BalanceGroup[]; totalRub: number }
export interface UnvaluedStock { productCount: number; qtySum: number; products: Array<{ sku: string; name: string; qty: number }> }
export interface BalanceSheet {
  date: Date
  assets: BalanceSection
  liabilities: BalanceSection
  capitalRub: number
  unvaluedStock: UnvaluedStock   // D-11
}
export async function loadBalanceSheet(asOf: Date): Promise<BalanceSheet>
```

Локации запасов → метки строк:
WB_WAREHOUSE→"Склады WB", WB_IN_WAY_TO_CLIENT→"WB в пути к клиенту",
WB_IN_WAY_FROM_CLIENT→"WB в пути от клиента", IVANOVO→"Склад Иваново".
+ отдельная строка "Товар в пути из Китая" (из закупок, Task 3, НЕ из снапшота).

⚠ Pitfall 2 + m4 (CNY): в рублёвый subtotal/total включать ТОЛЬКО строки без currency или currency==="RUB".
CNY-остатки банка — отдельная СПРАВОЧНАЯ строка `{ currency:"CNY", amountRub: <сумма CNY>, note:"справочно, не в итоге" }`,
НЕ конвертировать и НЕ суммировать в рублёвые подытоги/капитал. subtotalRub/totalRub считать только по строкам currency!=="CNY".
⚠ Pitfall 6: дебиторка НЕ дёргает API — только читает FinanceReceivablesSnapshot за asOf.
⚠ КОНВЕНЦИЯ ДАТЫ (m6 — единая во всех планах 24-05/24-06/24-09): `asOf` и `FinanceStockSnapshot.date`
  обозначают состояние на КОНЕЦ дня этой календарной даты. loadBalanceSheet(asOf): снапшот
  `date = asOf` + live-статьи `date <= asOf` — обе представляют конец дня asOf (внутренне
  согласованы, дрейфа при сверке нет). Cron D-02 «утром за вчера» пишет ВЧЕРАШНЮЮ дату (= конец
  вчера). Bootstrap на 01.07 = конец дня 01.07. ⚠ Для сверки с бухгалтерией, называющей «баланс на
  01.07» состоянием на конец 30.06 (D-02), выбирайте в UI дату 30.06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Типы BalanceSheet + денежные средства + кредиты + ручные статьи</name>
  <read_first>
    - lib/balance-data.ts (текущее содержимое — хелперы из 24-04; расширяем)
    - lib/credits-data.ts (образец loadX + computeLoanAggregates(...,asOf) с deletedAt:null)
    - app/(dashboard)/cash/page.tsx (155-164 — баланс кассы Σ INCOME − Σ EXPENSE)
    - lib/loan-math.ts (computeLoanAggregates сигнатура)
  </read_first>
  <action>
В `lib/balance-data.ts` добавить типы `BalanceLine/BalanceGroup/BalanceSection/UnvaluedStock/BalanceSheet`
(verbatim из interfaces) и начать `loadBalanceSheet(asOf)`:
- **Банк** (актив, группа «Денежные средства»): загрузить все BankAccount (select id, currency);
  для каждого currency==="RUR" — `getBankBalanceAsOf(acc.id, asOf)`; Σ ненулевых → строка
  «Банковские счета (₽)». CNY-счета (currency==="CNY") — отдельная СПРАВОЧНАЯ строка с
  `currency:"CNY"` и `note` (m4: НЕ в рублёвый subtotal/капитал; не конвертировать, Pitfall 2).
- **Касса** (актив, та же группа): `prisma.cashEntry.groupBy` или findMany where date<=asOf;
  balance = Σ(INCOME.amount) − Σ(EXPENSE.amount) → строка «Касса».
- **Кредиты** (пассив, группа «Кредиты и займы», M3 — point-in-time!):
  `loan.findMany({ include: { payments } })` (НЕ фильтровать deletedAt в запросе — фильтр по дате ниже).
  Для каждого кредита:
  - `issued = loan.issueDate ?? loan.createdAt` (fallback, issueDate nullable). Если `issued > asOf` →
    ПРОПУСТИТЬ (кредит ещё не выдан на asOf; деньги-актив в банке тоже отсутствуют — иначе тело
    попадает в пассивы прошлой даты без компенсирующего актива).
  - Мягко удалённые: если `loan.deletedAt != null && loan.deletedAt <= asOf` → ПРОПУСТИТЬ
    (удалён к asOf); если `deletedAt > asOf` → НЕ убирать ретроактивно (на ту дату ещё существовал).
  - Иначе Σ `computeLoanAggregates(Number(amount), payments, asOf).currentBalance` → строка «Остаток по кредитам».
- **Ручные статьи** (D-08): financeManualAdjustment.findMany where `effectiveFrom <= asOf` AND
  `(deletedAt = null OR deletedAt > asOf)`; type ASSET → добавить строки в активы, LIABILITY → в пассивы
  (label = adjustment.label, amountRub = Number(amountRub)).
Все Decimal → Number на границе. Пока собрать частичные секции; финальную сборку/капитал — Task 3.
Пометить TODO-маркерами `// TODO(24-05 Task2)` / `// TODO(24-05 Task3)` места запасов/дебиторки/авансов/налогов.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "balance-data" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export (interface BalanceSheet|async function loadBalanceSheet)" lib/balance-data.ts` → 2 совпадения
    - `grep "computeLoanAggregates" lib/balance-data.ts` находит с передачей asOf
    - `grep "currency === \"RUR\"" lib/balance-data.ts` находит (Pitfall 2 фильтр)
    - `grep -E "effectiveFrom.*lte|deletedAt.*gt" lib/balance-data.ts` находит (D-08 фильтр по дате)
    - `grep -E "issueDate|createdAt" lib/balance-data.ts` находит fallback выдачи (M3 — кредит не выдан на asOf → пропуск)
    - `grep "currency" lib/balance-data.ts` находит обработку CNY-строки (m4 — справочно, не в рублёвых итогах)
    - `npx tsc --noEmit` — нет ошибок в balance-data.ts
  </acceptance_criteria>
  <done>Денежные средства, кредиты, ручные статьи собираются на дату; каркас BalanceSheet готов.</done>
</task>

<task type="auto">
  <name>Task 2: Запасы (снапшот, D-10/11/13) + дебиторка WB (D-14)</name>
  <read_first>
    - lib/balance-data.ts (после Task 1)
    - .planning/phases/24-finance-balance/24-CONTEXT.md (D-10, D-11, D-13, D-14)
    - prisma/schema.prisma (FinanceStockSnapshot / FinanceReceivablesSnapshot из 24-01)
  </read_first>
  <action>
Расширить `loadBalanceSheet` (заменить TODO Task2):
- **Запасы** (актив, группа «Запасы»): `financeStockSnapshot.findMany({ where: { date: asOf } })`.
  Разбить по `location`: для каждой из WB_WAREHOUSE/WB_IN_WAY_TO_CLIENT/WB_IN_WAY_FROM_CLIENT/IVANOVO —
  Σ Number(valueRub ?? 0) → строка с меткой из маппинга (interfaces). Строки с valueRub=0/null-локацией
  всё равно показывать (0 ₽) для полноты.
- **Без оценки** (D-11): rows где `costPriceAtDate == null` → собрать `UnvaluedStock`:
  productCount = число уникальных productId, qtySum = Σ qty, products = [{sku,name,qty}] (топ по qty).
  Записать в `BalanceSheet.unvaluedStock`. НЕ включать их qty×null в сумму запасов.
- **Дебиторка WB** (D-14): `financeReceivablesSnapshot.findUnique({ where: { date: asOf } })`;
  если есть → строка «Дебиторка Wildberries» = Number(totalRub); если нет → строка 0 ₽ с
  `note: "нет снапшота на дату"` и `approximate: true`. НЕ вызывать WB API (Pitfall 6).
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx tsc --noEmit 2>&1 | grep -E "balance-data" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "financeStockSnapshot.findMany" lib/balance-data.ts` находит с `date: asOf`
    - `grep "financeReceivablesSnapshot" lib/balance-data.ts` находит (findUnique по date)
    - `grep "unvaluedStock" lib/balance-data.ts` находит присвоение (D-11)
    - `grep -E "costPriceAtDate == null|costPriceAtDate === null" lib/balance-data.ts` находит
    - `grep "reportDetailByPeriod\|fetchAccountBalance" lib/balance-data.ts` НЕ находит (агрегатор не дёргает API)
  </acceptance_criteria>
  <done>Запасы по локациям + строка «без оценки» + дебиторка из снапшота собраны.</done>
</task>

<task type="auto">
  <name>Task 3: Авансы/в пути из Китая (D-12) + налоги (D-15/16/17) + капитал + assembly-тест</name>
  <read_first>
    - lib/balance-data.ts (после Task 2)
    - lib/purchase-stages.ts (currentStageOf, STAGE_ORDER — граница SHIPMENT)
    - lib/date-periods.ts (startOfQuarterMsk)
    - .planning/phases/24-finance-balance/24-CONTEXT.md (D-12, D-15, D-16, D-17)
    - .planning/phases/24-finance-balance/24-RESEARCH.md (computeTaxLiability формула, Pitfall 5)
  </read_first>
  <action>
Завершить `loadBalanceSheet` (заменить TODO Task3):
- **Авансы поставщикам + Товар в пути из Китая** (D-12, B1/B2 — point-in-time!):
  `purchase.findMany({ include: { items: { include: { stages: true } }, payments: true } })`.
  ⚠ НЕ фильтровать по ТЕКУЩЕМУ `status` — у Purchase нет истории статуса, закупка, закрытая ПОСЛЕ
  asOf, ретроактивно исчезла бы (B1). Классификация — по ЭТАПУ на asOf, не по статусу. Для каждой закупки:
  - сумма уплаченного в ₽ на asOf = Σ по платежам, где `p.status==='PAID' AND p.paidDate != null AND
    p.paidDate <= asOf` (B1 — point-in-time; paidDate nullable → строго отбросить null): `Number(p.amount) × rate`,
    rate из `getRateForDate(p.currency, p.paidDate)` (currency==="RUB"/"RUR" → 1; approximate → пометить флаг).
    Платежи с `paidDate=null` или `paidDate > asOf` — ИСКЛЮЧИТЬ. Если уплачено 0 → закупку пропустить.
  - этап закупки на asOf: собрать по всем items их stages [{stage,date}], `stage = stageAsOf(allStages, asOf)`.
    Классификация (B2 — WAREHOUSE НЕ в «в пути»!):
      · stage ∈ {SHIPMENT, TRANSIT} → строка «Товар в пути из Китая» (группа «Запасы»).
      · stage ∈ {null, PRODUCTION, INSPECTION} (до отгрузки) → строка «Авансы поставщикам».
      · stage === WAREHOUSE на asOf → ИСКЛЮЧИТЬ полностью (товар уже принят и покрыт снапшотом
        остатков D-10 → иначе двойной счёт с запасами, B2).
  - если хоть один курс approximate → `approximate:true` на соответствующей строке.
- **Налоги** (пассив, группа «Налоговые обязательства», D-15/16/17, B3/M4 — ИСПРАВЛЕНО):
  Разнести НАЧИСЛЕНИЕ и ВЫЧИТАНИЕ платежей (иначе платежи внутри факт-квартала теряются → завышение, B3).
  - **Нижняя граница расчёта (M4):** `taxCalcStartQuarter` из AppSetting `finance.taxCalcStartQuarter`
    (дефолт `"2026-Q2"`; сид в 24-01 миграции). Кварталы ДО границы начисляются ТОЛЬКО через факт D-17
    (FinanceTaxPeriodActual), иначе 0 — WbCardFunnelDaily есть лишь с 01.04.2026, без границы кварталы
    2025 дали бы базу 0 «минус уплаченное» → фиктивный отрицательный долг (M4).
  - `startOfCalc` = начало квартала `taxCalcStartQuarter` (2026-Q2 → 2026-04-01).
  - vatPct/incomeTaxPct из AppSetting (finance.vatPct/finance.incomeTaxPct, дефолт 7/1).
  - **accruedTotal** = Σ по кварталам q от startOfCalc до текущего квартала asOf включительно:
      · факт: если FinanceTaxPeriodActual(year_q,quarter_q) есть и поля не null →
        accrual_q = Number(vatActualRub ?? 0) + Number(incomeTaxActualRub ?? 0) (D-17).
      · иначе начисление: buyouts_q = Σ WbCardFunnelDaily.buyoutsSumRub где date ∈
        [startOfQuarterMsk(q), min(конец q, asOf)]; accrual_q = computeQuarterAccrual(buyouts_q, vatPct, incomeTaxPct)
        (D-16 БЕЗ вычитания платежей). Текущий (незакрытый) квартал — ВСЕГДА начисление.
    Плюс отдельно прибавить факты за кварталы ДО startOfCalc, если введены (fact-only; иначе 0).
  - **taxesPaidTotal** (вычитание ЕДИНОЖДЫ, глобально — B3): `taxWindowStart` = min(startOfCalc,
    начало самого раннего квартала с введённым фактом). taxesPaidTotal =
      Σ BankTransaction.amount где category='TAX' AND date ∈ [taxWindowStart, asOf]
    + Σ CashEntry.amount где category.name='Налоги/банк/сборы' AND direction='EXPENSE' AND date ∈ [taxWindowStart, asOf].
  - строка «Отложенные налоги (расчётно)» = computeTaxLiability({ accruedTotal, taxesPaidTotal })
    с `note`-дисклеймером про приближённость категоризации (Pitfall 5).
  (Реализовать хелпер-цикл по кварталам [taxCalcStartQuarter .. текущий]; вычитание платежей —
   вне цикла, один раз по всему окну.)
- **Assembly**: subtotalRub каждой группы и totalRub каждой секции считать ТОЛЬКО по строкам
  `line.currency !== "CNY"` (m4 — CNY справочные, не в рублёвых итогах); `capitalRub =
  computeCapital(assets.totalRub, liabilities.totalRub)`. Вернуть BalanceSheet.

Создать `tests/balance-sheet.test.ts`: замокать `@/lib/prisma` (все findMany/groupBy/findUnique возвращают
детерминированные фикстуры для одной даты) + при необходимости `@/lib/wb-finance-api` (не должен вызываться).
Проверить: capitalRub === assets.totalRub − liabilities.totalRub; строка «Отложенные налоги (расчётно)»
= computeTaxLiability на фиктивных числах; unvaluedStock.productCount при наличии costPriceAtDate=null строки.
  </action>
  <verify>
    <automated>cd "C:/Claude/zoiten-pro" && npx vitest run tests/balance-sheet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/balance-sheet.test.ts` — green
    - `grep "computeTaxLiability" lib/balance-data.ts` находит (вызов при сборке налогов)
    - `grep "computeCapital" lib/balance-data.ts` находит (финальный капитал)
    - `grep -E "stageAsOf|currentStageOf" lib/balance-data.ts` находит (граница D-12)
    - `grep "getRateForDate" lib/balance-data.ts` находит (курс на дату платежа D-12)
    - `grep -E "category: \"TAX\"|category.*TAX" lib/balance-data.ts` находит (уплаченные налоги)
    - Тест подтверждает capitalRub === assets.totalRub − liabilities.totalRub
  </acceptance_criteria>
  <done>loadBalanceSheet собирает ВСЕ статьи; капитал балансирует; assembly-тест зелёный.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/balance-sheet.test.ts` — green
- `npx vitest run tests/balance-math.test.ts tests/balance-data.test.ts tests/balance-sheet.test.ts` — все green (регрессия)
- loadBalanceSheet собирает активы/пассивы/капитал + unvaluedStock
</verification>

<success_criteria>
- Полный баланс на дату считается из банка/кассы/кредитов/запасов/дебиторки/авансов/налогов/ручных статей
- Капитал = Активы − Пассивы (балансирующая строка)
- «Без оценки» (D-11), граница аванс/в-пути (D-12), факт налога (D-17) реализованы
</success_criteria>

<output>
After completion, create `.planning/phases/24-finance-balance/24-05-SUMMARY.md`.
⚠ НЕ КОММИТИТЬ.
</output>
