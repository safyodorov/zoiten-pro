import { describe, it } from "vitest"

// Wave 0 stub — Plan 08-04 реализует тесты server actions поддержки.
describe("support-actions (Wave 0 stub — Plan 08-04 will implement)", () => {
  it.skip("replyToTicket: шлёт ответ в WB API и создаёт OUTBOUND SupportMessage", () => {
    // Реализация: Plan 08-04
  })

  it.skip("assignTicket: обновляет assignedToId + revalidatePath", () => {
    // Реализация: Plan 08-04
  })

  it.skip("updateTicketStatus: переводит NEW → IN_PROGRESS → ANSWERED → CLOSED", () => {
    // Реализация: Plan 08-04
  })
})
