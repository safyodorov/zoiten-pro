import { describe, it } from "vitest"

describe("template picker (Wave 0 stub — Plan 11-03 will implement)", () => {
  it.skip("группирует шаблоны: сначала с nmId === ticket.nmId, потом общие", () => {
    // Реализация: Plan 11-03
  })
  it.skip("фильтрация по тексту/тегу регистронезависимо", () => {
    // Реализация: Plan 11-03
  })
  it.skip("substituteTemplateVars вызывается при onPick с customerName/productName из тикета", () => {
    // Реализация: Plan 11-03
  })
  it.skip("при пустом списке показывает «Нет шаблонов для этого канала»", () => {
    // Реализация: Plan 11-03
  })
  it.skip("группировка: шаблоны с nmId=null и nmId!==ticket.nmId относятся к «Общие»", () => {
    // Реализация: Plan 11-03
  })
  it.skip("показывает только шаблоны с isActive=true", () => {
    // Реализация: Plan 11-03
  })
})
